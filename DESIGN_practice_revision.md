# 練習モードの改訂（2026-08-15・ユーザーの実使用から出た指摘）設計書

v1300〜v1371 で「異性体の書き出し」「アルキル基の書き出し」「命名の確認」「答え合わせの2列表」が
一通り着地したあと、**ユーザーが実機を触って出した6件の指摘**に対する設計。実装前の合意用。

関連（この文書が参照する正）:
[DESIGN_isomer_practice.md](DESIGN_isomer_practice.md) ／ [DESIGN_iupac_check.md](DESIGN_iupac_check.md) ／
[DESIGN_reaction_execution.md](DESIGN_reaction_execution.md) ／ [DESIGN_reaction_mechanism.md](DESIGN_reaction_mechanism.md) ／
[DESIGN_entry_points.md](DESIGN_entry_points.md) ／ [DESIGN_molecule_modal.md](DESIGN_molecule_modal.md) ／
発注書 [video-scripts/ORDER_isomer_review_2026-08-15.md](video-scripts/ORDER_isomer_review_2026-08-15.md)

---

## 0. この文書の役割と、**正がどこにあるか**

### 0-1. なぜ新規1本にしたか（既存の節に書き足さなかった理由）

6件の指摘は**持ち主が4つに分かれている**（付け根＝異性体練習 §14／芳香族＝同 §4・§11／
立体＝**持ち主なし**／メニュー＝入口／名称の説明＝命名の確認）。そのまま散らして書くと:

1. **ユーザーが判断できない。** 判断が要るのは4件あり、しかも**互いに絡む**
   （付け根の決めが芳香族に効くか・名称の説明が発注書 D を畳むか）。4つの文書に散らすと、
   決めるために4冊を突き合わせて読むことになる
2. **§14-1 との衝突を隠すことになる。** 「あらかじめ N 組置く」は §14-1 が明文で禁じている。
   その節に上書きで書けば、**禁じた理由ごと消える**。衝突は衝突として1か所に立てる必要がある
3. **立体の書き出しには持ち主の文書が無い**（`StereoPractice` は learn.js にあるだけで、
   設計は DEVELOPMENT.md の P12-8 M2.5 その4 に1行あるだけ）。書き足す先が存在しない

### 0-2. **正の行き先**（実装したらここへ移す）

★ **この文書は「決定と、その理由と、判断待ち」を集める会議録**であって、機能の恒久の正ではない。
発注書 → 設計書 の流儀（`ORDER_isomer_review_2026-08-15.md` の C が §12-7 へ移ったのと同じ）に従う。

| 節 | 中身 | 実装したら正はどこへ | この文書に残すもの |
|---|---|---|---|
| §3 A | アルキル基の付け根の増やし方 | ✅ **移設ずみ（v1376）**: DESIGN_isomer_practice.md **§14-5**（§14-1 は据え置き・道しるべだけ足した） | 決めた理由の記録 |
| §4 B | プリセットと芳香族 | **DESIGN_isomer_practice.md §4.1 と §11-4** | 行き先の1行だけ |
| §5 C | 立体異性体の書き出しの答案用紙化 | ★ **この文書（§5 がそのまま正になる）**。育ったら `DESIGN_stereo_worksheet.md` に分ける | 本文ぜんぶ |
| §6 D | 学習メニューの言い直し | ✅ **移設ずみ（v1377）**: DESIGN_entry_points.md **§10**（入口の数は増えないので新節を1つ足した） | 決めた理由の記録 |
| §7 E | 名称の説明 | ✅ **移設ずみ（v1378）**: DESIGN_iupac_check.md **§3-0**（画面）・**§5-1/§5-2**（API）・**§8**（やらないこと） | 決めた理由と着手時の実測 |
| §8 F | 発注書 D をどう畳むか | **発注書の D 節に決着を書き、DESIGN_iupac_check.md §8 のやらないことへ** | 行き先の1行だけ |

---

## 1. 先に実測（推測で書いていないことの証拠）

**測った場所**: `feat/design-pass` の作業ツリー（**v1371**）を `http://localhost:8680/assembler/` で起動し、
コンソールから実際の関数を叩いた。⚠ **最初の測定は隣のレーンのサーバー（:8677・v1395）を掴んでいた**ので、
`location.href` と `.version` を毎回確かめ直して測り直してある。

| # | 測ったこと | 値（v1371） | 設計をどう変えたか |
|---|---|---|---|
| M1 | 学習メニュー（`#study-body > details > summary`）の項目 | **4項目**: `🎓 クイズに挑戦` `✏️ 構造の書き出し練習` `🔍 実験の結果から絞り込む` `⚗️ 反応機構ビューア` | 統合レーンの「5項目」は誤り。**`📖 R・S ってなに？` は学習メニューではなく立体モーダルの中**（`#stereo-rs-tips`）＝ **メニューに「読み物」は混ざっていない**。§6 の前提が変わった |
| M2 | 反応実行（reactor）の起動経路 | 学習メニューに**無い**。入口は2つとも**自由モード**: ①作業帯 `#ws-free` の `🔬 この分子を調べる（反応 N件）`（`#btn-molecule-modal`）②キャンバスの見出し `🔍 ① 乳酸` のタップ。中身は分子モーダルの `#reaction-actions`（できる反応）と `#mm-reagents-grid`（試薬 **21本**） | §6 の答えが「メニューに足す」から「**名札を直す**」に変わった |
| M3 | ★ **学習モードに入ると反応の入口が画面から消える** | `setMode('learn')` で **`#ws-free` が hidden**（実測 `wsFreeHidden: true`）。`#mm-reaction` も `renderMoleculeModal` が自由モード以外で非表示 | **reactor を学習メニューに置くことは原理的にできない**（押した瞬間 learn になり、反応が消える）。§6-1 の決定はこれ1つで決まった |
| M4 | 反応の件数はライブで出ているか | エタノールを呼び出すと **`🔬 この分子を調べる（反応 4件）`**／モーダルに `酸化 [O] → アルデヒド` `ヨードホルム反応` `分子内脱水` `ナトリウムとの反応` の4つ | **ユーザーが欲しがった機能は既にあって、しかも件数まで出ている。**足りないのは名札（§6-2） |
| M5 | 「＋ 答案」がどこに置くか | 3列の固定格子。C1 は `x = 168/420/672`・`y = 174 + 168k`（`AK_SLOT_X0/Y0/DX/DY`）。埋まり判定は `AK_SLOT_FREE = 96px`・上限 `AK_MAX_SLOTS = 20` | §3-3 |
| M6 | ★ **押すたびに画面がどれだけ飛ぶか** | 練習開始時の viewBox は **408×306**（PC 1280幅）。格子の横幅は 168〜672 ＝ **504px** で**最初から画面に入らない**。`＋答案` を押すと viewBox.x が **72 → 324 → 66 → 72 → 324** と**±258px 振れる**（3枠目で先に描いた2枠が画面外へ出る） | ユーザーの「位置がやりづらい」の正体。§3-3 で格子をやめる |
| M7 | ★ **お題ボタンが答えの個数を出している** | 選択画面のボタンは **`C₄H₉–（4種）`**（`renderList` の `${data.isomers.length}種`）。異性体側も `C₄H₁₀O（7種）`、立体側も `乳酸（2種）` | ★ **§14-1 の「盤面に N 組置くと『答えは N 個』と教えてしまう」という理由は、いまの実装では既に成り立たない**（総数はお題を選ぶ前から画面に出ている）。U1 の材料が根本から変わった |
| M8 | アルキル練習で孤立した C を1つ置くと | 採点表に `status: 'noroot'` の行が1つ増える（＝ 付け根が無い成分として**答え合わせで指される**） | 「C 以外を置いたら？」の答えは**すでに実装が持っている**。§3-2 |
| M9 | ★ **立体練習中に2分子目を描くと名前が漏れる** | `🔍 ① 乳酸` `🔍 ② エタノール` が SVG に出る。`worksheetActive()` は **false**・`canvasEntryEnabled()` は **true**（＝ 分子モーダルも開く → 🧊立体で見る → R/S まで届く） | **§12-3 で塞いだ穴が、立体のレーンだけ開いたまま。**いま赤くできる検査がある。§5-5 |
| M10 | 未整形（一直線）の 2-ブテンを登録すると | `canonicalCode` はお題と一致（`sameCode: true`）するのに、**登録は断られる**。文言は実測で「この図は立体として読めないため登録できません。**向きの読めない C=C が1本あります**（置換基を軸の上下に描く）。」 | 「シス/トランス整形が必須？」の答えは **YES（いまは必須）**。§5-4 で「未確定」の欄を作る |
| M11 | 構造異性体のプリセット数 | **6件**（C₄H₁₀ / C₅H₁₂ / C₃H₈O / C₆H₁₄ / C₄H₈ / C₄H₁₀O）＋ 任意の分子式の入力欄 | §4-3 |
| M12 | 芳香族の回は届いているか | `startFromFormula('C8H10')` は **`aromaticOnly: true`・4種**で開く（実装済み）。だが**プリセットのボタンには1件も無い** ＝ **入力欄に `C8H10` と打てる人にしか届いていない** | §4-3。傍用の基本問題（qa `org.aro.c8h10-isomers` は Lv2）が画面から見えていない |
| M13 | ベンゼン環はユーザーが置けるか | モジュールパレットに `benzene` がある（`cyclopentane` `cyclohexane` `n-ring` `haworth-*` と並ぶ全 11 種） | ★ **芳香族に「アプリが枠を置く」手当ては要らない**（付け根 R と違って**道具が既にある**）。§4-2 |
| M14 | 名称の説明の材料 | `iupacNameDetail` は実際にこう返す —— `2-メチル-1-プロパノール`: `{kind:'chain', mainChain 3炭素, locants:{ol:[1], ene:[], yne:[], subs:[{loc:2, name:'メチル', key:'methyl'}]}}`／`イソプロピルメチルエーテル`: `{kind:'ether', groups:['イソプロピル','メチル']}` | **部品に割るための材料は全部そろっている。表示していないだけ。**§7-2 |
| M15 | 番号の「向き」を選んだ理由は取れるか | `_iupacNameForMainChain` は `cOl / cUn / cEne / cSub` を計算して**その順で枝分かれ**して向きを決めている（chemistry.js 3648〜3665 行相当）。**どの比較で決まったかは変数に残っていない**（分岐した後に捨てている） | **3行の追加で取れる**（`dirReason` を返すだけ・出力は1バイトも変わらない）。§7-3 |
| M16 | W4（答案を並べ直す）の状態 | **未実装**。`tests.js` の台帳に「**7 は W4 に予約**」とあるだけ | §4-2 の芳香族と §5-6 の立体が**どちらも W4 に依存する**。§9 の順序が決まった |

---

## 2. ★ ユーザーの判断が要るもの

**ここに並べたものは、こちらでは決めない。**各案の害を書いたので、選んでほしい。
（★ が付いているものは、決まらないと下流が止まる）

---

### ✅ 決着（2026-08-15・ユーザー判断）

**5件とも決まった。以下は確定事項で、下の各節は「なぜそう決めたか」の記録として残す。**

| | 決定 | 備考 |
|---|---|---|
| **U1** 付け根の事前配置 | **案1・据え置き** | **§14-1 は改訂しない。**代わりに §3-1（C を置いたら付け根が生える）と §3-3（＋答案の置き場所）を入れる。⚠ **§14-1 の「理由」は現行実装では既に成り立っていない**（お題ボタンが総数を出している・M7）。それでも据え置くのは、**「（4種）」の文字は読み飛ばせるが盤面の枠は目に入り続ける**という**別の理由**による。§14-1 を読む人がこの食い違いで迷わないよう、**§14-1 側にこの追記への道しるべを置くこと** |
| **U2** 芳香族プリセット | **案1** | 6 → 7件。`C₈H₁₀（芳香族・4種）` を1件足す。⚠ **W4（並べ直す）が先** |
| **U3** 名札の文言 | **案1** | `⚗ 反応させる・調べる（反応 N件）`。★ 案3（件数で文言を切り替える）は**採らない** —— 探すもののラベルが状態で変わると、一度覚えた名前で探せなくなる |
| **U4** 未整形の図 | **案1・未確定として置く** | 不正解にしない・種類に数えない・**数は出す**・答え合わせの行から整形へ1手。⚠ `CLAUDE.md` の「未確定を確定させるのは整形モードのタップだけの仕事」は動かさない |
| **U5** 発注書 D | **案1・縮小して残す** | §7（名称の説明）を先に入れ、2列表（現 3列表）の右セルに `🔢` トグルを1つ。対抗馬の薄描き・選定理由の1行は**やらない** |

**U2・U3・U5 は「推奨どおりでよい」との判断**（統合レーンが「指示が無ければ推奨で進める」と伝え、
ユーザーが U1・U4 だけを名指しで選んだため）。**違えば実装前に差し戻すこと。**



### ★ U1. アルキル基の付け根を「あらかじめ必要数」置くか —— **§14-1 との正面衝突**

> ユーザーの言葉: 「あらかじめ必要数のRを置いておく手もある」

**DESIGN_isomer_practice.md §14-1 はこう書いてあり、これはユーザー自身が承認した決定である**:

> ★ **最初から N 組並べてはいけない** —— 盤面に8組の枠を置いた時点で「答えは8個」と教えてしまう。
> 上限は設けない（20 まで許す）ので、押した回数からも答えは漏れない

**★ ただし実測（M7）で前提が崩れている。** お題を選ぶボタンには最初から **`C₄H₉–（4種）`** と書いてある。
**総数は、練習を始める前から画面に出ている。** つまり §14-1 が守ろうとしていたもの（答えの個数）は
**別の場所から先に漏れている**。「N 組置くと答えが漏れる」は、いまの実装では**新たな漏れを作らない**。

| 案 | 中身 | 害 |
|---|---|---|
| **案1（推奨）** | §14-1 は**据え置き**。§3-1 の「C を置いたら付け根が生える」＋ §3-3 の「＋答案の置き場所を直す」だけ入れる | **無い**と考える。「増やし方が分からない」も「位置がやりづらい」も §3 で消える。§14-1 を触らないので、将来「総数を隠す」方向に直したくなったときの選択肢が残る |
| **案2** | §14-1 を改訂し、**最初から N 組**置く。理由は「総数はお題ボタンで既に見えているので、追加の漏れは無い」 | ① **盤面が答えを名乗る強さは、ボタンの文字とは違う。**「（4種）」は読み飛ばせるが、4つの枠は**目に入り続ける**。読み飛ばせない形で答えを渡すことになる ② 異性体側（§12・枠を置かない）と**規則が食い違う**。同じ「書き出し練習」で盤面の意味が2つになる ③ ヒント段1（「あと N 種」）が異性体側にはあり、その値打ちが下がる |
| **案3** | 総数を**隠す**方向へ揃える（お題ボタンの `（N種）` を外し、§14-1 の理由を生かす） | ① **選ぶ前に問題の重さが分からない**（C₄H₉– と C₆H₁₃– の差が見えない） ② スコアの満点は総数（§15-5b）なので、**満点が分からないまま解く**ことになる ③ クリア済みの ✓ と噛み合わない（何個そろえたら ✓ が付くのか分からない） ④ **異性体側6問・立体側4問も同じ表記なので、3か所を同時に変えることになる** |

**推奨: 案1。** ただし「案2 にしたい」なら §3 の直しと**同時に入れても矛盾しない**（枠が最初からあっても
「C を置いたら付け根が生える」は5枚目以降の受け皿として働く）。決めてほしいのは
**「§3 を入れたうえで、なお最初から N 組並べたいか」**。

---

### U2. 芳香族のプリセットを増やすか・どの式を入れるか

**実測（M12）**: 芳香族の回は**実装済みなのに、入力欄に `C8H10` と打てる人にしか届いていない**。

| 案 | 中身 | 害 |
|---|---|---|
| **案1（推奨）** | プリセットを **6 → 7 件**にし、**`C₈H₁₀（芳香族・4種）`** を足す | ボタンが1つ増える。**W4（並べ直す）が先に要る**（ベンゼン環4つは 800×600 に入らない・§4-2） |
| 案2 | **8件**にして `C₇H₈O（芳香族・5種）` も足す（アニソール・ベンジルアルコール・o/m/p-クレゾール。**5種とも名前が付く**＝ §11-1 の実測） | 選択画面が「一覧」に近づく。芳香族が2件で脂肪族6件と並ぶと、比率としてやや重い |
| 案3 | 増やさず、**入力欄の脇に例を書く**（`例: C5H10 / C8H10（芳香族）`） | 実装は最小だが、**打つ手間は残る**。傍用の基本問題への到達コストが「打てる人だけ」のまま |

⚠ **C₉H₁₂ は入れない**（o/m/p-エチルトルエンが compounds.json に無く「（名称未登録）」になる。§11-7 の申し送り）。

---

### U3. `🔬 この分子を調べる（反応 N件）` の名札をどう変えるか

**実測（M4）**: この1つのボタンが、ユーザーの言う「現在描画している分子についてどんな反応をするか」を
**すでにやっていて、件数まで出している**（エタノールで「反応 4件」）。見つからなかったのは名札の問題。

| 案 | 文言 | 害 |
|---|---|---|
| **案1（推奨）** | **`⚗ 反応させる・調べる（反応 N件）`** | 中身は反応だけではない（分類・試薬・🔬調べる・🧊立体・📚異性体・🔢命名）ので**両方名乗る**。やや長い（16文字。いまの `🔬 この分子を調べる（反応 —）` が15文字なので帯には収まる） |
| 案2 | `⚗ この分子を反応させる（N件）` | **0件のときに嘘になる**（「反応させる」と書いてあるのに反応が無い）。「反応 —」の表示で辻褄は合うが、名札としては弱い |
| 案3 | 件数で文言を切り替える（0件は `🔬 調べる`・1件以上は `⚗ 反応させる`） | ★ **これは採ってはいけない**と考える。**探すもののラベルが状態で変わる**と、一度覚えた名前で探せなくなる。この文書の他の決定（§6-1）と真逆の向き |

---

### ★ U4. 未整形の図（一直線の 2-ブテン）を答案としてどう採点するか

**実測（M10）**: いまは**登録の門で断っている**。文言は「この図は立体として読めないため登録できません。
向きの読めない C=C が1本あります（置換基を軸の上下に描く）。」——**つながり方はお題と一致している**のに断る。

| 案 | 中身 | 害 |
|---|---|---|
| **案1（推奨）** | **「未確定」として第3の欄に置く。**不正解にしない・種類にも数えない。答え合わせの行から**整形へ1手**で行けるようにする | 「答えが1つ足りない」ように見える瞬間がある（未確定は種類に数えないため）。→ 欄の見出しで「**まだ立体が決まっていない図: 1つ**」と数を出して埋める |
| 案2 | **不正解にする**（「シスでもトランスでもありません」） | ★ **化学として嘘になる。**一直線に描いた 2-ブテンは**構造式としては正しい**（構造異性体の練習ではこれで満点）。「決まらない」を「間違い」と呼ぶと、**同じ図が練習によって○にも×にもなる** |
| 案3 | **整形を強制する**（描いた瞬間に整形モードへ送る・未整形では答え合わせを押せない） | 門を1つ増やす ＝ W1 が捨てた形へ戻る。しかも**整形モードは別レーン（`reshape-tap`）が触っている最中**なので、そこへ依存を作るのは今は筋が悪い |

⚠ どの案でも **`CLAUDE.md` の「未確定を確定させるのは整形モードのタップだけの仕事」は動かさない**。
決めているのは「未確定の図を**採点表でどう呼ぶか**」だけで、整形の仕様には触れない。

---

### U5. 発注書 D（自分の図にも名称・主鎖・炭素番号）をどう畳むか

§8 に詳しく書いた。要点だけ:

| 案 | 中身 | 害 |
|---|---|---|
| **案1（推奨）** | **D は「縮小して残す」。**§7（名称の説明）を先に入れ、**答え合わせの2列表の右セルに `🔢` のトグルを1つ**置く（押した行だけ帯と番号が出る）。D の受け入れ条件④「お題に数えなかった図にも名前と番号が出る」は §7 の門番がそのまま満たす | 「どの行を押すか」を選ぶ手間が1つ増える |
| 案2 | **D を不要にする**（§7 だけ入れて D は閉じる） | ★ **面が違う。**§7 の置き場所はキャンバス（`🔢 主鎖と番号を見る` と同じ帯）で、D が言っているのは**答え合わせの表の中**。§7 だけでは「答え合わせを閉じてキャンバスへ戻る」往復が残る |
| 案3 | 全行に常時出す | C₄H₁₀O は7行。**7つの図が数字で埋まる**（1行 84px の「小」では読めない。§12-7 の実測） |

⚠ 発注書 D の「複数が主鎖となりうる場合の対応検討」の 2（対抗馬を薄く）・3（選んだ理由を1行）は
**やらない**に落とす提案（§8-2）。理由もそこに書いた。

---

## 3. A: アルキル基の付け根（ロック原子）の増やし方

> 2個目以降の構造の書き方（ロック原子の増やし方）がわかりにくい（アルキル基でない分子を増やせてしまう）／
> 別分子としてC原子を置いたときに自動でRにしてしまう方法もある（他の原子を置いた場合はどうする？）／
> ＋答案　で増やした場合の位置がやりづらい

> ✅ **実装済み（v1376・2026-08-15）。→ 正は [DESIGN_isomer_practice.md](DESIGN_isomer_practice.md) の
> **§14-5** へ移した**（§0-2 の行き先の表のとおり）。A1・A2・A3 の**仕様として読むのは §14-5**。
> **以下は「なぜそう決めたか」の記録**として残す（§2 の決着と同じ扱い）。
> ⚠ **仕様を直すときは §14-5 を直すこと。**ここを直しても実装の正にはならない。
> §14-1（最初から N 組並べない）は**据え置き**で、§14-1 側に §14-5 への道しるべを置いた。
> テストは **AK5〜AK8**（AK6・AK7・AK8 が否定対照）。

ユーザーの困りごとは**3つ**に分かれる。**1つの直しで3つとも消える**ので、まずそこを決める。

| # | 困りごと | いまの実装 |
|---|---|---|
| (a) | 増やし方が分からない | **「＋ 答案をもう1つ」を見つけて押す**必要がある（手順の知識が要る） |
| (b) | アルキル基でない分子を増やせてしまう | 孤立した C は `noroot` として答え合わせで指される（M8）＝ **害は既に受け止められている** |
| (c) | 増やした位置がやりづらい | 3列の固定格子・押すたびに画面が **±258px 振れる**（M6） |

### 3-1. 決定 A1: **孤立した炭素を置いたら、その場に付け根が生える**

**アルキル基の練習中にかぎり**、`placeAtomOrExplain` が**どこにも結合しない新しい炭素**を置いたとき
（＝ `getPlacementBondTargets` が空・`selectedAtomType === 'C'`）、その炭素を **C1** とし、
**隣の格子点に `R` を置いて単結合で結び、両方を `isLocked` にする**。

**なぜこれが筋がよいか**

1. **(a) が消える。** 手順を覚えなくてよい ——「**炭素を置けば答案が増える**」は、異性体側の
   「**描けば答案**」（§12-1）と**同じ規則**になる。書き出し練習の中で覚えることが1つ減る
2. **(c) が消える。** 置き場所は**ユーザーが押した所**なので、格子も画面の飛びも要らない
3. **§14-1 を破らない。** 枠は「押した回数ぶん」ではなく「**描いた回数ぶん**」増えるので、
   盤面は答えの個数を先回りして名乗らない
4. **付け根の供給は依然としてアプリの仕事のまま。** §14-1 が本当に守っているのは
   「ユーザーに R を足させない」（＝ R を置き忘れた図が採点に混ざらない）で、これは維持される

**R をどちら側に置くか**: 既定は**左隣**（いまの `addSlot` と同じ `x - GRID_SIZE`）。
左が塞がっている（42px 以内に重原子がある）ときは **右 → 上 → 下** の順で空いている向きへ。
⚠ **4方向とも塞がっていたら、炭素だけ置いて R を生やさない**（無理に置くと図が重なる）。
その成分は `noroot` として答え合わせで指される ＝ **黙って変な図を作らない**。

### 3-2. 決定 A2: **炭素以外を置いたときは、そのまま置かせる**（R を生やさない）

統合レーンの見立てのとおり、**門を増やさず答え合わせで返す**。W1 が「登録を断る」を採点表に移したのと同じ流儀。

- **R を生やしてはいけない理由**: `O–R` のような「アルキル基でないもの」に枠を与えると、
  **盤面が間違いを肯定する**。付け根は「答案の罫線」（§14-1）なので、罫線を引いた時点で
  「これは答案として成立する形だ」と言ってしまう
- **いまの採点表で足りるか**: 足りる（M8）。ただし**文言だけ直す** ——
  酸素を1つ置いた成分は `noroot`（付け根が無い）に落ちるが、
  「付け根がありません」より「**アルキル基は炭素から始まります（この図には炭素の付け根がありません）**」のほうが、
  何が起きたか分かる。⚠ `extra`（炭素と水素以外が混ざっている）と**言い分ける**
  —— 「C–O–R を描いた」（extra）と「O だけ置いた」（noroot）は別の間違い

### 3-3. 決定 A3: 「＋ 答案をもう1つ」は**残すが、置き場所を変える**

**残す理由**: ①炭素を置く操作に不慣れな人の保険 ②`AK3`（＋答案 の前は付け根が1組しか無い）が押している
③作業帯の `＋ 答案` は帯の3ボタンの1つで、消すと帯の構成が変わる

**変えるところ（M6 の実測に対して）**: 固定格子をやめ、**いま見えている範囲の空きを先に探す**。

```
1. viewBox の中の格子点を走査し、既存の重原子から AK_SLOT_FREE(96px) 以上離れた点を探す
   → 見つかったらそこへ置く（★ viewBox を1px も動かさない）
2. 見えている範囲に空きが無いときだけ、いまと同じく格子の次の行へ送り、scrollSlotIntoView で寄せる
```

- **これで「押しても画面が飛ぶ」が、画面が本当に埋まったときだけになる**
- **拡大率は触らない**（`scrollSlotIntoView` の注意書きをそのまま守る）

### 3-4. 完了条件と否定対照（A）

| | 中身 |
|---|---|
| **完了条件** | アルキル練習中に、キャンバスの空いた所を炭素で3回タップするだけで **3枠ぶんの答案が並び**、答え合わせが成立する（＋答案 を一度も押さない） |
| **AK5** | 孤立した炭素を置くと、その成分が **`C` と `R` の2原子・どちらも `isLocked`・単結合** になる。**練習をやめた後（`active === false`）に同じ操作をしても R は生えない** |
| **AK6 ★否定対照** | **炭素以外**（O）を孤立して置いても **R は生えず**、その成分だけが `noroot` として指され、**他の成分の採点は通る** ⇒ 「どの元素でも R を生やす」親切にした直しが赤くなる |
| **AK7 ★否定対照** | 見えている範囲に空きがある状態で `＋ 答案` を押すと、**viewBox が 1px も動かない** ⇒ 固定格子に差し戻すと、実測どおり **x が 258px 飛んで**赤くなる |

---

## 4. B: 構造異性体の書き出し —— プリセットと芳香族

> プリセットを増やす？／キシレンなど芳香族も入れる→アルキル基の書き出しと同じ問題にあたる

### 4-1. 「同じ問題」とは何か（言語化）

アルキル基の練習が特別なのは、**答案がどれも同じ土台から始まる**こと。芳香族も同じで、
**どの答案もベンゼン環から始まる**。共通するのは3つ:

| 共通点 | アルキル基 | 芳香族 |
|---|---|---|
| ① **同じ土台を答案の数だけ繰り返し描く** | C1–R を4回 | ベンゼン環を4回 |
| ② **土台が壊れた答案が採点に混ざる** | R が無い・2個ある | 環が5員環・環が2つ・環が無い |
| ③ **土台のぶん場所を食う** | 1答案 約 3×2 マス | 1答案 約 **5×4 マス**（環だけで 3×3） |

### 4-2. 決定 B1: **①は手当てが要らない。②は既存の門番で足りる。③だけが本物の問題**

**① について（★ ここが「1 の決定が芳香族に効くか」の答え）**

**効かない。そして効かせる必要が無い。**

- 付け根 `R` は**ユーザーには置けない**（パレットに R のボタンが無い・擬似元素）。
  だからアプリが置くしかなく、§3-1 の手当てが要った
- **ベンゼン環は 1タップで置ける**（M13。モジュールパレットに `benzene` がある）。
  **足りていないものが違う**ので、同じ手当ては要らない
- ★ **「炭素を置いたら環が生える」にしてはいけない。**環は6原子6結合の構造で、
  自由モードの作図（炭素を置いたら炭素が1つ増える）と**同じ操作が別の意味になる**。
  §3-1 が許されるのは「R を1個足すだけ・練習中だけ・孤立した炭素のときだけ」と条件が3重に狭いから

**② について**: 芳香族回の門番は**すでにある**（§11-4 の `aromaticOnly`。「分子式は合うが対象外」を
範囲の説明で断る＝ BZ5）。環が壊れた答案は正解集合に無いので、答え合わせで指される。**追加は要らない**。

**③ について（本物の問題）**: キャンバスは `viewBox` 800×600・格子 42px ＝ **19×14 マス**。
ベンゼン環の答案は1つ約 5×4 マスなので、**C₈H₁₀ の4種で 20×4 マス相当** ＝ 横に並べたら入らない。
→ **`W4`（答案を並べ直す・§12-5。実測 M16 で未実装）が芳香族回の前提条件**。

### 4-3. 決定 B2: プリセットに**芳香族を1件足す**（U2 で確定させる）

**実測 M12 が言っていること**: 芳香族の回は**動く実装があるのに、画面から見えていない**。
「打てる人にしか届かない機能」は、**入口が無いのと同じ**（`DESIGN_entry_points.md` の
「`select` に沈んでいた23種類の練習に入口を作る」＝ EP4 と同型の話）。

- 足すのは **`C₈H₁₀（芳香族・4種）`**。ボタンの文言に**「芳香族」を必ず入れる**
  （§11-4「宣言した以上、画面のどこでも隠さない」）
- ⚠ **`problems` の配列に足すと `enumerate(index)` の道に入ってしまう**（あちらは
  `enumerateConstitutionalIsomers` を呼ぶ）。芳香族回は `startFromFormula` の分岐が持っているので、
  **プリセットのボタンは `startFromFormula('C8H10')` を呼ぶ別の枠に置く** ——
  「よく出る芳香族の回」として、6件のグリッドの**下に1行**足すのがいちばん壊さない
- **クリア記録の鍵は `chemIsomerPractice.C₈H₁₀@ar`**（§11-4。既存のまま。ボタンの ✓ もこれで引く）

### 4-4. 完了条件と否定対照（B）

| | 中身 |
|---|---|
| **完了条件** | 選択画面のボタンを1つ押すだけで **`✏️ C₈H₁₀ の芳香族異性体（全 4 種）`** が開き、ベンゼン環を4つ置いて 4/4 が出る（入力欄に何も打たない） |
| **BZ6** | プリセットのボタンから開いた回が `aromaticOnly === true`・総数 4・見出しと注記に**「芳香族」の語が入っている** |
| **BZ7 ★否定対照** | 芳香族のボタンが `enumerate()` の道（生の列挙）へ**落ちていない** ⇒ `problems` の配列に足す実装にすると、C₈H₁₀ が **3523種で打ち切り**になって赤くなる（§11 の実測値がそのまま物差しになる） |

---

## 5. C: 立体異性体の書き出し —— 答案用紙化（帯 `SW`）

> シストランス成形が必須？／+登録方式になっているのがわかりづらい、登録方式でない方がよい／
> 複数分子描いたときに１つの分子を選んで登録する方法は？

### 5-1. いま何をしているか（実測）

`learn.js` の `StereoPractice`。**W1 前の異性体側とまったく同じ器**（`register()` ＋ `entries[]` のスナップショット）。

`register()` が通す門は**5つ**:

| # | 門 | 断り方 |
|---|---|---|
| 1 | キャンバスが白紙でない | 「キャンバスに分子を描いてから登録してください。」 |
| 2 | **分子が1つだけ**（`countMolecules() > 1` を断る） | 「分子が複数あります。**1分子ずつ登録**してください。」 |
| 3 | `canonicalCode` がお題と一致 | 分子式が違えば式を出し、同じなら「つながり方が変わっています」 |
| 4 | ★ **立体の単位がすべて図から読める** | M10 の文言（「向きの読めない C=C が1本あります」） |
| 5 | 変種集合（`byCode`）にある | 「判定できませんでした（開発ログに記録しました）」 |

- 登録してもキャンバスは消さない（動かして次を作る流儀）
- 作業帯の進捗は **`${entries.length}/${total}`**（例 `3/4`）
- お題は **4件**: 2-ブテン／乳酸／酒石酸／乳酸3分子の環状エステル

### 5-2. 決定 C1: **登録を廃止する（W1 の横展開）。帯は `SW`**

「登録方式でない方がよい」は W1 が異性体側で通した道そのもの。**キャンバス上の連結成分の集まりが答案**になる。
理由は §12-1 の4つがそのまま効く（答案の在りかを2つにしない／↩ が効く／スナップショット不要／並べて比べる）。

### 5-3. ★ W1 との**違い**（横展開で足りないところ）

| | 構造異性体（W1） | 立体（SW） | 手当て |
|---|---|---|---|
| 成分の同一性 | `canonicalCode` | **`readStereoOf(part).stereoCode`** | 判定関数を差し替えるだけ |
| お題との一致 | **分子式**が合うか | **つながり方**（`canonicalCode`）がお題と一致するか。分子式だけでは足りない（お題は「乳酸の立体異性体」であって「C₃H₆O₃ の異性体」ではない） | 採点表の `formula` 行を **`structure` 行**に読み替える |
| 正解集合 | 列挙エンジン | お題から機械生成した変種集合（`byCode`。`prepare()` が既に作っている） | そのまま |
| **★ 読めない図** | **起こらない**（トポロジーは必ず読める） | **起こる**（未整形の C=C・十字でない不斉炭素） | ★ **採点表に第3の状態が要る**（§5-4） |
| 答案の並べ直し | 剛体平行移動でよい | ★ **回転を混ぜてはいけない**（縦置きの図だけを立体として読む規則がある・v446 の `isFischerOriented`） | §5-6 の否定対照 |
| 番号 ①②③ | `markedMolecules` | 同じ | `worksheetActive()` に足すだけ（§5-5） |

### 5-4. 決定 C2: 採点表に「**まだ立体が決まっていない**」を足す（U4 の推奨）

答え合わせは**成分ごと**に、こう言う:

| 成分の状態 | 言い方 |
|---|---|
| 変種集合にある・初出 | ✓（発見） |
| 変種集合にある・既出 | 「①と③は**同じ立体**です」 |
| つながり方がお題と違う | 「④は**つながり方が違います**（お題は乳酸）」＝ **描きかけもここ。責めない文言** |
| ★ **立体が読めない** | 「⑤は**まだ立体が決まっていません**（C=C の向きが読めません）。**整形して決めましょう →**」 |
| 変種集合に無い立体コード | 従来どおり `console.error`（欠落として記録） |

- **未確定は「種類」に数えない**（正解でも不正解でもない）。代わりに**数を出す**:
  「**まだ立体が決まっていない図: 1つ**」——これで「1つ足りない」に見える瞬間が埋まる
- **整形への1手**: 行の「整形して決めましょう →」は、整形モードを開いてその成分へ寄せるだけ。
  ⚠ **整形の中身には触らない**（別レーン `reshape-tap` が作業中）。呼ぶ口を1つ用意するだけに留める
- ⚠ **トーストで叱らない**（毎回言うと、正しく描いている人を毎回叱る。§14-3 と同じ理由）

**「シス/トランス整形が必須？」への答え**: **必須のままだが、必須なのは「立体として数えられるため」であって、
「答案として受け付けられるため」ではなくなる。**——これが登録方式との違いの全部。

### 5-5. ★ 決定 C3: `worksheetActive()` に立体を足す（**いま開いている穴**）

**実測 M9**: 立体練習中に2分子目を描くと `🔍 ① 乳酸` `🔍 ② エタノール` が出て、
**分子モーダルも開く**（→ 🧊立体で見る → R/S ＝ **立体練習の答えそのもの**）。

- W3 でアルキルを足したときと**同じ直し**（旗は1つのまま・§12-3）。これで
  名前を伏せる門番・分子モーダルの封鎖・成分ごとの番号付けが**まとめて**効く
- ★ **これは「入れたら通る」ではなく「入れる前は赤い」検査**になる（IW4 が異性体側でそうだったのと同じ）

### 5-6. 決定 C4: 「1つの分子を選んで登録する方法は？」は**登録をなくせば消える問い**

いまの門2（`countMolecules() > 1` を断る）があるから「どれを登録するか選ぶ」必要が生まれていた。
答案用紙では**全部が答案**なので、**選ぶ相手がいない**。→ 新しい選択UIは作らない。

### 5-7. 決定 C5: 作業帯の進捗を **`3/4` から「いま 3個 描いてあります」へ**

`${entries.length}/${total}` は**判定を漏らしている**——登録できた ＝ 門5つを通った ＝ 正解集合にある、なので
`3/4` は実質「3種は当たり」に近い。W1 が引いた線（**書き出し中の画面に判定はゼロ**・§13-1）へ揃える。

### 5-8. マイルストーンと完了条件（C）

- **SW1: 登録の廃止**（C1・C3・C5） — 成分ごとの採点／`worksheetActive()` に立体／進捗を個数に。
  **完了条件**: 乳酸の2種を**1枚のキャンバスに**描いて答え合わせが 2/2 を出す（`register()` を一度も呼ばない）
- **SW2: 未確定の欄**（C2） — 第3の状態と、整形への1手。
  **完了条件**: 未整形の 2-ブテンを含む答案で採点が**成立し**、その成分だけが「まだ立体が決まっていません」と指される
- **SW3: 並べ直し**（W4 と共用） — §5-6 の回転禁止だけが追加

### 5-8a. ✅ 実装（**v1386**・SW1＋SW2。この節がそのまま正）

★ この節が育ったら **`DESIGN_stereo_worksheet.md` に分ける**（§0-2 の約束）。いまは §5 が正。

**やったこと**（`learn.js` の `StereoIsomerPractice`）:

| 何を | どうした |
|---|---|
| `register()` ＋ `entries[]` | **消した。** 答案の在りかは連結成分の集まりだけ（W1・W3 と同じ器） |
| 登録の門5つ | **`grade()`（採点表）へ移した。** 断りは書き出しの最中ではなく答え合わせで言う |
| 成分の同一性 | `readStereoOf(part).stereoCode`（W1 の `canonicalCode` から差し替え） |
| お題との一致 | **`canonicalCode` がお題と一致するか**＝ W1 の `formula` 行を **`structure` 行**に読み替え |
| ★ 第3の状態 | `status: 'unread'`。**種類には数えず**、答え合わせに「まだ立体が決まっていない図: N つ」と**数だけ**出す |
| 整形への1手 | `focusReshape(part)` … 左パレットの既存ボタン（`#btn-cistrans-reshape`）を**そのまま押して**その成分へ寄せるだけ。**整形の中身には1行も触っていない** |
| 2つ目の書き始め | **`＋ お題の図をもう1つ`（`addCopy`）を足した。** 登録をやめた瞬間、置換基を動かすと1つ目が消える ＝ **2つ目を書く手段が無くなる**ため。⚠ **平行移動だけ**（回すと v446 の縦置き規則で立体そのものが変わる） |
| 帯 | `${entries.length}/${total}` → **`いま N個 描いてあります`**（`onDrawingChange` で作図のたびに追う） |
| `pendingPractices`（game.js） | トレイを持つ練習が**1つも無くなった**ので確認は出なくなる。物差しは残す（次に足す人が同じ穴を掘らないため） |

**実測（v1386・実アプリをヘッドレスで直接操作）**:

- 乳酸で `＋ お題の図` を押す → 2成分。片方の置換基を動かすと採点表は `ok,ok` で
  **ちがう立体 2 ／ 全 2 種。ダブり 0個・未発見 0種**（`① D-乳酸` `② L-乳酸`）＝ **2/2。`register()` は無い**
- 帯 … `お題 乳酸 の立体異性体 全 2 種 ／ いま 2個 描いてあります`（進捗欄は `2個`。**`n/総数` は1つも残っていない**）
- 一直線の 2-ブテンを混ぜる → `ok,unread` ／ **ちがう立体 1・未確定 1**。答え合わせに
  `まだ立体が決まっていない図: 1つ（種類には数えていません）・② は まだ立体が決まっていません（C=C の向きが読めません（1本。置換基を軸の上下に描く））整形して決めましょう →`。
  そのボタンで `reshapeMode = true`・オーバーレイは閉じる
- コンソールエラー **0件**。回帰テスト **431件 全合格**（428 → SW1・SW2・SW3 を足した）
- ★ 否定対照 SW5 を実際に外した（`row.status = 'unread'` → `'structure'`）→ **ST38 と SW3 の2件が赤**
  （`ok,unread,structure` が `ok,structure,structure` に／`ok,ok,unread` が `ok,ok,structure` に）

**やらなかったこと**: `1つの分子を選んで登録する方法`（§5-6 のとおり登録をなくせば消える問い）・
`並べ直す`（W4 と共用の SW3／検査は SW6）・整形の中身（別レーンの持ち物）。

### 5-9. 完了条件と否定対照（C）

| # | 何を見るか |
|---|---|
| **SW1** ✅v1386 | 乳酸の2種を1枚に描き、答え合わせが **2/2** とクリア記録を出す（`register()` を呼ばない）。帯が `いま 3個 描いてあります`（`n/総数` が残っていないこと）まで見る |
| **SW2** ✅v1386 | 同じ立体を2つ描くと「①と③は同じ立体です」が出て、**ちがう種類は1**と数える |
| **SW3** ✅v1386 | 未整形（一直線）の 2-ブテンを混ぜても**採点が成立し**、その成分だけが「まだ立体が決まっていません」の欄に入る。**種類には数えない**が、**数は出る**。SW5 の否定対照を同じ検査の中に置いた |
| **SW4 ★否定対照** | 立体練習中にキャンバスへ2成分を置いても、SVG の見出しに**化合物名が1つも現れない**（番号だけ）。分子モーダルも開かない ⇒ **`worksheetActive()` に立体を足さないと `🔍 ① 乳酸` `🔍 ② エタノール` が現れて赤くなる**（実測 M9。**入れる前から赤い検査**） |
| **SW5 ★否定対照** ✅v1386 | 未確定の図を**不正解として数える**実装（U4 案2）に差し替えると、SW3 が「ちがう種類 = 1」ではなく採点の内訳で赤くなる ⇒ 「読めないものは間違い」に丸めた直しが弾かれる。**実際に外して確認ずみ**（`'unread'` → `'structure'` で ST38・SW3 の2件が赤。SW3 の中にも同じ差し替えを埋めてある） |
| **SW6 ★否定対照** | 「並べ直す」の前後で、各成分の **`stereoCode` が1つも変わらない** ⇒ **回転を混ぜた直しが赤くなる**（IW7 の「相対座標が変わらない」より強い物差し。立体側にはこちらが要る。v446 の縦置き規則を踏み抜く直しをここで止める） |

---

## 6. D: 学習メニューの言い直し

> ✅ **実装ずみ（v1377）→ 正は `DESIGN_entry_points.md` §10 へ移した。**
> 以下は「なぜそう決めたか」の記録として残す（§0-2 の表のとおり）。
> 実装で足した検査は `EP7`（`<details>` の id と並びが不変・見出し2つ）・
> `EP8`（名札と案内が同じ語／件数で文言が変わらない）・`EP9`（★否定対照・D1 の根拠）。

> 学習メニューの中で反応機構モードが、映像コンテンツ（見るだけ）、のようになっていることがわかりにくい／
> ユーザーの感覚としては、現在描画している分子についてどんな反応をするか、付加反応の機構がどうなっているか見たい

### 6-1. 決定 D1: **反応実行（reactor）を学習メニューに足さない**

★ **これは好みではなく、実測 M3 で決まる。**

- **`setMode('learn')` になった瞬間、`#ws-free` が hidden になる**（実測）。
  分子モーダルの `⚗ 反応` も `renderMoleculeModal()` が自由モード以外で非表示にする
- つまり**学習メニューに reactor のボタンを置くと、押した時点で learn モードに入っており、反応が消えている**。
  「メニューに足す」は**動く形が存在しない**

**それでいいのか（設計としての正しさ）**: よい。reactor は「メニューから選んで始める練習」ではなく
「**いま描いている分子に対して常にできること**」で、しかも**件数がライブで出る**
（`🔬 この分子を調べる（反応 4件）`・M4）。メニューへ移すと
「-OH を付けた瞬間に反応が生える」という気づき（`DESIGN_molecule_modal.md` §4-2）が消える。

### 6-2. 決定 D2: 直すのは**名札**（U3 で文言を確定させる）

**推奨**: `🔬 この分子を調べる（反応 N件）` → **`⚗ 反応させる・調べる（反応 N件）`**

ユーザーの言葉「現在描画している分子についてどんな反応をするか…見たい」は**このボタンそのもの**なのに
届いていなかった ＝ 「**調べる**」が「**反応させる**」に読めなかった。⚠ 詳細と害は U3。

### 6-3. 決定 D3: メニューは**群の見出しで「やる／見る」を言い分ける**

★ **`<details>` の数・id・並び順は1つも変えない。**（`#reaction-box` は reactions.json が
読めないとき reaction.js が丸ごと隠すので id を動かせない。`#learn-acc-quiz` は
`demos-stereo.json` が `>summary` で開く。`#learn-acc-narrowing` の位置は EP4 が見ている）

**足すのは `#study-body` に挿す見出しの `<div>` 2つだけ**（クイズの中で既に使っている
`quiz-group-head` と同じ語彙 ＝ 新しい仕掛けを作らない）:

```
── 手を動かす ──
  🎓 クイズに挑戦
  ✏️ 構造の書き出し練習
  🔍 実験の結果から絞り込む
── 登録された反応を見る ──
  ⚗️ 反応機構ビューア
```

### 6-4. 決定 D4: `⚗️ 反応機構ビューア` の中に**行き先の案内を1行**置く

**2つのことを言う**（どちらも実測に基づく）:

1. **ここは「代表例の再生」である** —— 「登録済みの反応を、代表例の分子で再生します。
   **いま描いている分子を反応させるのは、キャンバスの `⚗ 反応させる・調べる` です**」
   ★ この言い方は reactor 側の既存の注記と**同じ語**を使う
   （reactor.js の「※『機構を見る』を押すと学習モードに切り替わり、あなたの分子そのものではなく
   **代表例の分子**で機構を再生します」）＝ 同じ関係を2か所で違う言葉で説明しない
2. **見るだけではない** —— 「反応を選ぶと、**主生成物を自分で組み立てて答え合わせする 🎯 予測**も使えます」
   ★ 実測: `🎯 予測`（`#btn-rx-predict`）は**反応を選んだあとの作業帯にしか出ない**ので、
   メニューからは存在が見えていない。**ボタンは増やさない**（反応を選ばないと押せないので、
   前に出すと空振りする）。sunk な出題に入口を作る EP4 の流儀とはここが違う

### 6-5. 完了条件と否定対照（D）

| | 中身 |
|---|---|
| **完了条件** | 学習メニューを開いた人が、**メニューを閉じずに**「自分の分子を反応させるのはどこか」を読み取れる（4項目の並びに群の見出しがあり、機構ビューアの説明に行き先が書いてある） |
| **EP7** | `#study-body` の `<details>` が **4つのまま**・id が `learn-acc-quiz` / `learn-acc-practice` / `learn-acc-narrowing` / `reaction-box` の**この順**。群の見出しが2つ挿さっている |
| **EP8** | 機構ビューアの説明に **`⚗ 反応させる`（作業帯のボタンの実際の文言）が文字列として含まれる** ⇒ ★ **U3 でボタンの文言を変えたのに案内を直し忘れると赤くなる**（2か所が別の名前で同じものを指す事故を機械で止める） |
| **EP9 ★否定対照** | **学習モードでは `#ws-free` が hidden** であることを assert する ⇒ 「メニューに反応実行のボタンを足す」直しを入れると、**そのボタンが押しても何も起きない**ことがこの検査の存在で説明される（＝ D1 の根拠が実装から消えたら気づける） |

---

## 7. E: 名称の説明

> ✅ **実装ずみ（v1378）。→ 正は [DESIGN_iupac_check.md](DESIGN_iupac_check.md) の
> §3-0（画面）と §5-1・§5-2（API）へ移した**（§0-2 の行き先の表のとおり）。
> やらないこと（主鎖候補の選定理由・§7-3 の★）は同書 §8 に落としてある。
> 以下は**決めた理由と、着手時に測った値**の記録として残す。
>
> **実装で分かって設計と変わったこと（2つとも移設先に書いてある）:**
> 1. 下の表の割り方 `プロパン`｜`-オール` は**名前の中に存在しない並び**だった。
>    かけらを繋いだら名前に戻る（IN10）ことを守る切り方は `プロパ`｜`ノール`。4つに割れる点は同じ
> 2. `dirReason` は6通り書いたが、**実際に出るのは5通り**。`'ene'` はエンイン
>    （C=C と C≡C の同居）でしか起きず、エンインは命名が未対応なので**名前ごと出ない**。
>    IN13 は「5通りが出そろう」＋「エンインに名前が付かない」の2つで固定した

**主鎖表示（N2・v1320）と炭素番号（`C₁` 添え字・v1371）は済み。残るのは「名称の説明」だけ。**

### 7-1. 決定 E1: 置き場所は **`🔢 主鎖と番号を見る` と同じ帯**（面を増やさない）

`DESIGN_iupac_check.md` §9-1 の「主鎖の帯は自由モードにも常設」と一貫させる。
番号を出しているとき、**その番号を生んだ名前を同じ画面に出す**（同書 N-6 の規則）が既にあるので、
**その名前を部品に割るだけ**。新しいトグルもモーダルも作らない。

### 7-2. 決定 E2: 名前を**部品に割り、押すと図の対応部分が光る**

**材料は全部そろっている**（実測 M14。表示していないだけ）:

| お題 | `iupacNameDetail` が返すもの | 割り方 |
|---|---|---|
| `2-メチル-1-プロパノール` | `kind:'chain'`, `mainChain` 3炭素, `locants.ol:[1]`, `locants.subs:[{loc:2, name:'メチル'}]` | `2-メチル` ｜ `-1-` ｜ `プロパン` ｜ `-オール` |
| `2-メチル-2-ブテン` | `locants.ene:[2]`, `subs:[{loc:2,name:'メチル'}]` | `2-メチル` ｜ `-2-` ｜ `ブテ` ｜ `ン` |
| `イソプロピルメチルエーテル` | `kind:'ether'`, `groups:['イソプロピル','メチル']` | `イソプロピル` ｜ `メチル` ｜ `エーテル` |

- **押すと光るもの**: 幹（`プロパン`）→ 主鎖の帯／位置番号（`-1-`）→ その炭素の `C₁`／
  置換基（`2-メチル`）→ その枝の原子／エーテルの基 → 既にある**2色の塗り分け**（N-5 で実装済み）
- ★ **エーテルにも効く**のが実利。C₄H₁₀O の7種のうち**3種がエーテル**（§N-5 の実測）で、
  ここで無言になると主力問題の半分弱で機能が消える
- **門番は N-4 のまま**: `iupacNameDetail` が null なら**説明も出さない**。
  新しい対応範囲リストを作らない（作れば二重管理になる）

### 7-3. ★ 決定 E3: **番号の「向き」の理由**を1行だけ返す（`chemistry.js` の小さな追加）

**いちばん効くのはここ。**生徒が間違えるのは「どちら端から数えるか」で、
`_iupacNameForMainChain` は**すでに優先順で向きを決めている**（実測 M15）:

```
cOl（-OH の位置）≠ 0      → その向き
cUn（多重結合）≠ 0        → その向き
cEne（二重結合）≠ 0       → その向き
cSub（置換基の位置）≠ 0   → その向き
それ以外                  → 置換基名の辞書順
```

**どの比較で決まったかは、分岐した後に捨てている。**→ **`dirReason` を1つ返すだけ**
（`'ol' | 'unsat' | 'ene' | 'sub' | 'alpha' | 'tie'`）。

- **出力は1バイトも変わらない**（名前も主鎖も既存のまま）＝ F8・F9・LB 群・IN1 は無傷のはず
- 画面に出す文は6通りの定型文だけ:
  `-OH が 1番になる向きを選びました` / `二重結合が早く来る向きを選びました` /
  `置換基が早く来る向きを選びました` / `どちら向きでも同じでした` …
- ★ **主鎖候補の選定理由（どの鎖を主鎖にしたか）までは遡らない**（§8-2 でやらないことに落とす）

### 7-4. 完了条件と否定対照（E）

| | 中身 |
|---|---|
| **完了条件** | `2-メチル-1-プロパノール` を呼び出して `🔢` を押すと、名前が4つの部品に割れて並び、**`-1-` を押すと `C₁` が光り**、「-OH が 1番になる向きを選びました」が出る。エーテル（`イソプロピルメチルエーテル`）では2つの基の名前が並び、押すと**既存の2色の塗り分け**が対応する |
| **IN10** | 部品を連結した文字列が **`iupacNameDetail().name` と完全一致**する ⇒ ★ **説明用に名前を組み立て直す実装が赤くなる**（名前を作る場所が2つになる罠。§N-1 と同じ家族） |
| **IN11** | 部品と図の対応が **`mainChain` / `locants` からだけ**引かれている（押した部品 → 光る原子の集合が、`mainChain[loc-1]` と `subs[].loc` と一致） |
| **IN12** | `dirReason` を足しても、`compounds.json` 全件（939件）で `iupacName(mol)` の出力が **1件も変わらない** |
| **IN13 ★否定対照** | `dirReason` が**6通りとも実際に出る**（`compounds.json` を掃いて、各値が1件以上）⇒ 「いつも `'ol'` を返す」ような**空回りの実装**が赤くなる。あわせて `iupacNameDetail` が null の分子では**説明が0文字**であること（門番 N-4 を緩めた直しが赤くなる） |

---

## 8. F: 発注書 D をどう畳むか

> ユーザー: 「C（答え合わせ）の出来を見てから判断したい。答え合わせが分かりやすくなればキャンバスに戻る必要はない」

**C は v1370 で着地（2列対応表・§12-7）し、さらに3列化が別レーンで進行中。**

### 8-1. 決定 F1: D は「不要」ではなく「**縮小して残す**」（U5 の推奨）

統合レーンの見立て「§7 が入れば D はほぼ自動的に満たされる」は**半分正しい**。

- **正しい半分**: 「なぜその名前か」の中身（主鎖・番号・部品の対応・向きの理由）は §7 が全部出す。
  D の受け入れ条件④「**お題に数えなかった図にも名前と番号が出る**」も、
  §7 の門番（`iupacNameDetail` が非 null なら出す）が**そのまま満たす**
- **足りない半分**: **面が違う。**§7 の置き場所はキャンバス（`🔢` の帯）で、
  D が言っているのは**答え合わせの表の中**。§7 だけでは
  「答え合わせを閉じる → キャンバスで `🔢` を押す → 答え合わせを開き直す」の往復が残る
  —— これは §13-1a で学んだ「**手元を覆うものは、覆われたら用を成さない**」と同じ形

→ **答え合わせの2列（3列）表の右セルに `🔢` のトグルを1つ**置く。**押した行だけ**両側に帯と番号が出る。

**なぜ全行に常時出さないか**: C₄H₁₀O は7行・1行 84px（小）。**7つの図が数字で埋まると読めない**（§12-7 の実測）。

### 8-2. 決定 F2: 発注書 D の「対抗馬を薄く」「選んだ理由を1行」は**やらない**

発注書自身が「2と3は `iupacNameDetail` が『なぜそれを選んだか』を返せないと作れない」と書いている。

- **§7-3 で返すのは「番号の向きの理由」だけ**で、**主鎖候補の選定理由ではない**。
  主鎖の選定は `cands` を4段階で絞ってから `named.sort` が5つの規則で決める（実測）ので、
  「なぜこの鎖か」を1行で言うには**5つの規則を説明する文が要る**
- **同点の主鎖は 31件ある**（§1-1 (b) の実測）が、そのうち**残っている食い違いは9件**で、
  しかも §6 の注記どおり「炭素数が同じままの食い違いは**自己同型で入れ替わるだけ**」＝
  **見せても『どちらでも同じ』にしかならない**ものが多い
- → **「同点の主鎖を見せる」は、まず『見せる値打ちのある同点』がどれだけあるかを測ってから**。
  測っていない段階で画面を作るのは、このリポジトリの流儀に反する

### 8-3. 完了条件（F）

| | 中身 |
|---|---|
| **完了条件** | 答え合わせの表で1行の `🔢` を押すと、**その行の左（正解）と右（自分の図）の両方**に主鎖の帯と `C₁` の添え字が出る。エーテルの行では2色の塗り分けになる。**お題に数えなかった図の行でも出る** |
| **IW12** | `🔢` を押した行だけに番号が出る（他の行の番号テキストは 0 個）。図の大きさ 小/中/大 のどれでも、番号が**丸の中に収まる**（IN7 と同じ物差しをサムネイルの縮尺で掛ける） |

---

## 9. 実装の順序と依存

```
        ┌──────────────────────────────────────────┐
        │ 独立に走れる（互いに触る場所が違う）        │
        └──────────────────────────────────────────┘
  A（§3 アルキルの付け根）      learn.js AlkylPractice + game.js placeAtomOrExplain
  D（§6 メニューの言い直し）     index.html + game.js のラベル
  E（§7 名称の説明）            chemistry.js + game.js（表示）
  C-SW1/SW2（§5 立体の答案用紙化）learn.js StereoPractice + game.js worksheetActive

        ┌──────────────────────────────────────────┐
        │ 依存があるもの                             │
        └──────────────────────────────────────────┘
  W4（答案を並べ直す・§12-5）
      ├→ B（§4 芳香族プリセット）  … ベンゼン環4つが 800×600 に入らない
      └→ C-SW3（立体の並べ直し）    … 回転禁止の否定対照 SW6 はここで入る
  E（§7 名称の説明）
      └→ F（§8 発注書 D の縮小版）  … 表の中に描く前に、キャンバスで形を決める
  D2（§6-2 ボタンの文言）
      └→ EP8                       … 案内の文と実際の文言が一致すること
```

**推奨する着手順**: **A → D → E → C(SW1/SW2) → W4 → B・C(SW3) → F**

- **A が先**: いま**練習が1つ目で止まる**（2枚目が描けない）＝ 機能が丸ごと使われていない。害がいちばん大きい
- **D は最小**（文言と `<div>` 2つ）。**U3 が決まればすぐ入る**
- **E は独立で、しかも F の前提**
- **C は大きい**が、W1 の写しなので設計の迷いが少ない。⚠ **SW4 は入れる前から赤い検査**（M9）なので、
  「これを緑にする」ことが着手の目印になる
- **W4 が2つの下流を抱えている**ので、B と C-SW3 を急ぐならここを先に上げる

⚠ **並行レーンとの当たり**: `reshape-tap`（整形）と C の §5-4 が近い。
**C 側は整形を呼ぶ口を1つ用意するだけ**にして、整形の中身には触らない。
`answer-3col`（答え合わせの3列化）と F が同じ表を触るので、**F は 3列化の着地を待つ**。

---

## 10. 完了条件と否定対照の一覧（何が壊れたら気づけるか）

★ **設計の段階で「壊れたら赤くなるもの」を決めておくのが今回の要点**なので、一覧にする。
**接頭辞は `assembler/tests.js` 冒頭の台帳で未使用を確認した**（`SW` は未使用。`AK` は 5〜7 へ・
`BZ` は 6〜7 へ・`EP` は 7〜9 へ・`IN` は 10〜13 へ・`IW` は 12 へ伸ばす）。

**★ 実装時に台帳へ書き足すこと**:
`| SW | 1〜6 | 立体異性体の書き出しの答案用紙化（登録の廃止・未確定の欄・名前を伏せる門番・並べ直しで立体が変わらない） |`

| 検査 | 外すと壊れるもの | いま赤いか |
|---|---|---|
| **AK6** | 「炭素だけに付け根が生える」。どの元素でも生やす親切にした直しが赤くなる | 実装後 |
| **AK7** | 「見えている範囲に置く」。固定格子に差し戻すと **x が 258px 飛んで**赤くなる（実測 M6 が物差し） | 実装後 |
| **BZ7** | 芳香族回が種つき列挙の道を通ること。`problems` に足す実装だと **3523種で打ち切り**になる | 実装後 |
| **★ SW4** | 立体練習中に名前を伏せる門番（`worksheetActive()`）。**いま実際に漏れている**（M9） | ★ **入れる前から赤い** |
| **SW5** | 「未確定は不正解ではない」。読めない図を間違いに丸めた直しが赤くなる | 実装後 |
| **SW6** | 並べ直しが剛体平行移動だけであること。**回転を混ぜると `stereoCode` が変わって**赤くなる（v446 の縦置き規則を踏み抜く直しをここで止める） | 実装後 |
| **EP8** | メニューの案内文と作業帯のボタンの文言が**同じ名前を指す**こと。片方だけ直した事故が赤くなる | 実装後 |
| **EP9** | D1 の根拠（学習モードでは `#ws-free` が hidden）。根拠が実装から消えたら気づける | **いま緑にできる**（現状の実装がそうなっている） |
| **IN10** | 名前を作る場所が1つであること。説明用に名前を組み立て直す実装が赤くなる | 実装後 |
| **IN13** | `dirReason` が空回りしていないこと。いつも同じ値を返す実装が赤くなる | 実装後 |
| **IW12** | 表の中の番号が「押した行だけ」に出て、丸の中に収まること | 実装後 |

> **なぜ「入れる前から赤い」検査を1本は置くのか**: そうでない検査は
> 「入れたら通った」しか言わない ＝ **実装が本当に穴を塞いだのか、検査が空回りしているのか**を
> 区別できない。SW4 は実測で穴が開いていることを確かめてある（M9）ので、
> **先に検査を書いて赤いことを見てから直す**ことができる。

---

## 11. やらないこと（範囲外）

- **立体の採点を構造異性体の練習に持ち込む**。§15-4「構造異性のみ、という既存の線（§4.2）を動かさない」は据え置き
- **整形モードの仕様変更**。「未確定を確定させるのは整形モードのタップだけの仕事」（CLAUDE.md）は動かさない。
  §5-4 で決めたのは**未確定の図を採点表でどう呼ぶか**だけ
- **主鎖候補の選定理由の表示**（発注書 D の 2・3）。§8-2 の理由により、まず「見せる値打ちのある同点」を測ってから
- **反応実行を学習メニューに置くこと**。§6-1（実測 M3 で原理的に成立しない）
- **芳香族の系統名・環の番号づけ**。`iupacName` が未対応（P12-3）。門番 N-4 により自動的に番号も出ない
- **立体を含む名前**（E/Z・R/S を名前に織り込む）。P12-7/P12-8 側の決定待ち
- **ナフタレン等の2環系**（§11-3 の判断を維持）
- **アルキル基・立体の練習にヒント4段とスコアを入れること**。W2 は異性体側の話（§15-1）。
  面の分担（確認 ⇄ 答え合わせ）だけ揃える
- **`＋ 答案` ボタンの廃止**。§3-3 の理由（保険・AK3・帯の構成）
- **お題ボタンから総数を隠すこと**。U1 案3 として選択肢には出すが、こちらからは提案しない
