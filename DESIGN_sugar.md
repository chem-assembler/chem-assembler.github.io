# 設計書: 糖 —— 単糖 ⇄ 二糖・二糖を横から見る・環を裏返す（2026-08-21）

`DEVELOPMENT.md`「糖と高分子の4件」のうち **1・2・3** を1本にまとめた設計。
4（エチレン → 付加重合）は別レーンなのでここでは扱わない。

> **起草時の断り書き**: この文書は**設計だけ**で、実装は1行もしていない。
> **アプリのコード・データ・版は1つも変えていない**（`git diff` はこの `.md` 1件のみ）。
> 数はすべて実測で、測り方と再現手順は **§9** にある。推測で書いた数は1つも無い。

## 進捗（2026-08-22・v1442）

| 段 | 状態 |
|---|---|
| **段0** SG1〜SG5（反転の不変量のテスト） | ✅ **完了**。`tests.js` の **SG1〜SG7**。§1-2 の表がそのまま入っている |
| **段1** R-1 カメラの倒し角を 0〜180° に開ける | ✅ **完了**。プリセット「⟳ 裏返す」と 90°超の凡例つき |
| **段2** R-2 二糖を環ビューに入れる | ✅ **完了**（v1442）。**正は `DESIGN_3d_correspondence.md` §7 へ移した** |
| **段3** R-3 裏返しのボタン／R-4 断り文 | ✅ **完了**（v1442・段2と同時。割れなかった）。同上 |
| **段4-a** §4-2 切った -OH の向き | ✅ **完了**（v1444）。`haworthCleaveDirection` ＋ SG12・SG13。下の §4-2a が実測の正 |
| **段4-b** キャンバスで環を裏返す | ⚠ **v1447 → v1450 で二度作り替えた**（押すボタンは廃止・加水分解のとき**単独の図をまるごと写す**）。**正は `DESIGN_3d_correspondence.md` §8・§8.0a**。下調べの §4-9 は残す（訂正は §4-9a・§4-9b・§4-9c） |
| 段5 §4-8 つなぐ側 | 未着手 |
| **段6** 二糖の登録図を**真横**に並べ直す | ✅ **完了**（v1446）。**§11 が正**。§10-2 の分かれ道を「アプリの絵を教科書側に直す」で決着させた |

### 段4-b で足したもの（`compounds.json` / `stages.json` / `reactions.json` は1文字も触っていない）

- `chemistry.js` … **`haworthFlipPlan` / `haworthCanvasFlip`**（キャンバスの図で環を裏返す3手）と、
  環ビューと共有する **`haworthNumberingStart` / `haworthRingSense` / `orderHaworthRings`**
- `stereo.js` … `ringNumberingSense` / `orderDisaccharideRings` を上の1本へ**委譲**（実装の重複を消した）
- `index.html` / `game.js` … 分子モーダルの中の節 `#mm-haworth`（**新しいモーダルは作っていない**）
- `tests.js` … **SG14〜SG16**（否定対照は実際に外して赤くなることを確認した8通り）

### ⚠ 段4-b で分かった、§4-9 の読みの訂正（§4-9a）

1. ⚠ **§4-9(d) の「縦にずらす」だけだと、グリコシド結合が 335px（平常の4倍）に伸びる。**
   縦だけに逃がすと相手の環をまたぎ越すまで動かすことになるため。
   **横も候補に入れて「橋の結合がいちばん短くなる置き方」を選ぶ**と 152px に収まる。
   さらに**マルトース・セロビオース・ラクトースは、ずらさなくても重ならない**
   （最短 115/38/38px）＝ ずらしが要るのは**スクロースだけ**だった
2. ⚠ **「もう一度押すと戻る」は探索では作れない。** ずらしの向きは片道（相手から離れる側）なので、
   2回目に打ち消す置き方を選べない（実測: 二糖8件中6件で図がずれたまま）。
   **ずらし量を覚えて、その逆操作を当てる**のが正しい（16/16 で1ピクセルの誤差もなく戻る）
3. ⚠ **`haworthNumberingStart`（環の O ＋ その隣がアノマー）だけでは、ラクトンと酸無水物も通る**
   （登録 22件になる）。**アノマー炭素は sp3** で切り分けて 16件にそろえた
4. ⚠⚠ **「自動反転は要らなかった」は誤りだった**（v1447 で訂正・§4-9b）。
   根拠にしたのは「裏返してから加水分解しても**名前が変わらない**」だが、
   ⚠ **名前は裏返しても絶対に変わらない**（§1-2 の② が 16/16）。**見るべきは向きだった。**

### 段4-a で足したもの（`compounds.json` / `stages.json` / `reactions.json` は1文字も触っていない）

- `reactor.js` … `haworthCleaveDirection(mol, cId, oId)`（切る前に橋の -O- が出ていた側を返す）と、
  `freeSpotAround` の4つ目の引数 `prefer`（**既定の順序は1つも変えていない**）。
  caption に**変旋光の断り**を足した
- `tests.js` … **SG12**（4件×2つが名乗る・否定対照は「-OH を真横に戻すと3件落ちる」を作り込み）／
  **SG13**（トグル OFF でも黙って名無しにしない）

### ⚠ 段4-a で分かった、この文書の読みの訂正

1. ⚠ **§4-2 の直し方（「`freeSpotAround` に “環の sp3 炭素なら上下を先に試す” を足す」）は足りない。**
   上下**どちらか**は `freeSpotAround` には決められない —— 面（α/β）は**切る前の橋の向き**が決めるので、
   その情報は呼び出し側にしか無い。実装は**呼び出し側から向きを渡す**形にした
   （`freeSpotAround(mol, cId, [], dir)`）。⚠ こうすると**他の反応の見た目は1ピクセルも動かない**という
   副次的な利点もある（§4-2 の案は環の原子に触る全反応の配置を変えてしまう）
2. ⚠ **§4-2 の表の「スクロースだけ ○」は正しかったが、理由の側に続きがある。**
   実測すると「常に下（＝アルドピラノースなら α）」に置く案は、ラクトースを α-D-ガラクトースにできる代わりに
   **スクロースのフルクトース側を名無しに落とす**（候補2件・立体不一致）。**8/8 が名乗るのは「保存」だけ**（§4-2a）
3. **§4-1 の表の「名無し」は、立体トグル ON での話。** OFF では
   「アロース／ガラクトース ほか3種 のどれか（立体で決まります）」に丸まる（**設計どおり**・SG13 が見張る）。
   ⚠ **測るときは必ず `readStereo = true`**。OFF のままだと直ったかどうかが見えない

### 段2・段3 で足したもの（`compounds.json` / `stages.json` / `reactions.json` は1文字も触っていない）

- `chemistry.js` … **部分反転の呼び方**を決めた。`flipHaworth(mol, ids, axisY)` に
  **軸の引数**を足し、`haworthRingSideIds(mol, cycle)`（その環＋そのぶら下がり。
  相手の環を含む枝は捨てる）と `haworthRingBridge(mol, cA, cB)`（橋の原子）を建てた
- `stereo.js` … `buildRingModel` の門番を割って**二糖を受け入れ**、環2つの模型を組み、
  食い違うときは**環Bだけを裏返す**。「⇅」ボタン・仮定の断り書き・断り文の訂正
- `tests.js` … **SG8〜SG11**（否定対照は実際に外して赤くなることを確認した8通り）

### ⚠ 段2 で分かった、この文書の読みの訂正

1. ⚠⚠ **`flipHaworth(mol, ids)` を軸なしで部分反転に使ってはいけない**（§3-4 R-2 は
   「環Bのノードだけに (x,y,z)→(x,−y,−z) を当てる」としか書いておらず、**軸を決めていない**）。
   既定の軸（その集合の重心）で部分反転すると、**二糖4件 × どちらの環を動かすかの2通り ＝ 8/8 で
   立体コードが変わる**（実測）。正しい軸は**橋の原子の y** で、そこなら **8/8 で保たれる**。
   理由: 部分反転では**境目の結合（グリコシド酸素）だけが「片方だけ動いた」状態**になる。
   橋の原子の y を軸にすると、その結合ベクトルが**ちょうど y 反転**する
   ＝ 面の符号が反転し、**縦からの角度（±25° の読める/読めない）が変わらない**
   ＝ 動いた側の環中心のパリティが**回転として**保たれる
2. **絵の軸と分子の軸は別でよい。** 絵は**環自身の重心**で折り返すのが正しい
   （スクロースを橋の y で折り返すと、フルクトース環がグルコース環の上に重なる。実測: 環A y 152..248 /
   反転後の環B y 84..246）。両者は縦の平行移動ぶんしか違わないので、面（z）と立体は同じ
3. ⚠ **§3-4 R-2 の「橋の面を両側から読み直して食い違いを見る」は、絵を裏返したあとには使えない。**
   環自身の重心で折り返すと橋の原子は動かないので、読み直すと**同じ値が返る**。
   模型では**面を読み直さず、裏返しに合わせて符号を反転する**（`F = f => flip ? -f : f`）
4. **A/B の決め方は §3-4 の「アノマー炭素を持つ側。左／上にある方」では決まらない。**
   セロビオースは供与側が左下、マルトースは左 —— 左右は当てにならない。
   ⚠ **「橋をアノマー炭素で持っている側を B」＋「両方アノマーなら大きい環を A」**で
   4件すべて決まり、スクロースでは自動的に**フルクトース側だけが裏返る**（＝ 教科書の向き）
5. **スクロースの30°は §6-3 の保険で救われる**（§5-3 の読みどおり）。⚠ ただし
   **保険に頼ったことを画面に書く**（`bridge.derivedB`）。保険でも読めなければ**断る**

**段0・段1 で足したもの**（`compounds.json` / `stages.json` / `reactions.json` は1文字も触っていない）:

- `chemistry.js` … 「同じ分子のまま置き直す」2つの操作を門番つきで公開した。
  **`canFlipHaworth` / `flipHaworth`**（上下反転。y を折り返し、**面マークも一緒に反転**）と
  **`haworthSpinCycles` / `canSpinHaworthRing` / `spinHaworthRing`**（独楽回転）。
  ⚠ **鏡映は建てていない**（SG6 がその不在を見張る）。
- `stereo.js` / `index.html` … 環ビューの倒し角を 0〜180° に開けた（段1）。

### ⚠ §4-6 の読みのうち、実装して分かった訂正

1. **`canSpin90` が禁じている理由は「回すと立体が変わるから」ではない。**
   v472（`c31fc39`）のコミットメッセージが明記していた ——
   **「90° は図の読みの約束が変わる分子では使わない（フィッシャー投影とハース投影の向きを崩さないため）」**。
   ＝ **回転そのものは立体を変えない**が、**読みの規約が向きに縛られている**ので読めなくなる。
   ⚠ **したがって「上下と向きをセットで回せば心配が消える」のは反転だけで、90°回転には効かない。**
   `canSpin90` は**そのまま残す**のが正しい（コードにその旨を書いた）。
2. ⚠ **「独楽回転」を図のアフィン回転として実装してはいけない。**
   図をそのまま面内で回すと置換基が斜めになって面が読めなくなり、**180° はまさに §1-2 の③**
   （＝ 0/16 の否定対照）になる。正しい独楽回転は**環の席をずらして、置換基ごと平行移動する**
   置き直しで、これなら置換基の縦位置が1つも動かない ＝ 環に垂直な軸まわりの回転そのもの。
   実測: 16件 × 全ステップ数 × 全環で **立体コード 16/16 同一**。
3. **上下反転の門番は `haworthFace` の有無ではなく「フィッシャーとして読める中心があるか」で決まる。**
   フィッシャーは「縦＝奥」なので、y 反転は**奥の2本の入替**＝その中心が鏡像に化ける
   （ハース図とちょうど逆で、反転が読みの約束を壊す側）。
   ⚠ この門番は全登録 **1,059件で過不足なく一致**した（許して変わったもの0件・断ったのに安全だったもの0件）。

---

## 0. この文書の役割と、正がどこにあるか

| | |
|---|---|
| **正** | 実装するまではこの文書。実装したら (2)(3) は `DESIGN_3d_correspondence.md`、(1) は `DESIGN_reaction_execution.md` へ移す |
| **触る面** | (1) 反応実行モード（`reactor.js`）／(2)(3) 立体対照ビューの「⬡ 環を横から」（`stereo.js`） |
| **触らない面** | ⚠ **反応機構ビューア（`reaction.js` / `reactions.json`）は触らない。** 2026-08-21 のユーザー確定により (3) の行き先は環ビュー側になった |
| **前提にしているもの** | `readRingParityFromHaworth`（`DESIGN_stereochemistry.md` §11.3・§12.1）。⚠ **読み取りは1文字も変えない** |

**読む順**: §1（実測）→ §2（★ の問いへの答え）→ §3（(2)+(3)。同じ面）→ §4（(1)）
→ §5（化学の確認と出典）→ §6（着手の順番）→ §7（やらないことを勧めるもの）→ §8（決めてほしいこと）。

**ユーザーの原文**（2026-08-21）:

> **単糖⇆二糖　の反応を実装したい**
> **環をヨコから見る、で２糖の場合、２つのハース環が同一平面にあるという仮定で見れるようにしたい**
> **インベルターゼでフルクトースがフリップする、トレハロースのフリップなども反応機構として見たい**

**同日の追記（(3) の確定）**:

> **環の反転を意味します。**
> **ハース環で簡略化した構造を考えた時、フルクトース５員環構造を通常表示する向き（炭素番号が時計回り）から、
> スクロース中では逆向きになります。これはフルクトースの平面を反転させる操作で視覚化できます。
> 見せたいことは現在の反応機構ビューアーよりも、環をヨコから立体視した視点の方が近いです**

---

## 1. 先に実測（推測で書いていない証拠）

### 1-1. アプリは糖の環をどう持っているか ——「環の形」はトポロジー、「立体」は座標

| | 決まり方 | 根拠 |
|---|---|---|
| **ピラノース／フラノース**（6員か5員か） | **トポロジー** | `canonicalCode` は重原子グラフだけを見る。座標を平行移動しても、反転しても、鏡映しても **16/16 で同じコード**（下の表） |
| **α/β・D/L**（面の上下） | ⚠ **座標**（＝描かれた縦位置） | `readRingParityFromHaworth` が環外置換基の**±25°以内の縦位置**から面を導く（`DESIGN_stereochemistry.md` §12.1 の明示の例外） |

`CLAUDE.md` の「検証はトポロジーのみ・座標は見た目専用」は、**糖の α/β についてだけ例外**になっている。
これは §12.1 が意識して開けた穴で、事故ではない。**今回の3件はすべてこの穴の上に立っている。**

### 1-2. ★ 反転の不変量（**この文書でいちばん大事な表**）

**環をもつ糖の登録 16件**（ピラノース10・フラノース2・二糖4）に対し、描かれた図へ4つの操作を当てて、
`canonicalCode`（トポロジー）と `canonicalStereoCode`（立体）が変わるかを**全数**で測った。

| 図への操作 | 何が起きているか | canonicalCode 同一 | **canonicalStereoCode 同一** |
|---|---|---|---|
| ① 平行移動 | 何も変えない（対照） | **16/16** | **16/16** |
| ② ★ **正しい反転**（y を反転＝上下入替 **＋** たどる向き逆。面マークも反転） | 3D で見ると x 軸まわりの **180°回転**（行列式 +1） | **16/16** | **★ 16/16** |
| ③ ✗ **上下だけ入れ替える**（向きはそのまま＝面内180°回転） | 3D で見ると**点対称（行列式 −1）＝鏡像** | 16/16 | **0/16** |
| ④ ✗ **向きだけ逆にする**（上下はそのまま＝左右の鏡映） | 3D で見ると **yz 面の鏡映＝鏡像** | 16/16 | **0/16** |
| ⑤ ✗ 反転したが**面マーク（`haworthFace`）を直し忘れた** | 座標は回ったのにマークは回っていない | 16/16 | **8/16**（マークを持つ8件が全滅） |

**読み方**:

- ⚠ **② が 16/16 ＝ 反転は「同じ分子」を1つも壊さない。** これが実装すべき不変条件で、回帰テストの核。
- ⚠ **③ と ④ が 0/16 ＝ 半分だけやると全件が鏡像に化ける。**
  統合レーンの警告（「上下だけ入れ替えて向きを変えないと、アプリは立体が変わったと読む」）は**実測で裏が取れた**。
  しかも**確率的ではなく全件**なので、間違えたら必ず壊れる ＝ テストで捕まえられる。
- ⚠ **⑤ が 8/16 ＝ 面マークを持つ8件だけが落ちる。** ここが**いちばん踏みやすい地雷**（次項）。

### 1-2b. ★★ 平面のハース図には「どちら側から見ているか」を書く場所が無い（2026-08-24・Fable 判定）

**発端**: ユーザー「左右反転は立体構造が変わらないはずです。ハース平面の中心線を軸に回転するだけです」。
**この指摘は3次元の操作としては全面的に正しい**（実測でも、保存された立体コードは左右反転で 16/16 不変）。
それでも図の読みが変わる理由を、ここで1回で決着させる。**以後、甲案（読み手の変更）を再提案しないこと。**

**核心**: 平面のハース図は「環のどちら側から見て描いたか」を**記録できない**。
その結果、**x 鏡映した平面図は、鏡像異性体（L-糖）を標準の向きで描いた図と1画素も違わない。**
両方の解釈がまったく同じ絵になるので、紙の上では区別のしようがなく、
教科書の規則（**面の上下 × 番号をたどる向き**）で読めば **L-糖と読むのが正しい**。
⚠ **つまり読み手（`readRingParityFromHaworth` → `_parityFromDirs`）は壊れていない。教科書の規則そのものを実装している。**

**平面図への4つの座標操作と、意味の保存**（§1-2 の表の言い換え。実測済み）:

| 平面図への操作 | たどる向き s | 面 f | **f×s（＝読み）** | 意味 |
|---|---|---|---|---|
| 平行移動 | 保存 | 保存 | 保存 | 同じ分子 ✅ |
| ★ **② 上下フリップ**（y 反転＋面マーク反転） | 反転 | 反転 | **保存** | **同じ分子 ✅（唯一の非自明な操作）** |
| ✗ 左右の鏡映（x 反転） | 反転 | 保存 | 反転 | **鏡像の図** |
| ✗ 面内 180° 回転（メリーゴーランド） | 保存 | 反転 | 反転 | **鏡像の図** |

★ **「本のページをめくる」（3D の縦軸回転）を行ったあとに教科書が印刷する図は、②の図そのもの。**
実証: スクロース中のフルクトース環は、単独の登録に②を当てた図で描かれていて（v1448）、
ユーザーが「教科書と一致」と実機で確認している。**＝ 意図された3D操作は、平面上では②として既に実装されている。**
素朴な x 鏡映の図（環の手前の辺が上に来る図）は、**教科書がそもそも印刷しない絵**である。

**帰結（3つ）**:

1. ⚠ **甲案（読み手に「裏から見ている」ビットを足す）は採らない。**
   それをやると「アプリだけが正しく読めて、紙に印刷すると別の分子に読める図」を作ることになり、
   発注の芯（紙面での構造式を理解できるようにする）の**正反対**になる。
   `lookupCompoundName` に乗る検査 156 箇所にも触らずに済む。
2. ★ **加水分解の生成物は「登録の図（＝教科書の図）で描き直す」（乙案）。**
   向き（sense）だけ直しても環 O の位置までは標準図に揃わない（ユーザーの「上下逆に見える」報告）。
   登録の図に置き直せば、検収条件「反応前後とも教科書の図」を**構成的に**満たす。
   ✅ **v1450 で実装済み**（`game.standaloneDrawingOf` / `redrawProductsAsStandalone`。
   実測: 二糖4件×2断片ぜんぶ、単独の図と**平行移動を除いて完全一致**・残差 1e-13 台。§4-9c）。
3. ★ **組み立て画面に「同じ分子の操作」として出してよいのは②（上下フリップ）だけ。**
   メリーゴーランドと左右鏡映は**鏡像の図を作る操作**なので、同じ分子の操作としては出せない。
   ⚠ ただし捨てるのではない——「上下反転したマルトースの図から正しいものを選ばせる」入試の型の
   **トラップそのもの**なので、「どれが同じ分子？」の出題として見せる価値がある
   （読み手は今日すでに3つを正しく区別する。新しい判定は1行も要らない）。
   ✅ **v1450 で実装済み**（§4-10）。札は**作業帯**の `⇅ 上下に裏返す` 1つだけ。
   ⚠⚠ **→ 帰結3の「②だけ」は狭すぎた。§1-2c（2026-08-24 追補）で訂正。**
   ②しか無いのは**剛体の座標変換（鏡映・回転）に限った場合**の話で、罠の側の結論（素朴な
   左右鏡映・180°回転＝鏡像の図）は**そのまま正しい**。v1451 の出題も無傷。

### 1-2c. ★★ 追補（同日）: 置換基の「付け替え」を許すと、同じ分子の図は4枚ある

**発端**: ユーザー「同じ要領で左右に裏返す・180度メリーゴーラウンド回転のボタンも欲しい」。
§1-2b の帰結3のままでは実装禁止に読めるが、**帰結3が狭すぎた**。

★ **剛体変換だけでなく「環は動かし、置換基は付け根の環炭素について上下を付け替える」
という描き直しまで許すと、意味を保つ図はちょうど4枚**になる（クラインの四元群）:

| 図 | 作り方 | たどる向き s | 面 f | f×s | 紙の動かし方 |
|---|---|---|---|---|---|
| 元 | — | ＋ | ＋ | ＋ | — |
| ② 上下フリップ | y 鏡映＋面マーク反転 | 反転 | 反転 | **保存** | カレンダーをめくる |
| ★ **左右フリップ** | **x 鏡映 ＋ 置換基を付け根で上下に付け替え**（マークも反転） | 反転 | 反転 | **保存** | 本のページをめくる |
| ★ **メリーゴーランド** | **180°回転 ＋ 置換基を付け根で上下に付け替え**（マークも反転） | 保存 | 保存 | **保存** | 裏返さず180°回す |

★ **証拠は教科書自身が両方を印刷していること**:
- **セルロース・セロビオースの裏返った環**（環 O が下・CH₂OH が下）＝ **②の図**
- **スクロース中のフルクトース環**（環 O が上のまま・C2 が左・番号が反時計回り）＝ **左右フリップの図**
  （v1448 の登録がこれで、ユーザーが教科書と一致することを実機確認済み）

⚠ **罠との区別を混同しないこと**: v1451 の「どれが同じ分子？」の罠は**付け替えをしない素朴な**
x 鏡映・180°回転（＝鏡像の図）。上の2操作は**付け替えを行う**ので別の絵になり、同じ分子の図。
**罠と新しい操作は1画素単位で異なる絵**であり、両方が共存できる。

⚠ **実装の門番は v1452 と同じ**: 操作のたびに図から立体コードを読み直し、
**元と一致しなければ巻き戻す**（黙って鏡像の図を作らない）。付け替えの実装が甘くても
（例: 二糖の橋の扱い）、門番が跳ねるだけで壊れない。

### 4-10. ⇅ 上下に裏返す（組み立て画面の札・v1450）

**画面の言葉はユーザーの言い方**: 札は `⇅ 上下に裏返す`、説明（title）に
**「上下を入れ替えるように裏返します（カレンダーをめくるように）」**。

| 決めたこと | 中身 | 根拠 |
|---|---|---|
| **出す操作は②だけ** | 上下フリップ（y 反転＋面マーク反転）。⚠ 左右鏡映・面内180°回転の札は作らない | §1-2b 帰結3（鏡像の図になる） |
| **動かすのは分子まるごと** | 単糖も二糖も、その連結成分の全原子を1本の軸で折り返す。⚠ `haworthCanvasFlip` は**使わない**（環が2つあると**片方の環だけ**を当てる ＝ v1449 で禁止した操作） | §4-9b |
| **門番** | `haworthFlipPlan(part).ok` ＋ `canFlipHaworth`。実測で**登録 939件中ちょうど 16件**（§1-5 の内訳と一致） | FL1 |
| **置き場所** | **作業帯**（`#ws-free` の `#reaction-card` の行）。⚠ 分子モーダルの「🔬 調べる」は `#mm-reaction` の後ろ ＝ `DESIGN_molecule_modal.md` §6-2a の実測で**画面の外**（底まで 536px スクロール）。行は増やさず `↩ 反応前に戻す` と同じ扱いで相乗り | §6-2a |
| **2回押すと完全に戻る** | ★ **軸を覚える。** 折り返した図から重心を計算しなおすと丸めで戻らない（実測: chromium で3件中2件が戻らない）。同じ軸を使えば **16/16 で1ピクセルの誤差もなく戻る** | FL2 |
| **↩ 戻す にも積む** | 押すたびに `saveState()` ＝ 汎用の取り消しでも戻せる | FL2 ④ |

- ⚠ **糖でない分子では札が出ない**（エタノール・酢酸で実測）＝ **L3（機能解説ロング）の収録待ちに影響しない**。
  あの台本はエタノール・酢酸で進むので、帯のボタンは1つも増えない
- ⚠ **押せる置き直しの口は帯の1つだけ**（モーダルには置かない）。FL3 が両方を見張る

### 1-3. ⚠ 面マーク（`haworthFace`）を持つ8件が地雷

`compounds.json` 939件のうち、`haworthFace` を明示している登録は **8件**（`stages.json` は0件）:

```
beta-d-galactose / alpha-d-galactose / beta-d-mannose  / alpha-d-mannose
alpha-d-allose   / beta-d-allose     / alpha-d-gulose  / beta-d-gulose
```

- **どれも同じ1原子**に付いている: C5 の環外の炭素 `C(345,214) face=+1`。
  その真下 `(345,252)` が C5 なので、**縦位置からも同じ +1 が読める** ＝ **8件すべて、マークは冗長**
  （マークを外しても `canonicalStereoCode` は変わらないことを実測で確認）。
- ⚠ **冗長なのに害がある。** 面マークは**座標より優先**されるので、座標を反転してもマークは付いてこない。
  ＝ 反転すると「座標は下・マークは上」の食い違いが起き、**その1中心だけ鏡像になる**（⑤ の 8/16）。
- ⚠ **反転を実装するなら、面マークも一緒に反転させること。** データ側で8件のマークを落とす手もあるが、
  それは `compounds.json` を触る話なので**別の判断**（§8-④）。**コード側で両方反転させれば、データを触らずに済む。**

### 1-4. `readRingParityFromHaworth` が読んでいるもの／反転で変わるもの

読んでいるのは3つだけ（`chemistry.js` 2141行〜）:

1. **環の隣2本** … 描かれた 2D 座標そのまま、`z=0` の面内ベクトル
2. **環外の置換基の面** … `haworthFace` が明示ならそれ、無ければ**縦位置**（±25°以内。上=+1／下=−1）
3. **暗黙のH** … 置換基の**反対の面**

反転（y 反転 ＋ 面マーク反転）を当てると、この3つは
`(x, y, z) → (x, −y, −z)` という**ただ1つの3D回転**でまとめて動く。
`_parityFromDirs` は行列式の符号なので、回転（行列式 +1）では**必ず**符号が保たれる。
**②が 16/16 になったのは偶然ではなく、この構造から出ている。**

⚠ **逆に言うと、面内180°回転（③）は「z を回さない」ので回転にならない。**
紙の上で図をくるっと回すのは 3D では回転でも、**ハース図の約束（上に描く＝手前）を通すと鏡像になる**。
ハース投影は**向きが固定された表記**であって、回して読み直せる図ではない。

### 1-5. 糖の登録の内訳（939件中）

| 種類 | 件数 | 中身 |
|---|---|---|
| 開鎖（フィッシャー投影で読む） | 4 | D-グルコース・D-ガラクトース・D-マンノース・D-フルクトース（すべて `stereo` 記述子つき） |
| 環（ハース投影で読む）・単糖 | **12** | α/β × グルコース・ガラクトース・マンノース・アロース・グロース（10）＋ α/β-D-フルクトフラノース（2） |
| 環・**二糖** | **4** | マルトース・セロビオース・ラクトース・スクロース |
| 開鎖・その他 | 1 | デオキシリボース（閉路数0） |

⚠ **発注書は「二糖もライブラリに7件ある」と書いているが、実測は 4件**（`C12H22O11` は4件のみ）。
`glycosidicLinkages` が当たるのもこの **4件だけ**（939件全数で確認）。**イソマルトース・トレハロースは登録が無い。**

---

## 2. ★ の問いへの答え

> **いまのモデルは「同じ分子のまま形が変わる」を表せるか。**
> （統合レーンの読み替え: フリップは反応ではなく**見せ方**なので、「見せ方の操作として持てるか」）

### 2-1. 答え: **持てる。しかも既存の作りが味方している**

1. **反転はトポロジーを1つも変えない**（`canonicalCode` 16/16 同一）。
   ＝ `CLAUDE.md` の「検証はトポロジーのみ」に**まったく触れない**。
2. **正しい反転なら立体も変わらない**（`canonicalStereoCode` 16/16 同一）。
   ＝ **名前も変わらない。α/β も D/L も動かない。**「同じ分子である」がアプリの言葉で言い切れる。
3. **環ビューの模型はもともと「描いた図を 3D に持ち上げたもの」**（`buildRingModel`。環原子は z=0、
   置換基は `face * depth`）なので、**反転は模型に対する回転1つ**で表せる。新しい属性は要らない。

⚠ **したがって「新しい属性を足す」必要は無い。** 足すのは**操作（見せ方）**だけ。

### 2-2. ただし、いまはその操作に**手が届かない**

| 塞がっているもの | 実測 |
|---|---|
| **カメラの倒し角が 0〜90° に固定** | `stereo.js`: `this.ringTilt = Math.max(0, Math.min(Math.PI / 2, this.ringTilt + dTilt));` ／ `index.html`: `<input id="stereo-ring-tilt" min="0" max="90" ...>` |
| **二糖は環ビュー自体に入れない** | `buildRingModel` が閉路数2で断る（§3-1） |

⚠ **裏返しは「倒し角を 180° まで回せるようにする」だけで得られる。**
`rotateZX(v, angleZ, angleX)` の `angleX = π` は `(x, y, z) → (x, −y, −z)` ＝ §1-2 の②そのもの。
**式は既にある。上限を 90 から 180 に開けるだけ。**

### 2-3. 「反応として見せるのか」への答え

⚠ **反応としては見せない。**

- 反転の前後で `canonicalCode` も `canonicalStereoCode` も同じ ＝ **アプリにとって前後は同じ分子**。
  反応実行モードは「分子グラフを書き換えて別の分子にする」器なので、**入れると嘘になる**
  （Undo の履歴に「何も変わらない1手」が積まれ、名称判定カードも動かない）。
- 巻矢印の機構ビューア（`reaction.js`）も、**結合の組み替えを見せる器**なので同じ理由で合わない。
- **正しい置き場は「見せ方（ビュー）の操作」** ＝ 環ビューのカメラとモデル。ユーザーの2026-08-21 の追記と一致する。

---

## 3. (2)＋(3) 二糖を横から見る／環を裏返す —— **同じ面の、同じ操作**

### 3-1. いまの「⬡ 環を横から」の実測

本物の `StereoView.buildRingModel` を node で回した結果:

| 分子 | 結果 |
|---|---|
| α-D-グルコース | ○ 模型を作れた（環6・置換基5・H5 ＝ 節点16／面が読めなかった置換基 **0本**） |
| β-D-フルクトフラノース | ○ 模型を作れた（環5・置換基5・H3 ＝ 節点13／読めなかった **0本**） |
| **マルトース／セロビオース／ラクトース／スクロース** | **× 4件とも断られる** |

断り文（**そのまま画面に出る**）:

> この分子は環が2つ以上つながっているため「⬡ 環を横から」は使えません（このビューは環1つを平面とみなす模型なので、**縮合環**では上下の意味が決まりません）。

⚠ **文面が事実と合っていない。** 二糖は縮合環（デカリンのような環の共有）ではなく、
**-O- 1本でつないだ独立な2つの環**である。判定は「連結成分の閉路数 ≠ 1」なので二糖も一緒に落ちている。
**ユーザーが「二糖でも見たい」と言った直接の原因はこの1行。**

### 3-2. ★ 橋の酸素の面は、両側の環から見て一致するか（**(2) の芯**）

「2つのハース環が同一平面」で模型を組むと、**橋渡しの酸素 O は2つの環の置換基を兼ねる**ので、
**z をただ1つに決めなければならない**。ところが面は「その環の炭素から見た縦位置」で決まるので、
**両側から別々に読める**。実測（`StereoView.faceOfSubstituent` を両側から呼んだ）:

| 二糖 | 橋の O | 環A側から | 環B側から | 判定 |
|---|---|---|---|---|
| **マルトース**（α-1,4） | (500,414) | 縦から20° → **−1** | 縦から20° → **−1** | **一致 ＝ そのまま同一平面に置ける** |
| **セロビオース**（β-1,4） | (500,262) | 縦から0° → **+1** | 縦から0° → **−1** | ★ **食い違う** |
| **ラクトース**（β-1,4） | (500,262) | 縦から0° → **+1** | 縦から0° → **−1** | ★ **食い違う** |
| **スクロース**（α1↔β2） | (600,276) | 縦から**30°** → **0（読めない）** | 縦から0° → **−1** | 片方が読めない（§12.1 の ±25° を外れる） |

（環から出る置換基は4件とも 8本で、**橋の O 以外に読めないものは0本**。）

**ここが (2) と (3) が1つになる場所**:

- **食い違いは「片方の環が裏返っていない」ことの現れ**。
  ⚠ **片方の環を反転させると、その環の置換基の面がすべて反転するので、橋の O の面も反転して一致する。**
- **一致に必要な反転の回数は、リンケージの型がそのまま決めている**:
  - **α-1,4（マルトース）… 反転0回**。実測どおり最初から一致する
  - **β-1,4（セロビオース・ラクトース）… 反転1回**。教科書のセルロースの図で
    1つおきに環がひっくり返っているのは、まさにこれ
  - **α1↔β2（スクロース）… 反転1回**。**ユーザーの言う「スクロース中ではフルクトースが逆向き」がこれ**
- ⚠ **つまり「同一平面で見せる」を実装すると、フリップは避けて通れない。**
  逆に、**フリップを実装すると同一平面が手に入る。****(2) と (3) は別項目ではなく1つの機能。**

### 3-3. いまアプリが描いている二糖は、教科書と向きが違う

環の番号順に符号付き面積を取って、たどる向きを全数で測った（画面座標・正 ＝ 見た目の時計回り）:

- **登録されている環12件（単糖10＋フラノース2）も、二糖4件の各環も、すべて「時計回り」**（ピラノース +13680／フラノース +7710）。
- ＝ **アプリは全部の環を1つの向きで描き、二糖では相手の環を「上下にずらす」ことで逃げている**
  （セロビオース／ラクトースは環Bが 114px 高い位置にある）。**教科書は代わりに環を裏返す。**
- ⚠ **どちらも化学は同じ**（§1-2 の② が 16/16）。**違うのは絵の約束だけ。**
  ただし**ユーザーが見たいのは教科書側の絵**なので、そこを見せる道具が要る。

### 3-4. 設計

#### R-1. カメラの倒し角を 0〜180° に開ける（**最小・単独で意味がある**）

- `ringTilt` の上限を `Math.PI / 2` → `Math.PI`、スライダーの `max="90"` → `max="180"`。
- 90° を越えると**環の向こう側が見えてくる**ので、180° で「裏返したハース図」になる。
- **プリセットに「⟲ 裏返す（180°）」を1つ足す**（既存の `setRingPreset('haworth'|'side')` と同じ形）。
- ⚠ **単糖1件でも意味がある** ——「上下を入れ替えただけの図は別の分子だが、裏返した図は同じ分子」を
  **その場で見せられる**。α-D-グルコースを裏返しても名前は α-D-グルコースのまま（§1-2 ②）。
- **害**: 倒し角が 90° を越えると**手前/奥が入れ替わる**ので、既存の「手前＝実線／奥＝破線」の
  描き分けが逆転して見える。⚠ **画家のアルゴリズムは z でソートしているので絵は正しく出る**が、
  **凡例の文言（「上＝手前」）は倒し角で書き換える必要がある**（§3-5）。

#### R-2. 二糖を受け入れる（環を2つ持つ模型）

`buildRingModel` の門番を「閉路数 ≠ 1 なら断る」から、**3つに割る**:

| 形 | いまの扱い | 提案 |
|---|---|---|
| 閉路数1 | ○ | 変えない |
| 閉路数2で、**2つの環が原子を共有しない**（＝橋でつながっている） | × 縮合環として断る | **○ 受け入れる**（二糖） |
| 閉路数2で、**原子を共有する**（縮合環・スピロ） | × | **× のまま**（文面もこのときだけ出す ＝ いまの文面が正しくなる） |

模型の組み方は既存のまま**環ごとに**回し、最後に**片方だけ反転**して橋の面をそろえる:

1. 環A（アノマー炭素を持つ側。左／上にある方）を、いまと同じに z=0 平面へ置く
2. 環Bも、**描かれた 2D 座標のまま** z=0 平面へ置く（＝「同一平面」の仮定はここ）
3. 橋の O の面を**両側から読む**。食い違う（または片方が読めない）なら、
   **環Bのノードだけに `(x, y, z) → (x, −y, −z)`（環Bの中心まわり）を当てる**
4. 橋の O は**環A側の面**を採る（読めなければ環B側）。**H は既存の規約どおり反対の面**

⚠ **3 は §1-2 の②とまったく同じ変換**なので、**立体は1つも動かない**（テストで見張る・§3-6）。
⚠ **キャンバスの座標は1ピクセルも書き換えない。**模型の中だけの操作 ＝ 「座標は見た目専用」を守る。

#### R-3. 裏返しを**操作として見せる**（(3) の本体）

- 二糖を開いたとき、**「⟲ フルクトース側を裏返す」**（相手の環の名前は `substituentLabel` 相当で作る）
  というボタンを1つ置き、**押すと環Bだけがアニメーションで 180° 回る**。
- 既定は **R-2 の 3 が決めた向き**（＝橋の面がそろった、教科書の向き）。
  押すと**アプリの `compounds.json` が持っている向き**（両方時計回り）に戻る。**行き来できる。**
- ⚠ **どちらの向きでも、画面に出る名前は同じ**（スクロース）。**それを画面に書く**のが教材としての勘所。
  文面案は §3-5。

#### R-4. 「⬡ 環を横から」のタブを二糖でも有効にする

- タブの無効化は `_ringModel` の有無で決まっているので、R-2 が通れば自動的に有効になる。
- 断り文 `RING_FUSED_REASON` は**縮合環のときだけ**出るようになるので、
  ⚠ **文面から「環が2つ以上つながっているため」を外し、「環が原子を共有しているため」に直す**。

### 3-5. 画面に書く文（**仮定であることを言う**・`qa/KNOWLEDGE_CAVEATS.md` の型）

既存の環ビューにも平面近似の注記がある（いす形は表せない）。**二糖ではそこに2行足す。**

> **この図は「2つの環が同じ平面にある」と決めて描いています。**
> 実際にはグリコシド結合 -O- のまわりが回るので、2つの環の相対的な向きは決まっていません
> （結晶や酵素の中では特定の向きに固定されますが、水に溶けているときは向きが移り変わります）。
> **上下（α/β）はどの向きでも変わらないので、この図で読める α/β は正しいままです。**

裏返しのボタンを押したときに出す文（**(3) の教材としての芯**）:

> **環を裏返しても、分子は同じままです。**
> 裏返すと「置換基の上下」と「炭素番号をたどる向き」が**同時に**入れ替わります。
> この2つはセットなので、分子は変わりません（アプリが出す名前も変わりません）。
> ⚠ **片方だけ —— たとえば上下だけを入れ替える —— と、別の分子（鏡像体）になってしまいます。**
> スクロースの図でフルクトースの環が「ふつうと逆向き（反時計回り）」に描かれるのは、この裏返しのためです。

倒し角が 90° を越えたときの凡例（R-1）:

> いま**裏側から**見ています（倒し角 NNN°）。**手前と奥が入れ替わって見えます。**

### 3-6. 検収（回帰テストの核）—— **「同じ分子であることが変わらない」を数で示す**

⚠ **§1-2 の表をそのままテストにする。** 環をもつ糖 **16件で全数**、`tests.js` に1本:

| テスト | 主張 | いまの実測 |
|---|---|---|
| **SG1** | ★ 正しい反転（y 反転＋`haworthFace` 反転）で `canonicalStereoCode` が変わらない | **16/16** |
| **SG2** | 上下だけ入れ替える（面内180°回転）と**必ず**変わる | **0/16 が同一** ＝ 16件とも変わる |
| **SG3** | 向きだけ逆にする（左右の鏡映）と**必ず**変わる | **0/16 が同一** |
| **SG4** | 面マークを直し忘れると、マークを持つ8件だけが変わる | **8/16 が同一** |
| **SG5** | どの操作でも `canonicalCode`（トポロジー）は変わらない | **16/16 × 4操作** |
| **SG6** | 二糖4件で `buildRingModel` が模型を返し、**橋の O の面が両側で一致する** | いまは 4件とも模型が返らない（R-2 で 4/4 に） |
| **SG7** | 二糖4件を裏返しても `lookupCompoundName` が同じ名前を返す | —— |

⚠ **SG2・SG3 は「変わること」を主張するテスト。** 落とすと、実装が
「上下だけ入れ替える」に退化しても誰も気づかない。**SG1 だけでは守れない**（何もしない実装でも通る）。

---

## 4. (1) 単糖 ⇄ 二糖

### 4-1. 切る側（実装済み）の実測

`hydrolysis_glycoside`（`reactor.js` 3244行〜。試薬は `h2so4_dil`）。
`glycosidicLinkages` が当たるのは **939件中4件**、それぞれ**切れる箇所は1つ**:

| 二糖 | 切る場所 | 生成物（`lookupCompoundName` 相当で引いた名前） |
|---|---|---|
| マルトース | 非還元側グルコースの C1 | ⚠ **名無し**（「同じ骨格の候補11件あるが立体が一致しない」）／α-D-グルコース |
| セロビオース | 同上 | ⚠ **名無し**／β-D-グルコース |
| ラクトース | 同上 | ⚠ **名無し**／β-D-グルコース |
| スクロース | フルクトースの C2 | α-D-グルコース／β-D-フルクトフラノース（**2つとも名乗れる**） |

### 4-2. ⚠ 切る側にすでに穴がある（**つなぐ側より先に直す価値がある**）

**3/4 の二糖で、生成物の片方が名無しになる。** 原因は座標:

| 二糖 | 切ったアノマー炭素 | 生えた -OH の位置 | 縦からの角度 | 読めるか |
|---|---|---|---|---|
| マルトース | (458,300) | (543,300) | **90°** | ✗（許容は 25°） |
| セロビオース | (500,300) | (585,300) | **90°** | ✗ |
| ラクトース | (500,300) | (585,300) | **90°** | ✗ |
| スクロース | (540,382) | (606,382) | **90°** | ○（※） |

※ スクロースだけ読めるのは、フルクトースのアノマー炭素が**環外に2本**持っていて
（-CH₂OH と -OH）、`DESIGN_compound_coverage.md` §6-3 の「主置換基が読めなければ劣位側を反転して使う」
という**保険が効いている**から。アルドースの C1 は環外1本なので保険が無い。

**原因は `freeSpotAround`**（`reactor.js` 549行〜）。方向の候補が
`[右, 上, 下, 左]` の順で、右が空いていれば必ず右に置く。⚠ **ハース投影の約束を知らない。**
環パリティの読めた中心は **10 → 9** に減り、その1中心が α/β を失って名前が引けなくなる。

**直し方（小さい）**: `freeSpotAround` に「この原子が**環の sp3 炭素**なら、
**上下を先に試す**」という向きの優先を足す。⚠ **`freeSpotAround` 全体の順序は変えない**
（他の反応の見た目が動く）。**環の原子のときだけ** `[上, 下, 右, 左]` にする。

⚠ **化学としては、加水分解の直後は α と β の混合（変旋光）**なので、
「どちらか一方に決めて描く」こと自体が単純化である。**画面にそう書く**のが正直な形:

> 加水分解でできた単糖は、水の中で α形 と β形 が入れ替わっています（変旋光）。
> ここでは**片方の形だけ**を描いています。

### 4-2a. ★ 段4-a の実測（v1444・**ここが切る側の正**）

すべて `?summon=` で呼び出し → 「⚗ 反応させる・調べる」→ 加水分解の札、を**実画面で**押して測った
（`readStereo = true`）。

| 二糖 | 橋の -O- が出ていた側 | 直す前（-OH は真横） | **直したあと（-OH は縦）** |
|---|---|---|---|
| マルトース | 下 | **null** ＋ α-D-グルコース | **α-D-グルコース ＋ α-D-グルコース** |
| セロビオース | 上 | **null** ＋ β-D-グルコース | **β-D-グルコース ＋ β-D-グルコース** |
| ラクトース | 上 | **null** ＋ β-D-グルコース | **β-D-ガラクトース ＋ β-D-グルコース** |
| スクロース | 上 | α-D-グルコース ＋ β-D-フルクトフラノース | **同じ**（唯一もともと通っていた件） |

**置く向きの決め方は3案あって、8/8 が名乗るのは1つだけ**（全数実測）:

| 案 | マルトース | セロビオース | ラクトース | スクロース |
|---|---|---|---|---|
| **保存**（橋が出ていた側。★ 採用） | α＋α | β＋β | **β-ガラクトース**＋β | α＋β-フルクトフラノース |
| 常に上 | **β**＋α | β＋β | β-ガラクトース＋β | α＋β-フルクトフラノース |
| 常に下（＝アルドピラノースなら α） | α＋α | **α**＋β | **α-ガラクトース**＋β | ⚠ α＋**名無し** |

⚠ **「常に下」を採らなかったのは、スクロースのフルクトース側が名無しに落ちるから。**
α-D-フルクトフラノースの登録の図では C2 の -OH は下ではないので、下に置くと α にも β にも一致しない。
⚠ **「保存」は化学の言い方では「加水分解はアノマー炭素の立体を変えない」。**
実際には水中で変旋光が起きて α/β が混ざるので、**片方だけを描いていることを caption に書いた**（§8-⑥）。

⚠ **角度が読めるかどうかでは決めない。** スクロースの橋は縦から 29.5°（±25° の外）だが、
上下どちらの**側**かは符号だけで決まるので、読めない図からでも保存できる。

### 4-2b. ★ 「保存」を選んだことの利き目（ユーザー・2026-08-22）

**採ったあとで分かった**が、保存には**採点以外の値打ちが2つ**ある。**どちらも「常に α」では消える。**

**① デンプンとセルロースの分かれ目が、生成物の図に出る**

| 二糖 | 加水分解の生成物 | 縮合の側から見ると |
|---|---|---|
| マルトース | **α ＋ α** | α-1,4 ＝ **デンプン** |
| セロビオース | **β ＋ β** | β-1,4 ＝ **セルロース** |

⚠ **「常に α」だと両方 α＋α になり、「同じ単糖からできるのに、片方は消化できて片方はできない」
という肝心のところが図から読めなくなる。**

**② 反応機構のアニメーションに都合がよい**（ユーザー原文「これは反応機構をアニメーションしたときにも都合よいです」）

★ **-OH が橋の -O- と同じ側に生えるので、反応の前後で原子の対応が環の面をまたがない。**
⚠ **またぐと、アニメーションは酸素が環の裏側へ回り込む絵になり、
「起きていない立体の反転が起きたように見える」。**

⚠ **これは絵の好みの話ではなく、巻矢印を自動で引けるかどうかに直結する。**
[`video-scripts/ORDER_reaction_index_2026-08-20.md`](video-scripts/ORDER_reaction_index_2026-08-20.md) は
**二糖の加水分解を「機構を作る価値がある11件」（区分②）に挙げ**、§3-3 で
**「切れる結合 → できる結合の対応が取れれば巻矢印はそこから引ける見込み」**としている。
**その対応が安定するのは面を保存しているときだけ。**

### 4-9. 段4-b の下調べ（キャンバスで環を裏返す・v1444 実測）

⚠ **ここは「測っただけ」で、実装は1行もしていない。**

**(a) アプリの絵は、二糖も単糖も「番号順に時計回り」**（§3-3 の再確認。番号順 ＝ アノマー炭素 →…→ 環内酸素。
画面座標なので符号付き面積が**正 ＝ 時計回り**）:
マルトース／セロビオース／ラクトースは 6員環 +13680 が2つ、スクロースは 6員環 +13680 ＋ **5員環 +7710**。

⚠⚠ **したがって、加水分解の生成物は「もう教科書の向き」で出てくる。**
スクロースを切って出るフルクトース断片は **+7710（時計回り）** で、単独の β-D-フルクトフラノースの登録
（+7710）と**同じ向き**。⚠ **ユーザーの言う「反転」を見せるには、切ったあとではなく
「切る前のスクロースの絵」を教科書の向き（フルクトース環が反時計回り）にしないといけない。**
＝ **反転が要るのは生成物ではなく原料の側**（§10-2 と同じ食い違いの、実装から見た言い方）。

**(b) 環が1つの分子は、そのまま裏返してよい**（5件で実測。α/β-D-グルコース・α/β-D-フルクトフラノース・
β-D-ガラクトース）: `canFlipHaworth` は 5/5 で true、`flipHaworth` の**軸は既定でも環の重心でも
立体コードが 5/5 で同一**、名前も変わらない。裏返すと向きは 時計回り → 反時計回り になる。

**(c) ⚠ 二糖で「片方の環だけ」を裏返すと、立体が保たれる軸と、図が読める軸が食い違う**（4件×2環＝8通り）:

| 軸 | 立体コード同一 | 図 |
|---|---|---|
| **橋の原子の y** | **8/8** | ⚠ 2つの環が重なる（スクロースは**最短 5px**） |
| その環の重心 y | **1/8**（スクロースの5員環だけ偶然） | ○ 重ならない |
| 既定（集合の重心） | **0/8** | ○ 重ならない |

⚠ **`DESIGN_3d_correspondence.md` §7.3 の「絵は環の重心・分子は橋の y。両者は縦の平行移動ぶんしか
違わないので面と立体は同じ」は、キャンバスには当てはまらない。**
環ビューは**面を先に読んでから絵を折る**ので平行移動が効かないが、キャンバスは
**折った座標から面を読み直す**ので、平行移動ぶんだけ橋の結合の向きが変わって読みが変わる。

**(d) ただし、裏返したあとに縦へずらせば重なりは解ける**（刻み 76px 単位で実測）。
橋の結合の縦成分が**大きくなる向き**へずらす限り立体コードは保たれ、逆向きにずらすと符号が反転して壊れる:

| | k=0 | +1 | +2 | +3 | −1 | −2 | −3 |
|---|---|---|---|---|---|---|---|
| スクロース・5員環側 立体 | ○(5px) | ○(8px) | ○(10px) | **○(37px)** | ○(28px) | ○(28px) | ○(38px) |
| スクロース・6員環側 立体 | ○(5px) | ○(8px) | ○(10px) | **○(37px)** | × | × | × |

＝ **「橋の y で裏返す → 相手から離れる向きへ縦にずらす → 立体コードが変わっていないことを確かめる」**
の3手で、キャンバスの部分反転は成立する。⚠ **確かめる手を省かないこと**（軸を1つ間違えると 0/8 で鏡像になる）。

### 4-9b. ⚠ v1447 の訂正 —— フリップの置き場所は「押すボタン」ではなく「加水分解」だった

**ユーザー（2026-08-22）**:

> 「**五員環を裏返すボタンはいりません。加水分解時にフリップしろというオーダーです**」
> 「**分子全体をフリップするならともかく、2糖のうち一部を反転させる意味はありません**」
> 「**フリップするのは加水分解前後の分子の形に対応するためです**」
> 「**分子全体のフリップであれば、変形させた図形への対応という意味があります**」

★ **原則**（これを書いておかないと、次の人が「便利そうだから」と作り直す）:

> **二糖の「片方の環だけ」を裏返すことは、グリコシド結合を切らないとできない。
> そして切る操作こそが加水分解である。** ＝ つながったままの二糖に「片方だけ裏返す」ボタンを
> 置くのは、**起きえないことを押させている**。

⚠ **「フリップ全部が無意味」ではない。2行をセットで読むこと**:

| | 意味 | v1447 の扱い |
|---|---|---|
| **分子まるごとのフリップ** | **意味がある。**「同じ分子を違う向きで描いた図」を見抜く力そのもの ＝ **入試の応用問題の型**（例: マルトースを上下反転した図を並べて、どれが正しい図かを選ばせる） | ★ **土台を残す**（`canFlipHaworth` / `flipHaworth` / `haworthCanvasFlip`・環ビューの `⟳ 裏返す` と独楽回転） |
| **二糖の一部（片方の環）だけの反転** | ⚠ **結合を切らないと起こせない** | **人が押す口を廃止**（分子モーダルの `#mm-haworth` と環ビューの `⇅ もう一方の環を裏返す`）。切るとき ＝ 加水分解のときだけ内部で起こす |

⚠ **画面に内部の言葉を出さない**: 環ビューのボタンは「`⇅ 五員環を登録の向きへ`」と出ていたが、
「登録の向き」は `compounds.json` の話 ＝ **利用者に無関係な言葉**（同「まず言葉の意味が
ユーザーに対して伝わりません」）。⚠ **同一平面のための内部の反転そのものは残す**
——これが無いと二糖4件のうち3件が環ビューに入らない（v1442 の芯）。

**自動反転の正は `DESIGN_3d_correspondence.md` §8.0・§8.5**（目的・実測表・約束）。

### 4-9c. ⚠ v1450 の訂正 —— 向きを直すだけでは「教科書の図」にならなかった

**ユーザー（2026-08-24・検収条件）**:

> **スクロースの加水分解は、反応前後の分子の表示が、どちらも教科書の図になるようにしてください。**

⚠ **v1447 の自動フリップ（たどる向きだけを合わせる）では、この条件を満たせなかった。**
向きは合っても**環の O の位置がそろわない**ので、ユーザーには「上下逆に見える」（v1449 の報告）。

★ **v1450 は乙案（§1-2b 帰結2）＝「単独で描くときの図をまるごと写す」**。
`game.restoreHaworthOrientation` / `haworthRegisteredRingSense` は
`standaloneDrawingOf` / `redrawProductsAsStandalone` に置き換えて**外した**（参照は全部たどって確認）。

**実測（二糖4件 × 2断片 ＝ 8/8）**:

| 二糖 | 生成物 | 直す前のずれ（平行移動を除く） | **直したあと** | 重心の移動 |
|---|---|---|---|---|
| マルトース | α-D-グルコース ×2 | 42.97px / 79.60px | **0 / 5.7e-14** | 0 / 0 |
| セロビオース | β-D-グルコース ×2 | 42.97px / 79.60px | **0 / 8.5e-14** | 0 / 0 |
| ラクトース | β-D-ガラクトース ＋ β-D-グルコース | 42.97px / 79.60px | **0 / 8.5e-14** | 0 / 0 |
| スクロース | α-D-グルコース ＋ β-D-フルクトフラノース | 42.97px / 175.48px | **0 / 5.7e-14** | 0 / 0 |

- ⚠ **「立体コードが不変」は検収条件ではない**（裏返しても立体は変わらない ＝ §1-2 の②）。
  **座標で見る**のが正しい物差しで、SG18 をそう書き換えた
- ⚠ **重心の移動が 0** ＝ **位置は保っている**（単独の図の座標へ吸い寄せていない。
  吸い寄せると単独の図はどれも重心 (391,297)〜(406,311) なので**2つの生成物が重なる**）
- ⚠ **逃がし（`separateComponent`）は8件とも動いていない**（重ならなかった）。
  動く図が出たら SG18 の重心検査が赤くなる ＝ そのとき測り直すこと

### 4-3. つなぐ側で決まらないこと（**本題**）

**構成として作れてしまう二糖を全数で数えた**（登録単糖のアノマー炭素 × 相手の -OH のすべての組み合わせ。
トポロジーのみ・立体は数えていない）:

| 材料 | 骨格として相異なる二糖 | **ライブラリに名前があるもの** |
|---|---|---|
| グルコース2分子（α/β の4通り × つなぎ先） | **5通り**（1→1・1→2・1→3・1→4・1→6） | **1**（マルトース。※セロビオースは骨格が同じで立体だけ違う） |
| 登録単糖 **12件のすべての組み合わせ**（144通り） | **19通り** | **2**（マルトース・スクロース） |

立体まで数えると、グルコース2分子だけで
**つなぎ先5通り × 供与側の α/β = 10通り**（1→1 は両方のアノマーが効くので実際は11通り）。
**そのうち名前があるのはマルトース（α-1,4）とセロビオース（β-1,4）の2つだけ。**

⚠ **つまり「自由につながせる」と、9割が名無しの分子になる。**
これは (1) の本当の難所であって、α/β の選ばせ方ではない。

### 4-4. ⚠ **α か β かは、選ばせる必要が無い** ——**もう描かれている**

発注書は「α か β か・1→4 か 1→6 か をどう決めるのか」と問うているが、実測すると**半分は既に決まっている**:

- **α/β は、ユーザーが呼び出した単糖の図がすでに持っている。**
  α-D-グルコースを呼べば C1 の -OH は下、β-D-グルコースを呼べば上。
  グリコシド結合はその酸素をそのまま使う（切る側が「酸素は相手に残る」でやっているのと逆向き）ので、
  **供与側の α/β は生成物にそのまま乗る。**
- ⚠ **選ばせると、かえって嘘になる。** 「α-D-グルコースを描いたのに β-1,4 を選べる」のは化学的に誤り。
  **図がすでに答えているものを訊いてはいけない**（`lookupCompoundName` が「立体はトグルだけで決める」に
  したのと同じ筋。`game.js` 3754行〜のコメント）。
- **決まっていないのは「相手のどの -OH につなぐか」だけ** ＝ **4〜5通りの部位選択**。
  これは reactor の既存の作り（`detect` が**部位の配列**を返し、部位ごとにカードが出る）**そのまま**である。

### 4-5. §11（濃硫酸の温度）の「条件を選ばせる」仕組みは使えるか

**使えるが、使う場所が違う。**

| 仕組み | 何を割るもの | 糖に当てはまるか |
|---|---|---|
| `condition: {key, label, needs}`（v1424） | **同じ試薬瓶の中で、ルールが2本以上あるとき**にルールを選ばせる | ⚠ **α/β を割るのには使えない**（§4-4。ルールは1本でよい） |
| `detect` が返す**部位の配列** | 同じルールの中の**場所**を選ばせる | ○ **「相手のどの -OH か」はこれ** |

⚠ **ただし `condition` が効く場所が1つある: 酵素と酸の区別。**
`hydrolysis_glycoside` の caption はすでに
「希硫酸のかわりに酵素（マルターゼ・ラクターゼ・インベルターゼ）でも同じ反応が進みます」と書いている。
**酵素を瓶にして、`condition` で「マルターゼ／ラクターゼ／インベルターゼ」を選ばせる**と、
**「その酵素はその二糖にしか効かない」という高校で必ず問われる知識が、選択の失敗として体験できる**
（`needs` が「インベルターゼが切るのはスクロースです」と言う）。
⚠ **これは (1) の本体ではない。§7 の「後回し」に置く。**

### 4-6. ⚠ つなぐ側の**本当の難所は配置**（既存の門番に阻まれる）

2分子をつなぐ既存の反応（エステル化・分子間脱水）は `planAttachment` で相手を動かす。
その中の `canSpin90`（`reactor.js` 118行〜）は:

```js
if (atoms.some(a => a.haworthFace || a.isAsymmetricMarked || a.benzeneCenter)) return false;
if (bonds.length >= atoms.length) return false; // 環を含む
return !atoms.some(a => a.element === 'C' && mol.isAsymmetricCarbon(a.id));
```

⚠ **糖は3つの条件すべてに引っかかる**（環がある・不斉炭素だらけ・8件は面マーク持ち）。
＝ **糖は平行移動しかできない。** 回転も鏡映も禁止されている。理由は正しい
（「90°回すとハース投影の読みの約束が崩れる」＝ §1-4 と同じ話）。

**ここに (3) が効く**:

> ⚠ **反転（y 反転＋面マーク反転）は、`canSpin90` が禁じている操作の中で唯一「読みの約束を壊さない」もの。**
> 90°回転はハース図を縦にしてしまうので禁止でよいが、**反転は裏返したハース図＝やはりハース図**である。
> **§1-2 の② が 16/16 なので、`canSpin90` と並んで `canFlipHaworth(mol, ids)` を建てられる。**

**したがって、つなぐ側の配置は「平行移動 ＋ 必要なら反転」で足りる**（§3-2 の表がそのまま設計）:

- α-1,4（マルトース）… 反転なしで置ける
- β-1,4（セロビオース）・α1↔β2（スクロース）… **相手を1回反転させてから置く**

⚠ **これは (3) を実装しないと書けない。****(3) は (1) の前提である。**

### 4-7. 「切る側」と「つなぐ側」は逆反応として噛み合うか

⚠ **いまのままでは噛み合わない。** 実測（§4-2）で、切ると **3/4 の二糖で片方の生成物が α/β を失う**。
α/β を失った単糖をつなぎ直すと、**元の二糖とは別の立体コードになる**（あるいはやはり名無しになる）。

**噛み合わせるための条件は2つ**:

1. **§4-2 の `freeSpotAround` の向き**を直す（切った側の α/β が残る）
2. **つなぐ側が、切る側の逆の座標操作をする**（酸素は供与側に残し、相手を反転して寄せる）

⚠ **「切ってからつなぎ直すと元に戻る」を回帰テストにできる**（SG8）。
**4件すべてで `canonicalStereoCode` が一致すること**を主張する。**これが (1) の検収の核。**

### 4-8. 設計（つなぐ側）

- **ルール1本**: `condensation_glycoside`（試薬は `h2so4_conc`？ ⚠ 実際の生合成は酵素なので、
  **試薬瓶をどれにするかは §8-③ でユーザー判断**）。
- **`detect`**: 「別分子の、環のアノマー炭素の -OH」×「相手の環の -OH」の組を返す。
  ⚠ **`componentOf` で別分子どうしに限る**（分子間脱水と同じ粒度）。
  ⚠ **`detect` は環1つ分の近所だけを見る**（`sugarRingOf` の流儀を踏襲。全体を数えない）。
- **`apply`**: 供与側の -OH の O を橋にし、相手の -OH の O を落として `parkAsWater`。
  配置は **平行移動 ＋ `canFlipHaworth` が許すときの反転**。
- **生成物の名前**: ⚠ **登録があるのは4件だけ**なので、無いものは名無しになる。
  **黙って名無しにしない**（v1434 の「黙って減らさない」と同じ流儀）:

  > つないだ二糖は **α-1,6 結合**です。この形は名前のライブラリに入っていないので名前は出ませんが、
  > **イソマルトース**（デンプンの枝分かれの部分）に当たります。

  ⚠ **この文を出すには「1→何番か」をアプリが知る必要がある**。炭素番号は
  `iupacNameDetail` の系統では出せない（糖は環＋多価アルコールなので対象外）。
  **環の中でアノマー炭素から数えた位置**を数えるだけの小さな関数が要る（§8-②）。

---

## 5. (3) の化学 —— 読みと出典

### 5-1. 読みは1つに定まった（ユーザー確定・2026-08-21）

> **環の反転** ＝ **ハース環の平面を裏返す操作**。
> フルクトースのフラノース環は、単独で描くとき炭素番号が**時計回り**だが、
> **スクロースの中では逆向き（反時計回り）**になる。

⚠ **アノマーの反転（α⇄β）でも、いす形の配座反転でもない。**
発注書が挙げていた「遊離したフルクトースがフラノース形からピラノース形へ移る平衡」も**別の話**で、
今回のスコープには入らない。

### 5-2. 裏が取れたこと

- **スクロースの構造**: β-D-フルクトフラノシル-(2→1)-α-D-グルコピラノシド。
  グルコースの C1 とフルクトースの C2 —— **両方のアノマー炭素どうし**が結ばれているため非還元糖。
  （[Wikipedia: Sucrose](https://en.wikipedia.org/wiki/Sucrose)）
- **ハース投影の向きの約束**: 環の酸素を**右奥**に置き、**アノマー炭素を右端**に置いて、
  **炭素番号は時計回り**にたどる。
  （[Master Organic Chemistry: The Haworth Projection](https://www.masterorganicchemistry.com/2018/01/11/haworth-projections/)）
- **スクロースの図では、グルコース側が時計回り・フルクトース側が反時計回りに番号がふられる。**
  （[Haworth projection for sucrose（図の説明）](https://www.researchgate.net/figure/Haworth-projection-for-sucrose-alias-a-D-glucopyranosyl-12-b-D-fructofuranoside-The_fig5_338036179)）
  ＝ **ユーザーの言うとおり、スクロース中のフルクトースは裏返して描かれている。**
- **「上下」は分子の性質であって図の性質ではない**: いす形の反転（配座変化）では
  「上の基は上のまま・下の基は下のまま」——五員環・六員環は結合を切らずに裏返れないので、
  立体の関係は図をどう構えても動かない。
  （同上 Master Organic Chemistry）
  ⚠ **これは §1-2 の② が 16/16 になったことの化学側の言い方である。**

### 5-3. ⚠ アプリの現状は「教科書と違う絵」だった（**実測**）

- アプリの登録は **16件の環すべてが時計回り**（§3-3）。**スクロースのフルクトースも時計回り。**
  ＝ **アプリのスクロースは、教科書の絵とは向きが違う。**
- ⚠ **化学は正しい。** アプリのスクロースを加水分解すると
  **α-D-グルコース と β-D-フルクトフラノース** が出る（§4-1）。読み取りは合っている。
- **違うのは絵の約束だけ**（§1-2 の② が 16/16 なので、両方の絵は同じ分子）。
- ⚠ **ただし橋の酸素が縦から 30° ずれていて、フルクトース側からは面が読めない**（§3-2）。
  §6-3 の保険で救われているが、**裏返した向きで描けばこの無理は消える。**

### 5-4. トレハロースについて

⚠ **トレハロースは `compounds.json` に登録が無い**（実測。二糖の登録は4件のみ）。
α,α-1,1 グリコシド（両方のアノマー炭素どうし）なので、教科書の図では
**片方の環を裏返したうえに、上下にずらして**描かれる。**(3) の題材としてはスクロースより難しい**
（1,1 結合は環と環が向かい合うので、同一平面の仮定がいちばん苦しい）。
**まずスクロースで作り、トレハロースは登録を足す判断とセットで後から**（§8-①）。

---

## 6. 着手の順番（**何が何の前提か**）

```
  段0 …… SG1〜SG5（反転の不変量のテスト）を先に書く      ← 実装ゼロで書ける
    │        ⚠ テストファースト。§1-2 の表がそのまま入る
    ▼
  段1 …… R-1 カメラの倒し角を 0〜180° に開ける            ← 単糖だけで完結・単独で価値がある
    │        （+ 90°超の凡例の文言）
    ▼
  段2 …… R-2 二糖を環ビューに入れる（環2つの模型・橋の面をそろえる）
    │        ⚠ ここで初めて「片方だけ裏返す」が必要になる
    ▼
  段3 …… R-3 裏返しをボタンにする／R-4 断り文を直す        ← (2) と (3) がここで着地
    │        （SG6・SG7）
    ▼
  段4 …… §4-2 の freeSpotAround の向き（切る側の穴）      ← 単独で意味がある小さな修正
    │        ⚠ 段5 の前提（切った結果が名乗れないと往復が測れない）
    ▼
  段5 …… §4-8 つなぐ側（canFlipHaworth を使った配置）      ← (1) が着地
             （SG8 ＝ 切ってつなぎ直すと元に戻る）
```

⚠ **前提の向きは (3) → (1)**（発注書の番号とは逆）。
理由は §4-6 ——「糖は回せない」という既存の門番を通り抜けられるのは反転だけで、
その反転の正しさは段0〜段3 で確立される。

⚠ **段1・段4 はそれぞれ単独で出せる**（1修正=1コミットが成立する）。
**段2・段3 は割れない**（環2つの模型と裏返しは同じ操作）。

---

## 7. やらないことを勧めるもの

| 項目 | 勧め | 理由 |
|---|---|---|
| **フリップを反応（`reactor.js` / `reaction.js`）に入れる** | ⚠ **やらない** | 前後で `canonicalCode` も `canonicalStereoCode` も同じ ＝ アプリにとって同じ分子。Undo に「何も変わらない1手」が積まれ、名称判定カードも動かない。**器が合っていない**（§2-3。ユーザーの2026-08-21 の追記と一致） |
| **「同じ分子のまま形が変わる」を表す新しい属性（`ringFlipped` など）を持つ** | ⚠ **やらない** | 反転は**回転**なので、模型の座標を回すだけで表せる（§2-1）。属性を持つと `serializeState`・Undo・正準コード・テストの全部に波及し、**同じ分子が2通りの表現を持つ**（同型判定の穴になる） |
| **α/β を選ばせる UI** | ⚠ **やらない** | 図がすでに答えている（§4-4）。訊くと「α-D-グルコースを描いたのに β を選べる」という誤りが作れてしまう |
| **つなぎ先を自由に選ばせて、名前が無いものは黙って名無しにする** | ⚠ **やらない** | 19通り作れて名前があるのは2つ（§4-3）。**何につないだかを言う**（§4-8 の文面案） |
| **イソマルトース・トレハロース・ゲンチオビオースをこのレーンで登録する** | ⚠ **やらない**（別判断） | `compounds.json` はこのレーンで触らない。**登録を足すかは §8-① のユーザー判断**。足さなくても §4-8 の文面で教育的には成立する |
| **「2つの環が同一平面」を仮定と書かずに出す** | ⚠ **やらない** | `qa/KNOWLEDGE_CAVEATS.md` の型。文面案は §3-5 |
| **面マーク8件を `compounds.json` から落として掃除する** | ⚠ **このレーンではやらない**（§8-④） | 冗長であることは実測したが（§1-3）、**コード側で面マークも反転させれば害は消える**。データを触る判断は別に立てる |

---

## 8. 決めてほしいこと（**推奨を明示**）

| # | 問い | **推奨** | 代案と害 |
|---|---|---|---|
| **①** | **トレハロース・イソマルトースを `compounds.json` に足すか** | ⚠ **足さない（当面）。** (3) の題材はスクロース1つで足りる（教科書の図と実測が一致している）。トレハロースは α,α-1,1 で同一平面の仮定がいちばん苦しく、**最初の題材に向かない** | 足す → 「トレハロースのフリップ」がそのまま見られる。害は、1,1 結合の図が**同一平面では苦しい**ため、いちばん難しい形を最初の見本にしてしまうこと |
| **②** | **つないだ結合が「1→何番か」をアプリが言うか** | ⚠ **言う。** 名前が無い生成物のとき、これが無いと「何を作ったか分からない」ままになる。環の中でアノマー炭素から数えるだけの小さな関数で足りる（IUPAC 命名系には触らない） | 言わない → §4-8 の文面が書けず、19通りのうち17通りが無言になる |
| **③** | **つなぐ側の試薬瓶を何にするか** | ⚠ **既存の `h2so4_conc`（濃硫酸）に相乗り。** 分子間脱水（エーテル）と同じ「2分子から水がとれる」形なので、瓶を増やさずに済む（試薬パレット §10.2 の「瓶を増やすと帯が伸びる」を踏む） | 酵素の瓶を新設 → 「インベルターゼはスクロースにしか効かない」が体験できるが、**瓶が22本目になる**。§4-5 のとおり**後回しを勧める** |
| **④** | **面マーク8件を掃除するか** | ⚠ **掃除しない。コードで両方反転させる。** データを触ると `compounds.json` の1行1件の規約と回帰テストに波及する。冗長なマークは残しても、反転が両方を回せば害は無い | 掃除する → 反転の実装が座標だけで済んで簡単になる。害は、**8件のデータ変更が回帰テストの立体コード期待値に触れる**こと |
| **⑤** | **カメラの倒し角の上限を 180° にするのは単糖でも有効か** | ⚠ **有効にする。** 単糖1件でも「裏返しても同じ分子」が見せられる（§3-4 R-1）。段1 として単独で出せる | 二糖のときだけ有効 → 「同じ操作なのに分子によって出たり出なかったりする」（v933 で撤回した門番と同じ型） |
| **⑥** | **加水分解の生成物を α/β のどちらで描くか** | ⚠ **供与側の α/β をそのまま残す**（§4-2 の `freeSpotAround` を直す）**＋ 変旋光の断りを画面に書く**。「正しい1つ」を選ぶのではなく「片方だけ描いている」と言う | 常に β で描く → 教科書の「加水分解でグルコースができる」に近いが、**切る前の α が消える**ので往復（§4-7）が成立しない |

---

## 9. 測り方（再現手順）

すべて `node` で `vm` に `chemistry.js` / `stereo.js` / `reactor.js` を読み込んで、
**実物の関数をそのまま呼んで**測った（`tools/dump-canonical.js` と同じ流儀）。
アプリのコードは1文字も変えていない。

| 節 | 測ったもの | 呼んだ実物の関数 |
|---|---|---|
| §1-2・§1-3 | 反転の不変量（16件 × 5操作） | `canonicalCode` / `canonicalStereoCode` / `readRingParityFromHaworth` / `readAtomParityFromFischer` / `readBondGeoFromCoords` |
| §3-1 | 環ビューが二糖を断ること | **`StereoView.buildRingModel`**（`vm.runInContext('StereoView', ctx)` で取り出してプロトタイプから呼ぶ） |
| §3-2 | 橋の O の面 | **`StereoView.faceOfSubstituent`** |
| §3-3 | 環をたどる向き | 番号順に並べた環原子の**符号付き面積**（画面座標なので正 ＝ 見た目の時計回り） |
| §4-1・§4-2 | 加水分解の生成物と座標 | **`REACTION_RULES` の `hydrolysis_glycoside` の `detect` / `apply` を実際に走らせる**（`game` は `{ userMolecule: mol }` のスタブ） |
| §4-1 | 生成物の名前 | `game.getCompoundLibrary` / `lookupCompoundName` の照合手順を node で再現（正準コードで引き、立体コードで絞る。`verifyMolecule` は非公開なので正準コード一致で代用） |
| §4-3 | 作れてしまう二糖の数 | 登録の `target` どうしを結合してから `canonicalCode` |

⚠ **`compounds.json` / `stages.json` / `reactions.json` は読むだけ**（`JSON.parse` はしたが書き戻していない）。

---

## 10. ⚠ ユーザーがまだ言葉にしていない食い違い

1. **「二糖はライブラリに7件」は 4件**（実測。`C12H22O11` は maltose / cellobiose / lactose / sucrose のみ）。
   **イソマルトース・トレハロースは登録が無い**ので、「トレハロースのフリップを見たい」は
   **登録を足すかの判断とセット**になる（§8-①）。
2. ⚠ **アプリのスクロースの絵は、教科書とフルクトース環の向きが逆。**
   ユーザーは「スクロース中では逆向きになる」と言ったが、**アプリの中では逆向きになっていない**
   （16件すべて時計回り・§3-3）。**「アプリの絵を教科書側に直す」のか、
   「アプリの絵はそのままで、裏返す操作を見せる」のか**は別の判断。
   ⚠ **この文書は後者を勧める**（データを触らずに済み、行き来できるので教材としても強い・§3-4 R-3）。
3. **「2つのハース環が同一平面」は、いまの図では 4件中1件（マルトース）でしか成り立っていない。**
   セロビオース・ラクトースは環Bが 114px 高い位置に描かれ、スクロースは橋が 30° 傾いている（§3-2）。
   ＝ ⚠ **「同一平面で見せる」は、いまの図をそのまま横から見ることではなく、片方の環を裏返す操作を含む。**
   **(2) は (3) 抜きには実装できない。**
4. **切る側にすでに穴がある**（3/4 の二糖で生成物の片方が名無し・§4-2）。
   ユーザーは「無いのはつなぐ側」と認識しているが、**切る側も完全ではない**。
   ⚠ **つなぐ側を足す前にここを直さないと、往復（§4-7）が成立しない。**

---

## 11. 段6 —— 二糖の登録図を「2つの環を真横に」（2026-08-22・v1446。**ここが正**）

**ユーザーの原文**:

> **2糖は必ず真横に環を並べる、呼び出しのスクロースの構造がいまだに上下配置です**

### 11-1. 直す前と直したあと（実測・`?summon=` で呼び出した図の環の中心）

| 二糖 | 直す前 横 / 縦 | **直したあと 横 / 縦** | 登録の図の番号のたどる向き |
|---|---|---|---|
| マルトース（α-1,4） | 284 / **0** | **触っていない** | 時計回り・時計回り |
| セロビオース（β-1,4） | 200 / 114（斜め） | **284 / 0** | 反時計回り・時計回り |
| ラクトース（β-1,4） | 200 / 114（斜め） | **284 / 0** | 反時計回り・時計回り |
| スクロース（α1↔β2） | 30 / **192**（上下配置） | **254 / 0** | 時計回り（六員）・**反時計回り（五員）** |

⚠ **立体は1つも動かしていない** —— 4件とも `canonicalCode` / `canonicalStereoCode` /
環の面の読み（`readRingParityFromHaworth`）が**直す前と1つも変わらない**（`DS1` が全数で見張る）。

### 11-2. ★ 分かったこと: **真横に並べるには、片方の環を裏返した向きで描くしかない**

理由はハース投影の読みの約束の側にある:

1. 環外の置換基は**縦（±25°以内）**に描かないと面（α/β）が読めない（`DESIGN_stereochemistry.md` §12.1）。
2. 2つの環の中心を同じ高さに置くと、**橋の -O- は「両方の host の上」か「両方の下」にしか置けない**
   （1つの原子だから）。
3. したがって:
   - **α-1,4（マルトース）… C1 も C4 も -O- が下** ＝ そのまま真横に置ける（**裏返し不要**）
   - **β-1,4（セロビオース・ラクトース）… C1 は上・C4 は下** ＝ **矛盾する**
   - **α1↔β2（スクロース）… 同じく矛盾する**
4. 矛盾する3件は、**片方の環を裏返して面の読みを反転させる**ことではじめて真横に置ける。
   ⚠ これは**教科書のセルロースの図で1つおきに環がひっくり返っているのと同じ理由**であり、
   ユーザーの「スクロース中ではフルクトースが逆向きになる」（§5-1）そのもの。

⚠ **§10-2 の分かれ道はここで決着した。**「アプリの絵はそのままで裏返す操作を見せる」ではなく
**「アプリの絵を教科書側に直す」**を採った（ユーザーの発注が後から来たため）。

### 11-3. 何を基準にそろえたか

**環を作る原子の重心（環の中心）の y。** 4件とも**ずれ 0**。

⚠ **スクロースは五員環と六員環で大きさが違う**ので、五員環の頂点の y を1px だけ動かして
（環の O を 337 → 244 相当の位置へ置き直す際に高さ 93 → 94 にした）**重心の y が整数になる**ようにした。
＝ 座標はすべて整数のまま、中心の y はぴったりそろう。実画面で見て、
グルコース（左・六員）とフルクトース（右・五員）が同じ高さに並び、橋の -O- が下でつながる
——**教科書の図（グルコース左・フルクトース右・α-グリコシドは下向き）と同じ構え**になった。

### 11-4. ★ 副産物: **スクロースの「30°問題」が消えた**

§5-3 が「裏返した向きで描けばこの無理は消える」と予告していたとおり:

| | v1445 まで | **v1446** |
|---|---|---|
| 橋への結合（フルクトース側から） | 縦から **29.5°**（±25° の外・直接は読めない） | 縦から **18.7°**（**直接読める**） |
| 環ビューの橋の面 | `bridge.derivedB`（§6-3 の保険で導出） | **両側から直接読める**（`derivedA/B` とも false） |
| 環ビューの模型の裏返し | セロビオース・ラクトース・スクロースで**1回必要** | **4件とも不要**（登録の図がもうそろっている） |

⚠ **§6-3 の保険そのものは消していない**（ユーザーが図を手で動かせば同じ状況が起きる）。
検査は `ST41` が**その場で図を作って**見張る形に移した ——
登録がたまたま保険を通っているかどうかに、規約の検査を預けない。

### 11-5. 波及（**期待値が座標に依存していた検査**）

| 帯 | 何が変わったか |
|---|---|
| `LB2` / `LB13` | 切る場所の座標（橋の -O- とその両端）を新しい図の値へ |
| `ST41` | 「スクロースの登録が §6-3 の保険を通っている」→ **その場で図を作って保険を試す**形へ |
| `SG8` | 「裏返しが要るのは3件」→ **4件とも不要**。かわりに**登録の図の向きの食い違い**で見る |
| `SG10` | 「模型がフルクトース環を裏返して教科書の向きを作る」→ **登録がもう教科書の向き** |
| `SG12` | 否定対照（-OH を真横に戻すと落ちる）が **3件 → 4件**（切れる側がグルコース C1 へ移ったため） |
| `SG14` | 「登録4件は**ずらさずに**裏返せる」を新設。ずらしの否定対照は **v1445 のスクロースの図**を作って使う |
| `SG15` | 「⇅ で教科書の向きへ」→「⇅ で**両方 時計回り**の図へ行き来」 |
| **`DS1`（新設）** | ★ 中心の y のずれ 0 ／ 横 200px 以上 ／ 面の読みが v1445 と同一 ／ 否定対照3本 |

### 11-6. まだやっていないこと

- **トレハロース（α,α-1,1）の登録**（段2）。⚠ **両方がアノマー炭素**なのでスクロースと同じ型
  ＝ 片方の環を裏返した向きでないと描けない。
- **イソマルトース（α-1,6）・ゲンチオビオース（β-1,6）・メリビオース（α-1,6）ほかの登録**（段3）。
