# 引き継ぎ: 作図の当たり判定（Opus → Fable・2026-08-12）

> **決定済み（2026-08-12・Fable）**: 設計判断は `DESIGN_hit_areas.md` に確定した。
> 実装レーンはそちらを正とすること（本書は経緯と実測の記録として残す）。

**渡す理由**: 方針書（CLAUDE.md「モデルの使い分け」）の Fable 起用条件のうち、
**「実ブラウザでしか再現しない、イベント順序・スナップ幾何が絡む類」**と
**「決めが後を引く設計判断」**の両方に当たるため。Opus では**手を付けていない**。

元の発注書は `video-scripts/ORDER_drawing_hit_areas.md`（実測つき）。**先にそれを読むこと。**

---

## 0. Fable に決めてほしいこと（これが本題）

発注書の**要望 B・C・D・E・F を、ばらばらの修正ではなく1つの設計判断として**決める。

| 要望 | 中身 | いまの値 |
|---|---|---|
| B | 吸着半径を結合長より明確に大きくする | `SNAP_RADIUS = 45` / `GRID_SIZE = 42` |
| C | ベンゼン置換基だけ距離が違うのを揃えるか、ゴーストで見せるか | 置換基 `42×0.666 = 27.97px`、他は 42px |
| D | 空いているところにはどこでも別分子を置けるように | いまは 70〜100px の帯でしか置けない |
| E | いちばん近い原子が勝つ（候補点込みの最短距離で遠い原子が勝つ） | 5方位中3方位が環に横取りされる |
| F | 「足す」と「消す」のあいだに緩衝帯 | 境目が連続していて事故になる |

**まとめて渡すのは、共通解がありそうだから**。要望C の「ゴーストで見せる」は
2d の的の狭さ（要望E・F）にも効く。個別に直すと辻褄が合わなくなる恐れがある。

---

## 1. ユーザーの判断（確定済み・覆さないこと）

- **要望C は Fable の判断に委ねる**（2026-08-12。「揃える／揃えない」を先に決めなかった）
- 大方針:
  > 操作が可能なことと、やりやすいことは別なので、判定がシビアなものがあれば調整する必要がある。
  > それぞれの操作に対応するエリアの面積が狭すぎたり、直感に反する位置にある場合は調整が必要
  > （2026-08-11）

---

## 2. 要望C を決めるときに効く制約（Opus が調べた事実）

**`compounds.json` の登録図は 27.97px で描かれている。**
手描きだけ 42px に揃えると、**同じ分子なのに「名前で呼び出した図」と「手で描いた図」の形が変わる**。

前例として、五員環（フラノース）モジュールを作ったときに
**「五員環の形は compounds.json の図と 1px も違わない」**ことをわざわざ確かめている
（DEVELOPMENT.md のハース環の項）。揃える側に倒すなら、**登録図の描き直しまで込みの判断**になる。

---

## 3. すでに入っているもの（この上に設計してよい）

### 3-1. 置けなかった理由は画面に出る（v1111・要望A・Opus 実装済み）

外した瞬間に理由が出るので、**幾何をいじった結果どこで外れているかが目で見える**。

- 遠すぎ／近すぎ／空きなし／キャンバス上限 を言い分ける
- 「取られた」は**押した点に赤い ×、取った原子に橙の ○、間に引き出し線**
- そのために「指の真下にいちばん近い原子」を別に持っているが、
  **吸着先の決定には一切影響させていない**（＝ 要望E を実装するときの土台がすでにある）
- うるさくしない手当て済み（同じ理由の15連打で字幕の書き換えは1回、理由が変わっても 800ms は守る）

### 3-2. 的の狭さの正体が判明している（v1111 の副産物）

**押した点が既存原子から 28px 未満だと `findAtomAt`（半径28）が先に拾って「削除」になる。**

```
配置に使える帯 = 28px（findAtomAt の半径）〜 45px（SNAP_RADIUS） ＝ わずか 17px
```

発注書 2d の実測「削 ≤26px ／ ○ 32〜42px ／ × ≥46px」はこれで説明できる。
**要望F（緩衝帯）と要望B（半径を広げる）は同じ1つの数直線の話。**

---

## 4. 触ってよい場所・いけない場所

| | |
|---|---|
| 本体 | `assembler/game.js` の `getSnappedCoords`（**289行**）とその周辺 |
| 参考 | `secondBranchPoints`（候補点。`getSnappedCoords` の 6. と条件をそろえてある旨のコメントあり） |
| **触らない** | v1111 で入った「理由を伝える」層（トースト・`#miss-group`）。**壊さない**こと |
| **触らない** | `assembler/compounds.json`（要望C で描き直す判断をしたときだけ、別コミットで） |

---

## 5. 壊してはいけない不変

- **回帰テスト 343件全合格**（`node tools/run-tests.mjs <url>/assembler/test.html`）
  - とくに **PW1〜PW5**（理由を伝える）・**ZD1〜2**（原子の移動ドラッグの落下先）・
    **FR1**（ハース環モジュール）・**MM9**（試薬パレットのあふれ）
- **`node tools/check-mobile.mjs assembler <url>` が 25端末で合格**
- `node tools/verify-release.js assembler` / `verify-compounds.js` / `verify-reactions.js`
- **座標変換は `getScreenCTM()` 必須**（viewBox 比の手計算は禁止。CLAUDE.md）
- **`compounds.json` は 1行1件・末尾に追記のみ**（v1120 で戻したばかり。`verify-compounds.js` が見張る）

---

## 6. 検証で必ず出してほしいもの

Opus のレーンに課しているのと同じ基準:

1. **前後の実測値**（発注書 2a〜2d と同じ測り方で。数字が改善したことを表で）
2. **否定対照** — 直しを外すと赤くなるテストを足す。「直したつもりで何も検査していない」が最悪
3. **夜間監査への申し送り**（発注書 §4）— `audit.js` の④に
   「**環 → 鎖 → その先端に枝**」を1件足すと 2d が毎晩機械で出る。
   判定は「足せる方位が5方位中いくつか」「成功する距離の帯の幅」でよい

---

## 7. 作業の作法（レーンで回す場合）

- 版番号は `assembler/index.html` / `test.html` / `audit.html` の**全 `?v=` と `<div class="version">` を同時に**
- **1修正=1コミット。パスを指定してコミット。`git add -A` / `commit -a` / `commit --amend` は禁止**
- **`git checkout --ours/--theirs` は禁止**（衝突は `git show :2:` / `:3:` で取り出して解く）
- 共有ツリー `C:/Users/maequ/マイドライブ/...` は別セッションが同時に作業中。**worktree で作業する**
- `tools/run-tests.mjs` は worktree では素で動かない（Playwright が `tools/record/node_modules` にある）。
  `NODE_PATH` で借りる。`--engine=webkit` も使える
