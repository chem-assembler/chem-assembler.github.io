# DESIGN_organic_calc.md —— 有機の計算問題を、どの枠で解かせるか

**状態: 測定と設計だけ。`ratio/` も `assembler/` も1バイトも変えていない。**
基準は `origin/main` = `f6677df`。測定日 2026-09-01。

`DESIGN_paid_workbook.md` §1「型 × 枠の棚卸し」で、
**知識 → `/qa/`（済）／構造 → assembler（済）／反応式 → `/ion-equation/`（済）** と決まったあと、
⚠ **「計算」だけが担当の決まっていない型として残った。** ★ 本書はその1型だけを扱う。

---

## 0. ★★ 最初に —— 申し送りの数は **全部「語の出現回数」だった**

⚠⚠ **統合セッションの内訳表（`32 / 56`・`11 / 13`・`13`・`25 / 134`）は、
`qa/questions.json` に対する `grep -o <語> | wc -l` の値です。**
★ **項目数ではありません。** ⚠ **同じ1項目の中で同じ語が最大18回出ます。**

| 申し送りの型 | ★ 申し送りの数 | ★★ その数の正体 | ⚠ **本当の項目数** |
|---|--:|---|--:|
| **元素分析・組成式** | 32 / 56 | ★ `元素分析` の語 32回 ／ `組成式` の語 56回 | ★ **元素分析 13項目 ／ 組成式 7項目**（和 **14項目**） |
| **けん化価・ヨウ素価** | 11 / 13 | ★ `けん化価` 11回 ／ `ヨウ素価` 13回 | ★ **けん化価 2項目 ／ ヨウ素価 4項目**（和 **4項目**） |
| **等電点** | 13 | ★ `等電点` の語 13回 | ★ **3項目** |
| ⚠ **重合度・分子量** | 25 / 134 | ★ `重合度` 25回 ／ `分子量` 134回 | ★ **重合度 4項目 ／ 分子量 28項目**（和 **28項目**） |

★ **検算**（⚠ どちらも同じ値になることを確かめた）:

```
grep -o "分子量" qa/questions.json | wc -l          → 134
（patterns 316件を再帰的に走査して数えた合計）      → 134   ★ 一致
grep -o "分子量" qa/questions.json | wc -l  ≠  grep -c "分子量" …  （134 ≠ 112）
                                            ⚠ 1行に複数回出るため
```

⚠⚠ **「134は多すぎる。数え直しが要る」という統合セッションの疑いは正しかった。**
★ **ただし原因は「他の文脈でも使われる語だから」だけではなく、
そもそも *項目を数えていなかった* こと。**

---

## 1. ★★ ③ 分子量・重合度の数え直し

### 1-1. ★ 数え方（⚠ 再現できる形で書く）

1. **母数は `qa/questions.json` の `patterns` 316件。**
   ★ 1件 = 1知識項目（`code`）で、その下に `flip` と `choice` の2 variant が付く。
2. ★ **1項目を丸ごと再帰的に走査して文字列を全部集めた**
   （`knowledge` ／ 各 variant の `q` `a` `supplement` `options` ／ `link.label` など）。
   ⚠ **`q`/`a` だけを見ると 131 にしかならず、134 に届かない**（残り3は `options` の外側の欄）。
3. ★ **その連結文字列に語が1回でも出れば「その項目のヒット」と数えた。**
4. ★★ **さらに、ヒットした28項目を1件ずつ読んで、3つに仕分けた**（⚠ 語の出現では仕分けられない）。

| 区分 | 定義 | ⚠ 判定に使ったもの |
|---|---|---|
| ★ **A: 計算させる** | ★ **数値を出す手順そのものが、その項目の知識の本体** | **`knowledge` が「〜で求める」「〜÷〜」の形／`choice` の選択肢に具体的な数値の計算が出る** |
| ★ **B: 数値を使うが、問うているのは規則** | **数は出るが、やるのは「割り算」ではなく「判定」** | **偶奇・大小・差分。1手で終わる** |
| ⚠ **C: 語が文中に出るだけ** | ★ **計算が1つも要らない** | **「同程度の分子量の〜より沸点が高い」など、比較のための修飾語** |

### 1-2. ★★ 仕分けの結果 —— **28項目のうち、計算させるのは 6項目**

| 区分 | 件数 | ★ 内訳 |
|---|--:|---|
| ★ **A（計算させる）** | ★★ **6** | `org.anal.mass-c` / `org.anal.composition-formula` / `org.anal.molecular-formula` / `org.anal.mw-from-solution` / **`org.poly.average-mw`** / **`org.poly.end-group`** |
| ★ **B（数値を使う判定）** | **6** | `org.fat.saponification-value` / `org.fat.iodine-value` / `org.phenol.methyl-ether` / `org.clue.nitrogen-parity` / `org.clue.cho-ratio` / `org.clue.amino-acid-mw-parity` |
| ⚠ **C（語が出るだけ）** | ⚠⚠ **16** | `org.ali.alkane-bp` / `org.ali.alkane-state` / `org.ali.alkane-branch-bp` / `org.alcohol.bp` / `org.carbonyl.dimer` / `org.bio.amino-acid-crystal` / `org.bio.protein-classification` / `org.bio.nucleotide` / `org.poly.monomer-polymer` / `org.poly.addition-vs-condensation` / `org.poly.crystalline-amorphous` / `org.clue.order` / `org.clue.diethyl-ether-bp` / `org.clue.acetic-mp` / `org.clue.ch2o-ratio` / `org.clue.amino-acid-mp` |

⚠⚠ **＝ 134 → 28 → ★ 6。** ★ **語の出現回数の 4.5%。**

★ **C の16件が何をしているか**（⚠ **これが「134」を膨らませていた正体**）:

> **「アルカンは *分子量* が大きいほど分子間力が強く、沸点が高い」**
> **「カルボン酸の沸点が同程度の *分子量* のアルコールより高い理由は？」**
> **「アミノ酸は *分子量* の近い有機化合物より融点が高い」**

★★ **＝ `分子量` は「大きさをそろえて比べる」ときの *ものさし* として使われている。**
⚠ **計算の話ではまったくない。** ★ **`沸点`・`融点`・`分子間力` の項目群に付随する語。**

### 1-3. ★ 「重合度」だけを見ると **4項目**、うち計算は **2項目**

| code | ★ 区分 | 中身 |
|---|---|---|
| ★ **`org.poly.average-mw`** | ★★ **A** | ★ **`choice` の選択肢に実際の計算が入っている** —— 「繰り返し単位 -CH₂-CH₂- の式量を28とすると、平均分子量 1.0×10⁵ のポリエチレンの平均重合度はおよそ 3.6×10³」 |
| ★ **`org.poly.end-group`** | ★★ **A** | ★ **末端基の物質量 → 高分子の本数 → 全質量 ÷ 本数 = 平均分子量**。⚠ **中和滴定が前段に付く** |
| `org.poly.monomer-polymer` | C | 単量体・重合体・重合度の**定義** |
| `org.poly.crystalline-amorphous` | C | 「重合度もそろっていない」＝ 融点が決まらない理由の説明 |

★★ **＝ 高分子で計算させているのは `average-mw` と `end-group` の2項目だけ。**
⚠ **申し送りの「重合度 25」は、`average-mw` 1項目の中に `重合度` が 11回、
`monomer-polymer` に 8回、`end-group` に 3回、`crystalline-amorphous` に 3回出た合計。**

### 1-4. ⚠⚠ 測ってみたら違っていたもの（★ ③ に関するもの）

| 申し送り | ★ 実測 |
|---|---|
| ⚠ **「`分子量` が 134件出るのは、他の文脈でも使われる語だからです」** | ★ **半分あたり。**⚠ **`134` は *件* ではなく *語の出現回数*。**★ **項目数は 28、うち計算は 6。**★ **「他の文脈」は具体的には「沸点・融点を比べるときのものさし」で、16項目がそれ** |
| ⚠ **「重合度・分子量」を1つの枠の候補として並べていた** | ★ **2つに割れる。**★ `org.poly.average-mw` / `org.poly.end-group` は **高分子の平均分子量**（⚠ **式量 × 重合度 の *比例***）／ `org.anal.*` は **元素分析**（⚠ **質量 → mol → 整数比**）。★ **前者は比例の型、後者は assembler の M7 に既に器がある** |
| ⚠⚠ **「134は多すぎる。数え直しが要る」＝ *減らす* 方向の疑い** | ★★ **項目数では 6 まで減ったが、入試の頻度では *増えた*。**★ **`org.poly.average-mw` は入試 51件**（§2-5）＝ **元素分析に次ぐ第2位の計算**。⚠ **申し送りで切り捨てられかけていたほうが、実は本命だった** |

### 1-5. ★ 数え直しの副産物 —— 「計算」の全在庫（★ ②の判断の材料）

★ **`分子量`・`重合度` 以外の語も同じやり方で数え、有機の「計算させる項目」を1つの表にした:**

| 型 | ★ 項目数（実測） | ★ code |
|---|--:|---|
| **元素分析（質量 → 組成式 → 分子式）** | **5** | `org.anal.mass-c` `-h` `-o` `composition-formula` `molecular-formula` |
| ★ **手がかりの数（偶奇・組成式からの当たり）** | **3** | `org.clue.nitrogen-parity` `cho-ratio` `amino-acid-mw-parity` |
| ★ **高分子の平均分子量** | **2** | `org.poly.average-mw` `end-group` |
| **けん化価・ヨウ素価** | **2** | `org.fat.saponification-value` `iodine-value` |
| **溶液から分子量**（浸透圧・凝固点降下） | **1** | `org.anal.mw-from-solution` |
| ⚠ **等電点** | ⚠⚠ **0** | ★ **`org.bio.isoelectric-point` は計算ではない**（§2-4） |
| **計** | ★ **13 / 316**（**4.1%**） | |

---

## 2. ★★ ② `/ratio/` が有機の計算を受けられるか —— **受けられない**

⚠ **`ratio/` は1バイトも変えていない。** ★ **測ったのは ① 実機の5画面 ② `model.js` のデータ構造
③ `tests.js`（回帰テスト 937件）④ 砂場での否定対照。**

### 2-1. ★ 5画面が何をどう解かせているか（★ 実機で確かめた・`http://127.0.0.1:9531/ratio/`）

| 画面 | ★ 「何が同じ」ルール（入口のカードの文言） | ★ 学習者がやること（実機） | 問数 | 課程 |
|---|---|---|--:|---|
| **`proportion.html`**（比例式で解く mol 計算） | ★ **倍率が同じ** | ★ **2列×2行の表**。上の行＝基準（1 mol あたり）、下の行＝知りたい量。**たて／よこ の倍率を分数で入れる → 自動転記 → 答えを入力** | **20** | 化学基礎 |
| **`balance.html`**（天秤でみる平均） | ★ **腕の長さの比 ＝ 個数の逆比** | ★ **数直線と支点の SVG**。区間を「2/3/4/5/8/10等分」から選び、支点の位置を読む | **8** | 化学基礎 |
| **`stoich.html`**（反応の量的関係） | ★ **変化量 ＝ 係数 × 倍率** | ★ **3行表（係数・反応前・変化量・反応後）**。過不足は「量 ÷ 係数」の棒くらべ | **18** | 化学基礎 |
| **`titration.html`**（中和滴定） | ★ **H⁺ の数 ＝ OH⁻ の数** | ★ **価数 × 物質量のブロックを左右でつり合わせる**（公式を入口にしない） | **8** | 化学基礎 |
| **`thermo.html`**（エネルギーでみる熱化学） | ★ **どの経路でも高さの差は同じ** | ★ **エネルギー図の準位を置き、高さの差を読む** | **6** | 化学 |

★★ **5画面に共通する見せ方の芯:**

> ★ **「公式に代入する」を1画面も採っていない。**
> ★★ **どの画面も「すでにそろっている2つの量のあいだの、同じ倍率」を図で見つけさせる。**

★ **入口（`index.html`）は *教科書の単元* でカードを並べている**（実測）:
**物質量（mol）と濃度 ／ 化学反応の量的関係 ／ 酸と塩基・中和 ／ 化学反応とエネルギー** ＝ ★ **4単元とも理論化学。**
★ **課程の絞り込みは 化学基礎 54問 / 化学 6問**（⚠ **「化学」は熱化学の6問だけ**）。

### 2-2. ★★ 乗らない理由 —— **4つあり、うち3つは「器の形」・1つは「割り当ての軸」**

⚠ **統合セッションの選択肢（「比例ではない」／「単位が違う」／「段が多い」）で言うと、
★ 「比例ではない」は違う。**★ **けん化価もヨウ素価も、比例そのもの。**
⚠⚠ **乗らない理由は別のところにあった。**

#### ★ 壁1（いちばん決定的）: ⚠⚠ ratio は「分子量は与件」で作られている。有機の計算は「分子量が答え」

★ **`model.js` の実測:**

```js
function perMol(sub, q) {
  if (q === 'mole')   return 1;
  if (q === 'mass')   return sub.M;        // ★ 基準の行 ＝ 物質の分子量
  if (q === 'volume') return MOLAR_VOLUME; // 22.4
  return AVOGADRO;                         // 6.0e23
}
```

★★ **＝ 表の「基準の行」は物質の分子量から作られる。**
⚠ **分子量が分からないと表そのものが立たない。**

★ **実測: `ratio` の 60問すべてで、物質の分子量は与件**
（`SUBSTANCES` の 25件が `M` を文字列で持ち、`SPECS`・`REACTIONS`・`TITRATIONS` はそこを引く）。
⚠⚠ **「未知の物質の分子量を決める」問題は 0 件。**
★ 天秤モードの `b1`〜`b8` は分子量が答えになるが、**既知の2値の加重平均**であって、
**未知の物質の分子量ではない**（³⁵Cl 35.0 と ³⁷Cl 37.0 はどちらも与えられている）。

⚠ **一方、有機の計算は 13項目のうち 8項目が「分子量そのものを決める」問題**
（元素分析5・溶液から分子量1・けん化価/ヨウ素価は **けん化価 → 油脂の平均分子量** が定番）。
★★ **＝ 器の前提と、載せたい問題の向きが逆。**

#### ★ 壁2: 単位（★ 統合セッションの読みのうち、これは当たっていた）

```js
QUANTITIES = { mass: 'g', mole: 'mol', volume: 'L', count: '個' }   // ★ 4種で閉じている
```

| | ★ 答えの単位 | ⚠ `QUANTITIES` に在るか |
|---|---|---|
| **けん化価** | ★ **mg KOH / g 油脂** | ⚠⚠ **無い。**★ **そもそも「比」そのものが答え** |
| **ヨウ素価** | ★ **g I₂ / 100 g 油脂** | ⚠⚠ **無い。同上** |
| ★ **重合度** | ★ **（無次元の個数）** | ⚠ **`count` が近いが、単位は「個」で 6.0×10²³ が基準に来る** |
| ★ **等電点** | ★ **pH（対数・無次元）** | ⚠⚠ **無い** |

★★ **けん化価・ヨウ素価は「1 g（100 g）あたりにそろえた比」が答え ＝ ratio の表では答えのセルに入らない。**
⚠ **表の未知セルは「量」であって「比」ではない**（比は矢印のバッジのほうに出る）。

#### ★ 壁3: 係数が変数になる（★ ヨウ素価だけ）

```js
eq: [{ sub: 'CH4', coef: 1 }, { sub: 'O2', coef: 2 }, …]   // ★ coef は数値で固定
```

⚠ **ヨウ素価は「C=C 1個に I₂ 1分子」で、付加する I₂ の数 ＝ 二重結合の数 n。**
★ **n を求めるのが問題なので、係数の位置に未知数が入る。**⚠ **`REACTIONS` の形では書けない。**

★ **加えて `SUBSTANCES` は 25件の具体的な物質**（H₂O・CO₂・NaOH …）。
⚠ **油脂は R が一般基で、分子式も分子量も定まらない。**

#### ★★ 壁4（⚠ これは器の話ではない）: 5本のアプリは分野で割ってある。型では割っていない

★ **`DEVELOPMENT.md`（2026-09-01・ユーザー決定「案C」の書き下し）の明文:**

> ★ **有機 → assembler ／ 無機 → muki ／ 理論 → ion-equation・ratio ／ 知識（全分野）→ qa**

⚠⚠ **＝「計算だから ratio」という割り方は、この決定と正面から当たる。**
★ **`DESIGN_paid_workbook.md` §1-2 も「有機の全範囲を覆う責任があるのは `/assembler/` と `/qa/` の2本だけ」と実測で書いている。**
★ **入口の実測（単元4つとも理論・化学基礎54/化学6）は、その決定が画面にも出ているということ。**

⚠ **ただし公平のために書く: `DESIGN_ratio.md` の冒頭は
「カタログ上の位置づけは 5本のうちの『計算できるようになる』担当」とあり、
★ 領域を理論に限る文は ratio 側の設計書には無い。**
★★ **したがってこれは「ratio の設計に反する」ではなく「あとから決まった割り当ての軸に反する」。**
⚠ **どちらを上に置くかはユーザーの決定事項なので、`D-OC1` として立てる。**

### 2-3. ★★ 否定対照 —— 砂場で実際に足して、赤くなる件数を数えた

⚠ **「たぶん当たる／当たらない」と書かないこと**（`DEVELOPMENT.md`「どの数が固定されているかはファイルを見ないと分からない」）。
★ **`ratio/` は1バイトも変えず、scratchpad の `cf-sandbox/` に丸ごと複製して測った。**

| 走行 | 何をしたか | ★ 結果 |
|---|---|--:|
| ★ **基準（本番の worktree・:9531）** | 何もしない | ★ **ALL PASS (937)・終了コード 0** |
| ★ **基準（砂場・:9532）** | 複製しただけ | ★ **ALL PASS (937)・終了コード 0** |
| ⚠ **(R) 6つめのモードを足す** | `model.js` に `ORGANIC`（ダミー6問）／`progress.js` の `MODES`／`nav.js`／`portal.js` に単元「有機化合物」／`organic.html`+`organic.js` | ⚠⚠ **16 FAILED / 937・終了コード 1** |
| ★ **(S) 既存モードに問題を1問足す** | `REACTIONS` に `r19` を1件（`r18` の複製） | ★ **3 FAILED / 937**（⚠ **うち1件は複製ゆえの「id に重複がない」なので実質 2件**） |

★ **(R) で赤くなった 16件（名前をそのまま写す）:**

```
✘ 5モードぶんのモードidを持つ
✘ 全モードで自分の台帳の id だけを通す
✘ 単元が4つ並ぶ
✘ モードのカードが5枚ある
✘ 5つのモードすべてが入口に載っている
✘ 合計問題数が表示と一致する
✘ 各カードに「同じもの」のルールが書かれている
✘ 課程の札が出る（化学基礎4・化学1）
✘ 開くと5モードすべてへ行ける
✘ 既定は「すべて」で、5枚とも出ている
✘ 「化学」を選ぶと熱化学の1枚だけになる
✘ 中身が消えた単元は見出しごと出さない
✘ 絞り込んでも「モードの持ち物」は変わらない（nav との突き合わせを壊さない）
✘ 課程ごとの分母がモデルの問題数と一致する
✘ しぼる帯のボタンに課程ごとの進捗が出る
✘ 「すべて」に戻すと5枚とも戻り、隠した注記も消える
```

★ **(S) で赤くなった 3件:**

```
✘ 反応の問題は18問ある
✘ id に重複がない        ← ⚠ 複製したせい。本物の問題を書けば出ない
✘ ステージボタンが18個ある
```

⚠⚠ **＝ `ratio` は「モードの数（5）」を 9か所で不変条件にしている。**
★ **化学のロジックを1行も足していないのに 16件が赤くなる**
（⚠ **足したのは `{id:'g1'}` が6個のダミーと、入口の札だけ**）。
★★ **`DEVELOPMENT.md`「見積りは *見せ方* を数に入れないと外れる」の、いちばん素直な実例。**

### 2-4. ⚠⚠ 等電点 —— そもそも計算ではなかった（★ 申し送りを覆す）

★ **`等電点` の語は 13回出るが、項目は 3件。**⚠ **その3件を全部読んだ:**

| code | ★ 何を問うているか | ★ 型 |
|---|---|---|
| `org.bio.amino-acid-amphoteric` | 両性・双性イオン・強酸/強塩基と塩をつくる | ★ **知識** |
| `org.bio.isoelectric-point` | ★★ **定義**（正負がつりあう pH）＋ **`choice` は電気泳動**（pH 6 で グルタミン酸→陽極 / グリシン→動かない / リシン→陰極） | ★★ **観察・向きの判定** |
| `org.bio.amino-acid-polyprotic` | 電離平衡が二段か三段か | ★ **知識** |

⚠⚠ **`pI = (pK₁ + pK₂)/2` の型の計算は、`qa` に 1件も無い**（★ `pK` の語の出現も 0）。

★★ **＝「等電点の計算の枠が無い」ではなく「等電点の計算問題が無い」。**
★ **`org.bio.isoelectric-point` の `choice` が求めているのは *どちらの電極へ動くか* ＝
`DESIGN_paid_workbook.md` §2-3 の #1「観察」と同じ型で、`/muki/` が無機で実証している形。**
⚠ **`/ratio/` の5画面のどれとも似ていない。**

### 2-5. ★★ 入試の頻度で並べ直す（⚠ ここで優先順が入れ替わる）

⚠ **`DEVELOPMENT.md`「着手の判断は『安いから』ではなく『入試に出るから』で立て直した」に従い、
`qa/data/exam_usage.jsonl`（★ 全国大学入試問題正解 化学・有機の悉皆）で数えた:**

| 型 | code | ★ 入試の出題数 | ★ 枠はどこ |
|---|---|--:|---|
| ★★ **分子式（元素分析の出口）** | `org.anal.molecular-formula` | ★★ **94** | ★ **assembler M7（器あり）** |
| **炭素の質量 / 水素の質量** | `org.anal.mass-c` / `-h` | **69 / 69** | ★ **assembler M7** |
| **酸素の質量 / 組成式** | `org.anal.mass-o` / `composition-formula` | **64 / 64** | ★ **assembler M7** |
| ★★ **高分子の平均分子量** | `org.poly.average-mw` | ★★ **51** | ⚠⚠ **枠が無い** |
| ⚠ **等電点** | `org.bio.isoelectric-point` | **16** | ⚠ **計算ではない**（§2-4） |
| **溶液から分子量**（浸透圧・凝固点降下） | `org.anal.mw-from-solution` | **13** | ⚠ **理論の未実装領域**（ratio にも無い） |
| ⚠ **けん化価 / ヨウ素価** | `org.fat.saponification-value` / `iodine-value` | ⚠ **9 / 9** | ⚠ **枠が無い** |
| **末端基から平均分子量** | `org.poly.end-group` | **4** | ⚠ **枠が無い** |

⚠⚠ **＝ 統合セッションが「枠が無い」と名指しした3つ（けん化価9・ヨウ素価9・等電点16）は、
頻度で見ると下位。**
★★ **本当に効くのは `org.poly.average-mw` の 51件で、これは申し送りで
「134は多すぎる。数え直しが要る」と切り捨てられかけていたほう。**

★ **`org.poly.average-mw` の中身**（`choice` の選択肢に実際の計算が入っている）:

> **「繰り返し単位 -CH₂-CH₂- の式量を 28 とすると、平均分子量 1.0×10⁵ の
> ポリエチレンの平均重合度はおよそ 3.6×10³ である」**

★★ **これは 2列×2行の比例表そのもの**（基準 = 式量 28 : 繰り返し単位 1個 ／
知りたい量 = 1.0×10⁵ : ? 個）。⚠ **`ratio` の M1 と型は同じ。**
⚠ **それでも `QUANTITIES` に「無次元の個数」が無く、`SUBSTANCES` に「繰り返し単位」が置けないので、いまの器には入らない。**

### 2-6. ★★ 代案を数で比べる

★ **覆う対象を「けん化価・ヨウ素価・平均分子量・末端基 の4項目」に固定して比べる**
（⚠ **等電点は §2-4 で計算ではないと分かったので外す。元素分析5項目は ① の配線で片づく**）。

| | ⚠ **(R) `/ratio/` に6つめのモード** | ★★ **(A) assembler の絞り込みモードに置く** | ★ **(Q) 枠を作らない（`/qa/` の測定面のまま）** |
|---|---|---|---|
| ★ **覆う qa 項目** | **4** | **4** | ★ **4**（⚠ **既に `choice` で出題済み**） |
| ★ **覆う入試の出題数** | **73**（51+9+9+4） | **73** | ⚠ **0**（★ 知識としては当たるが、計算はさせない） |
| ⚠ **活動0のページが減る** | **1**（けん化価・ヨウ素価） | **1** | **0** |
| **判定・実行（新しい計算）** | **4本** | ★ **2〜3本**（⚠ **式量・組成の探索・官能基の式量引きが既にある**） | ★ **0** |
| ⚠ **見せ方** | ⚠ **新規1画面**（`organic.html` + `organic.js`。★ **既存5画面はどれもレンズが違うので流用できない**） | ★ **既存のタブ列に1枚足す**（**M7 と同じ「値を打ち込むと途中の数字が全部出る」形**） | ★ **0** |
| ⚠⚠ **触る既存の緑のテスト（★ 実測）** | ⚠⚠ **16**（§2-3） | ⚠ **未実測**（★ `assembler/tests.js` を `narrowing` のタブ数で grep したが、タブ数を固定した検査は見つからなかった。⚠ **`?open=` の受け口も固定されていない実績あり**＝ `DEVELOPMENT.md` 3550行） | ★ **0** |
| ⚠ **新しいアプリ間の辺** | ⚠⚠ **assembler ⇄ ratio が要る**（★ **いまは `ion → ratio` の一方向だけ**。`CLAUDE.md` の明文） | ★ **0**（**同一アプリ内**） | ★ **0** |
| ★ **割り当ての軸に合うか** | ⚠⚠ **合わない**（**有機 → assembler**） | ★ **合う** | ★ **合う** |

★★ **推奨: (A) —— 有機の計算は assembler の絞り込みモード側に置く。`/ratio/` には置かない。**

★ **根拠（数の大きい順）:**

| # | 根拠 | 数 |
|--:|---|--:|
| **1** | ★★ **入試で最も効く計算（元素分析 64〜94件）は、既に assembler の M7 に器がある** | ★ **5項目・入試 360件ぶん** |
| **2** | ⚠⚠ **(R) は化学を1行も足さないうちに緑のテストが 16件赤くなる**（★ 実測・砂場） | ⚠ **16** |
| **3** | ⚠ **`ratio` の器は「分子量が与件」。有機の計算は 13項目中 8項目が「分子量が答え」** | ⚠ **60問中 0件が「未知の分子量を求める」** |
| **4** | ★ **割り当ての軸（有機 → assembler）はユーザー決定・2026-09-01** | ★ **明文1件** |
| **5** | ⚠ **(R) は新しいアプリ間の辺（assembler ⇄ ratio）を1本増やす** | ⚠ **いまは 1方向のみ** |
| **6** | ★ **(Q) は手数0だが、入試 73件ぶんの計算をさせないまま残る** | ⚠ **0 / 73** |

⚠ **ただし (A) を「いま作る」とは書かない。**★ **§2-5 の頻度に従うなら、着手の順は:**

1. ★★ **元素分析の配線（＝ ① / 入試 360件・新しい計算 0本）** … **本書 §3**
2. ★★ **高分子の平均分子量（入試 51件・2項目）** … ⚠ **枠が無い。M7 の隣に置くのが素直**
3. ⚠ **けん化価・ヨウ素価（入試 18件・2項目）** … ★ **2 と同じ画面に相乗りできる**（**どちらも「1 単位あたりにそろえて分子量を逆算する」型**）
4. ⚠ **等電点** … ★ **計算の枠は要らない。**⚠ **要るなら「電気泳動でどちらへ動くか」＝ 観察の枠**（`DESIGN_paid_workbook.md` §2-3 #1 と同じ待ち行列）

### 2-7. ★ ②で立てる判断待ち

| # | 論点 | ★ こちらの推奨 | 根拠 |
|---|---|---|---|
| ★★ **D-OC1** | ⚠⚠ **5本のアプリを割る軸は「分野」か「型」か。**★ `DEVELOPMENT.md` は分野（有機→assembler）と書き、`DESIGN_ratio.md` は ratio を型（計算担当）と書いている | ★ **分野を上に置く**（**あとから決まったほうが新しく、画面にも出ている**） | ★ **ratio の入口は4単元とも理論・課程は 化学基礎54/化学6。**⚠ **有機を足すと入口の見え方が変わる** |
| ★ **D-OC2** | ★ **有機の計算を assembler の絞り込みモードに置いてよいか**（**M7 の隣にもう1枚**） | ★ **よい** | ★ **入試で最も効く元素分析が既にそこにある。**⚠ **(R) は実測16件の赤・(Q) は入試73件を落とす** |
| ★ **D-OC3** | ★★ **次に枠を作るのは `org.poly.average-mw`（入試51件）でよいか**（⚠ **申し送りの けん化価・ヨウ素価（18件）より先**） | ★ **よい** | ★ **頻度が 2.8倍。**⚠ **しかも同じ画面に相乗りできる** |
| ⚠ **D-OC4** | ⚠ **`org.anal.mw-from-solution`（浸透圧・凝固点降下・入試13件）は理論化学。**★ **`ratio` にも枠が無い。誰が持つか** | ⚠ **保留**（★ **有機の側からは「分子量が与えられる経路の1つ」としか見えない**） | ★ **`ratio` の60問に希薄溶液は0件** |

---

## 3. ★★ ① 元素分析の配線（★ 実装した）

★ **assembler v1494 が `?open=narrowing&panel=<enum|allot|frag|ea>` の受け口を出したので、
`qa` 側から指せるようにした。**⚠ **`assembler/` は1バイトも触っていない。**

### 3-1. ★★ `panel` をどこに足すか —— **`kind` ではなく `open` の隣**

⚠ **申し送りの宿題:「`panel` を渡す欄が棚卸し表の schema に無い（`practice` は `open` だけ）。
どう足すかを決めてください」**

★ **決めた形:**

```json
{"code":"org.anal.mass-c","kind":"practice","label":"元素分析から炭素の質量を出す",
 "open":"narrowing","panel":"ea","note":"…"}
```

★ **理由（3つ。⚠ どれも「軸を混ぜない」の言い換え）:**

| # | 理由 |
|--:|---|
| **1** | ★★ **`kind` と `open` は別の軸。**`kind` は **リンクの形**（何を添えるか —— `summon` なら分子・`mechanism` なら機構 id・`isomer` なら分子式）。`open` は **行き先**。⚠ **`narrowing` は assembler の `OPEN_TARGETS` の1値で、`naming` や `practice` と同じ層にある。**★ これを `kind` に置くと、1つの欄が2つの意味を持つ |
| **2** | ★★ **`panel` は `scope` / `field` と完全に同じ立場** ——「行き先の中のつまみ」。★ **前例があるので、運ぶ側（`gen_links.js` の1行・`app.js` の `linkQuery` の1語）に足すだけで済んだ** |
| **3** | ⚠ **`open` に埋め込む案（`"open":"narrowing:ea"`）は採らない。**★ `OPEN.indexOf(o.open)` の検査が効かなくなり、**分解の規約が `tests.js` / `app.js` / `gen_links.js` の3か所に散る**（`CLAUDE.md`「2か所に持つと必ずずれる」） |

⚠ **統合セッションの申し送りは「`kind` に `narrowing` を足す」と「`OPEN` 一覧に `narrowing` を足す」を
両方書いていたが、★ この2つは同時には成り立たない**（`OPEN` は `kind:"practice"` の
`open` の値の一覧なので、`kind` に置くなら `OPEN` は要らない）。★ **`OPEN` のほうを採った。**

### 3-2. ★ 触ったファイル（⚠ 版番号は1文字も触っていない）

| ファイル | 何をしたか | 差分 |
|---|---|--:|
| **`qa/data/assembler_links.jsonl`** | ★ **7行を `kind:"none"` → `kind:"practice"` に**（元素分析5・`org.clue.nitrogen-parity`・`org.clue.order`） | **7 / 7** |
| **`qa/questions.json`** | ★ `gen_links.js --write` で再生成 | ★ **42 / 0**（**7件 × 6行**） |
| **`qa/tools/gen_links.js`** | ★ 運ぶ欄に `panel` を追加 | **5 / 1** |
| **`qa/app.js`** | ★ `linkQuery` の `practice` に `panel` を追加 | **7 / 1** |
| **`qa/tests.js`** | ★ `OPEN` に `narrowing`／`OPEN_HAS_PANEL`／**CF1・CF2・CF3** | **47 / 2** |
| **`qa/test.html`** | ★ `assembler/index.html` を読む（CF2 の材料）。⚠ **配列の末尾に足した**（途中に挟むと `res[N]` が全部ずれる） | **8 / 2** |

⚠⚠ **版が上がるのは `qa` だけ。**★ **`assembler` は読んだだけ**（`assembler/index.html` を
テストが `fetch` するが、書いていない）。

### 3-3. ★★ 足した検査（★ 接頭辞 `CF`）

| # | 検査 | ⚠ どんな壊れ方を止めるか |
|---|---|---|
| ★ **CF1** | **`panel` は `kind:"practice"` かつ、タブを持つ行き先にしか付けられない** | ⚠ `open=naming` に `panel` を付けても assembler は無視する ＝ 黙って効かない |
| ★★ **CF2** | ★ **`panel` の値が `assembler/index.html` の実在のタブ（`.nw-mode-tab[data-panel]`）と一致する** | ⚠⚠ **受け口⑦は DOM に聞いて、無ければ *何もしない*。**★ 綴りを間違えると**エラーも出ず、画面も開く**（既定の「構造を数える」のまま）＝ **「元素分析から」を頼んだのに着かないことに誰も気づけない**。★ `scope` / `field` と同じ壊れ方 |
| ★★ **CF3** | ★ **配信データ（`questions.json`）の `open` / `panel` が棚卸し表と一致する** | ⚠⚠ **これが申し送りの「件数だけで見るな」への答え。**★ `panel` だけが落ちても **`kind` と `label` は合うので「繋がった1件」として数えられてしまう**。★ 運ぶ欄そのものを1つずつ突き合わせる |

### 3-4. ★★ 否定対照（⚠ 3件とも実際に壊して、鳴ることを確かめた）

| # | わざと壊したもの | ★ 結果 |
|--:|---|---|
| **0** | ★ **何もしない（基準）** | ★ **ALL PASS (94)・終了コード 0** |
| **1** | ★ `panel` の値を `ea` → **`eaX`** | ⚠ **2 FAILED** ——「知らない panel「eaX」（assembler の実在のタブは enum / allot / frag / ea）」＋「panel がずれている（表 eaX / 配信 ea）」 |
| **2** | ★★ **配信データから `panel` だけを落とす**（`gen_links.js` が運び忘れた状態） | ⚠ **1 FAILED** ——「panel がずれている（表 ea / **配信 なし**）。gen_links.js を回し直す」★ **件数は 182 のまま変わらないのに鳴る** |
| **3** | ★ **つまみを持たない行き先（`open:"naming"`）に `panel` を付ける** | ⚠ **2 FAILED** ——「open=naming にはタブが無い（panel を渡しても assembler が無視する）」 |
| **4** | ★ 元に戻す | ★ **ALL PASS (94)・終了コード 0** |

★ **走行の前後で印ファイル（`qa/cf-mark.txt`）を `curl` して確かめ、最後に消した**
（★ 消したあとは 404 を確認）。

### 3-5. ★★ 実機で確かめた（⚠ コードを読んだだけではない）

**① `qa` が出す URL**（★ 実際にアプリを進めて `a.a-link` の `href` を読んだ）:

```
../assembler/?from=qa&code=org.anal.molecular-formula&open=narrowing&panel=ea
札の文言: 「組成式と分子量から分子式を出す」
```

**② その URL を開いたときの assembler の状態**（★ DOM を直接読んだ）:

```
narrowing-modal … display: flex          ← ★ モーダルが開いている
nw-panel-enum   … display: none
nw-panel-allot  … display: none
nw-panel-frag   … display: none
nw-panel-ea     … display: flex          ← ★★ 「元素分析から」が選ばれている
タブの on       … ea
来た道の帯      … 「一問一答から来ました ← 一問一答へ戻る」
```

**③ ★★ 配線した項目が、その画面に *名指しで* 出るか**（⚠ **これが「件数で見ない」の中身**）。
★ **教科書の定番の値（試料 6.0 / CO₂ 8.8 / H₂O 3.6 mg・分子量 60）を入れて、画面の文字をそのまま写した:**

```
C 2.400／H 0.400／O 3.200                          ← ★ mass-c / mass-h / mass-o の3項目
物質量 C 2.00e-1／H 4.00e-1／O 2.00e-1 mol
組成式 CH2O （式量 30）                             ← ★ composition-formula
窒素則: 分子量が偶数 → 窒素の数は偶数（0個・2個…）  ← ★★ clue.nitrogen-parity が1行そのまま
                                    ※ハロゲンを含まないとき
分子式 C2H4O2 （組成式の 2 倍） 不飽和度 1・重原子 4  ← ★ molecular-formula（倍率まで見える）
この分子式を絞り込む
```

★★ **＝ 配線した7項目のうち6項目が、この1画面に名指しで出る。**
★ **残り1つ（`org.clue.order`）は `panel=enum`** —— **実験カードの棚（`nw-palette`）と
積んだカード（`nw-stack`）は第1タブ「構造を数える」の中にある**ので、そちらを指定した
（⚠ **既定に頼らず明示する。既定が変わってもリンクの約束が変わらないため**）。

### 3-6. ★★ 「活動0 が 8 → 6」は本当か —— **本当だった**（★ 機械で数え直した）

⚠ **`origin/main` の `questions.json` と、いまの `questions.json` を両方読んで、
52小項目それぞれの「`link` を持つ項目の数」を数えた**（★ 同じ数え方を両方に当てた）。

| | ★ 実測 |
|---|--:|
| **小項目の数** | **52** |
| ⚠ **配線由来の活動が 0 のページ（前）** | ⚠⚠ **8** |
| ★ **同（後）** | ★★ **6** |

★ **0 でなくなった2ページ（名指し）:**

| 単元 / 小項目 | 項目 | ★ 活動（後） | 中身 |
|---|--:|--:|---|
| ★★ **anal / 元素分析（定量）** | **10** | ★★ **5** | `practice` 5（**mass-c / mass-h / mass-o / composition-formula / molecular-formula**） |
| ★ **clue / 手がかりを使う順番** | **1** | ★ **1** | `practice` 1（**clue.order**） |

⚠ **「元素分析10項目ぜんぶ」ではない**（`DESIGN_paid_workbook.md` §2-3 の予測どおり）。
★ **残る5項目は 定義1・装置3・溶液から分子量1** で、どれも絞り込みモードでは見えない。

★ **後も 0 のまま残る6ページ（★ `DESIGN_paid_workbook.md` の一覧と1件も違わない）:**

```
anal   / 元素の確認（定性）   6項目   ← 観察の枠が要る
phenol / 製法                3項目   ← reactor に無い反応
aroN   / 分離                2項目   ← 分離の枠が要る
fat    / けん化価・ヨウ素価   2項目   ← ★ ②の判断待ち（D-OC2）
bio    / 糖類の分類          2項目   ← 一問一答で足りる型
bio    / 酵素                3項目   ← 一問一答で足りる型
```

⚠ **配線した7項目のうち、活動0のページに落ちたのは 6項目。**
★ **`org.clue.nitrogen-parity` は「数から当たりを付ける」の群にいて、そこは既に活動3だった**
（＝ **繋がったが、0ページを減らす働きはしていない**）。★ **`182 / 316`（前は 175）。**

### 3-7. ★ ついでに直した ——「★見直し候補」4件の *待っている相手*

⚠ **統合セッションからの申し送り: ユーザー決定「反応式を書く際には ion-equation」により、
`org.aroN.aniline-base` ／ `org.aroN.diazonium-decomp` ／
`org.bio.amino-acid-amphoteric` ／ `org.bio.amino-acid-polyprotic` の4件は、
待つ相手が「assembler がイオンを持てるようになる」から
「ion-equation にその物質が収録される」に変わった。**

★ **こちらでも数え直した**（`ion-equation/*.js` の全文検索）:

| 物質 | ★ ion-equation での出現 |
|---|--:|
| **酢酸** | ★ **74** |
| **エタノール** | ★ **16** |
| ⚠ **アニリン** | ⚠⚠ **0** |
| ⚠ **グリシン / アミノ酸** | ⚠⚠ **0 / 0** |
| ⚠ **フェノール** | ⚠⚠ **0** |
| ⚠ **サリチル酸** | ⚠⚠ **0** |
| ⚠ **ジアゾニウム** | ⚠⚠ **0** |

★★ **＝ 壁は「モデルの作り直し」から「データの追加」に下がった。**
⚠ **ただし *まだ繋がらない*（相手に物質が無い）ので `kind` は `none` のまま。**
★ **書き換えたのは `why` と `note` だけ**（4行・4/4 の差分）。
⚠ **ion-equation への収録そのものはこちらの担当ではない。**

### 3-8. ★ ①で立てる判断待ち

| # | 論点 | ★ こちらの推奨 |
|---|---|---|
| ★ **D-OC5** | ⚠ **`org.aro.c8h10-isomers`（`DESIGN_paid_workbook.md` の `D-W12`）は、いまも繋がっていない** ——★ 原因は「`link` の書き忘れ」ではなく **`gen_links.js` の `ISOMER_VERIFIED` に `C8H10` が無い**（**実機で開くか未確認**）。★ 棚卸し表には行がある | ★ **実機で `?open=isomer&formula=C8H10` を開いて確かめ、開けば `ISOMER_VERIFIED` に足す**（⚠ **推測で足すと押しても無反応の入口になる、と `gen_links.js` 自身が書いている**） |

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-01 | 新規。§0 申し送りの数は全部「語の出現回数」だった／§1 ③ 分子量・重合度の数え直し（134 → 28 → 6）／§2 ② /ratio/ は有機の計算を受けられない（壁4つ・砂場の否定対照で赤16件・入試の頻度で優先順が入れ替わる）／§3 ① 元素分析の7項目を配線（活動0 が 8 → 6・否定対照3件）／§4 verify-release.js は worktree の外から呼ぶと嘘の合格を出す |
---

## 4. ⚠⚠ 測るときに踏んだ罠 —— `verify-release.js` は **worktree の外から呼ぶと嘘の合格を出す**

★ **`DEVELOPMENT.md` の「測るときの罠」に足すべき内容なので、ここに書き残す**
（⚠ **このレーンは `DEVELOPMENT.md` を触らない約束なので、統合側で移してください**）。

### ★ 何が起きたか

⚠ **`CLAUDE.md` の「worktree に `tools/record/node_modules` が無いので、本体の
`tools/run-tests.mjs` を worktree の URL に向けて走らせる」に引きずられて、
`verify-release.js` も本体のものを呼んだ:**

```
cd C:/Antigravity/worktrees/calc-frame
node "C:/Users/maequ/…/OrganicChemistryPuzzle/tools/verify-release.js" qa
  → ✅ 不合格の問題はありません   ⚠⚠ 嘘
```

★ **同じ瞬間に、worktree 自身の copy を呼ぶと:**

```
cd C:/Antigravity/worktrees/calc-frame
node tools/verify-release.js qa
  → ❌ qa: push 待ちのコミットで5件を変えているのに、版が origin/main と同じ v91 のままです
```

### ★ 原因

★ **`verify-release.js` は `ROOT` を `__dirname` から決める** ——
⚠ **`cd` した先ではなく、*スクリプトが置かれている木* を見る。**
★ **`run-tests.mjs` は URL を引数で受けるので木を選べるが、`verify-release.js` は選べない。**

### ⚠⚠ なぜ危ないか

★ **「本体の tools を借りる」は `run-tests.mjs` では正しく、`verify-release.js` では *逆* になる。**
⚠ **しかも失敗の出方が「エラー」ではなく「合格」なので、間違えたことに気づけない。**
★ **本体の作業ツリーがきれいなら、どのレーンから呼んでも必ず ✅ が出る。**

| ツール | ⚠ worktree からどう呼ぶか | 理由 |
|---|---|---|
| **`tools/run-tests.mjs`** | ★ **本体のものを、worktree の URL に向けて** | ⚠ `playwright` が `tools/record/node_modules` にしか無い。★ **見る対象は URL で決まる** |
| ⚠⚠ **`tools/verify-release.js`** | ★★ **worktree 自身のものを** | ⚠⚠ **`ROOT` が `__dirname` 由来。外から呼ぶと *本体の木* を検査して合格する** |
| **`tools/verify-compounds.js`** | ★★ **worktree 自身のもの**（⚠ **実測:  ＝ 同じ罠**） | |


