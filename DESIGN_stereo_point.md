# 設計書: 「立体が分かれる場所」を指して、立体込みの総数を答える（2026-08-20）

`video-scripts/ORDER_isomer_2026-08-20.md` の **A-6**（構造異性体の書き出しに不斉炭素まで答えさせる段）と
**B-1**（「立体異性体は何種類？」の幅）を、ユーザーの提案で1本にまとめ直したもの。

> ## ★ 実装済み（v1435・2026-08-21）
>
> **正は [DESIGN_isomer_practice.md §18](DESIGN_isomer_practice.md) へ移した。**
> この文書には**実測（§1）と決め方**が残る（数の出どころ）。
> ⚠ 起草時と変わった点が3つあるので、§1 を読む前にここを読むこと:
>
> | 起草時 | 実装 | なぜ |
> |---|---|---|
> | 「隠すのは立体込みの総数だけ・構造の数は満点のために出す」（§7-1） | ★ **構造の数も隠す**（お題は `C₅H₁₀（立体まで）`） | ユーザー判断「**満点は採点時に示せばよいのでは？**」で §7-1 の制約が消えた。①③ への答えは **§18-4a** |
> | 「M1 ＝ 段1 だけで止める」（§3-1） | **段1・段2 とも入れた** | 段2 の答え（総数）を隠す形が決まったので、止める理由が無くなった |
> | 「§5-2 の掛け算のヒントを出す」 | **入れていない** | ヒントを 4 → 5 段にする話で、スコアの式に触れる。**段1・段2 とは独立に足せる**（§18-7） |
>
> ⚠ **ハースの糖（α/β）はこの文書の範囲外だったが、同じ版で一緒に入れた**（§18-6）。
> 発注書 ORDER_isomer_2026-08-20 の B-2「ろ」・判断25 の側。

**起草時の断り書き（そのまま残す）**: この文書は設計だけで、実装は1行もしていない。
アプリのコード・データ・版は1つも変えていない。数はすべて実測（測り方は §11）。

---

## 0. この文書の役割と、正がどこにあるか

| | |
|---|---|
| **正** | 実装するまではこの文書。実装したら §12 の行き先へ移す |
| **触る面** | 書き出し練習（`IsomerPractice`）＝ **既存の面の中**。⚠ 新しいモードは作らない |
| **触らない面** | `StereoCountQuiz`（4択）・`stereoPractice`（立体の書き出し）・パズル・自由モード |
| **前提にしているもの** | 別レーンが進めている**お題の追加**（ORDER A-5・§4.1）。⚠ **C₅H₁₀ が無いとメソ体が出せない** |

**読む順**: §1（実測）→ §2（ユーザーの言葉の読み直し。★ ここに芯の食い違いがある）→ §3〜§6（設計）
→ §7（満点）→ §8（A-6 との共有）→ §9（決めてほしいこと）。

---

## 1. 先に実測（推測で書いていない証拠）

### 1-1. `StereoCountQuiz` のプール 169 件を、型で数え直した

**調査レーンの数（B-1）を追認したうえで、型の軸を1本足した。**

| 型 | 件数 | 割合 | 畳み込みが起きるもの |
|---|--:|--:|--:|
| **不斉炭素だけ**（C=C なし） | **147** | **87.0%** | 4 |
| **C=C だけ**（不斉炭素なし） | **22** | **13.0%** | 1 |
| **両方をもつ** | **0** | **0.0%** | 0 |
| 計 | 169 | 100% | **5** |

★ **「両方」が1件も無い。**統合レーンの読み（「不斉炭素だけでなく C=C も」）は**正しい**が、
**その2つが同じ分子に同居する場面はプールに存在しない**。§5 の操作設計はこの数の上に立っている。

**単位の内訳**（`stereoUnitsOf` の戻り値をそのまま数えた）:

```
不斉炭素1個 / C=C 0本 … 132     不斉炭素0個 / C=C 1本 … 19
不斉炭素2個           …   8     不斉炭素0個 / C=C 2本 …  1
不斉炭素3個           …   3     不斉炭素0個 / C=C 3本 …  2
不斉炭素4個           …   3
不斉炭素5個           …   1
```

**答えの分布 —— 追認: 151 件（89.3%）が「2種」。**⚠ **「2」と答えるだけで9割正解できる。**

```
2種 … 151    3種 … 3    4種 … 7    6種 … 1    8種 … 3    16種 … 3    32種 … 1
```

**2ⁿ が崩れる 5 件（調査レーンの「5件」を追認）**:

```
酒石酸                        不斉2  2²=4 → 3
1,2-ジメチルシクロプロパン    不斉2  2²=4 → 3
1,2-ジメチルシクロブタン      不斉2  2²=4 → 3
乳酸3分子の環状エステル       不斉3  2³=8 → 4
トリオレイン（油脂）          C=C 3  2³=8 → 6
```

⚠ **調査レーンの「C=C だけ ＝ 19件」は、C=C が1本のものだけの数**。
2〜3本のもの（リノール酸・リノレン酸・トリオレイン）を足すと **22件**になる。
**「不斉炭素を指摘させる出題では問いが立たない件数」は 19 ではなく 22。**

### 1-2. お題ごとに「立体が分かれる場所」を持つ異性体が何件あるか

**列（すべて実測値）**: 構造＝構造異性体の数／場所あり＝立体が分かれる場所を1つ以上もつ種の数／
場所の総数＝それらの場所の個数の合計／**立体込み＝立体異性体まで数えた総数（＝段2の答え）**。

| 分子式 | 構造 | 場所あり | 場所の総数 | **立体込み** | 差 | 畳み込み種 | 素朴 2ⁿ の合計 |
|---|--:|--:|--:|--:|--:|--:|--:|
| C₄H₁₀ | 2 | **0** | 0 | 2 | +0 | 0 | 2 |
| C₅H₁₂ | 3 | **0** | 0 | 3 | +0 | 0 | 3 |
| C₃H₈O | 3 | **0** | 0 | 3 | +0 | 0 | 3 |
| C₆H₁₄ | 5 | **0** | 0 | 5 | +0 | 0 | 5 |
| C₄H₈ | 5 | 1 | 1 | 6 | +1 | 0 | 6 |
| C₄H₁₀O | 7 | 1 | 1 | 8 | +1 | 0 | 8 |
| **C₅H₁₀** | 10 | **2** | 3 | **13** | +3 | **1** | **14** |
| **C₅H₁₂O** | 14 | **4** | 4 | **18** | +4 | 0 | 18 |
| **C₆H₁₂** | 25 | **8** | 10 | **36** | +11 | **1** | 37 |
| C₅H₈ | 26 | 8 | 11 | 37 | +11 | 3 | 40 |
| C₄H₈O | 26 | 9 | 11 | 38 | +12 | 1 | 39 |

**C₈H₁₀（芳香族プリセット）は 4種・場所 0**（ORDER A-6 の実測。列挙が別経路なのでここでは測り直していない）。

**鎖式に絞ったとき**（別レーンが進めている ORDER A-5 のお題）:

| 分子式 | 構造 | **立体込み** | 差 | 畳み込み種 |
|---|--:|--:|--:|--:|
| C₄H₈（鎖式） | 3 | 4 | +1 | 0 |
| **C₅H₁₀（鎖式）** | 5 | 6 | +1 | **0** |
| C₅H₁₂O | 14 | 18 | +4 | 0 |
| **C₆H₁₂（鎖式）** | 13 | 18 | +5 | **0** |
| C₅H₈（鎖式） | 9 | 13 | +4 | 1 |

### ★ 1-2a. このモードが成立するお題・空振りするお題

- **空振り（場所 0）**: C₄H₁₀・C₅H₁₂・C₃H₈O・C₆H₁₄・C₈H₁₀ ＝ **現行7問のうち5問**。
  段1 の答えが「1つも無い」になり、段2 は構造の数をそのまま書き写すだけになる
- **細い**: C₄H₈（1件）・C₄H₁₀O（1件）＝ **1つ見つければ終わり**
- **成立する**: **C₅H₁₀（2件・うちメソ体1）／C₅H₁₂O（4件）／C₆H₁₂（8件）**

⚠ **現行7問だけでは、このモードは 5/7 で空振りする。**別レーンのお題追加が前提である
（ORDER A-5 決定10・A-6 決定14）。

### ★ 1-2b. 「鎖式に絞る」と C₅H₁₀ のメソ体が消える（別レーンとの正面衝突）

**C₅H₁₀ で場所をもつ2件は、1件が鎖（2-ペンテン）・1件が環（1,2-ジメチルシクロプロパン）。**
ORDER A-5 は「鎖式に絞ったお題」を勧めており、別レーンがそれを進めている。
**鎖式に絞ると 1,2-ジメチルシクロプロパンは落ちる ＝ ユーザーが出したいメソ体が消える。**

```
C₅H₁₀（全 10種）      場所あり2件・畳み込み1件・立体込み 13   ← メソ体が居る
C₅H₁₀（鎖式のみ 5種） 場所あり1件・畳み込み0件・立体込み  6   ← メソ体が消える
```

同じことが **C₆H₁₂** でも起きる（全 25種なら畳み込み1件＝1,2-ジメチルシクロブタン。
鎖式13種なら畳み込み **0件**）。⚠ **ユーザーの「メソ体は出題したい C₅H₁₀ で絡む」と、
別レーンの「鎖式に絞る」は同時に成り立たない。**§9 の決めてほしいこと 3 で挙げる。

### 1-3. C₅H₁₀ のメソ体 —— 居る。アプリは 3 と数えられる

**居る。**`enumerateConstitutionalIsomers(['C','C','C','C','C'], 10)` の 10 種のうち5番目が
1,2-ジメチルシクロプロパン（環の3炭素のうち2つにメチル）。

```
1. [鎖] 2-メチル-2-ブテン            場所なし  → 1種
2. [環] 1,1-ジメチルシクロプロパン   場所なし  → 1種
3. [鎖] 3-メチル-1-ブテン            場所なし  → 1種
4. [鎖] 2-メチル-1-ブテン            場所なし  → 1種
5. [環] 1,2-ジメチルシクロプロパン   不斉炭素2 → **3種**（2²=4 から畳み込み）★
6. [鎖] 2-ペンテン                   C=C 1本   → 2種
7. [環] メチルシクロブタン           場所なし  → 1種
8. [鎖] 1-ペンテン                   場所なし  → 1種
9. [環] エチルシクロプロパン         場所なし  → 1種
10.[環] シクロペンタン               場所なし  → 1種
                                              合計 **13種**
```

★ **`countStereoisomers` は 1,2-ジメチルシクロプロパンを 3 と数える**（`naive=4 / count=3 / folded=true`）。
**座標を1つも見ずに数えている**（総当たりのパリティを `canonicalStereoCode` に流して重複を除く）ので、
**図の描き方に左右されない**。⚠ ハース環や軸そろえの読み取りとは無関係に動く ＝ **段2 の答えは信用してよい**。

同じ形の畳み込みが **1,2-ジメチルシクロブタン（C₆H₁₂・4→3）** にもある。

### 1-4. 畳み込みの「理由」も、新しい化学を書かずに見分けられる

段2 でメソ体の話をするには、アプリが**なぜ畳み込んだか**を知らねばならない。
`countStereoisomers` は `folded`（畳み込んだか）しか返さないが、**判定はもう在庫にある**:

`chemistry.js:1890` の **`mirrorStereo(stereo)`** は「原子パリティを全反転・C=C の syn/anti は不変」を返し、
コメントに自分でこう書いている:

```
canonicalStereoCode(mol, s) === canonicalStereoCode(mol, mirrorStereo(s)) なら
その分子はアキラル（メソ体の検出に使える）。
```

これを畳み込みの 5 件に当てて実測した（「鏡像が自分自身」＝アキラルな立体異性体の数）:

| 分子 | 2ⁿ → 種類 | 鏡像が自分自身 | キラル | 見分け |
|---|---|--:|--:|---|
| 酒石酸 | 4 → 3 | **1** | 2 | **メソ体** |
| 1,2-ジメチルシクロプロパン | 4 → 3 | **1** | 2 | **メソ体** |
| 1,2-ジメチルシクロブタン | 4 → 3 | **1** | 2 | **メソ体** |
| 乳酸3分子の環状エステル | 8 → 4 | 0 | 4 | 環の回転対称 |
| トリオレイン | 8 → 6 | 6 | 0 | 同じ鎖3本の入れ替え |
| （対照）2-ブテン | 2 → 2・畳み込みなし | 2 | 0 | — |
| （対照）乳酸 | 2 → 2・畳み込みなし | 0 | 2 | — |

→ **判定規則**（新規の化学ゼロ・既存の2関数の組み合わせだけ）:

```
畳み込みがある かつ 不斉炭素が1個以上 かつ 鏡像が自分自身の種が1個以上   → 「メソ体」と名指す
畳み込みがある それ以外                                                 → 一般の文（下の (c)）
```

⚠ トリオレインは「鏡像が自分自身」が6件あるが**不斉炭素0個**なので、この規則ではメソ体と呼ばない。正しい。

### 1-5. 既存の判定でどこまで賄えるか —— **新しい化学は1行も要らない**

| 要るもの | 在庫 | 場所 |
|---|---|---|
| 不斉炭素はどれか | **`Molecule.isAsymmetricCarbon(id)`** | chemistry.js（`window` 公開はプロトタイプ経由） |
| シス/トランスの取れる C=C はどれか | **`_bondGeoRefs(mol, bond)`**（環内 C=C を `_bondInRingForStereo` で除外済み） | chemistry.js:1929 |
| その2つをまとめて返す | **`stereoUnitsOf(mol)` → `{ centers[], bonds[][] }`** ★ | chemistry.js:3877・`window` 公開済み |
| 立体込みの総数 | **`countStereoisomers(mol, limit)`** | chemistry.js:3902・`window` 公開済み |
| メソ体か否か | **`mirrorStereo` ＋ `canonicalStereoCode`**（§1-4） | chemistry.js:1890 / 公開済み |
| 原子に印を付けて描く | **`atom.isAsymmetricMarked` ＋ `renderAtom` のオレンジ** | game.js:5337 / 7194 |
| 結合をタップして拾う | **`reshapeBondUnderPoint(x, y)`**（整形モードが使っている） | game.js:4493 |

★ **`stereoUnitsOf` が段1の正解そのもの。**「不斉炭素」と「C=C」を別々に集める必要はない。
**この1本を正解の出どころにする**（2か所で数え直さない）。

**新規に書くもの**は次の3つだけで、どれも小さい:

1. `stereoFoldReason(mol)` … §1-4 の規則。純関数・`chemistry.js`
2. `bond.isStereoMarked` … 結合に付ける印（1ビット）。`serializeState` に足す
3. 採点1本（§8 の共有部品）

### ★ 1-6. 実測で見つかった穴 —— アレンの数え方がアプリで間違っている

**C₅H₈ を段2の対象にすると、アプリの答えが教科書と食い違う。**

| 分子 | アプリ | 正しくは | なぜ |
|---|--:|--:|---|
| **2,3-ペンタジエン**（CH₃-CH=C=CH-CH₃） | **3種** | **2種** | 累積二重結合は**軸不斉**で、エナンチオマー1対。アプリは C=C を2本の独立なシス/トランスとして数え、そのうえで畳み込んで 3 にしている |
| **1,2-ペンタジエン**（CH₂=C=CH-CH₂CH₃） | **2種** | **1種** | 中央の炭素は sp（直線）。この C=C にシス/トランスは無い |

`_bondGeoRefs` は「環に含まれない C=C で、両端に基準置換基がある」だけを見ており、
**隣の C=C（累積）を見ていない**。⚠ これは**このモードのバグではなく既存の穴**だが、
**総数を答えさせる面を作ると、いままで表に出なかったこの穴が答えとして画面に出る。**

→ **C₅H₈ はこのモードのお題にしない**（ORDER A-5 決定11「アレンを外す」が決まるまで）。
決定11 で列挙からアレンを外せば、この穴はこのモードの視界から消える（穴自体は残るので別途扱う）。

---

## 2. ★ ユーザーの言葉を読み直す —— 芯に食い違いが1つある

### 2-1. ユーザーの原文

> **実際の問題で出される可能性があるのは、立体異性体も含めて何種類？というパターンです。これについては、
> 不斉炭素を指摘させることでほぼ同等の内容を問うことができると思いますがどうですか？解説で補足すればよい**
>
> **メソ体は出題したいC5H10で絡みますね**
>
> **現在問題文に何種類か明示しているので、それを廃して、立体異性体（不斉炭素原子）を問うモードでは
> 異性体の総数を答えさせるのがよさそうです**

### ★ 2-2. 「何種類？」には別の数が2つある。統合レーンの出発点は片方だけ

| | 問い | C₅H₁₀ での答え | いまどこにあるか |
|---|---|--:|---|
| **(A) 分子式まるごと** | 「C₅H₁₀ の異性体は立体異性体も含めて何種類か」 | **13** | **書き出し練習**（お題ボタンに `C₅H₁₀（10種）` と**構造の数**が出ている） |
| **(B) 1つの分子** | 「1,2-ジメチルシクロプロパンの立体異性体は何種類か」 | **3** | **`StereoCountQuiz`**（4択。⚠ **総数は問題文に明示していない**） |

**統合レーンは (B)（＝ B-1「立体異性体は何種類？は測定として弱い」）を出発点に置いた。**
しかし**ユーザーの3つ目の文が指しているのは (A)**である ——
「現在問題文に何種類か明示している」は、**書き出し練習のお題ボタンの `（N種）`** のこと。
`StereoCountQuiz` は種類数を問題文に書いていない（選択肢に並ぶだけ）ので、「それを廃して」の対象になり得ない。
そのうえ **`DESIGN_practice_revision.md` §2 の U1 案3（総数を隠す）が却下されたのも、
お題ボタンの `（N種）` の話**だった —— 発注元が §2 U1 を突き合わせ先に選んだこと自体が (A) を指している。

→ **このモードの土台は `StereoCountQuiz` ではなく、書き出し練習（`IsomerPractice`）である。**
⚠ **B-1 の「89% が 2種」は、このモードを作っても直らない。**B-1 は別件として残る
（ORDER B-1 の ①重み付け・②データ追加。§10 の「やらないこと」）。

### 2-3. 統合レーンの読みのうち、追認するもの・補正するもの

| 統合レーンの読み | 判定 | 実測 |
|---|---|---|
| 2段構えにする | **追認** | 場所の数から総数は出ない（C₅H₁₀ は場所3個・総数13）。段1だけでは (A) に届かない |
| 不斉炭素だけでなく C=C も指させる | **追認・件数を補正** | 不斉炭素0個は **19 ではなく 22 件**（C=C 2〜3本を数え落としている） |
| 「2つ指摘したのに3種」が見どころ | **追認** | 1,2-ジメチルシクロプロパンで実在。アプリも 3 と数える |
| 指し方が2種類になる。**ここをどう1つの操作にまとめるか** | ⚠ **難所ではない** | **両方をもつ分子はプール169件・お題の全異性体を通じて 0 件**（§1-1・§1-2）。1つの図で2種類が混ざる画面は作らなくてよい。**本当の難所は別**（§5-3 のタップの取り合い） |
| メソ体はモードの中で扱う対象になった | **追認** | ただし ⚠ **鎖式に絞ると消える**（§1-2b） |

### ★ 2-4. ユーザーの「不斉炭素を指摘させれば**ほぼ同等**」は、実測では同等にならない

ユーザー自身が「ほぼ」と書いているが、**どれくらい足りないか**を数で置いておく:

- C₅H₁₀ で場所を過不足なく指せた人が組み立てられる数は **1×8 ＋ 2 ＋ 4 ＝ 14**。**正解は 13**
- **足りない1は、指摘では絶対に出てこない**（不斉炭素は2つあり、それは正しい）
- つまり **段1 は段2 の必要条件であって十分条件ではない**。ユーザーの「解説で補足すればよい」は、
  **補足ではなく段2 そのもの**でなければ届かない。→ **統合レーンの2段構えを採る**

---

## 3. 決定: 書き出し練習の**中に**段を2つ足す（面もモードも増やさない）

`DESIGN_isomer_practice.md` §13-1「**3つ目のモードを作らない。既にある2つの面に割り当てる**」を守る。

```
お題を選ぶ  →  描く  →  ☆ 立体の場所（段1）  →  総数を書く（段2）  →  🔍 答え合わせ
                 ↑ ここまでは今と同じ                                     ↑ ここで全部いっぺんに締める
```

- **作業帯に増えるボタンは `☆ 立体の場所` の1つだけ**（異性体側の帯は 4 → 5 になる）
- **段2 の入力欄は、段1 を一度でも開いたあとにだけ出る**（§4-2）
- **`🔍 答え合わせ` は 1問1回のまま**（§15-5 の決着を動かさない）。構造・場所・総数を同時に凍結する

### 3-1. 決定 S1: 段1 だけで止めてよい（M1 の停止点）

**段1 は単独で製品として一貫する。**「この分子式の異性体のうち、不斉炭素をもつのはどれか」
「シス-トランスが書けるのはどれか」は高校の設問そのもので、総数を聞かなくても問いになる。

→ **M1 ＝ 段1 だけ**（`☆ 立体の場所` と採点表の1列）。**M2 ＝ 段2（総数）を足す。**
⚠ **M1 で止めた場合、お題ボタンの `（N種）` は今のまま**（総数を答えさせないので隠す理由が無い）。
**表記に手を入れるのは M2 に入るときだけ。**

### 3-2. 決定 S2: このモードは「つまみ」ではなく**お題**として名乗る

ORDER A-5 が芳香族回（`aromaticOnly`）で確立した形をなぞる ——
**「宣言した出題」を1行のボタンで名乗る**（`stereoAsked` を1つ足す）。

| 案 | 中身 | 害 |
|---|---|---|
| ①つまみ（☑ 立体まで答える） | 既存お題のどれでも立体の段が付く | ⚠ **5/7 のお題で空振りする**（§1-2a）。つまみを入れた人が「場所は無い」を5回引く |
| **②お題（推奨）** | `C₅H₁₀（立体まで・構造 10種）` のようなボタンを足す | 同じ式が2行出る（`C₅H₁₀（10種）` と並ぶ）＝ ORDER A-5 案② が既に飲んだ害 |
| ③新しいモード | — | ✗ §13-1 が禁じている |

**②を推奨。**空振りするお題を出さないことが、このモードでは**設計ではなく数の問題**として効く。

---

## 4. 段1 —— どこで立体が分かれるか

### 4-1. 決定 S3: **自分の描いた図に印を付ける**。原子と結合を1つのモードで指す

`☆ 立体の場所` を押すと**印モード**に入り、キャンバスのタップが「印を付ける」に変わる。

| タップ先 | 何が起きる | 使う在庫 |
|---|---|---|
| **炭素原子** | `atom.isAsymmetricMarked` をトグル（オレンジの印） | `asymmetricMode` の分岐と `renderAtom` |
| **結合** | `bond.isStereoMarked` をトグル（新規の1ビット） | `reshapeBondUnderPoint` で拾う |
| 炭素以外の原子・何も無い所 | **黙って何もしない** | — |

⚠ **判定は「印の場所が合っているか」だけ。**どちらがシスでどちらが R かは**問わない**
（`DESIGN_isomer_practice.md` §15-4「立体異性の採点はやらない」の線の内側。§10 で再確認する）。

**なぜ「数を入れる」ではないか**: ユーザーの原文は「不斉炭素を**指摘させる**」。
数だけなら「1個」と答えてどこか分からないまま通れる ＝ §1-1 の「2と答えれば9割」と同じ病気を作る。

**なぜ「答え合わせの表の中でタップ」ではないか**: 答え合わせは**正解の図が並ぶ面**なので、
そこで指させると「まだ描けていない異性体の場所」まで指せてしまい、
**描くこととつながらない**。⚠ ただし**答え合わせのあとに `☆` で正解の図に印を重ねて見せる**のは別問題で、
これは **ORDER A-6 案①（見せるだけ）** そのもの。**両方あってよい**（§8）。

### 4-2. 決定 S4: 印モードは**印を消さずに何度でも直せる**。締めるのは答え合わせだけ

`☆ 立体の場所` は ON/OFF のトグル。OFF にしても印は残る。
**締める（＝採点する）のは `🔍 答え合わせ` を押したときだけ**で、それは1問1回。

⚠ **練習を始めたときに印はゼロから**（前の分子で付けた印が答案用紙に持ち越されない）。
これは完了条件（§9-2）で見張る。

### ★ 4-3. 実装の落とし穴 —— **結合のタップは判定線に食われる**

`BUGNOTE_touch_ipad.md` S6 と同じ型の事故が**必ず起きる**。
`svg-bond-hitbox` は `stroke-width:20` で、`pointerdown` の冒頭に `e.stopPropagation()` がある。
**結合の中央十数 px はキャンバス側のモード分岐に一度も届かない。**しかも無反応では済まず、
離したときの `click` が次数トグルへ落ちて **C=C が C≡C に化ける**（整形モードで実測されている）。

→ **`tapHasOtherMeaning()` に印モードを足すこと**（game.js:5973。一覧は1か所にまとまっている）。
`reshapeMode` / `asymmetricMode` / `haworthMode` が並んでいる列に1つ足すだけ。
**足し忘れると「印を付けようとしたら結合が三重になる」**という、いちばん質の悪い壊れ方をする。

### 4-4. 段1 の採点と、返す言葉

**行 ＝ 1つの構造異性体**（答え合わせの3列表と同じ粒度）。`stereoUnitsOf` の戻り値と印を突き合わせる。

| 印の状態 | 結果 | 返す言葉（実物） |
|---|---|---|
| 過不足なし・場所が1つ以上 | ○ | `立体が分かれる場所は 2 か所。合っています。` |
| 過不足なし・場所が0 | ○ | `この構造には立体が分かれる場所がありません。指さなかったのは正解です。` |
| 足りない | △ | `あと 1 か所あります（この図の C=C は、両端がちがう基なのでシス・トランスが書けます）。` |
| 余分 | △ | `印のうち 1 つは立体が分かれる場所ではありません（4つの基のうち 2 つが同じです）。` |
| **描けていない構造** | — | `この構造を描いていないので、場所も採れていません。` |

⚠ **突き合わせは `canonicalCode` の一致で行う**（`answerPairs` と同じ）。**並び順で突き合わせない**
（§12-4「①②③ は識別子ではない」）。

---

## 5. 段2 —— 立体込みの総数

### 5-1. 決定 S5: 数を1つ書かせる（4択にしない）

作業帯に `立体異性体も含めた総数: [    ] 種` を出す。**選択肢は出さない。**
⚠ 4択にすると `StereoCountQuiz` と同じになり、**2ⁿ を選ぶだけの作業**に戻る（quiz.js の二糖の教訓と同型）。

### ★ 5-2. 段1 の答えから段2 のヒントを出す —— 出せる。**掛け算の表**にする

統合レーンの問いへの答え: **出せる。しかも段1 を締めたあとにしか出せない**（段1 の答えが要るため）。

`💡 次のヒント` の段として、**段1 の正解を並べた掛け算の表**を出す:

```
立体が分かれる場所は、全部で 3 か所ありました。構造ごとに数えると:

  ① 2-メチル-2-ブテン              場所なし        →  1
  ② 1,1-ジメチルシクロプロパン     場所なし        →  1
  ③ 3-メチル-1-ブテン              場所なし        →  1
  ④ 2-メチル-1-ブテン              場所なし        →  1
  ⑤ 1,2-ジメチルシクロプロパン     不斉炭素 2 つ   →  2 × 2 = 4 ？
  ⑥ 2-ペンテン                     C=C 1 本        →  2
  ⑦ メチルシクロブタン             場所なし        →  1
  ⑧ 1-ペンテン                     場所なし        →  1
  ⑨ エチルシクロプロパン           場所なし        →  1
  ⑩ シクロペンタン                 場所なし        →  1
                                                  合計  14 ？

⚠ この足し算のどこか 1 行が、これでは多すぎます。
```

★ **最後の1行が仕掛け。**「14 ？」と出したうえで**どの行かは言わない**。
これで「場所を全部指せたのに答えが合わない」状態を**わざと作る** ＝ 畳み込みに自分で気づく余地を残す。

⚠ **ヒントは押した段数だけ減点**（§15-5 の規則をそのまま使う）。この表は1段ぶん。

### 5-3. 決定 S6: 番号（①②③）は**この表の中でだけ**使う

`DESIGN_isomer_practice.md` §12-4 が「①②③ は識別子ではない（安定しない）」と釘を刺している。
この表はキャンバスの並びに合わせて**毎回作り直す**（採点と同じく毎回ゼロから）。
⚠ **表の番号を保存したり、採点表の行と番号で突き合わせたりしない。**

---

## 6. ★ メソ体で何を言うか（文面の実物）

**出す場所**: 段2 を外したときの答え合わせ。**出す条件**: §1-4 の判定でメソ体と分かった行だけ。

### (a) 総数を外したとき、まず出す1行

```
立体異性体も含めた総数は 13 種です（あなたの答え: 14 種）。
場所の指摘は合っていました。ちがったのは「2 か所あれば 4 種」の掛け算のほうです。
```

⚠ **「場所は合っていた」を先に言う。**段1 を正しくやった人を、段2 の誤りで丸ごと否定しない。

### (b) メソ体の解説（1,2-ジメチルシクロプロパン。**この文面がそのまま画面に出る**）

```
⑤ 1,2-ジメチルシクロプロパン ―― 2 か所あるのに 3 種

不斉炭素は 2 つで、指摘は合っています。ふつうならそれぞれ 2 通りで 2 × 2 = 4 種ですが、
この分子は 3 種しかありません。

2 つのメチル基が環の**同じ側**にあるとき（シス）
    分子の真ん中に鏡の面ができ、鏡像が自分自身と重なります。
    左右の不斉炭素が打ち消し合っていて、**全体としては 1 種**です。これがメソ体です。
2 つのメチル基が環の**反対側**にあるとき（トランス）
    鏡の面ができず、鏡像は自分自身と重なりません。**鏡像の対で 2 種**です。

    1（シス・メソ体） ＋ 2（トランスの対） ＝ **3 種**

★ 不斉炭素の数だけでは種類数は決まりません。
   数えたあとに「分子の中に鏡の面ができないか」を確かめること。
```

### (c) メソ体ではない畳み込み（環の回転対称など）の一般文

```
⑧ 乳酸3分子の環状エステル ―― 3 か所あるのに 4 種

不斉炭素は 3 つで、ふつうなら 2³ = 8 種ですが、この分子は 4 種しかありません。
環に 3 回の回転対称があり、数え始める位置がちがうだけの組（RRS・RSR・SRR など）が
同じ分子にまとまるからです。
```

⚠ **(c) は `learn.js` の `stereoPractice` にある `foldNote` とまったく同じ文**。
**書き写さず、`foldNote` を共有すること**（ORDER A-6「同じ数の説明が2か所に出るので文言をそろえる」）。

### (d) 畳み込みが無かったとき（多数派）

```
⑥ 2-ペンテン ―― C=C が 1 本なので 2 種（シス形とトランス形）。掛け算どおりです。
```

---

## 7. ★ 「総数を書かない」は既存の決定に触れる —— ①②③への答え

`DESIGN_practice_revision.md` §2 の **U1 案3（総数を隠す）は却下**されている。理由は4つ。

### 7-1. ★ 決定 S7: 隠すのは **「立体込みの総数」だけ**。構造異性体の数は今までどおり出す

**これが ①②③ すべてへの答えである。**§2-2 のとおり **(A) と (B) は別の数**で、
**お題ボタンに出ているのは構造異性体の数**。段2 の答えは**立体込みの総数**で、
**これはいまどこにも表示されていない**。つまり**隠す必要があるのは、まだ表示していない数だけ**。

```
いまのお題ボタン        C₅H₁₀（10種）              ← 構造異性体の数
新しいお題ボタン        C₅H₁₀（立体まで・構造 10種） ← 構造の数は出す。総数（13）は出さない
```

| 却下理由 | 新モードで当たるか | 答え |
|---|---|---|
| **①選ぶ前に問題の重さが分からない** | **当たらない** | 構造 10 種という重さは**そのまま出ている**。⚠ むしろ「立体まで」の1語で**重いほう**だと分かる |
| **②満点が分からないまま解く** | ★ **当たる** | §7-2 |
| **③クリア済みの ✓ と噛み合わない** | **当たらない** | ✓ の基準は「構造 10 種を全部描き、段2 に正解した」。**両方とも人が確かめられる**（構造の数は見えている・段2 は当たれば当たったと分かる） |
| **④3か所を同時に変えることになる** | **当たらない** | **新しいお題ボタンだけが新しい表記**。既存の異性体6問・立体4問・アルキル基の表記は**1文字も変えない** |

⚠ **既存モードの表記は変えない。**この文書は既存の `（N種）` を1つも触らない。

### 7-2. ★ ②への答え —— 満点 ＝ **構造異性体の総数 N ＋ 1**

**式**（§15-5b の形をそのまま延長する）:

```
満点   = N ＋ 1          （N = そのお題の構造異性体の数。＋1 は段2 の1問ぶん）
素点   = 正しく描けた種類数 ＋（総数が当たれば 1）
スコア = 素点 − ヒント到達段数（下限 0）
```

- **N は画面に出ている**ので、**満点は最初から分かる**（②が消える）
- **＋1 は定数**。何も漏らさない
- **段1（場所の指摘）は点にしない**。理由は次の 7-3

### ★ 7-3. なぜ段1 を点にしないか —— **点にすると答えが漏れるか、無操作で点が入る**

3つ試して、どれも壊れた:

| 案 | 満点の作り方 | 壊れ方 |
|---|---|---|
| ①場所1つにつき1点 | 満点 ＝ **場所の総数（3）** | ⚠ **満点が段1の答えそのもの。**「3か所ある」と最初に教えることになる |
| ②構造1つにつき1点 | 満点 ＝ N（10） | ⚠ **何も指さなければ 8/10 取れる**（場所を持つ構造は2件だけ）。 §1-1 の「2 と答えれば9割」と**まったく同じ病気**を、直そうとしている面の中に作る |
| ③段1 全体で1点 | 満点 ＝ 1 | 1か所落としただけで 0 点。**部分点が無い段は「やらないほうが安い」と読まれる** |

→ **決定: 段1 は点にせず、段2 の関門にする。**
`☆ 立体の場所` を一度も開いていないと **段2 の入力欄が出ない**。点で釣らずに順序を守らせる。
⚠ **採点表には ○△ を出す**（点にしないだけで、正誤は必ず返す。§4-4）。

### 7-4. ★ ユーザーの言葉との差（確認が要る）

ユーザーは「**現在問題文に何種類か明示しているので、それを廃して**」と書いている。
**この設計は「構造異性体の数」は廃していない**（廃すと満点が作れないため）。
⚠ **「それ」が構造の数まで含むなら、この設計は違う。**§9 の決めてほしいこと 5 で挙げる。

---

## 8. ★ A-6 との関係 —— 部品を1つにする。その境界

ORDER A-6 の判断13・14 は「足す」で確定している。**別々に作ると判定が2か所に散る。**

### 8-1. 何が同じで、何が違うか

| | **A-6**（構造異性体の書き出しに不斉炭素の段） | **このモード**（立体の場所＋総数） |
|---|---|---|
| 正解の出どころ | `stereoUnitsOf(mol)` | **同じ** |
| 印の付け先 | **答え合わせの正解図**（案①・見せるだけ） | **自分の描いた図**（段1・答えさせる） |
| 総数 | 聞かない | **聞く** |
| お題 | C₅H₁₂O ほか | C₅H₁₀ ほか |

**A-6 案① と このモードの段1 は「見せる」と「答えさせる」で表裏**。
ORDER の勧め「①を先に、②を後から」に沿うと、**A-6 ① → このモードの段1 → 段2** が一直線になる。

### ★ 8-2. 部品の境界（3層。**判定は1か所**）

```
chemistry.js（化学。DOM 非依存）
  ├ stereoUnitsOf(mol)          … 既存。★ 正解の唯一の出どころ。両方がこれだけを呼ぶ
  ├ countStereoisomers(mol)     … 既存。段2 の正解
  └ stereoFoldReason(mol)       … 新設（§1-4）。'meso' | 'symmetry' | null を返す純関数

learn.js（採点。DOM 非依存にできる）
  └ gradeStereoPoints(mol, marks) → { ok, missingCenters[], missingBonds[], extra[] }
      ★ **これ1本だけ**。A-6 の段（正解図に印を重ねる）も、このモードの段1 も、これを呼ぶ
      marks = { centers:Set<atomId>, bonds:Set<'i_j'> } ← 呼ぶ側が図から集めて渡す

game.js（操作と絵）
  ├ 印モード（stereoPointMode）… 段1 専用。⚠ tapHasOtherMeaning() に足す（§4-3）
  ├ atom.isAsymmetricMarked     … 既存の印をそのまま使う（見た目も意味も自由モードと同じ）
  └ bond.isStereoMarked         … 新設の1ビット。serializeState に足す
```

**境界の言い方**: **「どこが立体の場所か」を知っているのは `chemistry.js` だけ。
「印が合っているか」を知っているのは `gradeStereoPoints` だけ。
`game.js` は印を集めて渡し、返ってきた結果を描くだけ。**

⚠ **A-6 を案①（見せるだけ）で作るときも `gradeStereoPoints` を通すこと。**
「見せるだけだから `stereoUnitsOf` を直接呼べばいい」とすると、
**そこが2つ目の判定になり、片方だけ直る事故の種になる**（このリポジトリで繰り返している罠）。

### 8-3. 共有できないもの（分けたままにする）

- **お題の表記**。A-6 のお題は `（N種）` を出したまま（総数を聞かないので隠す理由が無い）
- **スコアの式**。A-6 は §15-5b のまま。このモードだけ `N＋1`
- **`stereoPractice`（立体の書き出し・4問）**。あちらは「立体を**描く**」。印は付けない

---

## 9. 決めてほしいこと —— ✅ **決着（v1435）**

| # | 決めること | 推奨 | ✅ 決着 |
|---|---|---|---|
| **1** | 土台は書き出し練習（A）か、`StereoCountQuiz`（B）か（§2-2） | **A（書き出し練習）** | **A**。`IsomerPractice` の中に段を2つ足した |
| **2** | ★ **お題に C₅H₁₀（環を含む全10種）を足すか** | **足す** | **足した**（`problems[19]`）。⚠ ついでに C₅H₁₂O（14種・場所4）も入れた |
| **3** | ★ **鎖式に絞るお題と、このモードのお題を分けるか**（§1-2b） | **分ける** | **分けた**。`stereoAsked` の回に `skeleton` は付けない（メソ体を落とさない） |
| **4** | C₅H₈ をこのモードのお題にするか（§1-6） | **しない** | **していない**（アレンの数え方の穴に触らない） |
| **5** | ★ **「それを廃して」は構造異性体の数まで含むか**（§7-4） | 含まない | ★ **含む。**ユーザー判断「**満点は採点時に示せばよいのでは？**」で §7-1 の制約が消えた。→ **§9-5a** |
| **6** | 満点を `N＋1` にするか（§7-2） | **する** | **した**（`IW25` が凍結）。段1 は点にしていない |
| **7** | M1（段1 だけ）で一度止めるか（§3-1） | 止める | ★ **止めなかった。**総数を隠す形が決まった時点で、止める理由（満点が作れない）が消えた |
| **8** | A-6 を案①（見せるだけ）から入るか | **入る** | **まだ**。段1 が先に入った。⚠ 足すときも `gradeStereoPoints` を通すこと（§8-2） |
| **9** | ハースの糖（新規・ユーザー判断） | — | **α/β だけ**（`axis: 'anomeric'` で2種）。32種にはしない |

### ★ 9-5a. 決めてほしいこと 5 の帰結 —— ①③ にどう答えたか

**②（満点が分からないまま解く）は「採点時に示す」で解けた。**
⚠ **① と ③ は残る。**答えは **`DESIGN_isomer_practice.md` §18-4a** に書いた。要点だけ:

- **①（選ぶ前に重さが分からない）** → **重さは隣のボタンが名乗っている。**
  「立体まで答える回」は選択画面の別枠に置き、上の群に `C₅H₁₀（10種）` が並んでいる。
  この回は「同じ書き出し ＋ 総数の1問」であって、未知の重さではない。
  ⚠ **裏返すと構造の数は隣から読める ＝ 隠しきってはいない。**
  「10 を完全に伏せたい」なら素の C₅H₁₀ の回を畳む必要があり、**それは別の決定**（こちらでは決めていない）
- **③（クリア済みの ✓ と噛み合わない）** → **✓ の基準を段2 まで含め、満点は ✓ が付く瞬間に必ず出る。**
  ✓ ＝「構造を全部描き、かつ総数に当てた」（鍵は `chemIsomerPractice.<式>@stereo`）。
  答え合わせは1問1回で、そこに `スコア X点 / N＋1点満点` が式ごと出る
  ＝ **✓ の条件を知らないまま ✓ が付くことはない**。
  ⚠ **残る差**: 1回目を解き終えるまで「あと何個か」が分からない。
  これはこの回の狙いそのものなので害として飲む（数が要る人にはヒント段1 が −1点で買える）

---

## 10. やらないこと（範囲外）

- **R/S・シス/トランスの言い分けを答えさせる**。指すのは「場所」まで。
  `DESIGN_isomer_practice.md` §15-4「立体異性の採点はやらない」の線の**内側にとどまる**
  （⚠ ORDER B-2 の決めてほしいこと26 への答え: **越えていない。不斉炭素を指すのは構造の話**）
- **既存お題の `（N種）` 表記を変える**（§7-1 ④）
- **`StereoCountQuiz` の作り替え**。B-1（89% が「2種」）は**このモードでは直らない**別件。
  ORDER B-1 の ①重み付け・②メソ体のデータ追加はそのまま残る
- **アレンの数え方を直す**（§1-6）。既存の穴なので別途。ここでは**お題に選ばない**ことで避ける
- **立体を含めた「書き出し」**（立体異性体を全部描かせる）。それは `stereoPractice` の仕事
- **`canonicalCode` / `enumerateConstitutionalIsomers` に立体を入れる**
  （`DESIGN_stereochemistry.md` §8.3「異性体列挙には立体を入れない」を動かさない）

---

## 11. 完了条件と否定対照（何が壊れたら気づけるか）

帯は `IW` の続き。⚠ **`IW17` は ORDER A-3 が予約済み**なので **`IW18` から**取る（台帳は `assembler/tests.js` 冒頭）。

| # | 検査 | いまのコードで赤くなるか |
|---|---|---|
| **IW18** | `stereoUnitsOf` で C₅H₁₀ の 10 種を数えると、場所をもつのは**ちょうど2種**（1,2-ジメチルシクロプロパン＝不斉2・2-ペンテン＝C=C 1） | 緑（実測どおり）＝ **仕様の固定** |
| **IW19** | `countStereoisomers(1,2-ジメチルシクロプロパン)` が **`{naive:4, count:3, folded:true}`** | 緑＝固定。畳み込みを壊すと赤くなる |
| **IW20** | C₅H₁₀ の立体込み総数が **13**（10種の `count` の合計） | 緑＝固定 |
| **IW21** | §1-4 の規則で、酒石酸・1,2-ジメチルシクロプロパン・1,2-ジメチルシクロブタンが `'meso'`、乳酸3分子の環状エステルが `'symmetry'`、トリオレインが **`'meso'` にならない** | `stereoFoldReason` 未実装なので**赤**（＝実装の的） |
| **IW22** | `gradeStereoPoints` が「印なし・場所なし」を ○、「印なし・場所あり」を △（不足）、「余分な印」を △ と返す | 未実装なので**赤** |
| **IW23** | ★ **否定対照**: 印モード中に**結合の判定線がタップを食っていない**（`tapHasOtherMeaning()` から印モードを外すと赤くなること）。§4-3 | 実装後に、外して赤くなることを必ず確かめる |
| **IW24** | ★ **否定対照**: 段1・段2 の突き合わせを**添字で**行う実装に差し替えると赤くなる（`canonicalCode` で突き合わせているか）。`IW11` と同型 | 実装後に確かめる |
| **IW25** | 練習を始めた直後、答案用紙の印がゼロ（前の分子の印が持ち越されない）。§4-2 | 実装後 |
| **IW26** | 段1 を一度も開いていないと段2 の入力欄が無い（§7-3 の関門） | 実装後 |

⚠ **IW18〜IW20 は実装前でも緑になる**（いまの `chemistry.js` で成り立っている数だから）。
**これは無駄ではなく、「段2 の答えの出どころ」を先に固定するためのもの** ——
別レーンが列挙や畳み込みに触ったとき、このモードの答えが黙って変わることを防ぐ。

---

## 12. 実装したら正をどこへ移すか

| 節 | 行き先 |
|---|---|
| §3〜§5（段の設計・操作） | `DESIGN_isomer_practice.md` に新しい節（§16 相当） |
| §6（メソ体の文面） | 同上。⚠ `foldNote` を共有するので `learn.js` の `stereoPractice` の節にも道しるべを置く |
| §7（満点） | `DESIGN_isomer_practice.md` §15-5b の下に追記 |
| §8（部品の境界） | 同上。⚠ **A-6 の節と同じ場所に置く**（2か所に散らさない） |
| §1（実測） | この文書に残す（数の出どころ） |
| §9（決めてほしいこと） | 決着したら `DESIGN_practice_revision.md` §2 と同じ形で「✅ 決着」の表にする |

---

## 13. 測り方（再現手順）

**§1 の数はすべて `tools/stereo-point-census.js` が出す**（`DESIGN_compound_coverage.md` §2
「内訳を数え直すときは、分類器を tools/ に置いて次の人が同じ数字を出せるようにすること」に従った）。

```
node tools/stereo-point-census.js              # §1-1・§1-2・§1-4 の表がそのまま出る
node tools/stereo-point-census.js --list=C5H10 # §1-3 の10種の内訳が出る

# 中身（chemistry.js / quiz.js を vm に読む。tools/quiz-scope-census.js とまったく同じ流儀）
#   ⚠ window はサンドボックス自身にする（window:{} にすると環の判定が黙って空になる）

# 数えたもの
#   ① StereoCountQuiz のプール ＝ buildCompoundLibrary → isPolymerFragment 除外
#      → countStereoisomers(mol, UNIT_LIMIT=5) → overflow / naive<2 を除外 → canonicalCode で重複除け
#   ② お題 ＝ enumerateConstitutionalIsomers(重原子, H数, IP_ENUM_LIMIT=4000000)
#      ⚠ nodeLimit を既定の 600000 のままにすると C₆H₁₄・C₆H₁₂ が overflow になる（learn.js は 4000000 を渡す）
#   ③ 型分け ＝ stereoUnitsOf(mol) の centers / bonds の個数
#   ④ 畳み込みの理由 ＝ 全配置を canonicalStereoCode に流し、mirrorStereo と一致する種を数える
```

⚠ **アプリのコード・データ・版は1つも触っていない**（読むだけ）。
`compounds.json` / `stages.json` / `reactions.json` / `quiz-scope.json` は `JSON.parse` で読んだだけで、
書き戻していない。
