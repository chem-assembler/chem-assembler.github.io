# IUPAC 命名の確認（主鎖の明示・炭素番号の表示）設計書

**コード名: 命名の確認モード。** ユーザーの分子について「この名前は、**どの鎖を主鎖と決めて・どちら向きに番号を振った**結果か」を
キャンバス上で見せる機能。2026-08-13 ユーザー要望（「IUPAC命名の確認（主鎖の明示・炭素番号の表示）を実装したい。
構造異性体の書き出しともリンクする機能です」）に基づく、実装前の合意用ドキュメント。

関連: [DESIGN_isomer_practice.md](DESIGN_isomer_practice.md)（この機能を最初に使う場所）／
DEVELOPMENT.md の **P12-3**（`iupacName` の対応範囲。芳香族・カルボニル系は未実装）

---

## 0. なぜ設計書を別にしたか（1本にしなかった理由）

異性体の書き出しとは**持ち主が違う**から。

- 主鎖と番号は **chemistry.js が持つ化学の知識**で、使う側は異性体練習だけではない
  —— アルキル基の練習・分子モーダル（辞書引き）・自由モードの名称表示・反応の生成物、
  どれも同じものを見たがる。異性体練習の章の中に埋めると、次に
  「芳香族の系統名（P12-3 の残り）」を足す人がここを見つけられない
- 逆に、異性体の答案用紙・ヒントの段数は**あの練習だけの都合**で、命名の正しさとは無関係に変わる
- 決定的なのは、**この文書の中身のほとんどが「1つの関数に決めさせる」という約束**であること。
  約束は約束だけで読めるところに置く。使う場所の説明に混ぜると、次に使う人が約束のほうを読み飛ばす

異性体側からは §5 の API だけを参照する。**依存は 異性体練習 → 命名の確認 の一方向**に保つ。

---

## 1. いちばん危ない罠 —— 「ハイライトした主鎖」と「表示した名前」が黙って食い違う

`chemistry.js` には炭素鎖を選ぶ経路が**2つ**ある。

| 経路 | 選び方 | 使われている場所 |
|---|---|---|
| `findLongestCarbonChain(mol)` | 炭素だけの部分グラフの**最長単純パス**。同点は `_cmpCarbonPath`（原子IDの辞書順）で割る | `isomerSeriesKey`（系列分類）・M2 の主鎖ハイライト |
| `iupacName(mol)` の内部（3432〜3458 行） | **-OH を最も多く含む → 多重結合を最も多く含む → 最長** の順で候補を絞り、残った同点は「OH位置最小 → 多重結合位置最小 → **置換基数が最多** → 置換基位置最小 → 辞書順」で決める | 名前 |

**「最長の炭素鎖」は IUPAC の主鎖ではない。** 主特性基を含むこと・多重結合を含むこと・置換基が多いこと、が先に来る。
別々に計算したものを同じ画面に出せば、**ハイライトと名前が食い違う** —— しかも例外も警告も出ずに。

### 1-1. どれくらい起きるか（実測。憶測で線を引かない）

`enumerateConstitutionalIsomers` で分子を生成し、`findLongestCarbonChain` の結果と
`iupacName` が実際に使う主鎖候補（3432〜3451 行と同じ手順を再現）を**原子集合で**突き合わせた。

**(a) 書き出し練習の標準6問（C₄H₁₀ / C₅H₁₂ / C₆H₁₄ / C₃H₈O / C₄H₁₀O / C₄H₈ ＝ 25異性体）**

| | 件数 |
|---|--:|
| `iupacName` が名前を返した | 23 |
| うち **エーテル**（主鎖という概念が無い） | 4 |
| 主鎖の**炭素数が違う** | **0** |
| ★ **炭素数は同じなのに原子が違う** | **2**（2-メチル-1-プロパノール・2-メチルプロペン） |
| IUPAC 主鎖候補が同点で複数ある | 9 |

**(b) 範囲を広げて（上記＋C₅H₁₂O / C₅H₁₀ / C₆H₁₂ / C₄H₆ / C₅H₁₀O / C₄H₉Cl / C₅H₁₁Cl ＝ 169異性体）**

| | 件数 |
|---|--:|
| `iupacName` が名前を返した | 84 |
| うち エーテル | 10 |
| 主鎖の炭素数が違う | 2（2-エチル-1-ブテン: 5C対4C／2-エチル-2-プロペン-1-オール: 4C対3C） |
| ★ 炭素数は同じなのに原子が違う | **11**（2,2-ジメチル-1-プロパノール・2-メチル-1-ブタノール・2-メチル-1-ブテン・2,3-ジメチル-1-ブテン・2-メチル-1-ペンテン・2-メチル-2-ブテン-1-オール ほか） |
| 主鎖候補が同点で複数 | 31 |

### 1-2. ここから読み取るべきこと（3つとも設計を変えた）

1. **「表示する主鎖の炭素数」と「名前の幹の炭素数」を突き合わせる検査は、役に立たない。**
   食い違い 13件のうち **11件は炭素数が同じ**。標準6問にかぎれば **2件中2件が同じ炭素数** ＝
   **捕捉率 0%**。この検査を入れて「守った」と思うのが、このリポジトリが繰り返し踏んでいる
   「直したつもりで何も検査していない」の型そのもの。→ **検査は原子集合で行う**（§6）
2. **食い違いは辺境ではなく看板の上で起きる。** 2-メチル-1-プロパノール（イソブチルアルコール）は
   **主力問題 C₄H₁₀O の7種のうちの1つ**。2-メチルプロペンは C₄H₈ の5種のうちの1つ。
   「アルコールとアルケンで、枝が1本あるやつ」＝ 高校で最も出る形が、まさに壊れる形
3. **主鎖は一意ではない。** 同点候補が 31件（標準6問でも9件）ある。だから
   「IUPAC の主鎖はどれか」を後から計算し直す設計は原理的に成立しない。
   **`iupacName` が最後に選んだ 1本**を持ってくる以外に正解がない

---

## 2. 決定

### N-1. `iupacName` を `iupacNameDetail` の薄い包みにする（**採用**）

```js
function iupacName(mol) { const d = iupacNameDetail(mol); return d ? d.name : null; }
```

名前は、主鎖と番号を決めた計算の**副産物のひとつ**になる。呼ぶ側がどちらを使っても、
**同じ1回の計算の別の面**を見ていることになり、食い違いが**構造的に起こらない**。
「検査で見張る」より一段強い ——検査は入れる（§6）が、それは**戻ってしまったこと**を知らせる番人であって、
正しさの根拠は「決める場所が1つしかない」ことに置く。

**なぜ他の案を採らなかったか**

| 案 | 却下の理由 |
|---|---|
| `iupacName` の戻り値をオブジェクトに変える | 呼び出し元が広い（`lookupCompoundName`・reactor・learn・テスト F8/F9/LB 群）。**得るものが無いのに全部を書き換える**。包みなら既存の呼び出しは1バイトも変わらない |
| `iupacNameDetail()` を**別に**書く（並行実装） | これが罠そのもの。同じ規則の2つ目の実装が生まれ、片方だけ直る日が必ず来る（今日 `pointSegmentDistance` が同名で2つあった件と同型） |
| `describeStructure` に相乗りする | `describeStructure` は表示用の要約で、`iupacName` を通らない呼び出し経路がある。相乗りしても **`iupacName` を直接呼ぶ人**が別の主鎖を持ったままになる。塞ぎたい穴が塞がらない |

### N-2. 実装は「捨てている情報を捨てないようにする」だけ（大きな書き換えをしない）

いま `_iupacNameForMainChain` は、両方向を評価して `d`（採用した向き）を決めたあと、
**`d` が持っている原子列を捨てて**名前と比較用の配列だけ返している（3495〜3500 行）。

- `evalDir(order)` の戻り値に **`order`**（＝ 番号順の原子ID列）を足す
- `_iupacNameForMainChain` の戻り値に **`chain: d.order`** を足す
- `iupacName` は `named[0].name` を返すのをやめ、`named[0]` を**そのまま**詳細に載せる

**主鎖の原子列 ＝ `named[0].chain`。番号 k の炭素 ＝ `chain[k-1]`。向きは配列の順そのもの。**
「両方向で最小化」を画面側でやり直さない。やり直した瞬間に経路が2つに戻る。

### N-3. `findLongestCarbonChain` は残すが、**命名の表示から締め出す**

消さない。`isomerSeriesKey` の系列分類（表示・ヒント専用と明記済み）と、環分子の案内で使っている。
代わりに関数の頭に禁止事項を書く:

> ⚠ **これは IUPAC の主鎖ではない**（-OH・多重結合・置換基数の規則が先に来る）。
> 実測で 84件中 13件が `iupacNameDetail().mainChain` と食い違い、**うち11件は炭素数が同じ**。
> **名前を出す画面の主鎖・番号に使ってはいけない**。→ `iupacNameDetail`（DESIGN_iupac_check.md）

同じ注意書きを `isomerSeriesKey` の「表示・ヒント専用で検証には使わない」の隣にも足す
（系列分類の `chainLen` は**主鎖長ではなく最長鎖長**である、という事実を隠さない）。

### N-4. 門番 ＝ 「名前が出ないものには、主鎖も番号も出さない」

新しい対応範囲リストは**作らない**。作れば `iupacName` の実装と二重管理になり、片方だけ伸びる。

> **主鎖と番号を描いてよいのは `iupacNameDetail(mol)` が非 null を返したときだけで、
> 描くのはそれが返したものだけ。**

これ1行で、`iupacName` が既に断っているもの（環・芳香族・カルボニル・エノール形・
不飽和エーテル・複数エーテル・分岐ポリオール・ヘテロ原子・多重結合が炭素間でない・複数分子）が
**自動的に**番号なしになる。P12-3 で芳香族の命名が実装された日には、**この文書を書き換えなくても**
主鎖表示が芳香族に届く。届かせるために触る場所が無いのが正しい。

### N-5. 断り方は3種類に言い分ける（「未対応」の一語で済ませない）

`aromaticOnly` の回で「開発ログに記録しました」を生徒に見せてしまった失敗（DESIGN_isomer_practice.md §11-4・BZ5）と同じ轍を踏まない。
**正しく描けた人に不具合の顔を見せない。**

| `iupacNameDetail` の結果 | 画面 |
|---|---|
| `kind: 'chain'` | 主鎖を帯で示し、C1・C2… を振る。系統名を見出しに出す |
| `kind: 'ether'` | **エーテルは主鎖に番号をつけない。**両側のアルキル基を2色で塗り分け、それぞれの基の名前を出し、「エーテルは主鎖に番号をつけません。両側のアルキル基の名前で呼びます」と書く。★ これは未対応の言い訳ではなく**規則そのもの**で、同じ1回の計算から出るので食い違わない |
| `null` かつ **環がある**（`findAnyCycle`） | 「環が基本骨格です。環の番号づけはこのアプリではまだ扱いません」＋ 環をハイライト（既存の M2 の案内を流用） |
| `null` かつ 環がない（カルボニル・アミン等） | 「この官能基の系統名はこのアプリではまだ扱いません（名前は名称ライブラリから引いています）」 |

**エーテルの重みは実測で決めた。** 標準6問の 23件中 4件、広げた範囲では 84件中 10件。
主力の C₄H₁₀O にいたっては **7種のうち3種がエーテル**。ここで無言になると、
主力問題の半分弱で機能が消える。「未対応です」ではなく「そういう規則です」と言えるのが、
`iupacNameDetail` から出す最大の実利。

### N-6. 番号を出す画面では、**番号の出どころの名前**を必ず併記する（もう1つの食い違い）

`lookupCompoundName` は **compounds.json を先に引き、外れたときだけ `iupacName` に落ちる**。
つまり画面の名前は「イソブタン」「ネオペンタン」のような**慣用名**であることがある。
その隣に系統名の番号づけを描けば、**画面に出ていない名前の番号**を見せることになる。

→ 命名の確認では、見出しを **系統名**（`iupacNameDetail().name`）にする。
ライブラリ名が違うときだけ副見出しに `（慣用名: イソブタン）` を添える。

> **規則: 番号を描く画面には、その番号を生んだ名前を必ず同じ画面に出す。**

---

## 3. 画面（どこに出すか）

**新しいトップレベルモードは作らない。** 既存の3か所に相乗りする。

1. **自由モード / 辞書引き（分子モーダル）** … 名前の隣に `🔢 主鎖と番号を見る` トグル。
   押すとキャンバスの当該成分に帯と番号が乗る。もう一度押すと消える。**状態は残さない**（描き替えたら消える）
2. **異性体の書き出し練習の「答え合わせ」オーバーレイ** … 標準の書き方の図に番号を出す。
   ★ ここは既に `ipNumberedLayout`（「主鎖横一直線＋主鎖炭素に位置番号」・v147）が**独自に番号を振っている**。
   **これを `iupacNameDetail().mainChain` に置き換える**（＝ 3つ目の番号づけ経路を潰す。§6 の IN4 が見張る）
3. **アルキル基の練習** … 付け根 C1 から数える番号（§4）

**書き出しの最中には出さない。** 主鎖と番号は答えの一部（「その形の名前は何か」）なので、
DESIGN_isomer_practice.md §13 の面の分け方（書き出し中＝判定ゼロ）に従う。

### 3-1. 描き方

- **主鎖の帯**: 主鎖の結合に沿って太い半透明の線（`--neon-orange` 系）を敷く。原子を丸で囲む既存の
  `highlightAtoms` は**使わない** —— 丸は「この原子が問題」の語彙（不斉・エラー箇所）で既に埋まっている。
  主鎖は「この道」なので線で示す
- **番号**: 主鎖炭素の外側（分子の重心から遠い側）に `1` `2` … を置く。原子の文字（9px）と喧嘩しないよう
  少し小さく、外周へ逃がす。**分子モーダル内のサムネイルにも同じ規則で出す**
- **重なり**: 見出し（`renderMoleculeLabels`）と同じ「格子点を覆わない」制約は要らない（一時表示で、
  その間は作図を止めるため）。ただし**番号を出している間はタップで原子を置けない**ことを明示する

---

## 4. アルキル基の番号（付け根 C1）

`iupacAlkylName` も同じ罠を持つ。`best` を選んだあと **`{nm, key}` だけ返して鎖を捨てている**（3304〜3307 行）。
同じ直し方をする:

- `iupacAlkylName` の戻り値に **`chain`**（root から始まる原子ID列）を足す
- `iupacAlkylGroupName(mol, rootId)` / `iupacAlkylNameFromR(mol)` の詳細版
  `iupacAlkylDetailFromR(mol)` → `{ name, mainChain, rootId }`
- **付け根が必ず C1**（`_iupacPath(subAdj, root, t)` が root 始まりなので、この性質は既に実装が持っている）。
  番号は配列の添字そのままで、向きを選ぶ余地が無い ＝ **主鎖の場合より単純**

慣用名への写像（`IUPAC_RETAINED`: イソプロピル・sec-ブチル…）は名前だけを変える。
**鎖と番号は体系名の側のものを出す** ——「sec-ブチル」に番号は無いので、
`sec-ブチル（＝1-メチルプロピル）` と体系名を併記して番号を振る（N-6 と同じ規則）。

---

## 5. API（異性体練習など、使う側が見る面）

```js
// 非環式の炭化水素・ハロゲン化物・アルコール・エーテルの IUPAC 系統名と、
// **その名前を作るのに実際に使った主鎖・番号の向き**。対応外は null。
// ★ iupacName はこの関数の薄い包み。名前と主鎖は必ず同じ1回の計算から出る
function iupacNameDetail(mol) -> null | {
    name,          // 従来 iupacName が返していた文字列と 1バイトも変わらない
    kind,          // 'chain' | 'ether'
    mainChain,     // 番号順の炭素 ID 配列。番号 k の炭素 = mainChain[k-1]。kind==='ether' では null
    groups,        // kind==='ether' のときだけ: [{ ids:[…], rootId, name }, …]（2つ）
    locants        // { ol:[…], ene:[…], yne:[…], subs:[{loc, key, name}] } — 画面の説明文用
}

function iupacAlkylDetailFromR(mol) -> null | { name, mainChain, rootId }   // 付け根 = mainChain[0] = C1
```

**使う側の禁止事項（コメントに書く）**
- `mainChain` を受け取ったら**そのまま添字で番号にする**。並べ替えない・逆にしない・最小化し直さない
- `findLongestCarbonChain` / `isomerSeriesKey().chainLen` を番号や主鎖の表示に使わない

---

## 6. 検査と否定対照

接頭辞の帯は **`IN`（IUPAC Numbering）** を新設する（`assembler/tests.js` 冒頭の台帳で未使用を確認済み。
**実装時に台帳へ `| IN | 1〜4 | 命名の確認（主鎖と番号が名前と同じ計算から出ていること） |` を書き足す**）。

| # | 何を見るか | 落ちる直し方 |
|---|---|---|
| **IN1** | **包みの同一性**: 標準6問の全異性体＋`compounds.json` 全件で、`iupacName(mol)` と `iupacNameDetail(mol)?.name` が**完全一致**（両方 null も一致とみなす） | `iupacNameDetail` を別実装で書いた／`iupacName` に規則を足して包みを迂回した |
| **IN2** | **★否定対照（本命）**: 下の**凍結リスト**の各分子について、`findLongestCarbonChain(mol)` の原子集合が `iupacNameDetail(mol).mainChain` の原子集合と**一致しない**こと。あわせて、リスト中で**炭素数が同じもの**が 8件以上あることも assert する | 表示を `findLongestCarbonChain` に差し戻した（＝ この機能を「簡単にした」つもりの将来の直し）。**炭素数だけを見る検査に置き換えた** |
| **IN3** | **門番**: 環（シクロブタン・ベンゼン）・カルボニル（アセトン）・複数分子・エーテルで、キャンバスに**番号テキストが 0 個**であること。エーテルでは代わりに**2つの基の名前が出る**こと | 「未対応でも最長鎖くらい出しておこう」と門番を緩めた |
| **IN4** | **番号づけ経路が1本**: 番号を出した状態の SVG から番号テキストとその位置を読み、`番号 k のついた原子 = mainChain[k-1]` が全 k で成り立つこと。答え合わせオーバーレイの標準図（`ipNumberedLayout`）でも同じことを見る | 画面側で番号を振り直した／`ipNumberedLayout` の独自番号が残った |

**IN2 の凍結リスト**（実測で拾った食い違い。テストにはこの**名前**を書き、失敗時に化合物名で出す）

`2-メチル-1-プロパノール` / `2-メチルプロペン` / `2,2-ジメチル-1-プロパノール` / `2-メチル-1-ブタノール` /
`2-メチル-1-ブテン` / `2,3-ジメチル-1-ブテン` / `2-メチル-1-ペンテン` / `2-メチル-2-ブテン-1-オール` /
`2-メチル-3-ブテン-1-オール` / `3-メチル-3-ブテン-2-オール` / `3-メチル-3-ブテン-1-オール` ＝ **炭素数が同じ11件**、
`2-エチル-1-ブテン`（5C 対 4C） / `2-エチル-2-プロペン-1-オール`（4C 対 3C） ＝ **炭素数が違う2件**。

> **否定対照をどこに置いたか**: 「合っていること」を確かめる検査（IN1・IN4）だけでは、
> 両方を同じ間違え方にすれば通ってしまう。**IN2 は「間違った実装なら必ず別の答えになる具体的な分子」を
> 名指しで固定する**ので、`findLongestCarbonChain` に戻す直しは 13件の名前を挙げて赤くなる。
> さらに IN2 は**炭素数だけを見る検査への退化**も同時に禁じている（11件は炭素数が同じ）。

**性能**: `iupacNameDetail` は `iupacName` と同じ計算なので追加費用ゼロ（返す配列が1本増えるだけ）。
`compounds.json` 全件（939件）を回す IN1 は既存の F8 と同じ土俵なので、そこに相乗りしてもよい。

---

## 7. マイルストーン

- **N1: 1つの計算にする** — `evalDir` に `order` を足す／`_iupacNameForMainChain` に `chain` を足す／
  `iupacNameDetail` を作り `iupacName` を包みにする／`iupacAlkylName` にも `chain` を足す／
  `findLongestCarbonChain`・`isomerSeriesKey` に禁止事項のコメント。**画面はまだ変えない**。
  完了条件: **IN1・IN2 が緑**、F8・F9・LB 群・既存の全テストが**1件も変わらずに緑**（名前の出力が不変であること）
- **N2: キャンバスに出す** — 主鎖の帯＋番号の描画、分子モーダルのトグル、系統名と慣用名の併記（N-6）。
  完了条件: **IN3・IN4 が緑**。実機で 2-メチル-1-プロパノールと 2-メチルプロペンを描き、
  **帯が名前どおりの鎖に乗る**ことを目視（＝ 実測で壊れていた2件を人の目でも確かめる）
- **N3: 言い分け** — エーテルの2基表示、環・未対応官能基の断り文（N-5）。
  答え合わせオーバーレイの標準図を `mainChain` に載せ替える。
  完了条件: C₄H₁₀O の7種すべてで**何かしら意味のある表示が出る**（アルコール4種＝番号、エーテル3種＝2基）

---

## 8. やらないこと（範囲外）

- **芳香族・カルボニル系の系統名**。`iupacName` が未対応（P12-3 の残り）。門番 N-4 により自動的に番号も出ない。
  ここを先に進めたくなったら、それは P12-3 の仕事であってこの文書の仕事ではない
- **環の番号づけ**（シクロヘキサンの 1,2- など）。主鎖の概念が別物で、`iupacName` も断っている
- **ユーザーに名前を入力させる採点**。この機能は「確認」＝ 見せる側だけ。命名を答えさせるのは quiz.js の担当
- **立体を含む名前**（E/Z・R/S を名前に織り込む）。P12-7/P12-8 側の決定待ち
- **番号の付け替えを手で試させる**（「逆向きにも振ってみる」の操作）。教育的には面白いが、
  「アプリが決めた1つ」を見せることが先。要望が出たら別項目にする

---

## 9. 未決（実装前にユーザーに確認したいこと）

1. **主鎖の帯を出す入口を、自由モードにも常設するか**（分子モーダルの中だけにするか）。
   常設すると「名前が出るものは主鎖も見える」で一貫するが、キャンバスがにぎやかになる
2. **慣用名と系統名のどちらを大きく出すか**。§N-6 では系統名を主・慣用名を副にしたが、
   高校の教材としては「イソブタン」を主に出したい場面もある（ライブラリ名は教科書の呼び名）
