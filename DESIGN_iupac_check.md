# IUPAC 命名の確認（主鎖の明示・炭素番号の表示）設計書

**コード名: 命名の確認モード。** ユーザーの分子について「この名前は、**どの鎖を主鎖と決めて・どちら向きに番号を振った**結果か」を
キャンバス上で見せる機能。2026-08-13 ユーザー要望（「IUPAC命名の確認（主鎖の明示・炭素番号の表示）を実装したい。
構造異性体の書き出しともリンクする機能です」）に基づく、実装前の合意用ドキュメント。

関連: [DESIGN_isomer_practice.md](DESIGN_isomer_practice.md)（この機能を最初に使う場所）／
DEVELOPMENT.md の **P12-3**（`iupacName` の対応範囲。芳香族・カルボニル系は未実装）

---

## 0. なぜ設計書を別にしたか（1本にしなかった理由）

異性体の書き出しとは**持ち主が違う**から。

- 主鎖と番号は **chemistry.js が持つ化学の知識**で、使う側は異性体練習だけではない
  —— アルキル基の練習・分子モーダル（辞書引き）・自由モードの名称表示・反応の生成物、
  どれも同じものを見たがる。異性体練習の章の中に埋めると、次に
  「芳香族の系統名（P12-3 の残り）」を足す人がここを見つけられない
- 逆に、異性体の答案用紙・ヒントの段数は**あの練習だけの都合**で、命名の正しさとは無関係に変わる
- 決定的なのは、**この文書の中身のほとんどが「1つの関数に決めさせる」という約束**であること。
  約束は約束だけで読めるところに置く。使う場所の説明に混ぜると、次に使う人が約束のほうを読み飛ばす

異性体側からは §5 の API だけを参照する。**依存は 異性体練習 → 命名の確認 の一方向**に保つ。

---

## 1. いちばん危ない罠 —— 「ハイライトした主鎖」と「表示した名前」が黙って食い違う

`chemistry.js` には炭素鎖を選ぶ経路が**2つ**ある。

| 経路 | 選び方 | 使われている場所 |
|---|---|---|
| `findLongestCarbonChain(mol)` | 炭素だけの部分グラフの**最長単純パス**。同点は `_cmpCarbonPath` ＝ **`_heavyAtomRanks` の順位列**が最小のもの（v1365。それ以前は「原子IDの辞書順」の**つもりで実際は何もしていなかった** ＝ 追記その2）。順位は `canonicalCode` と**同じ** WL 精緻化（`wlRefine`）と**同じ**グラフ構成（`buildHeavyGraph`）から取る ＝ 同型不変 | `isomerSeriesKey`（系列分類）・M2 の主鎖ハイライト |
| `iupacName(mol)` の内部（3432〜3458 行） | **-OH を最も多く含む → 多重結合を最も多く含む → 最長** の順で候補を絞り、残った同点は「OH位置最小 → 多重結合位置最小 → **置換基数が最多** → 置換基位置最小 → 辞書順」で決める | 名前 |

**「最長の炭素鎖」は IUPAC の主鎖ではない。** 主特性基を含むこと・多重結合を含むこと・置換基が多いこと、が先に来る。
別々に計算したものを同じ画面に出せば、**ハイライトと名前が食い違う** —— しかも例外も警告も出ずに。

### 1-1. どれくらい起きるか（実測。憶測で線を引かない）

`enumerateConstitutionalIsomers` で分子を生成し、`findLongestCarbonChain` の結果と
`iupacName` が実際に使う主鎖候補（3432〜3451 行と同じ手順を再現）を**原子集合で**突き合わせた。

**(a) 書き出し練習の標準6問（C₄H₁₀ / C₅H₁₂ / C₆H₁₄ / C₃H₈O / C₄H₁₀O / C₄H₈ ＝ 25異性体）**

| | 件数 |
|---|--:|
| `iupacName` が名前を返した | 23 |
| うち **エーテル**（主鎖という概念が無い） | 4 |
| 主鎖の**炭素数が違う** | **0** |
| ★ **炭素数は同じなのに原子が違う** | **2**（2-メチル-1-プロパノール・2-メチルプロペン） |
| IUPAC 主鎖候補が同点で複数ある | 9 |

**(b) 範囲を広げて（上記＋C₅H₁₂O / C₅H₁₀ / C₆H₁₂ / C₄H₆ / C₅H₁₀O / C₄H₉Cl / C₅H₁₁Cl ＝ 169異性体）**

| | 件数 |
|---|--:|
| `iupacName` が名前を返した | 84 |
| うち エーテル | 10 |
| 主鎖の炭素数が違う | 2（2-エチル-1-ブテン: 5C対4C／2-エチル-2-プロペン-1-オール: 4C対3C） |
| ★ 炭素数は同じなのに原子が違う | **14**（2,2-ジメチル-1-プロパノール・2-メチル-1-ブタノール・2-メチル-1-ブテン・2,3-ジメチル-1-ブテン・2-メチル-1-ペンテン・2-メチル-2-ブテン-1-オール ほか。**ハロゲン化物3件を含む** —— 下の追記） |
| 主鎖候補が同点で複数 | 31 |

> **2026-08-13 追記（N1 実装時に測り直した）: 食い違いは 13件ではなく 16件だった。**
> 上の実測は「3432〜3451 行と同じ手順」＝**主鎖候補を絞るところまで**の再現で、
> **同点候補から1本を選ぶ最後の並べ替え**（`named.sort` の「置換基数が最多 → …」）を通していなかった。
> ハロゲン化物は -OH も多重結合も無いので候補が長さだけで決まり、**同点のまま最後の並べ替えに委ねられる**。
> そこで初めて主鎖が確定するので、途中で止めた再現では食い違いが見えなかった。
> N1 で `iupacNameDetail().mainChain` が実装から直接取れるようになったため、以後は**推定ではなく実測**できる。
> 増えた3件（いずれも**炭素数は同じ**）:
> `1-クロロ-2-メチルプロパン`（C₄H₉Cl）／`1-クロロ-2,2-ジメチルプロパン`・`1-クロロ-2-メチルブタン`（C₅H₁₁Cl）。
> **結論は変わらず、根拠が強くなった側にずれた**（原子集合で見る必要がいっそう強い）。

> **2026-08-13 追記その2（v1365・`_cmpCarbonPath` の事故を直して測り直した）**
>
> 上の (a)(b) は**壊れた同点処理での実測**だった。`findLongestCarbonChain` の同点は
> 端点IDの辞書順で割っている **つもり**だったが、実際には `na[i] - nb[i]` と**引き算**していて、
> 原子IDは `'atom_' + Math.random()...` の**文字列**なので常に NaN ＝ `NaN < 0` は false ＝
> **同点処理は一度も走っていなかった**。選ばれていた鎖は DFS が最初に見つけたもの
> ＝ **原子の作成順**で、同じ化合物でも `compounds.json` 由来と
> `enumerateConstitutionalIsomers` 由来で結果が違った。
> **実測**（stages.json ＋ compounds.json の炭素2個以上 **1026件**を、重原子の作成順だけ
> 変えて4通り作り直す）。ついでに `isomerSeriesKey` の `locantOf` も、-OH が2つ以上ある分子で
> 作成順に最初の1つを返していた —— 同じ家族の事故が2つ。両方 v1365 で直した:
>
> | 外した直し | 系列キーが変わる | 最長炭素鎖が変わる |
> |---|--:|--:|
> | 両方（直す前の状態） | **29件** | **297件** |
> | `_cmpCarbonPath` だけ戻す | 5件 | 297件 |
> | `locantOf` だけ戻す | 24件 | 0件 |
> | **直した後（v1365）** | **0件** | **0件** |
>
> **ID順に「直す」のは筋が悪い**（もう一方のレーンが両方実装して測った）。IDは乱数なので、
> ID順は一意化ではなく**作り直すたびに目の変わるサイコロ**:
> 同じ 2-メチルプロペンを30回組み立てると **ID順では6通り**の鎖が返る（作成順なら1通り）。
> さらに**旧凍結16件のうち14件**は、同点の最長鎖（原子集合で2〜6通り）の**ちょうど1通りだけ**が
> IUPAC 主鎖と一致していたので、ID順＝乱択にすると **§6 の IN2 が実行のたびに赤緑する**
> （8回試して4〜10件が「一致してしまった」・顔ぶれも毎回違う）。
> 採ったのは乱択ではなく、**`canonicalCode` と同じ WL 精緻化（`wlRefine`）のクラス番号**を
> 順位に使う方法 ＝ 同型不変なので「同じ化合物なら必ず同じ鎖」。
>
> その結果、**顔ぶれが変わった**:
>
> | | 直す前 | 直した後（v1365） |
> |---|--:|--:|
> | 照合した異性体（名前が出て kind==='chain'） | 84 | 137種中 113 |
> | 主鎖の**炭素数が違う** | 2 | **9** |
> | ★ 炭素数は同じなのに原子が違う | **14** | **0** |
>
> 炭素数が同じままの食い違いが消えたのは、**初期色（元素＋自由価標）が最初の周回を支配し、
> 自由価標の小さい炭素が先に立つ**ため —— それがたまたま IUPAC の優先順（主特性基・
> 多重結合を含む鎖）と同じ向きに働く。2-メチル-1-プロパノールなら、`-CH₂OH` の炭素は
> 自由価標2・末端メチルは3なので、同点の3炭素鎖のうち `-CH₂OH` を含むほうが先に立つ。
> **偶然の一致であって規則ではない**（2つのレーンが**別々の**同型不変な順位づけを実装して、
> 残った9件が完全に同じ顔ぶれになった ＝ 特定の並べ方の産物ではない、という傍証はある）
> —— 下の1-2の結論（後から計算し直す設計は成立しない）は**そのまま動かない**。
>
> なお、この直しの後も「炭素数が同じで原子集合だけ違う」例は探せば出る
> （3-エチルペンタン・3-エチル-2-ペンテン・3-エチル-1-ペンテン）が、いずれも
> **自己同型で入れ替わるだけ**（枝が同じ形で、どちらを選んでも構造は同じ）なので、
> 食い違いとして数えない・**凍結もしない**（§6）。

### 1-2. ここから読み取るべきこと（3つとも設計を変えた）

1. **「表示する主鎖の炭素数」と「名前の幹の炭素数」を突き合わせる検査は、役に立たない。**
   食い違い 16件のうち **14件は炭素数が同じ**。標準6問にかぎれば **2件中2件が同じ炭素数** ＝
   **捕捉率 0%**。この検査を入れて「守った」と思うのが、このリポジトリが繰り返し踏んでいる
   「直したつもりで何も検査していない」の型そのもの。→ **検査は原子集合で行う**（§6）
   - **v1365 追記**: 同点処理を直したことで、この節の「14件」は 0件になった
     （追記その2の表）。つまり**この理屈は現在の実装では成り立たない** ——
     いま残っている食い違い9件は炭素数からして違うので、炭素数の比較でも捕まる。
     それでも**原子集合で見る決まりは変えない**。同点の選び方は今後も変わりうるもので、
     そのたびに「今なら長さでも捕まる」を確かめ直す運用にはしない（§6 の IN2 も同じ立場）
2. **食い違いは辺境ではなく看板の上で起きる。** 2-メチル-1-プロパノール（イソブチルアルコール）は
   **主力問題 C₄H₁₀O の7種のうちの1つ**。2-メチルプロペンは C₄H₈ の5種のうちの1つ。
   「アルコールとアルケンで、枝が1本あるやつ」＝ 高校で最も出る形が、まさに壊れる形
3. **主鎖は一意ではない。** 同点候補が 31件（標準6問でも9件）ある。だから
   「IUPAC の主鎖はどれか」を後から計算し直す設計は原理的に成立しない。
   **`iupacName` が最後に選んだ 1本**を持ってくる以外に正解がない

---

## 2. 決定

### N-1. `iupacName` を `iupacNameDetail` の薄い包みにする（**採用**）

```js
function iupacName(mol) { const d = iupacNameDetail(mol); return d ? d.name : null; }
```

名前は、主鎖と番号を決めた計算の**副産物のひとつ**になる。呼ぶ側がどちらを使っても、
**同じ1回の計算の別の面**を見ていることになり、食い違いが**構造的に起こらない**。
「検査で見張る」より一段強い ——検査は入れる（§6）が、それは**戻ってしまったこと**を知らせる番人であって、
正しさの根拠は「決める場所が1つしかない」ことに置く。

**なぜ他の案を採らなかったか**

| 案 | 却下の理由 |
|---|---|
| `iupacName` の戻り値をオブジェクトに変える | 呼び出し元が広い（`lookupCompoundName`・reactor・learn・テスト F8/F9/LB 群）。**得るものが無いのに全部を書き換える**。包みなら既存の呼び出しは1バイトも変わらない |
| `iupacNameDetail()` を**別に**書く（並行実装） | これが罠そのもの。同じ規則の2つ目の実装が生まれ、片方だけ直る日が必ず来る（今日 `pointSegmentDistance` が同名で2つあった件と同型） |
| `describeStructure` に相乗りする | `describeStructure` は表示用の要約で、`iupacName` を通らない呼び出し経路がある。相乗りしても **`iupacName` を直接呼ぶ人**が別の主鎖を持ったままになる。塞ぎたい穴が塞がらない |

### N-2. 実装は「捨てている情報を捨てないようにする」だけ（大きな書き換えをしない）

いま `_iupacNameForMainChain` は、両方向を評価して `d`（採用した向き）を決めたあと、
**`d` が持っている原子列を捨てて**名前と比較用の配列だけ返している（3495〜3500 行）。

- `evalDir(order)` の戻り値に **`order`**（＝ 番号順の原子ID列）を足す
- `_iupacNameForMainChain` の戻り値に **`chain: d.order`** を足す
- `iupacName` は `named[0].name` を返すのをやめ、`named[0]` を**そのまま**詳細に載せる

**主鎖の原子列 ＝ `named[0].chain`。番号 k の炭素 ＝ `chain[k-1]`。向きは配列の順そのもの。**
「両方向で最小化」を画面側でやり直さない。やり直した瞬間に経路が2つに戻る。

### N-3. `findLongestCarbonChain` は残すが、**命名の表示から締め出す**

消さない。`isomerSeriesKey` の系列分類（表示・ヒント専用と明記済み）と、環分子の案内で使っている。
代わりに関数の頭に禁止事項を書く:

> ⚠ **これは IUPAC の主鎖ではない**（-OH・多重結合・置換基数の規則が先に来る）。
> 実測で 84件中 16件が `iupacNameDetail().mainChain` と食い違い、**うち14件は炭素数が同じ**。
> **名前を出す画面の主鎖・番号に使ってはいけない**。→ `iupacNameDetail`（DESIGN_iupac_check.md）

同じ注意書きを `isomerSeriesKey` の「表示・ヒント専用で検証には使わない」の隣にも足す
（系列分類の `chainLen` は**主鎖長ではなく最長鎖長**である、という事実を隠さない）。

### N-4. 門番 ＝ 「名前が出ないものには、主鎖も番号も出さない」

新しい対応範囲リストは**作らない**。作れば `iupacName` の実装と二重管理になり、片方だけ伸びる。

> **主鎖と番号を描いてよいのは `iupacNameDetail(mol)` が非 null を返したときだけで、
> 描くのはそれが返したものだけ。**

これ1行で、`iupacName` が既に断っているもの（環・芳香族・カルボニル・エノール形・
不飽和エーテル・複数エーテル・分岐ポリオール・ヘテロ原子・多重結合が炭素間でない・複数分子）が
**自動的に**番号なしになる。P12-3 で芳香族の命名が実装された日には、**この文書を書き換えなくても**
主鎖表示が芳香族に届く。届かせるために触る場所が無いのが正しい。

#### N-4a. ★ 「並べるための鎖」は門番の外にある（**v1440**・エーテルの正解図）

> **書き出しの正解／エーテルのアルキル基の主鎖を横一直線に**
> （ユーザー・発注書 `video-scripts/ORDER_isomer_2026-08-20.md` §A-4）

⚠ **N-4 は「番号」の門番であって、「置き場所」の門番ではない。**
v1439 まではこの2つが `ipNumberedLayout` の中で1つになっていたので、
**番号を出せないもの＝置き場所も決められないもの**として `layoutMolecule`（42px の直交BFS・
向きは成り行き）へ丸ごと落ちていた。落ちた先で横一直線になるかは運で、実測（エーテル10件）では

| 物差し | 折れていた数 |
|---|--:|
| **重原子の最長鎖が折れている** | **2 / 10**（sec-ブチルメチルエーテル・エチルイソプロピルエーテル。どちらも 90°） |
| 重原子が1本の水平線に乗っていない（発注書 §A-4 の数え方） | 5 / 10 |

→ **決定**: `learn.js` に **`ipStraightLayout(mol)`** を足し、
**鎖を横一直線に置くところだけ**を門番の外へ出した（`renderStandardFigure` は
`ipNumberedLayout(mol) || ipStraightLayout(mol)` の順に試す）。

★★ **番号は返さない**（`order: null`）。⚠ **ここが N-4 との境目**で、次の人がいちばん踏みやすい。
エーテルは IUPAC で主鎖に番号を振らないので、**並べるための鎖と番号のための鎖を同じものにしない**。
`ipStraightLayout` が使う鎖は `ipLongestHeavyPath`（重原子の最長路・同長ならヘテロ原子の多いほう）で、
**`iupacNameDetail` とは別の計算**。番号を足した瞬間、§0 の失効表が
`findLongestCarbonChain` について釘を刺しているのと同じ「同じ図に出す名前と黙って食い違う」罠に落ちる。

**縮尺も 1本にした**（発注書 §A-4 の 8）。鎖の図は `IP_HSTEP = 46px`、
`layoutMolecule` は 42px だったので、**環の図だけ 46/42 を掛けて渡す**。
⚠ **掛けるのは渡す座標だけ**で、`layoutMolecule` そのもの（＝ユーザーの作図・呼び出しが使う道）は
42px のまま **1px も触っていない** ＝ CLAUDE.md の作図規約に触れない。

**実測（v1440）**: エーテル10件すべて 主鎖の折れ **0°**・刻み **46px**・素の番号 **0個**。
同じ表に並ぶアルコール4件・環2件も含めて、描かれた結合長は **46px の1種類だけ**。
見張りは **`IW31`**（否定対照 ＝ `ipStraightLayout` を塞ぐと 2件が 90° に戻る／
陰性対照 ＝ `layoutMolecule` の刻みは 42px のまま）。

### N-5. 断り方は3種類に言い分ける（「未対応」の一語で済ませない）

`aromaticOnly` の回で「開発ログに記録しました」を生徒に見せてしまった失敗（DESIGN_isomer_practice.md §11-4・BZ5）と同じ轍を踏まない。
**正しく描けた人に不具合の顔を見せない。**

| `iupacNameDetail` の結果 | 画面 |
|---|---|
| `kind: 'chain'` | 主鎖を帯で示し、C1・C2… を振る。系統名を見出しに出す |
| `kind: 'ether'` | **エーテルは主鎖に番号をつけない。**両側のアルキル基を2色で塗り分け、それぞれの基の名前を出し、「エーテルは主鎖に番号をつけません。両側のアルキル基の名前で呼びます」と書く。★ これは未対応の言い訳ではなく**規則そのもの**で、同じ1回の計算から出るので食い違わない |
| `null` かつ **環がある**（`findAnyCycle`） | 「環が基本骨格です。環の番号づけはこのアプリではまだ扱いません」＋ 環をハイライト（既存の M2 の案内を流用） |
| `null` かつ 環がない（カルボニル・アミン等） | 「この官能基の系統名はこのアプリではまだ扱いません（名前は名称ライブラリから引いています）」 |

**エーテルの重みは実測で決めた。** 標準6問の 23件中 4件、広げた範囲では 84件中 10件。
主力の C₄H₁₀O にいたっては **7種のうち3種がエーテル**。ここで無言になると、
主力問題の半分弱で機能が消える。「未対応です」ではなく「そういう規則です」と言えるのが、
`iupacNameDetail` から出す最大の実利。

### N-6. 番号を出す画面では、**番号の出どころの名前**を必ず併記する（もう1つの食い違い）

`lookupCompoundName` は **compounds.json を先に引き、外れたときだけ `iupacName` に落ちる**。
つまり画面の名前は「イソブタン」「ネオペンタン」のような**慣用名**であることがある。
その隣に系統名の番号づけを描けば、**画面に出ていない名前の番号**を見せることになる。

→ 命名の確認では、見出しを **系統名**（`iupacNameDetail().name`）にする。
ライブラリ名が違うときだけ副見出しに `（慣用名: イソブタン）` を添える。

> **規則: 番号を描く画面には、その番号を生んだ名前を必ず同じ画面に出す。**

---

## 3. 画面（どこに出すか）

**新しいトップレベルモードは作らない。** 既存の3か所に相乗りする。

1. **自由モード / 辞書引き（分子モーダル）** … 名前の隣に `🔢 主鎖と番号を見る` トグル。
   押すとキャンバスの当該成分に帯と番号が乗る。もう一度押すと消える。**状態は残さない**（描き替えたら消える）
2. **異性体の書き出し練習の「答え合わせ」オーバーレイ** … 標準の書き方の図に番号を出す。
   ★ ここは `ipNumberedLayout`（「主鎖横一直線＋主鎖炭素に位置番号」・v147）が**独自に番号を振っていた**。
   **→ v1341 で `iupacNameDetail().mainChain` に載せ替え済み**（＝ 3つ目の番号づけ経路を潰した。
   見張りは `IP7`。キャンバスの帯側は §6 の IN4）。**実測で変わったのは標準6問の25異性体のうち6件**:
   番号を振る原子が変わったのが **2件**（`2-メチル-1-プロパノール`・`2-メチルプロペン` ＝ §1-1 (a) の実測どおり。
   どちらも炭素数は同じ）、番号が**消えた**のが**エーテル4件**（主鎖に番号をつけないのが規則＝ N-5。
   図は `layoutMolecule` に落ちる）。環2件はもともと番号なしで不変。
   **v1365 追記**: 同点処理を直した結果、標準6問の25異性体は **25件とも最長鎖と主鎖が一致する**
   ようになった（§1-1 追記その2）。上の「番号を振る原子が変わった2件」は v1341 当時の記録として正しいが、
   **いまは標準6問の中に否定対照の材料が無い** —— `IP7` の (b) は C₆H₁₂ と C₅H₁₀O から取っている
3. **アルキル基の練習** … 付け根 C1 から数える番号（§4）

**書き出しの最中には出さない。** 主鎖と番号は答えの一部（「その形の名前は何か」）なので、
DESIGN_isomer_practice.md §13 の面の分け方（書き出し中＝判定ゼロ）に従う。

### 3-0. 名称の説明 —— **名前を部品に割る**（v1378・DESIGN_practice_revision.md §7 から移設）

> ユーザー: 自分が描いている構造式の名称を理解するために、主鎖表示、炭素番号表示、**名称の説明**、には手をつけたい

**新しいトグルもモーダルも作らない。**置き場所は上の1（`🔢 主鎖と番号を見る` の帯）で、
番号と**同じ出入り**をする。番号を出しているとき、その番号を生んだ名前は N-6 の規則で
**既に同じ画面に出ている**ので、ここでやるのは**その名前を押せるかけらに割る**ことだけ。

- **かけらは `iupacNameDetail().nameParts` をそのまま出す**（§5）。
  ★ **説明のために名前を組み立て直さない** —— 作れば「名前を作る場所が2つ」になり、
  片方だけ伸びた日に説明と名前が黙って食い違う（§N-1 と同じ家族の罠）。
  `IN10` が「かけらを繋ぐと名前に戻る」ことを 200件超で見張っている
- **押すと対応する原子が光る**（cyan の円を結合線の下に敷く。**丸のハイライトとは別の語彙**で、
  §3-1 の「丸＝この原子が問題」とは重ならない）。対応は
  **`mainChain` と `locants`（エーテルは `groups`）からだけ**引く ＝ `IN11` が見張る:

  | かけら | 光るもの |
  |---|---|
  | 幹（`プロパ`）・飽和の印（`ン`） | 主鎖ぜんぶ |
  | 位置番号（`-1-`） | その番号の炭素だけ（`mainChain[loc-1]`） |
  | 接尾辞（`ノール`） | その炭素に付いた -OH の酸素 |
  | 接尾辞（`エン`・`イン`） | 多重結合の両端（`mainChain[loc-1]` と `mainChain[loc]`） |
  | 置換基（`2-メチル`） | 付け根の炭素＋そこから主鎖の外へ生えた枝 |
  | エーテルの基 | `groups[i].ids`（**既存の2色の塗り分けと同じ原子**） |

- **エーテルにも効く**のが実利。C₄H₁₀O の7種のうち**3種がエーテル**で、
  ここで無言になると主力問題の半分弱で機能が消える。実測: `イソプロピルメチルエーテル` は
  `イソプロピル`｜`メチル`｜`エーテル` の3つに割れ、押すとその基が光る
- **門番は N-4 のまま**。`iupacNameDetail` が null なら**説明も出さない**（行ごと消える）。
  新しい対応範囲リストは作っていない。`IN13` (c) が環・カルボニルで「説明が0文字」を見張る
- ★ **番号の「向き」の理由を1行出す**（`dirReason`。§5）。生徒が間違えるのはここで、
  既定の1行として常に見えている（`-OH が 1 番になる向きを選びました…`）。
  **文は画面側（game.js）が持ち、chemistry.js は理由の記号だけを返す**
- **帯の高さ**: 行は番号を出しているあいだだけ出る（実測 PC 1280px で作業帯 88 → 127px・
  375px で 128 → 203px）。番号の表示中は作図が止まっているので、覆う量が増えても手は塞がらない。
  ふだんの高さは1pxも変えていない ＝ `WS1`・`WS3` の可視域の勘定は動かない

#### 3-0-1. 幹の中の2色 —— 数詞（炭素数）と段（結合の種類）（v1413・発注書 C-1）

> ユーザー: **エタン の切り方 「エ」「タン」の方が自然**

**切り方（`nameParts`）は1バイトも変えない。**「エ｜タン」への変更は実測で一般化しないため
（`feat/name-mark` の実測。ORDER_review_2026-08-17.md §C-1）:

1. **アルコール** —— `エ｜タ｜ノール` の3片が要り、真ん中が**かな1字**で幹ごとに姿が変わる（タ/パ/サ/ナ/カ）
2. **ジエン** —— `1,3-ブタ｜ジエン` の「タ」は buta-diene の**つなぎの母音**で**飽和の印ではない**
3. **語尾が一定でない** —— タン/パン/サン/ナン/カン。「タン＝アルカン」を固定の札にできない

**採った直し: 幹のボタンの中だけを2色に塗り分ける。**

```
エタン       [ エ タ ][ ン ]        エタノール     [ エ タ ][ ノール ]
エテン       [ エ テ ][ ン ]        1,3-ブタジエン [1,3-][ ブ タ ][ ジエン ]
エチン       [ エ チ ][ ン ]        1,2-エタンジオール [1,2-][ エ タン ][ ジオール ]
               ↑  ↑
           炭素数  結合の種類
```

★ **これで初めて画面に出るもの: エタン / エテン / エチン の対比。**
3つとも語尾のかけらは「ン」で**字面が同じ**で、単／二重／三重を分けている
**幹の最後の字（タ / テ / チ）** が幹の中に埋まって見えなくなっていた。

- **境目は `size`（主鎖の炭素数）から引く**。`IUPAC_NUMERAL`（`chemistry.js`。幹の表と同じ場所）が
  炭素数 → 数詞（メ・エ・プロ・ブ・ペン・ヘキ・ヘプ・オク・ノ・デ・ウンデ・ドデ）を持つ。
  ⚠ **名前を文字数で割らない** —— `ペンタ`（ペン+タ）と `プロパ`（プロ+パ）で境目が違う。
  数詞は**3つの幹の表の共通の頭**でなければならず、`SC1` がそれを assert する
  （幹を増やして数詞を書き忘れると赤くなる ＝ 新しい幹が黙って間違った位置で割れない）
- **段の意味は「どの表から来た幹か」で決める**（字面から推し量らない）。
  `IUPAC_ENE_STEM` → 二重結合、`IUPAC_YNE_STEM` → 三重結合、
  `IUPAC_ALKANE_STEM` → 後ろに `ジエン`・`ジイン` が続けば **`link`（つなぎの母音）**・
  それ以外は `sat`（すべて単結合）
- ★ **ジエンの「タ」は「単結合」と説明したら化学的に誤り**（ブタジエンに単結合の印は無い）。
  灰色＋**点線**の下線にして「ここは印ではない」と言い、文でもそう言う。`SC3` (b) が見張る
- **割れない幹は割らない**（1色のまま）。黙って間違った位置で割るより安全側。`SC2` が台帳を凍結
- **ボタンは1つのまま**（中の `<span>` を2つに分けるだけ）。割ると `nameParts` と1対1でなくなり、
  押したときに何を光らせるかが壊れる。押しものの床 34px は `.work-strip button` のままで `TAP1` は動かない
- **色**: 数詞＝橙（**主鎖の帯と同じ語彙**＝ 主鎖の炭素の話。#ffc247・地に対して 7.13）／
  段＝菫（#c792ea・5.01）／つなぎ＝灰（#a0a8c0・5.72）。
  cyan（選択中・かけらの光）・赤（エラー）・緑（正解）とはぶつからない。
  ⚠ **色だけに頼らない**（数詞と段の輝度比は 1.50 しかない ＝ 色相だけの差）:
  2つの間に**縦の区切り線**、段に**下線**（つなぎだけ点線）。`SC4` が計算後の値で見張る

**説明文は画面の字面にそろえる**（`_iupacPartNote`）。旧文は画面と食い違っていた:

| かけら | 旧 | 新 |
|---|---|---|
| `エテン` の `ン` | 「**エン**」＝ 炭素間に二重結合 C=C があることを表します | 「ン」は語尾で、エタン・エテン・エチンのどれにも付きます。二重結合 C=C を示しているのは直前の「テ」です |
| `エタン` の `ン` | 「ン」＝ 炭素間はすべて単結合（アルカン）という印です | 「ン」は語尾で、エタン・エテン・エチンのどれにも付きます。単／二重／三重を分けているのは直前の「タ」です |
| `エタノール` の `ノール` | 「**オール**」＝ -OH（ヒドロキシ基）を持つ… | 「ノール」＝ -OH（ヒドロキシ基）を持つことを表す接尾辞です（-オール） |
| 幹 `エタ` | 主鎖の炭素が 2 個であることを表す幹です | 「エ」＝ 主鎖の炭素が 2 個。「タ」＝ 炭素間がすべて単結合であることを表す段です（エタン・エテン・エチンで タ→テ→チ と変わります） |

> **`エテン` の中に「エン」という並びは存在しない**（エ-テ-ン）。生徒が字面を照合すると合わない。
> 同じ食い違いが `ノール` にもあった（`エタノール` に「オール」は無い）。
> **`SC3` (c) が機械で見張る**: 説明が `「…」` で引いた語は**必ず画面の名前の中にある**こと
> （実測で 55件の違反を検出 ＝ 旧文に戻すと赤くなる）。

**割り方は「名前の partition」であって、教科書の語の切り方ではない。**
`2-メチル-1-プロパノール` は `2-メチル`｜`-1-`｜`プロパ`｜`ノール` に割れる
（`プロパン`｜`-オール` ではない —— 名前の中に `プロパン` という並びが**存在しない**ため。
`IN10` の連結一致を満たす切り方はこれ）。

**やらないこと**: 主鎖候補の選定理由（**どの鎖を主鎖にしたか**）までは遡らない（§8）。

### 3-1. 描き方

- **主鎖の帯**: 主鎖の結合に沿って太い半透明の線（`--neon-orange` 系）を敷く。原子を丸で囲む既存の
  `highlightAtoms` は**使わない** —— 丸は「この原子が問題」の語彙（不斉・エラー箇所）で既に埋まっている。
  主鎖は「この道」なので線で示す
- **番号**: その炭素の**字そのものに添え字として重ねる** —— `C₁` `C₂` … を1つの記号として、
  炭素の丸（半径 10px）の中に描く。**分子モーダル内のサムネイル・アルキル基の図にも同じ規則で出す**
  > **2026-08-13（N2 実装時）→ 2026-08-15（ユーザー判断）で置き換えた**。経緯を残す:
  >
  > **もとの案は「主鎖炭素の外側（分子の重心から遠い側）に置く」**だった。3つの絵で壊れた。
  > ① **横一直線の鎖**（ヘキサン）では中ほどの炭素の「重心から遠い側」は**鎖に沿った向き**で、
  > 番号が隣の炭素との中間に落ちる ＝ *どちらの番号か読めない*。
  > ② **四方が埋まった炭素**（2-メチル-1-プロパノールの C2 は上下左右が C3本＋H1本）では、
  > 重心から遠い側がちょうど**自動水素の真上**になる。③ 隣どうしの番号が固まる。
  > → v1320〜v1369 は `_iupacOutward` で **10°刻み × 半径 16〜32px の置き場所探し**をしていた。
  > 重なりは消えた（ライブラリ全件で 14px 未満が 184個 → 0個）が、**副作用が2つ**残った:
  > 番号が炭素から **29〜35px**（結合長 42px の 7〜8割）離れ、直鎖では
  > 1 が左上・2/3 が真下・4/5 が真上・6 が右下と**散った**（直鎖の炭素は上下が水素で埋まっているため、
  > 「通常は右下」という見た目にならない）。
  >
  > **→ 2026-08-15 のユーザー判断で探索そのものをやめた**（「C原子のグラフィックに重ねてはどうでしょう
  > （添え字になるようなイメージ）」）。丸の中に置けば **「どの炭素の番号か」は探索の出来ではなく
  > 構造で決まる** ＝ 上の①②③がまとめて消える。
  >
  > **採った作り（3案のうち③）**: ①丸を r=10 → 12 に広げる ②数字を縁にまたがせる は却下。
  > ③**`C` と数字を1つの `<text>` に入れて中央揃えにする** —— ブラウザが `C` を左へ寄せ、
  > 「C₁」がひとまとまりの記号として真ん中に載る。**書体が何であっても揃い、2桁でも同じ理屈**。
  > 実装は `game._iupacSubscript()`（添え字は `<tspan class="iupac-number">`）。
  > **2桁（デカンの C₁₀）** は全幅が `MAX_W`（14.0px）を超えたぶんだけ**添え字の字だけ**小さくする
  > （`textLength` で全体を潰すと `C` まで細り、番号の付いた炭素だけ元素記号の形が変わって見える）。
  > 実測（v1371・Orbitron）: 四隅の最遠は 1桁 7.8〜8.8px・**2桁 9.31px**（丸の半径 10px）。
  > **`_iupacOutward` は呼び出し元が無くなったので削除した**（`_iupacObstacles`・`_iupacCentroid` も同様）。

  > **★ 表示倍率「小」での可読性は範囲外**（2026-08-15 ユーザー判断）。
  > 「炭素の番号を気にするときは、1つの構造に注目して細かくみる場合だから、
  > 中・大のときに確認できればよく、小のときに視認性が下がる問題は切り捨ててよい」。
  > ＝ これは**用途から見た線引きであって、直し忘れではない**。
  > 「小で添え字が読みにくい」を不具合として直しにこないこと —— 直すなら、
  > まず「小のまま番号を確認したい場面」が本当にあるかを確かめるのが先。
  > 丸を広げる・字を大きくする類の手当ては、**番号を出していない普段の図の見た目を変える**
  > （＝ 番号は一時表示なのに、その都合で常時の図が動く）ので、代償のほうが大きい。
- **帯は結合線の下に敷く**（上に乗せない）。乗せると二重結合の2本線が帯に飲まれて
  C=C がどこか読めなくなる（2-メチルプロペンで実際にそう見えた。N2 実装時）
  > **2026-08-17（v1405）追記: 下に敷いても読めなくなる。**
  > ユーザー申し立て「名称のマーカー：C=C に重なるとどこが二重結合かわからなくなる」。
  > **線は1本も隠れていない** —— 潰れるのは **2本線の *あいだ* が塗られる**から。
  > 帯は太さ 11px（±5.5）で、二重結合の2本線（±5・太さ 2.5）の**すきまをまるごと覆う**。
  > すきまが地の色からオレンジに変われば、2本線は「太い1本の帯」に見える。
  > ＝ **「上に乗せない」だけでは足りない**。かけらの光（§3-0 の cyan の円 r=14）も同じことをする。
  >
  > 実測（2-ブテン。線と すきま のコントラスト比。図を実際に画素へ落として測った）:
  >
  > | 状態 | 直す前 | v1405 |
  > |---|--:|--:|
  > | 番号を出していない（これが基準） | 3.78 | 3.78 |
  > | 帯だけ | 2.61 | 6.80 |
  > | かけらの光だけ | 2.85 | 8.23 |
  > | **帯＋光**（語尾のかけらを押した状態） | **2.08** | **4.44** |
  >
  > **採った直し**: 幾何は 1px も動かさず、**番号を出しているあいだだけ結合線を濃くする**
  > （`rgba(255,255,255,0.4)` → `0.92`。`game._iupacLiftBondInk`）。
  > - 帯を細める・薄めるでは足りない（薄さ 0.2 でも 3.52 までしか戻らず、先に帯が読めなくなる）
  > - **光は円のままでなければならない**（§3-0。輪にすると「この原子が問題」＝ 不斉・
  >   エラー箇所の丸と語彙がぶつかる）ので、帯だけ直しても光の 2.85 が残る。
  >   線を濃くすれば**下にいるのが帯でも光でも効く** ＝ 1つの手当てで両方が直る
  > - `IN7`（番号が丸に収まる）・当たり判定・整形には触れない
  >
  > **物差しは「番号を出していないときを下回らない」**（絶対値で線を引かない）。
  > 見張りは **`NM1`**（2-ブテン・1,3-ブタジエンで実測）・**`NM2`**（★否定対照＝
  > 濃くするのをやめた絵をその場で作ると赤くなる）・**`NM3`**（呼び出し口3つ・後始末・門番）。
  > ⚠ **サムネイル側（`renderTargetBond`）は二重結合の線が ±2.5px しか離れていない**ので、
  > つぶれ方はキャンバスより激しい ＝ 手当てをキャンバスだけに入れると答え合わせの表が直らない
- **重なり**: 見出し（`renderMoleculeLabels`）と同じ「格子点を覆わない」制約は要らない（一時表示で、
  その間は作図を止めるため）。ただし**番号を出している間はタップで原子を置けない**ことを明示する

---

## 4. アルキル基の番号（付け根 C1）

`iupacAlkylName` も同じ罠を持つ。`best` を選んだあと **`{nm, key}` だけ返して鎖を捨てている**（3304〜3307 行）。
同じ直し方をする:

- `iupacAlkylName` の戻り値に **`chain`**（root から始まる原子ID列）を足す
- `iupacAlkylGroupName(mol, rootId)` / `iupacAlkylNameFromR(mol)` の詳細版
  `iupacAlkylDetailFromR(mol)` → `{ name, mainChain, rootId }`
- **付け根が必ず C1**（`_iupacPath(subAdj, root, t)` が root 始まりなので、この性質は既に実装が持っている）。
  番号は配列の添字そのままで、向きを選ぶ余地が無い ＝ **主鎖の場合より単純**

**付け根の `R` には添え字を付けない**（v1371 の判断）。`R` は鎖の一員ではなく
「この先に何かが続く」の置き換え記号で、番号を持たない —— 字幕も
「付け根（R）に付いた炭素が C1 です」と言う。`R₀` のようなものを描くと、
**R が 0番の炭素であるかのように読めてしまう**。番号が付くのは `mainChain` の炭素だけで、
**枝の炭素にも付かない**（sec-ブチル ＝ 炭素4個・主鎖3個 ＝ 添え字は3個。IN9 が名指しで見る）。

慣用名への写像（`IUPAC_RETAINED`: イソプロピル・sec-ブチル…）は名前だけを変える。
**鎖と番号は体系名の側のものを出す** ——「sec-ブチル」に番号は無いので、
`sec-ブチル（＝1-メチルプロピル）` と体系名を併記して番号を振る（N-6 と同じ規則）。

---

## 5. API（異性体練習など、使う側が見る面）

```js
// 非環式の炭化水素・ハロゲン化物・アルコール・エーテルの IUPAC 系統名と、
// **その名前を作るのに実際に使った主鎖・番号の向き**。対応外は null。
// ★ iupacName はこの関数の薄い包み。名前と主鎖は必ず同じ1回の計算から出る
function iupacNameDetail(mol) -> null | {
    name,          // 従来 iupacName が返していた文字列と 1バイトも変わらない
    kind,          // 'chain' | 'ether'
    mainChain,     // 番号順の炭素 ID 配列。番号 k の炭素 = mainChain[k-1]。kind==='ether' では null
    groups,        // kind==='ether' のときだけ: [{ ids:[…], rootId, name }, …]（2つ）
    locants,       // { ol:[…], ene:[…], yne:[…], subs:[{loc, key, name}] } — 画面の説明文用
    nameParts,     // ★ 名称の説明用の**かけらの列**（v1378。下記）
    dirReason      // ★ 番号の向きを決めた比較（下記）。kind==='ether' では null
}

function iupacAlkylDetailFromR(mol) -> null | { name, mainChain, rootId }   // 付け根 = mainChain[0] = C1
```

### 5-1. `nameParts` —— 名前を割ったかけら（v1378・§3-0 の画面が使う）

```js
nameParts = [{ text, role, … }]        // text を順に連結すると name と**1バイト単位で一致する**
```

★ **名前を割り直したものではない。** `name` を組み立てるときに `_iupacAssemble` /
`_iupacOlCore` / `_iupacUnsatCore` が**連結した当のかけら**を、そのまま並べて持ち出している。
だから「ハイフンの入れ方」「位置番号の省略（エタノール・プロペン）」「倍数接頭辞」を
説明側がもう一度実装することにならない —— **名前を作る場所は1つのまま**（§N-1）。

| `role` | 例 | 付随する値 |
|---|---|---|
| `sub` | `2-メチル`・`-2-メチル` | `locs`（位置番号の配列）・`label`（基の名前） |
| `locant` | `-1-`・`2-` | `kind`（`'ol'`/`'ene'`/`'yne'`）・`locs` |
| `stem` | `プロパ`・`ブテン` | `size`（主鎖の炭素数） |
| `suffix` | `ノール`・`ジエン` | `kind`・`locs` |
| `sat` | アルカンの `ン` | — |
| `ether-group` | `イソプロピル`・`ジエチル` | `groups`（`groups[]` の添字。同じ基なら `[0,1]`） |
| `ether-suffix` | `エーテル` | — |

⚠ **2つ目以降のかけらは、つなぎのハイフンを頭に持つ**（`-2-メチル`・`-1-`）。
落とすと連結が名前に戻らない（`IN10` が赤くなる）。

### 5-2. `dirReason` —— **番号の向きを決めた比較**（v1378）

`_iupacNameForMainChain` は前から数えた向きと後ろから数えた向きを、この順で比べて決めている。
**どの比較で決まったかは、分岐した直後に捨てていた**（実測。設計回 §7-3 の M15）。
返すのはその記号1つだけで、**名前の出力は1バイトも変わらない**（`IN12` が独立に導いて照合する）。

| 値 | 意味 | 画面の文（文は game.js が持つ） |
|---|---|---|
| `'ol'` | -OH の位置番号で決まった | `-OH が N 番になる向きを選びました` |
| `'unsat'` | 多重結合の位置番号で決まった | `多重結合（C=C・C≡C）が早く来る向きを…` |
| `'ene'` | 二重結合の位置番号で決まった | `二重結合が早く来る向きを…` |
| `'sub'` | 置換基の位置番号で決まった | `置換基が早く来る向きを…` |
| `'alpha'` | 位置番号が同点で、置換基名の辞書順で決まった | `位置番号は両向きで同じだったので…` |
| `'tie'` | どちら向きでも同じ | `どちら向きでも同じでした` |

★ **実際に出るのは 5通り**（実測 v1378。`compounds.json` 939件＋標準6問ほかの全異性体を掃いた）。
`'ene'` は「多重結合を合わせた比較では同点なのに、二重結合だけで見ると差が付く」ときにしか起きず、
それは **C=C と C≡C が同じ分子にある（エンイン）** 場合だけ。エンインは `_iupacUnsatCore` が
未対応（null）なので、**名前ごと出ない** ＝ `'ene'` は外へ漏れない。
`IN13` はこれを「5通りが出そろう」＋「エンインに名前が付かない」の2つで固定してある
——**エンインの命名を実装した日には後者が赤くなる**ので、そのとき測り直すことになる。

実測の内訳（`compounds.json` 939件・名前が出た 149件のうち向きの理由を持つ 143件）:
`tie` 61 ／ `unsat` 31 ／ `sub` 30 ／ `ol` 21（`alpha` は 0件。標準6問ほかの列挙では
`2-クロロ-3-メチルブタン` に1件出る）。

**使う側の禁止事項（コメントに書く）**
- `mainChain` を受け取ったら**そのまま添字で番号にする**。並べ替えない・逆にしない・最小化し直さない
- `findLongestCarbonChain` / `isomerSeriesKey().chainLen` を番号や主鎖の表示に使わない
- **説明のために名前を作り直さない。**部品は `nameParts` を使う（`IN10`）

---

## 6. 検査と否定対照

接頭辞の帯は **`IN`（IUPAC Numbering）** を新設する（`assembler/tests.js` 冒頭の台帳で未使用を確認済み。
**実装時に台帳へ `| IN | 1〜4 | 命名の確認（主鎖と番号が名前と同じ計算から出ていること） |` を書き足す**）。

| # | 何を見るか | 落ちる直し方 |
|---|---|---|
| **IN1** | **包みの同一性**: 標準6問の全異性体＋`compounds.json` 全件で、`iupacName(mol)` と `iupacNameDetail(mol)?.name` が**完全一致**（両方 null も一致とみなす） | `iupacNameDetail` を別実装で書いた／`iupacName` に規則を足して包みを迂回した |
| **IN2** | **★否定対照（本命）**: 下の**凍結リスト**の各分子について、`findLongestCarbonChain(mol)` の原子集合が `iupacNameDetail(mol).mainChain` の原子集合と**一致しない**こと。あわせて (a) リストの各件で**最長鎖のほうが長い**こと（＝ 同点の入れ替わりでは裏返らない対照であること）、(b) 列挙した全異性体を掃いた食い違いの総数が**リストの件数以上**あること（黙って減っていない）を assert する | 表示を `findLongestCarbonChain` に差し戻した（＝ この機能を「簡単にした」つもりの将来の直し）。命名側の主鎖選びを「最長でいいや」と簡単にした |
| **IN3** | **門番**: 環（シクロブタン・ベンゼン）・カルボニル（アセトン）・複数分子・エーテルで、キャンバスに**番号テキストが 0 個**であること。エーテルでは代わりに**2つの基の名前が出る**こと | 「未対応でも最長鎖くらい出しておこう」と門番を緩めた |
| **IN4** | **番号づけ経路が1本**: 番号を出した状態の SVG から番号テキストとその位置を読み、`番号 k のついた原子 = mainChain[k-1]` が全 k で成り立つこと。答え合わせオーバーレイの標準図（`ipNumberedLayout`）でも同じことを見る | 画面側で番号を振り直した／`ipNumberedLayout` の独自番号が残った |
| **IN7** | **番号が「その炭素の字の添え字」で、丸の中に収まっている**（v1371 で書き換え。下記）: (a) 番号は炭素の元素記号と同じ `<text>`（`C`＋数字）に入っている (b) その `<text>` の位置＝その炭素そのもの（最寄りの重原子まで 0px） (c) 数字の枠の四隅が**丸（半径 10px）の中** —— **2桁のデカン `C₁₀` を含む** (d) 図と字幕の縦の隔たりが 20〜30px | 番号を離れた場所に置く実装に戻した／2桁が丸からはみ出す／字幕の逃がしを勝手に広げた |
| **IN8** | **★否定対照**: v1369 までの置き方（原子から離れた独立の `<text>`）を**その場で作って IN7 と同じ物差しに掛け**、ちゃんと弾かれること（`d1 >= 10` ＝ (b) が外置きでも通るなら物差しが空回り）。あわせて 2桁の詰めが**2桁でだけ**働くこと（1桁の添え字は 6px のまま・`C₁₀` は 6px 未満） | 「丸の中にある」だけを見て*その文字自身*を中心に測る空回りの物差しにした／いつでも縮める（＝ 詰めの検査が意味を失う）／詰めをやめた |
| **IN9** | **アルキル基の図も同じ添え字**（`drawAlkylNumberingIntoSvg`。quiz.js の別の描画関数の上に重ねる経路）。1-メチルプロピル（sec-ブチル）で **炭素4個・主鎖3個** ＝ 添え字は `C₁ C₂ C₃` の3個、枝のメチルと**付け根 `R` は素のまま** | 場所3だけ旧い置き方に取り残された／炭素の数だけ番号を振った／`R` に添え字を付けた |

> **IN7・IN8 は v1371 で「削除」ではなく「書き換え」た。**
> v1369 の IN7・IN8 は「番号と自動水素の中心が 14px 以上離れている」ことを見ていた。
> 番号を添え字にすると**その物差しは意味を失う**（番号は丸の中・自動水素は 16px 先で当たりようがない）。
> **消してしまうと、守っていたものが誰にも見張られなくなる。**
> IN7・IN8 が本当に守っていたのは「**どの炭素の番号か迷わない**」ことなので、
> 添え字でそれが構造的に保証されることを**測る形に移した**（上の (a)(b)(c)）。
> 否定対照も「自動水素を外すと重なる置き場所が実在する」から
> 「**外置きにすると物差しが弾く**」へ移してある。

**IN2 の凍結リスト**（実測で拾った食い違い。テストにはこの**名前**を書き、失敗時に化合物名で出す）

**v1365 で測り直した現行リスト（9件。すべて「最長鎖のほうが長い」）**

| 分子 | 最長鎖 | IUPAC 主鎖 | 式 |
|---|--:|--:|---|
| `2-エチル-1-ブテン` | 5 | 4 | C₆H₁₂ |
| `2-エチル-1-ブタノール` | 5 | 4 | C₆H₁₄O |
| `2-エチル-2-ブテン-1-オール` | 5 | 4 | C₆H₁₂O |
| `2-エチル-3-ブテン-1-オール` | 5 | 4 | C₆H₁₂O |
| `3-エチル-3-ブテン-1-オール` | 5 | 4 | C₆H₁₂O |
| `3-エチル-3-ブテン-2-オール` | 5 | 4 | C₆H₁₂O |
| `2-プロピル-2-プロペン-1-オール` | 5 | 3 | C₆H₁₂O |
| `2-エチル-2-プロペン-1-オール` | 4 | 3 | C₅H₁₀O |
| `2-イソプロピル-2-プロペン-1-オール` | 4 | 3 | C₆H₁₂O |

`IN_FORMULAS` に **C₆H₁₄O と C₆H₁₂O を足した**（9件のうち7件がここから来る。列挙は合わせて約1秒）。

**旧リスト（直す前・16件）** は §1-1 の追記その2 のとおり **14件が消えた**（同点の選び方が変わり、
最長鎖が IUPAC 主鎖と一致するようになったため）。残った2件は上の表の
`2-エチル-1-ブテン` と `2-エチル-2-プロペン-1-オール`。

> **否定対照をどこに置いたか**: 「合っていること」を確かめる検査（IN1・IN4）だけでは、
> 両方を同じ間違え方にすれば通ってしまう。**IN2 は「間違った実装なら必ず別の答えになる具体的な分子」を
> 名指しで固定する**ので、`findLongestCarbonChain` に戻す直しは 9件の名前を挙げて赤くなる。

> **凍結してよいのは「最長鎖のほうが長い」ものだけ**（v1365 の学び）。
> 炭素数が同じままの食い違いは、実測すると**自己同型で入れ替わるだけ**
> （3-エチルペンタン・3-エチル-2-ペンテン・3-エチル-1-ペンテン ＝ 枝が同じ形なので
> どちらを選んでも構造は同じ）で、どちらの実装の同点処理が変わっても裏返る。
> **脆い対照を凍結すると、検査が「実装が変わったこと」ではなく「たまたま」を見張ることになる。**
> IN2 はこの条件そのものを assert する（凍結リストに同じ長さのものが混じったら赤くなる）。

> **旧「炭素数が同じものが 11件以上」の節は削除した**（v1365）。
> 直す前は14件あったので成立していたが、いまは 0件で、**満たそうとすると脆い対照を
> 凍結するしかなくなる**。守ろうとしていた「炭素数だけを見る検査への退化」は、
> 代わりに **(b) 掃き取りの総数**で見張る（命名側を「最長でいいや」にする直しはここが 0 に落ちる）。
> **原子集合で比べる決まりのほうは残す**（§1-2 の1 の追記）。

> **この食い違いは「同点の主鎖候補のうち、どれが選ばれたか」で決まっている**（§1-2 の3）。
> `findLongestCarbonChain` の同点処理が変われば、食い違う顔ぶれも変わりうる。
> そのときは **IN2 を測り直す**のであって、検査を緩めるのではない
> —— 「同点があるから後から計算し直せない」という結論のほうは、顔ぶれが変わっても動かない。
> （v1365 は実際にこの手順を踏んだ回。測り直しに使った掃き取りは `assembler/tests.js` の IN2 内。）

**性能**: `iupacNameDetail` は `iupacName` と同じ計算なので追加費用ゼロ（返す配列が1本増えるだけ）。
`compounds.json` 全件（939件）を回す IN1 は既存の F8 と同じ土俵なので、そこに相乗りしてもよい。

---

## 7. マイルストーン

- **N1: 1つの計算にする** — `evalDir` に `order` を足す／`_iupacNameForMainChain` に `chain` を足す／
  `iupacNameDetail` を作り `iupacName` を包みにする／`iupacAlkylName` にも `chain` を足す／
  `findLongestCarbonChain`・`isomerSeriesKey` に禁止事項のコメント。**画面はまだ変えない**。
  完了条件: **IN1・IN2 が緑**、F8・F9・LB 群・既存の全テストが**1件も変わらずに緑**（名前の出力が不変であること）
- **N2: キャンバスに出す**（**完了・v1320**） — 主鎖の帯＋番号の描画、分子モーダルのトグル、
  系統名と慣用名の併記（N-6）。完了条件: **IN3・IN4 が緑**。実機で 2-メチル-1-プロパノールと
  2-メチルプロペンを描き、**帯が名前どおりの鎖に乗る**ことを目視（＝ 実測で壊れていた2件を人の目でも確かめる）
  - 入口は2つ・**状態は1つ**（`game.iupacNumbering`）: 分子モーダルの `🔢 主鎖と番号を見る` と、
    自由モードの作業帯の同名ボタン（§9-1 の「自由モードにも常設」）
  - **アルキル基（§4）はキャンバス側だけ入った**。付け根 R があれば `iupacAlkylDetailFromR` に
    切り替わり、`sec-ブチル（＝1-メチルプロピル）` と体系名を併記して C1 から振る。
    **答え合わせのサムネイルへの繋ぎ込みは未着手** —— 呼び出し口
    `game.drawAlkylNumberingIntoSvg(svgId, target)` を用意してあるので、learn.js 側で
    `renderMoleculeIntoSvg` の直後に呼べばよい（learn.js を持つレーンの仕事）
  - **エーテルの2基表示は N3 ではなく N2 で入れた**（IN3 が「エーテルでは2つの基の名前が出ること」を
    見るので、N2 の完了条件を満たすのに必要だった）
- **N3: 言い分け**（**完了・v1366**） — 環・未対応官能基の断り文（N-5。それまで1つのトーストに畳んであった）。
  答え合わせオーバーレイの標準図を `mainChain` に載せ替える（§3 の場所2・`ipNumberedLayout` の置き換え。
  **これは v1341 で先に済んでいた**ので N3 では見張りを足すだけになった）。
  完了条件: C₄H₁₀O の7種すべてで**何かしら意味のある表示が出る**（アルコール4種＝番号、エーテル3種＝2基）
  → **実測で 4種＋3種**。`IN5` (d) がこの内訳そのものを assert する
  - **言い分けは `game.iupacNumberingNotice()` 1か所**が持つ。返すのは `{ code, ok, message, det }` で、
    `code` は **8通り** —— `chain` / `alkyl` / `ether`（出せる）と
    `empty` / `practice` / `multi` / `ring` / `unsupported`（出せない）。
    **門番（`iupacNumberingDetail`）は緩めていない** ＝ 分けたのは言い方だけで、
    `ok:false` の回は表示を点けない（`IN3` の「番号テキストが0個」は変わらず緑）
  - **新しい対応範囲リストは作っていない**（§N-4）。`code` は「門番が断ったあとに、
    その分子を見て分かること」（分子の個数・`findAnyCycle`）だけで決めている
  - 環は**丸で名指しする**（言葉だけでは「どれが環か」を図の上で指せない）。
    丸＝「この原子が問題」の語彙（§3-1）なので、主鎖の帯とは衝突しない
  - 見張り: **`IN5`**（場面ごとに `code`・文言・字幕・表示の入切を名指し）と
    **`IN6`（★否定対照）**（8通りの文言が**本当に8種類あるか**。
    断りを1つの一語へ畳み戻すと赤くなる ＝ 実測で「4種類しかない」と出た）。
    `IN6` はあわせて **エーテルを「まだ扱いません」の語で断っていないこと**も見る
    （エーテルは未対応ではなく**規則**。C₄H₁₀O の7種中3種がここに落ちる）
  - **`IN4` を `ipNumberedLayout` にも広げた**（統合レーンの申し送り）。
    それまで `IN4` はキャンバス側しか見ておらず、標準図側は `IP7` が**標準6問の25異性体だけ**で
    見ていた ＝ カルボニルと複数分子は**どちらの検査にも入っていなかった**。
    否定対照で確認: `ipNumberedLayout` の門番を「未対応なら最長鎖」に緩めると
    `IN4`（シクロブタン）と `IP7` の両方が赤くなる

---

## 8. やらないこと（範囲外）

- **芳香族・カルボニル系の系統名**。`iupacName` が未対応（P12-3 の残り）。門番 N-4 により自動的に番号も出ない。
  ここを先に進めたくなったら、それは P12-3 の仕事であってこの文書の仕事ではない
- **環の番号づけ**（シクロヘキサンの 1,2- など）。主鎖の概念が別物で、`iupacName` も断っている
- **ユーザーに名前を入力させる採点**。この機能は「確認」＝ 見せる側だけ。命名を答えさせるのは quiz.js の担当
- **立体を含む名前**（E/Z・R/S を名前に織り込む）。P12-7/P12-8 側の決定待ち
- **番号の付け替えを手で試させる**（「逆向きにも振ってみる」の操作）。教育的には面白いが、
  「アプリが決めた1つ」を見せることが先。要望が出たら別項目にする
- ★ **主鎖候補の選定理由**（**どの鎖を主鎖にしたか**）。名称の説明（§3-0）が出すのは
  **番号の向きの理由**（`dirReason`）までで、鎖の選び方までは遡らない。
  主鎖の選定は候補を4段階で絞ってから `named.sort` が5つの規則で決めるので、
  「なぜこの鎖か」を1行で言うには**5つの規則を説明する文が要る**。しかも残っている同点は
  実測で「自己同型で入れ替わるだけ」のものが多く、見せても「どちらでも同じ」にしかならない。
  → やるなら**まず「見せる値打ちのある同点」がどれだけあるかを測ってから**
  （理由の全文は DESIGN_practice_revision.md §8-2）
  - **✅ 測った（2026-08-20・`tools/count-mainchain-ties.js`）: 「本当に違う2本」は**
    **ライブラリ全体で 0件・書き出し練習の標準6問で 0件**（同点はそれぞれ 18件・9件あるが、
    **全部が自己同型で移り合うだけ**）。§1-1 (b) の範囲まで広げて 3件で、
    いずれも塩化アルキル＝ プリセットに無い。**→ この項目は「やらない」で確定。**
    ⚠ **§1-1 の 31件・9件は今も正しい**が、母数は列挙エンジン（169異性体・25異性体）で
    ライブラリではない。ライブラリ全体で数え直すと同点は 18件
  - ★ ただし**1つ手前には値打ちがある** ——「最長鎖が本当に2通りあるのに、
    **-OH（C=C）を含むほうが主鎖に残る**」分子がライブラリに2件・§1-1 (b) に8件。
    これは**同点ではない**ので1行で言い切れる（上の「5つの規則」の重さが無い）。
    やるなら **`dirReason` の兄弟として「絞る段の理由」を1つ返す**形になるが、
    **本項目とは別の項目として立てること**
  - ⚠ 同じ回に見つかった「**負けた候補の名前が壊れている**（`2-1-クロロメチルプロパン`）」のほうは、
    **対抗馬を出すかどうかとは無関係に直した**（v1426・**§11**）。
    採用名にも壊れる例が実在したため（`3-(クロロメチル)ペンタン`）

---

## 9. 決着（2026-08-13・ユーザー判断）

1. **主鎖の帯は自由モードにも「命名確認」として常設する**（分子モーダルの中だけにしない）。
   → 「名前が出るものは主鎖も見える」で一貫させる。にぎやかさはトグルで畳める形にすること
2. **系統名を主**、慣用名は副（§N-6 のまま）。
   ライブラリ名（教科書の呼び名＝「イソブタン」）は併記で残す

**未決は無し。** N1 から着手してよい。

---

## 10. 続き —— 「名称の説明」（2026-08-15・ユーザー要望）

> 自分が描いている構造式の名称を理解するために、主鎖表示、炭素番号表示、**名称の説明**、には手をつけたい

**✅ 着地（v1378）。正は §3-0（画面）と §5-1・§5-2（API）へ移した。**
やらないこと（主鎖候補の選定理由）は §8 に落とした。以下は着手時の要点の記録:

- **材料はすでに `iupacNameDetail` が返している**（実測。`2-メチル-1-プロパノール` なら
  `mainChain` 3炭素・`locants.ol:[1]`・`locants.subs:[{loc:2,name:'メチル'}]`／
  エーテルは `groups:['イソプロピル','メチル']`）。**表示していないだけ**
- **置き場所は `🔢 主鎖と番号を見る` と同じ帯**（§9-1 と一貫。新しい面を作らない）
- ★ **`chemistry.js` に要る追加は1つだけ** —— `_iupacNameForMainChain` が向きを決めた
  `cOl / cUn / cEne / cSub` の**どれで決まったか**（`dirReason`）を返す。**名前の出力は1バイトも変わらない**
- **門番は N-4 のまま**（`iupacNameDetail` が null なら説明も出さない）。新しい対応範囲リストは作らない
- **主鎖候補の選定理由（どの鎖を主鎖にしたか）までは遡らない** ——
  §8 の「やらないこと」に足す扱い（理由は同書 §8-2）

実装して着地したら、**§3（画面）と §5（API）へ本文を移し**、ここは行き先の1行に畳む。

---

## 11. 括弧つきの複合置換基名（2026-08-20・ユーザー要望・v1426）

> ユーザー: **2-(クロロメチル)プロパン** は対応できるようにしたいです

**複合置換基＝ 置換基そのものが置換されているもの。** 直す前の実装は基の中の位置番号を
主鎖の位置番号と**地続きに並べていた** ——`2-1-クロロメチルプロパン`。
**どちらの番号か読めない**うえ、括弧が無いので基の切れ目も見えない。

見つけたのは同点の主鎖を測った回（`tools/count-mainchain-ties.js`・
[発注書 D](video-scripts/ORDER_isomer_review_2026-08-15.md)）で、
「対抗馬を画面に出すなら、まず括弧の無い名前を直すことになる」と書き残されていた。

### 11-1. 先に測った範囲（憶測で線を引かない）

**採用された名前**（＝ 画面に出る名前）と**主鎖候補の名前**（＝ 負けた対抗馬。
`_iupacNameForMainChain` を包んで実装が返した実物を記録した）を分けて数える。

| 母数 | 採用名に括弧が要る | 主鎖候補に括弧が要る |
|---|--:|--:|
| **ライブラリ全体**（stages 120 ＋ compounds 939 ＝ 1059件。畳んで 975件・名前が付くのは 129件） | **0** | **0** |
| 書き出し練習の標準6問（25異性体） | **0** | **0** |
| §1-1 (b) の範囲（169異性体） | **0** | **3**（すべて塩化アルキル） |
| さらに広げて C₆H₁₃Cl・C₇H₁₅Cl・C₇H₁₆・C₈H₁₈・C₆H₁₄O を足した 284異性体 | **4** | **30**（延べ） |

- ★ **ライブラリでは1件も起きない。**複合置換基が名前に出るのは
  `イソプロピル`・`sec-ブチル`・`tert-ブチル`・`イソブチル` の**慣用名に置き換わった側**だけで、
  慣用名は1語なので括弧が要らない（＝ 既存の名前は1つも変わらない）
- ★ **採用名が壊れる例は実在する** ——`3-(クロロメチル)ペンタン`（C₆H₁₃Cl）ほか4件。
  直す前は `3-1-クロロメチルペンタン` と出ていた。**分子式を打てば生徒に届く**ので
  「対抗馬を出すときだけの話」ではない
- 高校で出る形（アルカン・アルケン・アルコール・エーテル）では**炭素だけの複合置換基が
  採用名に出ない** ——主鎖の絞り込みが「最長」を先に見るので、枝の枝は主鎖に吸われるため。
  実際に出るのは**ハロゲンが枝の炭素に付いた形**（クロロメチル・クロロエチル）だけだった

### 11-2. 決めた範囲（扱わないものは **null**。壊れた名前を返さない）

**扱う**

| | 規則 | 例 |
|---|---|---|
| (1) | 複合置換基は**丸括弧で囲む** | `2-(クロロメチル)プロパン`・`3-(1-クロロエチル)ペンタン` |
| (2) | **炭素1個の基（メチル）は位置番号を書かない** ——位置が1しかないので曖昧さが無い | `クロロメチル`（`1-クロロメチル` ではない）・`ジクロロメチル` |
| (3) | 慣用名に置き換わる基は**囲まない**（1語なので囲む理由が無い） | `4-イソプロピルヘプタン` |
| (4) | アルファベット順のキーは「基の中の置換基のキー＋ yl 幹のキー」を繋いで作る | クロロメチル → `chloro`＋`methyl` ＝ IUPAC の「完全な名前の頭文字で並べる」と一致 |

**扱わない（名前を返さない）**

| | なぜ | 例 |
|---|---|---|
| (5) | **同じ複合置換基が2つ以上** ——`ビス`／`トリス` が要る（`ジ(クロロメチル)` は誤り） | `3,3-ビス(クロロメチル)ペンタン` → **null** |
| (6) | **複合置換基の入れ子** ——角括弧 `[…]` が要る。しかもどちらの候補を IUPAC が採るかは、**両方に名前を付けてみないと決まらない**（位置番号だけでは割れない） | `5-[2-(クロロメチル)プロピル]ノナン` → **null** |

⚠ **(6) の門は「候補が1本でも入れ子なら基ごと null」**にしてある。名前の付く候補が
残っていても、入れ子の候補と**どちらが IUPAC の選ぶ基か決められない**（実測: 入れ子のほうが
位置番号で勝つ場合が実在する）。**選べないものを選ばないのが正しい側**。

### 11-3. どこに書いたか（**名前を作る場所を2つに増やさない**）

**足したのは既存の3か所の中だけ**で、新しい命名関数は1つも作っていない。

| 場所 | 何をした |
|---|---|
| `_iupacAssemble` | 置換基のかけらを組むところで、`composite` が真なら `(…)` で囲む。同じ複合置換基が2つ以上なら **null を返す**（(5)） |
| `iupacAlkylName` | 返り値に **`composite`** を足す（＝ 置換基つきの体系名が慣用名に置き換わらずそのまま出ているとき）。炭素1個の基は `omitLocants` で位置番号を落とす（(2)）。候補が1本でも入れ子なら null（(6)）。アルファベット順のキーを合成（(4)） |
| `_iupacCollectSubs` | `composite` を親へ運ぶだけ |

★ **`nameParts` は自動的に正しくなる。**括弧は `_iupacAssemble` が**連結している当のかけら**に
入るので、`IN10`（かけらを繋ぐと名前に戻る）は触らずに通り続ける ——
**説明のときだけ括弧を足す**実装にしていたら、そこで名前を作る場所が2つになっていた。
かけらの `label` は**括弧を含まない基の名前**のまま（`SC3` (c) が「説明が引く語は画面の名前の中にある」
ことを見張っているが、`クロロメチル` は `(クロロメチル)` の中にあるので通る）。

### 11-4. 検査

| # | 何を見るか | 落ちる直し方（★ 実際に外して赤くなることを確かめた） |
|---|---|---|
| **IN14** | (a) `(CH₃)₂CHCH₂Cl` の**主鎖候補**に `2-(クロロメチル)プロパン` がある・候補のどれにも「位置番号が続けて2つ」が無い (b) `3-(クロロメチル)ペンタン` が**採用名として**組み立ち、かけらを繋ぐと名前に戻る (c) `–CH₂Cl` の基名が `クロロメチル`・`–CHCl–CH₃` は `1-クロロエチル` (d) `4-イソプロピルヘプタン` に括弧が付かない | ★ 括弧をやめる（(a) が赤）／★ 炭素1個の位置番号の省略をやめる ＝ `2-(1-クロロメチル)プロパン`（(a) が赤）／位置番号を全部落とす（(c) の2つ目が赤） |
| **IN15 ★否定対照** | (a) 範囲外の2つが **null**（ビス・入れ子） (b) C₄〜C₆ の塩化アルキル 29異性体を掃いて、**採用名と主鎖候補の両方**に「位置番号が続けて2つ」が0件・かつ**括弧つきが実際に出ている**（0件なら物差しが空回り） (c) ライブラリ 1059件に括弧も壊れの印も0件・名前が出る件数が 150件を下回らない | ★ (5) の門を外す ＝ `3,3-ジ(クロロメチル)ペンタン` が出て赤／★ (6) の門を外す ＝ `5-(2-(クロロメチル)プロピル)ノナン` が出て赤／括弧をやめる ＝ (b) の「括弧つきが1件も無い」で赤 |

**★ 既存の名前が変わっていないことの確認**（v1426 の実施記録）:
直す前後の `chemistry.js` を並べて読み込み、ライブラリ 1059件 × **5種の問い合わせ**
（`iupacName` / `iupacNameDetail().name` / `nameParts` の連結 / `lookupCompoundName` /
`iupacAlkylNameFromR`）＝ **5,295回**を突き合わせて **差分 0件**。
広げた 284異性体でも **名前が消えた 0件・増えた 0件**で、
**壊れた名前は 採用名 4件 → 0件・主鎖候補 30件 → 0件**になった。

### 11-5. §8 との関係（この直しは「対抗馬を出す」ことではない）

§8 の「主鎖候補の選定理由（どの鎖を主鎖にしたか）」は**やらないで確定**したまま
（実測で「本当に違う2本」がライブラリ0件・標準6問0件）。**ここで直したのは名前の作り方だけ**で、
対抗馬を画面に出す仕掛けは1つも入れていない。
発注書 D が「対抗馬を出すなら名前の生成から直すことになる」と書いた**その前提のほう**を、
（対抗馬を出すかどうかとは無関係に）**採用名が壊れていたので**先に直した。
