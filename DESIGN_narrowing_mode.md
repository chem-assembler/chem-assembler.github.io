# 絞り込みモード 設計書

分子式と実験結果から構造を決めていく過程を、**候補が何通り残っているか**を見せながら操作させるモード。
実装前の合意用ドキュメント。

きっかけは入試問題の解析（`マイドライブ/化学/問題集/_解析/`）で、
東大 2021 前期1I の実験を順にかけると候補がこう減ることが計算で出たこと。

| | 候補 |
|---|---:|
| C6H12O の全構造異性体 | **211** |
| エノール18種を除く（単離できない） | 193 |
| 不斉炭素をちょうど1個（問題文の前提） | 55 |
| 実験5: カルボニル基が無い | 51 |
| 実験1: −OH をもつ | 26 |
| 実験2: 水素付加しない＝環 | 8 |
| 実験3: 酸化してケトン＝第二級 | 5 |
| 実験4: ヨードホルム陽性 | **3** |

**紙の上では絶対に見えない数字**で、ここから3つのことが分かった。

- **ヨードホルム陽性を最初に置くと 16**。順番で効きがまったく違う
- **実験だけでは B は3通りまでしか絞れない**。問イの「四員環をもつもの」で初めて1つに決まる
- **B を決めるだけなら必要な条件は2つだけ**（ヨードホルム陽性＋四員環）。残り5つは冗長

3つめは条件の部分集合を総当たりして出した（`_解析/tools/search-routes.js`）。
**どの順で解くか・どこまで条件が要るかは、人が判断しなくても計算で出る。**

## 1. 基本方針

- **候補を数えるのはアプリの仕事**。人は条件を選ぶだけにする。手で数えさせない
- **答えを当てさせるのではなく、絞り込む過程を見せる**
- **カードの順番を入れ替えられる**こと。効きの違いが見えるのが本体
- **複数の化合物を同時に追う**。入試の構造決定は A〜F が並ぶのが普通で、
  1つの候補集合を絞る形では実物に合わない（→ §4 マトリクス）
- 既存の作図・判定には**一切影響しない**。別モードとして足す（`DESIGN_ui_modes.md` の系列）
- 外部ライブラリを増やさない。列挙も描画も `chemistry.js` にあるもので済む

## 2. 入力は「分子式」ではなく「分子式＋制約」

東北大 2021後期3 を解いた答案は、**問題文の制約を3行の箇条書きにするところから始まっていた**。

```
・350 以下
・C, H, O のみ
・4員環以下はナシ
```

これは手順ではなく**候補集合の定義**で、列挙の入力そのもの。
モードの最初の画面は分子式の選択ではなく、**制約パネル**にする。

| 制約 | 効き方 |
|---|---|
| 分子式 | 列挙の元。`enumerateConstitutionalIsomers` に渡す |
| 分子量の上限 | 分子式が未確定のとき、候補の分子式を切る |
| 含まれる元素 | 同上 |
| 4員環以下は無い / 環は無い | 列挙後のフィルタ |
| 不斉炭素が n 個 | 同上 |
| **エノールを除く**（既定でオン） | 同上。単離できない形を候補から外す（v931） |

### ⚠ エノールで2つ別のことが起きる（神奈川大3 で判明・2026-08-08）

神奈川大3 の C3H6O は9通り。「臭素水を脱色し、ナトリウムとは反応しない」で1通りに決まるはずが、
最初の実装では **3通り**残った。原因を「エノールは教科書が扱わないから答えにならない」と読んだが、
**これは誤診だった。** 実際には別々の2つが重なっていた。

**① 述語のバグ（本命）。** `findFunctionalGroups` は C=C に直結した −OH を
`alcohol1/2/3` ではなく **`enol` として返す**。`startsWith('alcohol')` だけで見ていたため、
エノールが「−OH をもたない」側に落ちていた。**エノールにも −OH はあるのでナトリウムとは反応する。**
これを直すと、エノールを候補に残したままでも実験(c) は 1 通りに決まる
（つまり `skill.textbook-scope` を持ち出す必要はまったく無かった）。

**② それとは別に、エノールは答えにならない。** C=C に −OH が付いた形は単離できず、
すぐカルボニルに変わるので「化合物A」にはなれない。これは実験の判定とは無関係に、
**候補集合そのものから外すべき**もの。C6H12O では 211 のうち **18 がエノール**で、
残したままだと東大の途中の数が変わる（不斉炭素1個で 57 → 実際は **55**）。

つまり①は判定の誤り、②は候補集合の定義の問題で、**直す場所が違う**。両方入れた。
②を切れるようにしてあるのは、なぜ除くのかを説明する材料になるから。

**東大 2021 前期1I の数字は、エノールを除いた側が正しい**（冒頭の表もこの値に直した）。
26 から先は変わらないので、結論の 3 は動かない。

**問題文が「いずれも不斉炭素原子を一つだけもっている」と書いているなら、それは条件カードではなく制約。**
東大で 193 → 55 を担っているのはこれで、カードとして置くものではない。

## 3. 2つのエンジン

大きさで手法を変える。**どちらも試作して動作を確認済み**（`_解析/tools/`）。

### A. 配分エンジン（分子の大きさによらない）

不飽和度と酸素を**部品に割り振る**組合せを数える。構造は作らない。

```
C14H20 ＋「ベンゼン環1つ」  → 2通り（ベンゼン環＋C=C ／ ベンゼン環＋脂肪族の環）
C12H14O2 ＋「ベンゼン環1つ」 → 27通り
　　　　　＋「エステルをもつ」 → 2通り
```

部品は `ベンゼン環(不飽和度4)` / `脂肪族の環(1)` / `C=C(1)` / `C≡C(2)` /
`エステル結合(1, O2)` / `カルボキシ基(1, O2)` / `酸無水物(2, O3)` / `ケトン(1, O1)` /
`アルデヒド(1, O1)` / `ヒドロキシ基(0, O1)` / `エーテル結合(0, O1)`。

**芳香族はここで扱う。** 列挙が届かない大きさでも一瞬で終わり、
しかも人が実際に最初にやる作業（不飽和度6 ＝ ベンゼン環4 ＋ C=O 1 ＋ C=C 1）そのもの。

岡山大の答案が「O が4つ → 高々2価エステル → 不飽和度2を先に引く」から始まっていたのも同じ操作。

検算で分かったこと: **C6H12O にかけると6通りが出て、それが手で書く表の行と一致する。**

```
1. アルデヒド          4. C=C ＋ ヒドロキシ基
2. ケトン              5. 脂肪族の環 ＋ エーテル結合
3. C=C ＋ エーテル結合  6. 脂肪族の環 ＋ ヒドロキシ基
```

### B. 列挙エンジン（重原子8個まで）

`enumerateConstitutionalIsomers(elements, hCount, nodeLimit)` で構造を全部つくる。実測。

| 分子式 | 構造異性体 | 時間 |
|---|---:|---:|
| C4H10O | 7 | 0.0 秒 |
| C5H12O | 14 | 0.0 秒 |
| C5H10O | 74 | 0.2 秒 |
| C4H8O2 | 122 | 0.1 秒 |
| C4H6O2 | 263 | 0.2 秒 |
| C6H12O | 211 | 6.0 秒 |
| C5H10O2 | 400 | 2.9 秒 |
| C4H4O4 | 3328 | 80 秒 |

**重原子7個からブラウザでは重い。** C6H12O（211通り）と C5H10O2（400通り）は結果を JSON に焼いて
同梱する（静的配信なのでこれで足りる。`compounds.json` と同じ扱い）。`isomers-baked.json` は 47KB。

C4H4O4（重原子8個）は 3328通り・80秒で、**焼くには大きすぎる**。この領域は配分エンジンに任せる。

重原子9個以上は配分エンジンだけで進み、構造の列挙は出さない。

## 4. 画面 — マトリクスが主、候補数が従

**問題文に出てくる物質を列にとった表**が中心。条件カードを置くと、該当するセルが埋まる。

```
┌─ C6H12O ／ 不斉炭素1個・分子量350以下 ────────── [制約を編集] ┐
│                    A     B     C     D     E     F           │
│  −OH               ×     ○     ○     ×     ○     ○           │
│  C=C               ×     ×     ○     ○     ○     ○           │
│  C=O               ×     ×     ×     ×     ×     ×           │
│  環                ○     ○     ×     ×     ×     ×           │
│  アルコールの級     −    2級    3級    −    1級   1級          │
│  ヨードホルム       ×     ○     ×     ×     ×     ×           │
│  ────────────────────────────────────────────                │
│  残り候補          8     3    12    …                        │
└──────────────────────────────────────────────────────────────┘
  ▸ 積んだカード（並べ替え可）
     実験5 カルボニル基が無い     全体に効く   −4
     実験1 ナトリウムで水素が出る A・D と B・C・E・F を分ける
     実験4 ヨードホルム陽性       B のみ       −2
```

- **行が性質、列が化合物**。実験カードを置くと、その実験が言っている分だけセルが埋まる
- 各列の下に**その化合物の残り候補数**
- カードは**ドラッグで並べ替え**。順番を変えると候補数が全部引き直される
- 空のセルが「まだ決まっていないこと」を示す。**どこを埋めれば進むかが見える**

これは東大を解いた答案が実際に紙の上で作っていた表で、
**線形にたどるより取りこぼしが減り、どの欄が空かが見える**のが利点。
`_解析/db/moves_qa_map.json` では `skill.matrix` として記録している。

### 効きの表示

各カードの右に「−154」のような**減った数**を出す。**0 のカードは灰色**にする。
情報を持たないカードがひと目で分かるのがこの表示の狙い。

## 5. 条件カードの語彙

`_解析/tools/count-candidates.js` に実装済みの述語をそのまま持ってくる。

**官能基**: −OH をもつ／もたない・第一級/第二級/第三級アルコール・エーテル・
アルデヒド・ケトン・カルボニルなし

**検出反応**: 銀鏡反応陽性・ヨードホルム陽性/陰性（`CH3-CO-` と `CH3-CH(OH)-` を実際に探す）・
臭素水を脱色

**骨格**: 環をもつ／もたない・**環の大きさが 3〜8**・C=C をもつ／もたない・**直鎖／枝分かれ**

**酸・エステル・ラクトン**: カルボキシ基をもつ／もたない・エステル結合をもつ・**環状エステル（ラクトン）**

**生成物から言うもの**: オゾン分解で1種類（＝左右対称）／2種類・カルボニルを2つもつ（＝還元して二価アルコール）・
**臭素を付加したジブロモ体の不斉炭素が2つ**（⚠ 元の分子ではなく付加後で数える）

**立体**: 不斉炭素がちょうど n 個

カードの文言は**実験の言い方**にする（「ナトリウムを加えると水素が発生した」）。
裏に述語が何かを出す（「＝ −OH をもつ」）。**実験と判定の対応を覚えるのがこのモードの副産物**。

## 6. ルート探索 — 「他の解き方」を計算で出す

条件は絞り込みなので**かける順番を変えても最後の候補集合は同じ**（フィルタは可換）。
順番が効くのは途中の候補数だけ。そこで2段に分けて調べる。

1. どの条件の**部分集合**で1通りに届くか → 極小のものが「可能なルート」
2. その集合を**どの順**でかけると速く減るか → 各段で最も減る条件を選ぶ（貪欲）

条件が8個なら 2⁸ = 256 通りの総当たりで済むので、**実時間で全部調べられる**。

そして各条件を自動で分類する。「冗長」で片づけず、**なぜ冗長なのか**を分ける。

| 判定 | 意味 |
|---|---|
| **必須** | どの極小ルートにも入る。外すと届かない |
| **代替あり** | ルートによって使ったり使わなかったり |
| **使わなくてよい** | どの極小ルートにも入らない |
| **冗長** | 他の条件を全部使うなら、外しても結果が変わらない |
| **重複** | 別の条件と残す集合が完全に同じ。どちらか一方でよい |
| **情報ゼロ** | 単独でかけても1つも減らない。制約から自動的に満たされている |

⚠ **「冗長」は化合物ごと**。東大の実験5は B を決めるだけなら冗長だが、A〜F 全体には必要。
画面でもこの区別を落とさないこと。

**画面での使い方**: 生徒が1通りに絞れたあと、「**他にも解き方があります**」として
別の極小ルートを出す。使わなかった条件は「これは使わなくても解けました」と灰色で示す。

## 7. 模範解答の飛躍を見せる

模範解答は「○○であることから、構造が決定できる」と1文で済ませることが多い。
**その一文が本当に決定しているかは、候補数が1になるかで判定できる。**

`_解析/db/problems.jsonl` の `collapsed` に、模範解答が1文で飛ばしている手順の範囲を記録している
（文言は写さず、どこを何手ぶん飛ばしているかだけ）。

- 岡山大: 「分子量90で不斉炭素をもつカルボン酸だから乳酸」＝ 実際は**3手**
- 名古屋大: 「F が不斉炭素をもつことから」＝ 実際は**2手**

M3 で入試問題を素材にするとき、**この範囲を1枚のカードにまとめて置ける**ようにする。
押すと中の手順が展開される。「模範解答はここを1文で済ませています」という表示。

## 8. 候補の見せ方（3段階）

211個を全部描くと破綻するので、**候補が減るにつれて表現を具体にしていく**。

| 候補数 | 見せ方 |
|---:|---|
| 20以上 | **配分カード**。「C=C ＋ ヒドロキシ基」のような部品の組み合わせと、その内訳の数だけ |
| 20〜4 | **部分構造カード**。確定した部分を描き、残りを `R−`（アルキル基）でまとめる |
| 3以下 | **実際の構造図**。`layoutMolecule` で描く |

真ん中の段が肝で、既存資産がそのまま使える。
`chemistry.js` には擬似元素 `R`（価標1・アルキル基の付け根）と
`iupacAlkylGroupName` / `iupacAlkylNameFromR` があり、アルキル基の書き出し練習で使っている。
**「C4H9− は4種類」「C5H11− は8種類」を1枚のカードとして扱う**のはこの機構の延長。

## 9. 段階案

### M1（最初に実装）: 制約 → カード → 候補数 — **実装済み（v929・2026-08-08）**

- 制約パネル（分子式のプリセット＋不斉炭素の数＋環の除外）
- カードを積む・外す・並べ替える
- 候補数と減った数を出す
- 候補は**数と配分カードだけ**（構造は描かない）
- 1通りになったら構造を1つ描いて終わり

**列は1つ**（単一の化合物）でよい。これだけで「順番で効きが変わる」は体験できる。

実装は `assembler/narrowing.js`。入口は学習メニューの `🔍 実験の結果から絞り込む`。
設計から変えたところが3つある。

1. **配分カードではなく「候補の内訳」を出した。** 配分エンジンを移植しなくても、
   **手元にある候補を部品構成でまとめれば同じ表になる**（「3員環 ＋ 第二級 −OH … 2」）。
   しかも抽象的な割り振りではなく**実在する候補の数**なので、こちらのほうが正確。
   配分エンジンが要るのは列挙が届かない M5（芳香族）から
2. **入口はクイズのアコーディオンに置かなかった。** `#learn-acc-quiz` は「3群に整理された」ことを
   EP4 が検査していて、4つめの群を足すと落ちる。そもそもこれはクイズではないので、
   書き出し練習と同じ階層に独立した `<details>` を置いた
3. **操作ログを M1 から吐かせた**（`op.constraints` / `op.card` / `op.reorder` / `op.solved`）。
   §10 で決めたスキルの測り方が実際に効くかは実データでしか分からないので、貯め始めておく

**C6H12O だけ `isomers-baked.json` に焼いた**（17KB）。node で 5.5 秒かかり、ブラウザで走らせると
その間 UI が固まる。生成は `_解析/tools/bake-isomers.js`。

実測で直したもの（どちらも**狭い画面で初めて出た**）:
- カードの行がグリッドの固定列だったため、幅 280px で 1fr が潰れて**1文字ずつ折り返した** → flex + wrap へ
- `<select>` の幅は**最長の option で決まる**。ヒントを option の文言に入れていたため
  モーダルごと横に溢れた（233px の枠に 369px） → ヒントを別行に出した

### M2: マトリクス — **実装済み（v932・2026-08-08）**

- 列を複数にして A〜F を同時に追う
- 実験カードが「A・D と B・C・E・F を分ける」型の効き方をするようにする
- 空のセルが残っているところが「まだ決まっていない」

実装で決めたこと。

**カードの側に「表のどこを埋めるか」を持たせた**（`row` と `cell`）。
「ナトリウムを加えると水素が発生した」は `row:'−OH', cell:'○'`。
表を別に組み立てるのではなく、**カードを積めばセルが埋まる**形にすると、
実験と性質の対応がそのまま画面の構造になる。

**候補集合（プール）は全列で共有し、積んだカードだけを列ごとに持つ。**
分子式と制約は問題全体にかかるもので、化合物ごとに違うのは「どの実験をしたか」だけ。
列を切り替えるとカードの積みとパレットの点灯が入れ替わる。

**何も積んでいないうちは表を出さない。** 空の表は情報がゼロで、画面を圧迫するだけ。

**空欄は「・」で出す。** ここが M2 の値打ちで、
「どこを埋めれば進むか」が見える。実測で東大 2021 前期1I を A〜C の3列で追うと、
A は5手で3通り、B は3手で8通り、C は1手で3通りと、列ごとに独立して減る。

3つめの列（アルデヒド1手だけ）で `ヨードホルム` の欄が「・」のまま残るのが、
**まだ調べていないことと、調べて陰性だったことを区別している**印。

M3 の準備として、列ごとの `trace()`（各段の残り候補）を切り出してある。

### M3: ルート探索 — **実装済み（v933・2026-08-08）**

- 1通りに絞れたあと「他の解き方」を出す
- 使わなくてよかった条件を灰色で示す
- 条件の性質（必須／代替あり／冗長／重複／情報ゼロ）を表で見せる

実装で決めたこと。

**探索の対象は「積んだカードだけ」**（パレットの全カードではない）。
積んでいないカードまで含めると「実は問題文に無いこの実験をすれば1手で済んだ」が出てしまい、
**「他の解き方」ではなく「別の問題」**になる。

**目標は「1通り」ではなく、いま実際に到達している数**にした。
1通りに届かない積み方でも「同じところへもっと少ない手で行けたか」は意味のある問いで、
こうすると極小集合が空になることも無い（全部使えば必ず届く）。
実際、東大は実験だけでは3通りまでしか絞れないが、**その3通りにも2手で行ける**と出る。

**2枚以上積んだら常に出す。** 1通りに絞れるまで待つと、
届かない問題（絞り切れない出題）で一度も出ないことになる。

**カードが13枚以上なら探索しない**と表示する。2ⁿ の総当たりなので、
黙って固まるより「調べませんでした」と出すほうがよい。実問題の実験は多くて8個。

検証で分かったこと: **設計書の冒頭に人が書いた結論を、アプリが自力で再現した。**

```
東大 2021 前期1I（C6H12O・不斉炭素1個・エノール除く 55通り）
  実験5つを積む            → 3通り。極小ルートは1つで、2手（ヨードホルム＋水素付加しない）
  ＋ 問イ「四員環をもつ」  → 1通り。極小ルートは {四員環, ヨードホルム陽性} の2手だけ
                              残り4手はすべて「冗長」
```

このために**環の大きさのカード（3〜6員環）を足した**。設計書 §5 の「骨格」に挙げていながら
M1 で入れ忘れていたもので、これが無いと東大の問イが表現できない。

### M4: 出題（入試問題を素材にする） — **実装済み（v935・2026-08-08）**

- `_解析/db/narrowing/*.json` を読み込んで、実際の入試問題の実験列をカードにする
- `collapsed` を使って「模範解答はここを1文で済ませています」を表示
- **絞り切れない問題**（神戸大のような出題ミス）も、そのまま見せる価値がある

翻訳は `_解析/tools/build-narrowing-data.js` が行い、`assembler/narrowing-problems.json` を書く。
現在4問・12列。**対応が付かない条件は黙って捨てず、生成器が落ちる。**

**述語の語彙は解析側とアプリ側で意図的に別**にしてある。
解析側（「ヒドロキシ基あり」）は**何を言っているか**、アプリ側（`na`）は**どう操作するか**を表す。
橋を1か所に集めておけば、どちらかを増やしてもここだけ直せばよい。

⚠ **不斉炭素と環の有無は、制約かカードかを仕様が決める。**
東大の「いずれも不斉炭素原子を一つだけもっている」は問題文の前提＝制約だが、
東京都立大の「A・F・G には光学異性体が存在した」は実験＝カード。
**機械には見分けが付かない**ので、仕様側に `asConstraint` の印を置いた。
混ぜたときに何が起きるかは実測した ——
**片方の列の実験が、もう片方の候補集合まで削る**（東京都立大の母数が 14 → 4 になり、検算が4件落ちた）。

⚠ **カードは積まずに列だけ渡す。** 積んだ状態で渡すと答えを見せることになる。
どの実験があるかは「この列の実験」として別に並べ、置くのは生徒の仕事にする。

### M5: 芳香族（配分エンジン単体） — **実装済み（v935・2026-08-08）**

- 重原子9個以上は配分だけ。「ベンゼン環1つ・エステル1つ」で 27 → 2 のような絞り込みを見せる
- 構造は描かないが、**割り振りが決まる**ところまでは追える

**列挙と配分は画面ごと分けた。** 同じ「制約」でも意味が違う（構造の集合 vs 割り振りの集合）ので、
1つのパネルに混ぜると何を絞っているのか分からなくなる。行き来はタブでする。

実測: C24H38O4（重原子24個）＋ベンゼン環1＋エステルで 12 通りが **0ms**。
列挙エンジンは8個で頭打ちなので、この領域は配分でしか追えない。

#### エステルはこの順で考える（ユーザー指摘・2026-08-08）

```
① 酸素の数から価数を決める     エステル結合1つで酸素2個。O2 なら1価、O4 なら高々2価
② 不飽和度が余ればラクトン     C=O のぶんを引いても余る → 環状エステルを疑う
③ 2価以上なら結合の向き       R−COO−R−COO−R ／ R−COO−R−OCO−R ／ R−OCO−R−COO−R の3通り
④ 2価以上なら 1:1 ではない    加水分解で同じアルコールが2分子出ることがある
```

**①②は配分エンジンが数え上げる。③④は構造の話なので数え上げでは出ない**ため、
酸素の数に応じて注意書きとして画面に出す。④はとくに落としやすく、
「加水分解して A と B が 1mol ずつ」と決めてかかると取りこぼす。

`_解析/db/moves.json` では `ester-valence-from-oxygen` → `lactone-ring` → `ester-hydrolysis` の
3つの手筋に分けて、注意書きでこの順序を明示してある。

## 10. スキルの測定 — このモードが測定器を兼ねる

`/qa/` は**事実**を測る道具で、「候補を漏れなく書き出せますか」とは訊けない。
書き出させて漏れを数えるしかない。**スキルを測れる場所はこのモードしかない。**

入試15問から出た手筋43件を分解すると、こうなっていた（`_解析/db/moves_qa_map.json`）。

- 手筋はすべて知識に根を持つ。**スキルだけで成り立つ手筋は0件**
- 知識のみで出る手筋 16 件 ／ 知識＋スキル 27 件
- 知識と qa は **1:1 ではない**。純粋な1:1 は3件だけで、平均すると1手筋あたり qa 2項目弱の束

そこで**スキル1つにモードの操作1つを割り当てる**。`skills[].op` がその対応。

| スキル | 操作 | 測るもの |
|---|---|---|
| 書き出す | `op.enumerate` | 候補の**漏れ**と**重複**の数。正解率ではない |
| 逆にたどる | `op.reverse` | 生成物をつなぎ直して原料に戻せたか |
| 場合分け | `op.branch` | 枝の**立て忘れ**と、消えた枝の**消し忘れ** |
| 対称で数える | `op.dedupe` | 同一物を別に数えた回数 |
| 量で数える | `op.mole` | 物質量の比から個数を出す入力の正誤 |
| 差をとる | `op.subtract` | 引く官能基の選択と残りの式量 |
| 組み上げる | `op.assemble` | **既存のパズルモードがそのまま測定器** |
| 表にする | `op.matrix` | 空欄を残したまま先へ進んだ回数（§4 の盤面） |
| 枠を切る | `op.constraints` | 制約パネルの入れ忘れ（§2。忘れると候補が爆発するので自動で分かる） |
| 検算する | `op.altroute` | 正誤ではなく**実行率**。1通りに絞れたあと別ルートを見たか（§6） |

**正誤で測らないものが多い**のが要点。書き出しも場合分けも、間違いではなく
**漏れ**として現れる。候補数を持っているこのモードは、その漏れを機械で数えられる。

### スキルに用意するもの — 問題ではなく生成器

知識とスキルでは、**用意するものの性質そのものが違う**。

| | 知識（qa） | スキル |
|---|---|---|
| 実体 | 項目を**書いて貯める** | 項目を書けない |
| 量 | 283項目。増やすには人手が要る | **生成器で無限に作れる** |
| 正解 | 人が書く | **計算で持てる**（列挙エンジン・配分エンジン） |
| 人が書くもの | 問いと答え | **誤答の型だけ** |

「候補を漏れなく書き出せますか」という一問一答は作れない。書き出させて漏れを数えるしかない。
逆に、**正解は計算で持てる**のだから問題文を貯める必要が無い。用意するのは3つ。

1. **生成器** … パラメータから練習1問と正解をつくる
2. **梯子** … パラメータの並び。候補数で機械的に難易度が決まる
3. **誤答の型** … 何を間違えたかの名前。**ここだけ人が書く**

3つめが要。これが無いと「できなかった」までしか言えず、対策に落ちない。
「漏れ」と「重複」は同じ不正解でも打つ手が正反対になる。

`_解析/tools/gen-skill-drill.js` に試作済み（**梯子21段すべて期待値と一致**）。

| スキル | 生成器 | 梯子 | 誤答の型 |
|---|---|---|---|
| 書き出す | `enumerate` | C3H7− 2 → C4H9− 4 → C4H10O 7 → C5H11− 8 → C5H12O 14 → C5H10O 74 | 漏れ／重複／価標違反 |
| 場合分け | `allot` | C4H8 2 → C6H12O 6 → C14H20＋環1 2 → C12H14O2＋環1 27 | 枝の立て忘れ／消し忘れ／酸素を配らない |
| 対称で数える | `dedupe` | 同じ分子を原子の並び順を変えて混ぜ、グループ分けさせる | 別のものとして数える／同じものとして数える |
| 差をとる | `subtract` | 分子量60 → 90 → 122 → 150 | 式量の取り違え／不飽和を考えない |

**難易度を人が決めなくてよい**のが効く。書き出しの梯子は候補数（2→4→7→8→14→74）が
そのまま段になる。`_解析/db/moves_qa_map.json` の `skills[].drill` が正。

生成しないスキルもある。**組み上げる**は既存のパズルモード（`stages.json`）がそのまま梯子なので
新しく作らない。**表にする／枠を切る／検算する**の3つは実問題の中でしか観察できないので、
単独の練習にせず `_解析/db/narrowing/*.json` の中で見る。
**逆にたどる／量で数える**は生成器を作れるが未着手（前者は `reactions.json` と、
後者は ratio アプリと噛み合わせる必要がある）。

⚠ **`skill.textbook-scope`（教科書が説明している範囲かで絞る）だけ操作にも生成器にも落ちない。**
これは操作ではなく出題のメタ知識なので、スキルから外して
`qa/KNOWLEDGE_CAVEATS.md` 側へ移すのが筋。**未決**。

## 11. 既存資産との関係

| 使うもの | 出どころ |
|---|---|
| `enumerateConstitutionalIsomers` | `chemistry.js`（`audit.html` のライブラリ検査で既に使用） |
| `findFunctionalGroups` / `findAnyCycle` / `isAsymmetricCarbon` / `canonicalCode` | `chemistry.js` |
| `layoutMolecule` | `chemistry.js` |
| `R` 擬似元素 / `iupacAlkylGroupName` | `chemistry.js`（アルキル基の書き出し練習） |
| モードの追加 | `.mode-tab[data-mode="..."]` → `game.js` の `setMode` |
| 配分エンジン | `_解析/tools/allot-unsaturation.js`（試作済み・移植する） |
| 条件の述語・候補数 | `_解析/tools/count-candidates.js`（試作済み・移植する） |
| ルート探索・条件の分類 | `_解析/tools/search-routes.js`（試作済み・移植する） |

**新しい化学ロジックは書かない。** 既にあるものの組み合わせで済む。

## 12. やらないこと

- **入口の作り込み**。導線は `DESIGN_entry_points.md` で全体をまとめて見直す方針なので、
  ここでは暫定のモードタブ1つに留める
- **重原子9個以上の構造列挙**。配分までで止める
- **立体異性体の列挙**。構造異性体だけ。立体は `DESIGN_stereochemistry.md` の系列で別に扱う
- **問題文の収録**。M4 で使うのは実験の条件だけで、問題文は入れない
  （`_解析/SCHEMA_問題DB.md` の著作権の扱いと同じ）
- **模範解答の文章の収録**。飛ばしている手順の範囲だけを持つ

## 13. 検算

`test.html` に足す。

- C4H10O が 7 通り（アルコール4＋エーテル3）
- C5H12O が 14 通り、うちアルコールが **8 通り**
  ← 北大 2021 後期3I の問題文「8種の異性体が存在し」と一致する
- 東大の実験列で 211 → 3、そこに「四員環」を足して 1
- 東大の B の極小ルートが **{ヨードホルム陽性, 四員環} の1通りだけ**
- ヨードホルム陽性の述語が、アセトアルデヒドとエタノールを**陽性**、
  メタノールとホルムアルデヒドを**陰性**と判定する
- 配分エンジンが C6H12O で 6 通り、C14H20＋ベンゼン環1 で 2 通りを出す

下から2番目は `moves.json` の注意書きに書いた取り違えで、**述語の実装がそこを踏んでいないか**を見る。

## 14. 動画レーンからの申し送り（2026-08-09）

**SNS 動画の題材として実機で撮ってみたところ、動画には使えたが「いま出せる形ではない」と判断した。**
**このモードの動画は保留**（ユーザー判断: 機能がまだ成熟していないため）。以下は撮って分かったこと。

### 14-1. 縦画面ではカードの文言が読めない

実測: 810×1440（ショート収録の解像度）でモーダルは 720×1296 と画面いっぱいに広がるが、
**実験カード 35枚のパレットが縦の半分以上を占め、そこの文字がいちばん小さい**。
1080×1920 へ引き伸ばしても読めない。一方で

- **「残り N 通り」の数字**は読める
- **スタックに出る効き（`−44` / `11`）** も読める

つまり**数字は伝わるが、何をしたから減ったのかが伝わらない**。

**動画側の希望**: `.rec-short`（収録用のクリーン画面クラス）が付いているとき、
**`#nw-palette` を隠せる**と数字とスタックだけの画になって成立する。
カードは台本から `button` + `contains` で押すので、**画面に出ていなくても操作はできる**
（`tutorial.js` の `button` は文言で選べるように広げた・v944）。

### 14-2. 「順番で効きが変わる」は数字にちゃんと出る（これは良い）

東大 2021 前期1I で実測:

| 積んだカード | 残り | 効き |
|---|---:|---:|
| （開始。211 → エノール18種を除いて193 → 制約で55） | **55** | — |
| ヨウ素と水酸化ナトリウムで黄色の沈殿が生じた | **11** | **−44** |
| 臭素水を脱色しなかった | **4** | −7 |

**1枚目で44個が消える。** §1 の「順番で効きがまったく違う」が、そのまま画面の数字になる。
**5枚すべて積むと 3 まで行くが、途中2枚は1つも減らさない**（＝§1 の3つめ「冗長な条件」）。
ここは設計の狙いどおりに動いている。

### 14-3. §13 の検算と食い違う数字がある（要確認）

**§13 は「東大の実験列で 211 → 3」と書いているが、実験カード2枚（ヨードホルム陽性・
臭素水を脱色しない）を積んだ時点では 4 通りだった。** 5枚すべて積むと 3 になる。

- 動画の台本は実測に合わせて「実験2つで 4 通り」に直した
- **§13 の検算が「5枚すべて」を指しているなら食い違いではない**が、
  文面からはどちらとも読めるので、テストを書くときに枚数を明示してほしい

### 14-4. 撮り直すときの手順

デモは `assembler/demos-quiz.json` の `nw-todai-2021` にある（`sns: true`）。
**入試問題を `select` で選ぶので出題は決定的**（乱数なし）＝ 何度撮っても同じ数字になる。

```
node tools/record/record.mjs --demo=nw-todai-2021 --format=short --speed=2.0 \
  --base=http://localhost:8123/assembler/
```

等速だと 59秒、`--speed=2.0` で 31秒。
