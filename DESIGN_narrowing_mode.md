# 絞り込みモード 設計書

分子式と実験結果から構造を決めていく過程を、**候補が何通り残っているか**を見せながら操作させるモード。
実装前の合意用ドキュメント。

きっかけは入試問題の解析（`マイドライブ/化学/問題集/_解析/`）で、
東大 2021 前期1I の実験を順にかけると候補がこう減ることが計算で出たこと。

| | 候補 |
|---|---:|
| C6H12O の全構造異性体 | **211** |
| エノール18種を除く（単離できない） | 193 |
| 不斉炭素をちょうど1個（問題文の前提） | 55 |
| 実験5: カルボニル基が無い | 51 |
| 実験1: −OH をもつ | 26 |
| 実験2: 水素付加しない＝環 | 8 |
| 実験3: 酸化してケトン＝第二級 | 5 |
| 実験4: ヨードホルム陽性 | **3** |

**紙の上では絶対に見えない数字**で、ここから3つのことが分かった。

- **ヨードホルム陽性を最初に置くと 16**。順番で効きがまったく違う
- **実験だけでは B は3通りまでしか絞れない**。問イの「四員環をもつもの」で初めて1つに決まる
- **B を決めるだけなら必要な条件は2つだけ**（ヨードホルム陽性＋四員環）。残り5つは冗長

3つめは条件の部分集合を総当たりして出した（`_解析/tools/search-routes.js`）。
**どの順で解くか・どこまで条件が要るかは、人が判断しなくても計算で出る。**

## 1. 基本方針

- **候補を数えるのはアプリの仕事**。人は条件を選ぶだけにする。手で数えさせない
- **答えを当てさせるのではなく、絞り込む過程を見せる**
- **カードの順番を入れ替えられる**こと。効きの違いが見えるのが本体
- **複数の化合物を同時に追う**。入試の構造決定は A〜F が並ぶのが普通で、
  1つの候補集合を絞る形では実物に合わない（→ §4 マトリクス）
- 既存の作図・判定には**一切影響しない**。別モードとして足す（`DESIGN_ui_modes.md` の系列）
- 外部ライブラリを増やさない。列挙も描画も `chemistry.js` にあるもので済む

## 2. 入力は「分子式」ではなく「分子式＋制約」

東北大 2021後期3 を解いた答案は、**問題文の制約を3行の箇条書きにするところから始まっていた**。

```
・350 以下
・C, H, O のみ
・4員環以下はナシ
```

これは手順ではなく**候補集合の定義**で、列挙の入力そのもの。
モードの最初の画面は分子式の選択ではなく、**制約パネル**にする。

| 制約 | 効き方 |
|---|---|
| 分子式 | 列挙の元。`enumerateConstitutionalIsomers` に渡す |
| 分子量の上限 | 分子式が未確定のとき、候補の分子式を切る |
| 含まれる元素 | 同上 |
| 4員環以下は無い / 環は無い | 列挙後のフィルタ |
| 不斉炭素が n 個 | 同上 |
| **エノールを除く**（既定でオン） | 同上。単離できない形を候補から外す（v931） |

### ⚠ エノールで2つ別のことが起きる（神奈川大3 で判明・2026-08-08）

神奈川大3 の C3H6O は9通り。「臭素水を脱色し、ナトリウムとは反応しない」で1通りに決まるはずが、
最初の実装では **3通り**残った。原因を「エノールは教科書が扱わないから答えにならない」と読んだが、
**これは誤診だった。** 実際には別々の2つが重なっていた。

**① 述語のバグ（本命）。** `findFunctionalGroups` は C=C に直結した −OH を
`alcohol1/2/3` ではなく **`enol` として返す**。`startsWith('alcohol')` だけで見ていたため、
エノールが「−OH をもたない」側に落ちていた。**エノールにも −OH はあるのでナトリウムとは反応する。**
これを直すと、エノールを候補に残したままでも実験(c) は 1 通りに決まる
（つまり `skill.textbook-scope` を持ち出す必要はまったく無かった）。

**② それとは別に、エノールは答えにならない。** C=C に −OH が付いた形は単離できず、
すぐカルボニルに変わるので「化合物A」にはなれない。これは実験の判定とは無関係に、
**候補集合そのものから外すべき**もの。C6H12O では 211 のうち **18 がエノール**で、
残したままだと東大の途中の数が変わる（不斉炭素1個で 57 → 実際は **55**）。

つまり①は判定の誤り、②は候補集合の定義の問題で、**直す場所が違う**。両方入れた。
②を切れるようにしてあるのは、なぜ除くのかを説明する材料になるから。

**東大 2021 前期1I の数字は、エノールを除いた側が正しい**（冒頭の表もこの値に直した）。
26 から先は変わらないので、結論の 3 は動かない。

**問題文が「いずれも不斉炭素原子を一つだけもっている」と書いているなら、それは条件カードではなく制約。**
東大で 193 → 55 を担っているのはこれで、カードとして置くものではない。

## 3. 2つのエンジン

大きさで手法を変える。**どちらも試作して動作を確認済み**（`_解析/tools/`）。

### A. 配分エンジン（分子の大きさによらない）

不飽和度と酸素を**部品に割り振る**組合せを数える。構造は作らない。

```
C14H20 ＋「ベンゼン環1つ」  → 2通り（ベンゼン環＋C=C ／ ベンゼン環＋脂肪族の環）
C12H14O2 ＋「ベンゼン環1つ」 → 27通り
　　　　　＋「エステルをもつ」 → 2通り
```

部品は `ベンゼン環(不飽和度4)` / `脂肪族の環(1)` / `C=C(1)` / `C≡C(2)` /
`エステル結合(1, O2)` / `カルボキシ基(1, O2)` / `酸無水物(2, O3)` / `ケトン(1, O1)` /
`アルデヒド(1, O1)` / `ヒドロキシ基(0, O1)` / `エーテル結合(0, O1)`。

**芳香族はここで扱う。** 列挙が届かない大きさでも一瞬で終わり、
しかも人が実際に最初にやる作業（不飽和度6 ＝ ベンゼン環4 ＋ C=O 1 ＋ C=C 1）そのもの。

岡山大の答案が「O が4つ → 高々2価エステル → 不飽和度2を先に引く」から始まっていたのも同じ操作。

検算で分かったこと: **C6H12O にかけると6通りが出て、それが手で書く表の行と一致する。**

```
1. アルデヒド          4. C=C ＋ ヒドロキシ基
2. ケトン              5. 脂肪族の環 ＋ エーテル結合
3. C=C ＋ エーテル結合  6. 脂肪族の環 ＋ ヒドロキシ基
```

### B. 列挙エンジン（重原子8個まで）

`enumerateConstitutionalIsomers(elements, hCount, nodeLimit)` で構造を全部つくる。実測。

| 分子式 | 構造異性体 | 時間 |
|---|---:|---:|
| C4H10O | 7 | 0.0 秒 |
| C5H12O | 14 | 0.0 秒 |
| C5H10O | 74 | 0.2 秒 |
| C4H8O2 | 122 | 0.1 秒 |
| C4H6O2 | 263 | 0.2 秒 |
| C6H12O | 211 | 6.0 秒 |
| C5H10O2 | 400 | 2.9 秒 |
| C4H4O4 | 3328 | 80 秒 |

**重原子7個からブラウザでは重い。** C6H12O（211通り）と C5H10O2（400通り）は結果を JSON に焼いて
同梱する（静的配信なのでこれで足りる。`compounds.json` と同じ扱い）。`isomers-baked.json` は 47KB。

C4H4O4（重原子8個）は 3328通り・80秒で、**焼くには大きすぎる**。この領域は配分エンジンに任せる。

重原子9個以上は配分エンジンだけで進み、構造の列挙は出さない。

## 4. 画面 — マトリクスが主、候補数が従

**問題文に出てくる物質を列にとった表**が中心。条件カードを置くと、該当するセルが埋まる。

```
┌─ C6H12O ／ 不斉炭素1個・分子量350以下 ────────── [制約を編集] ┐
│                    A     B     C     D     E     F           │
│  −OH               ×     ○     ○     ×     ○     ○           │
│  C=C               ×     ×     ○     ○     ○     ○           │
│  C=O               ×     ×     ×     ×     ×     ×           │
│  環                ○     ○     ×     ×     ×     ×           │
│  アルコールの級     −    2級    3級    −    1級   1級          │
│  ヨードホルム       ×     ○     ×     ×     ×     ×           │
│  ────────────────────────────────────────────                │
│  残り候補          8     3    12    …                        │
└──────────────────────────────────────────────────────────────┘
  ▸ 積んだカード（並べ替え可）
     実験5 カルボニル基が無い     全体に効く   −4
     実験1 ナトリウムで水素が出る A・D と B・C・E・F を分ける
     実験4 ヨードホルム陽性       B のみ       −2
```

- **行が性質、列が化合物**。実験カードを置くと、その実験が言っている分だけセルが埋まる
- 各列の下に**その化合物の残り候補数**
- カードは**ドラッグで並べ替え**。順番を変えると候補数が全部引き直される
- 空のセルが「まだ決まっていないこと」を示す。**どこを埋めれば進むかが見える**

これは東大を解いた答案が実際に紙の上で作っていた表で、
**線形にたどるより取りこぼしが減り、どの欄が空かが見える**のが利点。
`_解析/db/moves_qa_map.json` では `skill.matrix` として記録している。

### 効きの表示

各カードの右に「−154」のような**減った数**を出す。**0 のカードは灰色**にする。
情報を持たないカードがひと目で分かるのがこの表示の狙い。

### 縦型収録（`?format=short`）ではパレットを隠す

**実験カード 35枚のパレットは `.rec-short` のとき隠す**（動画レーンの実測・2026-08-09）。
810×1440 だとパレットが縦の半分以上を占め、そこの文字がいちばん小さくて
1080×1920 へ引き伸ばしても読めない。一方「残り N 通り」と積んだカードの効き（−44）は読めるので、
パレットが消えれば**数字を追う画**として成立する。実装は `style.css` の `.rec-short #nw-palette`。

**隠しても台本の操作は止まらない**のが前提条件。カードは `button` + `contains` で**文言から**選び、
`tutorial.js` は rect=0 のボタンならカーソル演出を省いて `click()` だけ撃つ。
ここが崩れると**収録は成功したように見えて何も絞られていない動画**が焼けるので、
テスト N4 が「隠れている」と「隠れていても押せば積まれる」を同時に見ている。

## 5. 条件カードの語彙

`_解析/tools/count-candidates.js` に実装済みの述語をそのまま持ってくる。

**官能基**: −OH をもつ／もたない・第一級/第二級/第三級アルコール・エーテル・
アルデヒド・ケトン・カルボニルなし

**検出反応**: 銀鏡反応陽性・ヨードホルム陽性/陰性（`CH3-CO-` と `CH3-CH(OH)-` を実際に探す）・
臭素水を脱色

**骨格**: 環をもつ／もたない・**環の大きさが 3〜8**・C=C をもつ／もたない・**直鎖／枝分かれ**

**酸・エステル・ラクトン**: カルボキシ基をもつ／もたない・エステル結合をもつ・**環状エステル（ラクトン）**

**生成物から言うもの**: オゾン分解で1種類（＝左右対称）／2種類・カルボニルを2つもつ（＝還元して二価アルコール）・
**臭素を付加したジブロモ体の不斉炭素が2つ**（⚠ 元の分子ではなく付加後で数える）

**立体**: 不斉炭素がちょうど n 個

カードの文言は**実験の言い方**にする（「ナトリウムを加えると水素が発生した」）。
裏に述語が何かを出す（「＝ −OH をもつ」）。**実験と判定の対応を覚えるのがこのモードの副産物**。

## 6. ルート探索 — 「他の解き方」を計算で出す

条件は絞り込みなので**かける順番を変えても最後の候補集合は同じ**（フィルタは可換）。
順番が効くのは途中の候補数だけ。そこで2段に分けて調べる。

1. どの条件の**部分集合**で1通りに届くか → 極小のものが「可能なルート」
2. その集合を**どの順**でかけると速く減るか → 各段で最も減る条件を選ぶ（貪欲）

条件が8個なら 2⁸ = 256 通りの総当たりで済むので、**実時間で全部調べられる**。

そして各条件を自動で分類する。「冗長」で片づけず、**なぜ冗長なのか**を分ける。

| 判定 | 意味 |
|---|---|
| **必須** | どの極小ルートにも入る。外すと届かない |
| **代替あり** | ルートによって使ったり使わなかったり |
| **使わなくてよい** | どの極小ルートにも入らない |
| **冗長** | 他の条件を全部使うなら、外しても結果が変わらない |
| **重複** | 別の条件と残す集合が完全に同じ。どちらか一方でよい |
| **情報ゼロ** | 単独でかけても1つも減らない。制約から自動的に満たされている |

⚠ **「冗長」は化合物ごと**。東大の実験5は B を決めるだけなら冗長だが、A〜F 全体には必要。
画面でもこの区別を落とさないこと。

**画面での使い方**: 生徒が1通りに絞れたあと、「**他にも解き方があります**」として
別の極小ルートを出す。使わなかった条件は「これは使わなくても解けました」と灰色で示す。

## 7. 模範解答の飛躍を見せる

模範解答は「○○であることから、構造が決定できる」と1文で済ませることが多い。
**その一文が本当に決定しているかは、候補数が1になるかで判定できる。**

`_解析/db/problems.jsonl` の `collapsed` に、模範解答が1文で飛ばしている手順の範囲を記録している
（文言は写さず、どこを何手ぶん飛ばしているかだけ）。

- 岡山大: 「分子量90で不斉炭素をもつカルボン酸だから乳酸」＝ 実際は**3手**
- 名古屋大: 「F が不斉炭素をもつことから」＝ 実際は**2手**

M3 で入試問題を素材にするとき、**この範囲を1枚のカードにまとめて置ける**ようにする。
押すと中の手順が展開される。「模範解答はここを1文で済ませています」という表示。

## 8. 候補の見せ方（3段階）

211個を全部描くと破綻するので、**候補が減るにつれて表現を具体にしていく**。

| 候補数 | 見せ方 |
|---:|---|
| 20以上 | **配分カード**。「C=C ＋ ヒドロキシ基」のような部品の組み合わせと、その内訳の数だけ |
| 20〜4 | **部分構造カード**。確定した部分を描き、残りを `R−`（アルキル基）でまとめる |
| 3以下 | **実際の構造図**。`layoutMolecule` で描く |

真ん中の段が肝で、既存資産がそのまま使える。
`chemistry.js` には擬似元素 `R`（価標1・アルキル基の付け根）と
`iupacAlkylGroupName` / `iupacAlkylNameFromR` があり、アルキル基の書き出し練習で使っている。
**「C4H9− は4種類」「C5H11− は8種類」を1枚のカードとして扱う**のはこの機構の延長。

## 9. 段階案

### M1（最初に実装）: 制約 → カード → 候補数 — **実装済み（v929・2026-08-08）**

- 制約パネル（分子式のプリセット＋不斉炭素の数＋環の除外）
- カードを積む・外す・並べ替える
- 候補数と減った数を出す
- 候補は**数と配分カードだけ**（構造は描かない）
- 1通りになったら構造を1つ描いて終わり

**列は1つ**（単一の化合物）でよい。これだけで「順番で効きが変わる」は体験できる。

実装は `assembler/narrowing.js`。入口は学習メニューの `🔍 実験の結果から絞り込む`。
設計から変えたところが3つある。

1. **配分カードではなく「候補の内訳」を出した。** 配分エンジンを移植しなくても、
   **手元にある候補を部品構成でまとめれば同じ表になる**（「3員環 ＋ 第二級 −OH … 2」）。
   しかも抽象的な割り振りではなく**実在する候補の数**なので、こちらのほうが正確。
   配分エンジンが要るのは列挙が届かない M5（芳香族）から
2. **入口はクイズのアコーディオンに置かなかった。** `#learn-acc-quiz` は「3群に整理された」ことを
   EP4 が検査していて、4つめの群を足すと落ちる。そもそもこれはクイズではないので、
   書き出し練習と同じ階層に独立した `<details>` を置いた
3. **操作ログを M1 から吐かせた**（`op.constraints` / `op.card` / `op.reorder` / `op.solved`）。
   §10 で決めたスキルの測り方が実際に効くかは実データでしか分からないので、貯め始めておく

**C6H12O だけ `isomers-baked.json` に焼いた**（17KB）。node で 5.5 秒かかり、ブラウザで走らせると
その間 UI が固まる。生成は `_解析/tools/bake-isomers.js`。

実測で直したもの（どちらも**狭い画面で初めて出た**）:
- カードの行がグリッドの固定列だったため、幅 280px で 1fr が潰れて**1文字ずつ折り返した** → flex + wrap へ
- `<select>` の幅は**最長の option で決まる**。ヒントを option の文言に入れていたため
  モーダルごと横に溢れた（233px の枠に 369px） → ヒントを別行に出した

### M2: マトリクス — **実装済み（v932・2026-08-08）**

- 列を複数にして A〜F を同時に追う
- 実験カードが「A・D と B・C・E・F を分ける」型の効き方をするようにする
- 空のセルが残っているところが「まだ決まっていない」

実装で決めたこと。

**カードの側に「表のどこを埋めるか」を持たせた**（`row` と `cell`）。
「ナトリウムを加えると水素が発生した」は `row:'−OH', cell:'○'`。
表を別に組み立てるのではなく、**カードを積めばセルが埋まる**形にすると、
実験と性質の対応がそのまま画面の構造になる。

**候補集合（プール）は全列で共有し、積んだカードだけを列ごとに持つ。**
分子式と制約は問題全体にかかるもので、化合物ごとに違うのは「どの実験をしたか」だけ。
列を切り替えるとカードの積みとパレットの点灯が入れ替わる。

**何も積んでいないうちは表を出さない。** 空の表は情報がゼロで、画面を圧迫するだけ。

**空欄は「・」で出す。** ここが M2 の値打ちで、
「どこを埋めれば進むか」が見える。実測で東大 2021 前期1I を A〜C の3列で追うと、
A は5手で3通り、B は3手で8通り、C は1手で3通りと、列ごとに独立して減る。

3つめの列（アルデヒド1手だけ）で `ヨードホルム` の欄が「・」のまま残るのが、
**まだ調べていないことと、調べて陰性だったことを区別している**印。

M3 の準備として、列ごとの `trace()`（各段の残り候補）を切り出してある。

### M3: ルート探索 — **実装済み（v933・2026-08-08）**

- 1通りに絞れたあと「他の解き方」を出す
- 使わなくてよかった条件を灰色で示す
- 条件の性質（必須／代替あり／冗長／重複／情報ゼロ）を表で見せる

実装で決めたこと。

**探索の対象は「積んだカードだけ」**（パレットの全カードではない）。
積んでいないカードまで含めると「実は問題文に無いこの実験をすれば1手で済んだ」が出てしまい、
**「他の解き方」ではなく「別の問題」**になる。

**目標は「1通り」ではなく、いま実際に到達している数**にした。
1通りに届かない積み方でも「同じところへもっと少ない手で行けたか」は意味のある問いで、
こうすると極小集合が空になることも無い（全部使えば必ず届く）。
実際、東大は実験だけでは3通りまでしか絞れないが、**その3通りにも2手で行ける**と出る。

**2枚以上積んだら常に出す。** 1通りに絞れるまで待つと、
届かない問題（絞り切れない出題）で一度も出ないことになる。

**カードが13枚以上なら探索しない**と表示する。2ⁿ の総当たりなので、
黙って固まるより「調べませんでした」と出すほうがよい。実問題の実験は多くて8個。

検証で分かったこと: **設計書の冒頭に人が書いた結論を、アプリが自力で再現した。**

```
東大 2021 前期1I（C6H12O・不斉炭素1個・エノール除く 55通り）
  実験5つを積む            → 3通り。極小ルートは1つで、2手（ヨードホルム＋水素付加しない）
  ＋ 問イ「四員環をもつ」  → 1通り。極小ルートは {四員環, ヨードホルム陽性} の2手だけ
                              残り4手はすべて「冗長」
```

このために**環の大きさのカード（3〜6員環）を足した**。設計書 §5 の「骨格」に挙げていながら
M1 で入れ忘れていたもので、これが無いと東大の問イが表現できない。

### M4: 出題（入試問題を素材にする） — **実装済み（v935・2026-08-08）**

- `_解析/db/narrowing/*.json` を読み込んで、実際の入試問題の実験列をカードにする
- `collapsed` を使って「模範解答はここを1文で済ませています」を表示
- **絞り切れない問題**（神戸大のような出題ミス）も、そのまま見せる価値がある

翻訳は `_解析/tools/build-narrowing-data.js` が行い、`assembler/narrowing-problems.json` を書く。
現在12問・28列。**対応が付かない条件は黙って捨てず、生成器が落ちる。**

### M7: 元素分析から分子式へ — **実装済み（v1036・2026-08-10）**

**構造決定でいちばん多い出だしが、まるごと欠けていた。**
2022年版の「構造式の決定」10問のうち未収録だった6問を読んだところ、**3問が元素分析から始まっていた**
（兵庫県大・工5／関西大3 iii／甲南大3）。topic「元素分析，分子式の決定」は12巻で40問あり、
**そこに載らない問題も同じ入口を使う**（兵庫県大・関西大 iii がその例）。

```
燃焼の質量 → 各元素の質量 → 物質量 → 最簡整数比 → 組成式 → ×n → 分子式 → 絞り込みへ
```

⚠ **ここは測定値を扱う唯一のパネル。** 列挙も配分も断片も入力は整数（分子式）だが、
こちらは小数で、しかも丸めた値が来る。**割り切れなさを黙って丸めると、
誤った分子式を自信を持って出す**ことになるので、割り切れなさは隠さず出す。

実装で踏んだ穴が3つ。どれも実問題が教えてくれた:

1. **最小で割るだけでは最簡比にならない。** 甲南大3 は C:H = 0.75 : 1.0 で、
   最小で割ると 1 : 1.33 にしかならない。**3倍して初めて 3 : 4** になる。
   1〜12倍を順に試し、全部が整数に十分近くなった最初の倍率を採る
2. **酸素ゼロと入力ミスを区別する。** 甲南大3 は炭化水素なので O がちょうど 0 になる。
   測定値の丸めで少し負に振れるので、試料の 2% までは 0 とみなし、それを超えたら断る
3. **分子量の与えられ方が3通りある。** 「74であった」（割り算）／「300以下である」（収まる最大の n。
   関西大3 iii）／気体の状態方程式から（甲南大3）。**上限型は1つに決まったと言わない**

`db/narrowing/*.json` の `ea` に測定値を置き、`build-narrowing-data.js` が
`expect`（出るはずの分子式）と突き合わせる。⚠ 押すまで入力欄に入れない（列挙・断片と同じ）。

#### M7 に足した3つの条件（ユーザー指摘・2026-08-10）

**窒素則 —— H の偶奇・分子量の偶奇・N の個数の偶奇は、同じ1つの条件。**

```
不飽和度 (2C + 2 + N − H)/2 が整数      ⇔ H ≡ N (mod 2)
分子量 = 12C + H + 16O + 14N で 12C・16O・14N はどれも偶数 ⇔ 分子量 ≡ H (mod 2)
                                        ∴ 分子量の偶奇 = 窒素の数の偶奇
```

**分子量だけで窒素の数の偶奇が決まる**（ハロゲンを含まないとき）。
分子量146 のアミノ酸 → 偶数 → N は偶数 → アミノ酸なので0ではなく**2個**（リシン C6H14N2O2）。
関西大3 iv の C18H19NO3 は分子量297 ＝ 奇数で、N が1個であることと整合する。

倍率 n を絞る条件は3つ。効きは実測で大きい ——
**組成式 C2H5O・分子量300以下は、条件なしなら 1〜6倍の6通りだが、①②だけで2倍の1通りに決まる**
（4倍・6倍は不飽和度が負になる）。

1. **窒素則** H ≡ N (mod 2)
2. **不飽和度が 0 以上・炭素数以下**
3. **酸素の数の下限**（エステルの加水分解が言えれば O は2個以上、など）

**割る相手は「最小」ではなく「酸素」にする。** 入試に出る有機物は酸素がいちばん少ないのがふつうなので、
**O が1になるように規格化**すると人が紙でやる手順と同じ形になり、途中の数字が答案と突き合わせられる。
酸素を含まない炭化水素のときだけ最小で割る（甲南大3）。どちらで割っても最簡比は同じだが、
**途中の数字が答案と揃うかどうか**が違う。

### M8: 反応させた結果を数える — **実装済み（v1037・2026-08-10）**

**今までのカードと性質が違う。** 他は「その分子がどんな性質をもつか」を見るが、
これは「**反応させたら何種類できるか**」を見る。早稲田大 2021-3(1) がこの型。

C5H12O のアルコール8種は、脱水生成物の種類数だけで割れる:

| 種類数 | 個数 | 中身 |
|---:|---:|---|
| 1種 | 3 | 1-ペンタノール / 2-メチル-1-ブタノール / 3-メチル-1-ブタノール |
| 2種 | 2 | 2-メチル-2-ブタノール / 3-メチル-2-ブタノール |
| 2種（シス-トランスの組） | 1 | 3-ペンタノール |
| 3種（うち2つがシス-トランス） | 1 | 2-ペンタノール |
| 0種 | 1 | 2,2-ジメチル-1-プロパノール（**隣の炭素に H が無い**） |

数え方は入試に合わせる ——
**構造の違うアルケンを数え、そのうち C=C がシス-トランスを生むものは2つと数える。**
鏡像異性体は分けない（脱水で両方できるので「別の生成物」とは言わない）。

⚠ **反応を2つつなぐ判定も要る。** 早稲田の A は「脱水生成物を**さらにオゾン分解**すると
アルデヒドとケトン」で1つに決まる（`dehyd-ozone-ak`）。1種類しか出ない3つのアルコールは
これでしか分けられない。

⚠ **0種類はエーテルも満たす**（そもそも脱水しない）ので、対照列には −OH の条件が要る。
これを忘れると 1 のはずが 7 になる（実際にやった）。

### M9: 環に置換基を並べる — **実装済み（v1038・2026-08-10）**

**列挙が届かないのは「組合せ爆発」が理由であって、解析ができないわけではない。**
関西大 2021-3 iii（C10H20O・重原子11）は列挙エンジンの射程外だが、問題文が
「シクロヘキサン環の3つの炭素に −OH・枝分かれアルキル基・直鎖アルキル基が1つずつ」まで
枠を絞ってくれる。**枠の中だけ数えれば 6×5×4＝120 → 対称でまとめて10通り**にしかならない。

配分エンジン（M5）と同じ思想 —— 構造を全部つくらず、**問題文が与えた枠**の中だけ数える。
候補集合が「分子式の全異性体」でない例なので、`NARROW_FORMULAS` に `ring: {size, subs}` を
持たせて `buildPool` を分岐させた。積むカードは既存のものがそのまま効く。

⚠ **並べ方を知らないと判定できないカードがある。** 「どれが枝分かれをもつアルキル基か」は
分子の形からは読めないので、`ringPlacements` が `_nwPlacement`（どの置換基がどこに付いたか）を
分子に持たせ、カードはそれを見る。

⚠ **まとめ方は環の対称を手で書かず、正準コードに任せる。** 回転・反転を数え上げると
置換基が同じもの同士のときに二重に落とす事故が起きる。

⚠ **答えはメントールではない**（私が取り違えた）。メントール（メチルが5位）は
「−OH と隣接」も「不斉炭素3つ」も満たすが、**イソプロピルを H に換えた B に不斉炭素が2つ残る**
ので落ちる。最後の条件はメントールを外すためにある。正解は −OH 1位・イソプロピル 2位・メチル 4位。

なお **「不斉炭素が3つ」は1つも絞らない**（10通りすべてが該当する）。
問題文にある条件が必ず効くとは限らない実例として、列に残してある。

### M10: 窒素を含む分子 — **実装済み（v1038・2026-08-10）**

⚠ **見立てが外れていた。列挙エンジンも断片エンジンも、最初から N を扱えていた。**
C3H7NO2 は393通り出るし、アミンは級（`amine1/2/3`）まで検出される。
不飽和度も `(2C+2+N−H)/2` で N を数えている。**足りなかったのはカードのほう**で、
アミノ基・アミド・ニンヒドリンの言い方が1つも無かった。

関西大 2021-3 iv（C18H19NO3）で 345通り（393−エノール48）が1通りになる:

```
塩酸に溶けて水層へ（アミノ基）→ 120
メタノールでエステルになる（カルボキシ基）→ 3
ニンヒドリンで青紫（第一級のα-アミノ酸）→ 1 ＝ アラニン
```

⚠ **青紫になるのは第一級のα-アミノ酸だけ。** 第二級（N-メチルグリシン）は黄色。
実測で、カルボキシ基とアミノ基をもつ3件のうち**ここで2件が落ちる** ＝ 決め手になっている。

⚠ **元素の並びはヒル式**（C・H のあとはアルファベット順 ＝ N が O より先）。
以前は `C18H19O3N` と出しており、生徒が問題文の `C18H19NO3` と見比べられなかった。

⚠ **加水分解は切る結合1本あたり H2O を1つ**。A はアミド1つ＋エステル1つで
O を3つ使い切るので **2本切れて3つに割れる**（`hydrolysis` の valence に 2 を渡す）。

#### 収録の上限は 12/18 —— 残り6問は「まだ書いていない」のではない（2026-08-10 に数えた）

解析済み18問のうち、絞り込みの仕様が書けるのは12問で打ち止めになる。
残りが埋まらない理由は**手が回っていないからではなく、問題の型が違うから**なので、
ここを書き残しておかないと「未着手6問」に見えて、何度でも数え直す羽目になる。

| 問題 | 収録しない理由 |
|---|---|
| 香川大4・静岡大4 | 語句補充＋定義式の計算型。**そもそも絞り込みをしない**（手数が少ないのは易しいからではない） |
| 東海大5 | 有機の大問だが水和水の質量計算（無機）が混ざる。手筋の射程外 |
| 東京工大・前11 | 合成経路をたどる型。問われているのは反応の知識そのもので、手筋がほとんど働かない |
| 東京医科歯科大・医3 | 数え上げの対象がアルキル基でなく**ハロゲンの組み合わせ**。列挙エンジンは C・H・O しか扱わない |
| 東北大・後3 | 糖（グルコース／ガラクトース）。分子式が与えられず、候補集合が構造異性体の集合にならない |

つまり**解析済みの18問の中では**構造決定型をすべて収録した、という意味であって、
**母集団に対しては 2022年版の一部でしかない**（2026-08-10 に訂正）。実際の母数は下のとおり:

- topic「構造式の決定」は 2022年版に **10問**（12巻では74問）。うち収録は当初4問だった
- **索引の topic では構造決定を数え切れない。** 収録済み12問のうち同 topic に載るのは4問だけで、
  残り8問は `異性体` `アルケンと誘導体` `アルコールとエーテル` `エステル，油脂` から拾っている。
  実際の母数は `異性体`50 + `元素分析，分子式の決定`40 あたりまで広がり、12巻で 150〜200問規模

**未収録の6問を実際に読んだら、設計の穴が4つ出た**（M7 はその1つ目）。
「収録済み分では通っている」は「設計が耐える」の証拠にならない、という教訓そのものになった。

なお学習院大3 3-1 は**分子式が与えられていない**型で、収録できないと一度は思われたが、
M6 の「式量 → 組成 → 不飽和度で1つに絞る」がそのまま効いた（式量70 → C3H2O2 / C4H6O / C5H10 →
不飽和度1で C5H10）。**M6 は断片専用の道具ではなく、分子まるごとにも使える。**

**述語の語彙は解析側とアプリ側で意図的に別**にしてある。
解析側（「ヒドロキシ基あり」）は**何を言っているか**、アプリ側（`na`）は**どう操作するか**を表す。
橋を1か所に集めておけば、どちらかを増やしてもここだけ直せばよい。

⚠ **不斉炭素と環の有無は、制約かカードかを仕様が決める。**
東大の「いずれも不斉炭素原子を一つだけもっている」は問題文の前提＝制約だが、
東京都立大の「A・F・G には光学異性体が存在した」は実験＝カード。
**機械には見分けが付かない**ので、仕様側に `asConstraint` の印を置いた。
混ぜたときに何が起きるかは実測した ——
**片方の列の実験が、もう片方の候補集合まで削る**（東京都立大の母数が 14 → 4 になり、検算が4件落ちた）。

⚠ **カードは積まずに列だけ渡す。** 積んだ状態で渡すと答えを見せることになる。
どの実験があるかは「この列の実験」として別に並べ、置くのは生徒の仕事にする。

### M5: 芳香族（配分エンジン単体） — **実装済み（v935・2026-08-08）**

> ⚠ **配分だけでは大きい分子の問題は解けない。** 3問を数えたところ、配分が担うのは 14% で、
> 主役は「分子を断片に割る」手（41%）だった。断片は列挙の射程に落ちるので、
> **配分側にカードを作り直すのではなく、断片に割る層を挟む**。→ [DESIGN_fragment_split.md](DESIGN_fragment_split.md)

- 重原子9個以上は配分だけ。「ベンゼン環1つ・エステル1つ」で 27 → 2 のような絞り込みを見せる
- 構造は描かないが、**割り振りが決まる**ところまでは追える

**列挙と配分は画面ごと分けた。** 同じ「制約」でも意味が違う（構造の集合 vs 割り振りの集合）ので、
1つのパネルに混ぜると何を絞っているのか分からなくなる。行き来はタブでする。

実測: C24H38O4（重原子24個）＋ベンゼン環1＋エステルで 12 通りが **0ms**。
列挙エンジンは8個で頭打ちなので、この領域は配分でしか追えない。

#### エステルはこの順で考える（ユーザー指摘・2026-08-08）

```
① 酸素の数から価数を決める     エステル結合1つで酸素2個。O2 なら1価、O4 なら高々2価
② 不飽和度が余ればラクトン     C=O のぶんを引いても余る → 環状エステルを疑う
③ 2価以上なら結合の向き       R−COO−R−COO−R ／ R−COO−R−OCO−R ／ R−OCO−R−COO−R の3通り
④ 2価以上なら 1:1 ではない    加水分解で同じアルコールが2分子出ることがある
```

**①②は配分エンジンが数え上げる。③④は構造の話なので数え上げでは出ない**ため、
酸素の数に応じて注意書きとして画面に出す。④はとくに落としやすく、
「加水分解して A と B が 1mol ずつ」と決めてかかると取りこぼす。

`_解析/db/moves.json` では `ester-valence-from-oxygen` → `lactone-ring` → `ester-hydrolysis` の
3つの手筋に分けて、注意書きでこの順序を明示してある。

## 10. スキルの測定 — このモードが測定器を兼ねる

`/qa/` は**事実**を測る道具で、「候補を漏れなく書き出せますか」とは訊けない。
書き出させて漏れを数えるしかない。**スキルを測れる場所はこのモードしかない。**

入試15問から出た手筋43件を分解すると、こうなっていた（`_解析/db/moves_qa_map.json`）。

- 手筋はすべて知識に根を持つ。**スキルだけで成り立つ手筋は0件**
- 知識のみで出る手筋 16 件 ／ 知識＋スキル 27 件
- 知識と qa は **1:1 ではない**。純粋な1:1 は3件だけで、平均すると1手筋あたり qa 2項目弱の束

そこで**スキル1つにモードの操作1つを割り当てる**。`skills[].op` がその対応。

| スキル | 操作 | 測るもの |
|---|---|---|
| 書き出す | `op.enumerate` | 候補の**漏れ**と**重複**の数。正解率ではない |
| 逆にたどる | `op.reverse` | 生成物をつなぎ直して原料に戻せたか |
| 場合分け | `op.branch` | 枝の**立て忘れ**と、消えた枝の**消し忘れ** |
| 対称で数える | `op.dedupe` | 同一物を別に数えた回数 |
| 量で数える | `op.mole` | 物質量の比から個数を出す入力の正誤 |
| 差をとる | `op.subtract` | 引く官能基の選択と残りの式量 |
| 組み上げる | `op.assemble` | **既存のパズルモードがそのまま測定器** |
| 表にする | `op.matrix` | 空欄を残したまま先へ進んだ回数（§4 の盤面） |
| 枠を切る | `op.constraints` | 制約パネルの入れ忘れ（§2。忘れると候補が爆発するので自動で分かる） |
| 検算する | `op.altroute` | 正誤ではなく**実行率**。1通りに絞れたあと別ルートを見たか（§6） |

**正誤で測らないものが多い**のが要点。書き出しも場合分けも、間違いではなく
**漏れ**として現れる。候補数を持っているこのモードは、その漏れを機械で数えられる。

### スキルに用意するもの — 問題ではなく生成器

知識とスキルでは、**用意するものの性質そのものが違う**。

| | 知識（qa） | スキル |
|---|---|---|
| 実体 | 項目を**書いて貯める** | 項目を書けない |
| 量 | 283項目。増やすには人手が要る | **生成器で無限に作れる** |
| 正解 | 人が書く | **計算で持てる**（列挙エンジン・配分エンジン） |
| 人が書くもの | 問いと答え | **誤答の型だけ** |

「候補を漏れなく書き出せますか」という一問一答は作れない。書き出させて漏れを数えるしかない。
逆に、**正解は計算で持てる**のだから問題文を貯める必要が無い。用意するのは3つ。

1. **生成器** … パラメータから練習1問と正解をつくる
2. **梯子** … パラメータの並び。候補数で機械的に難易度が決まる
3. **誤答の型** … 何を間違えたかの名前。**ここだけ人が書く**

3つめが要。これが無いと「できなかった」までしか言えず、対策に落ちない。
「漏れ」と「重複」は同じ不正解でも打つ手が正反対になる。

`_解析/tools/gen-skill-drill.js` に試作済み（**梯子21段すべて期待値と一致**）。

| スキル | 生成器 | 梯子 | 誤答の型 |
|---|---|---|---|
| 書き出す | `enumerate` | C3H7− 2 → C4H9− 4 → C4H10O 7 → C5H11− 8 → C5H12O 14 → C5H10O 74 | 漏れ／重複／価標違反 |
| 場合分け | `allot` | C4H8 2 → C6H12O 6 → C14H20＋環1 2 → C12H14O2＋環1 27 | 枝の立て忘れ／消し忘れ／酸素を配らない |
| 対称で数える | `dedupe` | 同じ分子を原子の並び順を変えて混ぜ、グループ分けさせる | 別のものとして数える／同じものとして数える |
| 差をとる | `subtract` | 分子量60 → 90 → 122 → 150 | 式量の取り違え／不飽和を考えない |

**難易度を人が決めなくてよい**のが効く。書き出しの梯子は候補数（2→4→7→8→14→74）が
そのまま段になる。`_解析/db/moves_qa_map.json` の `skills[].drill` が正。

生成しないスキルもある。**組み上げる**は既存のパズルモード（`stages.json`）がそのまま梯子なので
新しく作らない。**表にする／枠を切る／検算する**の3つは実問題の中でしか観察できないので、
単独の練習にせず `_解析/db/narrowing/*.json` の中で見る。
**逆にたどる／量で数える**は生成器を作れるが未着手（前者は `reactions.json` と、
後者は ratio アプリと噛み合わせる必要がある）。

⚠ **`skill.textbook-scope`（教科書が説明している範囲かで絞る）だけ操作にも生成器にも落ちない。**
これは操作ではなく出題のメタ知識なので、スキルから外して
`qa/KNOWLEDGE_CAVEATS.md` 側へ移すのが筋。**未決**。

## 11. 既存資産との関係

| 使うもの | 出どころ |
|---|---|
| `enumerateConstitutionalIsomers` | `chemistry.js`（`audit.html` のライブラリ検査で既に使用） |
| `findFunctionalGroups` / `findAnyCycle` / `isAsymmetricCarbon` / `canonicalCode` | `chemistry.js` |
| `layoutMolecule` | `chemistry.js` |
| `R` 擬似元素 / `iupacAlkylGroupName` | `chemistry.js`（アルキル基の書き出し練習） |
| モードの追加 | `.mode-tab[data-mode="..."]` → `game.js` の `setMode` |
| 配分エンジン | `_解析/tools/allot-unsaturation.js`（試作済み・移植する） |
| 条件の述語・候補数 | `_解析/tools/count-candidates.js`（試作済み・移植する） |
| ルート探索・条件の分類 | `_解析/tools/search-routes.js`（試作済み・移植する） |

**新しい化学ロジックは書かない。** 既にあるものの組み合わせで済む。

## 12. やらないこと

- **入口の作り込み**。導線は `DESIGN_entry_points.md` で全体をまとめて見直す方針なので、
  ここでは暫定のモードタブ1つに留める
- **重原子9個以上の構造列挙**。配分までで止める
- **立体異性体の列挙**。構造異性体だけ。立体は `DESIGN_stereochemistry.md` の系列で別に扱う
- **問題文の収録**。M4 で使うのは実験の条件だけで、問題文は入れない
  （`_解析/SCHEMA_問題DB.md` の著作権の扱いと同じ）
- **模範解答の文章の収録**。飛ばしている手順の範囲だけを持つ

## 13. 検算

`test.html` に足す。

- C4H10O が 7 通り（アルコール4＋エーテル3）
- C5H12O が 14 通り、うちアルコールが **8 通り**
  ← 北大 2021 後期3I の問題文「8種の異性体が存在し」と一致する
- 東大の実験列（**実験カード5枚すべて**）で 211 → 3、そこに「四員環」を足して 1
  - **枚数を書かないと実測と食い違って見える**（動画レーンの指摘・2026-08-09）。
    211 は全異性体で、エノール18種を除いて193、不斉炭素1個の制約で **55 が開始**。
    そこから**2枚**（ヨードホルム陽性 → 臭素水を脱色しない）では **11 → 4** で止まり、
    残り3枚を足して **3**。3 は5枚すべての数字で、2枚なら 4 が正しい
  - **同じ5枚でも順番で途中の数はまったく違う**（§1 の主張そのもの）。
    問題文の順（カルボニル無 → Na → 臭素水を脱色しない → 第二級 → ヨードホルム）は
    51 → 26 → 8 → 5 → 3 と滑らかに落ちるが、動画の順はヨードホルム1枚で **−44**。
    後者では**最後の2枚が1つも減らさない**（＝冗長な条件が目に見える）
  - テストは NW4（問題文の順・5枚）と NW5b（動画の順・2枚と5枚）で別々に止めている
- 東大の B の極小ルートが **{ヨードホルム陽性, 四員環} の1通りだけ**
- ヨードホルム陽性の述語が、アセトアルデヒドとエタノールを**陽性**、
  メタノールとホルムアルデヒドを**陰性**と判定する
- 配分エンジンが C6H12O で 6 通り、C14H20＋ベンゼン環1 で 2 通りを出す

下から2番目は `moves.json` の注意書きに書いた取り違えで、**述語の実装がそこを踏んでいないか**を見る。

## 14. 動画レーンからの申し送り（2026-08-09）

**SNS 動画の題材として実機で撮ってみたところ、動画には使えたが「いま出せる形ではない」と判断した。**
**このモードの動画は保留**（ユーザー判断: 機能がまだ成熟していないため）。以下は撮って分かったこと。

### 14-1. 縦画面ではカードの文言が読めない

実測: 810×1440（ショート収録の解像度）でモーダルは 720×1296 と画面いっぱいに広がるが、
**実験カード 35枚のパレットが縦の半分以上を占め、そこの文字がいちばん小さい**。
1080×1920 へ引き伸ばしても読めない。一方で

- **「残り N 通り」の数字**は読める
- **スタックに出る効き（`−44` / `11`）** も読める

つまり**数字は伝わるが、何をしたから減ったのかが伝わらない**。

**動画側の希望**: `.rec-short`（収録用のクリーン画面クラス）が付いているとき、
**`#nw-palette` を隠せる**と数字とスタックだけの画になって成立する。
カードは台本から `button` + `contains` で押すので、**画面に出ていなくても操作はできる**
（`tutorial.js` の `button` は文言で選べるように広げた・v944）。

### 14-2. 「順番で効きが変わる」は数字にちゃんと出る（これは良い）

東大 2021 前期1I で実測:

| 積んだカード | 残り | 効き |
|---|---:|---:|
| （開始。211 → エノール18種を除いて193 → 制約で55） | **55** | — |
| ヨウ素と水酸化ナトリウムで黄色の沈殿が生じた | **11** | **−44** |
| 臭素水を脱色しなかった | **4** | −7 |

**1枚目で44個が消える。** §1 の「順番で効きがまったく違う」が、そのまま画面の数字になる。
**5枚すべて積むと 3 まで行くが、途中2枚は1つも減らさない**（＝§1 の3つめ「冗長な条件」）。
ここは設計の狙いどおりに動いている。

### 14-3. §13 の検算と食い違う数字がある（要確認）

**§13 は「東大の実験列で 211 → 3」と書いているが、実験カード2枚（ヨードホルム陽性・
臭素水を脱色しない）を積んだ時点では 4 通りだった。** 5枚すべて積むと 3 になる。

- 動画の台本は実測に合わせて「実験2つで 4 通り」に直した
- **§13 の検算が「5枚すべて」を指しているなら食い違いではない**が、
  文面からはどちらとも読めるので、テストを書くときに枚数を明示してほしい

### 14-4. 撮り直すときの手順

デモは `assembler/demos-quiz.json` の `nw-todai-2021` にある（`sns: true`）。
**入試問題を `select` で選ぶので出題は決定的**（乱数なし）＝ 何度撮っても同じ数字になる。

```
node tools/record/record.mjs --demo=nw-todai-2021 --format=short --speed=2.0 \
  --base=http://localhost:8123/assembler/
```

等速だと 59秒、`--speed=2.0` で 31秒。

## 15. カードが多すぎて探せない（2026-08-16・ユーザー指摘）

> カードが多すぎて実際に使うと大変だと思います。ここを使いやすくする
> （カードを分類する・部分一致の検索機能を追加など）のがよいのでは

発注元は [video-scripts/ORDER_features_2026-08-15.md](video-scripts/ORDER_features_2026-08-15.md) の §D。

### 15-1. 実測（着手時）

| | |
|---|---:|
| カードの枚数 | **58枚** |
| `row` の種類 | **20** |
| 一覧の高さ（絞り込みなし） | **755px** |
| モーダルの窓（`max-height: 90dvh`） | 968px |

⚠ **発注書の「52枚 → 19行」は数え落としがある。** 52 は `NARROW_CARDS` の
リテラルだけの数で、**ループで足している環の大きさ 3〜8員（6枚・1行）が入っていない**。
正しくは 58枚・20行。発注書が引用している 1448px も 56枚のときの値で、今は数えていない。

### 15-2. 直し方 —— 減らすのではなく畳む

**58枚は「全部入っている」証**（2021年の入試19問を実際に解ける網羅性）なので、減らさない。

1. **`row` でまとめ、`cell` を選択肢として横に並べる。** カードは M2 のときから
   「自分は表のどこを埋めるのか」を持っている（`row: '−OH', cell: '○'`）ので、
   **一覧をその通りに描くだけ**で 58枚 → 20行になる。**データ変更ゼロ**
2. **タグで絞る。** 8つ（構造／分子式／アルコール／アルデヒド・ケトン／反応性／酸・塩基／窒素／立体）。
   **排他的な群ではない** —— ヨードホルム陽性は `CH3-CO-` と `CH3-CH(OH)-` の両方を指すので、
   **アルコールにもアルデヒド・ケトンにも出る**。タグは絞り込みの道具であって分類学ではない
3. **`say` を配列にする。** `test`（制約の実体）は1つのまま、実験文だけ増やせる形。
   銀鏡とフェーリングは同じ「アルデヒド」なので、**行を増やさずに**言い換えを足せる
4. **部分一致の絞り込み。** `say`（**全要素**）と `mean` の両方に当てる ——
   問題文を読んでいる人は「ヨウ素と水酸化ナトリウム」で、意味から入る人は「ヨードホルム」で探す

### 15-3. 決めたこと

**タグは `row` 単位で付ける**（`NARROW_ROW_TAGS`）。同じ行のカードは同じことを訊いていて
○ と × の違いしかないので、タグも同じでよい。58枚に1枚ずつ書くと、
**足したカードだけタグが抜ける**事故が起きる。

**行の台帳は1つ（v1397）。** マトリクスの行は手書きの `NARROW_ROWS`（16行）だったので、
後から足した**芳香環・環上の位置・アミノ基・アミドの4行が抜けたまま**だった
（カードを積むと候補はちゃんと絞られるのに**表には何も出ない**。窒素を含む問題は
まるごと表に映らなかった）。**4行を足すのではなく手書きの台帳ごと捨て**、
パレットと同じ `NARROW_ROW_ORDER`（カードの `row` の出現順）を使う ——
足すだけでは次にカードを増やした人がまた忘れる ＝ 同じ事故が起き続けるため。

⚠ **行の並びを変えたいときは、台帳を書き戻すのではなくカードの定義順を動かす。**
手書きの16行が持っていた「環の大きさは環のすぐ下」という並びは、
環の大きさのカードを `push` から配列の中（環のカードの直後）へ移して担保した。
「カードに出てこない行は表に出さない」性質は `renderMatrix` の `used` 側が持っている。
検査は `NW30`（4行とも出る・台帳が同一参照）・`NW31`（出さない側の否定対照）・
`NW32`（`test` の効きが変わっていない）。

**見える文字は `cell` だけ。実験の文と意味は消さずに隠す**（`.nw-sr`）。
読み上げの中身であり、**収録の台本が文言でカードを選ぶ受け口**でもある
（`#nw-palette button` + `contains`）。ここを落とすと
**収録は成功したように見えて何も絞られていない動画**が焼ける。

**絞り込みの帯を貼り付け（sticky）にして「残り N 通り」を添えた。**
一覧はモーダルのいちばん下にあるので、**カードを探しているあいだ `#nw-result` は画面の外**にある
（§14 の V99 が 400px スクロールして固定する回避策を採っていたのはこれが理由）。実測でも、
一覧まで下げると `#nw-result` は窓の外に出ていた ＝ 帯にカウンタを置かないと条件を満たせない。

**タグは1つだけ押せる**（押し直しで切り替え・もう一度押すと解除）。
複数押しの OR は「なぜこれが出ているのか」が読めなくなる。

### 15-4. やらなかったこと

- **入試問題モードで、その問題の札を押せるようにする**（発注書の 5）。おまけと位置づけられていた
- **② の冗長・重複を一覧の段階で消すこと。** これは**問題に依存する**ので、
  一覧で判定すると**答えを先に見せる**ことになる。冗長は積んだあとに言う（M3）のが正しい
- **`test` には一切触っていない。** 一覧の見せ方とタグ・検索だけで、答えは1つも変わらない

検査は **NW20**（まとめ方とタグ）・**NW21**（検索と `say` の配列）・
**NW22**（否定対照 —— 絞り込み中でも積んだカードを外せる／台本が文言で選べる）。

## 16. 札1枚ごとの実験番号 `stepNo`（2026-08-26）

**発注**: `video-scripts/ORDER_narrowing_stack_order_2026-08-19.md`
（V99 の収録中に「東大 1I の実験の順と札が問題文と違う」と見つかった）。
**ユーザーの決定は (C) ＝ 順は解き筋のまま・番号を並べて持つ。**

### 16-1. 形

```json
{"name":"B","stack":["carbonyl-no","na","h2-no","ox2","iodo"],
 "stepNo":[5,1,2,3,4],"expect":3}
```

- **`stack` の順は動かさない。** 解き筋の順が教材の実体で、既定の初期表示でもある。
  順番はユーザーが画面で入れ替えられる（§1）ので、`stack` は「最初に見える並び」でしかない
- **`stepNo` はその札が問題文の何番の実験かを並べて持つ。**
  アプリは「問題文どおりに並べる」を切り替えで出せる
- ⚠ **著作権上、持ち出すのは番号だけ。** 問題文も試薬名も入れない

### 16-2. 欄を持たない列（null で埋めない）

**50 列中 30 列が欄を持つ。** 残り 20 列は**欄ごと作らない**。
`null` だけの配列は「番号が無い」と「番号を落とした」を混ぜてしまうため。

| 持たない理由 | 例 |
|---|---|
| 番号が1つも拾えない ＝ そもそも問題文の実験ではない | 解析者が立てた**「対照」の列 5 本**（九州大 C・学習院大 B・滋賀医大 C/D・関西大3iv D） |
| 番号の系列が混ざる（どちらが先かを機械が知らない） | 東大 1I の **B′**（実験4 → 問イ） |

⚠ 系列が1つで**一部の札だけ**番号を持つ列は、持つ側だけ番号・残りを `null` にする（滋賀医大3 / B ＝ `[2,null]`）。

### 16-3. 見張り —— 何を赤にするか

**赤は「番号を持っているのに `stack` と対応が取れていない」ほうだけ。**

- 長さが `stack` と違う ／ 正の整数でも `null` でもない値 ／ 全部 `null` ／ `stack` が空なのに欄がある

⚠ **非昇順は赤にしない。** `stack` の順は解き筋の順なので、問題文の順と違うこと自体は誤りではない。
代わりに**どの列がそうかを名指しで固定**する（いまは東京大1Ⅰ / B の1本だけ）。

置き場所は `tools/audit-narrowing-order.js` の **[R]**（仕様ディレクトリ無しで走る）と、
`assembler/tests.js` の **NW33**（否定対照つき）。
`assembler/narrowing-problems.json` は**1問1行**にして `tools/verify-compounds.js` の検査0の仲間へ入れた
（`?v=` が付かないファイルなので `verify-release.js` の対象外＝ここが唯一の見張り）。

### 16-4. ⚠ 直さないと決めたもの

**「問題文の実験」と「積まれる札」の食い違いは、機械で数えられるぶんで4件ある。
直したのは東大の1件だけで、残り3件は直さない。**

| 問題 | 問題文の実験 | 積まれる札 | 直さない理由 |
|---|---|---|---|
| **神戸大3 / A** | 銀鏡反応を示さないカルボニル化合物 | `ketone` | 「カルボニル化合物である」は**前の実験からの与件**で、それを言う札がアプリに無い。`silver-no` に差し替えると expect が **2 → 6** で壊れる |
| **神戸大3 / B** | 同上 | `ketone` | 同上。**1 → 56** で壊れる |
| **熊本大3 / C** | 水素付加で酢酸エチル | `iodo` | 素直な札がアプリに無い（畳むしかない） |
| **⚠⚠ 立命館大・理工他4 / A・D** | **沸点の比較**（最も高い／最も低い） | A ＝ `na`「ナトリウムを加えると水素が発生した」／D ＝ `na-no` ＋ `carbonyl-no`「赤外吸収でカルボニル基が見られなかった」 | ★ **上の3件と違って「出さない」ほうを選んだ**（下の節） |

★ **神戸大の2件は、数字は正しく、ずれているのは画面の文言だけ。**
直すなら札を1枚足すか制約に回すかで、**単純な差し替えでは直らない**。

#### ⚠⚠ 立命館大・理工他4 は出荷しない（2026-08-26・検分の結果）

**生成器は作れるが、出荷リストから外した**（`NEVER_SHIP`）。上の3件が「文言だけずれた列を**出したまま**にする」
判断なのに対し、こちらは**問題ごと出さない**。分けた理由は3つ。

1. **ずれているのが「決め手そのもの」。** 神戸大は前の実験からの与件を1枚に畳んだだけだが、
   立命館の A・D は**沸点の比較が唯一の決め手**で、そこを別の札に置き換えると
   「この実験でこう絞れた」という筋書きが丸ごと別物になる
2. **差し替え先が無い。** 仕様は沸点の並びを3段の分子間力
   （水素結合 ＞ 極性の大きいカルボニル ＞ どちらも無し）に翻訳して数は合わせてあるが、
   **アプリのカード58枚に「沸点」を見る札は1枚も無い**。畳み先が無いので `iodo` のような
   代用も選べない
3. ⚠⚠ **audit の `LITERAL` に「沸点」が無いので、出しても永久に報告されない。**
   `tools/audit-narrowing-order.js` が「問題文の実験と札が違う」と言えるのは、
   **その語が見張りの語彙に載っているときだけ**。載っていない語は
   **食い違っていること自体が数えられない** ＝ 出荷すると、赤も黄色も出ないまま
   「Na を加えると水素が発生した」という**やっていない実験**が画面に居座る。
   ★ **見張れないものを出さない**、が判断の芯

⚠ 出したくなったら、順序は「**沸点の札を1枚足す** → 仕様の翻訳をそれに差し替える →
`LITERAL` に語を足す → `NEVER_SHIP` から消す」。**`NEVER_SHIP` から消すだけでは元の木阿弥。**

#### ⚠ 昭和薬大・薬4 の B列も落とした（同日・こちらは列だけ）

問題は出すが、**B の列だけ出荷しない**（`DROP_COLUMNS`）。理由は文言のずれではなく**列として成立しない**こと。

- B の決め手は問題文の「**メチレン基をもたない**」で、**その述語がアプリに無い**
- そのため B は同じ第二級・不斉ありの C と分けられず、**2通りで止まる**
- ⚠⚠ しかも **4枚目（ヨードホルム陽性）が1つも減らさない**（14 → 8 → 3 → 2 → **2**）。
  ★ このモードの主張は「**カードの効きの違いを見せる**」（§1）なので、
  **最後の1枚が 0 効きのまま終わる列は、主張の反例をそのまま教材にしている**ことになる
- 残る A・C・D はどれも1通りに決まるので、**問題としては成立する**（列 3本で出荷）

⚠ `note` に「**B は出していない**」と理由まで書いた。§16-6 の直しで、この断りは実際に画面へ出る。

### 16-5. 申し送り

- **語彙の穴は `h2-no` を塞いで残り2枚**（`branched` と、**沸点を見る札**）。
  ⚠ `branched` は誰も要求していないので触っていない。沸点は上の立命館が要求している
- **生成器は 26 問を作れる。出荷は 19 → 25 問**（2026-08-26 に6問足した。
  残り1問＝立命館は §16-4 のとおり**出さないと決めた**）。
  出したことのない問題を `--write` が黙って出荷しないよう、
  `_解析/tools/build-narrowing-data.js` に**出荷リストの門番**を置いた
  （出荷済みの id 集合が台帳。新しい問題を出すときだけ `--accept-new`）。
  ⚠ **出荷を増やすのはデータの追加ではなくアプリの変更**（NW9 などが数を数え直す）
- ⚠⚠ **`--accept-new` だけでは「出さないと決めた」を表現できない。**
  あの印は「まだ出していない問題」を全部通すだけなので、
  次に走らせた人が**判断ごと素通し**にする。理由つきの `NEVER_SHIP`（問題まるごと）と
  `DROP_COLUMNS`（列だけ）を別に置き、**`--accept-new` を付けても出ない**ようにした。
  ★ 出したくなったら表から消す ＝ **消すときに理由を読むことになる**のが狙い
- **`marker()`（label から実験番号を拾う関数）は `_解析/tools/narrowing-predicates.js` の1か所だけ**に置く。
  ⚠ 出荷する番号と検査する番号が別の規則になると、見張りが嘘をつく
- ⚠ **生成器の書き先は既定で本体ツリーの絶対パス。** worktree のレーンが `--write` すると
  **別レーンが編集中の本体を黙って書き換える**ので、`--app=<path>` で差し替えられるようにした
  （読み込む `chemistry.js` / `narrowing.js` も一緒に動く ＝ 自分のツリーの札で検算される）

### 16-6. ⚠⚠ `note` が画面に出ていなかった（2026-08-26・出荷の前に直した）

**`pickProblem` は「列が無くて割り方だけある問題」＝ 断片パスでしか `note` を描いていなかった。**
出荷19問のうち17問は列をもつので、**そちらの `note` は1文字も画面に出ていなかった**（実測16問）。

★ **これは出荷とは無関係の、M4 のときからある欠陥。**
⚠ 届かなかったのが「数字の読み方の断り」ばかりなのが問題で、**放っておくと出荷を増やした数だけ嘘が増える**:

| 問題 | 届いていなかった断り |
|---|---|
| 早稲田大 1(7) | **画面の 6・3・2 と模範解答の 7・5・4 は違う**（模範解答は鏡像異性体を別に数える） |
| 東京理科大6 | **書籍の答えは6**。この画面はシス・トランスを1つに数える |
| 神戸大3 | **模範解答でも G は2通り残る**（だから `expect` が 2） |
| 昭和薬大4 | **B の列は出していない**（§16-4） |

- `nwNote()` を1か所に置き、**断片パスと列パスの両方**から呼ぶ
- ⚠ `note` は仕様側で Markdown（`**…**`）で書かれている（**7問**が該当）。
  **逃がしたあとに** `<b>` へ直す（逃がす前にやると `<b>` ごと逃げる）
- 検査は **NW34**（否定対照4本 —— note を空にすると欄が消える／別の問題へ移ると居残らない／
  生の `*` が残らない／出典ごと消える）。★ 列パスの1行を戻すと**実際に赤くなり**、16問が名指しで並ぶ

⚠ **`note` は画面に出る ＝ 著作権の扱いが `label` と同じになった。**
NW9 の否定対照（`/問\d|次の|下線部|答えよ|求めよ|書け/`）に、
**東京薬大・東海大の `note` が実際に当たった**（「問1」「問2」。⚠ **「設問1」も `問1` を含む**ので言い換えでは逃げられない）。
現行の言い方に直したうえで、**同じ規則を生成器側にも置いた** —— NW9 が落ちるのは**出荷したあと**なので、
書き出す前に止めるほうが早い。

## 17. M12 加水分解生成物の性質でも絞る（v1505・2026-09-02）

**ユーザー要望（2026-08-31）:**

> ★ **別に対応したいこととして、アルコールの分子内脱水生成物との対応（頻出）、
> エステルの加水分解生成物の条件からの絞りこみがあります**

### 17-1. 先に測った（★ 実装より前に）

**① 入試での出方**（`qa/data/exam_usage.jsonl`・有機 **283 大問の悉皆**。
数え方は **`printed`（設問の印字番号）を Set に入れて重複を畳んだ**もので、
`count` の単純合計ではない ＝ 同じ大問が複数のコードから指されても1と数える）:

| 手筋 | 大問数 | 109手筋中の順位 |
|---|--:|--:|
| **エステルの加水分解からアルコールと酸を割り出す** | **51 / 283** | **5位** |
| **脱水生成物からアルコールの骨格を決める** | **28 / 283** | 18位 |

**② いま何があるか**（★ 発注書の見立てを **半分だけ** 支持した）:

- ⚠⚠ **脱水は「逆向きの引き方」がすでにある。** `dehyd1` / `dehyd2` / `dehyd3` / `dehyd-no` /
  `dehyd-cis` / `dehyd-cis-no` / `dehyd-ozone-ak` の **7枚**（M8・早稲田大 2021-3(1)）が
  まさに「脱水生成物からアルコールの骨格を決める」を担っている。**足りないのは逆引きではない。**
- ★ **エステルは本当に何も無かった。** `ester`（エステル結合をもつ）と `lactone` の2枚だけで、
  **出てきた酸・アルコールの性質を言う手が1枚も無い**。51/283 の手筋に対して札が0枚。

**③ 逆引きの母数**（`stages.json` ＋ `compounds.json` の **1,150 件**を全部分子にして数えた）:

| 何 | 数 |
|---|--:|
| 一価アルコール（O が1個でそれが −OH・C と O だけ） | 29 件 |
| そのうち分子内脱水でアルケンを作れる | 27 件（ベンジルアルコールとプロパルギルアルコールは β 位に H が無く 0 種） |
| できるアルケン | 25 種 |
| ★ **同じアルケンを作るアルコールが2件以上ある組** | **7 組**（どれも **ちょうど2件**） |
| エステル結合をもつ化合物 | 131 件（うちラクトン・環状酸無水物 7 件） |
| ★ **同じカルボン酸を出すエステルが2件以上ある組** | **15 組**（最大はギ酸の33件・酢酸の23件） |
| ★ **同じアルコールを出すエステルが2件以上ある組** | **19 組**（最大はメタノールの29件・エタノールの18件） |

⚠⚠ **脱水の逆引きは、構造上 2 件で頭打ちになる。**
C=C の炭素は2つしかなく、−OH はそのどちらかに付いていた ＝ **1つのアルケンを作るアルコールは
最大2種**。列挙でも確かめた（C₃〜C₇ の CₙH₂ₙ₊₂O を全列挙して逆引き。**組の大きさは 1 か 2 だけ**）:

| 分子式 | アルコール | できるアルケン | 出発物が2件の組 |
|---|--:|--:|--:|
| C₃H₈O | 2 | 1 | 1 |
| C₄H₁₀O | 4 | 3 | 2 |
| C₅H₁₂O | 8 | 5 | 5 |
| C₆H₁₄O | 17 | 13 | 11 |
| C₇H₁₆O | 39 | 27 | 27 |

★ **＝「このアルケンを作れるアルコールを全部書き出せ」は、答えがいつも2つ**（下限ちょうど）。
**書き出し練習の型としては中身が無い**（§19-3 の成立条件①は 2〜20種）。
これは「対応（1対1・1対2の突き合わせ）」であって「絞り込み」ではない。

### 17-2. 決めたこと —— **(b) 絞り込みモードの条件に足す**

**落とした案:**

- **(a) 書き出し練習の条件をもう1種類増やす** … 脱水側は上の理由で母数が空。
  エステル側は成立する（「加水分解するとギ酸が得られる C₆H₁₂O₂ のエステル」＝ **8種** など。
  ★ 2〜20種 かつ 名前が全部そろうお題は 酸側11件・アルコール側7件の **計18件**）が、
  ⚠ **答えの集合が既存のお題と1対1**になる ——「加水分解でギ酸が出る C₆H₁₂O₂ のエステル」の
  答えは **C₅H₁₂O のアルコール8種と同型**で、数えているものが増えない（見せ方だけが変わる）。
  そのうえ お題ボタン・`IP_SCOPES`・記録の鍵・命名の受け皿がまとめて要る。
- **(c) 実験モードの課題として出す** … `quests.json` はすでに `eq-ester-hydrolysis`
  （酢酸エチル → 酢酸）と `eq-ethanol-ethene` を持っている。**実験モードは順方向の「作れ」**で、
  「生成物の条件から出発物を絞る」向きを表せない。

**採ったのは (b)** —— ユーザーの言葉そのもの（「条件からの**絞りこみ**」）で、
**測って空いていた唯一の穴**でもある。カードは `{id, say, mean, row, cell, test}` のデータなので、
**新しい関数は下請け1本（`NW.hydrolysis`）＋その言い換え1本だけ**で足りる。

### 17-3. 足した7枚（行 `加水分解生成物`）

| id | 実験の言い方 | 意味 |
|---|---|---|
| `hyd-acid-formic` | 加水分解して得られたカルボン酸が銀鏡反応を示した | 酸がギ酸 |
| `hyd-acid-formic-no` | 〃 示さなかった | 酸がギ酸でない |
| `hyd-alc-1` | 得られたアルコールを酸化するとアルデヒドになった | 第一級 |
| `hyd-alc-2` | 〃 ケトンになった | 第二級 |
| `hyd-alc-3` | 〃 酸化されなかった | 第三級 |
| `hyd-alc-iodoform` | 得られたアルコールがヨードホルム反応を示した | CH₃-CH(OH)- |
| `hyd-alc-chiral` | 得られたアルコールに光学異性体が存在した | 不斉炭素をもつ |

⚠ **具体名のカード（「酢酸が得られた」）は作らない。** 分子式ごとに札が要るうえ、
**1枚で答えが決まる** ＝ ユーザーの原則（「参照するなら表全体であるべき／該当部分を
ピンポイントで提示しては答えを見ているのと同じ」）に反する。出すのは**性質**だけにして、
1つに決めるのは積み重ねの側の仕事にする。

**実測（滋賀医大 2021-3 と同じ C₅H₁₀O₂・エノール除外後 352 通り・エステルは9種）:**

| カード | 残る候補 |
|---|--:|
| `hyd-acid-formic` / `hyd-acid-formic-no` | 4 / 5 |
| `hyd-alc-1` / `hyd-alc-2` / `hyd-alc-3` | 4 / 2 / 1 |
| `hyd-alc-iodoform` | 3 |
| `hyd-alc-chiral` | 1 |

★ **7枚とも「0 でも全部でもない」** ＝ どれも置く意味がある（`NW35` の④が毎回測り直す）。

### 17-4. ⚠⚠ 切る向きは「アシル-酸素開裂」（間違えても数は合う）

`NW.hydrolysis` は **reactor.js の `cleaveAcylOxygen` と同じ向き**で切る ——
カルボニル炭素と架橋酸素の間を切り、**架橋の O はアルコール側に残る**。

⚠ **このレーンは最初、逆に切った**（O–R′ を切って酸側に O を足した）。**数はどこも合う**
（組の数も、エステルの件数も、候補の割れ方も）。**壊れるのは名前だけ**:
**ギ酸メチル → 「炭酸」と「メタン」**、酢酸フェニル → 「ベンゼン」。
★ だから `NW35` の①は **数ではなく `lookupCompoundName` の戻り値を名指しで**見る。

⚠ **ラクトン・環状酸無水物は組を返さない**（切っても1分子）。そこは `lactone` カードの持ち場で、
**同じことを2枚で言わない**。`NW35` の②が γ-ブチロラクトンで7枚とも false・`lactone` が true を見張る。

### 17-5. 検査 `NW35` の否定対照（★ 全部「実際に赤くした」）

| 壊したもの | 出た赤 |
|---|---|
| 切る向きを逆にする（O をアルコール側に残さない） | 「酸の側が『null』（期待 酢酸）」 |
| ラクトンの門番を外す | 「ラクトンが2分子に分かれている」 |
| 不斉を**もとのエステル**から読む | 「2-メチルブタン酸メチルで hyd-alc-chiral が立っている」 |
| `hyd-alc-1` を「加水分解できれば true」にする | 「酢酸イソプロピルで立つカードが …,hyd-alc-1,hyd-alc-2,…」 |
| 行のタグ台帳から `加水分解生成物` を落とす | 「hyd-acid-formic の row / tags が台帳から出ていない」 |

⚠⚠ **3番目は最初 素通りした。** 対照を **ギ酸sec-ブチル（立つ）／ギ酸イソプロピル（立たない）**
の2件だけで書いていて、**どちらも「エステルの不斉」と「アルコールの不斉」が一致する**ため。
★ 分ける1件が **2-メチルブタン酸メチル**（エステルは不斉炭素をもつのに、出てくるアルコールは
メタノール）。**否定対照は「壊して赤くなるか」を実際にやらないと、書いたつもりで空振りする。**

### 17-6. やらなかったこと（次の人へ）

- ★ **脱水の側は1枚も足していない。** 上の実測どおり **7枚がすでにある**。
  もし足すなら「**どのアルケンができたか**」を名指しするカードだが、
  それは分子式ごとの札になる（`NARROW_CARDS` は静的な配列で、id で8か所から引かれている）。
  ⚠ **候補集合から札を生成する形に変えるなら、その8か所ぜんぶが改修対象**になる。
- ★ **「加水分解して得られた酸とアルコールの炭素数が同じ」**は測ったが落とした
  （C₄H₈O₂ と C₄H₆O₂ で1件ずつしか当たらず、入試の言い方としても定型ではない）。
- ★ **フェノールのエステル**は `hyd-acid-*` の2枚でしか言えない（アルコールの3枚は
  フェノールに立たない）。芳香族の分子式のプリセットが無いので、いまは出番が無い。
