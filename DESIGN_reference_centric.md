# DESIGN_reference_centric.md —— 参考書を中心に据える構成（草案）

2026-09-19・design-refcentric レーン（Opus）。**設計書の草案。コードは1行も書いていない。**
⚠ **§2（部品化の方式）だけは後を引く** ＝ Fable の検収にかける前提で書いた。ほかの節は作りながら直してよい。

## 0. 結論だけ先に

- ★ **背骨は `/reference/`（面A）**。ハブの最初の入口を参考書の索引にし、アプリは「単体で価値が大きいもの」だけ別の入口を持つ。
- ★★ **部品化は「アプリのページをそのまま iframe で埋める ＋ 埋め込みの目印 `embed=1`」を推す**（§2-3）。
  同じ HTML・同じ JS が、元の URL と参考書の中の両方で動く。**アプリ側の作り直しは「看板を隠す」「高さを知らせる」「戻り道の帯を出さない」の3つだけ**（§2-4 (2)・検収で確定）。
- ★ ロジックだけの小さな部品（DOM に触らない IIFE・`half-marks.js` 型）は、例外として**参考書のページに直接読み込んでよい**（○× の印を本文の横に出す用）。
- ★ 学習の記録は**全アプリが同じオリジン**（chem.schoollenz.com）なので、localStorage は**既に共有されている**。
  決めるべきは「共有のしかた」ではなく**「どのキーを誰が書くか」**（書くのは持ち主だけ・読むのは持ち主の部品を通して）。
- ★ 最初の1本（半反応式）は3段: **段1 器と一覧・係数決定の埋め込み → 段2 暗記テスト（反応式を書かせるだけの問題）と参考書の往復 → 段3 本文の式の横に ○×未**。

---

## 1. 全体の構成

### 1-1. 画面と導線

```
ハブ /                         … 入口① 参考書の索引へ（大きく1つ）
 │                                入口② 単体で使うアプリ（小さく並べる）
 ├─ /reference/                 … 索引（区分 → unit → group。§31-1 のまま）
 │    └─ /reference/<id>/       … 1ページ。読む・見る・動かす・解く・試す の5層（§3）
 │          ├─ [埋め込み] アプリの一画面（iframe・押してから読み込む）
 │          ├─ [埋め込み] 一問一答（/qa/?codes=…&mode=choice・既存 §17-4）
 │          └─ [リンク]   単体のアプリ・練習ページへ（from=reference&page=<id>）
 ├─ 単体の入口: /assembler/ /muki/ /ion-equation/portal.html /ratio/ /qa/
 └─ 練習ページ（反応式を書かせるだけ）: /ion-equation/halfquiz.html など
        └─ 1問ごとに「詳しく見る → /reference/<id>/#<anchor>」
```

- ★ **参考書 → アプリ** は既存の `:::link app:`（§31-3）＝ 別タブではなく同じタブで飛び、受け側が「← 参考書へ戻る」を出す。
- ★ **アプリ → 参考書** は §6-4（ref-inorg-design）の宿題どおり、ページが在ってから1本ずつ張る。
- ⚠ 面B（assembler の資料ペイン）は**有機だけ**のまま（§31-1）。無機・理論の埋め込みは面Aだけで起きる。

### 1-2. 今のアプリの行き先（⚠ **仮**。作りながら1本ずつ見直す）

| アプリ | 単体の入口 | 参考書への埋め込み | 仮の理由 |
|---|---|---|---|
| パズルでみる有機化学 `/assembler/` | **残す（主）** | 面B（資料ペイン）＋面Aから `?open=reference` | 単体の価値が大きい。キャンバスは全画面が要る（§3-3） |
| 一問一答 `/qa/` | **残す** | 既に iframe（§17-4） | 単元をまたいで回す使い方が主。習得マップは単体でしか見られない |
| 異性体ページ `/isomers/` | 残す（検索の着地） | 参考書からリンク | 検索で入ってくる面。構成はいじらない |
| 無機 `/muki/`（アキネーター・系統分離など） | **残す** | 候補（系統分離は埋め込みの2本目に向く） | ゲーム性が強く単体で回る。抽選の画面は焼けない（§31-4） |
| イオンでみる化学反応式 `/ion-equation/` | ポータルは残す | **半反応式・酸化数・電池/電解は埋め込みが主** | ユーザー方針「反応式をつくる系統は基本的に埋め込み」 |
| 比例式でみる化学計算 `/ratio/` | 残す（プロトタイプ） | 量的関係の1問を埋め込み候補 | ion→ratio の依存の向きは変えない |

⚠ **「単体の入口を残す」と「埋め込む」は排他ではない**（元の URL は全部残す・§7-3）。表は「ハブで大きく見せるか」の仮決め。

---

## 2. ★★ 部品化の方式（Fable 検収の対象）

### 2-1. 制約（実測）

| | 事実 | どこで確かめたか |
|---|---|---|
| ① | ビルドなし・静的配信・vanilla JS | CLAUDE.md |
| ② | ⚠⚠ ion の `model.js` は**トップレベルの `const`/`function` が 204 個**（`SPECIES` など）。assembler の `chemistry.js` も 154 個 | `grep -c` |
| ③ | ⚠ 半反応式のページは `document.getElementById("stageNav")` など**固定の id に直付け**の IIFE（`halfreaction.js`・`halflist.js`） | ソース |
| ④ | ion の `style.css` は 2,472 行・`header`/`main`/`.step` など**素のセレクタ** | ソース |
| ⑤ | 面A（`/reference/<id>/`）は**生成した静的 HTML**。スクリプトは埋め込み・目次の2つだけ | `gen-reference-pages.mjs` |
| ⑥ | 既に **iframe の前例が1つ**ある: 一問一答（`EMBED_JS`・押してから読み込む・高さは **640px 固定**） | `gen-reference-pages.mjs` 255・616 行 |
| ⑦ | 全アプリが**同一オリジン** ＝ localStorage・postMessage・`contentDocument` がそのまま使える | 配信構成 |
| ⑧ | ion の `style.css` に **`vh`・`%` の高さは固定シート（`.sheet` の `max-height: 82vh`）だけ**。`html`/`body` は高さを持たない | `grep vh\|height: *100%`（検収・2026-09-19） |
| ⑨ | ⚠ 「← 参考書へ戻る」の帯（`.refBack`・便0b）は **`<header>` の外（直後）** に置かれている（320×568 でヘッダー 120px 以下の約束のため） | `ref-inorg-merge` の `header-ui.js` |
| ⑩ | ⚠⚠ `verify-release.js` の規則6（他アプリの `?v=` は相手の版）は **`../` の相対参照しか解けない**。ルート絶対 `/ion-equation/…` は `path.resolve` でドライブの根に解けて相手不明。さらに `?v=` を1つでも持った `.html` は**そのディレクトリごとに1アプリ**として数えられる（`reference/<id>/` が48アプリになる） | `tools/verify-release.js` `foreignApp`・`apps` の組み立て |
| ⑪ | ion の `canonical` は `index.html`・`portal.html` だけ。`halfreaction.html`・`halflist.html` には無い | `grep canonical ion-equation/*.html` |
| ⑫ | 埋め込み候補の2ページ（halflist・halfreaction）に **`href="#…"` のリンクと JS の `location.href=` 代入は無い** | `grep`（検収・2026-09-19） |

⚠ ② のため、**別アプリの「画面の JS」を同じページに2つ読むと、クラシックスクリプトの共有の字句スコープで `const` が衝突して SyntaxError** になる（面Bに ion を載せる、面Aに ion と muki を並べる、のどちらでも起きる）。

### 2-2. 候補の比較

| | A. iframe ＋ `embed=1` | B. 同じ JS をページに読み込む部品 | C. Web Components（Shadow DOM） |
|---|---|---|---|
| 同じコードが2か所で動くか | ★ **HTML ごと同じ**。分岐は `embed=1` の見た目だけ | 画面の JS を `mount(root)` 形に書き直せば可 | 同左＋`customElements.define` |
| 既存アプリの改修量 | ★ **小**（帯を隠す CSS・高さの通知・戻り方の3点） | ⚠ **大**。③の直付けを全部 root 起点に。②の全局所化（IIFE か ES module 化） | ⚠ **最大**。B の全部＋④を Shadow 内へ読み込み直す |
| CSS の衝突 | ★ 起きない（文書が別） | ⚠ 起きる（④）。接頭辞付けの書き直しが要る | ★ 起きない（Shadow） |
| グローバルの衝突（②） | ★ 起きない | ⚠ 起きる。ES module 化すれば解けるが、ion の全ページの読み込み順を変える工事 | ⚠ 同左（Shadow は JS を隔離しない） |
| 1ページに2部品 | ★ 可 | ⚠ 同じアプリなら可・別アプリは②で不可 | ⚠ 同左 |
| 高さ・スクロール | ⚠ **自分で合わせる**（2-4） | ★ 自然に流れる | ★ 自然に流れる |
| 読み込みの重さ | ⚠ 部品ごとに model.js を読む（1回ぶん・キャッシュ後は軽い） | ★ 1回 | ★ 1回 |
| 版とキャッシュ | ★ **親は子の `?v=` を知らなくてよい**（2-6） | ⚠ 親が持ち主の `?v=` に追従（規則6）。面Aは焼き直しも要る | ⚠ 同左 |
| 計測（GA4） | ⚠ 子が page_view を送る → 押してから読み込めば「解こうとした回数」と一致（§17-4 の判断をそのまま） | ★ 親だけ | ★ 親だけ |
| 検索 | ★ 本文は親に在る。子の URL は元ページとして別に在る | ★ | ★ |

### 2-3. ★★ 推し: **A（iframe ＋ `embed=1`）を主、B は「DOM に触らない部品」に限って従**

理由（強い順）:

1. ★★ **② が決定的。** B と C は「別アプリの画面を1ページに並べる」ために、ion（5,717 行）・muki・ratio の全ページの**読み込み方式を変える工事**が先に要る。
   ユーザー方針は「埋め込みと往復の使い分けは作りながら決める」＝ **1本目の前に全アプリを作り直す案は方針と逆向き**。
2. ★ **元の URL と参考書の中が「同じもの」だと機械で言える。** A は同じ HTML を読むだけなので、元のページの test.html が通れば部品も通る。B/C は「元のページ」と「部品」の2つの起動口ができ、片方だけ壊れる道が生まれる。
3. ★ **前例がある**（⑥ 一問一答）。面A の生成器・`REF18` の見張り・「押してから読み込む」をそのまま延ばせる。
4. ★ **版の結合が起きない**（2-6）。いまの事故の型（参照側の `?v=` 追従もれ・規則6）を増やさない。

**B を許す範囲（従）**: 次の3つを満たすファイルだけ、参考書のページに `<script>` で直接読んでよい。
- DOM に触らない・トップレベルは IIFE で**グローバルは名前1つ**（例: `window.HalfMarks`）
- 持ち主のアプリの test.html が node でも単体で試している
- 参照側の `?v=` は持ち主の版（CLAUDE.md「アプリ間で資産を共有したら…」）。⚠ **ただし面Aからの参照は規則6が解けない**（2-1 ⑩）＝ B を面Aに入れるのは段3で、その便の最初に §7-2 の規則を足す
→ 今この条件を満たすのは `ion-equation/half-marks.js` と `ratio/model.js`（既に ion が読んでいる）。**qa の記録を読む部品は無い**（2-5）。

⚠ **C（Web Components）は採らない**。Shadow DOM が解くのは④だけで、②③を解かない。A の外側に `<slz-embed>` という名前を付ける程度なら後から足せる（中身は iframe）。

### 2-4. 受け渡しの約束（A の中身）

#### (1) 開く状態の渡し方 ＝ URL だけ
```
/ion-equation/halfreaction.html?q=MnO4_red&embed=1&from=reference&page=half-reaction
```
- ★ 状態は**既存の受け口の引数**だけで渡す（`APP_TARGETS` の台帳・§31-3）。**埋め込み専用の引数を作らない**。足すのは `embed=1` だけ。
- ★ 原稿には URL を書かない（§31-3 と同じ）。生成器が台帳から焼く。
- ⛔ `postMessage` で状態を**送り込む**ことはしない（URL だけで再現できないと、元の URL で開き直したときに同じ画面にならない）。

#### (2) `embed=1` で子がやること（**3つだけ**。⚠ 検収で中身を入れ替えた・2026-09-19）
| | 何を | どこで | なぜ |
|---|---|---|---|
| ① | **`<head>` の同期な1行**: `embed=1` なら `<html class="embed">` を付け、**`<base target="_top">` を head に足す**。CSS は `html.embed header, html.embed footer, html.embed .refBack { display: none }` | 埋め込む HTML ごとに1行（インライン。`header-ui.js` は body の末尾で読むので間に合わない） | 看板を二重に出さない。描いてから消すと一瞬ちらつくので head で。`<base target>` は **`target` の無い `<a>` 全部**（`hbBack`・モードの帯・フッター）を1行でタブごと動かす ＝ リンクを1本ずつ直さない |
| ② | **高さを親に知らせる**（`header-ui.js`・全ページが読む）: `window.parent !== window` かつ `embed=1` のときだけ、`ResizeObserver(document.documentElement)` → `parent.postMessage({ type: 'slz-embed', v: 1, h: Math.ceil(高さ) }, location.origin)`。**前と同じ h は送らない** | `header-ui.js` | 親が iframe を中身の高さに合わせる ＝ **iframe の中にスクロールを作らない**（スマホ縦の二重スクロールが一番の事故） |
| ③ | **「← 参考書へ戻る」の帯を作らない**（`embed=1` が `from=reference` に優先） | `header-ui.js` の帯の条件に1つ足す | ⚠ 帯は `<header>` の**外**（2-1 ⑨）なので、header を隠しても消えない。親のページの中で「参考書へ戻る」は無意味 |

- ⚠ **`<base target="_top">` の落とし穴**: `href="#…"` だけのページ内リンクも top を飛ばす（子の URL がタブに出る）。埋め込み候補の2ページには無い（2-1 ⑫）が、**ion の test.html で「`embed` を受けるページに `a[href^="#"]` が無い」を見張る**。JS の `location.href=` 代入も `_top` の外 ＝ 同じ検査で「無い」を見る。
- ⚠ 高さは `postMessage` にする（親が `contentDocument` を直接読むこともできるが、**子の DOM の形に親が依存する**のを避ける）。約束の全文は (6)。
- ⚠⚠ **発振の条件**（検収で確かめた）: 高さの往復は「中身 → 高さ → 親が iframe を伸ばす → 子の viewport が変わる → 中身」の輪。**子の `html`/`body` が `vh`・`%` の高さを持つと輪が閉じる**（`body` の margin ぶんずつ毎回育つ）。ion の `style.css` は持たない（2-1 ⑧）が、これは**約束として書き、機械で見る**: ion の test.html で「`embed=1` の高さ通知が **1 秒以内に止まる**（最後の3通が同じ値）」。もう1つの輪は親側 —— iframe が伸びて親に縦スクロールバーが出ると幅が 15px ほど縮み、折り返しで高さが変わる。これは1〜2往復で収まるので、**親は差が 1px 以下なら無視**するだけでよい。
- ⚠ 子の中でモーダル・`position: fixed` を使う画面は、iframe の高さに閉じ込められる（fixed の基準は iframe の viewport ＝ 伸ばした全高。**親をスクロールして下の方を見ているとき、上に出た覆いが見えない**）。ion の固定シート（ステージ一覧・`.sheetBack`）は header の ☰ からしか開かない ＝ embed では届かないので、halflist・halfreaction は当たらない。**それ以外の画面は、候補から外すか `embed=1` で出し方を変える**（どれが当たるかは作りながら・§6）。
- ★ **子の改修はこの3つで足りる**（検収）。「4つめ」が要る画面 ＝ 埋めない（§7 #1 の線）。

#### (3) スマホ縦
- ★ 高さを合わせるので、スマホ縦では**本文と同じ縦の流れ**に部品が挟まるだけ（§3-3 の「分割しない」と矛盾しない）。
- ★ 親の側の最小高さと上限: 上限は置かない（内側スクロールを作らない方を優先）。⚠ 係数決定のように縦に長い部品は、**埋めるのは1問だけ**（`q=` に1つ）にして長さを抑える。
- ⚠ 横に長い式（`sheetScroll`）は子の中で横スクロールのまま。これは元のページと同じ。
  ⚠ ただし iOS Safari は **iframe を中身の幅に広げる癖**がある（横スクロールの中身があると iframe 自体が横にはみ出す）。親の CSS は `iframe { width: 1px; min-width: 100%; }` にする（幅を中身で決めさせない定石）。375×812 の検収で「親に横スクロールが出ない」を必ず見る。
- ⚠ ソフトキーボード: 子の入力欄に触れると親が入力欄までスクロールする（同一オリジンの入れ子では祖先まで動く）。これは望む挙動。`vh` を使わない（上の発振の条件と同じ）ので、キーボードで viewport が縮んでもレイアウトは動かない。

#### (4) 戻り方
- ★ 埋め込みでは**戻る必要が無い**（親のページの中に居る）。`from=reference&page=` は**子の中では帯を出さない**（`embed=1` が優先）。
- ★ 部品の下に親が「▶ 大きな画面で開く」を置く（`embed=1` を外した同じ URL・同じタブ）。そちらでは既存の「← 参考書へ戻る」（§31-3）が効く ＝ **往復は既存の約束のまま**。

#### (5) 読み込みのきっかけ
- ★ **押してから読み込む**（§17-4 を踏襲）。理由は計測と重さ。ボタンの文言は先生の声掛け（「係数を決めてみよう」）。
- ⚠ 例外は作らない（最初から開いておく部品を認めると、ページを開いた回数がアプリの利用に数えられる）。
- ★ **`src` を付けてから挿す**（いまの `EMBED_JS` の順のまま）。挿してから `src` を変えると**タブの履歴に1件入る**（戻るを押すと iframe だけ戻って、親は何も変わらないように見える）。

#### (6) 受け渡しの約束（postMessage・検収で形を決めた）
```
子 → 親:  { type: 'slz-embed', v: 1, h: <整数 px> }      宛先 location.origin
親 → 子:  （無し）
```
- ★ **親は子を知らなくてよい**: 親は `message` の受け口を**1つだけ**持ち、`e.origin === location.origin` と `e.data.type === 'slz-embed'` と `e.data.v === 1` を見て、**`e.source` に一致する iframe** を自分の一覧から引いて高さを当てる（1ページに部品が2つあっても混ざらない）。それ以外の message は黙って捨てる。
- ★ 子は `window.parent === window`（埋め込まれていない）なら送らない。`embed=1` が無ければ送らない（元の URL の挙動を1バイトも変えない）。
- ⚠ test.html・audit.html の iframe に `embed=1` のページが入ると、そこへも message が飛ぶ。**受け口が無いなら捨てられるだけ**（害は無い）。ion の test.html はこれを**受けて値を見る**（2-7）。
- ⛔ `v` を上げるのは形を変えたときだけ。親は知らない `v` を捨てる ＝ 子を先に上げても親は壊れない（古い親は伸びないだけ）。

#### (7) 履歴・フォーカス・計測（検収で確かめたこと）
- ★ **履歴**: 動的に作った iframe の最初の読み込みは履歴に入らない（(5)）。子の中の遷移は `<base target="_top">` で全部タブごと動く ＝ **「戻る」で iframe だけが戻る道が無い**。halfreaction の「次の式へ」はページ内の状態（`pushState` を使っていない・実測）。
- ★ **フォーカス**: iframe を押せば焦点は子に移り、Tab は子の中を回って外へ抜ける。`header-ui.js` の `focus({ preventScroll: true })` は隠れたシートの中だけ。**子が勝手に親をスクロールさせる `scrollIntoView` は halfreaction に無い**（実測）＝ 足すときは `preventScroll` 相当を考える。
- ★ **GA4**: 子の計測スニペットは `window.top` が `test|audit|devices.html` なら止まる（既存）＝ **REF の検査が面Aを iframe で開いても、その中の子は計測しない**。本番では子の `page_view` の `page_location` に `embed=1` が入る ＝ **「埋め込みからの利用」として読む**（ホスト名の次にこれを見る・§7 #5）。`slz_internal` の印は localStorage ＝ オリジンごと ＝ 子にも効く。
- ★ **検索**: 子は元の URL に `canonical`（素の URL）を持つ（§5）。`embed=1` の URL は sitemap に載せない・`noindex` を動的に足す必要は無い（canonical で足りる）。

### 2-5. 学習の記録（○×・4状態）

**実測**:
- qa: キー `slz-qa-v2`。知識コードごとに `fRight/cRight/missF/missC` → 4状態（未着手・着手中・間違えた・どちらも正解。間違えた⇄どちらも正解を裏返す）。
- 半反応式: キー `ioneq_half_marks`。式 id ごとに `recall`（暗記）・`build`（係数）の**最後の結果**を ○/× で持ち、画面は `last` の1つだけ出す（2026-09-17 ユーザー決定）。

★ **気づいたこと: 2つは同じ形をしている。** qa の「めくり／選択問題」＝ 半反応式の「暗記／係数」。
半反応式の記録から qa と同じ規則で4状態を**計算だけで**出せる（記録の形は変えない）:
`どちらも無し→未着手`・`どちらかが ×→間違えた`・`両方 ○→どちらも正解`・`それ以外→着手中`。
★ **検収（2026-09-19）: qa の `stateOfRecord` と一致する**。qa は `miss（missF||missC）> done（fRight>0 && cRight>0）> wip > new` の順で判定し、`missF` は「そのモードの最後が不正解のまま」＝ 半反応式の「最後の結果が ×」と同じもの。優先順もこの順で書く（両方 ○ でも片方が × なら「間違えた」）。
⚠ ずれが1つだけ: qa は「出したが答えていない」を `seen` で着手中にするが、半反応式は**クリアしたときしか書かない**（half-marks.js の決定）ので、途中でやめた式は未着手のまま。これは記録の形を変えないかぎり埋まらない ＝ **埋めない**（「途中でやめたら印は変えない」がユーザー決定）。

**約束（推し）**:
1. ★ **書くのは持ち主だけ**。キーごとに持ち主のアプリを1つ決める（`slz-qa-v2`→qa、`ioneq_half_marks`→ion）。参考書は**書かない**。
2. ★ **読むのは持ち主の部品を通して**（2-3 の B の条件を満たす部品）。参考書のページが `ioneq_half_marks` の JSON を自分で解釈しない（形が変わったとき黙って壊れる）。
3. ★ iframe の中で書いた記録は、同一オリジンなので親もすぐ読める。親は `storage` イベントで印を塗り直せる（**受け渡しの約束に記録を入れない**）。
   ★ 検収: `storage` は**書いた window 以外**の同一オリジンの文書に飛ぶ ＝ 子（iframe）が書けば親に必ず来る。親は書かない（約束1）ので、取りこぼす向きが無い。
   実装の形（段3）: 面Aが `half-marks.js?v=<ion の版>` を読み、`:::reaction` の `half:` を `data-half="<id>"` に焼いておく → 面Aの小さなスクリプト（`EMBED_JS` と同じ置き方）が `HalfMarks.shown(id)`（判断①が (b) なら `get(id)` から4状態を計算）で印を塗り、`storage` で塗り直す。**参考書は JSON の形を知らない**（`KEY` も部品から取る）。
4. ⚠ **qa の記録に半反応式を混ぜない**。知識コード（qa）と式 id（ion）は別の台帳。まとめて見せたくなったら、**両方の部品を読む「集計だけの面」**を後から足す（統合はそこでだけ）。
5. ⚠ 4状態で見せるか、今の「最後の1つ」で見せるかは**ユーザー判断**（§8 の判断①）。

### 2-6. 版とキャッシュ

| 何を | 規約 |
|---|---|
| iframe の `src`（`halfreaction.html?…`） | ★ **`?v=` を付けない**。HTML はもともとキャッシュバスターの対象外で、中の資産は持ち主の `?v=` で更新される ＝ **親は子の版を知らなくてよい**。★ 検収: GitHub Pages は HTML も `max-age=600` ＝ 古い子が出るのは**最大10分・元の URL を直接開いたときと同じ**。`embed=1` を知らない古い子が10分だけ帯付きで出る、それ以上の事故は無い。`verify-release.js` の規則3.4（`?v=` の付け忘れ）は `.js/.css/.json` だけを見るので、`.html` への参照に `?v=` が無くても赤にならない（実測） |
| B の部品（`half-marks.js`） | 持ち主の `?v=` に合わせる（CLAUDE.md の既存規約）。⚠ 面Aは**生成物**なので、持ち主の版が上がったら焼き直しが要る → 生成器が持ち主の `index.html` から版を読んで焼く（手で書かない）。⚠⚠ **規則6はこの参照を解けない**（2-1 ⑩）→ 下 |
| 埋め込みの目印 `embed=1` | 子の HTML に入る ＝ 子の版上げの対象（ふつうの改修と同じ） |

⚠⚠ **B の部品を面Aに入れる前に `verify-release.js` を直す**（検収で実測・2026-09-19）。いまの規則は面Aに対して2つ誤る:
1. `foreignApp` は `path.resolve(dirname, url)` で相手を引くので、**ルート絶対 `/ion-equation/half-marks.js` はドライブの根に解けて「相手不明」**＝ 規則6が黙って通る。しかも 3.4 の実在検査が「実在しません」と**誤って赤**になる。
2. `?v=` を1つでも持った `.html` は**ディレクトリごとに1アプリ**として `apps` に入る ＝ `reference/<id>/index.html` が **48 個の「アプリ」**になり、規則5（変更があるのに版を上げていない）が**焼き直すたびに赤**になる（面Aは自分の版を持たないので上げようがない）。
→ 手当: **`reference/` を `apps` から外し、別の規則（規則12・案）「`reference/**/index.html` の `?v=` 付き参照は、すべてルート絶対で、持ち主の版と一致」**を足す。文字列比較だけ・playwright 不要。⚠ 面Aの `--check` は playwright が要るので `verify-release.js` に入っていない（§17-7）＝ **この規則が無いと、ion の版上げで面Aが古くなる道を誰も見ない**。段3（§4）でだけ B を入れ、**入れる便の最初にこの規則を足す**（§7-2）。

### 2-7. テスト（どこに置くか）

| 検査 | 置き場所 | 理由 |
|---|---|---|
| `embed=1` で header・footer・`.refBack` が隠れる・`<base target="_top">` が在る・高さの message が来て **1秒で止まる**・`a[href^="#"]` と `location.href=` が無い | **子の持ち主の test.html**（ion） | 子の約束は子が守る。⚠ **否定対照**: `embed` 無しの同じページで header が在り・帯が出て・message が**来ない**（元の URL を1バイトも変えていない） |
| 埋め込みの `app:`/`id:` が台帳に在り、相手のデータに id が在る。`embed: true` の受け口が `embeddable` | ion の test.html（既存 `refReceiversIon` の延長・受け口の表に `embed: true` を足す） | CLAUDE.md「横断の整合性検査は両方のデータが揃う側に」。⚠ 台帳の `embeddable` は**受け側の表の写し**（3表同時の既存規約に4つめとして乗る） |
| 焼いた面Aの iframe の URL が原稿と一致・`embed=1` が付いている・**押す前に iframe が無い** | assembler の test.html（`REF18` の延長） | 面Aの生成物を見ているのはここ |
| 親が高さを受けて iframe を伸ばす（実ブラウザ） | **assembler の test.html（`REF18` の延長）**: 面Aのページを iframe で開き、その中の釦を押して子（ion）が入るのを待ち、iframe の高さが子の `documentElement` の高さに追いつくのを見る（同一オリジンなので入れ子でも届く） | ★ 検収で置き場を変えた: `--check` は playwright が要り、コミット前の儀式に乗らない（§17-7）。`REF18` は毎回の全走に乗る。⚠ 子の計測は top が test.html なので止まる（2-4 (7)） |
| 面Aの `?v=` 付き参照が持ち主の版と一致（段3から） | `verify-release.js` の新しい規則（2-6） | 規則6は面Aを解けない（2-1 ⑩） |

---

## 3. 層（読む・見る・動かす・解く・試す）の持たせ方

| 層 | 今ある書式 | 足りる？ | 案 |
|---|---|---|---|
| 読む | 本文・`:::section`・`:::reaction`・`:::table`・`:::figure` | ★ 足りる | —— |
| 見る（講義動画の区間） | 前書き `video:`（ページに1本・YouTube id・0本） | ⚠ **区間が書けない** | `:::video` の囲みを節の中に置く（下） |
| 動かす（アプリ） | `:::link app:` ＋ `id:`（飛ぶだけ・§31-3） | ⚠ **埋め込みが書けない** | `:::link` に `embed: true` を足す（下）。新しい囲みは作らない |
| 解く（一問一答） | 前書き `codes:` → ページ末に iframe（§17-4） | △ ページ単位のみ | 節ごとに置きたくなったら `:::qa codes:` を足す（段3以降・⓵） |
| 解く（入試問題） | qa の出題実績の帯（`USAGE`） | △ | ⚠ 入試問題そのものは著作物 ＝ **載せない**。実測の頻度だけを見せる（表4 #3 の固有性） |
| 試す（実験） | 無し（muki の実験室モードは未着手） | ✕ | 器は `:::link app:` の埋め込みと同じ。中身ができたら台帳に足すだけ |

**案の書式（草案・名前は変えてよい）**:
```
:::link
app: ion-equation/halfreaction
id: MnO4_red
embed: true
text: 過マンガン酸イオンの係数を、手順どおりに決めてみよう
:::

:::video
id: <YouTube の動画ID>
start: 1:23
end: 4:05
text: 書き方その1を黒板で
:::
```
- ★ `embed: true` は `:::link` の1キー ＝ **台帳（`APP_TARGETS`）も id の検査も1本のまま**。台帳に「埋め込みできる」印（`embeddable`）を足し、印の無い受け口に `embed: true` を書いたら赤。
- ⚠ `:::video` の `start`/`end` は YouTube の `start=`/`end=` に焼く。**動画 ID は台本 id ではない**（§17-6 と同じ）。区間の数の見張り（`end > start`）だけ機械で。
- ★ 「解く」の本命は**ページ末の一問一答のまま**。「反応式を書かせるだけの問題」は参考書の外の練習ページに置く（§4 段2）。

---

## 4. 最初の1本 ＝ 半反応式の段取り

前提（実測）: 参考書の `half-reaction`・`half-reaction-condition`・`redox-equation`・`oxidation-number` などは **`ref-inorg-merge` にあって main にまだ無い**。
`half-reaction.md` には仮置きのメモ `//app: ion-equation/halflist`・`//app: ion-equation/halfreaction q=MnO4_red` が既に在る（§31-6 の移行待ち）。**段1 はこの統合が main に入ってから始める。**

### 段1 ——器を作り、一覧と係数決定を `half-reaction` に埋める

| 作るもの | 中身 |
|---|---|
| ion | `halflist.html`・`halfreaction.html` に `embed=1`（2-4 (2) の3点）。台帳の2受け口に `embeddable` |
| 参考書 | `:::link` の `embed: true`（reference-md.js）・面Aの生成器が iframe に焼く・親の高さ合わせ（`EMBED_JS` の延長・受け口は 2-4 (6) の形）。⚠ **qa の 640px 固定は段1では触らない** —— 高さ合わせにするには qa にも `embed=1`（看板を隠す・高さを送る）が要り、それは qa の便（別レーン）。親の受け口は同じなので、qa が送り始めた日に自然に伸びる |
| 原稿 | `half-reaction.md` の §method1 の後に係数決定（`q=MnO4_red`・1問）、§reducers の後に一覧を埋める |

- 一覧（`halflist`）は埋めると長い（全件）。⚠ **埋めるか、リンクに留めるかは段1の実機で決める**（スマホ縦で何画面ぶんか測る）。
- 検収: 375×812 と 1280×800 で、①押す前は枠だけ ②押すと中身の高さで伸びる ③中でクリアすると ○ が付き、元のページ（`halflist.html`）でも同じ ○ が見える。

### 段2 —— 暗記テスト（反応式を書かせるだけの問題）と参考書の往復

- ★ 暗記テストは**単体の練習ページ**として作る（見本 `mock-quiz-*.png`。`halfquiz.html` の名は `half-marks.js` の注記に既に在る）。
  ユーザー方針: 「ある程度習熟した人には反応式アプリは大がかりすぎる」＝ **書かせて○×を付けるだけ**。
- ★ 1問ごとに「**詳しく見る**」→ その式が載っている参考書の節。行き先は**式 id → 参考書の節**の対応で決める。
  - ★ 対応を ion が持つか参考書が持つか: **参考書が持つ**（`:::reaction` に `half: MnO4_red` を足す）。ここは草案どおり。
  - ⚠⚠ **検収で直した点（2026-09-19）**: 草案は「ion が焼いた `reference.json` から引く」だったが、それは**依存の向きが逆**。
    `reference.json` は assembler の生成物で `?v=<assembler の版>` で配られる ＝ ion が実行時に読むと **assembler の版上げ（毎日）のたびに ion の版も上げる**結合ができる（規則6）。しかも「id → ページ・節」の引き方（節立ての解釈）を ion が複製する ＝ ratio→ion の約束（「自分が誰かだけ送り、着地は相手が決める」）と**逆向き**。
    ★ 正しい形は既存の往復と同じ: **ion は `/reference/?half=<式 id>` へ送るだけ**（知ってよいのは URL の形だけ。`header-ui.js` の帯が `/reference/<page>/` の形しか知らないのと同じ）。**着地は参考書側が決める**: 面Aの索引ページ（`/reference/`）に、生成器が `half:` から焼いた小さな表 `{ MnO4_red: 'half-reaction#oxidizers', … }` と数行のスクリプト（`EMBED_JS` と同じ置き方）を置き、`?half=` が来たら `location.replace` でそのページの節へ送る。表に無い id は索引のまま（行き止まりにしない・qa の `?codes=` と同じ作り）。
    - 節は `half:` を持つ `:::reaction` を**包む `:::section` のアンカー**（式そのものにアンカーは無い・§19-2 の綴りのまま）。
    - ⚠ **1つの式 id が2ページに出たら赤**（`gen-reference.mjs`）。`REF18` の「1コードは1ページ」と同じ線。
  - ★ ref-inorg-design §6-4 の宿題「参考書の式と `HALF_REACTIONS` の一致」は ion の test.html で見る（**テストのときだけ** `../assembler/reference.json` を読む ＝ 便0b の `REF3` と同じ形。実行時には読まない）。両方向: `half:` の id が `HALF_REACTIONS` に在る／必ず覚える式（`HALF_LEVELS` 1）に `half:` が付いている。
- ★ 参考書 → 暗記テスト: `:::link app: ion-equation/halfquiz` ＋ `from=reference&page=`（既存の往復）。
- ★ 暗記テスト → 参考書: 面Aに「← 暗記テストへ戻る」の帯（`?from=ion-equation/halfquiz`）。⚠ 面Aは静的なので、帯は面Aの小さなスクリプト（`EMBED_JS` と同じ置き方）。**相手を名指しできるのは台帳に在る相手だけ**。
- 埋め込み: 参考書の §oxidizers の末に「9本を暗記テストで確かめよう」（`embed: true`・順に出題）。

### 段3 —— 本文の式の横に ○×未

- `half:` を持つ `:::reaction` の横に、`HalfMarks` の印を出す（2-3 の B。面Aに `half-marks.js` を読む最初の例）。
- 4状態で出すかは判断①の結果に従う。
- ⚠ ここで初めて「面Aが持ち主の `?v=` を持つ」＝ §7-2 の見張りを同時に入れる。
- 余力: `half-reaction-condition` に電解の陰極（`electrolysis.html?s=`）を埋める ＝ **2本目のアプリ（電池/電解）で器が効くかの試金石**。

---

## 5. 入口と検索

- ★ **ハブ**: 最上段を「参考書の索引」1つにし、アプリのカードは「単体で使う」として下に並べる（§1-2 の表の「残す」側）。⚠ ハブの書き換えはユーザーが見て決める（見た目の判断）。
- ★ **sitemap**: 参考書は1ページ1 URL で全部載せる（既に 48）。**埋め込み用の URL（`embed=1`）は載せない**。
- ⚠ **canonical**: ion は `index.html`・`portal.html` だけが `canonical` を持ち、`halfreaction.html`・`halflist.html` には無い（2-1 ⑪。qa は持っている）。`?q=`・`?embed=1` 付きが別ページとして拾われないよう、**段1で埋め込む ion のページに素の URL の canonical を足す**。★ 動的な `noindex` は要らない（canonical で足りる・JS で足した meta は拾われ方が不確か）。
- ⚠⚠ **孤立ページ**（`/isomers/` の実例・サイトマップに載せても内部リンクが無いと入らない）: 暗記テストのような新ページは、**参考書の本文からの `<a href>` を最低1本**（iframe の中のリンクは数えない前提で考える）。`REF18` の「ハブから `/reference/` へ1本」と同じ形で、新しい練習ページにも「参考書から1本」を見張る。
- ★ 元の URL（`/ion-equation/halfreaction.html` など）は**消さない・移さない**。SNS 動画の概要欄・検索の着地がここを指している。

---

## 6. 決めずに残すこと（作りながら1本ずつ決める）

1. どのアプリを「埋め込み」にし、どれを「リンクで往復」にするか（§1-2 の表は仮）
2. 単体の入口を残す基準（「単体で価値が大きい」の線）
3. 一覧（`halflist`）を埋めるか、リンクに留めるか（段1の実測で）
4. 埋め込みの中でモーダル・固定配置を使う画面の扱い（出し方を変えるか候補から外すか）
5. `:::qa`（節ごとの一問一答）を足すか
6. 講義動画の区間の書式の細部（`:::video` の名前・1節に複数本か）
7. 記録の「集計だけの面」を作るか・どこに置くか（qa の習得マップに相乗りか、参考書の索引か）
8. ハブの見た目（参考書を最上段にした後の並べ方）
9. 暗記テストのページ名・URL（`halfquiz.html` は仮）

⛔ ここに挙げたものを**設計書の段階で決めない**のがユーザー方針（2026-09-19）。1本目で決めたことは、その都度この節から消して §4 に記録する。

---

## 7. 危険とその手当

| # | 危険 | 手当 |
|---|---|---|
| 1 | ⚠⚠ **作り直しの量が膨らむ**（B/C を選ぶと全アプリの読み込み方式の工事が先に来る） | A を推す（§2-3）。子の改修は3点に限る。**3点で済まない画面は埋め込まずにリンクで往復**（それ自体が「作りながら決める」の答え） |
| 2 | ⚠⚠ **面Aが持ち主の版に遅れる**（B の部品を読んだ瞬間から） | B は段3まで入れない。入れる便の最初に `verify-release.js` を直す（2-6: `reference/` を `apps` から外す＋新しい規則「面Aのルート絶対の `?v=` ＝ 持ち主の版」。⚠ 規則6の延長では**済まない**・2-1 ⑩）。playwright 不要の文字列比較 |
| 3 | ⚠ **スマホ縦の二重スクロール** | 高さを親に合わせる（2-4 (2)②）。高さが決まらない画面（抽選・アニメで伸び縮み）は `ResizeObserver` で追う。検収は 375×812 必須 |
| 3b | ⚠⚠ **高さの発振**（中身→高さ→viewport→中身） | 子の `html`/`body` に `vh`・`%` の高さを持たせない（2-4 (2)・ion は今それを満たす）。ion の test.html で「通知が1秒で止まる」。親は 1px 以下の差を無視 |
| 3c | ⚠ **iOS の iframe が中身の幅に広がる**（横スクロールの式で親が横にはみ出す） | 親の CSS `iframe { width: 1px; min-width: 100% }`（2-4 (3)） |
| 3d | ⚠ **`<base target="_top">` が `#` リンクまで top へ飛ばす** | 埋め込む ion のページに `a[href^="#"]` が無いことを ion の test.html で見る（今は 0 件・2-1 ⑫） |
| 4 | ⚠ **既存 URL・SNS 動画のリンク切れ** | URL は1本も移さない。`embed=1` は**足すだけ**で、無ければ今と同じ画面（ion の test.html で「`embed` 無しの画面が変わっていない」を否定対照に） |
| 5 | ⚠ **計測の二重**（子の page_view） | 押してから読み込む（§17-4 の踏襲）。加えて `embed=1` を GA4 の page_location で区別できる（読むときにホスト名の次にこれを見る）。★ 回帰テストの入れ子（test.html → 面A → 子）は子の既存の `window.top` 検査で止まる（2-4 (7)・実測） |
| 5b | ⚠ **「戻る」で iframe だけが戻る** | `src` を付けてから挿す（2-4 (5)）＋ 子の遷移は全部 `_top`（2-4 (2)①）＝ iframe の中に履歴が積まれない |
| 6 | ⚠ **記録の形が変わって参考書が黙って壊れる** | 読むのは持ち主の部品を通してだけ（2-5 の2）。持ち主の test.html が部品を node で試す |
| 7 | ⚠ **並行レーンとの衝突**（参考書の統合レーンが reference-src・生成物・版を触っている） | 段1は `ref-inorg-merge` が main に入ってから。器（reference-md.js・生成器）と子（ion）を**別の便**に割る（§31-3 の便0a/0b と同じ割り方） |
| 8 | ⚠ 「読む→試すが1画面」（表4 #2・固有と確かめた2つの1つ）を**スマホで失う** | スマホでも同じページの縦の流れに部品が入る ＝ 1画面を保つ。リンクで別ページへ飛ばすのは「試す」ではなく「練習」側に限る |

---

## 8. 検収とユーザー判断

### Fable の検収にかける範囲
- **§2 全部**（とくに 2-3 の推しの理由①②、2-4 (2) の「子がやることは3つ」で足りるか、2-5 の「書くのは持ち主・読むのは部品を通して」、2-6 の iframe に `?v=` を付けない判断）
- §4 段2 の「式 id → 参考書の節」の対応を参考書が持つ判断（依存の向き）

★ **検収の結果（Fable・2026-09-19・詳細は §9）: 推し（A）は条件つきで採る。** 条件は §9 の①〜④で、どれも設計書の中で直した（コードは書いていない）。

### ユーザーの判断（2026-09-19 決定）

- **判断①** 半反応式の印 → **qa と同じ4状態の形**。ただしユーザー「文系は暗記はなくてもほぼ大丈夫、理系は必須」により：
  - **いま作る形（A）**: 緑は **係数の決定** だけで点ける（全員に要る）。**暗記テスト（反応前後の物質）は別の小さな印（★など）**。赤＝どちらかで間違えた（間違えた側で正解すれば解除）。赤⇄緑はフリップ。
  - **あとで足す（B）**: 化学基礎／化学の切り替えを**参考書全体の横断の設定**として別に設計（覚える範囲の ★★★／★★☆・qa の難易度にも効く）。B を足したら「化学を選んだ人は★もそろえて緑」と読み替える。A はそのまま読み替えられる形にしておく。
  - ⚠ 前提の裏取り（あとで）: 共通テストの過去問で「文系は半反応式の暗記が要らない」かを数える（取り込み済みの入試DBから）。
- **判断②** 一覧（全件の表）→ **(a) 参考書に埋め込む**
- **判断③** ハブの最上段 → **(a) 参考書の索引を1つ大きく置く**

---

## 9. 検収記録（Fable・2026-09-19・design-refcentric レーン）

対象は §2 全部と §4 段2。**コードは書いていない**。実測はすべて `grep`・ソースの読みで、実ブラウザは使っていない（段1の検収で測る）。

### 9-1. 判定 —— **A（iframe ＋ `embed=1`）を条件つきで採る**

- ★ 理由①（②の衝突）は実測どおり: `model.js` 204・`chemistry.js` 154 のトップレベル宣言。クラシックスクリプトは字句スコープを共有するので、同名の `const` が2本目で `SyntaxError`。**安く逃げる道は無い**（`type="module"` 化・IIFE 化は「全ページの読み込み方式の工事」そのもの・草案の見立てどおり）。
- ★ 理由②（同じ HTML＝同じテスト）はいちばん強い理由。B/C は起動口が2つになる。
- ⚠ 落とした案: 「1ページに1アプリだけなら B でも衝突しない」——③（固定 id 直付け）と④（素のセレクタ）が残るので安くならない。

### 9-2. 条件（設計書の中で直したもの・重い順）

| # | 何が起きていたか | どう直したか（節） |
|---|---|---|
| ① | ⚠⚠ **「子がやることは3つ」の中身が足りなかった**。「← 参考書へ戻る」の帯は `<header>` の**外**（便0b が 120px の約束で外に置いた）＝ header を隠しても残る。リンクの `_top` は1本ずつ直す形だった | 3つを入れ替えた: head の1行（`class` ＋ `<base target="_top">`）／高さ／**帯を作らない**。2-4 (2) |
| ② | ⚠⚠ **`verify-release.js` の規則6は面Aを解けない**（ルート絶対はドライブの根に解ける・`reference/<id>/` が48アプリになって規則5が焼き直しのたびに赤） | 「規則6にそのまま乗る」を消し、段3の便の最初に `reference/` を `apps` から外す＋新規則、と書いた。2-1 ⑩・2-6・§7 #2 |
| ③ | ⚠⚠ **§4 段2 の依存の向きが逆**（ion が実行時に `reference.json?v=<assembler の版>` を読む ＝ assembler の毎日の版上げが ion に結合・節立ての解釈を ion が複製） | ion は `/reference/?half=<id>` へ送るだけ・着地は参考書の索引が焼いた表で決める（ratio→ion・qa の `?codes=` と同じ形）。テストのときだけ `reference.json` を読む。§4 段2 |
| ④ | ⚠ **高さの発振の条件が書かれていなかった**（`vh`・`%` の高さで輪が閉じる）。iOS の iframe 幅の癖、`<base>` の `#` リンク、履歴、フォーカス、GA4 の入れ子も未記載 | 2-4 (2)(3)(5)(6)(7)・§7 3b/3c/3d/5b に足した。ion は今のところ発振の条件を満たさない（実測⑧）。**約束として書き、ion の test.html で見る** |
| ⑤ | ⚠ postMessage の約束が名前だけだった | 形・版・受け口を1つにして `e.source` で iframe を引く・親は子に送らない、を 2-4 (6) に |
| ⑥ | ⚠ 「親が高さを受けて伸ばす」の検査を `--check`（playwright）に置いていた ＝ コミット前の儀式に乗らない | `REF18` の延長（入れ子 iframe・同一オリジン）へ移した。2-7 |
| ⑦ | 4状態の対応の検算 | qa の `stateOfRecord` と一致（優先順 miss > done > wip > new も同じ）。ずれは「途中でやめた」の1点で、埋めない。2-5 |
| ⑧ | 小さな事実の訂正 | ion の canonical は index・portal に在る（§5）。qa の 640px は段1で触らない（§4 段1） |

### 9-3. 変えなかったこと（草案のまま正しいと確かめたもの）
- iframe の `src` に `?v=` を付けない（Pages は HTML も `max-age=600`・古い子は最大10分＝元の URL と同じ・規則3.4 は `.html` を見ない）
- 記録は「書くのは持ち主・読むのは部品を通して」（`storage` は他 window にだけ飛ぶので取りこぼす向きが無い）
- 状態は URL だけ・押してから読み込む・B は `half-marks.js` 型に限る・C は採らない
- `embed: true` は `:::link` の1キー（台帳1本のまま）
