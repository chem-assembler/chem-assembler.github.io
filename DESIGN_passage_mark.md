# 問題文マーク 設計書

**問題文を持ったうえで、「この札は問題文のこの一節から来ている」を示すための設計。**
実装前の合意用ドキュメント。**この設計書を書いた時点で、アプリのコード・データ・版は1バイトも変えていない。**

出どころ（ユーザー・2026-08-20）:

> 問題文そのものをデータとして保持できるなら、該当記述にマークし、問題文の順にナンバリングできませんか？
>
> 将来的に、問題文を取り込み、解説できるようにする場合、問題文を示した上で、
> **該当箇所をマークして示す**、という機能が重要です

⚠ **要求は2つあり、要るものが違う。**

| | 何のため | 要るもの |
|---|---|---|
| ① 番号を導く | `stack` の順が問題文どおりか機械で言えるようにする | **位置**（前後関係だけ） |
| ② 該当箇所を示す | 画面に問題文を出し、そこを光らせる | **範囲**（どこからどこまで） |

**②が満たせれば①は自動的に満たせる**（範囲があれば位置は出る）。逆は成り立たない。
したがって設計は②に合わせる。①だけを狙った軽い実装（step に番号を手で書く）は、
**発注書 R-4 が記録した事故そのもの**（「番号を書いたのに順を守らない」）を再生産する。

前提の3冊: [DESIGN_narrowing_mode.md](DESIGN_narrowing_mode.md) ／
[video-scripts/ORDER_narrowing_stack_order_2026-08-19.md](video-scripts/ORDER_narrowing_stack_order_2026-08-19.md)（R-1〜R-4）／
[video-scripts/POLICY_past_exams.md](video-scripts/POLICY_past_exams.md)（著作権）

---

## 1. 実測 — `_解析` は何を持っているか

⚠ **以下はすべて実物を読んで数えた値**（2026-08-20）。`_解析` には1バイトも書いていない。

### 1-1. 問題文は**すでにある**。取り込む話ではなく、つなぐ話

`_解析/問題文/` に **416 本の Markdown**（1問1ファイル・YAML front matter 付き）。

| | 数 |
|---|---:|
| ファイル | **416**（`README.md` を除く） |
| うち **本文が書き起こしてある**もの | **57** |
| うち メタ情報だけ（`transcript: なし`） | 359 |
| 本文ありの入試年 | **全件 2021 年**（＝2022年版の1巻ぶん） |
| 本文の分量 | 中央値 2.5KB・最大 5.8KB |
| `clearance`（許諾の確認） | **416 件すべて `未確認`** |
| `type` | 本文ありの 57 件すべて **`型2`**（＝問題文を出す型）。`published` / `reported` は**全件空** |

⚠ **ユーザーの「保持できるなら」は、すでに満たされている。**
必要なのは新しい取り込みではなく、**本文と絞り込み仕様のあいだに橋を架けること**。

**本文の形**（`tokyo-2021-zenki-1.md`。出典: 東京大学 令和3年度 前期 化学）:

- `## 問題` の下に、**実験が1行1段落**で並ぶ（`実験1：…` `実験2：…`）。
  九州大は `(1)　…` `(2)　…`、他に `条件(3)` の形もある
- **出題者が引いた下線は `<u>` で書いてある**（本文ありの 57 件中 **23 件・のべ 68 か所**）。
  `<u>a) 互いに鏡像異性体の関係にある2種類の化合物が生成した</u>` のように、
  **記号（`a)` `①`）を範囲の中に入れて**書く
- 図は**転記者が文章で描き直したもの**が入っている（原図の複製ではない）
- `## 索引タグ` `## 条件だけの言い換え（型1で使う）` `## 解法` `## 動画で見せる手順` が続く

⚠ **本文の中に「範囲に名前を付けて指す」という書き方が、出題者の側にすでにある。**
これは設計の決めどころ（§2-b）に直接効く。

### 1-2. 仕様ファイルは**別ディレクトリ・別命名・つながっていない**

| | |
|---|---|
| 置き場 | `_解析/db/narrowing/<問題id>.json` — **1問1ファイル・55 本** |
| 中身 | `targets[].steps[]`（**84 target / 187 step**）＋ `search.pool` |
| 命名 | `2022-東京大学-1I.json`（＝ `problems.jsonl` の `id`） |
| 問題文の命名 | `tokyo-2021-zenki-1.md`（ローマ字・**入試年**） |

⚠ **本文と仕様は同じファイルには置けない**（形式も粒度も別物）。そして**いま結び付いていない**:

- 問題文 416 本のうち `id` を持つのは **359 本（メタだけのほう）**。
  **本文を持つ 57 本には `id` が1つも無い**。⚠ **つながりが最初から逆**になっている
- 出荷19問に対して、**大学・年・大問まで一致する本文があるのは 9 問**（下表）。
  同大学・同年まで緩めれば 13 問だが、**節が違うものが混ざる**（神戸大は仕様が大問3・本文は大問4）

| 出荷19問 | 本文 | |
|---|---|---|
| 九州大学-4 / 兵庫県立大学-5 / 東京大学-1I / 浜松医科大学-3 / 名古屋大学-3 / 岡山大学-3 | あり | **国公立6** |
| 慶應義塾大学-3 / 東京理科大学-6 / 関西大学-3-iv | あり | 私大3 |
| 学習院大学-3-1 / 早稲田大学-3-1 / 東京農工大学-4 / 東京都立大学-2 / 滋賀医科大学-3 / 熊本大学-3 / 神奈川大学-3 / 神戸大学-3 / 関西大学-3-iii | **なし** | 10 |

### 1-3. `label` の実験番号 — **書式は揺れていないが、書いてある step が少ない**

187 step の `label` を数えた。

| 接頭辞（数字を N に正規化） | 数 |
|---|---:|
| （接頭辞なし。「ナトリウムを加えると水素が発生 ＝ −OH をもつ」のような言い換え） | **118** |
| `前提:` | 24 |
| `実験N:` | 17 |
| `実験:`（番号なし） | 8 |
| `対照:` | 6 |
| `与件:` | 5 |
| `条件(N):` | 3 |
| `問N:` / `問イ:` | 3 |
| `実験結果N:` / `設問:` / `臭素とN:` | 各 1 |

- **数字の書き方に揺れは無い**。全角数字・漢数字・「実験１」は **0 件**。
  拾えた番号はすべて半角算用数字で、`実験N` / `条件(N)` / `(N)`（行頭）の3形
- ⚠ **揺れているのは書式ではなく「書いてあるかどうか」**。
  発注書 R-2 の 44/187 と一致する（`^(N)` を含めて 44。番号語つきに限れば 28）
- ⚠ **118 個（63%）は番号どころか実験を指してすらいない。**
  `label` は**その step で解析者が引き出した結論**であって、問題文への参照ではない。
  ここを「参照に書き換えればよい」と読むと設計を誤る ——
  **結論の文は残す価値がある**（画面のカード見出しになる）。足りないのは**参照のほう**

### 1-4. 出典 — ⚠ **統合レーンの前提は誤り。もう列になっている**

> いまは `"id": "2022-東京大学-1I"` という**文字列に埋まっている**だけ

**そうではない。** 出荷 `narrowing-problems.json` は最初から3列を持っている:

```
{"id":"2022-東京大学-1I","university":"東京大学","printed":"東京大1Ⅰ","year":2021,"formula":"C6H12O", …}
```

`build-narrowing-data.js` が `problems.jsonl` から `university` / `printed` / `year` を引いて載せている。

⚠ **「id は 2022 だが中身は東大2021」という食い違いも、無い。**
`id` の先頭は**入試年ではなく書籍の巻**（2022年版 ＝ 2021年入試）。数えた結果:

| | 数 |
|---|---:|
| 仕様 55 本で `id 先頭 = book = year + 1` が成立 | **55 / 55** |
| `problems.jsonl` 全体で `book = year + 1` | **433 / 433** |

**食い違いは 0 件。** 規約どおりであって、分解しても新しいことは見えない。
⚠ **`id` は `problems.jsonl`・`db/splits`・`demos-quiz.json` の結合キーなので、書式を変えない。**

**本当に足りないもの**は別にある（§2-e）:

| 欄 | いま | 要るのは |
|---|---|---|
| `citation`（大学の指定書式・和暦） | **出荷にも仕様にも無い**。**問題文の front matter にはある** | 出典を画面に出すとき |
| `term`（前期/後期/学部） | `printed` に**入っていたり入っていなかったり**（`九州大・前4` / `東京大1Ⅰ`） | 同上 |
| `published` / `reported` | 問題文側にあるが**全件空** | 型2 の1か月報告（§2-f） |

---

## 2. 決めること

### 2-a. 本文をどの単位で持つか — **保存は大問まるごと・表示は段落**

**保存の単位は変えない。** `_解析/問題文/*.md` は**すでに1問1ファイル（大問まるごと）**で、
リード文・実験・設問・図の説明・注が1本に入っている。ここを実験ごとに割るのは害しかない:

- 「該当箇所を示したときに前後が読める」ためには**周りが要る**。
  東大の実験6は「**下線部②の結果を受け**，…オゾン分解実験を行った」で始まる ——
  実験2 を切り離すと、この一文の意味が消える
- 設問（問イ「四員環をもつものは一つである」）も**絞り込みの条件**として使っている。
  実験だけ持つと**問イが指せない**（東大 B（近道）の2手目がまさにこれ）

**表示の単位は「マークを含む段落」**。段落は本文の中で自然に1行1段落で並んでいるので、
改行で切るだけでよい。前後がさらに要るときは、**同じ段落の前後1つずつ**を薄く添える。

⚠ **保存の粒度と表示の粒度を混ぜないこと。** 混ぜると、
「表示に要らないから切っておこう」で保存側が壊れる。

### 2-b. ⚠ 構造で持つか、文字位置で持つか — **どちらでもない。本文の中に書く**

**ここが最大の決めどころ。** 3つの案を、実物に当てて比べた。

| 案 | 持ち方 | 害 |
|---|---|---|
| **(1) 文字位置** | `{"from": 412, "to": 438}` | ⚠ **本文を1文字直すと全部ずれる。しかも表示するまで気づかない。** 却下 |
| **(2) 構造（段落/文に id）** | 段落 `p7` の第2文 | ずれない。⚠ **だが実物には粗すぎる**（下記） |
| **(3) 本文の中にインラインで書く** | `<mark id="exp1-na">A と D からは水素が発生しなかった</mark>` | **ずれる余地が無い**（範囲と本文が同じ文字列） |

**(2) が粗すぎることは、東大の実験1で確定する。**

> 実験1：金属ナトリウムを加えると，A と D からは水素が発生しなかったが，B，C，E，F からは発生した。
> （東京大学 令和3年度 前期 化学）

**1つの文が、逆向きの2枚の札を出している** —— A・D は `na-no`、B・C・E・F は `na`。
文単位の id では**この2つを区別して指せない**。同じ形は実測で他にもある:

| 照合できた9問の 実験/条件 段落 | 27 |
|---|---:|
| その中の文（`。` 区切り） | 66 |
| **1文に化合物が2つ以上** | **25** |
| **1文に化合物2つ以上＋対比表現（〜が，／一方／のみ）** | **6** ＝ 文の途中で割らないと指せない |

**したがって (3) を採る。** 決め手は3つ。

1. **ずれない。** 範囲は本文と同じ1つの文字列の中にあるので、本文を直すと範囲も一緒に動く。
   ⚠ **「本文とマークを同じファイルで一緒に編集する」という発注書の案が、そのまま成立する**
2. **出題者がすでにこの書き方をしている。** 本文には `<u>a) …</u>` が 68 か所ある（§1-1）。
   **同じ形を1つ足すだけ**で、書く人が新しい作法を覚えなくてよい
3. **粒度が自由。** 文の途中でも、複数の文にまたがっても、同じ書き方で足りる

#### 書き方（決め）

```markdown
実験1：金属ナトリウムを加えると，<mark id="exp1-na-no">A と D からは水素が発生しなかった</mark>が，
<mark id="exp1-na">B，C，E，F からは発生した</mark>。
```

- **`<mark>` は「こちらが付けた印」・`<u>` は「出題者が引いた下線」。役割が違うので混ぜない。**
  ⚠ **`<u>` を `<mark>` に書き換えない**（下線部は設問が参照している本文の一部）
- **`id` は問題の中で一意な短い名前。** 番号ではない（§2-d）
- ⚠ **`<u>` と交差してはならない。** 交差すると入れ子にできず、パーサが壊れる。
  交差が要るときは**同じ `id` の `<mark>` を2つに割る**（下の「同じ id を何度でも書ける」で吸収される）
- ⚠ **`<mark>` の中に本文以外の文字を入れない。** 記号（`a)`）は出題者が書いたものだけ

#### 同じ `id` を何度でも書ける

**マークは「id → 範囲の集合」**とする。同じ `id` を2か所に書けば、それは1つのマークの2つの範囲。
これが §2-c の後半をそのまま解く。

### 2-c. step とマークは**多対多**。実測すると、多いほうが普通

⚠ **統合レーンの見立ては正しい。しかも「例外的に起きる」ではなく「これが標準」だった。**

**① 1つの記述から複数の札**（同じ実験番号を2つ以上の step が使っている）:

| | 数 |
|---|---:|
| 該当するファイル | **7 / 55** |
| 群（＝実験1つ） | **11** |
| 関与する step | **36 / 187** |

⚠ **番号を書いてある step は 44 個しかないので、36/44 ＝ 82% がこの形。**
「番号が書いてある step は、ほぼ必ず他の step と実験を共有している」。

内訳（`実験N × 何 step`）:

```
熊本大3      実験1×4  実験2×4  実験3×4  実験4×2
早稲田3-1    実験1×8  実験3×2
神戸大3      実験3×4
東大1I       実験4×2      東京農工大4 実験1×2
昭和薬科大4  実験3×2      東京薬科大4 実験1×2
```

番号を見ずに文字列で数えると、さらに増える —— **同じ `label` を共有する群 27・同じ `test` を共有する群 32**。

**② 1枚の札が複数の記述に支えられる**: いまの仕様では **1 件**
（東大 `問イ: {and:["環をもつ",{ring:4}]}`）。
少ないのは**書けないから**で、実物には普通にある ——
東大の実験2は「水素付加しない」だけでは環と決まらず、**実験5（カルボニル無し）と合わせて初めて環**になる。
⚠ **これは発注書 R-1 が「順番を崩した理由」として突き止めたものと同じ構造**で、
いまは**順番の入れ替えで代用している**。指せるようになれば代用が要らなくなる。

**決め**: **`step.marks` は id の配列**（②を吸収）、**`<mark id>` は同じ id を何度でも書ける**（①と、
1枚の札が離れた2か所に支えられる場合を吸収）。

⚠ **1対1で始めない。** 実測 36/44 が①なので、1対1は初日から破れる。
配列にする追加コストは `[]` の2文字で、後から直す費用とは比較にならない。

### 2-d. 番号は**本文中の位置から導く**。人は書かない

```
stepNo(step) = その step の marks のうち、本文中でいちばん早く現れるものの出現順位
```

- **本文の中の出現順 ＝ ファイルの中のバイト順**。マークが本文の中にあるので、これは無料で出る
- ⚠ **これで「番号を書いたのに順を守らない」が原理的に起こらなくなる。**
  発注書 R-4 の事故（東大 B が `5 → 1 → 2 → 3 → 4`）は、
  **番号（`label`）と並び（`steps` の配列順）が別々に書けたから**起きた。
  導出にすると**別々に書く場所が消える**。⚠ **これが本設計のいちばんの効果**で、
  「東大の1件を直す」ではなく「同じ事故が二度と作れない形にする」に当たる
- **出題者が書いた実験番号（`実験N`）は、検算に使う**。導いた順と食い違ったら落とす。
  ⚠ **番号を「正」にはしない** —— 東大の `問イ` のように番号を持たない条件があり、
  番号の列に混ぜられない

#### ⚠ マークの無い step をどうするか — **大半がそうなる。落ちてはいけない**

いまマークは0個なので、**初日は 187 step 全部が「番号なし」**。ここを厳しくすると
`build-narrowing-data.js` が全滅する。⚠ **これは実際に一度起きた失敗**で、
生成器のコメントが記録している ——「解析の工程がアプリの実装待ちで止まる形になっていた」。

**決め**:

- 番号が導けない step は **`stepNo` を持たない**。エラーではない
- 並べ替えるときは「**番号のあるものを本文順に前へ、無いものは仕様に書いた順のまま後ろへ**」
- ⚠ **順序の検査は「番号のある step どうし」だけに掛ける。** 番号の無いものを飛ばして昇順ならよい
- 出荷 JSON には **`stepNo` を持つものだけ**載せる。**載っていない ＝ まだ照合していない**を意味させる
  （`null` を載せて「照合したが番号が無い」と混ざるのを避ける）

### 2-e. 出典 — **列はもうある。足すのは `citation` と `term`**

§1-4 のとおり `university` / `printed` / `year` は出荷済みで、id の食い違いは0件。

**決め**:

- ⚠ **`id` は触らない**（結合キー）。分解もしない
- **`citation` を通す。** 問題文 front matter に**大学の指定書式で既に書いてある**
  （`東京大学 令和3年度 前期 化学`）。⚠ **POLICY §型2 の条件1「出典を明示」は、
  この文字列をそのまま出すことでしか満たせない**（阪大は書式例まで指定している）。
  こちらで `university + year` から組み立て直すと**和暦が出せず、指定書式から外れる**
- **`term` を列に足す。** いまは `printed` に混ざっていて機械で取り出せない
- ⚠ **`citation` の出どころは問題文ファイル**なので、**§2-f の橋（`id`）が架かるまで通せない**。
  順番が決まる（§3 の 2 が 3 の前）

### 2-f. ⚠ いつ出荷に切り替えるか — **いまは切り替えない。数えたら、切り替えられる問題は最大6件**

**方針**（POLICY_past_exams.md）:

| | いま | 本文を出す段になったら |
|---|---|---|
| 本文・マーク | **`_解析` に置く。リポジトリに入れない** | 型2 の条件を満たしてから出す |
| 出荷 JSON | 導いた **番号だけ**（＋既にある出典列） | ＋ 本文と範囲 |

**「永久に出さない」ではなく「まだ出さない」。** 出す段になったら要るものを数えた。

| 条件（POLICY 型2） | いまの状態 |
|---|---|
| **共通テストは事前申請制** | ⚠ **出荷19問・仕様55本・問題文416本すべてに共通テストは 0 件。** この論点は**空振り**。心配は要らない |
| 国立大に限る（私大は方針が無い） | ⚠ **出荷19問のうち私大が 7 件**（学習院・慶應・早稲田・東京理科・神奈川・関西×2）。公立2・国立10 |
| 出典を指定書式で明示 | `citation` は問題文にある。**出荷には無い**（§2-e） |
| 公表から1か月以内に報告 | ⚠ **57本すべて `published` / `reported` が空**。運用が始まっていない |
| 許諾・条件の確認 | ⚠ **416本すべて `clearance: 未確認`** |

⚠ **掛け合わせると、いま本文を出せる候補は 6 問**（本文あり9問 − 私大3問）:
**九州大4・兵庫県立大5・東大1I・浜松医大3・名古屋大3・岡山大3**。
「問題文を示して該当箇所をマークする」機能は、**出荷19問のうち3分の1でしか動かない**。
⚠ **機能としては成立するが、これを知らずに作ると「半分以上の問題で本文が出ない」画面になる。**
先に決めるべきは実装ではなく、**本文が無いときに何を出すか**（§4）。

#### 再発防止 — ⚠ **いまの機械の見張りは、この事故を止められない**

過去に2度リポジトリへ著作物が入り、**公開履歴は書き換えない**と決着している。
⚠ **そのうち漏れA は `past-exams/` ＝ まさに入試問題の書き起こし**だった。

`verify-release.js` の規則9 を読んだ。**見ているのは `qa/` 配下だけ**で、
探しているのは**傍用問題集・教科書の番号と見出し**（`基本例題42` / `PLUS`）。
⚠ **入試問題の本文がリポジトリのどこかに現れても、何も言わない。** 漏れA と同じ形が素通りする。

**決め: 検査を先に入れる。**（§3 の 1 と並ぶ最優先。本文を1文字も書かないうちに入れる）

- リポジトリ配下の**全テキスト**（`.md` を含む。⚠ 規則9 が `.md` を外していたのが漏れBの死角だった）に対し、
  **問題文の本文らしさ**を落とす:
  - `## 問題` の見出し
  - `実験N：` / `(N)　` で始まる段落が **3つ以上連続**
  - `<mark id=` を含む行（＝ マークを打った本文がそのまま来た印）
  - `_解析/問題文/` の front matter が丸ごと写っている（`university:` と `source_book:` と `citation:` が**同じファイルに揃う**）
  ⚠ **キー名を単独で禁止語にしない。** `citation:` も `source_book:` も
  POLICY・README・本設計書が**書式を説明するために**正当に持っている。**揃ったときだけ**落とす
- `narrowing-problems.json` に**本文が混ざっていないか**（`note` と `label` の長さの上限）
- ⚠ **除外リストを作らない。** 規則9 は対象を `qa/` に絞り `.md` を外していたせいで、
  `textbook_scope.md` の漏れに誰も気づけなかった。**絞り込みではなく、判定の形で外す**

### 2-g. 「人間のアプローチの再現」との関係 — **本体になる。ただし置き換えではない**

`ORDER_narrowing_human_path_2026-08-17.md` の芯:

> 機械的に総数が出せる → それはそれでツールとして有効
> **人間の解法アプローチの再現 → 解説・理解ではこちらが重要**

**意見（このレーンの判断）**: マークは**再現の本体になりうるが、帰着（アルキル基への還元）の代わりにはならない。**
役割が違う。

| | 何を示すか | 出どころ |
|---|---|---|
| **マーク** | **入力の出所**。「この札は問題文のこの一節から来た」 | 問題文に書いてある |
| **帰着**（`CH₃-CH(OH)-` ＋ `C₄H₇–`） | **推論の道筋**。「決まった部分を外し、残りを書き出す」 | ⚠ **問題文には書いていない** |

⚠ **帰着はマークでは出ない。** 人が問題文から引き出したものであって、問題文の一節ではない。
**片方に寄せると、どちらかの役目が消える**（発注書 §4 が言っているのと同じ）。

**そのうえで、マークにしかできないことが3つある。**

1. **「落ちた理由」を一節に結び付けられる**（ORDER §3-B）。
   いまは「不斉炭素が2個以上のものを2件除きました」までだが、
   **「この一文で2件落ちました」**と本文の上で言える。**人が間違えるのはまさにそこ**
2. ⚠ **マークの無い記述が、いちばん情報を持っている。**
   東大の実験6〜8は B には効かないが A・E・F には効く。
   「**まだ使っていない記述がここに残っている**」を本文の上で見せられるのは、この機構だけ。
   M2 のマトリクスの「・」（まだ調べていない）と**同じ役割を、問題文の側で**果たす。
   ⚠ **これは①（番号を導く）からは絶対に出てこない**ので、②を選ぶ理由がもう1つ増える
3. **模範解答の飛躍（DESIGN §7 の `collapsed`）が、本文の上で長さを持つ。**
   「ここを1文で済ませています」を、**その1文をマークして**示せる

**結論: マークは「順序の検証」の道具として始めるが、育つ先は解説側。**
⚠ **だから設計を①（位置だけ）に寄せてはいけない。**

---

## 3. 直す順（発注書 R-4 の「直す順」を組み替える）

発注書は 1〜6 を挙げていた。**この設計に合わせると順が変わり、1つ増える。**

| | やること | R-4 では | なぜ動かすか |
|---|---|---|---|
| **1** | ⚠ **本文がリポジトリへ入るのを止める検査**（§2-f） | **無かった** | **本文を1文字も書かないうちに入れる。**逆順にすると、漏れてから気づく |
| **2** | **`MAP` に `水素付加しない: { card: 'h2-no' }` を足す**（語彙の穴） | 1 | ⚠ **どの案でも要る。**据え置き。ここだけは無条件に得（発注書 R-1 の実測どおり） |
| **3** | **本文を持つ 57 本の front matter に `id` を書く**（§1-2） | 無かった | **本文と仕様が結び付いていない。**ここが橋。まず出荷19問に効く **9 本**だけでよい |
| **4** | **東大 1I にマークを打ち、`step.marks` を書く**（1問だけ） | — | **1問で機構を確かめる。**東大は①（実験4×2）も②（問イの `and`）も入っている |
| **5** | **東大 1I の `steps` を導いた順に直し、実験2 の `test` を `h2-no` に替える** | 2 | ⚠ **4 のあと。**先に直すと「人が並べ替えた」がもう一度起きる。**導出で並べ替える** |
| **6** | **`build-narrowing-data.js` に `stepNo` の導出と検査を入れる**（§2-d） | 3・4 | R-4 の 3（番号を載せる）と 4（昇順の検査）は**同じ1つの作業**になる |
| **7** | **`citation` / `term` を出荷列に足す**（§2-e） | 無かった | 3 が済んで初めて通せる |
| **8** | **`narrowing-problems.json` に見張りを掛ける**（1問1行化 ＋ `verify-compounds.js` 検査0） | 5 | 据え置き。⚠ いまこのファイルは**機械の見張りが1つも無い**（`?v=` も無く規則5の対象外） |
| **9** | **残り8問へマークを広げる**（本文のある9問のうち東大以外） | — | 6 の検査が効いてから。ここで初めて「他に何件ずれているか」が言える |
| **10** | 発注書 §3 の (A)/(B)/(C) を決める | 6 | 据え置き。**6 が済めば (C)（集合＋番号）は自動的に手に入る** |
| **11** | **画面に本文を出す**（②の本体） | — | ⚠ **§2-f の条件が揃うまで着手しない。**設計だけ先に置く |

⚠ **1〜2 と 3〜9 は独立に進められる**（1・2 はアプリ／解析の別の場所）。
⚠ **`build-narrowing-data.js` の `APP` は本体の作業ツリーへの絶対パス直書き**なので、
worktree から `--write` してはいけない（発注書 R-3）。

---

## 4. やらないこと

- ⚠ **文字位置（オフセット）でマークを持つこと。** §2-b で却下した。
  「いまは本文が固まっているから大丈夫」という理由で採らない —— 固まっていないから書き起こしなのであって、
  **ずれても表示するまで気づかない**という性質は分量では改善しない
- ⚠ **`label` を参照に書き換えること。** 118 個の `label` は解析者が引き出した結論で、
  **カードの見出しとして価値がある**。参照は `marks` として**別に足す**
- ⚠ **`id` の分解・改名。** 結合キー。食い違いは 0 件（§1-4）
- **すべての step にマークを付けること。** 187 個中、本文がある問題のものしか付けられない。
  **付いていない ＝ 未照合**を意味させる（§2-d）
- **本文を出荷 JSON に入れること。** §2-f の条件が揃うまで
- **問題文の新規書き起こし。** 本設計は既にある 57 本をつなぐ話。増やすかは別の判断
- **解答・解説の収録。** POLICY のとおり、ここだけははっきり駄目

---

## 5. 検算（実装するときに置くもの）

**解析側**（`_解析/tools/`。本文を読むので repo 側には置けない）:

- `<mark id>` が問題の中で**重複していない**（同じ id の複数範囲は許す。**別物に同じ id は禁止**）
- `<mark>` が `<u>` と**交差していない**
- `step.marks` が指す id が**本文に存在する**（無ければ落ちる。黙って捨てない）
- 導いた `stepNo` が、`label` の `実験N` と**食い違わない**
- 番号を2つ以上持つ target が**昇順**（＝ R-4 の 4。導出にすると自動で成り立つので、**否定対照**として
  「わざと並べ替えたら落ちること」を見る）

**repo 側**（`tools/verify-release.js` / `test.html`）:

- ⚠ **リポジトリに問題文らしいテキストが無い**（§2-f。**除外はこの設計書だけ**）
- `narrowing-problems.json` に本文が混ざっていない（`note` / `label` の長さ上限）
- `stepNo` を持つ列は、その順に積むと仕様の `expect` に届く
- ⚠ **`assembler/tests.js` の NW5 ③ のコメントを直す。**
  「デモ順は 11 から、問題文順は 51 から」は**誤り**（問題文順は 26 から。51 はデータの順）。
  発注書 R-4 の 4 が指摘したもので、**テストの主張が誤っているので、テストを見ても気づけない**

---

## 6. 推奨（1つ）

⚠ **§2-b の (3)（本文の中に `<mark id>` をインラインで書く）を採り、§3 の 1 → 2 → 3 → 4 まで進めて、
東大 1I の1問で機構を確かめてから広げる。**

理由:

1. **ずれる余地をそもそも作らない**という発注書の要求を、**そのまま満たす唯一の案**
2. **①（番号）と②（該当箇所の表示）を1つの機構で満たす。**
   ①だけの実装は安いが、**②に進むとき作り直しになる**（範囲を持っていないため）
3. **実測 36/44 が多対多**なので、1対1で始める選択肢は初日に破れる
4. **出題者がすでに同じ書き方をしている**（`<u>` 68 か所）ので、書く人の負担が増えない
5. ⚠ **番号を人が書く場所を消すこと自体が、発注書 R-4 の事故の再発防止**になる

**やめておくこと**: ⚠ **東大の1件を先に直すこと。**
発注書 R-4 が「1件だけ直しても同じ状態は残る」と結論した理由がそのまま効く ——
**いま直すと、直した証拠が `label` と `steps` の一致にしか残らず、次に同じ人が同じ順で並べ替えられる。**
§3 の 4（マークを打つ）を先にすれば、**5（並べ替え）は導出の結果として出てくる**。
