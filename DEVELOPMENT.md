# パズルでみる有機化学（コード名 Chem-Assembler）開発方針

本ドキュメントは、パズルでみる有機化学（内部コード名 Chem-Assembler）の開発における設計原則・コーディング規約・品質保証手順・作業ロードマップを定める。以後の修正・機能追加はすべて本方針に従って行う。

---

## 1. プロジェクトの目的と開発コンセプト

- 高校〜大学初級の有機化学学習者が、分子構造（トポロジー・結合次数・不斉炭素）を手で組み立てて理解するためのブラウザゲーム。
- **「化学的に正しい答えは必ず正解になる」ことを最優先の品質基準とする。** 誤検証（正しいのに不正解）はゲームとして最も致命的な欠陥であり、他のあらゆる作業に優先して修正する。

### 1.1 開発コンセプト（作図思想）

本アプリは **立体模型の忠実再現ではなく、紙に手書きするのと同じ感覚での構造式作図** を目指す。

1. **手書き感覚の優先**: 学習者がノートに構造式を書くときの感覚（直線・直角中心の作図）を再現する。例えばアルカンは、実際の結合角（109.5°）を反映したジグザグの折れ線ではなく、**直交方向（上下左右90°）に作図するのが本アプリの正**である。
   - したがって、直交グリッドスナップ・90°方向の水素配置は「不正確さ」ではなく**意図された仕様**であり、リアルな結合角に近づける方向の「修正」を行ってはならない（過去にsp3自動レイアウトがドリフトバグを生んで撤去された経緯がある）。
   - 例外として、構造式の慣習に従い sp2（C=C、ベンゼン環）は120°、sp（C≡C）は180°の作図を用いる。これは紙の上の構造式でも同様に書かれるためである。
2. **反応機構アニメーションへの発展**: 将来的に、巻矢印（曲がった矢印）による電子の動き・結合の組み換えをアニメーションで再現し、反応機構の学習をサポートすることを目指す。データモデル（原子・結合のグラフ構造）はこの発展を妨げない形を保つ。
3. **立体模型との対照**: 作図はあくまで平面の構造式としつつ、「作図上ではこう書くが、実際の分子はこうなっている」という対照提示（立体表現の併記・比較ビュー等）ができるとなおよい。平面作図の正しさと立体的な実像を混同させない見せ方を将来検討する。

## 2. アーキテクチャ原則

| ファイル | 責務 | 禁止事項 |
|---|---|---|
| `chemistry.js` | 化学モデル（原子・結合・分子・価標・同型判定・不斉判定）。**DOMに依存しない純粋ロジック** | DOM操作・描画・イベント処理を書かない |
| `game.js` | UI・描画（SVG）・入力処理・ステージ進行 | 化学的な判定ルールを直接実装しない（chemistry.jsに委譲） |
| `stages.json` | 問題データ（純粋データ、コメント不可） | ロジックを持ち込まない |
| `index.html` / `style.css` | 構造とスタイル | インラインstyleの新規追加は避け、style.cssに寄せる |

- 外部ライブラリ・ビルドツールは導入しない（vanilla JS + 静的配信を維持。GitHub Pages 互換）。
- 定数は一元管理する。価標は `VALENCIES`（chemistry.js）を唯一の情報源とし、重複定義（例: 旧 `getMaxBondType` 内のswitch）を作らない。

## 3. コーディング規約

### 3.1 文字エンコーディング（最重要）

- **すべてのファイルは UTF-8（BOMなし）で保存する。** 過去に Shift-JIS との二重変換でコメント・UI文字列が広範囲に文字化けした事故があるため、以下を徹底する:
  - コミット前に、変更ファイルに文字化けパターン（`縺`・`繧`・`繝`・`蜊`・`荳`・`邨`・`蛾`・`倡` 等の頻出化けクラスタ）が含まれていないか確認する。v104でこのパターン外の化け（`蜊倡ｵ仙粋`＝単結合）を発見したため検査文字を拡充した。
  - **この確認は `node tools/verify-release.js` で機械化済み**。化けは必ず連なって現れるので「2文字以上の連続」を条件にしており、この行のような**説明目的の単独引用は誤検出しない**。BOM の有無も同時に見る。
  - ユーザーに表示される日本語文字列（alert/confirm/textContent/innerHTML）を変更した場合は、必ずブラウザで実表示を確認する。
- 既存の文字化けコメントは、修正対象ファイルに触れる際に随時正しい日本語へ復元するか削除する。

### 3.2 スタイル

- 命名・コメント密度は既存コードに合わせる（コメントは日本語）。
- デッドコードは残さない。「無効化した関数」を空実装で残す場合は、呼び出し側ごと削除するか、残す理由をコメントで明記する。
- `substr` 等の非推奨APIは新規コードで使わない（既存は触るついでに置換）。

### 3.3 座標変換の原則

- **クライアント座標 ⇔ SVG座標の変換は必ず `svg.getScreenCTM().inverse()`（または `createSVGPoint().matrixTransform`）で行う。** `viewBox.width / rect.width` のような手計算は、`preserveAspectRatio` によるレターボックスを考慮できずズレを生むため禁止する。
- viewBox の操作（ズーム・パン・フィット）は既存方式を維持してよいが、座標の読み取りは上記に統一する。

### 3.4 入力処理の原則

- マウス・タッチの入力は **Pointer Events（`pointerdown`/`pointermove`/`pointerup`/`pointercancel`）に統一**することを目標とする。移行完了までの暫定対応でも:
  - タッチ系ハンドラでフェイクイベントを作る場合、**必ず座標（`changedTouches[0]` 等）を含める。** 座標なしイベントを座標依存の処理に渡さない。
  - 合成マウスイベントの二重発火を防ぐため、タッチを処理したら `preventDefault()` する。
- `alert` / `confirm` / `prompt` は原則使わない。通知は `#verify-result` のトースト表示、確認・入力はモーダルで行う。（既存の `window.onerror` の alert はデバッグ残骸であり撤去対象。）

### 3.5 Undo の原則

- `saveState()` は **実際に状態が変化する操作の直前のみ** 呼ぶ。空振り操作（何も消えない消しゴムクリック等）で履歴を消費しない。
- 状態の保存・復元は **原子の全プロパティ**（`isAsymmetricMarked`、`benzeneCenter`、`benzeneAngle` を含む）を対象とする。復元漏れを防ぐため、ホワイトリスト式のプロパティコピーではなく、シリアライズしたオブジェクトからの機械的復元を優先する。

## 4. 化学モデルの原則

1. **正解データ（stages.json）は、アプリ自身の価標ルールで構築可能でなければならない。** ユーザーが通常操作（原子配置・結合ツール・モジュール・結合次数トグル）で到達できない正解データを置かない。
2. **モジュール（官能基ボタン）が生成する構造は、そのモジュールを想定するステージの正解データと結合次数まで一致させる。** （例: -NO₂ モジュールは N(=O)(–O) を生成する。N(=O)(=O) は価標超過であり禁止。）
3. **芳香環のケクレ位相（単結合・二重結合の位置）の違いは正誤に影響させない。** 6員環で単・二重結合が交互になっている環は「芳香環」として扱い、検証時に環内結合の次数差を吸収する。
4. **二重結合の幾何（シス/トランス）は例外的に座標から読み取る。** 座標は原則「見た目専用」だが、C=C まわりの置換基の同側/反対側は2D構造式が幾何異性を伝える標準的な手段であるため、**命名表示（および将来の幾何指定ステージ）に限り**座標を参照する。対象は「環に含まれないC=Cで、両端の炭素がそれぞれ重原子置換基をちょうど1個持つもの」。直線に描かれた場合は「未指定」として扱い、通常のステージ正誤判定は引き続きトポロジーのみで行う。
5. 価標は `VALENCIES` を唯一の情報源とする。多価元素（S: 2/4/6 等）への拡張を行う場合は、モデル・検証・自動水素補完の三者を同時に対応させる。それまでは S を使う出題はスルホ基（6価）に限定する。
6. 自動水素補完は「描画とH数検証の一貫性」を守る。ユーザーがHを明示配置する機能は追加しない（現行仕様の維持）。

## 5. UI/UX の原則

### 5.0 作図スタイル（コンセプト1.1の具体化）

- スナップ・自動配置・自動水素は **直交（90°）を基本** とし、sp2は120°、spは180°とする。結合角の立体的リアリティを理由にこれを変えない。
- 検証ロジックは作図上の座標・角度に依存させない（トポロジーと結合次数のみで判定する現行方式を維持）。座標は見た目のためだけの情報とする。

- **ヘルプ・ヒントの記述と実装は常に一致させる。** 操作方法を変えたら同一コミットで `index.html` のヘルプを更新する。実装されていない操作をヘルプに書かない。
- 削除系の操作は影響を最小にする。**削除の影響は削除対象の原子・結合のみに限定し、連結が切れて孤立した原子を自動削除（巻き添え削除）しない。** 紙の上で消しゴムをかけたのと同じ挙動とする（コンセプト1.1-1）。孤立した原子が残ることは正常であり、ユーザーが自分で消すか再結合する。
- 判定結果メッセージは実際に検証した内容だけを述べる（不斉モードOFF時に「不斉炭素が一致」と表示しない）。
- 非表示中の機能（お手本表示・化学式・説明文）は、方針が決まるまで削除しない。ただし到達不能コードであることをコメントで明示する。

## 6. 品質保証（修正ごとの検証手順）

すべての修正は、コミット前に以下を実施する:

### 6.1 回帰テスト（必須）

1. ローカルサーバー（例: `python -m http.server 8123`）で起動し、コンソールエラーがないこと。
2. **`test.html` を開き、全テストが合格することを確認する。** 過去に修正した不具合（座標変換・ケクレ・削除・伸縮・振り分け・反応ビューア等）の再発を自動検出する回帰テストページであり、修正内容に関わらずコミット前に必ず実行する。
3. 新しい不具合を修正したときは、その再現ケースを `tests.js` にテストとして追加する。

### 6.2 回帰チェックリスト（該当領域を修正した場合）

- **座標系**: ウィンドウを縦長・横長にリサイズし、キャンバスの四隅付近でプレビュー（ゴースト）がカーソル直下に出ること。ズーム・パン後も同様。
- **検証ロジック**: 以下をブラウザコンソールで実行し、期待値になること。
  - ニトロベンゼン: -NO₂ モジュールで組んだ構造が正解になる。
  - o-キシレン: ベンゼン環モジュールの単結合側・二重結合側どちらにメチル基を付けても正解になる。
  - 酢酸: C-C-(=O)(-OH) が正解、C-C-(-O)(-OH)（次数違い）が不正解のまま。
- **Undo**: 不斉マーク → Undo → マークが復元される。配置 → Undo → 配置前に戻る。
- **削除**: プロパン中央Cの削除で両端のCが残る。
- **タッチ（対応時）**: タップ配置が1回で確定する（配置→即削除にならない）。タッチドラッグで結合が張れる。

### 6.3 検証の記録

- 各修正のコミットメッセージ本文に「何をどう検証したか」を1行以上書く。

## 7. バージョン管理・リリース手順

- コミットは **1修正=1コミット**。メッセージは既存の Conventional Commits 形式（`fix:` / `feat:` / `chore:`）を踏襲し、末尾に `(vNN)` を付ける。
- バージョン番号 `vNN` を上げる際は、**`index.html` と `test.html` の全キャッシュバスターを同一コミットで** 更新する（不一致はキャッシュ事故のもと。GitHub Pagesはmax-age=600で配信するため、バスターのないURLは最大10分間古い版が使われ続ける）:
  1. `index.html` の `style.css` / `chemistry.js` / `game.js` / `reaction.js` / `quiz.js` の `?v=NN`
  2. `index.html` ヘッダー表示 `<div class="version">vNN</div>`
  3. `test.html` の `index.html?v=NN`（iframe）と `tests.js?v=NN`
  4. `audit.html` の `index.html?v=NN`（iframe）と `audit.js?v=NN`（v108で追加）
  - 一括更新例: `sed -i 's/?v=OLD/?v=NEW/g' index.html test.html audit.html` ＋ ヘッダー表示の置換
  - **JSONデータの fetch には必ず `{ cache: 'no-cache' }` を付ける**（URLにバスターが付かないため。v125で tutorials.json だけ付け忘れ、更新が反映されない事象が発生した）。
  - **開発中にブラウザで検証する前にも、コードを変更したらバージョンを上げる。**同一 `?v=NN` のまま再読み込みしても旧版がキャッシュから配信され、「実装したのに動かない」と誤診する（v123・v125で2回発生）。
- main ブランチ直コミット運用を維持する（単独開発のため）。ただし大きなリファクタ（Pointer Events 移行等）はブランチを切ってもよい。

## 8. ロードマップ（優先順位順）

レビュー（2026-07-16 実施）で確認された問題を、以下の順序で修正する。各項目は完了時にチェックする。

### 並行レーンの割り当て（2026-08-02 起動・**各レーンはここを見て自分で判断すること**）

Gemini レビュー（外部の受け取り。`docs/` は追跡外なのでレビュー本文は main の作業ツリーにだけある）を
26項目に整理し、**触るファイルが重ならないように**3レーンへ分けて `git worktree` で走らせている。
**取り決めをチャットで配らないこと。** 変わったらこの表を直して main に push すれば全レーンに伝わる。

| レーン | ブランチ / worktree | 担当 | **持ち主ファイル**（他レーンは触らない） | 版帯 |
|---|---|---|---|---|
| **A 表示・スマホ** | **`feat/ui-portal`**（`C:/Antigravity/worktrees/ui-portal`） | 項目 1・2・3・7・10・11・25 | **`style.css` を専有**。`index.html` は SVG タグの属性1行ずつだけ | v380–v389 |
| **B 不具合** | `feat/fix-arrows` | 項目 16・17（Undo・全消去で巻矢印が残る） | `game.js`（undo/restoreState/全消去）・`reaction.js` | v390–v399 |
| **C 学習内容** | `feat/learn-content` | 項目 6・8・12 ＋ compounds.json のデータ不整合 | `learn.js`・`chemistry.js` の分類・`compounds.json`・文言 | v400–v409 |
| **D 立体ビューの操作性** | `claude/*`（チップから起こす） | 項目 4・13 | **`stereo.js` を専有** | v410–v419 |
| **E 反応まわりの案内** | `claude/*`（チップから起こす） | 項目 9・14 | `reactor.js`・`game.js` の反応まわり | v420–v429 |
| **F R/S 判定（CIP）** | `claude/*`（チップから起こす。**Fable 5 で**） | 発注書 第4段 4b | `chemistry.js`・`tests.js` | v440–v459 |
| **統合（main）** | `main` | 判断待ち項目・マージ・設計書 | **`quiz.js` を専有**・全体 | v430–v439 |

**第2波以降に回すもの（いま並列にしない理由つき）**:

- **項目18・19・24-3（入口の再編）** … `index.html` の骨格を作り替える。レーンA が
  `index.html` の属性を触っている間は必ず衝突する。**A の完了後に単独で**
- **項目20（試薬パレット）・26（アルキル基モジュール）** … どちらも `index.html` に
  新しいパレット／モーダルを足す。**入口の再編で置き場所が変わる**ので、その後にやるほうが
  手戻りが少ない。20 は先に DESIGN_*.md を書く
- **項目25（長鎖の縮約）** … 描画エンジン（`chemistry.js`／`quiz.js` のラベル整形）の話で
  レーンA（`style.css`）の担当ではない。統合セッションが `quiz.js` を空けてから回す
- **項目21** … ユーザー判断待ち

> **レーンA は `feat/ui-mobile` ではなく `feat/ui-portal` で動いている**（2026-08-02。
> worktree を開き違えたまま作業が進んだ）。**分岐点は当日の main（v376）なので中身は正しく、
> 統合済み。** 空の `feat/ui-mobile` は使わない。`ui-portal` という名前は 2026-07-31 の
> 別作業（学習タブのアコーディオン化）の名残で、いまの担当とは関係ない。

**版番号の扱い**: レーンは自分の帯で番号を進めてよい（`verify-release` を通すため）。
**main へ入れるときに統合側が振り直す。** `?v=` の衝突は行が特定できるので機械的に解ける。
**レーンに振り直しを頼まない**（人が伝言する手間を作らないため）。
既知の重複: main の **v377〜v379**（項目22・23）と `claude/*` の **v377** が同じ番号を使った。
**マージ時に統合側が後者を振り直す。レーン側の対応は不要。**

**全レーン共通の約束**:

- コミットは**パス指定**（`git commit <paths> -m …`）。**`commit -a` は禁止**（別セッションの変更を吸う）
- 衝突しても **`git checkout --ours/--theirs` は使わない**（過去に機能を巻き戻して4テストが落ちた）
- コミット前に `test.html` 全合格 ＋ `node tools/verify-release.js assembler`
- **RX13 は約10%の頻度で落ちる**（重合でできる鎖の座標が毎回変わるため。別レーンで調査中）。
  落ちたら1回再実行して、他の失敗と切り分けること

**入口の再編（項目18・19・24-3）は第1波に入れていない。** `index.html` の骨格を作り替えるので、
他レーンと同時に走らせると必ず衝突する。第1波が片付いてから単独でやる。
**項目21（直交描画と立体解釈の分離）はユーザー判断待ちで、着手しない。**

### 現在地（2026-08-01 時点・main は v361）

**検品レビュー（`video-scripts/REVIEW_2026-08-01.md`）のアプリ側は全項目クローズ**（v343〜v361）。
動画の在庫を通して見たユーザーの指摘を、アプリ改修セッションで順に片付けたもの。

| 版 | 内容 |
|---|---|
| v343 | 立体タイムアタックの操作を3つに絞る（⟲ 反時計回り／⟳ 時計回り／⇄ 鏡像の入れ替え）。180°回転と巡回を外した。**練習モード（fp）は据え置き** |
| v344 | 変化箇所のハイライト円を原子ごとの半径に（自動水素を包む。水素3本の炭素で25px） |
| v345 | アラニンを追加し、アミノ酸4種の主鎖を他の7種と同じ横置きに揃えた（**乳酸は縦置きのまま**＝V50 の前提を壊さない） |
| v346 | 鏡像ペインは「見た目が実際に変わる入れ替え」を選ぶ。左右が同じ置換基（2-プロパノール）だと鏡像が元と一枚も違わない絵になっていた |
| v347 | 生成物の向き。`planAttachment` に180°回転の候補を足し、エステルが横一線に出るようにした。**鏡映は入れない**（不斉炭素が黙って鏡像異性体に化けるため） |
| v348 | 2分子が結びつく反応を「①並ぶ → ②結合ができる」の2段で見せる（`morphStages: 'joinFirst'`。クリック待ち無し） |
| v350 | 反応させる分子をタップで選ぶ（`#btn-reaction-select`）。選んだ分子だけの反応に絞られ、**先に選んだ方が式の左**になる |
| v352 | 呼び出した分子の環外 C=C を ±120° に整える。**シス/トランス未確定の図は整形しない**（2-ブテン・ブテン二酸が trans に化けるため） |
| v353 | けん化を加水分解から分け、カルボン酸のナトリウム塩を生成する（**イオンは持たず -COO-Na を線1本で書く**。Na は価標1・パレット非表示） |
| v355 | 反応の箇所を原子IDではなく座標で決める。`addBond` が端点をIDで正規化するため、走査順に頼ると置換位置が揺れていた |
| v356 | **消しゴムで結合を消す経路にも価標の検査**（v341 監査の「価標超過 S(4/2)・S(3/2)」63件の原因） |
| v358 | 油脂（トリステアリン）と、けん化の途中に出るジ体・モノ体を登録 |
| v360 | 立体パズルの正解後に「実は最短は…」を出し、お題の最初から再生できるようにした |
| v361 | 立体パズルに**上級（中心を切り替えて解く）**を追加。最短手順の探索で「入れ替える中心が2種類以上」を保証する |
| v432 | **長い鎖を畳んで描く**（レビュー項目25・第1段。`DESIGN_chain_condense.md`）。直線に並んだメチレン鎖3つ以上を `(CH₂)ₙ` にまとめ、**畳んだぶんだけ向こう側を手前に寄せる**（ラベルに置き換えるだけでは幅が縮まらない）。モノステアリン酸グリセリドの viewBox 幅 890→260。**どの図で畳むかは呼び出しごとに選ぶ**——「🎓 同じ化合物？」は主鎖をわざと曲げて出すので、一律に畳むと**曲げた側だけ畳まれず同じ分子の2枚が別物に見える**。立体のクイズ（巡回＝剛体回転なので一直線は一直線のまま）でだけ畳む。**キャンバス側の縮小⇆展開は第2段**（`game.js` が空いてから） |
| v430 | 「同じ？違う？」を連続で出してタイムを計る（`ORDER_stereo_puzzle.md` 第3段の残り。**これで第1〜4a段が全部そろった**）。「🎯 同じ立体はどれ？」の出題に足す形で**入口は増やしていない**。図を1つだけ出して ⭕同じ／❌違う の2択にし、**正解すると1.2秒で自動的に次へ送る**。連続正解数と平均解答時間を出す。判定は4択と同じ `relationOf`。出題は狙いではなく**実際の関係で決める**（メソ体などで反転が打ち消されるため） |
| v370 | **D/L を図から計算する**（`ORDER_stereo_puzzle.md` 第4段 4a）。`chemistry.js` の `assignDLDescriptor(mol)`。基準炭素は3系統（アミノ酸＝α炭素の -NH₂／α-ヒドロキシ酸＝-COOH が付いた不斉炭素の -OH／糖＝カルボニルから最遠の -OH）。**CIP には踏み込まない**（4b は未着手）。ライブラリの D-/L- 付き11件すべてで名前と一致。**酒石酸・不斉炭素なし・主鎖が横向きの図は null**（断定しない）。出題は4択の3つ目として足した（**入口は増やさない**方針） |
| v365 | 「🎯 同じ立体はどれ？（4択）」を追加（`ORDER_stereo_puzzle.md` 第3段の最優先項目）。見本1つ＋選択肢4つが1画面に収まる＝そのままサムネにできる形。**記号（化学ぬき）と分子の両方**で出せて、判定は偶奇／`relationOf` の1本。解説に「見本から何手で作れるか」を出す。**「同じ？違う？」のタイムアタック化は未着手** |
| v364 | 記号パズル（模式模型）を追加（`ORDER_stereo_puzzle.md` 第2段）。学習タブ「🔤 記号でパズル（化学ぬき）」。分子を使わず A・B・C・D の並べ替え24通りで同じ規則を練習する。**判定は置換だけ**（正準コード・立体の読み直しを一切通らない）。十字の模型は `CrossModel` として共通部品に切り出し、分子モードと同じ操作面を使う。**完成は「見本とぴったり同じ並び」**にした（偶奇だけだと鏡1回で必ず終わり、最短手数の学びが消えるため。偶奇は毎手ごとに文言で出す） |
| v362 | 立体パズルの操作を**4種類**に作り直し、**十字の模型**に載せた（`video-scripts/ORDER_stereo_puzzle.md` 第1段＝検品レビュー C-5c）。⟳/⟲ 回す（1つ固定して3つ送る＝3巡回・偶）／↔ 縦軸の鏡／↕ 横軸の鏡（互換・奇）。**v343 の「⟳＝90°回転＋左右反転」は廃止**（⟲ が逆にならず、2回で元に戻る＝回転として振る舞わないので最短手数の土台が成り立たなかった）。**V15 は再収録** |

**このセッションで分かった落とし穴**（同じ形のバグを二度踏まないために）:

- **原子IDは乱数で、`addBond` は結合の端点をIDで正規化する。** だから `mol.bonds` の走査順に
  頼ると、同じ手順でも選ばれる原子が変わる。**順序が要る所は必ず座標で決める**。
  芳香族置換の位置（v355）と `vinylBonds` の頭-尾がこれを踏んでいた。
  後者は **RX13 が散発的に落ちていた原因**でもある
- **硫黄の許容価標は S=O の有無で 6↔2 と変わるので、結合を減らす操作でも違反が残りうる。**
  `revertIfValencyBroken` を通す経路を増やすときは、**すべての削除経路**に入っているか確かめる

### v331 レビュー（`docs/review_v331.md`）レーンC「学習内容」（2026-08-02・v400〜v403）

| 版 | 内容 |
|---|---|
| v400 | **D-フルクトース（鎖状）に立体指定を入れた**（データ不整合）。名前に D- が付くのに `stereo` が無く、`stereoCode` が null のままだったので「立体を名前に反映する」を OFF にしても D- が残っていた。`atomParity` は図から読み直した値（C3 左・C4 右・C5 右） |
| v401 | **項目12**: 異性体一覧の分類を厳密にした。`chemistry.js` に `findOutOfScopeMotifs(mol)`（ヘテロ原子どうしの結合／エノール形／同じ炭素に -OH・-NH₂ が2つ／官能基が無いのにヘテロ原子を含む）を追加し、`learn.js` は標準の分類を上に、範囲外を `details` で畳んで理由つきに。C₂H₄O₂ は 10種中3種（ギ酸メチル・酢酸・グリコールアルデヒド）だけが標準に残る |
| v402 | **項目8**: 書き出し練習の答え合わせで、未作成の異性体を官能基の分類ごとに要約（「エーテル 1件、アルコール 1件」）。**確認モードには出さない**（答えが割れる） |
| v403 | **項目6**: 画面に出る用語を**「不斉炭素原子」に統一**（高校教科書の表記）。ヘルプとツールバーのツールチップに「4つの異なる原子・原子団が結合した炭素原子」の説明を添えた。**`sp3炭素` は言い換えない**（不斉炭素原子とは別の概念）。**コード中のコメントは対象外**（画面に出ないため据え置き） |

**判断待ちで着手していない**: 項目21・22・23（直交描画と立体解釈の分離／D/L 既定表示／読めない図での操作解放）は
`docs/PLAN_review_lanes.md` のとおりユーザーの決め待ち。項目18・19・24-3 は「入口の全体見直し」に合流させる。

### レーンF「R/S 判定（CIP）」（2026-08-02・v440〜）

| 版 | 内容 |
|---|---|
| v440 | **R/S を図から判定する**（`ORDER_stereo_puzzle.md` 第4段 4b・`DESIGN_rs_descriptor.md`）。`chemistry.js` に `cipRank(mol, centerId)`（CIP 規則1a の順位づけ。原子番号・重複原子・同順位の掘り下げ）と `assignRSDescriptor(mol)`（フィッシャーの十字から各中心の R/S）を新設。**正準コード側には触っていない**（`canonicalCode` / `canonicalStereoCode` / `computeAtomParity` / `rootedFragmentCode` はそのまま。R/S は呼び名だけで、同値関係には使わない）。**UI へは出していない**（入口の全体見直し待ち） |

**これは方針変更**（R/S は「やらないこと」として明文化されていた）。`chemistry.js` の2か所は
書き換えた。**残り2か所は持ち主レーンが直す**（レーンF は `chemistry.js`・`tests.js` しか触らないため）:

- `quiz.js:640` 「**CIP（R/S）は使わない**」… **統合（main）レーン**の担当。
  記述は今も正しい（クイズの判定は立体コードの比較のまま）ので、直すなら
  「クイズでは使わない。判定そのものは `assignRSDescriptor` にある」程度の言い換え
- `stereo.js:183`・`stereo.js:453` 「R・S の判定（CIP）は実装しない方針」… **レーンD**の担当。
  183 は「ここでするのは姿勢を作ることだけ」、453 は「順位ではなく食い違う場所を示す」が
  実態なので、**やらない理由ではなく役割分担として**書き直すのが素直

**設計の要点と落とし穴**（同じ間違いを二度踏まないために）:

- **順位の比較は「球ごと」に行う。** 1本目の枝を掘り切ってから2本目へ行く素朴な深さ優先は
  CIP と食い違う。`-CH(CH₂CH₂OH)(CH₃)` と `-CH(CH₂CH₂CH₃)(CH₂CH₃)` は第3球の4つ目で
  分かれるが、掘り切る実装だと第1枝の第4球にある O が先に効いて逆が出る（ST36 で固定）
- **主鎖が縦に描かれた図しか読まない。** ライブラリの素のアラニン・システイン・
  フェニルアラニン・酒石酸は -COOH と側鎖を**横**に並べた普通の構造式で、投影の約束
  （縦=奥）で描かれていない。門番なしだと、立体を指定していない図に記号が付く。
  **システインは L 体が (R) になる有名な例外**なので、向きの取り違えは静かに正反対を出す
- 検証は3本立て（設計書5章）: 教科書の期待値11件（グリセルアルデヒド・アラニン・乳酸の
  D/L 各2件、糖4種と D-グルコン酸の多中心）／**鏡像で必ず反対になる**（`fischerOpSwap`。
  触っていない中心は動かない）／既存の同型判定が1つも変わらないこと
- 断定しない（null）: 環の中の中心（ハースの担当）・90°回した図・不斉炭素なし・
  順位が同点のまま尽きる・R（アルキル基の付け根）を含む図。**同位体と擬似不斉（規則4/5）は
  高校範囲外として踏み込まない**

### 前の現在地（2026-07-31 時点・main は v305／レーンD `feat/rs-tips` が v331）

直近: エーテル機構の検収（v227）／`tools/verify-release.js`（v227）／立体ビューの2カラム化（v228）／**Gemini 委託の化合物9件を検収して取り込み（v235）**／**Gemini のレビュー docs/ から9件対応（v238 図の破れ・v242 立体の誤読・v244 吸着範囲・v245 環の回転軸・v247 環ビューの範囲・v249 操作説明・v253 分子ごと移動・v255 複数分子の識別・v260 ①②③と折り返し）**。作業ツリーはクリーン。

**M2.5-A 重ね合わせビューは v299、M2.5-B フィッシャー操作は v301、M2.5-C タイムアタックは v305 で完了**（A: 立体異性体クイズの解答後に「🫟 重ねて確かめる」・`stereoIsomorphismCompare`＝正準ラベリング・ST27／B: 学習タブ「🔃 フィッシャー投影の操作練習」・偶置換の操作だけ・`fischerOp*` 純関数群・ST28／C: 「⏱ 立体タイムアタック」・B の継承＋奇置換 `fischerOpSwap`・ST29）。これで 2026-07-27 のユーザー提案3本（A→B→C）はすべて実装済み。**M2.5 その4の残り（立体のみの書き出し練習）は v311 で完了**（学習タブ「🪞 立体異性体の書き出し練習」・ST30）。

**M2.5 その3 の後半「R・S のティップス」は v331 で完了**（レーンD `feat/rs-tips`）。立体ビューのモーダルに折りたたみの読み物「📖 R・S ってなに？」を置き、「🧊 H を奥に向けて構える」で 3Dビューの軸＋向きへつなげた。**CIP は実装しない方針のまま**で、ST30 が「説明はある／分子の R/S は断定しない」の両方を固定している。

**次はユーザーと相談して決める**。残っている立体系の候補: M2.5 その3の前半（くさび図どうし・くさび図⇄フィッシャーの比較クイズ）／R/S 解説パック（Gemini 委託は未作成。v331 のティップスで最低限は埋まった）／CIP（R/S 判定）の実装（方針どおり優先度は最後）。立体以外なら Gemini レビューの保留分（項目19）、フラノース（五員環の糖）、P12-2（異性体列挙の対応範囲拡張）。**反応実行モードは M1〜M5 が完了・クローズ済み**なので、反応側の残りは P12-4（反応データの拡充）と P12-5（前後比較・モーフィング）にある。

Gemini のレビュー docs/development_plan.md（assembler 分は項目1〜23。24以降は ion-equation / ratio で別セッションの担当）の残り:
- ~~**項目1**（アプリ名を「パズルでみる有機化学」に変更）~~ — **v298 で確定・反映済**。表示名は「パズルでみる有機化学」（姉妹アプリの「◯◯でみる◯◯」に揃える）。
  URL `/assembler/` とリポジトリ名・コード内の識別子は **Chem-Assembler のまま**（内部コード名。リンク切れとキャッシュ事故を避けるため）。
  反映先: `assembler/index.html`（`<title>`・ヘッダーロゴ）、ハブ `index.html`（カード見出し・単元表6行）、`SNS_VIDEO_PLAN.md`・`video-scripts/`。
  test.html / audit.html と各 .js の先頭コメントは開発者しか見ないため内部コード名を残す
- ~~**項目20**（右ペイン「学習」タブのアコーディオン化）・**項目21**（パズルモードの操作手順の常時表示）~~ — **v302 で完了**
  （`feat/ui-portal` を統合。学習タブは `<details class="learn-acc">` 3つ＝🎓 クイズに挑戦／✏️ 構造の書き出し練習／⚗️ 反応機構ビューア で既定は折りたたみ。
  M2.5-B の「🔃 フィッシャー投影の操作練習」はクイズ群の中に置いた。パズルモードはお題カードの上に操作手順を常時表示）
- **項目19**（フィッシャー投影の解説リンク）— ユーザー判断で保留（別セッションの一問一答と内容が重なるため）
- **項目12**（フィッシャーの読みの検証）は ST15・ST26 で固定済み。ただし **R/S 判定（CIP）は実装していない**（項目9 は「枝を辿って比べる」で代替した）
- 項目22（キャンバス上の分子名表示）は v255・v260 の①②③と名札で対応済み

保留中: ティップス（R/S の説明）はユーザー判断で後回し。P12-B1 の iPad 実機確認はユーザー側の作業待ち。Gemini 委託の残りパックのうち**ジアゾ化機構は v301〜v302 で自作して解消**（P12-4 参照）、R/S 解説は未作成。

**並行作業の記録（2026-07-31）**: `git worktree` で3レーンを同時に走らせた（B=学習タブの整理／C=反応機構と命名／A=立体）。
実装ファイルが分かれていたため**内容の衝突は5回のマージで0件**。摩擦は版番号の取り合いだけだった。
次に並列でやるときは**着手時にレーンごとの版番号帯を予約する**とこれも消える。
**版番号だけの衝突でも `git checkout --ours/--theirs` のファイル単位解決は使わないこと**——
古い基点のブランチで main の機能を巻き戻し、Q1・ST27・ST28 が落ちた（衝突ハンクだけを置き換える）。

### フェーズ1: 誤検証の根絶（最優先）

- [x] **P1-1** 座標変換を `getScreenCTM()` ベースに置換（クリック/プレビューの上下端ズレ最大±37px の解消）— v57
- [x] **P1-2** -NO₂ モジュールの結合次数を N(=O)(–O) に修正（ニトロベンゼン・ピクリン酸が通常操作でクリア可能に）— v58
- [x] **P1-3** 芳香環のケクレ位相を検証で吸収（オルト二置換6ステージの理不尽な不正解を解消）— v59

### フェーズ2: データ破壊系バグ

- [x] **P2-1** Undo で `isAsymmetricMarked` が消える問題の修正（状態復元の機械化）— v60
- [x] **P2-2** 中央原子削除で分子全体が消滅する問題の修正（巻き添え削除の廃止。削除の影響は対象のみに）— v61
- [x] **P2-3** 空振り操作での `saveState()` 抑止（消しゴム空クリック・モジュール接続先なし・空の全消去・移動なしドラッグ）— v62

### フェーズ3: モバイル入力

- [x] **P3-1** `touchend` の座標欠落修正 — P3-2のPointer Events統一に包含（pointerupは常に座標を持つ）— v63
- [x] **P3-2** Pointer Events への統一（タップ二重発火・タッチドラッグ不能の根本解決。touch-action:none併用）— v63

### フェーズ4: 整合性・クリーンアップ

- [x] **P4-1** ユーザー可視の文字化け修正（Delete確認ダイアログ）と、化けコメントの一掃（自動逆変換14行＋手動書き直し117行）— v64
- [x] **P4-2** ヘルプと実装の整合 — 原子の右クリック削除を実装（右ドラッグはパンのまま、移動量3px以下で削除と判別）— v65
- [x] **P4-3** `window.onerror` の alert 撤去（#verify-resultトースト＋console出力へ）— v66
- [x] **P4-4** デッドコード削除（`autoLayoutBonds` 空実装と呼び出し7箇所、`belongsToSp2SpTree`、`translateSubtree`）、`getMaxBondType` の `VALENCIES` 一元化 — v67
- [x] **P4-5** 背景グリッド 60px → `GRID_SIZE`(42px) に一致させる — v68
- [x] **P4-6** 判定成功メッセージの文言を検証内容に合わせる（不斉モードON/OFFで出し分け）— v69
- [x] **P4-7** `renderTargetBond` の三重結合描画対応（お手本機能復活の前提）— v70

### フェーズ5: 機能・出題の拡充（方針決定後）

- [x] **P5-1** お手本表示・化学式・説明文をすべて復活（ユーザー決定 2026-07-16）— v71
- [x] **P5-2** 不飽和化合物ステージの復活 — 新シリーズ「不飽和化合物（多重結合）」7問＋アラニン（不斉炭素練習用）追加。stages.json のキャッシュ再検証も強制化 — v73
- [x] **P5-3** S出題の制限をUIに明記（6価=スルホ基用。パレットのツールチップ＋ヘルプに注意書き）。多価対応は将来課題 — v74
- [x] **P5-4** ベンゼン環原子の元素置換対応（価標チェック付き）＋ピリジンステージ追加 — v75
- [x] **P5-5** 反応機構アニメーション基盤の設計（→ [DESIGN_reaction_mechanism.md](DESIGN_reaction_mechanism.md)）
  - [x] **P5-5-M1** reactions.json スキーマ＋ローダー＋静的表示＋巻矢印静的描画（エテン+Br₂、ステップ送り・形式電荷表示付き）— v76
  - [x] **P5-5-M2** ステップ再生アニメ（矢印描画＋状態補間＋▶/⏸コントロール。非表示タブではsetTimeoutフォールバック）— v77
  - [x] **P5-5-M3** 代表反応データ追加＋片羽矢印 — 完了（計4反応収録）
    - [x] 片羽矢印・ラジカル(•)表示・mid矢印（生成中の結合を指す）対応＋メタンの塩素化（開始/成長/停止の3ステップ）— v78
    - [x] エステル化（酢酸＋エタノール、簡略2段階・電荷付き中間体）— v79
    - [x] ベンゼンのニトロ化（アレニウムイオン中間体を経る求電子置換）— v79
  - [x] **P5-5-M4** 生成物予測モード（主生成物をパズルUIで組み立て、明示水素除去＋最大連結成分のターゲットに対して verifyMolecule 判定。パズル作業分子は退避/復元）— v80
- [x] **P5-6** 立体模型との対照ビューの検討（平面構造式と実際の立体構造の比較提示。コンセプト1.1-3）— **P7-5（立体対照ビュー）→ P12-7 M3（1炭素の疑似3D）→ P12-8 M4a/b/c（分子全体の立体）で実現済み。2026-07-27 の計画確認でクローズ**

### フェーズ6: フィードバック対応

- [x] **P6-1** 原子配置のプレビューと実結合の不一致を解消（2026-07-17 ユーザー報告）。プレビューと配置が同一の判定関数 `getPlacementBondTargets` を共有する構成に変更し、複数原子と隣接できる位置では可能な結合をすべて形成する仕様（案2）を採用（環を閉じられる。不要な結合は右クリック削除＋deletedBonds履歴で解除可能）— v81
- [x] **P6-2a** 重なりを避けられない場合の原子配置を禁止（noSpaceフラグ＋案内トースト）— v82
- [x] **P6-2b** 結合の伸縮操作（2026-07-17 ユーザー提案）。結合線を軸方向にドラッグするとグリッド倍数で伸縮。橋判定（結合を除いた連結性）で環内結合は伸縮不可・環は剛体として移動、伸縮方向は結合の現在の軸（ナナメ結合はナナメのまま）、静止側と重なる長さは採用しない、伸縮ではautoConnectを適用しない — v83
- [x] **P6-3** 環への側鎖の振り分け配置（2026-07-17 ユーザー提案）。側鎖1本目=外向き二等分線、2本目=二等分線±30°で、既存側鎖が二等分線上なら枝ごと反対側へ平行移動（回転ではないためその先の直交レイアウトは不変）。環結合/側鎖の区別は橋判定。移動先が塞がっていれば移動しない。プレビューに移動先ゴースト表示。環⇆鎖の切り替え時は既存原子を動かさず、判定は常に配置時点の環所属で行う — v84

- [x] **P6-4** 結合クリックの次数トグルが効かない退行を修正（2026-07-20 ユーザー報告）。v83の伸縮機能で、純クリック時にもupdateDrawingが走ってヒットライン要素が再生成され、clickイベントが届かなくなっていた。純クリック時は再描画しないよう修正し、「クリック後も要素がDOMに残る」ことを検証する回帰テスト（C9）を追加 — v86

- [x] **P6-5** 三重結合の端の自動水素を180°直線配置に修正（2026-07-21 ユーザー報告）。calculateHydrogensに三重結合(sp)の分岐がなくsp3扱い（90°）になっていた。H–C≡C–H の手書き慣習どおり結合の反対側に配置し、回帰テストB7を追加 — v87

### フェーズ7: 品質基盤・公開・コンテンツ制作体制（2026-07-20 方針合意）

- [x] **P7-1** ブラウザ回帰テストページ（test.html + tests.js）— 実アプリをiframeで駆動する26テスト（基盤/検証ロジック/編集操作/伸縮・振り分け/反応ビューア/エクスポート）。6.1章のコミット前チェックを1クリック化
- [x] **P7-2** GitHub Pages で公開 — 既に有効であることを確認（公開URL: https://o9o4oo78o42.github.io/ChemistryGame/ 。main へのプッシュで自動デプロイされる。test.html も同URL配下で動作する）
- [x] **P7-3** 作図エクスポート機能 — キャンバス上部の「⤓ JSON」ボタンで現在の分子を stages 用（target: 重原子のみ）＋ reactions 用（withHydrogens: 自動水素を明示原子化）のJSONとしてクリップボードへコピー — v85
- [x] **P7-4** 小粒の学習体験改善: ①クリア状況のlocalStorage保存＋ステージ一覧の✓表示 ②Redo（Ctrl+Y / Ctrl+Shift+Z。新操作でRedo履歴破棄） ③alert/prompt撲滅（任意員環は員数選択モーダル、官能基の案内はトースト） ④不斉マーク誤りは座標文字列ではなくオレンジ点線円の原子ハイライトで提示 — v96
- [x] **P7-5** 立体対照ビュー（P5-6）の設計書作成 → [DESIGN_3d_correspondence.md](DESIGN_3d_correspondence.md)（M1: sp3炭素の局所くさび図／M2: 全体の標準くさび描画／M3: 疑似3D回転とプリセット配座。実装は設計合意後）
  - [x] **P7-5-M1** sp3炭素の局所くさび図モーダル（stereo.js）— 「🧊立体で見る」→キャンバスでsp3炭素をクリック→くさび形表記（紙面内2本＋手前くさび＋奥ハッシュ）と置換基ラベル（OH/CH₃/組成式）を自動生成。「作図は90°・実際は109.5°の正四面体」の教育文言と不斉判定の説明（不斉なら鏡像異性体、非不斉なら同一置換基の指摘）を連携表示 — v98
  - [ ] **P7-5-M2** 分子全体の標準くさび描画（正準レイアウト）
  - [~] **P7-5-M3** 疑似3D回転とプリセット配座（いす形など）— **疑似3D回転は P12-7 M3（v182）で実装済み**。残りはプリセット配座（いす形 conformers.json）のみ
- [x] **P7-6** 化合物名判定 — 右パネルに「いま描いている分子」カードを新設し、名称（stages.json 57件＋compounds.json 24件とのグラフ同型照合。ケクレ位相も吸収）と分子式（自動水素込み・Hill表記）をライブ表示。生成物予測モード中は名称を伏せる。系統名（IUPAC）アルゴリズムは将来検討 — v88
- [x] **P7-8** 縮合環の作図サポート — (1) 環モジュールの配置を配置計画方式（getRingPlacementPlan）に改修: カーソルが「既存結合を1辺とする正N角形の中心」40px以内なら縮合位置に吸着（辺の向き・長さ任意。五員環×六員環などの異種縮合も可）、それ以外は絶対グリッド丸めの自由配置。ホバー中に環のゴーストを表示（吸着頂点=白抜き、配置不可=赤）し、プレビュー＝実配置を保証。マージされない新規頂点が既存原子27.3px以内なら配置拒否（環の重なり事故防止）。ベンゼンの二重結合は貪欲割り当てで縮合してもケクレ交互を維持（ナフタレン対応） — v102 (2) 直交作図の環（環の隣接2方向がともに水平/垂直）では±30°振り分けではなく格子方向へ配置し、手描きの縮合環構築（長方形六員環→隣接環→中央結合の切断）を可能に — v103。テストJ1（ナフタレン・デカリン・重なり拒否）/J2（手描きデカリン骨格）追加。実クリック検証: ベンゼン2クリック→ナフタレン、六員環+五員環→ヒドリンダン
- [x] **P7-10** バグ探索パス第2弾（クイズ・反応・命名の網羅監査） — 全109化合物×3強度×2回の表記変形で同型性・正準コード安定性を機械検査（654件合格）、幾何指定4エントリのシス/トランス保存（30件合格）、命名クイズ全シリーズ×全強度で72問生成（選択肢重複・正解欠落なし）、反応9機構の前進/後退/再再生とコンソールエラー監視（クリーン）。発見: ニトロ基の単結合Oに自動水素が付き、ニトロベンゼンの分子式が C₆H₆NO₂ と誤表示（画面上も -N(=O)-OH に見えていた）→ getFreeValency にニトロ特例（N使用価標4以上＋N=Oを持つNの単結合Oは空き価標0）を追加して修正。テストK1 — v106
- [x] **P7-9** 官能基モジュールの計画方式化 — 配置ロジックを getFunctionalGroupPlan に分離し、ホバーで接続先原子に配置予定のゴースト（結合線＋元素記号）を表示。プレビュー＝実配置を保証。接続先の空き価標なし／新規原子が既存原子と重なる（27.3px未満）配置は拒否してトースト案内（Undo履歴非消費）。テストJ3（ゴースト表示・計画と実配置の一致・酢酸 C₂H₄O₂・価標拒否）追加 — v105
- [x] **P7-7** タッチパネル対応の堅牢化 — タッチエミュレーション検証で発見した実バグを修正: (1) ピンチ開始時、1本目の指のpointerdownが行った編集（原子配置・伸縮の履歴積み）をスナップショットから巻き戻す（trackPointerDown共通前処理）(2) 結合ヒットライン上から始まるタッチもピンチ判定に参加（従来は2本指と認識されず）(3) ピンチ開始でbondStretchもキャンセル。結合タップの次数トグルは合成clickに依存するためヒットライン側はpreventDefaultしない。テストI1（巻き戻し・履歴クリーン・結合上ピンチ・タッチ伸縮）追加。実機タッチパネルでの確認は未了（ユーザーがデバイス入手後に実施予定） — v101

### フェーズ8: 表記の同値性を学ぶ（2026-07-21 方針合意。「別の描き方でも同じ化合物」の理解を学習目標化）

- [x] **P8-1** シス/トランスの判定と命名区別 — `getDoubleBondGeometry`（環外C=C・2置換のみ対象、外積の符号で側を判定、直線描画は不定）を新設し、命名照合で幾何指定付きエントリを優先。シス-2-ブテン／トランス-2-ブテン／2-ブテン（未指定）を描き分けで区別 — v89
- [x] **P8-2** 正準形基盤 — `canonicalCode`（WL精緻化＋最小行配列のタイブレーク探索。連結成分ごとに正準化してソート結合＝同一成分の繰り返しによる組合せ爆発を防止）。同値関係はverifyMoleculeと同一（重原子＋自動H数＋結合次数、ケクレ吸収、立体は区別しない）。名称判定をコードMapのO(1)照合＋verify最終確認に変更。不斉判定を根付き正準コード（`rootedFragmentCode`）による厳密比較へ置換し、環近似だった旧serializeSubtreeを削除。性質テスト（全ライブラリの原子順シャッフル不変・全ペアのコード一致⇔同型・ケクレ不変・非連結ケース）で防御 — v95
- [x] **P8-3** 「同じ化合物？」クイズモード（quiz.js）— 「同じ」問題は1化合物を表記変換（90°回転・反転・ケクレ位相反転・橋結合の伸長。すべてトポロジー不変）して並置、「違う」問題は同分子式・別トポロジーの異性体ペア（自動抽出）を並置。正誤の正はverifyMolecule。解説で名称・分子式・「つながり方が同じなら同じ化合物」を提示。データ作成は不要（既存ライブラリ81件から自動生成）— v90
- [x] **P8-4** 命名クイズモード — 意図的に正準形でない図（表記変換を1〜2回適用）を提示して名称を4択で回答。誤答選択肢は同分子式の異性体名を優先して自動生成（不足時は他の名前で補完）。同一トポロジーで別名のエントリ（2-ブテン系）は正解が一意に決まらないため出題対象外。表記変換・描画・ライブラリ構築はP8-3と共有ヘルパー化 — v91
- [x] **P8-5** クイズ調整（2026-07-21 フィードバック）: ①出題範囲のシリーズ絞り込みドロップダウン（compounds.jsonは「その他の有名化合物」として選択可）②崩し方の強度3段階（弱=回転・反転のみ／標準／強=ケクレ反転必須＋伸長2回・最大84px。命名クイズは強で変換2回）③回答解説に「構造のポイント」（describeStructureによる骨格・多重結合・官能基の自動要約。正解時にも表示、「違う」問題では左右それぞれを表示）— v92
- [x] **P8-6** 命名可能化合物の範囲拡張（第1弾）— ソースは「高校化学（検定教科書）の有機分野の定番」。アルコール異性体（1-/2-ブタノール・tert-ブチルアルコール）、カルボニル系（プロパナール・ブタノン・ベンズアルデヒド）、酸・エステル系（アクリル酸・酢酸ビニル・無水酢酸・アジピン酸・フタル酸・テレフタル酸）、マレイン酸／フマル酸（幾何指定付き＋未指定名）、アミノ酸（グリシン）、ナイロン原料（ヘキサメチレンジアミン）、ハロゲン化物（クロロメタン・1,2-ジブロモエタン・1,1-/1,2-ジクロロエタン・クロロベンゼン）、芳香族（エチルベンゼン・クメン・アセトアニリド・TNT）、酸化エチレン等の27種を追加し計53種に。以後の拡充は⤓JSONエクスポートで随時 — v97
- [ ] **P8-7** 反応機構コンテンツ拡充（教科書定番の候補リスト: エテンへの水付加／エタノールの分子内脱水→エテン・分子間脱水→ジエチルエーテル／アルコールの酸化→アルデヒド→カルボン酸／エステルのけん化／ベンゼンのスルホン化・塩素化／アニリンのジアゾ化とカップリング。リクエストに応じて順次データ作成）
  - [x] エテンへの水付加（酸触媒・3ステップ: π攻撃→カルボカチオン→オキソニウム→脱プロトンで触媒再生）／エタノールの分子内脱水（逆反応の対・E1: プロトン化→水の脱離→β脱離）／酢酸エチルのけん化（OH⁻攻撃→四面体中間体→エトキシド脱離→酸塩基でカルボン酸塩に）を追加。全機構の価標・添字を機械検証するテストE4も追加（計7機構） — v99
  - [x] ベンゼンのスルホン化（SO₃の求電子攻撃→アレニウム→O⁻がH⁺を回収）／塩素化（分極したCl₂の攻撃→アレニウム→Cl⁻がH⁺を回収してHCl）を追加（計9機構）。E4は登録済み全機構の通し再生に拡張 — v100
  - [x] **エタノールの分子間脱水**（ジエチルエーテル生成・4状態3手順: プロトン化→オキソニウム／もう1分子のOがその炭素をSN2型で攻撃＋水が脱離／脱プロトンで触媒再生）を追加（計10機構）。`dehydration_inter` に `mechanismId` を付けて「反応実行→機構を見る」の導線も接続。**データ作成は Gemini に委託**（qa/gemini-pack-ether-mechanism.md）し、取り込み時に検収 — v227
    - 検収でやったこと: (1) Gemini 版は**ファイル全体が再整形**されて差分5105行になっていたため、構造比較で「既存9件は無改変・`ethanol_ether` の追加のみ」を確認し、**既存9件をバイト単位で再現できる整形器**を書いて新規分だけを追記（差分217行の純追加に圧縮）。(2) `verify-reactions.js` では判定できない「**攻撃されている炭素が本当に -OH₂⁺ の付いた炭素か**」を結合の添字を直接読んで確認（隣の炭素ではない）。(3) 実機で3手順を送り、巻矢印の始終点が意図した原子・結合に一致することを SVG のパスから確認。(4) 状態ごとの結合交差・原子の接近を機械チェック（交差なし・最近接38px）
    - 直した点: 2手順目の解説が「背面攻撃」と書いていたが、**直交作図では180°の背面攻撃を描けない**（図がそう見えない）。図が示していないことを断定しない文言に変更
    - 残り候補: アルコールの酸化（酸化剤の扱いが高校範囲では機構化しにくいため保留）／アニリンのジアゾ化とカップリング（多段・要設計）

### フェーズ9: 反応実行モードと学習の深化（2026-07-22 ユーザー要望を登録。優先順は都度相談）

- [x] **P9-1** 反応実行モード（M1〜M5・検収・フォローすべて完了につき2026-07-24クローズ） — 設計書: [DESIGN_reaction_execution.md](DESIGN_reaction_execution.md)（2026-07-22 作成・ユーザー確認待ち）。ユーザーが分子を1つ以上作成（または名称から呼び出し）→「この分子の反応」→反応種を選択（酸化／付加／置換／エステル化／脱水縮合…）→反応が進行して生成物に変化する。反応表示は「簡易版（前後モーフィング）」⇔「機構版（登録済み巻矢印機構の再生）」切替。ルールベース（detect/apply）＋官能基検出（chemistry.jsに純粋関数で追加）＋試薬パレット。M1: 名称呼び出し＋プロパティカード / M2: 単分子反応（酸化・脱水） / M3: 二分子反応（エステル化等） / M4: 付加・けん化・機構版連携 / M5: 芳香族置換ほか
  - [x] **P9-1検収フォロー**（ユーザー方針: 進行しにくい反応は「除外」でなく「原理的には可能だが…」と説明し、無水酢酸アセチル化へ誘導する教育スタイル）— 酢酸＋フェノールで「⚠エステル化（フェノールは進行しにくい）」解説ボタンを表示（共役による低反応性・平衡の偏りを説明し、アセチル化ボタンへ誘導。分子は不変・Undo非消費）。実行可能な「アセチル化（無水酢酸）」ルールを新設: フェノール性OHとアミンのNH₂にアセチル基CH₃CO-を付加（かたまり衝突判定つき配置）。アニリン→アセトアニリド、サリチル酸→アセチルサリチル酸（アスピリン）を名称判定で確認。learn.jsのフェノール学習ポイントにも同説明を追加。テストL6更新＋L7 — v118
  - [x] **P9-1検収**（2026-07-22 Fableによる差分レビュー・モデル分担ルールの「実装と検収の分離」の初回実施）— v111〜v115（M3〜M5・異性体列挙・監査修正）をレビュー。プローブで実バグ2件＋化学的不正確1件を発見し修正: (1) アルキンへの水付加がエノール（C=C-OH）のまま停止 → ケト・エノール互変異性でケト形を直接生成（アセチレン→アセトアルデヒド、プロピン→アセトン[マルコフニコフ則で内側Cに=O]）(2) エノールに酸化ルールが適用でき、ケテン CH₂=C=O（高校範囲外の異常構造）を作れた → findFunctionalGroups に enol 型を新設して通常のアルコールと分離（酸化・脱水・エステル化の対象外に。プロパティ表示は「エノール形のヒドロキシ基（不安定）」）(3) フェノールがエステル化の相手に許可されていた → 除外（直接エステル化は進みにくく、教科書は無水酢酸によるアセチル化。将来ルールで対応）。その他（マルコフニコフの向き・アシル-酸素開裂・同位体キャプション・スルホ6価・splitMolecules・isValencyValid・列挙上限）は妥当と判断。テストL6 — v116
  - [x] **P9-1-M5** 芳香族求電子置換 — ニトロ化（混酸）・スルホン化（濃硫酸・S=6価の-SO₃H）・塩素化（Cl₂/鉄触媒）。置換位置は環炭素をハイライトしてクリック選択（オルト/パラの学習に接続可能）。置換基は outwardSpot で環の外向き（直交に限らず環の角度に沿う方向も探索）に配置。ニトロ基は N(=O)(-O) の電荷分離形。ベンゼン→ニトロベンゼン C₆H₅NO₂／ベンゼンスルホン酸／クロロベンゼンを名称判定で確認。テストL5 — v113
  - [x] **P9-1-M4a** 付加反応とエステルの加水分解 — 多重結合（非芳香族C=C/C≡C）への付加4種: Br₂（臭素水の脱色）・H₂（Ni触媒・硬化油）・HBr・H₂O（酸触媒）。試薬ごとに独立したルールとして提示し、専用パレットは不要にした。水素は明示原子にせず自動水素に任せる（結合次数を1下げるだけで飽和が進む）。片側置換の HBr・H₂O は**マルコフニコフ則**（置換基の多い炭素に X）で主生成物を選択。C≡C は1段階ずつ進行。けん化・加水分解はエステルのアシル-酸素開裂で分割し、アルコール断片を引き離してカルボニルCに-OHを追加（ラクトンは分割しない）。実行時例外では restoreState で開始時状態へ確実に復帰するよう execute を修正。テストL4 — v112
  - [x] **P9-1-M3** 二分子反応の実行 — エステル化（カルボン酸＋別分子のアルコール/フェノール。カルボン酸側の-OHが水として脱離）、分子間脱水（アルコール2分子→エーテル＋水）。相手分子は planAttachment で平行移動して結合位置に寄せる（4方向を試し重なりを回避。空間がなければ実行せず案内）。適用箇所が複数のときは「候補を分けている原子だけをハイライト→クリックで段階的に絞り込む」方式に改良。あわせて名称カードを分子ごとの表示（`酢酸エチル ＋ 水`）に変更し、副生成物のある反応でも生成物名が読めるようにした（splitMolecules / lookupCompoundName）。テストL3 — v111
  - [x] **P9-1-M2** 単分子反応の実行（reactor.js）— 反応ルール5種を実装: 酸化[O]（1級/メタノール型→アルデヒド・2級→ケトン・アルデヒド→カルボン酸[空き直交方向にO追加]・3級は解説のみ表示）、分子内脱水（C-O切断＋α=β二重結合化。脱離したOは分子の外側へ移動し自動Hで水H₂Oとして残る。β候補が複数のときはザイツェフ則で置換の多い側を主生成物に）。カードに適用可能な反応ボタンを列挙し、適用箇所が複数のときはハイライト＋クリック選択。実行=1 Undo・変化箇所ハイライト・教育キャプション付き。エタノール→アセトアルデヒド→酢酸の連鎖と名称判定の連動を確認。テストL2 — v110
  - [x] **P9-1-M1** 「⚗この分子の反応」カード（プロパティ表示＋名称呼び出し）— findFunctionalGroups を chemistry.js に新設（アルコールの級・フェノール性OH・アルデヒド・ケトン・カルボキシ・エステル・エーテル・アミノ・ニトロ・C=C/C≡C・ベンゼン環を検出）し、右パネルに分類をライブ表示（複数分子は【N分子】表記）。名称検索（datalist・全ライブラリ）から分子をキャンバスへ配置（新IDでディープコピー・既存分子の右へグリッド整列配置・1呼び出し=1 Undo）。テストL1 — v109
- [x] **P9-2** 官能基の表示切替（縮約カード）— キャンバス上部の「🔤官能基をまとめる」で、-COOH / -CHO / -NO₂ / -SO₃H を1枚のカードに縮約表示（再クリックで全結合表示へ復帰）。対象は chemistry.js の findCondensableGroups（純粋関数）が検出し、「1つの原子に末端の枝だけがぶら下がる基」に限定（環や主鎖は縮約せず、骨格が消えるギ酸のような分子は対象外）。カードの向きは基が実際に伸びている方向を優先しつつ、既存原子と重なる向きを避けて直交4方向から選ぶ（方向の最適化）。**表示のみの切替**で作図データは不変のため、正解判定・反応実行・エクスポート・名称判定に影響しない。テストO1 — v123
- [x] **P9-3a** 構造異性体の全列挙と学習ビュー（learn.js）— chemistry.js に enumerateConstitutionalIsomers（純粋関数）を新設: 重原子の組成＋水素数から、価標制約つきの連結グラフをDFSで全生成し正準コードで重複除去（次数確定時の非連結枝刈りで高速化。C₄H₁₀O が13ms）。既知の異性体数と一致することを確認（C₄H₁₀O=7 / C₃H₈O=3 / C₄H₁₀=2 / C₅H₁₂=3 / C₄H₈=5 / C₂H₆O=2）。「📚同じ分子式の異性体を調べる」ボタンから、官能基による分類ごとの件数（例: アルコール4種・エーテル3種）、ライブラリ登録名（自分自身は「いま描いている分子」と明示）、書き出し方の4ステップ解説、検出官能基に応じた学習ポイント（級の判定・酸化生成物・銀鏡反応・付加/置換など）を表示。テストM1 — v114
- [x] **P9-3b** 異性体ギャラリー — chemistry.js に自動レイアウト layoutMolecule（純粋関数）を新設: 直交作図コンセプトに沿い、環は長方形/家型テンプレート（findAnyCycle で最初の閉路を検出、3〜8員対応）、鎖は親からの直進優先→直交→距離2倍→螺旋探索のBFS配置。6分子式・全39異性体で原子間24px以上・結合長130px以下を機械検証。学習モーダルの各分類ブロックに構造式サムネイル（自動水素込み・quiz系の renderMoleculeIntoSvg を再利用）を格子表示し、いま描いている分子はシアン枠＋「（この分子）」で強調、登録名はサムネイル下に表示。テストM1拡張（7サムネイル描画・強調枠1個・C₄H₈全異性体のレイアウト非重なり） — v117
- [ ] **P9-3** 分子プロパティ・学習リンク — 作成/呼び出した分子の分類・特徴をプロパティ表示（例: エタノール→1級アルコール）し、そこから関連学習へ誘導（アルコールの酸化反応・級の判定、2-ブタノール→C₄H₁₀Oの構造異性体：アルコール4種＋エーテル3種の全パターン表示と書き出し方の解説）
- [x] **P9-4** 同じ/違うクイズの出題改善（主鎖の屈曲）— 表記変形に屈曲（bend）を追加。単結合の橋を軸に、その先の枝全体を90°回転させて「主鎖が一直線でない」描き方を作る（直交作図のまま曲げるので手書き感覚を保つ）。多重結合を含む枝は sp2/sp の120°/180°作図を壊さないため回さない。候補と回転方向をランダム順に試して重なりを回避し、3パスの打ち消しで直線に戻った場合は最後に1回だけ曲げ直す。強度1で確率0.6・強度2で必ず試行、強度0は原形のまま。検証: 68化合物×20回=1360試行で価標NG0・トポロジー変化0・最小原子間35px、一直線の化合物が78%で屈曲（残りは多重結合のみで直線が正しいもの）。テストM3 — v122
- [x] **P9-6** アニメーション付きチュートリアル（M1〜M3完了につき2026-07-24クローズ） — 設計書: [DESIGN_tutorial.md](DESIGN_tutorial.md)
  - [x] **P9-6-M1** 再生器（tutorial.js: TutorialPlayer）＋FAQモーダル＋3パート（tutorials.json: 原子配置・結合編集・環と縮合）— ❓ヘルプ→一覧・検索（title/summary/keywords部分一致）・デバイス切替（pointer:coarse自動判定＋手動）。実PointerEventで本物のアプリを駆動し、ゴーストカーソル（イージング移動＋クリックパルス）と字幕バーで解説。再生前に作図・履歴・元素選択を退避し終了/中断（✕/Esc）で完全復元。デモの結末は名称判定と連動（エタノール／アセチレン／ナフタレン=この機会にcompounds.jsonへ追加し計54種）。fastモードで回帰テストが全パートを通し再生（座標陳腐化の自動検出）。テストN1 — v119
  - [x] **P9-6-M2** 残り4パート（結合を伸ばす／官能基を付ける／反応を実行する／画面の拡大・移動）＋ホバー導線。新アクション型: summon（名称呼び出し）・reactionButton（反応ボタン＋適用箇所の自動選択）・wheel（Ctrl+ホイールのズーム）・pan（右ドラッグ）。主要ボタン7箇所に `data-tutorial` を付与し、800msホバーで「▶デモを見る」チップを表示（タッチ環境では自然に無効）。**性能問題を1件修正**: 高速モードでもタイマー待機していたため、バックグラウンドタブのタイマー抑制（最大1秒）で7パートの再生が数分に膨張しテストが完走しなかった → `sleep(0)` をマイクロタスク化し、高速モードの全待機を0に。7パート通し再生が**149ms**で完走。テストN1拡張（全7パートの結末: エタノール/C₂H₂/ナフタレン/プロパン/フェノール/エタノール(Undo後)/ベンゼン、ホバー導線の存在確認） — v120
  - [x] **P9-6-M3** 追加2パート（反応機構を見る: モードON→機構選択→ステップ送り→巻矢印と形式電荷の説明／分子を調べる: 異性体ギャラリー・官能基のまとめ表示・立体ビューの案内）と、FAQテキスト項目3件（正しく描いたのに不正解／水素の置き方／保存の可否）をFAQ一覧に同居（`answer` を持つ項目は「答えを見る」で開閉。検索は本文も対象）。新アクション型 toggle（チェックボックス）・select（ドロップダウン）を追加。デモ終了時に反応機構モードと全モーダルを確実に閉じるようにした。全9デモの通し再生は194ms。テストN1拡張 — v125— 入力デバイス別（タッチパネル／マウス／タッチパッド）の操作チュートリアル。実キャンバス上でポインタの動きを再現するデモアニメ＋字幕。呼び出し方は邪魔にならない形: ❓ボタンからのFAQ一覧＋検索、各ボタンのマウスオーバー（長めのhoverで「デモを見る」チップ）、パート単位で個別再生可能。デバイスはpointerメディア特性で自動判定＋手動切替。データはtutorials.json（手順＋座標列）＋tutorial.jsで駆動（回帰テストのイベント駆動基盤を再利用できる見込み）
- [x] **P9-5c** 夜間監査（2026-07-22深夜・v120で5.6時間/5272件）の結果反映 — **実バグ1件を発見・修正**: 表記変形のケクレ位相反転が芳香族結合を一律に反転するため、縮合環（ナフタレン）では環の共有炭素が5本結合になり、クイズで化学的に不正な構造が出題されうる状態だった（v119でナフタレンを追加した翌日に検出）。反転を仮適用して isValencyValid で検査し、妥当な場合のみ採用する方式に修正（単環は従来どおり別のケクレ構造に反転、縮合環では反転せず回転・反転・伸長のみで表記を変える）。縮合系の正しい別ケクレ構造の生成は完全マッチングが必要なため将来課題。残る指摘は乱操作特有の混雑（原子間12〜24pxの接近23件・自動水素の接近36件）のみで、**0.0pxの完全重複と価標超過（ファズ由来）はゼロ**。テストM2 — v121
- [x] **P9-5b** 監査の強化と検出バグの修正（2026-07-22 実施）— ファズに反応実行（M2〜M5）と名称呼び出しを追加、ライブラリ検査に「異性体列挙に自分自身が含まれる」不変条件を追加、価標判定をアプリ本体と共通化（isValencyValid）、反復ごとに履歴をクリアしてシード再現性を確保、失敗シードを再現する診断フック（window.auditRerun）を追加。**発見・修正した実バグ4件**: (1) ニトロ基の -O を別元素に置換すると中心Nが4本結合のまま不正になる → 置換時に隣接原子の妥当性も検査 (2) 芳香族置換の置換基の酸素が衝突判定なしで配置され4pxまで接近 → 置換基をかたまりとして扱い向きを探索 (3) アルデヒド酸化の -OH が空き確認なしで追加され既存原子と完全に重なる（0.0px） → freeSpotAround を使用 (4) 異性体列挙がベンゼン等で5.7秒かかりUIが固まる → 打ち切り上限を調整し「計算中」表示＋描画を譲る非同期実行に変更、学習ビューの対象を重原子6個までに制限。監査の問題件数 92件 → 0〜数件（残りは混み合った分子での自動水素の接近＝表示上の軽微な事象）。テストK4 — v115
- [x] **P9-5** 夜間自動監査ページ（audit.html + audit.js）— ①ライブラリ検査: 全化合物（原形＋強度2変形）を自動作図し、名称対応（同型判定・正準コード）・孤児結合・価標超過（ニトロ特例込み）・原子/自動水素の重なりを検査 ②ランダム操作ファズ: 実イベントでランダム操作（配置・モジュール・次数トグル・切断・削除・伸縮・Undo/Redo）を流し込み同じ不変条件を検査。シード記録付きで失敗は再現可能。結果はJSON保存して後から確認・評価。試運転248件（ライブラリ218＋ファズ30反復）問題0件 — v108

- [x] **P9-7** UIフィードバックの改善（ユーザー実機フィードバック 2026-07-23）— (1) 官能基のカード化で-COOH等の接続線が9pxのスタブになり結合に見えなかった → カード距離を「アンカーからカード手前の辺まで丸1マス」に取り直し、通常の結合と同じ長さ（約31px）の接続線を引くよう修正。(2) 環など混み合った炭素に官能基を作図的に置けない場合、従来は拒否のみだったのを、直交4方向×標準/2倍の結合長で空きを探索して外側に伸ばして配置（完全に塞がれた場合のみ拒否）。getFunctionalGroupPlan を buildAt で方向・距離をパラメータ化。(3) 官能基モジュールと不斉炭素マークモードが排他でなく、handleMouseDownで不斉が優先されてモジュールが置けなかった → 相互排他に（モジュール選択で不斉解除、不斉ON でモジュール解除）。(4) 不斉炭素マークモード中のホバープレビューを追加（カーソル下の炭素に破線リング＋*。実際に不斉炭素ならオレンジ、そうでなければ赤で誤マークを警告、マーク済みはグレー×で解除を示す）。テストP1 — v126

- [x] **P9-8** 縮合環の接合原子への置換基の配置方向を改善（ユーザー実機フィードバック 2026-07-23）— 接合炭素（環結合3本）に炭素/官能基を付ける際、従来は直交4方向[0/90/180/-90]を順に試すフォールバックで、どちらか一方の環に偏った向きになりやすかった → largestGapDirection を新設し「隣接原子がつくる最も広い空き角の二等分線方向」を第一候補にする（直交はフォールバックとして残す）。getFunctionalGroupPlan でも隣接2つ以上なら同じ二等分線を優先し、単純な鎖（隣接0〜1）は従来どおり90°丸め。デカリン接合炭素で置換基が真上（空き角の中央）へ置かれ既存原子と重ならないことを確認。既存の環側鎖ロジック（D4/D5/D6のcyclohexane 2隣接ケース）は不変。テストP2 — v127

### フェーズ10: モード再編とメニュー整理（2026-07-23 ユーザー合意。3モード=学習/パズル/自由）

- [x] **P10** モード再編（M1〜M4完了につき2026-07-24クローズ）— 設計書: [DESIGN_ui_modes.md](DESIGN_ui_modes.md)。散在する機能を3モード（🧩パズル/📚学習/🧪自由）に整理し、上部にモード切替タブ。左パレットは共通、右パネルがモードで切替。不斉マークボタンを左パレットへ移設＋パズルの判定オプションに不斉判定スイッチ。自由モードは白紙の砂場（お題・判定なし）
  - [x] **P10-M4** FAQ「モードとは」を追加 — tutorials.json に faq-modes（パズル/学習/自由の違い・分子はモードをまたいで保持・前回モードの復元）を追加。テストN1拡張（FAQ4件以上＋faq-modes存在） — v144
  - [x] **P10-M3** 自由モードの純化 — 検証の結果、M1（v128）の実装で要件を満了していることを確認: 自由モードの右パネルは「いま描いている分子」「⚗この分子の反応（分析ツール: 異性体・立体・名称呼び出しを含む）」のみで、お題・判定・ステージ選択は非表示。モードのlocalStorage保存もv128で実装済み。ヒントボックスは全モード共通の操作説明のため存置 — 追加実装なし
  - [x] **P10-M2** 不斉炭素の扱いを再配置（#3・#4）— 兼用だった asymmetricMode を分離: (1) 不斉マーク編集モードを左パレット「操作モード」の✳ボタン（btn-asym-mark）へ移設。Select/Bond/Eraseと排他（ツール選択で解除）。(2) 判定に含めるかを、パズルの新設「判定オプション」枠のスイッチ check-judge-asymmetric（judgeAsymmetric）で制御。verifyCurrentStructure は judgeAsymmetric を参照。修正: btn-asym-markがtool-btnクラスを持つためtool-btnハンドラと二重発火し自身のactiveを消していた → ツール系の処理を .tool-btn[data-tool] に限定。テストP3、既存G4/P1を新方式へ更新 — v130
  - [x] **P10-M1** モード切替タブと右パネルの3分割 — 上部に🧩パズル/📚学習/🧪自由タブ。右パネルの各ブロックにdata-modes属性を付け、setMode(mode)で表示/非表示を切替（パズル=ステージ選択・お題・判定／学習=クイズ・命名・反応機構ビューア／自由=反応カード・異性体・立体・名称呼び出し。いま描いている分子カードはパズル+自由、判定結果/トースト先のverify-resultは全モード共通で常設）。作図中の分子はモード切替で保持。学習を離れると反応機構モードを終了。モードはlocalStorageに保存し次回復元。左パレット（作図の道具）は共通。テストQ1 — v128

### フェーズ11: スマホ対応（レスポンシブ）（2026-07-24 ユーザー合意。縦横両対応・設計書先行）

- [x] **P11** スマホ対応（M1〜M3f完了・実機確認済みにつき2026-07-24クローズ）— 設計書: [DESIGN_mobile.md](DESIGN_mobile.md)（作成済み・実装はユーザー確認後）。キャンバス最優先で、ツール・お題パネルをドロワー/下シート化。ブレークポイント min-width:900px=デスクトップ現状維持、899px以下は向き別レイアウト（縦=キャンバス上・ツール下バー・右パネル下シート／横=圧縮3カラム）。CSS中心＋最小限のJS、座標はgetScreenCTM依存で自動追従。M2:横レイアウト / M3:仕上げ（タップ領域・モーダル・チュートリアル）。
  - [x] **P11-M3f** ☰お題・設定をリボン列に統合（実機フィードバック: 固定レーン方式だと環境の dvh 差で数px不足しスクロールバーが出て鬱陶しい）— ☰ボタンのDOMを canvas-header 内へ移動し、横向きでは position:static でリボン列の最下段に通常フローで流し込む（固定64pxレーンを廃止し bottom:8px までを列が使う）。縦ではfixed（bottom:148px右下の浮動ボタン）・PCでは非表示のまま＝DOM位置は無関係。あふれた場合も scrollbar-width:none / ::-webkit-scrollbar で棒は出さない（スクロール自体は可能）。812x300で☰込み1列・オーバーフロー1px・バー非表示・開閉動作を実測。※検証時の注意: Browserペインはリサイズ直後にカスケード再評価が遅れることがある（縦向きの bottom:148 が一時 12px と誤計測 → リロードで正常）。実機の画面回転では発生しない — v143
  - [x] **P11-M3e** 横リボンの2列化是正とスリム化（実機フィードバック）— 実機でリボンが2列になる原因は、共通ブロックの flex-wrap:wrap が縦列にも効き、アドレスバーで列の高さが不足すると折り返して2列目を作っていたこと（フル高さの検証環境では再現せず）。flex-wrap:nowrap で1列固定にし、はみ出しは縦スクロール。ボタンをスリム化（min-height 34px・padding 5px 8px・gap 4px。☰は38px）。「官能基をまとめる」は <wbr> をやめ全角スペース（官能基を　まとめる）で折返し位置＋見た目を両立。812x300（アドレスバー相当）で1列・スクロール・☰非重なりを実測 — v142
  - [x] **P11-M3d** 横リボンの重なり是正と改行固定（実機フィードバック）— (1) ☰お題・設定が全消去と重なる問題: リボン列に bottom:64px の下端制約を付けて☰専用レーンを確保（アドレスバーで dvh が縮んでも両者は同時に追従するため重ならない。レーン不足の極低画面はリボン側をスクロール）。☰は幅108px・右端でリボン列の最下段にそろえた。(2) 「官能基をまとめる」の改行位置はCJK任意折返しでフォント環境依存だった → ラベルに <wbr>（官能基を|まとめる）を入れ word-break:keep-all で「<wbr>以外では折らない」ようにし、全環境で同じ2行に固定 — v141
  - [x] **P11-M3c** 横リボンの縦列化＋化合物名チップ（実機フィードバック）— (1) 横向きのオーバーレイボタン群を上の横並びから「右端に幅108px統一の縦積み」へ変更（アドレスバーで縦が貴重なため。❓フロートの下から開始。「官能基をまとめる」は2行折返し）。(2) モバイル両向きでキャンバス左下に化合物名チップを常設: syncMobileNameChip が右パネルの「いま描いている分子」と同期し「名称＋分子式」（名称なしは分子式のみ）を表示。学習モード・空分子では消える（:empty で非表示）。setMode/updateCompoundInfo の全分岐で同期。PCは .mobile-only で非干渉。テストR7 — v140
  - [x] **P11-M3b** 横画面の縦幅確保（実機フィードバック: 横だと縦が狭い）— 横向き限定で (1) ロゴバー（ヘッダー44px）を廃してヘッダーを absolute 化、❓ヘルプだけ右上にフロート表示（ロゴ非表示・ドロワーは top:0 に）(2) キャンバス上部リボン（↩↪/まとめ/全体表示/全消去）を半透明の absolute オーバーレイとしてキャンバスに重ねる（隙間は pointer-events:none でキャンバス操作を透過、ボタンだけ受ける）。座標表示は非表示。キャンバス実測 250px→361px（+45%）。縦・PCは非干渉を実測確認。テストR6 — v139
  - [x] **P11-M3** 仕上げ（タップ領域・モーダル・チュートリアル連動）— (1) タップ領域44px基準: 下ツールバー/横ツール列のボタン44px・シート開閉トグル45px・閉じるボタン44px・モードタブ45px・キャンバス上部ボタン42px（❓ヘルプのみヘッダー44px制約で34px）。⤓JSONはコンテンツ制作用のためモバイルでは非表示（DOM存置）。(2) モーダル: 余白を18pxに圧縮して内容幅を確保。FAQの検索欄が flex min-width:auto で縮まずデバイス選択が5pxはみ出していたのを min-width:0 で修正。learn/quiz/stereo は375px幅で横はみ出しなしを実測確認。(3) チュートリアルのシート連動: デモが右パネル内の要素（summon・反応ボタン・トグル等）を操作する前にシートを自動で開き、キャンバス操作の前に閉じる（setSheetOpen/syncSheetFor。PC≥900pxでは無効）。終了時は必ず閉じる。375px幅で reaction/mechanism デモの通し再生エラーなしを確認。テストR5 — v138
  - [x] **P11-M2d** 2本指ドラッグでキャンバスをパン（ユーザー質問で発覚: スマホに平行移動の操作が存在しなかった。ピンチは中点固定のズームのみ・右ドラッグパンはPC専用）— ピンチ処理をアンカー方式に変更: 開始時に2本指の中点の下にあった論理座標(anchor)を記録し、移動中は「新しい倍率を先に適用 → getScreenCTMで現在の中点の論理座標を取得 → anchorとのずれ分だけviewBoxを平行移動」。指の間隔の変化=ズーム・中点の移動=パンが同時に効く。中点が動かない場合は従来のズーム挙動と同一。テストI2（純パンで倍率不変・方向・続けてピンチ・誤配置なし） — v137
  - [x] **P11-M2c** Undo/Redoボタンをキャンバス上部に常設（ユーザー要望: スマホにキーボードがなく Ctrl+Z が使えない）— canvas-header に「↩ 戻す」「↪ やり直し」を追加（PC/モバイル共通。既存 undo()/redo() を呼ぶだけ）。分子分裂トーストの案内文も「↩ 戻す（Ctrl+Z）」に更新。付随して発見・修正したモバイルレイアウト2件: (1) canvas-header は基本CSSで高さ40px固定のため、折り返した2行目がキャンバスに重なっていた → モバイルでは height:auto。(2) 横向きで svg-wrapper がSVGの縦横比由来の固有高さ（812px幅で483px）を min-height:auto として保持し、キャンバス下部約40%がクリップされていた → min-height:0（M2からの潜在問題）。テストR4 — v136
  - [x] **P11-M2b** モバイルの操作ボタン削減（ユーザー合意: チュートリアル・初回ヒントで代替）— モバイル（≤899px）で Select ボタンと結合タイプ枠（単/二重/三重）を非表示に。DOMには残す（PC表示・チュートリアル・テストのため）。根拠: 結合次数は結合線タップのトグルで変更可能（C9）、Select は既定モードで原子/モジュール選択時に自動復帰。代替導線: (1) アクティブツールの再タップで Select へ復帰（setTool へ集約。結合次数ボタンは .click() でなく直接 setTool('bond')—連打での解除発火を防止）(2) 初回の結合作成時に一度だけ「結合線タップで次数変更」ヒントをトースト表示（localStorage: chemHintBondToggle）(3) チュートリアルのゴースト再生は非表示ボタン（rect=0）をカーソル演出なしでクリック実行。showToast のタイマーが後続の別メッセージを消さないよう修正。Eraser・Draw Bond・✳ は存置（削除の非常口／既存原子同士を結ぶ唯一の手段／不斉マークの唯一の入力）。左列スクロール高 908→716px。テストR3 — v134
  - [x] **P11-M2** モバイル横レイアウト（圧縮3カラム）— モバイルCSSを「縦横共通/(orientation:portrait)/(orientation:landscape)」の3ブロックに再構成。横向きは: 左パレット=幅148pxの縦スクロール列（原子ボタンは font-size:0 で記号の丸のみ・和名ラベル非表示）、キャンバス中央、右パネル=右からのドロワー（translateX。既存の body.sheet-open 開閉機構を流用しJS変更なし）。html に text-size-adjust:100% を追加（モバイルの自動文字拡大が指定フォントサイズを 10.5px→14px に壊すのを実測・防止）。PC・縦レイアウトは非干渉を確認。テストR2（向き別メディアクエリのCSSOM検査）— v133
  - [x] **P11-M1** モバイル縦レイアウト — max-width:899px のメディアクエリで、main を縦積みに: キャンバスを最大化（flex:1、375px幅で高さ618px確保）、左パレットを最下部の横スクロールツールバー（flex-direction:row）、右パネルをposition:fixed の下シート（transform:translateY で開閉、body.sheet-open で表示）。開閉トグル（浮動ボタン）・閉じるボタン・バックドロップを追加（.mobile-only、PCでは display:none）。100dvhでアドレスバー追従、モーダルはmax-height:90dvh。PC（≥900px）は完全非干渉を確認。テストR1 — v131
  - [x] **P11-M1b** モバイル下ツールバーの是正（実機フィードバック）— 各グループのボタンが折り返して縦に伸び、バー高132pxで下段が隠れていた → 各パレット（操作モード/結合/原子/モジュール）を横1列（flex-wrap:nowrap）にし、バーは横スクロールで到達。高さ82pxに圧縮しキャンバスを拡大。SO₃H追加で9モジュールでも縦の切れなし — v132PCは非干渉（メディアクエリ内に隔離）

- [x] **P9-5e** 夜間監査のフォロー（2026-07-30・v262 と v264 で(1)(2)とも修正）— ユーザーの夜間実行4回分（v232・合計 ok 136,254 / fail 3,072）を解析。**自動水素の重なり 約1008件**（Br付近481・C付近206・O付近202・Cl付近74・N付近45）と**原子の重なり 約530件**。**出荷ライブラリの419記録には1件も無く**（`verify-compounds` の警告7件はすべて H–H の距離で別原因）、全件が乱操作後の状態
  - (1) **自動水素12px級を修正**: 向きの選び方（候補角の除外）だけでは足りない。非結合原子が配置の許容下限 MIN_CLEARANCE 27.3px ぎりぎりの28pxに**合法に**置かれると、そちらを向いた水素は 28−16 = 12px まで寄る。**向きを変えられないときは長さで逃がす**方式にした（16px→最短9pxまで1pxずつ縮め、非結合重原子から13px以上あく長さを採る）。重原子の座標は動かさないので判定・反応・エクスポートに影響しない
  - 測り方で一度つまずいた。2原子だけの合成ケースでは**修正前も12px未満が0件**（最接近12.0px）で再現できなかった。監査の条件は「**複雑な分子の隣に合法な位置で原子が置かれる**」ことだったので、ライブラリ185件×アンカー×8方向で当てたところ再現した: **12072通りの合法配置で 105件 → 0件**、最接近 8px → 12.5px。テスト R14
  - (2) **原子どうしの接近も修正**（v264）— 原因は方針書の見立て（reactor の配置クリアランス）ではなく、`getSnappedCoords` の**ベンゼン環分岐**だった。この分岐はコメントに「既存動作を完全維持」とあり、**8pxの占有判定だけで可否を返して早期 return** していたため、他の経路が守っている MIN_CLEARANCE（GRID_SIZE*0.65 = 27.3px）を通っていなかった。環の近くに別の環があると置換基が非結合原子の12〜23pxまで寄って置ける
    - 特定の手順: 監査の `ops` には「どの結合を切ったか」が記録されておらず**厳密な再生はできない**（シード再生は audit.js の IIFE 内で外から呼べない）。そこで実ポインタイベントを投げる簡易ファズを書き、**1手ごとに最接近を測って原因の操作を絞った**。`place` に絞れたあと、スナップ結果を出力して `Br(455,313)` が既存 `C(468,315)` から 12.9px・`isValid=true` を確認
    - 途中で2回、**自作の再生器の誤り**を実バグと勘違いした。(a) `coords.adjust`（側鎖の振り分け）を適用していなかった (b) 環炭素の当たり判定の内側をタップしていて元素置換になっていた。実イベントを投げる形に直してから判断した
    - 効果: 2系列×60反復×50手で違反 0件（置けた原子は216/218個で過剰な拒否なし）。単独ベンゼンの6箇所すべてに置換基を置けることも確認（無回帰）。テスト R15

### フェーズ12: コンテンツ拡充と反応の可視化（2026-07-24 ユーザー要望登録）

- [x] **P12-1** 異性体の書き出し練習モード（M1〜M3 実装完了・v146）— 設計書: [DESIGN_isomer_practice.md](DESIGN_isomer_practice.md)（2026-07-24 作成、同日ユーザーレビュー反映済み: ①登録構造は必ず命名して表示（重複時は「◯番『名称』と同じ」）②未発見構造から系統的にヒント生成（系列内訳→書き出し手順の定型文「エチル基/メチル×2同一C/メチル×2別C」→答え）③答え合わせは系統順の一覧（isomerSeriesKey でソート）④非正準な書き方の検出時は主鎖をハイライト＋標準レイアウト並置（findLongestCarbonChain）。**実装は Opus で行う**（M1から順に。設計書に実装手順・テスト項目記載済み）。学習モードの一機能として、分子式を提示→構造異性体を1つずつ描いて登録→重複は正準コードで検知し該当サムネイルをハイライト（表記の同値性の実践）→全種そろえて完了。問題6問（C₄H₁₀ 2種〜C₄H₁₀O 7種、異性体数は列挙エンジンから計算し二重管理しない）。新規アルゴリズム不要（列挙・正準コード・ギャラリー描画を流用）。M1: コアループ / M2: 段階ヒント・完了演出 / M3: 任意分子式（P12-2連動）
  - [x] **P12-1-M1** コアループ — learn.js に IsomerPractice クラスを追加（学習パネルに「✏️ 異性体の書き出し練習」カード）。問題6問（C₄H₁₀〜C₄H₁₀O。異性体数は列挙エンジンから算出）。登録/重複/分子式・単一分子チェックを canonicalCode で判定し、名称付きサムネイルをトレイに表示。全種そろえて完了→クリア記録（localStorage: chemIsomerPractice.<分子式>）＋系統順（主鎖の長い順・カテゴリ順）の答え合わせ一覧＋分類まとめ。ヒントは残り数のみ（段階ヒントは M2）。登録時はキャンバスを saveState してから白紙化（↩で復帰）。学習モード離脱で練習を強制終了（setMode フック）。**C₆H₁₄ は既定の列挙上限60万ノードを超えるため練習では上限400万で列挙**（設計の「高速ケース」想定を実測で更新。6問で計~170ms・一度だけキャッシュ）。compounds.json に不足していた7異性体名（エチルメチルエーテル・1-ブテン・2-メチルプロペン・メチルシクロプロパン・メチルプロピルエーテル・イソプロピルメチルエーテル・2-メチル-1-プロパノール）を追補し、6問25異性体すべてが名称解決することをテストIP4で機械検証。テスト IP1（2種登録して完了・名称・クリア記録）/ IP2（重複拒否＋点滅）/ IP3（分子式違い・複数分子の拒否）/ IP4（25種の命名網羅＋列挙数の既知値一致）。全80テスト合格 — v145
  - [x] **P12-1-M2** 学習性の強化 — chemistry.js に純粋関数を追加: `findLongestCarbonChain`（最長炭素鎖を原子ID列で返す）・`isomerSeriesKey`（官能基カテゴリ→主鎖長→側鎖構成→gem/位置番号の系統キー。表示・ヒント専用で検証には使わない）。(1) 段階ヒント（💡を押すたびに 1=未発見の系列内訳／2=書き出し手順の定型文［位置ずらし・炭素2個分の側鎖=エチル/メチル×2同一C/別C・不飽和と環］／3=答え合わせ一覧を未発見マーク付きで開く）。(2) 答え合わせ一覧を isomerSeriesKey で系統順ソート（分類まとめ併記）。(3) 主鎖／環の教示: 登録・重複時に作図が非正準（まっすぐ描いた炭素列が最長鎖より短い）なら、最長炭素鎖をキャンバス上でオレンジ点線ハイライト（highlightAtoms 流用）＋標準レイアウトのサムネイル並置＋「続ける」でキャンバスを消す導線。環分子は「環が基本骨格」の案内をセッション1回だけ（幾何判定は表示支援のため座標を使う＝設計9章の例外）。(4) 完了バナーの登場演出（ip-pop）。テスト IP5（系統分類の純粋関数）/ IP6（段階ヒント）/ IP7（非正準作図の検出とハイライト・正準時は非表示） — v146
  - [x] **P12-1-M3** 出題の拡張 — 任意の分子式入力での練習（問題選択カードにテキスト入力＋「練習する」）。`parseFormula`（"C4H10O"→重原子リスト＋H数。対応: C・H・O・N・Cl・Br・S。小文字・未対応元素・不連続は拒否）。受理条件: 重原子≤6・列挙が打ち切りにならない・原子価が成立する・**異性体数≤20**（不飽和度の高い式は教科書外の構造を多数含むため上限で断る。例: C₆H₆ は217種で拒否）。名称未登録の異性体は「（名称未登録）」表示（P12-3で系統名化）。テスト IP8（解析・受理・拒否・上限）。全84テスト合格 — v146
  - [x] **P12-1-調整** ユーザーレビュー反映（2026-07-24。答え合わせを「並べて比較」体験に作り替え）— (1) **登録モデルを entries[] に変更**: 重複を弾かず全部保持（比較の材料にする）。M1/M2 の即時重複ブロック・点滅、非正準作図の即時教示（analyzeDrawing/主鎖ハイライト/続ける導線）は撤去。(2) **答え合わせオーバーレイ**（`#ip-review-overlay` をキャンバス領域に重ねる）: ユーザーが「答え合わせ」を押したとき（＝一通り書き終えてから）に開く。あなたの書き出しを番号①②…で並べ、**同じもの同士を色枠＋「①と②は同じ ＝ 名称」で明示**。標準の書き方と答えを系統順（isomerSeriesKey）で並べ、✓発見／未発見を表示。書いた個数・ちがう種類・ダブり・未発見を集計。(3) **標準レイアウトを主鎖横一直線＋主鎖炭素に位置番号**（`ipNumberedLayout`。番号付けは最小位置番号＝IUPAC風。環は layoutMolecule にフォールバック）。(4) 段階ヒントは 1=系列内訳／2=手順の2段階に（答えはヒントに出さず「答え合わせ」で自分で開く）。(5) 右パネルは書いた図の番号付き一覧＋登録／答え合わせ／ヒント／やめる。テスト IP1〜IP3・IP6・IP7 を新モデルに更新（IP2＝重複保持と「①と②は同じ」、IP7＝標準図の横一直線番号）。全84テスト合格 — v147
  - [x] **任意員環（n-ring）の操作改善**（ユーザー指摘: 挙動が分かりにくい）— 旧: 選択→キャンバスをクリック→員数モーダル（プレビュー無しで元クリック位置に配置）。新: **モジュール選択時に員数モーダルを開き、員数を先に決める → 以後は他の環と同じくカーソルにゴーストが追従しクリックで配置**（`this.nringSize` を保持。ゴースト条件から n-ring 除外を解除。キャンセルでモジュール選択も解除）。テスト G3 を新フローに更新 — v147
  - [x] **P12-1-調整2** レビューの見やすさ（ユーザー要望）— (1) 答え合わせ／確認オーバーレイの**図サイズを 小/中/大 で切替**（`IP_REVIEW_SCALES`。列幅とSVG高さを変える）。(2) **書き出しの確認モード**を追加（`openReview('progress')`）: 右パネルの書いた図を**クリックすると、答えを伏せたまま自分の書き出しだけを大きく並べて表示**（進行状況を自分で確認）。「答えを見る」ボタンでそのまま答え合わせへ。答え合わせは従来どおり「答え合わせ」ボタン（`openReview('answer')`）。全84テスト合格 — v148
  - [x] **P12-1-調整3** 命名・同一判定を答え合わせに寄せる＋サムネ再クリックで作図へ（ユーザー要望）— (1) **書き出し中・確認モードでは名前と同一判定を伏せる**（右パネルは番号のみ・ヘッダーから「ちがう種類 N/M」を撤去、登録トーストも枚数のみ、クリア達成の告知もしない）。名前・「①と②は同じ」・ちがう種類/ダブり/未発見の集計は**答え合わせモードでのみ**表示。オーバーレイに「答えを見る（名前・同一判定）⇄ 名前を隠す（確認に戻る）」のモード切替を追加。(2) **サムネ再クリックで作図に戻る**（`toggleReview`。右パネルの図・オーバーレイの自分の図はクリックで作図へ）。テスト IP2 を更新（確認モードは同一判定を伏せ／答え合わせで表示／トグルで復帰）。全84テスト合格 — v149
  - [x] **P12-1-調整4** 答え合わせのモード切替を上部に常設（下部フッターだと発見しにくかった）＋**「描きながら名称を表示」トグル**（ユーザー要望）— (1) レビュー上部に「表示: 確認／答え合わせ」のセグメント切替を常設・現モードをハイライト。(2) 練習カードに任意トグルを追加。ONにすると作図中の分子の分子式＋名称を game.updateDrawing のフック（`isomerPractice.onDrawingChange`）でライブ表示（目標分子式と一致=シアン／不一致・複数分子=橙）。設定は localStorage に保存。既定OFF（書き出し中は名前を伏せる方針のため、補助が欲しい人向けの任意モード）。テスト IP9。全85テスト合格 — v150〜v151
- [ ] **P12-B1（既知不具合・修正実装済み/実機確認待ち）** iPad中心のタッチ操作不具合 — 症状: ①iPadでピンチがページ全体ズームになる ②結合のダブルタップ・消しゴム削除が効かない ③スマホは長押し/ダブルタップで消える（端末依存）④原子タップが結合次数トグルになる ⑤作図できない状態（条件不明）。**Fable が v152 で修正を実装**: iOS gesture 抑止（モーダル除く）＋`touch-action`/`-webkit-touch-callout` 補強、幽霊ポインタの自動復旧（isPrimaryタッチで activePointers/pinch を破棄）、消しゴムの結合タップ削除、タッチ長押し550ms＋自前ダブルタップ削除（`removeBondByGesture` に集約・二重削除保護・伸縮履歴の巻き戻し）、hitbox上の原子優先（半径16px）。テスト I3〜I7 追加・全90件合格。詳細は [BUGNOTE_touch_ipad.md](BUGNOTE_touch_ipad.md)。**残: iPad/Android 実機での確認**（確認できたらチェックを付けて閉じる）
- [ ] **P12-2** 異性体列挙の対応範囲拡張 — 現状は重原子6個まで・不飽和度が高い分子式は打ち切り。列挙アルゴリズムの高速化（正準コードの重複除去の効率化・枝刈り強化）や、環/多重結合を含む分子式への対応範囲を広げる
- [ ] **P12-3** 命名可能化合物の拡張（P8-6の継続）— compounds.json をさらに拡充。系統名（IUPAC）アルゴリズムの検討も。**主要な非環式の系統名（アルカン・アルケン・アルキン・ハロゲン化物・アルコール・エーテル）＋アルキル基名＋アルキル基の書き出し練習は完了（第1〜6弾, v157〜v163）。残りは芳香族・カルボニル系の系統名（iupacName の拡張）**。**⚠ 糖類・アミノ酸の立体異性体（α/β・D/L）は構成が同一で現行モデルでは区別できないため、追加は P12-7（立体化学対応）の決定待ち**
  - [x] **P12-3-化合物拡充3**（Fable・2026-07-31）— compounds.json を 131→142 に拡充（11件・芳香族/カルボニル系・v303）。p-ジクロロベンゼン（防虫剤）／o-・p-ニトロトルエン（トルエンのニトロ化）／安息香酸メチル／ヒドロキノン／アゾベンゼン・p-ヒドロキシアゾベンゼン（v301-302 のジアゾ化・カップリング機構の生成物）／無水マレイン酸／3-ペンタノン／ブタナール／イソ酪酸（酪酸・酢酸エチルと同分子式の構造異性体＝クイズ題材）。座標は既存様式を踏襲（角度付きニトロ基は TNT と同形・酸無水物は五員環半径40）。verify-compounds.js 合格（259件・新規は警告なし）・F8 含む全148テスト合格
  - [x] **P12-3-第6弾**（Opus）— **アルキル基の書き出し練習モード**（ユーザー要望）。異性体練習と同じ流儀で、アルキル基 CnH(2n+1)– を「付け根マーカー R（価標1の擬似元素・v162で追加）を1個付けた分子」として `enumerateConstitutionalIsomers` で列挙・正準コードで一意判定し `iupacAlkylNameFromR` で命名。`AlkylPractice`（learn.js）: 炭素数選択（C3=2/C4=4/C5=8種）→ 付け根の炭素C1とRをロック状態で自動配置（ユーザー提案）→ C1から伸ばして描き登録→答え合わせで自分の書き出しと全アルキル基（名前つき）を並置。異性体練習と相互排他。付け根の保護（ロック原子は消しゴム/右クリック削除不可、R結合はジェスチャ/消しゴム削除不可・価標1で次数トグル不可）。パネル #ak-body・オーバーレイ #ak-review-overlay。**テスト F10**（R価標=1・C1〜C5のアルキル基数1,1,2,4,8・命名一意）・**AK1**（付け根配置とロック・Rが消せない・プロピル/イソプロピル登録と命名・答え合わせ・炭素数不足の拒否）。全99テスト合格 — v162〜v163
  - [x] **P12-3-第5弾**（Opus）— **アルコール・エーテルの IUPAC 系統名**（含酸素官能基へ拡張）。-OH を主特性基として主鎖選択（-OHを最も多く含む鎖優先）・番号付け（-OHが多重結合・置換基より優先で最小位置番号）に反映。接尾辞: 単一=-オール（メタノール〜、C1/C2は位置省略）、複数=ジ/トリオール（1,2-エタンジオール・1,2,3-プロパントリオール）。エーテルは高校流の慣用名「ジアルキル/アルキルアルキルエーテル」（両側のアルキル基をアルファベット順、同一はジ。例: エチルメチルエーテル・ジプロピルエーテル）。対象元素を C/H/ハロゲン/O に拡張し、O は -OH（炭素1個）かエーテル（炭素2個）のみ許可。C=O・不飽和アルコール・OHとエーテル併存・分岐ポリオール等は未対応(null)。環判定を findAnyCycle に統一。**テスト F9 拡張**（アルコール8種＋エーテル2種＋カルボニルnull＋lookupがライブラリ外3-ペンタノールを命名。Nodeでアルカン22件無回帰＋アルコール13例＋エーテル5例）。アルカン〜アルキンの挙動は不変。全97テスト合格 — v161。**残り: アルキル基の書き出し練習モード**
  - [x] **P12-3-第4弾**（Opus）— **アルケン・アルキンの IUPAC 系統名**（不飽和へ拡張）。主鎖選択を「多重結合を最も多く含む鎖→最長」に変更（不飽和が主鎖に入る）。番号付けは 多重結合の位置番号を最小（置換基より優先）→二重結合を優先→置換基位置最小。接尾辞: 二重結合=-エン（専用幹ブテン等）、三重結合=-イン（ブチン等）、複数二重結合=倍数接頭辞＋エン（1,3-ブタジエン）。C2/C3は位置番号を省略（エテン・プロペン・エチン・プロピン）。幹が数字始まり（2-ブテン等）のとき置換基部との間にハイフンを補う。全多重結合を1本の鎖に収められない分岐ジエン・エンイン混在は null。**テスト F9 拡張**（エテン〜1,3-ブタジエン・2-ブチン、二重結合が置換基より小番号の3-メチル-1-ブテン、ハロアルケン4-クロロ-1-ブテン。アルカン22件＋アルケン/アルキン17例の機械検証）。アルカン・ハロアルカンの挙動は不変。全97テスト合格 — v160。**残り: アルコール/エーテル等の官能基、アルキル基の書き出し練習モード**
  - [x] **P12-3-第3弾**（Opus）— **ハロゲン化アルキルの IUPAC 系統名**（ユーザー要望「ハロゲン化アルキルなども対応したい」）。`iupacName` の対応範囲を「炭素・水素・ハロゲン（F/Cl/Br/I）のみ・単結合・非環式・炭素骨格は木」へ拡張。ハロゲンは主鎖に入らず炭素に1本の単結合で付く末端置換基として、クロロ/ブロモ/フルオロ/ヨードの接頭辞で命名。アルキル基と同じ土俵でアルファベット順（ブロモ<クロロ<エチル<メチル）・倍数接頭辞・最小位置番号を適用（例: 1-ブロモ-2-クロロエタン、2-クロロ-3-メチルブタン、2-ブロモ-2-メチルプロパン）。置換基収集を `_iupacCollectSubs` に共通化（主鎖・アルキル置換基の両方で炭素枝＋ハロゲンを再帰収集）。メタン誘導体（主鎖炭素1個）は位置番号を省略（クロロメタン・テトラクロロメタン）。アルカンの挙動は不変。**テスト F9 拡張**（ハロゲン化アルキル7種＋lookupがライブラリ外の2-クロロブタンを命名／アルカン22件の無回帰も継続）。全97テスト合格 — v159。**残り: アルケン/アルキン・アルコール/エーテル、アルキル基の書き出し練習モード**
  - [x] **P12-3-第2弾**（Opus）— **非環式アルカンの IUPAC 系統名＋アルキル基名**（ユーザー要望「異性体の書き出しでIUPAC系統名がほしい・アルキル基名もほしい」）。chemistry.js に `iupacName(mol)`（対応: 炭素と水素のみ・単結合・非環式の飽和炭化水素）を新設: 全最長鎖を主鎖候補に列挙→(a)置換基数最大 (b)位置番号セット最小（両方向でfirst point of difference比較）(c)アルファベット最先の置換基に小さい番号、で主鎖・番号付けを決定。倍数接頭辞（ジ/トリ）＋アルファベット順（エチル<メチル）で連結。`iupacAlkylName`/`iupacAlkylGroupName` はアルキル基を再帰命名し体系名を慣用名（イソプロピル・イソ/sec-/tert-ブチル・イソ/ネオペンチル）へ写像。`lookupCompoundName` はライブラリ照合が外れたとき `iupacName` にフォールバック（自由モードの名称表示・異性体練習・反応生成物がアルカンで系統名に。クイズは影響なし）。対応外（環・不飽和・ヘテロ原子・複数分子）は null。**テスト F9**（ライブラリ全アルカン＝C4〜C7の完全異性体集合が既知正解名に一致／混在置換基のアルファベット順・多重位置番号／アルキル基名8種／対応外null／lookupがライブラリ外オクタンを命名）。全97テスト合格 — v158。**残り: 第3弾（アルケン/アルキン・アルコール/エーテルへ拡張）、アルキル基の書き出し練習モード**
  - [x] **P12-3-化合物拡充2**（Opus）— compounds.json を 76→87 に拡充（11件・芳香族/環状）。単置換ベンゼン（アニソール・アセトフェノン・ベンゾニトリル・フェニルアセチレン）／p-ジニトロベンゼン／脂環（シクロヘキサノン・シクロヘキサノール・シクロヘキセン・1,3-シクロヘキサジエン・シクロペンタノン・シクロペンテン）。座標は既存様式（芳香環＝正六角形・半径40・左右頂点、脂環＝上下頂点）に合わせ、Nodeの機械検証（価標・正準コード＋幾何の衝突なし・自動水素込みの非重なり）を通して投入。全99テスト合格（A3・F8含む）— v164
  - [x] **P12-3-第1弾**（Opus）— compounds.json を 61→76 に拡充（15件）。エステル（酢酸メチル・プロピオン酸メチル・酢酸プロピル・酪酸エチル・酢酸イソアミル）／カルボン酸（酪酸・吉草酸・マロン酸・コハク酸・グルタル酸）／アミン（メチル・ジメチル・トリメチル・エチルアミン）／アセトアミド。座標は `layoutMolecule` でグリッド整列（作図密度は既存中央値16pxと同等）。Nodeで chemistry.js を読む機械検証（価標妥当性・正準コードの既存衝突なし・原子非重なり）を通してから投入。一部は既存と同一分子式の構造異性体で「同じ化合物？」クイズの題材になる。**テスト F8 新設**（名称ライブラリ全132件が自己命名でき・同一構造の別名なし＝命名の一意性を機械検証）。全96テスト合格 — v157
- [x] **P12-4 の下準備: reactions.json の機械検証ツール**（Opus・2026-07-27）— `tools/verify-reactions.js`。`verify-compounds.js` と同じ立ち位置で、機構データを投入する前に「再生できない・原子が消えている」事故を止める。検査は ①形式と id の一意性 ②各状態が分子として成立（結合の添字・自己結合・重複結合・**電荷を考慮した価標**。カルボカチオン C⁺ は3本、オキソニウム O⁺ は3本まで） ③**状態間で元素ごとの原子数が保存** ④**電荷の総和が全状態で一定** ⑤巻矢印が実在する原子・結合を指す ⑥孤立した状態が無い ⑦原子が近すぎない ⑧**reactor.js の mechanismId がすべて実在**（「機構を見る」ボタンの死にリンク検出）。既存9件は合格。**13種類の故意の壊し方（原子を消す・電荷を変える・存在しない結合を指す・id 重複・死にリンク等）をすべて検出することを確認済み**（検証ツール自身の検証）
- [x] **公開前のリリース検査ツール**（Opus・2026-07-28）— `tools/verify-release.js`。コミット前の儀式のうち**人の記憶だけが安全装置だった2つ**——`?v=` の一括更新と文字化けの目視確認——を機械化する。どちらも忘れてもテストは落ちず、そのまま「古い JS がキャッシュから配られる」「化けたまま公開される」に直結する。検査は ①アプリ内の `?v=` が全ファイルで同じ番号 ②`<div class="version">` の表示が一致 ③`?v=` を付けて読み込むファイルが実在（改名・削除の取り残し） ④ローカル js/css/json に `?v=` の付け忘れが無い（新規ファイルで起きやすく、そのファイルだけ永久にキャッシュされる） ⑤**変更があるのに版を上げていない**（git の HEAD と比較。本命） ⑥文字化け ⑦BOM。アプリは「`?v=` を含む html があるディレクトリ」として自動認識するので assembler / ion-equation / ratio を一度に見る（ルートのハブは自己完結につき対象外）。**8種類の故意の壊し方をすべて検出し、対照（説明目的で化け文字を引用している行）を誤検出しないことを隔離した箱庭で確認済み**。化けの判定は「2文字以上の連続」＋ドキュメントはバッククォート内を除外。文字集合は `\u` エスケープで書く（そのまま書くとこのファイル自身が検出対象になり必ず不合格になる。箱庭検証で実際に起きた）
- [x] **高校定番で欠けていた化合物9件の追加**（データ作成は Gemini に委託・qa/gemini-pack-compounds-gap.md／検収 Opus・2026-07-29・v235）— α-アミノ酸4件（セリン・リシン・バリン・ロイシン）／1-ナフトール・2-ナフトール・クロロプレン／パルミチン酸・ステアリン酸。176→185件。**アミノ酸と油脂の単元に穴があった**（脂肪酸は吉草酸C5が最長で、油脂で使うC16/C18が1件も無かった）
  - 依頼時に**名指しで禁止した**もの: システイン・メチオニン（このアプリの硫黄は6価固定＝スルホ基用で、**チオール -SH が表現できず価標検査で落ちる**）／トリプトファン（縮合複素環）／オレイン酸（シス二重結合の指定が別の仕組みに絡む）。教科書に載っているぶん Gemini が足しにきやすいので先に塞いだ
  - 前回（エーテル機構）でファイル全体を再整形されて差分5105行になった反省から、依頼文に「**再整形しない・既存行を1文字も変えない・追記した行だけが差分に出る状態にする**」を明記。結果は**1055行の純追加・既存の変更と削除はゼロ**で、意図どおりになった
  - 検収で見つかった不具合: `summonMolecule`（名称呼び出し）が `fitCanvasToTarget()`＝**お題**に視野を合わせていたため、既定の視野（360px幅）より大きい分子は画面外に出ていた。**ステアリン酸は幅756pxで既定の2倍以上**。`fitCanvasToMolecule(mol)` に切り出し、呼び出し時は**呼んだ結果のキャンバス**に合わせるよう修正（`fitCanvasToTarget` は委譲する形にしてパズルモードの挙動は不変）。テスト R9
  - 併せて、v228 で追加したテストの ID が既存の R4 と衝突していたので R8 に付け替えた
- [x] **クイズの変形で「原子が結合線に乗る」図を作らないようにする**（Gemini のレビュー docs/development_plan.md 項目15／調査・修正 Opus・2026-07-29・v238）— ユーザー報告「グリシンの比較問題で、カルボキシ基の O が中心炭素に結合している異常な図が出た」。**トポロジーは壊れていなかった**（400回試して正準コードの変化ゼロ）。原因は幾何で、`transformCompoundDepiction` の受理判定が**原子どうしの距離しか見ておらず、結合線との距離を見ていなかった**。伸長で結合が2〜3マスに伸びると、その線の途中に無関係な原子が乗る。実測でグリシンは**500回中192回、距離0.0px＝線の真上**に載っていた
  - 修正: 伸長と屈曲で別々だった受理判定を `isReadableLayout` に統合し、原子と結合線分の距離も見る。合格ラインは**絶対値ではなく「元の図と同じ読みやすさを保つ」**（`gapFloor = min(GRID_SIZE*0.65, 元の図のいちばん詰まった隙間)`）。一律 27.3px を要求すると、**ハース環のテンプレートが元から26pxの隙間を持つ**ため（185件中7件）糖の問題だけ変形の選択肢が激減する（実測でβ-D-ガラクトースが 6通りまで落ちた）。相対判定にして 20通り以上に回復
  - 検証: ライブラリ全185件×20回＝3700回の変形で、線に乗る原子ゼロ・トポロジー破壊ゼロ・平均バリエーション17.4通り（修正前と同等）。テストは既存の屈曲テストに追記（グリシン・アラニン・乳酸・セリン・β-D-グルコースの5件×40回＋糖の変形数の下限）
  - **Gemini の分析は外していた**（「結合の接続先インデックスが誤って書き換わった可能性」＝トポロジー破壊を疑っていた）。症状の報告は正確だったが、原因は座標だけの問題だった
- [x] **クイズの変形が図の立体を変えないようにする**（Gemini のレビュー docs/development_plan.md 項目13／調査・修正 Opus・2026-07-29・v242）— ユーザー報告「α-D-マンノース同士の比較で、一方が90°回転して枝の上下が逆になった図が『同じ化合物』と誤判定された」。原因は `transformCompoundDepiction` の第1段階にある**90°回転（1〜3回）＋50%で左右反転**。コード中のコメントは「剛体変換なのでシス/トランスも保存される」としていたが、**シス/トランスは保存されてもフィッシャーとハースは保存されない**（どちらも画面上の絶対的な上下・左右で読む規約のため）。実測でライブラリ185件中**29件**が回転で別の立体異性体の図になっていた（α-D-マンノースは30回中24回）
  - 修正: 立体クイズと同じ原則にした＝**変形のたびに図から立体を読み直し、変わっていない配置だけを採用する**（生成側の意図を信用しない）。剛体変換は8通り（4回転×鏡像有無）を候補として立体が保存されるものだけを選び、伸長・屈曲の受理判定にも同じ条件を足した。フィッシャー分子で保存されるのは t=0 / t=180° / t=90°+鏡 / t=270°+鏡 の4通り＝**偶置換の集合と一致**し、ハースのピラノースでは t=0 / t=180°+鏡 の2通りだけになる
  - 途中で2回間違えた。(1) 判定用の分子に渡す配列が座標だけ（`{x, y}`）で **element が無く**、読み取りが常に null になって**全候補を弾き**、立体を持つ33件が1通りしか出ない状態になった。(2) `haworthFace` を復元し忘れ、環の立体が読めず**実際の描画より甘い判定**になって75件すり抜けた（`createTargetFromData` は復元している）。どちらも「変化ゼロ」と「バリエーション維持」を**両方**測っていたから気づけた
  - 検証: ライブラリ全185件×20回＝3700回で、立体の変化ゼロ・トポロジー破壊ゼロ・平均バリエーション17.4通り（修正前と同等。立体を持つ33件は平均18.8通り・最小13通り）。テスト ST19
- [x] **環炭素に2本目の側鎖を付けるときの吸着範囲を広げる**（Gemini のレビュー docs/development_plan.md 項目2／調査・修正 Opus・2026-07-29・v244）— ユーザー指摘「メチルシクロヘキサン等の同じC原子に2つ目のメチル基を結合する際、吸着判定が狭い」。**2本目を置くロジック自体は実装済み**（二等分線±30°／P6-3）で、問題は `getSnappedCoords` の**吸着先の選び方**だった。距離を「原子そのものまで」だけで測っていたため、2本目が出る位置は環炭素より既存の側鎖に近く、素直にドラッグすると側鎖に吸着してしまう
  - 実測（メチルシクロヘキサン・環炭素(400,258)・メチル(400,216)）: 2本目の出現位置は (379,222)/(421,222) なのに、**環炭素の吸着範囲は y≥238 までしか届いていなかった**。その位置ではメチルまで21.8px・環炭素まで41.7pxで、メチルが勝つ
  - 修正: 吸着の距離を「原子まで」と「**そこを選んだら新しい原子が出る位置まで**」の小さいほうにする。`secondBranchPoints(atom)` を新設し、条件は `getSnappedCoords` の「側鎖2本目」分岐と**同じにそろえた**（環結合2本＋側鎖1本・空き価標あり・ハース環と格子上の環は候補角が別なので対象外）。該当しない原子では空配列を返すので他の作図に影響しない
  - 効果（同条件の実測）: 環炭素の吸着範囲は 565→2041 サンプル点に拡大し、2本目の出現位置そのもので環炭素に吸着するようになった。**側鎖の真上は側鎖に吸着したまま**なので、メチル→エチルと鎖を伸ばす操作は壊れていない。通しで 1,1-ジメチルシクロヘキサンが作れることを確認。テスト R10
- [x] **環ビューの回転軸を環の面に垂直にする**（Gemini のレビュー docs/development_plan.md 項目3／調査・修正 Opus・2026-07-29・v245）— ユーザー指摘「現在の自動回転/ドラッグ回転が意図した挙動になっていない」。環モデルは `buildRingModel` で **z=0 平面**に組んである（環原子 `v=[x-cx, y-cy, 0]`、環外置換基が z=±depth）＝**環の法線は z 軸**。面内で回す（メリーゴーランド）には z 軸まわりに回す必要があるが、`rotateYX` で**画面の縦軸 y** まわりに回していた。この軸は環の平面**内**にあるため、回すと環が裏返る
  - 実測（真横＝倒し角90°で環原子の画面上の縦の広がり。0なら横一線を保てている）: 旧 `rotateYX` は yaw30°で100px・60°で173px・**90°で200px＝真横が完全に崩れて正面向き**になっていた。新 `rotateZX` は 0°〜150° すべてで 0.0px を保ち、奥行き z だけが変わる＝奥にあった原子が手前に出てくる
  - ボタンの説明文が最初から「奥にあった原子が手前に出てきます」と書いてあり、**意図と実装がずれていた**ことがそこからも確かめられた。テスト ST20
- [x] **環ビューの対応範囲を飽和単環に絞り、解説文を化合物に合わせる**（Gemini のレビュー docs/development_plan.md 項目4／調査・修正 Opus・2026-07-29・v247）— ユーザー指摘「糖以外の環（シクロブタン等）でも有効になっているが、枝の方向が反映されていない」。実測でベンゼン・ナフタレン・シクロヘキセン・1,3-シクロヘキサジエン・フェノールでもタブが有効になっていた
  - 対応範囲: このビューは「環1つを平面とみなし、置換基が上下に突き出す」模型なので、平面近似が成り立たない環は対象外にした。**縮合環/多環**（隣の環の原子が「置換基」扱いになり上下の意味が決まらない。環の数は連結成分の閉路数 辺−頂点+1 で数える）と**環内に多重結合**（ベンゼン環のような平面の環では置換基が上下に出ない）。無効化の理由は場合ごとに出し分ける
  - **枝の向きを斜めの結合から推測する案は採らなかった**（Gemini の提案とはここだけ違う）。ハース図の上下は作図規約による立体の主張だが、シクロヘキサンの外向きに描いたメチルは**立体を何も主張していない**。「環の中心より上だから上の面」と読むと、ユーザーが描いていない立体を作ることになり、P12-7 M3 で決めた「**読めない描き方では断定しない**」に反する。面マーク（⬍）は糖以外の環でも使えるので、指定したい人は指定できる
  - 解説文: 環に酸素を含む（＝ピラノース環）ときだけ「ハース図」「α/β」の語を使い、それ以外は「環炭素の真上・真下に描いたか」と言い換える。上下が1つも決まっていない図では「**斜めに描かれた結合からは上下を決められないので、勝手には決めません**」と明示して面マークへ誘導する。テスト ST21
- [x] **C=C まわりの平面性とシス/トランスの3D保存を検証してテストで固定**（Gemini のレビュー docs/development_plan.md 項目8／調査 Opus・2026-07-30・v265）— 指摘は「二重結合が複数ある場合も、3Dモデル上で平面性が保たれているか検証する必要がある」。**調べたら既に正しかった**ので、コードは変えずにテストで留めた（黙って壊れないようにするのが目的）
  - 実測: C=C を持つ**61化合物すべてで平面からのずれ 0.000**（結合長1.0が基準）。ベンゼン3本・ナフタレン5本・共役ジエン（1,3-ブタジエン／イソプレン／クロロプレン／1,3-シクロヘキサジエン）2本を含む。作図座標をそのまま持ち上げず**トポロジーと立体記述子から組み直している**ので、二重結合の本数に関係なく平面になる
  - シス/トランスも3Dで図どおり（4/4一致）。幾何が読めない「2-ブテン」「ブテン二酸」は3Dを断り、整形ツールへ案内する（P12-7 M3 の「読めない描き方では断定しない」どおり）。テスト ST23
  - 後半の提案（ジグザグ方向を示す補助線での可視化）は**採らなかった**。3D模型が既に正しい形を示しており、解説文でも「描いた図から読んだものと一致します（回しても入れ替わりません）」と述べている。補助線は情報を足さずに図を混雑させるだけと判断
- [x] **枝を1原子ずつ辿って「どこで食い違うか」を示す**（Gemini のレビュー docs/development_plan.md 項目9／実装 Opus・2026-07-30・v268）— ユーザー要望「環の構成炭素が不斉のとき、環の右回りと左回りは分子式で表すと同じに見えるため、不斉である理由がわかりにくい。原子を1つずつ辿って比較したい」
  - **CIP を実装しない方針と両立させた**。提案には「CIP規則に沿った比較アニメーション」とあったが、順位づけ（原子番号による優先順位・重複原子の扱い）には踏み込まず、**中心から数えた階層ごとの組成を並べて、最初に食い違う層を指すだけ**にした。画面にも「これは順位づけ（R/S を決める規則）ではなく、どこで違うかを辿って示したもの」と明記する
  - 追加した純関数（DOM非依存・テスト可能）: `branchShells(mol, rootId, excludeId)`＝中心を通らずに辿った各層の元素組成／`firstDifferingShell(a, b)`＝最初に食い違う層。既存の `rootedFragmentCode` は正準コード（不透明な文字列）で「1原子ずつ辿る」表示には向かないため、別に用意した
  - 実測（3-メチルシクロヘキサン-1-オールの C1）: 環の右回りは `1:C 2:C 3:C,C 4:C 5:C`、左回りは `1:C 2:C 3:C 4:C 5:C,C` で、**第3層で初めて違う**。辿れる層の数は同じ（＝分子式では同じに見える）ことも確認。対称なシクロヘキサノールでは違いが出ず不斉でもない。テスト ST24
  - 立体モーダルのくさび図ペインに「🔎 枝を辿って比べる」を追加（開閉式）。中心を選び直すと内容も作り直す
- [x] **環ビューの手前側を太線に／「水」のシリーズを直す**（Gemini のレビュー docs/development_plan.md 項目11・23／実装 Opus・2026-07-30・v270）
  - 項目11: ハース投影の慣習にならい、環ビューで**手前側の環結合を1.7倍の太さ**にした。手前かどうかは 3D モデルの z で決めるので、環を回しても正しく入れ替わる。**倒し角0°（ハース図の向き）では環が z=0 平面にあるため差が出ない**のが正しい挙動で、実測でも 0°=手前0本、45°/90°=手前3本（太さ 3.3→6.0）になった。キャンバス側の作図には既に手前太線が入っていた（`_haworthFrontBondKeys`）ので、環ビューだけが未対応だった
  - 項目23: 無機物の「水」が「有名な慣用名（**脂肪族**）」に入っていたので「はじめに（操作の練習）」に移した。並び順は変えていない（今回はシリーズ名だけの修正で足りるため）。シリーズ選択にも新しい項目が出ることを実機で確認。テスト ST25
  - **訂正**: このとき「クリア記録をステージ索引で持っているため並べ替えは記録をずらす」と書いたが**誤り**。`markStageCleared(name)` が示すとおり記録は**ステージ名**で保存されており（localStorage の `chemAssembler.cleared`）、**並べ替えも挿入も安全**。ラインナップの組み替えに制約はない
  - 項目23の後半（パズルのラインナップ全体の再検討）は [DESIGN_puzzle_lineup.md](DESIGN_puzzle_lineup.md) にドラフトを作成（2026-07-30・**未適用**）。分け方の原則は「簡単／複雑」ではなく**何のために描くか**——名前で覚える化合物と、命名規則を当てる練習台（枝分かれアルカン）を分ける。教科書定番の**54件は compounds.json に作図済み**でステージ化するだけ、新規作図はオクタン・ノナン・デカンの3件のみ。規模・重複・粒度の4点をユーザーに相談中
- [x] **パズルのラインナップを教科書基準で再構成**（ユーザー承認・2026-07-30・v273）— [DESIGN_puzzle_lineup.md](DESIGN_puzzle_lineup.md) の案を適用。**56→115ステージ / 5→19シリーズ**
  - 分け方の原則は「簡単／複雑」ではなく**何のために描くか**。枝分かれアルカン15件を「異性体をつくる」として切り出した（2,2,3-トリメチルブタンは教科書に名前で登場せず「C₇H₁₆ の異性体は9種類」の文脈で出る＝覚える対象ではなく規則を適用する対象）。芳香族は教科書の章立てに合わせ「ベンゼンとその同族体／置換反応でできるもの／フェノール類／芳香族カルボン酸／芳香族の窒素化合物／そのほか」に割った
  - 補った欠落: **メタン**（アルカンの起点）・**ベンゼン**（芳香族の起点）。新規作図はオクタン・ノナン・デカンの3件のみ（他54件は compounds.json に作図済みだった）
  - 重複配置を許した（塩化ビニル・イソプレン・エチレングリコール・テレフタル酸・エチレンを「高分子の単量体」にも置く）。クリア記録は**ステージ名**で持つので記録は1つで済む
  - **シス/トランスはパズルにできない**と判明したため案から外した。`verifyMolecule` はトポロジーのみを見るので、トランス体を描いてもシスのお題が合格する（立体コードは区別できているので情報はある）。ステージ側の立体判定はオプトインの別課題として残す
  - 適用時にテストが3件落ち、いずれも**実在する衝突**だった: (1) 命名ライブラリが `STAGES` から `stereo` を落としていたため、立体未指定の糖が糖名に一致した（ST3）→ ステージの stereo も渡すよう修正 (2) 同じ化合物が stages と compounds の両方に載って「同一構造に複数の名前」の検査が同じ名前を2つ数えた（F8）→ 名前・構造・立体が同じ重複を畳むよう修正 (3) テストが旧シリーズ名を直書きしていた（F6）→ 選択肢から実在するシリーズを拾う形に直し、名前→シリーズの逆引きもやめた（重複配置があると後に読んだ側が返る）
  - 検証: 19シリーズすべてでステージが出て、**115件すべてがお題どおり描けば合格する**ことを実機で確認（自己判定の失敗なし）。全141テスト合格
- [x] **重合反応**（ユーザー要望・2026-07-30・v276）— 単量体11ステージを揃えた直後の実装。**付加重合は本物の反応として、縮合重合は説明として**入れた。この非対称には理由がある
  - **付加重合**（`addition_polymerization`）: 同じ単量体2分子のときだけ検出し、二重結合を開いて頭-尾でつなぎ、**両端に R を付ける**。R は価標1の擬似元素で、アルキル基練習でも使っている既存の表記。これで「この先も同じ単位が続く」を表す（教科書の −[ ]ₙ− の代わり）。実測で塩化ビニル2分子が **R−CH₂−CHCl−CH₂−CHCl−R**（PVC の頭-尾の並び）になり、二重結合が残らず価標も正常
    - **R を使う理由**: 2分子のエチレンをただ繋ぐと `•CH₂CH₂CH₂CH₂•` の二重ラジカルになり、このアプリの価標モデルで**閉じた分子として成立しない**。水素で埋めるとブタンになってしまい化学的に嘘になる。R で端を止めるのが唯一の正直な表し方
    - 共重合（別種の単量体）は高校範囲外なので、正準コードが一致する2分子だけを対象にする。環内・芳香族の C=C は除外
  - **縮合重合**（`condensation_polymer_info`）: 2価カルボン酸＋2価アルコール／2価アミンが揃ったときに説明を出す info ルール。**実際の連結は既存の「エステル化」「アセチル化」で1段ずつできる**ので、重複するルールを作らずに「これは縮合重合だ」と教えることに絞った。1価どうし（酢酸＋エタノール）では出ない
  - テスト RX13。字幕から markdown の `**` を除いた（プレーンテキストのトーストにそのまま出るため。以前も踏んだ）
- [x] **監査の結果ファイルに実行条件を記録する**（ユーザー指摘・2026-07-30・v277）— **比較できない形で記録していたことが問題の本質**だった。v262/v264 の効果を測ろうとして v232 と v273 の失敗率を並べたが、**1反復の操作数が 105→100 に変わっていた**（記録が無いので `ops` 配列の長さから後で気づいた）。7%短い実行なら失敗は減るので、原子の重なりの「1.2分の1に改善」は撤回した
  - 記録に追加: `config`（`opsCount` / `iterations` / モードの有無 / **しきい値**）、`counts.fuzzIterations` と `libraryChecks`（率の分母。従来は `ok` から推測するしかなく、**ライブラリを増やしただけで率が下がって見えた**）、`summary`（ファズ1反復あたりの%・種類別件数・最悪距離を**一度だけ数えて書き出す**）、`stopped`（途中停止は完走と比べない）
  - `summary.comparableKey`（例 `ops=105/thr=24,12`）を入れた。**これが一致する実行だけ並べてよい**とファイル自身が示す
  - しきい値は `THRESHOLDS` 1か所に集め、検査コードもそこを参照する。記録される値と実際に使う値がずれない（`REGISTERED_NAMES` と同じ考え方）
  - 効果測定の現状: **自動水素は 1.91%→0.43%（4.4分の1）で、操作数7%の差では説明できない大きさ＝修正の効果**と判断できる。**原子の重なりは判定できない**（1.38%→1.15% は操作数の差で説明がつく範囲）。v264 で直したベンゼン環のクリアランス欠落はコードと実測で確かに実在するバグだったが、失敗率への寄与は未確認
  - 追跡で分かった副産物: `window.auditRerun(seed, ops)` によるシード再生の仕組みが既にあった。ただし**ブラウザのタブが隠れていると rAF が発火せず、反応のモーフィング待ちでファズが止まる**（アプリの不具合ではなく実行環境の制約。表示された状態では6003反復完走している）
- [x] **付加重合を「並べた分だけまとめて」に拡張**（ユーザー要望・2026-07-30・v279）— 「横一列に単量体を並べた状態から重合するところを拡大・縮小しながら見られるとよい」。2分子限定をやめ、**同じ単量体が2つ以上あれば並んでいる全部を一度に繋ぐ**ようにした。左から右へ x 順に繋ぐので、画面の並びと繋がる順が一致する
  - 段階的な重合（2つ繋いでから3つ目）は**できない**。R で端を止めた生成物には C=C が残らないため。並べた数をまとめて繋ぐのが、この表し方での唯一の形
  - 1分子に C=C が2本以上あるもの（1,3-ブタジエン等）は**除外**した。どちらを開くかが一意に決まらないため（1,4-付加重合は高校では扱うが、開く位置の選択をUIで問う設計が要る）
  - 反応結果に `refit` を足し、**伸びた鎖の全体が見えるように視野を合わせる**（`fitCanvasToMolecule`）。ルールが返したときだけ効くので他の反応の見え方は変えない。実測: 塩化ビニル6個で −[CH₂−CHCl]₆−（幅294px）が生成され、視野が632pxに広がって全体が収まる
  - 字幕で「ホイールやピンチで拡大すると繋がり目を1つずつ確かめられます」と案内する（拡大縮小の操作は v249 で操作説明にも載せてある）。テスト RX13 を拡張
- [x] **1,4-付加重合（合成ゴム）**（ユーザー要望・2026-07-30・v281）— v279 で除外した共役ジエンに対応。要点は**二重結合が両端から中央へ移る**こと（C1=C2−C3=C4 → C1−C2=C3−C4 で、C1 と C4 が繋がる）。これがゴムに二重結合が残る理由で、加硫（硫黄による架橋）の土台になる
  - 対象は `conjugatedDienes()` が返す形＝分子内に C=C がちょうど2本あり、単結合1本を挟んで並んでいるものだけ。1,3-ブタジエン・イソプレン・クロロプレンが該当する。**単官能ビニルとは検出が排他**になっている（塩化ビニルは付加重合のみ0件/1件、ブタジエンは 1,4- のみ1件/0件を実測）
  - 実測（ブタジエン3個）: 主鎖の結合が `-=---=---=-` ＝ −[CH₂−CH=CH−CH₂]− のくり返しで、二重結合3本・R2個・価標正常。イソプレンとクロロプレンも同じ形
  - **シス/トランスは断定しない**。生成した中央の二重結合は直交作図なので幾何が読めない（`readBondGeoFromCoords` が空を返す）。ただし**整形ツール（⇄）が生成物にも効く**ことを実測したので、字幕でそこへ誘導した。天然ゴム（シス）とグタペルカ（トランス）を自分で描き分けられる。テスト RX13 を拡張（整形後に幾何が読めることまで確認）
- [x] **硫黄の価数を文脈で決める**（加硫の下ごしらえ・2026-07-30・v283）— 加硫を実装しようとして**現状の化学モデルでは架橋が描けない**ことが判明したため、先に土台を直した。6価固定だったので `C-S-C` の硫黄に空き価標が4残り、**架橋の硫黄に自動水素が2つ描かれる**（SH₄ のような有り得ない形）
  - 判別規則は「**S=O を持てば6価、持たなければ2価**」。実際の化学（チオール/チオエーテル/ジスルフィド=2価、スルホ基/硫酸=6価）に対応する。ニトロ特例と同じ「パターンで見分ける」やり方で、`maxValencyOf(mol, atomId)` に集約し `getFreeValency` と `isValencyValid` の両方から参照する
  - **無回帰の根拠**: ライブラリで硫黄を含むのは**ベンゼンスルホン酸1件だけ**で、その S は `C, O(=), O(=), O` ＝ S=O を2本持つため6価と判定される。実測で空き価標0・自動水素0・命名も一致
  - これで解けるもの: **加硫（-S- 架橋）／チオール -SH／H₂S／ジスルフィド -S-S-（タンパク質の立体構造）／システイン・メチオニン**（compounds のパックで「表現できない」として除外した2件）。テスト K5
  - 残る限界: 構造異性体の列挙（`enumerateConstitutionalIsomers`）は分子式（元素の並び）だけを受け取り分子を持たないので、硫黄を6価と仮定したまま。硫黄を含む異性体列挙は高校範囲でほぼ出ないため据え置き
- [x] **加硫（硫黄で鎖を架橋する）**（ユーザー要望・2026-07-30・v287）— 1,4-付加重合でできたゴムに硫黄を入れて架橋する。**ゴムに二重結合が残っているのは 1,4-付加重合の結果**で、そこに硫黄が結びつく——この繋がりが画面で追える
  - 対象は「両端に R がある分子（＝重合の生成物）の C=C」に限る。単量体そのものやふつうのアルケンは加硫しない（実測でイソプレン単量体は候補0件）
  - 硫黄は2本の C=C 炭素の**中点**に置き、両方に単結合でつなぐ。二重結合は2本とも単結合になる。**同じ鎖の隣どうしは中点が鎖の内部に来るため自動的に落ちる**＝小さな環ができない
  - 途中で設計をやり直した。(1) 最初は代表の炭素どうしの中点に置いたが、鎖が横並びのとき**別の鎖の内部に硫黄を置こうとして失敗**した → 全ての C=C の組から置ける位置のあるものを探し、近い順に選ぶ形にした (2) 次に「鎖が2本以上」を条件にしていたため、**1本目の架橋で1分子になると2回目が出なくなった**（字幕で「もう一度押すと別の場所も架橋できます」と書いているのに嘘になる） → 条件を「架橋できる C=C の組があるか」に変えた
  - 実測（3単位×2本＝二重結合6本）: 架橋を1本ずつかけて候補が 36→16→4→0 と減り、**3本かけて二重結合を使い切って打ち止め**。硫黄を増やすほど架橋が増えてエボナイトになる、という教科書の流れと一致する。架橋の硫黄に自動水素は描かれない（v283 の価数修正が効いている）。テスト RX13 を拡張
- [x] **硫黄を含むアミノ酸2件を追加**（2026-07-30・v288）— システイン・メチオニン。**compounds のパック（v235）で「このアプリでは表現できない」として名指しで除外した2件**で、v283 の価数修正によって登録できるようになった。compounds.json と stages.json（アミノ酸と糖）の両方へ
  - 実測: システイン **C₃H₇NO₂S**（-SH の硫黄は空き価標1＝水素1つ）、メチオニン **C₅H₁₁NO₂S**（チオエーテルの硫黄は空き0）。どちらも分子式・命名が正しい
  - **`verify-compounds.js` の検査が古くなっていた**。「S は6価固定なのでチオールは登録できない」という前提の検査があり、システインを弾いた。前提が変わったので**検査の内容を差し替え**、いまは「S=O を持てば6価、なければ2価」という文脈どおりの価数かを見る
  - 差し替えた検査が実際に働くことを確認した（`maxValencyOf` を一時的に6価固定へ戻すと4件を検出し、戻すと合格）。テスト K5 を拡張
- [x] **「同じ化合物？」クイズに出題の前提を明示**（Gemini のレビュー docs/development_plan.md 項目16／実装 Opus・2026-07-30・v291）— 指摘は「フィッシャー投影の問題と単なる2D平面図の問題が混在し、どの前提で解くべきか分かりづらい」。**選択肢で切り替えられるのに、出た問題がどちらなのか画面に書いていない**のが実質の不足だった
  - 先に確かめたこと: このクイズで**構造が同じ・立体だけ違う組は出題されない**（120問中0件）。正解は `verifyMolecule`＝つながり方だけで決まるので、立体を読む必要が無い。それを図の上に書くようにした
  - **立体の種類を取り違えないこと**が要点。最初の実装で**シス-2-ブテンを「フィッシャー投影」と表示**してしまった。`readStereoOf` が非nullなら不斉炭素だと決めつけたのが誤りで、シス/トランスは別物。`centers>0 && fromRing` → ハース図、`centers>0` → フィッシャー投影、`geoms>0` のみ → C=C のシス・トランス、と3つに分けた。テスト F9 で4種類（シス-2-ブテン／D-アラニン／ピラノース／酢酸）を固定
  - 項目19（フィッシャー投影の解説へのリンク）は**保留**。ティップスは別セッションの一問一答と内容が重なるため（ユーザー判断）。「フィッシャー投影です」と書くところまでで止めている
- [x] **クイズの変形で「主鎖を曲げる」を優先**（Gemini のレビュー docs/development_plan.md 項目14／実装 Opus・2026-07-31・v293）— 指摘は「結合が伸びただけの問題が出やすい」。実測すると強度2で**一直線の分子の53%が「一直線のまま伸びただけ」**だった
  - **提案（伸長の確率を下げる）は採らなかった**。切り分けると、曲げられる分子22件は既に**一直線のまま0%**で、屈曲の保証は効いていた。残っていたのは「曲げられない31件」で、そこで伸長を減らすと**変形が何も起きず図が同一になる**（もっと悪い）
  - 本当の原因は屈曲の候補から `movingSet.size < 2`（回す側が1原子）を除外していたこと。**炭素3個の鎖（プロパン・ジメチルエーテル・エチルアミン等）は端の1原子を回すしか曲げようがない**のに、それを禁じていた。1原子でも許すようにした
  - 効果: 重原子3個以上の直鎖40件で**一直線のまま 58%→8%**、トポロジー破壊0。プロパン・ジメチルエーテルは25/25で曲がる。多重結合を含む枝を回さない制限（120°作図を守る）はそのまま
  - 立体は壊れない。v242 で入れた「変形のたびに立体を読み直して変わっていない配置だけ採用する」が1原子の屈曲でも効いており、シス-2-ブテン／トランス-2-ブテン／マレイン酸／フマル酸／D-アラニン／糖のいずれも立体の変化0。テスト F10
- [x] **くさび図モードの立体クイズ（不斉炭素1個・鎖状に限定）**（Gemini のレビュー docs/development_plan.md 項目17・18／実装 Opus・2026-07-31・v297）— 「フィッシャー投影に不慣れなユーザー向けに、手前/奥を明示した図で出題したい」
  - **docs の前提が実装と食い違っていた**。項目17は「本アプリでは不斉炭素に面マーク（▲/▽）を付与できる」としているが、**面マーク（`haworthFace`）は環の置換基専用**で（`_isHaworthFaceTarget` が隣接原子に環炭素を要求する）、鎖状の不斉炭素には付けられない。非環の立体は `readAtomParityFromFischer`＝**描いた向き**から読んでいる
  - そこで、化学モデルには触れず**描画だけ**を変えた。`readAtomParityFromFischer` と同じ軸判定（上下左右・±25°以内）を使い、**横＝手前を塗りつぶしのくさび／縦＝奥を破線のくさび**で描く。判定に使うのと同じ読みから描くので、図とアプリの答えが食い違いようがない。作図データは不変（表示専用）
  - **▲▽の字を置く案は捨てた**。実測で字が原子ラベルに **6.2px** まで寄る図があり、そもそも「くさび図」なのに結合が線のままだった。結合そのものをくさびに置き換え、不斉炭素から出る素の線は描かない
  - **水素だけ豆粒になる問題**: 自動水素は 16px、重原子は 42px なので、そのままだと4本のうち1本のくさびが 6px にしかならず「4つの基の並び」を見る図にならない。このモードに限り**水素を 42px まで伸ばす**（伸ばした先に他の原子が来る図では伸ばさない）。結果、くさびは全 240 本が 22.6px で揃った。伸長による原子の重なりの悪化は無し（160図で重なり2件・最悪 -4.5px は伸ばす前後で同値＝元からのもの）
  - **範囲（項目18）を絞ると答えが偏る**罠があった。不斉炭素1個どうしで構造式が同じなら関係は**必ず鏡像異性体**で、しかも該当する組は D/L の3組だけ。最初の実装ではライブラリのペアが54%を占め、実測 **same 23 / enantiomer 97 ＝「いつも鏡像」で81%正解**できた。ペアを使わず回した図だけにして 59/91（39%/61%）に均した
  - 対象は不斉炭素1個・鎖状・C=C の幾何なしの15件（乳酸・アラニン・グリセルアルデヒド・2-ブタノール・セリン・システイン・メチオニン・バリン・ロイシン・リシン・3-メチルヘキサン・2,3-ジメチルペンタン ほか）。テスト ST26（範囲・偏り・くさびの向きと本数・素の線が残らないこと・他モードに漏れないこと）
- [x] **キャンバスの移動・拡大縮小を操作説明に載せる**（Gemini のレビュー docs/development_plan.md 項目5／調査・実装 Opus・2026-07-29・v249）— ユーザー指摘「パン・ズームの操作方法が右ペイン等に載っておらず分かりづらい」。調べると **tutorials.json には `view-control` デモが既にあり**、デバイス別の文言も用意されていた。足りなかったのは中身ではなく**見つけやすさ**で、常時見えている右パネルの「📝 遊び方と操作方法」にだけ項目が無かった。そこに1項目を足した（style.css は並行セッションの未コミット変更があるため触らず、既存のインラインスタイルに合わせた）
  - **外部レビューの記述が実装と食い違っていた**: docs では「ホイールスクロールで拡大縮小」となっていたが、実装（game.js の wheel ハンドラ）では**素のホイールは移動**で、拡大縮小は **Ctrl+ホイール**（タッチパッドのピンチは ctrlKey が立つので同じ経路）。実装を読んでから書いた
  - 説明と実装がずれると案内が嘘になるので、テスト R11 で**両方を突き合わせる**（説明文に必要な語が入っていること＋素のホイールで幅が変わらないこと・Ctrl+ホイールで幅が変わること・右ボタンドラッグでパンが始まって viewBox が動くこと）。実機でも3種類とも確認
- [x] **Shift+ドラッグで分子を丸ごと動かす**（Gemini のレビュー docs/development_plan.md 項目6の前半／実装 Opus・2026-07-30・v253）— ユーザー要望「複数分子の位置関係を調整する移動機能がほしい」。**反応実行は場所が足りないと「分子を離してから実行してください」と案内するのに、離す手段が無かった**（select ツールでは素の原子のクリックは削除／元素置換になり、ドラッグ移動はロック原子とベンゼン環内原子だけ）
  - 判定は**削除・元素置換より先**に置く必要がある（後ろに置くと届かない。最初そこで踏んだ）。`moveComponentBy(ids, dx, dy)` は形（結合長・角度・トポロジー）を一切変えないので、検証にも立体の読みにも影響しない
  - **吸着は使わない**。吸着は「隣に結合を作る位置」へ引っ張るので、分子を離したいのに相手分子へ吸い寄せられる（実測で横に16pxずれた）。ポインタの生の移動量を格子単位に丸めて平行移動する。自動結合もしない（離すための操作であって、くっつけるための操作ではない）
  - 他の分子と重なる位置には置かず、理由を出す。テスト R12（掴んだ分子だけが動く・構造コード不変・横ずれ0・相手は動かない・重なる位置は拒否・Shiftなしは従来どおり削除）
- [x] **複数分子があるとき立体ビューがどの分子かを示す**（Gemini のレビュー docs/development_plan.md 項目7／調査・実装 Opus・2026-07-30・v255）— まず**どこが実際に壊れているか**を測った。命名カードは「酢酸 ＋ エタノール」、反応カードは「【2分子】」と両分子の官能基を出し、分子内反応（酸化）と分子間反応（エステル化）の両方を提示していて、**どちらも複数分子を正しく扱えていた**。3D模型も2D上でどれだけ離しても同一（分離2.44・広がり6.1で不変。トポロジーから組み直すため）で、重なりもしない
  - 残っていた不足は**識別**だけだった: (1) 中心の炭素がどの分子のものか出ない（「他に sp3炭素が N 個」の N は別の分子の炭素まで数えている） (2) 分子全体の図には全部入るのに、1つの分子の模型に見える。この2点に分子名を出すようにした（1分子のときは但し書きを出さない）
  - **Gemini が提案した「アクティブ分子」の導入とホバー選択UIは採らなかった**。命名カードと反応カードは全分子を見せるほうが情報が多く、アクティブ分子に絞ると**むしろ機能が落ちる**（分子間反応の提示ができなくなる）。くさび図・3Dの対象選択は既存の「⊹ 別の炭素を選ぶ」が担っており、選択手段は既にある。隠れた状態を1つ増やすより、いま何を見ているかを書くほうが小さく確実。テスト ST22
- [x] **複数分子に①②③を振り、図の下に名前を出す／呼び出しのオーバーフロー対策**（ユーザー要望・2026-07-30・v260）— 「分子名は図上の化合物の下側に表示」「分子に識別記号を振り、右ペインの化合物名にも反映」。**記号は A/B/C を使わない**——ユーザーの懸念どおり **C＝炭素・B＝ホウ素**と元素記号がぶつかり、α/β も糖のアノマー表記で使用中。丸数字（①〜㉟）はどちらともぶつからない
  - 見出しを出す条件は**重原子2個以上**。作図中に離れた場所へ C を1つ置いた瞬間に見出しが出入りするのを避ける。ただし**反応でできた副生成物は1原子でも出す**（ユーザー指摘「CH4 や H2O が生じた場合は表示すべき」）。`parkAsWater` で `fromReaction` を立て、それを条件に含める（`restoreState` は `Object.assign` なので Undo/Redo でも保持される）
  - 番号の付け方は `markedMolecules()` 1か所に集約し、**図と右パネルで必ず同じ番号**にした（別々に数えるとずれる）
  - **オーバーフロー対策**: 名称呼び出しは1分子あたり約504px右へ伸び、**10分子で作図の上限 |x|>5000 に到達**する。そこから先も呼び出しは成功するが、その位置では新しい原子を置けない（`getSnappedCoords` が tooLarge で弾く）＝**編集も反応もできない分子が黙って増える**状態だった。幅2400pxで下の段へ折り返し（段間3マス＝見出しと重ならない幅）、それでも収まらなければ理由を出して止める。実測で **184分子**（23段）まで全部が上限内・編集可能になった（従来は約10分子）
  - 途中で踏んだ不具合: 折り返し後の横位置を**全体の右端**から計算していたため1段に1分子しか入らなかった（45分子で48段）。「いまの段の右端」を見るようにして1段約8分子に。縦の位置合わせも同じ段の平均に直した
  - あわせて `GRID_SIZE` / `CANVAS_LIMIT` / `moleculeMark` を window に公開し、テストが同じ定義を参照できるようにした（`REGISTERED_NAMES` と同じ考え方）。テスト R13
- [ ] **P12-4** 高校化学の教科書反応の網羅（P8-7の継続）— 反応機構データ（reactions.json）と反応実行ルール（reactor.js）の両面で、教科書定番の反応を一通りそろえる。（エタノールの分子間脱水は v227、アルコール酸化の機構版は v299〜v300、ジアゾ化とカップリングは v301〜v302 で完了。次の候補はエステルの酸加水分解の機構版・マルコフニコフ付加の機構版 ほか）
  - [x] **P12-4 アルコール酸化の機構版**（Fable・2026-07-31）— reactions.json に `ethanol_oxidation`（エタノール→アセトアルデヒド・v299）と `propanol2_oxidation`（2-プロパノール→アセトン・v300）を追加（各4状態・3手順・新シリーズ「酸化反応」）。実行モードのルールは既にあったが巻矢印アニメが無かった分。**使用可能元素（C/H/O/N/S/Cl/Br）の制約で金属酸化剤（Cr/Mn/Cu）は表現できない**ため、酸化剤には次亜塩素酸 HClO を採用（実在の機構: O攻撃→次亜塩素酸エステル→E2型の脱水素。desc で「二クロム酸カリウム等でも同じ骨組み」と明記）。「O–H と C–H の2つの水素が失われて C=O ができる（脱水素）」を見せ場に据えた。reactor.js の `oxidize_primary` / `oxidize_secondary` に mechanismId を結線（detect/apply は不変）。テスト RX14・RX15（RX12 と同じ双方向固定＋矢印の指す先の添字検証）。全148テスト合格
  - [x] **P12-4 アニリンのジアゾ化とカップリング**（Fable・2026-07-31）— reactions.json に `aniline_diazotization`（7状態・6手順・v301）と `diazo_coupling`（3状態・2手順・v302）を新シリーズ「ジアゾ化・カップリング」で追加。ジアゾ化はニトロ化と同じ流儀で **NaNO₂+HCl から生じた NO⁺ を出発点**にし（desc に明記・Cl⁻ は省略）、N-ニトロソ化→水2分子によるプロトン移動→水の脱離、最終キャプションで氷冷の理由（温まるとフェノールに分解）。カップリングはナトリウムフェノキシド＋ジアゾニウムで、**O⁻ から環へ押し出す4連の玉突き矢印**でパラ位攻撃を見せ、中間体は電荷ゼロのキノン型（フェノキシドが反応しやすい理由がそのまま図になる）。生成物 p-ヒドロキシアゾベンゼンは v303 で名称ライブラリにも追加し、機構→ライブラリの導線がつながった。全148テスト合格
- [x] **P12-5** 反応実行の前後比較・機構ジャンプ・モーフィング（2026-07-24 ユーザー要望「反応後に変化してしまい前後の比較ができない・機構がわかるようにしたい」）— **設計（Fable）**: [DESIGN_reaction_execution.md](DESIGN_reaction_execution.md) **8章**。第1弾=前後比較オーバーレイ＋差分ハイライト＋機構ジャンプ／第2弾=簡易モーフィング。**実装 Opus**。第1弾・第2弾とも完了（全95テスト合格 — v153〜v156）
  - [x] **P12-5-第1弾**（Opus）— (1) **データ基盤**: reactions.json の全9機構に安定ID `id` を新設（name表示は不変で互換）、reactor.js の8ルールに `mechanismId` を付与、`REACTION_RULES` を window に公開。テスト RX3（全 mechanismId が id に実在・id重複なしを機械検証）。(2) **前後比較**: `Reactor.execute` で直近反応のキャンバス全体スナップショット（原子ID付き独立コピー）を before/after 保持（`lastReaction`。次の反応で上書き・全消去/モード離脱で破棄）。反応カードに「🔍 反応の前後を見る」ボタン、`#rx-compare-overlay` に反応前/後の2図を並置（図サイズは `IP_REVIEW_SCALES` 共用、CSSは共通クラス `.canvas-overlay` に括り出し）。**差分ハイライト**（原子ID対応で機械判定）: 消えた/次数減の結合＝反応前にオレンジ点線、できた/次数増＝反応後にシアン、脱離原子＝オレンジ枠、付加原子＝緑枠。テスト RX1。(3) **機構ジャンプ**: mechanismId のある反応に「⚗ この反応の機構を見る（代表例）」ボタン（反応カード・オーバーレイ共用）。押すと learn モードへ切替え、反応機構ビューアで id 照合により該当機構を再生（select 同期）。「※あなたの分子そのものではなく代表例で再生」の注記。テスト RX2。全93テスト合格 — v153〜v155
  - [x] **P12-5-第2弾**（Opus）— **実行時の簡易モーフィング（表示のみ）**: 反応実行時に約0.8秒で前→後をモーフィング。分子データは実行時点で即確定（updateDrawing・カード・名称・解説は同期）＝検証/Undo/監査に一切影響しない。補間は before/after スナップショット（原子ID対応）で共通原子=座標を線形補間・脱離原子=フェードアウト・付加原子=フェードイン・結合の次数変化=クロスフェード・生成/消滅=フェード（`interpolateMorph` は DOM非依存の純関数）。フレーム駆動を reaction.js から共通関数 `animateFramesLoop` に切り出して機構アニメと共用（`ReactionPlayer.animateFrames` は委譲。E系不変）。世代トークン `_morphGen` で多重反応・中断を安全化、再生中のキャンバス操作は `finalizeMorph` で即完了（picking中は続行・通常タップはスキップ）、全消去/モード離脱でも停止、`prefers-reduced-motion`/rAF無しでは即確定。テスト RX4（補間の純関数 t=0/t=1）・RX5（実行時に分子即確定・アニメ中/後も正準コード不変）。全95テスト合格 — v156
- [ ] **P12-6**（P10検討残り）まとめ表示中の官能基編集 — カード化した官能基を編集する手段（案: カードをタップして一時展開／別の官能基へ変換）。要方針決定
- [x] **P12-7** 立体化学対応（**M0〜M3 完了・v168〜v182 公開済**。残りは P12-8 へ集約）（2026-07-25 ユーザー指摘「糖類については α/β をどうするかという問題がある」／ユーザー案「糖専用の環ユニット」vs「より広範な設計」）— **たたき台メモ（Opus）**: [DESIGN_stereochemistry.md](DESIGN_stereochemistry.md)。**設計判断は Fable**（正準コード・同型判定に関わるコア変更＋決めが後を引く判断のため）。要点: 現行の同一判定は構成のみ（例外は cis/trans の座標判定と不斉炭素マークのオプトイン判定）→ **α/β 以前に、すべてのアルドヘキソースが構成同一のため「グルコース」を構成エントリで入れると他の糖を誤命名する**（F8に抵触）。同じ壁はアミノ酸（D/L）・乳酸・酒石酸（メソ体）にも及ぶ。論点=(1)識別に立体をどこまで入れるか（現行維持／相対配置／CIP風R/S／糖専用ユニット）(2)**2D直交作図での配置入力手段**（最難関）(3)命名（D/L・α/β・R/S と既存 cis/trans の統合）(4)既存機能・テストへの波及（IP4の列挙数・F7/F8・クイズ）(5)段階導入の切り方。**糖類の追加は本項目の決定まで意図的に保留**
  - **[方針決定済み 2026-07-25・Fable]** DESIGN_stereochemistry.md **8章**。採用=**オプトインの立体レイヤ**。識別は案C（CIP）ではなく、既存 `rootedFragmentCode` の正準順序に錨を打った**パリティ記述子**で立体異性体を区別（CIP 不要／メソ酒石酸も正しく1種に畳める）。**既定 `canonicalCode` は不変＝回帰ゼロ**、立体は `canonicalStereoCode` でオプトインのみ。**命名はゼロから計算せず compounds.json の登録参照と照合**（cis/trans の `geometry` 照合を一般化、α/β・D/L・R/S の計算表示はスコープ外）。**入力は糖・アミノ酸系の「フィッシャー投影モード」**（直交格子＝フィッシャーで幾何一致、モード外は座標を見ない）。案D（糖専用ユニット）は識別モデルとしては却下し、実利は入力モード側で回収。段階: **M0**（`canonicalStereoCode`＝原子パリティ＋結合E/Z の二本立て＋自己同型下での畳み込み。性質テスト先置き＝Fable 級の要）→ **M1**（データ＋lookup 1段追加・既存 `geometry` の統合）→ **M2**（フィッシャー入力＋sp2 の±120°スナップ候補）→ **M2.5**（立体異性体の総数当て・立体のみ書き出し・同一性/鏡像クイズ）→ **M3**（任意: 一般くさび・R/S計算表示）。**M0 は Fable 着手、M1/M2 定型部は Opus 委譲可**
  - **[レビュー反映 2026-07-25]** 同 **8.9節**: (1) ロールバック=実装は **`feat/stereo` ブランチ**で進め停止点ごとに `--no-ff` マージ（マージ1個の revert で巻き戻し。main は Pages 公開元なので WIP を置かない）＋オプトイン設計＋データは任意フィールド追加のみの3層。(2) C=C の cis/trans は**別途でなく本レイヤに統合**（入力が座標から読める唯一の立体要素。データモデルは M0 から二本立て）。(3) **結合次数の変更で座標は動かさない**。sp2 から伸ばすときの描画スナップ候補に ±120° を追加（環の `candidateAngles` と同じ「置くときだけの補助」）。90° 描画でも cis/trans 判定は成立（外積符号）。自動生成レイアウトのみ 120° を標準に。加えて**「シス/トランス整形」ボタン**（ユーザー提案・採用）: 整形モードで C=C をタップ→両端の置換基を ±120° へシフト（描かれている側＝外積符号を保存・不定なら trans 既定）、再タップで cis⇄trans 反転、saveState で Undo 可。化学モデル非依存の作図支援のため **M0 と独立に実装可＝Opus 委譲可**
  - [x] **P12-7 先行: シス/トランス整形モード**（Opus・2026-07-25）— 左パレットに ⇄ ボタン。整形モードで C=C の炭素/結合をタップ→両端置換基を軸から±120°へ（現在長・外積符号=側を保存、不定なら trans 既定）、同じ結合の再タップでタップに近い側の炭素の部分木だけを鏡映し cis⇄trans 反転。saveState で Undo 可。chemistry.js 無変更。タップ検出は「炭素タップ→接続 C=C」方式（結合中点が原子当たり判定 28px に潜るため）。テスト RF1（実 PointerEvent 駆動: 整形→反転→Undo 完全復帰→単結合無反応）。全100テスト合格 — v165（main: 50e91c8）
  - [x] **P12-7-M0 立体レイヤのコア**（Fable・2026-07-25・**feat/stereo ブランチ 5c4c923・マージ待ち**）— chemistry.js: `canonicalRowsCore` に最適ラベリング全収集の collect 引数（既定 null=従来と完全同一）／`computeAtomParity`（4置換基の断片コード辞書順に対する置換の偶奇±1。擬似不斉は null）／`canonicalStereoCode(mol, stereo)`（原子パリティ＋結合 syn/anti のトークン列を自己同型全体で最小化して付加。メソ体は自己同型で畳まれ、無効記述子は無視）／`mirrorStereo`（code(mirror)==code ⇔ アキラル）。既定の canonicalCode・verifyMolecule・列挙は不変。テスト ST1（乳酸鏡像・酒石酸3種と鏡映不変・E/Z 3区別・無効無視・既定不変）＋ Node 性質テスト32項目（16アルドヘキソース完全区別・構築順不変性）。worktree（C:\Antigravity\worktrees\stereo）で全100テスト合格 — v166（feat/stereo: 5c4c923・push済）
  - [x] **P12-7-M1 命名＝参照照合・E/Z 統合**（Opus・feat/stereo 7398102・push済・v167・Fable レビュー済）— chemistry.js: `readBondGeoFromCoords`（座標から適格な全 C=C の syn/anti を読む汎用版。getDoubleBondGeometry の「対象1本・2置換のみ」制限を超え3置換・複数C=C対応）＋適格判定を `_bondGeoRefs` に共通化（canonicalStereoCode と共有・挙動不変）。compounds.json: シス/トランス-2-ブテン・マレイン酸・フマル酸の `geometry` を `stereo.bondGeo`（syn/anti）へ移行し `geometry` 全削除。game.js: `getCompoundLibrary` が stereo 付きエントリの stereoCode を事前計算（`_mapStereoToMol` で添字→実行時ID写像・atomParity にも対応済）、`lookupCompoundName` に立体コード照合を1段追加（stereo 無しは従来どおり幾何不問＝挙動互換）。テスト ST2＋F8 更新。全101テスト合格・Node 性質32/32 無回帰。**次: M2a（sp3 パリティの座標読み取り＋開鎖形の糖・アミノ酸データ投入）**
  - **M2 入力設計確定（Fable・DESIGN 10章）**: フィッシャー専用モードは**新設せず**、直交格子＝フィッシャーとして `lookupCompoundName` でのみ sp3 パリティを常時読む（cis/trans と同格の命名限定例外）。`readAtomParityFromFischer`（軸方向±25°・[上右下左]タプル→computeAtomParity）。3性質（左右入替=反転・90°=反転・180°=不変）で規約を検収。M2a=開鎖形（グリセルアルデヒド・グルコース/ガラクトース/マンノース・アラニン・乳酸）、環の α/β は Haworth 上下マークが要るため **M2b** に分離
  - [x] **P12-7-M2c 教科書ライクなハース投影入力**（ユーザー FB「難易度が高い／教科書ライクに」）— DESIGN 12章。**main へマージ・公開済（マージ a1503f6・v175）**。向き固定のハーステンプレート＋「上下は縦位置で読む」方式。**六角形を1クリックで置き（環内 O 配置済み）、各炭素に -OH を上下に置くだけで α/β-グルコースが描け命名される**（別マークモード不要・向き固定で鏡像の曖昧さなし）。
    - コア（Fable・1c112a5・v173）: `readRingParityFromHaworth` に「面マーク未指定なら縦位置(±25°)から面を導出」を追加（明示 haworthFace は不変＝後方互換）。ST4 更新＋ST6 新設
    - プラミング（Opus・16506b7・v174）: `haworth-pyranose` モジュール（`getHaworthPlacementPlan`/`placeHaworthPyranose`・ゴースト・Undo）／縦置きスナップ（`_atomInOxygenRing` で糖の環に限定・全炭素環は非干渉）／α/β-D-グルコピラノースを縦位置ベースへ再作図／ST7
    - 仕上げ（Fable レビュー・9accedc・v175）: 旧テンプレートは前縁と奥辺が同一 x のため上向き置換基が真上の環原子とぶつかり、**32配置の最悪ケースで自動水素間隔が 4px**まで詰まる問題を発見 → 縦幅を広げ(±48)前縁を内側へ寄せる座標に変更し **最悪 11.5px・出荷データ 16px**（監査基準12px超）に改善。α/β を同一レイアウトへ再作図（差分48行の最小変更）。F8 全154エントリ重複0
    - **残る改善余地**: モジュールは骨格のみのため CH₂OH と OH×5 はユーザーが付ける手数がある（C5 に -CH₂OH を前置きする案）。フラノース（フルクトース）未対応
  - [x] **P12-7-M2b プラミング: 面マークモード・α/β-グルコピラノース**（Opus・feat/stereo 0341fa6・push済・v170・Fable レビュー済）— game.js: 「α/β 面マークモード」`haworthMode`（`btn-haworth-mark`。asymmetric/reshape と同じトグル・排他）。環外置換基クリックで `haworthFace` を上(+1)/下(-1) トグル・saveState で Undo。レンダは上=塗り三角/下=破線三角バッジ。`serializeState`/`restoreState`（Object.assign 経路）・`createTargetFromData`・`buildExportJson` が haworthFace を保存復元・出力。`getCompoundLibrary` は target に haworthFace があれば `readRingParityFromHaworth` で stereoCode を事前計算。`lookupCompoundName` に環リーダー合流（非環Fischer／環Haworth 相互排他）。compounds.json に α/β-D-グルコピラノース2件（β: C1上/C2下/C3上/C4下/CH₂OH上・α は C1 のみ下＝標準ハース）。テスト ST5。全105テスト合格。**Fable レビュー**: serialize/restore の haworthFace 往復・α/β の canonicalCode 同一/stereoCode 相異・**F8 全154エントリ重複0**・実アプリで β⇄α 命名切替を機械確認。**未マージ（feat/stereo）**
  - [x] **P12-7-M2b コア: 環立体パリティのハース読み取り**（Fable・feat/stereo 8a045d8・push済・v169）— DESIGN 11章。Node 試作で de-risk 後に実装。`_ringAtomIds`（環所属原子集合）・`_parityFromDirs`（4置換基の3D方向ベクトル→フラグメントコード順の三重積でパリティ。M0 の computeAtomParity と一貫符号）・`readRingParityFromHaworth`（環sp3中心を、環隣接=面内(z=0)・環外置換基の `haworthFace`(±1)=z±face で読む。面未指定/非標準構成はスキップ）。フィッシャー(非環)は環中心を除外し相互排他に。テスト ST4（環5中心読取・α/β/エピマー区別・2^5=32配置相異・回転/平行移動不変・鏡映=mirrorStereo一致・相互排他）。全104テスト合格・Node 32/32 無回帰。**プラミング（面マークUI・serialize・α/β-グルコピラノースデータ）は Opus 実装中**
  - [x] **P12-7-M2a フィッシャー読み取り＋開鎖糖の立体登録**（Opus・feat/stereo 2049be7・push済・v168・Fable レビュー済）— chemistry.js: `readAtomParityFromFischer`（軸±25°・スロット[上右下左]→`computeAtomParity`。軸外れ/スロット衝突/暗黙H数不一致はスキップ）。game.js: `lookupCompoundName` のユーザー立体コードに atomParity を合流（1行）。compounds.json 87→96（D/L-グリセルアルデヒド・D-グルコース/ガラクトース/マンノース〈鎖状〉・D/L-アラニン・D/L-乳酸。パリティは読み取り関数の出力を貼付）。**グルコース/ガラクトース/マンノースが canonicalCode 同一・stereoCode 相異で一意命名＝F8 が立体で初めて破れる実証**。既存 乳酸(compounds)・アラニン(stages.json) は全軸そろえで特定配置に化けるため OH/NH₂ を45°オフ軸へナッジ（トポロジー・canonicalCode 不変・総称名維持）。酒石酸は慣用名リスクで見送り。テスト ST3。全102テスト合格・Node 性質32/32 無回帰・F8 一意性0重複を Fable が実ファイルで再確認。**含意**: 軸そろえ描画は常に記述子を生む（DESIGN 10.7）。**次: M2b（環 α/β の Haworth 上下マーク・serializeState 拡張）／または停止点として feat/stereo を main へ --no-ff マージ**
  - [x] **P12-7-M3 疑似3D回転ビューア**（コア=Fable 2aa87dc／UI=Opus 6398392／レビュー修正=Fable f76a5b6・**main マージ公開済 v182**）— chemistry.js: `tetrahedralDirs(mol, atomId, parity)`（4置換基を指定パリティに一致する正四面体配置の3D方向ベクトルへ割当）・`parityFromDirs`（回転で不変・鏡映で反転）。stereo.js: モーダルにタブ（✏くさび図／🧊3Dで回す）、球棒モデルをドラッグ回転・自動回転・正面リセット、z ソートの画家アルゴリズムと弱い透視投影、**🪞鏡像と並べる**（左右同時回転で非重ね合わせを体感。不斉でない中心は「回すと重なります」と出し分け）。表示は**ユーザーが描いた立体と一致**し、読めない描き方では断定しない。小画面はモーダルがスクロール不要になるよう図を圧縮（809→723px）。テスト ST9。全109テスト合格。**判明**: 既存くさび図の様式（上下=紙面内・右=手前・左=奥）は左右が正反対のため4方向が同一平面に乗り、手性を幾何学的に表現できない（行列式が常に0）→ くさび図は「一例」のまま据え置き

- [ ] **P12-8 立体化学の発展課題**（2026-07-26 ユーザーとの検討で整理。P12-7 M0〜M3 公開後の残り）
  - [x] **くさび図のフィッシャー準拠化＋結合を軸にした回転**（Opus・v183。ユーザー提案）— 旧様式（上下=紙面内・右=手前・左=奥）は左右が正反対で4方向が同一平面に乗り手性を担えなかった問題を解消。
    - **くさび図（A）**: chemistry.js の `fischerSlots(mol, atomId)`（Fable 実装・呼ぶだけ）で読めたスロットを**ユーザーが描いた配置そのまま**に描画。**縦（上・下）＝紙面の奥＝破線くさび（ハッシュ）／横（左・右）＝紙面の手前＝塗りくさび（▶）**。読めない中心（軸から外れた作図）と環中心は従来どおり「一例」と明示し、環では「⬍ α/β 面マーク」「🧊 3Dで回す」への案内を添える（嘘の立体を断定しない原則を維持）。各結合・各ラベルに `data-slot` / `data-bond` 属性を付けて検証可能にした
    - **結合を軸にした回転（B）**: 3Dビューに「回転軸: [画面] [置換基名×4]」のボタン列を追加。軸を選ぶとドラッグ（横）と自動回転が**その結合まわりの回転**になる（**ロドリゲスの回転公式**を自前実装・外部ライブラリなし）。選択中の結合は水色で強調。軸選択時は軸が画面と平行になる向きに構え、さらに `STEREO3D_AXIS_TILT` の見下ろしと初期位相の自動選択で、残り3つが重なって見えないようにしている。鏡像ペインは軸も鏡映して回すので軸上の置換基が両側で固定される。教育文言「結合を軸に回しても同じ分子です（回転では鏡像になりません）」＝ `parityFromDirs` が回転で不変であることが根拠
    - テスト **ST10** 新設（左右の描き分けでくさび図の左右スロットが入れ替わる／縦=hash・横=wedge の要素種別／斜め描きは「一例」／軸上の置換基は回転で不変・他3つは動く・パリティ不変／鏡像側でも軸固定／「画面」復帰）。**H1 は期待値を更新**（塗りくさびが右1本→左右2本になったため polygon 数 1→2）。全110テスト合格
    - **副産物**: 実ブラウザ確認で `--color-cyan` が未定義の CSS 変数（`--neon-blue` が正）と判明し `stroke:none` で線が消えていた。ST10 に「3D図の全要素の computed stroke が none でないこと」の検査を追加して同種事故を防止。**index.html の `#stereo-title` も未定義の `var(--color-cyan)` を使っており、いずれ整理したい**
    - **未実施（この項の残り）**: 「3Dの向きに連動してくさび図が書き換わる」（同じ分子でも向きでくさび図は何通りもある、という体験）は今回は入れていない
  - [x] **くさび図のローテーション（クリックした枝を上へ）＋くさび図の鏡像比較**（Opus・v187）— stereo.js: くさび図の置換基（ラベル・結合・透明な当たり判定 rect）をクリックすると、その置換基が上に来るようスロットの**中身**を動かす。動かし方は**パリティを保つ偶置換のみ**（`StereoView.rotateSlotsTo`）: down=180°回転 `(up down)(left right)`／right=left 固定の3巡回 `up→down→right→up`／left=right 固定の3巡回／up=恒等。UI は「⟲ 元の並びに戻す」と、並べ替え後の明示「**この操作では分子は変わりません**（フィッシャー投影で許される動かし方です）」。`fischerSlots` が読めない中心（斜め描き・環中心）はボタンを無効化し理由を出す。**🪞 鏡像と並べる**は左右スロットの入れ替え（転置1回＝奇置換）で作り、左右2ペインを `data-pane` 付きで並べる（鏡像側も並べ替え可能）。文言は不斉の有無で出し分け。テスト **ST11** 新設（フィッシャー規約の3Dベクトル `up=[0,-1,-1] right=[1,0,1] down=[0,1,-1] left=[-1,0,1]` と `rootedFragmentCode` から `parityFromDirs` を独立に計算し、**4方向どのクリックでもパリティ不変**・連続操作でも不変・鏡像ペインは符号反転・両ペイン操作でも関係維持を機械検証／元に戻す／不斉でない中心の出し分け／立体未指定でローテーション不可）。全111テスト合格・既存 ST9/ST10/H1 無回帰（期待値変更なし）。**旧メモ（実装前の注意）**: **⚠化学的な落とし穴があるので実装前に必ず読むこと**: フィッシャー投影は「紙の上で90°回すと鏡像異性体になる」「2つを入れ替えると鏡像になる」という規約を持つ。素朴に「クリックした枝を上へ動かす＝十字を回す」と実装すると、**表示している分子が別物（鏡像）にすり替わる**。正しくは**パリティを保つ操作**（=偶置換。軸1つを固定した残り3つの巡回置換、または180°回転）で並べ替える。実装時は移動の前後で `parityFromDirs`／`computeAtomParity` の値が**不変**であることをテストで機械検証する（変わったら実装が誤り）。この制約自体が「フィッシャー投影で許される操作／許されない操作」の学習内容になるので、**UIで明示する**とよい（「この操作では分子は変わりません」）
  - [x] **くさび図でも鏡像異性体を並べて比較**（Opus・v187。上の項と同時に実装）— 3Dビューの🪞と同じことをくさび図でも。鏡像は左右スロットの入れ替え（＝1回の転置＝奇置換）で作れる。「くさび図では左右を入れ替えると鏡像になる」という規約の学習にもなる
  - [x] **3D回転軸の向きを選べるように**（Fable・v189）— 「軸の向き: 自動／奥／上／右／左」。奥はニューマン投影に近い見え方で R/S の姿勢に直結。オイラー角では右・左に向けられないため alignRotation（ロドリゲス）で合わせ込む。実測で4方向とも正確に向くことを確認。（元の要望）— 選んだ結合軸を画面上で「上／奥／右／左」のどこに向けるか切り替える。現在は自動で見やすい角度を選んでいるだけ
  - [x] **鏡像ペアの配置モード切替**（Opus＋Fable レビュー・v192）— 「鏡面対称」＝**画面空間の鏡映**（left の最終ベクトルの x を反転）に作り直し、100ケース（軸5×向き5×角度4）でずれ**0**・パリティ -1/+1 を確認。従来は分子空間で鏡映してから同じ視点変換をかけていたためずれ1.908の非鏡像だった（鏡映と回転は非可換）。「軸を揃える」＝鏡像側の軸をオリジナルと同じ向きに構える（軸ベクトルのずれ0）。くさび図側は偶置換12通りを総当たりして最接近させ、残る食い違い2か所を枠で明示。（元の要望）— 「**鏡面に対称な状態**（本当の鏡像配置）」⇄「**オリジナルと回転軸の位置を一致させた状態**（軸を揃えて比較）」をトグルする。後者は「軸を揃えても他が重ならない」ことを示せるので、非重ね合わせの説明として強い
  - [x] **回転が中心原子と重なって見えにくい問題**（Fable・v186）— 軸回転時、他の3置換基が中心原子の丸と重なって回って見えた。**幾何（109.5°）は歪めず**、`STEREO3D_HUB` 21→13・`STEREO3D_BOND` 62→78 に調整（ユーザー判断。傘を閉じる＝角度を変える案は正しさを損なうため不採用）。中心半径に対する結合長の比が 2.95倍→6.0倍になり円すいの動きが見える。4通りの角度で viewBox 内（最大X 86<120・最大Y 69<114）に収まることを実機確認
  - [x] **置換基ラベルの慣用表記**（Fable・v185。ユーザー要望）— 組み立てた -COOH が組成式 "CHO₂" と表示されていた。`conventionalGroupLabel` を新設し -COOH／-CHO／-CH₂OH／-C≡N を慣用表記に（当てはまらない枝は従来どおり組成式）。表示専用で判定に非影響
  - [x] **未定義だった CSS 変数 --color-cyan**（Fable・v184。P12-8 レビューで発見）— learn.js 18箇所ほか計27箇所が参照していたが定義がなく、実測 rgb(245,246,250)＝白に落ちて強調が効いていなかった。`--neon-blue` の別名として1行定義
  - [x] **スマホ縦画面のレイアウト分離**（Fable・v194）— 幅760px未満で鏡像2ペインを上下積みに。`isNarrowLayout`／`drawWedgePane`・`drawPane` に縦オフセット／回転・リサイズで組み直し（150msデバウンス）。375px で表示倍率 **0.49→0.90**。（元の指摘「スマホ縦画面とデザインを分ける必要があるかもしれません」）— 立体ビューで鏡像2ペインを**横並びから上下積みに切り替える**（縦画面では横並びだと各ペインが小さくなる）。v193 で SVG の実寸を広げて縮小は緩和したが、狭い画面では max-width で結局縮む。実装は `renderWedgeAll` / `render3D` のペイン配置（現在は横オフセット ±158 / ±120 のみ）に**縦オフセット**を持たせ、`window.innerWidth`（または matchMedia）で切り替える。viewBox も縦長（例: くさび `-165 -300 330 600`）にする。他にも縦画面固有の調整（モーダルの高さ・ボタン列の折り返し）をまとめて見直す価値がある
  - [x] **環の「横から見る」ビュー**（2026-07-26 ユーザー提案）— 環の実際の立体構造（いす形）を再現するのではなく、**環を平面とみなして真横から見る**新ビュー。環は線（または細い楕円）に潰れ、置換基が上下に突き出すので、**α/β や各OHの上下がもっとも直感的に把握できる**。データ側は既に `haworthFace`（±1）と環の2D座標を持っているので、環原子を z=0 平面に、環外置換基を z=±d に置けば、**既存の3D投影・回転機構をそのまま流用**できる（単一sp3中心の3Dビューを「環全体」へ拡張する形）。カメラを真横（≈90°）にするプリセットと、ハース図の見え方（斜め）へ戻すスライダー/ボタンを用意すると、**ハース投影が何を描いた図なのかが繋がる**（**実装済み・v198前後**。カメラの倒し角スライダー0〜90°＋「ハース図の向き」「真横」プリセット＋H表示切替。v209 で縦軸まわりの回転ボタンも追加。テスト ST13。チェック漏れを 2026-07-27 の計画確認で修正）
  - [x] **立体ビューのレイアウト刷新**（2026-07-28 ユーザー指摘「幅が狭くフォントもかなり小さい・左右のペインも見直したい」・v228）— 1280×800 で実測すると **430×1073px**、つまり**横に850px余らせたまま縦がはみ出す**形だった。図の下に操作と解説を積んでいたのが原因。**図を左・操作と解説を右**の2カラムにして 1073→729px（くさび図単体）に短縮し、モーダル幅を 430→980px、解説文を 12〜12.5px→13.5px、ボタン等を 11〜11.5px→12〜12.5px に上げた。**2カラムは 1000px 以上でだけ効かせ、スマホ縦は1カラムのまま据え置き**（stereo.js の `isNarrowLayout()`＝760px未満で鏡像を上下に積む挙動には触れていない）。テスト R4（CSSOM 検査）
    - 図のサイズ指定は変えていない。**鏡像モードでは stereo.js が SVG を 612px に広げているのに、モーダルが430pxだったため `max-width:100%` で398pxまで潰れていた**。器を広げるだけでこれは解消する（ユーザーが以前「鏡像と並べると縮小されて読みづらい」と指摘した件の根本原因）
    - 途中で見つかった2件: (1) グリッドの `grid-row: 1 / -1` は**暗黙の行には効かない**（-1 が明示グリッドの終端を指す）ため図が1行目だけを占め、解説が図の下に落ちて75px無駄になっていた → `span` で数え上げに変更。(2) `@media (max-width:899px)` の `#stereo-caption { font-size }` が**マークアップ側の inline style に負けてずっと死んでいた** → inline を外して CSS に一本化（基準を先、狭い画面の上書きを後ろに置く）
    - 鏡像を並べているあいだは図が612pxまで広がって右カラムが痩せるため、その間だけ1カラムに戻す（stereo.js が `.wide-figure` を付け、CSS が判定）。それでも1画面に収まらない場合があるので `max-height: 92dvh; overflow-y: auto` で**画面外に切れるのではなくモーダル内でスクロール**させる
    - **明示すべき制約（ユーザー指摘）**: 平面近似のため**アキシャル/エカトリアルの区別は表現できない**。実際のグルコースはいす形。図に「環を平面とみなした模式図です。実際はいす形で、アキシャル/エカトリアルの区別があります」と注記を必ず添える（高校範囲ではハース＝平面近似が標準なので教材としては妥当な割り切り）
    - 発展: いす形は将来 conformers のプリセット（DESIGN_3d_correspondence.md M3）で別途扱う
  - [x] **M2.5 立体の学習コンテンツ その1: 立体異性体クイズ**（Opus・v214）— 「2つの図の関係は 同じ分子 / 鏡像異性体 / 別の立体異性体（ジアステレオマー）か」の3択。**CIP（R/S）は使わず**、P12-7 の立体コードの比較だけで判定する（立体コード一致＝同じ／鏡像の立体コードが一致＝エナンチオマー／構造式は同じでどちらでもない＝ジアステレオマー）。出題は2通り: (a) ライブラリの2エントリ（D/L-アラニン・D/L-乳酸・D/L-グリセルアルデヒドの3組が鏡像／糖のアノマー・エピマーとシス/トランスで53組がジアステレオマー） (b) 1つの図を**紙面内で回した図**と並べる。**フィッシャー投影は180°回すと同じ分子・90°回すと鏡像**という教科書の定番を、規則を書き込むのではなく「回した図から立体を読み直す」ことで自然に出している（実測でその通りになった）。正解は必ず実際に描かれた2つの図から読んだ立体で決める（生成側の意図は信用しない）。**設計上の判断**: 「図を回す」出題はフィッシャー（非環）限定にした。ハース投影を紙面内で180°回すと規約どおり読めば面が上下逆＝鏡像を描いた図になってしまい、理屈は同じでも教科書で扱わない紛らわしい問題になるため（環はライブラリのペアで十分よい問題になる）。テスト ST15
  - [x] **M2.5 立体の学習コンテンツ その2: 総数当て**（Opus・v218）— 「この分子の立体異性体は何種類？」の4択。判定は `countStereoisomers`（自己同型で最小化するため、メソ体も回転対称も同じ仕組みで畳み込まれる）。**誤答の選択肢には必ず 2ⁿ を混ぜ**、2ⁿ を選んだときは「そこが引っかけ」と名指しで解説する。ライブラリで畳み込みが起きるのは酒石酸のみ（少数派）なので、**半々の確率で畳み込み分子を出す重み付け**を入れた（素直に選ぶと6%程度しか出ず、ねらいが伝わらない）。あわせて **酒石酸を compounds.json に追加**（高校の定番なのに未収録だった）。テスト ST18。**残り**: 立体のみの書き出し練習
  - [ ] **M2.5 立体の学習コンテンツ その3**（ユーザー要望・2026-07-27）
    - **くさび図で一致/不一致を答える問題** — いまの立体異性体クイズは平面の構造式（フィッシャー/ハース）どうしの比較。くさび図どうし、あるいは「くさび図 ⇄ フィッシャー」の読み替えを問う形にすると、P12-7/M3 で作った表記の橋渡しが学習になる
    - [x] **ティップス（解説）の充実**（Opus・v331）— 立体ビューのモーダルに、既定は折りたたみの読み物「📖 R・S ってなに？」を新設（`#stereo-rs-tips`）。学習タブではなく**モーダル内に置く**のは、図と並べて読めることが要点だから
      - 書くのは ①R・S は不斉炭素まわりの並び方の呼び名 ②決め方の骨組み（4つに優先順位→最下位を奥→残り3つが時計回りなら R・反時計回りなら S）③優先順位は原子番号で決め、同点なら1つ外側へ進む ④このアプリは判定せず重ね合わせ・鏡像比較で見る ⑤**D・L は R・S とは別の規約**（高校段階の典型的なつまずき）まで。**CIP は実装しない方針は変えず、アプリが特定の分子・炭素を R / S と名指すことは一切しない**
      - **文字だけにせず図と連動させる**のが要点。⑥に「🧊 H を奥に向けて構える」を置き、押すと 3Dビューへ切り替えて H への結合を回転軸にし、軸の向きを「奥」にする（`faceHydrogenAway`。既存の軸選択＋軸の向きをそのまま呼ぶ）。ニューマン投影に近い見え方になり「残り3つがどちら回りか」を実際に読める。中心に H が無ければ無効化して理由を title に出す。**最下位をアプリが決めているのではなく**、「H は原子番号が最小なので中心にあれば必ず最下位」という一般則を使うだけ
      - **D/L の判定基準は糖とアミノ酸で違う**（糖＝いちばん下の不斉炭素の -OH／アミノ酸＝α炭素の -NH₂）。実装時に「いちばん下の不斉炭素」で一括りにしかけたが、スレオニン等は α炭素が下端でないので誤り。分けて書いた
      - テスト **ST30**: 必要な項目がそろっていることと、**モーダル全体の文言が R/S を断定する形**（`(R)-`／`R体`／「この炭素は R」／「…は R です」）を含まないことを両方固定する。導線側は mode/軸/向き・描画ベクトルの z（＝奥）・残り3つが中心に重ならない・パリティ不変・H 無しの中心で無効化まで検査
  - [x] **M2.5-A 重ね合わせビュー（ユーザー提案・2026-07-27）**（v299）— 立体異性体の比較で、ボタンを押すと**一方をシャドウ化してもう一方へ平行移動して重ね**、一致/不一致を示す。「重ね合わせられるか」という立体異性の定義そのものを操作で見せられる。
    **土台はほぼ揃っている**: (a) どの中心が食い違うかは既に計算できる（`canonicalStereoCode` の元になるパリティを中心ごとに比較するだけ） (b) くさび図の「並びを揃える」モードで**残った食い違いを枠で示す**表示を既に実装済み（`mismatchSlots`）。これを2分子の重ね合わせに広げる形。
    **設計の要点**: 2つの図の原子をどう対応づけるかは座標ではなく**正準ラベリング（グラフの同型写像）**で決めること。座標で対応づけると、描き方が違うだけで破綻する。平行移動は対応づけ後に重心を合わせればよい
    **実装（v299）**: 中核は chemistry.js の `stereoIsomorphismCompare(molA, stereoA, molB, stereoB)`（DOM非依存）。
    「A の正準割当を1つ固定し、B の正準割当（`canonicalRowsCore` の collect＝自己同型の個数だけある）を全列挙して合成する」ことで**同型写像を全部尽くし、一致数が最大の対応**を返す。だから「最もよく重なる対応でも食い違いが残る＝重ね合わせられない」と正確に言え、同じ分子なら必ず全一致になる（テスト ST27 で「全一致⇔同じ分子」を出題25問にわたり機械検証）。
    入口は立体異性体クイズの**解答後**（答えが透けるので解答前は出さない）。図Bを重原子骨格だけのゴーストにして図Aへ滑り込ませ（平行移動のみ・重心合わせ）、不斉炭素と C=C に一致=緑◯・食い違い=赤破線◯＋✓/✗ を付け、言葉でも「n個中k個が食い違い」と示す。
    座標・立体は**実際に描かれている図**（120°整形後の表示分子）から読む（「生成側の意図を信用しない」の方針どおり）。
    注意: 鏡像異性体でも分子骨格に対称性があると「最良対応での食い違い」は全中心より少ないことがある（例: 骨格対称な3中心 (+,+,−) の鏡像は最良対応で2中心一致）。解説の「すべての不斉炭素で逆」は鏡映対応での話なので、重ね合わせの文言は実測値（n個中k個）だけを言う設計にした
  - [x] **M2.5-B フィッシャー投影の操作学習（ユーザー提案・2026-07-27）**（v301）— 2つの立体異性体を並べ、画面操作で (i) 図全体を cw/ccw に回す (ii) 1つのC原子を選び、軸にする結合を選んで左右に回す。回した結果どうなるかを見ながら比較する。
    **実装（v301）**: 学習タブ「🔃 フィッシャー投影の操作練習」。操作は target 入出力の純関数
    `fischerOpRotate180` / `fischerOpRotate90`（90°＋回転後の左右入れ替え＝偶置換。実装は `rotateTargetInPlane(t, ±1, mirrorX=true)`）/ `fischerOpCycle`（軸の枝を固定して残り3枝のサブツリーを±90°/180°の剛体回転で巡回）で、
    どれも `applyVerifiedFischerOp` が**適用後の図から立体を読み直し**、canonicalCode / canonicalStereoCode が不変であることと原子の非重複を確かめてから確定する。壊れる操作（別の不斉炭素を含む枝を回す巡回など）は null になり、軸ボタンは事前の空振り実行で無効化して**UIに出さない**。
    お題は「同じ分子をかき混ぜた図（許可操作だけで生成＝必ず戻せる）」と「鏡に映した図（何回操作しても一致しない）」の2種。毎手、図から読み直した関係（同じ/鏡像）と手数を表示し、見た目が一致したら `drawingKey`（平行移動だけ無視した図の同一性）で 🎯 を出す。テスト ST28（90°単独＝鏡像の前提・3巡回×3=恒等・骨格を壊す巡回の棄却・UI不変量・お題2種の関係）。
    実機確認: 酒石酸で「もう1つの不斉炭素を含む枝」を軸にした巡回だけが無効化される／かき混ぜお題8題がすべて深さ2以内で解けることをBFSで確認（M2.5-C はこの操作系とタイマーだけで作れる）
    **部品は既にある**: 全体回転＝`rotateTargetInPlane`（立体異性体クイズの出題で使用）／原子ごとの軸選択回転＝3Dビューの回転軸切替と、くさび図の「残り3つを回す」（`cycleOthers`）。新しいのは**2D のフィッシャー図の上で対話的にやる**ことと、2つ並べて比較すること。
    **方針（2026-07-27 ユーザー指定）**: このモードで許す操作は**分子を変えない図上の変形だけ**にする。90°回転も「同じ分子を保ったまま」行えるようにする。
    根拠（置換の偶奇で決まる）: フィッシャーの4スロットを入れ替える操作のうち、**偶置換は同じ分子・奇置換は鏡像**になる。
      ・180°回転 = 上下と左右の同時入れ替え（転置2回＝偶）→ **同じ分子**
      ・1つ固定して残り3つを巡回 = 3-巡回（偶）→ **同じ分子**（くさび図の「残り3つを回す」で実装済み）
      ・2つだけ入れ替え = 転置1回（奇）→ **鏡像**（＝許さない）
      ・90°回転 = 上→右→下→左→上 の4-巡回（奇）→ **そのままだと鏡像**
    したがって90°回転は、**縦（または横）の2つの入れ替えを同時に行って偶に戻す**。これは「90°回すと縦横＝奥/手前の意味が入れ替わるので、同じ分子を保つには2つを入れ替える必要がある」ということで、フィッシャーの規約そのものを操作で見せられる。実測でも 90°単独＝鏡像／180°＝同じ、と確認済み（ST15）
    実装上は、操作のたびに図から立体を読み直し、**立体コードが変わっていないことを確かめてから確定する**（生成側の意図を信用しない、という既存の方針どおり）。変わってしまう操作はUIに出さない
    **教育上の要点**: フィッシャー投影は**90°回すと別の分子（鏡像）になる**ので、操作の結果を毎回読み直して「いま何を描いているか」を表示すること。回しても分子が変わらない3Dビューとの違いが、まさに学ぶ内容になる
  - [x] **M2.5-C 立体のタイムアタック（ユーザー提案・2026-07-27）**（v305）— お題の立体異性体と同じものを、操作で作るまでの時間・手数を競う（ルービックキューブ／マインスイーパー的）。**判定は `canonicalStereoCode` の一致だけで済む**ので中核は完成済み。必要なのは操作系（M2.5-B）とタイマー・手数カウント・記録。M2.5-B の自然な出口になるので、B → C の順に作るのがよい
    **実装（v305）**: 学習タブ「⏱ 立体タイムアタック」。`StereoTimeAttack extends FischerPractice`（B を要素ID接頭辞 `fp`→`ta` で継承し、出題・状態表示・タイマーだけ差し替え）。
    B の偶置換に加えて **奇置換の `fischerOpSwap`（2枝の入れ替え＝選んだ中心の立体だけが反転する、分子が変わる操作）** を追加。左右の入れ替えを試し、枝が重なるなら上下で代替。つながり方（canonicalCode）と中心の数の不変は検証する（枝サブツリーの180°点対称移動は、枝の中の別の中心のパリティを保つので、反転するのは選んだ中心だけ）。
    出題は「お題の中心を1〜n個反転させ、見た目もかき混ぜた図」（メソ体で打ち消されて同じ分子に戻ったら出し直す）。**完成判定は図ではなく分子**（`relationOf === 'same'`）で、図の向きが違っても完成になる——「同じ分子かどうかは見た目ではなく立体で決まる」がそのまま学習内容。
    出題範囲: 入門（1中心）/ 本格（2中心以上）/ すべて。自己ベスト（秒・手数）を localStorage `chemAssemblerTimeAttack` に分子ごとに保存。完成後は操作をロック、やり直しでタイマー仕切り直し。
    テスト ST29（入れ替え＝奇置換で鏡像・2回で戻る・多中心の1反転＝ジアステレオマー・M2.5-A の対応づけで違う中心を特定して反転→完成・記録保存・完成後ロック・やり直し）。実機確認: L-グリセルアルデヒド（鏡像お題）を1手で完成、タイマー・自己ベスト表示・完成後ロックを確認
  - [x] **M2.5 立体の学習コンテンツ その4**（DESIGN 8.9-(2) の残り）— 立体異性体の**総数当て**（v218・ST18）・**立体のみの書き出し練習**（v311・ST30）。総数当ての題材は「素朴な 2ⁿ が崩れる理由」が2通りあるので、対にすると良問になる:
    - **メソ体の畳み込み**（酒石酸: 2²=4 ではなく3）。M0 の ST1 で検証済み
    - **環の回転対称**（**乳酸3分子の環状エステル**: 2³=8 ではなく4）。ユーザー提供の題材（大学入試で出題例があるとのこと。年度・大学の出典は未確認）。9員環 [-O-CH(CH₃)-CO-]×3 で不斉炭素3個。環に3回回転対称があるため RRS・RSR・SRR が同じ分子（RSS・SRS・SSR も同じ）にまとまる。**既存の `canonicalStereoCode`（自己同型で最小化）が変更なしで正しく4種類に畳み込むことを確認**し、テスト **ST16** で固定した（1,1,3,3 のまとまり方・鏡像対応・アキラル体が無いことまで）。メソ体のために作った仕組みが環の回転対称にもそのまま効いた形
    - **書き出し練習の実装（v311）**: 学習タブ・アコーディオン「✏️ 構造の書き出し練習」内の「🪞 立体異性体の書き出し練習」（learn.js `StereoIsomerPractice`。IsomerPractice の流儀を踏襲）。お題は 2-ブテン(2種)・乳酸(2種)・**酒石酸(3種=メソ体)**・**乳酸3分子の環状エステル(4種=環の回転対称)**。お題の図をキャンバスへ置き、**つながり方は固定のまま置換基の付き方だけを動かして**全種を登録する。重複判定は `canonicalStereoCode` の一致のみ・目標数は `countStereoisomers`。答えの図は「立体1単位だけ反転する操作」（フィッシャー中心=`fischerOpSwap` 流用／環中心=環外置換基の枝の上下反転／C=C=片端の枝を軸で鏡映。どの操作も適用後の図から立体を読み直して検証）を 2ⁿ 通り合成して機械生成し、**種類数が countStereoisomers と一致することまで確かめてから**お題に出す。立体の読めない図（斜めに描いた不斉炭素・向きの読めない C=C）は理由を出して拒否。答え合わせで「①と③は同じ」（ダブり）と「鏡像＝B／鏡像＝自分自身（アキラル）」（変種ごと）を注記し、2ⁿ が崩れる理由をお題別の文で説明する。環状エステルのお題図は CH₃ 3つとも上（ホモキラル体）から始める＝**どの1中心を反転しても回転対称で同じ分子**になることが手で分かる。テスト ST30
  - [ ] **M2.5 の元メモ**（参考）— 立体異性体の総数当て（酒石酸が4でなく3になる）・立体のみの書き出し練習・**同一性/鏡像クイズ**（パリティ比較だけで判定でき CIP 不要）。R/S を名指しさせる問題は M3' 扱い
  - [ ] **R/S 表記（CIP）＋疑似3D連動の解説**（ユーザー要望「R/S を判定するなら解説で疑似3Dが欲しい」）— 3Dビューが揃ったので土台はある。CIP 規則の実装は重く誤りが致命的なので、**優先度は最後**
  - [ ] **フィッシャー投影の練習モード**（発展扱い。ユーザー見解「必ずしもマスターする必要はないが発展的に扱ってよい」）— 現状すでに「軸方向に描いたときだけ立体として読む」二段構えになっている
  - [ ] **二糖（スクロース・トレハロース）とフリップ表示**（ユーザー要望）— 前提として**グリコシド結合で二糖を作る手段**が要る。表示は既存モーフィング基盤に乗る
  - [x] **環糖の拡充（アルドヘキソース）**（Gemini によるデータ作成・v210。α/β-ガラクト/マンノは先行して投入済、アロ/グロを追加）— これで**10個の環状糖が同じ構造式（正準コード1種類）で、立体コードはすべて異なる**状態になった。P12-7 の設計（構造式は同じで立体だけ違うものを区別する）が実データで機能していることの実証でもある。検証: `verify-compounds.js` 合格（175件）／3D検査（環の置換基の面51個が作図と一致）／**フィッシャー配置からの独立検算**（アロ RRR・グルコ RLR・マンノ LLR・グロ RRL・ガラクト RLL、右→下の規約）で10件すべて一致。**依頼時の指示表は D-グロースの C3 を「上」と書いており誤りだった**（正しくは下）。Gemini が調べ直して訂正したものを採用
  - [ ] **フラノース（フルクトース）** — 五員環の糖。M2a の開鎖糖と対にする。**前提**: ハース環モジュールはピラノース（六員環）専用なので、五員環版のモジュールが要る。データだけでは足りない（＝そのままでは委託できない）
  - [ ] **ハース環テンプレートの手数削減** — C5 に -CH₂OH を前置きする案（現在は骨格のみで OH×5＋CH₂OH をユーザーが付ける）
  - [ ] **酒石酸の命名エントリ**（(R,R)/(S,S)/メソ）— メソ体の畳み込みは M0 の ST1 で検証済み。D/L↔(2R,3R) の慣用名の確証を取ってから投入
  - [ ] **可能な反応の判定を精査する — 設計方針**（2026-07-26 ユーザーとの検討）— ユーザー分析: 「代表的な分子はデータベースで対応できるが、汎用性を持たせるにはヒドロキシケトンのような**複数の官能基にまたがる反応機構**の把握と、**ルールがバッティングした際の解決策**が要る。立体構造や電子状態まで考慮すると**推定**も必要になる」。この見立てに沿った**現実的な設計**は次の三層構造:
    1. **確実層（データベース）**: 代表分子×反応の既知ペアを明示的に持つ（グルコースの環化のように、対象を限定して**登録済みの正解**を返す）。教科書の定番はここで100%正しく出せる。**教材としての信頼性はこの層が担保**する
    2. **規則層（現行の REACTION_RULES）**: 官能基パターンからの一般則。ここに**優先順位と排他規則**を持たせる（例: 第一級アルコールは「アルデヒドまで」と「カルボン酸まで」で条件が違う／分子内脱水は環化可能な鎖長のときだけ／芳香環の置換は空きがあるときだけ）。**バッティングの解決＝ルールに優先度と前提条件を明記する**こと。現状は detect が独立に候補を出すため、鎖状グルコースで酸化3種＋分子内脱水が同時に並ぶ
    3. **保留層（出さない）**: 判断できないものは**候補に出さない**のが正解。教材では「誤った反応を提示しない」ことが「多くの反応を提示する」ことより重要。推定に踏み込むのは、電子状態や立体を持たない現行モデルでは危険
    - **当面の実務**: (a) ルールごとに「適用条件・優先度・出さない条件」を棚卸しして表にする (b) 実行不能な候補を出さない（`aromatic_sulfonation` の「置換基を置く空間がありません」は detect 側で空間を見る） (c) 代表分子で「出るべき反応／出てはいけない反応」の期待値テストを作る（回帰の土台）
    - 将来: 電子状態（求電子・求核性）や立体障害まで扱うなら**別の設計判断**が要る。現行の「トポロジーのみ」モデルの外側なので、着手前に設計書を起こすこと
  - [ ] **可能な反応の判定を精査する（具体例・実装記録）**（2026-07-26 ユーザー指摘・**別件**）— `REACTION_RULES` の各 `detect` が「その分子で本当に起こる反応か」を洗い直す。出発点の症状: 鎖状 D-グルコースで `oxidize_primary`／`oxidize_secondary`／`oxidize_aldehyde`／`dehydration_intra` が同時に候補に出る／監査ログに `aromatic_sulfonation` の「置換基を置く空間がありません」が多発（**検出はするが実行できない**）。上の三層構造に沿って、**保留層（出さない）を4回に分けて実装**した:
    - [x] **① 判断できないものは出さない**（Opus・v197）— `dehydration_intra` の適用条件を「-OH が1つだけ・他の官能基なし」に限定。多価アルコール・糖・α-ヒドロキシ酸は対象外（高校で扱わないうえ、現行モデルでは正しい生成物を出せない）。期待値テスト **RX6** 新設（代表分子ごとに must／never を固定）
    - [x] **② 実行できないものは出さない**（Opus・v200）— 芳香族置換の `detect` で `attachGroup(..., dryRun)` により置換基を**置ける空間があるかを試す**。サリチル酸などで apply が例外を投げていた候補が消えた。テスト **RX7**（候補に出た反応は必ず実行できる）
    - [x] **③ 区別できないものは並べない**（Opus・v201）— `aromaticSiteClass`（位相だけの複製に目印を付けて `canonicalCode`）で「置換すると同じ生成物になる位置」を1クラスにまとめる。ベンゼン 6→1／トルエン・フェノール→3（o/m/p）／ナフタレン→2（α/β）／p-キシレン 4→1。テスト **RX8**。副産物: 既存の L5 が価標を `VALENCIES` で単純比較していてニトロ基の N（電荷分離形で4本＝仕様上の特例）を弾いていた誤りを修正（`isValencyValid` へ）
    - [x] **④ 先に反応する方だけを出す（酸化の序列）**（Opus・v202）— `alcoholOxidationAllowed` を新設し、アルコールの酸化は (i) 同じ分子に -CHO があれば出さない（-CHO の方が酸化されやすく先に反応する＝糖の還元性）(ii) アルコール性 -OH が2つ以上なら出さない（多価アルコール・糖の酸化は扱わない＝①と同じ線引き）。1級と2級のあいだには**序列を置かない**（高校では順序を扱わず実際にも同程度）。判定は**連結成分ごと**（キャンバスに2分子を並べる練習で隣の分子に消されないため）。実測: 鎖状グルコース 5→3ボタン（酸化はカルボン酸のみ＋環化2種）／グリセリン・エチレングリコール→0／乳酸は `oxidize_secondary` のまま。アルデヒド酸化の解説に「-OH もあるが -CHO が先」の一文を文脈依存で追加。テスト **RX9** 新設・RX6 に never を追加。全117テスト合格
    - [x] **⑤ 配向性（o,p-配向 / m-配向）**（Opus・v212。規則層の第一歩）— 全化合物にルールを当てて棚卸ししたところ、**芳香族置換で o/m/p を等しく並べていて、どれが主生成物かを何も言っていない**ことが最大の穴だった（配向性は教科書の中心的な話題）。`ringDirector` で環の置換基を「電子を押し込む基＝o,p-配向（-OH・-OR・-NH₂・アルキル・ハロゲン）」「電子を引く基＝m-配向（-NO₂・-SO₃H・-COOH・-COOR・-CHO・-CO-・-C≡N）」に分類し、`aromaticSiteRole` が選んだ位置を o/m/p と主・副で判定する。実行後の解説に「なぜその位置なのか」と「いま選んだのは主生成物か副生成物か」を添える。**断定するのは単環に置換基が1つのときだけ**（置換基2つ以上＝重ね合わせ、縮合環は何も言わない）。ハロゲンの「o,p-配向だが反応は遅い」例外も明示。あわせて `aromatic_deactivated_info`（電子を引く基が2つ以上＝置換が非常に起こりにくい環の注意）を追加。実測: トルエン/フェノール/アニリン/クロロベンゼン→o,p が主、ニトロベンゼン/安息香酸→m が主、ベンゼン・ナフタレン・サリチル酸→判断しない。テスト RX10 新設・全119テスト合格
    - [x] **⑥ 官能基の見分け（アミド・酸無水物）**（Opus・v213）— 棚卸しで見つかった2つの誤り。(1) `findFunctionalGroups` は「単結合だけで水素が残る N」を一律 `amino` とするため、**アミドの N もアミンとして扱われ、アセトアニリド（アニリンをアセチル化した生成物）をさらにアセチル化できてしまっていた**（尿素・アセトアミド・ε-カプロラクタムも同様）。`isAmideNitrogen` を新設し、反応ルール側で除外（分類そのものは変えない＝命名など既存の用途に影響しない）。パラセタモールはフェノールの -OH だけが対象に残る。(2) 酸無水物 -CO-O-CO- は形が -CO-O- で同じため `ester` として拾われ、**「エステルの加水分解・けん化」として説明されていた**。`isAnhydrideLinkage` で見分け、専用ルール `hydrolysis_anhydride` に分離（けん化とは呼ばない旨も明記）。生成物は実機で検算: 無水酢酸 C₄H₆O₃ → C₂H₄O₂ + C₂H₄O₂（酢酸2分子）／無水フタル酸 C₈H₄O₃ → C₈H₆O₄（フタル酸）。RX6 に9件・RX7 に3分子を追加して全119テスト合格
    - **方針の見直し（2026-07-27）**: 当初は「官能基クラスの語彙（糖・多価アルコール・α-ヒドロキシ酸などの複合クラス）を定義し、クラス単位で許す反応を宣言する」という抽象化を予定していたが、**これは作らないことにした**。④〜⑥で実際に見つかった誤りは「クラス分けの不足」ではなく**個々の官能基の見分けの粗さ**（アミドとアミン、酸無水物とエステル、-CHO と -OH の酸化されやすさ）だった。抽象化を先に作っても、当てるべき事実はこの粒度でしか出てこない。**続けるべき手順は「全化合物にルールを当てて棚卸し → 化学的に読んで誤りを見つける → その条件を該当ルールに書く」**（`scratchpad` の棚卸しスクリプトと同じことを、ルールを増やしたら再実行する）。抽象化が必要になるのは、同じ条件を3つ以上のルールに書き写すようになったとき
    - **残っている個別の論点**: フルクトース（-CHO なし・-OH 多数）は**現状 候補が0**になる。ケト糖の還元性（塩基性下でエンジオール経由でアルデヒドに異性化してフェーリング液を還元する）は高校でも扱うので、`⚠ info` で説明を出すのが妥当か検討する。また**確実層（代表分子×反応の登録済み正解のデータベース）**は、グルコースの環化以外にはまだ無い
  - [x] **立体ビューの入り口と上部リボンの2点**（Opus・v204。2026-07-27 ユーザー要望）
    - **中心炭素を選ばなくても立体ビューに入れる** — 「立体表示」ボタンが選択モードに入るだけだったため、立体を見る前に一手止まっていた。`autoCenter` を新設し、① 不斉炭素 ② 重原子の置換基が多い炭素 ③ 描いた順、の優先度で中心を自動選択して即開く。中心を選び直す経路はモーダル内の「⊹ 別の炭素を選ぶ」に移した（`startPicking`）。どの炭素を中心にしているかは「中心の炭素: 不斉炭素（他に sp3炭素が N 個）」と常時表示。sp3炭素が無い分子（ベンゼン等）は開かずに理由を出す。H1 と ST 系のヘルパー6箇所を新しい入り口に合わせて更新
    - **座標表示で右のボタン列が動く** — `.canvas-header` が `space-between` で、座標のテキスト幅が 60px〜276px と変わるためボタンが数〜数十px ずれていた。(a) 表示をスナップ後の格子座標だけにし、生のマウス座標は tooltip へ (b) デスクトップ（≥900px）で `justify-content: flex-end` ＋ 座標欄を `flex: 1 1 0`（＝余りを受け取るだけの欄）に。**flex-basis を 0 にするのが要点**で、auto のままだと横幅が埋まっているとき座標のテキストが幅計算に参加して 7px ずれが残った。実測: ボタンの left/width が座標の値によらず完全一致。幅が足りない画面では座標側が隠れる（ボタン優先）
  - [x] **環ビューを縦軸まわりに回せるようにする**（Opus・v205。2026-07-27 ユーザー要望「回転もできるとよい」）— 回転自体は図の上のドラッグで**すでにできていた**が、操作が見えないため気づけなかった。`⟲ 左へ／⟳ 右へ`（30°刻み・`nudgeRingYaw`）と現在角度の表示を追加し、ドラッグできることも tooltip に明記。30°刻みなのは六員環で「隣の炭素が正面に来る」1/12 ずつになり変化を追いやすいため。実測: 倒し角0°（ハース図の向き）から回すと六角形の横幅が 208→181→105px と狭まり、環が横を向いていく。`⟲ 向きを戻す` は倒し角と回転の両方を戻す。テスト ST13 に C-2 節を追加（角度表示・見え方の変化・同じ角度に戻したときの一致・一周で0°・回しても置換基の面は不変）
  - [x] **分子全体の立体ビュー M4a: 非環分子**（Opus・v207。2026-07-27 ユーザー要望）— `chemistry.js` に `buildMolecule3D`（DOM非依存）を新設。作図座標は**使わない**（直交格子＝結合角90°をそのまま立体にすると誤った模型になる）。つながりと立体記述子だけから、電子対の数で立体構造を決めて（三重→直線／二重→平面三角／他→正四面体。O・N は非共有電子対も数える）正しい角度で組み直す。**手性は `tetrahedralDirs` の方向セットを真の回転で親に接ぐことで自動的に保たれる**。ねじれ角は候補（単結合＝ねじれ形3通り／二重結合＝面が重なる2通り）から「既に置いた原子といちばん離れるもの」を選び、同点ならアンチ＝ジグザグ。加えて**不斉中心でない原子では大きい置換基ほど空いた方向へ**割り当てる（2,4-ジメチルペンタンの syn-ペンタン型の重なりはこれで解けた）。水素の結合長は 0.7（実測比 1.09/1.54）にしないと1,3位の水素が重なって見える。UI は4つ目のタブ「🧊 分子全体」で、描画は環ビューのパイプラインを流用。**対象外（環・シス/トランスのある C=C）はタブを無効化して理由を出す**。検証: ライブラリ全件で108分子が組め、結合長・結合角・原子間距離・**不斉炭素28個の手性一致**を機械検査（0件の問題）。テスト ST14 新設
  - [x] **分子全体の立体ビュー M4b（シス/トランス）・M4c（環）**（Opus・v209。2026-07-27）
    - **M4b**: C=C を渡るとき、面が重なる2通りのうち `readBondGeoFromCoords` の読み（syn/anti）と一致する方を選ぶ。ここは空き具合より優先する（幾何を取り違えると別の分子を見せることになるため）。この原子では置換基の入れ替えも禁止（入れ替えるとシス⇄トランスが反転する）。**描き分けていない C=C は組まない**（「⇄ シス/トランス整形で描き分けてから」と案内する）
    - **M4c**: 環は平面とみなし、**単環は正多角形に組み直す**。作図のハース六角形は遠近を出すため潰してあり、小さい環も正多角形ぴったりには描かれていないので、そのまま回すと結合長が最大1.8倍ばらつく模型になる。描いた向き（最初の原子の方角と回る向き）は保つので見た目は変わらない。縮合環（ナフタレン等）は作図が正六角形のまま並んでいるのでそのまま使う。環の原子の方向スロットは環の形から直接作り、sp3 なら残り2本を面の上下へ、**どちらに置換基を置くかはハース投影の面（面マーク or 縦位置）から決める**＝描いた図と一致する。sp2・芳香環は3本目を面内の外向きへ
    - **入り口も拡張**: sp3炭素が1つも無い分子（ベンゼン・ナフタレン・サリチル酸）でも「🧊 分子全体」だけを開けるようにした（`showWhole`）。くさび図・1炭素3Dのタブは理由つきで無効化する
    - 検証: ライブラリ171件中**169分子**が組め、結合長・結合角・平面性・**不斉炭素58個の手性**・**C=C 4本の幾何**・**環の置換基31個の面**がすべて描いた図と一致（問題0件）。組めない2件は「シス/トランスを描き分けていない C=C」で、これは仕様どおり。ST14 を拡張・H1 を更新して全118テスト合格
  - [ ] **分子全体の立体ビュー M4d（残り）**（未着手）— 環系が2つ以上ある分子（ビフェニル型）は鎖でつないだ先が合わないため未対応。いす形は P7-5-M3 のプリセットで別途— 現在の立体ビューは「1つの炭素まわり」の図。分子まるごとを立体で回すには全原子の3D座標が要る。**キャンバスの直交作図をそのまま3Dにすると結合角90°の立体になり、教材として誤り**なので、トポロジー＋既存のパリティ記述子から正しい角度で3D配置を組み直す。設計は [DESIGN_3d_correspondence.md](DESIGN_3d_correspondence.md) 6章に記載（M4a 非環 → M4b 二重結合の面 → M4c 環 → M4d 操作）。要点は「`tetrahedralDirs` が返す方向セットを**真の回転で親に接ぐ**ことで手性が自動的に保たれる」こと。描画は環ビューのパイプラインを流用できる。既定の配座はアンチ（ジグザグ）で、**代表例である旨の注記が必須**
  - 開発運用メモ: 同一フォルダで別アプリ（ratio・ion-equation）を並行作業すると**互いの変更を巻き込んでコミットする事故**が起きた（3回発生）。以後、並行作業は `git worktree` で分離し、コミット前に必ず `git status` でステージ内容を確認する
  - 関連: [DESIGN_3d_correspondence.md](DESIGN_3d_correspondence.md)（立体表示は判定に非影響／R/S はスコープ外という既存合意。M2「全体の標準くさび描画」は上記「くさび図の扱い」に統合、M3「疑似3D」は **P12-7-M3 で実装済み**）

### フェーズ13: SNS素材制作基盤（録画モード）（2026-07-27 方針合意。設計: DESIGN_recording_mode.md）

SNS展開（SNS_PLAN.md）の素材制作を半自動化する。アプリ本体の学習機能ではなく**制作ツール層**なので、
通常利用・回帰テスト・監査に影響を与えないこと（?rec= が無ければ完全に不活性）が絶対条件。

- [x] **P13-1 録画モード M1**（Fable・v229）— `?rec=<デモID>` で tutorials.json のデモを自動再生。
  rec.js 新設（パラメータ解釈・appReady とチュートリアルロードの待ち受け・`window.__recState`
  loading→playing→done/error の進行通知・クリーン画面クラス付与）。tutorial.js は最小注入のみ
  （`speedScale` を sleep/moveCursor に掛ける・`keepResult` で終了時の復元と後片付けをスキップし
  最終フレームに結果を残す）。パラメータ: speed（0.25〜4）/ cursor（mouse/touch/none）/
  caption（1/0）/ delay。クリーン画面はヘッダー・座標表示・JSON/まとめる/全消去ボタン・
  デモ終了ボタン・薄暗幕を隠し、パレットは残す（style.css の `.recording`）。
  検証: place-atom を speed=2 で完走（`__recState: done`・C₂H₆O が画面に残存・オーバーレイ撤去）、
  cursor=none/caption=0 のクラス適用、パラメータ無しでは recState undefined・ヘッダー表示のまま、
  を実機確認。**全126テスト合格**
- [x] **P13-2 M2**（Fable・v230）— demos.json 新設（tutorials.json と同スキーマ＋任意 `state`＝開始状態。
  rec.js が ?rec= のときだけ合流させる）。第1号は V1 台本の `intro-draw`
  （炭素3個→二重結合→ベンゼン環→OH→フェノール名称表示→誤配置C→Undo→全体表示）。
  `?format=short` で縦型収録向けのSNS字幕（大きく太く中央・縁取り）とモバイルUI隠し。
  tutorial.js に `opts.initialState` 注入（1行分岐）。
  ハマりどころ2件: (1) 分子があるとき離れた空きマスへの配置は棄却される（連結維持の仕様）ため
  「誤配置」の演技は**環に隣接するマス**（437,366）で行う (2) ベンゼンの頂点は非グリッド座標
  （六角形ジオメトリ）なので隣接セル座標は実測から取ること。
  検証: intro-draw が C₆H₆O→C₇H₈O（誤配置）→C₆H₆O フェノールで完走（実測タイムライン）、
  375×812 のモバイルビューポート＋format=short でも完走・クラス適用を実機確認。
  回帰テスト **N2**（demos.json 全デモの完走と結末検証）を追加し**全127テスト合格**
- [x] **P13-3 M3（収録自動化の第1段）**（Opus 5・2026-07-29）— `tools/record/record.mjs`（Playwright で
  `?rec=` を開き `__recState==='done'` まで録画）＋ `tools/record/mux.mjs`（ffmpeg で mp4 化・音声合成・拡大）。
  **V1「構造式、ノートに書く感覚で描ける」の実物 mp4 を生成（1080x1920 / 42.36秒 / H.264+AAC）**。
  実装で判明した3つの罠を仕様に反映済み:
  - **Playwright 同梱の ffmpeg は VP8 専用**で H.264/AAC を持たず mp4 を作れない
    → `pip install imageio-ffmpeg` のフルビルドを探して使う（管理者権限不要）
  - **viewport を 1080 幅にするとモバイル判定（max-width:899px）を超え、
    デスクトップ3カラムのまま縦に潰れる**
  - **Playwright の録画は CSS ピクセル基準**で、deviceScaleFactor を上げても録画解像度は上がらない
    → short は 899px 未満で最大の **810x1440 で収録し、ffmpeg（lanczos）で 1080x1920 へ拡大**
  - 音声との同期は現状 `--speed` の手動調整（V1 は 0.87 で映像42.32秒 vs 音声42.4秒）。
    **narration 文字数からの尺自動算出は未実装（次段）**
- [x] **P13-3b 構図アクション `frame`**（v292）— 台本から viewBox を張り直せるアクションを追加
  （`{ type:'frame', cx, cy, width }`。高さは fitCanvasToMolecule と同じ 4:3。座標省略時は作図中の分子の中心）。
  DESIGN_recording_mode.md §3 で予告していた演出用アクションの実装。**V2 の被写体が画面右寄り・小さい**
  （幅の35%・中心から+17%）問題をこれで解消し、中央・幅の54%へ再収録した。
  注意点2つ: **`#btn-clear-all` は fitCanvasToTarget で視野を戻す**ので消去の直後に `frame` を置き直すこと、
  **`#btn-reset-view` はお題基準**なので SNS 台本では使わない（構図が壊れる）。
  尺は収録ごとに±3秒ぶれる（実測 33.1〜40.5秒）ため、**音声より少し短く撮って mux の静止延長で埋める**のが安全。
- [x] **P13-3c クレジットの二重担保**（2026-07-31）— mux.mjs が meta の `credits` を読み、
  **映像の右下へ焼き込む**（`--credit` で上書き可）＋**投稿文 .txt の全媒体のチェックリストに出す**。
  VOICEVOX 利用規約は「利用したことがわかるクレジット表記」を求めるだけで媒体の例外も
  場所の指定もない ＝ **投稿する媒体それぞれで必要**。投稿文だけに頼ると媒体を増やすたびに漏れる
- [x] **P13-3d 頭合わせ・尻切り・貼るだけの投稿文**（v294・2026-07-31）— 毎日1本の運用に耐える形へ。
  - **`--trim=auto`**: 同じ台本でも**アプリのロードで「演技が始まるまで」が3〜4秒ぶれる**
    （演技のテンポ自体はぶれない）。最初の操作の `--lead`（既定1.5）秒前まで映像の頭を落として音と合わせる。
    効果音の位置も同じだけ手前へ寄せる。音声をずらす方式ではなく映像を詰める方式（ショートは頭が勝負）
  - **`--tail`**（既定1秒）: 喋り終わったあとに残る収録停止ぶんの無音を切る
  - 投稿文 .txt を**「ここから貼る」1ブロック**に再構成（ハッシュタグもクレジットも本文に同梱）。
    貼らない操作メモは別枠に分離
  - N2（SNSデモ完走テスト）を **demos.json の並び順に依存しない**形へ（動画が増えるたびに落ちない）
- [ ] **P13-3 の残り**: narration からのステップ尺自動算出・字幕焼き込みの選択肢・
  合成音声プレビューの一括生成（VOICEVOX / VOICEROID2+AssistantSeika の2系統）
- [ ] **P13-4 M4**: ion-equation / ratio へ共通規約で横展開

---

## 9. 本方針の変更

方針自体の変更もコミットとして記録する。ロードマップの完了チェック・項目追加は随時行ってよいが、第1〜7章のルール変更は理由をコミットメッセージに残す。
