# 図に印を重ねる（作図器の図 ＋ 印）—— 設計

課題台帳: **I-0082**（本体）／ **I-0081**（なぜ作図器ひとつにするか）／ **I-0083**（その先・装飾の道具立て）
起草: 2026-09-22（Assembler 統合）　決定者: ユーザー

---

## 1. なぜ要るか

**構造式は作図器（`:::figure` の `gen:`）で焼く。SVG で描き直さない**（I-0081・ユーザー決定
「作図方法は複数は必要ないかと思います」）。

ところが参考書の図は「分子を出すだけ」では足りず、**どこを見るかを指す印**が要る。
いまの作図器には印を重ねる口が無いので、2026-09-22 のレーンは**構造式ごと SVG で手描き**し、
作図器が持っている約束を全部落とした（`disaccharide-glycosidic-bond.svg`:
手前の太線もテーパーも無い／グリコシド結合が水平で上下が読めない／左端の `OH` が O でなく H に結合）。

＝ **印を重ねる口を作ることが、作図器ひとつに戻すための前提**。

### 測ったこと（2026-09-22）

- `reference-svg` 70枚で使われている部品: **囲み 42・矢印 43・破線 40・枠 29**（枚数）
- 手描きした構造式12枚に限ると: **囲み 9・破線 8・矢印 6・枠 2**
- 手描き12枚 = **759行**。`gen:` の図は原稿で **5行**（印を足して 6〜8行）
- 原稿の行数より効くのは**座標を考える手間**。svg-2b は9枚のうち**4枚を描き直している**
  （環が開いた・文字が線と重なった・番号が破線を食った・矢印と文字が近すぎ）

---

## 2. 取り下げた12枚が本当に要るもの（実測で3つに分かれた）

| | 枚数 | 要るもの | 状態 |
|---|---|---|---|
| ① 紙の図に H を出す | **1** | **NH₃・CH₄** … `gen: name=アンモニア paper plain expand=H` | ✅ **済**（v1608・`expand=H` を足した） |
| ② 元素を足す | **2** | **テフロン（F）・シリコーン（Si）** … 印は要らない | ✅ **済**（F v1607・Si v1610。2件を登録して `plastic-ptfe.png`・`rubber-silicone.png` を焼いた） |
| ③ 印が要る | **9** | 下の 2-a / 2-b | ⏭ 段1・段2 |

⚠ **①について統合側が一度読み違えた**（2026-09-22）。`--src=` の1枚焼きは `paper` と**明示しないと**紙の図に
ならない（原稿の `gen:` は既定で紙の図）。それを知らずに焼いて丸の図が返り、「型を変えれば済む」と報告した。
実際には**紙の図は水素を元素記号へまとめる**ので、`expand=H` を足す改修が要った。

### ★★ 紙の図は、ユーザーの言う作図の決まりを**すでに持っている**（2026-09-23 に確かめた）

| ユーザーの言葉 | 実装 |
|---|---|
| 糖類は環の手前を太線に、テーパーも入れる | `haworthFrontBondNode`（手前6/奥3・太さが違えば台形）。検査 **FR2**・**HQ8** |
| 価標は結合している原子につける（**I-0079 済**） | `drawPaperMolecule` の `atEnd`／「左端の原子は H を前に書く（H₃C−・HO−・H₂N−）」 |
| 官能基は価標が右に伸びる場合に左右を入れ替える（**I-0080 済**） | `PAPER_GROUP_REVERSED`（NO₂→O₂N・COOH→HOOC・CHO→OHC…）＋ `const rev = !!(anc && anc.x > a.x + 1)` |

しかも寸法は**教科書を実測して決めてある**（`quiz.js` のコメント。数研出版 R5化学 Vol.2 をベクタと画素で測り、
文字の高さ 0.42・線の太さ 0.028・二重線の間隔 0.125・**ハース式の手前の辺はふつうの線の 3.1 倍**）。

⇒ **手描きの SVG はこれを全部落としていた。** I-0081（作図器ひとつ）の根拠は、当初思っていたより強い。

### 2-a. 1枚に分子を複数並べる（3枚）

ケクレ式2つを両矢印で／鎖が環になる途中を矢印で／2分子を破線で結ぶ水素結合。
**分子の中の印ではなく、図の組み立て**。→ §5

### 2-b. 分子の中の原子・結合に印を付ける（6枚）

糖4（1位の −OH・不斉炭素4個・グリコシド結合の O・還元性の残る端）／
トルエンの o・m・p の文字／ポリエチレンの繰り返し単位の枠。→ §4

---

## 3. ★ どの原子かをどう書くか（この設計の核）

**原子 ID は乱数**（`chem-atom-id-hazard`）なので原稿に書けない。画素で書くのも駄目
——登録の座標を直したり型を変えたりするたびに印がずれ、**ずれても検査が赤にならない**
（図は焼けてしまうため）。

### 決定（ユーザー 2026-09-22）: **意味で指すのを主、位置番号を補助にする**

> 「2を主、1を補助で設計を起こしてください」

- **主: 化学の言葉で指す** —— `不斉炭素`・`グリコシド結合`・`カルボキシ基`。
  原稿が「**何を見せたい図か**」を書く形になり、座標も個数も人が数えない。
  分子を差し替えても印が付き直る。
- **補助: 位置番号で指す** —— `C2`・`C4`・`C6`。意味で言えないもの
  （トルエンの o・m・p ＝ 環の2位・4位・6位）に使う。

### 使える語彙（**すべて実装済みの判定を借りる。新しい化学判定を書かない**）

| 書き方 | 借りる判定 | 例 |
|---|---|---|
| 官能基名 18種 | `findFunctionalGroups`（`aldehyde` `amide` `ammonium` `anhydride` `carboxyl` `carboxylate` `carboxylate_ion` `diazonium` `enol` `ester` `ether` `halide` `ketone` `nitrile` `nitro` `phenol` `sulfo` `sulfonate`） | `at=カルボキシ基` |
| `不斉炭素` | `mol.isAsymmetricCarbon(atomId)` | `at=不斉炭素` → **当たった全部**に付く |
| `グリコシド結合` | `haworthRingBridge(mol, cycleA, cycleB)` → `{ atom, hostA, hostB }` | `at=グリコシド結合` |
| 糖の環の位置 | `haworthCarbonNumbers(mol)`（★ **選んだ炭素に依らず同じ番号**） | `at=C1` `at=C1-OH` |
| 主鎖の位置 | `findLongestCarbonChain` ＋ 既存の番号付け（`numbered` が描いているもの） | `at=C2` |
| 芳香環 | `findAromaticBondKeys` | `at=ベンゼン環` |
| 環の酸素 | ハース環の環内 O | `at=環の酸素` |
| 環（2026-09-24） | `_ringAtomIds` ＋ `_ringSystems`（独楽回転・ハース判定と同じ環の取り方）。縮合環はひとかたまりで1つ | `at=環` → ビニロンの六員環を囲む |
| 環の水素の種類（2026-09-24） | `findAromaticBondKeys` の環の炭素のうち H が残るもの ＋ **正準コードと同じ WL のクラス**（`_heavyAtomRanks`）。同じクラスに同じ文字 a・b・c（`label=` は要らない）。`kinds=` で種類の数を確かめる（合わなければ赤） | `mark: kind=文字 at=環の水素の種類 count=3 kinds=2`（1,2,3-トリメチルベンゼン） |

⚠ **語彙を増やすときは、必ず既にある判定に対応づける。**
印のために化学の判定を新しく書くと、アプリの判定と2本になって食い違う
（`compounds.json` を書き戻すのと同じ型の事故）。

### 当たらなかったときは赤

`at=` が **0個に当たったら赤で止める**（`gen-figure.mjs` が落ちる）。
⚠ ここを黄にすると、**印の無い図が黙って焼ける**。§1 の事故がまさにそれ
（図は出るので、見ている人は「そういう図なんだ」と思う）。

期待する個数を書けるようにする: `at=不斉炭素 count=4` —— 4個でなければ赤。
★ **数が変わったことに気づける**のが狙い（登録の座標を直したときなど）。

---

## 4. 印の種類（測った4つ＋文字）

原稿に `mark:` を1行1つ、`:::figure` の中に何本でも。`gen:` と同じ `key=value` の並べ方。

```
:::figure
src: disaccharide-glycosidic-bond.png
gen: name=マルトース（麦芽糖） haworth
mark: kind=囲む at=グリコシド結合 label=グリコシド結合
alt: マルトースの構造式で、2つの環をつなぐ酸素だけが丸で囲まれている
caption: 2つの環は**酸素1個をはさんで**つながっています。
:::
```

| `kind=` | 何を描くか | 使う図 |
|---|---|---|
| `囲む` | 原子・結合を丸/楕円で囲む（破線・色つき） | 糖4枚 |
| `枠` | 範囲を四角で囲む | 繰り返し単位 |
| `文字` | 位置に短い文字を置く | トルエンの o・m・p |
| `破線` | 2か所を点線で結ぶ | 水素結合 |
| `矢印` | 2か所を矢印で結ぶ／指す | 環になる途中 |

共通のキー: `at=`（どこに）・`to=`（`破線`/`矢印` のもう一方）・`label=`（添える文字）・
`color=`（既定は紫 `#6a1b9a`。⚠ **色だけに意味を持たせない**＝ 形か文字でも分かるように）

### 位置は作図器が決める

`label=` の置き場所・囲みの大きさ・矢印の曲げ方は**作図器が計算する**（人が座標を書かない）。
⚠ ここを原稿に書かせると、§1 の「描き直し4枚」がそのまま戻ってくる。

---

## 5. 1枚に分子を複数並べる（2-a）

`gen:` を複数行書けるようにし、間をつなぐものを `between:` で書く。

```
:::figure
src: benzene-kekule-resonance.png
gen: name=ベンゼン circle plain kekule=1
gen: name=ベンゼン circle plain kekule=2
between: kind=両矢印 label=実際はこの中間
:::
```

- 並べ方は**横一列**が既定（`between:` の向きで縦にもできる）
- 大きさは作図器がそろえる（**本のどのページでも結合の長さを同じにする**＝ `gen-figure.mjs` の
  既存の決まりをそのまま使う）
- `between: kind=破線 at=1:フェノール to=2:ニトロ基` のように、**何番目の分子か**を `1:` `2:` で指す

⚠ `kekule=` は**この設計で新しく要るもの**（ケクレ式2つを描き分ける）。
ほかは既存の `gen:` のキーで足りる。

---

## 6. 何を機械で見張るか

| | 検査 | なぜ |
|---|---|---|
| 赤 | `at=` が0個に当たった | 印の無い図が黙って焼ける（§3） |
| 赤 | `count=` と実際の数が違う | 数が変わったことに気づけない |
| 赤 | `kind=` が表に無い | 綴り違いが黙って無視される |
| 赤 | `to=` の要る `kind` に `to=` が無い | |
| 黄 | 印が図の枠からはみ出した | 焼いた絵を人が見る前に気づく |
| 黄 | 印どうしが重なった | 同上 |

⚠ **`gen-reference.mjs` は既に「原稿から生成し直して1バイト一致」を見ている**（REF17・規則10）。
`mark:` はその中に入るので、原稿を直して焼き直さないと赤になる ＝ **生成物だけ直せない**。

---

## 7. 段取り

| 段 | 中身 | これで焼ける図 |
|---|---|---|
| **0** ✅ | 紙の図に `expand=H` を足す（v1608）。⚠ 当初「`circle plain` で済む」と書いたのは誤り（§2 の訂正） | NH₃・CH₄ = **1枚** 済 |
| **0b** ✅ | **元素を足す** … **F**（v1607）・**Si**（v1610）とも済。どちらも全1,171件の正準コードほか差分0で確認。テフロン・シリコーンを登録して焼いた | テフロン・シリコーン = 2枚 済 |
| **1** ✅ | `mark:` の `囲む`・`文字`・`枠` ＋ §3 の語彙 ＋ §6 の赤4本（v1609〜v1610） | 糖4・トルエン・繰り返し単位 = **6枚** 済（糖は α/β・マルトース/スクロースが2枚ずつで計8枚） |
| **2** ✅ | `gen:` の複数行 ＋ `between:`（`両矢印`・`破線`・`矢印`）＋ `kekule=`（v1610） | ケクレ式・環になる途中・水素結合 = **3枚** 済 |

✅ **2026-09-23 に全段が済んだ。取り下げた12枚はすべて作図器で戻った**（公開 v1610・全941テスト合格）。`//図:` は 35 → **18**。
発注書 `brief-ref-svg-figures.md` の例外も消した（I-0081）。

⚠ 段2 の便が見つけた**段1の不具合**を統合で直した（**I-0098**）: 主鎖の番号 `C<n>` が `findLongestCarbonChain` の並びのままで、
**どちらの端から数えるか決まっていなかった**（鎖状グルコースで `C5-OH` が実際の2位に当たった）。**1個には当たるので「0個なら赤」で止まらず、黙って外れた。**
⇒ 命名の番号（`iupacNameDetail().mainChain`）に合わせ、**命名できない鎖では向きで変わる指し方を赤で止める**（検査 FGT7）。
⚠ 実測: `iupacNameDetail` は**鎖状グルコースに主鎖の番号を付けない** ＝ グルコースで `C<n>` を使うと赤になる（推測しないのが正しい）。糖は意味（`不斉炭素`・`環の酸素`）か糖の環の番号で指す

⚠ **0b は元素だけでは足りない。** テフロン（ポリテトラフルオロエチレン）もシリコーンも
`compounds.json` に**登録が無い**ので、登録を足すまで `gen: name=…` で呼べない。
⚠ **Si は F と違う**（4価・ハロゲンでない）ので別便にする。F は I の写しで済んだが、
Si は正準コード・異性体の数え上げ・命名への効き方が広い。

⚠ 段1 と段2 は**別の便に分ける**（段1 は分子の中・段2 は図の組み立てで、触る所が違う）。

★ 段1 が終わった時点で、**発注書 `brief-ref-svg-figures.md` §3 の例外
（「分子＋印は SVG で描いてよい」）を消す**（I-0081）。消さないと同じことが起きる。

---

## 8. この先（I-0083・急がない）

ユーザーの言葉:
> 作問者や講義者が、説明上使いたい装飾ができるようにしたい（ppt のグラフィックのイメージ）
> 急ぎませんが、挿絵などをユーザーが作りたいときに意図通りに表現できることを目指したい

この設計の `mark:` は**原稿に文字で書く**形だが、その先は**画面で置く**形になる。

⚠ **設計の分かれ目**: 参考書の図は「原子に結びつけて置く」（焼き直しでずれないため）、
人が挿絵を描くときは「置きたい所に置く」。両立させるなら、
**原子の上に落としたら結びつき、そうでなければ自由位置**にする（ppt で図形を要素にスナップ
させるのと同じ感覚）。§4 の `at=` は前者そのものなので、後者を足す形で広げられる。

⚠ **巻き矢印は `reaction.js` に既にある**（反応機構のアニメ）。二重に作らない。

---

## 9. 立体の図 —— くさび図とフィッシャー投影（I-0104・2026-09-23）

立体異性体のページの2枚（乳酸の鏡像の1対・酒石酸の3種）が `//図:` のまま残っていた。
**作図器ひとつ（I-0081）のまま**、`gen:` に口を2つと、`mark:` に印を1つ足した。どれも**アプリの描画を呼ぶだけ**。

| 書き方 | 呼ぶアプリの描画 | 紙面の色への替え方 |
|---|---|---|
| `gen: name=D-乳酸 stereo=mirror`（`stereo=wedge` は1つだけ） | `stereo.js` の StereoView（「🧊 立体で見る」）。`openAuto` で開き、`setWedgeMirror(true)` ＝「🪞 鏡像と並べる」。描き上がった `#stereo-svg` を写して撮る | `gen-figure.mjs` の `PAPER_CSS` の `svg.figbake-stereo` の段（白 → 墨・鏡の破線 → 灰・字は紙の図の書体）。index.html の暗い台紙（style 属性）は写しから外す |
| `gen: name=酒石酸 fischer=RS paper condense=COOH` | 紙の図の型（`renderStandardFigure` → `drawPaperMolecule`）。図は `fischerOpRotate90/180`・`fischerOpMirror`（フィッシャーの練習・タイムアタックの操作）で作り、`assignRSDescriptor` の読みで選ぶ | 既存の紙の図の型そのまま。足したのは `opts.fischer` ＝ **不斉炭素の H だけ**を線で描く（十字の4本目） |
| `mark: kind=対称面 at=3:不斉炭素 count=2 label=対称面` | `drawFigureMarks`（`quiz.js`）に種類を1つ足した。at= の2か所の真ん中を直角に横切る破線 | 印の既定（紫） |

- **`fischer=` は R・S で指す。** アプリは酒石酸の D/L を断定しない（`assignDLDescriptor` が「頭の候補が2つで割れる」として null）ので、
  原稿は上から R・S を並べて書き、D形・L形の呼び名は caption が持つ（L-(+)-酒石酸 ＝ (2R,3R)）
- **対称面は引く前に確かめる。** 描いた図の原子を線で折り返し、同じ元素・同じ幅の文字が同じ所に無ければ赤
  （D形・L形に対称面を引けない）。★ 化学の判定（メソ体か）ではなく**図の幾何**で見ている ＝ 新しい化学判定は書いていない。
  メソ体かどうかは作図器が `readStereoOf` の `stereoCode === mirrorCode` で出力に書く（`・メソ体`）
- ⚠ くさび図の中心の上下左右は**登録の図の並びのまま**。主鎖を縦に描いた登録（`D-乳酸`・`L-乳酸`）を名指すと COOH が上になる
- ⚠ くさび図に `mark:` と横並び（2行目の `gen:`）はまだ載らない（立体ビューの絵は紙の図の型を通らないため）
