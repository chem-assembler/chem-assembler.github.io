# DESIGN_review_pack2.md —— v1476 → v1495（20版）の定期レビュー（差分レビュー＋監査ページの長時間実行）

**状態: レビューのみ。コード・既存設計書・データ・版番号は1バイトも変えていない。**（2026-09-02・audit-review レーン）

**対象**: `git log d350b78..HEAD -- assembler/`（89 コミット・14 ファイル・+7,370 / −498 行）。
⚠ **このレビューは v1495（`90ba424`）時点のもの。** 執筆中に統合セッションが v1496（`iupacAlkylName` に「置換基数が最多」を足す直し・
`DESIGN_isomer_practice.md` §19-6c が「直した」に・`ID11` の架け替え・`AK10`／`AK11` 追加・出典は A-2.3(a) でなく A-2.6(a)）を取り込んでいるが、
**ここには取り込んでいない**（差分は1関数と検査3本）。§3・§5-4 の `ID11`／§19 に関する記述は v1495 の姿。
主な中身: 反応（加硫の寄せ・-S-S-／ビニロン／開環重合／切る反応の印の束ね）・図（整形でカルボキシ基の O を外へ／
トレハロース／糖の縮合の重なり直し／ε-カプロラクタムの潰した環）・出題（種類数を伏せる・ヒント5段・分類つきお題15件・
C₇ の名前83件）・画面（実験モードの器・`?open=narrowing`・主鎖と番号・鏡像を重ねる）・検査（CV1〜3・ST26）。

**凡例**（すべての指摘に出典を付ける）:
- 【現物】= このレビューがリポジトリの現物（コード・データ）を読んで確かめたこと（`ファイル:行`）
- 【実測】= このレビューが今日、実アプリを headless Chromium で動かして測ったこと（スクリプトは scratchpad・リポジトリには入れていない）
- 【記述】= 設計書・DEVELOPMENT.md に書いてあること（節番号／行）
- 【意見】= このレビューの判断

---

## 0. 要旨（先に結論だけ）

1. ★★ **印（`changed`）の穴が 3 本残っている**（§1）。**49 本すべて**の反応を「結合が変わった原子の集合 vs `changed`」で機械的に突き合わせた【実測】。
   **渡し落とし**: `diene_polymerization`（**中央へ移った C=C の炭素 6 個に印が無い** —— caption が「二重結合は中央へ移っています」と言っている当の原子）／
   `vulcanization`（C=C が単結合になった相方の炭素 2 個）／`iodoform`（-OH → =O になった酸素）。
   **渡しすぎ**: `acetalization_pva`（結合が1本も変わっていない cA・cB に印）。
   ⚠ **`CV1` はこのどれも捕まえない**（分子が分かれた反応しか見ない・§3-1）。
2. ★★ **監査ページのファズは、この20版で足した反応に 1 回も届いていない**（§4-3）。200 シード×80 操作＝16,000 操作で、
   **49 本中 16 本の反応が 0 回**（重合4本・加硫・ビニロン・開環重合・縮合重合・糖の縮合／加水分解・酸無水物2本・ワッカー・環化／開環・活性化環の臭素化）。
   「react」を引いた回の **45% はボタンが無くて空振り**、押せた回の **38% は「🔍 反応の前後を見る」**（反応ではなく比較画面）。
   ＝ ★ **ファズは「反応で図が壊れないか」の門番として、いま足している反応を1本も見ていない。**
3. ⚠ **監査ページのファズ失敗率は 14.2%**（300反復・CI 10.9〜18.4%）で、**中身は「結合線の下の自動水素」37 件・「結合線の下の原子」15 件が9割**（§4-2）。
   ★ v1476 と同じ種 300 個で A/B した結果、**43 件 vs 49／52 件（同じ版の再走の差が 3）で区間が重なる ＝ 退行ではない**（§4-2b）。
   この率は v1240 で「伸縮・モジュール・反応の実行の経路はまだ塞いでいない」と意図して残した赤（`DESIGN_hit_areas.md` §10-7）。
4. ★ **加硫の橋は「C-S-S-C が一直線」が 1,3-ブタジエンでは成り立たない**（§2-1【実測】: S での折れ **155°／159°**、`VL1` の物差し ±20° を割る。
   イソプレン 179°・クロロプレン 168〜179°）。原因は足場の炭素が**格子に乗っていない**のに席だけ格子へ丸めること（`stackChainsForBridge` と `vulcanizablePairs` の両方）。
   `VL1` はイソプレンしか回さないので緑。**実害は小さい（線が原子をかすめてはいない）が、設計の断定と実物がずれている。**
5. ★ **空振りしうる検査は 4 件**（§3）。いちばん重いのは **`EX4`「履歴も1つも変えない」と名乗って `g.history` を一度も読んでいない**こと。
   次に `IH1` の「隠す4か所」のうち3か所が旧文言 `全 10 種` の一致だけで守られていること。
6. ⚠ **設計書の古い記述は 6 文書・約 25 か所**（§5）。**ユーザー決定と逆の推奨が立ったまま**のものが 6 か所
   （`DESIGN_experiment_mode.md` §3-4／D-E1／D-E3／D-E4、`DESIGN_sugar.md` §8-①／§8-③）。
   コードを直すべきものは **1 行**（`reactor.js:4064` の caption にトレハロースが無い）。
7. ○ **確かめて問題なかった範囲**は §7 に列挙（回帰 707 件全合格・qa 94 件全合格・`verify-release`／`verify-compounds` 合格・
   ライブラリ 1,145 図の自動作図 0 件・画面サイズ／当たり判定 0 件・`planAttachment` 系の互いの前提・切る反応 14 本の印）。

---

## 1. 軸1: 「直した」ものが本当に直っているか（印まわり）

### 1-1. 直した4件は直っている【現物】【実測】

| 直し | 版 | 現物 | 確かめ方 |
|---|---|---|---|
| 持ち越し（前の反応の印が次の分子に乗る） | v1477 | `game.js:10402-10433`（`data-hl-atom` を印に持たせ、`dropStaleHighlights` が `updateDrawing` ごとに消す） | `RV3`／`RV4`（`tests.js:44068`・`44114`）が緑。印は `[data-hl-atom]` で引くので DOM の実物を見ている |
| 渡しすぎ（活性化環の臭素化で -OH の付け根に印） | v1480 | `reactor.js:4695-4701`（`anchor` を入れない） | `RV11`（`tests.js:44519`）＋ §1-2 の悉皆で `bromination_activated_ring` は **missing 0・extra 0** |
| 渡し落とし（重合の両端の炭素） | v1481 | `reactor.js:2656-2665` `attachREnds`（R と付け先の両方を返す） | `RV12`（`tests.js:44586`）＋ 悉皆で重合4本（付加・アセチレン・縮合・開環）は missing 0 |
| 渡し落とし（切る反応の切り離される側） | v1490 | `reactor.js:801-816` `cleaveAcylOxygen`（`changed: [cId, o.id, oId]`） | `CV1`〜`CV3`（`tests.js:44794`〜）。全走の注記「見張った 49/49 本（分かれた反応 14 本）」を今日の全走で確認 |

### 1-2. ★★ 同じ形の穴が他に残っていないか —— 49 本を機械で突き合わせた【実測】

**方法**: 各ルールに題材（`CV1` と同じ拾い方＋相手が要るものは `CV_PAIR_SAMPLES` と同じ組）を置き、`detect` → `apply` の前後で
**重原子ごとの「隣の原子 id ＋ 結合次数」の集合**を取り、変わった原子・新しく生えた原子（脱離した水 `fromReaction` は除く）の集合を
`result.changed` と突き合わせた（印の意味は「何が変わったか」＝ `reactor.js:4697` の原則をそのまま物差しにした）。
49 本＋別の枝 4 件（ヨードホルムの -CH(OH)- 側・アルキンの水和・末端／環の酸化開裂）。

**結果: 44 本は missing 0・extra 0。次の 5 本がずれている。**

| # | ルール | 種類 | 中身 | 出典 | 重さ |
|--:|---|---|---|---|---|
| **1** | ★★ `diene_polymerization` | **渡し落とし 6 個** | 各単量体の **C2・C3**（`b23.type = 2` で中央へ移った二重結合の両端）に印が無い。印が付くのは繋ぎ目（C4–C1）と両端だけ。⚠ caption 自身が「**二重結合は両端から中央へ移っています。ここが付加重合との違い**」と言っている当の原子に印が無い | 【現物】`reactor.js:4282-4289`（結合の書き換え）・`4291-4323`（`changed` は繋ぎ目と `endIds` のみ） | ★★ 直す。v1481 の「端の炭素」と同じ「変化は大きいのに印が無い」形で、しかも教材の要点そのもの |
| **2** | ★ `vulcanization` | 渡し落とし 2 個 | C=C が単結合になった**相方の炭素 `ca2`・`cb2`** に印が無い（`ab.type = 1; bb.type = 1` で両端とも変わる）。同じ「多重結合を1本減らす」`addAcrossMultipleBond` は `[id1, id2, ...added]` と**両端を入れている**（`reactor.js:2820`）ので、加硫だけ流儀が違う | 【現物】`reactor.js:4369-4370`・`4388` | ★ 直す（1行）。急がない |
| **3** | ★ `iodoform` | 渡し落とし 1 個 | CH₃-CH(OH)- の場合、-OH の酸素が C=O になる（`getBond(kId, oh).type = 2`）が `changed` に無い。メチルケトンの場合は起きない | 【現物】`reactor.js:3715-3722`・`3741` | ★ 直す（1行）。急がない |
| **4** | ⚠ `acetalization_pva` | 渡しすぎ 2 個 | `cA`・`cB`（-OH をぶら下げていた主鎖の炭素）は結合が1本も変わっていないのに印。同じ六員環の `cMid` には無いので「環になった原子」という読みでも一貫しない。v1480 で外した `anchor` と同じ形 | 【現物】`reactor.js:4432` | ⚠ 直す（1行）。急がない |
| **5** | ○ `condensation_glycoside`／`dehydration_anhydride` | 渡しすぎ 各 1 個 | 橋になった -OH を**持っていた側の炭素**（`site[1]`＝アノマー炭素／`cA`）は結合が変わらない。ただし「1位を使った」を示す意図が caption に書いてある（`reactor.js:4062`）ので**残してよい** | 【現物】`reactor.js:4072`・`4959` | 放置でよい（意図を注記するなら1行） |

⚠ **`CV1` はこの 5 本を1つも捕まえない。** `cvUnmarkedProducts` は `split`（分子が 2 つ以上に分かれた）成分だけを見る
（【現物】`tests.js:44760-44771`）ので、**分子が増えない反応の「変わったのに印が無い／変わっていないのに印がある」は見張られていない** —— 発注書の推測どおり。
★ 上の方法（隣の id＋結合次数の集合の差分）は `CV1` の隣にそのまま置ける（§8 の発注書 A）。

### 1-3. ついでに見つけた「赤くなる側」の弱さ（空振りではない）

- `parkAsWater` は置き場が無いと **`fromReaction` を付けずに戻る**（【現物】`reactor.js:118` の `return` が `124` の `o.fromReaction = true` より前）。
  そのとき `CV1` の水の除外（`cvIsLeavingWater`・`tests.js:44750-44754`）が効かず、**水が「印の無い生成物」に数えられて赤くなる**。
  ⚠ 緑にはならない（安全側）が、赤の文面「印の無い生成物」が事実と違うので読み違える。放置でよいが、`CV1` が赤くなったときの読み方として覚えておく。

---

## 2. 軸2: 幾何の直しが互いに壊し合っていないか

### 2-1. ★ 加硫: 「C-S-S-C が一直線」は足場が格子に乗っているときだけ【実測】

**手順**: V130 と同じ道（単量体 ×N → 1,4-付加重合 → 単量体 ×N → 重合 → 加硫）を `summonMolecule` → `detect` → `apply` で流し、
S の席・折れ角・「結合線の下の原子」（アプリの `atomUnderBondLine`）を測った。

| 単量体 | N | 足場 ca が格子上か | ca | S1 | S2 | cb | C-S-S 折れ角 | S-S-C | 結合線の下の原子 |
|---|--:|---|---|---|---|---|--:|--:|--:|
| イソプレン | 2 | **×**（463,336） | (463,336) | (462,252) | (462,168) | (462,84) | 179° | 180° | 0 |
| イソプレン | 3 | × | (631,409) | (630,504) | (630,588) | (630,672) | 179° | 180° | 0 |
| **1,3-ブタジエン** | 2 | **×**（526,336） | (526,336) | **(546,294)** | (546,252) | (546,210) | ★ **155°** | 180° | 0 |
| **1,3-ブタジエン** | 3 | × | (694,409) | **(714,462)** | (714,504) | (714,546) | ★ **159°** | 180° | 0 |
| クロロプレン | 2 | × | (547,373) | (546,462) | (546,546) | (546,630) | 179° | 180° | 0 |
| クロロプレン | 3 | × | (694,409) | (714,504) | (714,588) | (714,672) | 168° | 180° | 0 |

【現物】原因は 2 か所が「足場は格子に乗っている」前提で書かれていること:
- `stackChainsForBridge`（`reactor.js:2541-2546`）: `mx = round(ca.x / G) * G` を席の x にし、**ca 自身はその x へ動かさない**。
- `vulcanizablePairs`（`reactor.js:2166-2170`）: 1/3・2/3 の点を `round(・/G)*G` で格子へ丸める。
- 注記（`reactor.js:2508-2511`）は「`round(1/3 の点/G)*G` … が s1・s2 そのものになるので **C—S—S—C が一直線**」と断定。
- 実物: 重合の生成物は `planAttachment`（`reactor.js:287-288` `anchor.x + G cos θ`）で相対に置くので、**呼び出した分子の位置しだいで格子から外れる**（6 例中 6 例とも外れていた）。
  イソプレン・クロロプレン（刻み 84px）は 1〜20px のずれが 84px の橋では 1〜12° に収まるが、**ブタジエン（刻み 42px）は 20px のずれが 42px の橋で 25° になる**。

【意見】**実害は小さい**（`atomUnderBondLine` は 0 件・原子の詰まりも無い）。ただし
- `VL1`（`tests.js:25868-25872`）の物差し「180° ± 20°」を**ブタジエンは割る**のに、`VL1` はイソプレン 2+2 だけ（`tests.js:25775`）を回すので緑。
  ★ **VS1／VL1 に単量体をもう1種（ブタジエン）足せば、そのまま赤くなる**（検査の空振りの一種・§3）。
- 直し方は 2 通り: (a) `stackChainsForBridge` で **ca の x をそのまま席の x にする**（`vulcanizablePairs` 側の丸めが 1/3・2/3 を同じ点に落とすかは要確認）
  (b) 寄せるときに **動かす側を「ca の格子ずれぶん」も含めて動かす**。⚠ どちらも v1484 の「剛体平行移動だけ」の約束は守れる。急がない。

### 2-2. 互いの前提を踏んでいないか【現物】

この20版で座標を触った 5 か所と、それが依存する共通の道具を並べた:

| 直し | 座標を動かす主体 | 共通の道具に触ったか | 他の直しとの接点 | 判定 |
|---|---|---|---|---|
| 加硫の寄せ（v1484/1486/1487） | `stackChainsForBridge`（`reactor.js:2515-2572`。剛体平行移動のみ・置けなければ座標を戻す `2354-2358`） | **`planAttachment` には触っていない**（注記 `2499-2501`。呼び出し 6 か所 `912-923, 3990, 4156, 4224, 4301, 4584` はすべて重合・縮合側で、加硫は呼ばない） | 重合 → 加硫の順にしか起きない。加硫後は `moving.has(caId)` で二度と寄せない（`2520`） | ○ |
| 糖の縮合の描き直し（v1477） | `redrawProductsAsStandalone`（`game.js:8944-9035`。`only` で縮合してできた成分だけ・`escape`/`overlaps` は加水分解と同じ `componentOverlaps`/`separateComponent`） | 加水分解が使う同じ関数を**逆向きに使うだけ**。`alignRow`／`arc`／`anchor` は加水分解の経路にしか渡していない（`reactor.js:4047-4052` vs `5080-5127`） | `applyGlycosidicCondensation` が先に `flipHaworth` することがある（`reactor.js:689-695`）→ 描き直しは指紋 `haworthStereoFingerprint` が一致するときだけ本物へ写す（`game.js:8993`） | ○（`RV5`/`RV6` が縮合側、`SG18` 系が加水分解側） |
| 整形でカルボキシ基の O を外へ（v1483） | `_orientCarbonylsOutward`（`game.js:5077-5135`。カルボニル炭素より先だけを 90°/180° 回す） | `reshapeDoubleBond` の末尾（`5046`）に足しただけ。呼び出し側は `reshapeVinylAngles`（呼び出し時・`7953`）と `handleReshapeTap`（`4955`）の 2 本で、どちらも通る | 立体コード（フィッシャー・ハース込み）が変わったら戻す（`5121-5130`）／貫通が増えたら採らない（`5129`）／もともと外向きなら触らない（`5101`） | ○（`RS`/`CT` 系の既存テストが緑） |
| ε-カプロラクタムの潰した環（v1491） | データのみ（`compounds.json`・`stages.json` の図） | コードは触っていない | 開環重合は環の座標を使わず `straightChainSpot` で一直線に描き直す（`reactor.js:4487-4498`）ので、図の形に依存しない | ○（`PY13` が stages と compounds の一致を見る） |
| 開環重合の一直線（v1491） | `straightChainSpot`（`reactor.js:2447-2473`。点距離 0.65G で空きを探す） | `planAttachment` を**意図して呼ばない**（注記 `4456-4457`） | 水は出ないので `parkAsWater` と無関係。両端の R は `attachREnds` | ○ |
| ビニロンの橋（v1488） | `acetalBridgeSpot`（`reactor.js:2318-2346`。主鎖から結合1本ぶん外へ） | 独自の置き場。`parkAsWater` で HCHO の O を水に | 相手の鎖は R で止めた鎖に限る（`2270-2271`）ので、加硫の鎖（ジエン）とは相手が排他 | ○（`PY5`/`PY9`） |

⚠ 唯一の「前提の踏み違い」は §2-1（加硫が「重合の生成物は格子上」を暗黙に仮定）。それ以外は互いに独立で、共通の道具（`planAttachment`／`freeSpotAround`／`separateComponent`）はこの20版で変わっていない
（【現物】`git diff d350b78..HEAD -- assembler/reactor.js` にこれらの関数本体の差分は無い）。

---

## 3. 軸3: 検査が「空振り」していないか

新しく足された検査 67 件（`MN`4・`CF`5・`IH`4・`IC`3・`ID`11・`IS`5・`PY`10・`VL`2・`VS`2・`EX`4・`SF`6・`RV`12・`CV`3）を本文まで読んだ。
**「機能が壊れていても緑のまま」になりうるものは 4 件＋条件つき 1 群。**

| # | テスト | 何を見ていて何を見ていないか | 壊れても通るシナリオ | 出典 | 判定 |
|--:|---|---|---|---|---|
| **1** | ★ `EX4`「…分子も**履歴も**1つも変えない」 | `snap()` は `atoms.length/bonds.length` だけ。**`g.history` を一度も読まない** | 効かない瓶で `saveState()` が走って Undo 履歴が 1 つ増えても緑／原子を1つ差し替える（数が同じ）実装でも緑 | 【現物】`tests.js:31803-31809`・`31822` | ★ 直す（`g.history.length` を snap に足す・2行） |
| **2** | ★ `IH1`「隠すのは4か所そろえる」 | ボタンは `/[0-9０-９₀-₉]\s*種/` で広く弾く（`16614`）が、**見出し・作業帯・確認モードの3か所は旧文言 `全 10 種` の一致だけ**（`16630`・`16632`・`16636`）。⑥の自己検査（`16654-16657`）は「旧文言が弾かれる」ことしか確かめていない | 見出しに「（10種）」「10種類」「総数 10」の形で種類数が再び出ても緑 | 【現物】`tests.js:16630-16636` | ★ 直す（②と同じ物差しを3か所へ。ヒント段1「全部で 10 種」は別扱い） |
| **3** | ⚠ `PY6` ④⑤⑥／`PY11` ③④／`VS1` ③／`VL2` ④ | `if (!g.resolveCompound(name)) return;` で**通った件数を数えない**（`VL2` ①・`VS2` ①・`PY11` ② には `checked >= N` がある） | 今日は該当化合物が全部 `compounds.json` に在るので効いているが、**改名・削除された日にその否定対照だけが黙って消える** | 【現物】`tests.js:25966, 26036, 26059, 26228, 26240, 26251, 26475, 26482, 26488` | ⚠ 直す（`checked` の数え上げを揃える）。急がない |
| **4** | ⚠ `SF4` (b) | `deg > 70.52`（`32124`）は、`SF1` が「不斉中心はどう回しても 70.5288° を下回らない」を示している以上、**スナップ実装が何であれ通る**。「スナップしない」を担保しているのは `!/重なりました/`（`32126`）の文言だけ | スナップの閾値が広がって不斉中心でも「重なりました」が出ない（文言だけ消えた）実装でも `deg` 側は緑 | 【現物】`tests.js:32121-32127` | 放置でよい（文言側が残っている）。直すなら `fitState().snapped === false` の類を見る |
| **5** | ○ `VL1`／`VS1` | イソプレン 2+2 だけ（`25775`）。§2-1 のとおり**ブタジエンでは折れ角の物差し（±20°）を割る**が回していない | 単量体依存の幾何のずれ | 【現物】`tests.js:25775`・`25868-25872` | ★ ブタジエンを1件足す（§8 発注書 B） |
| **6** | ○ `CV1` | `split` した成分しか見ない（`44763`）。「分かれた反応 ≥ 5」（`44857`）は数だけだが、`CV_MUST_SPLIT` のラチェット（`44717-44722`）が中身を名指ししているので空振りではない | —— | 【現物】`tests.js:44760-44771` | 放置でよい。**分子が増えない反応は §1-2 の物差しを隣に置く**（発注書 A） |

**迷ったもの（低）**: `IS1`(b)／`IS5`(d) は帯の文言を見るが、`hideCanvasToast()` は class を付けるだけで文字を消さず（`game.js:3501-3505`）、`c.reset()` も消さない —— 拒否経路が無言になったとき前回の文言が残って緑になりうる（現状は全経路が帯を出すので空振りしていない）。

**読んで問題なかった検査**（担保の一言）: `MN1-4`（番号は座標で炭素へ引き直し・帯の端点座標・MN4 は実ボタン `click()`）／`CF1-5`（画面文字列 `functionalGroupSummary` まで・否定対照が名指し）／`IH2-4`・`IC1-3`・`ID10-11`（件数＋名前集合＋図の名指し・`IC1` は全域を数え直して集合一致）／`IS1-5`（入力欄と同じ経路 `startFromFormula`・素朴版との集合一致・代表図の凍結）／`PY1, PY5, PY7-10, PY12-13`（環の中身・残る -OH・正準コード・実ボタン）／`EX1-3`（使い捨て iframe の本物の URL・`getComputedStyle` の display・瓶→条件を実クリック）／`SF1-3, SF5-6`（重なる例と重ならない例が対・`checkVisibility` で可視だけ・「1文足すと超える」を実証）／`ID1-9`（データ全件・`resolveCompound` の `name` まで・本物の URL）／`RV1-12`（`clientHeight`・`[data-hl-atom]`・指紋の完全一致・門番に届く `baseIds`）／`CV2-3`（「直しを外すと 1 個残る」を実証）。

**門番の余裕**: 全走 707 件は **918 秒**（headless。⚠ 隣のレーンの全走と同時走行の条件。統合セッションの実測は単独 598 秒・同時 986〜1,103 秒）。`run-tests.mjs` の上限 1,500 秒に対し **61%**。同ファイルの注記（`tools/run-tests.mjs:81-86`「上限は実測の2倍以上を保つ。残り3割を切ったら判断」）の **2倍は既に割っている**（1.63倍）。⚠ 次に 10% 伸びたら「テストが落ちた」ではなく「タイムアウト」で赤くなる形になる。

---

## 4. 軸4: 監査ページの長時間実行（headless・モデル消費ゼロ）

**環境**: worktree を `python -m http.server 9701` で配信、`tools/record/` の Playwright（Chromium）で `assembler/audit.html` を駆動
（Browser ペイン非表示の節流・幅 2px の偽の赤は起きない）。①ライブラリ ②ファズ ③画面サイズ ④当たり判定をすべて ON、1反復 80 操作。

### 4-1. ①③④ は 0 件【実測】

| 検査 | 件数 | 問題 |
|---|--:|--:|
| ①ライブラリ（原形＋変形s2＋異性体列挙） | **1,145 図・2,487 検査** | **0** |
| ③画面サイズ（5 画面 × 縦横に長い 8 図） | 40 | **0** |
| ④当たり判定（枝の上に枝 6 図＋環→鎖→先端） | 7 | **0**（最小の受け皿 5,400px²・方位 5/5・帯 62px） |

★ **C₇ の 83 件・トレハロース・ビニロン・ナイロン6 はこの 1,145 図に入っている**（`buildCompoundLibrary` は `compounds.json` 942 件＋ステージ）。
⚠ ただし `tools/verify-compounds.js` は新しい C₇ の 5 件（3,4-ジメチルペンタナール・2,2,3-トリメチルブタン酸・3,4-ジメチルペンタン酸・ギ酸(1,1,2-トリメチルプロピル)・ギ酸(2,3-ジメチルブチル)）に
「自動水素がやや近い（11.5px）」の**警告**を出す（`tools/verify-compounds.js:322`）のに、監査は 0 件 —— **監査の物差しは H と重原子（`audit.js:150-158`）で、H どうしの近さは見ていない**。物差しの違いであって矛盾ではないが、監査の外にある量が1つある。

### 4-2. ②ファズ: 失敗率 14.2%・中身は「結合線の下の H／原子」【実測】

**300 反復（実 337・基点シード 1588511441）**: 失敗 **48 件（14.2%・Wilson 95% CI 10.9〜18.4%）**。

| 種類 | 件数 |
|---|--:|
| 結合線の下の自動水素 | **37** |
| 結合線の下の原子 | **15**（うち 0.0px が 4 件） |
| 見出しどうしの重なり | 7 |
| 自動水素の重なり（11.5px） | 3 |
| 原子の重なり（**O-O 0.0px** が 1 件） | 2 |

⚠ `DEVELOPMENT.md` に記録のある率（v501: 0.62%、v640/650: 0.4% 台、1488 行の表 0.56%→0.48%）とは**比較できない**
（`comparableKey` が違う: 当時は `bondLinePx`／`hydrogenLinePx` の検査が無い）。
★ この2つの検査が入ったあとの記録は **`DESIGN_hit_areas.md` §10-7（641-644 行）** にある: 「初回 … ファズ 150 反復で **21 件**（＝ 14%）。
手描き以外の経路（結合の伸縮ドラッグ・モジュール配置・反応の実行）から出ている ＝ **この穴もまだ全部は塞がっていない**」、
同 §10-7「やっていないこと」（655 行）に「伸縮・モジュール・反応の実行は未着手」。
＝ **14% は v1240 の時点で既知の、意図して残してある赤**であり、今日の率（14.2%）はそれと同じ水準。

#### 4-2b. ★ 同じ種で v1476 と A/B した —— 退行ではない【実測】

`auditRerun(seed, 80)` を **種 1000〜1299 の 300 個**で、v1476（`d350b78` を別 worktree で配信）と v1495 で流した。v1495 は**同じ種をもう一度**流してノイズの床も取った
（`audit.js:927-933` の注記どおり、同じ版でも再現は完全ではない）。

| 版 | 失敗 / 300 | 率 | Wilson 95% CI | 種類の内訳 |
|---|--:|--:|---|---|
| **v1476** | **43** | 14.3% | 10.8〜18.8% | 結合線の下の自動水素 37・結合線の下の原子 19・見出し 4・原子の重なり 3・見出しが図に乗る 1 |
| **v1495**（1回目） | **49** | 16.3% | 12.6〜21.0% | 自動水素 36・原子 27・見出し 5・原子の重なり 2・乗る 1 |
| **v1495**（2回目・同じ種） | **52** | 17.3% | 13.5〜22.1% | 自動水素 34・原子 27・見出し 7・原子の重なり 2・乗る 1 |

- **区間が重なる** ＝ 差を主張してはいけない（`audit.js:856` の読み方）。**同じ版の2回の差（49→52）が、版の差（43→49）の半分ある。**
- 失敗した種の重なり: v1495 の 1 回目と 2 回目は **44/49** が同じ種、v1476 と v1495 は **13** しか重ならない
  —— ⚠ これは退行の印ではなく、**`summon` がライブラリの添字で分子を引く**（`audit.js:663`）ので、C₇ の 83 件などが増えた v1495 では同じ種でも別の分子を呼ぶため。
  ★ **版をまたいだ種ごとの比較は、ライブラリが変わると成立しない**（`audit.js` の注記には無い落とし穴。§4-4 の発注書に「summon は名前で引く」を含めた）。
- 「結合線の下の原子」が 19 → 27・27 と**同じ向きに 2 回**出ているのは気になるが、種ごとの分子が違うので判定できない。5000 反復の本走（§4-2c）と次回の夜間監査で見る。

**結論**: 14〜17% は v1240（`DESIGN_hit_areas.md` §10-7）から続く既知の赤で、**この20版で増えたとは言えない**。

#### 4-2c. 5000 反復の本走【実測】

（本文執筆時点で走行中。完走後にこの節へ追記する）

### 4-3. ★★ ファズは新しい反応に届いていない【実測】

`auditTrace` で **200 シード × 80 操作 = 16,000 操作**を流し、実際に押した操作と反応ボタンの札を数えた。

| | 数 |
|---|--:|
| 操作の内訳 | place 4,806／module 2,252／toggle 1,508／undo 1,152／erase 1,029／**react 894**／summon 837／cut 826／stretch 696／redo 655 |
| react の期待値（11%） | 約 1,612 |
| ★ 実際に押せた react | **894（55%）** ＝ **45% は `#reaction-actions button` が 0 個で空振り**（`audit.js:647-648`。ボタンが無ければ ops にも載らない） |
| 押した 894 の内訳 | **「🔍 反応の前後を見る」 344（38%）**・「⚗ この反応の機構を見る」 40・⚠ の解説カード 86・**実際に分子を変える反応 約 420** |

**49 本中、200 シードで 1 回も押されなかった反応 16 本**:
`addition_polymerization`・`alkyne_polymerization`・`diene_polymerization`・`vulcanization`・`acetalization_pva`（ビニロン）・
`ring_opening_polymerization`（ナイロン6）・`condensation_polymerization`・`condensation_glycoside`・`hydrolysis_glycoside`・
`hydrolysis_anhydride`・`dehydration_anhydride`／`_info`・`wacker_oxidation`・`bromination_activated_ring`・`cyclize_glucose_α/β`・`open_glucopyranose`。

【意見】理由は構造的で、乱数を増やしても届かない:
- 重合4本・加硫・ビニロン・開環重合・糖の縮合は**同じ分子を 2〜4 個並べる**か**特定の相手**が要る。`summon` は 1,145 件から一様に 1 件引く（`audit.js:662-665`）ので、同じ単量体が 2 個そろう確率は反復あたりほぼ 0。
- ワッカー・環化・臭素化・酸無水物は題材が限られ、`place`（30%）が先に分子を壊す。
- ★ **この20版で足した反応（加硫の寄せ・ビニロン・開環重合）は、監査の門番を 1 回も通っていない。** 幾何の退行（§2-1 の類）は夜間監査では出ない。

### 4-4. 監査の外にあるものの一覧【現物】

`audit.js` が押すのは **キャンバス上のポインタ操作・`#reaction-actions button`・`summonMolecule`・undo/redo** だけ（`audit.js:645-732`）。したがって次は監査の外:

| 面 | この20版で触ったか | 監査 | 代わりの門番 |
|---|---|---|---|
| ★ **実験モード（🧪 タブ・`#exp-reagents-grid`）** | ★ 新設（v1494） | **外**（レーンの申告どおり。タブも瓶も押さない） | `EX2-4` の 3 件のみ |
| ★ **重合・加硫・ビニロン・開環重合・縮合重合**（複数分子が要る反応） | ★ 加硫・ビニロン・開環を新設／変更 | **外**（§4-3） | `PY5-13`・`VL`・`VS`・`CV1`（各 1 題材） |
| **糖の縮合／加水分解・環化／開環** | 縮合の描き直し・トレハロース | **外**（§4-3） | `RV5-6`・`SG` 系 |
| **書き出し練習（異性体）** | ★ 種類数を伏せる・ヒント5段・お題15件・C₇ | 外（`learn.js` は一度も呼ばれない） | `IH`・`IC`・`IS`・`ID10-11` |
| **絞り込みモード・`?open=narrowing`** | ★ 受け口を新設 | 外 | `EX1`・`ID5-6` |
| **クイズ（ST26 の資格判定）** | ★ 変更 | 外 | `ST` 系 |
| **立体視（鏡像を重ねる）** | ★ 新設（v1478） | 外 | `SF1-6` |
| **主鎖と番号（選んだ分子）** | ★ 新設（v1484） | 外 | `MN1-4` |
| **整形モード（カルボキシ基の O）** | ★ 変更（v1483） | 外（ファズは整形モードに入らない） | `CT`/`RS` 系 |
| **反応の前後比較・字幕の2行たたみ** | ★ 変更（v1477/1479） | 前後比較は押される（38%）が**中身は検査されない**（`inspectMolecule` は分子しか見ない） | `RV1-2`・`RV9-10` |
| **反応機構ビューア・チュートリアル・分子モーダルの UI** | —— | 外 | 既存テスト |
| **H どうしの近さ** | —— | 外（§4-1） | `verify-compounds.js`（警告のみ） |

★ **監査が見ているのは「自由キャンバスで1分子を壊す」面だけ**で、この20版の変更 14 件のうち**監査の内側にあるのは 0 件**（切る反応の印は `inspectMolecule` の対象外）。
＝ 「バグの発見は監査ページに任せ、モデルは修正に使う」（CLAUDE.md）の分業は、**この20版の変更に関しては成立していない**。回帰テストが唯一の門番。

---

## 5. 軸5: 記録と実物のずれ

6 文書を現物と突き合わせた。**ずれ 25 か所、一致 14 か所**。★ 印はユーザー決定と逆の推奨が立ったままのもの。

### 5-1. `DESIGN_reaction_execution.md`

| 節・行 | 記述 | 現物 | 直すべきか |
|---|---|---|---|
| §21 見出し **2733-2736** | 「重合まわりの見直し（**設計のみ**）…⚠ 決めるところまで。**実装はしない**」 | §21-6（v1488）・§21-7（v1491）が同じ節の中で実装済みを記録 | 設計書（見出しに「2本は実装済み → §21-6/§21-7」） |
| §21-4 (e) **3247-3248** | 「新しく書く関数は **4本**、触る既存の関数は **0本**…緑のテストを1つも触らない」 | 実際は 5+2 本・既存 1 本・緑テスト 2+2 件（§21-6 (a) 3324-3331 と `DEVELOPMENT.md:3184-3189` が同じ数） | 設計書（後方参照の1行。`DEVELOPMENT.md:3178` の教訓の出所なので放置すると同じ数字を次が引く） |
| D-P5 **3288-3291**／D-P6 **3293-3296**／D-P9 **3312-3315** | 判断待ちのまま | D-P5＝1タップで 2/3（`reactor.js:2204`）・D-P6＝描き直し済み（`PY13`）・D-P9＝古い数字が根拠 | 設計書（決着印） |
| D-P8 **3306-3308** | 「未登録: **ビニロン・ナイロン6**・…」 | `compounds.json:942-943` に登録済み | 設計書 |
| §22-3 **3502-3510** | 「硫黄を1個挟む＝2本ぶん」「C-S-C が一直線」 | `reactor.js:2500, 2508-2513`（v1487 で 2 個・3 本ぶん）。§20-6 が新しい形だが §22-3 本文に注記が無く、しかも §20-6 はファイル上 §22 より**前** | 設計書（§22-3 冒頭に1行） |
| ○ §21-6 (a) の表・§21-7 (a)・§21-1 (i) の訂正・§20-6 の幾何（1/3・2/3 の席・3k 倍・1本目だけ寄せる）・「1タップで 2/3」 | —— | `reactor.js:2215-2352, 2389-2473, 2508-2546, 4458` と一致 | 一致 |

### 5-2. `DESIGN_experiment_mode.md`

| 節・行 | 記述 | 現物 | 直すべきか |
|---|---|---|---|
| **3 行目** | 「⚠ この文書は設計だけ。**実装はまだ1行も無い**」 | 第1段（器）は実装済み: `game.js:8497-8545`（`setupPaletteTabs`/`setPalette`）・`reactor.js:5876-5883, 6320-6341`・`index.html:97, 176`・`EX2-4` | ★ 設計書（進捗欄。`DESIGN_sugar.md:10-21` の型） |
| ★ **§3-4 全体 321-352**・§3-2 の表 #3 **291, 302-304** | 「条件の2択で行き先を伏せる」「§11.3 を破ってよい理由」「§3-4 の手当ては必須」 | ユーザー決定 D-E3「伏せない」（`DEVELOPMENT.md:3327, 3332-3336`）。`renderConditionChoice`（`reactor.js:6633`）はこの20版で**触られていない**。`EX3` の題名が「行き先は伏せない・D-E3」 | ★ 設計書（節冒頭に「D-E3 で不採用」） |
| ★ **§8-1 D-E1 614／D-E3 616／D-E4 617** | 判断待ち（D-E3 の推しは **(a) 伏せる**） | D-E1 覆す／D-E3 **(b)**／D-E4 目標がある（`DEVELOPMENT.md:3324-3342`） | ★ 設計書（決着印。D-E4 は (a) か (c) かが `DEVELOPMENT.md` 側でも1文に混ざっているので、第2段の着手前に1行確認） |
| **§9-1 650-657** | 見せ方 6 件・触る既存関数 4・触る緑テスト 22〜29 | 実装されたのは ① のみ。③⑤⑥ は未実施、④ は D-E3 で不要。既存テストは **0 件**触っていない（`reactor.js:6328`） | 設計書（§21-6 (a) と同じ「見積り／実際」の表を足す。⚠ **見積りが外れた記録が無い**のは、`DEVELOPMENT.md:3178` の教訓の直後の文書として問題） |
| **§9-1 663-666** | 「瓶を『増やす』＝ D-E1 の (b) と同じ話」 | 実装は「増やす」を採りつつ D-E1 (a) 覆す と名乗る（`game.js:8497`） | 設計書（「増やす」は (a)/(b) と独立の手段、と書き換え） |
| ○ §3-3・§5-3 案A・D-E10・D-E12・D-E14・§7（課題の流れ） | —— | `game.js:8503-8511`・`reactor.js:6350-6355`・`EX3:31791`・`DEVELOPMENT.md:3338-3342` と一致 | 一致 |

### 5-3. `DESIGN_sugar.md`

| 節・行 | 記述 | 現物 | 直すべきか |
|---|---|---|---|
| ★ **§8-① 1416**／519・1043・1187・§5-4 1357-1363・§10-1 1447-1449・進捗表 20 | トレハロースは未登録／二糖は 4 件／推奨「足さない（当面）」 | `compounds.json:941` に登録済み（v1477）。§13-8 だけが新しい | ★ 設計書（§8-① に決着印は必須。他は「4件（当時）」の注記） |
| ★ **§8-③ 1418**・§4-8 1036-1037 | 推奨「既存の `h2so4_conc` に相乗り」 | `reactor.js:4003-4008`「⚠ 試薬の瓶は持たせない…この判断は §8-③ の推奨と違う」（コードが自白している） | ★ 設計書（決着「瓶なし」） |
| **§8-② 1417**・§4-8 1043-1051 | 名無しの二糖に「α-1,6 結合…」の文を出す（推奨「言う」） | §4-8d の決着で候補は登録一致のものだけ → 名無しは出ない。`reactor.js` に該当文言 0 件 | 設計書（問いごと消滅、と印） |
| **§13-6 1751 vs 1784-1786** | 見出し「✅ 決着」の同じ節の末尾に「⚠ どちらにするかは決めていない」 | `reactor.js:5097` `alignRow: true`（残す側） | 設計書（1784 を「決定前の記録」と明示） |
| ⚠ **`reactor.js:4064-4065`**（コード） | caption「教科書に名前の出る二糖（マルトース・セロビオース・ラクトース・スクロース）」 | 設計書 §4-8b 1164 の文案は 5 つ（トレハロース含む）・ユーザー原文 1809「トレハロースは教科書の平文に載っています」 | ★ **コードを直す（1行）** |
| ○ §4-9d（`alignRow` は加水分解だけ）・§13-2 の表（`anchor`/`arc`/`arcFrom`/`fitIgnore`）・§4-8c (e)・§4-8d・§13-8・§12-4 | —— | `reactor.js:3961-3979, 639-651, 5052-5119` と一致 | 一致 |

### 5-4. `DESIGN_isomer_practice.md` §19

| 節・行 | 記述 | 現物 | 直すべきか |
|---|---|---|---|
| §19-5b **1845, 1856-1865** | 「★ 採ったのは B … 13件」（断定のまま） | 直後の §19-5b-2（1867-1922）で 15 件・門番から外した、に覆っている。`learn.js:1236-1284`・`IH2`・`IC1` | 設計書（1856 に「同日 v1493 で外した → 19-5b-2」） |
| §19-5c **1934 の表, 1937, 1945-1947, 1966** | 「線の外: アルデヒド C₇／カルボン酸 C₇」「残った13件」「腕がペンチル以内のまま 21 種以上が現れたとき」 | お題は 15 件（両者は内側）。腕の線は門番ではない（引き金は「上限20を超える飽和の式」） | 設計書（13→15・引き金の言い換え。結論「作らない」は 17 ≤ 20 で今も成り立つ） |
| ○ §19-5b-2・§19-5d・§19-6 | —— | `learn.js:1236-1266, 1275, 1283` と一致 | 一致 |

### 5-5. `DEVELOPMENT.md`・`CLAUDE.md`

| 節・行 | 記述 | 現物 | 直すべきか |
|---|---|---|---|
| `DEVELOPMENT.md` **688** | 「★ 現在地（2026-08-20・main は **v1421**／回帰テスト **505件**）」＋690「新しい現在地はここに書き足すこと」 | v1495・707 件。「現在地」の見出しは 688/1836/2046 の 3 つで 688 より新しいものは無い。9/1 の決定群は 3205-3565 に散っている | 設計書（数字の更新＋「決定は 3205 行〜」の1行） |
| `DEVELOPMENT.md` **745-763**「Assembler の未処理（ここが一覧の正）」 | 「新しい発注書を作ったらこの表に1行足すこと」 | 重合4本（§21）・実験モード（v1494）・参考書／有料・C₇ お題の行が無い（「ビニロン」「実験モード」とも表の中に 0 件） | 設計書（4 行） |
| `CLAUDE.md` 構成表 **30-40** | 10 行 | `learn.js`・`narrowing.js`・`deadend.js`・`rec.js`・`tutorial.js`・`tutorials.json`・`demos-*.json`（8本）・`style.css` が表に無い。今回の `learn.js`（書き出し練習）を表から辿れない | CLAUDE.md（責務1行ずつ） |
| `CLAUDE.md` リポジトリ構成 **11-18** | 3 アプリ | `qa/`・`muki/`（各 test.html 持ち・ハブからリンク）が無い。`muki` は CLAUDE.md に 0 件 | CLAUDE.md（2 行） |
| ○ `DESIGN_iupac_check.md` §N-8（288-327）・`DESIGN_3d_correspondence.md` §9（503-593） | —— | `game.js:9452, 9479, 9618, 8459, 9639`・`stereo.js:139, 1844-1951, 2346` と一致 | 一致 |

---

## 6. 見つけた問題（重い順）と「直すべきか・急ぐか」

| # | 問題 | 出典 | 直すべきか | 急ぐか |
|--:|---|---|---|---|
| **1** | ★★ `diene_polymerization` の印が「中央へ移った C=C」の炭素 6 個に無い（教材の要点） | §1-2 #1・`reactor.js:4282-4323` | 直す（`changed.push(u.c2, u.c3)`・1行） | ★ 次の動画（合成ゴム）を撮る前 |
| **2** | ★★ ファズが新しい反応（重合・加硫・ビニロン・開環・糖）に届かない ＝ 監査の分業が成立していない | §4-3・`audit.js:645-665` | 直す（`summon` に「同じ単量体を 2〜4 個」「相手つきの組」の枝を足す・`react` の空振り率を記録する） | ★ 次に幾何を触るレーンの前 |
| **3** | ★ 分子が増えない反応の印を見張る検査が無い（§1-2 の物差しはあるのに `CV1` の隣に置かれていない） | §1-2・`tests.js:44760-44771` | 直す（悉皆テスト 1 本・**#1〜#4 を否定対照にできる**） | #1 と同じレーン |
| **4** | ★ 設計書に**ユーザー決定と逆の推奨**が 6 か所立ったまま（exp §3-4/D-E1/D-E3/D-E4・sugar §8-①/§8-③） | §5-2・§5-3 | 直す（決着印＋節冒頭の1行。コードは触らない） | ⚠ 実験モード第2段・参考書レーンが読む前 |
| **5** | ★ 加硫の橋がブタジエンで 25° 折れる（「一直線」の断定は格子上の足場が前提）＋ `VL1` がイソプレンしか回さない | §2-1・`reactor.js:2541-2546, 2166-2170`・`tests.js:25775` | 直す（席を ca の実座標から取る／`VL1` にブタジエンを足す） | 急がない（線が原子をかすめてはいない） |
| **6** | ★ `EX4` が名乗りどおり履歴を見ていない／`IH1` の3か所が旧文言の一致だけ | §3 #1・#2 | 直す（各 2〜3 行） | 急がない |
| **7** | ⚠ `vulcanization`・`iodoform` の渡し落とし各 1 種・`acetalization_pva` の渡しすぎ | §1-2 #2〜#4 | 直す（各1行） | #1 と同じレーン |
| **8** | ○ ファズ失敗率 14〜17%（中身は「結合線の下の H／原子」）は **退行ではない**（A/B で区間が重なる）。v1240 から意図して残してある赤 | §4-2b・`DESIGN_hit_areas.md:641-656` | 放置でよい（塞ぐなら §10-7「やっていないこと」の 3 経路。⚠ `summon` を名前で引くようにしないと、版をまたいだ種の比較が成立しない） | —— |
| **9** | ⚠ `run-tests.mjs` の上限に対し全走が 61%（「2倍以上」の注記を既に割っている） | §3 末尾・`tools/run-tests.mjs:81-86` | 上限を 2,400 秒へ上げる（1行） | 急がない |
| **10** | ⚠ 否定対照の `if (!resolveCompound) return;` が 9 か所（通った件数を数えない） | §3 #3 | 直す（`checked` を揃える） | 急がない |
| **11** | ⚠ `reactor.js:4064` の caption にトレハロースが無い | §5-3 | 直す（1行） | 急がない |
| **12** | ⚠ 見積りの外れが記録されていない文書がある（exp §9-1）・古い数字（§21-4 (e)・isomer §19-5c・DEVELOPMENT 688） | §5 | 設計書 | 急がない |
| **13** | ○ `SF4`(b) の恒真・`IS1/IS5` の帯の文言の残り・`CV1` の水の除外が `fromReaction` 依存 | §3 #4・迷ったもの・§1-3 | 放置でよい（覚えておく） | —— |

---

## 7. 確かめて問題なかったもの（見ていない所と区別がつくように）

**回したもの【実測】**
- `assembler/test.html` 全走（headless）: **✅ 707 件全合格・918 秒**。注記「見張った 49/49 本（分かれた反応 14 本・分かれない反応 35 本）／題材が無く見張れない 0 本／脱離した水として除外 11 個」
- `qa/test.html`: **✅ 94 件全合格**（「棚卸し: assembler の反応の在庫」を含む ＝ v1488 で赤かった件は HEAD で緑）
- `node tools/verify-release.js`（**worktree の中のを呼んだ**）: ✅ 5 アプリ（assembler v1495・ion v198・muki v29・qa v92・ratio v41）・535 テキストに化け／BOM なし
- `node tools/verify-compounds.js`（同上）: ✅ 合格（警告 5 件は §4-1）
- 監査ページ ①③④: 0 件（§4-1）／②: §4-2
- 印の悉皆（49 本＋4 枝）: 44 本が missing 0・extra 0（§1-2）。とくに **v1490 で束ねた切る反応 14 本はすべて両側に印**
- 加硫の幾何（3 単量体 × 2 通り）: 「結合線の下の原子」0 件・S が R に付いた例 0 件（§2-1）

**読んで一致していたもの【現物】**
- 直した 4 件（持ち越し／渡しすぎ／渡し落とし 2 種）の実装と検査の対応（§1-1）
- 座標を触った 5 か所が共通の道具（`planAttachment`／`freeSpotAround`／`separateComponent`／`componentOverlaps`）を変えていないこと（§2-2）
- 設計書の一致 14 か所（§5 の ○ 行）
- 新しい検査 67 件のうち 62 件の担保（§3「読んで問題なかった検査」）

**見ていないもの（正直に）**
- iPad 実機・WebKit エンジン（`--engine=webkit` は回していない）
- 動画レーンの完成品（mp4）での目視
- `muki`／`ion-equation`／`ratio` の回帰テスト（版整合は `verify-release` で見た）
- 5000 反復の本走は §4-2c（本文執筆時点で走行中。完走後に追記）

---

## 8. 直す発注書（レーン1本ぶんの粒度・⚠ このレビューでは直していない）

### 発注書 A: 印の悉皆（分子が増えない反応）＋ 渡し落とし 3 本・渡しすぎ 1 本（★ 急ぐ）

1. `tests.js` の `CV1` の隣に **`CV4`** を足す: 全 49 本を `CV1` と同じ題材で回し、**重原子ごとの「隣の原子 id ＋ 結合次数」の集合の差分（＋新しく生えた原子）**と `result.changed` を突き合わせる。
   除外は脱離した水（`fromReaction`）だけ。**missing（変わったのに印が無い）は赤**。extra（変わっていないのに印）は `condensation_glycoside` の `site[1]` と `dehydration_anhydride` の `cA` の 2 件を名指しで許容し、それ以外は赤。
   ⚠ 先に書いて **赤 5 本（§1-2 の #1〜#4）を見てから** 直す ＝ 否定対照を無料で得る。
2. `reactor.js:4306`（ジエン）: `changed.push(u.c2, u.c3)` を各単量体に。`4388`（加硫）: `[a1, b1, best.ca2, best.cb2, s1.id, s2.id]`。`3741`（ヨードホルム）: `oh` 側の O を足す。`4432`（ビニロン）: `g.cA, g.cB` を外す。
3. `RV12` の要領で、動画レーンの完成品（V128/V130 の合成ゴム）で目視。版バンプ・全走・qa 全走。
4. 見積り: 新規テスト 1・触る既存関数 4（各1行）・触る緑のテスト 0（`RV12`・`VL1`・`VS1` は印の**有無**を数だけで見ていないことを先に grep で確かめる）。

### 発注書 B: 監査ファズに「相手つきの題材」を足す（★ 次に幾何を触る前）

1. `audit.js` の `summon` 枝に **確率 1/3 で「組」を引く**枝を足す: 同じ単量体 ×2〜4（エチレン・アセチレン・ブタジエン・イソプレン・スチレン・ε-カプロラクタム）、
   PVA＋HCHO、アジピン酸＋HMDA ×2、酸＋アルコール、二糖 1 件、単糖 ×2。`OP_MIX_ID` を 3 に上げる（`comparableKey` が変わる）。
2. `react` で `#reaction-actions button` が 0 個だった回数を `report.counts.reactMissed` に記録し、「🔍 反応の前後を見る」など**分子を変えない札は別に数える**。
3. `VL1`／`VS1` に **1,3-ブタジエン 2+2** を1件足す（§2-1 の 155° がそのまま赤になる）。同時に `stackChainsForBridge:2541-2546` の席を `ca` の実座標から取るよう直し、`vulcanizablePairs:2166-2170` の丸めと一致することを `VS1` で確かめる。
4. 見積り: `audit.js` 2 関数・`tests.js` 2 件・`reactor.js` 1 関数。⚠ `OP_MIX_ID` を上げるので夜間監査の率は前の実行と並べられない（§4-2 と同じ理由で当然）。

### 発注書 C: 設計書の決着印と古い数字（急がないが、次のレーンが読む前）

1. `DESIGN_experiment_mode.md`: 3 行目に進捗欄／§3-4 冒頭に「D-E3 で不採用」／§8-1 D-E1・D-E3・D-E4 に決着／§9-1 に「見積り／実際」の表。
2. `DESIGN_sugar.md`: §8-①②③ に決着／「二糖4件」7 か所に「（当時）」／§13-6 の 1784 行を整理。
3. `DESIGN_reaction_execution.md`: §21 見出し・§21-4 (e)・D-P5/6/8/9・§22-3 に各1行。
4. `DESIGN_isomer_practice.md` §19-5b/5c: 13→15・引き金の言い換え。
5. `DEVELOPMENT.md:688` の数字と「決定は 3205 行〜」／745 行の表に 4 行。`CLAUDE.md` 構成表に 5 ファイル・リポジトリ構成に `qa/`・`muki/`。
6. `reactor.js:4064` の caption にトレハロース（コードはこれ 1 行だけ。版バンプが要る）。
7. `EX4` に `g.history.length`／`IH1` の 3 か所に ② の物差し／`resolveCompound` の 9 か所に `checked`／`run-tests.mjs` の上限 2,400 秒。

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-02 | 新規。5 軸（印 49 本の悉皆／幾何 5 か所の相互依存／新検査 67 件の空振り／監査 headless 5000＋300＋A/B／設計書 6 文書）＋問題 13 件＋発注書 3 本 |
