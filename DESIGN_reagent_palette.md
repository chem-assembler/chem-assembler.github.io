# 試薬パレット 設計書（Gemini レビュー項目20）

「分子を描くと、できる反応がボタンで出てくる」いまの自動案内に対して、
**試薬を選んでから分子に掛ける**手動実験を足す。レビュー項目20（`docs/review_v331.md` 320行目〜）の
「試薬パレット」「反応条件選択モーダル」「自動案内ガイドとの棲み分け」に答える。

実装前の合意用ドキュメント。**この文書ではコードを1行も変えていない**（設計のみ）。
反応の実行そのものは [DESIGN_reaction_execution.md](DESIGN_reaction_execution.md) が正で、
本書はその上に「試薬」という層を足す話に限る。

> **置き場所（画面のどこに出すか）はここでは決めない。**
> ユーザーの「機能への入口がわかりづらい」を受けて `DESIGN_entry_points.md` で入口の全体再編を
> 設計中であり、パレットとモーダルの居場所はその結論に従う。本書が答えるのは
> **「何が要るか」**（データの形・試薬の数・失敗時の応答・段階の割り方）だけ。

---

## 0. 先に確かめた事実

レビューの記述をそのまま前提にせず、`assembler/reactor.js`（v444 時点・2742行）で確認した。

| 確かめたこと | 実際 |
|---|---|
| `REACTION_RULES` の件数 | **27件**（うち `info: true` の解説専用が4件 ＝ **実行できる反応は23件**） |
| ルールが持つフィールド | `id` / `label` / `detect(mol)` / `apply(game, site)` ＋ 任意で `mechanismId`・`morphStages`・`info` |
| **試薬という概念** | **無い。** 試薬名は `label` と `caption` の**日本語の文字列の中**にだけある（「付加: Br₂（臭素水の脱色）」「芳香族置換: ニトロ化（濃硝酸＋濃硫酸）」）。機械では引けない |
| 条件（温度）の分岐 | **すでにルールが分かれている。** `dehydration_intra`（160〜170℃）と `dehydration_inter`（130〜140℃）は別の `id` を持つ独立したルール。温度は `caption` の文中にある |
| 実行の入口 | `Reactor.refresh()` が全ルールの `detect` を回し、非空のものをボタンにする（`reactor.js:2004`）。押すと `onRuleClick` → `narrow`（箇所が複数なら選ばせる）→ `execute` |
| 解説専用ルールの扱い | `onRuleClick` が `info` を見て**トーストを出すだけで返る**（`reactor.js:2162`）。`saveState()` を積まない ＝ Undo 履歴を汚さない |
| 反応が出ないときの案内 | v422 で `findPartnerHints`（`reactor.js:1716`）。**相手の分子**が足りない場合に「＋ エタノール を呼び出す → エステル化」を出す |
| 呈色・検出反応 | **実装は無い。** 銀鏡・フェーリング・塩化鉄(III) は `learn.js:250` と `reactor.js` の `caption` の**説明文の中**にあるだけ |

**レビューの記述で正しくなかった点**（項目5・24-1・7 と同じく、そのまま実装に移さない）:

- 「`conditionBranch`（温度選択肢など）のスキーマを定義する」…… **分岐の実体はもうある**。
  130〜140℃ と 160〜170℃ は別ルールとして書かれている。要るのは新しい分岐機構ではなく、
  **「どちらの温度か」をユーザーに選ばせる1画面**と、ルールに温度ラベルを1つ足すことだけ。
- 「**カップリング試薬**」…… カップリングの相手は**フェノール**（＋NaOH 水溶液）で、
  これは**キャンバスに置ける分子**。瓶にすると2分子反応の作法（`componentOf` で別分子だけを繋ぐ）から
  外れる。瓶にするのは**ジアゾ化の NaNO₂＋HCl だけ**で、カップリングは既存のエステル化・
  分子間脱水と同じ「相手はキャンバスの分子」で扱う。
- 「**濃塩酸＋ZnCl₂（ルーカス試薬）**」…… **日本の高校化学の範囲外**（英米の教科書のアルコールの級の判別法で、
  日本の教科書・入試には出ない）。`CLAUDE.md` の「高校範囲を超える試薬は入れない」に従い**採らない**。
- 「ニンヒドリン」「濃硝酸（キサントプロテイン反応）」…… ニンヒドリンは高校範囲（アミノ酸の検出）なので入れる。
  キサントプロテインは**対象がタンパク質**で1分子として描けず、しかも反応の実体は
  既存の `aromatic_nitration`（環のニトロ化）そのもの。**瓶にはせず注記にとどめる**。

---

## 1. いまとの棲み分け

### 1.1 結論: 入口を2つにしない。**同じ1つのルール表を、2つの向きから引く**

| | 自動案内（いま） | 試薬パレット（足すもの） |
|---|---|---|
| 引く向き | 分子 → できる反応 | 試薬 → 起こること |
| 出るもの | **できる反応だけ** | **瓶は常に全部出る**（できない組み合わせも押せる） |
| 押した結果 | 反応が進む | 反応が進む **or** 「効きません」の説明が返る |
| 想定 | 何ができるか知りたい初学者 | 試薬を覚えていて確かめたい学習者 |

**主は自動案内のまま。試薬パレットは従。** 切り替えではなく併存させるが、
**モードにはしない**（モードにすると「いまどっちにいるか」を覚える負担が増える。
`DESIGN_ui_modes.md` の3モードの上にさらに階層を作らない）。

両者は**同じ `REACTION_RULES` の上に立つ**。試薬パレットは新しい反応を1つも持たず、
**既存のルールを試薬で逆引きした表示にすぎない**。だから:

- 反応を1つ足せば、自動案内にもパレットにも同時に出る（片方だけ古くなる壊れ方をしない）
- 化学の正しさの根拠は `detect`/`apply` の1か所のまま。試薬側に判定を書き写さない

これは v422 の `findPartnerHints` が採った考え方と同じ ——
「案内をルールごとに書き写さず、実際に `detect` を回して確かめる」。

### 1.2 それでもパレットに固有の価値がある（＝逆引きだけでは足りない）

自動案内は**できるものしか出さない**。手動実験の価値は逆で、
**効かない組み合わせを選べてしまうこと**にある（エタンに臭素水を入れても脱色しない、
という体験そのものが検出法の理解になる）。

したがってパレットは**瓶をグレーアウトしない**。全部の瓶がいつでも押せて、
押して初めて結果（反応 or 説明）が返る。これが自動案内と重ならない唯一の点であり、
パレットを足す理由でもある。詰まりにしないための応答は §4。

### 1.3 何が要るか（置き場所は決めない）

入口再編の結論がどうであれ、**必要なものは次の4つ**。これらは置き場所に依存しない。

1. **瓶の一覧を出す面** … 20本前後が一望できて、押せる。区分（変換／検出）が読めること
2. **温度などの条件を選ぶ面** … 同じ瓶で行き先が2つ以上あるときだけ出る（§2.4）
3. **結果を返す面** … 反応が起きたときは既存の実行経路（トースト＋前後比較）にそのまま乗る。
   起きなかったときは「効かない理由と次の一手」（§4）
4. **自動案内との行き来** … 自動案内のボタンに**瓶の名前**が出ること（いまも文字列としては出ているが、
   `reagentId` が付けば「この反応はこの瓶」と機械的に対応が取れる）

> 実装の順としては、**1〜4 は既存の「⚗ この分子の反応」カードの中だけで成立する**
> （新しいモーダルを1つも作らなくても、カードの中に瓶の行を足し、結果はいまと同じトーストで返せる）。
> 入口再編が独立したパレット面を用意するならそこへ移せばよく、**データの形（§2）は同じ**。

---

## 2. データの形

### 2.1 方針: 既存の `detect` / `apply` には**一切触らない**

足すのは**静的なフィールドと、新しい静的な表**だけ。関数は書き換えない。
これで既存の23件の反応の挙動（＝ RX 系テストと夜間監査が押さえている範囲）は変わらない。

### 2.2 `REAGENTS`（新設・試薬瓶の表）

```js
// 試薬瓶。1本＝パレットに並ぶ1個。reactor.js の先頭側（REACTION_RULES の前）に置く
const REAGENTS = [
    {
        id: 'h2so4_conc',
        name: '濃硫酸',
        formula: 'H₂SO₄',
        kind: 'transform',              // 'transform' | 'detect'
        // この瓶が効かなかったときに返す固定文（§4）。ルールごとではなく**瓶ごとに1つ**
        acts: 'アルコール（脱水）・カルボン酸とアルコール（エステル化の触媒）・ベンゼン環（スルホン化）',
        note: '加熱の温度で行き先が変わります。'
    },
    // ...
];
```

- `id` は安定キー。`mechanismId` と同じく**死にリンクを機械検証する**（§5 のテスト）
- 呈色試薬も同じ表に `kind: 'detect'` で並べる（ユーザーから見れば同じ「瓶」だから）

### 2.3 `REACTION_RULES` に足すフィールド（2つだけ）

```js
{
    id: 'dehydration_intra',
    mechanismId: 'ethanol_e1',
    label: '分子内脱水（-H₂O） → アルケン',
    reagentId: 'h2so4_conc',                        // ← 足す①
    condition: { key: 'hot', label: '約160〜170℃' }, // ← 足す②（同じ瓶で行き先が割れるときだけ）
    detect(mol) { /* いまのまま */ },
    apply(game, site) { /* いまのまま */ }
}
```

- `reagentId` … **任意**。付いていないルールは「試薬なしで起こる」＝ グルコースの環化・開環
  （鎖状⇄環状の平衡）と、解説専用の `info` ルール。パレットからは出ない
- `condition` … **任意**。**同じ `reagentId` を持つルールが同じ分子で同時に `detect` を通す**
  ときにだけ意味を持つ。付いていなければ条件を訊かない

**1つのルールが複数の瓶で起こる場合**（酸化剤は KMnO₄/H⁺ でも K₂Cr₂O₇/H⁺ でも同じ [O]）は、
`reagentId` を配列にせず**瓶を1本にまとめる**（§3.2）。配列にすると「どちらの瓶を押したか」で
`caption` を変えたくなり、`apply` に条件分岐が入ってしまう。

### 2.4 条件分岐（レビューの `conditionBranch`）は**新しい機構を作らない**

エタノール＋濃硫酸の 130〜140℃／160〜170℃ は、**すでに別ルール**（`dehydration_inter` /
`dehydration_intra`）として存在する。だから要るのは分岐の実装ではなく、
**「同じ瓶に行き先が2つ以上あったときに選ばせる1画面」**だけ。

しかも、その1画面が本当に要るのは調べたかぎり**1ケース**しかない:

| 瓶 | 行き先（`detect` を通すルール） | 何で割れるか |
|---|---|---|
| 濃硫酸 | 分子内脱水 / 分子間脱水 | **温度でしか割れない** ← ここだけ |
| 濃硫酸 | エステル化 | 相手にカルボン酸が要る ＝ `detect` が割る |
| 濃硫酸 | スルホン化 | 基質が芳香環 ＝ `detect` が割る |
| 酸化剤 [O] | 1級アルコール／2級アルコール／アルデヒド | 基質の構造 ＝ `detect` が割る |
| 希硫酸 | エステルの加水分解／酸無水物の加水分解 | 基質の構造 ＝ `detect` が割る |

つまり **`detect` がほとんどの分岐をすでに解いている**。条件モーダルは
「同じ瓶で `detect` が2つ以上通ったときに、`condition.label` を並べて選ばせる」
という**一般の選択UI**として書けばよく、温度という概念を仕込む必要がない。

これは既存の `narrow(rule, sites)`（適用箇所が複数あるときにハイライトして選ばせる。
`reactor.js:2174`）の**1段上**にあたる。同じ流儀で:

```
瓶を押す
  → その瓶の reagentId を持つルールで detect を回す
  → 通ったルールが 0個 … §4 の応答
  → 通ったルールが 1個 … そのまま narrow(rule, sites)（＝いまと同じ経路に合流）
  → 通ったルールが 2個以上 … condition.label（無ければ label）で選ばせる → narrow(...)
```

**`execute` から先はいまと完全に同じ**。だから Undo・前後比較・機構ジャンプ・モーフィングは
何も足さずにそのまま効く。

### 2.5 呈色・検出は反応ルールにしない（`DETECTION_TESTS` を別に持つ）

銀鏡反応・フェーリング液・塩化鉄(III)・ニンヒドリンは**分子の構造を変えない**。
これを `REACTION_RULES` に混ぜると `apply` が「何もしない」ものになり、
`saveState()` が空の履歴を積む・前後比較が「変化なし」の2枚を出す、という壊れ方をする。

```js
// 呈色・検出。構造を変えないので apply を持たない
const DETECTION_TESTS = [
    {
        id: 'tollens',
        reagentId: 'ag_ammonia',
        // 陽性かどうかと、光らせる原子を返す（reactor の highlightAtoms に渡せる形）
        detect(mol) { /* findFunctionalGroups で aldehyde を拾う */ },
        positive: '銀が析出して試験管の内側が鏡のようになります（銀鏡反応）。…',
        negative: 'この分子に還元性の -CHO はありません。ケトンは同じカルボニル基でも…'
    },
];
```

- 実行経路は**既存の `info: true` ルールと同じ**（トーストで文を出して返る・`saveState()` を積まない）。
  すでに `onRuleClick` にその分岐があるので、**新しい実行経路を1本も足さない**
- 陽性のときは該当原子を `highlightAtoms` で光らせる（どこが効いたのかを図の上で見せる）

**厳密には変換でもあるが、アプリでは構造を変えないもの**（この判断を明記しておく）:

| 試薬 | 実際に起きること | アプリでの扱いと理由 |
|---|---|---|
| アンモニア性 AgNO₃ | R-CHO → R-COO⁻（アンモニウム塩） | **変えない**。高校の主眼は「銀が析出する＝還元性がある」。構造を変えると既存の `oxidize_aldehyde`（[O]）と二重になり、同じ変換に入口が2つできる |
| フェーリング液 | R-CHO → R-COO⁻ ＋ Cu₂O(赤色沈殿) | 同上 |
| KMnO₄/H⁺ を C=C に | 実際は酸化開裂まで進む | **変えない**。高校で扱うのは「赤紫色が消える＝C=C の検出」まで。開裂生成物は範囲外 |
| I₂ ＋ NaOH（ヨードホルム） | CHI₃（黄色沈殿）＋ カルボン酸塩 | **第1〜3段では変えない**（黄色沈殿の確認が主眼）。生成物まで出すなら新しい化学が要るので第4段（§5） |
| Na | R-OH → R-ONa ＋ H₂ | **第4段で変える**。`-COONa` を線1本で書く前例（v353・けん化）があるので `-ONa` も同じ書き方でできる。第3段までは「H₂ が発生します」の説明のみ |

---

## 3. 高校範囲でいくつ要るか

### 3.1 数え上げ（実数）

**変換する試薬 15本 ＋ 検出する試薬 5本 ＝ 20本。**
うち**既存の23ルールで中身が埋まるのが12本**、**新しい化学が要るのが8本**。

#### 変換する試薬（15本）

| # | 瓶 | 効く相手 | 既存ルール |
|---|---|---|---|
| 1 | 酸化剤 [O]（KMnO₄/H⁺・K₂Cr₂O₇/H⁺） | 1級/2級アルコール・アルデヒド | ✅ `oxidize_primary` `oxidize_secondary` `oxidize_aldehyde` `oxidize_tertiary_info` |
| 2 | 濃硫酸（加熱） | アルコール・カルボン酸＋アルコール・芳香環 | ✅ `dehydration_intra` `dehydration_inter` `esterification` `aromatic_sulfonation` |
| 3 | 希硫酸（加熱） | エステル・酸無水物 | ✅ `hydrolysis_ester` `hydrolysis_anhydride` |
| 4 | NaOH 水溶液（加熱） | エステル（油脂） | ✅ `saponification` |
| 5 | H₂ / Ni 触媒 | C=C・C≡C | ✅ `add_h2` |
| 6 | 臭素水 Br₂ | C=C・C≡C ／ **フェノール** | ✅ `add_br2` ／ ❌ 臭素化（2,4,6-トリブロモフェノール・白色沈殿） |
| 7 | 臭化水素 HBr | C=C・C≡C | ✅ `add_hbr` |
| 8 | 水 H₂O（酸触媒） | C=C・C≡C | ✅ `add_water` |
| 9 | Cl₂ / 鉄触媒 | 芳香環 | ✅ `aromatic_halogenation` |
| 10 | 混酸（濃HNO₃＋濃H₂SO₄） | 芳香環 | ✅ `aromatic_nitration` `aromatic_deactivated_info` |
| 11 | 無水酢酸 (CH₃CO)₂O | フェノール性-OH・アミノ基 | ✅ `acetylation_anhydride` `esterification_phenol_info` |
| 12 | 硫黄 S（加硫） | ジエン系ゴム | ✅ `vulcanization` |
| 13 | 金属ナトリウム Na | アルコール・フェノール（H₂ 発生） | ❌ |
| 14 | Sn（または Fe）＋ 濃塩酸 | ニトロ基の還元（ニトロベンゼン→アニリン） | ❌ |
| 15 | NaNO₂ ＋ HCl（0〜5℃・ジアゾ化） | 芳香族アミン | ❌ |

#### 検出する試薬（5本）

いずれも**既存の実装は無い**（説明文の中にあるだけ）。

| # | 瓶 | 陽性になる構造 | 見えるもの |
|---|---|---|---|
| 16 | アンモニア性硝酸銀 | -CHO（アルデヒド・還元糖） | 銀鏡 |
| 17 | フェーリング液 | -CHO（アルデヒド・還元糖） | 赤色沈殿 Cu₂O |
| 18 | 塩化鉄(III) FeCl₃ | フェノール性 -OH | 紫〜青紫の呈色 |
| 19 | ヨウ素 I₂ ＋ NaOH | CH₃CO- / CH₃CH(OH)- | 黄色沈殿 CHI₃ |
| 20 | ニンヒドリン | アミノ酸の -NH₂ | 紫色 |

**NaHCO₃（炭酸水素ナトリウム）** は迷ったが**入れる（21本目）か、入れないか**を実装時に決める:
高校では「カルボン酸は CO₂ を出すがフェノールは出さない」という**酸の強さの比較**で必ず出る。
ただし構造は変わらず気体が出るだけなので**検出試薬（6本目）として扱うのが素直**。
本書は 20本 ＋ 保留1本 として数え、第3段の着手時に判断する。

**入れないもの**: ルーカス試薬（範囲外）・カップリング試薬（試薬ではなく分子。§0）・
キサントプロテイン反応（対象がタンパク質。実体は既存のニトロ化）・
重合開始剤（`addition_polymerization` などは「並べた単量体をまとめる」操作であって
試薬を掛ける体験にならない。パレットには出さず自動案内のまま）。

### 3.2 20本という数が画面設計に意味すること

- **20本は一望できる数**（4×5 のグリッドで1画面に収まる）。段組みや検索は要らない
- ただし**初学者に20本を最初から見せるのは多い**。区分を2つに割る:
  **「変えるもの」15本 ／ 「調べるもの」5本**。この2語なら初学者にも読める
  （呈色試薬は「反応させる」のではなく「確かめる」ものだ、というのは高校化学の重要な区別でもある）
- **12本は既存ルールで中身が埋まる**ので、第1〜2段はデータを繋ぐだけで進む（§5）

---

## 4. 効かない組み合わせを選んだとき

手動実験は「反応しない組み合わせ」を選べることが価値（§1.2）だが、そこで手が止まると詰まりになる。

### 4.1 v422 の仕組みは**一部だけ**そのまま再利用できる

`findPartnerHints`（`reactor.js:1716`）は **PARTNER_CANDIDATES（エタノール・メタノール・酢酸・
グリセリン・フェノール）を実際に足した分子で全ルールの `detect` を回し、
「2分子に跨る箇所が生まれたルール」を拾う**。つまり答えられるのは
**「相手の分子が足りない」**という失敗だけで、これはパレットでも起きる
（フェノールに濃硫酸を掛けたが、エステル化の相手のカルボン酸が無い、など）。

**再利用の形**: `findPartnerHints(game, baseIds)` に**ルールの絞り込みを1つ足す**だけでよい。

```js
// 現在: 全ルールを見る
findPartnerHints(game, baseIds)
// 追加: この瓶に紐づくルールだけを見る（省略時は従来どおり全部）
findPartnerHints(game, baseIds, ruleIds)
```

キャッシュ（`cachedPartnerHints`）のキーに `ruleIds` を混ぜれば、
既存の「結合のつながりが変わったときだけ計算する」最適化もそのまま効く。

### 4.2 3段階で返す（上から順に、当たったところで止める）

瓶を押して `detect` がすべて空だったとき:

| 段 | 条件 | 返すもの |
|---|---|---|
| ① | 相手の分子を足せば通るルールがある | **「＋ 酢酸 を呼び出す → エステル化」**（v422 と同じ緑のボタン。押すと `summonMolecule`）。**そのまま次の一手になる** |
| ② | 相手を足しても通らない | **「この試薬が効くのは〜です」**＝ 瓶の `acts`（§2.2）。例:「濃硫酸が効くのは、アルコール（脱水）・カルボン酸とアルコール（エステル化）・ベンゼン環（スルホン化）です。いまの分子にはどれもありません」 |
| ③ | **効かないこと自体が教材**の組み合わせ | 瓶ごとの**固定の解説文**。例: 臭素水×アルカン →「脱色しません。臭素水が脱色されるのは C=C や C≡C があるときで、これが不飽和結合の検出法です」 |

- ②の文は**瓶ごとに1つ**（20本分）でよい。ルール23件それぞれに書き写す必要はない ——
  「どの官能基に効くか」は瓶の性質であってルールの性質ではないから
- ③は**教科書の「〜しない」がそのまま出題になる**組み合わせだけを列挙する。
  最低限そろえたいのは次の6つ（いずれも入試の定番）:

  | 掛けたもの | 返す内容 |
  |---|---|
  | 臭素水 × アルカン・ベンゼン | 脱色しない（不飽和結合の検出。ベンゼンは付加より置換） |
  | 酸化剤 [O] × 3級アルコール | 酸化されにくい（**既存の `oxidize_tertiary_info` がそのまま使える**） |
  | 酸化剤 [O] × ケトン | それ以上酸化されにくい（アルデヒドとの違い） |
  | 銀鏡・フェーリング × ケトン | 陰性。同じカルボニル基でも還元性は -CHO だけ |
  | FeCl₃ × アルコール（フェノールでない -OH） | 呈色しない。紫になるのは環に直結した -OH |
  | NaHCO₃ × フェノール | CO₂ が出ない（炭酸より弱い酸）。カルボン酸との区別 |

- **叱らない。**「間違いです」ではなく「効くのはこれ」を返す。
  自動案内が「できること」を教える面なら、パレットは**「できないこと」を教える面**になる ——
  これが入口を2つ持つ意味そのもの

### 4.3 空振りでも履歴を汚さない（守るべき不変条件）

反応が起きなかったとき、**分子は1原子も変わってはならない**。
既存の `info` ルールの経路（`saveState()` を積まずトーストだけ）に合流させることで満たす。
テストで固定する（§5・第1段 (c)）。

---

## 5. 段階に割る

各段は独立して入れられ、途中で止めても壊れない。**新しい化学が入るのは第4段だけ**。

### 第1段（最小）: 試薬という概念を通す。**瓶3本**

化学は1つも増やさない。既存ルールに `reagentId` を足し、瓶から逆引きできることだけを作る。

**作るもの**
- `REAGENTS` に **3本**: 臭素水 Br₂ ／ 酸化剤 [O] ／ 濃硫酸
- 該当ルール7件に `reagentId` を付ける（`add_br2` / `oxidize_primary` `oxidize_secondary`
  `oxidize_aldehyde` `oxidize_tertiary_info` / `dehydration_intra` `dehydration_inter`
  `esterification` `aromatic_sulfonation`）
- `dehydration_intra` `dehydration_inter` に `condition`（160〜170℃ / 130〜140℃）
- 瓶 → `detect` → 0個 / 1個 / 2個以上 の振り分け（§2.4）と、2個以上のときの選択面
- 効かなかったときの応答（§4 の①②。③は第2段でもよい）

**なぜこの3本か**（3本で設計上の論点が全部通る）
- **臭素水** … 行き先が1つ・条件なし。**空振りの体験**（アルカンに掛けても脱色しない）が最短で作れる
- **酸化剤 [O]** … 1本の瓶に4ルール。**`detect` が基質で割る**ことの確認
- **濃硫酸** … 1本の瓶に4ルールで、うち2つは**温度でしか割れない**。
  **条件選択という設計上いちばん重い部分を、最小構成のまま通せる**

**テストで固定すること**
- **RG1**: すべての `reagentId` が `REAGENTS` に実在する／`REAGENTS` の `id` に重複がない
  （RX3 の `mechanismId` 死にリンク検査と同じ機械検証）
- **RG2**: エタノールに濃硫酸を掛けると**行き先が2つ**出て、
  160〜170℃ を選ぶとエテン・130〜140℃ を選ぶとジエチルエーテル（正準コードで確認）
- **RG3**: エタンに臭素水を掛けると**説明が返り、分子が1原子も変わらない**
  （正準コードが同一・Undo スタックの深さが変わらない）
- **RG4**: 同じ分子で、**パレット経由で出せる反応の集合 ⊆ 自動案内のボタンの集合**
  （＝ パレットが独自の反応を持っていないことの保証）

### 第2段: 残りの既存ルールに瓶を割り当てる（**12本すべて** → 実装は13本。§8.2）

新しい化学は足さない。§3.1 の 1〜12 番を全部埋め、`reagentId` を持たないルールが
「試薬なしで起こるもの」（グルコースの環化・開環）と `info` だけになるようにする。
§4 の③（効かないことが教材になる6組）もここで入れる。

**テストで固定すること**
- **RG5**: `reagentId` を持たない**実行できるルール**は、環化・開環の3件**だけ**
  （反応を足したときに瓶の割り当てを忘れると落ちる）
  → 実装では **環化3件＋重合2件の5件**。重合は「並べた単量体をまとめる」操作であって
  試薬を掛ける体験にならないので瓶を持たない（§3.1 の「入れないもの」がそのまま効く）
- **RG6**: 代表10分子について、**パレット経由の反応の集合 ＝ 自動案内のボタンの集合**
  （第1段の ⊆ を ＝ に強める。入口が2つでも中身が1つであることの保証）

### 第3段: 検出試薬 5本（＋NaHCO₃ の判断 → **入れた**。§8.2）

`DETECTION_TESTS` を新設（§2.5）。**構造を変えず、Undo 履歴も積まない。**

**テストで固定すること**
- **RG7**: 陽性・陰性の判定。アセトアルデヒド→銀鏡陽性／アセトン→陰性、
  フェノール→FeCl₃ 陽性／エタノール→陰性、アラニン→ニンヒドリン陽性／酢酸→陰性
- **RG8**: **どの検出試薬を掛けても分子が変わらない**（全 `DETECTION_TESTS` × 代表分子の総当たりで
  正準コードが不変・Undo スタックの深さが不変）

### 第4段: 新しい化学が要る変換 3〜4本（**未着手**）

ここは**試薬パレットの話ではなく反応の話**なので、[DESIGN_reaction_execution.md](DESIGN_reaction_execution.md)
側の項目として立てる。パレットは第2段で完成しているので、**瓶とルールを足すだけで載る**。

| 追加する反応 | 生成物 | 備考 |
|---|---|---|
| ニトロ基の還元（Sn＋HCl） | ニトロベンゼン → アニリン | 高校の最重要経路。ライブラリに両方ある |
| フェノールの臭素化（Br₂ 水） | 2,4,6-トリブロモフェノール | **既存の Br₂ 瓶に行き先が増える**（付加とは `detect` が割る） |
| 金属ナトリウム Na | R-OH → R-ONa ＋ H₂ | `-COONa` を線1本で書く前例（v353）に倣う |
| ジアゾ化＋カップリング | アニリン → 塩化ベンゼンジアゾニウム → p-ヒドロキシアゾベンゼン | 瓶は NaNO₂＋HCl のみ。カップリングは**フェノールを相手にした2分子反応**。生成物はライブラリに登録済み |

~~ヨードホルムの CHI₃ 生成もここに入れるかは、第3段の呈色で足りるかを見てから決める。~~
→ **本書を書いた後（2026-08-04 ヨウ素レーン）にすでに実装済み**。第2段で
「変えるもの」の瓶を付けた（§8.2）。

---

## 6. 未決事項

**第3段までで全部決着した。** 決めた内容は §7・§8 に書いてある。

- ~~**瓶の面をどこに置くか**~~ … **分子モーダルの「⚗ 反応」節の中**（`DESIGN_molecule_modal.md` §6-1。別の面は作らない）
- ~~**KMnO₄ と K₂Cr₂O₇ を1本にまとめる判断**~~ … **1本にまとめた**（瓶 `oxidant`・第1段）。
  高校ではどちらも [O] で生成物が同じ。2本に分けると `apply` に「どちらの瓶か」の分岐が入り、
  §2.1 の「関数を書き換えない」が崩れる
- ~~**分子を選んでいるときの絞り込み**~~ … **自動案内と `siteFilter()` を共有する**（§7.6）
- ~~**NaHCO₃ を入れるか**~~ … **入れる**（§8.2 の3点目・§8.3）

残っているのは **第4段（新しい化学が要る変換 3〜4本）だけ**。

---

## 7. 第1段の実装で分かったこと（v700〜v703・2026-08-05）

設計どおりに通ったところは書かない。**設計を書いた時点では見えていなかった落とし穴**だけを残す。

### 7.1 ⚠ 濃硫酸の2択は、直すまで**原理的に出せなかった**（最大の落とし穴）

§2.4 は「行き先が温度でしか割れないのは分子内脱水／分子間脱水だけ」と書いたが、
**実装ではこの2つが同時に `detect` を通ることが構造的にありえなかった**。

| ルール | 通る条件（v701 まで） |
|---|---|
| `dehydration_intra` | キャンバス**全体**の -OH が**ちょうど1つ** |
| `dehydration_inter` | **別分子に** -OH が**2つ以上** |

`mol` は分子が何個あってもキャンバス全体で1つの `Molecule` なので、この2つは**排他**になる。
エタノール1つなら intra だけ、2つ並べると intra が消えて inter だけ ——
つまり**第1段でいちばん重い部分（条件選択）が一度も起きない**ままになる。

原因は `dehydration_intra` の適用条件が「**その分子の中の** -OH の数」ではなく
「キャンバス全体の -OH の数」を数えていたこと。これは試薬とは無関係の**既存のバグ**でもある:

- エタノールを2つ並べただけで、どちらも分子内脱水できなくなる
- エタノール＋アセトンでも「カルボニルがある」と判定されて消える（`blocking` も全体を見ていた）

**直し方**: `componentOf(mol, oId)` で候補のアルコールが属する分子を取り、
-OH の数も阻害基の有無も**その分子の中だけ**で数える（`dehydration_inter` が
「別分子どうしのみ」を見るのと同じ粒度）。`apply` は1行も変えていない。

> **教訓**: 「`detect` がほとんどの分岐をすでに解いている」（§2.4）は正しかったが、
> **`detect` が見ている範囲がキャンバス全体か1分子かは、ルールごとにばらばら**だった。
> 第2段で12本に広げるときは、瓶を割り当てる前に**そのルールの `detect` が
> 何を単位に数えているか**を1件ずつ確かめること。同種の全体数えが残っていれば、
> そこでも「2つの行き先が同時に立たない」が静かに起きる。

### 7.2 モーダルの「押したら閉じる」から瓶を外す必要がある

`DESIGN_molecule_modal.md` 第2段は、モーダルの中のボタンを**捕獲フェーズの1本**で
まとめて「押したら閉じる」にしている。瓶をその中に置くと、
**空振りの説明が出た瞬間に画面ごと消える**。

`#mm-reagents` を節ごと除外し、**閉じるかどうかは `runReagentHit` が
「反応が進むときだけ」自分で決める**形にした。条件選択のボタンも同じ節の中に出るので、
節ごと外すのが正しい（ボタン単位で例外を書くと、増やすたびに書き足しになる）。

### 7.3 案内のキャッシュは瓶ごとに分ける

`cachedPartnerHints` のキーは「選択の原子ID ＋ 分子のトポロジー」だった。
`ruleIds`（＝ どの瓶か）を混ぜ忘れると、**濃硫酸の空振りで作った案内が
次に押した臭素水の答えとして返る**。キーに `ruleIds` を足して解決。

### 7.4 320px では瓶3本が1行に入らない（設計の 4×5 は 320px では成り立たない）

`DESIGN_molecule_modal.md` §6-2 は「20本（4×5・44px札）で 244px」と見積もっていたが、
**320px 端末のモーダルの中身は 288px しかない**（実測）。
札に「名前＋示性式」の2行を入れると最小幅は 78px 必要で、3列 ＝ 246px。
`padding` を差し引くと 320px では**入らない**。

`repeat(auto-fit, minmax(78px, 1fr))` にして、**入らなければ折り返す**ようにした
（320px は 2＋1 の2行、375px 以上は1行3列）。横スクロールを作らないことを優先する。
**第2段で20本にしても CSS は書き直さない**（列数だけが増える）が、
§3.2 の「4×5 で一望」は **375px 以上での話**だと読み替える必要がある。
→ **実測で確かめた（§8.5）。CSS は1行も書き直していない。**
ただし 320px は**2列固定**で、18本だと瓶だけで 558px ＝ 1画面ぶんになる。
「4×5 で一望」が成り立つのは 1280px（4列）だけ。

実測（v703）:

| 画面 | 瓶の寸法 | 行数 | 格子幅 | モーダル中身 |
|---|---|---|---|---|
| 320×568 | 105.0 × 44.0 | 2行（2＋1） | 216px | 288px |
| 375×667 | 84.5 × 44.0 | 1行3列 | 265.5px | 337.5px |
| 1280×800 | 114.0 × 44.0 | 1行3列 | 354px | 450px |

床は **44px**（32px の床に対して余裕を持たせた）。瓶は**並んだ標的の格子**で、
隣を押し間違えるとまったく別の試薬を掛けてしまうため、Apple の 44pt に合わせている。

### 7.5 §4.2 ③ は `oxidize_tertiary_info` でそのまま賄えた（ただし返る場所が違う）

3級アルコールに [O] を掛けると、`oxidize_tertiary_info` が**1件だけ**当たるので
`hits.length === 1` の道に入り、`onRuleClick` の `info` 分岐でトーストが出る
（分子は変わらず・モーダルも開いたまま）。設計 §4.2 ③ の1つが**新しい仕組みなしで**成立した。

⚠ ただし**返る場所が違う**: 空振り（0件）の説明は `#mm-reagent-note` に残るが、
`info` ルールの解説は**トーストなので数秒で消える**。同じ「効かない」の説明が
2か所に出る形になっている。第2段で §4.2 ③ の残り5組を入れるときに、
**`info` ルールの文面も瓶の節に残す**か決めること（未決）。
→ **決着: ノートに寄せた（§8.4）。**

### 7.6 §6 の未決「選択があるときの絞り込み」への答え: **自動案内と共有する**

実物を触って決めた（§6 の3つめ）。`refresh()` の中にあった `siteAllowed` を
`siteFilter()` として切り出し、**瓶（`reagentHits`）と自動案内が同じ関数を呼ぶ**。

理由は使い勝手より**構造**にある。絞り込みを2か所に書くと、条件がずれた瞬間に
「瓶からだけ出せる反応」が生まれ、**RG4（入口が2つでも中身は1つ）が静かに破れる**。
1つの関数を共有していれば、ずれようがない。

---

## 8. 第2段・第3段の実装で分かったこと（v780〜v782・2026-08-06）

第1段と同じく、**設計を書いた時点では見えていなかったこと**だけを残す。

### 8.1 ⚠ `detect` の全体数えは芳香族に2件残っていた（§7.1 の申し送りの答え）

瓶を割り当てる前に、**27件のルールの `detect` が何を単位に数えているかを1件ずつ確かめた**。
結果は次のとおり（「1分子」＝ 連結成分ごと、「跨ぐ」＝ 2分子反応なので跨いで当然）。

| 単位 | 件数 | ルール |
|---|---|---|
| 1分子（正しい） | 15 | `oxidize_primary` `oxidize_secondary`（`alcoholOxidationAllowed` が `componentOf` で見る）／`oxidize_aldehyde` `oxidize_tertiary_info` `acetylation_anhydride` `hydrolysis_ester` `hydrolysis_anhydride` `saponification` `iodoform` `add_br2` `add_h2` `add_hbr` `add_water` `aromatic_sulfonation` `dehydration_intra`（v702 で修正済み） |
| 跨ぐのが正しい | 6 | `esterification` `esterification_phenol_info` `dehydration_inter` `addition_polymerization` `diene_polymerization` `vulcanization` `condensation_polymer_info`（いずれも `componentOf` で「別分子どうし」を明示している） |
| **全体数えのまま ＝ 直した** | **2** | `aromatic_nitration` / `aromatic_sulfonation` / `aromatic_halogenation` が共有する **`aromaticSiteClass`** と、**`aromatic_deactivated_info`** |
| 全体数えだが**意図的** | 3 | `cyclize_glucose_beta` `cyclize_glucose_alpha` `open_glucopyranose` |

**直した2件**（どちらも v780。`apply` は1行も変えていない）:

1. **`aromaticSiteClass`** … 「置換して同じ生成物になる位置」の鍵を**キャンバス全体**の
   正準コードで取っていた。同じ形の分子が2つあると鍵まで同じになるので、
   **ベンゼン2個で置換できる箇所が1件に潰れる**（実測。2件であるべき）。
   さらに `siteFilter()` で2つめだけを選ぶと、生き残った箇所が1つめの側なので
   **候補が0 ＝ 混酸の瓶を押しても「効きません」が返る**。
   ⚠ ベンゼン＋トルエンは前から4件で正しかった ——
   **同じ分子を並べたときだけ**壊れるので、ふつうの操作では気づけない。
   連結成分だけを複製し、成分の同一性を鍵に混ぜて分けた。
2. **`aromatic_deactivated_info`** … 全キャンバスの芳香族原子を1つの集合にまとめて
   電子求引基を数えていた。**1つずつしか持たない分子を2つ並べただけで警告が出る**
   （ニトロベンゼン2個 → 1件／ニトロベンゼン＋ベンゼンスルホン酸 → 1件。本来0件）。
   単独のニトロベンゼンでは0件で正しかったので、これも**並べたときだけ**の嘘。

**意図的な全体数え（直さない）**: 糖の環化・開環は `isRegisteredCompound` で
**キャンバス全体の正準コードが登録エントリと一致すること**を要求する。
グルコース＋エタンでは候補が消える（実測）が、これは**ガード**である ——
`applyCyclize` は登録エントリの座標を丸ごと移して立体を作るので、
1分子であることが前提。ここを緩めると誤った立体を生む。**瓶も持たない**（§5 第2段）ので
パレットからは出ない。

> **教訓の更新**: §7.1 は「`detect` が見ている範囲はルールごとにばらばら」と書いたが、
> より正確には **`detect` が呼ぶ共通の下ごしらえ（`aromaticSiteClass` のような
> 正準コードを取る関数）が全体を見ている**のが残りの温床だった。
> ルール本体を読むだけでは見えない。**正準コードを取っている場所は、
> 何を複製しているかまで見ること。**

### 8.2 瓶は 20本ではなく **18本**（§3.1 の数え直し）

§3.1 は「変える15 ＋ 検出5 ＝ 20本、うち既存ルールで埋まるのが12本」と数えたが、
第2段の実装時点では次のとおりになった。

| 区分 | 本数 | 内訳 |
|---|---|---|
| 変えるもの | **13** | §3.1 の 1〜12（既存ルールで埋まる12本）＋ **ヨウ素・NaOH** |
| 調べるもの | **5** | 銀鏡 / フェーリング / FeCl₃ / ニンヒドリン / **NaHCO₃** |

ずらした点が3つある。

1. **ヨードホルムは「変えるもの」**。§2.5 は「第1〜3段では構造を変えない（生成物まで
   出すなら第4段）」としていたが、**`iodoform` は本書を書いた後（2026-08-04 ヨウ素レーン）に
   CHI₃ とカルボン酸塩まで作る反応として実装済み**だった。これを「調べるもの」に置くと
   **RG8（検出は分子を1原子も変えない）と食い違う**ので、変えるものに置く。
   §2.5 の表のヨードホルムの行は**もう当てはまらない**。
2. **NaHCO₃ は入れる**（§3.1・§6 の保留の決着）。§4.2 ③ の6組の最後
   「NaHCO₃ × フェノール（CO₂ が出ない）」が、この瓶が無いと画面のどこからも出せない。
   ヨードホルムが変えるものへ移ったので、調べるものはちょうど5本になる。
3. **`esterification_phenol_info` は無水酢酸ではなく濃硫酸に付ける**（§3.1 の表は
   無水酢酸の行に置いていた）。この解説の中身は「濃硫酸でエステル化しようとしても
   フェノールでは進まない」なので、**濃硫酸を押したときに返る**のが素直で、
   §4.2 ③ がそのまま成立する。無水酢酸に付けると、フェノールに掛けたときに
   アセチル化と2択になってしまう。

残る「新しい化学が要る」ものは §3.1 の 13〜15（Na / Sn＋HCl / ジアゾ化）と
フェノールの臭素化で、**第4段のまま**。

### 8.3 §4.2 ③ の6組は**全部そろった**

| 掛けたもの | どこから返るか |
|---|---|
| 臭素水 × アルカン・ベンゼン | 瓶の `miss`（第1段） |
| 酸化剤 × 3級アルコール | `oxidize_tertiary_info`（info ルール） |
| 酸化剤 × ケトン | 瓶の `miss`（第1段） |
| 銀鏡・フェーリング × ケトン | `DETECTION_TESTS` の `negative` |
| FeCl₃ × アルコール | `DETECTION_TESTS` の `negative` |
| NaHCO₃ × フェノール | `DETECTION_TESTS` の `negative` |

### 8.4 §7.5 の未決「説明の返る場所」は **ノートに寄せる**

**瓶を押して返る説明はすべて `#mm-reagent-note` に集める**（トーストは使わない）。

- v703 では `info` ルールだけが `onRuleClick` 経由でトーストに出て**数秒で消え**、
  空振り（0件）の説明はノートに残っていた ——「効かない」という**同じ答えが2か所に割れて**いた。
  本数が3本のうちは目立たないが、18本になると「押したのに答えがどこかへ消えた」が頻発する
- 寄せ先をノートにしたのは、**押した瓶のすぐ下に、消えずに出る**方が読み切れるから。
  検出（第3段）の陽性/陰性も同じ場所に返るので、**瓶の答えは必ずここ**と言い切れる
- **自動案内側は従来どおりトースト**。あちらは押すとモーダルを閉じてキャンバスへ返る
  流れなので、ノートが画面から消えている（＝ノートに書いても読めない）

### 8.5 320px は 2列。18本で 2.7画面になる

§7.4 の CSS（`repeat(auto-fit, minmax(78px,1fr))`）は**1行も書き直していない**。
列数だけが増えた。実測（フェノールを1つ置いて分子モーダルを開いた状態）:

| 画面 | 瓶の寸法 | 列×行 | 格子 | モーダル中身 | 横あふれ | 32px未満 |
|---|---|---|---|---|---|---|
| 320×568 | 105.0 × 44.0〜53.2 | 2列×10行 | 216×558 | 1375px（可視511 ＝ **2.7画面**） | なし | 0件 |
| 375×667 | 84.5 × 44.0〜53.2 | 3列×7行 | 265.5×427 | 1244px（可視600） | なし | 0件 |
| 1280×800 | 84.0 × 44.0〜53.2 | 4列×6行 | 354×377 | 1186px（可視1190 ＝ 1画面） | なし | 0件 |

- 行数には**区分の見出し**（変えるもの／調べるもの）の折り返しを含む。
  見出しは格子の中に `grid-column: 1 / -1` の全幅1行として入れた ——
  格子を2つに割ると、列数が変わったときに区分ごとに折り返しがずれて
  「同じ大きさの札が並ぶ」という読み方が崩れる
- 名前が2行になる札（アンモニア性硝酸銀・水酸化ナトリウムなど）はその行だけ 53.2px に伸びる。
  **床の 44px は割らない**
- ⚠ **§3.2 の「20本は4×5で一望できる」は 320px では成り立たない**（§7.4 に続く2度目の確認）。
  320px は2列固定で、瓶だけで 558px ＝ 1画面ぶん。一望できるのは 1280px（4列）だけ。
  ただし**横スクロールは1つも作っていない**ので、縦に読めば必ず全部に届く

### 8.6 テストの番号（この段で足したもの）

| 番号 | 何を固定したか | 否定対照 |
|---|---|---|
| RG5 | 瓶を持たない実行ルールは環化3件＋重合2件の**5件だけ** | `reagentId` を1つ外した写しを同じ式で数えると必ず拾う |
| RG6 | 代表12分子で「瓶から出せる反応 ＝ 自動案内のうち瓶を持つ反応」（両向きの差0・延べ12件以上） | 1件抜いた集合は同じ比較で食い違いとして出る |
| RG7 | 検出13組の陽性/陰性（陽性5件以上・陰性6件以上） | 酢酸とアニリンを**並べても**ニンヒドリンは陰性 |
| RG8 | 代表7分子 × 検出5本 ＝ **35通り**で正準コード・原子数・Undo が不変 | 同じ数え方で酸化剤を押すと必ず動く |
| RG9 | 芳香族の `detect` は分子ごと（ベンゼン2個→2件・別分子に1つずつ／ニトロベンゼン2個→0件） | v779 と同じ全体数えの式を再現すると1クラスに潰れる／2,4-ジニトロフェノールでは1件出る |
| RG10 | 新しい6本で、瓶からの生成物が自動案内と同じ正準コード | けん化と加水分解は違うコード |
| RG11 | 瓶から引いた解説はノートに残り**トーストに出ない** | 自動案内の同じボタンでは目印が必ず上書きされる |
| MM9 | 320px で横あふれ0・32px 未満0・瓶は44px 以上 | 18列×200px にすると必ずあふれる |

---

## 9. `miss` は「陰性で何が説明できるか」の欄である（qa の需要から分かったこと・2026-08-06）

### 9.1 空振りを教材にする方針が、想定以上に当たっていた

§4.2 ③ で決めた「**効かない瓶を押しても叱らない・空振りそのものを教材にする**」が、
一問一答（qa）の**9項目**をそのまま拾った。設計時に見込んでいた以上の当たりで、
理由は需要側の事情にある:

> 一問一答には**否定形の知識項目が構造的に多い**。作問基準で
> 「排反肢を置かない・正解数を2〜4に分散させる」を課しているので、
> 「AもBもCも正しい」型ばかりでは肢が作れず、
> **「これだけは違う」を問う項目が自然に増える**（qa レーン・283項目の棚卸し）。

実際に拾われたもの:

| qa の項目 | 拾った文 |
|---|---|
| `org.carbonyl.ketone-no-reduce`（ケトンは還元性を示さない） | 銀鏡反応の `negative`「同じ C=O でもカルボニル炭素に水素が無いので酸化されない」 |
| `org.phenol.def`（側鎖に付いた -OH はアルコール） | 塩化鉄(III) の `negative` が**ベンジルアルコールを名指し**して境目を陰性側から説明 |
| `org.phenol.weaker-than-carbonic` | NaHCO₃ の陽性（酢酸）と陰性（フェノール）が**対で2項目**を埋めた |
| `org.bio.sucrose`（還元性を示さない） | フェーリング液の `negative` |

**従来の reactor は「効かない」で終わっていたので、qa 側は `none` にするしかなかった。**
`miss` / `negative` に理由を書いた瞬間、否定形の項目が拾えるようになった。

### 9.2 したがって、瓶の採否は「陽性で何が見えるか」だけで決めない

**次に瓶を足すときの判断材料**として、基準を1つ足す:

> その瓶は、**陰性のときに何を説明できるか**。
> 陽性の絵より陰性の説明のほうが多くの項目を埋めることがある。

### 9.3 ⚠ ただし `miss` の一般化は、そのまま「逆を教える」事故になる（v816 で実発生）

`br2_water` の `miss` は v815 までこう書かれていた:

> 赤褐色が消えないこと自体が「不飽和結合が無い」ことの証拠で、これが臭素水による検出法です
> （**ベンゼン環は付加ではなく置換なので、この条件では脱色しません**）。

括弧の中が**ベンゼン環一般に広げすぎている**。フェノールとアニリンは触媒なしで置換が進み、
2,4,6-トリブロモ体の白色沈殿ができる（高校の必修事項）。**瓶が効かない**うえに
**この文が返る**ので、アプリは教科書と正反対のことを教えていた。

**`miss` は構造を見ずに返る固定文**（§4.2 ③ でそう決めた）なので、
**例外のある一般化を書いてはいけない**。書くなら例外まで文の中に入れる。v816 の書き分け:

1. **ベンゼン環一般** … 付加ではなく置換で反応し、その置換にも鉄などの触媒が要る ＝ この条件では脱色しない
2. **活性化された環（フェノール・アニリン）** … -OH / -NH₂ が環に電子を押し込むので
   触媒なし・常温で置換が進み、2,4,6-トリブロモ体の**白色沈殿**ができる

②は `bromination_activated_ring` として実装済みなので、**この `miss` が実際に出るのは①のときだけ**。
その結果、`org.phenol.bromination`（フェノールは触媒なしで置換が進む）は
**陽性側（フェノールを描いて瓶を押す）からも陰性側（ベンゼンで押したときの説明）からも**説明できる形になった。

**書くときの手順**: `miss` に「〜は反応しません」と書いたら、
**その文が当てはまらない分子を1つ思い出して、実装があるか確かめる**。
無ければ `miss` を狭めるか、反応を足すかのどちらかで、放置は「逆を教える」を意味する。

### 9.4 テストの番号（この回・接頭辞 RC）

| 番号 | 何を固定したか | 否定対照 |
|---|---|---|
| RC1 | フェノール・アニリンの臭素化が**登録済み 2,4,6-トリブロモフェノールと同じ正準コード**／フェノール2個で**2件**（分子ごとに数えている証明） | ベンゼン・トルエン・ナフタレン・シクロヘキサン・エタン・アニソール・アセトアニリドの**7件が0件**。瓶の経路でもベンゼンは1原子も変わらず、`miss` に例外の書き分けが入っていること／旧い一般化が残っていないことを本文で検査 |

---

## 10. 瓶は 18本 → **20本**（H–X 付加の枝分かれ・v818）

§8.2 で「瓶は 20本ではなく **18本**」と数え直したが、v818 で**変えるものが 15本**になり、
合計は **20本**（変えるもの15 ＋ 調べるもの5）に戻った。増えたのは
**塩化水素 HCl と ヨウ化水素 HI** の2本。

### 10.1 増やしたのに「新しい化学」は1つも増えていない

§1.1 の「同じ1つのルール表を、2つの向きから引く」をさらに一段進めて、
**瓶とルールを同じ表から生成**している（`HYDROGEN_HALIDES`）。

- 3本の瓶の `acts` / `miss` は表から作るので**文面が枝ごとにずれない**
- 3本のルールは `detect` を**同じ関数オブジェクトとして共有**する
  （RC3 が `new Set(rules.map(r => r.detect)).size === 1` で固定）
- 増えたのは**ハロゲンの種類だけ**。マルコフニコフ則はコードの中に1か所しかない

**次に「同じ規則で試薬だけ違う」ものを足すときは、この形にする。**
候補は Cl₂/Br₂ の芳香族置換（いまは `cl2_fe` の1本だけで、臭素化は鉄触媒側が無い）。

### 10.2 画面への影響（§8.5 の数え直し）

320px は2列固定なので、20本だと瓶だけで 11行 ＝ **3画面ぶん**になる（18本のときは 2.7画面）。
**横スクロールは1つも増えていない**（MM9 が 320px であふれ0・32px未満0を見ている）。
一望できるのは 1280px の4列だけ、という §8.5 の結論は変わらない。

⚠ **これ以上「変えるもの」を増やすなら、区分をもう一段割ることを先に決める**
（付加／置換／酸化還元／加水分解…）。20本が2列に縦積みされる形はもう限界に近い。

> **[2026-08-12 追記] この「先に決める」への答えは §10.5。** 区分割りは**先送り**（ユーザー判断）で、
> 代わりに「瓶を1本足す前に既存の瓶に付けられないか先に見る」を規約にした。いま **21本**。

### 10.3 テストの番号（この回・接頭辞 RC）

| 番号 | 何を固定したか | 否定対照 |
|---|---|---|
| RC1 | フェノール・アニリンの臭素化が登録済み 2,4,6-トリブロモフェノールと同じ正準コード／フェノール2個で2件 | 陰性7件が0件。瓶の経路でもベンゼンは不変で、`miss` に例外の書き分けがあり旧い一般化が無い |
| RC2 | 側鎖酸化・酸化開裂の生成物**5組**が登録エントリと一致（安息香酸・フタル酸・テレフタル酸・酢酸2分子・アセトン＋酢酸） | 陰性17件が0件。範囲外の案内は4件で理由の語（二酸化炭素・アジピン酸・安息香酸）を含み、範囲内5分子では0件 |
| RC3 | H–X の3本が `detect` を**共有**／塩化ビニルほか3組が登録エントリと一致／プロペンで3本ともマルコフニコフ則 | 同じエチレンで**3種類**の生成物になる（1種類なら瓶を見分けていない）。エタンでは3本とも空振り |

### 10.4 v819 で瓶は増えていない（ルールだけ2本増えた）

酸塩基（§10.6 of DESIGN_reaction_execution.md・qa の需要7項目）を入れたが、
**瓶は 20本のまま**。中和は既存の「水酸化ナトリウム」、弱酸の遊離は既存の「希硫酸」に付けた。

これは §1.1 の「瓶は新しい化学を1つも持たない」がそのまま効いた例で、
**瓶を増やさずに届く範囲がまだ残っている**ことを示している。
瓶を1本足す前に、**既存の瓶に足りない反応が無いか**を先に見る。

`acts` と `miss` は反応を足したら**必ず書き足す**（`naoh_aq` は「けん化」だけを名乗っていた）。
今回 `naoh_aq` の `miss` に足したのは陰性側の説明:

> なお、**アルコールの -OH は中和されません**（中性なので塩をつくらない）。
> 同じ -OH でも、カルボン酸・フェノールの -OH だけが酸性です。

§9.2 の基準（陰性で何が説明できるか）に沿った1行で、RC4 がこの文が返ることまで見ている。

| 番号 | 何を固定したか | 否定対照 |
|---|---|---|
| RC4 | 酸 ⇄ 塩の**往復6組**が登録エントリと同じ正準コード（酢酸／フェノール／ベンゼンスルホン酸）。サリチル酸は候補2件 | 中性の -OH をもつ7分子 × 2ルール ＝ 14通りが0件。瓶の経路でもエタノールは不変で、`miss` に陰性側の説明が返る |

### 10.5 規約: 瓶を1本足す前に、既存の瓶に付けられないかを先に見る（2026-08-12・ユーザー判断）

§10.2 の「これ以上増やすなら区分をもう一段割ることを先に決める」に対する答え。
**区分割りは先送りする。** 代わりにこの規約を置く。

#### いまの数（実測）

| | 数 |
|---|---|
| 瓶の総数 | **21本**（変えるもの **16** ／ 調べるもの **5**） |
| 320px（2列） | 瓶だけで **11行・608px** ＝ 画面高 568px の **1.07画面ぶん** |
| 320px のモーダル中身 | **1651px**（可視 507px ＝ **3.3画面ぶん**） |
| 横あふれ | **0件**（§8.5 から変わらず。MM9 が瓶21本で見ている） |
| 瓶の寸法 | 105.0 × 44.0〜53.2px（床の 44px は割らない） |

#### 規約1: 瓶を1本足す前に、既存の瓶に付けられないかを先に見る

§1.1 の「瓶は新しい化学を1つも持たない（ルール表を別の向きから引くだけ）」の帰結で、
**瓶を増やさずに届く範囲がまだ残っている**（§10.4）。実績で見ても、届く方が多い:

| 版 | 足した反応 | 瓶 |
|---|---|---|
| v819 | 中和・弱酸の遊離 | **増やさない**（既存の「水酸化ナトリウム」「希硫酸」に付けた） |
| v880 | 付加重合（ポリアセチレン） | **増やさない**（20本のまま） |
| v881 | 縮合重合（ナイロン66・PET） | **増やさない**（20本のまま） |
| v882 | 二糖のグリコシド結合の加水分解 | **増やさない**（既存の「希硫酸」に付けた） |
| v883 | アルコールとエーテルの見分け | **金属ナトリウムを1本足した**（20 → 21本） |

直近5回で瓶が増えたのは **1回だけ**。足すと決める前に、この順で確かめること:

1. **その変換をふつう何で起こすか**を言葉にする。既にその名前の瓶があるなら、そこへ付ける
2. 付けたら **`acts` と `miss` を必ず書き足す**（§10.4。`naoh_aq` は「けん化」だけを名乗っていた）。
   瓶の説明が実際に起きることと食い違うのは、瓶が足りないより悪い
3. 新しい瓶が要るのは「**既存のどの瓶の名前でも嘘になる**」ときだけ。
   金属ナトリウムがそれ（水酸化ナトリウムでも希硫酸でも、アルコールと反応する理由を説明できない）
4. 「同じ規則で試薬だけ違う」ものは **§10.1 の形**（1つの表から瓶とルールを生成）にする。
   HBr / HCl / HI が3本に見えて化学は1つなのがこれ

#### 規約2: 区分を割るのは、22本目が要ると分かってからにする

**いま割らない。** 切り方（付加／置換／酸化還元／加水分解…）は
**高校化学をどう教えるかの判断**であって、画面の都合で先に決めると教え方の側が歪む。
どの箱にも入りにくい1本が出てきたときに切り方を決めるほうが、良い切り方になる。

したがって手順はこうする:

1. 22本目が本当に必要だと分かる（規約1の 1〜3 を通ったうえで）
2. **その1本がどの区分に入るかを見てから**、区分の切り方を決める
3. 区分を割ってから、その1本を足す

⚠ 割るときに一緒に決めること: 見出しは **`grid-column: 1 / -1` の全幅1行**として格子の中に置く
（§8.5。格子を区分ごとに割ると、列数が変わったときに折り返しがずれて
「同じ大きさの札が並ぶ」という読み方が崩れる）。区分が増えれば見出しの行も増えるので、
320px の 3.3画面はさらに伸びる —— **区分割りは画面を短くする施策ではない**。
短くしたいなら折りたたみか絞り込みで、それは別の設計。

---

## 11. 条件は「結果に書くもの」ではなく「選ぶもの」（v1423・2026-08-20・ユーザー判断）

### 11.1 何が起きていたか（実測で追認した）

温度という概念はコードに無く（§2.4 のとおり、それでよい）、`condition` は
**選ばせる画面に出す見出し**として2本のルールに付いているだけだった。
ところがその画面が出るのは **同じ瓶で `detect` が2つ以上通ったときだけ**で、
`dehydration_inter` の `detect` は「**別分子の**アルコールが2つ」を要求する。

したがって:

| キャンバス | 通る `detect` | 画面 |
|---|---|---|
| エタノール **1分子** | `dehydration_intra` だけ | **訊かずに 160〜170℃ が走る**。温度は反応後のキャプションに事後的に書かれるだけ |
| エタノール **2分子** | intra ＋ inter | 2択が出る |

**130〜140℃ を選んだらどうなるかは高校で必ず問われるのに、
分子を2つ並べた人にしか選択肢が見えなかった。**
1分子しか描いていない人 ＝ まさに「エタノールに濃硫酸を加えると何ができるか」を
確かめに来た人が、選択肢の存在を知らないまま片方の答えだけを見せられていた。

> **決定（ユーザー・2026-08-20）**: 濃硫酸を押したら**常に温度を訊く**。
> 2分子必要な場合は、**その時点で相手を呼び出す**。

### 11.2 直し方: 「行き先が1つしかないから訊かない」をやめる（瓶は名指ししない）

`reagentHits()`（＝いま通っているもの）と、画面に**並べる選択肢**を分けた。
新設は `reagentOptions(reagent, hits)` の1つだけで、やることは1行で言える:

> **同じ瓶の `condition` 付きルールは、いま `detect` が通っていなくても選択肢に出す。**
> ただし、その瓶の条件付きルールが**1つでも通っている**ときだけ。

- **`if (reagent.id === 'h2so4_conc')` のような名指しは書いていない。** 判定は
  `condition` というデータの有無だけを見るので、別の瓶に条件が付けば同じように効く
  （§10.1 の「同じ表から瓶とルールを生成する」と同じ考え方 —— 挙動をデータ側に置く）
- ⚠ **「1つでも通っているときだけ」の但し書きが要る理由**（外すと壊れる）:
  濃硫酸には脱水以外の行き先がある（エステル化の触媒・スルホン化）。但し書きを外すと
  **ベンゼンに濃硫酸を掛けても温度の2択が出て**、スルホン化がその中に埋もれる。
  さらに悪いことに、酢酸だけを置いたときの**空振りの応答（§4.2 ①の呼び出し案内）が
  条件の一覧に置き換わって消える**（実測。RG3 が落ちた）。
  ＝ 温度の話は**割れ目に片足でも掛かっているとき**にだけ始める

| キャンバス | 押した結果（v1423） |
|---|---|
| エタノール1分子 | **2択**（160〜170℃ ／ 130〜140℃「いまの分子では条件が足りません」） |
| エタノール2分子 | 2択（従来どおり。どちらも通っている） |
| 酢酸＋エタノール | 3択（温度2つ ＋ エステル化）。条件どうしは**隣り合わせて**並べる |
| エチレングリコール2分子 | 2択（130〜140℃ が通る／160〜170℃ が「足りない」側） |
| ベンゼン | 従来どおり**訊かずにスルホン化**（条件付きが1つも通らない） |
| 酢酸だけ | 従来どおり空振りの応答（§4.2 ①の呼び出し案内） |

### 11.3 通らない選択肢を「押せるが何も起きない」にしない

選ばせる以上、選ばれた条件は必ず答えを返す（`explainConditionMiss()`）。

1. **押す前**から「（いまの分子では条件が足りません）」と札に書き、枠も淡くする。
   **隠して押させることはしない**。それでも押せるのは、
   選んだ結果として「何が足りないか」を知ることが学習だから（§1.2・§9.1 と同じ立場）
2. **押したら**、足りないものを言う。文面は `condition.needs` に1行で持たせた
   （`miss` が瓶ごとに1つなのと同じで、**条件ごとに1つ**。ルールに書き写さない）
3. 相手の分子を足せば通るなら、**その場で呼び出す札**を出す
4. **一覧ごと出し直してから**下に足すので、そのまま**もう片方の温度に選び直せる**

### 11.4 呼び出しは v1420 の導線をそのまま使う（**呼ばれる場所だけが新しい**）

`findPartnerHints` / `makePartnerHintButton` / `runPartnerHint` は1行も書き換えていない。
押すと「呼んで・選んで・実行まで」進むのは v1420 で作ってある。

- 従来の入口は「**実行できる反応が0件のとき**」（`explainReagentMiss`）
- 今回の入口は「**条件を選んだ結果として足りないと分かったとき**」

⚠ **同名の分子を2つ並べる**必要がある（エタノール ＋ エタノール）。
`summonMolecule` は名前で引いた後に**IDを振り直して複製する**ので同名を弾かない
（実測で確認: 2回呼んで分子数2）。ここは直す必要がなかった。

⚠ 呼び出した結果は、**最初から2分子並べて 130〜140℃ を選んだ場合と同じ正準コード**に
なることをテストで固定した（入口が違っても中身は1つ ＝ RG4 の考え方）。

### 11.5 なぜ「条件は結果に書くものではない」のか（この節の要旨）

反応が終わったあとのキャプションに「エタノールでは約130〜140℃。より高温の
160〜170℃ では分子内脱水が優先してアルケンになります」と書いてあっても、
**その分岐を自分で選んだ人にしか意味が伝わらない**。
選ばずに片方だけを見せられた人にとっては、読み飛ばす1行でしかない。

> **条件は、結果の説明文ではなく、実行前の分かれ道として出す。**
> 分かれ道が見えるのは「両方できる状態にした人」だけ、という設計は逆立ちしている ——
> **1分子しか描いていない人にこそ選択肢が要る。**

### 11.6 テストの番号（この回）

| 番号 | 何を固定したか | 否定対照（実際に外して赤くなることを確かめた） |
|---|---|---|
| RG12 | エタノール**1分子**で条件の一覧が出る／訊く前に反応も Undo も動かない／130〜140℃ を選ぶと足りないものが言われ呼び出しの札が出る／札を押すとエーテルまで行き、**2分子から選んだ場合と同じ正準コード**／説明を出しても一覧は残る | **A** `reagentOptions` を hits のまま返す（直しを外す）→「通っていない条件が足されていない」／**B** 足りない条件を押しても何もしない →「何が足りないかが言われない」／**C** 呼び出しの導線を外す →「札が出ない」／**D** 一覧を出し直さない →「もう片方の温度に戻れない」／**E** 但し書きを外して常に訊く → **ベンゼンで訊いてしまう**（RG3 も同時に落ちる）。同じ場面で `hits.length` は1件 ＝ 直す前は訊かずに走っていたことを、テストの中で対比して残した |

陰性対照（回帰していないこと）も同じ RG12 に入れた: エタノール2分子は2択のまま・
ベンゼンは訊かずにスルホン化してモーダルを閉じる・臭素水は付加も空振りの説明も変わらない。
