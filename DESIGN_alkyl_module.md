# 設計書: アルキル基のモジュール化（Gemini レビュー項目26）

いまのモジュールパレットは**環**（五員環・六員環・ベンゼン・任意員環・ハース環）と
**官能基**（-OH・-COOH・-NH₂・-NO₂・-SO₃H）だけで、アルキル基がない。
炭素鎖は原子ツールで1個ずつ置くしかない。レビューは「パレットで炭素数を選び、
設置時のモーダルで構造異性体を選ぶ」案を優先度 **高** で挙げている。

**この設計書は方針を決めるだけで、コードは1行も変えていない。**

---

## 0. 先に事実を確かめた（レビューの記述を鵜呑みにしない）

`docs/review_v331.md` は項目5・24-1・7 で事実誤認があった。今回もコードで裏を取った。

| 確かめたこと | 結果 |
|---|---|
| C1〜C5 のアルキル基の種類数 | **1・1・2・4・8 ＝ 16種**。`enumerateConstitutionalIsomers(['C'×n,'R'], 2n+1)` を node で実行して確認。レビューの記述と一致 |
| **列挙・命名・作図はすでにある** | `learn.js:1136` の `AlkylPractice`（P12-3）が C3・C4・C5 を出題している。`chemistry.js` の `enumerateConstitutionalIsomers` / `canonicalCode` / `iupacAlkylNameFromR` / `layoutMolecule` がそろっている |
| 命名器が返す名前 | C4 = tert-ブチル／イソブチル／sec-ブチル／ブチル。C5 = ネオペンチル／**1,1-ジメチルプロピル**／1,2-ジメチルプロピル／イソペンチル／2-メチルブチル／1-メチルブチル／1-エチルプロピル／ペンチル |
| テンプレートの座標 | 16種すべて `layoutMolecule` に通すと**42px の直交格子にきれいに乗る**。非直交の結合はゼロ、重原子どうしの最小間隔は 42px（MIN_CLEARANCE 27.3px の1.5倍） |

**レビューと食い違った点（設計に効く）:**

1. **「tert-ペンチル」はこのアプリでは出ない。** 慣用名（イソ／sec-／tert-）を持つのは C4 まで。
   C5 の第三級アルキル基は `1,1-ジメチルプロピル` になる。
   **モーダルのラベルを手で書くと命名器とズレる** ので、名前は必ず `iupacAlkylNameFromR` から取る。
   慣用名を足すのは `chemistry.js` を触る別件で、この設計には含めない。
2. **「作図のストレスが大幅に軽減」は炭素数3以上でしか成り立たない。** クリック数の実測:

   | 基 | いまの手順（原子ツール） | モジュール（パレット→モーダル→キャンバス） | 差 |
   |---|---|---|---|
   | メチル | 1 | 3 | **−2（モジュールが遅い）** |
   | エチル | 2 | 3 | −1 |
   | プロピル／イソプロピル | 3 | 3 | ±0 |
   | ブチル4種 | 4 | 3 | +1 |
   | ペンチル8種 | 5 | 3 | +2 |

   モジュールが常に3クリックなのは、選択が配置後に解除されるため（`game.js:1448`）。
   **手数の得は小さい。本当の値打ちは「枝をどこへ出すか考えなくてよい」＝位置決めの精度**で、
   そこを設計の軸に置く。

---

## 1. どこまで用意するか

### 案の比較

| 案 | 中身 | 判断 |
|---|---|---|
| **A**（レビュー案） | パレットに C1〜C5 の5ボタン。C3 以上は押すとモーダル | ✗ |
| **B** | 炭素数だけ選んで**直鎖**を置き、枝は既存操作で足す | ✗ |
| **C**（**採用**） | パレットは「アルキル基」**1つ**。モーダル1枚に **C3〜C5 の14種**を炭素数ごとの行で並べる | ✓ |

**案A を採らない理由**
- 「押すと置けるボタン」と「押すとモーダルが開くボタン」が混ざり、挙動が不揃いになる。
  既存の任意員環は**必ず**モーダルを経由する
- パレットを5つ食う。`DESIGN_entry_points.md`（入口の全体再編）と正面からぶつかる。
  1つなら置き場所の議論を先送りできる

**案B を採らない理由**
- 分岐が作れないので、レビューが挙げた tert-ブチルベンゼン・イソペンチルが救えない
- **直鎖のほうが置き場所に困る。** 付け根からの広がりを実測すると
  ペンチル＝一直線に **210px**、tert-ブチル＝**94px 以内**。
  「分岐は難しい／直鎖は簡単」は作図の感覚であって、**幾何では逆**

**案C の中身**
- **C1・C2 は入れない**（上の表のとおりモジュールのほうが遅い）。
  モーダルの先頭に一行だけ「メチル・エチルは炭素を1つずつ置くほうが速いので入れていません」と出す
- **一覧は起動時に列挙して作る。** 種類・名前・図をデータに書かない ＝ 命名器とズレようがない
- モーダルは**縦スクロールのグリッド**にする。レビュー項目1（スマホ375px で見切れ）と同じ轍を踏まない

### 「設置時モーダル」ではなく「選択時モーダル」にする

レビューは「キャンバスをクリック → モーダルが立ち上がる」と書いているが、**この順序は採らない**。
任意員環の実装（`game.js:396-405`）は **パレットを押した時点でモーダル → 員数が決まったら
カーソルにゴーストが追従 → クリックで配置** で、これが既存の流儀。

理由は挙動の統一だけではない。このアプリは
**「ゴーストプレビュー＝実配置の結果」を全モジュールで守っている**
（`getRingPlacementPlan` / `getHaworthPlacementPlan` / `getFunctionalGroupPlan` が
ゴーストと実配置の両方から呼ばれる）。クリックしてからモーダルで基を選ぶ順序だと、
**選ぶ前にゴーストを出せない** ＝ 置ける／置けないが分からないまま決めさせることになる。

- 連続して同じ基を置きたい場合は、選んだ基を `nringSize` と同じように保持して
  「もう一度置く」を2クリックにできる。実装時の選択肢として残す（既定の挙動は変えない）

---

## 2. 座標をどう決めるか（いちばんの難所）

過去に2回事故が起きている:
- **P9-5e** … ベンゼン環の置換基が `MIN_CLEARANCE`（27.3px）の検査を通らない経路があった
- **v434** … 呼び出した分子の ±120° 整形で -COOH の枝が 30° 回り、21.7px まで詰まった

### 2.1 テンプレートは実行時に作り、正規化する

1. `enumerateConstitutionalIsomers(['C'×n, 'R'], 2n+1)` で列挙する
2. `layoutMolecule` で 42px 直交の座標を付ける（**0章のとおり16種すべてが格子に乗る**）
3. **正規化**: R を原点へ平行移動 → **R→C1 の向きが +x になるよう回す** → R を捨てる。
   R→C1 は必ず軸に乗っているので**回転は 90° の倍数**で、丸め誤差が出ない
4. `canonicalCode` をキーにキャッシュする（`AlkylPractice.enumerate` と同じ流儀）

正規化を明示するのは、`layoutMolecule` の出す向きが基ごとにばらばらだから
（tert-ブチルは R から見て左上へ、イソペンチルは右下へ広がる）。

### 2.2 「置いてから戻す」ではなく「置ける姿勢を先に探す」

**先に探せる。** テンプレートは**既存の原子を1つも動かさない**ので、
座標が他の原子の移動に依存しない。ここが環モジュールの側鎖振り分け（`ringSplit`）と違う点で、
全姿勢を先に試して可否を決められる。

姿勢は3つの自由度で表す:

| 自由度 | 候補 | 備考 |
|---|---|---|
| **向き θ** | `preferred`, +90°, −90°, +180° | `getFunctionalGroupPlan` と同じ決め方。付け根の重原子の隣が0〜1個なら90°丸め、2個以上なら `largestGapDirection`（ベンゼンの ipso では外向き二等分線になる） |
| **鏡映 m** | y 反転の有無 | **既存モジュールには無い新しい自由度。** sec-ブチル・2-メチルブチルのような左右非対称なテンプレートで、枝を上に出すか下に出すかを選べる。対称な基（メチル・エチル・プロピル・tert-ブチル・ネオペンチル…）は同じ座標になるので重複を捨てる |
| **幹の長さ r** | 42px から 6.3px 刻みで 84px まで | **幹（付け根→C1）だけを伸ばす。テンプレート全体は拡大しない**（枝が 42px から変わると手描きの図と見分けがつかなくなる）。刻みは `getSnappedCoords` の `EXTEND_STEP`（`GRID_SIZE*0.15`）と同じ |

### 2.3 各姿勢に通す検査

1. **付け根の空き価標** … `getFreeValency(baseAtom.id) >= 1`。姿勢に依らないので最初に1回だけ
2. **間隔** … 新しい原子と、付け根以外のすべての既存重原子が **MIN_CLEARANCE（27.3px）以上**
3. **貫通** … 新しい結合（幹＋テンプレート内）が既存の重原子の上を通らない／既存の結合が
   新しい原子の上を通らない。しきい値は `_hasBondThroughAtom` と同じ 16px。
   **間隔だけでは足りない。** テンプレートが大きいほど貫通の危険は上がり、
   とくに幹を 84px まで伸ばすと**途中に既存の原子を跨げる**（既存の官能基モジュールは
   最大3原子・伸長も1段だけなのでここまで要らなかった）
4. **キャンバス上限** … `|x|`・`|y|` が `CANVAS_LIMIT`（5000）以内。
   `summonMolecule` が「編集できない場所には黙って置かない」と同じ理由

**探索順**: r を外側のループにして**短い幹を優先**、その中で θ・m を回す。
通った姿勢が複数あれば、**テンプレートの重心がカーソルにいちばん近いもの**を選ぶ
（`getSnappedCoords` の 8. と同じ流儀 ＝ ユーザーがマウスで枝の向きを選べる）。

### 2.4 置けないときの振る舞い

- **赤いゴースト＋トースト。`saveState()` を呼ばない**（開発方針 3.5: 配置しないなら Undo 履歴を消費しない）
- 文言は理由で分ける ——「空き価標がない」／「まわりに場所がない（原子を減らすか、結合を伸ばしてから）」
- **置いたあとに整形をかけない。** ±120° 整形（`reshapeVinylAngles`）は
  「名称から呼び出した分子」だけの仕事で、モジュール配置には一切かけない

### 2.5 実装の置き場所

`getAlkylPlacementPlan(spec, baseAtom, cursor)` を1本作り、
**ゴーストと実配置の両方がこれを呼ぶ**。既存3モジュールがすべてこの形なので揃える。
返すのは `{ atoms, bonds, valid, reason }` で、`getFunctionalGroupPlan` と同じ形にして
`placeModule` の適用部（`game.js:2707-2715`）をそのまま使い回す。

---

## 3. 教育的に妥当か

**衝突するのは「パズルモード全体」ではなく「書き出し練習のセッション中」。**

`AlkylPractice`（P12-3・`learn.js:1136`）は
**まさに C3・C4・C5 のアルキル基を1つずつ手で描く練習**で、
ワンタップで tert-ブチルが出せると練習が成立しない。
`IsomerPractice`（P12-1）も分岐アルキルを描く工程を含む。

**決め: `alkylPractice.active` または `isomerPractice.active` の間は
アルキル基モジュールを無効化する**（ボタンを disabled にし、理由を1行出す）。

- 前例がある。`AlkylPractice.start` は開始時に `isomerPractice` / `stereoPractice` を
  止めており（`learn.js:1203-1204`）、練習どうしを排他にする作りはすでにある
- 依存は**モジュール側が練習の状態を見る**一方向に保つ。`learn.js` は触らない

**パズルモードでは使ってよい。** お題は「この化合物を作れ」であって
「異性体を数えろ」ではない。ただし実装時に `stages.json` を見て、
アルキル基の異性体そのものを問うステージが無いことを確認する
（今回調べた範囲では、その手の出題は書き出し練習の側にある）。

**もう一段の配慮**: モーダルには**形と名前を並べて出す**（名前は `iupacAlkylNameFromR`）。
形だけ並べるより、「選ぶ操作がそのまま名前と形の対応を見る機会になる」形にしたい。

---

## 4. 段階に割る

### 第1段（最小。**C3 と C4 をまとめて**）

**「メチルとエチルだけ」では成立しない。** 0章の表のとおり手数がむしろ増え、
分岐が1つも出ないので姿勢探索・鏡映・幹の延長がどれも動かない ＝ 何も検証できない。
C3 だけでも手数は ±0 で、得が無い。**最小で意味を持つのは C3＋C4 の6種**
（直鎖・分岐・左右非対称の sec-ブチルがそろい、慣用名も出る）。

作るもの:
- `getAlkylPlacementPlan` ＋ ゴースト ＋ `placeModule` の分岐
- パレットのボタン1つと、6種だけの仮モーダル（`#alkyl-module-modal`）

テストで固定すること:
- (a) 置いた結果の `canonicalCode` が、列挙した基のコードと一致する（6種すべて）
- (b) 新しい原子と既存重原子の間隔が MIN_CLEARANCE 以上（ベンゼンの ipso・鎖の途中の炭素・
      すでに側鎖がある炭素の3通りで）
- (c) **置けないとき、`userMolecule` が1原子も変わらず Undo 履歴も増えない**
- (d) **ゴーストの座標が実配置の座標と一致する**（プレビュー＝実結果）
- (e) 新しい結合が既存の重原子を貫通しない

### 第2段（C5 の8種・モーダルの本実装）

- 一覧を列挙から自動生成に切り替え、サムネは `layoutMolecule` ＋ 既存のギャラリー描画を流用
- **ここで初めてモーダルのレイアウト（スマホ375px 幅）を詰める**
- テストで固定: 8種すべてが正しい正準コードで置けること。
  とくに広がりの大きいペンチル（付け根から 210px）で、
  **置けない場面がきちんと `valid:false` になる**こと

### 第3段（守りを固める）

- 練習モード中の封じ込め（3章）
- `audit.js` のファズにアルキル基を追加（`audit.js:234` の `mods` 配列）。
  夜間監査の重なりしきい値は 24px なので、27.3px を守る設計なら素通りするはず ＝
  **落ちたら設計どおりに動いていない**という信号になる
- チュートリアル（`data-tutorial` の追加）

各段共通の儀式: 版番号バンプ → `test.html` 全合格 → `node tools/verify-release.js assembler` → 1修正1コミット。

---

## 5. 触らないもの・申し送り

- **`chemistry.js` は呼ぶだけ**（列挙・正準コード・命名・レイアウト）。中身は変えない
- **`learn.js` の練習ロジックは触らない**。封じ込めはモジュール側から見る一方向にする
- **パレットの置き場所はここでは決めない。** `DESIGN_entry_points.md`（入口の全体再編）の
  決定に従う。この設計書が要求するのは「**ボタン1つとモーダル1枚**」だけ

### ユーザー判断が要る点

1. **C1・C2 を入れるか** — 推奨: 入れない（手数が増えるだけ）
2. **パズルモードで使えるようにするか** — 推奨: 使える。書き出し練習のセッション中だけ封じる
3. **C5 の第三級基を「tert-ペンチル」と呼ぶか** — 推奨: 呼ばない。
   慣用名を足すと `chemistry.js` の命名器を触ることになり、別件として切り出したほうがよい
4. **同じ基を連続で置けるようにするか** — 2クリックに縮むが、既定の挙動（配置後に選択解除）を変える
