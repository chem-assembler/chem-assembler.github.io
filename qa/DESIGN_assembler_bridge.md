# 一問一答 → assembler の飛び道具：何を見せるかの棚卸し（設計）

一問一答の283項目それぞれについて、**assembler 側で何を見せるのが学習の助けになるか**を決める。
いま `link` を持つのは13項目だけで、これは作問中に思いついたもの＝体系的な検討をしていない。
リンクを増やす前にこの表を確定させる（先にリンクを付けると、あとで規約を変えたときに
黙って壊れる先が増える）。

規約の出所は `TAXONOMY.md` §2.6。ただし後述のとおり **`link: {label, build}` の1形式では
実態を表せない**ことが分かったので、ここで作り直す。

---

## 1. まず分かったこと（2026-08-06 の調査）

### assembler 側の受け口は**もう出来ている**

`assembler/game.js` の `applyOpenParam()` が次を受ける。**待つ必要はない。**

| パラメータ | 受ける値 | 何が起きるか |
|---|---|---|
| `?open=` | `free` `puzzle` `learn` `quiz` `naming` `stereoquiz` `choicequiz` `symbolpuzzle` `timeattack` `fischer` `countquiz` `practice` `mechanism` `isomer` `stereo` の15種 | その画面・モードを開く |
| `?summon=` | **化合物の名称**（例「メタン」） | ライブラリ（stages＋compounds.json の568件）から探してキャンバスに配置 |
| `?series=` | シリーズ名の部分一致 | パズルのシリーズを選ぶ |
| 未知のパラメータ | — | **無視する**（前方互換。壊れない） |

### いま qa が送っているものは噛み合っていない

qa の `link.build` は `CH4` `C₆H₆` のような**分子式**だが、assembler は `build` を知らない。
さらに `build` の表記自体が揺れている（`CH4`・`C2H4` は ASCII 数字、`C₆H₆`・`C₈H₁₀` は Unicode 下付き）。

### assembler が「見せられること」の在庫

| 見せられるもの | 実体 | 規模 |
|---|---|---|
| 分子を組む・眺める | `?summon=<名称>` ＋ 自由モード | ライブラリ **568件** |
| 異性体を書き出す | `?open=isomer` | 分子式を与える口は**未整備**（要調整） |
| 立体を見る | `?open=stereo` / `?open=fischer` | くさび図・フィッシャー投影 |
| 反応を実行する | 反応実行モード（reactor.js） | **31種**（付加・置換・酸化・脱水・エステル化・けん化・重合・加硫・糖の環化など） |
| 反応機構のアニメを見る | `?open=mechanism` | **14件**（臭素付加・塩素化・エステル化・ニトロ化・水付加・分子内/分子間脱水・けん化・スルホン化・酸化2件・ジアゾ化・カップリング） |
| 命名を練習する | `?open=naming` | クイズ形式 |
| 数え上げを練習する | `?open=countquiz` | 異性体の個数 |

---

## 2. 283項目の一次分類（タグによる機械的な仕分け）

「構造で見せられそうなタグ」（官能基・分子の形・異性体・命名・反応・付加・置換・脱水・酸化・
重合・縮合・加水分解）を持つかで分けると:

| 単元 | 構造系 | 非構造系 | 備考 |
|---|--:|--:|---|
| aliphatic | 42 | 24 | 構造系の本命 |
| alcohol | 10 | 9 | |
| anal | **0** | 15 | 元素分析は組めない。**純・一問一答のまま** |
| carbonyl | 26 | 11 | 反応が多く reactor と相性がよい |
| aro | 15 | 7 | 置換反応が mechanism にある |
| phenol | 6 | 9 | |
| aroN | 11 | 5 | ジアゾ化・カップリングが mechanism にある |
| fat | 3 | 12 | けん化は reactor にあるが油脂の分子は大きい |
| bio | 23 | 21 | 糖の環化が reactor にある |
| poly | 23 | 11 | 付加重合・加硫が reactor にある |
| **合計** | **159** | **124** | |

> **タグだけでは粗い。** 「反応」タグを持っていても、reactor に対応する反応がなければ見せられない。
> 逆に「性質」タグでも、分子を見せると分かるもの（沸点と水素結合、分子の形と極性）がある。
> §4 の作業で1件ずつ突き合わせる。

---

## 3. `link` の形を作り直す（TAXONOMY §2.6 の改訂案）

いまの `{label, build}` は「分子式を渡す」1通りしか表せない。実際の13件を見ると**意図が3種類**ある。

```json
"link": { "kind": "summon",    "label": "メタンを組み立てる",        "name": "メタン" }
"link": { "kind": "isomer",    "label": "C₅H₁₂ の異性体を書き出す",  "formula": "C5H12" }
"link": { "kind": "mechanism", "label": "エステル化の機構を見る",     "id": "esterification" }
"link": { "kind": "reaction",  "label": "エタノールの脱水を試す",     "name": "エタノール", "reagent": "h2so4_conc" }
```

- **`kind` を必須にする。** 何を見せたいかが読めば分かるようにし、URL の組み立ても分岐できる
- `summon` … `?open=free&summon=<name>`。**name はライブラリの名称と完全一致**が要る（下記の落とし穴）

> ### ⚠ summon の落とし穴：名称は別名を括弧で抱き込んでいる
>
> `summonMolecule(name)` は `entry.name === name` の**完全一致**で探す。ところがライブラリの
> 名称は **「エチレン（エテン）」「アセチレン（エチン）」「D-グルコース（鎖状）」** のように
> **別名や状態を括弧で抱き込んだ1つの文字列**になっている。
> つまり `summon=エチレン` では**引けない**。実際に照合すると:
>
> | 引きたい名前 | 完全一致で引けるか | ライブラリの実際の表記 |
> |---|:--:|---|
> | メタン・エタン・ベンゼン・トルエン・フェノール・酢酸・エタノール・アセトン・アニリン・サリチル酸・スチレン・ナフタレン・乳酸・マレイン酸 | ○ | そのまま |
> | エチレン | **×** | エチレン（エテン） |
> | アセチレン | **×** | アセチレン（エチン） |
> | プロペン | **×** | （単体では未収録） |
> | グルコース | **×** | D-グルコース（鎖状）／α-D-グルコース（α-D-グルコピラノース）ほか |
>
> **ここが設計判断の分かれ目**:
> - (a) qa 側がライブラリの正式表記をそのまま持つ … qa だけで閉じるが、**assembler が名称を
>   変えた瞬間に黙って壊れる**（依存の向きとしても好ましくない）
> - (b) assembler 側で **`formula` でも引けるようにする** … `compounds.json` は `formula` を
>   持っている（`"C2H4"`）。qa は分子式を送るだけでよく、**表記変更に強い**。
>   ただし assembler の改修が要る（§5 に入れる）
> - (c) assembler 側で括弧を無視した部分一致・別名対応にする … 曖昧一致は誤爆の恐れ
>
> **(b) を推す。** 分子式は化学的に一意で、qa 側が相手の表記に依存しない。
> `?summon=` は名称のまま残し、`?formula=` を足す形なら前方互換も保てる。
- `isomer` … `?open=isomer&formula=<式>`。**formula の受け口は assembler 側に未整備**（§5）
- `mechanism` … `?open=mechanism&id=<機構id>`。14件のどれか
- `reaction` … `?open=free&summon=<name>&reagent=<反応id>`。reactor の31種のどれか。
  **reagent の受け口も未整備**（§5）
- `formula` は **ASCII 数字で書く**（URL に載せるため。表示用の下付きは `label` 側で持つ）

### 現在の13件を新形式に割り当てた案

| コード | kind | 渡す値 | 今すぐ繋がるか |
|---|---|---|:--:|
| `org.ali.alkane-shape` | summon | メタン | ○ |
| `org.aro.benzene-structure` | summon | ベンゼン | ○ |
| `org.aro.styrene` | summon | スチレン | ○ |
| `org.aro.naphthalene` | summon | ナフタレン | ○ |
| `org.carbonyl.lactic` | summon | 乳酸 | ○ |
| `org.carbonyl.maleic-fumaric` | summon | マレイン酸（＋フマル酸） | ○ |
| `org.ali.alkene-shape` | summon | エチレン | **×（正式表記は「エチレン（エテン）」）** |
| `org.ali.markovnikov` | reaction | プロペン＋`add_hbr` | **×（名称なし・reagent 未整備）** |
| `org.alcohol.dehydration` | reaction | エタノール＋`dehydration_intra` | ×（reagent 未整備） |
| `org.ali.iso-c4h10` | isomer | C4H10 | ×（formula 未整備） |
| `org.ali.iso-c5h12` | isomer | C5H12 | ×（formula 未整備） |
| `org.ali.iso-count` | isomer | C6H14 | ×（formula 未整備） |
| `org.aro.c8h10-isomers` | isomer | C8H10 | ×（formula 未整備） |

**6件は qa 側の変更だけで今すぐ繋がる**（メタン・ベンゼン・スチレン・ナフタレン・乳酸・マレイン酸は
名称が完全一致で引けることを機械で確認済み）。残り7件は §5 の調整が要る。

---

## 4. これからやること（283項目の棚卸し）

**この設計の本体はここ。** 全項目に「assembler で何を見せるか」を1つ決める。

### 手順

1. 構造系159項目を単元ごとに見て、次のどれかを割り当てる
   - `summon` … その分子を見せると分かるもの（形・官能基・異性体の実物）
   - `isomer` … 異性体の個数・書き出しを問うもの
   - `mechanism` … 14件の機構に対応するもの
   - `reaction` … reactor の31種に対応するもの
   - `naming` / `countquiz` / `stereo` / `fischer` … 対応する練習があるもの
   - **`none`** … 構造系だが assembler では見せられない／見せても学習の助けにならない
2. 非構造系124項目は原則 `none`。ただし**分子を見せると理解が進むもの**は拾う
   （例: 沸点と水素結合 → 分子を並べて見せる、油脂の分子の大きさ → 見せる価値がある）
3. `summon` を割り当てるときは**ライブラリ568件に名称があるかを機械で確認**する
   （無い場合は「ライブラリに追加する」か「別の kind にする」かを判断）
4. 表を `qa/data/assembler_links.jsonl` に落とし、`questions.json` の `link` は**そこから生成**する
   （対応表と作問を分ける。理由は難易度データと同じ ＝ 更新の担当と頻度が違う）

### 決めておくこと（判断が要る）

- **`none` を明示的に持つか、単に link を書かないか。** 明示すると「検討済みで見せない」と
  「まだ検討していない」を区別でき、棚卸しの進捗が測れる。**明示する案を推す**
- **1項目に複数の見せ方があるとき**（例: エチレンは「形を見る」と「臭素付加を試す」の両方）
  どうするか。`link` を配列にするか、代表1つに絞るか

---

## 5. assembler 側に要る調整（レーンをまたぐので要相談）

**qa 側だけでは閉じない**もの。assembler は現在活発に改修中（v736→v748→v758→v766、
リボン統合 第4段・ハース環モジュール）なので、**割り込まずに調整する**。

1. **`?formula=<式>` で分子を呼べるようにする**（最優先）。`summonMolecule` が名称の完全一致でしか
   引けないため、「エチレン（エテン）」のような括弧つき表記に qa 側が依存してしまう。
   `compounds.json` は `formula` を持っているので、そこで引ければ **qa は分子式を送るだけで済み、
   相手の表記変更に強くなる**。`?summon=`（名称）は残したまま `?formula=` を足せば前方互換
2. **`?open=isomer&formula=<式>`** … 異性体練習を分子式つきで開く。
   現状の `?open=isomer` はコメントに「キャンバスが空だと調べようがない」とあり、
   分子を summon してから開く前提になっている
3. **`?reagent=<反応id>`** … summon した分子に対して試薬を選んだ状態にする。
   reactor の31種の id をそのまま使えるはず
4. **`?open=mechanism&id=<機構id>`** … 14件のうち特定の機構を開く。
   現状は mechanism ビューを開くだけで、どれを見せるかを指定できるか未確認

> どれも「未知パラメータは無視」で前方互換なので、**qa 側が先に送っておいても壊れない**。
> assembler 側が受けるようになった時点で自動的に効き始める。この順で進めるのが安全。

---

## 変更履歴

- 2026-08-06: 初版。summon が名称の完全一致でしか引けず、ライブラリの表記が
  「エチレン（エテン）」のように別名を抱き込んでいることを実照合で確認。
  分子式で引けるようにする案（assembler 側の調整・最優先）を §5 に入れた。assembler 側の受け口が既にあること・qa の `build` が噛み合っていないこと・
  意図が3種類（→4種類）に分かれることを調査で確定させ、`link` の形を作り直す案と
  283項目の棚卸し手順を決めた。棚卸し自体は未着手
