# リボン統合 設計書（右パネルを無くし、リボン＋モーダルにする）

**この文書は設計だけで、コードは1行も変えていない。** 版番号も上げていない。実装はここで合意してから別レーンで行う。

基準の版は **v581**。実測はすべて v581 のコードをローカルサーバー（`python -m http.server`）で開き、
Playwright（`tools/record` の依存を借用）で 320×568 / 375×812 / 568×320 / 640×360 / 667×375 /
740×360 / 812×375 / 844×390 / 896×414 / 900×700 / 1024×768 / 1280×800 / 1920×1080 を測った値。
`DESIGN_entry_points.md`（v446）・`DESIGN_molecule_modal.md`（v524）の数字は**この文書で取り直している**。
取り直した結果、**前の文書の見立てが外れていた箇所が3つある**（§2-1 の注・§2-3・§6）。

## 0. なぜこの文書か

ユーザーの指示（原文）:

> **右パネルの内容はすべて上のリボンに統合し、必要に応じて中央にモーダルを出す**のがよいと思います
>
> 右にある内容は **Study　Puzzle　Help** といった感じでまとめられるかと。
> **この分子**は**分子名をクリックして出す分子ごとのモーダル**でまとめられるのでは。
> **複数分子を選択した状態ではそれに対応する必要があります**

目標は「**右パネルを無くす**」。答えるのは6つ:

| # | 問い | 答える節 |
|---|---|---|
| 1 | リボンに何個の入口が載るか（実測） | §2-1・§3 |
| 2 | 38項目を Study / Puzzle / Help ＋ 分子モーダルへどう振り分けるか | §5 |
| 3 | 学習の3アコーディオンを1枚に畳めるか | §6 |
| 4 | 構造判定（Submit）の置き場所 | §7 |
| 5 | `setMode` と `data-modes` はどうなるか | §8 |
| 6 | 段階に割る／何が失われるか | §9・§10 |

関連する既存の設計書との関係:

- **`DESIGN_entry_points.md`** … 入口の全数（§2）と不変条件（§7）の出どころ。案A の8手は v447〜v454 で決着済み
- **`DESIGN_molecule_modal.md`** … 「この分子」の面。**§10 がユーザーの決定で正**。
  **§5-2 の複数分子の設計（見出しのタブ・`① 酢酸 ＋ ② エタノール`・先に選んだ方が式の左）は
  この文書では作り直さず、前提として使う**。第1段・第2段が**並行して走っている**
- **`DESIGN_mobile.md`（P11）/ `DESIGN_ui_modes.md`（P10）** … 骨格。**どちらも作り替えない**。
  ただし P10 §3.3・P11 §3 の**未実装分を2件回収する**（§8-3）
- **`DESIGN_reagent_palette.md`** … 試薬の置き場所は分子モーダルの中（molecule_modal §6-1）。この文書は触らない

---

## 1. 結論の先出し

1. **リボンに載るのは 9 枠。** ただし**いまのラベル付きボタンのままでは 1 枠も増えない。**
   横画面の床は iPhone SE 横（568×320）で、いまの6項目が 235px／器 258px ＝ **余り 23px**。
   PC の床は 900px（ブレークポイント）で、**9枠でちょうど1行**。
   **52×46 のアイコン＋短ラベルのタイル**に替えると 320px から 1920px まで**全端末で 9 枠が成立する**（§2-1）
2. **面は2つではなく4つになる。**「リボン＋モーダル」だけでは足りない。
   **反応機構ビューアと書き出し練習3種は本体キャンバスを動かす**ので、モーダルで覆うと自分の仕事が見えない（§2-3）。
   **キャンバス上の「作業帯」**という第3の面と、**常時表示（名前チップ・見出し・トースト）**という第4の面が要る（§4）
3. **リボンのタイルは `.mode-tab` そのものにする。** 🧪標準・🧩パズル・📚学習 を
   `class="mode-tab" data-mode="..."` のまま右パネルからリボンへ**移設**すれば、
   `setMode` の3値も台本13箇所も**無傷のまま**、タブが入口になる（§8）
4. **構造判定（Submit）はリボンに置かない。** お題と離すと視線が飛ぶ。
   **作業帯の「お題ストリップ」にお題名・分子式と一体で置く**（§7）。リボンに置く案も測ったが、
   9枠が道具5＋入口4でちょうど埋まるので**予備がゼロになる**
5. **学習の3アコーディオンは1枚に畳めない。** 320px 幅で **2259px ＝ 4.5画面**（§6）。
   **畳んではいけない**理由もある（上の 2）。Study モーダルは**メニュー1枚**にして、中身は既存モーダル・作業帯へ散らす。
   それでも**深さは 4段 → 3段に浅くなる**（診断 D3）
6. **いちばん大きい利得は PC のキャンバス。** 右パネルを外すと
   **1024×768 で 362px → 697px（+93%）**、1280×800 で 618 → 953px（+54%）。
   モバイルはキャンバスが1px も広がらない（シート／ドロワーはもともと画面外）——
   **モバイルの利得は面積ではなく手数と発見しやすさ**（§10-2）

---

## 2. 先に確かめた事実（v581）

### 2-1. リボンの容量（実測）★この文書の核心

現物は `.canvas-header`（`index.html:116`）。中身は **座標表示＋6ボタン＋☰**。
`⤓ JSON` はモバイルで `display:none`、`座標表示` は横画面で `display:none`。

**いまの器と使用量**

| 画面 | リボンの器 | 見えている項目 | 使っている | 余り |
|---|---|---|---|---|
| 320×568 縦 | 306×**151**（**3段**） | 7（座標＋5＋☰浮動） | — | 3段目まで使用済み |
| 375×812 縦 | 361×**102**（2段） | 7 | — | 2段目に約55px |
| **568×320 横**（SE 横） | 114×**258** | 6 | **235px** | **23px** |
| 640×360 / 740×360 横 | 114×298 | 6 | 235px | 63px |
| 667×375 / 812×375 横 | 114×**313** | 6 | 235px | **78px** |
| 844×390 横 | 114×328 | 6 | 235px | 93px |
| 896×414 横 | 114×352 | 6 | 235px | 117px |
| 1280×800 PC | 618×**40**（1行・`nowrap`） | 7 | — | あふれず**縮む**（下の注） |

> ⚠ **`DESIGN_entry_points.md` §2-1 の「375px 横のリボン 114×313 でちょうど収まりきる＝ゼロ」は、
> 端末の取り方が甘かった。** 812×375 には実測で **78px（＝2枠）の余り**がある。
> **本当にゼロなのは iPhone SE 横（568×320）で、余り 23px。**
> 横画面の容量は幅ではなく**画面の高さ**で決まる（器 = 画面高 − 62px）ので、
> 「375px 横」という書き方自体が容量の指標にならない。以後は**画面高**で数える。

> ⚠ **PC のリボンはあふれない。黙って縮む。** `flex-wrap:nowrap` なので、
> ボタンを1個足しただけで**座標表示が 39px → 0px に消え**、5個足すと
> **最小ボタン幅が 33px になって文字が潰れる**（実測）。`scrollWidth === clientWidth` のままなので、
> **「あふれ」を見る検査では気づけない**。

**ラベル付き（108×34・現状と同じ形）を縦1列に並べたとき、横画面に入る数**

| 画面高 | 入る数 | いま |
|---|---|---|
| 320px（SE 横） | **6** | 6 ＝ **追加ゼロ** |
| 360px | 7 | 6 |
| 375px | 8 | 6 |
| 414px | 9 | 6 |

**52×46 のアイコン＋短ラベル（2行・`↩` の下に `戻す`）に替えたとき**

| 画面 | 並び | 入る数 | リボンの高さ | いま |
|---|---|---|---|---|
| 320×568 縦 | 5個/行 | **10**（2段） | 約110px | 151px ＝ **41px 返る** |
| 375×812 縦 | 6個/行 | **12**（2段） | 約110px | 102px（＋8px） |
| 568×320 横 | 2列 | **10** | 258px | — |
| 812×375 横 | 2列 | 12 | 313px | — |
| 896×414 横 | 2列 | 14 | 352px | — |
| **900×700 PC** | 1行 | **9** | 59px | 40px（＋19px） |
| 1024×768 PC | 1行 | 11 | 59px | — |
| 1280×800 PC 以上 | 1行 | 11以上 | 59px | — |

- 実測時に**文字の切れは 0 件**（`scrollWidth > clientWidth` の走査）
- **タップ標的は 46px** ＝ `tools/check-mobile.mjs` と TAP1 の 32px の床を満たす
- 40×44・48×44・56×48 も測ったが、**52×46 が「全端末で最大」かつ「字が切れない」最小**だった

> ⚠ **上の PC の行（900×700 で 9枠・1024px で 11枠）は「右パネルが無くなったあと」の数字。**
> v650 で実装して測り直したところ、**右パネルが残っている間は 900px のキャンバス列が 238px しかなく、
> 1行に 3枠**しか入らない（左パレット＋右パネル＋余白で 662px を使うため、
> リボンの器は「画面幅 − 662px」になる）。**第5段で右パネルを外すと器は 573px になり、
> 9枠がちょうど1行に収まる**（実測ずみ）。
> **第1段〜第4段のあいだ、900px の PC ではリボンが2〜3段になる。**
> ただし旧リボンは同じ 900px で `nowrap` のままボタン幅 33px まで潰れ、
> **中身が枠から溢れていた**（実測）ので、段が増えても悪化ではない。

> **したがってリボンの容量は 9 枠。**
> 床は2つあり、**モバイル横は SE 横の 10 枠**、**PC は 900px（ブレークポイント）の 9 枠**。
> 小さいほうを採って **9**。10枠目・11枠目は 1024px 以上の PC と 812px 以上の横画面でしか出ない
> ので、**そこに入口を置いてはいけない**（機種で消える入口になる）。

### 2-2. 右パネルの現物（v581）

`#right-panel` の中身は **ボタン 42・input 5・select 3・summary 4・label 6**、id 付き要素 **61個**。

**総スクロール高（実測）**

| 画面 | 🧪 自由 | 🧩 パズル | 📚 学習 | 学習（3acc 全開） | 可視高 |
|---|---|---|---|---|---|
| 320×568 | 632px | **1253px** | 408px | 2401px | 464px |
| 375×812 | 633px | 1168px | 408px | 2184px | 633px |
| 812×375 横 | 618px | 1287px | 395px | 2371px | 373px |
| 1280×800 | 718px | 1278px | 718px | 2355px | 718px |

> `DESIGN_entry_points.md` の「学習 3アコーディオン全開で 3158px」は v446 の値。
> v451（遊び方を畳む）と v453（クイズを3群に）を経て、v581 では **2401px**（320px 幅）。
> 学習ブロック単体では **2167px**。

**主要ブロックの高さ（320×568・幅 286px）**

| ブロック | 高さ |
|---|---|
| `#compound-info`（🔍 いま描いている分子） | 245px |
| `#reaction-card`（⚗ この分子の反応・反応1件） | 141px |
| `#hint-details`（閉じている状態） | 44px |
| `.hint-box`（開いた状態） | **1512px** |
| `[data-modes="learn"]`（3acc 全開） | **2167px** |
| `#mode-tabs` | 45px |

### 2-3. モーダルに入らないものが4つある ★前の設計書に無い発見

`DESIGN_molecule_modal.md` §2-5 は「**反応の実行**はキャンバスの上で起きるからモーダルに入らない」と
書いた。**同じ理由でモーダルに入らないものが、学習モードにあと4つある。**

| 機能 | 何を動かすか | 出典 |
|---|---|---|
| **反応機構ビューア** | 巻矢印を**本体 SVG** の `#arrows-group` に描く。生成物予測は `game.userMolecule` を**丸ごと差し替える** | `reaction.js:16, 394-398` |
| **異性体の書き出し練習** | `g.userMolecule = new Molecule(); g.updateDrawing()` でキャンバスを空にして書かせる。答え合わせは `#ip-review-overlay`（`canvas-overlay`） | `learn.js:545-547, 359` |
| **アルキル基の書き出し練習** | 同上。付け根マーカー R を**キャンバスに置いてから**始める | `learn.js:1211-1226, 1140` |
| **立体異性体の書き出し練習** | 同上。`g.fitCanvasToMolecule()` まで呼ぶ | `learn.js:1825-1828, 1683` |

さらに **異性体の書き出し練習には「描きながら更新される表示」がある**:

> `onDrawingChange()` … 作図が変わるたびに `game.updateDrawing` から呼ばれ、
> 「いま: C₄H₁₀　ブタン」を書き換える（`learn.js:742-771`）

これは `DESIGN_molecule_modal.md` §4-1 の「**常に見えているべきもの**（描くたびに変わる ＝
変わる過程が学習内容）」そのもの。**モーダルの中に置いたら意味が消える。**

> **いまはどうなっているか。** 右パネルはモバイルでシート（`body.sheet-open`）だが、
> **練習を始めてもシートは自動で閉じない**（`game.js:681-688` に開閉のトグルがあるだけ）。
> つまり**いまも「自分で閉じてから描く」**必要がある。
> モーダル化してもここは悪くならない——ただし**「開始したら閉じる」を実装すれば良くなる**
> （`DESIGN_molecule_modal.md` §5-3 の反応ボタンと同じ作法）。

### 2-4. 右パネルを消すと、キャンバスはどれだけ広がるか

| 画面 | いまの `#svg-wrapper` | 右パネルなし | 増加 |
|---|---|---|---|
| **1024×768** | **362×646** | **697×646** | **幅 +93%** |
| 1280×800 | 618×678 | 953×715 | 幅 +54% |
| 1920×1080 | 1258×958 | 1593×1195 | 幅 +27% |
| 812×375 横 | 644×361 | 644×361 | **0**（ドロワーは画面外） |
| 375×812 縦 | 361×563 | 361×563 | **0**（シートは画面外） |

> **1024×768 の +93% がいちばん効く。** ここは学校のプロジェクタ・古いノート・
> iPad 横の幅で、いまキャンバスが 362px しかない ＝ **左パレット 280px より狭い**。
> 逆に**モバイルの利得は 0px**。モバイルで良くなるのは「☰ を押してシートを開く」1手が消えることだけ（§10-2）。

### 2-5. タップ標的の床（32px）が、いま隠れて破られている

`<details>` を開いた状態で 320px 幅を走査すると、**32px 未満の押せるもの**がこれだけある:

| 場所 | 要素 | 実測 |
|---|---|---|
| 反応機構ビューア | 🔄 `#btn-rx-restart` | 37×**28** |
| 反応機構ビューア | ⏮ `#btn-rx-prev` | 31×**28** |
| 反応機構ビューア | ▶ `#btn-rx-play` | 33×**28** |
| 反応機構ビューア | ⏭ `#btn-rx-next` | 31×**28** |
| 書き出し練習 | お題の入力欄 | 112×**31** |
| 書き出し練習 | 「練習する」 | 70×**31** |
| パズル | 判定オプションのスイッチ2つ | 50×**24**（トラック。ラベルが当たり判定） |

- **いまこれらが検査に出てこないのは、閉じた `<details>` の中にいて `rect=0` だから。**
  作業帯とモーダルへ出すと**新たに検出される**
- PC のリボンのボタンも **26〜28px** で床を割っている。これは意図された例外で、
  `style.css:517` に理由が書いてある——「`.canvas-header` が高さ 40px・上下パディング 8px ＝ 中身 24px の枠だから」。
  **リボンをタイル化すると `height:40px` の枠が無くなるので、この例外の根拠も消える**（＝床を敷ける。§3-3）

### 2-6. 台本・テストの当たり（v581 で数え直し）

| 実測 | 数 |
|---|---|
| `steps` を持つ台本 | **49本**（`demos*.json` 36本＋`tutorials.json` 13本） |
| 回帰テスト | **189件** |
| `setMode` の呼び出し（`tests.js`） | 77箇所 |

**右パネルの中を指している台本のセレクタ**

| セレクタ／アクション | 箇所 | 本数 |
|---|---|---|
| `summon`（`#summon-input` を直に引く） | **23** | **13** |
| `reactionButton`（`#reaction-actions button` をテキストで探す） | **14** | **9** |
| `.mode-tab[data-mode="free"]` | 12 | 12 |
| `#btn-stereo` | 5 | 4 |
| `#btn-quiz` / `#quiz-series` / `#quiz-strength` | 2 / 2 / 2 | — |
| `#btn-rx-next` / `#btn-rx-prev` / `#select-reaction` / `#check-reaction-mode` | 2 / 1 / 1 / 1 | — |
| `.mode-tab[data-mode="puzzle"]` / `#select-series` / `#btn-show-target` / `#btn-verify` | 各1 | — |
| `#btn-isomers` / `#btn-time-attack` / `#btn-stereo-quiz` | 各1 | — |

> **`summon` と `reactionButton` は同じ本に重なっている。** `demos-reaction.json` の**7本すべて**が
> `summon` を使い、うち6本が `reactionButton` も使う。ほかに `l1-digest`（longform）と
> `tutorials.json` の `reaction` が両方持つ ＝ **8本が両方**。
> つまり `DESIGN_molecule_modal.md` 第2段で**すでに1手足すことになっている本**と大きく重なるので、
> **名称呼び出しをモーダルへ入れる案の追加費用は、見た目の数字（23箇所）より小さい**。
> それでも**この文書は名称呼び出しをモーダルへ入れない**（§5・46 の行）。

**動きの契約（`tutorial.js` を読んで確認済み・molecule_modal §2-7 と同じ）**

| アクション | 隠れた要素（rect=0）のときの挙動 |
|---|---|
| `button` | **カーソル演出を省いてクリックだけ実行**（`tutorial.js:340`）。`syncSheetForButton` が右パネルの外なら シートを開かない |
| `summon` | ⚠ **ガードが無い**。カーソルが画面左上 (0,0) へ飛ぶ（`tutorial.js:435`） |
| `reactionButton` | ⚠ **同じくガードが無い**（`tutorial.js:448`） |

### 2-7. 判定結果は、もうキャンバス側にある

`#verify-result` は右パネルにあるが、**主役はすでに `#canvas-toast`**（キャンバス内の字幕）。

> 「右パネルの `#verify-result` はスクロールで見切れて気づかれないことがあるため、
> **キャンバス内の字幕を主役にする**（`#verify-result` も従来どおり更新して互換を保つ）」
> — `game.js:2023-2034`

**つまり「判定の結果を見せる」は、右パネルが無くても今日そのまま動く。** 器だけ残せばよい。

---

## 3. リボンの設計（9枠をどう使うか）

### 3-1. タイルの形

| 項目 | 値 | 根拠 |
|---|---|---|
| 大きさ | **52×46**（アイコン1行＋短ラベル1行） | §2-1。全端末で 9 枠・文字切れ 0 |
| タップ標的 | 46px | 32px の床（TAP1・`check-mobile.mjs`）を満たす |
| 並び（縦画面） | 折り返しの帯。375px で 6個/行・320px で 5個/行 | §2-1 |
| 並び（横画面） | **2列 × 5行** | 568×320 でちょうど 10 枠 |
| 並び（PC） | 1行（900px で 9個・1024px 以上で 11個） | §2-1 |

### 3-2. 9枠の中身

| # | タイル | いまどこ | 種別 |
|---|---|---|---|
| 1 | ↩ 戻す | リボン | 道具 |
| 2 | ↪ やり直し | リボン | 道具 |
| 3 | 🔍 全体表示 | リボン | 道具 |
| 4 | 🗑 全消去 | リボン | 道具 |
| 5 | 🔤 まとめる | リボン | 道具 |
| 6 | 🧪 標準 | 右パネル `.mode-tab[data-mode="free"]` | **入口** |
| 7 | 🧩 パズル | 右パネル `.mode-tab[data-mode="puzzle"]` | **入口** |
| 8 | 📚 学習 | 右パネル `.mode-tab[data-mode="learn"]` | **入口** |
| 9 | ❓ ヘルプ | ヘッダー `#btn-help` | **入口** |

**道具5 ＋ 入口4 ＝ 9。予備はゼロ。**

- **横画面では「左列＝道具5・右列＝入口4」に割る。** 2列×5行にちょうど収まり、
  区切り線もラベルも足さずに「道具」と「行き先」が分かれる
- 縦画面では 375px で「1行目＝道具5＋余り1 / 2行目＝入口4」、320px で「1行目＝道具5 / 2行目＝入口4」と
  **自然に割れる**（実測の 5個/行・6個/行 から）
- `⤓ JSON`（制作用）は**いまどおりモバイルで非表示**。PC でも 900px では 10枠目が2行目に落ちるので、
  **Help モーダルの中へ移すか、1024px 以上でだけ出す**。§11 の未決 1

> **予備がゼロであることは、この設計のいちばん硬い制約。**
> 新しい入口を足したくなったら、**リボンではなくモーダルの中**に足すこと。
> どうしてもリボンに要るなら、`🔤 まとめる` を分子モーダルの「🔬 調べる」節へ移して1枠空ける
> （`DESIGN_entry_points.md` §8 未決4 が「`DESIGN_ui_modes.md` §3.3 は分析ツールに集約と書いたが
> 実装はリボン」と記録している論点。移せば**設計書どおりに戻る**）。
> ⚠ `#btn-condense` は台本 2箇所で使われている。

### 3-3. リボンをタイル化すると何が変わるか

| 変わるもの | 中身 |
|---|---|
| `.canvas-header { height: 40px }` | **無くなる**（`height:auto`）。PC で 40 → 59px。キャンバスの高さが 678 → 660px（−18px）。右パネルを外した後の幅の利得（+335px）に比べれば無視できる |
| `style.css:517` の「32px の床を敷かない理由」 | **根拠が消える**。タイルは 46px なので、床を敷いてよい。⚠ **コメントも同時に直す**（開発方針 5章） |
| `#coord-display` | PC で 39px しか無く、タイル化すると居場所が無い。**PC でも横画面と同じく隠す**か、キャンバス左下へ移す。§11 の未決2 |
| `#mobile-sheet-toggle`（☰ お題・設定） | 右パネルが無くなる段（第5段）で**消える**。⚠ テスト 4箇所・`#sheet-close` 2箇所・`body.sheet-open` の仕組みが不要になる |
| 台本の画作り | **49本すべてでリボンの見た目が変わる**。⚠ ただし収録は **810×1440 ＝ モバイル縦**（`record.mjs:53`）なので、変わるのは**上の帯だけ**。タップの輪は `getBoundingClientRect()` から毎回引くので**誤操作に見える事故は起きない**（`DESIGN_entry_points.md` §7-2）。**撮り直しはしない** |

---

## 4. 面は4つになる

「リボン＋モーダル」だけでは §2-3 のものが行き場を失う。**4つの面**に分ける。

| 面 | 何を置くか | 実体 |
|---|---|---|
| **① リボン** | 入口の名前だけ（9枠） | `.canvas-header` |
| **② モーダル** | 手を止めて**読む・選ぶ**もの | `.modal-overlay > .modal-content`（既存の規約に乗る） |
| **③ 作業帯** | **いま進めている作業の操作と状態**。キャンバスを見ながら押すもの | `canvas-overlay`（新設。`#rx-compare-overlay` と同じ種類） |
| **④ 常時表示** | 描くたびに変わるもの | `#mobile-name-chip`・分子の見出し・`#canvas-toast`（**すべて既にある**） |

### 4-1. ②と③の分け方

`DESIGN_molecule_modal.md` §4-1 の基準をそのまま延長する。

> 作図の手を止めずに知りたいものは常時、手を止めて読むものは呼び出し。

これに1本足す:

> **キャンバスを見ながら押すものは作業帯。**

| ② モーダル（手を止めて読む） | ③ 作業帯（キャンバスを見ながら押す） |
|---|---|
| どの練習をするか選ぶ（Study のメニュー） | 反応機構のステップ送り 🔄⏮▶⏭ |
| どのお題を解くか選ぶ（Puzzle のシリーズ・問題） | 書き出し練習の「次を書く」「答え合わせ」と進捗 |
| 判定オプション・遊び方・FAQ | お題の名前と分子式、**構造判定**（§7） |
| 分子の反応・試薬・異性体・立体（＝分子モーダル） | 名称から分子を呼び出す（§5 の 46） |

### 4-2. ③ 作業帯の性質

- **モードで中身が入れ替わる、1つの帯**（3つ作らない）
  - 🧪 標準 … 名称から分子を呼び出す入力欄だけ（`DESIGN_ui_modes.md` §7「自由モードの初期状態:
    名称呼び出しの導線を目立たせる」の**未実装分の回収**）
  - 🧩 パズル … `🧩 酢酸　CH₃COOH` ＋ `[✅ 構造判定]` ＋ `[お手本]`
  - 📚 学習（機構） … `⏮ ▶ ⏭ 🔄` ＋ ステップ説明
  - 📚 学習（書き出し） … `いま: C₄H₁₀　ブタン` ＋ `[次を書く]` ＋ `3/5`
- **キャンバスを覆う量に上限を置く。** 実装時に測ること:
  - 縦画面で **48px 以内**（1段）。2段にしたら 320px でキャンバスが 155px しか残らない
  - **`#mobile-name-chip`（キャンバス左下・`style.css:985`）とぶつからない**位置に置く
  - **横画面ではリボンが右端 114px を占める**ので、帯は左端から `right: 118px` までに収める
- **押せるものは 32px 以上**（§2-5 の 28px・31px を敷き直す）

---

## 5. 38項目の振り分け（全数）

`DESIGN_entry_points.md` §2 の通し番号 1〜47 に、`DESIGN_molecule_modal.md` §3-3 の 48〜52 を足して
**1つずつ**振り分ける。「分子モーダル」の行は**別レーンで決着済み**なので再検討しない。

### 5-1. ヘッダー・リボン（1〜9）

| # | 入口 | 行き先 | 理由 |
|---|---|---|---|
| 1 | ❓ ヘルプ | **リボン 9枠目**（ヘッダーから移設） | ユーザーの案の Help。中身は 47 と統合（§5-4） |
| 2 | X: 0, Y: 0 | **PC でも隠す**（または常時表示 ④ へ） | 入口ではない。タイル化で居場所が無い。§11 未決2 |
| 3 | ↩ 戻す | リボン 1枠目 | 道具 |
| 4 | ↪ やり直し | リボン 2枠目 | 道具 |
| 5 | ⤓ JSON | **1024px 以上の PC だけ** or Help モーダル | 制作用。900px で 10枠目は2行目に落ちる（§3-2） |
| 6 | 🔤 官能基をまとめる | リボン 5枠目（**または分子モーダルへ移して1枠空ける**） | §3-2 の注 |
| 7 | 🔍 全体表示 | リボン 3枠目 | 道具 |
| 8 | 全消去 | リボン 4枠目 | 道具 |
| 9 | ☰ お題・設定 | **消える** | 右パネルが無くなる。⚠ テスト4箇所・`#sheet-close`・`body.sheet-open` |

### 5-2. 右パネル 38項目（10〜47）

| # | 入口 | 行き先 | 理由・注意 |
|---|---|---|---|
| 10 | 🧪 自由 タブ | **リボン 6枠目**（`.mode-tab` のまま移設） | §8。台本 12箇所が生きたまま |
| 11 | 🧩 パズル タブ | **リボン 7枠目**（同上）＋ Puzzle モーダルを開く | 台本 1箇所 |
| 12 | 📚 学習 タブ | **リボン 8枠目**（同上）＋ Study モーダルを開く | — |
| 13 | シリーズを選択 | **Puzzle モーダル** | 台本 1箇所（`#select-series`）。`select` アクションは rect=0 でも動くが**画作りが変わる** |
| 14 | 問題を選択 | **Puzzle モーダル** | — |
| 15 | **構造判定 (Submit)** | **③ 作業帯（お題ストリップ）** | **§7 で単独に扱う** |
| 16 | お手本を見る | **③ 作業帯**（お題の隣） | 押すと `#target-modal`。お題を見ながら押すもの。台本 1箇所 |
| 17 | ☑ 不斉炭素原子の判定 | **Puzzle モーダル** | 一度決めたら変えない設定。⚠ スイッチ 24px（§2-5） |
| 18 | ☑ 立体を名前に反映する | **Puzzle モーダル** | 同上。⚠ **モーダルの見出しの名前にも効く**（molecule_modal §3-2 の注） |
| 19 | 🎓 クイズに挑戦（acc） | **Study モーダル**（群の見出しに） | ⚠ **`#learn-acc-quiz` を消さない**（`demos-stereo.json` が `#learn-acc-quiz>summary` で開く） |
| 20–27 | クイズ8ボタン | **Study モーダル**（メニュー） | 押したら Study を閉じて既存モーダルを開く（重ねない） |
| 28 | ✏️ 書き出し練習（acc） | **Study モーダル**（群の見出しに） | — |
| 29–31 | 異性体／アルキル基／立体異性体の書き出し | **Study モーダル（選ぶ）→ 閉じて ③ 作業帯（やる）** | **§2-3**。キャンバスを動かす。⚠ カード内14個のお題ボタンに id が無いのは**この見直しでも直らない** |
| 32 | ⚗️ 反応機構ビューア（acc） | **Study モーダル**（群の見出しに） | — |
| 33 | ☑ 反応機構モード | **Study モーダル** | 台本 1箇所（`#check-reaction-mode`） |
| 34 | 反応の選択（select） | **Study モーダル** | 台本 1箇所（`#select-reaction`）。選んだら閉じて再生へ |
| 35–38 | 🔄 ⏮ ▶ ⏭ | **③ 作業帯** | **§2-3**。巻矢印は本体 SVG に出る。台本 3箇所。⚠ 28px → 32px へ |
| 39 | 🎯 生成物を予測する | **③ 作業帯** | `game.userMolecule` を差し替える（`reaction.js:394`） |
| 40 | 予測を判定 / やめる | **③ 作業帯** | 同上 |
| 41 | 🔍 いま描いている分子 | **④ 常時表示**（`#mobile-name-chip` を全画面に） | `.mobile-only` を外すだけ。`syncMobileNameChip()`（`game.js:2210`）はそのまま |
| 42 | 🎯 反応させる分子を選ぶ | **分子モーダル**（第2段・決着済み） | molecule_modal §3-2 |
| 43 | 反応ボタン列 `#reaction-actions` | **分子モーダル**（第2段・決着済み） | 同上。⚠ `reactionButton` 14箇所／9本に1手 |
| 44 | 📚 異性体を調べる | **分子モーダル**（第1段・決着済み） | 同上 |
| 45 | 🧊 立体で見る | **分子モーダル**（第1段・決着済み） | 同上 |
| 46 | 名称から分子を呼び出す | **③ 作業帯（標準モード）** | **モーダルに入れない。**理由は下 |
| 47 | 📝 遊び方と操作方法 | **Help モーダル**（1 と統合） | §5-4 |

> **46（名称呼び出し）をモーダルに入れない理由。**
> ① `DESIGN_molecule_modal.md` §3-2 の判断（**作図の道具**であって「この分子を調べる」ではない。
> A-4 でわざわざ反応カードから救い出した）をそのまま引き継ぐ。
> ② `summon` **23箇所／13本にガードが無い**（`tutorial.js:435`）ので、
> 隠れた入力欄になると**カーソルが画面左上 (0,0) へ飛ぶ**。
> ③ 作業帯に置けば **rect が出るので台本は1箇所も直さなくてよい**。
> ④ `DESIGN_ui_modes.md` §7 の「自由モードの初期状態: **名称呼び出しの導線を目立たせる**」は
> **まだ果たされていない**（`DESIGN_entry_points.md` §2-7）。標準モードの作業帯に置けば、ここが片づく。

### 5-3. v524 で増えた分（48〜52）

| # | 入口 | 行き先 |
|---|---|---|
| 48 | ← 自由に戻る（`#btn-back-to-free`） | **Puzzle / Study モーダルのフッター**（「← 標準にもどる」）。リボンの 🧪標準 タイルでも同じことができるので**保険**。id 据え置き |
| 49 | ✕ 閉じる（`#sheet-close`） | **消える**（シートが無くなる） |
| 50 | 🅳 D体・L体はどれ？ | Study モーダル |
| 51 | ⚖ 同じ？違う？（連続） | Study モーダル |
| 52 | ⏱ タイムアタック 上級 | Study モーダル |

### 5-4. 行き先ごとの要約

| 行き先 | 入る項目 | 数 |
|---|---|---|
| **① リボン（9枠）** | 1・3・4・6・7・8・10・11・12 | 9 |
| **② Puzzle モーダル** | 13・14・17・18（＋48 フッター） | 4 |
| **② Study モーダル** | 19〜34 のうち選ぶ部分・50・51・52 | 15入口（裏に23種類） |
| **② Help モーダル** | 1 と 47 を統合（＋5 の JSON を入れる案） | 1 |
| **② 分子モーダル**（別レーン） | 42・43・44・45・`#molecule-props` のチップ | 4＋α |
| **③ 作業帯** | 15・16・29〜31（やる部分）・35〜40・46 | 12 |
| **④ 常時表示** | 41（`#mobile-name-chip`）・2（座標。任意） | 1〜2 |
| **消える** | 9（☰）・49（✕閉じる）・`#right-panel` そのもの | 3 |

### 5-5. Help モーダルの中身（1 と 47 の統合）

- `#tutorial-modal`（❓ ヘルプ・FAQ・デモ再生）が**すでにある**。
  `.hint-box`（📝 遊び方と操作方法）を**その中の1タブ／1節として入れる**
- 高さ: `.hint-box` は 320px 幅のモーダルで **1800px ＝ 3.5画面**（実測）。
  読み物なのでスクロールでよい。**ただし `#tutorial-modal` の既存の中身と足すと相当長くなる**ので、
  節の見出しで畳む（`<details>` は既に `#hint-details` で使っている作法）
- ⚠ **`.hint-box` の中身は R11 が中の `<li>` を読む**ので、**構造を変えない**こと
- ⚠ 遊び方の文面に「☰ お題・設定」「右のパネル」「下の帯」など、**この見直しで無くなるものへの言及**が
  あれば同時に直す（開発方針 5章「ヘルプと実装は常に一致させる」）

---

## 6. 学習の3アコーディオンを1枚に畳めるか

### 6-1. 答え: **畳めない。しかも畳んではいけない。**

**寸法**（モーダルの枠に入れて実測。可視高は 320px で 507px・375px で 727px・PC で 803px）

| 中身 | 320px（枠 288px） | 375px（枠 338px） | PC（枠 450px） | 320px で何画面 |
|---|---|---|---|---|
| **学習ブロック丸ごと（3acc 全開）** | **2259px** | 2135px | 1857px | **4.5画面** |
| ① 🎓 クイズに挑戦 だけ | 848px | 811px | 794px | 1.7画面 |
| ② ✏️ 書き出し練習 だけ | 1140px | 1053px | 792px | 2.2画面 |
| ③ ⚗️ 反応機構ビューア だけ | 265px | 265px | 265px | 0.5画面 |

- 横あふれは **全部 0 件**（`scrollWidth === clientWidth`）。縦だけの問題
- **1枚にすると 4.5画面。** `#stereo-modal` の 2.6画面（molecule_modal §2-6）より長い。成立しない

**畳んではいけない理由**は §2-3。②と③は**キャンバスを動かす**ので、
全画面のモーダルで覆うと自分の仕事が見えない。

### 6-2. 割り方: Study モーダルは「メニュー1枚」にする

```
📚 学習                                    [× 閉じる]
├ ① 見比べる（2つの図が同じものかを見分ける）
│   🎓 同じ化合物？ / 📝 命名 / 🪞 立体異性体 / 🎯 同じ立体はどれ？
│   🅳 D体・L体はどれ？ / ⚖ 同じ？違う？
├ ② 並べ替える（十字の模型を回す）
│   🔤 記号でパズル → ⏱ タイムアタック → ⏱ 上級 → 🔃 フィッシャー操作練習
├ ③ 数える
│   🔢 立体異性体は何種類？
├ ✏️ 書き出し練習
│   異性体 / アルキル基 / 立体異性体          ← 押すと**閉じて**作業帯へ
└ ⚗️ 反応機構ビューア
    ☑ 機構モード ＋ 反応の選択（select）      ← 選ぶと**閉じて**作業帯へ
                                           [← 標準にもどる]
```

- **v453 の3群（① 見比べる／② 並べ替える／③ 数える）と順路の矢印はそのまま使う。**
  作り直さない
- **クイズ11個は既存モーダルを開く。** `DESIGN_molecule_modal.md` §5-5 の「重ねない」に従い、
  **Study を閉じてから開く**（14枚とも `z-index:1000` なので、DOM の後ろが上に来る）
- ⚠ **`#learn-acc-quiz` を残す**（`demos-stereo.json` が `#learn-acc-quiz>summary` で開く）。
  `<details>` のままメニューの中に置くのがいちばん安い
- 高さの見込み: ① の 848px に②③④⑤の見出しと 5ボタンを足して **約 1100px ＝ 2.2画面**（320px）。
  スクロールで成立する。1画面には収まらない

### 6-3. 深さは 4段 → 3段に浅くなる

| | いま（モバイル） | この設計 |
|---|---|---|
| 命名クイズに着くまで | ☰ → 📚 タブ → 🎓 acc → 📝 命名 ＝ **4段** | 📚 タイル → 📝 命名 ＝ **2段** |
| 書き出し練習を始めるまで | ☰ → 📚 → ✏️ acc → お題ボタン ＝ **4段** | 📚 タイル → 異性体 → お題ボタン ＝ **3段** |
| PC | 📚 タブ → acc → ボタン ＝ 3段 | 📚 タイル → ボタン ＝ **2段** |

`DESIGN_entry_points.md` の診断 **D3（深さ3〜4段の入口が多すぎる）に、モバイルで 1〜2段ぶん効く。**

---

## 7. 構造判定（Submit）の置き場所

**パズルの中心操作なので、単独で扱う。**

### 7-1. 何を守るのか

パズルの筋書きは「**お題を読む → キャンバスで組む → 判定を押す**」。
このうち**お題は組んでいるあいだずっと見えていなければならない**——それが課題そのものだから。

- ⚠ **判定をモーダルの中に入れると「解いて → 開いて → 押す」になる**（ユーザーの指摘のとおり）
- ⚠ **お題をモーダルの中に入れると、もっと悪い。** 覚えるか、何度も開き直すことになる
- **いまのモバイルでも、お題は組んでいるあいだ見えていない**（シートは画面外）。
  つまりこれは**新しい問題ではなく、いまある問題**。この見直しで直せる

### 7-2. 3つの案

| 案 | 置き場所 | 良いこと | 悪いこと |
|---|---|---|---|
| **A** | **③ 作業帯のお題ストリップに、お題名・分子式と一体で** | 視線が動かない。**リボンの枠を1つも使わない**。お題も同時に見える | 帯がキャンバスを 48px 覆う。新しい面が要る（ただし §2-3 で**どのみち要る**） |
| B | リボンに 10枠目として独立 | 場所が固定で覚えやすい | **9枠が道具5＋入口4でちょうど埋まる**ので予備がゼロに。SE 横は 10枠が上限で、**そこに置くと機種による余裕が消える**。しかもお題は別の場所 |
| C | Puzzle モーダルの中 | 面が増えない | **「解いて → 開いて → 押す」**。却下 |

### 7-3. 推し: **案A**

理由を3つ:

1. **判定は「お題と見比べて押す」操作**で、お題から離すと視線が飛ぶ。
   案B は判定だけ画面の反対側（横画面ではリボンは右端）に置くことになる
2. **作業帯はどのみち要る**（§2-3。機構ビューアと書き出し練習のために）。
   案A は**新しい面を増やさない**。案B は枠を1つ食ったうえで作業帯も要る
3. **リボンは常に同じ並びでいられる。** 案B だと「パズルのときだけ 10枠目が生える」ことになり、
   リボンの並びがモードで動く ＝ 覚えた位置が変わる

**id は据え置く**（`#btn-verify`・`#target-name`・`#target-formula`・`#btn-show-target`）ので、
台本 2箇所（`#btn-verify` 1・`#btn-show-target` 1）とテスト 1箇所は無傷。

### 7-4. ストリップに入るもの・入らないもの

| 入る | 入らない（Puzzle モーダルで見る） |
|---|---|
| `#target-name`（酢酸） | `#target-desc`（説明文） |
| `#target-formula`（CH₃COOH） | シリーズ・問題の選択 |
| `[✅ 構造判定]` | 判定オプション2つ |
| `[お手本]` | — |

⚠ **`#target-desc`（説明文）が常時見えなくなるのは、この設計で失うもの**（§10-1）。
48px の1段に説明文は入らない。**お題ストリップをタップすると Puzzle モーダルが開く**形にして、
説明を読みたい人が1手で戻れるようにする。

---

## 8. `setMode` と `data-modes` はどうなるか

### 8-1. 結論: **`setMode('puzzle'|'learn'|'free')` の3値は変えない。3モードも残す。**

`DESIGN_entry_points.md` §7 の不変条件をそのまま守る。

**変えるのは「モードが何を出し分けるか」だけ。**

| | いま | この設計 |
|---|---|---|
| モードが出し分けるもの | `#right-panel [data-modes]`（`game.js:4062`） | **③ 作業帯の中身**（＋どのモーダルが開くか） |
| モードの表示 | 右パネルの `.mode-tab` | **リボンのタイル**（同じ `.mode-tab` 要素を移設） |
| モード離脱の後始末 | reactionPlayer / isomerPractice / alkylPractice / stereoPractice / reactor の5つ（`game.js:4065-4085`） | **1行も変えない** |

### 8-2. 3モードは要るか — **要る。減らさない。**

踏み込んで検討したが、減らす理由がない:

1. **モードは「作業帯に何が出ているか」そのものになる。** パズル中はお題と判定、
   機構の再生中はステップ送り、書き出し中は進捗。**これは同時に出せない**ので、状態は要る
2. **`setMode` は5つの後始末を分岐している**（`game.js:4065-4085`）。
   モードを畳むと、この5つを別の仕組みで面倒を見ることになる ＝ 危険が増えて得がない
3. **台本13箇所（`.mode-tab[data-mode=...]`）と `tests.js` の `setMode` 77箇所**が3値を前提にしている。
   `DESIGN_entry_points.md` §5-2 が案B を退けた理由がそのまま生きている

> **`DESIGN_entry_points.md` §8b のユーザー決定（自由を標準に、パズル・学習は呼び出す ⇆ 抜けて戻る）は、
> この設計でそのまま形になる。** リボンの 🧪標準 が「戻る」、🧩パズル・📚学習 が「呼び出す」。
> v448 で既定値と導線だけ変えたものが、ここで**見た目としても揃う**。

### 8-3. `#right-panel [data-modes]` の置き換え

`game.js:4062` の1行:

```js
document.querySelectorAll('#right-panel [data-modes]').forEach(el => { ... });
```

- **セレクタから `#right-panel` を外すだけ**でよい（`document.querySelectorAll('[data-modes]')`）。
  作業帯の中の節に `data-modes="puzzle"` 等を付ければ、**出し分けの仕組みは1つのまま**
- ⚠ **右パネルが DOM から消える最後の段まで、このセレクタは変えない。**
  変えると、まだ右パネルに残っている要素と作業帯の要素の**両方**が動いて、段の切り分けが崩れる

### 8-4. あわせて回収する「決めたのに未実装」の2件

| 出典 | 決めたこと | この設計での回収 |
|---|---|---|
| `DESIGN_mobile.md` §3 | 「ヘッダー（ロゴ・**モードタブ**・❓）」「モードタブを**主役に**」 | **リボンへ移す**（ヘッダーではないが、常時見える所へ）。`DESIGN_entry_points.md` の A-2 は「やらない」と決めた分だが、**リボン統合の副産物として実現する** |
| `DESIGN_ui_modes.md` §7 | 「自由モードの初期状態: **名称呼び出しの導線を目立たせる**」 | **標準モードの作業帯に置く**（§5-2 の 46） |

⚠ **A-2 を「やらない」と決めた理由**（`DESIGN_entry_points.md` §8b）は
「投稿済み31本の字幕と表記を揃えるため」＝ **A-3（ラベルを動詞に）とセットの判断**だった。
**この設計はラベルを変えない**（🧪 自由・🧩 パズル・📚 学習の**名詞のまま**）ので、
8b の決定とは矛盾しない。⚠ ただしタイルは 52px 幅なので、**「自由」「パズル」「学習」の3〜4文字**に収める。

---

## 9. 段階（各段は独立して入れられる）

**不変条件は `DESIGN_entry_points.md` §7 のまま**: **`id` を変えない・消さない／`setMode` の3値を変えない**。
版番号は実装レーンが自分の帯で振る。

### ⚠ 並行レーンとの関係

| レーン | この設計との関係 |
|---|---|
| **分子モーダル 第1段**（`#btn-isomers`・`#btn-stereo` をモーダルへ） | **第4段の前提**。終わると `#compound-info` から 44・45 が抜ける |
| **分子モーダル 第2段**（`#reaction-actions` をモーダルへ） | **第5段の前提**。終わると `[data-modes="free"]` の `#reaction-card` が空になる |
| 化合物2本（`compounds.json`・`tools/`） | 衝突しない |
| ヨウ素（`chemistry.js`・`reactor.js`） | 衝突しない |

> **第1段〜第3段は分子モーダルのレーンと完全に独立**（触るのは `.canvas-header`・
> `[data-modes="learn"]`・`#tutorial-modal`）。**先に入れてよい。**
> **第4段・第5段だけが分子モーダルの完了を待つ。**

### 第1段 — リボンをタイルにする（中身は1つも動かさない）

| 動かすもの | 中身 |
|---|---|
| `style.css` | `.canvas-header` を 52×46 タイルの折り返し格子に。PC の `height:40px` を `auto` へ。横画面は 2列 |
| `index.html` | ボタンのラベルを「アイコン＋改行＋短ラベル」に。**id・クラス・`data-tutorial` は1つも変えない** |
| `style.css:517` のコメント | 「40px の枠だから床を敷かない」の**根拠が消える**ので書き換え、`.canvas-header .view-btn` に 32px の床を敷く |

- **テストで固定すること**
  - 新規 **RB1**: 「**320px 縦・568×320 横・900×700 PC の3つで、リボンの全項目が可視域に入っている**」
    （`getBoundingClientRect()` が器の中。⚠ iframe 幅を変えるテストは**必ず finally で戻す**
    ——`DESIGN_entry_points.md` §6 Step 2 と同じ注意。既存 189件は「≥900px」を暗黙の前提にしている）
  - 新規 **RB2**: 「リボンのタイルが 32px を下回らない」（TAP1 の走査にも入るが明示でも押さえる）
  - 既存で守られる: 台本は `#btn-undo` `#btn-condense` など **id で引く**ので壊れない
- **台本の影響**: **49本すべてで上の帯の見た目が変わる。** セレクタは1つも壊れない。**撮り直しはしない**

### 第2段 — Help をリボンへ（`.hint-box` を `#tutorial-modal` に統合）

| 動かすもの | 中身 |
|---|---|
| 移設 | `#btn-help` をヘッダー → リボン 9枠目。**id 据え置き** |
| 移設 | `#hint-details` の中身（`.hint-box`）を `#tutorial-modal` の1節へ。**`.hint-box` の内部構造は変えない**（R11） |
| 直す | 遊び方の文面のうち、無くなるもの（☰・右パネル）への言及 |

- **テストで固定すること**
  - 既存 **R11**（`.hint-box` の `<li>` を読む）が**そのまま通る**こと ＝ 内部構造を変えていない証明
  - 新規 **RB3**: 「`#btn-help` がリボンの中にあり、押すと `#tutorial-modal` が開く」
- **台本の影響**: `#btn-help` は台本 0箇所（`tests.js` に2箇所）。**壊れない**

### 第3段 — Study をリボンへ ＋ 作業帯の新設

**この段がいちばん大きい。** 作業帯という面を作るのがここ。

| 動かすもの | 中身 |
|---|---|
| 新設 | `#work-strip`（`canvas-overlay`）。モードで中身が入れ替わる1つの帯（§4-2） |
| 新設 | `#study-modal`（`modal-overlay`）。**`#stereo-modal`（`index.html:856`）より前**に置く（重ね順。molecule_modal §5-5） |
| 移設 | `[data-modes="learn"]` のメニュー部分 → `#study-modal`。**`#learn-acc-quiz` を残す** |
| 移設 | `#reaction-controls`（🔄⏮▶⏭）・`#reaction-step-label`・`#reaction-caption`・`#btn-rx-predict`・`#btn-rx-judge`・`#btn-rx-cancel-predict` → **作業帯** |
| 移設 | 書き出し練習3種の**進捗と操作**（`#ip-body` などが生成する部分のうち、練習中に押すもの）→ **作業帯**。**お題を選ぶ部分はモーダルに残す** |
| 移設 | `.mode-tab[data-mode="learn"]` → リボン 8枠目。押すと `setMode('learn')` ＋ `#study-modal` を開く |
| 直す | 32px の床を `#reaction-controls`（28px）と書き出し練習の入力欄・「練習する」（31px）に敷く |

- **テストで固定すること**
  - 新規 **RB4**: 「📚 学習タイルを押すと `setMode('learn')` になり `#study-modal` が開く」
  - 新規 **RB5**: 「Study からクイズを開くと **`#study-modal` が閉じている**」（重ねない）
  - 新規 **RB6**: 「**反応機構の再生中、`#study-modal` は閉じていて `#arrows-group` に巻矢印が出ている**」
    （＝ キャンバスが見えている証明。**この段でいちばん価値のある1件**）
  - 新規 **RB7**: 「異性体の書き出し練習中に作図を変えると、作業帯の『いま:』が書き換わる」
    （`learn.js:742` の `onDrawingChange` が生きている証明）
  - 既存: `#learn-acc-quiz` を使うテスト 7箇所・`molecule-props` 7箇所は**この段では触らない**
- **台本の影響**
  - `.mode-tab[data-mode="learn"]` 1箇所・`#btn-quiz` 2・`#quiz-series` 2・`#quiz-strength` 2・
    `#btn-time-attack` 1・`#btn-stereo-quiz` 1 … **id/属性で引くので壊れない**が、
    **`#study-modal` を開いてから押す絵になる**（`syncSheetForButton` は右パネルの外と判定してシートを開かない）
  - `#check-reaction-mode` 1・`#select-reaction` 1・`#btn-rx-next` 2・`#btn-rx-prev` 1 …
    **⚠ `#select-reaction` はモーダル、`#btn-rx-next` は作業帯**に分かれる。
    `demos-stereo.json` などが「機構モードON → 反応を選ぶ → 次へ」と続けて押すので、
    **モーダルが閉じるタイミングを台本と突き合わせて確認すること**
  - ⚠ **`video-scripts/V*.md` の該当台本の記述も同時に直す**（開発方針 5章）

### 第4段 — Puzzle をリボンへ ＋ お題ストリップ（※ 分子モーダル第1段の完了が前提）

| 動かすもの | 中身 |
|---|---|
| 新設 | `#puzzle-modal`。中身は 13・14・17・18 ＋ フッターに `#btn-back-to-free` |
| 移設 | `#target-name`・`#target-formula`・`#btn-verify`・`#btn-show-target` → **作業帯のお題ストリップ**。**id 据え置き**（§7-3） |
| 移設 | `#target-desc` → `#puzzle-modal`（ストリップには入らない。§7-4） |
| 移設 | `.mode-tab[data-mode="puzzle"]` → リボン 7枠目 |
| 移設 | `#compound-info` の名前・式 → `#mobile-name-chip` を**全画面表示**に（`.mobile-only` を外す） |

- **テストで固定すること**
  - 新規 **RB8**: 「**パズルモードで、`#puzzle-modal` を閉じたままお題名・分子式・構造判定が見えている**」
    （§7 の核心。320px・375px・PC の3つで）
  - 新規 **RB9**: 「作業帯の `#btn-verify` を押すと、いままでと**同じ判定結果**（`#canvas-toast` と `#verify-result`）が出る」
  - 新規 **RB10**: 「`#mobile-name-chip` が PC でも出る」
  - 既存: 判定まわり（`verifyMolecule` を呼ぶテスト群）が**そのまま通る**こと。**判定ロジックは1行も変えない**
- **台本の影響**
  - `.mode-tab[data-mode="puzzle"]` 1・`#select-series` 1・`#btn-show-target` 1・`#btn-verify` 1 …
    **id で引くので壊れない**。`#select-series` だけモーダルの中に入るので**画作りが変わる**
  - ⚠ **お題の連続プレイ**: 正解すると `#win-modal` の「次のステージへ」（`#btn-next-stage`）が出るので、
    **問題を替えるのに `#puzzle-modal` を開き直す必要はない**。ここは実害が小さい（§10-1）

### 第5段 — 右パネルを消す（※ 分子モーダル第2段の完了が前提）

| 動かすもの | 中身 |
|---|---|
| 移設 | `#summon-input`（46）→ **作業帯（標準モード）**。**id 据え置き ＝ `summon` 23箇所は無傷** |
| 移設 | `.mode-tab[data-mode="free"]` → リボン 6枠目 |
| 削除 | `#right-panel`・`#mobile-sheet-toggle`（9）・`#sheet-close`（49）・`#sheet-backdrop`・`body.sheet-open` |
| 直す | `game.js:4062` のセレクタから `#right-panel` を外す（§8-3） |
| 直す | `tutorial.js` の `syncSheetForButton` … **右パネルが無ければ常に「開けない」に落ちる**。
消さずに無害化しておく（`?rec=` の再生経路を最後に触るのは避ける） |

- **テストで固定すること**
  - 新規 **RB11**: 「`#right-panel` が DOM に無い」
  - 新規 **RB12**: 「`setMode` の3値がすべて受け付けられ、知らない値が `free` に落ちる」（`game.js:4054` の契約）
  - 新規 **RB13**: 「320px 縦・568×320 横で、**本体が横スクロールせず 32px 未満の標的が 0 件**」
    （`tools/check-mobile.mjs` と同じ観点を回帰テストにも。molecule_modal MM9 と同じ考え方）
  - 既存: `mobile-sheet-toggle` 4箇所・`sheet-close` 2箇所・`right-panel` 3箇所の**テストを畳む**
- **台本の影響**
  - `.mode-tab[data-mode="free"]` **12箇所／12本** … リボンのタイルが `.mode-tab` のままなので**壊れず、
    カーソル演出も残る**（§8-1）。**これが「タイルを `.mode-tab` にする」ことのいちばんの見返り**
  - `summon` 23箇所／13本 … 作業帯なので rect が出る。**壊れない**

### 全段共通の手順（開発方針 6章・7章）

1. `assembler/index.html` / `test.html` / `audit.html` の `?v=` とヘッダー表示を同時更新
2. `test.html` を開いて**全件合格**（RX13 は約10%の頻度で落ちるので1回だけ再実行して切り分ける）
3. `node tools/verify-release.js assembler`
4. `node tools/check-mobile.mjs assembler`（**320px を含む20端末**）
5. **320px 縦・375px 縦・568×320 横・900×700・1280×800 の5つで実機確認**し、コミットメッセージに書く
   （**568×320 と 900×700 は、この見直しの容量の床そのもの**。省かない）
6. **UTF-8（BOMなし）**・化けパターンの確認

---

## 10. 何が失われるか

`DESIGN_molecule_modal.md` §4 と同じ厳しさで数える。

### 10-1. 実際に失われるもの（6つ）

| 失うもの | いまの姿 | 手当て |
|---|---|---|
| **PC の3カラムが同時に見える** | 左パレット・キャンバス・右パネルが並び、**作図しながら反応も学習の道具も見えている** | **PC でも1つずつになる。**引き換えにキャンバスが 1024px で **+93%**・1280px で +54%（§2-4）。**ここが最大の取引** |
| **お題の説明文が常時見えなくなる** | `#target-desc`（「お酢の主成分であり…」）が右パネルに出ている | ストリップには入らない（48px）。**ストリップをタップで Puzzle モーダルが開く**形にして1手で戻す（§7-4） |
| **学習の3つの入口が「そこにある」ことが見えなくなる** | 3つのアコーディオンが右パネルに居座り、閉じていても存在は見える | Study モーダルを開けば1枚に並ぶ。**閉じているあいだは見えない**——ここは正直に失う |
| **シリーズ・問題の切替が1段深くなる** | 右パネルに常時ある | 正解時は `#win-modal` の「次のステージへ」が出るので**連続プレイは通らない**。**途中で問題を変える人だけが 2手になる** |
| **反応の件数のライブ更新** | 描くたびに `#reaction-actions` が組み直る | `DESIGN_molecule_modal.md` §4-2 で決着済み（見出しに件数）。**この文書では変えない** |
| **座標表示（X: 0, Y: 0）** | PC のリボン左端 | タイル化で居場所が無い。**PC でも隠す**か常時表示へ。§11 未決2 |

### 10-2. モバイルでは、面積は1px も広がらない

**これは正直に書いておく必要がある。**

- シート（縦）もドロワー（横）も**もともと画面外**なので、右パネルを消してもキャンバスは変わらない（§2-4）
- **モバイルで良くなるのはこの3つだけ**:
  1. **☰ を押してシートを開く1手が消える**（入口がリボンに常時見えている）
  2. **深さが 4段 → 2〜3段**（§6-3）
  3. **320px 縦でリボンが 151px → 約110px** ＝ **キャンバスが 41px 広がる**（§2-1）
- 逆に **375px 縦ではリボンが 102 → 約110px** ＝ **キャンバスが 8px 狭くなる**

### 10-3. `DESIGN_molecule_modal.md` §4-1 の「常に見えているべきもの」は全部守られる

| 常に見えているべきもの（§4-1） | この設計での置き場所 | 守れるか |
|---|---|---|
| 名前・分子式 | `#mobile-name-chip`（全画面に）＋ キャンバスの見出し | ✅ |
| ①②③ の番号 | キャンバスの見出し（`renderMoleculeLabels`） | ✅ |
| いま選んでいる分子の枠（青の破線／琥珀の実線） | キャンバス（`game.js:4608`） | ✅ |
| できる反応が何件あるか | 分子モーダルの入口の件数（molecule_modal §4-2） | ✅（別レーン） |
| キャンバスそのもの | — | ✅ **広がる** |

**さらにこの文書で1つ足す:**

| 常に見えているべきもの（追加） | 置き場所 |
|---|---|
| **いま進めている作業の状態**（お題／機構のステップ／書き出しの進捗） | ③ 作業帯 |

---

## 11. 未決事項（実装前にユーザーに確認したいこと）

1. **`⤓ JSON`（制作用）をどうするか。** 900px の PC で 10枠目が2行目に落ちる。
   **Help モーダルへ移す**／**1024px 以上でだけ出す**／**そのまま2行目に落とす** の3択。
   ⚠ 台本 0箇所なので、どれでも壊れない
2. **座標表示（X: 0, Y: 0）を PC でも隠してよいか。** タイル化すると居場所が無い。
   横画面ではもう隠れている。作図の役に立っているなら**キャンバス左下**へ移す
3. **`🔤 官能基をまとめる` を分子モーダルへ移して、リボンに予備を1枠作るか。**
   移すと `DESIGN_ui_modes.md` §3.3（分析ツールに集約）どおりになり、
   `DESIGN_entry_points.md` §8 未決4 が片づく。⚠ 台本 2箇所の画作りが変わる
4. **作業帯をキャンバスの上端に置くか、下端に置くか。**
   下端は親指が届くが `#mobile-name-chip`（左下）とぶつかる。上端は縦画面でリボンの真下になる。
   **実装時に 320px で両方作って見比べる**のを推す
5. **モードのラベルをタイルの3〜4文字に縮めることの是非。**
   「🧪 自由 / 🧩 パズル / 📚 学習」は名詞のまま（8b の決定）だが、
   52px 幅だと「自由」「パズル」「学習」に縮む。**投稿済み動画の字幕とはズレない**（同じ語の短縮）が、
   `.hint-box` と FAQ の表記は揃えること
6. **背景タップ・Escape で閉じられるようにするか**（molecule_modal §9-1 と同じ論点）。
   Study / Puzzle / Help の3枚が増えると**閉じる作法の統一の価値が上がる**。
   **やるなら 14＋4 枚まとめて**別項目にするのを推す
7. **第3段（Study ＋ 作業帯）を1コミットで入れるか、割るか。**
   いちばん大きい段で、作業帯という新しい面を作る。
   **「作業帯を作って機構ビューアだけ移す」→「Study モーダルを作る」→「書き出し練習を移す」**の
   3コミットに割ったほうが、実機確認の粒度が合う

---

## 12. ユーザーの決定（2026-08-05）

§11 の未決7件に回答を得た。**設計はこの節が正。**

| # | 論点 | 決定 |
|---|---|---|
| 1 | `⤓ JSON` の行き先 | **Help モーダルへ**（台本0箇所なので壊れない） |
| 2 | 座標表示（X: 0, Y: 0） | **消す**（PC でも出さない） |
| 3 | `🔤 官能基をまとめる` | **分子モーダルへ移す。台本の画作りは気にしない** |
| 4 | 作業帯は上端か下端か | 実装時に 320px で両方作って見比べる |
| 5 | モードのラベル | **「自由」ボタンは廃止**（下の 12-1） |
| 6 | 背景タップ・Escape | 今回はやらない。14＋4枚まとめて別項目 |
| 7 | 第3段の割り方 | **3コミットに割る**（作業帯 → Study モーダル → 書き出し練習） |

### 12-1. 「🧪 自由」ボタンは廃止し、`free` タブを「← 自由に戻る」に統合する

ユーザーの言葉:

> **自由というボタンはいらない**と考えていますがどうですか？ パズル・学習から抜けたら自由です。

**そのとおりで、v448 の決定（自由を標準にし、パズル・学習は呼び出す⇆戻る）の延長線上にある。**
いまは同じことを**2つの UI で表している**: `.mode-tab[data-mode="free"]` と `#btn-back-to-free`。

⚠ **ただし `data-mode="free"` のタブを押す台本が12箇所ある**
（demos-reaction 7・demos-stereo 4・demos-longform 1。実測）。単純に消すと全部落ちる。

**解き方: 消さずに統合する。**

- `class="mode-tab" data-mode="free"` は**セレクタごと据え置く**
- ラベルを「← 自由に戻る」にする
- **パズル・学習にいるときだけ出す**（標準にいる間は場所を取らない）
- `#btn-back-to-free` は役目が重なるので**統合して1つにする**

これで **台本12箇所・`setMode` の3値・回帰テストは無傷**のまま、画面からは「自由」ボタンが消える。
**リボンの枠が1つ空く**（§3-2 は道具5＋入口4でちょうど埋まり予備ゼロだった）。
③でもう1枠空くので、**予備は2枠**になる。

---

## 13. 抜け方の実測と、先に入れた手当て（v644）

ユーザーの指摘:

> **各パズル・学習からの抜け方は確認しておく必要があります。**
> 答え合わせ or ギブアップ 答えを見る など

リボン統合は「抜ける」導線をまとめる話なので、**先に現状を数えた**。

### 13-1. 抜け方は3種類あり、揃っていなかった

| 活動 | 抜け方 | 途中でやめられるか |
|---|---|---|
| モーダルのクイズ11枚 | **閉じるボタンだけ** | できるが**採点されない** |
| **書き出し練習3種** | **🔍 答え合わせ** ／ **練習をやめる** ／ 答えを見る | **2通りある（採点する／しない）** |
| 反応機構ビューア | 「やめて再生に戻る」（予測モードから） | ビューアを出る導線は別 |
| パズルのお題 | 構造判定 ／ お手本を見る | **「やめる」が無かった** |

**書き出し練習だけが「答え合わせ／ギブアップ」を両方持っていて、いちばん良くできている。**

### 13-2. 判断と、入れたもの（v644 で実装ずみ）

| # | 論点 | 決定 | 実装 |
|---|---|---|---|
| A | 抜け方の語を揃えるか | **揃えない**（無理に統一しない） | — |
| B | 書きかけを捨てるとき確認するか | **出す** | `leaveGuard` ＋ `#confirm-modal` |
| C | パズルに「やめる」を足すか | **足す** | `#btn-give-up`「↷ このお題をやめて次へ」 |

**B の実装で守った線**: 確認は**人がタブや「← 自由に戻る」を押したときだけ**挟み、
**`setMode` の中では挟まない**。setMode は台本・テスト・`?open=` からも呼ばれるので、
そこで止めると無人再生が固まる。`window.confirm` も使わない（スレッドを止めるため）。

### 13-3. ⚠ リボン統合で引き継ぐこと

**`leaveGuard` は「人の操作」に掛かっている。** リボンのタイルが新しい入口になるとき、
**タイルのクリックも `leaveGuard` を通すこと**（`.mode-tab` の click に付けてあるので、
タイルを `.mode-tab` のまま移設すれば自動的に効く ＝ §1-3 の方針と噛み合う）。

**実装中に見つかった行き止まりも直した**: `goToNextStage` がシリーズ内で先頭へ巻き戻す作りで、
**1問しかないシリーズ（既定の「はじめに（操作の練習）」）では押しても何も起きなかった**。
シリーズの最後では次のシリーズの先頭へ移るようにした。

---

## 14. 第1段の実測（v650・実装ずみ）

「リボンをタイルにする。中身は1つも動かさない。」を入れた。**入口は1つも移していない。**

### 14-1. タイルの寸法

| 項目 | 値 |
|---|---|
| 大きさ | **52×46**（`width:52px` / `min-height:46px`。全端末で同じ） |
| 中身 | `<span class="tile-icon">`（17px）＋ `<span class="tile-label">`（10px）の2段 |
| 隙間 | 5px |
| セレクタ | **`.canvas-header button`**（クラスではなく**置き場所**で書く。移設のたびに指定が外れるのを防ぐ） |

⚠ **`.mobile-only { display: none }`（クラス1つ）はタイル指定（クラス1つ＋要素1つ）に負ける。**
☰ が PC のリボンに生えたので、`.canvas-header .mobile-only` で敷き直した。
**置き場所で書いた指定は、打ち消しも置き場所で書く。**

⚠ **ラベルを差し替えるときは `textContent` ごと入れ替えないこと。** span が消えて1行に潰れる。
`game.js` の 🔤 トグルは `.tile-icon` / `.tile-label` だけを書き換えている。

### 14-2. 6つの画面幅での枠数（実測。9枠に増やした状態で数えた）

| 画面 | リボンの器 | 1行（横画面は1列）の枠数 | 9枠のときの段数 | リボンの高さ | 旧 |
|---|---|---|---|---|---|
| 320×568 縦 | 306×59 | **5** | 2段 | 110px | 151px ＝ **41px 返った** |
| 375×812 縦 | 361×59 | **6** | 2段 | 110px | 103px（＋7px） |
| **568×320 横** | 109×258 | **2列 × 5行 ＝ 10** | 5行2列・全部器の中 | — | 1列 114px |
| **900×700 PC** | 238×110 | **3** ⚠ | 3段（161px） | — | 40px（ただし**中身が溢れていた**） |
| 1280×800 PC | 618×59 | **10** | **1行** | 59px | 40px |
| 1920×1080 PC | 1258×59 | **21** | **1行** | 59px | 40px |

- **キャンバスの高さ**: 320px 縦 271 → **363px**（+92px）／375px 縦 563 → **607px**（+44px）。
  §10-2 の「375px 縦では 8px 狭くなる」は外れで、**実際は広がった**（座標表示の1行が消えたぶん）
- **文字の切れは 0 件**（`scrollWidth > clientWidth` の走査）。短ラベルは**4文字まで**
  （「やり直し」「まとめる」「全体表示」「お題設定」「結合表示」）
- **32px 未満の押しものは 0 件**（`check-mobile.mjs` 20端末・TAP1・RB2）。
  旧リボンは PC で 26〜28px と床を割っていた

> ⚠ **900px の PC だけ 3枠/行**。右パネルが残っているあいだの制約で、第5段で解ける（§2-1 の注）。

### 14-3. §12 の決定のうち、この段で片づけたもの

| # | 決定 | どうしたか |
|---|---|---|
| ② | 座標表示（X: 0, Y: 0）を消す | **要素は残して CSS で `display:none`**。`id` を消さない不変条件と、`game.js` が毎回書き込む先を守るため。R6 が「横画面だけ隠す CSS ルールの有無」を見ていたので、**要素の実効スタイル**を見る形に書き換えた |
| ① | `⤓ JSON` を Help モーダルへ | `#tutorial-modal` の閉じるボタンの上へ。**id 据え置き**（ハンドラは id で結んでいる）。モバイルで隠していた `display:none` も一緒に畳んだ ＝ **隠す理由そのものが無くなった** |

③（🔤 を分子モーダルへ）と ⑤（自由ボタンの統合）は**手を付けていない**（第2段以降）。

### 14-4. 新しく固定したテスト

| | 内容 |
|---|---|
| **RB1** | 320×568 縦・568×320 横・900×700 PC で、**リボンの全タイルが器の中**にあり、ラベルが切れず、本体が横スクロールしない。⚠ 共有 iframe は幅を変えず、**使い捨ての iframe** を開く（N3 と同じ作法） |
| **RB2** | 320・568横・900・1280 の4つで、タイルが **32px の床を割らない**かつ **52×46 のまま**（端末ごとに大きさが変わると9枠の勘定が崩れる） |
| **RB3** | `⤓ JSON` が Help モーダルの中にある／座標表示は**要素を残したまま**隠れている／🔤 のトグルで `.tile-label` が消えない |

**全 215 テスト合格**（212 → +3）。`verify-release.js assembler` 合格・`check-mobile.mjs assembler`（20端末）不合格 0・警告 0。

---

## 15. 第2段の実測（v651・実装ずみ）

「❓ ヘルプをリボンへ。📝 遊び方を Help モーダルへ。」を入れた。**入口の移設としては1つめ。**

### 15-1. 動かしたもの・動かさなかったもの

| | 中身 |
|---|---|
| 動かした | `#btn-help` … ヘッダー → リボン 9枠目。**id 据え置き**（`tutorial.js:24` が id で結んでいる）。形も他の8枠と同じ 52×46 タイル |
| 動かした | `#hint-details`（📝 遊び方と操作方法）… 右パネルの末尾 → `#tutorial-modal` の中 |
| **動かさなかった** | `.hint-box` の**内部構造**（R11 が `<li>` を読む・EP2 が畳みを見る）。中身も1文字も変えていない |
| **動かさなかった** | 右パネルそのもの・モードタブ・`setMode` の3値・`game.js` の `#right-panel [data-modes]` セレクタ |

> **遊び方の文面に「☰」「右パネル」「下の帯」への言及は元から無かった**（`.hint-box` の
> テキストを機械的に走査して確認）。第2段で直す文面は無い。
> `tutorials.json` の FAQ 側には「右パネル」が5箇所あるが、**右パネルは第5段まで実在する**ので
> ここでは触らない。

### 15-2. 5画面の実測

| 画面 | リボンの器 | タイル | 段/列 | キャンバス | 字切れ | 32px未満 |
|---|---|---|---|---|---|---|
| 320×568 縦 | 306×110 | 6 | 2段 × 5列 | 306×312 | 0 | 0 |
| 375×812 縦 | 361×59 | 6 | 1段 × 6列 | 361×607 | 0 | 0 |
| **568×320 横** | 109×**290** | 7 | 5行 × 2列 | 400×306 | 0 | 0 |
| 900×700 PC | 238×110 | 6 | 2段 × 3列 | 238×508 | 0 | 0 |
| 1280×800 PC | 618×59 | 6 | 1段 × 6列 | 618×659 | 0 | 0 |

- **横画面のリボンの上端を 40px → 8px に上げた。** 40px は「右上に浮く ❓」を避けるための
  余白だったが、その ❓ がリボンに入ったので**避ける相手が消えた**。器は 258 → **290px**。
  枠数は 5行×2列 ＝ 10 のまま（6行目には 51px 足りない）だが、**余りが 8px から 40px になった**
- Help モーダルの高さ（320px 幅）: 遊び方を**閉じて 676px / 開いて 2246px**。枠 511px の中でスクロール

### 15-3. ⚠ 落とし穴（3つ）

**① `id` で書いた古い上書きが、置き場所で書いたタイル指定に勝つ。**
`#btn-help { min-height: 32px }`（詳細度 0,1,0）が
`.canvas-header button { min-height: 46px }`（0,1,1）を**上回り**、❓ だけ 52×**38** の
背の低いタイルになった。横画面では列送りの段がそろわなくなる。**RB2 が捕まえた。**
§14-1 の「置き場所で書いた指定は、打ち消しも置き場所で書く」の裏返しで、
**置き場所で見た目を決めたら、id で書いた古い上書きは残さない**。
移設のたびに「移すものに付いていた id 指定」を探すこと。

**② `overflow-y: auto` を持つ flex の子は、min-height が 0 に落ちて潰れる。**
Help モーダルの枠を縦スクロール（`max-height:90dvh; overflow-y:auto`）にした瞬間、
FAQ の一覧（`#tutorial-list`）が**高さ 0 になって画面から消えた**。
`.modal-content` は縦の flex で子は既定 `flex-shrink:1`。ふつうは `min-height:auto` が下限を守るが、
**自分が `overflow-y:auto` の子は min-height が 0 になる**ので、2000px の遊び方が入ってきた瞬間に
縮められる側が全部を譲る。`#tutorial-modal .modal-content > * { flex-shrink: 0 }` を敷いて、
あふれは枠のスクロールで受ける。
⚠ **これは第3段以降の作業帯にもそのまま効く**（作業帯もキャンバスの上の縦 flex になる）。

**③ インラインの `max-height` は `:has()` の詰め直しを無効にする。**
「遊び方を開いているあいだは FAQ の一覧を詰める」を
`#tutorial-modal .modal-content:has(#hint-details[open]) #tutorial-list { max-height: 22vh }`
で書いたが、`#tutorial-list` の 55vh が**インライン属性**にあったため効かなかった。
インラインへ書いた寸法は、あとから条件付きで上書きできない ＝ **CSS 側へ出す**。

### 15-4. 新しく固定したテスト

| | 内容 |
|---|---|
| **RB4** | ❓ヘルプが**リボンの中**にあり（ヘッダーに残っていない）／アイコン＋短ラベルの2段で 32px の床を満たし／押すと `#tutorial-modal` が開き／遊び方と ⤓ JSON が**同じ1枚**にあり／枠が縦スクロールする |
| R11（既存） | **無改変で合格** ＝ `.hint-box` の内部構造を変えていない証明 |
| EP2（既存） | 「開いたら見える」を見る前に Help モーダルを出す1行だけ追加（置き場所の変更に追随） |

**全 216 テスト合格**（215 → +1）。`verify-release.js assembler` 合格・`check-mobile.mjs assembler`（20端末）不合格 0。

> ⚠ **`ST29`（立体タイムアタック）が1回落ちた。** 乱数で出題を作るテストで、再実行で合格。
> `RX13` と同じ性質（§9 の手順 2 の注）。**落ちたら1回だけ再実行して切り分ける**対象に `ST29` も入る。

---

## 16. 第3段の実測（v677〜v679・実装ずみ）

「Study をリボンへ ＋ 作業帯の新設」。**§12 の決定7どおり3コミットに割った。**

| | 版 | 中身 |
|---|---|---|
| その1 | v677（`ef11536` を検証） | `#work-strip` の新設と ⚗ 反応機構の面 |
| その2 | v678 | 📚 学習タイルをリボンへ ＋ `#study-modal` |
| その3 | v679 | 書き出し練習3種の進捗と操作を作業帯へ |

> ⚠ その1 は前の担当が**未検証のまま**枠の上限で止まった 242行（`ef11536`）。
> 引き継いで最初に回した回帰テストが**そのまま全合格**したので、コードは足りていた。
> 足りていなかったのは **入口（📚 タイルと Study モーダル）と、実測と、テストの主張**。

### 16-1. 面ごとの振り分け（実装した現物）

| 面 | 中身 |
|---|---|
| ① リボン | 📚 学習タイル（`.mode-tab[data-mode="learn"]` を**移設**。8枠目） |
| ② Study モーダル | 3つのアコーディオン（クイズ11枚・書き出し練習のお題選び・機構モードのスイッチと `#select-reaction`） |
| ③ 作業帯 | ⚗ … `#reaction-controls`・ステップ表示・説明・🎯予測/判定/やめる<br>✏️ … 「お題／いま:」・進捗 `n/m`・＋登録／🔍答え合わせ／やめる |
| ④ 常時表示 | 変更なし（`#mobile-name-chip` は帯の高さぶん上へ逃がす） |

**モーダルに残したもの**（帯に持ち込まなかったもの）: 書いた図のサムネイル・段階ヒント・
「↺ 付け根を置き直す」「🔄 お題の図に戻す」・任意分子式の入力。
**練習中でも 📚 タイルをもう一度押せばメニューは開き直せる**ので、
「帯に無い ＝ 手が届かない」にはならない。帯を厚くする方が失うものが大きい。

### 16-2. 4画面の実測

| 画面 | リボン | キャンバス | ⚗ の帯 | ✏️ の帯 | Study の枠 | 32px未満 | 横スクロール |
|---|---|---|---|---|---|---|---|
| 320×568 縦 | 306×110 | 306×312 | 306×**87** | 306×**70** | 288×366（送り無し） | 0 | 無し |
| 375×812 縦 | 361×**110** | 361×**556** | 361×87 | 361×70 | 338×349（送り無し） | 0 | 無し |
| 568×320 横 | 109×290 | 400×306 | 282×87 | 282×70 | 511×288（枠内で送る） | 0 | 無し |
| 1280×800 PC | 618×59 | 618×659 | 618×87 | 618×70 | 520×349（送り無し） | 0 | 無し |

- **横画面の帯は `right: 118px` でリボンを避ける。** 実測で帯の右端 443 / リボンの左端 448 ＝ 5px あく
- **帯はキャンバスの下端に貼り付く。** 320px でキャンバスの 22%（✏️）〜28%（⚗）を覆い、残りは 225〜242px
- `check-mobile.mjs assembler`（20端末）で **⚗ の 28px 警告が消えた** ＝ 帯で敷いた 34px の床が効いている

### 16-3. ⚠ §4-2 の「縦画面で 48px 以内（1段）」は**守れなかったし、守らなくてよかった**

§4-2 は「2段にしたら 320px でキャンバスが 155px しか残らない」と書いていたが、**見積り違い**。
実測ではキャンバスは 312px あり、2段の帯（87px）を置いても **225px 残る（72%）**。

守らなかった理由は寸法だけではない:

- **⚗ の説明はコンテンツそのもの。** 「どの電子対がどこへ動くか」の1文が無いと、
  巻矢印を見ても何が起きているか読めない。1段に詰めるなら説明を捨てるしかなく、
  それは「作業帯に何を置くか」ではなく「機構ビューアをやめるか」の話になる
- **✏️ は「見るもの」と「押すもの」で段を割った方が短い。** 1行に混ぜると
  「いま: 2-メチルプロパン」のような長さの読めない文字列がボタンを押し出し、
  **結局2段に折れる**。先に2段と決めて上下に割ると 70px で収まる

**新しい上限**: 縦画面で **キャンバスの 30% 以内**。RB5 が 50% を上限として機械で見張っている。

### 16-4. ⚠ 落とし穴（4つ）

**① `active` は「お題がある」ことを意味しない。**
`tests.js` の leaveGuard 検査は、書きかけを偽装するために
`ip.active = true` **だけ**を立てる（`problem` は null のまま）。
帯を `active` だけで描くと `problem.formula` で例外になり、**検査が復元
（`ip.active = savedActive`）に到達できずに終わる**。すると以降の全テストが
壊れた状態を引き継ぎ、最後は完走しない（v679 で 600秒タイムアウトを実際に踏んだ）。
⚠ **失敗は「落ちたテスト」ではなく「完走しないテスト」として出る。**
症状から原因の場所が読めないので、`page.on('pageerror')` で**スタックを出す**のが最短だった。
判定は `active && problem` で行う。

**② 「閉じる相手」を id の表で持つと必ず書き忘れる。**
Study から始まる行き先はクイズ11枚・練習3種・機構ビューアで 15通り以上あり、
しかも `#ip-body` などの中身は `learn.js` が毎回描き直す。
**列挙せず、結果で決める**: モーダル自身に付けた `click` / `change` の listener が、
ボタンの handler が走り終えたあと（bubble）に「別のモーダルが開いたか／作業帯が出たか」を見て
自分を閉じる。**`setTimeout` は要らない** —— 祖先で受けているので子の handler は既に終わっている。
非同期にすると、押した直後を見るテストと台本が通らなくなる。

**③ モードタブは「移設」であって「複製」ではない。**
`.mode-tab` は `game.js` が一括で配線し、`setMode` が一括で `.active` を付け替える。
2つ置くと同じモードのボタンが2箇所で点灯し、台本の `.mode-tab[data-mode="learn"]` が
どちらを指すかも DOM 順まかせになる。**クラスを `.mode-tab` のまま移す**と、
`leaveGuard`（書きかけを捨てる確認・§13-3）がタイルにも自動で掛かる ＝ §1-3 の方針の見返り。

**④ `check-mobile.mjs` は、同じツリーで別のサーバーが動いていると嘘をつく。**
`python -m http.server` を自分の作業ツリーで立てたまま `check-mobile.mjs` を回すと、
**毎回ちがう端末が** `page.goto` の 25秒タイムアウトで落ち、
たまたま CSS が届かなかった1台が「横スクロールする・タップ標的が114件小さい」と報告される。
実体は**未スタイルの素の HTML を測っている**だけ。サーバーを止めたら不合格 0・警告 0 になった。
⚠ 端末ごとに症状が入れ替わったら、まず**自分が立てたサーバーを疑う**。

### 16-5. 新しく固定したテスト

| | 内容 |
|---|---|
| **RB5** | **📚 タイル → 機構モード ON の順に人と同じ道で入り**、再生中に `#study-modal` が閉じていて `#arrows-group` に巻矢印が出ていること／ステップ送り4つが作業帯の中にあり右パネルに残っていないこと／帯がキャンバス下端に貼り付き半分以上を覆わないこと／⏭ で説明が帯の中で書き換わること／抜けたら帯ごと畳むこと |
| **RB6** | 📚 タイルがリボンの中に**1つだけ**あり（複製していない）、押すと `learn` になって Study が開き、タイルが点灯し、枠が縦スクロールし、「閉じる」で閉じられる |
| **RB7** | Study から 🎓・📝・🔢 を開くと **Study 自身が閉じている**（重ねない） |
| **RB8** | 練習中に作図を変えると帯の「いま:」が書き換わり（`onDrawingChange` が生きている証明）、進捗が `0/n` → `1/n` と進み、押しもの3つが 32px の床を満たし、お題選びは Study モーダルに残っている |
| Q0・Q1（既存） | 居場所の変更に追随。タブの並びは**居場所ごとに**見る／learn の節が右パネルに無いことを明示で押さえる |
| EP4・EP5（既存） | **無改変で合格** ＝ Study の中身（id・内部構造・`?open=` の acc）を変えていない証明 |

**全 221 テスト合格**（218 → +3）。`verify-release.js assembler` 合格・
`check-mobile.mjs assembler`（20端末）不合格 0・**警告 0**。

### 16-6. 台本との突き合わせ

- `#select-reaction` は**モーダル**、`#btn-rx-next` は**作業帯**に分かれた。
  `tutorials.json` の「反応機構を見る」は「機構モードON → 反応を選ぶ → ⏭」と続けて押すので、
  **反応を選んだ瞬間に Study が閉じ、そのまま帯の ⏭ が押せる**（落とし穴②の配線）。
  字幕が「右パネルの…」と言っていたので、📚 タイルを押す1手を足して現物に合わせた
- `.mode-tab[data-mode="learn"]` は台本1箇所。**セレクタごと据え置いた**ので壊れない
- `#btn-quiz`・`#btn-time-attack`・`#btn-stereo-quiz` などは隠れていても `click()` が効き、
  `tutorial.js` は矩形 0 の要素のカーソル演出を省く ＝ **モーダルの中に入っても台本は動く**

### 16-7. ⚠ 次の段への申し送り

- **375px 縦のリボンが 1段 → 2段になった**（59 → 110px。キャンバス 607 → 556px）。
  361px には 6枠しか並ばず、8枠目で折り返す。**第4段（🧩）・第5段（🧪）でさらに2枠増える**ので、
  §14-2 の枠数表はここで見直しが要る。逃げ道は §12-1 の「🧪 自由の廃止」（1枠空く）と
  ☰ の撤去（第5段。もう1枠空く）で、**枠数は増やさずに入れ替えられる**見込み
- `#right-panel [data-modes]` のセレクタは**第5段まで変えない**（§8-3）。
  learn の節が抜けただけで、puzzle / free の出し分けは今までどおり
- 作業帯はまだ ⚗ と ✏️ の2面。**🧩 のお題ストリップ（第4段）と 🧪 の名称呼び出し（第5段）**が
  同じ器に入る。`game.setWorkPane(paneId, on)` と `.ws-pane` の作法をそのまま使うこと
