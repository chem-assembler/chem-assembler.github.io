# 立体化学対応 設計メモ（Fable への引き継ぎ用たたき台）

**ステータス: 未着手（実装ゼロ・データ変更ゼロ）。設計判断は Fable が行う。**
CLAUDE.md のモデル運用に従い、「化学モデルの正しさが問われる変更（正準コード・同型判定）」および
「決めが後を引く設計判断」に該当するため、Opus は現状整理と論点の洗い出しまでを担当した。

発端（2026-07-25 ユーザーとの対話）:
1. 化合物拡充の続きとして糖類を検討 → ユーザー指摘「糖類については α/β をどうするかという問題がある」
2. ユーザー案「糖類専用の環ユニットを作る」vs「より広範な設計を考えるべきか」の判断が必要
3. あわせて確認された疑問「以前導入した sp2 の結合角は現状無視しているのか」→ 2章で回答

---

## 1. 何が問題か（糖類は氷山の一角）

**現行の同一判定は「構成（つながり方）だけ」**を見る。したがって:

- **α-グルコースと β-グルコース**は、環状形のアノマー炭素(C1)の**立体だけ**が違い、構成は同一 → **区別不能**
- さらに深刻なのは、**すべてのアルドヘキソース（グルコース・ガラクトース・マンノース…）は構成が同一**
  （開環形はいずれも CHO-CHOH-CHOH-CHOH-CHOH-CH₂OH）。違いは C2〜C5 の立体配置だけ
- つまり「グルコース」を構成エントリとして compounds.json に入れると、**ガラクトースやマンノースを描いても
  同じ正準コードに一致し「グルコース」と誤命名される**。テスト F8（命名の一意性）にも抵触する
- **α/β をどう表すか以前の、より根本的な壁**である

**糖類だけの問題ではない**:

| 化合物 | 立体の種類 | 現行モデルでの状態 |
|---|---|---|
| α/β-グルコース | アノマー（C1配置） | 区別不能 |
| 各アルドヘキソース | C2〜C5 の配置 | 相互に区別不能 |
| D/L-アラニン（**既に stages.json にあり**） | 鏡像 | 構成のみで登録済み |
| 乳酸（**既に compounds.json にあり**） | 鏡像 | 構成のみで登録済み |
| 酒石酸（未登録） | (R,R)/(S,S)/**メソ体** | 3種すべて区別不能 |

現状は「立体を区別しない」ことで**一貫はしている**（既存の乳酸・アラニンも立体記述子なしの名前）。
糖類を入れようとすると、この一貫性が初めて破れる。

---

## 2. 現状の正確な棚卸し（「sp2 の結合角は無視されているか？」への回答）

**結論: 角度を捨てているのではなく、「同一判定が角度を見ない」だけ。sp2 の 120° は環ジオメトリとして現役。**

| 対象 | 座標・角度を見るか | 実装位置 |
|---|---|---|
| 同一判定 `canonicalCode` / `verifyMolecule` | **一切見ない**（重原子グラフ＋結合次数＋自動H数のみ） | chemistry.js:824 / `canonicalCode` |
| **例外1: cis/trans** | **見る**。C=C 軸に対する置換基の側を外積で判定（2置換の非環 C=C のみ／約6°未満は不定→null） | `getDoubleBondGeometry`。`compounds.json` の `geometry` と `lookupCompoundName` で照合 |
| **例外2: 不斉炭素マーク** | 座標は見ない。**どの炭素が不斉か**をユーザーが `*` で指定し、判定オプション時に照合 | `isAsymmetricCarbon`（4置換基の `rootedFragmentCode` が相異＝トポロジー判定）／game.js の `judgeAsymmetric` 後段 |
| 環の作図角度（**sp2 の 120° 系**） | **見る**。ベンゼン＝正六角形、環炭素の側鎖2本目は二等分線 **±30°**（≒120°） | game.js の `candidateAngles`（テスト D4/D5 が保護） |
| 鎖式の sp2（開鎖の C=C・C=O） | 直交（90°）のまま。一般の sp2 に 120° を強制する仕組みは無い | — |
| sp3 の実角 109.5°・くさび | **別枠表示のみ**（判定に非影響） | stereo.js。DESIGN_3d_correspondence.md が「判定に一切影響を与えない」「R/S はスコープ外」と明記 |
| 異性体列挙・練習 | 構成異性体のみ（重原子8個まで）。UI に「シス/トランス・鏡像は数えません」と明示 | `enumerateConstitutionalIsomers` / learn.js |
| IUPAC 系統名（v158〜v161 で実装） | 立体記述子を出さない（E/Z・R/S 非対応） | `iupacName`（非環式のアルカン/アルケン/アルキン/ハロゲン化物/アルコール/エーテル） |

**設計上、重要な2つの前例**:
- **cis/trans**: 「描き方から立体を1種類だけ推定し、名前に反映する」実装が既にある
- **`judgeAsymmetric`**: 「既定はトポロジー判定、立体的な厳格化はユーザーのオプトイン」という
  レイヤ構造の前例がある（影響を局所化できた実績）

---

## 3. 決めるべき論点（Fable の判断事項）

### 論点1: 同一判定に立体をどこまで入れるか

- **案A: 入れない（現行維持）** — 糖類は非対応と明示。既存の一貫性を守る
- **案B: 相対配置を正準コードに組み込む** — 各立体中心に up/down 等の記述子を持たせ、コードに含める。
  絶対配置（R/S・D/L）は名乗らず「相異なる立体異性体を区別する」ところまで
- **案C: CIP 風 R/S を計算して組み込む** — 絶対配置まで決定し、D/L・α/β を計算で出す。最も強力だが最も重い
- **案D: 糖専用の環ユニット**（ユーザー案） — α/β を選んで配置する特製モジュール。糖だけ特別扱い

### 論点2（最難関）: 2D直交作図で立体配置をどう「入力」するか

現状の絵（直交格子・平面）は sp3 の 3D 配置を表現できない。**cis/trans は座標から読めたが、
sp3 の配置は平面配置に自由度がないため読めない** — ここが本質的な難所。

- (a) **原子ごとの配置指定**（up/down トグル等をデータに持つ）— `isAsymmetricMarked` の拡張。直交作図と喧嘩しない
- (b) **くさび結合を第一級の入力に**（結合に wedge/hash 属性）— 教科書的だが描画の自由度を要求し、直交作図と衝突しやすい
- (c) **フィッシャー投影の専用入力モード** — 糖・アミノ酸に強い。学習効果も高いが新モードの新設
- (d) **糖専用の環ユニット**（論点1の案D と対応）

評価軸: 手描き感覚の維持 / 直交作図との整合 / 学習者が「見て分かる」か / 実装量 / 他分野への波及

### 論点3: 命名（D/L・α/β・R/S・E/Z の統合方針）

- 既存の `geometry`（cis/trans）と新機構をどう統合するか（二重管理を避ける）
- 糖の名前は**慣用名で持つ**（α-グルコース等をデータに登録）か、**計算で出す**か
- `iupacName` に立体記述子を出させるか（出すなら E/Z も同時に整合させる）

### 論点4: 既存機能・テストへの波及（数値・文言が動く）

立体を識別に入れると、以下の前提が広範に動く。**オプトイン設計なら影響を局所化できる**（`judgeAsymmetric` の前例）。

- 異性体列挙数（**IP4 が期待値 [2,3,3,5,5,7] を固定**）と「鏡像は数えません」の UI 文言
- クイズのペア生成（同一分子式で非同型のペアを「違う」問題にしている）
- **F7**（同値⇔コード一致の性質）、**F8**（命名の一意性）、**A3**（全エントリの自己整合）
- **B5 / G4**（不斉マーク）、**H1**（くさび図）
- 既存 143 エントリ（stages 56 ＋ compounds 87）の再解釈

### 論点5: 段階導入の切り方

M1/M2… をどう切り、**どこで止めても製品として一貫している**状態を保つか。

---

## 4. 案の比較（暫定評価。決定は Fable）

| | 糖類 α/β | 各アルドヘキソース | アミノ酸・乳酸・酒石酸 | 実装量 | 既存哲学との整合 | 主なリスク |
|---|---|---|---|---|---|---|
| A 現行維持 | × | × | ×（現状維持） | なし | ◎ | 糖類を永久に扱えない |
| B 相対配置をコードに | ○ | ○ | ○ | 大 | △（判定に立体が入る） | 入力手段の設計次第。絶対配置は名乗れない |
| C CIP風 R/S | ◎ | ◎ | ◎ | 特大 | △ | CIP 規則の正しい実装は重く、誤りが致命的 |
| D 糖専用ユニット | ○ | △（糖内のみ） | × | 中 | ×（孤島化） | 汎用の列挙/命名/判定と噛み合わない。後で行き詰まる |

### Opus の暫定推奨（あくまで参考。決定は Fable）

1. **案D（糖専用ユニット）は非推奨** — 早く届くが孤島化し、アミノ酸・酒石酸に効かず、
   「判定はトポロジーのみ」という原則に穴を空ける形で後戻りしにくい
2. **「オプトインの立体レイヤ」**（既定はトポロジー判定のまま、立体は選択的に厳格化）が
   既存哲学と最も整合する。`judgeAsymmetric` が前例
3. **入力手段は (a) 原子ごとの配置指定**が直交作図と最も喧嘩しにくい。
   ただし糖の教育文脈では (c) フィッシャー投影の訴求力が高く、要判断
4. α/β・D/L を**正しく名乗る**には案C 相当（または参照構造との比較）が必要。
   「区別はするが名乗らない」（案B）で止める判断もあり得る

---

## 5. 影響範囲（ファイル・関数）

- **chemistry.js**: `canonicalCode` / `canonicalRowsCore` / `rootedFragmentCode` / `verifyMolecule` /
  `isAsymmetricCarbon` / `getDoubleBondGeometry` / `enumerateConstitutionalIsomers` / `iupacName` 系
- **game.js**: 判定（`judgeAsymmetric` 後段）/ `lookupCompoundName` / `getCompoundLibrary`（`geometry` 照合）/
  `serializeState`（配置データの保存＝Undo/エクスポート）/ `renderAtom`・`renderBond`（くさび描画を採る場合）
- **learn.js**: `IsomerPractice`（列挙数・文言）/ `AlkylPractice` / **quiz.js**（ペア生成・命名クイズ）
- **stereo.js**: 既存くさび図との統合（DESIGN_3d_correspondence.md の M2「全体の標準くさび描画」と接続できる）
- **データ**: compounds.json（`geometry` の一般化）/ stages.json
- **テスト**: A3, B5, F3, F7, F8, F9, F10, G4, H1, IP1〜IP9, AK1

---

## 6. 検収の勘所（テスト設計の種）

- **性質テストの再定義**: F7 の「同値 ⇔ コード一致」を立体込みでどう定義するか
- **既知の立体異性体対で必ず区別され、同一物は必ず一致する**:
  α/β-グルコース、D/L-アラニン、乳酸の鏡像、**酒石酸の (R,R)/(S,S)/メソ体**
- **メソ酒石酸**（不斉中心2個だが分子として非キラル）を正しく扱えるかは**案の分かれ目になる良い試験紙**
- 既存 143 エントリの命名一意性（F8）が壊れないこと
- **立体を入れない既定モードでの回帰ゼロ**（全テスト合格を維持できる設計か）

---

## 8. Fable の決定（2026-07-25）

**結論: 「オプトインの立体レイヤ」を採る。識別の追加手段は案C（CIP）ではなく、
既存の `rootedFragmentCode`（＝正準な断片順序）に錨を打った "パリティ記述子" とする。
案A は既定モードとしてのみ残し、案C・案D は最終形として却下する。**

### 8.1 なぜこの形か（問題の再定義）

メモの論点1が案A〜Dを「識別に立体をどこまで入れるか」の一軸で並べているが、実際には
**3つの別問題**が絡んでおり、分けて設計すると結論が変わる。

1. **識別**（`canonicalCode` が立体異性体を区別できるか）＝ F8/グルコース問題の本体
2. **入力**（2D直交作図で配置をどう与えるか）＝ 最難関
3. **命名**（α/β・D/L を出す手段）

決定的な観察: **糖・アミノ酸・酒石酸はいずれも「compounds.json に登録する既知の名前つき実体」**であり、
名前を**ゼロから計算する必要はない**。cis/trans が既に「座標から立体を1種推定し、
`geometry` 付きエントリと照合して名前を付ける」形で動いているのと同じ土俵に乗せられる。
つまり **CIP を実装して R/S を計算する（案C）必要はない**。必要なのは
「異性体を確実に**区別**し、登録済みの参照構造と**照合**する」ことだけ。

### 8.2 識別の中核: フラグメント順序に錨を打ったパリティ（案B の厳密版）

各立体中心について、`isAsymmetricCarbon` が既に計算している**4置換基の
`rootedFragmentCode`**（H は 'H'）を辞書順に並べると、**構成的（＝achiral）な正準順序**が得られる。
この順序を基準に、実際の3D配置の**パリティ（偶奇＝ハンドednessの1ビット）**を記述子とする。

- **区別できる**: 置換基が全て相異なる中心なら、2つの配置はパリティビットが異なる ⇔ 別の立体異性体。
  → α/β-グルコース・各アルドヘキソース・D/L が正準コードで区別され、**F8 の誤命名が解消**する。
- **CIP を要さない**: 優先順位は CIP の原子番号規則ではなく**フラグメントコードの辞書順**を使う。
  よって「案C の CIP 実装が重く・誤りが致命的」というリスクを負わない。
- **鏡像も扱える**: フラグメントコードは achiral なので、分子全体を鏡映すると全中心のパリティが反転する。
  (R,R)={+,+} と (S,S)={-,-} は別コード＝区別、(R,S)={+,-} と (S,R)={-,+} は分子の内部対称
  （自己同型）で同一コードに畳まれる＝**メソ酒石酸を正しく1種**として扱える。
  `canonicalRowsCore` が既に自己同型で正準化しているので、**パリティは正準ラベリング確定後に
  合成する**設計にすれば畳み込みは既存機構に乗る（ここが唯一の難所。8.6 参照）。
- **ビットの絶対的意味は持たない**: この1ビットは「R か S か」ではなく「配置1か2か」。
  だが**表示しない**（命名は 8.4 のライブラリ照合で行う）ので問題にならない。

これは実質「メモの案B を、既存の正準断片順序に錨づけて厳密化したもの」。案D（糖専用ユニット）が
持つ「早く届く」利点は 8.4 の入力モード側で回収し、**識別モデル自体は糖に依存させない**。

### 8.3 既定は不変（回帰ゼロ）

`canonicalCode` の既定の戻り値は**一切変えない**。立体は
`canonicalStereoCode(mol)`（新設）でのみ付与し、**照合したい文脈だけがオプトイン**で使う。
`judgeAsymmetric` と同じレイヤ構造。これにより IP4（列挙数）・F7・F8・クイズ・既存143エントリは
既定モードで無変更。**異性体列挙（`enumerateConstitutionalIsomers`）には立体を入れない**
（UI 文言「鏡像は数えません」と整合。列挙は構成異性体のままが正しい）。

### 8.4 命名は「登録参照との照合」（計算しない）

compounds.json のエントリに、現行の `geometry`（cis/trans）と並ぶ**任意の立体記述子**を持たせる。
`lookupCompoundName` は現状すでに `候補を canonicalCode で引く → geometry で絞る → verifyMolecule で確定`
という構造なので、そこに**「立体記述子が一致するか」の絞り込みを1段足すだけ**（加算的・既存挙動は不変）。

- α-グルコース／β-グルコース／D-アラニン等を**別エントリ**として登録できる（立体コードが違うので F8 を割らない）。
- **α/β・D/L・R/S をゼロから計算して名乗ることは、既存合意どおりスコープ外**
  （DESIGN_3d_correspondence.md の「R/S はスコープ外」を踏襲）。名前はライブラリが出す。
- ユーザー分子に立体記述子が無い（未入力）場合、lookup は特定の立体異性体を**名乗らない**。
  「アルドヘキソース（立体未指定）」等の正直なフォールバックにする。これも一貫した停止点。

### 8.5 入力: 糖・アミノ酸には「フィッシャー投影モード」（直交作図の強みが活きる）

sp3 配置は一般の直交作図の座標からは読めない（メモ論点2の核心）。しかし
**フィッシャー投影は「横＝手前／縦＝奥」の固定規約を持つ直交図**であり、
本アプリの「手描き感覚の直交格子」と**幾何が完全に一致する**。すなわち、直交格子が
一般くさび図では弱点だったのに対し、**フィッシャーでは長所に転じる**。

- 糖・アミノ酸・酒石酸に限定した**専用入力モード**（異性体練習モードと同じ流儀の別モード）として実装。
  そのモード内でのみ縦横配置を意味づけ、パリティビットを読む（cis/trans が C=C で座標を読むのと同格の、
  局所化された例外）。**モード外では座標は従来どおり見た目専用**（「検証はトポロジーのみ」を割らない）。
- ユーザー案「糖専用の環ユニット」が直感していた実利（糖の配置を選ばせる）は、
  **識別モデルを汚さずに入力モード側で**満たす。
- 一般のくさび入力（非環式の任意中心）は**後回し**（8.7 の M3）。

### 8.6 唯一の難所と検収（テストファースト必須）

パリティを**正準ラベリング確定後**に、自己同型で畳み込める形で合成する部分だけが本質的に難しい。
ここは**先にテストを書いてから実装**する（メモ6章の試験紙を採用）:

- 必ず区別: α/β-グルコース、D/L-アラニン、乳酸の鏡像、酒石酸 (R,R)/(S,S)。
- 必ず同一: メソ酒石酸の2通りのラベリング（自己同型で畳まれること）。
- 回帰ゼロ: 既定 `canonicalCode` で全既存テスト合格を維持。
- 参照の自己整合: フィッシャーで描いた登録グルコースが α/β を正しく往復する（登録＝一度正しく描いて記述子を捕捉）。

### 8.7 段階導入（各停止点が製品として一貫）

- **M0（不可視・化学モデルの核）**: atom に任意 `stereo` 記述子＋`canonicalStereoCode` を新設
  （原子パリティ＋結合 E/Z の二本立て。8.9-(3)）。既定 `canonicalCode` は不変。
  8.6 の性質テスト群を先に用意。**ここが Fable 級の要**。
- **M1（命名＝参照照合）**: compounds.json に立体記述子を許可、`lookupCompoundName` に絞り込み1段追加。
  α/β-グルコース等を F8 を割らずに登録可能に。未指定分子は「立体未指定」フォールバック。
  既存 `geometry`（cis/trans）の新記述子への統合と検出制限の緩和もここ（8.9-(3)）。
- **M2（入力）**: フィッシャー投影モード（糖・アミノ酸系）＋ sp2 の ±120° スナップ候補（8.9-(4)）。
  ここで初めて糖の体験が成立。
- **M2.5（学習コンテンツ）**: 立体異性体の総数当て・立体のみ書き出し・同一性/鏡像クイズ（8.9-(2)）。
- **M3（任意・将来）**: 一般くさび入力／D/L・R/S の計算表示（R/S 名指しクイズ含む）。各々独立に停止可能。

### 8.8 却下した案と理由

- **案A（現状維持）を最終形として却下**: ユーザーが明示的に糖を望む。ただし**既定モードとしては採用**（回帰ゼロの土台）。
- **案C（CIP で R/S 計算）を却下**: 「名乗る」必要が無い（ライブラリ照合で足りる）のに、
  実装が重く誤りが致命的。区別だけならパリティで CIP なしに達成できる。
- **案D（糖専用ユニット）を最終形として却下**: 識別モデルを糖に特化させると孤島化し、
  アミノ酸・酒石酸に効かず「識別＝トポロジー（＋局所例外）」原則に穴を空ける。
  実利は M2 のフィッシャー入力モード（糖・アミノ酸に限定した**入力**の特別扱い）で回収する。

### 8.9 ユーザーレビュー反映（2026-07-25 同日）

**(1) ロールバック戦略（「大幅な変更なのでロールバックできるように」）**

3層で担保する:

- **git**: 方針・設計書は main に通常コミット（revert 可能）。**実装（M0以降）は作業ブランチ
  `feat/stereo` で進め、マイルストーン単位の停止点でのみ `--no-ff` マージ**する。
  マージコミット1個の revert でそのマイルストーン全体を巻き戻せる。main は GitHub Pages の
  公開元なので、WIP を main に置かないこと自体が安全策になる。
- **実行時**: 8.3 のオプトイン設計により、既定挙動は M0〜M2 を通じて不変（レイヤを使わなければ現行どおり）。
- **データ**: compounds.json の立体記述子は**任意フィールドの追加のみ**（既存エントリ無変更）。
  記述子を無視すれば現行の照合に退化する＝データ起因の巻き戻しも安全。

**(2) 立体の学習コンテンツ案（列挙・クイズ）— M2.5 として採録**

構成異性体の列挙（`enumerateConstitutionalIsomers`・IP4・「鏡像は数えません」）は不変のまま、
**別枠のオプトイン練習**として以下が M0 のコアだけで作れる:

- **立体異性体の総数を答える問題**: 骨格の各立体中心にパリティを、対象 C=C に E/Z を
  総当たりで割り当て、`canonicalStereoCode` で重複除去して数える
  （例: 酒石酸が **4 でなく 3** になること自体が M0 の性質テストを兼ねる）。
- **立体異性体のみの書き出し練習**: 異性体練習（IsomerPractice）の流儀で、構成を固定して
  立体だけを描き分けて登録する（入力は M2 のフィッシャー/座標読み取りに依存）。
- **同一性クイズ**: 「この2つは同じ？鏡像？」— パリティ比較だけで判定でき、**CIP 不要**。
- **注意**: R/S・D/L を**名指しさせる**問題（「これは R？S？」）は CIP か参照構造が要るため
  M3 扱い。同一性・鏡像関係までなら M2.5 で出せる。

**(3) C=C の cis/trans は「別途」ではなく本レイヤに統合する**

手書きでも描き分ける＝重要度が高い、かつ**入力問題が既に解けている**（座標から読める）唯一の
立体要素なので、後回しにせず**データモデルは M0 から二本立て**にする:

- `canonicalStereoCode` は**原子のパリティ（sp3）＋結合の E/Z（C=C）**の両方を含む設計とする。
  E/Z 側は既存 `getDoubleBondGeometry` の判定を流用（読み取りは実装済み）。
- 現行の制限（非環・2置換・分子内に対象1本のみ）の緩和と、compounds.json の既存 `geometry`
  フィールドの新記述子への統合（二重管理の解消）は **M1 で実施**。それまで `geometry` は現行どおり動く。

**(4) 作図角度のポリシー: 「座標は描いた時のまま・自動レイアウト変更はしない」**

立体を考えるとき C=C まわりを 120° で描きたい、しかし結合次数の切り替えで角度が変わるのは不可
（ユーザー指摘）。方針:

- **結合次数の変更で既存原子の座標を動かすことは絶対にしない**（Undo・スナップ・監査と衝突し、
  「座標は見た目専用」の原則にも反する）。
- 代わりに**描画時のスナップ候補**で解決する: sp2 炭素（C=C 端）から次の原子を伸ばすとき、
  直交候補に加えて **±120° 方向を候補に追加**する（環炭素の「二等分線±30°」`candidateAngles` と
  同じ流儀の、置くときだけの補助）。描いた後は動かさない。90° のままでも cis/trans 判定は
  外積の符号で成立する（閾値 sin 6°）ので、120° はあくまで見た目の選択肢。
- 練習・答え合わせ等の**自動生成レイアウトは 120° 描画を標準**にする（ユーザーの絵は触らない）。
- **追記（同日ユーザー提案・採用）: 「シス/トランス整形」ボタン**。
  「この結合まわりのシス/トランスを考えたい」という**明示の意図**で、選択した C=C まわりを
  120° の教科書レイアウトへシフトする操作を用意する。禁止しているのは「結合次数の切り替えに
  連動した**自動**の座標変更」であり、**ユーザー起動＋saveState（Undo可能）の整形は
  既存の原子移動と同格**で原則に反しない。仕様の骨子:
  - UI は `asymmetricMode`（マークモード→原子タップ）と同じ流儀: 整形モードで C=C をタップ。
  - 両端の置換基を C=C 軸から ±120° に再配置。**現在描かれている側（外積の符号）を保存**する
    （＝描き手の cis/trans 意図を変えない）。直線描画で側が不定なら trans を既定として展開。
  - **同じ結合をもう一度タップで cis⇄trans を反転**（シフト済みの図で立体を切り替える手段を兼ねる）。
  - 置換基の部分木はルートの移動量で剛体移動する（重なりが生じ得るが Undo 可能・手動調整可）。
  - 実装は化学モデルに触れない純粋な作図支援なので **M0 に依存せず単独で実装可能＝Opus 委譲可**。
    cis/trans の読み取り（`getDoubleBondGeometry`）は実装済みのため、現時点でも命名表示に効く。

### 8.10 次アクション

- **本決定を Opus に渡さず、M0 は Fable が着手**（正準コード・自己同型畳み込み・パリティ合成のコア）。
  M0 完了後の M1（データ＋lookup 1段追加）と M2 の定型部分は Opus に委譲可能。
- 実装前に **M0 の性質テスト（8.6）を tests.js に先置き**する。
- 実装は **`feat/stereo` ブランチで開始**し、停止点ごとに `--no-ff` マージ（8.9-(1)）。

---

## 10. M2 入力設計（Fable・2026-07-25。M0/M1 完了を受けて）

### 10.1 再定義: 「フィッシャー投影モード」は不要 — 常時読み取りでよい

8.5 では専用入力モードを想定したが、M1 実装後に再検討した結果、**モードを新設しない**:

- 本アプリの作図は直交格子であり、**フィッシャー投影の幾何（中心から上下左右へ結合）と最初から一致**している。
- cis/trans が既に「**命名照合に限り**座標を読む」例外として常時動いている（開発方針4章-4）。
  sp3 パリティも**同じ地位の例外**として `lookupCompoundName` でのみ常時読む。
  検証（verifyMolecule・パズル判定・クイズ）は従来どおり一切座標を見ない。
- 挙動互換は M1 と同じ一方向の意味論で保たれる: **stereo 付きエントリだけ**が読み取り結果との
  一致を要求する。既存エントリ・既存テストには影響しない。読めない中心は「記述子なし」に
  なるだけで、誤読は起きない（下の適格条件が厳しい方向に倒してある）。

### 10.2 スロット読み取り規約（`readAtomParityFromFischer`）

対象: sp3 の不斉炭素（`isAsymmetricCarbon`）のうち、**描かれた重原子隣接がすべて軸方向**
（上下左右いずれかから **±25°以内**。既定のスナップでは厳密に軸上）で、**相異なるスロット**を
占め、空きスロットが自由価数（暗黙H）でちょうど埋まるもの。1つでも条件を外れた中心は
黙ってスキップ（記述子なし）。

読み取り: スロットを **[上, 右, 下, 左]** の固定順に並べたタプル（空きスロットは 'H'）を
`computeAtomParity` に渡す。フィッシャーの規約（横=手前・縦=奥）はこのタプル順に暗黙に
織り込まれ、以下の性質が自動的に成り立つ（**規約の正しさはこの3性質で検収する**）:

1. **左右の入れ替え＝パリティ反転**（鏡像。転置1回）
2. **90°回転＝パリティ反転**（フィッシャーの規則どおり。4巡回=奇置換）
3. **180°回転＝パリティ不変**（同一分子。転置2回=偶置換）

戻り値は `{ atomId: ±1 }`。`lookupCompoundName` のユーザー立体コードを
`{ atomParity: readAtomParityFromFischer(mol), bondGeo: readBondGeoFromCoords(mol) }` に拡張する。

### 10.3 環の立体（α/β）は M2b へ分離

環内炭素は隣接が軸方向に揃わないため 10.2 の対象外（自然にスキップされる）。
α/β-グルコース（ピラノース環）には **Haworth 相当の上下マーク**（環外置換基に「手前/奥」の
1ビットを持たせ、環の巻き方向＋マークからタプルを構成）が必要で、データモデル・描画・
操作が新規になるため **M2b として別途設計**する（このマークは serializeState への追加を伴う）。

### 10.4 データ投入（M2a）: 開鎖形の糖・アミノ酸・乳酸

compounds.json に `stereo.atomParity`（添字キー→±1。`_mapStereoToMol` は M1 で対応済み）を
持つエントリを追加する。**パリティ値は手計算しない**: 教科書のフィッシャー投影どおりに
target 座標を作図（主鎖を縦一直線・OH等を左右へ）し、**新設の読み取り関数に通して得た値を
JSON に書く**（自己命名（F8）が定義により成立し、化学的正しさは「OHをどちら側に描いたか」
という教科書照合に還元される）。初期セット:

- D/L-グリセルアルデヒド（最小の教材。CHO上・OH右=D）
- D-グルコース・D-ガラクトース・D-マンノース（開鎖形。C2〜C5 の左右は教科書どおり。
  相互に正準コード同一・立体コードのみ相異＝**F8 が立体で初めて破れない**ことの実証）
- D/L-アラニン（COOH上・NH₂左=L）・D/L-乳酸（COOH上・OH右=D）

立体未指定の描画（左右を描き分けていない・環レイアウト等）はどのエントリにも一致せず
従来の挙動（既存名または null）に落ちる。「アルドヘキソース（立体未指定）」等の属名
フォールバックは M2.5 以降の任意課題とする。

### 10.5 検収（テスト）

- **ST3**: 10.2 の3性質（左右入替・90°・180°）＋ 軸外れ/スロット重複のスキップ ＋
  D-グリセルアルデヒドの左右で別コード
- **F8 拡張**: 立体付き全エントリの自己命名と、glucose/galactose/mannose の
  「正準コード同一・立体コード相異・名称一意」
- 既存全テスト無回帰（読み取りは lookup 限定なので判定系は不変のはず）

### 10.6 分担

読み取り規約の設計は本章で確定済みのため、**M2a の実装は Opus 委譲可**
（純関数1本＋lookup 1行拡張＋データ生成＋テスト。3性質テストが規約の実装誤りを機械検出する）。

### 10.7 M2a 実装で判明した設計含意（2026-07-25・実装後追記）

- **軸そろえ描画は常に記述子を生む**: 不斉中心の重原子置換基がたまたま全て軸方向に
  そろった描画は、専用モード外でも特定配置として読まれる。直交格子では自然に起こり得る。
  現状は無害（stereoCode は候補エントリに stereoCode がある時だけ比較する一方向意味論）だが、
  **既存の立体未指定エントリで置換基が全軸そろえだと、意図せず D/L 等に化ける**。
  → M2a では既存 **乳酸（compounds）・アラニン（stages）** の OH/NH₂ を 45° オフ軸へ
  ナッジして総称名を維持した（トポロジー・canonicalCode 不変の座標調整）。
  今後 stages/compounds に不斉分子を足すときは「総称のままにしたいなら置換基を軸からずらす／
  特定異性体にしたいなら軸そろえで描く」を意識する。M2b（環）・将来のくさび入力もこの
  軸読み取りの存在を前提に設計する。
- **F8 は更新不要だった**: 既に `canonicalCode + stereoCode` をキーにしていたため、
  グルコース/ガラクトース/マンノース（構成同一・立体相異）が一意性を満たしたまま通る。
- 酒石酸は D/L ↔ (2R,3R) 等の慣用名割り当てに誤りリスクがあるため M2a では見送り
  （メソ体の畳み込み自体は M0 の ST1 で検証済み。命名エントリは後日、確証を取ってから）。

## 12. M2c 設計: 教科書ライクなハース投影入力（Fable・2026-07-25）

**動機（ユーザー FB）**: M2b の「平面正六角形＋▲▼面マーク」は①教科書のハース投影と見た目が違う
②環の向きが曖昧（鏡像で別物になる）③手数が多い（環→O置換→OH×5→面マーク×5）。
「難易度が高い／できるだけ教科書ライクに」との要望。

**方針**: **向き固定のハース投影テンプレート＋「上下は縦位置で読む」**。
教科書のハース図をそのまま描けば読み取れる形にする。R3（座標から読む）案の採用。
Node 試作で成立を確認済み（`<scratchpad>/m2b_pyranose.js` 系＝α/β・グルコ/ガラクト区別・
32配置相異・向き固定で曖昧さ解消）。

### 12.1 なぜ「縦位置で読む」か

M2b は面を明示ビット `haworthFace` で持たせた（R1）。M2c はそれに加え、**面マーク未指定なら
環外置換基の縦位置から面を導出**する（R3）。教科書ハースでは「置換基を炭素の上に描くか下に描くか」
が面そのものなので、これが最も直感的・教科書的で、別マークモードも不要になる。
向きは**テンプレートが固定**するので、M2b で問題だった鏡像の曖昧さ（左右反転＝エナンチオマー）が
起きない。

### 12.2 コア（実装済み・Fable / feat/stereo 1c112a5・v173）

`readRingParityFromHaworth` を拡張:
- 環外置換基の面 `face` = (優先) `atom.haworthFace` が明示ならそれ（M2b 互換）。
- 未指定なら、置換基が環炭素から**±25°以内で縦**に描かれていれば縦位置から導出
  （画面 y は下が正。上=手前 +1／下=奥 -1）。縦から外れる置換基はスキップ（記述子なし）。
- 以降は M2b と同じ（環隣接=面内、置換基=z±face、`_parityFromDirs`）。
- **後方互換**: 明示 haworthFace の既存 α/β-グルコピラノース・ST4/ST5 は無回帰。
- テスト ST6（縦位置だけで環5中心読取・α/β・グルコ/ガラクト・32配置相異・横向きスキップ・鏡像別）。

### 12.3 プラミング（Opus 委譲）

- **ハース環テンプレート・モジュール**: モジュール群に「⬡ ハース環（ピラノース）」を追加。
  1クリックで**向き固定の平たいハース六角形**を配置する。標準の正準座標（試作準拠。中心 (400,300) 付近）:
  - O(右奥)=(460, 250)、C1(右)=(460, 300)、C2(右前)=(400, 330)、C3(左前)=(280, 330)…
    ※正確には「平たい六角形（横長）＋前縁（下側）を手前」に。座標は実装時に既存作図スケールへ調整可。
  - 環炭素は配置済み・環内 O も配置済み（O 置換の手間をなくす）。C5 に -CH₂OH の骨格を前置きしてもよい。
- **縦置き補助**: 環炭素から置換基を伸ばすとき、スナップ候補に**真上・真下**を含める
  （sp2 の ±120° 補助（8.9-(4)）と同じ「置くときだけの補助」）。上下に置くだけで面が決まる。
- **見た目**: 平たい六角形＝ハースらしい外形。前縁を太線にするなど教科書的装飾は任意。
- **データ再作図**: 既存 α/β-D-グルコピラノース（M2b・正六角形＋明示 haworthFace）を、
  **テンプレートと同じ向き・レイアウトで縦位置ベースに作り直す**（`haworthFace` を落とし、
  置換基の縦位置で面を表す）。これでテンプレートで描いたグルコースが登録エントリと一致する
  （両者が同一レイアウト＝同一 handedness ゆえ stereoCode 一致）。**リーダーの出力で検証**（手計算しない）。
- 明示 ▲▼ 面マークモード（M2b）は**残す**（非テンプレート環やフラノース等の保険）。テンプレートが主経路。

### 12.4 検収（プラミング）

- テンプレート配置→C1〜C4 に -OH、C5 に -CH₂OH を**上下に置く**→ `lookupCompoundName` が
  α/β-D-グルコピラノースを返す。アノマーを上下入れ替えると α⇄β が入れ替わる。
- 面を指定しない（横向き等）と総称/該当なしに落ちる（一方向意味論）。
- 既存全テスト無回帰（明示マーク経路・鎖糖・cis/trans）。

### 12.5 将来

- フラノース（5員環・フルクトース）、フィッシャー⇄ハースの対応表示、いす形（M3）。

## 11. M2b 設計: 環の立体（α/β）— ハース面マーク（Fable・2026-07-25）

10.3 で分離した「環内炭素の α/β」の設計。**Node 試作で核心を de-risk 済み**
（`<scratchpad>/m2b_probe.js`＝パリティ原始関数、`m2b_pyranose.js`＝実ピラノースで16項目合格）。

### 11.1 なぜ「面マーク」か（R1 を採用）

環中心の α/β は本質的に「環平面のどちら側に置換基があるか」であり、**平らな2D六角形の
座標には存在しない情報**（面外方向 z が図にない）。M2a のフィッシャー軸読み取りは
そのまま環に使えない。検討した2案:

- **R3: ハース図レイアウトで縦位置=上下を読む** — 座標から読めモード不要だが、
  正六角形（既存の環レイアウト）では上下端の炭素の置換基方向が曖昧で、専用の
  「つぶした六角形＋垂直置換基」レイアウトを強制する必要がある。
- **R1: 面ビットを明示データに持たせる**（採用） — 置換基原子に `haworthFace`(+1=上/-1=下)。
  既存の正六角形レイアウトのまま、どの環でも曖昧さなく効く。α/β を「面」として明示する
  教育 UI（くさび/破線 or ▲/▼ バッジ）にも素直。`isAsymmetricMarked`・整形モードと
  同じ「モード＋要素ごとのビット」パターンで、`serializeState` 拡張も既存踏襲。

**判断**: 化学的曖昧さを避ける R1 を採る。座標読み取りへのこだわりより、α/β という
「面の概念」を明示に持つ正しさを優先する。

### 11.2 パリティ原始関数（`_parityFromDirs`）— M0 と整合

4置換基の**3D方向ベクトル**をフラグメントコード辞書順に並べ、
`det[v1-v0, v2-v0, v3-v0]` の符号をパリティ(±1)とする。試作で
`computeAtomParity`（M0のタプル版）と**一貫した符号関係で一致**することを確認済み
（グローバル符号のオフセットは、参照データと利用者読み取りに同一関数を使う限り相殺され無害）。
環中心と鎖中心は**別構成**（環はC-O結合が増える）なので stereoCode は決して比較されず、
環リーダーは自己整合（参照 vs 利用者）だけ保てばよい。

### 11.3 環リーダー（`readRingParityFromHaworth`）

対象: **環に属する** sp3 不斉中心で、環隣接ちょうど2本＋環外の重原子置換基1本＋暗黙H1本のもの。
- 環隣接2本 → 2D座標そのままの面内ベクトル（z=0）
- 環外置換基 → `haworthFace` を z に（`[0,0,+face]`）、暗黙H → 反対面（`[0,0,-face]`）
- `_parityFromDirs` でパリティ。面マーク未指定・非標準構成の中心はスキップ（記述子なし）。

**フィッシャー（M2a）と環（M2b）の住み分け**: `readAtomParityFromFischer` は
**非環**中心のみ、`readRingParityFromHaworth` は**環**中心のみを扱う（相互排他）。
`lookupCompoundName` のユーザー立体コードは
`{ atomParity: {...Fischer(非環), ...Haworth(環)}, bondGeo: readBondGeoFromCoords }`。
キー衝突なし。→ フィッシャーリーダーは環原子を明示的に除外する（findAnyCycle 相当の環判定）。

### 11.4 検収（試作で確認済み → 本実装でも同じ性質テスト ST4 に）

`m2b_pyranose.js` で全16項目合格:
- 環5中心（C1-C5）すべてで面パリティが読める
- **α/β（C1面のみ反転）で別コード・canonicalCode 同一**
- 各エピマー（C2/C3/C4/C5 面反転）で別コード
- **2⁵=32 の面配置がすべて相異なる立体コード**
- 回転・平行移動で不変
- 鏡映でエナンチオマー＝別コード、かつ `mirrorStereo(元)` と一致

### 11.5 データモデル・UI・データ投入（分担）

- **コア（chemistry.js・Fable 実装）**: `_parityFromDirs` ＋ `readRingParityFromHaworth`
  ＋ window 公開。ST4（純関数の性質テスト）。**←ここまで先に実装・コミット**
- **プラミング（game.js・Opus 委譲可）**:
  - `serializeState`/復元に `haworthFace` を含める（Undo/エクスポート。F7/復元テスト系に準拠）
  - **「α/β 面マークモード」**（`asymmetricMode`/`reshapeMode` と同じトグル・排他）:
    環外置換基をクリックで面を上下トグル。レンダは くさび(上)/破線(下) または ▲/▼ バッジ
  - `lookupCompoundName` のユーザー立体コードに `readRingParityFromHaworth` を合流（1行）
- **データ（compounds.json・Opus 委譲可）**: α/β-D-グルコピラノース等を教科書ハース図で作図し、
  `haworthFace` を付けて登録。パリティ値は手計算せず**リーダーの出力を貼付**（M2a と同流儀）。
  面配置の化学的正しさ＝「各OHを教科書ハース図の上下どちらに描いたか」に還元。

### 11.6 リスク・留意

- 環外置換基が複数（分岐）や環が縮環の中心は 11.3 の「標準構成」を外れてスキップ＝記述子なし
  （高校範囲の単糖ピラノース/フラノースはこの標準構成で足りる）。
- CH₂OH（C5 の環外置換基）も面マーク対象。D/L は C5 の面で決まる（教科書どおり作図）。
- 面マーク未指定の環描画はどの α/β エントリにも一致せず総称/既存名に落ちる（M2a と同じ一方向意味論）。

## 9. 引き継ぎ状態

- **実装ゼロ・データ変更ゼロ。糖類は意図的に未追加**（構成のみで入れると誤命名を生むため保留した）
- 直近の関連作業: **P12-3 の命名拡充（v157〜v164）**。`iupacName` が非環式のアルカン/アルケン/アルキン/
  ハロゲン化物/アルコール/エーテルに対応、アルキル基名と書き出し練習（付け根マーカー R）も実装済み。
  **いずれも立体記述子は未対応**
- 既存の合意: **DESIGN_3d_correspondence.md**（立体表示は判定に非影響／R/S 命名はスコープ外／
  M2「全体の標準くさび描画」・M3「疑似3D」は未実装）
- 全97〜99テスト合格の状態でクリーン（v164 時点）

---

## 13. フィッシャー投影の「可能な操作」の検分（2026-08-26・ユーザー発注）

> **フィッシャー投影の操作練習　可能な操作を検討**

⚠ **これは検討であって、実装ではない。** この節を書いた時点で `quiz.js` の
フィッシャー系のコードは1行も変えていない。**採るかどうかはユーザーが決める。**

★ 併せて聞かれたのは「**ハース環の4操作（`DESIGN_sugar.md` §1-2c）と同じ形に整理できるか**」。
**答えは「群としては同じ形・枚数は違う」** —— 理由は §13-3 と §13-4 にある。

### 13-1. すでに出来ていること（コードと設計書から読み取った現状）

**フィッシャー投影の操作は、部品・練習モード・出題の3層とも既にある。**

| 層 | 実体 | 状態 |
|---|---|---|
| **操作の部品**（`quiz.js` の純関数） | `fischerOpRotate180` ／ `fischerOpRotate90(dir)`（90°回転＋回転後の左右入れ替え）／`fischerOpCycle(center, fixedSlot, dir)`（1スロット固定＋残り3つの巡回）／`fischerOpMirror(center, axis)`・`fischerOpSwap(center)`（向かい合う2つの入れ替え＝**分子が変わる**操作） | ✅ v301／v305 |
| **門番** | `applyVerifiedFischerOp` —— 当てたあとの図から立体を読み直し、`canonicalCode` / `canonicalStereoCode` / 中心の数が変わっていないことと、原子が 21px 以内に重なっていないことを確かめてから採る。壊れる操作は `null` を返し、ボタンは**空振り実行して無効化**する | ✅ |
| **練習モード（砂場）** | 学習タブ「🔃 フィッシャー投影の操作練習」（`FischerPractice`・`#fischer-practice-modal`）。2つの図を並べ、右だけを操作して重ねる。3つの全体操作ボタン＋中心の選択＋軸の選択＋3巡回2つ | ✅ v301・ST28 |
| **タイムアタック** | 「⏱ 立体タイムアタック」（`StereoTimeAttack extends FischerPractice`）。**十字の模型**（`CrossModel`）で4スロット×2向きの回転と外枠4辺の鏡を押す。奇置換の `fischerOpSwap` を足して「お題の立体異性体を作る」。入門／本格／上級（中心を切り替えないと解けない出題だけを、最短手順の探索で選ぶ）・自己ベスト保存 | ✅ v305・ST29 |
| **最短手順の探索** | `StereoTimeAttack.shortestSolution`（`drawingKey` で重複を除く幅優先）と、4択の解説が使う `StereoChoiceQuiz.rotationRoute`（記号12通りの幅優先） | ✅ |
| **出題（4択）** | 「🎯 同じ立体はどれ？」の **記号**（`symbolQuestion`。偶置換1つ＋奇置換3つ）と **分子**（`moleculeQuestion`。3巡回で崩した同じ分子＋中心を反転させた別の立体異性体3つ） | ✅ |

⚠ **無いのは「操作そのものを問う出題」**（「この操作で分子は変わる？」）と、
★ **採点のあとに実際に押して確かめる場**（＝ 今日ハースに入れたもの）の2つだけ。

### 13-2. ★ 可能な操作の一覧（**実測**。プールは鎖状で不斉炭素が読める登録 **119件**）

物差しは `StereoQuiz.relationOf`（図から立体を読み直した関係）。**全件で例外なし・「読めない図」も0件。**

#### (a) 図まるごとの剛体操作 —— 8通りしかない（回転4×鏡映の有無2）

| 図への操作 | 4スロットの置換 | 偶奇 | **意味** | 実測（119件） |
|---|---|---|---|---|
| **元** | 恒等 | 偶 | **同じ分子** ✅ | same **119/119** |
| ★ **180°回す** | (上下)(左右) | 偶 | **同じ分子** ✅ | same **119/119** |
| ★ **対角線の鏡**（軸＝左上–右下の対角線。上と左が入れ替わる）＝ アプリの「⟳ 90°回す＋左右入れ替え」 | (上左)(右下) | 偶 | **同じ分子** ✅ | same **119/119** |
| ★ **反対角線の鏡**（軸＝右上–左下の対角線。上と右が入れ替わる）＝ アプリの「⟲ 90°回す＋左右入れ替え」 | (上右)(下左) | 偶 | **同じ分子** ✅ | same **119/119** |
| ✗ **90°回す（時計）** | (上右下左) | 奇 | **鏡像** | enantiomer **119/119** |
| ✗ **90°回す（反時計）** | (上左下右) | 奇 | **鏡像** | enantiomer **119/119** |
| ✗ **縦軸の鏡**（左右を入れ替える） | (左右) | 奇 | **鏡像** | enantiomer **119/119** |
| ✗ **横軸の鏡**（上下を入れ替える） | (上下) | 奇 | **鏡像** | enantiomer **119/119** |

- ★ **「対角線の鏡」は新しい操作ではない。** `fischerOpRotate90(cw)` の中身は
  `rotateTargetInPlane(t, 1, mirrorX=true)` そのもの ＝ **作りからして同じ図**で、
  実測（`drawingKey` で 119/119 一致）が言えるのは「**門番が1件も跳ねない**」という方だけ。
  呼び名が違うだけで同じ操作である、というのがここの要点。
  ⚠ アプリの画面の言い方（「90°回す＋左右入れ替え」）は**教科書から導ける言い方**なので変えない。
  「対角線の鏡」はこの文書の中だけの呼び名。
- ⚠ **多中心の分子でもジアステレオマーは出ない**（`other` 0/119×8）。剛体操作は**全部の中心に同じ置換**を
  当てるので、全部そろって反転するか、全部そのままか、の二択にしかならない。
- ⚠ **丸めの注意**: 「2回押すと図までぴったり戻る」と「対角線の鏡→反対角線の鏡 ＝ 180°回転」は
  **114/119**。落ちる5件（フェニルアラニン2件・チロシン2件・マンデル酸）は**ベンゼン環を持ち重心が
  非整数**の登録で、`rotateTargetInPlane` が重心まわりに回して `Math.round` するため **±1px** ずれる。
  **化学は無傷**（この5件の往復も立体の関係は 15/15 とも `same`）。
  ★ ハースの ⇄／⟳ で同じ問題が起きないのは、軸を**環原子の重心**に取っていて
  その軸が操作で不変だから（`DESIGN_sugar.md` §4-10b KV5）。**ここを直すなら同じ手**（軸を格子に載せる）。

#### (b) 1つの中心の中だけで動かす操作（＝ 十字のスロットを入れ替える）

| 操作 | 置換 | 偶奇 | 意味 | 実測 |
|---|---|---|---|---|
| ★ **1つ固定して残り3つを巡回**（`fischerOpCycle`） | 3巡回 | 偶 | **同じ分子** ✅ | 320回試して**230回成立**・成立した **230/230 で分子は不変** |
| ✗ **向かい合う2つを入れ替える**（`fischerOpMirror`／`fischerOpSwap`） | 転置1回 | 奇 | **その中心だけ反転** | 80回中 **77回成立**。1中心なら**鏡像 49件**、多中心で1つだけ反転すると**ジアステレオマー 28件**。**77/77 が2回で元に戻る**（鏡は自分自身が逆操作） |

⚠ **3巡回が断られる 90回**（320 − 230）は化学の話ではなく**作図の話** ——
枝の中に別の不斉炭素や環があると、枝を中心まわりに回した図が壊れるので `applyVerifiedFischerOp` が跳ねる。
これは正しい断り方で、タイムアタックの案内も「先に回してみてください」と言う（ST29 の但し書き）。

#### (c) ⚠ 紙から持ち上げて裏返す（面外の反転）

★ **この操作は「4スロットの置換」では表せない** —— 縦（奥）と横（手前）の**役割そのもの**が入れ替わるため。
裏返した図を今までどおりの約束で読むと**鏡像**になる。
⚠ **アプリはこの操作を持っていない**（剛体の8通りは全部が紙の面内の操作）。
⚠ **ハース図の ⇅ とはここが正反対**: ハースでは面外の裏返しこそが唯一の非自明な保存操作
（`DESIGN_sugar.md` §1-2 の②）だが、**フィッシャーでは読みを壊す側**。
理由は `DESIGN_sugar.md` §4-6 の訂正1がすでに書いている ——
**`canFlipHaworth` が「フィッシャーとして読める中心があると断る」のはこのため**（実測で 1,059件が過不足なく一致）。

### 13-2b. ★ 一次資料での裏取り（`マイドライブ/化学/参考書` の5冊・2026-08-26）

⚠ **5冊とも本文にテキスト層の無いスキャン PDF**なので、ページ画像に起こして読んだ。**PDF は1つも書き換えていない。**
⚠ **アプリの実測（§13-2）と食い違う記述は1つも見つからなかった**が、
★ **資料の側は5つのうち3つしか書いていない**（下の表の × はすべて「その本に記述が無い」の意味で、
「否定している」ではない）。

| 操作 | ハート有機化学 三訂版 | 化学新研究2015 | 単位がとれる有機化学ノート | 総合的研究 化学 | 駿台 有機化学 |
|---|---|---|---|---|---|
| **縦＝奥／横＝手前 の約束** | ○ 本文 p.178・p.179・p.498 | ○ 本文 p.558 | ○ 本文 p.80（「〜と約束する」の形） | △ 図のラベルだけ（p.541） | ✗ 記述なし |
| **(A) 面内 180°回転 ＝ 同じ** | ★ **○ p.182 脚注（いちばん明確）** | ○ p.556（メソ酒石酸の個別説明として） | ○ p.81 | ✗ | ✗ |
| **(B) 面内 90°回転 ＝ 鏡像** | ✗ | ✗ | ★ **○ p.81（5冊で唯一）** | ✗ | ✗ |
| **(C) 紙から裏返す** | ✗ | △ p.665（前方投影と後方投影で D/L が反転する、という等価な形） | ✗ | ✗ | ✗ |
| **(D) 2つ入れ替え ＝ 鏡像（1回）** | ○ p.178 | ○ p.666（多中心では1つだけならジアステレオマー、とも） | ✗ | ✗ | ✗ |
| **(E) 1つ固定して3つ巡回** | ✗ | ✗ | ✗ | ✗ | ✗ |

★ **設計に効く読み**:

1. ★ **(B) の根拠は1冊しかない**（単位がとれる p.81）。しかもその本は
   **くさび図との対比**で書いている ——「くさび図なら 90°回しても同じ物質だが、
   フィッシャー投影式では違う」。★ **出題の言葉づかいはここに寄せるのがよい**
   （提案2の「この操作で分子は変わる？」は、まさにこの対比を問う形になる）。
2. ⚠⚠ **(E)（3巡回）を書いた資料は5冊とも無い。** アプリの練習モードの主力操作が、
   **教科書の言葉では裏づけられていない**ということ。⚠ 化学として正しくない、という話ではない
   （偶置換なので保存する。実測 230/230）。★ **「教科書に載っている操作」と
   「このアプリが足した操作」を画面で分けて書く**のが `qa/KNOWLEDGE_CAVEATS.md` の型に沿う。
   ⚠ **総合的研究 p.404 と駿台 p.43 に形の似た記述があるが、あれは
   「R/S を読むための視線の取り方」であって、投影図に加えてよい操作の規則ではない**（取り違え注意）。
3. ⚠ **(D) の「2回で元に戻る」も5冊とも書いていない**（アプリは実測 77/77 で戻る）。
   同じく「アプリが足したもの」の側。
4. ⚠ **駿台 有機化学にはフィッシャー投影式そのものが1度も出てこない**（270ページ・索引にも無い）。
   ★ **入試対策の本が1冊まるごと投影式を使わずに書けている** ＝
   DEVELOPMENT.md の「必ずしもマスターする必要はないが発展的に扱ってよい」という位置づけの裏づけになる。
5. ⚠ **引用の落とし穴**: 化学新研究の **p.554 の乳酸の図だけは逆の約束**
   （-CH₃ が手前・-OH が奥）。同じ本の中に2種類の約束の十字図が同居している。
6. ⚠ **ハートの (A) の根拠 p.182 は巻末索引に載っていない**（索引は 178/179/498/533 の4か所だけ）。
   索引だけを頼りにすると (A) を見落とす。

### 13-3. ★★ ハースの4枚と同じ形か —— 群としては同じ・枚数は違う

| | ハース環（`DESIGN_sugar.md` §1-2c） | **フィッシャー投影** |
|---|---|---|
| **意味を保つ操作の群** | クラインの四元群（元・⇅・⇄・⟳） | ★ **クラインの四元群**（元・180°・対角線の鏡・反対角線の鏡）＝ **同じ形** |
| **群の正体** | 図の描き直し（付け替え）まで許した4枚 | 十字の対称群 D₄ を4スロットの置換として見たときの**偶な部分**（D₄ ∩ A₄） |
| **4枚が互いに別の絵か** | 16/16 | ★ **119/119** |
| ⚠ **4枚とも教科書が印刷する図か** | ★ **はい**（②はセロビオース・⇄はスクロース中のフルクトースで実証） | ⚠⚠ **いいえ。2枚だけ** |
| **意味を壊す操作** | 素朴な x 鏡映・素朴な面内180°（付け替えをしない） | 90°回転（両向き）・縦軸の鏡・横軸の鏡 |

★ **枚数が違う理由（ここがこの検分のいちばんの収穫）**:

**対角線の鏡・反対角線の鏡は、主鎖を横向きにしてしまう。** 実測（D-アラニン・D-グルコース（鎖状））:

| 図 | 主鎖の向き | `isFischerOriented` | D/L が読めるか |
|---|---|---|---|
| 元 | **縦** | true | **D** |
| **180°回す** | **縦** | true | **D** |
| ⚠ 対角線の鏡（＝90°＋左右入れ替え） | **横** | false | **読めない** |
| ⚠ 反対角線の鏡 | **横** | false | **読めない** |
| ✗ 90°単独 | 横 | false | 読めない |

★ **プール全体でも同じ**: D/L が読める登録は 12件あるが、**180° のあとは 12件とも読めるまま**、
**対角線の鏡のあとは 0件**になる。
＝ 対角線の鏡は「同じ分子だが、標準の向きのフィッシャー投影ではなくなった図」を作る。

★ **だから「同じ分子・主鎖は縦のまま」の図は、フィッシャーでは 元 と 180° の2枚しかない。**
1中心の分子（D-アラニン・D-乳酸・2-ブタノール）で
{180°, 3巡回} だけを使って到達できる図を全部数えると **12枚（＝ A₄ そのもの）**、
そのうち**主鎖が縦のままなのは 2枚**（実測。多中心の D-グルコース（鎖状）でも到達 18枚・縦は 2枚）。

⚠⚠ **ハースの「付け替え」に当たる手はフィッシャーには無い。**
ハースで枚数が2枚から4枚に増えたのは「置換基を付け根の環炭素について上下に付け替える」描き直しを
許したからだが（§1-2c）、**フィッシャーで置換基を付け替える ＝ スロットを入れ替える ＝ 奇置換 ＝ 必ず鏡像**。
**ここは対称にならない。**

### 13-4. ★ 出題への提案（⚠ 実装はしない。ユーザーの判断待ち）

#### 提案1（推奨・いちばん軽い）: 4択の**採点後に、実際に押して重ねられるようにする**

★ **今日ハースに入れたもの（`DESIGN_sugar.md` §1-2c の「動かしてみる」）の、フィッシャー版。**

- **対象**: 「🎯 同じ立体はどれ？」の **記号**モードと **分子**モード。
- **押せる札**: 既存の3つ（`fischerOpRotate180` / `fischerOpRotate90` ×2）＋中心を選んでの3巡回。
  ⚠ **新しい操作は1つも要らない。**
- **なぜ効くか（実測）**: 分子モードの正解は `scrambleByCycles`（3巡回だけ）で作られているので、
  **見本から既存のボタンだけで必ず作れる**。25問を実際に作って幅優先で測ったところ
  **25/25 が 0〜2手**（0手 4問・1手 19問・2手 2問）で正解の図に届いた。
- ★ **記号モードは解説がすでに手順を文字で出している**（`rotationRoute` が
  「⟳ 上を固定 → …の2手」と書く）。**その手順を押せるようにするだけ**で、
  「解説のとおりになるか自分で確かめる」がそのまま成立する。
- ⚠ **採点前は出さない**（ハースと同じ理由 —— 押せると答えが分かる）。
- **一致判定**: `FischerPractice.drawingKey`。⚠ **立体コードは使えない**
  （同じ分子の図は12枚あって全部コードが同じ ＝ どの図に着いたかを区別できない）。ハース版と同じ理由。

#### 提案2: 「この操作で分子は変わる？」の2択（＝ 教科書がいちばん問う形）

- 図を1つ見せ、操作を1つ名指しして「**同じ分子？ 鏡像？**」を答えさせる。
  正誤は**表に持たず、操作を当てた図から読み直して決める**（ハース版と同じ流儀）。
- ★ **出題に使ってよい操作は5つに絞る**: **元 ／ 180°回す ／ 90°回す ／ 縦軸の鏡（左右を入れ替える）
  ／ 横軸の鏡（上下を入れ替える）**。
  ⚠⚠ **対角線の鏡は出題に使わない** —— 正解が「同じ分子」なのに**主鎖が横になった図**が出るので、
  「教科書の図として正しいか」と「同じ分子か」が画面の上で混ざる。§13-3 の表がその理由。
  ★ ただし**練習モード（砂場）には今までどおり置いてよい**。あちらは「重ねる」のが目的で、
  途中の図が標準の向きでなくてよいから。
- ★ **言葉づかいは「くさび図との対比」に寄せる** —— (B) を書いている唯一の資料
  （単位がとれる有機化学ノート p.81）がその形で書いているから（§13-2b の読み1）。
  「くさび図なら 90°回しても同じ物質。フィッシャー投影式では鏡像になる」。
- ★ **中心ごとの操作も同じ形で出せる**: 「1つ固定して残り3つを回す」＝ 同じ分子／
  「向かい合う2つを入れ替える」＝ その中心だけ反転（1中心なら鏡像・多中心ならジアステレオマー）。
  ⚠ **多中心での「入れ替え1回は鏡像ではない」は、実測 28件が示すとおり**間違えやすい所なので、
  出題の価値がいちばん高いのはここ（(D) は資料2冊が裏づけている）。
  ⚠⚠ **ただし「3つを巡回」は5冊とも書いていない操作**（§13-2b の読み2）。
  出すなら**教科書の操作と並べず、「このアプリが足した見方」と分かる形にする**こと。

#### 提案3: ⚠ **やらないことを勧めるもの**

| やらない | 理由 |
|---|---|
| ハースの ⇄・⟳ に当たる「付け替え」の札をフィッシャーにも作る | **作れない。** スロットの入れ替えは必ず奇置換 ＝ 鏡像（§13-3） |
| 「紙から持ち上げて裏返す」札を作る | 読みの約束（縦＝奥）そのものを壊す。ハースの ⇅ とは正反対（§13-2 (c)） |
| 出題の対象を「主鎖が縦の登録」に絞る | ⚠ **プール119件のうち `isFischerOriented` が true なのは 14件だけ**（酒石酸の登録も主鎖が横）。 絞ると題材が枯れる。★ **操作の練習には絞る必要が無い**（`readAtomParityFromFischer` は軸に沿った十字ならどの向きでも読む）。**絞るのは D/L を問う出題だけ**（そちらは `assignDLDescriptor` が自分で断る） |
| 対角線の鏡の丸め（±1px・5件）を今すぐ直す | 化学は無傷で、**練習モードの往復にしか効かない**。直すなら軸を格子に載せる（ハースの KV5 と同じ手）——**別の1コミット**にすべき |

