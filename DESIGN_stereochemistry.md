# 立体化学対応 設計メモ（Fable への引き継ぎ用たたき台）

**ステータス: 未着手（実装ゼロ・データ変更ゼロ）。設計判断は Fable が行う。**
CLAUDE.md のモデル運用に従い、「化学モデルの正しさが問われる変更（正準コード・同型判定）」および
「決めが後を引く設計判断」に該当するため、Opus は現状整理と論点の洗い出しまでを担当した。

発端（2026-07-25 ユーザーとの対話）:
1. 化合物拡充の続きとして糖類を検討 → ユーザー指摘「糖類については α/β をどうするかという問題がある」
2. ユーザー案「糖類専用の環ユニットを作る」vs「より広範な設計を考えるべきか」の判断が必要
3. あわせて確認された疑問「以前導入した sp2 の結合角は現状無視しているのか」→ 2章で回答

---

## 1. 何が問題か（糖類は氷山の一角）

**現行の同一判定は「構成（つながり方）だけ」**を見る。したがって:

- **α-グルコースと β-グルコース**は、環状形のアノマー炭素(C1)の**立体だけ**が違い、構成は同一 → **区別不能**
- さらに深刻なのは、**すべてのアルドヘキソース（グルコース・ガラクトース・マンノース…）は構成が同一**
  （開環形はいずれも CHO-CHOH-CHOH-CHOH-CHOH-CH₂OH）。違いは C2〜C5 の立体配置だけ
- つまり「グルコース」を構成エントリとして compounds.json に入れると、**ガラクトースやマンノースを描いても
  同じ正準コードに一致し「グルコース」と誤命名される**。テスト F8（命名の一意性）にも抵触する
- **α/β をどう表すか以前の、より根本的な壁**である

**糖類だけの問題ではない**:

| 化合物 | 立体の種類 | 現行モデルでの状態 |
|---|---|---|
| α/β-グルコース | アノマー（C1配置） | 区別不能 |
| 各アルドヘキソース | C2〜C5 の配置 | 相互に区別不能 |
| D/L-アラニン（**既に stages.json にあり**） | 鏡像 | 構成のみで登録済み |
| 乳酸（**既に compounds.json にあり**） | 鏡像 | 構成のみで登録済み |
| 酒石酸（未登録） | (R,R)/(S,S)/**メソ体** | 3種すべて区別不能 |

現状は「立体を区別しない」ことで**一貫はしている**（既存の乳酸・アラニンも立体記述子なしの名前）。
糖類を入れようとすると、この一貫性が初めて破れる。

---

## 2. 現状の正確な棚卸し（「sp2 の結合角は無視されているか？」への回答）

**結論: 角度を捨てているのではなく、「同一判定が角度を見ない」だけ。sp2 の 120° は環ジオメトリとして現役。**

| 対象 | 座標・角度を見るか | 実装位置 |
|---|---|---|
| 同一判定 `canonicalCode` / `verifyMolecule` | **一切見ない**（重原子グラフ＋結合次数＋自動H数のみ） | chemistry.js:824 / `canonicalCode` |
| **例外1: cis/trans** | **見る**。C=C 軸に対する置換基の側を外積で判定（2置換の非環 C=C のみ／約6°未満は不定→null） | `getDoubleBondGeometry`。`compounds.json` の `geometry` と `lookupCompoundName` で照合 |
| **例外2: 不斉炭素マーク** | 座標は見ない。**どの炭素が不斉か**をユーザーが `*` で指定し、判定オプション時に照合 | `isAsymmetricCarbon`（4置換基の `rootedFragmentCode` が相異＝トポロジー判定）／game.js の `judgeAsymmetric` 後段 |
| 環の作図角度（**sp2 の 120° 系**） | **見る**。ベンゼン＝正六角形、環炭素の側鎖2本目は二等分線 **±30°**（≒120°） | game.js の `candidateAngles`（テスト D4/D5 が保護） |
| 鎖式の sp2（開鎖の C=C・C=O） | 直交（90°）のまま。一般の sp2 に 120° を強制する仕組みは無い | — |
| sp3 の実角 109.5°・くさび | **別枠表示のみ**（判定に非影響） | stereo.js。DESIGN_3d_correspondence.md が「判定に一切影響を与えない」「R/S はスコープ外」と明記 |
| 異性体列挙・練習 | 構成異性体のみ（重原子8個まで）。UI に「シス/トランス・鏡像は数えません」と明示 | `enumerateConstitutionalIsomers` / learn.js |
| IUPAC 系統名（v158〜v161 で実装） | 立体記述子を出さない（E/Z・R/S 非対応） | `iupacName`（非環式のアルカン/アルケン/アルキン/ハロゲン化物/アルコール/エーテル） |

**設計上、重要な2つの前例**:
- **cis/trans**: 「描き方から立体を1種類だけ推定し、名前に反映する」実装が既にある
- **`judgeAsymmetric`**: 「既定はトポロジー判定、立体的な厳格化はユーザーのオプトイン」という
  レイヤ構造の前例がある（影響を局所化できた実績）

---

## 3. 決めるべき論点（Fable の判断事項）

### 論点1: 同一判定に立体をどこまで入れるか

- **案A: 入れない（現行維持）** — 糖類は非対応と明示。既存の一貫性を守る
- **案B: 相対配置を正準コードに組み込む** — 各立体中心に up/down 等の記述子を持たせ、コードに含める。
  絶対配置（R/S・D/L）は名乗らず「相異なる立体異性体を区別する」ところまで
- **案C: CIP 風 R/S を計算して組み込む** — 絶対配置まで決定し、D/L・α/β を計算で出す。最も強力だが最も重い
- **案D: 糖専用の環ユニット**（ユーザー案） — α/β を選んで配置する特製モジュール。糖だけ特別扱い

### 論点2（最難関）: 2D直交作図で立体配置をどう「入力」するか

現状の絵（直交格子・平面）は sp3 の 3D 配置を表現できない。**cis/trans は座標から読めたが、
sp3 の配置は平面配置に自由度がないため読めない** — ここが本質的な難所。

- (a) **原子ごとの配置指定**（up/down トグル等をデータに持つ）— `isAsymmetricMarked` の拡張。直交作図と喧嘩しない
- (b) **くさび結合を第一級の入力に**（結合に wedge/hash 属性）— 教科書的だが描画の自由度を要求し、直交作図と衝突しやすい
- (c) **フィッシャー投影の専用入力モード** — 糖・アミノ酸に強い。学習効果も高いが新モードの新設
- (d) **糖専用の環ユニット**（論点1の案D と対応）

評価軸: 手描き感覚の維持 / 直交作図との整合 / 学習者が「見て分かる」か / 実装量 / 他分野への波及

### 論点3: 命名（D/L・α/β・R/S・E/Z の統合方針）

- 既存の `geometry`（cis/trans）と新機構をどう統合するか（二重管理を避ける）
- 糖の名前は**慣用名で持つ**（α-グルコース等をデータに登録）か、**計算で出す**か
- `iupacName` に立体記述子を出させるか（出すなら E/Z も同時に整合させる）

### 論点4: 既存機能・テストへの波及（数値・文言が動く）

立体を識別に入れると、以下の前提が広範に動く。**オプトイン設計なら影響を局所化できる**（`judgeAsymmetric` の前例）。

- 異性体列挙数（**IP4 が期待値 [2,3,3,5,5,7] を固定**）と「鏡像は数えません」の UI 文言
- クイズのペア生成（同一分子式で非同型のペアを「違う」問題にしている）
- **F7**（同値⇔コード一致の性質）、**F8**（命名の一意性）、**A3**（全エントリの自己整合）
- **B5 / G4**（不斉マーク）、**H1**（くさび図）
- 既存 143 エントリ（stages 56 ＋ compounds 87）の再解釈

### 論点5: 段階導入の切り方

M1/M2… をどう切り、**どこで止めても製品として一貫している**状態を保つか。

---

## 4. 案の比較（暫定評価。決定は Fable）

| | 糖類 α/β | 各アルドヘキソース | アミノ酸・乳酸・酒石酸 | 実装量 | 既存哲学との整合 | 主なリスク |
|---|---|---|---|---|---|---|
| A 現行維持 | × | × | ×（現状維持） | なし | ◎ | 糖類を永久に扱えない |
| B 相対配置をコードに | ○ | ○ | ○ | 大 | △（判定に立体が入る） | 入力手段の設計次第。絶対配置は名乗れない |
| C CIP風 R/S | ◎ | ◎ | ◎ | 特大 | △ | CIP 規則の正しい実装は重く、誤りが致命的 |
| D 糖専用ユニット | ○ | △（糖内のみ） | × | 中 | ×（孤島化） | 汎用の列挙/命名/判定と噛み合わない。後で行き詰まる |

### Opus の暫定推奨（あくまで参考。決定は Fable）

1. **案D（糖専用ユニット）は非推奨** — 早く届くが孤島化し、アミノ酸・酒石酸に効かず、
   「判定はトポロジーのみ」という原則に穴を空ける形で後戻りしにくい
2. **「オプトインの立体レイヤ」**（既定はトポロジー判定のまま、立体は選択的に厳格化）が
   既存哲学と最も整合する。`judgeAsymmetric` が前例
3. **入力手段は (a) 原子ごとの配置指定**が直交作図と最も喧嘩しにくい。
   ただし糖の教育文脈では (c) フィッシャー投影の訴求力が高く、要判断
4. α/β・D/L を**正しく名乗る**には案C 相当（または参照構造との比較）が必要。
   「区別はするが名乗らない」（案B）で止める判断もあり得る

---

## 5. 影響範囲（ファイル・関数）

- **chemistry.js**: `canonicalCode` / `canonicalRowsCore` / `rootedFragmentCode` / `verifyMolecule` /
  `isAsymmetricCarbon` / `getDoubleBondGeometry` / `enumerateConstitutionalIsomers` / `iupacName` 系
- **game.js**: 判定（`judgeAsymmetric` 後段）/ `lookupCompoundName` / `getCompoundLibrary`（`geometry` 照合）/
  `serializeState`（配置データの保存＝Undo/エクスポート）/ `renderAtom`・`renderBond`（くさび描画を採る場合）
- **learn.js**: `IsomerPractice`（列挙数・文言）/ `AlkylPractice` / **quiz.js**（ペア生成・命名クイズ）
- **stereo.js**: 既存くさび図との統合（DESIGN_3d_correspondence.md の M2「全体の標準くさび描画」と接続できる）
- **データ**: compounds.json（`geometry` の一般化）/ stages.json
- **テスト**: A3, B5, F3, F7, F8, F9, F10, G4, H1, IP1〜IP9, AK1

---

## 6. 検収の勘所（テスト設計の種）

- **性質テストの再定義**: F7 の「同値 ⇔ コード一致」を立体込みでどう定義するか
- **既知の立体異性体対で必ず区別され、同一物は必ず一致する**:
  α/β-グルコース、D/L-アラニン、乳酸の鏡像、**酒石酸の (R,R)/(S,S)/メソ体**
- **メソ酒石酸**（不斉中心2個だが分子として非キラル）を正しく扱えるかは**案の分かれ目になる良い試験紙**
- 既存 143 エントリの命名一意性（F8）が壊れないこと
- **立体を入れない既定モードでの回帰ゼロ**（全テスト合格を維持できる設計か）

---

## 7. 引き継ぎ状態

- **実装ゼロ・データ変更ゼロ。糖類は意図的に未追加**（構成のみで入れると誤命名を生むため保留した）
- 直近の関連作業: **P12-3 の命名拡充（v157〜v164）**。`iupacName` が非環式のアルカン/アルケン/アルキン/
  ハロゲン化物/アルコール/エーテルに対応、アルキル基名と書き出し練習（付け根マーカー R）も実装済み。
  **いずれも立体記述子は未対応**
- 既存の合意: **DESIGN_3d_correspondence.md**（立体表示は判定に非影響／R/S 命名はスコープ外／
  M2「全体の標準くさび描画」・M3「疑似3D」は未実装）
- 全97〜99テスト合格の状態でクリーン（v164 時点）
