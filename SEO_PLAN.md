# 検索チャネル（2026-08-12 開始）

SNS とは別の入口。**生徒が実際にいる場所**であり、`SNS_PLAN.md` の
「視聴者層が想定と違う」で出た結論（届く道は検索と学校経由の2つ）の片方にあたる。

## 1. なぜ手を付けたか

2026-08-12 時点で **google organic は 7 セッション**。ほぼゼロだった。
塞いでいたのは robots.txt ではなく、**こちら側に到達先の情報が無かったこと**:

- `sitemap.xml` が 404
- `assembler/index.html` に description も OGP も canonical も無い（主力アプリ）
- そもそも Search Console に登録していない ＝ **何回表示されたかすら見えない**

## 2. やったこと

| | |
|---|---|
| `sitemap.xml` | `node tools/gen-sitemap.js` で生成（25 URL） |
| `robots.txt` | `Sitemap:` 行を追加 |
| assembler / qa | description・canonical・OGP を追加 |
| 異性体ページ | `/isomers/` 配下に13式（`node tools/gen-isomer-pages.mjs`） |
| Search Console | 登録・所有権確認ずみ（GA4 タグ経由で自動確認された） |

## 3. 第1弾に異性体ページを選んだ理由

1. **他社が原理的に真似できない。** 異性体の列挙にはエンジンが要る。
   「C₄H₁₀O 異性体」で検索した生徒に7種すべての構造式を出せるサイトは他にない
2. **検索意図が完全に一致する。** 定期テストと入試の定番設問そのもので、生徒が実際に打つ語
3. **検索結果の中でアプリの価値が見える。** 構造式が並んでいれば「これ触れるのか」と分かる

**923件の化合物から機械的にページを量産しない。** 中身の薄いページを一気に大量公開するのは
Google が「誘導ページ」として評価を下げる典型。**少数で出して測ってから広げる。**

## 4. 生成の決まりごと（道具に埋め込んである）

- **アプリを実際に走らせて作る。** 異性体の顔ぶれも構造式も chemistry.js と quiz.js が正。
  ページに書き写すと**エンジンを直したときにページだけ古くなる**
- **分類外が1つでも出る式は載せない。** 列挙が正しくても教科書の答えと食い違う式がある
  （C4H8O2 は分類内だけで44種になり、生徒が探す「エステル4・カルボン酸2」とは別物）。
  **数として正しくても、探している答えと違うページは害になる**
- **出力は決定的にする。** 原子IDが乱数なので、そのままだと結合線の向きと、
  二重結合の2本の並び順が実行ごとに変わる。座標でそろえて吸収した（絵は変わらない）
- **計測スニペットは `qa/index.html` から実物を切り出す。** 書き写すと片方だけ取り残される
- 出力は自己完結（CSS インライン）＝ `?v=` 規約の対象外。版の上げ忘れが起きない

## 5. 現在地と次

- [x] 土台（sitemap・OGP・Search Console）
- [x] 異性体ページ 13式
- [ ] **Search Console にサイトマップを送信**（ユーザー操作）
- [ ] 2〜3週間後、検索パフォーマンスで**どの語で表示されたか**を見る。
      表示があって順位が低いなら中身を厚くする。**表示そのものが無いなら需要が無い**ので、
      別の型（qa の知識項目・化合物ページ）へ移る
- [ ] `info.schoollenz.com` / `koku.schoollenz.com` も Search Console に登録（別リポジトリ）

**⚠ 検索ボリュームは未検証。** 「C₄H₁₀O 異性体」の実需要が月10件か1,000件かは
Search Console のデータが溜まるまで分からない。だから少数で出して測る順番にしてある。

## 6. 第2弾: 単元グループページ ＋ 動画（2026-09-07 着手）

**きっかけ（ユーザー）**:

> 「ロングは全く伸びていません」
> 「参考書の内容が網羅されることで検索からの流入が増えるかもしれません」
> 「Assembler側にも該当の動画のリンクを用意しておく（テキスト解説＋動画）とよさそうです」
> 「そろそろSEO対策は真剣に考えたいです。場合によっては課金します」

### 6-1. いま手元にある材料（実測・2026-09-07）

| 材料 | 数 |
|---|---|
| `qa/questions.json` の知識項目（コード・難易度・根拠つき） | **11単元 / 52グループ / 316項目** |
| 完成済みの動画（`video-scripts/meta/V*.json`） | **110本** |
| 公開済みの検索ページ | 異性体 13式（`/isomers/`） |

**動画の内訳**: 反応25・化合物作ってみた24・クイズ23・異性体13・高分子8・官能基7・立体6・構造決定2・その他2。

### 6-2. ⭐ ページの単位は「グループ（52枚）」にする。項目（316枚）にしない

⛔ **1行知識のページを316枚出すのは §3 に自分で書いた「誘導ページ」そのもの。**
⭐ **52枚なら1枚あたり 知識6件＋動画2本**が乗る（110 ÷ 52 ≒ 2）。**厚みが出る。**

| 単元 | グループ数 |
|---|---:|
| 脂肪族炭化水素 | 6 |
| アルコール・エーテル | 3 |
| 元素の確認・元素分析 | 3 |
| アルデヒド・ケトン／カルボン酸・エステル | 6 |
| 芳香族炭化水素 | 4 |
| フェノール類 | 4 |
| 芳香族窒素化合物・芳香族の分離 | 4 |
| 油脂・セッケン | 5 |
| 糖・アミノ酸・タンパク質 | 8 |
| 合成高分子 | 5 |
| 手がかりから物質に当たりを付ける | 4 |

### 6-3. 1枚の中身（決め）

1. **見出し ＝ グループ名**（⚠ 生徒が実際に打つ語に寄せる。台帳の内部名のままにしない）
2. **知識項目6件前後の解説** —— `qa` の `knowledge` を出発点に、**文章として書き直す**
   （⚠ 貼り付けない。一問一答と同じ文字列が並ぶと、どちらのページも薄くなる）
3. **該当する動画を埋める**（YouTube 埋め込み。⭐ **ここが他サイトと差が付くところ**）
4. **アプリへの入口** —— その単元で実際に触れる操作へ直リンク
5. **一問一答へのリンク** —— `qa` の該当グループ（→ TAXONOMY §2.6 飛び道具リンク）

### 6-4. 測り方（先に決めておく）

- ⭐ **最初は5枚だけ出す**（§3 の「少数で出して測ってから広げる」を守る）
- **出す5枚の選び方**: **動画が2本以上あり、かつ検索語が素直なグループ**
- **4週間後に Search Console で「表示回数のあるクエリ」を見る。**
  **表示がゼロなら需要が無い** ＝ 52枚に広げない（§5 と同じ判定）

### 6-5. ⛔ 課金の前に確かめること（2026-09-07 時点で未確認）

- [ ] **Search Console にサイトマップを送信したか** —— §5 のチェックが**未了のまま**
- [ ] **8/12 以降の検索パフォーマンスに表示回数が出ているか**（クエリが1つでも出ているか）

**この2つが空のまま課金しても、測る対象が無い。**

### 6-6. 課金の当たり外れ（見立て・2026-09-07）

| | 判断 |
|---|---|
| **キーワードの実需要データ** | ⭐ **ここだけは価値がある。** §5 に「検索ボリュームは未検証」と書いたとおり、**最大の不確実性がここ** |
| Google キーワードプランナー | **まず無料で見る**（Ads アカウントを作るだけ。出稿しないと幅表示だが桁は分かる） |
| **SEO 代行・記事外注** | ⛔ **やめる。** 強みは「エンジンが実際に動く」ことで、**外注ライターが書けるのは他社と同じ内容** |
| **被リンク購入** | ⛔ 論外（ペナルティ対象） |

### 6-7. アプリ側の入口（Assembler レーンへ）

**発注は [video-scripts/ORDER_video_links_2026-09-07.md](video-scripts/ORDER_video_links_2026-09-07.md)。**
⚠ **ページをどこに置くか（`/assembler/` の中か、`/guide/` のような別ディレクトリか）は決めていない。**
**アプリの構成に関わるので、統合レーンとユーザーの判断。**
