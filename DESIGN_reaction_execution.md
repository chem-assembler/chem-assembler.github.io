# 反応実行モード 設計書（P9-1）

ユーザーが作図（または名称から呼び出し）した分子に対して「この分子の反応」を提示し、
反応を選ぶと生成物へ変化するモード。既存の反応機構ビューア（reaction.js＝登録済みデータの
観賞用）と異なり、**ユーザー自身の分子を対象に反応を実行する**のが本質。
実装前の合意用ドキュメントであり、実装の進行に合わせて更新する。

---

## 1. 体験イメージ

1. エタノールを作図（または名称呼び出しで配置）
2. 右パネルの「⚗ この分子の反応」カードに、分子のプロパティ（例: **1級アルコール**）と
   実行可能な反応（例: **酸化 → アセトアルデヒド**、**分子内脱水 → エテン**、
   **分子間脱水 → ジエチルエーテル**〔要2分子〕、**エステル化**〔要カルボン酸〕）が並ぶ
3. 「酸化」を選ぶと、キャンバス上の分子がアニメーションでアセトアルデヒドに変化。
   キャプションで「1級アルコールの酸化。さらに酸化すると酢酸になる」と解説
4. 続けて「酸化」を選べば酢酸へ（連鎖実行）。Undoでいつでも巻き戻せる
5. 反応表示は2段階を切替可能:
   - **簡易版（既定）**: 反応前 → 反応後のモーフィング（結合の組み換えをフェードで表現）
   - **機構版**: 対応する反応機構データ（reactions.json）がある場合、巻矢印アニメーションを再生

## 2. スコープ（対象反応・高校化学の定番）

| 分類 | ルール | 必要条件 | 生成物 |
|---|---|---|---|
| 酸化 | 1級アルコール→アルデヒド | C(-OH)にH2つ | R-CHO + (H₂O相当は省略) |
| 酸化 | アルデヒド→カルボン酸 | -CHO | R-COOH |
| 酸化 | 2級アルコール→ケトン | C(-OH)にH1つ | R-CO-R' |
| （案内） | 3級アルコールは酸化されにくい | C(-OH)にH0 | 実行不可の解説を表示 |
| 脱離 | 分子内脱水 | C-C(-OH)、隣接CにH | アルケン + H₂O |
| 縮合 | 分子間脱水（エーテル生成） | アルコール×2 | R-O-R' + H₂O |
| 縮合 | エステル化 | -COOH と -OH（別分子） | エステル + H₂O |
| 加水分解 | けん化（簡易） | エステル結合 | カルボン酸(塩) + アルコール |
| 付加 | X₂・HX・H₂O・H₂ の付加 | C=C / C≡C | 対応する付加体 |
| 置換 | ベンゼン環の置換（ニトロ化等） | 芳香環のC-H | 置換体（既存機構と連携） |

- 試薬（Br₂・HBr・H₂O・H₂・濃硫酸…）は**内蔵の試薬パレット**から選ぶ（毎回作図させない）。
  二分子反応（エステル化など）は、キャンバス上の別分子を相手として選択する
- マルコフニコフ則など位置選択性は M1〜M3 では扱わず、対称な付加から始める。
  非対称付加は「主生成物のみ・解説付き」で M5 以降に検討

## 3. アーキテクチャ

### 3.1 ファイル構成

- **reactor.js（新設）**: 反応ルールベース＋実行エンジン＋UIカード。DOM操作はここに集約
- **chemistry.js**: 官能基パターン検出の純粋関数を追加（DOM非依存を維持）
  - `findFunctionalGroups(mol)` → `[{type: 'primary_alcohol'|'aldehyde'|'carboxyl'|'ester'|'cc_double'|..., atoms: [...]}]`
  - 既存の isSp3Carbon / 橋検出 / findAromaticBondKeys を流用
- **game.js**: モード結線と分子呼び出しUIのみ（既存パターン踏襲）

### 3.2 反応ルールの表現

```js
{
  id: 'oxidize_primary_alcohol',
  name: '酸化（1級アルコール → アルデヒド）',
  category: '酸化',
  reagentLabel: '酸化剤 [O]',
  // 適用可否: 対象分子（と相手分子）から適用箇所を列挙して返す
  detect(mol) { return sites; },       // 例: [-OH炭素のid, ...]
  // 実行: 分子グラフを書き換える（座標は最小変更の原則）
  apply(mol, site) { ...; return { caption, byproducts: ['H₂O'] }; },
  mechanismName: null,                  // reactions.json に対応機構があれば名前
}
```

- ルールは配列で登録し、「この分子の反応」カードは `detect` が非空のものだけを列挙する
- `apply` は**元の原子座標をできる限り動かさない**（開発方針「環⇔鎖の切替で既存原子を
  動かさない」と同じ思想）。脱離する原子（H₂Oなど）は分子の外側へ平行移動して残す
  （反応機構データと同じ「原子は消さない」原則）か、副生成物として小さく表示する

### 3.3 実行と履歴

- 反応実行は通常の編集と同じく `saveState()` を積んでから行う（**Undo/Redoがそのまま効く**）
- 実行後は updateDrawing → 名称判定カードが自動更新され、生成物名が表示される
  （例: 酸化後に「アセトアルデヒド」）。この既存機能が答え合わせを兼ねる

### 3.4 アニメーション

- **簡易版**: 反応前後の2状態を reaction.js の状態補間（原子は線形移動・結合はフェード）で
  1秒モーフィング。reaction.js の補間ロジックを関数として切り出して共用する
- **機構版**: `mechanismName` があれば反応機構ビューアをその機構で開く（登録データの再生）。
  ユーザー分子への巻矢印の自動付与は将来課題（M5以降）とし、当面は
  「あなたの分子で起きたことを、代表例の機構で見る」体験とする

## 4. 分子の名称呼び出し（ライブラリから配置）

- 「この分子の反応」カードに検索ボックス（datalist）を設置。compounds.json + stages.json の
  名称ライブラリから選ぶと、キャンバスの空き領域（既存分子と重ならない位置）に配置する
- 配置は既存の createTargetFromData → 平行移動 → userMolecule へマージ。1操作=1 Undo
- これにより「エタノールを呼び出す → 酢酸を呼び出す → エステル化」が作図なしで試せる

## 5. UI

- 右パネルに新カード **「⚗ この分子の反応」**（化合物名カードの直下）:
  - 上段: プロパティ表示（P9-3連携）例:「1級アルコール／分子式 C₂H₆O」
  - 中段: 適用可能な反応のボタン列（分類アイコン＋名前＋生成物プレビュー名）
  - 下段: 分子の名称呼び出し検索ボックス
- 分子が複数あるときは、反応ボタン押下時に対象分子（連結成分）をハイライトで選択
- 表示切替（簡易版⇔機構版）はカード内のトグル。既定は簡易版
- パズルモードの正解判定とは独立（ステージのお題とは無関係に遊べる砂場）。
  反応実行モード中も通常編集は可能（実行はあくまで1回の編集操作）

## 6. マイルストーン

- **M1**: 名称呼び出し（検索→空き領域に配置）＋「この分子の反応」カードの骨組み
  （プロパティ表示: アルコールの級・アルデヒド・カルボン酸・C=C の検出まで）
- **M2**: 単分子反応（1級/2級アルコール酸化・アルデヒド酸化・分子内脱水）＋簡易モーフィング
- **M3**: 二分子反応（エステル化・分子間脱水）＋相手分子の選択UI
- **M4**: 試薬パレットと付加反応（Br₂/HBr/H₂O/H₂）・けん化。機構版切替（該当機構へのジャンプ）
- **M5**: 置換（芳香族）・非対称付加の主生成物・ユーザー分子への巻矢印自動付与の検討

## 7. リスク・未決事項

- **生成物レイアウト**: 結合組み換えで原子が重なる場合の退避方向の決め方
  （まずは既存の MIN_CLEARANCE 検査＋外向き平行移動で対応し、監査ページで検証）
- **「酸化」の表現**: 酸化剤は [O] として抽象化（高校教科書の書き方）。
  機構は扱わない（DEVELOPMENT.md P8-7 の保留判断と整合）
- **複数適用箇所**: ジオールなど適用箇所が複数ある場合は、箇所をハイライトして選択させる
- **級の判定と教育**: プロパティ表示から P9-3 の学習リンク（異性体列挙など）へ将来接続する

---

## 8. P12-5: 反応の前後比較と機構ジャンプ・簡易モーフィング（2026-07-24 設計）

ユーザー要望: **反応を実行すると生成物に置き換わるだけで、前後の比較ができず、
機構もわからない**。3.4節の構想（簡易モーフィング・機構版）を、確実に届く順で2段階に分けて実装する。

### 8.1 第1弾: 前後比較ビュー＋機構ジャンプ

**直近反応の記録**
- `Reactor.execute()` が成功したとき `this.lastReaction = { ruleId, label, before, after }` を保持。
  `before`/`after` は対象分子の **target形式スナップショット（独立コピー・原子ID付き）**。
  反応対象の連結成分だけでなく**キャンバス全体**を写す（分子間反応・副生成物も比較に含める）。
- 保持は直近1件のみ。**次の反応実行で上書き、全消去・モード離脱で破棄**。
  Undo とは独立（↩で戻しても記録は残り、比較は見られる）。

**前後比較ビュー**
- 反応実行後、「⚗ この分子の反応」カードに **「🔍 反応の前後を見る」** ボタンを表示
  （`lastReaction` があるときのみ）。
- 押すとキャンバス領域に **`#rx-compare-overlay`** を重ねて表示（異性体練習の
  `#ip-review-overlay` と同じ流儀。CSSは共通クラス `.canvas-overlay` に括り出して共用）。
  - 「反応前」「反応後」の2図を並置。ヘッダに反応名（例: 分子内脱水）。
  - 図サイズ切替（小/中/大）は `IP_REVIEW_SCALES` を共用。
  - 「← 描画に戻る」で閉じる。図をクリックしても閉じる（異性体練習と同じ操作感）。
- **差分ハイライト**（原子IDの対応付けで機械的に判定。reactor は既存原子のIDを保持し、
  追加原子には新IDを振るため、対応付けは ID 一致だけで取れる）:
  - 消えた結合・次数が下がった結合 → **反応前**の図でオレンジ点線
  - 生成した結合・次数が上がった結合 → **反応後**の図でシアン強調
  - 脱離した原子（前にあって後に無い）→ 反応前の図でオレンジ枠
  - 付加した原子（後にだけある）→ 反応後の図で緑枠
  - 要約文の自動生成（「H₂Oが脱離」等）は第1弾ではやらない（ラベルは反応名のみ）

**機構ジャンプ**
- **reactions.json の各機構に安定ID `id` を新設**（現状は name のみで照合が壊れやすい。
  既存UIは name 表示のままなので互換性に影響なし）。
- reactor の各ルール定義に `mechanismId` を追加。対応表（初期値）:

  | reactor ルール | mechanismId（reactions.json） |
  |---|---|
  | add_br2 | ethene_br2（エテンへの臭素の付加） |
  | add_water | ethene_h2o（エテンへの水付加） |
  | dehydration_intra | ethanol_e1（エタノールの分子内脱水） |
  | esterification | esterification（酢酸とエタノールのエステル化） |
  | hydrolysis_ester | saponification（酢酸エチルのけん化）※加水分解の代表として |
  | aromatic_nitration | benzene_nitration |
  | aromatic_sulfonation | benzene_sulfonation |
  | aromatic_halogenation | benzene_chlorination |
  | oxidize_* / add_h2 / add_hbr / dehydration_inter / acetylation_anhydride | なし（P12-4 の機構追加と連動して随時埋める） |

- `mechanismId` のあるルールは、比較オーバーレイと反応カードに
  **「⚗ この反応の機構を見る（代表例）」** ボタンを表示。押すと
  `setMode('learn')` → 反応機構ビューアで該当機構を選択・再生開始。
  注記を添える: **「※ あなたの分子そのものではなく、代表例の分子で機構を再生します」**
  （ユーザー分子への巻矢印自動付与は M5 の将来課題のまま）。
- 分子はモードをまたいで保持される（P10仕様）ので、見終えたら自由モードに戻れば続きから編集できる。

**テスト（第1弾）**
- RX1: エタノール→「酸化」実行→前後比較を開くと2図が描画され、
  反応前=エタノール・反応後=アセトアルデヒドのトポロジーで、差分ハイライトが存在する
- RX2: mechanismId のあるルール実行→「機構を見る」→ learnモードに切替わり、
  ビューアが対応機構をロードしている
- RX3: 全ルールの mechanismId が reactions.json の id に実在する（データ整合の機械検証）。
  reactions.json の id 重複なしも検証

### 8.2 第2弾: 簡易モーフィング（実行時アニメ）

- `execute()` の適用後、生成物への切替を**約1秒のモーフィング**で見せる:
  共通原子（ID一致）は座標を線形補間、消える原子・結合はフェードアウト、
  現れる原子・結合はフェードイン、次数変化はクロスフェード。
- 実装: reaction.js の状態補間・描画ロジックから共通関数を切り出して共用
  （3.4節の方針どおり）。アニメは一時レイヤに描き、完了時に通常描画へ戻す。
- **分子データは実行時点で即確定**させ、アニメは表示のみ（検証・Undo・監査に一切影響しない
  ＝「検証はトポロジーのみ」の原則に整合）。
- 再生中のキャンバス入力は無視（最長1秒）。タップでスキップ即完了。
  `prefers-reduced-motion` 環境ではアニメせず即切替。
- テスト: 補間関数の純関数テスト（t=0で反応前・t=1で反応後に一致）＋
  実行後の最終分子がアニメ無し時と同一（分子式・正準コード）。見た目は手動確認。

### 8.3 実装分担（CLAUDE.md のモデル運用）

- 設計: **Fable**（本節。ユーザーレビュー済みの方針を反映）
- 第1弾: **Opus** — id付与＋対応表（RX3で機械検証）、スナップショット、オーバーレイUI流用。1機能=1コミット
- 第2弾: **Opus で開始** — 補間の切り出しで reaction.js の回帰（テストD系）を必ず確認。
  イベント・描画タイミングで2〜3回詰まったら Fable へ
- 検収: **Fable** で差分レビュー＋監査ページ実行

---

## 9. 複数分子の反応（Gemini レビュー項目15・2026-08-03 設計）

レビューの指摘は3つ束ねてある: **選択分子のハイライト**・**複数選択への対応（2価エステル等）**・
**整列の最適化**。実装前に実物で測った結果、後ろ2つは「機能が無い」のではなく
**あるのに途中で止まる**形で壊れていた。

### 9.1 実測（v439・グリセリン＋酢酸3分子）

| 症状 | 実測 |
|---|---|
| 連続エステル化が2回目以降できない | エステル化の候補4件のうち **3件が「生成物を配置する空間がありません」で失敗**。成功するのは1件だけ |
| 副生成物がキャンバスの外に出る | 脱離した水が x=1360、そのときの viewBox は 238〜1312 ＝ **画面外** |
| 選択は2分子まで | `selectedMolecules.length > 2` で古い方を捨てる。3分子を要する反応列に届かない |

**この3つは同じ根から来ている**: 反応の配置ロジックが「分子は2つ・1回きり」を前提にしている。
油脂（グリセリン＋脂肪酸3分子）やジエステル（2価カルボン酸＋アルコール2分子）は
**同じ反応を2〜3回繰り返す**ので、回を重ねるほど場所が足りなくなる。

### 9.2 一括反応は作らない（方針）

「2価エステル化」を1つのルールにして一気に進める案は**採らない**。
v358 でジ体・モノ体（モノステアリン酸グリセリド等）をわざわざ登録したとおり、
**途中の分子に名前が付く**ことがこのアプリの答え合わせになっている。
一括にすると中間が見えなくなり、`lastReaction` の前後比較も「何が起きたか」を1枚に潰してしまう。

**やることは「同じ反応を繰り返せるようにする」**。選択は繰り返しの間ずっと効き続ける。

### 9.3 選択を N 分子へ広げる

- 上限を **4** にする（グリセリン＋脂肪酸3分子＝油脂まで届く）。
- `selectedMoleculeSets()` は**連結成分で重複を除く**。反応で2分子が1つになると
  選択が同じ成分を2回指すため（先に選んだ方＝式の左を残す）。
- 絞り込み `siteAllowed` を一般化する。現行の「**すべての**選択分子に跨る箇所だけ」は
  2分子専用で、**3つ選んだ瞬間に2分子反応が全滅する**（跨れるのは常に2分子だから）:

  | 選択数 | 通す箇所 |
  |---|---|
  | 0 | すべて |
  | 1 | その分子の原子を含む箇所（相手は未選択でよい） |
  | 2以上 | 箇所の原子がすべて選択の中にあり、かつ **2つ以上**の選択分子に跨る |

### 9.4 選択の見え方

- 番号を**丸数字（①②）から塗りバッジの算用数字（1・2・3）へ**変える。
  丸数字は図の下の見出し（`renderMoleculeLabels`）が**キャンバスの通し番号**として使っており、
  選択枠の丸数字（**選んだ順**）と意味が食い違う。エタノール→酢酸の順に選ぶと
  「見出しでは①酢酸なのに選択枠では②酢酸」になる。記号を分けて衝突をなくす。
- 選択中の分子に**薄い塗り**（青・低不透明度）を敷く。枠線だけだと、分子が3つ以上あるとき
  どれが選ばれているか一目で読めない。塗りは作図の下（`uiGroup`）なので線を隠さない。
- 2つ以上選んでいるときだけ、1番の枠に **「式の左」** の注記を出す。
- 右パネルの案内文も N 対応にする。ただし **「2 エタノール」とは書かない**——
  化学の文脈では係数（2分子のエタノール）と読めてしまう。**「2番目 エタノール」**と書く。

### 9.5 整列（配置の失敗をなくす）

**原則: 反応に関わらない分子は、じゃまなら退避させてよい。** 座標は見た目専用なので化学は変わらない。

実装したのは4つ（v472。設計時の見立てでは1つ目だけのつもりだったが、
実測すると原因が重なっていた）。

1. **傍観分子の退避**（`planShoveAside`）。`planAttachment` は
   「全部を避けて置ける向き」→「傍観分子を無視して置ける向き＋その分子を外へ逃がす」の順に探す。
   逃がしきれなければ従来どおり失敗する
2. **刻みをそろえる**。名称ライブラリはエントリごとに刻みが違い（グリセリン 42px・酢酸 80px）、
   42px 間隔の枝のあいだへ 80px 幅のアセチル基が差し込まれて結合線が隣の炭素を
   0.0px で貫通していた。動かす側を**母体の刻みへ相似に伸縮**してから繋ぐ
   （相似なので形も結合角も変わらず、一様な正の倍率だから鏡像にもならない）
3. **動かす側を大きさで決める**。**ここは設計時の見立てを変えた**——
   「式の左右（v347／C-2）は変えない」つもりだったが、大きい方を動かすと
   置き場が無く、これが油脂で止まる主因だった。選択が無いときは**小さい方**を動かす。
   酢酸(4原子)＋エタノール(3原子) では従来どおりアルコールが動くので、
   CH₃COOH + HOCH₂CH₃ の並びは変わらない。分子を選んでいるときは
   従来どおり**先に選んだ方（式の左）を動かさない**。決めた向きで置けなければ反対向きも試す
4. **90°回転を候補に足す**。鏡映は従来どおり入れない（不斉炭素が鏡像異性体に化ける）。
   90° で変わるのは図の**読み方の約束**の方だけ——フィッシャー投影（縦＝奥）と
   ハース投影（環は横置き）——なので、**不斉炭素も面マークも環も持たない分子**に限る。
   脂肪酸やアセチル基がここに入り、「縦に立てて置く」候補が使えるようになる

**残っている限界**: グリセリン＋**ステアリン酸**3分子は3本目が置けない（2本まで）。
炭素18個の直鎖は1400px あり、直交4方向 × 回転4通りでは収まらない。
ここから先は生成物側の再配置（母体ごと組み直す）が要るので、別項目として立てる。

### 9.6 副生成物の置き場

`parkAsWater` は「全原子の右端＋2マス」に置くため、反応を重ねるほど右へ伸びて画面から出る。
**脱離した場所の近くから外へ向かって空きを探す**方式へ変える:

- 候補は脱離した O の周り 8マス四方の格子点。**近い順**に見て、
  重原子から `1.5マス`以上・**結合線から 0.5マス**以上離れた最初の点に置く
  （結合線からの距離も見ないと、伸ばした結合の上に水が乗って構造式が別物に見える）。
- 「水がとれた場所のそば」に出るので、どの反応で出た水かも読める。
- `fromReaction` の印は従来どおり付ける（図の下の見出しに水を出すため）。

### 9.7 テスト（RX 系に追加）

- **RX17**: 3分子を選んでもエステル化の候補が消えない／選んでいない4つ目の分子が絡む候補は消える
- **RX18**: グリセリン＋酢酸3分子で、エステル化を**3回連続**実行できてトリエステルになる
  （エステル結合3本・遊離の -OH ゼロ）。v439 では2回目で「配置する空間がありません」
- **RX19**: 反応で出た副生成物が**呼び出し直後の視野の中**に収まり、
  どの原子・結合線とも重ならない

---

## 10. reactor の穴埋め（qa レーンの283項目棚卸し・2026-08-06。v816〜）

一問一答（qa）が知識項目283件を反応と突き合わせたところ、
**「反応が無いので繋げられない」項目が30件**あった。その内訳と、ここで埋めた分の判断を残す。

### 10.1 需要の実数（qa の棚卸しが数えたもの）

| 足したい反応 | 繋がる項目数 | この回の扱い |
|---|---|---|
| **酸塩基**（弱酸の遊離・塩の生成） | **7件** ← 最大 | §10.5（`br2` の次に大きい穴） |
| 共重合（2種の単量体を1本の鎖に） | 3件 | 未着手・申し送り（§11.6 で「入れない」と判断） |
| **酸化開裂** | 1件だが**構造決定の中心** | §10.3 で実装 |
| グリコシド結合の加水分解 | 2件 | **§11 で実装**（v882） |
| ナトリウムとの反応（水素発生） | 2件 | **§11 で実装**（v883） |

数字は「項目数」であって「重要度」ではない。**酸化開裂は1件しか繋がらないが、
構造決定という単元そのものの主役**なので、件数だけで順位を付けない。

### 10.2 ⚠ 臭素水 × フェノールは「逆を教えていた」（最優先で直した穴・v816）

**症状**: `br2_water` に紐づく反応が `add_br2`（不飽和結合への付加）1本しか無かったため、
フェノールに臭素水を掛けると空振りになり、瓶の `miss`

> ベンゼン環は付加ではなく置換なので、この条件では脱色しません

が返っていた。教科書の必修事項（触媒なしで置換が進み **2,4,6-トリブロモフェノールの白色沈殿**が
できる）と**正反対**。「反応が足りない」ではなく「**間違いを教えている**」ので最優先。

**実装**: `bromination_activated_ring`（`reactor.js`）。

- 対象は **-OH（フェノール）と -NH₂（アニリン）が付いた一置換ベンゼンだけ**。
  判定は `activatingSubstituent()` で、**環外の重原子がちょうど1つ**＝裸の -OH / -NH₂ に絞る。
  アニソール（-OR）・アセトアニリド（-NHCOR）も理屈の上では活性化基だが、
  **白色沈殿として高校で扱わない**ので候補に出さない（判断できないものは出さない・開発方針4章）
- **2,4,6 を一度に置換する**。1箇所ずつ3回に割らないのは省略ではなく**教科書どおり**で、
  一置換体・二置換体は取り出せずトリブロモ体まで進む。途中の図を出すと実在しない中間体になる
- 一置換体（オルト2つとパラが3つとも空いている形）に限る。o-クレゾールのように
  空きが足りない環は「どこまで入るか」を高校の範囲で決められないので出さない

**`detect` の数える単位**（第1段・第2段の教訓の確認）:
`activatedRingBrominationSites()` は `componentOf()` で**連結成分を切り出してから**
環の大きさと置換基の数を数える。芳香族の下ごしらえは過去に2度
「キャンバス全体で数えていて、同じ分子を2つ並べると1件に潰れる」壊れ方をしている
（`DESIGN_reagent_palette.md` §7.1・§8.1）ので、**フェノール2個 → 2件**を RC1 で固定した。
新設した `aromaticAtomSet()` は集合を作るだけで、成分をまたいで判断しない。

**生成物の一致**: フェノールの生成物の正準コードが、登録済みの
`2,4,6-トリブロモフェノール` と**完全に一致**することを RC1 で主張する。
アニリンの生成物 **2,4,6-トリブロモアニリンは未登録**なので、
登録済みトリブロモフェノールの O を N に置き換えた分子を期待値として組み立てて比べている。
**登録の依頼は §10.7 に出す**（compounds.json は別レーンが作業中のため触らない）。

### 10.3 酸化剤 [O] を側鎖酸化・酸化開裂まで広げる（v817）

v816 まで `oxidant` は **1級・2級アルコールとアルデヒドにしか作用しなかった**。
そのため高校の必修が2つ、画面のどこからも出せなかった:

- **トルエンの側鎖酸化**（→ 安息香酸）
- **アルケンの酸化開裂**（KMnO₄ で C=C を切る）＝ **構造決定の主役**

酸化開裂は**条件で生成物が変わる**（ケトン／アルデヒド／カルボン酸／CO₂）ので、
「判断できないものは出さない」（DEVELOPMENT.md 4章）に従って線を引いた。
**線を引いた理由をここに残す。**

#### 実行する（`oxidize_side_chain` / `oxidative_cleavage`）

| 形 | 生成物 | 根拠 |
|---|---|---|
| 芳香環に直結した **-CH₃** | -COOH（安息香酸・フタル酸・テレフタル酸） | 炭素が減らないので、切れて出ていく側の行き先を決めなくてよい |
| 炭化水素の、環でない C=C で **両端に炭素がついている**もの | 炭素2つ（R₂C=）→ ケトン ／ 炭素1つ（RCH=）→ カルボン酸 | 酸性の強い酸化剤（KMnO₄・K₂Cr₂O₇）1本ぶんに行き先が**一意に決まる**組み合わせ |

行き先は「**その炭素についていた炭素の数**」だけで決まる。この1行が規則の全部で、
コードにも `carbons(id, other)` の数え上げとしてそのまま出ている。
オゾン分解（O₃/Zn → アルデヒドで止まる）は**別の試薬**なので、この瓶では出さない
——「同じ瓶で条件が割れる」形（§2.4 の濃硫酸）にはしない。

#### 実行しない（`oxidation_out_of_scope_info` が理由だけ返す）

| 形 | 出さない理由 |
|---|---|
| **末端の C=C（=CH₂）** | CO₂ と水になって出ていく。**有機の図に残らないもの**をキャンバスに置くと、以後その CO₂ が反応の相手として数えられる |
| **環の中の C=C** | シクロヘキセン → アジピン酸は正しいが、「切ったのに1分子のまま」で前後比較の読み方が変わる。別項目として立てる |
| **C≡C の開裂** | 高校では扱いが安定しない（案内も出さない） |
| **炭素と水素だけでできていない分子** | 他の官能基との**酸化されやすさの順序**を高校の範囲で決められない（アルコールの酸化に置いた線引きと同じ） |
| **炭素2個以上の側鎖**（エチルベンゼン・クメン・スチレン） | 生成物は安息香酸で確定だが、**切れて出ていく側**の行き先が条件で変わる |
| **環に -OH / -NH₂ が付いた分子** | 環そのものが酸化されて壊れるので、側鎖だけを残した図は書けない |

⚠ **`info` は1本にまとめた。** 種別ごとに分けると、スチレン（末端 C=C ＋ 長い側鎖）で
瓶を押したときに hits が2件になり、`renderConditionChoice`（本来は温度の2択のための画面）が
解説2つを並べてしまう。`info` ルールは `apply(game)` を**箇所なしで**呼ばれるので、
文面は分子をもう一度見て組み立てる（`oxidationOutOfScope()` が `{ sites, kinds }` を返す）。

#### `detect` の数える単位（第1段・第2段の教訓の確認）

新しい3つの `detect` は、どれも `componentOf()` で連結成分を切り出してから数える。
とくに `sideChainOxidationSites()` は **RX8 と同じ「同じ生成物になる位置はまとめる」**を
入れている（p-キシレンの2つの -CH₃ は等価なので1通り）が、その鍵
`sideChainProductKey()` には **`aromaticSiteClass` が §8.1 で直したのと同じ手当て**
——連結成分だけを複製し、成分の同一性を鍵の前に置く——を最初から入れてある。
RC2 で「トルエン2個 → 2件」「p-キシレン2個 → 2件」「2-ブテン2個 → 2件」を固定した。

#### 一致の主張（空振りの緑を避ける）

生成物が**登録エントリと同じ正準コード**になることを5組で主張する:
トルエン→安息香酸 ／ o-キシレン(2回)→フタル酸 ／ p-キシレン(2回)→テレフタル酸 ／
2-ブテン→酢酸2分子 ／ 2-メチル-2-ブテン→アセトン＋酢酸。
否定対照は同じ数え方で **陰性17件が0件**（側鎖9件・開裂8件）。

### 10.4 H–X 付加を HBr / HCl / HI の3本にする（v818）

**症状**: 瓶もルールも **HBr の1本だけ**だったので、「HCl の付加で塩化ビニルができる」を
画面で追うと**生成物が臭化物になる**。マルコフニコフ則そのものは伝わるが、
ラベル（何を加えたか）と図（何が付いたか）がずれる。

**まとめ方**: `HYDROGEN_HALIDES` という**表を1つ**置き、そこから
**瓶（`REAGENTS`）とルール（`REACTION_RULES`）の両方を生成**する。

```
HYDROGEN_HALIDES = [ {key:'hbr', element:'Br', …}, {key:'hcl', …}, {key:'hi', …} ]
   ├→ HYDROGEN_HALIDE_REAGENTS  … 瓶3本（… REAGENTS に展開）
   └→ HYDROGEN_HALIDE_RULES     … add_hbr / add_hcl / add_hi（… REACTION_RULES に展開）
```

- **`detect` は3本が同じ関数オブジェクトを共有する**（`multipleBondSites`）。
  RC3 が `new Set(rules.map(r => r.detect)).size === 1` でこれを固定するので、
  片方の枝にだけ条件を足すと赤くなる
- `apply` も枝ごとに書かない。違うのは**付ける元素と一言の補足**だけ
- `id` は `add_hbr` を含めて従来どおり。既存のテスト・台本・デモが名指ししているので改名しない

**一致の主張**: アセチレン＋HCl → **塩化ビニル**（③の症状そのもの）、
エチレン＋HCl → クロロエタン、エチレン＋HBr → ブロモエタン の3組が登録エントリと同じ正準コード。
否定対照は「同じエチレンでも3本の瓶で**3種類**の生成物になる」（1種類なら瓶を見分けていない）と、
エタンでは3本とも空振りしてマルコフニコフ則の説明が返ること。
マルコフニコフ則そのものは、プロペンで**ハロゲンが炭素2個の炭素につく**ことを3本とも同じ数え方で確かめる。

**ヨウ化物の生成物は未登録**（§10.7）なので、HI だけは構造での主張にとどめている。

### 10.5 まだ埋めていない穴（需要の実数つき・申し送り）

| 反応 | qa の項目数 | メモ |
|---|---|---|
| **酸塩基**（弱酸の遊離・塩の生成） | **7件** | §10.6。**塩が16件登録済み**なので、反応を足すだけで正準コードの一致まで確かめられる |
| 共重合（2種の単量体を1本の鎖に） | 3件 | 既存の `addition_polymerization` は1種類の単量体を前提にしている。**§11.6 で「入れない」と判断**（高校範囲外・並ぶ順が決まらない） |
| ~~グリコシド結合の加水分解~~ | ~~2件~~ | **§11 で実装済み**（v882・`hydrolysis_glycoside`） |
| ~~ナトリウムとの反応（水素発生）~~ | ~~2件~~ | **§11 で実装済み**（v883・`react_sodium`）。H₂ の置き場は「描かない」で決着（§11.5） |

### 10.6 酸塩基（次に大きい穴・7件）

このアプリは電荷をモデルに持たず、**塩は「線1本の共有結合」として書く**流儀
（`DESIGN_compound_coverage.md` §6-2・v353 決定）。その流儀の塩がすでに16件登録されている
（酢酸ナトリウム／酢酸カリウム／ナトリウムフェノキシド／ベンゼンスルホン酸ナトリウム／
サリチル酸ナトリウム／パルミチン酸ナトリウム／ステアリン酸ナトリウム／フタル酸水素カリウム …）。
**生成物が既にあるので、反応を足すだけで正準コードの一致まで確かめられる。**

| qa の項目 | 実装の形 |
|---|---|
| `org.phenol.naoh` | フェノール ＋ NaOH → ナトリウムフェノキシド（**瓶は既にある**「水酸化ナトリウム」） |
| `org.phenol.phenoxide-co2` | ナトリウムフェノキシド ＋ CO₂ → フェノール（炭酸より弱い酸であることの証明）。CO₂ の瓶が要る |
| `org.carbonyl.salt-strong-acid` | カルボン酸の塩 ＋ 強酸 → 弱酸の遊離。希硫酸の瓶で引ける |
| `org.carbonyl.vs-carbonic` | **第3段の検出瓶「炭酸水素ナトリウム」がすでに扱っている**。反応にするか検出のままにするかは要判断 |
| `org.carbonyl.acidity` | 酸の強さの比較。反応ではなく `info` で足りる可能性 |

**当面扱わない2件**（ユーザー決定・2026-08-06）:
`org.aroN.aniline-base`（アニリンの塩基性）と `org.bio.amino-acid-amphoteric`（双性イオン）。
アンモニウム型の N（単結合4本）は `chemistry.js` の `maxValencyOf` がすでに許しているが、
**対イオンが結合を持たない種**なので、いまのモデルとキャンバスには置き場がない。
**「イオンは最終的に assembler で扱う」というのがユーザーの方針**なので、
他のアプリへ渡す形にはしない。足りないのは対イオンの持ち方だけ、と記録しておく。

### 10.7 登録が必要な化合物（このレーンでは触らない）

`assembler/compounds.json` は別レーンが全行に `id` を振る作業中のため**1行も触っていない**。
この回の実装で「生成物として画面に出るのに名前が出ない」ものが3件ある:

| 名前 | 出てくる操作 | 補足 |
|---|---|---|
| **2,4,6-トリブロモアニリン** | アニリン ＋ 臭素水（§10.2） | フェノール側は登録済み。白色沈殿の相方なので対で欲しい |
| **2-ブロモプロパン／2-クロロプロパン** | プロペン ＋ HBr / HCl（§10.4） | マルコフニコフ則の主生成物の代表。**異性体（1-ハロプロパン）と対で**あると効く |
| **ヨードエタン（ヨウ化エチル）** | エチレン ＋ HI（§10.4） | HI だけ生成物の名前が出ない |

### 10.8 F と Si を入れるとしたら（**調査のみ・実装していない**）

**背景**: 元素表に F と Si が無いので、**テフロン（ポリテトラフルオロエチレン）と
シリコーンゴムは原理的に作図できない**。過去にユーザーは「F は保留」と判断している
（`DESIGN_compound_coverage.md` §6-1）。ここは判断材料だけを置く。

⚠ **`DESIGN_compound_coverage.md` はこのレーンでは触っていない**（別レーンが編集中）。
同書 §6-1 の「元素を1つ足すときの手順（5か所）」が土台で、以下はその**追補**。

#### 実測（`VALENCIES` に `F:1` / `Si:4` だけを足した写しで確かめた）

| 見たもの | F（テトラフルオロエチレン） | Si（(CH₃)₂Si-O-Si(CH₃)₂） |
|---|---|---|
| 正準コード | 出る | 出る |
| 空き価標（自動水素） | C は 0 で正しい | Si は結合数どおりで正しい |
| `describeStructure` | 「C=C二重結合 ×1」まで出るが **F に一言も触れない** | 「最長の炭素鎖 C1」しか出ず**誤解を招く** |
| `findOutOfScopeMotifs` | `[]`（範囲内） | **`no_group`「高校で習う官能基にあてはまらない」** |
| `findFunctionalGroups` | 影響なし | **`[]`** —— Si-O-Si を見る型が無い |
| 系統名 | **「1,1,2,2-テトラフルオロエテン」が出る**（`IUPAC_HALOGEN` は F を最初から持っている） | **`null`**（シロキサンの命名規則が無い） |
| R/S（`cipRank`） | **`null`**（`CIP_ATOMIC_NUMBER` に F が無く、**分子ごと**判定が消える） | 同上 |

#### F: 追加は**現実的**。同書の5か所＋2か所で足りる

1〜5 は同書のとおり（`VALENCIES` / `CIP_ATOMIC_NUMBER` に `F: 9` / `--color-f` /
`stereo.js` の `colorOf` / `describeStructure` に「フッ素 F ×n」）。**追補は2つ**:

6. **`stereo.js` の `colorOf` は先頭1文字で元素を切る**（`/^(Cl|Br)/` のときだけ2文字）。
   F は1文字なので**表に足すだけでよい**（Si は下記のとおりここが罠になる）
7. **`tools/verify-reactions.js` の `MAX_VALENCY`** には I すら入っていない。
   反応機構（`reactions.json`）に F を使う日が来たら同時に足す

**壊れないことの確かめ方**は同書の手順どおり（全件の正準コード・立体コード・分子式・
系統名・空き価標を書き出して前後で diff）。**F を含む既存分子は0件**なので無差分になるはず。

**入れると何が届くか**: テトラフルオロエチレンを compounds.json に登録すれば、
**既存の `addition_polymerization` がそのままテフロンを作る**（`vinylBonds` は
環でも芳香族でもない C=C を見るだけなので、置換基がハロゲンでも通る）。
**原子パレットに F を出す必要はない** —— I・Na と同じく「名称から呼び出す」と
「反応の生成物」だけで単元が成立する（375px で原子ボタンを1つ増やすと帯が +45px 伸び、
`DESIGN_entry_points.md` A-1 で詰めた順路がその分だけ画面外へ動く）。

#### Si: **追加は現実的でない**。壊れるのは価標ではなく「分類・命名・反応」

`VALENCIES` に `Si: 4` を足すだけでは、シリコーンは**描けるが読めない**分子になる:

- **`findOutOfScopeMotifs` が `no_group` を返す** ＝ アプリが自分から
  「高校で習う官能基にあてはまらない」と言う。これは価標の問題ではなく**分類の問題**で、
  Si-O-Si（シロキサン結合）を官能基として認める判断が要る
- **命名器がシロキサンを知らない**（`iupacName` が null）。名前が出ない分子は
  ライブラリの規約（名前で呼び出す）に乗らない
- **`stereo.js` の `colorOf` は 'Si' を 'S' と読む**（先頭1文字で切るため）。
  硫黄の黄色になる。`/^(Cl|Br|Si)/` へ直す必要がある —— **F には無い罠**
- **作る反応が無い**。シリコーンはジクロロジメチルシランの加水分解＋縮合でできるので、
  既存の付加重合でも縮合重合（エステル・アミド）でも作れない。**新しい重合ルールが要る**
- 単量体を登録しても、上の3つが揃わないと**呼び出せるだけの死んだエントリ**になる

**結論の材料**: F は「5か所＋2か所を同時に直し、テトラフルオロエチレンを1件登録する」で
テフロンまで届く。Si は「分類・命名・重合ルール」の3つを新設する話で、
**元素を1つ足す作業ではない**。同じ労力を §10.6 の酸塩基（7項目）に向けたほうが効く。

### 10.9 酸塩基のうち、既存の流儀のまま解ける分を入れた（v819）

§10.6 の7項目のうち、**新しい概念（電荷・対イオン）を1つも持ち込まずに解ける分**を実装した。
入ったのは**ルール2本だけ**で、瓶は1本も増えていない（既存の `naoh_aq` と `h2so4_dil` に付けた）。

| ルール | 瓶 | 中身 |
|---|---|---|
| `neutralize_naoh` | 水酸化ナトリウム | 酸性の -OH に Na を付ける（カルボン酸・フェノール・スルホン酸の**3つの入口を1つのルールに**） |
| `liberate_weak_acid` | 希硫酸 | -O-Na / -O-K の金属を外す。**自動水素がそのまま -OH を描く**ので、外すだけで酸に戻る |

**この2本はちょうど逆向き**なので、テストは「往復してもとの正準コードに戻る」形で書ける（RC4）。
突き合わせた6組はすべて登録済みのエントリと一致した:
酢酸 ⇄ 酢酸ナトリウム ／ フェノール ⇄ ナトリウムフェノキシド ／
ベンゼンスルホン酸 ⇄ ベンゼンスルホン酸ナトリウム。

**けん化・ヨードホルム反応の生成物（-COONa）からそのまま引ける**のが効いている ——
油脂 → けん化 → セッケン → 希硫酸 → 脂肪酸、という順路が1本につながった。

**まだ残っている酸塩基**:

- `org.phenol.phenoxide-co2`（ナトリウムフェノキシド ＋ CO₂ → フェノール）… **CO₂ の瓶が要る**。
  §10.2（試薬パレット §10.2）で「瓶20本が2列に縦積みされる形はもう限界に近い」と測ったばかりなので、
  **足すなら先に区分をもう一段割る**（付加／置換／酸化還元／酸塩基…）ことを決めてから
- `org.carbonyl.vs-carbonic` … 第3段の検出瓶「炭酸水素ナトリウム」が**すでに陽性/陰性で扱っている**。
  反応にすると同じことを2か所で教えることになるので、**検出のまま置く**
- `org.carbonyl.acidity`（酸の強さの比較）… 独立した `info` は作らず、
  上の2本の `caption` に「カルボン酸 > 炭酸 > フェノール」を入れた。
  `naoh_aq` の `miss` には**陰性側**（アルコールの -OH は中和されない）を書いた（試薬パレット §9.2）

### 10.10 ⚠ 反応を足すと、回帰テストの「全件 × 全反応」が二乗で効いてくる（v819 で実発生）

反応が **24本 → 30本**になった時点で、`tools/run-tests.mjs` の**10分の門番を超えた**
（実測: 600秒でタイムアウト。それまでは 264秒 → 294秒 → 352秒 → 407秒 と伸び続けていた）。

原因は RX の「ライブラリ全件 × 全反応で貫通ゼロ」で、**ルールごとに
`summonMolecule` を呼び直していた**こと。889件 × 全ルール ＝ 2万回以上の呼び出しになる。

**直し方**（判定も件数も変えていない）: 母体は
「**下見用に1回**」＋「**当たったルールのぶんだけ**」しか呼び出さない。
`detect` は分子を変えないので下見の母体を使い回せる。実行だけが書き換えるので、そこは毎回新しく作る。

**申し送り**: 反応をさらに足すときは、まず**この掃き出しの形を確かめる**。
「全件 × 全反応」の掃除は品質の要なので減らせないが、
**`detect` の下見と `apply` の実行を分ける**だけで呼び出し回数は桁が変わる。
同じ形の掃き出しは `tests.js` に他にもある（作図の掃除・候補の実行可能性）ので、
次に門番が近づいたらそちらも同じ手で軽くする。

---

## 11. 「図はあるのに反応から行けない」4件を埋めた（v880〜v883・2026-08-07）

§10.5 の申し送りのうち2件と、そこに載っていなかった**重合の穴**2件を埋めた。
共通する症状は **「`compounds.json` に図はあるのに、反応実行モードからそこへ行けない」**
——— 名称ライブラリから呼べば描けるが、**自分の手で作れない**、という片道の状態だった。

| ルール | 瓶 | 行けるようになった図 | qa の項目 |
|---|---|---|---|
| `alkyne_polymerization` | なし（重合） | ポリアセチレン | `org.poly.conducting`（4件） |
| `condensation_polymerization` | なし（重合） | ナイロン66 ／ PET（PET は図が未登録・§11.5） | `org.poly.nylon66` `org.poly.pet` |
| `hydrolysis_glycoside` | 希硫酸（**既存**） | β-D-グルコース ／ β-D-フルクトフラノース | 2件（§10.5） |
| `react_sodium` | **金属ナトリウム（新設・21本目）** | ナトリウムフェノキシド ／ 酢酸ナトリウム | `org.alcohol.na` `org.alcohol.ether-props`（2件） |

### 11.1 重合は既存ルールを広げず、**横に足す**

「アセチレンも重合できるようにする」のは、一見すると `vinylBonds`（`addition_polymerization`
の下ごしらえ）を `type === 3` まで広げるだけに見える。**それをやってはいけない。**
`vinylBonds` は `conjugatedDienes`（1,4-付加重合）と `vulcanizablePairs`（加硫）も使っており、
三重結合を混ぜると**ジエンの判定と加硫の相手選びまで一緒に変わる**。
ルールを1本足すほうが、壊れる範囲が読める。

同じ理由で、対象は**アセチレンだけ**にした（分子全体が C≡C の2原子）。
1-アルキンを重合させると頭-尾の並びが問題になるが、ビニル系のように
「置換基の数で頭を決める」根拠が教科書に無い。**根拠のない一般化はしない。**

縮合重合も、既存の `esterification` を「くり返す」形にはしていない。
エステル化は**1本の結合を作る**ルールで、そこにくり返しを持ち込むと
「どこまで伸ばすか」がルールの中に入ってしまう。
**画面に並んでいる単量体を全部つなぐ**という `addition_polymerization` の作法に合わせた。

### 11.2 縮合重合が出る条件は「2組（4分子）以上」

1組（酸1・相手1）のときは、できるのは**ふつうのエステル化／アミド化**であって重合ではない。
そこで:

- **2組以上そろったときだけ** `condensation_polymerization` を出す
- 1組のときは従来どおり `condensation_polymer_info`（説明）だけを出す
- **両方が同時に出ることはない**（`condensation_polymer_info` の `detect` が、
  実行できるときは空を返す）＝ 同じことを2つのボタンで言わない

説明側の文面には「もう1組ずつ並べると『縮合重合』が選べる」を足した。
案内が**次の操作**を指すようにしておかないと、説明で行き止まりになる。

鎖の端は `R` で止める。**残っている -COOH は -OH を落として -CO-R にする**
（次に来るのはアミン／アルコールなので、そこでも水がとれる）。
この扱いにすると、アジピン酸3＋ヘキサメチレンジアミン3 の生成物が
**登録エントリ `nylon-66` と正準コードまで一致する**（下）。

### 11.3 `detect` の数える単位（第1段・第2段の教訓の確認）

4本とも「1分子だけを見る」ことを、実装と試験の両方で確かめてある。

| ルール | 1分子に閉じている根拠 |
|---|---|
| `alkyne_polymerization` | `componentOf` で連結成分を切り出し、**重原子が2個**であることを見る |
| `condensation_polymerization` | 官能基は `findFunctionalGroups` がキャンバス全体を返すので、**代表原子がどの連結成分に属すか**で振り分けてから数える。単量体がそろっているかは `componentCode`（成分だけを複製してから正準コード）で見る |
| `hydrolysis_glycoside` | 見るのは**1つの環の近所だけ**（`sugarRingOf` は環の原子を BFS で拾う）。キャンバス全体の正準コードは数えない |
| `react_sodium` | 官能基1つ（酸素1個）を見るだけ |

⚠ **試験でも固定した**: `GH1` が「二糖を**2分子**置いたら候補は**2件**」
「二糖＋エタノールでも候補は**1件**」を主張する。
全体数え（§8.1 で3件出た壊れ方）に戻ると、どちらも赤くなる。

**座標で決める・原子IDに頼らない**も4本すべてで守っている（`chem-atom-id-hazard`）。
アセチレンの左右、縮合重合でどの基から使うか、スクロースのようにアノマー炭素が
両側にあるときどちらを切るか —— すべて x → y の順で決めている。

### 11.4 一致の主張（空振りの緑を避ける）

生成物が**登録エントリと同じ正準コード**になることを7組で主張する:

| 反応 | 生成物 | 突き合わせた登録エントリ |
|---|---|---|
| アセチレン ×3 | ポリアセチレン | `polyacetylene` |
| アジピン酸 ×3 ＋ ヘキサメチレンジアミン ×3 | ナイロン66 | `nylon-66` |
| マルトース ＋ H₂O | グルコース ×2 | `beta-d-glucose` |
| セロビオース ＋ H₂O | グルコース ×2 | `beta-d-glucose` |
| スクロース ＋ H₂O | グルコース ＋ フルクトース | `beta-d-glucose` ＋ `beta-d-fructofuranose` |
| フェノール ＋ Na | ナトリウムフェノキシド | `sodium-phenoxide` |
| 酢酸 ＋ Na | 酢酸ナトリウム | `sodium-acetate` |

**否定対照**は3種類そろえた:

1. **陰性の数え上げ**（同じ数え方を陽性にも陰性にも掛ける）——
   アセチレン重合5件・縮合重合5件・グリコシド加水分解9件・ナトリウム8件が、すべて0件
2. **突き合わせ方そのものの対照** —— 「ポリアセチレンとポリビニルアルコール」
   「グルコースとフルクトフラノース」「酢酸ナトリウムとナトリウムフェノキシド」が
   **互いに違うコードになる**ことを先に主張する。ここが同じなら上の一致は空振りの緑
3. **往復** —— エタノール →[Na]→ ナトリウムエトキシド →[希硫酸]→ エタノールで
   正準コードが戻る（`liberate_weak_acid` が既にアルコキシドにも効く）

PET は**図が未登録**なので構造で主張する（C30 O12 R2・エステル結合5か所・水6分子）。
ラクトースの生成物（グルコース＋ガラクトース）は**立体だけが違う**ので、
構造の正準コードでは区別できない。そこは「ピラノース2つ」までしか主張していない。

⚠ **糖の名称カードは「立体で決まります」と返る**（実測・v882）。
マルトースを加水分解すると、名称カードは「グルコース」ではなく
**「アロース／ガラクトース ほか3種 のどれか（立体で決まります）」**と出る。
これは加水分解の不具合ではなく、**このアプリの既存の振る舞い**である ——
`β-D-グルコース` を名称ライブラリから呼び出しただけでも同じ文が返る
（登録エントリ側に立体が無い糖は、構造だけでは1つに決まらない）。
マルトース自身も「セロビオース／マルトース のどれか」と出る。
**したがって加水分解で名前の精度は1段も落ちていない**が、
糖のレーンが立体を入れたら、この反応の生成物もそのまま名前が決まるようになる。

### 11.5 瓶が 20 → 21 本になった（試薬パレット §10.2 の申し送りに触れる）

金属ナトリウムは新しい瓶なので、`DESIGN_reagent_palette.md` §10.2 の
「**これ以上『変えるもの』を増やすなら、区分をもう一段割ることを先に決める**」に触れる。
それでも足した理由:

- 同書 **§3.1 の「変換する試薬15本」の13番目が最初から「金属ナトリウム Na」**であり、
  **§5 第4段に「瓶とルールを足すだけで載る」と書いてある**。新規の判断ではなく予定分の消化
- 塩は線1本の共有結合として書く流儀（v353）に**そのまま乗る** ＝ 電荷モデルは要らない
- **水酸化ナトリウム水溶液の隣に置くことに意味がある**。
  アルコールは NaOH とは中和しないが Na とは反応して水素を出す ——
  この対比が qa の2項目そのもので、**瓶が並んでいないと画面で比べられない**

⚠ **区分をもう一段割る判断は、まだ済んでいない**（21本目でいよいよ近い）。
320px では2列固定なので、21本は瓶だけで 11行 ＝ 3画面ぶん。
`MM9` と `tools/check-mobile.mjs` で**横あふれ0・32px未満0**は保っているが、
**次に「変えるもの」を足すときは、先に区分（付加／置換／酸化還元／加水分解／酸塩基…）を決めること。**

**H₂ は描かない。** とれる水素はもともと自動水素（明示原子ではない）なので、
Na が付いた時点で自動的に消える —— `neutralize_naoh` が水を描かないのと同じ流儀で、
「画面に無い分子は描かない」を守っている。発生は `caption` が言う。

### 11.6 入れなかったもの（判断と理由）

| 見送ったもの | 理由 |
|---|---|
| ~~**ポリエチレンテレフタラート（PET）の図の登録**~~ | **反応は入った**（テレフタル酸＋エチレングリコールで作れる）が、`compounds.json` は「末尾に追記のみ・整形し直さない」規約の資産で、このレーンは反応ルールの担当。**§11.7 に申し送る** → **v942 で登録ずみ** |
| **共重合**（2種の単量体を1本の鎖に・qa 3件） | 高校範囲外。`addition_polymerization` も `condensation_polymerization` も「単量体は1種類」を正準コードで確かめている。混ぜると**どの順で並ぶか**が決まらない |
| **ナイロン6（ε-カプロラクタムの開環重合）** | 単量体（ε-カプロラクタム）が未登録。環状アミドを開いてつなぐ操作は縮合重合とは別の形（水がとれない）なので、ルールも別に要る |
| **1-アルキンの付加重合** | 頭-尾の並びの根拠が教科書に無い（§11.1） |
| **多糖（デンプン・セルロース）の加水分解** | 図が未登録。ルールとしては `hydrolysis_glycoside` がそのまま効く（グリコシド結合を1つずつ切る）ので、**図を足せば反応は動く** ⚠ **v942 に実測を追記**: 反応は動くが、高分子の規約（両端を R で止める）と噛み合わず**切ってできる断片が両方とも「（名称未登録）」**になる。判断と数字は `DESIGN_compound_coverage.md` §19.4 |
| **エーテルの合成としての Na 経路**（ウィリアムソン合成） | 高校範囲外。エーテルは既存の分子間脱水で作れる |

### 11.7 登録が必要な化合物（このレーンでは触らない・§10.7 の続き）

| 名前 | 出てくる操作 | 補足 |
|---|---|---|
| ~~**ポリエチレンテレフタラート（PET）**~~ | テレフタル酸 ×3 ＋ エチレングリコール ×3 の縮合重合 | **v942 で登録ずみ**（`DESIGN_compound_coverage.md` §19）。⚠ **名乗るのは3組（6分子）から**。2組では2単位のオリゴマーにしかならない |
| ~~**ナトリウムエトキシド**~~ | エタノール ＋ Na | **v941 で登録ずみ**（同 §19）。`-ONa` を線1本で書く既存の流儀そのまま |
| ε-カプロラクタム | （ナイロン6を入れるなら） | §11.6 |
| デンプン／セルロースの繰り返し単位 | （多糖の加水分解を入れるなら） | §11.6。**v942 でいったん見送った**。`hydrolysis_glycoside` は実際に発火する（試作したグルカンで候補2件）が、両端を R で止める規約のせいで**切った断片が両方とも「（名称未登録）」**になる ＝ 名前の無さが1手先に移るだけ。α/β の描き分け自体は可能（実測は `DESIGN_compound_coverage.md` §19.4） |

---

## 12. 「直近の反応」は1つの文脈として扱う（v1423・2026-08-20 ユーザーの実機レビュー）

ユーザーの言葉（原文・2件）:

> 試薬を作用させた後、
> 反応の前後を見る
> この反応の機構を見る
> が、生成物に対するボタンの下に区別なく並んでいるのがわかりづらい

> この反応の機構を見る、に進むと、反応前に戻す、ができなくなる
> これについては、設計がどうあるべきか

### 12.1 割る軸（★ここが設計の芯）

最初に浮かぶのは「**分子を変えるか、変えないか**」で割る案だが、**それは採らない**。
ユーザー自身が観点を言い直している:

> 分子を変えるか、変えないか、という観点よりも、
> **1つ前の物質を変化させたという文脈の続きかどうか**、という観点で考えていました

**この言い直しのほうが正しい。** 「変えるか変えないか」で割ると、

| 札 | 分子を変えるか | 文脈 |
|---|---|---|
| 🔍 反応の前後を見る | 変えない | **直近の反応** |
| ⚗ この反応の機構を見る | 変えない | **直近の反応** |
| ↩ 反応前に戻す | **変える** | **直近の反応** |

—— **「↩ 反応前に戻す」だけが反対側へ行ってしまう**。3つとも指しているのは
「いましがた起きた1つの反応」で、**実装上もすべて `reactor.lastReaction` を見ている**
＝ 同時に現れて同時に消えるべきもの。**反応カードは「次の反応」なので別のまとまり。**

### 12.2 画面の割り方（`reactor.refresh()`）

`#reaction-actions` を **2つの節**（`.rx-section` ＋ `.rx-section-head`）に割る。
文言は定数1つ（`RX_SECTION_NEXT` / `RX_SECTION_LAST` / `RX_UNDO_POINTER`）に閉じ込め、
テストと実装が同じものを見る（`PARTNER_HINTS_SUMMARY` と同じ約束）。

| 節 | 見出し | 中身 |
|---|---|---|
| ① | **この分子にできること** | 反応カード（`REACTION_RULES`）＋「もう1つ分子が要る反応」の案内（§9・v1420） |
| ② | **いま起きた反応（〜）** | 🔍 反応の前後を見る ／ ⚗ この反応の機構を見る ／ 「↩ 反応前に戻す」の**在り処の案内** |

- 見出しには**反応名**を入れる（「いま起きた反応（分子内脱水）」）＝ 何の続きかが節の頭で読める
- 中身が1つも無い節は**見出しごと出さない**（空の見出しは「ここに何か出るはず」と読ませる）
- ⚠ **「↩ 反応前に戻す」のボタンをここに増やさない。** 実体は帯（`#ws-free`）に1つだけで、
  **モーダルを閉じても押せる**のが v1409 の要点。同じ出口を2か所に置くとその要点がぼやける。
  節②からは**在り処を指すだけ**にし、しかも `syncUndoButton()` が
  **実際に札を出しているときだけ**言う（無い出口を名指ししない ＝ RX39 と同じ約束）

### 12.3 機構を見にいっても文脈は終わらない（バグの正体）

**症状**: 「⚗ この反応の機構を見る」へ進むと、戻ってきても「↩ 反応前に戻す」が二度と出ない。

**原因**（実測で追認した経路）:

```
reactor.jumpToMechanism()
  → game.setMode('learn')
    → 「自由モードを離れるときの掃除」（game.js）が reactor.exitCompare() を呼ぶ
      → lastReaction = null   ← 記録ごと捨てていた
```

⚠ **分子は失われていない。** 機構ビューア（`reaction.js`）は `borrowCanvas()` で
答案・履歴・視野を退避し、`exit()` の `returnCanvas()` でそのまま戻す。
**捨てられていたのは記録（`beforeState` という文字列）だけ** ＝
必要があってそうしていたのではなく、**全消去用の掃除が機構ジャンプにも効いていただけ**。

**直し方**: `exitCompare()` を「**閉じる**」と「**捨てる**」に割る。

| いつ | 呼ぶもの | 記録 |
|---|---|---|
| モード離脱（`setMode`）・機構ジャンプ | `closeCompare()` | **残す**（文脈の続き） |
| 全消去（`refresh()` が空のキャンバスを見た） | `discardLastReaction()` | 捨てる |
| ↩ 反応前に戻す（`undoLastReaction()`） | `discardLastReaction()` | 捨てる |

※ `exitCompare()` は `discardLastReaction()` へ**改名**した（名前が「閉じる」に読めるまま
「捨てる」を意味していたのが、そもそもの取り違えの温床だった）。

### 12.4 安全は既存の門番が担保する（弱めない）

記録を残すようにしても「押した瞬間にその後の作図が黙って消える」が復活しないのは、
**`syncUndoButton()` が「いまのキャンバスのトポロジーが `lastReaction.after` と一致するか」**で
札を出し入れしているから（v1409）。帰ってきた図が違えば札は出ない。**この門番は弱めない。**

同じ理由で、`reaction.js` の `enter()` が呼ぶ `syncUndoButton()`（ビューアの絵が出ているあいだ
札を引っ込める）も**残す** —— 指す先の分子は退避されて画面に無い。
借りたキャンバスは空なのでトポロジーが一致せず、**引っ込めは自動的に効く**。
帰りは `exit()` → `returnCanvas()` → `updateDrawing()` → `refresh()` → `syncUndoButton()` で
そのまま復活する（新しい手当ては1つも要らなかった）。

### 12.5 テスト（RX40〜RX42）

- **RX40**: 反応の前は節①だけ／反応の後は2節に割れ、**「🔍 反応の前後を見る」が
  反応カードと同じ節に入っていない**（`.rx-section` の同一性で見る）。
  節②は「↩ 反応前に戻す」の在り処を**指すだけ**でボタンを二重に置かない
- **RX41**: 機構を見にいって戻ると札が**また出る**。押せば本当に反応前へ戻る
  （札が出るだけの空振りにしない）。ビューア中は引っ込んだまま
- **RX42（否定対照）**: 文脈が切れているときは出さない ——
  ①「全消去 → 学習 → 自由」／②「反応のあと1原子描き足してから機構へ行って戻る」。
  ②は `syncUndoButton()` の門番を外すと赤くなる。
  「描き足しを戻せば札も戻る」まで見て、②が「常に出さない」で緑になっていないことを確かめる
- 既存の **RX1・RX2** は「モード離脱で記録が破棄される」を主張していたので**書き換えた**
  （破棄の主張は全消去の側へ移した）

---

## 13. 反応は「いま見ている分子」のものだけを出す（v1429・2026-08-20 ユーザーの実機報告）

ユーザーの言葉（原文）:

> **ブタン酸とエチルメチルケトンを２つ並べた状態で、ブタン酸の反応を見ると
> ヨードホルム反応が表示され、ボタンを押すとケトンが反応します。**
> 2分子以上ある状態での可能な反応が混ざっています。選択した分子の反応のみ表示するのがよいでしょう

**表示だけの問題ではない**（押すと本当に別の分子が反応した）。

### 13.1 原因（実測で追認した）

`reactor.siteFilter()` の1行目、**選択が空なら素通し**:

```js
if (!selSets.length) return true;   // ← ここ
```

`selectedMoleculeSets()` は「🎯 反応させる分子を選ぶ」で**明示的に選んだ**集合で、
**分子モーダルを開いただけでは選択にならない**。したがって `selSets.length === 0` のまま
`siteAllowed` が全部 true を返し、**キャンバス上のすべての分子の箇所**が一覧に並んでいた。

⚠ **情報は既にあった。** `openMoleculeModal(atomId)` はタップされた原子を受け取り
`setFocusedMolecule()` を通す。`moleculeModalPart()` は見出し（`#mm-name`）と
タブ（`#mm-tabs`）が指す1分子をすでに返している —— それを絞り込みに使っていなかっただけ。

### 13.2 「関わる」の定義（★絞りすぎない）

**箇所（site）の原子が、見ている分子の原子を1つでも含むこと。** ＝ 選択が1つのときと同じ条件。

⚠ **「見ている分子の中で完結する反応」に絞ってはいけない。** エステル化のように
2分子に跨る箇所は**片側が見ている分子なら残す** —— 消すと v1420 で作った
「＋ エタノール を呼び出す → エステル化」の導線がまるごと死ぬ。
相手の呼び出しの試算（`findPartnerHints` の `baseIds`）も同じ範囲を土台にする
（土台を全体のままにすると「隣のケトンに相手を足す案内」が生えて混ざりが残る）。

| 選択の数 | 通す箇所 | 変えたか |
|---|---|---|
| 0 | **見ている分子の原子を含む箇所**（キャンバスに2分子以上あるときだけ） | **★これが今回** |
| 1 | その分子の原子を含む箇所 | 変えない |
| 2以上 | 選択の中で完結し、2つ以上の選択分子に跨る | 変えない |

**選択があれば選択が勝つ**（`scope = selSets.length ? allSel : focus`）。
「🎯 反応させる分子を選ぶ」は式の左右を決める道具なので、上書きされては困る。

### 13.3 判定は1か所（`siteFilter()`）のまま

同じ絞り込みを通る口を数え直した。**新しい判定は1つも書いていない**:

| 口 | 通し方 |
|---|---|
| 反応カード（`refresh()`） | `siteAllowed` |
| 試薬の瓶（`reagentHits()`） | `siteAllowed` |
| **呈色・検出（`runDetection()`）** | `atomAllowed`（★同じ混ざり方があった。下記） |
| 瓶の空振り（`explainReagentMiss()`） | `scope` を `cachedPartnerHints` の土台に |
| 条件の選択肢（`renderConditionChoice()`） | 同上 |
| 相手の呼び出し（`renderPartnerHints()`） | 同上 |
| 呼び出しの検算（`runPartnerHint()`） | `siteAllowed`（選択済みなので既存どおり） |

⚠ **呈色にも同じ混ざり方があった**（今回いっしょに直した）。
ブタン酸とエチルメチルケトンを並べて**ケトンを見ている**とき、
炭酸水素ナトリウムが**陽性**（隣のカルボン酸を数えていた）。
`runDetection` の絞り込みが `if (selSets.length)` の中にあったのが原因で、根は同じ。

### 13.4 「全体で開く」は存在しない

`openMoleculeModal()` を引数なしで呼ぶ道（右パネルのボタン・`?go=molecule`）はあるが、
**着地するのは必ず1分子**（`moleculeModalPart()` が `focusedMolecule` →
番号付きの先頭 → `parts[0]` の順で1つに決める）。見出しに名前と分子式が出て、
タブで切り替える画面なので、**「全体」という状態はもともと無い**。
したがって絞り込みも常に1分子ぶんでよく、例外を作らない。

**右パネルの「⚗ 反応させる・調べる（反応 N件）」も同じ数**になる
（`executableCount` は `refresh()` が数えるので自動的にそろう）。
ここだけ全体の合計を出すと、押して開いた画面と数が食い違う ＝ 同じ混ざり方が札に残る。

### 13.5 文言（黙って減らさない）

⚠ 隣の分子の反応を落とすだけだと「**この分子には反応が無い**」と読まれる。
`#reaction-selection`（選択中の案内と同じ場所）に、何で絞ったかと隣を見る手を出す:

> いま見ている「酪酸（ブタン酸）」が関わる反応だけを出しています（ほかの分子の反応は、上のタブか図の分子名から切り替えると出ます）。

文言は定数 `RX_SCOPE_NOTE(name)` 1つ（テストと実装が同じものを見る）。
名前は `moleculeModalPart()` から引く ＝ 見出し（`#mm-name`）と必ず同じ分子を名乗る。
**分子が1つのときは従来どおり無言**（`moleculeModalAtomIds()` が `null` を返す）。

⚠ **1文にした**（375px / 1280px の実物を見て決めた）。この文の真上には
見出しの名前・タブ・「分析中: ① 〜」で**同じ分子名が既に3回**出ている。
「いま見ているのは〜です。」を独立した文にすると4回目の名乗りが1行まるごと増えるので、
名前は絞り込みの説明の中に埋めた。

### 13.6 性能

`refresh()` は作図のたびに走る（v1420 で「1原子置くたびに5倍」の回帰を出した前科がある）。
同じページの上で `moleculeModalAtomIds()` を `() => null` に差し替えた「前」と
交互に3回ずつ測った中央値（100回の平均・chromium）:

| 画面 | 前 | 後 |
|---|---|---|
| 1分子（エタノール） | 0.465 ms | 0.544 ms |
| 2分子（酪酸＋MEK）・モーダル開 | 0.743 ms | 0.753 ms |
| 2分子・モーダル閉 | 0.706 ms | 0.734 ms |
| 5分子・モーダル開 | 3.607 ms | **3.562 ms** |

足したのは `splitMolecules()` 1回ぶん（+0.08ms）。分子が増えるほど**むしろ速くなる**のは、
相手の呼び出しの試算（候補5件 × 全ルールの detect）の土台が1分子に縮むため。

### 13.7 テスト（RX43・RX44）

- **RX43**: ユーザーの再現そのもの。ブタン酸を開くとヨードホルムが出ない／
  **瓶を押しても隣のケトンが反応しない**（表示だけでなく実行も塞がっている）／
  自身の中和は残る／エステル化への道は残る。
  ★陰性対照3つ ——「ケトンを開けばヨードホルムは出る」「1分子のときは変わらない」
  「1分子のときは断りの文も出ない」
- **RX44（否定対照）**: 2分子に跨るエステル化の箇所は**どちら側から見ても**残る／
  「🎯 反応させる分子を選ぶ」が**勝つ**（既存の絞り込みと案内文の回帰）／
  呈色も同じ絞り込みを通る（ケトンを見れば NaHCO₃ は陰性・酸を見れば陽性）
- 既存の **RX17** は「選択なしで6箇所」を主張していたので**4箇所に書き換えた**
  （＝ 今回変えた既定そのもの。選択が2つ・3つのときの主張は1つも変えていない）。
  **RX34 ④** の「選ぶモードでないときは無言」も、2分子のときは断りが出るように書き換えた

---

## 14. 重合の生成物を一直線に伸ばす（v1436・2026-08-21 ユーザーの実機報告）

> **付加重合　２分子並べたら出ました**
> **１分子でも重合を出せるようにしたい　→　複数分子を横一線に並べ反応させる**
> **現状、エチレンの付加重合後が一直線にならないのでわかりにくい**

### 14.1 実測（何がどう折れていたか）

エチレンを3個呼び出して `addition_polymerization` を実行し、**主鎖の座標を数で出した**
（呼び出し直後の3分子は `C(379,300) C(421,300) / C(505,300) C(547,300) / C(631,300) C(673,300)`
＝ **完全な横一線**。つまり「元の並べ方が斜め」ではない）。

| 生成物 | 主鎖の折れ（180° でない頂点） | 主鎖の y のばらつき |
|---|---|---|
| ポリエチレン（単量体3個） | **6頂点中5つが 90°** | 84px |
| ポリエチレン（単量体6個） | **12頂点中11が 90°** | 210px |
| ポリ塩化ビニル（3個） | 1（末端の R だけ） | 42px |
| ポリアクリロニトリル（3個） | 1（末端の R だけ） | 42px |
| ポリアセチレン（3個） | 1（末端の R だけ） | 42px |
| 合成ゴム（ブタジエン3個） | 7 | 109px |
| ナイロン66（2組） | 3（うち1つは**単量体まるごと 90° 立っていた**） | 294px |
| PET（2組） | 11 | 161px |

ポリエチレンは `R(463,300) C(421,300) C(379,300) C(379,258) C(337,258) C(337,216) C(295,216) R(295,258)`
＝ **1単位ごとに1段のぼる階段**。単量体を増やすほど段が増える（6個で 210px 上がる）。

### 14.2 原因（a/b/c の切り分け）

依頼の3つの候補のうち、**(a) 重合のときの原子の置き方**だった。(b) 並べ方は上のとおり
完全な横一線で無実、(c) モーフィングは座標を戻すだけなので無関係（実行後の
`userMolecule` そのものを読んで確かめた）。中身は**2つ**ある:

**① 鎖が伸びる向きと、単量体を繋ぐ順が逆**（ポリエチレン特有・これが階段の正体）

`vinylBonds` は head（置換基の多い炭素）に次の tail を繋いで頭-尾にする。
**エチレンは左右が同じ**なので置換基では決まらず、`vinylBonds` の同数の場合分けが
「**x の小さいほうを head**」にしている（v377。原子IDが乱数なので座標で決めた）。
一方 `detect` は単量体を **x の昇順**で繋いでいた。鎖は `R-tail₀-head₀-tail₁-…` と
**tail から head の向き**に伸びるので、head が左にある＝鎖は**左へ**伸びたいのに、
繋ぐ順は左からになっていて、**次の単量体を置く先（右）は必ず自分の tail で塞がっている**。
`planAttachment` の既定の順は `右・上・下・左` なので、塞がった右をあきらめて**上**へ逃げる
＝ 単位ごとに1段のぼる。

塩化ビニル・アクリロニトリルが最初からまっすぐだったのは、たまたま
**置換基が右の炭素に付いていて head が右**になり、繋ぐ順と伸びる向きが一致していたため。
つまり**まっすぐだったほうが偶然**で、対称な単量体では必ず折れる。

**② まだ繋いでいない単量体を「邪魔者」として避けていた**（縮合重合で特に大きい）

`planAttachment` はキャンバス上の他の原子をすべて障害物として扱う。重合では
**横に並べた残りの単量体がそこにいる**ので、次の1個を置くときに残り全部を避ける。
ナイロン66 で最初のジアミンが**まるごと 90° 立って**下へ伸びていたのはこれ
（横に置くと右隣に駐まっているアジピン酸と当たるので、`canSpin90` の 90° 回転が先に通った）。
だが避けた相手は**この後どうせ動かして同じ鎖に繋ぐ**ので、避ける必要がそもそもない。

### 14.3 直した形（★作図規約の例外は増やしていない）

⚠ **CLAUDE.md の「例外は1つだけ（名称から呼び出した分子の C=C まわりの ±120°）」に触れていない。**
今回入れたのは**直交4方向のうちどれを先に試すか**の並べ替えだけで、
置く位置は従来どおり**直交の格子上**にしかない ―― 一直線の主鎖（0°/180°）も
垂れ下がる置換基（90°）も、**もともと直交作図の中にある形**である。
「まっすぐ」は新しい幾何ではなく、**同じ直交作図のうち折れないほうを選ぶ**というだけ。

1. **`chainDirection(mol, backId, fromId)`**（新設）… 主鎖の1つ内側から端へ向かう向きを
   直交4方向に丸めて返す。斜めに描かれていても軸へ丸めるので、返るのは必ず直交の向き
2. **`planAttachment(..., prefer)`**（引数を1つ追加）… `prefer` の向きを**最初に**試す。
   置けなければ従来の順（右・上・下・左）へ落ちるので、**置ける場所は1つも減らない**
3. **`attachR(mol, atomId, prefer)`** … 末端の R も同じ向きの規則で置く。
   R は「この先も続く」印なので、鎖の続きの位置に出ないと端だけ折れて見える
4. **`addition_polymerization.detect` の並べ替えを「鎖が伸びる向き」に合わせた** …
   単量体の `tail → head` の合計ベクトルで昇順を決める。head が左にある単量体
   （エチレン）では x の**降順**になり、鎖は左へ伸びながら、
   **画面の左にあった単量体が鎖の左端に来る**（並びと鎖の順の一致は保たれる）
5. **繋ぐ前の単量体を `ignoreIds` に入れた**（4つの重合ルールすべて） …
   「邪魔者」ではなく**この鎖の続き**だと `planAttachment` に伝える。
   最後の1個を置くときには全員が鎖の上に乗っているので、重なりは残らない

**ユーザーが手で描いた分子には一切触れていない。** 動かすのは
「重合ルールがいま繋ごうとしている単量体」だけで、`prefer` は候補の**順序**を変えるだけ。
検証（正準コード・同型判定）は座標を1つも読まないので、見た目だけが変わる。

### 14.4 直した後の実測

| 生成物 | 折れ | y のばらつき |
|---|---|---|
| ポリエチレン（3個） | **0** | **0px** |
| ポリエチレン（6個） | **0** | **0px** |
| ポリ塩化ビニル（3個） | **0** | **0px** |
| ポリアクリロニトリル（3個） | **0** | **0px** |
| ポリアセチレン（3個） | **0** | **0px** |
| ナイロン66（2組・3組） | **0** | **0px** |
| PET（2組） | 8（**すべてベンゼン環の 120°**） | 35px（環の高さ） |
| 合成ゴム（ブタジエン3個） | 6（**すべて C=C の 120°**） | 109px |

ポリエチレン3個は
`R(715,300) C(673,300) C(631,300) C(589,300) C(547,300) C(505,300) C(463,300) R(421,300)`
＝ **R から R まで y が1つも動かない**。

### 14.5 直していないもの（理由つき）

- **合成ゴム（1,4-付加重合）の 120° のジグザグは残す。** 折れの中身は
  **C=C まわりの ±120°**＝ CLAUDE.md がすでに認めている唯一の例外そのもので、
  ここを潰すと**シス/トランスが読めなくなる**（天然ゴムとグタペルカの描き分け・RX13）。
  単量体1つぶんで 36px 下がるのはこの 120° の帰結。**繋ぎ目と末端 R は直した**ので、
  折れは 7 → 6 に減っている
- **PET の 8 か所はベンゼン環の頂点**（パラ位で主鎖が環を通り抜ける）。環の作図は
  正六角形が正で、ここを直すのは「まっすぐ」ではなく「環を潰す」ことになる。
  主鎖の y のばらつきは 161px → 35px（＝環の高さそのもの）まで落ちた
- **スチレン・酢酸ビニルの付加重合は依然として
  「生成物を配置する空間がありません」で止まる**（**この修正の前からそうで、退行ではない**）。
  原因は別で、**置換基が鎖の伸びる先に描かれている**こと ―― 呼び出したスチレンは
  ベンゼン環が head 炭素の真横（鎖の続きの位置）にあるため、どの向きに置いても
  自分の環と当たる。直すには**置換基を主鎖に垂直へ倒す**作図が要り、
  `reshapeVinylAngles`（±120° の例外そのもの）と正面からぶつかる。別件として申し送る

---

## 15. 1分子からでも重合へ行ける入口（v1437・同じ実機報告）

> **付加重合　２分子並べたら出ました／１分子でも重合を出せるようにしたい**

### 15.1 何個呼ぶか（★決めたこと）

**自分を含めて3個**（＝ 呼び出すのは2個）。`SELF_PARTNER_UNITS = 3`。根拠は4つ、すべて実測:

1. **このアプリ自身の高分子の図が「3単位＋両端 R」の規約**（LB23・FG2）。実際
   アセチレンは**3個のときだけ**生成物が「ポリアセチレン」と名乗る
   （実測: 2個 →（未登録）、**3個 → ポリアセチレン**、4個 →（未登録））。
   ここを外すと、1分子から作った生成物が**自分のライブラリに載っていない図**になる
2. **2個では「くり返し」と「二量体」の区別が付かない。** 3個で初めて -A-A-A- と読める
3. **`MAX_REACTION_SELECTION` が 4。** 呼んだあと `runPartnerHint` は並べた分子を全部選ぶが、
   `siteFilter()` は「2つ以上選んだら箇所は選んだ分子の中に収まること」を要求するので、
   **5個並べると押せなくなる**（札が「押したらそこまで連れて行く」約束を破る）。
   3個は上限4の内側で、伸ばす余地も1つ残る
4. **重さ・大きさの心配は無い**（実測）:
   - 重合の実行 … 0.4ms（6個でも 0.8ms）
   - 鎖の幅 … 294px（6個なら 546px）
   - 画面上の結合1本 … デスクトップ 1280px で 66.3px、**375px 幅でも 29px**
     ＝ 「見えた」の床 `SUMMON_MIN_BOND_PX`（24px）を上回る
   - `findPartnerHints` の総時間 … 追加前後で 3.5〜12.4ms（分子による差のほうが大きく、
     この入口ぶんの増分は測定誤差に埋もれる）
   - `summonMolecule` が横一線を保てるのは **16個まで**（17個目で `SUMMON_ROW_WIDTH`＝2400px に
     当たって下の段へ折り返す）。3個は遠く手前

### 15.2 作り（★新しい仕組みを作らない）

v1424（濃硫酸の 130〜140℃・§11）とまったく同じ形 ―― **相手が要る反応を、相手を並べていない人に見せる**。
v1420 の3点セットをそのまま使い、**変えたのは呼ばれ方だけ**:

- `findPartnerHints` … 末尾で `findSelfPartnerHints` を呼ぶ。
  既存の `PARTNER_CANDIDATES` の総当たりは「**別の化合物を1つ**足したら通るか」しか試さないので、
  「**自分と同じ分子をもう何個か**」は原理的に落ちてこない。そこだけを足す
- `makePartnerHintButton` … `h.count > 1` のとき札に個数を書く
  （`＋ エチレン（エテン） をもう2つ呼び出す → 付加重合…`）。目印は `data-count`
- `runPartnerHint` … 呼び出しを `h.count` 回まわす。①呼べたか ②箇所が生えたか
  ③選んだ後も押せるか を**実測してから**実行する段取りは1つも変えていない
- `selectPartnerPair` … 呼び出した側が2つになるので、**並べた全部を選ぶ**ように一般化
  （`count` が 1 のときは従来と1文字も変わらない式になる）

⚠ **既存の重合ルールは1文字も変えていない。**
`addition_polymerization.detect` の `if (list.length < 2) return;`
（＝「横一列に単量体を並べた状態から重合するところを見たい」という過去のユーザー要望の実装）は**そのまま**。
足したのは入口だけで、自分で並べた人の道は元のまま通る。

### 15.3 出す条件（絞り込み）

- 対象は **`SELF_PARTNER_RULES` の3つ**（付加重合・アセチレンの付加重合・1,4-付加重合）。
  **縮合重合は入れない** ―― 相手が別の2価単量体で、しかも2組（4分子）要るので
  「自分をもう何個か」では説明が付かない（`condensation_polymer_info` が説明を持っている）
- **名前で引ける分子だけ**（`summonMolecule` は名前しか受け取らない）。
  名乗れない分子に「呼び出す」と書くと札が約束を破る
- **もう並べてある人には出さない**（`rule.detect(mol)` が既に箇所を返すなら黙る）。
  すでに押せる反応を「さらに2つ呼びなさい」と案内するのは案内ではない
- 見出しの文言を `もう1つ分子が要る反応` → **`相手の分子が要る反応`** に変えた。
  数を決め打つと、札の中身（「もう2つ呼び出す」）と食い違う

### 15.4 実測（押したあと）

エチレン1分子から札を押すと、2個が横一線に呼ばれ（`C(379,300) C(421,300) / C(505,300)
C(547,300) / C(631,300) C(673,300)` ＝ y がすべて 300）、そのまま重合して
`R(421,300) C(463,300) … C(673,300) R(715,300)` ＝ **折れ0・y のばらつき 0px**（§14 の直しが効く）。
アセチレン1分子から押すと生成物は「ポリアセチレン」と名乗る。

### 15.5 テスト（PM5・PM6）

- **PM5**: 3つの重合すべてで1分子から札が出る／札が呼び出す個数を言う／
  呼び出した結果が横一線（y が1種類・折り返し幅に届かない）／押すと重合まで進み、
  鎖は折れ0・y 0px／モーダルは閉じる／アセチレンからは「ポリアセチレン」と名乗る／
  **並べる数が `MAX_REACTION_SELECTION` を超えていない**
- **PM6（否定対照）**: 自分で2つ並べた人には出さない（＝ 既存の道はそのまま）／
  重合できない分子には出さない／既存の札（別の化合物を呼ぶ道）の件数も文面も不変／
  名前で引けない分子には出さない／**重合ルールの `detect` は「2つ以上」の約束のまま**

---

## 16. 2段階モーフィングの①で止まっていることを画面に残す（v1454・2026-08-25 ユーザーの実機報告）

> 「グルコースの 鎖状 → 環化 で**環になっていない**」

### 16.1 実測（何が起きていたか）

`?summon=D-グルコース（鎖状）` → `⚗ 反応させる・調べる` → `環化 → β-D-グルコース` を押した直後の状態:

| 見るもの | 実測 | 判定 |
|---|---|---|
| 分子データ（`game.userMolecule`） | 重原子12・重原子間の結合12・**環1つ（6員）**・`lookupCompoundName` は **β-D-グルコース（β-D-グルコピラノース）**・`haworthFlipPlan` も通る | ✅ 正しい |
| **環の形** | 環結合の長さ `[66,85,60,85,66,110]` ＝ **登録の β-D-グルコピラノースと1pxも違わない**（平行移動を除いて全原子一致） | ✅ 正しい |
| **画面** | **①の静止画のまま**（`renderStaticSnapshotWithHydrogens`）＝ C1–環O の結合がまだ無い**開いた絵**に、名前だけ「β-D-グルコピラノース」が付く | ❌ ここ |
| 止まっている断り | **9秒で消えるトースト1つだけ**。消えたあとの画面には理由がどこにも書いていない | ❌ ここ |

★ **つまり「環になっていない」は正しい観察**で、直すのは**図でも化学でもなく、止まっていることの見せ方**。

⚠ **`standaloneDrawingOf` / `redrawProductsAsStandalone` で描き直す手当ては要らない**
（②の着地はすでに登録の図そのもの。上の表の2行目。SB4 ③ が数で押さえる）。

### 16.2 直したこと（**止め方そのものは変えない**）

止めるのは P12-7 M2f のユーザー要望（「①で止めてじっくり観察できる」）なので**残す**。
足したのは2つだけ:

1. **キャンバスの常設バッジ（`#canvas-mode-badge`）に2つめのモードを足した**（`morph-pause`）。
   器は v1416 で「将来ほかのモードにも足せる形」に作ってあったので、
   `canvasModeBadgeSpec()` の分岐が1つ増えただけ（CSS も DOM も追加なし）。
   - 見出し `⏸ 反応の途中で止めています` ／ 数 `① / ②`
   - 説明 `いまは①環の形に折りたたんだ状態です（水素の数と位置もここで確認できます）。続きを見ると②結合ができて環が閉じます。画面をタップしても続きが見られます。`
   - ⚠ 文言に「編集できません」とは書かない（v1416 と同じ約束）
2. **押しどころ `続きを見る`**。⚠ **進み方を2つにしない** ——
   キャンバスのタップと同じ `reactor.advanceMorph()` を通す。

⚠ **バッジは `updateDrawing()` の先頭でそろえるのが原則**だが、止まっているあいだの画面は
`renderStaticSnapshotWithHydrogens` が直に描いていて `updateDrawing()` を通らない。
そこで**止めたとき**と**進めたとき**の2か所からも `syncCanvasModeBadge()` を呼ぶ
（進めたときに呼ぶのは、`advanceMorph` の `updateDrawing()` が 800ms の再生後で、
待つとバッジだけ 0.8秒 残るため）。

⚠ 文言は**バッジ側（game.js）の1か所**に持つ。reactor は `morphPauseInfo()` で
「いま何の状態か（`now`）／続きに何が起きるか（`next`）」の材料だけ渡す。

### 16.3 テスト（SB4）

止まったことを**待ってから**見る（⚠ `execute()` が返った時点ではまだ第1段階の再生中で
`_morphPause` は null ＝ 待たずに書くと中身が1行も走らない空振りの緑になる）。
- ① バッジが出る・`data-mode="morph-pause"`・続きに何が起きるか書いてある・
  「編集できません」と書いていない・`続きを見る` が 32px の床を割らない
- ② `続きを見る` で進み、バッジが消える（＝ 行き止まりではない）
- ③ ★ 着地が**登録の図と平行移動を除いて完全一致**（＝ 描き直す手当ては要らないことの根拠）
- ⚠ **否定対照**: `canvasModeBadgeSpec` を null に差し替える（＝ v1453 までの実装）と
  止まった画面に断りが1つも無くなる ＝ 申し立ての画面が再現する
