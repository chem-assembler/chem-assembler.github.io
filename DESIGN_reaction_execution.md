# 反応実行モード 設計書（P9-1）

ユーザーが作図（または名称から呼び出し）した分子に対して「この分子の反応」を提示し、
反応を選ぶと生成物へ変化するモード。既存の反応機構ビューア（reaction.js＝登録済みデータの
観賞用）と異なり、**ユーザー自身の分子を対象に反応を実行する**のが本質。
実装前の合意用ドキュメントであり、実装の進行に合わせて更新する。

---

## 1. 体験イメージ

1. エタノールを作図（または名称呼び出しで配置）
2. 右パネルの「⚗ この分子の反応」カードに、分子のプロパティ（例: **1級アルコール**）と
   実行可能な反応（例: **酸化 → アセトアルデヒド**、**分子内脱水 → エテン**、
   **分子間脱水 → ジエチルエーテル**〔要2分子〕、**エステル化**〔要カルボン酸〕）が並ぶ
3. 「酸化」を選ぶと、キャンバス上の分子がアニメーションでアセトアルデヒドに変化。
   キャプションで「1級アルコールの酸化。さらに酸化すると酢酸になる」と解説
4. 続けて「酸化」を選べば酢酸へ（連鎖実行）。Undoでいつでも巻き戻せる
5. 反応表示は2段階を切替可能:
   - **簡易版（既定）**: 反応前 → 反応後のモーフィング（結合の組み換えをフェードで表現）
   - **機構版**: 対応する反応機構データ（reactions.json）がある場合、巻矢印アニメーションを再生

## 2. スコープ（対象反応・高校化学の定番）

| 分類 | ルール | 必要条件 | 生成物 |
|---|---|---|---|
| 酸化 | 1級アルコール→アルデヒド | C(-OH)にH2つ | R-CHO + (H₂O相当は省略) |
| 酸化 | アルデヒド→カルボン酸 | -CHO | R-COOH |
| 酸化 | 2級アルコール→ケトン | C(-OH)にH1つ | R-CO-R' |
| （案内） | 3級アルコールは酸化されにくい | C(-OH)にH0 | 実行不可の解説を表示 |
| 脱離 | 分子内脱水 | C-C(-OH)、隣接CにH | アルケン + H₂O |
| 縮合 | 分子間脱水（エーテル生成） | アルコール×2 | R-O-R' + H₂O |
| 縮合 | エステル化 | -COOH と -OH（別分子） | エステル + H₂O |
| 加水分解 | けん化（簡易） | エステル結合 | カルボン酸(塩) + アルコール |
| 付加 | X₂・HX・H₂O・H₂ の付加 | C=C / C≡C | 対応する付加体 |
| 置換 | ベンゼン環の置換（ニトロ化等） | 芳香環のC-H | 置換体（既存機構と連携） |

- 試薬（Br₂・HBr・H₂O・H₂・濃硫酸…）は**内蔵の試薬パレット**から選ぶ（毎回作図させない）。
  二分子反応（エステル化など）は、キャンバス上の別分子を相手として選択する
- マルコフニコフ則など位置選択性は M1〜M3 では扱わず、対称な付加から始める。
  非対称付加は「主生成物のみ・解説付き」で M5 以降に検討

## 3. アーキテクチャ

### 3.1 ファイル構成

- **reactor.js（新設）**: 反応ルールベース＋実行エンジン＋UIカード。DOM操作はここに集約
- **chemistry.js**: 官能基パターン検出の純粋関数を追加（DOM非依存を維持）
  - `findFunctionalGroups(mol)` → `[{type: 'primary_alcohol'|'aldehyde'|'carboxyl'|'ester'|'cc_double'|..., atoms: [...]}]`
  - 既存の isSp3Carbon / 橋検出 / findAromaticBondKeys を流用
- **game.js**: モード結線と分子呼び出しUIのみ（既存パターン踏襲）

### 3.2 反応ルールの表現

```js
{
  id: 'oxidize_primary_alcohol',
  name: '酸化（1級アルコール → アルデヒド）',
  category: '酸化',
  reagentLabel: '酸化剤 [O]',
  // 適用可否: 対象分子（と相手分子）から適用箇所を列挙して返す
  detect(mol) { return sites; },       // 例: [-OH炭素のid, ...]
  // 実行: 分子グラフを書き換える（座標は最小変更の原則）
  apply(mol, site) { ...; return { caption, byproducts: ['H₂O'] }; },
  mechanismName: null,                  // reactions.json に対応機構があれば名前
}
```

- ルールは配列で登録し、「この分子の反応」カードは `detect` が非空のものだけを列挙する
- `apply` は**元の原子座標をできる限り動かさない**（開発方針「環⇔鎖の切替で既存原子を
  動かさない」と同じ思想）。脱離する原子（H₂Oなど）は分子の外側へ平行移動して残す
  （反応機構データと同じ「原子は消さない」原則）か、副生成物として小さく表示する

### 3.3 実行と履歴

- 反応実行は通常の編集と同じく `saveState()` を積んでから行う（**Undo/Redoがそのまま効く**）
- 実行後は updateDrawing → 名称判定カードが自動更新され、生成物名が表示される
  （例: 酸化後に「アセトアルデヒド」）。この既存機能が答え合わせを兼ねる

### 3.4 アニメーション

- **簡易版**: 反応前後の2状態を reaction.js の状態補間（原子は線形移動・結合はフェード）で
  1秒モーフィング。reaction.js の補間ロジックを関数として切り出して共用する
- **機構版**: `mechanismName` があれば反応機構ビューアをその機構で開く（登録データの再生）。
  ユーザー分子への巻矢印の自動付与は将来課題（M5以降）とし、当面は
  「あなたの分子で起きたことを、代表例の機構で見る」体験とする

## 4. 分子の名称呼び出し（ライブラリから配置）

- 「この分子の反応」カードに検索ボックス（datalist）を設置。compounds.json + stages.json の
  名称ライブラリから選ぶと、キャンバスの空き領域（既存分子と重ならない位置）に配置する
- 配置は既存の createTargetFromData → 平行移動 → userMolecule へマージ。1操作=1 Undo
- これにより「エタノールを呼び出す → 酢酸を呼び出す → エステル化」が作図なしで試せる

## 5. UI

- 右パネルに新カード **「⚗ この分子の反応」**（化合物名カードの直下）:
  - 上段: プロパティ表示（P9-3連携）例:「1級アルコール／分子式 C₂H₆O」
  - 中段: 適用可能な反応のボタン列（分類アイコン＋名前＋生成物プレビュー名）
  - 下段: 分子の名称呼び出し検索ボックス
- 分子が複数あるときは、反応ボタン押下時に対象分子（連結成分）をハイライトで選択
- 表示切替（簡易版⇔機構版）はカード内のトグル。既定は簡易版
- パズルモードの正解判定とは独立（ステージのお題とは無関係に遊べる砂場）。
  反応実行モード中も通常編集は可能（実行はあくまで1回の編集操作）

## 6. マイルストーン

- **M1**: 名称呼び出し（検索→空き領域に配置）＋「この分子の反応」カードの骨組み
  （プロパティ表示: アルコールの級・アルデヒド・カルボン酸・C=C の検出まで）
- **M2**: 単分子反応（1級/2級アルコール酸化・アルデヒド酸化・分子内脱水）＋簡易モーフィング
- **M3**: 二分子反応（エステル化・分子間脱水）＋相手分子の選択UI
- **M4**: 試薬パレットと付加反応（Br₂/HBr/H₂O/H₂）・けん化。機構版切替（該当機構へのジャンプ）
- **M5**: 置換（芳香族）・非対称付加の主生成物・ユーザー分子への巻矢印自動付与の検討

## 7. リスク・未決事項

- **生成物レイアウト**: 結合組み換えで原子が重なる場合の退避方向の決め方
  （まずは既存の MIN_CLEARANCE 検査＋外向き平行移動で対応し、監査ページで検証）
- **「酸化」の表現**: 酸化剤は [O] として抽象化（高校教科書の書き方）。
  機構は扱わない（DEVELOPMENT.md P8-7 の保留判断と整合）
- **複数適用箇所**: ジオールなど適用箇所が複数ある場合は、箇所をハイライトして選択させる
- **級の判定と教育**: プロパティ表示から P9-3 の学習リンク（異性体列挙など）へ将来接続する

---

## 8. P12-5: 反応の前後比較と機構ジャンプ・簡易モーフィング（2026-07-24 設計）

ユーザー要望: **反応を実行すると生成物に置き換わるだけで、前後の比較ができず、
機構もわからない**。3.4節の構想（簡易モーフィング・機構版）を、確実に届く順で2段階に分けて実装する。

### 8.1 第1弾: 前後比較ビュー＋機構ジャンプ

**直近反応の記録**
- `Reactor.execute()` が成功したとき `this.lastReaction = { ruleId, label, before, after }` を保持。
  `before`/`after` は対象分子の **target形式スナップショット（独立コピー・原子ID付き）**。
  反応対象の連結成分だけでなく**キャンバス全体**を写す（分子間反応・副生成物も比較に含める）。
- 保持は直近1件のみ。**次の反応実行で上書き、全消去・モード離脱で破棄**。
  Undo とは独立（↩で戻しても記録は残り、比較は見られる）。

**前後比較ビュー**
- 反応実行後、「⚗ この分子の反応」カードに **「🔍 反応の前後を見る」** ボタンを表示
  （`lastReaction` があるときのみ）。
- 押すとキャンバス領域に **`#rx-compare-overlay`** を重ねて表示（異性体練習の
  `#ip-review-overlay` と同じ流儀。CSSは共通クラス `.canvas-overlay` に括り出して共用）。
  - 「反応前」「反応後」の2図を並置。ヘッダに反応名（例: 分子内脱水）。
  - 図サイズ切替（小/中/大）は `IP_REVIEW_SCALES` を共用。
  - 「← 描画に戻る」で閉じる。図をクリックしても閉じる（異性体練習と同じ操作感）。
- **差分ハイライト**（原子IDの対応付けで機械的に判定。reactor は既存原子のIDを保持し、
  追加原子には新IDを振るため、対応付けは ID 一致だけで取れる）:
  - 消えた結合・次数が下がった結合 → **反応前**の図でオレンジ点線
  - 生成した結合・次数が上がった結合 → **反応後**の図でシアン強調
  - 脱離した原子（前にあって後に無い）→ 反応前の図でオレンジ枠
  - 付加した原子（後にだけある）→ 反応後の図で緑枠
  - 要約文の自動生成（「H₂Oが脱離」等）は第1弾ではやらない（ラベルは反応名のみ）

**機構ジャンプ**
- **reactions.json の各機構に安定ID `id` を新設**（現状は name のみで照合が壊れやすい。
  既存UIは name 表示のままなので互換性に影響なし）。
- reactor の各ルール定義に `mechanismId` を追加。対応表（初期値）:

  | reactor ルール | mechanismId（reactions.json） |
  |---|---|
  | add_br2 | ethene_br2（エテンへの臭素の付加） |
  | add_water | ethene_h2o（エテンへの水付加） |
  | dehydration_intra | ethanol_e1（エタノールの分子内脱水） |
  | esterification | esterification（酢酸とエタノールのエステル化） |
  | hydrolysis_ester | saponification（酢酸エチルのけん化）※加水分解の代表として |
  | aromatic_nitration | benzene_nitration |
  | aromatic_sulfonation | benzene_sulfonation |
  | aromatic_halogenation | benzene_chlorination |
  | oxidize_* / add_h2 / add_hbr / dehydration_inter / acetylation_anhydride | なし（P12-4 の機構追加と連動して随時埋める） |

- `mechanismId` のあるルールは、比較オーバーレイと反応カードに
  **「⚗ この反応の機構を見る（代表例）」** ボタンを表示。押すと
  `setMode('learn')` → 反応機構ビューアで該当機構を選択・再生開始。
  注記を添える: **「※ あなたの分子そのものではなく、代表例の分子で機構を再生します」**
  （ユーザー分子への巻矢印自動付与は M5 の将来課題のまま）。
- 分子はモードをまたいで保持される（P10仕様）ので、見終えたら自由モードに戻れば続きから編集できる。

**テスト（第1弾）**
- RX1: エタノール→「酸化」実行→前後比較を開くと2図が描画され、
  反応前=エタノール・反応後=アセトアルデヒドのトポロジーで、差分ハイライトが存在する
- RX2: mechanismId のあるルール実行→「機構を見る」→ learnモードに切替わり、
  ビューアが対応機構をロードしている
- RX3: 全ルールの mechanismId が reactions.json の id に実在する（データ整合の機械検証）。
  reactions.json の id 重複なしも検証

### 8.2 第2弾: 簡易モーフィング（実行時アニメ）

- `execute()` の適用後、生成物への切替を**約1秒のモーフィング**で見せる:
  共通原子（ID一致）は座標を線形補間、消える原子・結合はフェードアウト、
  現れる原子・結合はフェードイン、次数変化はクロスフェード。
- 実装: reaction.js の状態補間・描画ロジックから共通関数を切り出して共用
  （3.4節の方針どおり）。アニメは一時レイヤに描き、完了時に通常描画へ戻す。
- **分子データは実行時点で即確定**させ、アニメは表示のみ（検証・Undo・監査に一切影響しない
  ＝「検証はトポロジーのみ」の原則に整合）。
- 再生中のキャンバス入力は無視（最長1秒）。タップでスキップ即完了。
  `prefers-reduced-motion` 環境ではアニメせず即切替。
- テスト: 補間関数の純関数テスト（t=0で反応前・t=1で反応後に一致）＋
  実行後の最終分子がアニメ無し時と同一（分子式・正準コード）。見た目は手動確認。

### 8.3 実装分担（CLAUDE.md のモデル運用）

- 設計: **Fable**（本節。ユーザーレビュー済みの方針を反映）
- 第1弾: **Opus** — id付与＋対応表（RX3で機械検証）、スナップショット、オーバーレイUI流用。1機能=1コミット
- 第2弾: **Opus で開始** — 補間の切り出しで reaction.js の回帰（テストD系）を必ず確認。
  イベント・描画タイミングで2〜3回詰まったら Fable へ
- 検収: **Fable** で差分レビュー＋監査ページ実行

---

## 9. 複数分子の反応（Gemini レビュー項目15・2026-08-03 設計）

レビューの指摘は3つ束ねてある: **選択分子のハイライト**・**複数選択への対応（2価エステル等）**・
**整列の最適化**。実装前に実物で測った結果、後ろ2つは「機能が無い」のではなく
**あるのに途中で止まる**形で壊れていた。

### 9.1 実測（v439・グリセリン＋酢酸3分子）

| 症状 | 実測 |
|---|---|
| 連続エステル化が2回目以降できない | エステル化の候補4件のうち **3件が「生成物を配置する空間がありません」で失敗**。成功するのは1件だけ |
| 副生成物がキャンバスの外に出る | 脱離した水が x=1360、そのときの viewBox は 238〜1312 ＝ **画面外** |
| 選択は2分子まで | `selectedMolecules.length > 2` で古い方を捨てる。3分子を要する反応列に届かない |

**この3つは同じ根から来ている**: 反応の配置ロジックが「分子は2つ・1回きり」を前提にしている。
油脂（グリセリン＋脂肪酸3分子）やジエステル（2価カルボン酸＋アルコール2分子）は
**同じ反応を2〜3回繰り返す**ので、回を重ねるほど場所が足りなくなる。

### 9.2 一括反応は作らない（方針）

「2価エステル化」を1つのルールにして一気に進める案は**採らない**。
v358 でジ体・モノ体（モノステアリン酸グリセリド等）をわざわざ登録したとおり、
**途中の分子に名前が付く**ことがこのアプリの答え合わせになっている。
一括にすると中間が見えなくなり、`lastReaction` の前後比較も「何が起きたか」を1枚に潰してしまう。

**やることは「同じ反応を繰り返せるようにする」**。選択は繰り返しの間ずっと効き続ける。

### 9.3 選択を N 分子へ広げる

- 上限を **4** にする（グリセリン＋脂肪酸3分子＝油脂まで届く）。
- `selectedMoleculeSets()` は**連結成分で重複を除く**。反応で2分子が1つになると
  選択が同じ成分を2回指すため（先に選んだ方＝式の左を残す）。
- 絞り込み `siteAllowed` を一般化する。現行の「**すべての**選択分子に跨る箇所だけ」は
  2分子専用で、**3つ選んだ瞬間に2分子反応が全滅する**（跨れるのは常に2分子だから）:

  | 選択数 | 通す箇所 |
  |---|---|
  | 0 | すべて |
  | 1 | その分子の原子を含む箇所（相手は未選択でよい） |
  | 2以上 | 箇所の原子がすべて選択の中にあり、かつ **2つ以上**の選択分子に跨る |

### 9.4 選択の見え方

- 番号を**丸数字（①②）から塗りバッジの算用数字（1・2・3）へ**変える。
  丸数字は図の下の見出し（`renderMoleculeLabels`）が**キャンバスの通し番号**として使っており、
  選択枠の丸数字（**選んだ順**）と意味が食い違う。エタノール→酢酸の順に選ぶと
  「見出しでは①酢酸なのに選択枠では②酢酸」になる。記号を分けて衝突をなくす。
- 選択中の分子に**薄い塗り**（青・低不透明度）を敷く。枠線だけだと、分子が3つ以上あるとき
  どれが選ばれているか一目で読めない。塗りは作図の下（`uiGroup`）なので線を隠さない。
- 2つ以上選んでいるときだけ、1番の枠に **「式の左」** の注記を出す。
- 右パネルの案内文も N 対応にする。ただし **「2 エタノール」とは書かない**——
  化学の文脈では係数（2分子のエタノール）と読めてしまう。**「2番目 エタノール」**と書く。

### 9.5 整列（配置の失敗をなくす）

**原則: 反応に関わらない分子は、じゃまなら退避させてよい。** 座標は見た目専用なので化学は変わらない。

実装したのは4つ（v472。設計時の見立てでは1つ目だけのつもりだったが、
実測すると原因が重なっていた）。

1. **傍観分子の退避**（`planShoveAside`）。`planAttachment` は
   「全部を避けて置ける向き」→「傍観分子を無視して置ける向き＋その分子を外へ逃がす」の順に探す。
   逃がしきれなければ従来どおり失敗する
2. **刻みをそろえる**。名称ライブラリはエントリごとに刻みが違い（グリセリン 42px・酢酸 80px）、
   42px 間隔の枝のあいだへ 80px 幅のアセチル基が差し込まれて結合線が隣の炭素を
   0.0px で貫通していた。動かす側を**母体の刻みへ相似に伸縮**してから繋ぐ
   （相似なので形も結合角も変わらず、一様な正の倍率だから鏡像にもならない）
3. **動かす側を大きさで決める**。**ここは設計時の見立てを変えた**——
   「式の左右（v347／C-2）は変えない」つもりだったが、大きい方を動かすと
   置き場が無く、これが油脂で止まる主因だった。選択が無いときは**小さい方**を動かす。
   酢酸(4原子)＋エタノール(3原子) では従来どおりアルコールが動くので、
   CH₃COOH + HOCH₂CH₃ の並びは変わらない。分子を選んでいるときは
   従来どおり**先に選んだ方（式の左）を動かさない**。決めた向きで置けなければ反対向きも試す
4. **90°回転を候補に足す**。鏡映は従来どおり入れない（不斉炭素が鏡像異性体に化ける）。
   90° で変わるのは図の**読み方の約束**の方だけ——フィッシャー投影（縦＝奥）と
   ハース投影（環は横置き）——なので、**不斉炭素も面マークも環も持たない分子**に限る。
   脂肪酸やアセチル基がここに入り、「縦に立てて置く」候補が使えるようになる

**残っている限界**: グリセリン＋**ステアリン酸**3分子は3本目が置けない（2本まで）。
炭素18個の直鎖は1400px あり、直交4方向 × 回転4通りでは収まらない。
ここから先は生成物側の再配置（母体ごと組み直す）が要るので、別項目として立てる。

### 9.6 副生成物の置き場

`parkAsWater` は「全原子の右端＋2マス」に置くため、反応を重ねるほど右へ伸びて画面から出る。
**脱離した場所の近くから外へ向かって空きを探す**方式へ変える:

- 候補は脱離した O の周り 8マス四方の格子点。**近い順**に見て、
  重原子から `1.5マス`以上・**結合線から 0.5マス**以上離れた最初の点に置く
  （結合線からの距離も見ないと、伸ばした結合の上に水が乗って構造式が別物に見える）。
- 「水がとれた場所のそば」に出るので、どの反応で出た水かも読める。
- `fromReaction` の印は従来どおり付ける（図の下の見出しに水を出すため）。

### 9.7 テスト（RX 系に追加）

- **RX17**: 3分子を選んでもエステル化の候補が消えない／選んでいない4つ目の分子が絡む候補は消える
- **RX18**: グリセリン＋酢酸3分子で、エステル化を**3回連続**実行できてトリエステルになる
  （エステル結合3本・遊離の -OH ゼロ）。v439 では2回目で「配置する空間がありません」
- **RX19**: 反応で出た副生成物が**呼び出し直後の視野の中**に収まり、
  どの原子・結合線とも重ならない

---

## 10. reactor の穴埋め（qa レーンの283項目棚卸し・2026-08-06。v816〜）

一問一答（qa）が知識項目283件を反応と突き合わせたところ、
**「反応が無いので繋げられない」項目が30件**あった。その内訳と、ここで埋めた分の判断を残す。

### 10.1 需要の実数（qa の棚卸しが数えたもの）

| 足したい反応 | 繋がる項目数 | この回の扱い |
|---|---|---|
| **酸塩基**（弱酸の遊離・塩の生成） | **7件** ← 最大 | §10.5（`br2` の次に大きい穴） |
| 共重合（2種の単量体を1本の鎖に） | 3件 | 未着手・申し送り（§11.6 で「入れない」と判断） |
| **酸化開裂** | 1件だが**構造決定の中心** | §10.3 で実装 |
| グリコシド結合の加水分解 | 2件 | **§11 で実装**（v882） |
| ナトリウムとの反応（水素発生） | 2件 | **§11 で実装**（v883） |

数字は「項目数」であって「重要度」ではない。**酸化開裂は1件しか繋がらないが、
構造決定という単元そのものの主役**なので、件数だけで順位を付けない。

### 10.2 ⚠ 臭素水 × フェノールは「逆を教えていた」（最優先で直した穴・v816）

**症状**: `br2_water` に紐づく反応が `add_br2`（不飽和結合への付加）1本しか無かったため、
フェノールに臭素水を掛けると空振りになり、瓶の `miss`

> ベンゼン環は付加ではなく置換なので、この条件では脱色しません

が返っていた。教科書の必修事項（触媒なしで置換が進み **2,4,6-トリブロモフェノールの白色沈殿**が
できる）と**正反対**。「反応が足りない」ではなく「**間違いを教えている**」ので最優先。

**実装**: `bromination_activated_ring`（`reactor.js`）。

- 対象は **-OH（フェノール）と -NH₂（アニリン）が付いた一置換ベンゼンだけ**。
  判定は `activatingSubstituent()` で、**環外の重原子がちょうど1つ**＝裸の -OH / -NH₂ に絞る。
  アニソール（-OR）・アセトアニリド（-NHCOR）も理屈の上では活性化基だが、
  **白色沈殿として高校で扱わない**ので候補に出さない（判断できないものは出さない・開発方針4章）
- **2,4,6 を一度に置換する**。1箇所ずつ3回に割らないのは省略ではなく**教科書どおり**で、
  一置換体・二置換体は取り出せずトリブロモ体まで進む。途中の図を出すと実在しない中間体になる
- 一置換体（オルト2つとパラが3つとも空いている形）に限る。o-クレゾールのように
  空きが足りない環は「どこまで入るか」を高校の範囲で決められないので出さない

**`detect` の数える単位**（第1段・第2段の教訓の確認）:
`activatedRingBrominationSites()` は `componentOf()` で**連結成分を切り出してから**
環の大きさと置換基の数を数える。芳香族の下ごしらえは過去に2度
「キャンバス全体で数えていて、同じ分子を2つ並べると1件に潰れる」壊れ方をしている
（`DESIGN_reagent_palette.md` §7.1・§8.1）ので、**フェノール2個 → 2件**を RC1 で固定した。
新設した `aromaticAtomSet()` は集合を作るだけで、成分をまたいで判断しない。

**生成物の一致**: フェノールの生成物の正準コードが、登録済みの
`2,4,6-トリブロモフェノール` と**完全に一致**することを RC1 で主張する。
アニリンの生成物 **2,4,6-トリブロモアニリンは未登録**なので、
登録済みトリブロモフェノールの O を N に置き換えた分子を期待値として組み立てて比べている。
**登録の依頼は §10.7 に出す**（compounds.json は別レーンが作業中のため触らない）。

### 10.3 酸化剤 [O] を側鎖酸化・酸化開裂まで広げる（v817）

v816 まで `oxidant` は **1級・2級アルコールとアルデヒドにしか作用しなかった**。
そのため高校の必修が2つ、画面のどこからも出せなかった:

- **トルエンの側鎖酸化**（→ 安息香酸）
- **アルケンの酸化開裂**（KMnO₄ で C=C を切る）＝ **構造決定の主役**

酸化開裂は**条件で生成物が変わる**（ケトン／アルデヒド／カルボン酸／CO₂）ので、
「判断できないものは出さない」（DEVELOPMENT.md 4章）に従って線を引いた。
**線を引いた理由をここに残す。**

#### 実行する（`oxidize_side_chain` / `oxidative_cleavage`）

| 形 | 生成物 | 根拠 |
|---|---|---|
| 芳香環に直結した **-CH₃** | -COOH（安息香酸・フタル酸・テレフタル酸） | 炭素が減らないので、切れて出ていく側の行き先を決めなくてよい |
| 炭化水素の、環でない C=C で **両端に炭素がついている**もの | 炭素2つ（R₂C=）→ ケトン ／ 炭素1つ（RCH=）→ カルボン酸 | 酸性の強い酸化剤（KMnO₄・K₂Cr₂O₇）1本ぶんに行き先が**一意に決まる**組み合わせ |

行き先は「**その炭素についていた炭素の数**」だけで決まる。この1行が規則の全部で、
コードにも `carbons(id, other)` の数え上げとしてそのまま出ている。
オゾン分解（O₃/Zn → アルデヒドで止まる）は**別の試薬**なので、この瓶では出さない
——「同じ瓶で条件が割れる」形（§2.4 の濃硫酸）にはしない。

#### 実行しない（`oxidation_out_of_scope_info` が理由だけ返す）

| 形 | 出さない理由 |
|---|---|
| **末端の C=C（=CH₂）** | CO₂ と水になって出ていく。**有機の図に残らないもの**をキャンバスに置くと、以後その CO₂ が反応の相手として数えられる |
| **環の中の C=C** | シクロヘキセン → アジピン酸は正しいが、「切ったのに1分子のまま」で前後比較の読み方が変わる。別項目として立てる |
| **C≡C の開裂** | 高校では扱いが安定しない（案内も出さない） |
| **炭素と水素だけでできていない分子** | 他の官能基との**酸化されやすさの順序**を高校の範囲で決められない（アルコールの酸化に置いた線引きと同じ） |
| **炭素2個以上の側鎖**（エチルベンゼン・クメン・スチレン） | 生成物は安息香酸で確定だが、**切れて出ていく側**の行き先が条件で変わる ⚠ **→ §10.3-b で決着し、実行するようになった（v1465 予定）** |
| **環に -OH / -NH₂ が付いた分子** | 環そのものが酸化されて壊れるので、側鎖だけを残した図は書けない ＝ **ここは変えていない** |

⚠ **`info` は1本にまとめた。** 種別ごとに分けると、スチレン（末端 C=C ＋ 長い側鎖）で
瓶を押したときに hits が2件になり、`renderConditionChoice`（本来は温度の2択のための画面）が
解説2つを並べてしまう。`info` ルールは `apply(game)` を**箇所なしで**呼ばれるので、
文面は分子をもう一度見て組み立てる（`oxidationOutOfScope()` が `{ sites, kinds }` を返す）。

#### `detect` の数える単位（第1段・第2段の教訓の確認）

新しい3つの `detect` は、どれも `componentOf()` で連結成分を切り出してから数える。
とくに `sideChainOxidationSites()` は **RX8 と同じ「同じ生成物になる位置はまとめる」**を
入れている（p-キシレンの2つの -CH₃ は等価なので1通り）が、その鍵
`sideChainProductKey()` には **`aromaticSiteClass` が §8.1 で直したのと同じ手当て**
——連結成分だけを複製し、成分の同一性を鍵の前に置く——を最初から入れてある。
RC2 で「トルエン2個 → 2件」「p-キシレン2個 → 2件」「2-ブテン2個 → 2件」を固定した。

#### 一致の主張（空振りの緑を避ける）

生成物が**登録エントリと同じ正準コード**になることを5組で主張する:
トルエン→安息香酸 ／ o-キシレン(2回)→フタル酸 ／ p-キシレン(2回)→テレフタル酸 ／
2-ブテン→酢酸2分子 ／ 2-メチル-2-ブテン→アセトン＋酢酸。
否定対照は同じ数え方で **陰性17件が0件**（側鎖9件・開裂8件）。
⚠ **この数は v1464 まで。**§10.3-b で **9組・陽性13件・陰性23件**へ増えている。

### 10.3-b 側鎖酸化を炭素2個以上へ広げた ＝ **図を変える**（2026-08-26 ユーザー決定・決着）

⚠ **上の §10.3 は消していない。** v817 に「なぜ断ったか」が書いてあり、
**その理由のどこが間違っていたか**を残すことに意味があるので、上書きせず決着だけを足す。

#### なぜ断っていたか（v817 の判断）

「炭素2個以上の側鎖は、**生成物は安息香酸で確定だが、切れて出ていく側の行き先が条件で変わる**」。
`oxidation_out_of_scope_info` が文章だけ返し、図は動かさなかった。
根っこは**末端 C=C に置いた線引きと同じ**で、
「**有機の図に残らないもの**（CO₂）をキャンバスに置くと、以後それが反応の相手として数えられる」。

⚠ **この理由の**前半**は正しく、**後半**が飛躍だった。**
「CO₂ を**置けない**」から「**図を変えない**」へ飛んでいる。
CO₂ を置かない選択肢（＝**描かない**）が、判断の候補に上がっていなかった。

#### なぜ方針が変わったか（ユーザーの言葉）

> **図を変えたほうが、多くのユーザーの感覚に合うと思います。
> アプリで見ながら問題を解くときは図が変化してほしい**

> **図に「酸化生成物による」と残す必要はなく、テキストで補足すればよいでしょう**

**この2行が原則を1つ立てている**: 図は**残るもの**だけを描き、
**出ていくもの**は `caption` で補う。決められないのは「出ていく側が何になるか」だけで、
**残る側は最初から一意**（環に直結した炭素だけが -COOH になる）だったのだから、
図を止める理由になっていなかった。

⚠ **この原則が当たるのは、いま断っている他の2件も同じ**。測っただけで実装はしていない（§10.3-d）。

#### 何を実行し、何を実行しないか（実装の線）

対象は「**環に直結していて・ベンジル位に水素がある・炭化水素の側鎖**」。
炭素1個（トルエン）も炭素2個以上（エチルベンゼン）も**同じ1本のルール**が扱う
（`sideChainOxidationCandidates` → `sideChainOxidationSites`）。

| 見るもの | 通す／落とす | 落とす理由 |
|---|---|---|
| ベンジル位に水素があるか（`getFreeValency ≥ 1`） | `tert`-ブチルベンゼン・α-メチルスチレンを落とす | **既存の判定をそのまま維持**。抜ける水素が無ければ酸化されない |
| 環に -OH / -NH₂（`activatingSubstituent`） | フェノール・アニリンを落とす | **§10.3 のまま変えていない**。環そのものが酸化されて壊れる |
| **枝が環に届かないか**（`ringAtomIdsOf`） | ジフェニルメタン・テトラリン・アントラセン・インドールを落とす | 出ていく側にもう1つ環がある（＝「残りは CO₂ などになる」が事実に反する）／縮環していてそもそも「ぶら下がった枝」ではない |
| **枝が炭素と水素だけか** | フェニルアラニン・フェニル酢酸・フェニル酢酸メチル・ケイ皮酸を落とす | 他の官能基との**酸化されやすさの順序**を高校の範囲で決められない（酸化開裂の `hetero` と同じ線） |

⚠ **門番は「環かどうか」の1つに寄せた。**芳香環は環の部分集合なので、
`aromatic.has()` と `rings.has()` を並べると**片方を壊してもテストが赤くならない**
（実測: 芳香環の門番だけ外しても RC2 は緑のままだった）。**門番は数を減らして、1つずつ赤くできる形にする。**

**`apply` の書き換え**: 枝を**先に決めてから**（原子を消したあとでは枝をたどれない）
ベンジル炭素以外を `removeAtom` し、空いた場所に `=O` と `-OH` を置く。
`freeSpotAround` は**側鎖を落としたあと**に呼ぶので、いま側鎖があった場所をそのまま使える。

**`caption`**（⚠ **資料に無いことは言い切らない**。「二酸化炭素**など**」「条件によって」で止める）:

> 側鎖が炭素2つ以上（エチルベンゼン・クメン・スチレンなど）でも、残るのは**環に直結した炭素だけ**なので、
> できるのは同じ安息香酸です。切れて出ていった残りの炭素は、条件によって二酸化炭素などになります。
> ここでは**図に残していません**。

この一文は**枝を落としたときだけ**出す（トルエンでは出ない ＝ 消えたものが無いのに断らない）。

#### `oxidationOutOfScope` の `chain` の枝は**消した**（確かめたうえで）

図が出るようになったので不要になる、が発注の見立て。**実測すると完全には重ならなかった**ので、
重ならなかったぶんも含めて判断を残す。

`chain` の枝が拾っていたのは**ライブラリ19件**（`compounds.json` + `stages.json`・名前の重複込み）。
新しい `sideChainOxidationSites` が引き取ったのは 9 件で、残り 10 件は上の表で落とした:

| 旧 `chain` が拾っていた | いま |
|---|---|
| スチレン・エチルベンゼン・クメン・プロピルベンゼン・ブチルベンゼン・アリルベンゼン・o/m/p-エチルトルエン | **実行（図が変わる）** |
| フェニルアラニン・フェニル酢酸・フェニル酢酸メチル・ケイ皮酸 | 何も出さない（側鎖が炭化水素でない） |
| ジフェニルメタン・テトラリン・アントラセン・インドール | 何も出さない（枝が環に届く） |

**残り10件で `chain` を残さなかった理由**: 残していたら
**その10件にこそ間違ったことを言う**枝になる。旧文面は「環に直結した炭素だけが残って安息香酸になります」だが、
ジフェニルメタン（両側に環）・アントラセン・インドール・テトラリン（縮環）ではこれが成り立たない。
＝ **消したのは「不要になったから」だけでなく、「残すと嘘になるから」**。
落ちた10件は、ベンズアルデヒドやベンジルアルコールと同じく「何も出ない」に揃う。

#### **実行できる側鎖の中の C=C は、案内から外す**

スチレン・アリルベンゼンは**末端 C=C を持つ**ので、そのままだと
「酸化 → 側鎖酸化（押すと安息香酸になる）」の横に
「⚠ 酸化（ここでは図を変えない範囲）＝ 末端の C=C は切りません」が並ぶ。
**押すと変わるボタンの横に「変えません」を置くと、どちらが起きるのか読めない。**
そこで `oxidationOutOfScope` は、**側鎖酸化の枝に丸ごと含まれる C=C を案内から落とす**
（`consumed`）。α-メチルスチレンはベンジル位に水素が無く側鎖酸化が出ないので、
**従来どおり末端 C=C の案内が出る** ＝ 落とす条件が効きすぎていないことの対照になる。

#### `sideChainProductKey` は**切り落としてから**鍵を作る

等価な位置をまとめる鍵（RX8 と同じ考え方）は、**側鎖を落とした後の分子**に -COOH を付けて作る。
落とす前に作ると、ベンジル炭素に5本目・6本目の結合が生えた**実在しない分子**の
正準コードを鍵にすることになる（スチレンのベンジル炭素は環1＋二重結合2で埋まっている）。
⚠ **件数としては観測できない**（対称な位置にある側鎖は長さも同じになるので、
切っても切らなくても同じ鍵になる）。それでも直したのは、**鍵が生成物を指していないと、
次に「まとめる／まとめない」を考える人が鍵を読めなくなる**から。

#### 実測（8化合物 ＋ ライブラリ全件）

| 化合物 | 側鎖酸化の札 | 生成物 | 案内（`info`） |
|---|---|---|---|
| トルエン | **1件** | 安息香酸 ✓ | 0 |
| キシレン（o / m / p） | **各1件**（2つの -CH₃ は等価） | 2回でフタル酸 / テレフタル酸 ✓ | 0 |
| エチルベンゼン | **1件**（v1464 は0件） | **安息香酸** ✓ | 0（v1464 は1件） |
| クメン | **1件**（v1464 は0件） | **安息香酸** ✓ | 0（v1464 は1件） |
| スチレン | **1件**（v1464 は0件） | **安息香酸** ✓ | **0**（v1464 は2件） |
| `tert`-ブチルベンゼン | **0件** | —— | 0 |
| フェノール | **0件** | —— | 0 |
| アニリン | **0件** | —— | 0 |

ライブラリ全件（`compounds.json` + `stages.json`・名前で重複を除いた **40件**が陽性）で
`apply` を1回ずつ回して**例外0件**。うち 31 件は -CH₃（v1464 と同じ）、
**9件が新しく図の変わるようになった分子**（スチレン・エチルベンゼン・クメン・プロピルベンゼン・
ブチルベンゼン・アリルベンゼン・o/m/p-エチルトルエン）。

#### 一致の主張と否定対照（RC2 を書き足した）

一致は **5組 → 9組**（エチルベンゼン→安息香酸／クメン→安息香酸／スチレン→安息香酸／
p-エチルトルエン(2回)→テレフタル酸 を追加）。陽性13件・陰性23件。

⚠ **「安息香酸と同じ正準コード」だけでは足りない**（側鎖を落とさずに -COOH を足す実装を見逃す）ので、
**原子の数**で直接押さえる: エチルベンゼン・クメン・スチレン・ブチルベンゼンの生成物が
**重原子9個ちょうど**（＝安息香酸）で、消えた炭素の数が側鎖の長さと合っていること。

**この直しを外すと赤くなること**（6通り・すべて実測で確認）:

| 壊し方 | 落ちる主張 |
|---|---|
| A. 側鎖酸化をふたたび `isMethylCarbon` だけに戻す | エチルベンゼンの候補が 0 件 |
| B. 「枝が環に届く」門番を外す | ジフェニルメタンが候補に出る |
| C. `apply` が枝を `removeAtom` しない | エチルベンゼンが安息香酸の正準コードにならない |
| D. 実行できる側鎖の C=C を案内から外す処理を消す | スチレンに「図を変えない範囲」の案内が出る |
| E. `caption` の「図に残していません」を消す | 出ていった側の行き先が補われていない |
| F. 「枝が炭素と水素だけ」の門番を外す | ベンズアルデヒドが候補に出る |

### 10.3-d ⚠ **測っただけ・実装していない** —— 同じ原則が当たる残り2件

§10.3-b でユーザーが立てた原則（**図は残るものだけを描き、出ていくものはテキストで補う**）は、
**いま断っている他の2件にもそのまま当たる**。次に誰かが判断するときの材料として実測を置く。
⚠ **この節の内容は1行も実装していない。**`oxidation_out_of_scope_info` は
`terminal` と `ring` を従来どおり案内している。

#### (1) 末端の C=C の酸化開裂 —— **原則を当てれば図を変えられる**

いまの断り文句は「**図に残らないものを置くと以後の反応の相手として数えられてしまう**ので、
ここでは切りません」。§10.3-b で決着したとおり、**置かなければよい**。

**何を描き、何を描かないことになるか**: `=CH₂` の側は CO₂ と水になるので**描かない**（原子を消す）。
残る側は `oxidative_cleavage` の既存の規則がそのまま決める
——**その炭素についていた炭素の数**が1ならカルボン酸、2ならケトン。

ライブラリで `terminal` が付く**17件**の行き先（実測）:

| 残る側 | 件数 | 例 |
|---|---|---|
| カルボン酸 | 12 | 1-ブテン→プロピオン酸 ／ プロペン→**酢酸** ／ 1-ヘキセン ／ 1,3-ブタジエン ／ 1,3-ペンタジエン ／ イソプレン ／ アレン |
| ケトン | 5 | 2-メチルプロペン（イソブテン）→**アセトン** ／ α-メチルスチレン→アセトフェノン ／ 2-メチル-1-ブテン ／ リモネン ／ イソプレン |
| **分子が丸ごと消える** | **1** | **エチレン** —— 両端が `=CH₂` なので全部 CO₂ になり、キャンバスから分子がゼロになる |

⚠ **判断が要るのはエチレン1件だけ**（アレンも両端 `=CH₂` だが、まん中の炭素が残る）。
分子が0個になること自体は §17（v1454）で扱い済みなので画面は壊れないが、
**「押したら自分の分子が消えた」は反応として親切ではない**。
両端とも炭素0のときだけ `info` に残す、という形になるはず。

**既存のどのテストに当たるか**:

| テスト | いま主張していること | 変わり方 |
|---|---|---|
| **RC2 (1)** `cleaveNegative` | エチレン・プロペン・1,3-ブタジエンが**0件** | エチレン以外は**陽性へ移る**。陰性8件の数え（`cleaveNegative.length === 8`）も動く |
| **RC2 (2)** 案内 | エチレン → 案内1件・「二酸化炭素」を含む／α-メチルスチレン → 案内1件 | α-メチルスチレンは**実行へ移る**。エチレンだけ案内に残る |
| **RC2 (4)** 一致 | 9組 | プロペン→酢酸・イソブテン→アセトンを足せる（**どちらも登録済み**） |
| **RX の全件走査**（貫通ゼロ・一直線ゼロ） | 全ライブラリ × 全反応 | 開裂が**16件ぶん新しく実行される**。原子を消す反応なので、置き場の検査はむしろ緩む方向 |
| **§10.10 の二乗** | 全走の所要 | `detect` は増えないので伸びない（当たるルールが増えるぶんだけ `apply` が増える） |

#### (2) 環の中の C=C の酸化開裂 —— **断り方が違う。そして実測では扱えている**

いまの断り文句は「いまは**『切ったのに1分子のまま』を図で扱えない**ので、ここでは変えません」。
⚠ **これは副生成物の話ではなく、図の扱いの話** ＝ §10.3-b の原則では解けない。
そこで**本当に扱えないのかを実際に試した**（`oxidative_cleavage.apply` は環かどうかを見ていないので、
`detect` を通さずに直接呼べる）。

| 分子 | 実行 | 連結成分 | 生成物 |
|---|---|---|---|
| **シクロヘキセン** | **成功**（例外なし） | 1 → **1**（1分子のまま） | **登録エントリ `アジピン酸` と正準コードが完全一致** |
| シクロペンテン | 成功 | 1 → 1 | グルタル酸の構造（**未登録**なので構造での主張のみ） |
| 1-メチルシクロヘキセン | 成功 | 1 → 1 | ケトンとカルボン酸を**1分子の両端に**持つ形（6-オキソヘプタン酸） |

★ **「図で扱えない」は事実ではなかった。** 環を開いて両端に -COOH を置く操作は
既存の `apply` がそのまま通り、置き場（`freeSpotAround`）も足りている。
**シクロヘキセン → アジピン酸は、いま書き足さなくても描ける。**

**では何が本当に残っているか** —— **`caption` の文面**だけ:

- いまの文面は生成物の数を「その炭素についていた炭素の数」から組み立てて
  「**カルボン酸が2つ**になりました」「**ケトンとカルボン酸**になりました」と言う。
  環を切った場合は**1分子の両端**なので、この言い方は**分子が2つできたと読める**。
- 前後比較（§8.1）も「1分子 → 1分子」になる。差分ハイライト自体は原子IDで取れるので壊れないが、
  **「切った」ことが図から読み取りにくい**（環が開いただけに見える）。

＝ **残っているのは「切ったのに1分子」を**言葉で**言い分ける仕事**であって、図を描く仕事ではない。
（`alkeneCleavageClass` が `'ring'` を返すことを `apply` にも渡し、
「環が開いて、両端にカルボキシ基をもつ**1つの分子**になりました」と言い分ければ足りる見込み。）

**既存のどのテストに当たるか**: RC2 (1) の `cleaveNegative` からシクロヘキセンが抜け、
RC2 (2) の案内（シクロヘキセン → 1件・「アジピン酸」を含む）が消える。
一致の主張には**シクロヘキセン → アジピン酸**を足せる（**登録済み**）。
`info` の `ring` の枝も消えるので、`kinds` は `terminal` だけになる。

### 10.3-e ①環の中／②末端 の C=C —— **実験事実・教科書・入試の3つで裏を取った**（2026-08-26・調査のみ）

⚠ **この節も1行も実装していない。**§10.3-d が「アプリの図で扱えるか」を測ったのに対し、
ここは**化学として妥当か／高校の教材として妥当か**を、一次資料と入試問題DBで確かめた記録。
★ **①と②で答えが違う。まとめて「両方いける」とは言えない。**

#### 調べたもの（後で数え直せるように）

| 資料 | 何を見たか |
|---|---|
| 教科書 | 東京書籍『化学』令和5年検定済・全7分冊（`マイドライブ/化学/教科書/`）を全ページ走査＋該当ページを画像で目視 |
| 傍用問題集 | セミナー化学＋化学基礎（問題編・解答編）。⚠ `セミナー化学26.pdf` は**テキスト層なしの画像スキャンで未確認** |
| 参考書 | 化学新研究2015／総合的研究化学／ハート有機化学三訂版／駿台有機化学／基本ノート［有機］の5冊。⚠ **5冊ともテキスト層ゼロ**なので、目次からアルケンの章を当て、該当ページを PNG 化して目で読んだ |
| 入試問題DB | `マイドライブ/化学/問題集/_解析/db/problems.jsonl`。**有機の大問 563 件の悉皆**（2020〜2023年版＝**2019〜2022年入試**の4年ぶん。年別 150/143/140/130）。⚠ 記憶にある「3年ぶん433大問」は古い。`index.json`（4,782大問・12年ぶん）は**物質名を持たない**ので、この問いには使えない |

**検索語**（problems.jsonl の1件を JSON 全文にして正規表現で当てた）:
`オゾン分解|オゾン酸化|酸化開裂|C=C の?(切断|開裂)` と
`過マンガン酸[^"]{0,60}(酸化開裂|C=C|二重結合|開裂|切断)`。
環は `環状アルケン|環内の|環の中に|環が開|断片が(2つに)?分かれず|1分子の(二価|ジ|2価)カルボン酸|生成物が1種類`、
末端は `末端|=CH2|CH2=|ホルムアルデヒド|HCHO|二酸化炭素|CO2` で拾ってから**1件ずつ目で確かめた**
（自動タグは元素分析の CO₂ やフェノール樹脂原料の HCHO を拾うので、そのままでは使えない）。

---

#### ① 環の中の C=C の酸化開裂

##### A. 実験事実

**起こる。ただし条件で行き先が4つに割れる**（★ **うち1つは「切れない」**）。

| 試薬・条件 | シクロヘキセンの行き先 | 出典 |
|---|---|---|
| **硫酸酸性の KMnO₄・加熱** | **アジピン酸 1分子**（環が開いて両端 -COOH） | 教科書 p.129【発展】／セミナー 問題編 p.260 要項・p.299 演習C／駿台有機化学 p.81-82 |
| **オゾン分解（O₃ → Zn などで還元的に後処理）** | **ヘキサンジアール 1分子**（両端 -CHO で止まる） | 教科書 p.129【発展】／上智大 2021年入試 |
| **オゾン分解（O₃ → H₂O₂ で酸化的に後処理）** | アジピン酸 1分子（⚠ **一般式からの推論**。環の例は書かれていない） | 基本ノート［有機］p.131（⚠ **5冊で唯一、還元的 Zn と酸化的 H₂O₂ を書き分けている**） |
| **微アルカリ性・薄い（約1%）／低温の KMnO₄** | ⚠ **切れない**。**シクロヘキサン-1,2-ジオール**（隣接ジオール・シス付加） | 化学新研究 p.513／総合的研究 p.418／ハート p.111-113／基本ノート p.131／**九州大 2020年入試（★）** |

⚠ **4段目は「開裂しない」枝**なので見落とすと危ない。入試でも撃ち分けられている:
**九州大 2020年入試は、同じ大問・同じシクロヘキセンで
「酸性 KMnO₄ → アジピン酸」「塩基性 KMnO₄ → シクロヘキサン-1,2-ジオール」を並べて問う**
（さらに生じたジオールの立体異性体を3つ数えさせる）。鎖状でも同じで、
同志社大 2022年入試は `(CH₃)₂C=C(CH₃)₂ + KMnO₄/H₂O → 2,3-ジメチル-2,3-ブタンジオール` の図を与えている。
★ **＝ 「KMnO₄ を掛けたら C=C が切れる」は無条件では成り立たない。酸性・加熱が要る。**

**分岐の書き方は本ごとに違う**（★ **どれも同じことを言っているが、語が揃っていない**）:

| 本 | 切れない側 | 切れる側 |
|---|---|---|
| 化学新研究 p.513-514 | **微アルカリ性・薄い（約1%）** | **硫酸酸性・加熱** |
| 基本ノート p.131 | **低温・アルカリ性**（OsO₄ も） | **常温で酸性／加熱下でアルカリ性**。⚠ **開裂はジオールを経由する**とまで書く |
| 総合的研究 p.418 | 「おだやかな条件」（中性） | 「さらに酸化が進んで」。⚠ **酸性・加熱の語が無い** |
| 駿台有機化学 p.80 | 「条件をゆるやかにすると」 | （既定）。⚠ 酸性・加熱の語が無い |
| ハート p.111-113 | アルカリ性 → グリコール | ⚠ **切断そのものを書いていない**（KMnO₄ は「二重結合の検出試験」の位置づけ） |

⚠ **化学新研究の理由づけが分岐をいちばんよく説明している**（補足14）:
微アルカリ性では酸化力が足りず**弱いほうの π 結合しか切れない**。
酸性にすると酸化力が上がって**強いほうの σ 結合まで切れる**。

★ **「本当に1分子のジカルボン酸になるのか」＝ なる。**環を切っても両端はまだ鎖でつながっているので、
分子は1つのまま。⚠ **ただし「アジピン酸になる」と言い切ってよいのは酸性 KMnO₄ のときだけ**で、
オゾン分解ならジアルデヒド、塩基性・低温ならジオール（切れない）。
**アプリの瓶は「硫酸酸性の過マンガン酸カリウム」を既定にしている**（`oxidative_cleavage.usually`）ので、
**アジピン酸に決め打ちしてよい**。⚠ 逆に、瓶にオゾンを足すなら生成物を別に持つ必要がある。

##### A-2. アジピン酸の工業的製法は**これではない**

⚠ **シクロヘキセンの酸化開裂は工業的製法ではない。**入試が「工業的製法」として問うのは:

- **立命館大 2022年入試** … フェノール →（H₂・Ni・高温高圧）→ **シクロヘキサノール** →（酸化剤）→ アジピン酸
- **東北大・後 2020年入試** … ベンゼン →（H₂）→ **シクロヘキサン** → 空気酸化 → **硝酸で酸化** → アジピン酸
- **滋賀医科大 2022年入試** … 「アジピン酸はベンゼンから合成される」

★ **どれも C=C の酸化開裂を経由しない**（環はいったん飽和させてから、-OH／-C=O を硝酸で切る）。
⚠ **教科書はアジピン酸の製法に一切触れていない**（p.155 の分類表、p.250・251・257・265 で
**ナイロン66 の原料としてだけ**登場。7編の「有機工業化学」p.282 は酢酸の製法だけ）。

★ **参考書も同じ結論**（★ **製法を書いている2冊とも硝酸酸化ルート**）:

- **化学新研究 p.727** … フェノール →(Ni・+3H₂)→ シクロヘキサノール →(HNO₃)→ シクロヘキサノン →(HNO₃)→ アジピン酸
- **総合的研究 p.525** … フェノール →(H₂)→ シクロヘキサノール →(HNO₃)→ アジピン酸。
  ⚠ 「ベンゼン → 部分付加 → **シクロヘキセン** → 水の付加 → シクロヘキサノール」という別経路も併記されるが、
  **そこでのシクロヘキセンは水を付加してアルコールになる役で、酸化開裂ではない**
- ⚠ **「シクロヘキセンの酸化開裂で作る」と書いた本は、教科書・傍用・参考書5冊のどこにも無かった。**

＝ **「シクロヘキセン → アジピン酸」は作り話ではないが、工業の話でもない。構造決定の道具**。

##### B. 教科書に載るか —— ⚠ **載っていない**

| | 教科書（東書 R5） | セミナー化学 |
|---|---|---|
| オゾン分解そのもの | **p.129【発展】のみ**（教科書全体で1か所） | 要項 p.260・対照表 p.297・発展例題37（p.264）・問題450/504/505 |
| KMnO₄ で C=C が**切れる** | **p.129【発展】**。⚠ **本文 p.127 は「赤紫色が消える＝不飽和の検出」だけ**で、切れるとは書かない | 要項 p.260・対照表 p.297・演習問題C（p.299） |
| **①環状アルケン → 1分子のジカルボン酸** | ⚠ **無い**（p.129 の発展は鎖状 R¹R²C=CR³H の一般式だけ。シクロアルケンは p.128 で「付加しやすい」と紹介されるのみ） | **有り**（**演習問題C・p.299**／解答 p.362。**【発展】【やや難】・12 お茶の水女子大 改**。五員環の C₆H₁₀ 4種を KMnO₄・加熱で開裂させ、開環して鎖1本になる図が構造式で描かれている） |
| トルエン → 安息香酸（比較用） | **有り・本文扱い**（p.183） | 有り |

★ **教科書の扱いは非対称**: 同じ KMnO₄ でも「**芳香族の側鎖が切れて安息香酸**」は**本文**、
「**アルケンの C=C が切れる**」は**発展**。**環はその発展にも無い。**

**参考書5冊でも、①はほとんど書かれていない**:

| 本 | ①環状アルケン → 1分子のジカルボン酸 |
|---|---|
| **駿台有機化学 p.81-82** | ★ **有り（5冊で唯一）**。例題9-2(3) は「生成物が `HOOC(CH₂)₄COOH` **のみ**」から出発し、**「鎖状ではどうしても末端が違った分解物になる。環状にすれば分解物を同じにできる」**と論じてシクロヘキセンに着地する。⚠ **入試の論法そのもの** |
| 化学新研究 | ⚠ **無い（索引で確定）**。索引に「シクロヘキセン」の項目が存在せず、「アジピン酸」も p.726 のナイロン節1件のみ |
| 総合的研究／ハート／基本ノート | 確認した範囲（アルケンの節・高分子の節）で**無い** |

⚠ **①を実装すると「教科書は扱わないのにアプリだけが知っている」形になる**——
ただし**傍用問題集（セミナー 演習問題C）と受験参考書（駿台有機化学）には載っている**ので、**空白ではない**。
★ **正確に言うと「教科書の外・受験の中」**。教科書が扱わないのは
「環状アルケンそのものを深追いしない」ためで、**反応が疑わしいからではない**。

> ✅✅ **【2026-08-27・入試DBレーンが独立に検算した】§10.11 の件数は「最新の 563」で測られている。**
> ⚠ **同じ日に私大13校ぶん 35件が DB へ追加されており**（**2022年入試が 95 → 130**）、
> ★ **「追加より前に数えていたら過少になる」という疑いがあった。**
>
> ★★ **検算の結果、疑いは晴れた** —— **クメン法 48・ジアゾ 47・共重合 43 が、
> 追加後の 563 と1件も違わずに一致した**（⚠ **528 で測っていれば 46・42・41 になるはず**）。
> ★ **数え直しは不要。**
>
> ⚠⚠ **ただし手筋件数（/423）の側は別。**★ **2023年版130件は `routes` が空のままなので、
> そのぶんは永久に 0 に見える。**⚠ **「新しく足したい反応ほど手筋件数が小さく出る」（B-2）は、
> ルート付けが済むまで効き続ける。**★ **順位付けには使わないこと。**

##### C. 大学入試ではどう出るか —— **563大問中 14件**

★ **出る。しかも「これで環と決める」という決め手として使われる。**

| 入試年 | 大学（学部・日程） | 使われ方 |
|---|---|---|
| 2019 | 上智大・理工 | 4種のシクロアルケンを開裂させ、生成物が直鎖か／不斉炭素をもつかで二重結合の位置を決める |
| 2019 | 名古屋市立大・医 | ナイロン66 の原料から逆算してアジピン酸 → 開裂で1分子だからシクロヘキセン |
| 2020 | 九州大・前（★） | シクロヘキセン。**酸性 KMnO₄ でアジピン酸／塩基性 KMnO₄ でジオール**を撃ち分ける |
| 2020 | 同志社大・理工他 | シクロヘキセンのオゾン分解で「鎖状のカルボニル化合物」（1分子）を書かせる |
| 2020 | 名古屋大・前 | 切って **1 mol の1種類**しか出ない → 二重結合は環の中 |
| 2020 | 大阪市立大・理工医他 | 断片が1つ → 環内 C=C。生成物を酸化するとナイロンの原料 |
| 2020 | 横浜国立大・前 | 「開裂の生成物が1種類なら環状アルケン」＋**オゾン分解と酸性 KMnO₄ で止まる段が違う**ことを問う |
| 2020 | 東北大・前（★） | C₆H₁₀ を酸性 KMnO₄ で酸化すると**直鎖ジカルボン酸だけ**が出る → シクロヘキセン。**模範解答と一致で検証済み** |
| 2021 | 上智大 | シクロヘキセンを **O₃/Zn** で切って**ヘキサンジアール**（1分子）→ 酸化してアジピン酸 |
| 2021 | 大阪大・前 | 「酸化で炭素数が減らず枝分かれのない二価カルボン酸」→ 二重結合は6員環の中 |
| 2021 | 慶應義塾大・看護医療 | 天然物。開裂で生じた2つの -COOH をつなぎ戻して6員環（シクロヘキセン）を復元する**逆向き**の使い方 |
| 2022 | 九州大・前（★） | シクロヘキセン＋酸性 KMnO₄ → アジピン酸 → ナイロン66 |
| 2022 | 熊本大・前 | **シクロペンテン**＋酸性 KMnO₄ → **グルタル酸1分子** → 加熱して無水グルタル酸 |
| 2022 | 慶應義塾大・看護医療 | リモネンのオゾン分解。環内 C=C が開いて1分子になる |

**年別 2/6/3/3 で、4年とも出ている**（母数は年 150/143/140/130）。
⚠ **大きさの見当**: C=C の酸化開裂が**本文・解答・手順のどこかに現れる**大問は**全部で 40 件**
（試薬の言い方は**オゾン分解 27件／過マンガン酸 16件**、両方出るのが5件。
⚠ **オゾン分解のほうが多い**。アプリの瓶には**オゾンが無い**——これは別の穴）。
手筋の出現数で並べると `ozonolysis` は 27 件で、`side-chain-oxidation`（63件・9位）より下、
`chiral-carbon-count`（120）・`degree-of-unsaturation`（116）・`combustion-analysis`（104）よりは大きく下。
★ **アプリがすでに実装した側鎖酸化の半分弱**の規模で、**そのうち3分の1が環**という位置づけ。
★ **手筋台帳には専用の手が立っている**: `ring-cleavage-to-diacid`
「環内の C=C を切ると断片が分かれず1分子の二価カルボン酸になる」（2026-08-16 採用。
採用理由に「既存の `ozonolysis` は**切ると2つに分かれる**という数の扱いしか持たない」と書かれている）。

⚠ **試薬の言い方**: この14件のうち **KMnO₄ が9件・オゾン分解が8件**（両方出るのが3件）。
★ **②と違い、環は KMnO₄ 側のほうがむしろ多い**（アジピン酸・グルタル酸という
「名前のある答え」に着地させたいので、アルデヒドで止まるオゾン分解より酸まで進む KMnO₄ が使いやすい）。
＝ **アプリの瓶（硫酸酸性 KMnO₄）と、入試での言い方がいちばんよく合うのが①**。

---

#### ② 末端の C=C の酸化開裂

##### A. 実験事実 —— ⚠ **①と違い、試薬で答えが変わる**

| 試薬・条件 | `R-CH=CH₂` の `=CH₂` 側 | 残る側 |
|---|---|---|
| **硫酸酸性の KMnO₄・加熱** | **ギ酸 HCOOH を経て CO₂ ＋ H₂O**（図に残らない） | R-COOH（R に炭素1つならケトン） |
| **オゾン分解（O₃ → Zn 還元）** | **ホルムアルデヒド HCHO**（★ **分子として残る**） | R-CHO |
| オゾン分解（O₃ → H₂O₂ 酸化） | ギ酸を経て CO₂（⚠ 基本ノート p.131 のみが書き分け） | R-COOH |

★ **「本当に CO₂ になるのか」＝ 酸性 KMnO₄ なら、なる。ただし**一段はさむ**。**
教科書 p.129【発展】に「R³＝H のときは、生成物の**ギ酸 HCOOH がさらに KMnO₄ で酸化され、
CO₂ と H₂O が生成する**」と本文＋反応図（ギ酸 → 炭酸 H₂CO₃ → CO₂＋H₂O）で明記
（**このページは PNG 化して目で読んだ**。一般式は `R¹R²C=CR³H` の**鎖状だけ**で、環の図は無い）。

⚠⚠ **見落としやすい分岐がここにある。**セミナー解答編 p.362（**同じく PNG 化して目で読んだ**）では、
メチレンシクロペンタン（環外の末端 C=C）を過マンガン酸カリウムで酸化開裂させた答えが
**「ギ酸 HCOOH ＋ シクロペンタノン」**で、**CO₂ は傍注**
（`ギ酸は過マンガン酸カリウムによって酸化されると、水と二酸化炭素を生じる。HCOOH --酸化--> CO₂+H₂O`）。
★ **＝ 同じ硫酸酸性 KMnO₄ でも、問題によっては答えがギ酸で止まる。**
**「=CH₂ 側は必ず消える」とは言えない**——消えるのは「十分に酸化したとき」。
⚠⚠ **ただしオゾン分解では CO₂ にならず HCHO で止まる。**教科書もセミナーも
**この区別を徹底している**（セミナー 問題編 p.297 の試薬対照表は KMnO₄ と O₃ の生成物を左右に並べている）。
**駿台有機化学 p.81 が理由まで書いている**:
「KMnO₄ では H の付いた側はアルデヒドで止まらずカルボン酸まで行く。
**O₃ の場合は還元性の Zn 粉末を加えておくとカルボン酸への酸化を止められる**。
通常の入試問題では O₃ はこの条件で使われている」。

★ **参考書での分布**（★ **CO₂ まで書く本は少数派**）:

| 本 | 末端 `=CH₂` の行き先 |
|---|---|
| **化学新研究 p.514 詳説16** | ★ **CO₂**（5冊で唯一の明記）。`2-メチル-1-ブテン → エチルメチルケトン ＋ [HCHO] → [HCOOH] → CO₂↑ + H₂O` と**3段階で追う**。⚠ **硫酸酸性 KMnO₄・加熱の文脈だけ** |
| 総合的研究 p.418／ハート p.114（式3.45）／駿台 p.81 | **HCHO**（いずれもオゾン分解の文脈） |
| 基本ノート p.131 | 一般式のみ。末端の扱いは書いていない |

★ **アプリの瓶は硫酸酸性 KMnO₄ なので、`=CH₂ → CO₂` は正しい。**
⚠ **ただしそれは「よく見る言い方」ではない**——**参考書5冊のうち1冊、入試では2件**（下の C）。

##### A-2. エチレンを酸化開裂すると何になるか

- **硫酸酸性 KMnO₄** … 両端が `=CH₂` なので**全部 CO₂ ＋ H₂O**。有機物は何も残らない。
  ⚠⚠ **これを名指しで書いた資料は、教科書・傍用・参考書5冊のどこにも無かった**
  （教科書 p.129 と化学新研究 p.514 の規則を、両端に2回当てた**こちらの推論**）
- **オゾン分解** … **HCHO 2分子**（分子は残る）。
  ★ **駿台有機化学 p.84 の「アルケンの反応／まとめ」表に、`CH₂=CH₂ の場合の生成物` 欄があり
  `H₂C=O ＋ O=CH₂`（HCHO 2分子）と明記されている** ＝ **エチレンだけは、切った答えが本に書いてある**
- ⚠ **微アルカリ性の KMnO₄** … **切れずにエチレングリコール**（化学新研究 p.513 が
  まさにエチレンを例に挙げている）
- ⚠ **入試では2件出るが、どちらも生成物を答えさせていない**:
  - 明治大 2020年入試 … エチレン＋**塩基性** KMnO₄ で「色が消えて黒色沈殿（MnO₂）」＝**脱色の観察だけ**
  - 東海大・医 2022年入試 … 「エチレン＋硫酸酸性 KMnO₄」は**アセトアルデヒドを与えない選択肢（ダミー）**として置かれている

★ **＝ エチレンを「硫酸酸性 KMnO₄ で切ったら何になるか」は、教科書にも入試にも「答え」として存在しない。**
（★ **存在するのは駿台の「オゾン分解なら HCHO 2分子」だけ** ＝ **アプリの瓶では出せない答え**。）

##### B. 教科書に載るか —— ★ **載っている（①と違う）**

- **教科書 p.129【発展】**: `R³＝H` のとき **CO₂ と H₂O** になる、と**明記**。①（環）と違い、**教科書に有る**。
  ⚠ ただし**発展**であって本文ではない。また**KMnO₄ の文脈だけ**で、オゾン分解側では触れていない。
- **参考書**: 化学新研究 p.514 詳説16 が**同じことを3段階の図で**書いている。⚠ 他の4冊は書いていない。
- **セミナー**: 演習問題C（p.299／解答 p.362）で、環外メチレン型が KMnO₄ で切れて
  **ギ酸＋シクロペンタノン**（CO₂ は傍注）という形で出る。
  一方、問題450（p.265）では**オゾン分解の答えは HCHO**。**同じ本の中で使い分けている。**
  ★ **同じ演習問題Cが①と②を同時に扱っている**（(a)(b)(c) が環内＝1分子のジ酸／ケト酸、
  (d) が環外の末端＝ギ酸＋シクロペンタノン）。**入試の東北大・前 2020 と同じ構図。**

##### C. 大学入試ではどう出るか —— ⚠ **「CO₂ で数える」は 2件だけ。HCHO のほうが 9件で多い**

★ **ここが①といちばん違うところ。**

| 言い方 | 件数 | 大学（入試年） |
|---|---|---|
| **オゾン分解 → 末端は HCHO**（分子として残る） | **9** | 神戸大・前（2019）／慶應義塾大・理工（2019）／東京農工大・前（2019）／同志社大・理工他（2020）／大阪市立大・理工医他（2020）／上智大（2021）／早稲田大・理工3-1（2021）／慶應義塾大・看護医療（2022）／慶應義塾大・薬（2022） |
| **酸性 KMnO₄ → 末端は CO₂**（図から消える） | **2** | 上智大・理工（2019）／東北大・前（2020・★） |

★ **「CO₂ になって失われた炭素の数から元の構造を当てる」使い方は、確かに存在する。**
**東北大・前 2020年入試（★）がその見本**で、問題文が酸化開裂の型①〜④を与えたうえで:

- 型③ `R-C(R')=CH₂ → R-C(R')=O ＋ CO₂` ／ 型④ `R-CH=CH₂ → R-COOH ＋ CO₂`
- 判別表の見出しがそのまま **「酸化開裂で二酸化炭素が出るかどうか」**、
  ラベルが **「二酸化炭素が生成物に挙がっているかどうかで末端 =CH₂ の有無が決まる」**
- 実際に化合物C（3-メチル-5-ヘキセン酸）を「CO₂ が出た＝末端 C=C」と読んで決めている。
  **模範解答と照合して一致を確認済み**（DB の `verify_verdict: 一致`）

⚠ **①と②は同じ大問に同居する**（6件: 上智大・理工 2019／同志社大 2020／大阪市立大 2020／
**東北大・前 2020**／上智大 2021／慶應義塾大・看護医療 2022）。
東北大・前 2020 は1つの大問の中で
**「CO₂ が出た → 末端 C=C」**（実験4）と**「断片が1つだけ → 環内 C=C」**（実験7）を両方使わせる。
★ **アプリで片方だけ実行できて片方は案内、という状態は、この型の問題を追えない。**

上智大・理工 2019年入試も同じで、大問の冒頭に
「R³ が水素で骨格の末端に二重結合があるときは**水と二酸化炭素まで酸化される**」という規則を置き、
メチレンシクロペンタン（環外の末端 C=C）の候補を「その炭素は CO₂ になり**シクロペンタノンが残る**」で落としている。

⚠ **2件とも、大学が問題文で規則を与えている。**
実際、C=C の酸化開裂が出る **40大問のうち 7件**が「一般式・型・図」を本文で与えている
（東北大・前／上智大・理工／学習院大 2020・2022／大阪市立大／東京農工大／同志社大 2022）。
★ **これは「教科書の発展にしかないので、大学が読者に配っている」ことの傍証**。

---

#### まとめ（①と②の違い）

| | ① 環の中の C=C | ② 末端の C=C |
|---|---|---|
| 化学として正しいか | ○（酸性 KMnO₄ でアジピン酸1分子） | ○（酸性 KMnO₄ で CO₂）。⚠ **オゾン分解なら HCHO** |
| 教科書（東書 R5） | **× 無い** | **○ 有る**（p.129 発展・KMnO₄ の文脈） |
| セミナー（傍用） | ○（演習C p.299） | ○（同じ演習C。⚠ 答えは**ギ酸**で止まる） |
| 参考書 5冊 | **1冊のみ**（駿台 p.81-82）。化学新研究は**索引レベルで無い** | **1冊のみ**（化学新研究 p.514）。他4冊は HCHO 止まり |
| 入試 563大問中 | **14件**（4年とも出る） | **11件**。⚠ ただし **CO₂ の言い方は2件**、HCHO が9件 |
| アプリが決め打ちできるか | ○（瓶が硫酸酸性 KMnO₄ なので迷わない） | ○（同上） |

★ **表の読み方**: ①は**教科書に無いが入試には14件出る**（受験の中・教科書の外）。
②は**教科書には有るが、入試で使われる言い方は主にオゾン分解**（教科書の中・入試の言い方とずれる）。
⚠ **「どちらも中途半端」ではなく、ずれ方が逆向き。**

**推奨（別々に）**:

- ★ **① は緩めてよい。**図はもう描けている（§10.3-d）ので、残るのは `caption` を
  「環が開いて、両端にカルボキシ基をもつ**1つの分子**になりました」と言い分けるだけ。
  入試での需要（14件・4年連続）が側鎖酸化に次いで大きく、しかも**「1分子で出てくるのが環の印」**
  という**判別の道具**として使われている ＝ **図で見せる価値がいちばん高いのはここ**。
  ⚠ 教科書に無いことは、**画面に「発展」と断る**か、caption で
  「傍用問題集や入試では出ます」の一言を添えれば十分（トルエン→安息香酸が本文なのと差がある、
  という事実は残しておく）。★ **駿台有機化学 p.81-82 の論法**
  （「鎖状だと末端が違う分解物になる。環状なら分解物を同じにできる」）は
  **そのまま caption に使える言い方**で、しかも入試14件の使われ方と一致している。
- ★ **② も緩めてよいが、①より優先度は下げる。**教科書の発展に**明記がある**ので
  「アプリだけが知っている」形にはならない。
  ⚠⚠ **ただし caption を2か所直さないと嘘になる**（いまの文面は
  「二酸化炭素 CO₂ と水になって出ていきます」で言い切っている）:
  1. **試薬を名指しする** … 「**硫酸酸性の過マンガン酸カリウムでは**」を頭に付ける。
     ⚠ **試薬を書かずに「CO₂ になります」とだけ言うと、入試で多数派の
     オゾン分解の答え（HCHO・分子として残る）と食い違う**。
     §10.3-b の「出ていくものはテキストで補う」を守るだけでは防げない、**②に固有の穴**
  2. **ギ酸を経ることを書く** … 「まず**ギ酸 HCOOH** になり、十分に酸化すると CO₂ と水まで進みます」。
     ⚠ **セミナーの答えはギ酸で止まっている**（p.362）ので、
     「必ず消える」と書くと**傍用問題集の答えと食い違う**
  ★ 逆に言えば、**②は「図を変えるか」より「文をどう書くか」のほうが難しい**。
  ①（言い分けるのは「1分子になった」の一言だけ）とは、**残り仕事の重さが違う**。
- ⚠ **エチレンは実行しない（`info` に残す）**。理由は §10.3-d が書いた「分子が消えると親切でない」だけでなく、
  ★ **硫酸酸性 KMnO₄ で切った答えが、資料のどこにも書かれていないから**
  （教科書・傍用・参考書5冊とも名指しせず、入試2件はどちらも生成物を問うていない。
  ⚠ 唯一ある答えは**駿台 p.84 の「オゾン分解なら HCHO 2分子」**で、**アプリの瓶では出せない**）。
  **両端とも炭素0のときだけ案内に残す**、という §10.3-d の見立てでよい。
  ★ **案内の文面はこう書ける**: 「エチレンは両端が `=CH₂` なので、硫酸酸性の過マンガン酸カリウムでは
  すべて二酸化炭素と水になり、**有機化合物が残りません**。
  （オゾン分解ならホルムアルデヒド2分子になります。）」
  ⚠ **微アルカリ性の過マンガン酸カリウムなら切れずにエチレングリコールになる**（化学新研究 p.513）——
  こちらは**アプリが別途もっていてよい反応**だが、いまは無い（§10.5 の穴に追加する候補）。

**申し送り**: ① を入れると **シクロペンテン → グルタル酸**（熊本大 2022年入試の答え）が描けるようになるが、
**`グルタル酸` はライブラリ未登録**（`アジピン酸` は登録済み）。§10.7 の登録待ち行列に足す候補。

**未確認のまま残したこと**（⚠ 断定していない）:

- `セミナー化学26.pdf`（604ページ）は**テキスト層のない画像スキャン**で、走査できていない。
  ⚠ 同じ理由で **重要問題集・化学IB・IIの新演習・化学基礎問題精講・駿台理系標準問題集も未確認**
  （4冊とも `get_text()` が空＝OCR なしでは全文検索できない）
- 参考書5冊は**アルケンの酸化の節と高分子（ナイロン）の節だけ**を読んだ。次は未確認:
  化学新研究 5-3「有機化合物の構造決定」p.489-496 の中身／総合的研究の索引と特別講義6 の例題群／
  ハートの3.17節・14.8節以外と巻末索引／駿台の高分子章／基本ノートの高分子まわり
- 2019年より前の入試（`index.json` に 12年ぶん 4,782大問の索引はあるが、**物質名を持たない**ので
  「酸化開裂が出るか」は引けない）。**件数はすべて 2019〜2022年入試の4年ぶん・有機563大問が母数**
- エチレンを酸化開裂したときの生成物を**名指しで書いた資料**は見つかっていない（規則からの推論）
  ⚠ **この1点は §10.3-f で検算し、結論を維持した**（ただし入試の件数は数え直しで 2件 → 6件に増えている）

### 10.3-f エチレンだけを別に調べ直した —— **「切った答え」は無いが「酸化の答え」は教科書の本文にある**（2026-08-27・調査のみ）

⚠ **この節も1行も実装していない。**§10.3-e の②で「エチレンは実行しない」と書いた根拠を、
**ユーザーの指示（2026-08-26「エチレンについては実験事実をリサーチする」）に従って独立に取り直した**記録。
★ **前レーンの推奨（実行しない・`info` に残す）は維持する。**
⚠ **ただし理由の一部は書き換わり、件数は1つ増える方向に外れていた。**

#### 調べ直したもの

| 資料 | 何を見たか |
|---|---|
| 教科書 | 東京書籍『化学』令和5年検定済・全11分冊を `エチレン`／`エテン` で全ページ走査し、当たったページを**本文の文字で**読んだ（5編 p.124-132・p.140-150、7編 p.282、巻末索引） |
| 傍用問題集 | セミナー化学＋化学基礎（問題編371ページ・解答編463ページ）。**両方ともテキスト層があるので全文検索した** |
| 参考書 | 化学新研究 p.513-514・p.529・巻末索引「エ」／駿台有機化学 p.81・p.84／総合的研究 p.416・p.419／基本ノート［有機］p.130-131。⚠ **4冊ともテキスト層ゼロなので該当ページを PNG 化して目で読んだ**（見えたことを確認済み） |
| 入試問題DB | `problems.jsonl`（**有機563大問**・2020〜2023年版＝2019〜2022年入試）。§10.3-e と同じ母数 |

**検索語**（後で数え直せるように）:
`エチレン` の素の出現は **138大問**。ここから
`ポリエチレン*|エチレングリコール|エチレンテレフタラート|エチレンジアミン|エチレンオキシド|プロピレン|スチレン|塩化ビニル`
を伏せ字にして**単独のエチレンだけ**を残すと **70大問**。その各出現の前後110字に
`過マンガン酸` / `オゾン` / `パラジウム|PdCl|ワッカー` / `銀触媒|酸素酸化|空気酸化|接触酸化|直接酸化` を当てて拾い、
**1件ずつ `tests`・`constraints`・`asks` を読んで真偽を確かめた**（伏せ字を入れないと
ポリエチレンテレフタラートの側鎖酸化が大量に混じる）。

---

#### A. 実験事実 —— 条件ごとの生成物と、その出典

★ **エチレンの「酸化」は5通りある。** ⚠ **教科書に本文で載っているのは、このうち酸化開裂ではない。**

| 条件 | 生成物 | 名指しの出典 | 教科書／傍用／参考書の別 |
|---|---|---|---|
| **硫酸酸性 KMnO₄・加熱** | CO₂ ＋ H₂O（有機物は残らない） | ⚠⚠ **無い**。教科書 p.129【発展】と化学新研究 p.514 詳説16 の**規則を両端に2回当てた推論**にとどまる | ⚠ **どこにも「答え」として無い** |
| **微アルカリ性・薄い（約1%）KMnO₄** | ★ **切れない。エチレングリコール** | ★ **化学新研究 p.513 §5(1)**。⚠ **エチレンを名指しし、係数まで合った式を載せている**: `3CH₂＝CH₂ ＋ 2KMnO₄ ＋ 4H₂O → 3CH₂(OH)CH₂(OH) ＋ 2MnO₂ ＋ 2KOH`（MnO₄⁻ の赤紫色が消え、**黒褐色の MnO₂ が沈殿**する）。一般形は基本ノート p.131 の図③（低温アルカリ性 KMnO₄ または OsO₄ → 2価アルコール） | **参考書のみ**（教科書・セミナーに無い） |
| **オゾン分解（O₃ → Zn）** | ★ **HCHO 2分子** | ★ **駿台有機化学 p.84「アルケンの反応／まとめ」**。表の最下行が `CH₂＝CH₂ の場合の生成物` で、O₃ の列に `H₂C＝O　O＝CH₂` と書いてある（H₂+Ni の列は `CH₃CH₃`、H–O–O–H の列は `-(CH₂CH₂)-ₙ`） | **参考書のみ**（1冊） |
| **オゾン分解（O₃ → H₂O₂）** | ⚠ **ギ酸2分子と読める**（CO₂ までは書かれていない） | 基本ノート p.131 の図⑥（`O₃[酸化] / H₂O₂[酸化分解] → カルボン酸 ＋ ケトン`）。⚠ **一般形だけで、エチレンの名指しは無い＝未確認** | **参考書のみ**（1冊） |
| ★ **O₂ ／ PdCl₂・CuCl₂ 触媒（ワッカー法）** | ★★ **アセトアルデヒド** | ★★ **教科書 p.150 の本文**に `2CH₂＝CH₂ ＋ O₂ →(PdCl₂, CuCl₂) 2CH₃CHO` として載っている（7編 p.282 の「酢酸の製法の転換」でも同じ式を再掲）。セミナー 問題編 p.272 問457・p.276 問468／解答編 p.327・p.332。総合的研究 p.419 は**「ワッカー法」という名前まで**書く | ★ **教科書の本文**（発展ではない） |
| **O₂ ／ Ag 触媒 → エチレンオキシド →(H₂O)→ エチレングリコール** | エチレンオキシド／エチレングリコール | 基本ノート［有機］p.130「**エチレングリコールの工業的製法**」の図解のみ（Ag₂O が触媒として回る絵） | ⚠ **参考書1冊だけ**。教科書は**全11分冊に「エチレンオキシド」「酸化エチレン」の語が1回も無い**（走査済み）。化学新研究も**索引に項目が無い**（「エ」の欄はエタノール528／エチレングリコール529／エチレン系炭化水素508／エチレンの製法510 だけ）。総合的研究 p.419 の「エチレンの反応」も**エタノール・塩化ビニル・アセトアルデヒドの3本だけ** |

##### ★ いちばん効く発見: **教科書はエチレン＋硫酸酸性 KMnO₄ の絵を載せている。そして生成物を書いていない**

教科書 p.127 の本文はこう書く——「アルケンの二重結合は酸化されやすく，過マンガン酸カリウム KMnO₄ 水溶液と
反応させると，MnO₄⁻ の赤紫色が消える。**この反応も，不飽和結合の検出に用いられる**」。
★ **そこに添えられた図11 のキャプションが「エテンと硫酸酸性の過マンガン酸カリウム水溶液の反応　
エテンが酸化され，MnO₄⁻ の赤紫色が消える」** ＝ **まさにエチレンの絵**で、
⚠ **エチレン側が何になったかは一言も書かれていない**。
さらに p.128 の図13「エテン（エチレン）の反応」まとめ図には、H₂・H₂O・Br₂・HCl・Cl₂・付加重合の6本しか矢印が無く、
★ **酸化の矢印が入っていない**。

⚠ **＝ 教科書は、エチレン＋硫酸酸性 KMnO₄ を「色が消える」で意図的に止めている。**
p.129【発展】の酸化開裂の一般式は `R¹R²C＝CR³H` で、
**左側は「ケトン」とラベルされている**（R¹＝R²＝H ならホルムアルデヒドでケトンではない）＝
★ **エチレンはこの一般式の想定外**。前レーンの「名指しの資料が無い」は正しい。

---

#### B. 大学入試ではどう出るか —— **エチレンの酸化は出る。ただし出るのはワッカー法**

| 使われ方 | 件数（563大問中） | 大学（入試年） |
|---|---|---|
| ★ **ワッカー法**（エチレン ＋ O₂ ／ PdCl₂・CuCl₂ → アセトアルデヒド） | ★ **13** | 岡山大・前／中央大・理工／日本女子大・理／鹿児島大・前（2019）、名古屋市立大・薬／立教大・理／神戸薬科大・薬（2020）、慶應義塾大・一般／信州大・前（2021）、静岡大・前／東海大・医／同志社大・全／甲南大／立命館大（2022） |
| **エチレン ＋ 過マンガン酸カリウム** | **6**（⚠ **前レーンの「2件」は過少**） | 下の表 |
| **エチレンのオゾン分解** | ⚠ **0** | —（`エチレン` と `オゾン` が同居する2件は、どちらもオゾン分解の対象が**別の化合物**。名古屋大・前 2020 はエチレンを結合長の比較と水の付加に使うだけ、信州大・前 2021 は油脂の脂肪酸を切っている） |
| **エチレンを酸化してエチレンオキシド／エチレングリコールを作らせる** | ⚠ **0** | —（`エチレンオキシド` の語は4件あるが、3件は C₃H₆O・C₄H₈O の**環状異性体の数え上げ**でオキシラン骨格が候補に並ぶだけ、1件は解答用紙の**記入例**。東京医科歯科大 2021 だけが C₂H₄O を三員環と当てて水と反応させるが、**エチレンオキシドは与えられた原料**で、エチレンから作らせてはいない） |
| **エチレングリコール**（語そのもの） | 54 | ⚠ **ほぼ全部が PET の原料**としての登場。作り方を問う問題は**0件** |

##### ★ エチレン ＋ 過マンガン酸カリウムの6件（★ **全件、生成物を問うていない**）

| 入試年 | 大学 | 条件 | 何を問うているか |
|---|---|---|---|
| 2019 | 中央大・理工 | **硫酸酸性** | 「エチレンの C=C が酸化されて赤紫色が消える」という**記述の正誤**（正）。小問集合の1つ |
| 2020 | 岐阜大・医他 | **硫酸酸性・薄い** | メタン／エチレン／アセチレンを吹き込み、赤紫色が「消える・変わらない・濃くなる」を気体ごとに選ぶ。**色だけ** |
| 2020 | 上智大・理工 | （液性の指定なし） | 選択肢「過マンガン酸カリウム水溶液に加えると赤紫色が消える」が3つの炭化水素のどれに当たるか |
| 2020 | 明治大・理工（★） | ⚠ **塩基性** | 「十分に反応させると色が消え、**黒色沈殿**を生じる」まで問題文が与え、色と**臭素水の**生成物の組を選ばせる。⚠⚠ **この小問は選択肢に正答が無く、大学が全員に加点した**（問題集 PDF 319 の編集部注。DB でも `unresolved` 扱い） |
| 2022 | 東海大・医 | **硫酸酸性** | 反応(a)〜(f) のうち「アセトアルデヒドが主生成物である反応はいくつあるか」。★ **正解は(a) のワッカー法1つだけで、硫酸酸性 KMnO₄ の(c) は落とすための選択肢** |
| 2022 | 名城大・理工 | **硫酸酸性** | 記述「硫酸酸性の過マンガン酸カリウム水溶液によって容易に酸化される」が、エチレン／アセチレン／ベンゼンのどれに当たるか |

★ **6件に増えても結論は変わらない。むしろ強くなる**——
⚠ **6大学が6通りの聞き方をしていて、そのどれも生成物を答えさせていない。**
これは「まだ誰も聞いていないだけ」ではなく、**入試の側もそこで止めている**という形をしている。

★ **明治大 2020 の黒色沈殿は、化学新研究 p.513 の式そのもの**（塩基性で MnO₂ が沈殿する）。
⚠ **つまり「切れない側」の反応が入試に現れている唯一の例**だが、
そこでも**エチレングリコールという答えは要求されていない**。

⚠ **付記**: 傍用も同じ止め方をしている。セミナー解答編 p.322 の脚注は
「エチレン，アセチレンの確認には，過マンガン酸カリウム水溶液を用いることもできる」「赤紫色が脱色される」で終わり、
★ **問題編 p.259 でもエチレンは臭素水の脱色にしか使われていない**（アルケンの酸化開裂は同じ節の【発展】枠）。

---

#### C. 推奨 —— ★ **実行しない（`info` に残す）。ただし案内文を直し、「別の反応を足す」は今は見送る**

##### ★ 1. エチレンの酸化開裂は実行しない（前レーンと同じ結論）

⚠ **理由は前レーンより1つ多い**:

1. ⚠⚠ **答えが資料に無い**（前レーンの理由・**検算して維持**）。教科書・傍用・参考書4冊のどこにも
   「エチレン＋硫酸酸性 KMnO₄ ＝ CO₂」は書かれていない
2. ⚠ **分子が丸ごと消える**（§10.3-d の理由）
3. ★ **新しい理由: 教科書が同じ絵で止めている。**教科書 p.127 の図11 は
   **エチレンと硫酸酸性 KMnO₄ そのもの**でありながら「色が消える」で止め、
   p.128 のまとめ図から酸化の矢印を外している。
   ★ **アプリが案内で止めるのは、教科書と同じ止め方**であって、逃げではない
4. ★ **入試も6大学そろって止めている**（上の表）。生成物を答えさせた例が1件も無い

**案内の文面**（★ 前レーンの案を1か所だけ直す）:

> エチレンは両端が `=CH₂` なので、硫酸酸性の過マンガン酸カリウムでは
> すべて二酸化炭素と水になり、**有機化合物が残りません**。
> **教科書も入試も、エチレンについてはここで「赤紫色が消える」までしか扱いません。**
> （オゾン分解ならホルムアルデヒド2分子になります。）

⚠ **「教科書に無い」を消極的な断りではなく、そこで止まる理由として書く**のが要点。
★ 逆に **CO₂ と言い切る文言をこれ以上強くしないこと**——資料の裏づけが無いのは変わらない。

##### ⚠ 2. 「微アルカリ性ならエチレングリコール」を反応として足すのは、**いま足す価値が低い**

★ **前レーンは §10.5 の穴の候補に挙げているが、頻度で測ると弱い**:

- **エチレン → エチレングリコール**を問う入試: ⚠ **563大問中 0件**
- **アルケン ＋ 塩基性／低温 KMnO₄ → ジオール（切れない）** に広げても ⚠ **2件だけ**
  （九州大・前 2020＝シクロヘキセン、同志社大・全 2022＝2,3-ジメチル-2-ブテン。
  ⚠ 前レーンが挙げた2件と一致し、**それ以上は増えなかった**）
- ⚠ **教科書にもセミナーにも無い**（化学新研究 p.513 と基本ノート p.131 の図だけ）
- ⚠ **瓶が増える**（硫酸酸性 KMnO₄ とは別に「微アルカリ性の KMnO₄」が要る）。
  ★ **同じ瓶で条件だけ違う2つの反応**になるので、いまの「瓶＝試薬」の作りにそのまま乗らない

★ **＝ 足すなら、①（環）②（末端）を入れて、その caption で「酸性・加熱が要る」と書き分けるほうが先**。
⚠ **条件で答えが割れることは §10.3-e ①A の4段目ですでに測ってあり、そこで書けば足りる。**

##### ★★ 3. **エチレンで穴が空いているのは酸化開裂ではなく、ワッカー法のほう**（★ 新しい指摘）

⚠⚠ **前レーンはここを見ていない。**エチレンの酸化として教材が実際に扱っているのは:

| | 硫酸酸性 KMnO₄ で切る | ★ ワッカー法 |
|---|---|---|
| 教科書 | ⚠ **図はあるが生成物は無い**（p.127 図11） | ★ **本文に式がある**（p.150、再掲 p.282） |
| セミナー | ⚠ 脱色の脚注だけ（解答編 p.322） | ★ 問題2件（問457・問468） |
| 参考書 | 規則のみ（新研究 p.514・駿台 p.81） | ★ 総合的研究 p.419 が**「ワッカー法」と命名**、基本ノート p.131 の図④ |
| 入試 563大問 | ⚠ **6件・すべて色だけ** | ★ **13件** |
| 入試DB の手筋台帳 | `ozonolysis`（27件・環は `ring-cleavage-to-diacid`） | ★ **`ethylene-wacker-oxidation` が「基本」として採用済み**（2026-08-16・採用理由「工業製法の手が並ぶなかでアセトアルデヒドの現行製法だけが空」） |

★ **図としても素直**: エチレン → アセトアルデヒドは**炭素が2個のまま残り、分子が消えない**。
⚠ **ただし瓶が別**（O₂ ／ PdCl₂・CuCl₂）なので、これは §10.5（反応の穴）側の話であって、
**酸化開裂①②の判断には混ぜないこと**。★ **申し送りとしてここに置く。**

⚠ **エチレンオキシドは足さなくてよい**——教科書に語が1つも無く、化学新研究の索引にも無く、
**入試で「エチレンから作る」問題は0件**。★ **同じ工業製法でも、ワッカー法とは扱いの重さが2桁違う。**

---

**未確認のまま残したこと**（⚠ 断定していない）:

- ⚠⚠ **エチレン＋硫酸酸性 KMnO₄ の生成物を名指しした資料は、今回も見つからなかった。**
  ★ **前レーンの結論を維持する。**「CO₂ ＋ H₂O」は**規則からの推論のまま**
- ⚠ **オゾン分解 → H₂O₂ でエチレンが何になるか**（ギ酸で止まるか CO₂ まで行くか）は**未確認**。
  基本ノート p.131 の図は一般形で「カルボン酸」までしか書いておらず、**エチレンの名指しが無い**
- ハート有機化学の巻末索引を引いていない（テキスト層が無く、今回はアルケンの節だけを見た）。
  ⚠ **大学教科書なのでエポキシ化を扱っている可能性が高いが、確認していない**
- §10.3-e と同じく、**画像スキャンの問題集5冊**（セミナー化学26・重要問題集・新演習・
  基礎問題精講・駿台理系標準）は走査できていない
- 件数はすべて **2019〜2022年入試・有機563大問**が母数。2019年より前は引けない（§10.3-e と同じ）

### 10.4 H–X 付加を HBr / HCl / HI の3本にする（v818）

**症状**: 瓶もルールも **HBr の1本だけ**だったので、「HCl の付加で塩化ビニルができる」を
画面で追うと**生成物が臭化物になる**。マルコフニコフ則そのものは伝わるが、
ラベル（何を加えたか）と図（何が付いたか）がずれる。

**まとめ方**: `HYDROGEN_HALIDES` という**表を1つ**置き、そこから
**瓶（`REAGENTS`）とルール（`REACTION_RULES`）の両方を生成**する。

```
HYDROGEN_HALIDES = [ {key:'hbr', element:'Br', …}, {key:'hcl', …}, {key:'hi', …} ]
   ├→ HYDROGEN_HALIDE_REAGENTS  … 瓶3本（… REAGENTS に展開）
   └→ HYDROGEN_HALIDE_RULES     … add_hbr / add_hcl / add_hi（… REACTION_RULES に展開）
```

- **`detect` は3本が同じ関数オブジェクトを共有する**（`multipleBondSites`）。
  RC3 が `new Set(rules.map(r => r.detect)).size === 1` でこれを固定するので、
  片方の枝にだけ条件を足すと赤くなる
- `apply` も枝ごとに書かない。違うのは**付ける元素と一言の補足**だけ
- `id` は `add_hbr` を含めて従来どおり。既存のテスト・台本・デモが名指ししているので改名しない

**一致の主張**: アセチレン＋HCl → **塩化ビニル**（③の症状そのもの）、
エチレン＋HCl → クロロエタン、エチレン＋HBr → ブロモエタン の3組が登録エントリと同じ正準コード。
否定対照は「同じエチレンでも3本の瓶で**3種類**の生成物になる」（1種類なら瓶を見分けていない）と、
エタンでは3本とも空振りしてマルコフニコフ則の説明が返ること。
マルコフニコフ則そのものは、プロペンで**ハロゲンが炭素2個の炭素につく**ことを3本とも同じ数え方で確かめる。

**ヨウ化物の生成物は未登録**（§10.7）なので、HI だけは構造での主張にとどめている。

### 10.5 まだ埋めていない穴（需要の実数つき・申し送り）

| 反応 | qa の項目数 | メモ |
|---|---|---|
| **酸塩基**（弱酸の遊離・塩の生成） | **7件** | §10.6。**塩が16件登録済み**なので、反応を足すだけで正準コードの一致まで確かめられる |
| 共重合（2種の単量体を1本の鎖に） | 3件 | 既存の `addition_polymerization` は1種類の単量体を前提にしている。**§11.6 で「入れない」と判断**（高校範囲外・並ぶ順が決まらない） |
| ~~グリコシド結合の加水分解~~ | ~~2件~~ | **§11 で実装済み**（v882・`hydrolysis_glycoside`） |
| ~~ナトリウムとの反応（水素発生）~~ | ~~2件~~ | **§11 で実装済み**（v883・`react_sodium`）。H₂ の置き場は「描かない」で決着（§11.5） |

### 10.6 酸塩基（次に大きい穴・7件）

このアプリは電荷をモデルに持たず、**塩は「線1本の共有結合」として書く**流儀
（`DESIGN_compound_coverage.md` §6-2・v353 決定）。その流儀の塩がすでに16件登録されている
（酢酸ナトリウム／酢酸カリウム／ナトリウムフェノキシド／ベンゼンスルホン酸ナトリウム／
サリチル酸ナトリウム／パルミチン酸ナトリウム／ステアリン酸ナトリウム／フタル酸水素カリウム …）。
**生成物が既にあるので、反応を足すだけで正準コードの一致まで確かめられる。**

| qa の項目 | 実装の形 |
|---|---|
| `org.phenol.naoh` | フェノール ＋ NaOH → ナトリウムフェノキシド（**瓶は既にある**「水酸化ナトリウム」） |
| `org.phenol.phenoxide-co2` | ナトリウムフェノキシド ＋ CO₂ → フェノール（炭酸より弱い酸であることの証明）。CO₂ の瓶が要る |
| `org.carbonyl.salt-strong-acid` | カルボン酸の塩 ＋ 強酸 → 弱酸の遊離。希硫酸の瓶で引ける |
| `org.carbonyl.vs-carbonic` | **第3段の検出瓶「炭酸水素ナトリウム」がすでに扱っている**。反応にするか検出のままにするかは要判断 |
| `org.carbonyl.acidity` | 酸の強さの比較。反応ではなく `info` で足りる可能性 |

**当面扱わない2件**（ユーザー決定・2026-08-06）:
`org.aroN.aniline-base`（アニリンの塩基性）と `org.bio.amino-acid-amphoteric`（双性イオン）。
アンモニウム型の N（単結合4本）は `chemistry.js` の `maxValencyOf` がすでに許しているが、
**対イオンが結合を持たない種**なので、いまのモデルとキャンバスには置き場がない。
**「イオンは最終的に assembler で扱う」というのがユーザーの方針**なので、
他のアプリへ渡す形にはしない。足りないのは対イオンの持ち方だけ、と記録しておく。

### 10.7 登録が必要な化合物（このレーンでは触らない）

`assembler/compounds.json` は別レーンが全行に `id` を振る作業中のため**1行も触っていない**。
この回の実装で「生成物として画面に出るのに名前が出ない」ものが3件ある:

| 名前 | 出てくる操作 | 補足 |
|---|---|---|
| **2,4,6-トリブロモアニリン** | アニリン ＋ 臭素水（§10.2） | フェノール側は登録済み。白色沈殿の相方なので対で欲しい |
| **2-ブロモプロパン／2-クロロプロパン** | プロペン ＋ HBr / HCl（§10.4） | マルコフニコフ則の主生成物の代表。**異性体（1-ハロプロパン）と対で**あると効く |
| **ヨードエタン（ヨウ化エチル）** | エチレン ＋ HI（§10.4） | HI だけ生成物の名前が出ない |

### 10.8 F と Si を入れるとしたら（**調査のみ・実装していない**）

**背景**: 元素表に F と Si が無いので、**テフロン（ポリテトラフルオロエチレン）と
シリコーンゴムは原理的に作図できない**。過去にユーザーは「F は保留」と判断している
（`DESIGN_compound_coverage.md` §6-1）。ここは判断材料だけを置く。

⚠ **`DESIGN_compound_coverage.md` はこのレーンでは触っていない**（別レーンが編集中）。
同書 §6-1 の「元素を1つ足すときの手順（5か所）」が土台で、以下はその**追補**。

#### 実測（`VALENCIES` に `F:1` / `Si:4` だけを足した写しで確かめた）

| 見たもの | F（テトラフルオロエチレン） | Si（(CH₃)₂Si-O-Si(CH₃)₂） |
|---|---|---|
| 正準コード | 出る | 出る |
| 空き価標（自動水素） | C は 0 で正しい | Si は結合数どおりで正しい |
| `describeStructure` | 「C=C二重結合 ×1」まで出るが **F に一言も触れない** | 「最長の炭素鎖 C1」しか出ず**誤解を招く** |
| `findOutOfScopeMotifs` | `[]`（範囲内） | **`no_group`「高校で習う官能基にあてはまらない」** |
| `findFunctionalGroups` | 影響なし | **`[]`** —— Si-O-Si を見る型が無い |
| 系統名 | **「1,1,2,2-テトラフルオロエテン」が出る**（`IUPAC_HALOGEN` は F を最初から持っている） | **`null`**（シロキサンの命名規則が無い） |
| R/S（`cipRank`） | **`null`**（`CIP_ATOMIC_NUMBER` に F が無く、**分子ごと**判定が消える） | 同上 |

#### F: 追加は**現実的**。同書の5か所＋2か所で足りる

1〜5 は同書のとおり（`VALENCIES` / `CIP_ATOMIC_NUMBER` に `F: 9` / `--color-f` /
`stereo.js` の `colorOf` / `describeStructure` に「フッ素 F ×n」）。**追補は2つ**:

6. **`stereo.js` の `colorOf` は先頭1文字で元素を切る**（`/^(Cl|Br)/` のときだけ2文字）。
   F は1文字なので**表に足すだけでよい**（Si は下記のとおりここが罠になる）
7. **`tools/verify-reactions.js` の `MAX_VALENCY`** には I すら入っていない。
   反応機構（`reactions.json`）に F を使う日が来たら同時に足す

**壊れないことの確かめ方**は同書の手順どおり（全件の正準コード・立体コード・分子式・
系統名・空き価標を書き出して前後で diff）。**F を含む既存分子は0件**なので無差分になるはず。

**入れると何が届くか**: テトラフルオロエチレンを compounds.json に登録すれば、
**既存の `addition_polymerization` がそのままテフロンを作る**（`vinylBonds` は
環でも芳香族でもない C=C を見るだけなので、置換基がハロゲンでも通る）。
**原子パレットに F を出す必要はない** —— I・Na と同じく「名称から呼び出す」と
「反応の生成物」だけで単元が成立する（375px で原子ボタンを1つ増やすと帯が +45px 伸び、
`DESIGN_entry_points.md` A-1 で詰めた順路がその分だけ画面外へ動く）。

#### Si: **追加は現実的でない**。壊れるのは価標ではなく「分類・命名・反応」

`VALENCIES` に `Si: 4` を足すだけでは、シリコーンは**描けるが読めない**分子になる:

- **`findOutOfScopeMotifs` が `no_group` を返す** ＝ アプリが自分から
  「高校で習う官能基にあてはまらない」と言う。これは価標の問題ではなく**分類の問題**で、
  Si-O-Si（シロキサン結合）を官能基として認める判断が要る
- **命名器がシロキサンを知らない**（`iupacName` が null）。名前が出ない分子は
  ライブラリの規約（名前で呼び出す）に乗らない
- **`stereo.js` の `colorOf` は 'Si' を 'S' と読む**（先頭1文字で切るため）。
  硫黄の黄色になる。`/^(Cl|Br|Si)/` へ直す必要がある —— **F には無い罠**
- **作る反応が無い**。シリコーンはジクロロジメチルシランの加水分解＋縮合でできるので、
  既存の付加重合でも縮合重合（エステル・アミド）でも作れない。**新しい重合ルールが要る**
- 単量体を登録しても、上の3つが揃わないと**呼び出せるだけの死んだエントリ**になる

**結論の材料**: F は「5か所＋2か所を同時に直し、テトラフルオロエチレンを1件登録する」で
テフロンまで届く。Si は「分類・命名・重合ルール」の3つを新設する話で、
**元素を1つ足す作業ではない**。同じ労力を §10.6 の酸塩基（7項目）に向けたほうが効く。

### 10.9 酸塩基のうち、既存の流儀のまま解ける分を入れた（v819）

§10.6 の7項目のうち、**新しい概念（電荷・対イオン）を1つも持ち込まずに解ける分**を実装した。
入ったのは**ルール2本だけ**で、瓶は1本も増えていない（既存の `naoh_aq` と `h2so4_dil` に付けた）。

| ルール | 瓶 | 中身 |
|---|---|---|
| `neutralize_naoh` | 水酸化ナトリウム | 酸性の -OH に Na を付ける（カルボン酸・フェノール・スルホン酸の**3つの入口を1つのルールに**） |
| `liberate_weak_acid` | 希硫酸 | -O-Na / -O-K の金属を外す。**自動水素がそのまま -OH を描く**ので、外すだけで酸に戻る |

**この2本はちょうど逆向き**なので、テストは「往復してもとの正準コードに戻る」形で書ける（RC4）。
突き合わせた6組はすべて登録済みのエントリと一致した:
酢酸 ⇄ 酢酸ナトリウム ／ フェノール ⇄ ナトリウムフェノキシド ／
ベンゼンスルホン酸 ⇄ ベンゼンスルホン酸ナトリウム。

**けん化・ヨードホルム反応の生成物（-COONa）からそのまま引ける**のが効いている ——
油脂 → けん化 → セッケン → 希硫酸 → 脂肪酸、という順路が1本につながった。

**まだ残っている酸塩基**:

- `org.phenol.phenoxide-co2`（ナトリウムフェノキシド ＋ CO₂ → フェノール）… **CO₂ の瓶が要る**。
  §10.2（試薬パレット §10.2）で「瓶20本が2列に縦積みされる形はもう限界に近い」と測ったばかりなので、
  **足すなら先に区分をもう一段割る**（付加／置換／酸化還元／酸塩基…）ことを決めてから
- `org.carbonyl.vs-carbonic` … 第3段の検出瓶「炭酸水素ナトリウム」が**すでに陽性/陰性で扱っている**。
  反応にすると同じことを2か所で教えることになるので、**検出のまま置く**
- `org.carbonyl.acidity`（酸の強さの比較）… 独立した `info` は作らず、
  上の2本の `caption` に「カルボン酸 > 炭酸 > フェノール」を入れた。
  `naoh_aq` の `miss` には**陰性側**（アルコールの -OH は中和されない）を書いた（試薬パレット §9.2）

### 10.10 ⚠ 反応を足すと、回帰テストの「全件 × 全反応」が二乗で効いてくる（v819 で実発生）

反応が **24本 → 30本**になった時点で、`tools/run-tests.mjs` の**10分の門番を超えた**
（実測: 600秒でタイムアウト。それまでは 264秒 → 294秒 → 352秒 → 407秒 と伸び続けていた）。

原因は RX の「ライブラリ全件 × 全反応で貫通ゼロ」で、**ルールごとに
`summonMolecule` を呼び直していた**こと。889件 × 全ルール ＝ 2万回以上の呼び出しになる。

**直し方**（判定も件数も変えていない）: 母体は
「**下見用に1回**」＋「**当たったルールのぶんだけ**」しか呼び出さない。
`detect` は分子を変えないので下見の母体を使い回せる。実行だけが書き換えるので、そこは毎回新しく作る。

**申し送り**: 反応をさらに足すときは、まず**この掃き出しの形を確かめる**。
「全件 × 全反応」の掃除は品質の要なので減らせないが、
**`detect` の下見と `apply` の実行を分ける**だけで呼び出し回数は桁が変わる。
同じ形の掃き出しは `tests.js` に他にもある（作図の掃除・候補の実行可能性）ので、
次に門番が近づいたらそちらも同じ手で軽くする。

### 10.11 「入試に出るのに、アプリに無い反応」の全数突き合わせ（2026-08-27・調査のみ）

⚠ **この節も1行も実装していない。**⚠ **発注は「§10.6 として追記」だったが、
§10.6 はすでに「酸塩基（次に大きい穴・7件）」で埋まっている**ので、章の末尾に §10.11 として置いた。
§10.5（まだ埋めていない穴）の**続き**として読むこと。

**きっかけはユーザーの物差し**（2026-08-26）:

> **他にも実装していない反応はたくさんあるので。入試に出るものについては網羅する方向でよいかと。**

★ **＝「足したい反応」を思いつきで挙げるのをやめ、入試の頻度で並べる、という決め。**
これまで（§10.1）は **qa の283項目**から需要を測っていたが、その母数は「知識項目」であって
「出題」ではない。ここでは**入試そのもの**を母数にして数え直した。

#### A. 粒度をどう決めたか —— **手筋台帳をそのまま使う。ただし `kind` では切れない**

★ **手筋台帳は実在する**: `マイドライブ/化学/問題集/_解析/db/moves.json`。
`{_readme, moves}` の形で **moves は 257項目**、各項目は
`id / name / kind / level / premise / conclusion / caution / related` を持つ。
`problems.jsonl` の `routes[].steps[].move` がこの `id` を指す。

**そのまま粒度に使えた**（自分で粒度を作り直す必要はなかった）。理由は台帳の単位が
「**解くのに要る操作**」で、reactor のルールの単位（**分子をどう書き換えるか**）とほぼ1対1に対応するから。
例: `side-chain-oxidation` ↔ `oxidize_side_chain`、`ester-hydrolysis` ↔ `hydrolysis_ester`。

⚠ **ただし `kind`（分類）で反応を切り出すことはできない。**
`_readme` は kind を **6種**（読み取り／絞り込み／変換／計算／数え上げ／組み立て）と書くが、
**実際には30種に増えている**（反応38・高分子22・変換17・糖10・生体9・立体7・検出6・油脂4…）。
★ **台帳の中身が readme を追い越している。**しかも反応は kind をまたいで散らばる:

| 例 | kind | 実体 |
|---|---|---|
| `intramolecular-anhydride`（フタル酸 → 無水フタル酸） | **位置決定** | ⚠ **立派な反応**。オルト位を決める道具として使うので「位置決定」に入っている |
| `separation-funnel`（分液漏斗） | 変換 | ⚠ **反応ではない**（分離操作）。reactor の守備範囲外 |
| `amine-acetyl-protect-nitration` | 標準 | 反応（保護 → ニトロ化 → 脱保護） |

→ **こちらの判断で「分子を書き換える操作か」で選び直した**。除いたのは
分液・沸点比較・数え上げ・計算・検出（呈色）で、検出は reactor ではなく `DETECTION_TESTS`（5本）の担当。

#### B. 母数と数え方 —— ⚠ **数が2つある。混ぜないこと**

| | 何を数えるか | 母数 | 使いどころ |
|---|---|---|---|
| ★ **入試件数**（本節の主） | その反応が**大問の中に現れる**か | **563大問**（2019〜2022年入試。年別 150/143/140/130） | 頻度の並べ替え |
| **手筋件数** | その手筋を**解く手順として実際に使う**か | ⚠ **423大問** | 参考値 |

⚠⚠ **手筋件数を主にしなかった理由は2つある。どちらも実測。**

1. ★ **`routes` は 2022年入試に1件も入っていない。**`routes[].steps[].move` を持つのは
   **423大問だけ**で、その内訳は 2019年入試 150／2020年 142／2021年 131 ＝
   **book 2023（＝2022年入試）の130大問は routes がゼロ**。
   ⚠ **記憶にある「3年ぶん433大問」はこの routes の母数**であって、
   物質名で引ける母数（563）とは別物。**「563が正しくて433が古い」ではなく、指しているものが違う。**
2. ★ **後から台帳に足した手は、既存の大問に遡って貼られていない。**
   `ring-cleavage-to-diacid` と `ethylene-wacker-oxidation` は
   どちらも **2026-08-16 の悉皆第3・第4便で採用**された新しい手で、
   routes での出現は **2件・1件**しかない。⚠ **同じものを §10.3-e / §10.3-f が
   目で数えたときは 14件・13件**だった ＝ **桁で外れる**。
   ★ **新しく足したい反応ほど、手筋件数は小さく出る**（＝ 順位付けに使うといちばん危ない）。

**入試件数の取り方**（★ 後で数え直せるように）: `problems.jsonl` の1件を **JSON 全文**にして
正規表現を当て、**大問単位**で数える（§10.3-e / §10.3-f と同じ流儀）。
⚠ **伏せ字**（`ポリエチレン` が `エチレン` に当たる類）は、当たり方が問題になる語だけに入れた
——今回の語はほとんどが**固有名詞**（無水フタル酸・ε-カプロラクタム・ジアゾ・クメン）なので、
§10.3-f のエチレンのような伏せ字は要らなかった。**代わりに当たりを目で見た**（下の「使った語」参照）。
⚠ **それでも広めに当たる語が3つある**（`還元` `水素化` `付加縮合`）ので、
その3件は**表の中で ⚠ を付けて上限扱い**にしている。

#### C. アプリの側 —— `REACTION_RULES` は **42本**（図を変える 37／`info` 5）

`assembler/reactor.js` の `REACTION_RULES`（2645〜4205行）を数えた。
配列に直接書かれた **39本** ＋ `...HYDROGEN_HALIDE_RULES`（`HYDROGEN_HALIDES` の表から作る
`add_hbr` / `add_hcl` / `add_hi`）の **3本** ＝ **42本**。

**`info: true`（図を変えず、理由だけ返す）は5本**:
`oxidize_tertiary_info`（3級アルコール）／`oxidation_out_of_scope_info`（末端 C=C・環内 C=C・
C≡C・ヘテロ原子を含む分子）／`esterification_phenol_info`（フェノールの直接エステル化）／
`aromatic_deactivated_info`（不活性化された環）／`condensation_polymer_info`（縮合重合になる組み合わせ）。

**図を変える37本**（category ごと）:
酸化7（1級→アルデヒド／1級→一気に酸／2級→ケトン／アルデヒド→酸／側鎖酸化／酸化開裂／ヨードホルム）、
脱水2（分子内・分子間）、縮合3（エステル化／アセチル化／グリコシド）、
重合5（付加・アルキン・ジエン・加硫・縮合）、付加6（Br₂／H₂／H₂O／HBr／HCl／HI）、
芳香族置換4（ニトロ化・スルホン化・塩素化・活性化環の臭素化）、
加水分解3（酸無水物・エステル・グリコシド）、酸塩基3（中和・弱酸の遊離・Na との反応）、
けん化1、糖の環化・開環3。

⚠ **呈色・検出（銀鏡・フェーリング・FeCl₃・ニンヒドリン・NaHCO₃）は `DETECTION_TESTS` の5本**で、
`REACTION_RULES` には入っていない（§2.5 の決め）。**この5本は下の表の対象外**。

---

#### D. ★★ 「出るのに無い」を頻度順に並べる

⚠ **「アプリの状態」の `info` だけ**＝ 押しても図が変わらず、文章で断るもの。
⚠ **「実装の重さ」は粗い見積り**。★ **何が難しいのかを一言で書く**のが目的で、工数の数字ではない。

| # | 反応 | 入試件数<br>/563 | 手筋件数<br>/423 | 教科書（東書 R5） | アプリ | 実装の重さ（何が難しいか） |
|---|---|---|---|---|---|---|
| 1 | ⚠ **付加縮合（フェノール樹脂・尿素樹脂・メラミン樹脂）** | **64** | 13 | **本文**（6編） | 無い | ⚠ **重い**。三次元網目は**図に描けない**（§14 の「一直線に伸ばす」約束と正面衝突） |
| 2 | 環状ヘミアセタール（糖が環を巻く） | **64** | 29 | **PLUS（発展枠）** p.219 | **グルコース専用の3本だけ**有る | 中。分子内で -CHO と -OH をつなぐ一般ルール。**α/β の決め方**が糖ごとに違う |
| 3 | ★ **分子内脱水 → 酸無水物**（フタル酸→無水フタル酸／マレイン酸→無水マレイン酸） | **53** | **41** | ★ **本文** p.184・p.157 | ⚠ **無い（逆の加水分解だけ有る）** | ★ **軽い**。オルト位の -COOH 2つから水1分子。**生成物は登録済み**（無水フタル酸・無水マレイン酸とも有り） |
| 4 | クメン法（フェノールの工業的製法） | **48** | 34 | **本文** p.181 | 無い | ⚠ **重い**。3段（アルキル化→空気酸化→酸分解）。中間体クメンヒドロペルオキシドが **-O-O-**（過酸化物の価標） |
| 5 | ★ **ニトロ化合物の還元 → 芳香族アミン** | **47**<br>⚠ 上限 | 36 | ★ **本文** p.188（Sn/HCl・工業は H₂/Ni） | 無い | 中。-NO₂ → -NH₂ は**原子の置き換えだけ**。⚠ ただし教科書は**アニリン塩酸塩を経る2段**で書く（塩の段はイオン＝ §10.6 の壁）。**アニリン・ニトロベンゼンとも登録済み** |
| 6 | ジアゾ化・ジアゾカップリング | **47** | 25 | **本文** p.190 | 無い | ⚠ **重い**。ジアゾニウム塩が**陽イオン**。§10.6 で「当面扱わない」とした対イオンの問題そのもの |
| 7 | ★ **開環重合（ε-カプロラクタム → ナイロン6）** | **44** | 19 | ★ **本文** p.243 | 無い | 中。**単量体 ε-カプロラクタムは登録済み**。§15（1分子からでも重合へ行ける入口）がそのまま使える |
| 8 | 共重合 | **43** | 12 | **本文** p.252 | 無い | ⚠ **§11.6 で「入れない」と判断済み**（並ぶ順が決まらない）。順位のためだけに載せる |
| 9 | ★ **ベンゼン環の水素化 → シクロヘキサン** | **35**<br>⚠ 上限 | 6 | ★ **本文** p.177（Pt/Ni・3H₂） | 無い（`add_h2` は `multipleBondSites` が芳香族を外す） | ★ **軽い**。門番を1つ緩めるだけ。⚠ ただし**「ベンゼンは付加しにくい」を壊さない書き方**が要る |
| 10 | ビニロン（PVA のアセタール化） | **34** | 2 | **本文** p.246 | 無い | 中。**高分子鎖の途中**の -OH 2つ ＋ HCHO で環をつくる（鎖の書き換え） |
| 11 | コルベ・シュミット（フェノキシド＋CO₂→サリチル酸） | **32** | 20 | **本文** p.185 | 無い | 中。⚠ **CO₂ の瓶が要る**。§10.9 が「瓶を足す前に区分をもう一段割る」と申し送っている |
| 12 | 弱塩基の遊離（アニリン塩酸塩＋NaOH） | **32** | 4 | **本文** p.189 | 無い | ⚠ **重い**。アンモニウム塩＝対イオン。§10.6 の「当面扱わない2件」と同じ壁 |
| 13 | ★ **カルボン酸＋アミン → アミド（単発）** | 32（手筋） | 32 | **本文**（アミドの加水分解の逆として p.189） | ⚠ **縮合重合の中だけ**（`links.length < 3` で4分子以上を要求） | ★ **軽い**。`esterification` の `detect` が `ALCOHOL_TYPES` しか見ていないので、**相手にアミンを足す**だけ |
| 14 | アルコール発酵（グルコース → エタノール＋CO₂） | **30** | 18 | **本文** p.142 | 無い | 軽い。⚠ ただし **CO₂ が出る**＝ §10.3-b の「出ていくものは描かず caption で補う」が当たる |
| 15 | イオン交換樹脂 | **25** | 11 | **本文** p.252 | 無い | ⚠ **重い**。架橋（網目）＋イオン交換（電荷）で、1・6・12 の難しさが両方乗る |
| 16 | 炭化カルシウム＋水 → アセチレン | **19** | 7 | **本文** p.132 | 無い | ⚠ **重い**。CaC₂ は**無機の塩**。Ca が元素表に無い |
| 17 | 多糖のメチル化（枝分かれの分析） | **17** | 6 | ⚠ **載らない** | 無い | 中。-OH を -OCH₃ に**まとめて**替える。どこまで替えるかの線引きが要る |
| 18 | ジスルフィド結合（-SH ×2 → -S-S-） | **17** | 6 | **本文**（6編・タンパク質） | 無い | ★ **軽い**。**S は元素表に有る**。2分子（または分子内）の -SH をつなぐだけ |
| 19 | 脱炭酸（-COONa ＋ NaOH 加熱 → 炭素が1つ減る） | **17** | 10 | ⚠ **語としては無い**。ただし **p.121 図4 に `CH₃COONa+NaOH → CH₄` の式がある**（メタンの実験室製法） | 無い | 軽い。⚠ **CO₂（炭酸塩）が出る**＝ 14 と同じ扱い |
| 20 | ラクトン（分子内エステル化） | **16** | 9 | ⚠ **載らない** | ⚠ **無い（`esterification` が明示的に除外）** | ★ **軽い**。`if (comp.has(al.atomIds[0])) return;` を外し、**環の大きさ（5・6員）で門番**を立てる |
| 21 | ベンゼンの光塩素付加（C₆H₆Cl₆） | **15** | 8 | **本文** p.177 | 無い（置換3本だけ有る） | 中。環の6箇所に一度に Cl を付ける。9 とほぼ同じ形 |
| 22 | アセチレン3分子 → ベンゼン | **15** | 13 | **本文** p.131 | 無い | 中。3分子を1つの環にまとめる。既存の重合の枠組みに近い |
| 23 | カルボニルの還元 → アルコール | **15**<br>⚠ 上限 | 8 | ⚠ **還元剤（NaBH₄ 等）は0件**。H₂/Ni の付加は有る | 無い | 中。⚠ **アプリには「還元」というルールが1本も無い**（`add_h2` は付加であって還元の札ではない） |
| 24 | カルボン酸塩の乾留 → ケトン | **14** | 6 | **本文** p.152 | 無い | 中。⚠ **Ca 塩＝2価金属**。いまの塩は Na/K の「線1本」（§10.6 の流儀）なので、2本引く形が要る |
| 25 | ★ **環内の C=C の酸化開裂** | **14**<br>（§10.3-e が目で精査） | 2<br>⚠ 台帳が新しい | ⚠ **載らない**（傍用 セミナー演習C・駿台 p.81-82 には有る） | ⚠ **`info` だけ** | ★★ **いちばん軽い**。§10.3-d で**図はもう描けると実測済み**（シクロヘキセン→アジピン酸が正準コード一致）。残りは **caption を「1つの分子になりました」と言い分けるだけ** |
| 26 | アルカンの光ハロゲン置換 | **13** | 3 | **本文** p.121-122 | 無い | 中。⚠ **生成物が4段**（クロロメタン→…→テトラクロロメタン）に分かれ、どこで止めるかの決めが要る |
| 27 | ★ **ワッカー法（エチレン → アセトアルデヒド）** | **12**<br>（§10.3-f は13） | 1<br>⚠ 台帳が新しい | ★ **本文** p.150（式のみ。⚠ **「ワッカー法」の名前は無い**） | 無い | 中。**瓶が1本要る**（O₂ ／ PdCl₂・CuCl₂）。★ **図は素直**——炭素2個のまま、分子が消えない |
| 28 | 末端の C=C の酸化開裂 | **11**<br>（§10.3-e が目で精査） | — | **発展** p.129（KMnO₄ の文脈） | ⚠ **`info` だけ** | 中。図は描ける。⚠ **caption を2か所直さないと嘘になる**（§10.3-e ②の推奨） |
| 29 | アルカリ融解（ベンゼンスルホン酸Na → フェノール） | **10** | — | **本文** p.181 | 無い | 中。4 と同じ「フェノールの製法」束 |
| 30 | クロロベンゼン → フェノール（高温高圧 NaOH） | **8** | — | **本文** p.181 | 無い | 中。同上 |
| 31 | フリーデル・クラフツ アシル化 | **7** | 1 | ⚠ **載らない** | 無い | 中。酸塩化物が要る（-COCl は価標としては置ける） |
| 32 | エステル交換 | **3** | 1 | ⚠ **載らない** | 無い | 軽い。件数が小さい |

⚠ **「上限」印の3件**（5・9・23）は、`還元` `水素化` `付加` という**広い語**で当てているので、
**当たりの一部は別の反応の話**（例: 9 には「ニトロ基だけが還元されベンゼン環は水素化されない」を
問う大問が入る）。★ **順位を1つ2つ上下させる程度で、位置は変わらない。**

##### 使った語（★ 後で数え直せるように。当たりは目で見た）

| # | 正規表現 |
|---|---|
| 1 | `フェノール樹脂|ノボラック|レゾール|付加縮合|尿素樹脂|メラミン樹脂` |
| 2 | `ヘミアセタール|環状構造[^"]{0,20}(とる|巻)|環を巻` |
| 3 | `無水フタル酸|無水マレイン酸|無水コハク酸|無水グルタル酸` |
| 4 | `クメン` |
| 5 | `(ニトロ[^"]{0,60}(還元|スズ|Sn|亜鉛|Zn))|((スズ|Sn|鉄)[^"]{0,30}塩酸[^"]{0,80}(還元|アニリン))` |
| 6 | `ジアゾ` |
| 7 | `開環重合|カプロラクタム` |
| 8 | `共重合` |
| 9 | `(ベンゼン(環)?|ナフタレン)[^"]{0,50}(水素[^"]{0,12}(付加|化)|H2 ?(を|の)?付加)|シクロヘキサン[^"]{0,50}(ベンゼン|水素化|付加)` |
| 10 | `ビニロン|アセタール化` |
| 11 | `コルベ|(ナトリウムフェノキシド|フェノキシドナトリウム)[^"]{0,80}(二酸化炭素|CO2|高温高圧)|(高温高圧|高温・高圧)[^"]{0,60}二酸化炭素` |
| 12 | `アニリン塩酸塩|弱塩基[^"]{0,10}遊離|(塩酸塩|アンモニウム塩)[^"]{0,40}(水酸化ナトリウム|NaOH|強塩基)` |
| 14 | `発酵|チマーゼ|酵母` |
| 15 | `イオン交換` |
| 16 | `炭化カルシウム|カーバイド|CaC2` |
| 17 | `メチル化` |
| 18 | `ジスルフィド|シスチン` |
| 19 | `脱炭酸` |
| 20 | `ラクトン(?!ース)` ⚠ **`(?!ース)` が無いと「ラクトース」に当たる** |
| 21 | `C6H6Cl6|ヘキサクロロ|光[^"]{0,20}(塩素|付加)` |
| 22 | `(アセチレン[^"]{0,60}(3分子|三分子|3 分子))|(ベンゼン[^"]{0,40}アセチレン[^"]{0,30}(重合|3))` |
| 23 | `水素化ホウ素ナトリウム|NaBH4|LiAlH4|水素化アルミニウム|(アルデヒド|ケトン|カルボニル)[^"]{0,40}還元[^"]{0,40}(アルコール|第一級|第二級|1級|2級)|還元[^"]{0,25}(第一級|第二級)アルコール` |
| 24 | `乾留` |
| 26 | `(メタン|アルカン|エタン)[^"]{0,40}(塩素|臭素)[^"]{0,30}置換|クロロメタン|塩化メチル` |
| 27 | `ワッカー|PdCl2|塩化パラジウム` ⚠ **`パラジウム` を単独で入れると 15件に増える**（付加の触媒に当たる） |
| 29 | `アルカリ融解` |
| 30 | `クロロベンゼン[^"]{0,60}(水酸化ナトリウム|NaOH|高温高圧)` |
| 31 | `フリーデル|アシル化` |
| 32 | `エステル交換` |

教科書の欄は **東京書籍『化学』令和5年検定済・全7分冊**を `fitz` で全ページ走査し、
**当たったページの本文を読んで**「本文／発展（PLUS）／載らない」を決めた
（§10.3-e と同じ資料・同じやり方。⚠ **ページ番号は紙面のもの**）。

---

#### E. ⚠ 「アプリに有るのに入試に出ない」

★ **逆側も測った。多くはない ——「網羅していないから空振りが多い」のではなく、
すでに有る37本はよく出るものに当たっている。**

| ルール | 入試件数/563 | 判断 |
|---|---|---|
| **`add_hi`（HI の付加）** | ⚠ **1件**。しかもその1件は**ハロゲン化水素の沸点**の話で、付加ではない ＝ **実質0** | ★ **消さなくてよい**。§10.4 が `HYDROGEN_HALIDES` の**表1つから瓶とルールを作る**形にしたので、HI を外しても軽くならない。「HI > HBr > HCl の順に付加しやすい」は教科書の記述で、**3本そろって初めて言える** |
| **`hydrolysis_anhydride`（酸無水物の加水分解）** | ⚠ **2件**。⚠⚠ **しかも2件とも「加熱すると酸無水物になる」＝ 逆向きの話** | ⚠ **向きが逆になっている**。上の表 #3（**分子内脱水 → 酸無水物・53件**）が無いまま**戻す方だけ**が有る。★ **「有るのに出ない」ではなく「片道になっている」** |
| `condensation_glycoside`（単糖2分子 → 二糖） | **12件**（加水分解の向きは 20〜22件） | 出る。⚠ ただし**作る向きは少数派**。教材として対で要るので正当 |
| `alkyne_polymerization`（→ ポリアセチレン） | **8件** | 出る。導電性高分子として定着している |
| `vulcanization`（加硫） | **17件** | 出る |
| `bromination_activated_ring`（→ トリブロモフェノール） | **17件** | 出る（§10.2 が「逆を教えていた」穴を埋めた分） |
| `cyclize_glucose_*` / `open_glucopyranose` | **76件** | ★ **よく出る**（α/β・アノマー・鎖状⇄環状） |
| `iodoform` / `saponification` | **121件 / 131件** | ★ **最頻クラス** |
| `info` 5本（3級アルコール・フェノールの直接エステル化ほか） | ⚠ **測れない** | 「**起こらない**」を教える札なので、出題語の頻度では測れない。**教材として正当**（DEVELOPMENT.md 4章「判断できないものは出さない」の表側） |

⚠ **＝ はっきり「出ないのに有る」と言えるのは `add_hi` の1本だけ**で、
それも**消す理由にならない**（表から生成しているので単独では減らせない）。
★ **いま問題なのは「余っている」ことではなく、`hydrolysis_anhydride` のように
片道しか無いこと**のほう。

---

#### F. ★ 推奨 —— 次に足す5件（⚠ **決めるのはユーザー**）

⚠ **件数だけで決めていない。**（a）入試件数、（b）教科書に載るか、（c）実装の重さ、
（d）**この教材の芯（紙面の構造式を読めるようにする）に合うか**の4つで並べた。

| 順 | 反応 | なぜ |
|---|---|---|
| **1** | ★ **環内の C=C の酸化開裂**（#25・14件） | ⚠ **いちばん軽く、いちばん近い**。§10.3-d の実測で**図はもう描けている**（シクロヘキセン→アジピン酸が登録エントリと正準コード一致）ので、残りは **caption を「環が開いて、両端にカルボキシ基をもつ1つの分子になりました」と言い分けるだけ**。★ 入試での使われ方が**「1分子で出てくるのが環の印」という判別の道具**で、**図で見せる価値がいちばん高い**。⚠ **申し送りの訂正: §10.3-e は「グルタル酸は未登録」と書いたが、いま確認したら `グルタル酸（ペンタン二酸）` は登録されている**（熊本大 2022 の答えが描ける） |
| **2** | ★ **分子内脱水 → 酸無水物**（#3・53件） | ★ **本文・53件・生成物が全部登録済み**（無水フタル酸・無水マレイン酸・フタル酸・マレイン酸）。⚠ **しかも `hydrolysis_anhydride` の逆向きが欠けている**という形の穴（上の E）。★ **側鎖酸化と2段で効く**——台帳の `side-chain-oxidation` の `caution` が名指しで「**intramolecular-anhydride と続けて効く形が定番**（側鎖の位置を -COOH に写し、隣り合うかを分子内脱水で見る）」と書いている。**アプリは1段目だけ実行できて2段目で止まる** |
| **3** | ★ **ニトロ化合物の還元 → アニリン**（#5・47件） | ★ **本文・47件・アニリンもニトロベンゼンも登録済み**。図の書き換えは **-NO₂ の O を外して H を出す**だけで素直。⚠ **決めが1つ要る**——教科書は「アニリン塩酸塩を経て、NaOH で遊離」の**2段**で書くが、塩の段はイオン（§10.6 の壁）。★ **1段で直接アニリンにし、caption で「実際は塩の形で得られ、NaOH で遊離させる」と補う**なら、§10.3-b の原則（**残るものを描き、出ていくものは文で補う**）がそのまま当たる |
| **4** | ★ **開環重合（ε-カプロラクタム → ナイロン6）**（#7・44件） | ★ **本文・44件・単量体が登録済み**。⚠ **いまの重合3本（付加・ジエン・縮合）はどれも「同じ単量体を並べる」形**で、**環を開いてつなぐ形だけが無い**。★ **§15（1分子からでも重合へ行ける入口）と §14（一直線に伸ばす）がそのまま使える**ので、重合まわりの作りに新しい概念を持ち込まない |
| **5** | ★ **カルボン酸＋アミン → アミド（単発）**（#13） | ★ **いちばん安い**。`esterification.detect` が相手を `ALCOHOL_TYPES` に限っているだけで、**アミンを足せば通る**（`apply` の水の抜き方は同じ）。⚠ **いまは「4分子以上並べて縮合重合」でしかアミド結合を作れない**——アセトアニリドは無水酢酸から作れるのに、**酢酸＋アニリンからは作れない**という片道になっている |

**次点2件**（★ どちらも本文・図が素直。⚠ 上の5件より一手多い）:

- **ベンゼン環の水素化**（#9・35件）… `multipleBondSites` の門番を緩めるだけだが、
  ⚠ **「ベンゼンは付加しにくい」という教科書の順序を壊さない書き方**が要る
  （`aromatic_deactivated_info` と同じ棚に「特別な条件では起こる」を置く形になるはず）
- ★ **ワッカー法**（#27・12件）… **ユーザーが「足す」と決めている**。
  ⚠ **瓶が1本増える**ので、§10.9 の申し送り（**瓶を足す前に区分をもう一段割る**）を先に片づけるか、
  区分を割らずに1本だけ足すかの判断が要る。★ **図は素直**（炭素2個のまま・分子が消えない）

⚠ **入れないほうがよいもの**: #1 付加縮合（**三次元網目が描けない**）・#6 ジアゾ化・
#12 弱塩基の遊離・#15 イオン交換樹脂（**どれも電荷／対イオンが要る** ＝ §10.6 の壁を先に決める話）・
#8 共重合（**§11.6 で判断済み**）。★ **件数は大きいが、足りないのは反応ではなくモデル。**

---

**未確認のまま残したこと**（⚠ 黙って落とさない）

- ⚠ **入試件数は「その語が大問に出るか」であって「その反応を実行しないと解けないか」ではない。**
  両者を分けて数えられるのは routes が入っている 423大問だけで、**2022年入試（130大問）は測れない**
- ⚠ **2019年より前は引けない**（`index.json` に12年ぶん4,782大問の索引はあるが**物質名を持たない**）。
  ★ **件数はすべて 2019〜2022年入試・有機563大問が母数**
- ⚠ **「上限」印の3件**（ニトロ還元・ベンゼン環の水素化・カルボニルの還元）は
  当たりを**全件は見ていない**（各6件ずつ目で確かめた）。真の値はこれより小さい
- ⚠ **手筋台帳の `kind` は readme と食い違っている**（6種と書いてあるが実際は30種）。
  ★ **台帳側の問題**であって、この節の数え方の問題ではないが、
  **次に台帳を機械で切る人は kind を信用しないこと**
- ⚠ **アプリ側は `reactor.js` を読んで判断した**（ブラウザで `detect` を回してはいない）。
  「無い」と書いたものは `REACTION_RULES` に該当ルールが無いことと、
  既存ルールの `detect` が明示的に除外していること（`esterification` の分子内除外、
  `multipleBondSites` の芳香族除外）を**コードで確かめた**が、
  ⚠ **実機で1件ずつ押して確かめてはいない**
- ⚠ **compounds.json / stages.json の登録の有無は名前で引いた**。
  ★ 名前に括弧書きが付くもの（`クメン（イソプロピルベンゼン）`・`グルタル酸（ペンタン二酸）`）が
  あるので、**完全一致で引くと「無い」と誤読する**（実際に1度そうなった）
- ⚠ **画像スキャンの問題集5冊**（セミナー化学26・重要問題集・新演習・基礎問題精講・
  駿台理系標準）は今回も走査していない（§10.3-e / §10.3-f と同じ）
- ⚠ **傍用問題集・参考書での扱い**は、この節では調べていない（教科書と入試だけ）。
  ★ §10.3-e が①②についてやったような**5冊の突き合わせ**は、
  上位に残った反応を実際に足すときに、その反応だけについてやること

---

## 11. 「図はあるのに反応から行けない」4件を埋めた（v880〜v883・2026-08-07）

§10.5 の申し送りのうち2件と、そこに載っていなかった**重合の穴**2件を埋めた。
共通する症状は **「`compounds.json` に図はあるのに、反応実行モードからそこへ行けない」**
——— 名称ライブラリから呼べば描けるが、**自分の手で作れない**、という片道の状態だった。

| ルール | 瓶 | 行けるようになった図 | qa の項目 |
|---|---|---|---|
| `alkyne_polymerization` | なし（重合） | ポリアセチレン | `org.poly.conducting`（4件） |
| `condensation_polymerization` | なし（重合） | ナイロン66 ／ PET（PET は図が未登録・§11.5） | `org.poly.nylon66` `org.poly.pet` |
| `hydrolysis_glycoside` | 希硫酸（**既存**） | β-D-グルコース ／ β-D-フルクトフラノース | 2件（§10.5） |
| `react_sodium` | **金属ナトリウム（新設・21本目）** | ナトリウムフェノキシド ／ 酢酸ナトリウム | `org.alcohol.na` `org.alcohol.ether-props`（2件） |

### 11.1 重合は既存ルールを広げず、**横に足す**

「アセチレンも重合できるようにする」のは、一見すると `vinylBonds`（`addition_polymerization`
の下ごしらえ）を `type === 3` まで広げるだけに見える。**それをやってはいけない。**
`vinylBonds` は `conjugatedDienes`（1,4-付加重合）と `vulcanizablePairs`（加硫）も使っており、
三重結合を混ぜると**ジエンの判定と加硫の相手選びまで一緒に変わる**。
ルールを1本足すほうが、壊れる範囲が読める。

同じ理由で、対象は**アセチレンだけ**にした（分子全体が C≡C の2原子）。
1-アルキンを重合させると頭-尾の並びが問題になるが、ビニル系のように
「置換基の数で頭を決める」根拠が教科書に無い。**根拠のない一般化はしない。**

縮合重合も、既存の `esterification` を「くり返す」形にはしていない。
エステル化は**1本の結合を作る**ルールで、そこにくり返しを持ち込むと
「どこまで伸ばすか」がルールの中に入ってしまう。
**画面に並んでいる単量体を全部つなぐ**という `addition_polymerization` の作法に合わせた。

### 11.2 縮合重合が出る条件は「2組（4分子）以上」

1組（酸1・相手1）のときは、できるのは**ふつうのエステル化／アミド化**であって重合ではない。
そこで:

- **2組以上そろったときだけ** `condensation_polymerization` を出す
- 1組のときは従来どおり `condensation_polymer_info`（説明）だけを出す
- **両方が同時に出ることはない**（`condensation_polymer_info` の `detect` が、
  実行できるときは空を返す）＝ 同じことを2つのボタンで言わない

説明側の文面には「もう1組ずつ並べると『縮合重合』が選べる」を足した。
案内が**次の操作**を指すようにしておかないと、説明で行き止まりになる。

鎖の端は `R` で止める。**残っている -COOH は -OH を落として -CO-R にする**
（次に来るのはアミン／アルコールなので、そこでも水がとれる）。
この扱いにすると、アジピン酸3＋ヘキサメチレンジアミン3 の生成物が
**登録エントリ `nylon-66` と正準コードまで一致する**（下）。

### 11.3 `detect` の数える単位（第1段・第2段の教訓の確認）

4本とも「1分子だけを見る」ことを、実装と試験の両方で確かめてある。

| ルール | 1分子に閉じている根拠 |
|---|---|
| `alkyne_polymerization` | `componentOf` で連結成分を切り出し、**重原子が2個**であることを見る |
| `condensation_polymerization` | 官能基は `findFunctionalGroups` がキャンバス全体を返すので、**代表原子がどの連結成分に属すか**で振り分けてから数える。単量体がそろっているかは `componentCode`（成分だけを複製してから正準コード）で見る |
| `hydrolysis_glycoside` | 見るのは**1つの環の近所だけ**（`sugarRingOf` は環の原子を BFS で拾う）。キャンバス全体の正準コードは数えない |
| `react_sodium` | 官能基1つ（酸素1個）を見るだけ |

⚠ **試験でも固定した**: `GH1` が「二糖を**2分子**置いたら候補は**2件**」
「二糖＋エタノールでも候補は**1件**」を主張する。
全体数え（§8.1 で3件出た壊れ方）に戻ると、どちらも赤くなる。

**座標で決める・原子IDに頼らない**も4本すべてで守っている（`chem-atom-id-hazard`）。
アセチレンの左右、縮合重合でどの基から使うか、スクロースのようにアノマー炭素が
両側にあるときどちらを切るか —— すべて x → y の順で決めている。

### 11.4 一致の主張（空振りの緑を避ける）

生成物が**登録エントリと同じ正準コード**になることを7組で主張する:

| 反応 | 生成物 | 突き合わせた登録エントリ |
|---|---|---|
| アセチレン ×3 | ポリアセチレン | `polyacetylene` |
| アジピン酸 ×3 ＋ ヘキサメチレンジアミン ×3 | ナイロン66 | `nylon-66` |
| マルトース ＋ H₂O | グルコース ×2 | `beta-d-glucose` |
| セロビオース ＋ H₂O | グルコース ×2 | `beta-d-glucose` |
| スクロース ＋ H₂O | グルコース ＋ フルクトース | `beta-d-glucose` ＋ `beta-d-fructofuranose` |
| フェノール ＋ Na | ナトリウムフェノキシド | `sodium-phenoxide` |
| 酢酸 ＋ Na | 酢酸ナトリウム | `sodium-acetate` |

**否定対照**は3種類そろえた:

1. **陰性の数え上げ**（同じ数え方を陽性にも陰性にも掛ける）——
   アセチレン重合5件・縮合重合5件・グリコシド加水分解9件・ナトリウム8件が、すべて0件
2. **突き合わせ方そのものの対照** —— 「ポリアセチレンとポリビニルアルコール」
   「グルコースとフルクトフラノース」「酢酸ナトリウムとナトリウムフェノキシド」が
   **互いに違うコードになる**ことを先に主張する。ここが同じなら上の一致は空振りの緑
3. **往復** —— エタノール →[Na]→ ナトリウムエトキシド →[希硫酸]→ エタノールで
   正準コードが戻る（`liberate_weak_acid` が既にアルコキシドにも効く）

PET は**図が未登録**なので構造で主張する（C30 O12 R2・エステル結合5か所・水6分子）。
ラクトースの生成物（グルコース＋ガラクトース）は**立体だけが違う**ので、
構造の正準コードでは区別できない。そこは「ピラノース2つ」までしか主張していない。

⚠ **糖の名称カードは「立体で決まります」と返る**（実測・v882）。
マルトースを加水分解すると、名称カードは「グルコース」ではなく
**「アロース／ガラクトース ほか3種 のどれか（立体で決まります）」**と出る。
これは加水分解の不具合ではなく、**このアプリの既存の振る舞い**である ——
`β-D-グルコース` を名称ライブラリから呼び出しただけでも同じ文が返る
（登録エントリ側に立体が無い糖は、構造だけでは1つに決まらない）。
マルトース自身も「セロビオース／マルトース のどれか」と出る。
**したがって加水分解で名前の精度は1段も落ちていない**が、
糖のレーンが立体を入れたら、この反応の生成物もそのまま名前が決まるようになる。

### 11.5 瓶が 20 → 21 本になった（試薬パレット §10.2 の申し送りに触れる）

金属ナトリウムは新しい瓶なので、`DESIGN_reagent_palette.md` §10.2 の
「**これ以上『変えるもの』を増やすなら、区分をもう一段割ることを先に決める**」に触れる。
それでも足した理由:

- 同書 **§3.1 の「変換する試薬15本」の13番目が最初から「金属ナトリウム Na」**であり、
  **§5 第4段に「瓶とルールを足すだけで載る」と書いてある**。新規の判断ではなく予定分の消化
- 塩は線1本の共有結合として書く流儀（v353）に**そのまま乗る** ＝ 電荷モデルは要らない
- **水酸化ナトリウム水溶液の隣に置くことに意味がある**。
  アルコールは NaOH とは中和しないが Na とは反応して水素を出す ——
  この対比が qa の2項目そのもので、**瓶が並んでいないと画面で比べられない**

⚠ **区分をもう一段割る判断は、まだ済んでいない**（21本目でいよいよ近い）。
320px では2列固定なので、21本は瓶だけで 11行 ＝ 3画面ぶん。
`MM9` と `tools/check-mobile.mjs` で**横あふれ0・32px未満0**は保っているが、
**次に「変えるもの」を足すときは、先に区分（付加／置換／酸化還元／加水分解／酸塩基…）を決めること。**

**H₂ は描かない。** とれる水素はもともと自動水素（明示原子ではない）なので、
Na が付いた時点で自動的に消える —— `neutralize_naoh` が水を描かないのと同じ流儀で、
「画面に無い分子は描かない」を守っている。発生は `caption` が言う。

### 11.6 入れなかったもの（判断と理由）

| 見送ったもの | 理由 |
|---|---|
| ~~**ポリエチレンテレフタラート（PET）の図の登録**~~ | **反応は入った**（テレフタル酸＋エチレングリコールで作れる）が、`compounds.json` は「末尾に追記のみ・整形し直さない」規約の資産で、このレーンは反応ルールの担当。**§11.7 に申し送る** → **v942 で登録ずみ** |
| **共重合**（2種の単量体を1本の鎖に・qa 3件） | 高校範囲外。`addition_polymerization` も `condensation_polymerization` も「単量体は1種類」を正準コードで確かめている。混ぜると**どの順で並ぶか**が決まらない |
| **ナイロン6（ε-カプロラクタムの開環重合）** | 単量体（ε-カプロラクタム）が未登録。環状アミドを開いてつなぐ操作は縮合重合とは別の形（水がとれない）なので、ルールも別に要る |
| **1-アルキンの付加重合** | 頭-尾の並びの根拠が教科書に無い（§11.1） |
| **多糖（デンプン・セルロース）の加水分解** | 図が未登録。ルールとしては `hydrolysis_glycoside` がそのまま効く（グリコシド結合を1つずつ切る）ので、**図を足せば反応は動く** ⚠ **v942 に実測を追記**: 反応は動くが、高分子の規約（両端を R で止める）と噛み合わず**切ってできる断片が両方とも「（名称未登録）」**になる。判断と数字は `DESIGN_compound_coverage.md` §19.4 |
| **エーテルの合成としての Na 経路**（ウィリアムソン合成） | 高校範囲外。エーテルは既存の分子間脱水で作れる |

### 11.7 登録が必要な化合物（このレーンでは触らない・§10.7 の続き）

| 名前 | 出てくる操作 | 補足 |
|---|---|---|
| ~~**ポリエチレンテレフタラート（PET）**~~ | テレフタル酸 ×3 ＋ エチレングリコール ×3 の縮合重合 | **v942 で登録ずみ**（`DESIGN_compound_coverage.md` §19）。⚠ **名乗るのは3組（6分子）から**。2組では2単位のオリゴマーにしかならない |
| ~~**ナトリウムエトキシド**~~ | エタノール ＋ Na | **v941 で登録ずみ**（同 §19）。`-ONa` を線1本で書く既存の流儀そのまま |
| ε-カプロラクタム | （ナイロン6を入れるなら） | §11.6 |
| デンプン／セルロースの繰り返し単位 | （多糖の加水分解を入れるなら） | §11.6。**v942 でいったん見送った**。`hydrolysis_glycoside` は実際に発火する（試作したグルカンで候補2件）が、両端を R で止める規約のせいで**切った断片が両方とも「（名称未登録）」**になる ＝ 名前の無さが1手先に移るだけ。α/β の描き分け自体は可能（実測は `DESIGN_compound_coverage.md` §19.4） |

---

## 12. 「直近の反応」は1つの文脈として扱う（v1423・2026-08-20 ユーザーの実機レビュー）

ユーザーの言葉（原文・2件）:

> 試薬を作用させた後、
> 反応の前後を見る
> この反応の機構を見る
> が、生成物に対するボタンの下に区別なく並んでいるのがわかりづらい

> この反応の機構を見る、に進むと、反応前に戻す、ができなくなる
> これについては、設計がどうあるべきか

### 12.1 割る軸（★ここが設計の芯）

最初に浮かぶのは「**分子を変えるか、変えないか**」で割る案だが、**それは採らない**。
ユーザー自身が観点を言い直している:

> 分子を変えるか、変えないか、という観点よりも、
> **1つ前の物質を変化させたという文脈の続きかどうか**、という観点で考えていました

**この言い直しのほうが正しい。** 「変えるか変えないか」で割ると、

| 札 | 分子を変えるか | 文脈 |
|---|---|---|
| 🔍 反応の前後を見る | 変えない | **直近の反応** |
| ⚗ この反応の機構を見る | 変えない | **直近の反応** |
| ↩ 反応前に戻す | **変える** | **直近の反応** |

—— **「↩ 反応前に戻す」だけが反対側へ行ってしまう**。3つとも指しているのは
「いましがた起きた1つの反応」で、**実装上もすべて `reactor.lastReaction` を見ている**
＝ 同時に現れて同時に消えるべきもの。**反応カードは「次の反応」なので別のまとまり。**

### 12.2 画面の割り方（`reactor.refresh()`）

`#reaction-actions` を **2つの節**（`.rx-section` ＋ `.rx-section-head`）に割る。
文言は定数1つ（`RX_SECTION_NEXT` / `RX_SECTION_LAST` / `RX_UNDO_POINTER`）に閉じ込め、
テストと実装が同じものを見る（`PARTNER_HINTS_SUMMARY` と同じ約束）。

| 節 | 見出し | 中身 |
|---|---|---|
| ① | **この分子にできること** | 反応カード（`REACTION_RULES`）＋「もう1つ分子が要る反応」の案内（§9・v1420） |
| ② | **いま起きた反応（〜）** | 🔍 反応の前後を見る ／ ⚗ この反応の機構を見る ／ 「↩ 反応前に戻す」の**在り処の案内** |

- 見出しには**反応名**を入れる（「いま起きた反応（分子内脱水）」）＝ 何の続きかが節の頭で読める
- 中身が1つも無い節は**見出しごと出さない**（空の見出しは「ここに何か出るはず」と読ませる）
- ⚠ **「↩ 反応前に戻す」のボタンをここに増やさない。** 実体は帯（`#ws-free`）に1つだけで、
  **モーダルを閉じても押せる**のが v1409 の要点。同じ出口を2か所に置くとその要点がぼやける。
  節②からは**在り処を指すだけ**にし、しかも `syncUndoButton()` が
  **実際に札を出しているときだけ**言う（無い出口を名指ししない ＝ RX39 と同じ約束）

### 12.3 機構を見にいっても文脈は終わらない（バグの正体）

**症状**: 「⚗ この反応の機構を見る」へ進むと、戻ってきても「↩ 反応前に戻す」が二度と出ない。

**原因**（実測で追認した経路）:

```
reactor.jumpToMechanism()
  → game.setMode('learn')
    → 「自由モードを離れるときの掃除」（game.js）が reactor.exitCompare() を呼ぶ
      → lastReaction = null   ← 記録ごと捨てていた
```

⚠ **分子は失われていない。** 機構ビューア（`reaction.js`）は `borrowCanvas()` で
答案・履歴・視野を退避し、`exit()` の `returnCanvas()` でそのまま戻す。
**捨てられていたのは記録（`beforeState` という文字列）だけ** ＝
必要があってそうしていたのではなく、**全消去用の掃除が機構ジャンプにも効いていただけ**。

**直し方**: `exitCompare()` を「**閉じる**」と「**捨てる**」に割る。

| いつ | 呼ぶもの | 記録 |
|---|---|---|
| モード離脱（`setMode`）・機構ジャンプ | `closeCompare()` | **残す**（文脈の続き） |
| 全消去（`refresh()` が空のキャンバスを見た） | `discardLastReaction()` | 捨てる |
| ↩ 反応前に戻す（`undoLastReaction()`） | `discardLastReaction()` | 捨てる |

※ `exitCompare()` は `discardLastReaction()` へ**改名**した（名前が「閉じる」に読めるまま
「捨てる」を意味していたのが、そもそもの取り違えの温床だった）。

### 12.4 安全は既存の門番が担保する（弱めない）

記録を残すようにしても「押した瞬間にその後の作図が黙って消える」が復活しないのは、
**`syncUndoButton()` が「いまのキャンバスのトポロジーが `lastReaction.after` と一致するか」**で
札を出し入れしているから（v1409）。帰ってきた図が違えば札は出ない。**この門番は弱めない。**

同じ理由で、`reaction.js` の `enter()` が呼ぶ `syncUndoButton()`（ビューアの絵が出ているあいだ
札を引っ込める）も**残す** —— 指す先の分子は退避されて画面に無い。
借りたキャンバスは空なのでトポロジーが一致せず、**引っ込めは自動的に効く**。
帰りは `exit()` → `returnCanvas()` → `updateDrawing()` → `refresh()` → `syncUndoButton()` で
そのまま復活する（新しい手当ては1つも要らなかった）。

### 12.5 テスト（RX40〜RX42）

- **RX40**: 反応の前は節①だけ／反応の後は2節に割れ、**「🔍 反応の前後を見る」が
  反応カードと同じ節に入っていない**（`.rx-section` の同一性で見る）。
  節②は「↩ 反応前に戻す」の在り処を**指すだけ**でボタンを二重に置かない
- **RX41**: 機構を見にいって戻ると札が**また出る**。押せば本当に反応前へ戻る
  （札が出るだけの空振りにしない）。ビューア中は引っ込んだまま
- **RX42（否定対照）**: 文脈が切れているときは出さない ——
  ①「全消去 → 学習 → 自由」／②「反応のあと1原子描き足してから機構へ行って戻る」。
  ②は `syncUndoButton()` の門番を外すと赤くなる。
  「描き足しを戻せば札も戻る」まで見て、②が「常に出さない」で緑になっていないことを確かめる
- 既存の **RX1・RX2** は「モード離脱で記録が破棄される」を主張していたので**書き換えた**
  （破棄の主張は全消去の側へ移した）

---

## 13. 反応は「いま見ている分子」のものだけを出す（v1429・2026-08-20 ユーザーの実機報告）

ユーザーの言葉（原文）:

> **ブタン酸とエチルメチルケトンを２つ並べた状態で、ブタン酸の反応を見ると
> ヨードホルム反応が表示され、ボタンを押すとケトンが反応します。**
> 2分子以上ある状態での可能な反応が混ざっています。選択した分子の反応のみ表示するのがよいでしょう

**表示だけの問題ではない**（押すと本当に別の分子が反応した）。

### 13.1 原因（実測で追認した）

`reactor.siteFilter()` の1行目、**選択が空なら素通し**:

```js
if (!selSets.length) return true;   // ← ここ
```

`selectedMoleculeSets()` は「🎯 反応させる分子を選ぶ」で**明示的に選んだ**集合で、
**分子モーダルを開いただけでは選択にならない**。したがって `selSets.length === 0` のまま
`siteAllowed` が全部 true を返し、**キャンバス上のすべての分子の箇所**が一覧に並んでいた。

⚠ **情報は既にあった。** `openMoleculeModal(atomId)` はタップされた原子を受け取り
`setFocusedMolecule()` を通す。`moleculeModalPart()` は見出し（`#mm-name`）と
タブ（`#mm-tabs`）が指す1分子をすでに返している —— それを絞り込みに使っていなかっただけ。

### 13.2 「関わる」の定義（★絞りすぎない）

**箇所（site）の原子が、見ている分子の原子を1つでも含むこと。** ＝ 選択が1つのときと同じ条件。

⚠ **「見ている分子の中で完結する反応」に絞ってはいけない。** エステル化のように
2分子に跨る箇所は**片側が見ている分子なら残す** —— 消すと v1420 で作った
「＋ エタノール を呼び出す → エステル化」の導線がまるごと死ぬ。
相手の呼び出しの試算（`findPartnerHints` の `baseIds`）も同じ範囲を土台にする
（土台を全体のままにすると「隣のケトンに相手を足す案内」が生えて混ざりが残る）。

| 選択の数 | 通す箇所 | 変えたか |
|---|---|---|
| 0 | **見ている分子の原子を含む箇所**（キャンバスに2分子以上あるときだけ） | **★これが今回** |
| 1 | その分子の原子を含む箇所 | 変えない |
| 2以上 | 選択の中で完結し、2つ以上の選択分子に跨る | 変えない |

**選択があれば選択が勝つ**（`scope = selSets.length ? allSel : focus`）。
「🎯 反応させる分子を選ぶ」は式の左右を決める道具なので、上書きされては困る。

### 13.3 判定は1か所（`siteFilter()`）のまま

同じ絞り込みを通る口を数え直した。**新しい判定は1つも書いていない**:

| 口 | 通し方 |
|---|---|
| 反応カード（`refresh()`） | `siteAllowed` |
| 試薬の瓶（`reagentHits()`） | `siteAllowed` |
| **呈色・検出（`runDetection()`）** | `atomAllowed`（★同じ混ざり方があった。下記） |
| 瓶の空振り（`explainReagentMiss()`） | `scope` を `cachedPartnerHints` の土台に |
| 条件の選択肢（`renderConditionChoice()`） | 同上 |
| 相手の呼び出し（`renderPartnerHints()`） | 同上 |
| 呼び出しの検算（`runPartnerHint()`） | `siteAllowed`（選択済みなので既存どおり） |

⚠ **呈色にも同じ混ざり方があった**（今回いっしょに直した）。
ブタン酸とエチルメチルケトンを並べて**ケトンを見ている**とき、
炭酸水素ナトリウムが**陽性**（隣のカルボン酸を数えていた）。
`runDetection` の絞り込みが `if (selSets.length)` の中にあったのが原因で、根は同じ。

### 13.4 「全体で開く」は存在しない

`openMoleculeModal()` を引数なしで呼ぶ道（右パネルのボタン・`?go=molecule`）はあるが、
**着地するのは必ず1分子**（`moleculeModalPart()` が `focusedMolecule` →
番号付きの先頭 → `parts[0]` の順で1つに決める）。見出しに名前と分子式が出て、
タブで切り替える画面なので、**「全体」という状態はもともと無い**。
したがって絞り込みも常に1分子ぶんでよく、例外を作らない。

**右パネルの「⚗ 反応させる・調べる（反応 N件）」も同じ数**になる
（`executableCount` は `refresh()` が数えるので自動的にそろう）。
ここだけ全体の合計を出すと、押して開いた画面と数が食い違う ＝ 同じ混ざり方が札に残る。

### 13.5 文言（黙って減らさない）

⚠ 隣の分子の反応を落とすだけだと「**この分子には反応が無い**」と読まれる。
`#reaction-selection`（選択中の案内と同じ場所）に、何で絞ったかと隣を見る手を出す:

> いま見ている「酪酸（ブタン酸）」が関わる反応だけを出しています（ほかの分子の反応は、上のタブか図の分子名から切り替えると出ます）。

文言は定数 `RX_SCOPE_NOTE(name)` 1つ（テストと実装が同じものを見る）。
名前は `moleculeModalPart()` から引く ＝ 見出し（`#mm-name`）と必ず同じ分子を名乗る。
**分子が1つのときは従来どおり無言**（`moleculeModalAtomIds()` が `null` を返す）。

⚠ **1文にした**（375px / 1280px の実物を見て決めた）。この文の真上には
見出しの名前・タブ・「分析中: ① 〜」で**同じ分子名が既に3回**出ている。
「いま見ているのは〜です。」を独立した文にすると4回目の名乗りが1行まるごと増えるので、
名前は絞り込みの説明の中に埋めた。

### 13.6 性能

`refresh()` は作図のたびに走る（v1420 で「1原子置くたびに5倍」の回帰を出した前科がある）。
同じページの上で `moleculeModalAtomIds()` を `() => null` に差し替えた「前」と
交互に3回ずつ測った中央値（100回の平均・chromium）:

| 画面 | 前 | 後 |
|---|---|---|
| 1分子（エタノール） | 0.465 ms | 0.544 ms |
| 2分子（酪酸＋MEK）・モーダル開 | 0.743 ms | 0.753 ms |
| 2分子・モーダル閉 | 0.706 ms | 0.734 ms |
| 5分子・モーダル開 | 3.607 ms | **3.562 ms** |

足したのは `splitMolecules()` 1回ぶん（+0.08ms）。分子が増えるほど**むしろ速くなる**のは、
相手の呼び出しの試算（候補5件 × 全ルールの detect）の土台が1分子に縮むため。

### 13.7 テスト（RX43・RX44）

- **RX43**: ユーザーの再現そのもの。ブタン酸を開くとヨードホルムが出ない／
  **瓶を押しても隣のケトンが反応しない**（表示だけでなく実行も塞がっている）／
  自身の中和は残る／エステル化への道は残る。
  ★陰性対照3つ ——「ケトンを開けばヨードホルムは出る」「1分子のときは変わらない」
  「1分子のときは断りの文も出ない」
- **RX44（否定対照）**: 2分子に跨るエステル化の箇所は**どちら側から見ても**残る／
  「🎯 反応させる分子を選ぶ」が**勝つ**（既存の絞り込みと案内文の回帰）／
  呈色も同じ絞り込みを通る（ケトンを見れば NaHCO₃ は陰性・酸を見れば陽性）
- 既存の **RX17** は「選択なしで6箇所」を主張していたので**4箇所に書き換えた**
  （＝ 今回変えた既定そのもの。選択が2つ・3つのときの主張は1つも変えていない）。
  **RX34 ④** の「選ぶモードでないときは無言」も、2分子のときは断りが出るように書き換えた

---

## 14. 重合の生成物を一直線に伸ばす（v1436・2026-08-21 ユーザーの実機報告）

> **付加重合　２分子並べたら出ました**
> **１分子でも重合を出せるようにしたい　→　複数分子を横一線に並べ反応させる**
> **現状、エチレンの付加重合後が一直線にならないのでわかりにくい**

### 14.1 実測（何がどう折れていたか）

エチレンを3個呼び出して `addition_polymerization` を実行し、**主鎖の座標を数で出した**
（呼び出し直後の3分子は `C(379,300) C(421,300) / C(505,300) C(547,300) / C(631,300) C(673,300)`
＝ **完全な横一線**。つまり「元の並べ方が斜め」ではない）。

| 生成物 | 主鎖の折れ（180° でない頂点） | 主鎖の y のばらつき |
|---|---|---|
| ポリエチレン（単量体3個） | **6頂点中5つが 90°** | 84px |
| ポリエチレン（単量体6個） | **12頂点中11が 90°** | 210px |
| ポリ塩化ビニル（3個） | 1（末端の R だけ） | 42px |
| ポリアクリロニトリル（3個） | 1（末端の R だけ） | 42px |
| ポリアセチレン（3個） | 1（末端の R だけ） | 42px |
| 合成ゴム（ブタジエン3個） | 7 | 109px |
| ナイロン66（2組） | 3（うち1つは**単量体まるごと 90° 立っていた**） | 294px |
| PET（2組） | 11 | 161px |

ポリエチレンは `R(463,300) C(421,300) C(379,300) C(379,258) C(337,258) C(337,216) C(295,216) R(295,258)`
＝ **1単位ごとに1段のぼる階段**。単量体を増やすほど段が増える（6個で 210px 上がる）。

### 14.2 原因（a/b/c の切り分け）

依頼の3つの候補のうち、**(a) 重合のときの原子の置き方**だった。(b) 並べ方は上のとおり
完全な横一線で無実、(c) モーフィングは座標を戻すだけなので無関係（実行後の
`userMolecule` そのものを読んで確かめた）。中身は**2つ**ある:

**① 鎖が伸びる向きと、単量体を繋ぐ順が逆**（ポリエチレン特有・これが階段の正体）

`vinylBonds` は head（置換基の多い炭素）に次の tail を繋いで頭-尾にする。
**エチレンは左右が同じ**なので置換基では決まらず、`vinylBonds` の同数の場合分けが
「**x の小さいほうを head**」にしている（v377。原子IDが乱数なので座標で決めた）。
一方 `detect` は単量体を **x の昇順**で繋いでいた。鎖は `R-tail₀-head₀-tail₁-…` と
**tail から head の向き**に伸びるので、head が左にある＝鎖は**左へ**伸びたいのに、
繋ぐ順は左からになっていて、**次の単量体を置く先（右）は必ず自分の tail で塞がっている**。
`planAttachment` の既定の順は `右・上・下・左` なので、塞がった右をあきらめて**上**へ逃げる
＝ 単位ごとに1段のぼる。

塩化ビニル・アクリロニトリルが最初からまっすぐだったのは、たまたま
**置換基が右の炭素に付いていて head が右**になり、繋ぐ順と伸びる向きが一致していたため。
つまり**まっすぐだったほうが偶然**で、対称な単量体では必ず折れる。

**② まだ繋いでいない単量体を「邪魔者」として避けていた**（縮合重合で特に大きい）

`planAttachment` はキャンバス上の他の原子をすべて障害物として扱う。重合では
**横に並べた残りの単量体がそこにいる**ので、次の1個を置くときに残り全部を避ける。
ナイロン66 で最初のジアミンが**まるごと 90° 立って**下へ伸びていたのはこれ
（横に置くと右隣に駐まっているアジピン酸と当たるので、`canSpin90` の 90° 回転が先に通った）。
だが避けた相手は**この後どうせ動かして同じ鎖に繋ぐ**ので、避ける必要がそもそもない。

### 14.3 直した形（★作図規約の例外は増やしていない）

⚠ **CLAUDE.md の「例外は1つだけ（名称から呼び出した分子の C=C まわりの ±120°）」に触れていない。**
今回入れたのは**直交4方向のうちどれを先に試すか**の並べ替えだけで、
置く位置は従来どおり**直交の格子上**にしかない ―― 一直線の主鎖（0°/180°）も
垂れ下がる置換基（90°）も、**もともと直交作図の中にある形**である。
「まっすぐ」は新しい幾何ではなく、**同じ直交作図のうち折れないほうを選ぶ**というだけ。

1. **`chainDirection(mol, backId, fromId)`**（新設）… 主鎖の1つ内側から端へ向かう向きを
   直交4方向に丸めて返す。斜めに描かれていても軸へ丸めるので、返るのは必ず直交の向き
2. **`planAttachment(..., prefer)`**（引数を1つ追加）… `prefer` の向きを**最初に**試す。
   置けなければ従来の順（右・上・下・左）へ落ちるので、**置ける場所は1つも減らない**
3. **`attachR(mol, atomId, prefer)`** … 末端の R も同じ向きの規則で置く。
   R は「この先も続く」印なので、鎖の続きの位置に出ないと端だけ折れて見える
4. **`addition_polymerization.detect` の並べ替えを「鎖が伸びる向き」に合わせた** …
   単量体の `tail → head` の合計ベクトルで昇順を決める。head が左にある単量体
   （エチレン）では x の**降順**になり、鎖は左へ伸びながら、
   **画面の左にあった単量体が鎖の左端に来る**（並びと鎖の順の一致は保たれる）
5. **繋ぐ前の単量体を `ignoreIds` に入れた**（4つの重合ルールすべて） …
   「邪魔者」ではなく**この鎖の続き**だと `planAttachment` に伝える。
   最後の1個を置くときには全員が鎖の上に乗っているので、重なりは残らない

**ユーザーが手で描いた分子には一切触れていない。** 動かすのは
「重合ルールがいま繋ごうとしている単量体」だけで、`prefer` は候補の**順序**を変えるだけ。
検証（正準コード・同型判定）は座標を1つも読まないので、見た目だけが変わる。

### 14.4 直した後の実測

| 生成物 | 折れ | y のばらつき |
|---|---|---|
| ポリエチレン（3個） | **0** | **0px** |
| ポリエチレン（6個） | **0** | **0px** |
| ポリ塩化ビニル（3個） | **0** | **0px** |
| ポリアクリロニトリル（3個） | **0** | **0px** |
| ポリアセチレン（3個） | **0** | **0px** |
| ナイロン66（2組・3組） | **0** | **0px** |
| PET（2組） | 8（**すべてベンゼン環の 120°**） | 35px（環の高さ） |
| 合成ゴム（ブタジエン3個） | 6（**すべて C=C の 120°**） | 109px |

ポリエチレン3個は
`R(715,300) C(673,300) C(631,300) C(589,300) C(547,300) C(505,300) C(463,300) R(421,300)`
＝ **R から R まで y が1つも動かない**。

### 14.5 直していないもの（理由つき）

- **合成ゴム（1,4-付加重合）の 120° のジグザグは残す。** 折れの中身は
  **C=C まわりの ±120°**＝ CLAUDE.md がすでに認めている唯一の例外そのもので、
  ここを潰すと**シス/トランスが読めなくなる**（天然ゴムとグタペルカの描き分け・RX13）。
  単量体1つぶんで 36px 下がるのはこの 120° の帰結。**繋ぎ目と末端 R は直した**ので、
  折れは 7 → 6 に減っている
- **PET の 8 か所はベンゼン環の頂点**（パラ位で主鎖が環を通り抜ける）。環の作図は
  正六角形が正で、ここを直すのは「まっすぐ」ではなく「環を潰す」ことになる。
  主鎖の y のばらつきは 161px → 35px（＝環の高さそのもの）まで落ちた
- **スチレン・酢酸ビニルの付加重合は依然として
  「生成物を配置する空間がありません」で止まる**（**この修正の前からそうで、退行ではない**）。
  原因は別で、**置換基が鎖の伸びる先に描かれている**こと ―― 呼び出したスチレンは
  ベンゼン環が head 炭素の真横（鎖の続きの位置）にあるため、どの向きに置いても
  自分の環と当たる。直すには**置換基を主鎖に垂直へ倒す**作図が要り、
  `reshapeVinylAngles`（±120° の例外そのもの）と正面からぶつかる。別件として申し送る

---

## 15. 1分子からでも重合へ行ける入口（v1437・同じ実機報告）

> **付加重合　２分子並べたら出ました／１分子でも重合を出せるようにしたい**

### 15.1 何個呼ぶか（★決めたこと）

**自分を含めて3個**（＝ 呼び出すのは2個）。`SELF_PARTNER_UNITS = 3`。根拠は4つ、すべて実測:

1. **このアプリ自身の高分子の図が「3単位＋両端 R」の規約**（LB23・FG2）。実際
   アセチレンは**3個のときだけ**生成物が「ポリアセチレン」と名乗る
   （実測: 2個 →（未登録）、**3個 → ポリアセチレン**、4個 →（未登録））。
   ここを外すと、1分子から作った生成物が**自分のライブラリに載っていない図**になる
2. **2個では「くり返し」と「二量体」の区別が付かない。** 3個で初めて -A-A-A- と読める
3. **`MAX_REACTION_SELECTION` が 4。** 呼んだあと `runPartnerHint` は並べた分子を全部選ぶが、
   `siteFilter()` は「2つ以上選んだら箇所は選んだ分子の中に収まること」を要求するので、
   **5個並べると押せなくなる**（札が「押したらそこまで連れて行く」約束を破る）。
   3個は上限4の内側で、伸ばす余地も1つ残る
4. **重さ・大きさの心配は無い**（実測）:
   - 重合の実行 … 0.4ms（6個でも 0.8ms）
   - 鎖の幅 … 294px（6個なら 546px）
   - 画面上の結合1本 … デスクトップ 1280px で 66.3px、**375px 幅でも 29px**
     ＝ 「見えた」の床 `SUMMON_MIN_BOND_PX`（24px）を上回る
   - `findPartnerHints` の総時間 … 追加前後で 3.5〜12.4ms（分子による差のほうが大きく、
     この入口ぶんの増分は測定誤差に埋もれる）
   - `summonMolecule` が横一線を保てるのは **16個まで**（17個目で `SUMMON_ROW_WIDTH`＝2400px に
     当たって下の段へ折り返す）。3個は遠く手前

### 15.2 作り（★新しい仕組みを作らない）

v1424（濃硫酸の 130〜140℃・§11）とまったく同じ形 ―― **相手が要る反応を、相手を並べていない人に見せる**。
v1420 の3点セットをそのまま使い、**変えたのは呼ばれ方だけ**:

- `findPartnerHints` … 末尾で `findSelfPartnerHints` を呼ぶ。
  既存の `PARTNER_CANDIDATES` の総当たりは「**別の化合物を1つ**足したら通るか」しか試さないので、
  「**自分と同じ分子をもう何個か**」は原理的に落ちてこない。そこだけを足す
- `makePartnerHintButton` … `h.count > 1` のとき札に個数を書く
  （`＋ エチレン（エテン） をもう2つ呼び出す → 付加重合…`）。目印は `data-count`
- `runPartnerHint` … 呼び出しを `h.count` 回まわす。①呼べたか ②箇所が生えたか
  ③選んだ後も押せるか を**実測してから**実行する段取りは1つも変えていない
- `selectPartnerPair` … 呼び出した側が2つになるので、**並べた全部を選ぶ**ように一般化
  （`count` が 1 のときは従来と1文字も変わらない式になる）

⚠ **既存の重合ルールは1文字も変えていない。**
`addition_polymerization.detect` の `if (list.length < 2) return;`
（＝「横一列に単量体を並べた状態から重合するところを見たい」という過去のユーザー要望の実装）は**そのまま**。
足したのは入口だけで、自分で並べた人の道は元のまま通る。

### 15.3 出す条件（絞り込み）

- 対象は **`SELF_PARTNER_RULES` の3つ**（付加重合・アセチレンの付加重合・1,4-付加重合）。
  **縮合重合は入れない** ―― 相手が別の2価単量体で、しかも2組（4分子）要るので
  「自分をもう何個か」では説明が付かない（`condensation_polymer_info` が説明を持っている）
- **名前で引ける分子だけ**（`summonMolecule` は名前しか受け取らない）。
  名乗れない分子に「呼び出す」と書くと札が約束を破る
- **もう並べてある人には出さない**（`rule.detect(mol)` が既に箇所を返すなら黙る）。
  すでに押せる反応を「さらに2つ呼びなさい」と案内するのは案内ではない
- 見出しの文言を `もう1つ分子が要る反応` → **`相手の分子が要る反応`** に変えた。
  数を決め打つと、札の中身（「もう2つ呼び出す」）と食い違う

### 15.4 実測（押したあと）

エチレン1分子から札を押すと、2個が横一線に呼ばれ（`C(379,300) C(421,300) / C(505,300)
C(547,300) / C(631,300) C(673,300)` ＝ y がすべて 300）、そのまま重合して
`R(421,300) C(463,300) … C(673,300) R(715,300)` ＝ **折れ0・y のばらつき 0px**（§14 の直しが効く）。
アセチレン1分子から押すと生成物は「ポリアセチレン」と名乗る。

### 15.5 テスト（PM5・PM6）

- **PM5**: 3つの重合すべてで1分子から札が出る／札が呼び出す個数を言う／
  呼び出した結果が横一線（y が1種類・折り返し幅に届かない）／押すと重合まで進み、
  鎖は折れ0・y 0px／モーダルは閉じる／アセチレンからは「ポリアセチレン」と名乗る／
  **並べる数が `MAX_REACTION_SELECTION` を超えていない**
- **PM6（否定対照）**: 自分で2つ並べた人には出さない（＝ 既存の道はそのまま）／
  重合できない分子には出さない／既存の札（別の化合物を呼ぶ道）の件数も文面も不変／
  名前で引けない分子には出さない／**重合ルールの `detect` は「2つ以上」の約束のまま**

---

## 16. 2段階モーフィングの①で止まっていることを画面に残す（v1454・2026-08-25 ユーザーの実機報告）

> 「グルコースの 鎖状 → 環化 で**環になっていない**」

### 16.1 実測（何が起きていたか）

`?summon=D-グルコース（鎖状）` → `⚗ 反応させる・調べる` → `環化 → β-D-グルコース` を押した直後の状態:

| 見るもの | 実測 | 判定 |
|---|---|---|
| 分子データ（`game.userMolecule`） | 重原子12・重原子間の結合12・**環1つ（6員）**・`lookupCompoundName` は **β-D-グルコース（β-D-グルコピラノース）**・`haworthFlipPlan` も通る | ✅ 正しい |
| **環の形** | 環結合の長さ `[66,85,60,85,66,110]` ＝ **登録の β-D-グルコピラノースと1pxも違わない**（平行移動を除いて全原子一致） | ✅ 正しい |
| **画面** | **①の静止画のまま**（`renderStaticSnapshotWithHydrogens`）＝ C1–環O の結合がまだ無い**開いた絵**に、名前だけ「β-D-グルコピラノース」が付く | ❌ ここ |
| 止まっている断り | **9秒で消えるトースト1つだけ**。消えたあとの画面には理由がどこにも書いていない | ❌ ここ |

★ **つまり「環になっていない」は正しい観察**で、直すのは**図でも化学でもなく、止まっていることの見せ方**。

⚠ **`standaloneDrawingOf` / `redrawProductsAsStandalone` で描き直す手当ては要らない**
（②の着地はすでに登録の図そのもの。上の表の2行目。SB4 ③ が数で押さえる）。

### 16.2 直したこと（**止め方そのものは変えない**）

止めるのは P12-7 M2f のユーザー要望（「①で止めてじっくり観察できる」）なので**残す**。
足したのは2つだけ:

1. **キャンバスの常設バッジ（`#canvas-mode-badge`）に2つめのモードを足した**（`morph-pause`）。
   器は v1416 で「将来ほかのモードにも足せる形」に作ってあったので、
   `canvasModeBadgeSpec()` の分岐が1つ増えただけ（CSS も DOM も追加なし）。
   - 見出し `⏸ 反応の途中で止めています` ／ 数 `① / ②`
   - 説明 `いまは①環の形に折りたたんだ状態です（水素の数と位置もここで確認できます）。続きを見ると②結合ができて環が閉じます。画面をタップしても続きが見られます。`
   - ⚠ 文言に「編集できません」とは書かない（v1416 と同じ約束）
2. **押しどころ `続きを見る`**。⚠ **進み方を2つにしない** ——
   キャンバスのタップと同じ `reactor.advanceMorph()` を通す。

⚠ **バッジは `updateDrawing()` の先頭でそろえるのが原則**だが、止まっているあいだの画面は
`renderStaticSnapshotWithHydrogens` が直に描いていて `updateDrawing()` を通らない。
そこで**止めたとき**と**進めたとき**の2か所からも `syncCanvasModeBadge()` を呼ぶ
（進めたときに呼ぶのは、`advanceMorph` の `updateDrawing()` が 800ms の再生後で、
待つとバッジだけ 0.8秒 残るため）。

⚠ 文言は**バッジ側（game.js）の1か所**に持つ。reactor は `morphPauseInfo()` で
「いま何の状態か（`now`）／続きに何が起きるか（`next`）」の材料だけ渡す。

### 16.3 テスト（SB4）

止まったことを**待ってから**見る（⚠ `execute()` が返った時点ではまだ第1段階の再生中で
`_morphPause` は null ＝ 待たずに書くと中身が1行も走らない空振りの緑になる）。
- ① バッジが出る・`data-mode="morph-pause"`・続きに何が起きるか書いてある・
  「編集できません」と書いていない・`続きを見る` が 32px の床を割らない
- ② `続きを見る` で進み、バッジが消える（＝ 行き止まりではない）
- ③ ★ 着地が**登録の図と平行移動を除いて完全一致**（＝ 描き直す手当ては要らないことの根拠）
- ⚠ **否定対照**: `canvasModeBadgeSpec` を null に差し替える（＝ v1453 までの実装）と
  止まった画面に断りが1つも無くなる ＝ 申し立ての画面が再現する

---

## 17. 分子が0個になったら「反応させる分子を選ぶ」は終わる（v1454・2026-08-25 ユーザーの実機報告）

> 「**反応分子を選ぶ → 全消去などしてもモードが維持される**」

### 17.1 実測（どの操作でモードが残るか）

| 操作 | `reactionSelectMode` | 分子数 | 判定 |
|---|---|---|---|
| 🗑 全消去 | **true のまま** | 0 | ❌ 申し立てそのもの |
| 🗑 全消去 ×2 | **true のまま** | 0 | ❌ |
| ↩ 戻す（空に戻る） | **true のまま** | 0 | ❌ |
| 消しゴム等で全部消す | **true のまま** | 0 | ❌ |
| 名称から呼び出す | true のまま | 2 | ✅ **これは正しい**（先に1つ選んでから相手を呼ぶ使い方・v1409 の案内どおり） |
| 道具（選択・結合・消しゴム）を選ぶ | false | 1 | ✅ v1409 の4経路 |
| モードタブ ／ 機構ビューア | false | 1 | ✅ 同上 |

★ **残る4つの共通点は「分子が0個になった」**。
⚠ 空のキャンバスで選ぶモードに居ると、タップは選択に振り替えられたまま何も選べない
＝ **作図に戻れない行き止まり**になる（v1409 で塞いだ穴とまったく同じ形）。

### 17.2 直したこと

**経路ごとに手当てを足さない。** v1416 のバッジと同じ「**出し入れは状態から導く**」で、
`updateDrawing()` の先頭に条件1つを置いた:

```
if (this.reactionSelectMode && this.canvasMoleculeCount() === 0) this._dropReactionSelectState();
```

- ⚠ `deactivateReactionSelectMode()` をそのまま呼ぶと中で `updateDrawing()` が走って
  **二重描画**になる。下ろす中身を `_dropReactionSelectState()` に切り出し、
  `deactivateReactionSelectMode()` は「それ＋`updateDrawing()`」にした（**下ろす道は増えていない**）。
- ⚠ `canvasMoleculeCount()` を呼ぶのは**モードが ON のときだけ** ＝
  OFF のときの `splitMolecules()` の回数は1回も増えない。
- バッジの説明も直した: `🗑 全消去はできます` → **`🗑 全消去 で分子が無くなると、選ぶのも終わります。`**
  （できることと、その結果起きることを両方書く）。

⚠ **呼び出しでは終わらせない。** 「先に1つ選んでから相手を呼ぶ」は正しい使い方で
（`REACTION_SELECT_LONELY_HINT` がそう案内している）、ここを一緒に閉じると案内が嘘になる。

### 17.3 テスト（SB5）

4経路（全消去・全消去×2・↩戻す・図を直に空にする）すべてで
モードが下りる／選択が空になる／バッジが消える／★ **その場で原子が置ける**（＝ 旗だけ見て終わらない）。
呼び出しでは下りないことも同じテストで押さえる。
⚠ **否定対照**: `canvasMoleculeCount` を「いつも1」に差し替える（＝ v1453 までの実装）と
全消去してもモードが残る。

⚠ **SB3 ④ の下ごしらえを直した** —— あの回は空のキャンバスで `reactionSelectMode = true` と
旗だけ立てていたので、この直しのあとは**自分で下りてバッジが出ない**。分子を1つ置いてから測る。
## 18. 重合まわり3件（2026-08-26・動画レーンの実測報告）

発注は `video-scripts/ORDER_reactor_findings_2026-08-26.md`。反応シリーズ・高分子シリーズの
収録で「押せるのに効かない」が3つ出た。**3件とも原因は別**で、直しも別のところにある。

### 18-1. スチレンの付加重合が ×3 以上で必ず落ちていた（原因は2つとも作図）

**症状**: スチレン ×4 は「ボタンは出て押せるのに何も起きない」。塩化ビニル ×4 は通る。

**測ったこと**: `addition_polymerization.detect` は**正しく1件・要素数8**を返していた。
落ちていたのは `apply` の中の `planAttachment` で、投げていたのは
「生成物を配置する空間がありません」。**×2 は通り ×3 以上が必ず落ちる**。
＝ ルールの検出でも化学でもなく、**置き場所の幾何**だけの問題。

**原因1（呼び出しの整形）**: `reshapeVinylAngles` → `reshapeDoubleBond` が、
±120° に開くとき**枝を平行移動（`_moveSubtree`）していた**。平行移動は結合の長さは保つが
**枝の内部の向きを置き去りにする**ので、環がぶら下がっていると環の外向き結合が
環の中心を向かなくなる。スチレンで実測すると ipso 炭素まわりの3本が
**-60° / 120° / -120°**（＝ ビニルとの結合が環結合と 60°）で、
六角形の隣の炭素が**頭の炭素の真反対 41px**まで回り込んでいた。
鎖が伸びる先（頭の、尾と反対側 42px）との隙間は**実測 2.0px**。自分の環で道を塞いでいた。

→ **環を含む枝は「回して」置く**（`_rotateSubtree`）。回転は剛体なので結合長も角度も変わらず、
環は放射状に戻る（実測で ipso まわりが 120°/120°/120°）。
⚠ **環を含まない枝は平行移動のまま**にした。回すと直鎖が 30° 傾いて直交作図が崩れる
（オレイン酸のような長い枝で効く）。六角形は 60° 対称なので回しても形が変わらない。

**原因2（重合の置き方）**: 原因1を直しても ×3 以上は落ちた。単量体1つぶんの刻みは
**主鎖2結合＝84px** しかないのに、隣り合うベンゼン環どうしが **4.0px** まで重なるため。

→ `uprightChainSubstituent`: **二重結合を開いた直後に、頭の置換基を主鎖と直交する向きへ立て直す**。
二重結合が開いた時点で頭の炭素は sp3 になるので、±120° に開いておく理由はそこで消える
（教科書の −[CH₂−CH(C₆H₅)]ₙ− も −[CH₂−CHCl]ₙ− も置換基を主鎖と直交に描く）。
**単量体ごとに交互の側**へ出す ―― 同じ側にそろえると環どうしがぶつかって置けない。

**結果（実測）**: スチレン ×2〜×7 が通り、主鎖は**折れ0・y のばらつき 0.0px**、
フェニル基は**上下上下**の交互で主鎖と直交。塩化ビニルの図は1原子も変わらない
（置換基が道を塞いでいないので `uprightChainSubstituent` が回した結果を採用しても
Cl が縦に立つだけ ―― 主鎖は今までどおり一直線）。

⚠ 置換基が2本以上ある頭（メタクリル酸メチル等）は**触らない**。どちらを回すかが一意でない。

### 18-2. 鎖ができた後、2回目の重合が一覧から消えていた

**症状**: イソプレン×2 → 1,4-付加重合 →（成功）→ イソプレン×2 を足して重合、が効かない。

**測ったこと**: `diene_polymerization.detect` は2回目も **1件・要素数8** を返し、
`apply` を直接呼べば**成功する**。落としていたのは `ReactorPanel.siteFilter()` の
`siteAllowed` ただ1つ ―― 何も選んでいないときは「いま見ている分子（①）の原子を
1つでも含む箇所」しか通さない（§13・v1429）。2回目の箇所は ②③ の8原子だけなので **false**。

→ 「**並べた単量体をまとめて**」繋ぐ重合（`addition_polymerization` /
`alkyne_polymerization` / `diene_polymerization`）に **`wholeCanvas: true`** を付け、
**選択が無いときの focus 絞り込みから外す**。

⚠ §13（v1429）の事故とは形が違う。あれは「ブタン酸を見ているのに**ケトンの**ヨードホルムが
押せる」＝ **押すと見ていない分子が別の反応をする**という取り違え。重合のラベルは
「並べた単量体をまとめて」で、押した結果は必ずラベルどおり。
⚠ **選択があるときは今までどおり選択が勝つ**（この抜け道は選択が無いときだけ）。
⚠ `siteAllowed` は第2引数にルールを取るようになった。`sites.filter(siteAllowed)` と書くと
filter が第2引数に**添字**を渡すので、呼ぶ側は `sites.filter(s => siteAllowed(s, rule))` と書く。

### 18-3. 加硫が1本の鎖の中で橋を架けていた

**症状**: イソプレン×4 を1本の鎖に重合してから加硫を押すと、硫黄は入るが
**同じ鎖の中でループ状に橋が架かる**。

**測ったこと**: `vulcanizablePairs` が返した **3組すべてが同一成分**（別の鎖どうしは 0組）。
`vulcanization.detect` も 3件返していた。「中点に硫黄を置く空きがあるか」の門番は
**同じ鎖の隣どうしを落とすだけ**で、鎖の端と端のように離れた同一鎖の組は素通りしていた。

**どう振る舞うべきか（化学と教材の両方から）**:
- **化学**: 加硫の要点は、ポリイソプレンの鎖**どうし**を硫黄で結んで三次元の網目を作り、
  ずれにくくして弾性を出すこと。分子内のループは現実には起こりうるが、
  弾性にも強度にも寄与せず、教科書が扱うのは常に「2本の鎖＋橋」。
- **教材（紙面の構造式を読めるようにする）**: 1本の鎖に橋が架かった図は
  **「加硫＝網目」という要点をむしろ壊す**。動画レーンも「化学として誤解を招くので出せない」と判断した。

→ **(ア) ボタンを出さない**を採った。理由は「(イ) 押せるが断る」を作るための**新しい仕組みが要らない**こと ――
硫黄の瓶にはもともと空振りの説明（`miss`）があり、**押した人に理由と次の一手を返す口が既にある**。
そこに「鎖が1本だけのときも架橋できません／もう一度 単量体を並べて 1,4-付加重合し、
鎖を2本にしてから硫黄を加えてください」を足した ＝ **実質 (イ) の親切さを既存の道で出している**。
⚠ **(ウ)（分子内の橋を許す）は採らない。**

⚠ **「別の鎖」は連結成分では測れない**。1本目の架橋で2本の鎖は1分子になるが、加硫は
続けて何本も橋を架けられる必要がある（硫黄を増やすとエボナイト）。そこで
**硫黄を通らない成分**＝架橋する前の鎖を「鎖の身元」にした。
否定対照で確かめてある: ここを素の連結成分に戻すと **RX13 と PM11 が赤**（2本目の架橋が架からない）。

### 18-4. 回帰テスト

| 番号 | 見るもの |
|---|---|
| PM7 | スチレンの ipso まわりが 120° ずつ／×2〜×5 が全部通る／主鎖の y のばらつき 0px・フェニル基が交互で直交 |
| PM8（否定対照） | 正準コードは作図に依らない／主鎖はすべて直交・刻みぴったり／**環を含まない枝は平行移動のまま**（オレイン酸が斜めだらけにならない）／置換基2本の頭は触らない／エチレンの鎖は §14 のまま |
| PM9 | 鎖 ① を見たまま 1,4-付加重合が一覧に出て、押すと鎖が2本になる |
| PM10（否定対照） | `wholeCanvas` を持つのは重合の3つだけ／v1429（ブタン酸とヨードホルム）は再発していない／**選択があるときは選択が勝つ** |
| PM11 | 鎖1本では加硫の候補が 0件／鎖2本なら架橋でき、1本目のあとも続けて架橋できる／硫黄を外すと鎖が2本 |
| PM12（否定対照） | 硫黄の `miss` が理由と次の一手を言う／単量体やふつうのアルケンは相手にしない／1,4-付加重合そのものは1件も減っていない |

---

## 19. 場所不足で断ったときの出口（v1466・2026-08-26 ユーザー決定「案い」）

### 19-1. 症状 ——「案内どおりのことができない」

反応は置き場所が無いと `throw` して終わっていた。断り文は **25 か所**あり、そのうち
**11 か所が「分子を離してから実行してください」**と案内していた。

⚠⚠ **ところが「離す」手段は `Shift＋ドラッグ` しかない**（判定は `game.js` の `shiftKey` 1か所）。
★★ **Shift キーの無いタブレット・スマホでは、案内されたとおりのことができない。**
＝ 断り文そのものが行き止まりだった。

### 19-2. 3案とユーザーの決定

| 案 | | 決定 |
|---|---|---|
| (あ) 断る代わりにアプリが自動で並べ直して、もう一度試す | ⚠ **勝手に動く** | ✗ |
| ★★ (い) 断られたときに「並べ直す」ボタンを出す。押すと分子が離れる | ★ ユーザーの操作を上書きしない・タッチでも押せる | ✅ |
| (う) タッチのジェスチャを足す（長押し・二本指） | ⚠ 長押しは結合削除・二本指はピンチと衝突 | ✗ |

⚠ **(う) を捨てた理由は `DESIGN_hit_areas.md`** ——「ジェスチャの割り当ての余り」で機能を作るのを
一度やめた経緯がある。**この決定は蒸し返さない。**

### 19-3. 25 か所をどう束ねたか ——**型で分ける。文言では分けない**

`NoRoomError`（`reactor.js` 冒頭）と `noRoom(msg)` を新設し、場所が無い／置き直せない
25 か所を `throw noRoom('…')` にした。`execute()` の catch は `e.noRoom` **だけ**を見る。

⚠ **`e.message` の字を読む作りにしない。** 文言を1文字直した日に静かに札が出なくなり、
元の行き止まりへ戻る（気づく手立てが無い壊れ方）。検査は **RS1**（`reactor.js` に
「空間がありません」を素の `new Error` で投げる行が1つでもあれば赤）。

⚠ **場所不足でない失敗には印を付けない。**「多重結合が見つかりません」の類にまで札を出すと、
押しても何も変わらない ＝ 潰したはずの行き止まりの作り直しになる（RS1 ③ が見張る）。

### 19-4. 並べ直しの実体 ——**既にある道具を借りる**

押したときに動くのは `game.tidyAnswerSlots()` ＝ 異性体練習の「🧹 並べ直す」
（`DESIGN_isomer_practice.md` §12-5・W4）と**まったく同じ道具**。新しく書かない理由:

- **成分ごとの剛体平行移動だけ**・移動量は格子の整数倍・回転を混ぜない、と決まっている
  （「整形で幾何が変わるなら座標を戻す」「シス/トランスが未確定の図は整形しない」を既に満たす。IW7）
- 落下先の判定が `MIN_COMPONENT_CLEARANCE` ＝ Shift＋ドラッグ（`canMoveComponentBy`）と**同じしきい値**
  ＝ 0.0px の完全重複を作る経路を1本も増やさない
- ⚠ **中で `saveState()` を積む** ＝ ↩ 戻す で1手で取り消せる（勝手に動いたと感じた人の逃げ道）

置き場所は帯（`#ws-free` の `#reaction-card` 行）で、`↩ 反応前に戻す` と同じ行に相乗りする。
⚠ **モーダルには置けない** —— 反応を押した時点で分子モーダルは閉じており、
そもそもモーダルの底は 375×812 で y=1002 ＝ **画面の外**（§6-2a の実測と同じ理由）。

### 19-5. 断ったあと・押したあとに言うこと

| 場面 | 画面に出る文 |
|---|---|
| 断った | 「（理由）。下の「🧹 分子を並べ直す」を押すと、分子どうしの間隔を空けます。」 |
| 押して動いた | 「分子 N個を C×R に並べ直しました。**もう一度お試しください**（↩ 戻す で元に戻せます）。」 |
| 押しても動かせない | 「並べ直しても場所が足りませんでした。要らない分子を消すか、結合線をドラッグして結合を伸ばすと空きます。」 |

⚠ **並べ直したあと、反応を自動で実行し直さない**（案「あ」を採らなかった理由がそれ）。
⚠ **入らなかったときは正直に言う。黙って何も起きないのがいちばん悪い。**
⚠ 次の手は**タッチで実際にできること**だけを書く ——「画面を広くする」は**書かない**。
置き場所はモデル座標で決まるので、拡大率を変えても空きは1pxも増えない。
結合の伸縮（`beginBondStretch`）は `shiftKey` を見ないのでタッチで効く。

### 19-6. 回帰テスト

| 番号 | 見るもの |
|---|---|
| RS1（否定対照） | 「場所が無い」を**型**で伝えている（素の `Error` で投げる行が1つでもあれば赤）／場所不足でない失敗には印を付けていない |
| RS2 | 寄せた2分子で断られ、原子は1つも増えず、札が出て、**字幕が Shift を要求していない** |
| RS3（否定対照） | 押すと離れる／構造コードが変わらない／**原子が増えない ＝ 勝手に反応をやり直していない**／↩ で元の配置に戻る／並べ直したあとなら同じ反応が通る |
| RS4 | 入らなかったときに「場所が足りませんでした」と次の手を言う／**できないこと（拡大）を案内しない**／図は1pxも動かない |

**タッチでの実測**（Playwright・実タップ・`hasTouch`）:

| 画面 | 札の位置 | 可視域内 | 覆われ | 高さ | 押した結果 |
|---|---|---|---|---|---|
| 375×812 | (160, 676) | ○ | 無し | 34px | 最短距離 38 → 218.3px・同じ反応が通る |
| 393×635（実測 iPhone） | (17, 499) | ○ | 無し | 34px | 同上 |

⚠ **帯の段**: 320px・375px では増えない（既存の2行目に相乗り）。
**393×635 だけ 73 → 113px に1段増える**（札が出ているあいだだけ）。
キャンバスの高さは変わらず、横スクロールも出ない（実測）。

### 10.11-b. ★★ 付加縮合の「描けない」は取り下げ（2026-08-26・ユーザー）

**§10.11 は付加縮合（入試 64件・最頻）を「⚠ 三次元の網目が描けない」として「入れないほうがよい」に置いた。**

**ユーザーの指摘**:

> ★★ **付加縮合は平面上で表現できるかと思います**

✅ **取り下げる。**⚠⚠ **こちらの見立てが、この教材の芯とずれていた。**

> ★ **実物のフェノール樹脂は三次元の網目。**⚠ **しかし教科書が描いているのは平面の図。**
> ★★ **このアプリの仕事は「紙面の構造式を読めるようにする」ことで、実物の立体を再現することではない**
> （★ **発注の芯**）。**平面で描けるなら、描いてよい。**

⚠ **加硫（§18）と混同していたのが原因。**★ **あちらは「1本の鎖に橋が架かる絵」が化学として誤解を招くから断った**のであって、
**「三次元だから描けない」ではなかった。**★ **付加縮合には、その種の誤解は無い。**

### ★ そのうえで、本当に残る費用（⚠ 実装前に決めること）

| # | 何が要るか | ⚠ いまと違うところ |
|---|---|---|
| ★ **1** | **分岐した高分子** | ⚠⚠ **既存の重合3本はどれも「1本の鎖」**（`R–[単位]ₙ–R`）。**1つの環から -CH₂- が2本以上出る形を持っていない** |
| ★ **2** | **結合手 R が3本以上出る** | ⚠ **いまは両端の2本が前提**（★ **`attachR` の使い方・名前の付け方・正準コードの見え方を確かめること**） |
| ★ **3** | **どこまで描くか** | ⚠ **網目は無限に伸びる。**★ **代表の断片を何量体で止めるかを決める**（**教科書の図が何個ぶん描いているかを見て合わせる**） |
| **4** | **o/p の位置** | ★ **-CH₂- が付くのはヒドロキシ基の o と p**（**配向性の規則層 `aromaticSiteRole()` が既にある**） |

★ **1 と 2 が本体。**⚠ **「描けない」ではなく「1本の鎖しか持っていない」が正しい言い方。**

⚠ **同じ形の穴がもう1つある**: **共重合**（§11.6 で判断済み）と**開環重合**（§10.11 の推奨④）。
★ **重合まわりを一度まとめて見直すほうが、1本ずつ足すより安い可能性がある。**⚠ **未測定。**

⚠⚠ **→ 測った（§21・2026-08-30）。安くならなかった** ——
共通化で減る新規関数は3本だが、**代わりに緑のテストを持つ既存関数を2つ触ることになる**。
★ **項目2「R は両端の2本が前提」も測ったら半分外れ**（モデルの制約ではなく、`attachR` を
2回呼んでいる8か所の書き方。`chemistry.js` の価標表は `'R': 1` の1行だけ）。詳細は **§21-1・§21-4**。
---

## 20. 加硫の箇所選びが永久に終わらない（v1467・2026-08-27 動画レーンの実測報告 §6-1）

**症状**: 鎖を2本つくって加硫を押すと「弾性ゴム（**7箇所**）」の札は出るのに、
光った原子をクリックしても確定しない。何度押しても同じ候補に戻り、**硫黄は1つも入らない**。

**測ったこと**（`reactor.picking` を読んで `handlePick` を4回呼んだ実測）:

```
#0 sites=7 → click C(316,300)
#1 sites=3 → click C(316,300)
#2 sites=3 → click C(316,300)
#3 sites=3 → click C(316,300)
式: C₂₀H₃₂R₄   S: （なし）
```

### 20-1. 原因 ——「向きだけ違う同じ組」を7本返していた

`vulcanizablePairs` は C=C の組ごとに**二重結合の両端どちらでも架橋しうる**として4通り
（`[ca, ca2, cb, cb2]`）を返していた。4通りは「**硫黄がどちらの炭素に付くか**」が違うだけで、
**site の4原子はまったく同じ**。実測した7組も、①③⑥ が同じ4原子・②④⑤⑦ が同じ4原子だった。

`Reactor.narrow` は候補を**原子の集合**でしか分けられない:

```js
const distinguishing = [...ids].filter(id => !sites.every(s => s.includes(id)));
const pickIds = distinguishing.length ? distinguishing : [...ids];   // ← ここが罠だった
```

残った候補の原子集合が全部同じだと `distinguishing` が**空**になり、`pickIds` が全原子に落ちる。
`handlePick` は `matched.length > 1` で `narrow` に戻すので、**押す → 同じ候補に戻る**の堂々巡り。
⚠ この形は §18-3（同じ鎖の中で橋が架かる）とは別。あちらは「**間違った場所**に架かる」で、
こちらは「**どこにも架からない**」。18-3 を直して鎖が2本になったところで初めて表に出た。

### 20-2. 直した形（★2段構え。片方だけでは足りない）

**① `vulcanizablePairs` は C=C の組ごとに橋を1本だけ返す。**
どちらの炭素に硫黄が付くかは化学的に等価（どちらでも二重結合が単結合へ移るだけ）なので、
**短い橋になるほう**を選んで1件にまとめた。教科書の図に近く、
**「（N箇所）」の N が実際に選べる橋の本数と一致する**（実測 7 → **2**）。
⚠ 4通りを**生成するのはやめない**。空きの検査（`MIN_CLEARANCE`）を通るのが1通りだけ、
ということがあるので、4通り試して**通ったうちの最短**を採る。

**② `narrow` に安全弁を入れた。**
`distinguishing` が空 ＝ どの原子を押しても候補を分けられない ＝
**どれを選んでも同じ原子が反応する**ということなので、選ばせずに先頭を実行する。
以前の「分けられないなら全原子をハイライトする」は**選べない選択肢を見せていた**だけだった。

```js
if (!distinguishing.length) { this.execute(rule, sites[0], reagent); return; }
```

⚠ ①だけでは足りない ——**他のルールが同じ形の site を返した日に、また同じ止まり方をする**。
⚠ ②だけでも先へは進むが、**札が「7箇所」と言い続ける**（選べるのは2つしかないのに）。

### 20-3. 実測（直した後・実アプリ）

イソプレン ×2 → 1,4-付加重合 → イソプレン ×2 → 1,4-付加重合 → 加硫:

| 見たもの | 直す前 | 直した後 |
|---|---|---|
| 札 | 弾性ゴム（**7箇所**） | 弾性ゴム（**2箇所**） |
| 箇所選び | 7 → 3 → 3 → 3 →……（終わらない） | 8原子が光り、**1クリックで確定** |
| 硫黄 | 0個 | 1本目 1個 → 2本目は候補1件で即実行 ＝ **2個** |
| 硫黄を外したときの鎖 | —— | **2本**（同じ鎖の中で架かっていない） |
| 価標 | —— | 全原子 正常 |

### 20-4. 台本（動画レーン）

`assembler/demos-reaction.json` の `reaction-rubber-vulcanization` に**加硫の一手を戻した**
（§18-3 の時点では `reactionButton` が文言を見つけられず N2 が落ちるので外してあった）。
`reactionButton` は**箇所選びに入ったら候補の原子を1つ押す**作りなので、①で1クリックに
なったことで台本のまま最後まで通る。⚠ **`sns` は `false` のまま**にした ——
台本が通ることと SNS へ出すことは別で、語りと構図は収録側の判断。

### 20-5. 回帰テスト

| 番号 | 見るもの |
|---|---|
| PM13 | 候補に**原子の集合が同じ組が無い**／`narrow` → クリックで確定して硫黄が1個入る（クリック数に上限）／**安全弁そのもの** ＝ 向きだけ違う2件を渡すと選ばせず先頭を実行する |
| PM14（否定対照） | **分けられる候補では今までどおり箇所選びに入る**（安全弁が先食いしていない）／**橋の本数が減っていない**（鎖2本 × C=C 2つ ＝ 2本架かる・硫黄を外すと鎖2本）／単量体は今までどおり相手にしない |

---

## 10.12 §10.11 の推奨から7件を実装した（v1472・2026-08-27〜28）

★ **§10.11-F の推奨5件＋次点2件のうち、7件が着地した。**⚠ **開環重合（推奨④）だけ落ちた**——
理由は下の「⚠ 落としたもの」に測った値つきで置く。★ **1修正=1コミットで積んである。**

### A. 着地した7件（★ どれも否定対照つき）

| # | 反応 | テスト | 実測（★ 正準コードが登録エントリと一致） |
|---|---|---|---|
| **1** | **環内 C=C の酸化開裂** | RC2 | シクロヘキセン → **アジピン酸** ／ シクロペンテン → **グルタル酸** |
| **2** | **単発のアミド化** | RC5 | 酢酸＋アニリン → **アセトアニリド** ／ 酢酸＋メチルアミン → **N-メチルアセトアミド** |
| **3** | **分子内脱水 → 酸無水物** | RC6 | フタル酸 → **無水フタル酸** ／ マレイン酸 → **無水マレイン酸** ／ コハク酸 → **無水コハク酸**（★ 往復も通る） |
| **4** | **末端 C=C の酸化開裂** | RC2 | プロペン → **酢酸** ／ イソブテン → **アセトン** ／ α-メチルスチレン → **アセトフェノン** |
| **5** | **エチレンの案内文** | RC2 | 実行しない。⚠ **文面はユーザー承認済みのものをそのまま** |
| **7** | **ワッカー法** | RC8 | エチレン → **アセトアルデヒド**（⚠ 瓶が1本増えた） |
| **8** | **ニトロ還元 → 芳香族アミン** | RC7 | ニトロベンゼン → **アニリン** ／ p-ニトロトルエン → **p-トルイジン** ／ m-ジニトロベンゼン → **m-フェニレンジアミン** |

⚠ **#1 は「統合レーンによる退避コミット・未検証」（`768364d`）として先に積まれている。**
★ **再開後に実装レーンが検証し直した**（`--amend` は禁じられているのでコミットメッセージは直せない）:
caption の言い分けが入っていること／シクロヘキセン → アジピン酸・シクロペンテン → グルタル酸の
正準コード一致／**否定対照4本が壊すと赤くなること**（環を実行対象から外す・caption の言い分けを外す・
炭化水素の門番より先に環を見る・案内に `ring` を残す）を、いずれも実測で確かめた。

### B. ⚠ 設計書と食い違って判断したところ（★ 黙って変えていない）

| # | 設計書の記述 | ★ 実装で変えたこと・理由 |
|---|---|---|
| **1** | §10.3-d 「残っているのは caption の文面だけ」 | ★ **門番の順序も変えた**。`alkeneCleavageClass` は環かどうかを**炭化水素かより先に**見ていたので、そのまま `ring` を実行に回すと **2-シクロヘキセン-1-オンのような環内 hetero まで通ってしまう**。順序を入れ替え、鎖の `hetero` と扱いをそろえた。⚠ その副作用で、環内 hetero は `info` からも外れた（鎖の hetero がもともと黙っているのと同じ扱いになった） |
| **2** | §10.3-d の表 「アレン → terminal・カルボン酸」 | ⚠⚠ **そのままでは壊れる**。アレン `C=C=C` の端を切ると、残る炭素に **=O と =C が同時に付いて価標が5本**になる（実測）。★ **「残る側の結合が単結合であること」という門番を1つ足した**（設計書に無い判断）。アレンは `hetero` へ落ちる |
| **3** | §10.11-F ⑤ 「`esterification.detect` にアミンを足すだけ」 | ★ **別ルール `amidation` にした**。`esterification` は `label`・`mechanismId: 'esterification'` を持っており、アミドの生成にエステル化の機構アニメを結びつけたくない。⚠ **`apply` は1か所も違わない**ので `applyAcidCondensation` に抜き出して**共有**した（写すと片方だけ直る） |
| **4** | §10.11-D #3 「オルト位の -COOH 2つから水1分子」 | ★ **鎖状の二酸（コハク酸・グルタル酸）も入れた**（5員・6員になるものは同じ形で開く）。⚠ かわりに**メタ（イソフタル酸）を落とす門番**が要る——大きさだけ見ると6員で通ってしまうため、**経路が環を通るならオルトのときだけ**とした |
| **5** | （設計書に記述なし） | ★ **マレイン酸／フマル酸の見分けを入れた**。C=C をまたぐ分子内脱水は**シス形だけ**通す。⚠ **入試がいちばん問う点**で、`stereo` レイヤと同じ「C=C まわりの座標だけは読む」例外に乗せた。**図から読めないとき**（ブテン二酸）は「まず描き分けて」と言う |
| **7** | §10.11-F 次点 「§10.9 の申し送りを先に片づけるか、1本だけ足すかの判断が要る」 | ★ **区分を割らずに1本だけ足す**を選んだ。区分割りは試薬パレット側の設計であり、またこの瓶は**エチレン専用で他では必ず空振りする**ので区分が増えるたぐいではない。⚠ **CO₂ の瓶（§10.9）を足すときに、そこで区分割りを決めること** |
| **8** | §10.11-D #5 「-NO₂ → -NH₂ は原子の置き換えだけ」 | ★ **芳香環に直結した -NO₂ だけ**に絞った。ニトロアルカンの還元も化学としては起こるが、教科書が扱うのは芳香族だけ（§4-1）。★ **瓶は増やさず `h2_ni` に相乗り**させた結果、「**H₂/Ni ではベンゼン環は水素化されず、ニトロ基だけが還元される**」が画面でそのまま起こる ＝ 入試の頻出点と一致した |

### C. ⚠ 落としたもの —— **開環重合（§10.11-F ④）**

⚠⚠ **実装して、動かなかった。**★ **設計書の見立て「§15 と §14 がそのまま使える」は当たらない。**

**測った値**:

- `lactamUnits`（環状アミドの単量体を集める `detect`）は**正しく動いた** ——
  ε-カプロラクタム 2個で 1件・3個で 1件、アセトアニリド／シクロヘキサン／エチレン／
  アジピン酸／グリシンでは 0件
- ⚠⚠ **`apply` が落ちる**。**2個でも**「生成物を配置する空間がありません」（`planAttachment` が `null`）
- ★ **原因（幾何）**: 環を開いても**図は7角形の弧のまま**で、`planAttachment` は
  **弧を弧の隣に置けない**。C-N の結合1本（約39px）でつなぐと、7員環どうしの中心距離が
  半径2つぶんとほぼ同じになり、`minGapAmong` の間隔を満たせない

★ **本当に要るのは「開いた環を、直線の鎖に描き直す」処理**（§14 の一直線化は
**もともと鎖状の単量体**を前提にしていて、環をほどく仕事は持っていない）。
⚠ **これは caption や門番の話ではなく、新しい作図routine。**
★ **§10.11-b の末尾が「重合まわりを一度まとめて見直すほうが、1本ずつ足すより安い可能性がある」と
書いているのは正しい** —— **付加縮合（分岐）・共重合・開環（環をほどく）は、
どれも「1本の鎖しか持っていない」という同じ1点に当たっている。**

⚠⚠ **→ ここは測ったら違った（§21-3 (e)・2026-08-30）。**「同じ1点」ではなく **3つの別々の点**である
（付加縮合＝`linkFrom` がスカラ／開環＝`planAttachment` が 25.5px を要求して 17.5px しか出ない／
共重合＝`componentCode` でそろえる門番）。★ **ビニロンはどれにも当たっていない**（既存の橋と鎖判定の
組み合わせで届く）。⚠ **落ちる場所の特定は正しかった**（`planAttachment`。16通り全滅を数で確かめた）。

### D. ⚠ 縮合重合の `wholeCanvas` 付け忘れ（v1465 の穴・機構レーンの申し送り）

★ **穴は実在した**（推測で付けず、**PM13 で再現してから**付けた）。

⚠ **4分子ちょうどでは出ない穴**——縮合重合の箇所は 酸→相手→酸→相手 と
**4分子ぜんぶの原子を含む**ので、どれをモーダルで見ていても `focus` に必ず当たる。
★ **出るのは PM9 と同じ形**: 1本目の鎖を作ったあと単量体を4つ並べ直すと、
2本目の箇所には①の鎖の原子が1つも無いのでここで落ちる
（実測: `detect` は1件返すのに、ボタン一覧は空）。

### E. 数の変化（★ 後で数え直せるように）

| | 前 | 後 |
|---|---|---|
| `REACTION_RULES` | 42本 | **47本**（＋ amidation・dehydration_anhydride・dehydration_anhydride_info・reduce_nitro・wacker_oxidation） |
| 瓶（`REAGENTS`） | 22本 | **23本**（変えるもの 17 → 18） |
| 瓶を持たない実行ルール | 8件 | **10件**（＋ amidation・dehydration_anhydride） |
| 瓶に紐づくルール | 33件 | **35件**（＋ reduce_nitro・wacker_oxidation） |
| `wholeCanvas` を持つルール | 3件 | **4件**（＋ condensation_polymerization） |
| `REVERSIBLE_REACTION_PAIRS` | 2組 | **3組**（＋ 分子内脱水 ⇄ 酸無水物の加水分解） |
| 回帰テスト | 644件 | **648件**（＋ RC5・RC6・RC7・RC8・PM13。RC2 は書き換え） |

### F. ⚠ 未確認のまま残したこと（★ 黙って落とさない）

- ⚠ **`amidation` の `isAmideNitrogen` の門番は、外しても赤くならない**。
  §9.6-7 で `findFunctionalGroups` 自身がアミドの N をアミンから外したため。
  ★ `acetylation_anhydride` と同じ「二重の防波堤」として残してある（コメントに明記）
- ⚠ **`amidation` は `SELF_PARTNER_RULES`（1分子からの入口・§15）に入れていない**。
  相手が別の分子（アミン）なので「自分をもう何個か呼ぶ」形にならない。
  ★ ただし `PARTNER_CANDIDATES` 側に足すかは**測っていない**
- ⚠ **分子内脱水の架橋 O の置き場は「中点 ＋ 外向きに逃がす」の当てはめ**で、
  正五角形を狙ってはいるが**登録エントリの座標には合わせていない**
  （無水フタル酸は結果的に一致した。無水マレイン酸・無水コハク酸は一致していない ＝ **図の見た目だけの差**）
- ⚠ **ワッカー法を末端アルケン一般に広げるか**は測っていない（教科書がエチレンしか書かないので絞った）
- ⚠ **ニトロ還元の入試47件は「上限」印**（§10.11-D の注記）。数え直していない

---

## 21. 重合まわりの見直し（設計のみ・2026-08-30）

★ **目的**: 入試頻度の高い4本 —— **付加縮合 64件・開環重合 44件・共重合 43件・ビニロン 34件** ——
を足すために、いまの重合の作りをどう直すかを決める。⚠ **決めるところまで。実装はしない。**

⚠ **このレーンは `assembler/reactor.js` を1行も変えていない。**この節はすべて
**v1479 のコードを読んで測った値**で書く。★ **測れなかったものは「測れなかった」と書く**
（推測と実測を混ぜない）。

### 21-1. いまの重合が何を前提にしているか（★ すべて v1479 の実測）

#### (a) 重合まわりのルールは7本（`REACTION_RULES` 全47本のうち）

| id | 行 | 瓶 | `wholeCanvas` | 単量体をどう受け取るか | 何分子要るか |
|---|---|---|---|---|---|
| `addition_polymerization` | 3629 | 無し | **true** | `vinylBonds` ＋ `componentCode` で**同じ単量体をまとめる**。⚠ **C=C が2本以上ある分子は除く**（どちらを開くかが一意でないため） | **2以上** |
| `alkyne_polymerization` | 3742 | 無し | **true** | `acetyleneUnits`（**分子全体が C≡C の2原子だけ** ＝ アセチレンに限る） | **2以上** |
| `diene_polymerization` | 3802 | 無し | **true** | `conjugatedDienes`（C=C がちょうど2本・単結合1本を挟む）＋ `componentCode` | **2以上** |
| `condensation_polymerization` | 3926 | 無し | **true** | `condensationPolymerUnits`（2価カルボン酸 n 個 ＋ 2価アルコール／2価アミン n 個） | **4以上（2組）** |
| `condensation_polymer_info` | 4023 | 無し（`info`） | —— | 上が成立しないときの `condensationPolymerPartners`（**1個ずつ**） | 2（説明だけ） |
| `vulcanization` | 3880 | `sulfur` | ⚠ **無し** | `vulcanizablePairs`（**R を含む成分**の C=C・**別の鎖どうし**に限る） | 鎖2本 |
| `condensation_glycoside` | 3560 | 無し | ⚠ **無し** | 糖どうしのグリコシド結合 | 2 |

⚠ **`vulcanization` に `wholeCanvas` が無いのは穴ではない**（実測して確かめた）——
箇所は2本の鎖にまたがるので、どちらの鎖をモーダルで見ていても `focus` に必ず当たる
（`siteFilter()` の `ids.some(id => focus.has(id))` が true）。

#### (b) 何ユニットぶん描くか ——★ **固定していない。「並べた単量体を全部」**

4本の重合ルールはどれも `detect` が**並べた単量体をすべて含む site を1件**返し、
`apply` はそれを端から順に繋ぐ。**n を決め打つ定数はコードのどこにも無い。**

★ **3という数が現れるのは2か所だけ**（どちらもルールの外側）:

1. **入口** `SELF_PARTNER_UNITS = 3`（4999行）…「1分子しか無い人にもう2つ呼ばせる」個数
2. **登録エントリ** `assembler/compounds.json` …
   ⚠ **R を持つエントリは全940件中5件**（実測）。うち高分子は4件で、**4件とも ちょうど3単位＋両端 R**:

   | 名前 | 示性式 | 全原子 | R | 単位あたりの重原子 | 単位数 |
   |---|---|---|---|---|---|
   | ポリアセチレン | `[CH=CH]n` | 8 | 2 | 2 | **3** |
   | ポリビニルアルコール | `[CH2-CH(OH)]n` | 11 | 2 | 3 | **3** |
   | ナイロン66 | `[NH(CH2)6NHCO(CH2)4CO]n` | 50 | 2 | 16 | **3** |
   | ポリエチレンテレフタラート | `[CO-C6H4-CO-O-C2H4-O]n` | 44 | 2 | 14 | **3** |

   （5件目は `アルキルベンゼンスルホン酸ナトリウム`。**R が1本**の界面活性剤の図）

⚠ **ポリエチレン・ポリ塩化ビニル・ポリスチレンは登録されていない**（実測）。
＝ 付加重合の生成物の多くは、いまも「（未登録）」のまま出ている。

#### (c) 端の表し方 ——★ **「R は両端の2本」はルール側の書き方であって、モデルの制約ではない**

⚠ **§10.11-b の表の項目2「いまは両端の2本が前提」は、測ったら半分だけ当たっていた。**

**実測**:

- `attachR` の呼び出しは**ちょうど8か所 ＝ 4ルール × 2回**（3720/3721・3785/3786・3862/3863・4000/4001）。
  ★ 他に呼ぶところは1つも無い。**「2本」はこの8か所の書き方**である
- ⚠ **モデル側に制約は無い**。`assembler/chemistry.js` の価標表は **`'R': 1` の1行だけ**で、
  1分子に R が何本あってもよい。実際 `アルキルベンゼンスルホン酸ナトリウム` は **R が1本**
- ★ **3本以上の登録エントリは0件**。⚠ **禁じられているのではなく、前例が無い**
- `attachR(mol, atomId, prefer)` は `prefer` の向きを最初に試し、だめなら
  **直交4方向 → 斜め4方向**の計8方向。空きの判定は `MIN_CLEARANCE = bondStep × 0.65`

★ **正しい言い方は「R を3本出す仕組みが無い」ではなく「R を3本出した前例が無い」。**
⚠ **本当に無いのは、鎖を枝分かれさせる `apply` の書き方**（下の (d)）。

#### (d) `apply` の形 ——★ **4本とも「端を1つだけ持って前へ進む」ループ**

4本の重合はどれも同じ骨格で書かれている（`addition_polymerization` 3679〜3722 ほか）:

```js
let linkFrom = units[0].head;      // ★ 鎖の端は「1つ」しか持たない
let linkBack = units[0].tail;      //    伸びる向きを決めるための1つ内側
for (let i = 1; i < units.length; i++) {
    ...planAttachment(mol, linkFrom, u.tail, movingIds, [...pending],
                      chainDirection(mol, linkBack, linkFrom));
    mol.addBond(linkFrom, u.tail, 1);
    linkBack = u.tail; linkFrom = u.head;   // ★ 端を1つ「進める」だけ
}
```

⚠ **分岐が無いのはここ**。`linkFrom` がスカラで、枝が生えても行き先を覚えられない。

⚠ **ただし「橋を架ける」形はすでに2本ある**（★ ここが §10.11-b の見立てとずれた点）:

| 既存ルール | 何を橋にするか | 置き場の決め方 |
|---|---|---|
| `vulcanization`（3880） | **S 1原子** | 2つの炭素の**中点を格子に丸める**（`Math.round(mid / G) * G`）→ 空きを見る |
| `dehydration_anhydride`（4313） | **O 1原子** | `anhydrideBridgeSpot`（1753）… 中点から**経路の内側の重心と反対向き**へ逃がす。正五角形に近い距離から順に4段 |

★ **「2つの原子の間に原子を置いて両方に繋ぐ」routine は、すでに2通り書かれている。**

#### (e) キャンバスの空きの見方

- `NoRoomError`（28行）／`noRoom(message)`（37行）… 場所不足を**型**で伝える（§19）
- 重合4本はすべて `if (!plan) throw noRoom('生成物を配置する空間がありません')`
- 判定の本体は **`planAttachment`（205行）**:
  - `G = bondStep(anchorId)`（母体の刻み）／`MIN_CLEARANCE = G × 0.65`
  - 向きは **`prefer` → 右・上・下・左**、回転は **[0, 180]**（`canSpin90` が通れば +90・270）
  - **strict（結合線の貫通も見る）→ 非strict** の2周、それぞれで
    「**反応に関わらない傍観分子をどかす**」（`planShoveAside`）を試す
- ★ 重合は**まだ繋いでいない単量体を `ignoreIds`（`pending`）に入れて障害物から外す**（§14）
- `wholeCanvas` は `siteFilter()`（5467行）の**分岐1つ**でしかない ——
  「選択が無いとき、いま見ている分子で絞らない」だけ

#### (f) ★★ 開環重合が落ちる場所 ——**`planAttachment` の空き判定。16通り全滅**

⚠ **まず事実**: **v1479 に開環重合のルールは無い**。`lactamUnits` も無い
（§10.12-C の実装は**取り込まれていない**）。★ したがってコードで測れるのは
「**入れたら、どこで、なぜ落ちるか**」だけ。

**測ったこと**（`assembler/compounds.json` の `ε-カプロラクタム` の実座標を使い、
`planAttachment` と同じ式を当てた）:

- 登録されている ε-カプロラクタムは **ほぼ正七角形**。
  環7原子の重心 (350, 300.1)・半径 **45.1px**・環の結合長 **38.9〜39.2px**（C=O だけ 42.0px）
- `bondStep` の中央値 **G = 39.2px** → **`MIN_CLEARANCE` = 25.5px**
- 環を開いても座標は**七角形の弧のまま**。この2つを結合1本でつなぐ置き方
  **4方向 × 4回転 = 16通り**について、動かした側と元の側の**最小間隔**を出した:

| 回転＼向き | 右 | 上 | 下 | 左 |
|---|---|---|---|---|
| 0° | 17.5 | 2.8 | 5.2 | 17.3 |
| 180° | 17.5 | 2.8 | 9.1 | 17.5 |
| 90° | 9.1 | 2.8 | 9.1 | 17.5 |
| 270° | 15.7 | 2.8 | 12.2 | 5.6 |

★ **16通り全滅。**いちばんましな置き方でも **17.5px** で、要求の 25.5px に **8px 足りない**。

⚠ **逃げ道も無い**: `planAttachment` の `search(true, …)`（傍観分子をどかす道）は
blockers を `core = componentOf(anchorId)` に絞るだけで、**ここでは邪魔者そのものが core
（相手の環）**なので1つも減らない。`planShoveAside` に渡る `bystanderIds` は**空**になる。
よって `planAttachment` は null を返し、**`throw noRoom('生成物を配置する空間がありません')`** に落ちる。

★ **結論**: 落ちるのは **`planAttachment` の空き判定（205行・`search` の `ok = pts.every(...)`）**
であって、`detect` でも門番でも caption でもない。
⚠ **`MIN_CLEARANCE` を下げて通す道は無い** —— 17.5px は**実際に重なって見える**距離である
（結合1本 39px の 45%）。★ **環をほどいて直線に描き直す以外に道が無い**ことを、数で確かめた。

#### (g) `findCoPolymerHints`（v1477）は足がかりになるか ——★ **入口としては、なる。本体は別**

**実測**（4910〜4966行）:

- `COPOLYMER_PAIRS` は**名前の組の表**が2組だけ
  （`アジピン酸 / ヘキサメチレンジアミン`・`テレフタル酸 / エチレングリコール`）
- `findCoPolymerHints` は「**相手を2つ ＋ 自分をもう1つ**」を並べた試算を作り、
  `condensation_polymerization.detect` が**実際に箇所を返したときだけ**札を出す
  （名前の一致で出しているのではない）
- ★ **「2種類の単量体を並べさせる入口」は、これでもう在る。**
  共重合（スチレン＋ブタジエン）も付加縮合（フェノール＋ホルムアルデヒド）も、
  **この表に1行足すだけで札が立つ**

⚠ **ただし立つのは入口だけ。**繋ぐ本体のほうは、むしろ**共重合を明示的に拒んでいる**:

```js
// condensationPolymerUnits（2288行）
const ps = sameAs(partners.filter(p => p.kind === kind), partners[0]);
const as = sameAs(acids, acids[0]);          // ← componentCode が一致するものだけ
```

★ **`sameAs`（`componentCode` の一致）が「単量体は1種類」の門番の実体**である。
`addition_polymerization` も `groups`（正準コード → 単量体の配列）で同じことをしている。

#### (i) ★ 呼び出せる単量体（実測。⚠ **自分の見立てが1度外れた**）

⚠ **名称ライブラリは `compounds.json` だけではない**（`getCompoundLibrary()` は
**stages.json ＋ compounds.json** を合わせる。game.js:3668）。
★ **compounds.json だけを見て「フェノールが無い」と書きかけて、外した。**
合わせた名称は **1000件**で、実測は次のとおり:

| 単量体 | 呼べるか | 出どころ |
|---|---|---|
| フェノール／ホルムアルデヒド／1,3-ブタジエン／ベンゼン | **○** | **stages.json**（⚠ compounds.json には無い） |
| スチレン／アクリロニトリル／イソプレン／クロロプレン／酢酸ビニル／尿素／ε-カプロラクタム | **○** | compounds.json |
| ポリビニルアルコール／ナイロン66／ポリエチレンテレフタラート／ポリアセチレン | **○** | compounds.json（**3単位＋R の高分子の図**） |
| **ジビニルベンゼン** | ⚠ **×** | —— |
| **メラミン** | ⚠ **×** | —— |
| ポリエチレン／ポリスチレン／ポリ塩化ビニル／ポリ酢酸ビニル／フェノール樹脂／ビニロン／ナイロン6 | ⚠ **×** | ——（**生成物が名乗れない**） |

★ **4本のうち3本は、単量体がもう全部呼べる**（付加縮合＝フェノール＋ホルムアルデヒド、
開環重合＝ε-カプロラクタム、ビニロン＝ポリビニルアルコール）。
⚠ **足りないのは生成物側の図**（＝ 実行できても「（未登録）」と出る）。

#### (j) ⚠ 測れなかったもの（★ 黙って落とさない）

- **酢酸ビニルの付加重合がいま通るか**は測れなかった。§14.5 は
  「スチレン・酢酸ビニルは**置換基が鎖の伸びる先にあって置けない**」と書いているが、
  その後 §18-1（v1466）で `uprightChainSubstituent` が入り、**スチレン ×3 は直っている**。
  ★ **ビニロンの前段（酢酸ビニル → ポリ酢酸ビニル）が通るかどうかは実アプリで要確認**
- **`aromaticSiteClass` が -CH₂- 置換をどう束ねるか**は読んだだけで、実物では動かしていない
- **実アプリを1度も開いていない**（このレーンは設計だけで、`reactor.js` に触れないため）。
  ★ 上の数はすべて**コードとデータを読んで出した値**である

### 21-2. 教科書が実際にどう描いているか（★ 実物を読んで測った）

**出典**: 数研出版系『R5化学Vol.2』6編
（`マイドライブ/化学/教科書/R5化学Vol.2-6編.pdf`。**PDFページ ＝ 書籍ページ − 208**）。
★ **以下の「何個描いてあるか」は、図を数えた値である。**

#### (a) 比較の基準 —— 付加重合と、いまのアプリの一致

| 場所 | 描き方 |
|---|---|
| p.256 式(7)（付加重合の一般式） | `n H₂C=CHX → -[CH₂-CHX]-ₙ`。**角括弧＋右下 n・繰り返し単位1つ**・結合線が括弧を貫く |
| p.257 表1（熱可塑性樹脂8種） | **8種すべて `[ ]ₙ` の1単位** |
| p.212〜213（重合の分類） | **球棒モデルの模式図**。⚠ **開環重合・付加縮合・共重合には図が無く文章のみ** |

★ **いまのアプリは「3単位＋両端 R」で、教科書の「1単位＋[ ]ₙ」とは違う。**
⚠ **これは今回の4本で新たに生じるずれではない**（既存の4つの登録エントリが全部そう）。

#### (b) 付加縮合（p.258・再掲 p.266／表3 p.259）

★ **フェノール樹脂は、平面の構造式ではっきり描かれている**
（本文は「三次元網目状」と書くが、**図は平面**。⚠ **遠近法の絵は6編のどこにも無い**）。

- **ベンゼン環6個（横3列 × 縦2段）**、**-CH₂- が7本**（横4・縦3）
- ★ **各環から -CH₂- が3本**（オルト2＋パラ1 ＝ **2,4,6-三置換**）
- 上段は OH が真上、**下段は上下反転**
- **端は `n` も角括弧も使わず、左右4か所に「…」**

**中間体は `[ ]ₙ` 形式で併記**:

| 中間体 | 触媒 | n | 形 |
|---|---|---|---|
| **ノボラック** | 酸 | 1〜10 | `H-[環-CH₂]ₙ-環` ★ **左端に H、右端に環1個を括弧の外に実際に描く** |
| **レゾール** | 塩基 | 1〜2 | `H-[環（パラに CH₂OH）-CH₂]ₙ-OH` |

★ **素反応が2段で明示されている**（式(8)(9) p.258）:

1. **(8) 付加** … フェノール ＋ HCHO → **o-ヒドロキシベンジルアルコール**
2. **(9) 縮合** … そのアルコール ＋ フェノール → **二量体（環2個＋-CH₂-）＋ H₂O**

⚠ **素反応式が2段に分解されているのはフェノール樹脂だけ。**

| 樹脂 | 図（p.259 表3） |
|---|---|
| **尿素樹脂** | **示性式のみ・環も骨格線も無い平面の網目図**。2本の鎖 × 各3ユニット ＋ 縦の橋3本（CO 1・CH₂ 2）。端は素の「—」が5か所。**`n` なし** |
| **メラミン樹脂** | ★ **トリアジン環1個から -NH-CH₂- が3本**（上・左下・右下）、先は素の「—」。**`n` なし** |

#### (c) 開環重合（p.251 式(2)）

★ **環の図と鎖の図が、同じ式の左右にある。**

- 単量体（ε-カプロラクタム）は**正七角形ではなく、横長の「潰した環」** ——
  左端の頂点に H₂C、上枝 `-CH₂-CH₂-NH`、下枝 `-CH₂-CH₂-C=O`、
  **右端で NH と C=O を縦1本線で閉環**。前に係数 `n`
- 重合体は**繰り返し単位1つだけ** `-[N(H)-(CH₂)₅-C(=O)]-ₙ`（角括弧＋右下 n・結合線が括弧を貫く）
- **副生成物なし**
- ★ **直前の式(1) がナイロン66**（`n + n → [ ]ₙ + 2n H₂O`）で、
  **脱水の有無を並べて比較できる配置**になっている
- ⚠ 一般定義は p.213 にあるが**文章だけで図は無い**

⚠⚠ **ここが §21-1 (f) と正面からつながる**: 登録エントリの ε-カプロラクタムは
**ほぼ正七角形**（半径 45.1px）で、**教科書の「潰した環」ではない**。

#### (d) 共重合 —— ★ **描き方が2通りある**

**① SBR / NBR（p.264 表4）… 入れ子の括弧**

- 内側は**丸括弧**で、スチレン単位に添字 **x**、ブタジエン単位に添字 **y**
- 外側は**角括弧**で全体に **n**
- ★ **交互ではなく、ブロック的に「A が x 個、B が y 個」と並べる**
- ⚠⚠ **「ランダム」の語も記号も無い**
- NBR は環が -CN に替わるだけ

**② p.260 式(11)（スチレン ＋ p-ジビニルベンゼン）… 梯子状の平面図**

- スチレン `n` ＋ p-ジビニルベンゼン `m` →
  **ポリスチレン鎖を上下2本・各3ユニットぶん実際に描き、
  間を p-フェニレン1個で架橋した梯子状の図**
- 端は素の「—」＝ **付加縮合の網目図と同じ流儀**

★ **②は「2本の鎖を橋でつなぐ」＝ いまの `vulcanization` の絵とまったく同じ形。**

#### (e) ビニロン（p.254 式(5)）—— ★ **3段階で表記が変わる**

| 段 | 反応 | 描き方 |
|---|---|---|
| (1) | 酢酸ビニル → ポリ酢酸ビニル | `-[CH₂-CH(OCOCH₃)]-ₙ`（**1単位**） |
| (2) | けん化 → PVA | `-[CH₂-CH(OH)]-ₙ`（**1単位**） |
| (3) | **アセタール化** | ★ **PVA を3ユニットぶん実際に描く**。端は**短い破線2本＋実線**、**`n` も角括弧も無し** |

- 生成物は **隣り合う2つの OH が O-CH₂-O の六員環アセタールになり、3つ目の OH は残る**
  （**3ユニット中2つ ＝ 2/3**）
- ⚠⚠ **「-OH の約1/3が」のような割合の但し書きは、本文にも脚注にも無い**
- 代わりに **章末問題 p.268 問5(5)** が同じ3ユニット構造を `[ ]ₙ` で囲んで与え、質量計算をさせる
  ＝ ★ **教科書は 2/3 アセタール化を暗黙の理想化として固定している**

★★ **(3) の「3ユニットぶん実際に描く」は、いまのアプリの「3単位＋両端 R」と一致する。**
⚠ **4本のうち、教科書の図とアプリの規約が最初から噛み合っているのはビニロンだけ。**

### 21-3. 4本それぞれに何が足りないか（★ 21-1 の実測 × 21-2 の教科書）

**仕掛けに名前を付けて数える。**★ 以下 `P-a`〜`P-h`。

| 記号 | 仕掛け | いまの状態 |
|---|---|---|
| **P-a** | **橋を架ける**（2つの原子の間に原子（群）を置いて両方に繋ぐ） | ⚠ **既に2通りある**（`vulcanization` の S・`dehydration_anhydride` の O）。**どちらも1原子** |
| **P-b** | **分岐**（鎖の端を2つ以上持って伸ばす `apply`） | **無い**。4本とも `linkFrom` がスカラ |
| **P-c** | **環をほどいて直線の鎖に描き直す** | **無い** |
| **P-d** | **できた鎖（R 止めの高分子）を反応の対象にする `detect`** | **1つある**（`vulcanizablePairs` の `inPolymer`） |
| **P-e** | **繋ぎ方の違う2種類の単量体を1本の鎖に並べる** | **無い**。`componentCode`／`sameAs` が明示的に拒む |
| **P-f** | **置換基が2つ以上ある環での o/p 判定** | ⚠ **無い**。`aromaticSiteRole` は `substituted.length !== 1` で **null を返す**（実測1332行） |
| **P-g** | **相手を呼ぶ入口の札** | **2つある**（`findSelfPartnerHints`・`findCoPolymerHints`）。★ **表に行を足すだけ** |
| **P-h** | **生成物の登録エントリ**（`compounds.json`） | 高分子は**4件のみ**（全部 3単位＋R 2本） |

#### (a) 付加縮合（フェノール樹脂・入試64件）—— ★ **4本でいちばん重い**

教科書 p.258 の図（環6個・各環から -CH₂- 3本・端は「…」）を出すのに要るもの:

| 要るもの | いまとの差 |
|---|---|
| **P-a** -CH₂- の橋（**炭素1つ ＋ 水素**。HCHO の O は水として抜ける） | ★ 既存の橋は S・O の**1原子**。⚠ **元素が違うだけ**で形は同じ |
| **P-b** ★ **1つの環から3本**（オルト2＋パラ1） | ⚠⚠ **前例ゼロ**。`linkFrom` を1つしか持たない `apply` の骨格そのものを変える |
| **P-f** ★ **2置換目からの o/p** | ⚠ `aromaticSiteRole` は1置換の単環しか答えない。**2,4,6-三置換を導くには別の判定** |
| **P-g** 入口 | フェノール・ホルムアルデヒドとも**呼べる**（stages.json）。★ `findCoPolymerHints` の表に1行 |
| **P-h** 生成物 | フェノール樹脂・ノボラックとも**未登録**。⚠ **R が3本以上のエントリは前例ゼロ** |
| 端の印 | 教科書は「…」4か所。★ アプリの R は同じ意味だが、**R が3〜4本出る図**になる |
| 何量体で止めるか | 教科書は**環6個**。⚠ アプリの規約は**3単位**、`MAX_REACTION_SELECTION` は **4** |

★ **付加縮合だけが P-b と P-f を要る。**⚠ **そしてこの2つは他の3本が1つも使わない。**

#### (b) 開環重合（ナイロン6・入試44件）—— ★ **要るのは P-c 1つだけ**

| 要るもの | いまとの差 |
|---|---|
| `detect`（環状アミド ＝ 環の中に N-C(=O) がある成分） | ★ §10.12-C が**実測で動くことを確かめている**（ε-カプロラクタム 2個で1件・3個で1件、他8種で0件）。⚠ **コードは残っていない** |
| **P-c** ★ **環をほどいて直線に描き直す** | **無い**。§21-1 (f) の実測で**16通り全滅**（最良 17.5px／要求 25.5px） |
| **P-a・P-b・P-d・P-e・P-f** | ⚠ **1つも要らない**（端は2本・分岐なし・橋なし・単量体は1種類） |
| **P-g** 入口 | ε-カプロラクタムは呼べる。★ **`SELF_PARTNER_RULES` に1行足すだけ**（自分をもう2つ ＝ 既存の形そのまま） |
| **P-h** 生成物 | ナイロン6 が**未登録**（ナイロン66 はある） |

##### ★★ 追加で測ったこと ——「登録図を教科書の潰した環に描き直せば済むか」

⚠ **`compounds.json` の図を差し替えるだけ（＝ データだけの修正）で通るなら、
P-c を書かずに済む。**そこで **教科書 p.251 の「横長の潰した環」を刻み 42px で組み、
同じ式で当てた**（左端に H₂C・上枝 -CH₂-CH₂-NH・下枝 -CH₂-CH₂-C=O・右端を縦1本で閉環）:

| 何個目 | 結果 | 最小間隔 |
|---|---|---|
| **2個目** | ★ **○ 置ける**（右・180°回転） | **42.0px**（要求 27.3px） |
| **3個目** | ⚠ **× 置けない** | —— |

★ **結論**: **潰した環に描き直すと 2個目までは通るが、3個目で落ちる。**
⚠ **アプリの規約は3単位**（§21-1 (b)）なので、**描き直しだけでは足りない**。
★ **§10.12-C の「本当に要るのは『開いた環を直線の鎖に描き直す』処理」は、数の裏付けを得た。**
⚠ ただし**「2個までなら図の差し替えで動く」という中間の道がある**ことも分かった
（＝ **P-c を後回しにして先に動くものを出す**という選び方が、いちおう存在する）。

#### (c) 共重合（SBR・NBR・入試43件）—— ★ **教科書の描き方が2通りあり、重さが違う**

**① ブロック型（p.264 表4・`(スチレン)ₓ(ブタジエン)_y` を `[ ]ₙ` で括る）**

| 要るもの | いまとの差 |
|---|---|
| **P-e** ★ **繋ぎ方の違う単量体を1本の鎖に** | ⚠⚠ **ここが本体**。スチレンは **C=C 1本を 1,2-で開く**、ブタジエンは **共役ジエンを 1,4-で開く** ＝ **`addition_polymerization` と `diene_polymerization` の両方の開き方を1つのループに混ぜる**。既存3本はどれも「同じ開き方」前提 |
| 並ぶ順 | ★★ **§11.6 の「並ぶ順が決まらないから入れない」は、教科書を読んで取り下げられる** —— **教科書はブロック的に「A が x 個、B が y 個」と並べる**（⚠ **「ランダム」の語も記号も無い**）。★ **画面に並べた順をそのまま繋げば、教科書の図になる**（＝ 既存の「x の昇順で繋ぐ」がそのまま答え） |
| **P-g** 入口 | スチレン・1,3-ブタジエン・アクリロニトリルとも**呼べる**。★ `findCoPolymerHints` の表に1〜2行 |
| **P-h** 生成物 | SBR・NBR とも**未登録** |
| ⚠ 教科書の表記 | **添字 x・y と入れ子の括弧はアプリに無い**（R 止めの鎖しか描けない）。★ ただし教科書が**ブロック的に並べる**図を採っているので、**実物の鎖を A×x ＋ B×y として描くほうが、むしろ教科書の図に近い** |

**② 架橋型（p.260 式(11)・スチレン ＋ p-ジビニルベンゼン）**

| 要るもの | いまとの差 |
|---|---|
| **P-a** ★ **p-フェニレン（環1つ）の橋** | ⚠ 既存の橋は**1原子**。**環1つ**を架けるので橋の「群」対応が要る |
| **P-d** 鎖を対象にする `detect` | ★ `vulcanizablePairs` の `inPolymer` がそのまま使える形 |
| **P-e・P-b・P-c・P-f** | ⚠ **1つも要らない** |
| **P-h** | ⚠ **ジビニルベンゼンが未登録**（実測。名称ライブラリ1000件に無い）。生成物も未登録 |

★★ **②は「2本の鎖を橋でつなぐ」＝ `vulcanization` の絵とまったく同じ形。**
⚠ **①と②では要る仕掛けがまったく違う。**「共重合を入れる」は**1つの決めではない**。

#### (d) ビニロン（PVA のアセタール化・入試34件）—— ★ **4本でいちばん軽い**

| 要るもの | いまとの差 |
|---|---|
| **P-a** ★ **O-CH₂-O の橋（3原子）** | ⚠ 既存の橋は1原子。★ ただし **`dehydration_anhydride` が「同じ分子の2つの基を結んで環にする」形をすでに持っている**（`anhydrideDehydrationCandidates` ＋ `anhydrideBridgeSpot`）。⚠ **違いは架橋原子が1個か3個か**だけ |
| **P-d** ★ **R 止めの鎖の中の -OH を見る `detect`** | `vulcanizablePairs` の `inPolymer`（2070〜2085行）と**同じ絞り方** |
| **P-b・P-c・P-e・P-f** | ⚠ **1つも要らない** |
| **P-g** 入口 | ★ **ポリビニルアルコールが登録済み（3単位＋R 2本・11原子）＝ そのまま呼べる**。⚠ 相手の HCHO も呼べる |
| **P-h** 生成物 | ビニロンが**未登録** |
| 何ユニットで止めるか | ★★ **教科書が3ユニットで描いている**（p.254 式(5)(3)）。**アプリの3単位規約と一致** |
| 2/3 アセタール化 | ★ **3ユニット中2つ ＝ 隣り合う組が2つあるうち1つだけ架ける**。⚠ 教科書に割合の但し書きは無く、**2/3 を暗黙に固定**している（章末 p.268 問5(5) が同じ構造で質量計算をさせる） |

⚠ **前段（酢酸ビニル → ポリ酢酸ビニル → けん化 → PVA）は測れなかった**（§21-1 (j)）が、
★ **PVA を直接呼べるので、前段が通らなくてもビニロンは出せる。**

#### (e) ★ 共通して要るものと、その1本だけに要るものの仕分け

| 仕掛け | 付加縮合 | 開環 | 共重合① | 共重合② | ビニロン | **新規で使う本数** | 既存で使っている本数 |
|---|---|---|---|---|---|---|---|
| **P-a 橋** | ○ | — | — | ○ | ○ | **3** | 2（加硫・分子内脱水） |
| **P-b 分岐** | ○ | — | — | — | — | **1** | 0 |
| **P-c 環をほどく** | — | ○ | — | — | — | **1** | 0 |
| **P-d 鎖を対象** | — | — | — | ○ | ○ | **2** | 1（加硫） |
| **P-e 2種を1鎖に** | — | — | ○ | — | — | **1** | 0 |
| **P-f 多置換 o/p** | ○ | — | — | — | — | **1** | 0 |
| **P-g 入口** | ○ | ○ | ○ | ○ | ○ | **5** | 2 |
| **P-h 登録エントリ** | ○ | ○ | ○ | ○ | ○ | **5** | 4 |

★★ **仕分けの結論**:

- **2本以上で使う（＝共通）のは P-a・P-d・P-g・P-h の4つ。**
  ⚠ **そしてこの4つは、どれも新しい概念ではない** —— P-a と P-d は**既存の仕掛けの一般化**、
  P-g と P-h は**表・データへの追記**である
- **1本しか使わないのは P-b・P-c・P-e・P-f の4つ。**
  ★★ **そしてこの4つが、本当に新しく書くもの**（分岐・環をほどく・2種を1鎖に・多置換 o/p）。
  ⚠⚠ **互いに1文字も共有できない。**

⚠⚠ **§10.11-b と §10.12-C の見立て「付加縮合（分岐）・共重合・開環（環をほどく）は、
どれも『1本の鎖しか持っていない』という同じ1点に当たっている」は、
測ったら成り立たなかった。**★ **当たっているのは3つの別々の点である**:

| | 本当に当たっている点 |
|---|---|
| 付加縮合 | **鎖の端を1つしか持てない**（`linkFrom` がスカラ） |
| 開環重合 | **環の座標のまま繋ごうとする**（`planAttachment` が 25.5px を要求して 17.5px しか出ない） |
| 共重合① | **単量体を正準コードでそろえる門番**（`sameAs`／`groups`） |
| ビニロン | ⚠ **どれにも当たっていない**（既存の橋と既存の鎖判定の組み合わせで届く） |

### 21-4. ★★ 結論を数で出す ——「まとめて見直す」と「1本ずつ足す」

⚠ **数える単位は「新しく書く関数の数」「触る既存の関数の数」「新しいルールの数」
「新しい登録エントリの数」の4つ。**★ **行数の当てずっぽうは書かない。**

#### (a) 1本ずつ足す場合

| 反応 | 新しく書く関数 | 触る既存の関数 | 新ルール | 新登録エントリ |
|---|---|---|---|---|
| **ビニロン** | 2（`acetalizableDiols`・`acetalBridgeSpot`） | 0 | 1 | 1（ビニロン） |
| **開環重合** | 2（`lactamUnits`・`unrollRingToChain`） | 0 | 1 | 1（ナイロン6）＋ ε-カプロラクタムの図の描き直し（データ1件） |
| **共重合②架橋型** | 2（`crosslinkableChains`・環1つを架ける橋置き） | 0 | 1 | 2（ジビニルベンゼン・架橋ポリスチレン） |
| **共重合①ブロック型** | 1（`coPolymerUnits`） | 1（`addition_polymerization` と `diene_polymerization` の開き方を1つに束ねる） | 1 | 2（SBR・NBR） |
| **付加縮合** | 3（`resinBridgeSites`・`opSitesOnMultiSubstituted`・分岐対応の連結ループ） | 1（`apply` の骨格 ＝ `linkFrom` をスカラから集合へ） | 1〜2 | 1〜3（ノボラック・フェノール樹脂） |
| **合計** | **10** | **2** | **5〜6** | **7〜9** |

⚠ **`findCoPolymerHints` / `findSelfPartnerHints` は「表に行を足す」だけ**なので、
上の「触る既存の関数」には数えていない（`COPOLYMER_PAIRS` と `SELF_PARTNER_RULES` は**定数の配列**）。

#### (b) まとめて見直す場合（共通化する）

| 括るもの | 効果 | ⚠ 代償 |
|---|---|---|
| **P-a を `planBridge(mol, aId, bId, elements[])` 1本に** | 新規関数 **10 → 8**（ビニロン・共重合②・付加縮合が共有） | ⚠ 既存の `vulcanization`（S）と `dehydration_anhydride`（O）を載せ替えると、**いま緑のテスト PM13・PM14・RC6 を危険に晒す**。載せ替えないなら**置き場の決め方が3通り**（格子丸め・重心の反対向き・新しい共通版）残る |
| **P-d を `polymerChains(mol)` 1本に** | 新規関数 **8 → 7**（ビニロン・共重合②が共有） | ⚠ `vulcanizablePairs` の中の同じ処理（2070〜2085行）を抜き出す ＝ **既存関数の改修が1つ増える**。★ ただしこちらは**素の抜き出し**なので壊れにくい |
| P-g・P-h | ★ **括る余地が無い**（元から表とデータへの追記） | —— |

**差し引き**:

| | 1本ずつ | まとめて |
|---|---|---|
| 新しく書く関数 | **10** | **7**（−3） |
| 触る既存の関数 | **2** | **4**（＋2。⚠ **うち2つは緑のテストを持つ**） |
| 新ルール | 5〜6 | 5〜6（変わらない） |
| 新登録エントリ | 7〜9 | 7〜9（変わらない） |

★★ **「まとめて直すほうが安い」という結論は、数では出なかった。**

- 削れるのは **P-a と P-d だけ**（−3本）。⚠ **代わりに「いま通っているものを触る」が2つ増える**
- ⚠⚠ **本当に重い P-b（分岐）・P-c（環をほどく）・P-e（2種を1鎖に）・P-f（多置換 o/p）は、
  まとめようがない** —— **使うのが1本ずつだから**（§21-3 (e) の表）
- ★ **共通化する意味が薄いことを、使用本数で言える**: P-b・P-c・P-e・P-f は**どれも1本**

#### (c) ★ そのうえで「まとめて決めるべきもの」は在る（⚠ コードの共通化とは別）

⚠ **「まとめて見直す」を「コードを括る」と読むと、上のとおり得が無い。
だが「規約をそろえる」と読むと、話が変わる。**★ **先に決めておかないと4本がばらけるのは次の3つ**:

1. **何単位で止めるか** … いまは「3単位＋両端 R」（登録4件が全部そう）。
   ⚠ 教科書は**付加縮合が環6個**・**ビニロンが3ユニット**・**開環と共重合は1単位＋[ ]ₙ** と**ばらばら**
2. **端の印** … いまは R。⚠ **付加縮合では R が3〜4本になる**（前例ゼロ）。
   教科書は「…」「素の —」「`n`」を使い分ける
3. **どこまでを1つの反応にするか** … 教科書は**フェノール樹脂だけ素反応を2段に割っている**（式(8)(9)）。
   ⚠ ここをそろえないと、付加縮合だけ操作の粒度が違う

★ **この3つは D-P1・D-P2・D-P7 として §21-x に置く（ユーザー判断）。**

#### (d) ⚠ 「やらない」判断は取らない（★ §10.11-b と矛盾しないことを確かめた）

**4本とも描ける。**§10.11-b が取り下げた「付加縮合は描けない」を**蒸し返さない**:

- **付加縮合** … 教科書 p.258 の図は**平面**で、環6個 ＋ -CH₂- 7本の**骨格線の図**。
  ★ **このアプリの作図（骨格＋原子）でそのまま描ける。**⚠ 重いのは「描けない」ではなく
  **分岐（P-b）の前例がゼロ**だから
- **開環重合** … **数で確かめた**（§21-3 (b)）。P-c を書けば通る
- **共重合** … ⚠ **教科書の表記（入れ子の括弧と添字 x・y）はアプリに無い**。
  ★ しかし教科書が**ブロック的に「A が x 個、B が y 個」と並べる**図を採っているので、
  **実物の鎖として A を x 個・B を y 個描くほうが、むしろ教科書の図に近い**
  ＝ **「描けない」ではなく「表記が違う」**
- **ビニロン** … 既存の橋と既存の鎖判定の組み合わせで届く

⚠ **唯一「保留」に置くもの**: **尿素樹脂・メラミン樹脂**（付加縮合の残り2種）。
理由は**描けないからではなく**、① **メラミンが名称ライブラリ1000件に無い**（実測）、
② **教科書 p.259 表3 の図が示性式のみで骨格線が無い** ＝ **このアプリの作図と流儀が違う**から。
★ **フェノール樹脂を骨格で描けば教科書 p.258 と同じ図になる**ので、**付加縮合はフェノール樹脂で足りる**。

#### (e) ★★ 推奨する順番

⚠ **入試の頻度とは逆順になる。理由をはっきり書く。**

| 順 | 反応 | 入試 | 新規関数 | 理由 |
|---|---|---|---|---|
| **1** | **ビニロン** | 34件 | **2** | ★ **いちばん軽い**。単量体（PVA）が**3単位＋R で登録済み** ＝ そのまま呼べる／橋は `dehydration_anhydride` の形をほぼ写せる／⚠ **教科書が3ユニットで描いていて、アプリの規約と最初から一致している唯一の1本**（§21-2 (e)）。★ **P-a（橋）と P-d（鎖を対象）の1本目**をここで作る |
| **2** | **開環重合** | 44件 | **2** | 新しいのは **P-c（環をほどく）1つだけ**。分岐も橋も門番も要らない／`detect` は §10.12-C が**実測で動くことを確かめてある**／★ 入口は `SELF_PARTNER_RULES` に**1行足すだけ**（自分をもう2つ ＝ 既存の形そのもの） |
| **3** | **共重合・架橋型（②）** | 43件の一部 | **2** | ★ **P-a の2本目 ＝ ここで橋を括る**（三度目を待たず、2度目で括る。1本目のビニロンと形が同じことを確かめてから）／`vulcanization` の絵と同じなので**説明も既存に乗る**／⚠ **ジビニルベンゼンの登録が要る** |
| **4** | **付加縮合（フェノール樹脂）** | **64件** | **3** | ⚠ **最頻だが最後**。★ **唯一「分岐（P-b）」と「多置換 o/p（P-f）」を要る**＝ **前例ゼロの仕掛けが2つ**。1〜3 で **橋・鎖判定・入口・登録エントリの4つの型を3回ぶん練ってから**向かうほうが安い |
| **5** | **共重合・ブロック型（①・SBR/NBR）** | 43件の残り | **1** | ⚠ **関数は1本だが中身が重い** —— **1,2-で開く単量体と 1,4-で開く単量体を1つのループに混ぜる**（P-e）。★ **D-P4 で「架橋型だけでよい」と決まれば、これは不要になる** |

★ **頻度の逆順になる理由（1行）**: **64件を先にやると、前例ゼロの「分岐」を、
橋も入口も登録エントリの型も未整備のまま入れることになる。**
⚠ **これは「付加縮合をやらない」ではない**（§10.11-b の取り下げと矛盾しない）。**順番の話だけ**である。

★ **1〜2 だけで 78件（44＋34）に届く。**新しく書く関数は **4本**、触る既存の関数は **0本**。
⚠ **1〜2 の範囲では、既に緑のテストを1つも触らない。**

### 21-5. ★ こちらで決めたこと（ユーザーに聞かない・⚠ 根拠つき）

⚠ **勝手に決めてよいものと、聞くべきものを分ける。**以下は**根拠が実測か教科書にあるもの**。

| # | 決めたこと | 根拠 |
|---|---|---|
| **1** | **まとめてコードを括るのはやらない。1本ずつ足す** | §21-4 (b) の数。共通化で減るのは3本、増える改修が2つ（うち2つは緑のテスト持ち） |
| **2** | **P-a（橋）と P-d（鎖を対象）は、2本目を足すときに括る**（最初から共通化しない） | 1本目（ビニロン）で形が定まる前に括ると、2本目で作り直しになる |
| **3** | ★★ **共重合の「並ぶ順」は、画面に並べた順で決める**（§11.6 の「並ぶ順が決まらない」は取り下げ） | ⚠ **教科書はブロック的に「A が x 個、B が y 個」と並べ、「ランダム」の語も記号も使っていない**（p.264 表4）。★ 既存の「x の昇順で繋ぐ」がそのまま教科書の図になる |
| **4** | **尿素樹脂・メラミン樹脂は保留**（付加縮合はフェノール樹脂で足りる） | メラミンが名称ライブラリ1000件に無い（実測）／教科書の図が示性式のみでアプリの作図と流儀が違う（p.259 表3） |
| **5** | **開環重合の `detect` は環内の N-C(=O) を見る形にする** | §10.12-C が実測で確かめた `lactamUnits` の判定（8種の否定対照つき）をそのまま使う |
| **6** | **付加縮合を「描けない」に戻さない** | §10.11-b（ユーザー判断）。教科書 p.258 の図は平面の骨格線図 |

### 21-x. ⚠ 判断待ち（★ ユーザーに聞くこと）

⚠ **何を選ぶと何が変わるかを1〜2行で。**

- **D-P1｜付加縮合を何量体で描くか。**
  教科書は**ベンゼン環6個（3×2）＋ -CH₂- 7本**（p.258）。アプリの規約は **3単位**、
  分子の選択上限 `MAX_REACTION_SELECTION` は **4**。
  → **6個にすると教科書の図そのものになるが、入口（呼び出す個数と選択の上限）を作り直す**。
  **3個にすると既存の入口のまま**だが、**環3個では「各環から3本」の網目に見えない**。

- **D-P2｜付加縮合を素反応2段で見せるか、一発で網目にするか。**
  教科書は**フェノール樹脂だけ2段に割っている**（式(8) 付加 → o-ヒドロキシベンジルアルコール／
  式(9) 縮合 → 二量体 ＋ H₂O）。
  → **2段にすると中間体の登録エントリが1件増え、ルールが2本になる**が、
  「付加」と「縮合」がなぜ両方の名前を持つのかが画面で見える。**一発なら1本で済む**。

- **D-P3｜ノボラック／レゾールを出すか（酸触媒と塩基触媒を分けるか）。**
  教科書は両方を `[ ]ₙ` で併記している（酸 n=1〜10／塩基 n=1〜2）。
  → **分けると瓶が2本増える**（酸・塩基）。**分けなければ「付加縮合」1本**で、中間体は出ない。

- **D-P4｜共重合はどちらの描き方を採るか。**
  ① **ブロック型**（SBR/NBR・p.264 表4）／② **架橋型**（スチレン＋ジビニルベンゼン・p.260 式(11)）。
  → **②だけなら既存の加硫と同じ「橋」で軽い（新規関数2）。①は 1,2-で開く単量体と
  1,4-で開く単量体を1つのループに混ぜる**のが本体で、**入試で名前が出るのは SBR/NBR＝①**。

- **D-P5｜ビニロンのアセタール化を「1押しで 2/3 まで」にするか、「押すたびに1組」にするか。**
  教科書は **3ユニット中2つ（2/3）を暗黙に固定**していて、割合の但し書きは無い（p.254・p.268 問5(5)）。
  → **1押しなら caption で「-OH の 2/3 がアセタール化」をそのまま言える。
  押すたびなら加硫と同じ形**（「もう一度押すと別の場所も」）だが、**どこで止めるかがユーザー任せ**になる。

- **D-P6｜ε-カプロラクタムの登録図を、教科書の「横長の潰した環」に描き直すか。**
  ★ **実測**: 描き直すと **2個目までは置ける（間隔 42.0px）が、3個目で落ちる**（§21-3 (b)）。
  → **描き直しても P-c（環をほどく）は要る**。⚠ ただし**教科書 p.251 の図と揃う**という別の値打ちがあり、
  **収録済み動画の構図が動く**という代償がある。

- **D-P7｜端の印を R のままにするか。**
  いまは全部 R 2本。⚠ **付加縮合では R が3〜4本になる**（前例ゼロ）。
  教科書は場所によって「…」（フェノール樹脂）・素の「—」（尿素樹脂・架橋ポリスチレン）・
  **短い破線＋実線**（ビニロン）・`[ ]ₙ`（それ以外）を使い分けている。
  → **R のままなら一貫するが、教科書のどの図とも表記が違う。
  網目のときだけ別の印にすると、アプリの中で表記が2通りになる。**
  ⚠ **これはアプリ全体の表記の話なので、こちらでは決めない。**

- **D-P8｜生成物の登録エントリをどこまで足すか。**
  未登録: **ビニロン・ナイロン6・フェノール樹脂（ノボラック）・SBR・NBR・架橋ポリスチレン・
  ジビニルベンゼン（単量体）**。⚠ 足さないと**実行はできても「（未登録）」と出る**。
  → ⚠ **`compounds.json` は1行1件・末尾追記のみ**（CLAUDE.md）。**R が3本以上のエントリは前例ゼロ**なので、
  フェノール樹脂を足すときに**その形が正準コード・同型判定でどう見えるかを先に確かめる**必要がある。

- **D-P9｜§21-4 (e) の順番（ビニロン → 開環 → 共重合架橋 → 付加縮合 → 共重合ブロック）でよいか。**
  ⚠ **入試の頻度（64 / 44 / 43 / 34）とは逆順**になる。
  → **この順なら 1〜2 の2本（78件ぶん）で「新しく書く関数4本・触る既存関数0本」で済む。
  頻度順（付加縮合から）にすると、前例ゼロの分岐を最初に入れることになる。**

---

## 22. 加硫の橋が「鎖の途中」に見えず「R から生えて」見えた（v1484・2026-08-31 動画レーン V130 の収録映像）

★ **発注は要望2件**（動画レーン）。⚠ **どちらも実測したら、言われていた原因とは違うところにあった。**

| 要望 | 言われたこと | ★ 実測でわかったこと |
|---|---|---|
| **1-A** | 2本の鎖がほぼ一直線に並ぶので、S が鎖の途中に埋まって見える | **本当**（数は下の 22-1） |
| **1-B** | S を R から生やしている ＝ 鎖の末端どうしを結んだ絵になっている | ⚠ **S が R に結合したことは一度も無い**（5通り・橋10本で **0本**）。起きていたのは**橋の線が R の上を通る**こと（実測 **0.6〜3.3px**） |

### 22-1. 測ったこと（★ 台本 `reaction-rubber-vulcanization` と同じ手順）

イソプレン×2 → 1,4-付加重合 → イソプレン×2 → 1,4-付加重合 → 加硫 ×2:

| 見たもの | 値 |
|---|---|
| 鎖1の x 帯 / 鎖2の x 帯 | `[232..568]` / `[610..946]` ＝ **重ならない（-42px）** |
| 鎖1の y 帯 / 鎖2の y 帯 | `[263.6..373]` / `[305.6..415]` ＝ **67.4px 重なる** |
| 両鎖まとめた y の標準偏差 | **39.7px** ＝ 結合1本ぶん（**「ほぼ一直線」は本当**） |
| 重原子どうしの完全重複（<1px） | **0件**（重なってはいない。並びが一直線なだけ） |
| 橋の足場 C=C どうしの距離 | **357.0px** ＝ 刻み 42px の **8.5本ぶん** |
| S-C の結合線の長さ | **167 / 190px** ＝ 刻みの **4.5倍** |
| その線が主鎖の炭素の上を通る距離 | **0.0 / 0.1 / 0.2 / 0.3 / 1.2 / 1.3px** ＝ **完全に重なる** |
| その線と R の距離 | **2.1px・2.7px** |
| S が R に直接結合した数 | **0本** |

⚠ **単量体・単位数を変えても同じ**（イソプレン 2+2・3+3・2+3／1,3-ブタジエン 2+2／クロロプレン 2+2）:
**5通りすべてで橋の線が R をかすめ**（0.6〜3.3px が4本・20〜27px が4本）、
**橋10本中 S が R に付いたのは 0本**。

★ **つまり 1-A と 1-B は同じ1つの原因から出ている** ―― 2本の鎖が左右一直線に並ぶので、
そのあいだに架ける橋は**必ず長い水平線**になり、その線が両方の鎖と R の上を通る。
**箇所選び（`vulcanizablePairs`）は最初から正しく「鎖の途中の C=C」だけを選んでいた。**

### 22-2. 教科書がどう描いているか（数研『R5化学Vol.2』6編。⚠ PDFページ ＝ 書籍ページ − 208）

| 出典 | 描き方 |
|---|---|
| **p.260 式(11)「架橋構造のポリスチレン」** | ★ **構造式で「2本＋橋」を描いている唯一の図**。主鎖を **上下2段の横並び**にし、架橋（p-ジビニルベンゼン由来の環）を**その間に縦に**渡す。⚠ **端は `R` ではなく素の「—」**で、**橋は必ず鎖の途中の CH から**出ている |
| **p.263 図22「硫黄による架橋構造」** | 模式図。波線の鎖を**層に重ね**、`-S-S-` を**隣り合う鎖のあいだに短く**渡す。鎖の端は図の外へ出ていて描かれない |
| p.263 図20 | ゴム弾性の模式図（球の連なり）。架橋は描かない |

⚠ **教科書の橋は `-S-S-`（硫黄2個）**。アプリは **S 1個**。★ これは今回の要望とは別の話なので触っていない（下の「残っていること」）。

### 22-3. 直した形 ——**`planAttachment` には触らない**

`stackChainsForBridge`（`reactor.js`・`vulcanizablePairs` の直後）を1つ足し、
`vulcanization.apply` の頭で呼ぶ。**1本目の橋のときだけ**（＝ ca と cb がまだ別の連結成分のときだけ）
相手の鎖を**剛体平行移動**で真下（だめなら真上）へ寄せる。

- 橋の中点 M を「ca の真下（真上）にある**格子点**」に取り、cb を **M について ca と点対称**な位置へ運ぶ。
  こうすると `vulcanizablePairs` が硫黄を置く `round(中点/G)*G` が **M そのもの**になり、**C-S-C が一直線**になる
- 通す条件は3つ: ① 原子どうしが `G × 0.65` 以上あく ② 硫黄の席 M が空いている
  ③ 結合線が相手の原子を貫通しない（`SHOVE_LINE_CLEARANCE`）。**1つでも欠けたら座標を変えない**
- 段は「1歩下 → 1歩上 → 2歩下 → 2歩上」の順

⚠ **`planAttachment` を選ばなかった理由**（発注の「既定を変えるなら影響を全部測れ」への答え）:
- あれは**47本中46本の反応が使う共通の道具**で、**「結合1本ぶんの距離に置く」**ためのもの。
  加硫は**硫黄を1個挟む＝2本ぶん**離すので、`prefer` を渡しても目的の距離にならない
- ★ したがって**既定の挙動は1行も変えていない**。他の反応への影響はゼロ（測る対象が無い）

⚠ **回転・伸縮・鏡映はしない**（剛体平行移動だけ）＝ 幾何が変わらないので
「整形で幾何が変わるなら座標を戻す」の約束を満たす。
⚠ **2本目からは動かさない**（動かすと1本目の橋が伸びる）。VL2 ③ が見張る。

### 22-4. 直した後の実測

| 見たもの | 直す前 | 直した後 |
|---|---|---|
| 橋の線が R をかすめる | **5通り / 8本** | **0通り / 0本** |
| S が R に直接結合 | 0本 | 0本（変わらず） |
| S-C の結合線（イソプレン 2+2） | 167〜190px（刻みの **4.5倍**） | **81〜91px（刻みの 2.0〜2.2倍）** |
| 鎖の並び（硫黄を外して見る） | y が 67px 重なり x が重ならない ＝ **左右一直線** | **y は重ならず x が重なる ＝ 上下** |
| C-S-C の角 | 水平に折れる | **180°付近** |
| 原子どうしの最短間隔 | —— | **`G × 0.65` 以上**（重ねて達成していない） |

### 22-5. 回帰テスト

| 番号 | 見るもの |
|---|---|
| **VL1** | ①**症状の前提**（加硫前は左右一直線）② 1本目で **y の帯が重ならず x の帯が重なる**＝上下 ③ ⚠ **重なっていない**（最短間隔）④ ★ **橋が鎖の途中の C=C に架かる**を名指しで（硫黄の隣が炭素2つ・どちらも R の隣でない・detect の箇所に含まれる・別々の鎖）⑤ 橋が刻みの 2.5倍以内・C-S-C が 180°付近 |
| **VL2（否定対照）** | ① 単量体3種で **S が R に付かず線も R から離れる**＋⚠ **同じループで「硫黄が1個入り足場が C=C だった」**（「R に架からない」だけだと**橋を架けない実装でも通る**）② **橋の本数が減っていない**（台本の形で2本・硫黄を外すと鎖2本・価標が無事）③ ⚠ **2本目では既存の原子が1つも動かない** ④ 鎖1本では候補0件・単量体は相手にしない |

★ **テスト自身の否定対照**: `stackChainsForBridge` の呼び出しを1行コメントアウトすると
**VL1・VL2 とも赤**になり、症状そのものの文言が出る
（VL1「上下に分かれていない（y の帯が 67px 重なっている）」／
VL2「イソプレン: 橋の線が R(610,342) から 2.7px ＝ R から生えているように見える」）。

### 22-6. ⚠ 残っていること（★ 直していない。統合セッションへ）

1. **1,3-ブタジエン・クロロプレンでは橋が 2本 → 1本に減った。**
   ★ **減ったのは病的なほうの橋**である ―― 直す前の2本目は **254〜277px（刻みの 6.5倍）**で、
   **R から 0.6px** を通っていた。寄せたあとは、残る組の中点が**1本目の硫黄の席と重なる**ので
   候補から落ちる（実測: 唯一残る組は **374px** の対角線）。
   ⚠ **台本（V130）はイソプレンで2回押す形なので、そこは 2本のまま**。
2. **イソプレン 3+3 の2本目の橋は 268px（刻みの 6.4倍）のまま。**
   1本目で2本の鎖が1分子になるので、2本目では寄せられない。
   ★ 直すなら「**すでに繋がっている鎖どうしでは長すぎる橋を出さない**」という上限が要るが、
   **上限値がイソプレン 2+2 の 172px と 3+3 の 268px のあいだにしか置けず、1例に合わせた数字になる**ので入れていない。
3. **教科書の橋は `-S-S-`（硫黄2個）**。アプリは S 1個。★ 化学としてはどちらもあるが、
   **図を教科書に合わせるなら S を2個にする**という別の判断が要る。
4. **端の印が `R`** のまま。教科書 p.260 の架橋ポリスチレンは**素の「—」**で、
   **端がある鎖には見えない**ように描いてある。⚠ これはアプリ全体の表記の話（§21-4 D-P7 と同じ）。
