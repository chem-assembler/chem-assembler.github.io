# 入口（導線）の全体見直し 設計書

**この文書は設計だけで、コードは1行も変えていない。** 実装はここで合意してから別レーンで行う。

## 0. なぜこの文書か

ユーザーの継続的な指摘「**機能への入口がアプリ全体でわかりづらい。まとめて見直したい**」の受け皿。
Gemini レビュー（`docs/review_v331.md`）の次の3項目を、個別に動かさずここへ合流させてある
（`DEVELOPMENT.md`「第2波の割り当て」で保留と決めた3件）:

| レビュー項目 | 要旨 | この文書での扱い |
|---|---|---|
| **18** | 🧩パズルを📚学習に統合し、2大モード（🧪自由／📚学習）へ再編 | §5 案B。**推さない**。理由は §5-4 |
| **19** | 「名称から分子を呼び出す」を「⚗ この分子の反応」から「🔍 いま描いている分子」へ移設 | §5 案A の A-4。**採用**（§6 Step 1） |
| **24-3** | 「🔤 記号でパズル」を「🔃 フィッシャー投影の操作練習」へ統合 | §2-6 で**統合先が違う**ことを実証。代案は A-7（§6 Step 4） |

関連する既存の設計書と、この文書の関係:

- **`DESIGN_ui_modes.md`（P10）** … 3モード構成を決めた文書。**破棄しない。**
  この文書は「3モードの中の入口の並べ方」を扱い、モードの数を変える案（案B）は退ける。
  ただし §2-7 のとおり、P10 で決めたのに**実装されていない項目が2つある**ので、そこを回収する
- **`DESIGN_mobile.md`（P11）** … 縦横のレイアウトを決めた文書。§3 の「モードタブをヘッダーに置く」が
  **未実装**（§2-7）。この文書の A-2 がその続き
- **`DESIGN_recording_mode.md`** … SNS 収録。§7 の不変条件はここと突き合わせて書いた

基準の版は **v446**。実測はすべて v446 のコードをローカルサーバー（`python -m http.server`）で
開いて計測した値で、レビュー本文の記述はいちど疑って裏を取っている
（レビューの項目5・24-1・7 は CSS を読んだだけの推論で事実誤認だった前例があるため）。

---

## 1. 結論の先出し

1. **いま最も強い痛点は、タブ構成ではなく左パレット（道具箱）にある。**
   375px の縦画面で、**炭素ボタン C は画面外 265px 先**にいる。作図の第一歩が初期状態で見えない。
   1280×800 の PC でも、**ベンゼン環を含む環・官能基モジュール10個すべてが折り返しの下**にある（§2-4）
2. **レビュー項目18（2タブ化）は、この痛点を1つも解決しない。** 右パネルの中の話だから。
   しかも 31本の収録済み動画が現行の DOM とモバイル縦レイアウトに結びついている（§7）ので、
   骨格の作り替えは費用対効果がいちばん悪い
3. よって推しは **案A「小さく直す」**（§5-1）。骨格（3モード・3カラム・右パネル）は触らず、
   **並べ替え・畳み込み・ラベル・深いリンク**の4手で直す
4. 台本と回帰テストを守る不変条件はただ2つ —— **`id` を変えない／消さない**と、
   **`setMode('puzzle'|'learn'|'free')` の3値を変えない**（§7）。この2つを守るかぎり、
   要素を DOM のどこへ動かしても 31本の台本と 170件のテストは通る

---

## 2. いまある入口の全数（実測）

推測を混ぜず、`assembler/index.html`（1013行）と `assembler/game.js` を読み、
実機（Chromium・375×812 / 812×375 / 1280×800）で位置を測って数え上げた。

### 2-1. ヘッダー・キャンバス上ツールバー

| # | 入口 | どこ | 何が起きるか | 375px 縦 | 375px 横 |
|---|---|---|---|---|---|
| 1 | ❓ ヘルプ | ヘッダー右 | `#tutorial-modal`（FAQ・デモ再生） | 見える | 見える（右上に浮く） |
| 2 | X: 0, Y: 0 | 上ツールバー | 座標表示（入口ではない） | 見える | **非表示** |
| 3 | ↩ 戻す | 上ツールバー | Undo | 見える | 見える |
| 4 | ↪ やり直し | 上ツールバー | Redo | 見える | 見える |
| 5 | ⤓ JSON | 上ツールバー | 作図を JSON でコピー（制作用） | **非表示** | **非表示** |
| 6 | 🔤 官能基をまとめる | 上ツールバー | -COOH 等をカード表記に畳む | 見える | 見える |
| 7 | 🔍 全体表示 | 上ツールバー | ビューを戻す | 見える | 見える |
| 8 | 全消去 | 上ツールバー | キャンバスを空に | 見える | 見える |
| 9 | ☰ お題・設定 | 浮動 / リボン最下段 | 右パネル（シート）を開く | 見える | 見える |

- 縦では上ツールバーが**2段に折り返して 102px** を使う。横では右端の縦リボン（幅114px・高さ313px）になり、
  **この高さでちょうど収まりきる**（あふれない）
- **レビュー項目7（横画面で ❓ とキャンバスヘッダーが衝突する）は再現しない。** ヘッダーは
  `position:absolute; top:6px` に逃がしてあり、リボンは `top:40px` から始まる。
  レーンH でも「再現せず・事実誤認」と記録済みで、実測でも重なりはなかった

### 2-2. 左パネル（TOOLS）— 25ボタン

| グループ | ボタン | 375px 縦 | 375px 横 | 1280×800 |
|---|---|---|---|---|
| 操作モード | Select / Move | **非表示**（既定なので省略） | **非表示** | 見える |
| 操作モード | Draw Bond（結合） | **見える**（x=17） | 見える | 見える |
| 操作モード | Eraser（消しゴム） | **見える**（x=132） | 見える | 見える |
| 操作モード | ✳ 不斉炭素原子マーク | **右端で切れる**（x=245–379／可視域 361） | 見える | 見える |
| 操作モード | ⇄ シス/トランス整形 | **画面外**（x=384） | 見える | 見える |
| 操作モード | ⬍ α/β 面マーク | **画面外**（x=516） | 見える | 見える |
| 結合タイプ | 単・二重・三重 | **非表示**（結合線タップで代替） | **非表示** | 見える |
| 原子の配置 | **C 炭素** | **画面外**（x=626） | 見える | 見える |
| 原子の配置 | O 酸素 | **画面外**（x=713） | 見える | 見える |
| 原子の配置 | N・Cl・S・Br | **画面外**（x=800〜1143） | **折り返しの下** | 見える |
| 環・官能基 | 五員環・六員環・**ベンゼン環**・任意員環・ハース環 | **画面外**（x=1157〜1631） | **折り返しの下** | **折り返しの下** |
| 環・官能基 | -OH・-COOH・-NH₂・-NO₂・-SO₃H | **画面外**（x=1636〜2176） | **折り返しの下** | **折り返しの下** |

**実測値（v446）**

| 画面 | パレットの形 | 中身 | 見えている | 倍率 |
|---|---|---|---|---|
| 375×812（縦） | 下の横スクロール帯 | **2179px** | 361px | 6.0倍 |
| 812×375（横） | 左の縦列 | **1096px** | 361px | 3.0倍 |
| 1280×800（PC） | 左の縦列 | **1225px** | 718px | 1.7倍 |

グループごとの幅（縦画面）: 操作モード **595px** ／ 原子の配置 **517px** ／ 環・官能基 **1020px**。
**可視幅 361px はどのグループ1つぶんにも足りない。**

> ⚠ **レビュー項目2 の見立ては小さすぎた。** 項目2 は「✳・⇄・⬍ の3つが右端に隠れる」と書いており、
> レーンA はそこに「まだ続きがある」ことを示すグラデーションのヒントを入れた（v380台。`style.css` に
> 「中身 2157px / 見えている 361px」の実測コメントあり）。
> だが実際に隠れているのは**その3つだけではない**。3つが 595px を占めるせいで、
> **その後ろにある原子6個と環・官能基10個の合計16ボタンが丸ごと画面外**になっている。
> ヒントの追加は正しい手当だが、**並び順を変えないかぎり「炭素を置く」に届かない**。

### 2-3. 右パネル（モード別）

`#right-panel` の中身。モバイルでは**シート／ドロワーで、開くまで存在ごと見えない**。

| # | 入口 | モード | 何が起きるか |
|---|---|---|---|
| 10–12 | 🧩 パズル / 📚 学習 / 🧪 自由（`.mode-tab`） | 共通 | `setMode()`。右パネルの `[data-modes]` を出し分け |
| 13 | シリーズを選択 | パズル | ステージ群の切替 |
| 14 | 問題を選択 | パズル | お題の切替 |
| 15 | 構造判定 (Submit) | パズル | 正誤判定 |
| 16 | お手本を見る | パズル | `#target-modal` |
| 17 | ☑ 不斉炭素原子（*）の指定も判定する | パズル | 判定オプション |
| 18 | ☑ 立体（D/L・α/β）を名前に反映する | パズル | 既定 OFF（v376） |
| 19 | 🎓 クイズに挑戦（アコーディオン） | 学習 | 下の8ボタンを開く |
| 20–27 | 同じ化合物？ / 命名 / 立体異性体 / 何種類？ / フィッシャー操作練習 / 立体タイムアタック / 記号でパズル / 同じ立体はどれ？ | 学習 | モーダル8枚（§2-5） |
| 28 | ✏️ 構造の書き出し練習（アコーディオン） | 学習 | 下の3カード |
| 29–31 | 異性体 / アルキル基 / 立体異性体 の書き出し練習 | 学習 | **カード内に id なしのボタン計15個**（§2-5 の注） |
| 32 | ⚗️ 反応機構ビューア（アコーディオン） | 学習 | 下の機構UIを開く |
| 33 | ☑ 反応機構モード | 学習 | 巻矢印アニメの有効化 |
| 34 | 反応の選択（select） | 学習 | 機構の切替 |
| 35–38 | 🔄 ⏮ ▶ ⏭ | 学習 | ステップ送り |
| 39 | 🎯 生成物を予測する | 学習 | 予測モードへ |
| 40 | 予測を判定 / 予測をやめて再生に戻る | 学習 | 予測モードの出入り |
| 41 | 🔍 いま描いている分子（カード） | **パズル・自由**（学習では非表示） | 名称・分子式のライブ表示 |
| 42 | 🎯 反応させる分子を選ぶ | 自由 | 分子のタップ選択（最大4つ。v470） |
| 43 | （動的）反応ボタン列 `#reaction-actions` | 自由 | 検出された反応の実行 |
| 44 | 📚 同じ分子式の異性体を調べる | 自由 | `#learn-modal` |
| 45 | 🧊 立体で見る（くさび図・3D） | 自由 | `#stereo-modal` |
| 46 | 名称から分子を呼び出す（`#summon-input`） | 自由 | ライブラリから召喚（**項目19 の対象**） |
| 47 | 📝 遊び方と操作方法（`.hint-box`） | **全モード** | 折りたためない長文ヘルプ |

**縦位置の実測（375×812・シートを開いた状態。可視高 664px）**

| モード | 右パネルの総スクロール高 | 折り返しの下に落ちるもの |
|---|---|---|
| 🧩 パズル | **2333px** | ☑判定オプション2つ（y=667 / y=733）・🔍いま描いている分子（y=829）・ヘルプ |
| 📚 学習 | 1702px（3アコーディオン全開で **3158px**） | ヘルプ（3つのアコーディオンは畳んだ状態なら全部見える） |
| 🧪 自由 | 1923px | ヘルプ（反応カードは y=261–528 で収まる） |

- **`.hint-box`（📝 遊び方と操作方法）だけで 1345px**（PC では 1611px）。
  **3モードすべてで右パネルの末尾に出る**ので、パズルの総高 2333px のうち 58%、
  学習の 1702px のうち 79% がこの1ブロック。畳めない
- パズルの**判定オプション2つは、スマホでは初期表示で見えない**。
  「立体を名前に反映する」を既定 OFF にした v376 の判断（初学者が迷わないように）は正しいが、
  **ONにしたい人が入口に辿り着けない**状態でもある

### 2-4. モーダル14枚

`class="modal-overlay"` は **14枚**。開き方の内訳:

| モーダル | 開く入口 | 入口の場所 |
|---|---|---|
| `#win-modal` | 構造判定に正解 | 自動 |
| `#target-modal` | お手本を見る | パズル |
| `#nring-modal` | ◯ 任意員環 | 左パレット |
| `#tutorial-modal` | ❓ ヘルプ | ヘッダー |
| `#learn-modal` | 📚 異性体を調べる | **自由のみ** |
| `#stereo-modal` | 🧊 立体で見る | **自由のみ** |
| `#quiz-modal` | 🎓 同じ化合物？ | 学習 |
| `#naming-modal` | 📝 命名クイズ | 学習 |
| `#stereo-quiz-modal` | 🪞 立体異性体クイズ | 学習 |
| `#count-quiz-modal` | 🔢 立体異性体は何種類？ | 学習 |
| `#fischer-practice-modal` | 🔃 フィッシャー投影の操作練習 | 学習 |
| `#time-attack-modal` | ⏱ 立体タイムアタック | 学習 |
| `#symbol-puzzle-modal` | 🔤 記号でパズル | 学習 |
| `#choice-quiz-modal` | 🎯 同じ立体はどれ？（4択） | 学習 |

**8枚が「学習タブ → 🎓 クイズに挑戦 → ボタン」の3手の先にある。**
`#stereo-modal` は4つの内部タブ（✏くさび図／🧊3Dで回す／⬡環を横から／🧊分子全体）を持ち、
実質4画面ぶん。**自由モードからしか開けない**ので、パズルで乳酸を組んだ生徒が
その場で立体を見ることはできない（いちど 🧪 自由へ移る必要がある）。

### 2-5. 選択肢の中に沈んでいる出題（＝入口を作らなかったもの）

v362 以降に足した機能は「入口の全体見直し待ち」として**わざと画面に出していない**。
その結果、いまは `<select>` の中に沈んでいる:

| 沈んでいる場所 | 中身 | 版 |
|---|---|---|
| `#pk-kind`（🎯 同じ立体はどれ？の中） | 記号 / 分子 / **D体・L体はどれ？** / **同じ？違う？（連続・タイム計測）** | v365・v370・v430 |
| `#sq-mode`（🪞 立体異性体クイズの中） | 標準 / 入門（くさび印つき） / 発展（回した図） / すべて | — |
| `#ta-mode`（⏱ タイムアタックの中） | 入門 / 本格 / **上級（中心を切り替えて解く）** | v361 |
| `#sp-mode`（🔤 記号でパズルの中） | ランダム / 同じ立体から / 鏡像から | v364 |
| `#quiz-series` `#quiz-strength` / `#naming-series` `#naming-strength` | 出題範囲・崩し方（弱/標準/強） | — |

**立体まわりの select だけで 15通り**。8ボタン＋15通り＝**23種類の練習が、8個の入口の裏にある**。

さらに**画面に一度も出ていない**もの:

| 機能 | 版 | いまの唯一の入口 |
|---|---|---|
| **クイズの出題を指定する**（答えを固定して出す） | v441 | URL `?quiz=` `?stereoQuiz=` ／ `setForced()` ／ 台本の `quizForce` |
| **R/S 記述子の判定**（`assignRSDescriptor`） | v440（レーンF） | **入口ゼロ**。`chemistry.js` の定義と `tests.js` からしか呼ばれていない（全ファイル grep 済み） |
| 立体パズルの4操作（十字模型 `CrossModel`） | v362 | ⏱ タイムアタック と 🔤 記号でパズル の中（モーダルを開かないと存在が見えない） |
| 書き出し練習の 15 個のお題ボタン | — | `#ip-body` `#ak-body` `#sp-body` に JS が生成。**id が付かない**ので台本からも指定できない |

### 2-6. ⚠ レビュー項目24-3 の統合先は間違っている

項目24-3 は「🔤 記号でパズルを **🔃 フィッシャー投影の操作練習** の Step 1 として組み込む」と書いている。
コードを読むとこれは成立しない:

- 操作面の共通部品 `CrossModel`（`quiz.js:1845`）を使っているのは
  **`ta`（⏱ 立体タイムアタック）と `sp`（🔤 記号でパズル）の2つだけ**（`quiz.js:1964` と `quiz.js:2393`）
- **`fp`（🔃 フィッシャー投影の操作練習）は CrossModel を使っていない。**
  操作が別系統（90°回す＋左右入れ替え／180°回す／軸を1本選んで残り3つを回す）で、
  記号パズルの4操作（⟲⟳回す・↔↕鏡）とボタンが1つも対応しない
- アプリ自身のヘルプ文がすでに正しい行き先を書いている ——
  記号パズルの説明（`index.html:652`）は「ここで手つきを覚えてから、**⏱ 立体タイムアタック**で
  実物の分子に当ててみてください」

**したがって Step 1→Step 2 の正しい組は「🔤 記号でパズル → ⏱ 立体タイムアタック」。**
項目24-3 の「統合したい」という動機（記号で覚えてから分子で練習する流れが分断されている）は
正しいので、**行き先だけ差し替えて A-7 で引き取る**。

### 2-7. 既存の設計書で「決めたのに未実装」の2件

| 出典 | 決めたこと | いまの実装 |
|---|---|---|
| `DESIGN_mobile.md` §3 | 「ヘッダー（ロゴ・**モードタブ**・❓）」「モードタブ（🧩📚🧪）を**主役に**」 | **未実装**。`#mode-tabs` は `#right-panel` の中にあり、スマホでは ☰ でシートを開くまで**現在のモードすら見えない** |
| `DESIGN_ui_modes.md` §7 | 「自由モードの初期状態: 空キャンバス。**名称呼び出しの導線を目立たせる**」 | **逆**。`#summon-input` は「⚗ この分子の反応」カードの**最下段**（y=485）にいる ＝ レビュー項目19 が指している場所 |

また `DESIGN_ui_modes.md` §3.3 は「分析ツール: 📚異性体・🧊立体・**🔤官能基をまとめる**」を
自由モードに集約すると書いたが、実装では 🔤 だけがキャンバス上ツールバー（全モード）に置かれた。
**意図的な逸脱か流れた結果かが記録されていない**（§8 の未決）。

### 2-8. ボタンのない入口

ヘルプ（`.hint-box`）に書いてあるだけで、画面上に手がかりがない操作:

| 操作 | 手段 |
|---|---|
| 原子・結合の削除 | 右クリック ／ 同じ元素を重ねてクリック ／ 結合線を長押し ／ すばやく2回タップ |
| 結合次数の変更 | 結合線をシングルクリック（単→二重→三重） |
| 結合の伸縮 | 結合線を軸方向にドラッグ |
| **分子を丸ごと動かす** | **Shift + 原子ドラッグ**（PC のみ。スマホに代替手段なし） |
| キャンバスの移動 | 右ボタンドラッグ ／ ホイール ／ 2本指 |
| キャンバスの拡大縮小 | **Ctrl + ホイール** ／ ピンチ |
| Undo / Redo | Ctrl+Z / Ctrl+Y（Ctrl+Shift+Z） |
| 削除 | Delete キー |
| デモの中断 | Escape |

- **道具を切り替えるキーボードショートカットは無い**（`game.js:695` の keydown は Undo/Redo/Delete のみ）。
  PC で炭素と酸素を往復するたびにパレットまでマウスを動かす
- **Shift+ドラッグ（分子を丸ごと動かす）にタッチの代替が無い。** 反応実行が
  「分子を離してから実行してください」と案内する場面で、スマホの生徒には手段がない

### 2-9. 外からの入口（ハブ・URL）

| 入口 | いまの行き先 |
|---|---|
| 化学レンズのハブ `index.html` の単元別インデックス（**7リンク**） | **全部 `https://chem.schoollenz.com/assembler/` の1点**。`?` も `#` も付かない |
| `?rec=<demoId>` ＋ `format` `speed` `delay` `cursor` `caption` | SNS 収録モード（`rec.js`） |
| `?quiz=` `?stereoQuiz=` | クイズの答えを固定（v441。画面には出さない） |
| localStorage `chemAssembler.mode` | 前回のモードを復元。既定は 🧩 パズル |

> ⚠ **ハブが約束している行き先と、実際に開く画面がずれている。**
> ハブの単元行は「パズルでみる有機化学 — **命名クイズ**」「— **反応機構ビューア**」と機能名まで書いているのに、
> リンクは 7本とも `/assembler/` のトップ（＝🧩 パズルモード、または前回のモード）に着地する。
> 命名クイズに辿り着くには、そこから **☰ → 📚 学習 → 🎓 クイズに挑戦 → 📝 命名クイズ の4手**が要る。
> **深いリンクの受け口（`?open=`）は、この見直しでいちばん費用対効果が高い1手**（A-6）。

---

## 3. 診断（数え上げから読み取れること）

**D1. 道具箱の並びが、使う順になっていない。**
「原子を置く → つなぐ → 環や官能基を足す → 仕上げに立体の印を付ける」の順で使うのに、
並びは「操作モード（印を含む）→ 結合タイプ → 原子 → モジュール」で、**ほぼ逆**。
その結果、いちばん最初に要る C ボタンが 375px では 265px 先、
いちばん象徴的なベンゼン環は **PC でも折り返しの下**にいる。

**D2. モードが「何をする所か」を名乗っていない。**
🧩 パズル・📚 学習・🧪 自由 は**名詞**で、初めての人には「学習」と「パズル」の区別がつかない
（レビュー項目18 の動機はここ）。ただし直すべきは**タブの数ではなくラベル**。

**D3. 深さ3〜4段の入口が多すぎる。**
立体の練習23種類が「☰ → 📚 → 🎓 → ボタン → select」の5段目にある。
外（ハブ・SNS）から来た人が目的の画面に着地する方法がない。

**D4. モードの壁が学習の流れを切っている。**
パズルで組んだ分子の立体を見るには 🧪 自由へ移る必要がある（🧊 は自由のみ）。
異性体調べも同じ。**「解く」と「調べる」が別モードに分かれているのに、学習では連続した行為**。

**D5. 長文ヘルプが常に最下段を占めている。**
`.hint-box` 1345px（PC 1611px）が3モード共通で出る。
畳めないので、右パネルは常に「本題 + 2画面ぶんの説明書」になっている。

---

## 4. 使う人の道筋（順路）で並べ直す

機能が増えた順ではなく、**学習の順路**で並べるとこうなる。
右2列は「いまどこにあるか」と「順路との食い違い」。

| 段 | やること | いまの入口 | 食い違い |
|---|---|---|---|
| **0. 触る** | 原子を置く・つなぐ・消す | 左パレット（原子・結合・消しゴム） | **C が画面外**（D1） |
| **1. つくる** | お題どおりに組む | 🧩 パズル | — |
| **2. たしかめる** | 名前・分子式を見る／お手本と見比べる | 🔍 いま描いている分子（パズル・自由）／お手本を見る | 学習モードでは 🔍 が消える |
| **3. 見比べる** | 同じ？違う？・名前を当てる | 📚 学習 → 🎓 → 同じ化合物？／命名 | 3段目 |
| **4. 反応させる** | 手を動かして反応／機構を見る | 🧪 自由 の反応カード ／ 📚 学習 の機構ビューア | **同じ「反応」が2モードに分かれている** |
| **5. 立体を読む** | くさび図・3D・フィッシャー・記号→分子 | 🧪 自由 の 🧊 ／ 📚 学習 の立体クイズ6種 | **入口が2モードに分かれ、片方は select の中**（D3・D4） |
| **6. 書き出す** | 異性体・アルキル基・立体異性体を列挙 | 📚 学習 → ✏️ → カード内のボタン15個 | id なし・3段目 |
| **7. 呼び出す** | 既存の化合物を出して調べる | 🧪 自由 の反応カード最下段 | **項目19。「反応」の中に「作図」がある** |

**この表がそのまま案の評価軸になる。** 直したいのは「食い違い」列であって、モードの数ではない。

---

## 5. 案

### 5-1. 案A（推し）— 小さく直す：並べ替え・畳み込み・ラベル・深いリンク

骨格（3モード・3カラム・右パネル・14モーダル）は**一切作り替えない**。

| # | やること | 直る診断 | 触るファイル |
|---|---|---|---|
| **A-1** | 左パレットを**順路順**に並べ替える（原子 → 環・官能基 → 作図の道具 → **仕上げの印**）。✳・⇄・⬍ の3つは「仕上げの道具」として最後尾へ。縦画面では原子ボタンを横画面と同じ**記号だけの丸**に圧縮 | D1 | `index.html`（ブロックの順序）＋ `style.css` 少々 |
| **A-2** | **モードタブをスマホでも常時見える所へ**（`DESIGN_mobile.md` §3 の未実装分）。DOM は動かさず、モバイルだけ `position:fixed` の細い帯に見せる | D2・D3 | `style.css` |
| **A-3** | タブのラベルを**動詞**にする（例: 🧩 **お題を解く** / 📚 **練習する** / 🧪 **自由につくる**）。`data-mode` の値は変えない | D2 | `index.html` 3行 |
| **A-4** | **`#summon-input` を `#compound-info`（🔍 いま描いている分子）カードへ移す**（項目19）。カードは `data-modes="puzzle free"` なので、パズルでも呼び出せるようになる | D4・順路7 | `index.html`（label ブロックの移動） |
| **A-5** | `.hint-box` を `<details>` で畳む（既定は閉。中身と id はそのまま） | D5 | `index.html` 2行 |
| **A-6** | **深いリンク `?open=<名前>`** を足し、ハブの単元別リンク7本を機能名どおりの行き先へ繋ぐ。`?open=naming` `?open=mechanism` `?open=stereo` 等 | D3 | `game.js`（数十行）＋ ルート `index.html` の href |
| **A-7** | 学習タブのクイズ8ボタンを**3群の見出しつき**に整理し、`select` に沈んだ出題のうち**独立した練習になっているものを見出しに出す**（D/L・同じ？違う？・上級）。記号パズル → タイムアタック の**順路を矢印で明示**（項目24-3 の代案。§2-6） | D3 | `index.html`（学習タブの中）＋ `game.js`（select の初期値を渡す小さな配線） |
| **A-8** | 🧊 立体で見る・📚 異性体を調べる を**パズルモードでも出す**（`data-modes="puzzle free"`。判定には影響しない） | D4 | `index.html` 1行 |

**良くなること**: §4 の食い違い8件のうち **7件が消える**（残るのは「反応が2モードに分かれている」）。
最大の痛点 D1 が直る。ハブからの導線が機能名どおりになる。

**失うもの**:
- A-1 で下ツールバーの見た目が変わる ＝ **31本の収録動画の画作りが変わる**（§7・撮り直しはしない）
- A-2 でモバイルの縦が数十px 減る（キャンバスが少し狭くなる）
- A-3 のラベル変更は、投稿済み動画の字幕・ヘルプ文と表記がずれる（`.hint-box` と FAQ の同時更新が必要。開発方針 5章「ヘルプと実装は常に一致させる」）
- 2大モード化の**すっきり感は得られない**（タブは3つのまま）

**規模**: `index.html` の並べ替え中心。新規ロジックは A-6 の URL ハンドラだけ。

### 5-2. 案B — レビュー項目18 そのまま：2大モードへ再編

`[🧪 自由][📚 学習]` の2タブにし、🧩 パズルを学習タブの筆頭アコーディオンに入れる。

**良くなること**: トップレベルの選択が「つくる／学ぶ」の二択になる。初見の迷いは減る。

**失うもの・リスク**:
- **パズルが1段深くなる。** いまトップレベルで即座に始められるお題が、
  「📚 → 🧩 お題パズル を開く」の2手になる。**既定モードがパズル**（初回訪問時にお題が出る）なので、
  初見のユーザーの第一歩がむしろ遠のく
- **`setMode()` の3値契約が壊れる。** `game.js:4001` は `['puzzle','learn','free']` を検証し、
  `localStorage` に保存し、モード離脱時に reactionPlayer / isomerPractice / alkylPractice /
  stereoPractice / reactor の**5つの後始末を分岐している**。ここは慎重に写す必要がある
- **`tests.js` の `setMode` 呼び出し 60箇所**と、`demos*.json` の
  `.mode-tab[data-mode="free"]` 15箇所・`[data-mode="learn"]` 1箇所が、`data-mode` の値を前提にしている
- **D1（C ボタンが画面外）を1つも解決しない。** 左パレットに触らないため
- 学習タブが「パズル＋クイズ8＋練習3＋機構」の**4アコーディオン**になり、全開時 3158px がさらに伸びる

**規模**: `index.html` の右パネル全面 ＋ `game.js` の `setMode` 周辺 ＋ テスト調整。

### 5-3. 案C — 順路の1本道：起動時にホーム画面を置く

起動時に「今日は何をする？」の**ホームカード**（0. 触ってみる／1. お題を解く／2. 見比べる／
3. 反応させる／4. 立体を読む／5. 書き出す）を出し、選ぶと該当モード＋該当パネルが開いた状態へ飛ぶ。
モードタブは残すが、迷ったら ⌂ でホームに戻れる。

**良くなること**: §4 の順路がそのまま画面になる。D2・D3・D4 が根本から消える。
A-6 の深いリンクと同じ仕組みで動くので、ハブ・SNS からの着地とも一本化できる。

**失うもの・リスク**:
- **起動が1タップ遅くなる。** リピーターには邪魔（「次から出さない」が要る＝ localStorage の状態が増える）
- **31本の台本すべてが、起動直後にホームを踏むようになる。** `rec.js` は
  `?rec=` で来たら自動再生するので**ホームを抑止する分岐が要る**。抑止を忘れると全台本が1手ずれる
- 新規画面なので、回帰テストの土台がない（ホームの遷移テストを新規に書く）

**規模**: 新規画面 + ルーティング。3案でいちばん大きい。

### 5-4. 比較と推し

| | 案A 小さく直す | 案B 2大モード | 案C ホーム画面 |
|---|---|---|---|
| D1 道具箱の並び（**最大の痛点**） | **直る** | 変わらない | 変わらない |
| D2 モードが名乗らない | ラベルで直る | 直る | 直る |
| D3 深さ3〜4段 | A-6・A-7 で浅くなる | 1段減って1段増える（差し引きゼロ） | 直る |
| D4 モードの壁 | A-4・A-8 で緩む | 変わらない | 直る |
| D5 長文ヘルプ | 直る | 変わらない | 変わらない |
| 31本の台本への影響 | 画作りのみ（§7） | **`data-mode` 契約に触れる** | **全台本が起動時に1手増える** |
| 170件のテストへの影響 | ほぼ無し | `setMode` 60箇所の前提を確認 | 新規テストが要る |
| 段階に割れるか | **8手すべて独立** | 一括でしか入らない | 一括 |

**推しは案A。** 理由を3つ:

1. **実測した最大の痛点（D1）を直せるのは案Aだけ。** 案B・案C はどちらも右パネルの話で、
   「炭素ボタンが画面外」には手が届かない。レビュー項目18 を額面どおり実装しても、
   ユーザーが感じている「入口がわかりづらい」の**いちばん太い部分は残る**
2. **8手すべてが独立して入れられる。** 案B・案C は「入れる／入れない」の一発勝負で、
   途中で気に入らなくても戻しにくい。案A なら A-1 だけ試して判断できる
3. **資産（31本の動画・170件のテスト）への当たりが最も柔らかい。**
   §7 の不変条件（id と `data-mode` の3値）を守れる案は A だけ

**案B・案C を捨てるわけではない。** A-3（動詞ラベル）と A-6（深いリンク）は
案C のホーム画面の**部品そのもの**なので、案A を入れたあとで案C に進む道は残る。
案B は、案A の A-7 でクイズが整理された後に「それでも学習とパズルが並列で迷うか」を見て判断すればよい。

---

## 6. 段階（各段は独立して入れられる）

各段に「何を動かすか」「テストで何を固定するか」「どの台本が壊れるか」を書く。
版番号は実装レーンが自分の帯で振る。**1段=1コミット**とはかぎらない（A-1 は2コミットに割れる）。

### Step 1 — 台本にも見た目にも触らない範囲（A-4・A-5・A-3）

| 動かすもの | 中身 |
|---|---|
| A-4 | `#summon-input` を含む `<label>` ブロックを `#reaction-card` から `#compound-info` の末尾へ移す |
| A-5 | `.hint-box` を `<details>`（既定 閉）で包む |
| A-3 | `.mode-tab` の表示テキストを動詞に。`data-mode` の値は据え置き。`.hint-box` と FAQ の該当文言も同時に直す |

- **テストで固定すること**
  - 新規: 「`#summon-input` が `#compound-info` の子孫にあり、**パズルモードでも呼び出せる**」
    （`setMode('puzzle')` → `summon-input` に値を入れて `change` → `compound-name` が変わる）
  - 既存で守られる: `tests.js` の `getElementById('summon-input')` **15箇所**は id 参照なので無傷
  - 新規: 「`.hint-box` を包む `<details>` が既定で閉じている」
- **台本の影響**
  - `summon` アクション（**18箇所**）は `getElementById('summon-input')` で引くので**壊れない**
  - ただし `tutorial.js:409` の `summon` は `button` と違って **rect=0 のガードが無い**。
    移設先の `#compound-info` はパズル・自由の両方で表示されるので、
    いまの台本（全部 `.mode-tab[data-mode="free"]` を先に押す）は今までどおり動く。
    **`#compound-info` を将来 `data-modes` から外すときは、このガードを先に足すこと**
  - A-3 のラベル変更は `data-mode` 属性セレクタに影響しない（**16箇所すべて安全**）
  - **画作り**: `.hint-box` を畳むと右パネルの見た目が変わるが、収録は右パネルをほぼ映さない
    （`syncSheetFor` が必要なときだけシートを開く）ので影響は小さい

### Step 2 — 左パレットの並べ替え（A-1）※ ここだけ画作りが変わる

| 動かすもの | 中身 |
|---|---|
| A-1a | `.tool-group` の**順序**を「原子の配置 → 環・官能基モジュール → 操作モード → 結合タイプ」に |
| A-1b | ✳・⇄・⬍ を「操作モード」から切り出して「仕上げの印」グループへ（最後尾） |
| A-1c | 縦画面の原子ボタンを記号だけの丸に圧縮（横画面の `font-size:0` 方式を portrait にも） |

- **テストで固定すること**（**この見直しでいちばん重要な新規テスト**）
  - 新規: **「375px 幅で `.atom-btn.atom-c` が左パレットの可視域に入っている」**
    （`test.html` の iframe 幅を一時的に 375px にして
    `getBoundingClientRect().right <= panel.getBoundingClientRect().right` を確認し、**必ず幅を戻す**）。
    いまこれを見張るテストは1件も無い（`tests.js` の R1 は DOM の存在と CSS ルールの有無だけを見ている）
  - 新規: 「⌬ ベンゼン環が 1280×800 で左パネルの可視域に入っている」（PC の折り返し下問題）
  - 既存で守られる: 台本は `.atom-btn.atom-c`・`[data-module=benzene]` のように
    **クラス/データ属性**で引くので、DOM の並べ替えでは壊れない
  - ⚠ **iframe 幅を変えるテストは、他のテストがメディアクエリ発火状態を引き継がないよう
    必ず finally で元に戻すこと。** 既存 170件は「≥900px」を暗黙の前提にしている
- **台本の影響**
  - セレクタは**壊れない**（`.atom-btn.atom-c` 20箇所・`.atom-btn.atom-o` 14箇所・
    `#btn-tool-select` 15箇所・`#btn-tool-bond` 3箇所・`#btn-asym-mark` 2箇所・
    `#btn-tool-erase` 1箇所・`#btn-cistrans-reshape` 1箇所・
    `.mod-btn[data-module=...]` 3箇所 — すべて id かクラス指定）
  - ⚠ **画作りは変わる。** 収録は **810×1440 = モバイル縦レイアウト**（`tools/record/record.mjs:53`。
    「viewport を 1080 幅にするとモバイル判定 899px を超えてしまう」ため意図的に 810 を選んでいる）。
    つまり **31本すべての動画の下ツールバーの見た目が変わる**
  - ただし**タップの輪（ゴーストカーソル）は `getBoundingClientRect()` から毎回引く**ので、
    ボタンが動いても輪は正しい位置に出る ＝ **誤操作に見える事故は起きない**
  - **撮り直しは不要**と判断する。理由: 投稿済み動画は「その版の記録」であり、
    下帯のボタン順が違うことは学習内容の誤りにならない。**次に撮る回から新しい並びになる**
  - ただし **`.hint-box` と FAQ に「下の帯を横にスクロールすると…」の類の記述があれば同時に直す**
    （開発方針 5章）

### Step 3 — モードタブをスマホで常時表示（A-2）

- **テストで固定すること**
  - 新規: 「375px で `#mode-tabs` が `body.sheet-open` でなくても可視域にある」
  - 既存: R1（シートの開閉配線）が通ること。`#mode-tabs` を DOM ごと動かさない（CSS だけで見せる）なら
    `#right-panel` の子である前提は保たれる
- **台本の影響**
  - `.mode-tab[data-mode="free"]`（15箇所）・`[data-mode="learn"]`（1箇所）を押すとき、
    `tutorial.js` の `syncSheetFor` は「要素が `#right-panel` の中か」で判定する。
    **DOM を動かさず CSS で見せる方式なら、この判定は今までどおり ＝ シートが開いてから押される**
  - ⚠ **DOM ごとヘッダーへ移すと `syncSheetFor` がシートを開かなくなり、
    16箇所の演出（シートが開いてタブを押す絵）が変わる。** CSS 方式を強く推す
  - ⚠ 収録画面（810×1440）で**タブ帯が常時映り込む**。`.recording` / `.rec-short` で
    隠すかどうかは収録側の判断（`style.css:1223` に前例あり）。**隠さないほうが
    「どのモードの話か」が動画で伝わる**ので、まず隠さずに1本撮って判断する

### Step 4 — 深いリンクとクイズの整理（A-6・A-7・A-8）

| 動かすもの | 中身 |
|---|---|
| A-6 | `?open=<名前>` を `game.js` の初期化末尾で解釈（`appReady` の直前）。名前は `puzzle` `naming` `quiz` `mechanism` `stereo` `isomer` `practice` 等。**`?rec=` が付いているときは無視する**（収録の1手目を汚さない） |
| A-7 | 学習タブのクイズ8個を3群に。①**見比べる**（同じ化合物？・命名・立体異性体クイズ・同じ立体はどれ？）②**並べ替える**（記号でパズル **→** 立体タイムアタック **→** フィッシャー操作練習）③**数える**（立体異性体は何種類？）。select に沈んでいる D/L・同じ？違う？・上級 を見出しに出す |
| A-8 | 🧊 立体で見る・📚 異性体を調べる を `data-modes="puzzle free"` に |

- **テストで固定すること**
  - 新規: 「`?open=naming` で `#naming-modal` が開く」「`?rec=` があるとき `?open=` は無視される」
  - 新規: 「A-7 の見出しから開いた 4択が、指定どおりの `#pk-kind` で始まる」
    （v441 の `setForced` と同じく、**生成の狙いではなく実際の出題**で確かめる）
  - 新規: 「`setMode('puzzle')` で `#btn-stereo` が表示される」
- **台本の影響**
  - A-7 でボタンを群に入れ替えても、**id を変えなければ**
    `#btn-time-attack`・`#btn-stereo-quiz`・`#btn-quiz` は無傷
  - ⚠ **`#learn-acc-quiz>summary`（demos-stereo.json に1箇所）は構造セレクタ**なので、
    アコーディオンを3群に割るときに**この id を消すと1本壊れる**。
    `#learn-acc-quiz` を①群の id として残すこと
  - ハブ側のリンク7本を書き換えるのは `../index.html`。
    **`?v=` の対象外**（ハブは自己完結）だが、`node tools/verify-release.js` の死にリンク検査は通す

### 全段共通の手順（開発方針 6章・7章）

1. `assembler/index.html` / `test.html` / `audit.html` の `?v=` とヘッダー表示を同時更新
2. `test.html` を開いて**全件合格**（RX13 は約10%の頻度で落ちるので1回だけ再実行して切り分ける）
3. `node tools/verify-release.js assembler`
4. 375px 縦・375px 横・1280×800 の**3つで実機確認**し、コミットメッセージに書く
5. **UTF-8（BOMなし）**・化けパターンの確認（パターンの一覧は `CLAUDE.md`「重要ルール」節）

---

## 7. 台本・収録への影響（守るべき不変条件）

**この節が段階分けの土台。** 見積もりを外すと収録済み31本が撮り直しになる。

### 7-1. 資産の規模（実測）

- **`assembler/demos*.json` に 31本**（`demos.json` 2 / `demos-fg.json` 6 / `demos-isomer.json` 10 /
  `demos-reaction.json` 7 / `demos-stereo.json` 6）。**全部 `sns: true`**
- `video-scripts/V*.md` は **31本**あり、demo id で1対1に対応する
- 台本は **`selector` を 161箇所**で使う。アクション種別の内訳は
  `wait` 251 / `click`（キャンバス座標）227 / `button` 159 / `frame` 86 / `summon` 18 /
  `reactionButton` 11 / `clickBond` 8 / `hover` 7 / `undo` 5 / `drag` 3 / `cutBond` 2 /
  `select` 2 / `quizForce` 1
- 収録は **810×1440（縦・`--format=short`、31本すべて）／ 1920×1080（横・`--format=wide`）**。
  **810 は「899px のモバイル判定を超えないための上限」として意図的に選ばれている**
  （`tools/record/record.mjs:46-53`）＝ **投稿されている動画はすべてモバイル縦レイアウト**

### 7-2. 壊れる／壊れない の判定基準

| 変更 | 台本への影響 |
|---|---|
| 要素を DOM の別の場所へ移す（id 据え置き） | **壊れない**（`button` `toggle` `select` は `querySelector(a.selector)` で引くだけ） |
| 要素を並べ替える | **壊れない**（同上） |
| 要素の表示テキストを変える | **壊れない**（`select` アクションの `contains` は `<option>` の文字だけを見る） |
| **id を変える／消す** | **壊れる**。`tutorial.js:313` が「ボタンが見つかりません」で例外を投げ、収録が止まる |
| **`data-mode` の値を変える** | **壊れる**（16箇所） |
| **`#learn-acc-quiz` を消す** | **壊れる**（1箇所。構造セレクタ `#learn-acc-quiz>summary`） |
| **`#reaction-actions` の器か、反応ボタンの文言を変える** | **壊れる**（`reactionButton` 11箇所が `#reaction-actions button` の**テキスト一致**で探す。`エステル化` `けん化` `ニトロ化` `スルホン化` `塩素化` `Br₂` `HBr` `アルデヒド` `カルボン酸` 等） |
| `#quiz-series` / `#quiz-strength` の**選択肢の文言**を変える | **壊れる**（`select` 2箇所が `枝分かれ` `弱` を `contains` で探す） |
| 要素をモードで隠す（`display:none`） | **壊れない**。`button` アクションは rect=0 を検出してカーソル演出を省き、`el.click()` だけ実行する（`tutorial.js:316-322`）。**V52 はこれを使って `#btn-stereo-quiz` を直接押し、メニューを見せずに 1.3秒で本題に入っている** |
| **キャンバスの縦横比が変わる** | **座標は壊れない**が**構図が変わる**。`click` は SVG 論理座標を `getScreenCTM()` で変換するので正しい位置を打つ。ただし `frame`（86箇所）は viewBox を**常に 4:3** で張る（`tutorial.js:460`）ので、器の縦横比が変われば余白の入り方が変わる |

### 7-3. 案A の各段が触るもの

| 段 | id を変えるか | `data-mode` を変えるか | 画作りが変わるか |
|---|---|---|---|
| Step 1 | いいえ | いいえ | ほぼ変わらない |
| Step 2 | いいえ | いいえ | **変わる**（下ツールバーの並び。31本すべて） |
| Step 3 | いいえ | いいえ | **変わる**（タブ帯の映り込み。31本すべて） |
| Step 4 | いいえ（`#learn-acc-quiz` を残す） | いいえ | ほぼ変わらない |

**撮り直しの必要は無いと判断する。** Step 2・3 で変わるのは下帯とタブ帯の見た目であって、
テロップ・ナレーション・化学の中身・タップ位置は変わらない。投稿済みの動画は
その版の記録として残し、**次に撮る回から新しい画で撮る**。

> ⚠ ただし **`video-scripts/LANES.md` に「収録画面の見た目」を前提にした取り決め
> （タップの輪を逃がす座標 `(400,583)` など）があれば、Step 2・3 の後に実測し直すこと。**
> V52 §4 の `hover (400,583)` は「810×1440 で画面下端」という**画面サイズ依存の値**で、
> レイアウトが変われば死角の位置も変わる。

---

## 8b. ユーザーの決定（2026-08-03）

**§5 の案A・B・C のどれでもない、第4の形が指定された。**

> 「2大モードというか、**標準（自由モード）から、各パズル・学習を呼び出す ⇆ 抜けてもとにもどる**。
> **左パレットのレイアウトも修正**」

つまり **自由モードを「標準（ホーム）」に据え、パズル・学習はそこから出入りする行き先にする**。
案C（新しいホーム画面を挟む）に近いが、**新しい画面を作らず、いまある自由モードをホームに
昇格させる**点が違う。全台本に起動時の1手が増える案C の弱点を踏まない。

| 項目 | 決定 |
|---|---|
| 全体の形 | **自由モードを標準にし、パズル・学習は呼び出す ⇆ 戻る**（第4案） |
| A-1 左パレットの並べ替え | **やる**（明示指示。§5-1 のとおり順路順へ） |
| A-3 タブのラベルを動詞に | **変えない**（名詞のまま。投稿済み31本の字幕と表記を揃えるため） |
| R/S に入口を作るか | ~~保留~~ → **やった**（v570。§8 未決事項3。ヘルプの「④ 判定しません」も同時に書き換え） |

**守る不変条件は §7 のまま**（`id` を変えない・消さない／`setMode` の3値を変えない）。
自由モードを標準にするのは**既定値と行き来の導線の話**で、3モードの契約は保つ。

### 実測した台本への当たり（第4案の前提確認）

| 事実 | 数 |
|---|---|
| `steps` を持つ台本（demos*.json ＋ tutorials.json） | 40本 |
| **puzzle タブを押す台本** | **0本** |
| free タブを押す台本 | 11本 |
| learn タブを押す台本 | 1本 |
| **モードを一度も切り替えない台本** | **28本** |

**puzzle へ切り替える台本は1本も無い。** 既定を free にして困るのは
「切り替えずに puzzle 前提で動く28本」だけで、しかも v437 で N2 が
`initialState` を渡して**1本ごとに失敗を検出する**ようになったので、
壊れれば必ずテストが落ちる。

## 8c. 実装の記録（どこまで入ったか）

**案A の8手のうち7手が入った。** 残るのは A-2（モードタブをスマホで常時表示）だけで、
これは A-3 とあわせて**やらない**と決めた分（8b）。

| 手 | 版 | 状態 | 備考 |
|---|---|---|---|
| A-1 左パレットの並べ替え | v447 | ✅ | 炭素 x=626→11。実測は `DEVELOPMENT.md` の版表 |
| （8b 第4案）自由を標準に | v448 | ✅ | 既定モードと行き来の導線だけ。3モードの契約は不変 |
| A-2 モードタブの常時表示 | — | **やらない** | 8b |
| A-3 ラベルを動詞に | — | **やらない** | 8b（名詞のまま。投稿済み31本の字幕と揃えるため） |
| A-4 名称呼び出しの移設 | v450 | ✅ | 移設先が `puzzle free` なのでパズルでも呼べる |
| A-5 遊び方を畳む | v451 | ✅ | 右パネル 375px縦: パズル 2386→1085 / 学習 1708→408 / 自由 1923→623 |
| A-6 深いリンク `?open=` | v454 | ✅ | 受け口16個＋ハブの単元行6本。`?rec=` のときは踏まない |
| A-7 クイズを3群に | v453 | ✅ | 沈んでいた出題3つをボタンに。順路 🔤→⏱ を矢印で |
| A-8 立体・異性体をパズルでも | v452 | ✅ | `#compound-info` へ移すだけ。判定は不変 |

**§7 の不変条件は8手とも守られた**（`id` を変えない・消さない／`setMode` の3値を変えない）。
新規テストは EP1〜EP6 の6件で、回帰テストは **172件 → 178件**。

**§4 の食い違い6件のうち、消えたのは3件・半分だけ直ったのが1件・残ったのが2件。**
§5-1 の「7件が消える」は数え方が甘かった（診断 D と食い違いを混ぜて数えていた）。

| 段 | 食い違い | どうなったか |
|---|---|---|
| 0. 触る | C が画面外 | **消えた**（A-1） |
| 2. たしかめる | 学習モードでは 🔍 が消える | **残る**。学習タブは自前の練習で埋まっており、A-8 の対象外にした |
| 4. 反応させる | 同じ「反応」が2モードに分かれている | **残る**（§5-1 の時点から残す見込みだったもの） |
| 5. 立体を読む | 入口が2モードに分かれ、片方は select の中 | **消えた**（A-8 で 🧊 がパズルにも出る／A-7 で select の中身を見出しに出した） |
| 6. 書き出す | id なし・3段目 | **半分**。外からは `?open=practice` で1手になったが、**アプリの中では3段目のまま**で、カード内の15個のお題ボタンに id が無いのも変わっていない |
| 7. 呼び出す | 「反応」の中に「作図」がある | **消えた**（A-4） |

診断のほうは **D1・D3・D5 が消え、D4 が緩み、D2 は手を付けていない**（A-3 をやらないと決めたため）。

**§2-6 の検証は実装でも裏が取れた。** レビュー項目24-3 の「🔤 記号でパズルを
🔃 フィッシャー操作練習へ統合」は成立せず、正しい組は **🔤 → ⏱ 立体タイムアタック**。
A-7 では統合せず、②群の中で順路を矢印で示す形に落とした。

**A-6 で足した引数2つは設計書に無かったもの**（実装で必要になった）:
- `series=<部分一致>` … ハブの有機の単元行6本のうち**4本がパズルで、違いはシリーズだけ**。
  これが無いと4本が同じ画面に着地して「機能名どおり」にならない。
  **部分一致にしたのは、単元名（「芳香族化合物」）とシリーズ名（「ベンゼンとその同族体」）が
  完全には一致しないから**。完全一致だと綴りを1文字変えただけで黙ってトップに落ちる
- `summon=<化合物名>` … `open=stereo` `open=isomer` は**キャンバスが空だと調べようがない**

## 8. 未決事項（実装前にユーザーに確認したいこと）

> **1・3 は 8b で決着**（1＝変えない、3＝保留）。以下は残りの未決分。

1. **タブのラベル（A-3）を動詞にしてよいか。** 動画の字幕とヘルプの表記が
   「🧩 パズル」で固まっているので、変えるなら `.hint-box`・FAQ・今後の台本を揃える必要がある。
   変えない選択（3タブのまま名詞）でも案Aは成立し、直るのは D2 だけが残る
2. **モードタブの常時表示（A-2）を収録画面にも出すか。** 出すと動画で「どのモードの話か」が
   伝わるが、縦が数十px 減る。`.rec-short` で隠す前例はある（`style.css:1240`）
3. ~~**R/S（`assignRSDescriptor`・v440）に入口を作るか。**~~ → **決着（2026-08-04・v570）。**
   A-1〜A-8 が全部入って入口の形が定まったので、**`#stereo-modal` の中心炭素ラベルの直下**に出した。
   ここを選んだのは (a) 判定の材料がくさび図＝フィッシャー投影の十字そのものであること
   (b) A-8（v521）で 🧊 がパズルモードからも開けるようになったこと
   (c) 内部タブ4つすべてで見えるので、3Dビューの ⑥「最下位を奥に構える」で
   自分が読んだ向きの答え合わせに使えること。
   **設計の要点は「判定できない図で何を見せるか」**だった（読める図はライブラリ207件中13件しかなく、
   記号が出ないほうが普通なので、黙って空欄にすると壊れて見える）。5通りの理由と直し方を出す。
   ヘルプ ④ の「判定しません」は同じコミットで書き換えた。詳細は `DEVELOPMENT.md`
   「R/S 入口レーン」の節
4. **`DESIGN_ui_modes.md` §3.3 との食い違い**（🔤 官能基をまとめる を自由モードの分析ツールに
   集約すると書いたが、実装はキャンバス上ツールバーで全モード共通）は、
   **実装が正**として設計書側を直すか、設計書が正として実装を戻すか
5. **項目20（試薬パレット）・項目26（アルキル基モジュール）の置き場所。**
   どちらも新しいパレット／モーダルを `index.html` に足す大型項目で、
   `DEVELOPMENT.md` は「入口の再編で置き場所が変わるので、その後に」としている。
   **案A の A-1 で左パレットが「原子 → 環・官能基 → 道具 → 印」に並ぶなら、
   試薬パレットは第5グループ、アルキル基モジュールは「環・官能基」グループの拡張**が自然。
   この文書の並びを前提にしてよいかを確認したい
6. **Shift+ドラッグ（分子を丸ごと動かす）のタッチ代替**（§2-8）。
   反応実行が「分子を離してから」と案内するのにスマホに手段がない。
   入口の話ではあるが**新機能**なので、この見直しに含めるかは別判断

---

## 9. 未処理の持ち込み（2026-08-07・robots.txt 対応のついでに見つけた）

AI クローラー拒否（robots.txt ＋ `noai` メタ・commit 9470572）で全 html を触ったときに、
入口の話として見つけたものをここへ置いておく。**その場では直していない**
——「入口は全体でまとめて見直す・個別に作り込まない」という §8b の決定に従った。

### 9-1. ハブへ戻るリンクが assembler と qa だけ無い

各アプリのトップで `../index.html`（🏠 化学レンズ）への導線を数えたところ:

| アプリ | ハブへ戻る |
|---|---|
| ion-equation | あり（`🏠 化学レンズ`） |
| ratio | あり（`🏠 化学レンズ`） |
| muki | あり（ヘッダーと終了画面の2か所） |
| **assembler** | **無し** |
| **qa** | **無し**（`<title>` と本文に「化学レンズ」の文字はあるがリンクではない） |

サブディレクトリへ直リンクで着地した人が、ほかのアプリの存在に気づけない。
3アプリが同じ `🏠 化学レンズ` を持っている以上、残り2つが無いのは**方針ではなく取り残し**に見える。

ただし assembler のヘッダーは `DESIGN_ribbon_consolidation.md` の作業帯集約が進行中なので、
**リボンの置き場所が決まってから**でないと二度手間になる。案A の A-1（左パレットの並べ直し）と
同じ段でまとめて入れるのが自然。

### 9-2. 「直リンク着地は即離脱」という読みは、まだ根拠にできない

GA4 で `/ion-equation` 144・`/` 143・`/ratio` 108・`/muki` 68・`/assembler` 25 の着地があり、
トップ以外は全員が滞在0秒だった——という数字が導線改善の根拠として挙がったが、
**この0秒セッションの大半は 8/4 のボット**（3〜4時間に485ユーザー・キーイベント0%・
各ページ 1.05〜1.17回の機械的巡回・iPad / iPhone の User-Agent を騙ったクラウドノード）。

人の流入は YouTube 4・Instagram 2 セッションの規模しかなく、
**どちらの向きの結論を出すにも足りない**。GA4 側でボットを除いた母集団を作ってから、
この節の 9-1 も含めて数字で確かめること。それまでは「取り残しだから揃える」という
一貫性の理由だけで進めるほうが安全（0秒離脱を理由にすると、効果が出たかどうかも測れない）。
