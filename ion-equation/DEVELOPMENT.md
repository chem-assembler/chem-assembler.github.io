# イオンでみる化学反応式（Ion-Equation）開発方針

中学理科〜高校化学基礎向けの化学反応式学習アプリ。
反応式の係数や生成物を「丸暗記」ではなく、**水中のイオンの組み変わりを見て納得する**ことを目的とする。
vanilla JS + SVG、ビルドツールなし・静的配信（GitHub Pages 互換）。Chem-Assembler の姉妹プロジェクト。

## コンセプト（M1: 中和モード）

学習ループ:

1. **投入** — 酸・塩基の分子をボタンでビーカーに落とす
2. **電離** — 水面に達すると分子がイオンに分かれるアニメ（HCl → H⁺ + Cl⁻）。強電解質は完全電離
3. **反応** — 「反応させる」で H⁺ と OH⁻ がペアで引き合い、結合して H₂O が生まれる
4. **試行** — H⁺ が余れば「まだ酸性」。塩基を追加してちょうど中和を探す
5. **係数へ橋渡し** — 「H₂SO₄ 1個に NaOH 2個でちょうど中和」という体験がそのまま係数のヒントになる
6. **反応式完成** — 係数ステッパーを操作すると左右の原子数が即時集計され、つり合うと正解。
   同時にイオン反応式（H⁺ + OH⁻ → H₂O）と傍観イオンの解説を表示し、「塩は残ったイオンの組」であることを示す

クリア条件 = 「ビーカーでちょうど中和を達成」かつ「係数が最簡整数比でつり合う」の両方。

## 設計原則

- **model.js は DOM 非依存の純粋ロジック**（種の定義・電離表・原子数集計・係数判定）。UI から分離しテスト対象とする
- **粒子の座標・動きは見た目専用**。正否判定はイオンの個数と原子数のみで行う
  （Chem-Assembler の「検証はトポロジーのみ」と同じ思想）
- 正否判定は模範解答との文字列比較ではなく**原子数・電荷の保存＋最簡整数比**で行う（別解を機械的に許容できる形）
- データ（2026-07-24 更新）: 反応ライブラリ `reactions.json` を導入し `library.js` が **fetch** で読む。
  **index/検索（Phase 2 以降）を使うにはサーバー必須**（file:// では fetch が失敗）。
  ただし**ゲームプレイは当面 model.js 内の STAGES で両立**しており、fetch 失敗時も遊べる（app.js が握りつぶす）。
  将来ゲームプレイも reactions.json 駆動に移行すると完全にサーバー必須化する（その時点で本記述を更新）
- 全ファイル **UTF-8（BOMなし）**。コミット前に文字化けパターン（`縺`・`繧`・`繝`・`蜊`・`荳`・`邨`）がないか確認
- バージョン番号 `vNN` はコミットごとに **index.html・test.html のキャッシュバスター＋ヘッダー表示**を同時更新
- **1修正=1コミット**。修正ごとにブラウザで実挙動を検証し、検証内容をコミットメッセージに記録
- **コミット前に test.html を開いて全テスト合格を確認**

## 構成

| ファイル | 責務 |
|---|---|
| model.js | 化学モデル（化学種・電離表・ステージ定義・原子数集計・係数判定）。DOM非依存の純粋ロジック |
| app.js | UI・SVG 描画・粒子アニメーション・係数ステッパー・ステージ進行 |
| schematic.js | ブロック模式図の共通描画（中和＝H⁺と受け皿／酸化還元＝e⁻）。見た目専用で化学の判断は持たない |
| style.css | 見た目 |
| index.html | エントリ |
| redox.html + redox.js | 酸化還元モード（半反応式×倍率、e⁻ の授受アニメ）。model.js と style.css を共用 |
| test.html + tests.js | 回帰テスト（モデル純ロジック＋iframe駆動UIテスト×2モード。コミット前に全合格必須） |
| audit.html + audit.js | 夜間自動監査。実アプリを iframe で駆動し「原子の保存・反応が止まらない・粒が重ならない・枠外へ出ない」を全ステージ＋ランダム投入ファズ＋酸化還元で無人検査。結果はJSON保存可 |
| DESIGN_reaction_types.md | 反応タイプ分類と群別アニメーション設計（実装の正） |
| DESIGN_redox.md | B群（酸化還元）モードの設計書 |
| DESIGN_reaction_library.md | 反応の網羅・reactions.json・分類/物質検索・アニメタイプ管理・KMnO₄等の溶液中酸化還元（上位設計） |

起動: index.html を直接開くか、ローカルサーバー（例: `python -m http.server 8124`）。

## ロードマップ

反応タイプを3群に大別し、群ごとにアニメーションエンジンを作る（2026-07-22 合意）。
アプリの軸は「反応式」。無機化学・理論化学への対応づけはステージの単元タグで行う（アプリを教科の枠に内包させない）。

- **A群: イオンの組み変わり**（電子は移動しない）— 中和・沈殿・気体発生。現行エンジンの拡張で賄う
- **B群: 電子の移動** — 酸化還元（金属+酸、イオン化傾向、電池・電気分解）。e⁻ 粒の新エンジン
- **C群: 分子の組み換え** — 化合・分解・燃焼。イオンが出ないため別形式（v40 で着手：気体の空間・原子化）

- [x] M1: 中和モード初版 — 強酸×強塩基3ステージ、電離・中和アニメ、係数チェック、イオン反応式表示（v1）
- [x] M1.5: 数合わせビュー — 反応式の直下に係数ぶんの粒を並べ、組み変えアニメで余り・不足を見せる（v2）
- [x] M1.6: 磨き込み — 粒子ドラッグ操作（イオンを相手に重ねて1組だけ反応）・モバイルのタップ標的・遊び方4ステップの折りたたみパネル（v13〜v15）
- [x] M2: 沈殿反応（A群）— ルール表 `stage.rules` 導入、沈殿の沈降・堆積アニメ、ステージ4（AgNO₃+NaCl）・5（BaCl₂+Na₂SO₄）（v5）
- [x] M3: 気体発生（A群）— ルールを多重集合対応（2H⁺+CO₃²⁻）に一般化、中間体 H₂CO₃ を一瞬表示→分解、CO₂ の泡が水面で逃げるアニメ、ステージ6（v6）
- [~] M4: 弱酸・弱塩基 — 部分電離の表現（v38: 酢酸×NaOH。分子のまま溶け、中和時に電離して補う＝
  ルシャトリエ／v39: NH₃×HCl。弱塩基は H⁺ を受け取る＝ブレンステッド／**v88: 弱酸の遊離**
  CH₃COONa×HCl。強酸が中和の座を奪う置き換えビュー付き）。
  **v93: NH₃＋H₂O→NH₄⁺＋OH⁻**（溶媒の水を反応に参加させる。アンモニア水で沈殿させる版で回収）。
  残り: 弱酸の塩の加水分解、電離度・pH の扱い
- [x] M5: 酸化還元エンジン（B群・B1）— 半反応式×整数倍の足し合わせ方式（DESIGN_redox.md）。redox.html に3ステージ（Zn×Cu²⁺／Cu×Ag⁺銀樹／Zn×2H⁺）（v7）
- [ ] M5.5: B群の拡張 — イオン化傾向の予想モード、電池・電気分解（B3）、半反応式の自作練習
- [ ] M6: 自由組み立てモード — 生成物も自分で選ぶ。分子反応式とイオン反応式の書き分け練習。無機コンテンツパック（単元タグ）
- [x] 品質保証の移植 — iframe 駆動 UI テスト（v4〜）と**夜間監査ページ**（v80。audit.html/audit.js）

### 出題タイプ「目標の塩をつくる」（2026-07-24 着手・A群の深掘り）
- [x] 投入数を反応式の形で大表示・ちょうど反応で matched（v16）
- [x] 酸性塩：NaHSO₄（v17）・NaHCO₃＝二段中和/部分プロトン化（v18）。saltGoal で評価を一般化
- [x] 全ステージ目標バナー（v19）・単元タグ 正塩/酸性塩ほか（v20）
- [ ] 塩基性塩、H₃PO₄ の三段中和、比予想クイズ

### 反応ライブラリ化＋インデックス（2026-07-24 合意・[DESIGN_reaction_library.md](DESIGN_reaction_library.md) が正）
「反応式を網羅し、分類・登場物質から検索」。ライブラリ層（データ）とアニメ層（実装）を分離。
- [ ] Phase 1: reactions.json へ移行＋機械検証（原子/電荷保存・分類 enum・species 逆引き・animationType 実在）。
  **fetch 導入で file:// 直開き不可＝サーバー必須化**（本設計原則の記述を移行時に更新すること）
- [~] Phase 2: インデックス/検索 UI — library.html＋library-ui.js で分類フィルタ（型・塩・難易度★・単元）＋
  物質検索＋ディープリンク（?rxn=id）を実装（v24〜v25）。参照のみ反応の数合わせ表示は未
- [ ] Phase 3: アニメタイプのレジストリ化（既存を animationType に整理・未実装は「準備中」）
- [~] Phase 4: `redox-solution` — MnO₄⁻×Fe²⁺ を溶液中アニメで実装（v27 モデル→v29 アニメ本体：
  板なし・両者溶液中・e⁻ 溶液内授受・H⁺参加・**溶液色 紫→無色**・酸化数表示）。redox.html の
  ステージ5（rs1）で遊べ、**インデックスの KMnO₄ から「▶酸化還元モードで見る」で直接飛べる**（v30）。
  v31 で Cr₂O₇²⁻×Fe²⁺（rs2・橙→緑）、v32 で MnO₄⁻×シュウ酸（rs3・紫→無色＋CO₂の泡）を追加。
  溶液色は有色種の重み付けブレンド、酸化アニメは多生成物＋気体の泡に一般化（金属モードと後方互換）。
  溶液中3反応（rs1〜rs3）が遊べ、index の KMnO₄/K₂Cr₂O₇/シュウ酸から飛べる。残りは H₂O₂ など
- [~] Phase 5: 錯イオン生成 — 参照エントリで先行（v33〜v34: アンミン錯体 Cu²⁺/Ag⁺・沈殿の再溶解
  Cu(OH)₂/AgCl・両性水酸化物 Al(OH)₃/Zn(OH)₂ の計6反応を index に。単元 錯イオン/沈殿の再溶解/両性水酸化物）。
  **v35 でアンミン錯体2反応をプレイ可能化**（ステージ13 Cu²⁺+4NH₃／14 Ag⁺+2NH₃。新ルール kind:"complex"、
  NH₃ は電離しない分子として扱う）。**v36 で沈殿の再溶解**（ステージ15 CuSO₄×NaOH×NH₃ の二段変化）。
  **v37 で両性水酸化物**（ステージ16 AlCl₃×NaOH／17 ZnSO₄×NaOH。少量で沈殿・過剰で再溶解）。
  → 錯イオン生成は index の6反応すべてプレイ可能に。弱酸弱塩基（M4）・分子反応（C群）は今後

## 作業記録

- v34（2026-07-24・錯イオン生成の網羅拡大: 沈殿の再溶解・両性）: 錯イオン生成に4反応を追加（参照エントリ）。
  Cu(OH)₂+4NH₃→[Cu(NH₃)₄](OH)₂（青白沈殿の再溶解）、AgCl+2NH₃→[Ag(NH₃)₂]Cl（白沈殿の再溶解・
  ハロゲン化銀の識別）、Al(OH)₃+NaOH→Na[Al(OH)₄]、Zn(OH)₂+2NaOH→Na₂[Zn(OH)₄]（両性水酸化物）。
  新種 [Cu(NH₃)₄](OH)₂・[Ag(NH₃)₂]Cl・Al(OH)₃・Na[Al(OH)₄]・Zn(OH)₂・Na₂[Zn(OH)₄]。単元に
  「沈殿の再溶解」「両性水酸化物」を追加。錯イオン生成は計6反応に。全PASS（反応 17→21）。
- v106〜v107（2026-07-29・**酸化数を対象原子の真下へ**・**液性で書き換えるモード新設**）:
  - **v106（ユーザー指摘）**: 「NO₃⁻ などの多原子イオンで酸化数を表示する場合は該当原子の
    真下に。アンダーラインにするとよいかも」。v103 で式の側は直したが**ビーカーの粒が
    中央のまま**だった。粒の化学式を tspan で分け、getBBox() で対象原子の中心を測って
    その真下に置き、対象原子に**下線**を引く（符号で色分け）。式の側にも下線を足した。
  - **v107（項目33）**: 新ページ **condition.html / condition.js**（液性で書き換える）。
    「酸性の式の両辺に OH⁻ を同数足す → H⁺ と OH⁻ が H₂O になる → 両辺の H₂O を相殺する」
    という操作を、酸化還元モードと同じ筆算の流儀で手で解かせる。収録3件
    （水の電気分解の陰極・陽極、過酸化水素の還元）。相殺する H₂O には斜線を引く。
    H₂O₂ の O は −1（過酸化物の例外）としてデータに持たせ、既存の機械検証を通す。
    - **オゾンと過マンガン酸の塩基性形は入れていない**。O₃ は O 3個のうち1個だけが
      還元されるため、「その元素の原子が全部そろって変化する」前提の酸化数モデルでは
      扱えない（既存テストが弾く）。MnO₄⁻ の塩基性形は生成物も e⁻ の数も違う別の
      半反応式で、OH⁻ を足して相殺する操作では導けない。どちらもモデル拡張が要る
    - v105 の回帰も1件修正（溶液モードで e⁻ の到着前に予約して rs1 が待ちぼうけ）
  - 検証: 回帰テスト全合格（モデル29・反応ライブラリ11・UIイオン46・酸化還元19・**液性3**）
- v100〜v105（2026-07-29・**ユーザー指摘（docs/development_plan.md 項目24〜27・32）への対応**）:
  Gemini がまとめたユーザー要望の一覧に対し、実機で1件ずつ再現確認してから直した。
  **推奨順（①バグ→②タイムライン化→③表現の精密化）で一気に実施**。
  - **v100 図の重なり（項目24-1）**: 組み換えの図で見出しの文字に粒が食い込んでいた
    （見出し下端 84.5 に対し粒の上端 81）。見出しを下げ、粒は「見出し下端＋8＋半径」から。
  - **v101 入力のたびに画面が揺れる（項目24-2）**: `#calcSheet{min-width:max-content}` と
    1fr 列のせいで、⑤行目の項数が変わるたび列の取り分と全体幅が動いていた
    （rn1 で 610→737→800→928→…）。`lockSheetWidth()` で**5列とも px 固定**にした（626px で一定）。
  - **v102 縦スクロール・タイムライン（項目27＋24-3）**: 2カラムをやめ、
    ①半反応式 → ②e⁻ の数合わせ（ビーカー＋模式図）→ ③足し合わせ → ④省略イオンの補充 →
    ⑤化学反応式 の縦1本に。③〜⑤は1枚のグリッドなので → の位置が段をまたいでそろう。
    段が現れるときだけスライドイン。**「足すと…」の1行は消し、③の「倍率をかけた2本を
    縦に足して e⁻ に斜線」という筆算そのものに置き換えた**（項目27が求めていた打ち消し演出）。
  - **v103 酸化数を元素記号の真下に（項目25-2）**: MnO₄⁻ の +7 が式の中央（O のあたり）に
    出ていた。記号だけを `.oxAnchor` で囲って絶対配置の基準にした。
    項目25-1（半反応式は倍数化しない）は従来から満たしており、v102 で倍数化が③でだけ
    起こることが見た目にも分かるようになった。
  - **v104「この H⁺ は誰が出す？」の図（項目26）**: 必要な H⁺ を1個ずつ
    「連れてきた NO₃⁻ との対」に分け、酸化剤と一緒に来たぶん／足りなくて追加したぶん を
    色で分ける。追加ぶんの数は④で両辺に足す傍観イオンの数と必ず一致する（電荷保存）。
  - **v105 アニメの軌跡（項目32-1〜3）**: 板の原子を上詰めから中央そろえに
    （×9 で水の外まで並んでいた）。置いていかれた e⁻ は原子がいた高さに残す。
    イオンは受け取る e⁻ を予約してその高さへ泳ぐ。
    **項目32-4（析出は板の下側）・32-5（気体は e⁻ 受領後に浮上）は以前から満たしていた**
    ので、テストで固定しただけ（ドキュメントの分析と実装が食い違っていた）。
  - 未着手: 項目33（酸性⇄塩基性の切り替えと両辺 OH⁻ 追加）・項目34（有機の酸化還元）。
    どちらもコンテンツ拡充の大物で、別枠として残してある。
  - 検証: 回帰テスト全合格（モデル28・反応ライブラリ11・UIイオン46・**酸化還元19**）。
    追加したテストは、重なり・幅の固定・段の出方・酸化数の位置・H⁺ の由来・粒の軌跡。
- v98〜v99（2026-07-28・**酸化還元を5行の筆算にする**・イオン反応式→化学反応式の図）:
  ユーザーの指示「手作業では5行で筆算形式で行う。アプリもこれに近づけたい／
  イオン反応式→化学反応式については、酸化還元反応が終了した分については別グラフィック、
  余ったイオンのみ既存のイオン組み換え のイメージ」。
  ・**v98: 5行の筆算**。半反応式の行・イオン反応式・分子反応式をばらばらの箱に置いていたのをやめ、
    1枚のグリッド（`#calcSheet`）に5行で並べた。列は［符号｜左辺｜→｜右辺｜注記］で、
    どの行も **→ の位置がそろう**（実測: 全行の矢印 x が同値）。
    ```
      ×3 ) Cu                  → Cu²⁺ ＋ 2e⁻
      ×2 ) NO₃⁻ ＋ 4H⁺ ＋ 3e⁻  → NO ＋ 2H₂O
      ────────────────────────────────
           3Cu ＋ 2NO₃⁻ ＋ 8H⁺ → 3Cu²⁺ ＋ 2NO ＋ 4H₂O    イオン反応式
      ＋)  6NO₃⁻                 6NO₃⁻                    傍観イオンを両辺に
      ────────────────────────────────
           3Cu ＋ 8HNO₃        → 3Cu(NO₃)₂ ＋ 2NO ＋ 4H₂O 化学反応式
    ```
  ・**入力を1つに減らした**。v97 では分子反応式の係数5つをステッパーで入れさせていたが、
    手で解くときにやるのは「両辺に傍観イオンを何個足すか」だけ。5行目は `molecularizeStep()`
    で導出するので答えを別に持たなくてよい。足す数が足りなければ**あぶれたイオンが5行目に
    残ったまま出る**（0個なら `3Cu ＋ 2HNO₃ ＋ 6H⁺ → 2NO ＋ 4H₂O ＋ 3Cu²⁺`）ので、
    何が分子になれていないかが式のかたちで分かる
  ・必要数は左辺（H⁺→HNO₃）と右辺（Cu²⁺→Cu(NO₃)₂）から**別々に数えられて必ず一致する**
    （電荷保存）。この一致をテストで固定した。`checkMolecularEq()` は入力の判定係から
    **導いた式の検算係**に役目を変え、使われなくなった acidShort の助言は消した
  ・**v99: 図を2つに分けた**（ユーザーの言う「別グラフィック」と「余ったイオンのみ組み換え」）。
    上段「**済 ─ 酸化還元で決着したぶん**」は `3Cu→3Cu²⁺` と `2NO₃⁻＋8H⁺→2NO＋4H₂O` を
    灰色の枠で置き、もう組み換えないことを示す。下段「**残ったイオンの組み換え**」は
    イオン反応モードと同じ見方（1列＝生成物1個・上段に本体イオン・下段に価数ぶんの傍観イオン）で
    `H⁺＋NO₃⁻→HNO₃`（左辺）と `Cu²⁺＋2NO₃⁻→Cu(NO₃)₂`（右辺）を描く。
    **図は④行目の入力に連動**し、足りないぶんは点線の空席・余ったぶんは赤い印になる
  ・横幅: 筆算は式が長いと横に伸びるので `#sheetScroll` の中だけで横スクロールさせる。
    幅0＋`min-width:100%` で親に幅を要求させないため、375px 幅でもページ自体は横に伸びない
  ・検証: 回帰テスト全合格（モデル28・反応ライブラリ11・UIイオン46・酸化還元15）。
    実機で希（×3・×2 → 6個足す）と濃（×1・×2 → 2個足す）の両方と、375px 幅を確認
- v97（2026-07-27・**分子反応式に戻す段**）: ユーザー要望③の後半。**③はこれで完了**。
  ※ v98 で入力の形を「係数5つ」から「足す傍観イオンの数1つ」に作り替えた（下記は当時の記録）。
  半反応式 → イオン反応式 → **分子反応式** の3段目を酸化還元モードに足した。
  ・イオン反応式（倍率）がそろうと **#molEqWrap** が現れ、`Cu ＋ HNO₃ → Cu(NO₃)₂ ＋ NO ＋ H₂O`
    の係数パズルになる。傍観していた NO₃⁻ を戻す作業＝分子反応式に戻す作業
  ・**「e⁻ はぴったりだが H⁺ が足りない」を名指しで助言**（ユーザーの指定した山場）。
    `3Cu＋2HNO₃` と入れると「e⁻ の授受はぴったり（6個ずつ）。でも HNO₃ が足りない。
    還元されて NO になるのは2個だけで、残りは NO₃⁻ のまま Cu(NO₃)₂ に入る（6個）。
    だから HNO₃ は 2＋6＝8 個要る」。濃硝酸なら `Cu＋2HNO₃` が同じ形になり、2本並べて試せる
  ・正解すると**硝酸の二役の図**が出る（何個が還元されて気体になり、何個が NO₃⁻ のまま塩に入るか）。
    これがこの反応でユーザーの言う「工夫」にあたる部分
  ・判定は `checkMolecularEq()`（model.js）。酸の係数＝**還元されるぶん＋塩に入る傍観ぶん**、
    という関係をデータ（`molecularEq`）に持たせ、そこから助言を組み立てている。
    データの自己整合（模範係数がこの関係を満たすこと）もテストで固定した
  ・**数合わせビューは流用しなかった**: あれはイオンの組み替え（再ペアリング）の見方で、
    NO₃⁻→NO のように酸化数が変わる部分は表せない。代わりに二役の図を専用に作った
  ・検証: 回帰テスト全合格（モデル28・反応ライブラリ11・UIイオン46・酸化還元15）。
    監査は全ステージ(30)＋ファズ31＋酸化還元 r1〜r4 まで問題0で通過したが、
    ブラウザのバックグラウンド抑制で最後まで回らなかった（**次回まとめて回すこと**）
- v96（2026-07-27・**銅と硝酸（希・濃）**）: ユーザー要望③の前半。硝酸が
  **酸と酸化剤の二役**をこなすところを、酸化還元モードで先に扱えるようにした。
  ・半反応式2本を追加: NO₃⁻＋4H⁺＋3e⁻→NO＋2H₂O（希・N は +5→+2）／
    NO₃⁻＋2H⁺＋e⁻→NO₂＋H₂O（濃・+5→+4）。**還元されるのは NO₃⁻ で、H⁺ も一緒に消費する**
    ＝この H⁺ が「酸としての顔」。既存の枠組み（左辺の非 e⁻ 項をすべて溶液に置く）でそのまま動いた
  ・ステージ8 銅×希硝酸（×3・×2 で e⁻ 6個）／ステージ9 銅×濃硝酸（×1・×2 で e⁻ 2個）。
    **銅は塩酸には溶けないのに硝酸には溶ける**（ステージ3の亜鉛と対照）＝溶かしているのは
    H⁺ ではなく酸化剤としての NO₃⁻、という対比が狙い
  ・**「板に析出するのは単体の金属だけ」に直した**: 金属モードでは気体でない還元生成物を
    すべて析出として数えていたため、H₂O が「NO ×4（析出）」と表示されていた。
    元素1種・電荷0 の種だけを析出とし、水などは溶液中に漂わせる（Cu・Ag の析出は今までどおり）
  ・REDOX_STAGES のタイトルからも番号を外し、並び順から作るようにした（STAGES と同じ流儀）
  ・検証: 回帰テスト全合格（モデル27・反応ライブラリ11・UIイオン46・酸化還元14）
- v94（2026-07-27・**3反応物のまとめ版を廃し、錯イオンの並びを学ぶ順に**）:
  [DESIGN_ionic_two_step.md](DESIGN_ionic_two_step.md) の最終段。②はこれで完了。
  ・**`complex-cuoh2-nh3` を削除**（CuSO₄＋NaOH＋NH₃→[Cu(NH₃)₄](OH)₂＋Na₂SO₄）。
    ユーザー指摘のとおり**NaOH とアンモニアを1本の反応式に混ぜるのは一般的でない**。
    NaOH 版（s9・1段階）／アンモニア水版（v93 の2段階）に置き換わり、役目を終えた
  ・錯イオンの並びを**学ぶ順**に: 少量で沈殿（14）→過剰で再溶解（15）→
    中間を省いた式（16）→Ag の錯イオン（17）→AgCl の再溶解（18）。
    `complex-cu-nh3` は「まとめ」ではなく**中間を省いた別の書き方**として位置づけた
    （2段階の合計は (NH₄)₂SO₄＋[Cu(NH₃)₄](OH)₂ になり、係数の足し算では一致しないため。
    どちらの書き方も使われることを doneNote に明記）
  ・削除で行き場を失った3つの回帰テストは、同じ不変条件を保ったまま付け替えた:
    「NH₃ が足りなければ溶け残る」→ cu-nh3-step2、「段階を踏んで溶ける」→ cu-nh3-step2、
    「一度の反応させるで連鎖する」→ amphoteric-aloh3-naoh（2つのルールを持つ側へ）
  ・検証: 回帰テスト全合格（モデル26・反応ライブラリ11・UIイオン46・酸化還元13）
- v93（2026-07-27・**弱塩基の電離（NH₃＋H₂O）とアンモニア水版2段階**）:
  [DESIGN_ionic_two_step.md](DESIGN_ionic_two_step.md) の (c)。M4 で「溶媒の水を要する」として
  残していた宿題をここで回収した。
  ・**NH₃ は「相手が来たときだけ分かれる」既存の弱電解質の仕組みに乗せた**（`WATER_IONIZATION`）。
    OH⁻ が使われるときだけ電離が進む＝ルシャトリエ。違いは**分解に溶媒の水を1個使う**ところで、
    使った瞬間に H₂O の粒を1個だけ見せ、`solventUsed` に数えておく（原子の保存検査に要る）
  ・**ステージ15 硫酸銅×アンモニア水（少量）**: Cu²⁺＋2NH₃＋2H₂O→Cu(OH)₂↓＋2NH₄⁺。
    水は溶媒なので**投入ボタンには出さないが式には現れる**。ビーカーの試薬（reactants）と
    式の項（molecular/ionic）を分ける `stage.molecular` を追加してこれを表した
  ・**ステージ16 水酸化銅(Ⅱ)×アンモニア水（過剰）**: Cu(OH)₂＋4NH₃→[Cu(NH₃)₄]²⁺＋2OH⁻。
    ここでは NH₃ は電離せず配位子として働く（`solventUsed` が空のままなのを検査で固定）。
    **同じ NH₃ が少量では塩基・過剰では配位子**、が2段に分けて初めて見える
  ・NaOH 版（s9）と `variantOf` で相互リンク。イオン反応式で見るとどちらも Cu²⁺＋2OH⁻→Cu(OH)₂
  ・数合わせビュー・`simulateFormation` も**分子反応式の項**を使うようにした。おかげで
    「水が H⁺ と OH⁻ に分かれ、OH⁻ が Cu²⁺ と組み、H⁺ が NH₃ と組む」が粒で見える
  ・reactions.json にも `ionic` を持たせ、**イオン反応式の原子・電荷・最簡比を機械検証**する
    テストを追加（9反応）。物質検索もイオン反応式の項で引けるようにした
  ・検証: 回帰テスト全合格（モデル26・反応ライブラリ11・UIイオン46・酸化還元13）
- v92（2026-07-27・**イオン反応式を主にできるようにする**）: [DESIGN_ionic_two_step.md](DESIGN_ionic_two_step.md) の (a)。
  ユーザーの「Cu²⁺＋2OH⁻→ とイオン反応式の形を標準にするのが一般的かつ分かりやすい」に対応。
  ・`stage.ionic`（イオン反応式の項と模範係数）と `stage.primary`（既定の表し方）を追加。
    `eqOf(stage, mode)` が反応式パネルの項を返し、`checkStageCoeffs(stage, coeffs, mode)` が
    そのモードで判定する。**電荷は compareSides が元から見ている**ので判定はそのまま使えた
  ・**分子反応式 ⇄ イオン反応式の切り替えボタン**を反応式の上に置いた。入試では分子式を
    書かせることもあるので**どちらも書けるようにして行き来させる**（消すのではなく主従を変える）
  ・イオン反応式のときは**電荷の行**を数合わせ表に出す（原子だけ合わせても正解にならない）。
    逆に「粒で見る数合わせ」は傍観イオンを組み替えて分子をつくる見方なので、こちらでは出さない
  ・適用: 沈殿生成の s4（Ag⁺＋Cl⁻）・s5（Ba²⁺＋SO₄²⁻）・s9（Cu²⁺＋2OH⁻）と、
    両性の分割版4本。分割版は `primary:"ionic"` で**既定がイオン反応式**（傍観イオンを除くと
    NaOH 版もアンモニア水版も同じ式になる、というのが次の段の土台）
  ・係数の数と項の数が食い違う呼び出しを不正解として弾くようにした（分子式とイオン式の取り違え）
  ・検証: 回帰テスト全合格（モデル26・反応ライブラリ9・UIイオン45・酸化還元13）
- v91（2026-07-27・**両性水酸化物を2本に分ける**）: [DESIGN_ionic_two_step.md](DESIGN_ionic_two_step.md) の
  「両性は分割版とまとめ版の両方を持つ」。設計メモでは `primary:"ionic"` を先にする順だったが、
  **使う相手（Cu の NH₃ 版）が出てから一緒に入れるほうが検証できる**ので順序を入れ替えた。
  ・分割版4本を追加: Al³⁺＋3OH⁻→Al(OH)₃↓ ／ Al(OH)₃＋OH⁻→[Al(OH)₄]⁻ ／
    Zn²⁺＋2OH⁻→Zn(OH)₂↓ ／ Zn(OH)₂＋2OH⁻→[Zn(OH)₄]²⁻。
    **2本に分けて初めて「量で結果が変わる」が式として見える**（まとめ版は「結局いくつ要るか」を残す）
  ・reactions.json に `steps`（まとめ→分割）／`combined`（分割→まとめ）を持たせ、
    **インデックスで相互に飛べる**ようにした。飛ぶ前に絞り込みを外すので、
    相手が絞り込みで隠れていても確実に見える
  ・**分割版の合計＝まとめ版**を機械検証するテストを追加（係数を足し合わせて、
    途中でできる沈殿が打ち消し合い、残りがすべて0になること）。2本に分けても同じ反応であることの担保
  ・**テストの誤検出を修正**: 枠つきの沈殿と丸いイオンを「箱どうし」で判定していたため、
    箱の角の外側で接しているだけの組（NO₃⁻ と AgCl・実距離56.7 で接触していない）を
    重なりと誤って報告していた。丸どうし＝中心間距離／枠どうし＝箱／**丸と枠＝箱の最近点から円まで**
    に分け、20回の反復で誤検出ゼロを確認
  ・**監査の数え方も修正**: 投入を「押した回数」で数えていたため、C群の投入上限で入らなかったぶんを
    「原子が消えた」と誤報していた（ステージが増えてファズの当たり方が変わり露見）。
    `IonEq.state().added` に**実際に入った数**を出し、そちらで数えるようにした
  ・検証: 回帰テスト全合格（モデル25・反応ライブラリ9・UIイオン44・酸化還元13）、監査82件すべて合格
- v89（2026-07-27・**沈殿を反応物として投入できるようにする**）: [DESIGN_ionic_two_step.md](DESIGN_ionic_two_step.md) の (b)。
  錯イオン・両性を2本に分けると2段目はどれも沈殿から始まるため、その土台を先に作った。
  ・`dissociateMolecule` で `SOLID_SPECIES` の種は電離させず、そのまま底へ沈める
    （水にとけないのだから電離しない、という当たり前を実装に反映しただけ）
  ・**ステージ15 塩化銀 × アンモニア**（AgCl＋2NH₃→[Ag(NH₃)₂]Cl）を追加＝
    `complex-agcl-nh3` が参照のみ→**プレイ可能**に。ハロゲン化銀の識別（AgCl は溶ける・
    AgBr は溶けにくい・AgI は溶けない）につながる。単元タグ「ハロゲン化銀」を新設
  ・**ステージ番号をデータから外した**: タイトルに「ステージN：」が直書きされていて、
    途中に足すたび手で振り直す必要があった（今回も10個ずれた）。番号は**並び順から作る**
    ように変え、直書きを禁じる回帰テストを追加
  ・検証: 回帰テスト全合格（モデル25・反応ライブラリ8・UIイオン43・酸化還元13）。
    新規は「投入した AgCl が電離せず底に沈む → NH₃ 2個で溶けて錯イオンと Cl⁻ になる」
- v88（2026-07-27・**弱酸の遊離＋置き換えビュー**）: ユーザーの指示
  「酢酸と水酸化ナトリウムがちょうど中和しているブロック図から始めて、塩酸で酢酸のブロックを置き換える」。
  ・**ステージ19 酢酸ナトリウム × 塩酸**（CH₃COONa＋HCl→CH₃COOH＋NaCl）を追加。ルールは
    `{find:["H+","CH3COO-"], make:"CH3COOH"}` の1本で、種はすべて既存＝データだけで足りた。
    模式図も protonSchema が受け皿を OH⁻ に限定していないため追加実装ゼロで出る
    （左＝[Na⁺｜CH₃COO⁻]・右＝[Cl⁻｜H⁺]・中央＝CH₃COOH）
  ・**置き換えビュー（#displaceWrap）**を新設。上段に「中和ずみの組（塩基＋弱酸→水）」を描き、
    ▶ で強酸ブロックが入ってきて**席替え**する。弱酸ブロックは下段へ降り、枠が消えて
    自分の H⁺ と結びついた分子に戻る＝遊離。最後に残ったイオンどうしを点線で結んで塩を示す。
    H⁺ の総数は2個（もとの酸＋強酸）のままで、1個が水・1個が遊離した弱酸に入る＝図の中で数が合う。
    stage.displace = { from, to, base } のデータ駆動で、他の弱酸の遊離にも使える
  ・単元タグ「弱酸の遊離」を新設し、s6（Na₂CO₃＋HCl）・s10（Na₂SO₃＋HCl）にも付けて串刺しに
    （遊離した弱酸が①気体になる ②液に残る、の対比）
  ・**無限ループのバグを発見・修正**: 生成物の CH₃COOH を engine が「H⁺ の供給源」とみなして
    ばらし、また作る、を12秒で379回くり返していた。同じ分子がステージ18では反応物・
    ステージ19では生成物になるため、**そのステージの生成物は donorPartsOf でばらさない**ようにした
  ・**演出の連鎖が消える不具合も修正**: 補間の完了処理で配列を作り直していたため、
    onDone の中で積まれた次の動き（数合わせの連鎖）が捨てられていた。その場で取り除く形に
- v87（2026-07-27・**酸化還元にもブロック模式図**・最簡整数比の助言）: ユーザーの要望
  「最小公倍数でない正解に対してはアドバイスが欲しい」「酸化還元も作成してください」に対応。
  ・**schematic.js を新設**（ブロック模式図の共通描画）。中和と酸化還元は
    「1個あたり何個やりとりするかがそろって初めて過不足なく反応する」という同じ構造なので、
    図法を1つにまとめた。DOM だけを扱い、何が受け渡されるか（H⁺ か e⁻ か）の判断は持たない。
    app.js / redox.js はそれぞれ spec（左右のブロック・中央の生成物・見た目表）を組んで渡すだけ
  ・**酸化還元の模式図**: 左＝還元剤（e⁻ を出す）・右＝酸化剤（e⁻ を受け取る）。縦1行＝e⁻ 1個。
    Al は3行・Cu²⁺ は2行の高さになるので、**倍率をそろえる＝最小公倍数を探す**ことが
    ブロックの高さでそのまま見える（2:3 で 6行にそろう）。中央に生成物は置かず、
    e⁻ が左のブロックから右の**席**へ直接わたる矢印にした。埋まっていない席は点線の輪で示す。
    MnO₄⁻＋8H⁺ のように本体側の個数が多い半反応のため、core は { sp, n } を受け取り
    「8H⁺」と1つにまとめて描く
  ・**最簡整数比でないときの助言**: 「最も簡単な整数比にしよう」だけでは何をすればよいか
    分からないので、**何で割るか・割った先はいくつか**まで出すようにした。
    反応式側「係数がすべて 2 で割り切れる。2 : 4 : 2 : 4 → 1 : 2 : 1 : 2 に直そう」／
    模式図側「同じ組み合わせを 2 回くり返しているだけ。すべて 2 で割って …（1H₂SO₄ ： 2NaOH）」／
    酸化還元「どちらも 2 で割って ×4・×6 → ×2・×3 に直そう（e⁻ 12個 → 6個 でも同じ反応）」。
    共通関数 simplestRatioAdvice() に集約し、checkStageCoeffs・checkRedoxMultipliers も
    gcd と割った先を返すようにした
  ・**図の段数に上限（12組）**: 倍率を上げすぎると図が 1872px まで伸びていた（Cr₂O₇²⁻ ×9 で54行）。
    そこまで来た答えは行きすぎなので、12組ぶんで打ち切り「多すぎて図に入りきらない」と伝える。
    最大 560px（酸化還元）／684px（中和）に収まった
  ・検証: 回帰テスト全合格（モデル25・反応ライブラリ8・UIイオン40・酸化還元13）。
    新規は模式図の高さ・矢印数・空席・助言の文面と、全ステージ×係数/倍率の総当たり
    （中和77通り・酸化還元42通り）でのブロック重なり・枠外・高さ上限の検査
- v86（2026-07-26・**沈殿が横に並ぶと重なる不具合を修正**）: v85 公開後、**公開サイトの test.html が
  1件 FAIL**（`AgCl(settled) と AgCl(settled) dx=44.8 dy=0.0`）。ローカルでは通っていた＝
  着地位置が乱数で決まるため**たまにしか出ない**種類の不具合だった。
  原因は v84 で直した縦方向の裏返し: 「先客の上に積むか、隣に並べるか」の判定に**外接半径**を
  使っていた（`(p.r+q.r)*0.62`）。枠つきの沈殿は横長なので、外接半径で見ると実際より細く見え、
  隣に並べたつもりで枠が重なっていた。判定を**見た目の幅**（hw）に統一し、
  枠がわずかでもかぶるなら上に積むようにした（`|Δx| < phw+qhw+1`）。あわせて横位置の枠内クランプを
  積む相手を決める**前**に移した（あとで動かすと判定がずれるため）。
  ・**テストの判定そのものも直した**: 丸い粒に「見た目の幅・高さの箱」を当てていたため、
    斜めに接しているだけの丸い粒どうしが重なり扱いになっていた（4個→8個に増やしたら露見）。
    判定を描かれている形に合わせ、**丸い粒どうしは中心間距離／枠つきが絡むときだけ箱**に変更
  ・検証: 沈殿4個の生成を40回くり返して重なりゼロ・最小すきま2px（修正前は40回中2回重なった）。
    BaSO₄・Cu(OH)₂・Al(OH)₃・Zn(OH)₂ の各ステージでも各15回で重なりゼロ
- v85（2026-07-26・**模式図をブロック図に作り替え**）: ユーザーの指摘
  「**中和は H⁺ の授受より、H⁺ と OH⁻ の反応としたほうが理解しやすい。ブロック模式図化してパズル的にしたい**」
  を受け、v84 の2段ドット表示を、反応物1個＝1ブロックの**ブロック図**に置き換えた。
  ・左＝受け皿側（塩基）・右＝H⁺側（酸）。各ブロックは［本体イオン ＋ 価数ぶんの受け渡し粒］で、
    粒から**太い黄色の矢印**が中央の生成物（H₂O など）に集まる。相手のいない粒は色枠で残り、
    そのブロック自体も色が変わる＝**余りが見た目で分かる**
  ・**行＝生成物1個ぶん**として縦位置を決め、ブロックはその粒を囲うように高さが決まる。
    2価の塩基は2行ぶんの高さ、1価の酸は1行ぶん、と**価数が高さで見える**（ユーザー提供の図と同じ構図）
  ・**パズル操作**: ブロックをクリックで1個へらす／「＋ HCl」ボタンで1個ふやす。反応式の係数と双方向に連動
  ・model.js に **protonSchema()**（反応物を本体＋受け渡し粒に分解）と **protonBalance()**
    （係数から組の数とあまりを計算）を追加。描画はこの2つの結果だけを使い、判定ロジックは DOM 非依存に置いた
  ・2H⁺＋CO₃²⁻ のように1組に2個要るルール、NH₃ のように本体イオンを持たない受け皿にも対応。
    酸性塩ステージ（NaHSO₄）では残る H⁺ を「この課題はそれでよい」と別扱いにする
  ・**テストで実在の重なりを検出**: 1行に粒が2個並ぶ炭酸・亜硫酸ステージで、行内の粒の間隔が
    ブロックの高さより狭く、隣のブロックと4px 重なっていた。間隔をブロック高さから逆算して修正。
    全ステージ×6通りの係数（77通り）でブロックの重なり・枠外を検査する回帰テストを追加
- v81〜v84（2026-07-24・**模式図（やりとりの数）を新設**・用語修正）: ユーザーの方針
  「**ビーカー模型＝どの物質がどんな状態で存在するかの可視化／模式図＝中和・酸化還元の量関係の可視化**、
  よって両者は共存すべき。配置は右側の係数決定エリア」に沿って実装。
  ・右パネル（係数ステッパーの下）に **#schematicWrap** を追加。酸が出す H⁺ と、塩基が受け取れる数を
    向かい合わせに並べ、**係数に連動**して本数が変わる。過不足は赤リングで示し、
    「ぴったり！ 出す H⁺ 2個 ＝ 受け取れる 2個。この比が係数」と結ぶ
  ・1個あたりの数はデータから自動導出（出す＝PARTS 中の H⁺ の数／受け取る＝ルールの find に含まれる
    H⁺ の数×相手の個数）。H₂SO₄=2個出す・Ca(OH)₂=2個受け取る・Na₂CO₃=2個受け取る・NH₃=1個受け取る、
    が追加データなしで出る。H⁺ のやりとりが軸でない反応（沈殿・燃焼）では自動的に非表示
  ・**用語修正**: 半反応式のタグを「酸化／還元」→「**還元剤／酸化剤**」に（電子を出す側が還元剤、
    受け取る側が酸化剤。単なる言い換えではなく対応が入れ替わる）。関連メッセージも整合
  ・**テストが実在のバグを検出**: 沈殿を積むとき縦の間隔を外接半径で計算する一方、着地位置の制限は
    見た目の高さで行っていたため、2個目が1個目にめり込んでいた（v43 の変更に起因）。
    積み上げを見た目の高さ基準に統一して修正。あわせて重なり判定（テスト・監査とも）を
    外接半径ではなく**見た目の幅・高さ**で見るようにした（枠つきの粒は横長のため）
- v78〜v80（2026-07-24・**夜間監査ページを新設**）: audit.html / audit.js。実アプリを iframe で駆動し、
  ①全ステージを模範比で実行 ②ランダム投入ファズ（シード付きで再現可能）③酸化還元モードの倍率ランダム再生、
  を無人で回して次の不変条件を検査する:
  **原子の保存**（投入した原子数＝ビーカー内＋空気中へ逃げた気体の原子数）／**反応が止まらない**
  （seek・moveTo のまま固まらない）／**粒が重ならない**（静止中の粒どうしが半径和の4割未満に近づかない）／
  **枠外へ出ない**／模範比ならクリアする。結果は JSON 保存でき、失敗はシードで再現できる。
  これらは回帰テスト（結果の個数を見る）では拾えない「途中経過と見た目の破綻」を対象にしている。
  **初回実行で本物のバグを検出**: 模範係数より多く生成物ができると、位置が模範係数前提の計算と衝突し
  **H₂O と CO₂ が距離0で完全に重なる**（combustion-ch4-o2 に 3CH₄+3O₂）。
  → 生成物は毎回「実際にできた数」で並べ直す relayoutGasProducts に変更して修正。
  あわせて IonEq.state() に busy（段取り演出の途中か）を追加し、監査が演出中を「静止＝おしまい」と
  誤判定しないようにした。修正後は 97件すべて合格。
- v58〜v77（2026-07-24・C群アニメの詰めとメッセージ改善／ユーザー実機フィードバック多数）:
  ・**生成物は種類ごとに整列**（多いときは2列。エタンで CO₂4個／H₂O6個が別の列に）。
    気体の空間の高さを 290→310 に広げて2列ぶんの余地を確保
  ・**原子が重なる不具合を解消**（原因は2つ、実測で特定）: (1) 分子をほどいた瞬間に原子が
    すべて同じ点へ生成されていた → 分子内の位置（STRUCTURE を粒サイズに拡大）から現れるように。
    (2) 並べ先と現在位置の順序が対応せず経路が交差 → 左右の順序を保って着席。
    あわせて組む相手は**左から順**に選び、組む場所を原子2列の中間にした（待機中の原子を通り抜けない）
  ・**余分な分子をほどかない**（O₂ を8個入れても必要な数しかほどかない）。分解数を
    「1回ぶんに要る原子数 −（すでに余っている原子）」から算出
  ・**入れすぎの案内を改善**: 片方を使い切って他方が余る＝入れすぎ、と判定。さらに
    gasCanProgressByAdding() で「足して進めるか（投入上限・余りの量）」を見て、
    進めないときは「足しても比が合わない。やり直そう」と案内を切り替える
  ・**作り込んで撤回した不具合**: 移動中の粒どうしも押し離す変更（v72）で、組みに行く原子が
    押されて目的地に届かず反応が停止した。実機確認で気づき撤回（v74）。
    移動中の粒は押し離さない、を app.js にコメントで明記した
- v54〜v57（2026-07-24・簡易アニメの磨き込みと C₂H₆ 対応）: ユーザー評価「方向性はよし」を受けた調整。
  ・簡易モードで**分子を1点に重ねず、中心分子のまわりの輪に配置**（何個の分子が反応したか数えられる）。
    C₃H₈＋5O₂ で最小間隔 +8.7px の重なりなしを実測
  ・**生成物が枠からはみ出す**問題を修正。生成物の間隔を模範の総数から算出して枠内に収める
    （C₃H₈ の7分子で 左71〜右405／枠 55〜425）
  ・**ステージ22「エタンの燃焼」**（2C₂H₆＋7O₂→4CO₂＋6H₂O）を追加。1列8原子で詳細アニメの
    上限にちょうど収まる。閾値「1列8原子」は横に並べられる数から決めている（プロパンは11で簡易）
  ・原子の列は前のぶんを使い切ったら先頭から並べ直す（複数回の反応で右へはみ出さない）
  ・総なめテストの進行時間を延長（C群は1組ずつ見せるため生成物10個で約45秒かかる）
- v52〜v53（2026-07-24・大きい分子への対応方針を決定）: C₃H₈＋5O₂→3CO₂＋4H₂O のような
  係数の大きい反応をどこまでアニメ化するかをユーザーと相談し、**「大きい分子は簡易アニメ」**で決定。
  ・**投入上限を模範係数から自動算出**（`max(answer)+1`。従来は固定4で、係数5の反応は入力すらできなかった）
  ・**簡易モード**（`useSimpleGas`／stage.animMode="simple"）: 原子にほどかず**分子のまま**
    反応式まるごと1組で組み替える。判定は「1列に並ぶ原子が8個を超えるか」で自動（明示指定も可）
  ・ステージ22「プロパンの燃焼」を追加（原子表示だと上列11・下列10で並びきらないため簡易モード）
  ・目標文も「分子を組み替えて…」に切り替え
  方針: **原子レベルの詳細アニメは小分子（各列8原子以内）に限定**。それを超える反応は簡易アニメ。
  いずれの場合も**数合わせビュー（PARTS で原子に分解）は全反応で成立**するので、係数の学習は損なわれない。
- v49〜v51（2026-07-24・動作確認フィードバック 第3・4弾）: 「変化を追えること」と視認性を軸に調整。
  ・OH⁻ は O と H を水平に（斜め上だと電荷バッジと重なっていた）
  ・**錯イオンの配位は □（四隅）**。平行四辺形も試作したがユーザー評価で取りやめ（COORDINATION_OFFSETS）。
    中心と配位子の輪郭を少し重ねて結合を示す（硫酸イオンと同じ流儀）。CH₄ も同様に重ねた
  ・図内のフォントを全体的に拡大（原子8→10、構成イオン8.5→10.5、式12→14.5、電荷バッジ10→12）
  ・**沈殿の再溶解**: ほどけたら構成イオンを左右に分け、**沈殿にくっついていたイオンを消さず**
    そのまま配位子に使う（Al(OH)₃ の OH⁻3個）。配位子は1個ずつ定位置へ入り、位置が揃ってから
    錯イオンに差し替えるので**見た目がジャンプしない**。沈降中は次へ進まない（過程をスキップしない）
  ・**反応の場に傍観イオン・他分子を近づけない**（reactionZone。水溶液・気体の両方）
  ・**C群**: 反応物1を1分子まるごと上列、必要数の反応物2を下列にほどいて**2列整列**→左から順に
    1分子ずつ生成。ほどく前に反応成立を確認し、相手がいないのに分子を壊さない。
    1種4分子までの投入制限＋整列間隔の自動圧縮で枠外へのはみ出しを防止
  ・**生成物は下段に取り出す方式で確定**（ユーザー判断。反応物：生成物の個数比＝係数が数えやすい。
    混合気体として泳がせる案は、物質量を扱う将来の題材で再検討）
- v43〜v48（2026-07-24・動作確認フィードバック 第2弾）: ユーザーが実機で確認して出した4点を反映。
  (1) **沈殿をビーカーの底まで沈める**（枠が横長だと外接半径では浮くので、見た目の高さ hr を基準に）。
  (2) **錯イオンを対称配置**（COORDINATION）: [Cu(NH₃)₄]²⁺=四隅の□（平面四配位）、
      [Zn(OH)₄]²⁻/[Al(OH)₄]⁻=上下左右の◇（正四面体）、[Ag(NH₃)₂]⁺=左右（直線）。
  (3) **C群を段階的な振り付けに**: 整列（CH₄上段・O₂下段）→1.4秒→1組ずつ・速度半分で組み替え。
      ばらけた原子は3段目に整列して待機（泳がせない）、生成物は4段目へ。演出用スケジューラ
      （simTime/schedule）を app.js に追加。**食い散らかし防止**として、ルール選択を
      「すでにばらけている原子を多く使えるほう優先」（freeUsage）に変更 — 2CH₄+2O₂ で CO₂ を2個
      作り H を8個取り残す不具合を修正し、CH₄ 1分子ぶんだけ反応して残りは分子のまま、にした。
  (4) **沈殿の再溶解を6段階で見せる**（runDissolveSequence）: 沈殿を持ち上げ→配位子が近づく→
      枠がほどけて中心イオンと OH⁻ に→OH⁻ が横へ離れる→錯イオン形成→OH⁻ が泳ぎだす（約4.5秒）。
      **実装中に無限ループを作り込んだ**（演出中の粒がまだ反応可能と判定され同じ粒で何度もグループ生成）
      → 粒に busy フラグを持たせ isReactive から除外して解消。取り残されるイオンが無い反応
      （Al(OH)₃＋OH⁻）で落ちる不具合もテストが検出し修正。
  UIテスト3本追加（イオン反応 32→34）。全PASS。
- v41〜v42（2026-07-24・動作確認フィードバック反映）: ユーザーが実機確認して出した4点を修正。
  (1) **枠の形で状態を区別**: 沈殿・錯イオンを「もとの構成イオンが枠に入った姿」で描く
      （COMPOSITION／SOLID_SPECIES。**〇枠＝とけているイオン・□枠＝沈殿**、中に Ag⁺・Cl⁻ や
      Cu²⁺・NH₃×4 を並べる）。原子の房より「どのイオンが組んだか」が直接見える。
  (2) **気体の空間はビーカーでなく□**（閉じた四角い容器）。粒の可動域も箱全体（GAS_AREA）に。
  (3) **C群は投入時にばらさず、反応の瞬間に組み替える**。弱電解質用の「必要になったら分かれる」
      仕組みを donorPartsOf/breakApart に一般化し、電離（酢酸）と原子化（CH₄・O₂）を同じ振る舞いに。
      過不足があればばらけた原子が漂う（ユーザー方針）。残った分子は intermediates で未完了扱い。
  (4) **電荷の二重表示を解消**: 円内の式から右肩の電荷を外し（stripCharge）バッジのみに
      （Na⁺ が「Na⁺」＋「＋」と重複していた）。下付き（SO₄ の ₄）は残す。
  UIテスト2本追加（イオン反応 31→32）。全PASS。
- v40（2026-07-24・C群 分子反応に着手）: ステージ20「水素の燃焼 2H₂+O₂→2H₂O」・21「メタンの燃焼
  CH₄+2O₂→CO₂+2H₂O」を追加。**最後の未着手エンジン C群**を、既存の A群エンジンの一般化で実現:
  (1) **ATOMIZATION**（分子→原子の分解表）を新設し、dissociateMolecule が 電離→原子化→分子のまま の
      順に解決。(2) **stage.parts / partsOf()** でステージごとに数合わせの分解を上書き（同じ H₂O でも
      A群は H⁺＋OH⁻・C群は H＋H＋O）。(3) `phase:"gas"` で水面を描かず「気体の空間」表示にし、
      目標文も「原子を組み替えて…」に。新種 O₂・CH₄・原子 H/O/C。
  **見つけて直した不具合**: 気体ステージで CO₂ が泡になって逃げてしまい、原子が消えたように見えた
  （BUBBLE_SPECIES が phase を見ていなかった）→ 気体の空間では泡にしない。原子保存が主題の群なので重要。
  UIテスト1本追加（イオン反応 30→31）。テストが「reactions.json に未登録」も検出。全PASS（反応25件）。
- v39（2026-07-24・M4 弱塩基）: ステージ19「アンモニア×塩酸（1:1）」を追加。NH₃ は**OH⁻ を出すのではなく
  H⁺ を受け取る**（ブレンステッドの塩基）形で実装＝`find:["NH3","H+"] → NH4+`。この形なら溶媒の水を
  持ち出さずに原子・電荷が保存する（NH₃＋H₂O⇌NH₄⁺＋OH⁻ の平衡表現は水を要するため今後の課題）。
  数合わせでは PARTS で NH₄Cl を「NH₃＋H⁺＋Cl⁻」に開き、H⁺ を受け取った姿として見せる。
  新種 NH₄⁺（房・env）・NH₄Cl。**弱電解質の保存検査**（原子/電荷が保存し、強電解質の電離表と
  二重定義しない）をモデルテストに追加（モデル 20→21）。UIテスト1本追加（イオン反応 29→30）。全PASS。
- v38（2026-07-24・M4 弱酸の部分電離）: ステージ18「酢酸×NaOH（1:1）」を追加し、**弱電解質**の
  仕組みを新設（WEAK_ELECTROLYTES）。酢酸は水に入れても**分子のまま溶け**（.weak のゆらぎ表示）、
  反応で H⁺ が必要になったときに初めて電離する＝「H⁺ が使われると残りがさらに電離して補う」
  ルシャトリエの原理の表現。dissociateMolecule で弱電解質は分割せず分子のまま浮かせ、
  launchGroups の findReactant が不足分を電離で供給する。**canSatisfy を先に判定**し、
  成立しない反応のために分子を電離させてしまうのを防ぐ（相手がいなければ電離しない）。
  intermediates に CH₃COOH を入れ、分子が残る間は未完了に。数合わせでは PARTS で H⁺＋CH₃COO⁻ に
  開いて見せる（中和に必要な塩基の数は電離度でなく酸の総量で決まる、という要点）。
  新種 CH₃COOH（房）・CH₃COO⁻（房）・CH₃COONa。UIテスト1本追加（イオン反応 28→29）。全PASS。
- v37（2026-07-24・両性水酸化物）: ステージ16「AlCl₃×NaOH（1:4）」・17「ZnSO₄×NaOH（1:4）」を追加。
  **同じ NaOH が少量なら沈殿・過剰なら再溶解**（Al³⁺: OH⁻3個で Al(OH)₃↓ → 4個目で [Al(OH)₄]⁻／
  Zn²⁺: 2個で Zn(OH)₂↓ → 合計4個で [Zn(OH)₄]²⁻）という両性の本質を体験できる。
  v36 で作った仕組み（isReactive の沈殿参加・反応の連鎖・producedCount・intermediates）に
  **データを足すだけで実現**＝エンジン改修ゼロ。新種 [Al(OH)₄]⁻・[Zn(OH)₄]²⁻・AlCl₃・ZnSO₄。
  reactions.json の両性2反応を全体反応の式に更新して playable 化（21反応中20が遊べる状態に）。
  UIテスト2本追加（イオン反応 26→28）。**SPECIES に Al³⁺ を重複定義しかけたのを検査で発見・除去**
  （既に酸化還元用に定義済みだった）。全PASS。
- v36（2026-07-24・沈殿の再溶解＝二段変化）: ステージ15「CuSO₄×NaOH×NH₃」を追加。NaOH で青白沈殿
  Cu(OH)₂ ができ、続けて NH₃ を加えると溶けて [Cu(NH₃)₄]²⁺ になる（無機化学の重要パターン）。
  エンジンを3点拡張:
  (1) **isReactive** を新設し settled/sink（沈殿）も反応に参加できるように（従来ステージは沈殿種が
      どの find にも無いので挙動不変）。ドラッグの相手判定も同関数に統一。
  (2) **反応の連鎖**: launchGroups を doReact から切り出し、maybeEvaluate で「生成物がさらに反応できるなら
      続ける」ように（沈殿生成→再溶解が一度の「反応させる」で進む。粒を消費するので必ず停止）。
  (3) **producedCount** を導入し、生成物として放出された種（再溶解で戻る OH⁻ など）を「余ったイオン」と
      誤判定しないように。あわせて stage.intermediates（溶かすべき沈殿）が残る間は未完了とし、
      「Cu(OH)₂ が残っている。溶かす試薬を加えよう」と促す（沈殿だけ作ってクリアできる穴を塞いだ）。
  goal 文は錯イオンを沈殿より優先し「沈殿を溶かして 錯イオン X をつくる」。UIテスト2本追加（イオン反応 24→26）。
  **テストが検出した不具合2件**（reactions.json の species 過不足／沈殿生成だけで完了扱い）も同時に修正。全PASS。
- v35（2026-07-24・錯イオン生成をプレイ可能化）: ステージ13「CuSO₄×NH₃（1:4）」・14「AgNO₃×NH₃（1:2）」を
  追加し、reactions.json の該当2反応を playable 化（index の「▶このパズルを遊ぶ」→ ?rxn で直行）。
  新ルール kind:"complex"（既存の多重集合ルールエンジンをそのまま利用＝生成物は浮遊）。
  **NH₃ は電離しない分子**として扱えるようにモデル/UIを一般化: PARTS を「電離表を土台に手書きで上書き」
  方式へ変更（錯塩は配位子まで開いて数合わせできる）、simulateFormation の反応物プールを DISSOCIATION→PARTS、
  app.js の dissociateMolecule は電離表が無ければ分子のまま溶かす。stageGoalText に錯イオンの目標文、
  STAGE_TAGS に 錯イオン/配位。**発見した不具合**: STYLE 未登録の種（NH₃）で buildRecombine が例外を投げ、
  onCoeffChange が中断して係数が正解にならなかった → STYLE 追加＋`STYLE[psp] || MOLECULE_STYLE` の
  フォールバックで再発防止（総なめテストが検出）。UIテスト2本追加（イオン反応 22→24）。全PASS。
- v33（2026-07-24・錯イオン生成を参照エントリで先行）: 新分類「錯イオン生成」を index に追加。
  Cu²⁺+4NH₃→[Cu(NH₃)₄]²⁺（深青）と Ag⁺+2NH₃→[Ag(NH₃)₂]⁺（銀鏡の試薬）をアンミン錯体の
  molecular 参照として reactions.json に。animationType="complex-ion"（未実装）・playable:false。
  新種 NH₃・[Cu(NH₃)₄]SO₄・[Ag(NH₃)₂]NO₃。index で「nh3」検索・型フィルタ「錯イオン生成(2)」を確認。
  設計の「参照先行」で網羅を広げ、complex-ion アニメ（配位・沈殿再溶解・色変化）は今後。全PASS。
- v32（2026-07-24・溶液中酸化還元 第3弾 MnO₄⁻×シュウ酸＋酸化アニメの一般化）: rs3「過マンガン酸
  カリウム × シュウ酸（5:2）」を追加。**酸化側が気体（CO₂）を複数生む**新パターンに対応するため、
  oxidizeAtom を「多生成物＋気体の泡」に一般化（BUBBLE_SP。金属モードと後方互換）、transformUnit の
  泡判定も一般化。新種 C₂O₄²⁻/H₂C₂O₄、半反応式 oxalate_ox（C₂O₄²⁻→2CO₂+2e⁻）、酸化数 C+3/+4。
  reactions.json に molecular 参照（2KMnO₄+5H₂C₂O₄+3H₂SO₄→…）を redoxStage:"rs3" で追加。
  実機で 紫→無色＋CO₂10個が泡で逃げ・Mn²⁺2/H₂O8・クリアを確認。UIテスト追加（酸化還元 10→11）。全PASS。
- v31（2026-07-24・溶液中酸化還元 第2弾 Cr₂O₇²⁻＋色の一般化）: REDOX_STAGES に rs2「Cr₂O₇²⁻×Fe²⁺
  （6:1）」を追加（既存の Cr2O7_red 半反応式・OXIDATION・色を流用＝データ追加のみ）。溶液色を
  「残る酸化剤だけ」から「有色種の重み付けブレンド」に一般化（SOLUTION_TINT：MnO₄⁻紫・Cr₂O₇²⁻橙・
  Cr³⁺緑。Fe/Mn²⁺ は淡色として除外）→ MnO₄⁻ は紫→無色、Cr₂O₇²⁻ は橙→緑 が自然に出る。
  reactions.json の Cr₂O₇ 参照エントリを redoxStage:"rs2" で playable 化。rs2 の UIテスト追加
  （6:1で緑・生成物一致）＋総なめに自動編入。実機で #e3a465→#6fb687（橙→緑）を確認。全PASS
  （酸化還元 9→10）。
- v30（2026-07-24・Phase 4 ステージ3: インデックス↔酸化還元モードの連携）: redox.js に ?rxn ディープリンク
  受けを追加（app.js と同型）。reactions.json の KMnO₄ 参照エントリに redoxStage:"rs1" を付けて playable 化。
  library-ui は redoxStage を持つ反応の「▶遊ぶ」を redox.html?rxn=<stage> へ振り分け（それ以外は index.html）。
  検証テストに「redoxStage は実在の REDOX_STAGE を指す・playable であること」を追加。
  実機で「kmno4」検索→「▶酸化還元モードで見る」→redox.html?rxn=rs1→ステージ5 の一連を確認。全PASS。
- v29（2026-07-24・Phase 4 ステージ2: 溶液中酸化還元アニメ本体）: redox.js に溶液モード（mode:"solution"）を
  追加し、REDOX_STAGES に rs1「MnO₄⁻×Fe²⁺」を追加。既存の e⁻ プール／単位（unit）機構を**板なしで流用**:
  酸化側 Fe²⁺ は溶液中の浮遊粒として e⁻ を出し Fe³⁺ に、還元側 MnO₄⁻＋8H⁺ が集合して e⁻ を受け取り
  Mn²⁺＋4H₂O に。**溶液全体の色を残留 MnO₄⁻ 量で更新**（紫→無色＝滴定終点）、粒は SPECIES_COLOR、
  酸化数（Mn+7→+2・Fe+2→+3）を円内表示。判定は既存どおり個数のみ（e⁻不足＝紫が残り未クリア）。
  併せて **redox.html を v12 のまま放置していたキャッシュバスターを現行に更新**（CDN で旧コード配信の恐れ）。
  UIテスト2本追加（5:1成立＋色が無色化・1:1で紫残留）＋総なめに rs1 が自動編入。全PASS
  （モデル20・ライブラリ8・UIイオン22・酸化還元9）。次は reactions.json との playable 接続と Cr₂O₇ 等。
- v27（2026-07-24・Phase 4 実装ステージ1: 溶液中酸化還元のモデル拡張・アニメなし）: DESIGN_redox.md の
  設計に沿ってモデルだけ先に拡張（アニメ無しで検証が回る安全な増分）。HALF_REACTIONS に MnO4_red
  （MnO₄⁻+8H⁺+5e⁻→Mn²⁺+4H₂O）・Cr2O7_red・Fe2_ox を追加。OXIDATION に Mn+7/+2・Cr+6/+3・Fe+3・
  H₂O(H+1,O−2) を追加。SPECIES_COLOR 新設（MnO4⁻紫→Mn²⁺無色・Cr2O7²⁻橙→Cr³⁺緑・Fe²⁺淡緑→Fe³⁺黄褐）。
  既存の半反応式保存・酸化数（合計=電荷/Δ=e⁻数）テストが新半反応式を自動検証。溶液中の combineHalves
  つり合い（MnO₄⁻×Fe²⁺ 1:5、Cr₂O₇²⁻×Fe²⁺ 1:6）と色データのテストを追加（モデル 18→20）。
  REDOX_STAGES は未追加＝redox.js は無改修・ゲームプレイ不変。次ステージ2で redox-solution アニメ本体。
- v26（2026-07-24・網羅拡大: 溶液中酸化還元を参照エントリで先行）: reactions.json に KMnO₄×FeSO₄×H₂SO₄
  と K₂Cr₂O₇×FeSO₄×H₂SO₄ を追加（animationType=redox-solution・playable:false＝参照のみ）。
  アニメ未実装でも分子反応式・イオン反応式・酸化数・色変化の解説が index に載り、「kmno4」等で検索可能に
  （設計の「参照先行」を実証）。SPECIES/DISSOCIATION に Mn/Cr/Fe(Ⅲ) 系（MnO₄⁻・Mn²⁺・Cr₂O₇²⁻・
  Cr³⁺・KMnO₄・K₂Cr₂O₇・FeSO₄・MnSO₄・K₂SO₄・Fe₂(SO₄)₃・Cr₂(SO₄)₃）を追加。既存の電離表保存テストと
  反応ライブラリ検証（原子/電荷保存・species逆引き）で自動担保。実機で「kmno4」検索・準備中表示・
  酸化還元/酸化還元滴定フィルタを確認。全PASS。次は Phase 4（redox-solution アニメ本体）の設計＋実装。
- v25（2026-07-24・Phase 2 仕上げ・難易度/単元フィルタ）: 反応インデックスに難易度（★1〜5・件数つき）と
  単元（byUnit・多い順）のフィルタ群を追加。型/塩/難易度/単元＋物質検索の AND 絞り込みに。
  単元「中和」(7) と 型「中和」(6) の差（s8=型は沈殿・単元に中和）で、軸が交差する設計が見える。全PASS。
- v24（2026-07-24・反応ライブラリ Phase 2 増分1・インデックス/検索UI）: library.html＋library-ui.js。
  window.IonLib を使い、反応の型・塩の分類フィルタ（件数つきチップ）＋物質検索（^/大小無視の部分一致）＋
  各反応の反応式整形・分類バッジ・難易度★・イオン反応式・解説・「▶遊ぶ」（?rxn=id ディープリンク）。
  app.js に ?rxn=<id> でステージを開くディープリンク受けを追加。index.html に「🔎反応インデックス」導線。
  検索/整形は純関数 matchesQuery/formatEquation として library.js に置きテスト（反応ライブラリ 7→8）。
  実機で 12件表示・co3/h2so4 検索・酸性塩フィルタ・?rxn=s11→ステージ11 を確認。
- v23（2026-07-24・反応ライブラリ Phase 1 増分2・ローダ＋逆引き索引）: library.js を新設。
  `buildReactionIndex`（純関数：byId／bySpecies＝物質逆引き／byType／bySalt／byUnit／allSpecies）と
  `loadReactionLibrary`（fetch, cache:no-store→window.IonLib）。index.html は起動時に裏読み（app.js が
  非致命で握りつぶす＝file:// でもゲームプレイ継続＝両立）。設計原則の「JSON化＝サーバー必須」記述を更新
  （index/検索はサーバー必須・ゲームプレイは STAGES 両立）。逆引き検証テスト1本追加（反応ライブラリ 6→7）。
  実機で window.IonLib に12反応・分類/物質の索引が載ることを確認。次は Phase 2 の検索UI。
- v22（2026-07-24・反応ライブラリ Phase 1 増分1・両立/アプリ挙動不変）: reactions.json を新設し
  既存 A群12反応を新スキーマへ移行（reactants/products/coeffs/netIonic/classes/units/species/
  animationType/rules/note/difficulty/playable）。既存データからブラウザで正確に生成。
  検証ハーネス「反応ライブラリ」6本を追加（fetch）：id一意・coeffs で原子/電荷保存＆最簡比・
  species が全登場種を過不足なく含む（検索逆引きの穴を検出）・分類/アニメ/難易度が enum 内・
  **STAGES との同一性（両立期間の担保）**。アプリはまだ STAGES で動く（reactions.json は未接続）。
  次: ローダ実装＋サーバー必須化の記述更新（設計原則）、その後インデックス/検索UI（Phase 2）。
- 2026-07-24（設計・アプリ変更なし）: [DESIGN_reaction_library.md](DESIGN_reaction_library.md) を作成
  （ユーザーと合意）。反応の網羅・分類/物質検索・アニメタイプ管理・KMnO₄ 等の溶液中酸化還元の上位設計。
  データは reactions.json へ移行（fetch＝サーバー必須化）、ライブラリ層とアニメ層を分離し
  未実装アニメは参照エントリ扱い、という方針を文書化。着手順は「設計書→Phase1データ化」。
- v20（2026-07-24・単元タグ＋文言微修正）: 塩の分類（正塩・酸性塩）と反応の型（中和・沈殿・
  気体発生）を単元タグとして各ステージに付与（STAGE_TAGS）。目標バナー下に小さなピルで表示、
  酸性塩タグは暖色で強調。「アプリを教科の枠に内包させずステージ横断の単元づけで表す」方針の足場。
  モデルテストで saltGoal↔酸性塩タグの整合を検証。s11 成功メッセージ／doneNote の NaHSO₄
  重複を詰めて簡潔化。全PASS（モデル18・UIイオン22・酸化還元7）。
- v19（2026-07-24・全ステージに目標バナー）: 全12ステージを「目標の○をつくる」枠に統一。
  ステージ種別から目標文を自動生成（stageGoalText）：中和→「ちょうど中和して 塩 X」、
  沈殿→「沈殿 X↓」、気体→「気体 X↑ を発生」、酸性塩→saltGoal から「酸性塩 X」。
  酸性塩の目標だけ暖色（.goal.acid）で「ふつうの中和と違う」と際立たせる。手書きの
  ステージ文言を増やさず種別から導出。UIテスト1本追加（全ステージ目標＋種別文言）、全PASS。
- v18（2026-07-24・酸性塩 第2弾 NaHCO₃・二段中和）: s12「Na₂CO₃×HCl（酸性塩）」を追加。
  新ルール `H⁺＋CO₃²⁻→HCO₃⁻`（部分プロトン化・泡なし）で、1:1 なら HCO₃⁻ ができて
  NaHCO₃＋NaCl に。酸を足しすぎると HCO₃⁻ がもう1個 H⁺ を受け取り CO₂＝既存ステージ6の全体反応、
  という二段中和のつながりを doneNote で明示。saltGoal を複数の塩に対応するよう一般化
  （`ions`＝成功時に残るイオンの多重集合の整数倍で判定、`label`＝目標の酸性塩、`overNote`＝
  外したときの助言）。s11 も新形式に移行。新種 HCO₃⁻（房 env つき）・NaHCO₃。
  UIテスト2本追加（1:1成立・酸過剰で未達）、全PASS（モデル17・UIイオン21・酸化還元7）。
- v17（2026-07-24・出題タイプ拡張: 目標の塩＝酸性塩）: 「目標の塩をつくる」課題を導入
  （ユーザーと合意）。ステージに `saltGoal` を持たせ、評価を「完全中和（余りゼロ）」から
  「塩基を使い切り、残ったイオンが目標の塩を構成する」に一般化（saltGoal 無しの既存ステージは
  挙動不変）。第1弾は s11「H₂SO₄×NaOH（酸性塩）」: 1:1 で NaHSO₄（H⁺ が1個残る＝酸性）、
  1:2 だと正塩 Na₂SO₄ になり目標未達、と比で生成物が変わることを体感。目標バナー・
  投入数の大表示（matched）と連動。新種 NaHSO₄（SPECIES/STRUCTURE房/PARTS=Na⁺+H⁺+SO₄²⁻）。
  evaluateSaltGoal・multipleOf を追加。UIテスト2本（1:1成立/1:2正塩・係数クリア）追加で全PASS。
  次候補: NaHCO₃（Na₂CO₃+HCl 1:1、H⁺+CO₃²⁻→HCO₃⁻ の部分プロトン化ルールが要る二段中和）、
  正塩ステージにも目標表示、塩の分類（正塩・酸性塩）の単元タグ。
- v15（2026-07-24・M1.6 チュートリアル文言）: labPane 先頭に折りたたみ式の
  「❓ 遊び方（4ステップ）」パネルを追加（native `<details>`。初期は開・localStorage で開閉記憶）。
  各ステージの intro が示さないアプリ全体の学習ループ（入れる→反応→ちょうどを探す→式を完成）を明示。
- v14（2026-07-24・M1.6 モバイル対応）: パネルは既に flex-wrap で縦積み・横スクロール無し
  だったが操作系が指に小さかったため、max-width:560px のメディアクエリでタップ標的を拡大
  （投入/反応ボタン44px・ステージ丸40px・係数ステッパー40×42px）。ドラッグはポインタイベント＋
  touch-action:none で既にタッチ対応。デスクトップの2カラムは不変。
- v13（2026-07-24・M1.6 粒子ドラッグ操作）: ビーカー内のイオンをドラッグして相手（反応ルールの
  相方）に重ねると、その1組だけを反応させられる（一括の「⚡反応させる」に対し「1個の H⁺ と
  1個の OH⁻ が H₂O になる」を手で確かめる体験）。Pointer Events、クライアント⇔SVG座標は
  getScreenCTM 経由、つかむと影・相方は黄点滅・重なると緑グロー。多重集合ルール（2H⁺+CO₃²⁻）は
  相方に重ねると近傍から残りを寄せ集める。傍観イオンはドラッグ不可。makeGroup を doReact と共通化。
  IonEq.dragReact フックと UIテスト3本を追加（総UIテスト10→13）。
- 2026-07-24（構成統合・アプリ変更なし）: 開発を親リポジトリ（chem-assembler.github.io）の
  `ion-equation/` サブディレクトリに一本化。旧「別フォルダ `../IonEquation/` で開発→8ファイル手動コピー」
  方式を廃止（コピー漏れ・バージョン不一致・remote なしバックアップ欠如の解消）。姉妹 Chem-Assembler と
  同じ「github.io リポジトリ内で直接開発」モデルに揃えた。開発ドキュメント（CLAUDE.md・DEVELOPMENT.md・
  DESIGN_*.md）もこのディレクトリへ移設。旧 IonEquation フォルダは退役（DEPRECATED マーク）。
- v12（2026-07-23）: ステージパック。イオン反応 s7〜s10
  （HNO₃×KOH／H₂SO₄×Ba(OH)₂=中和+沈殿の2ルール同時／CuSO₄×NaOH=Cu(OH)₂青白沈殿・1:2ルール／
  Na₂SO₃×HCl=H₂SO₃経由のSO₂発生）、酸化還元 r4（Al×Cu²⁺、倍率2:3）と
  半反応式ライブラリ Mg/Fe/Al を追加。チップ表示に未登録種フォールバックを追加。
  **すべてデータ追加のみで動作**（エンジン変更なし）。総なめテストが自動で回帰対象化。
  以後の同種のステージ追加は Opus 4.8 の守備範囲（CLAUDE.md の使い分け方針）。
- v11（2026-07-23）: 全ステージ総なめの汎用UIテスト（データ駆動）。ステージを足すと
  自動的に「模範比で投入→反応→係数→数合わせ→クリア」が検証される。コンテンツ拡充の足場。
- v10（2026-07-23）: 粒子の重なり防止。浮遊粒子は毎ステップ位置補正で押し離し
  （沈殿・析出などの固定物は動かさない）、沈殿は底または先客の上に着地して山に積もる。
  両モードに適用。UIテストで「浮遊+沈殿の全ペアが非重なり」を検証。
- v9（2026-07-23）: 房表示（原子クラスタ）。多原子イオン=元素色の原子＋包み円＋全体電荷バッジ、
  分子=裸の原子クラスタ、沈殿=イオンがくっついた姿、投入分子=電離前の房（落下中も表示）。
  model.js に STRUCTURE 表を追加し「房の内訳=SPECIES の組成」を機械検証。
  単原子イオンは従来表示。数合わせビューは記号カプセルのまま（個数の見やすさ優先）。
  粒へのホバーツールチップ（化学式＋名称）も追加。redox モード（H₂ 等）への適用は未着手。
- v8（2026-07-23）: 酸化数表示＋半反応式の単体再生（ユーザーと設計合意の上で実装）。
  (1) 酸化数はその反応で**変化する原子だけ**、**粒の円の中**（記号の直下）に表示。
  実電荷の丸バッジ（数字先・右上）と酸化数（符号先・円内/式直下）を空間と記法で区別。
  表示対象は OXIDATION 表と oxChangeOfHalf() から自動導出（手動フラグなし）。
  不変条件テスト: 酸化数合計=電荷、Δ酸化数=e⁻ 数。半反応式・イオン反応式の
  項の直下にも酸化数タグ（.oxtag）。変化の瞬間は黄リングでフラッシュ。
  (2) 各半反応式に「▶ 単体」ボタン。酸化単体=板の原子だけ、還元単体=e⁻ を
  あらかじめ板にストック（電池で導線から来る e⁻ の布石）。
  テスト: モデル16本＋UI16本（酸化還元6本）全合格。
- v7（2026-07-23）: M5 酸化還元モード（B1）。redox.html/redox.js 新設（model.js・style.css 共用）。
  半反応式を部品として提示し、倍率 ×a・×b がビーカー配置（板の原子 a 個・溶液イオン b 単位）に
  連動。再生で e⁻（黄粒）が板にたまり→イオンが受け取って析出（金属樹）／H₂ 泡。
  e⁻ 余り＝赤点滅、不足＝待ちぼうけで失敗を表現。ぴったり＋最簡比でクリアし、
  足し合わせ（e⁻ 打ち消し線）→イオン反応式を表示。モデルに HALF_REACTIONS／
  checkRedoxMultipliers／combineHalves を追加（e⁻ は atoms 空・charge −1 の特別種）。
  テスト: モデル15本＋UI13本（酸化還元3本含む）全合格。
- v6（2026-07-23）: M3 気体発生。反応エンジンをペア方式から**グループ方式**に一般化
  （rule.find が多重集合可。全員が集合地点にそろってから合体）。rule.via で中間体
  （H₂CO₃）を700ms表示→分解、make は複数生成物の配列可。CO₂ は泡モードで浮上し
  水面で逃げる（escaped として集計・チップ表示）。ステージ6（Na₂CO₃+2HCl）追加。
  数合わせビューは gasGroup で H₂O+CO₂ を「H₂CO₃ が分かれてできる組」の1列に統合。
  違う係数を入れると「同じ数になるはず」と指摘。
- v5（2026-07-22）: M2 沈殿反応。反応処理をステージのルール表
  `rules: [{find, make, kind}]` 駆動に一般化（combine=浮遊 / precipitate=沈降・堆積）。
  ステージ4（AgNO₃+NaCl→AgCl↓）・5（BaCl₂+Na₂SO₄→BaSO₄↓+2NaCl）追加。
  数合わせビューは PARTS に AgCl・BaSO₄ を足すだけで無変更で対応。
  内部名を中和専用から汎用に変更（neutralDone→reactionDone、waterMade→madeCount）。
- v1（2026-07-22）: M1 初版。3ステージ（HCl+NaOH / H₂SO₄+NaOH / HCl+Ca(OH)₂）、
  model.js 純ロジックテスト。
- v2（2026-07-22）: M1.5 数合わせビュー。反応式の直下に係数ぶんの粒を並べ、
  「⇄ 組み変える」で左辺の粒が右辺の枠に飛び移る。余り（赤リング）・不足（赤点線枠）を明示。
  H₂O は表示専用の分解表 PARTS で H⁺+OH⁻ に対応づけ（電離ではない旨をコード注記）。
- v4（2026-07-22）: DESIGN_reaction_types.md（反応3群分類・アニメ設計・優先順位）を作成。
  iframe 駆動の UIテストを test.html に導入（6本。IonEq.advance で決定論的に駆動）。
  以後、手動でのブラウザ検証は「新機能の初回確認」のみとし、既存機能はUIテストが守る。
- v3（2026-07-22）: 数合わせビュー改良（ユーザーフィードバック反映）。
  (1) **左辺の係数だけで試せる**方式に変更（発見学習: 組み変え結果の個数を右辺係数に写す）。
  (2) 移動は弧軌道・2フェーズ（H⁺+OH⁻→H₂O が先、塩への整列が後。MECH_FIRST 定数で反転可）。
  (3) 左辺不備→イオンが余る（赤リング）、右辺不備→「できた個数≠入力係数」をラベル赤・
  あふれ枠赤・空き主張枠赤点線で表現。simulateFormation を model.js に追加（純ロジック）。
