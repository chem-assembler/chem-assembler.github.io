# M6: 酸化剤×還元剤 組み合わせシミュレーターの設計（2026-08-06）

`ion-equation/DEVELOPMENT.md` ロードマップの **M6「自由組み立てモード」**の第1弾。
収録済みの14ステージを順に解くのではなく、**酸化剤と還元剤を自分で選んで組み合わせ、
反応するかどうかを確かめる**モード。着想は Gemini のレビュー
（`docs/PLAN_stage_nav_and_combinatorial_matching.md` 【フェーズ2】。**`docs/` は追跡外**の
手元メモなので、clone しただけの人の手元には無い）。本書はそれを実装可能な粒度に落とし、
**化学として言い切ってよい範囲**を決める ＝ .gitignore が言う「レビューから決まった方針は
追跡されるファイル側に書き写すこと」の実行にあたる。
**着想から変えた点（採らなかった案）は §8 に、判断を仰ぐ論点は §9 にまとめてある。**

設計の型は `ion-equation/DESIGN_redox.md`（半反応式×倍率）と
`ion-equation/DESIGN_battery_electrolysis.md`（電極を選ばせる＝発見学習）を踏襲する。
とくに B3 で決めた3原則をそのまま引き継ぐ:

- モデルは既存の枠を流用する（`HALF_REACTIONS` × `checkRedoxMultipliers` → `combineHalves`）。
  拡張は「データ追加」と「アニメ／画面の新規」に閉じる
- 判定は個数のみ。座標は見た目専用
- **判断できない反応は候補に出さない**

---

## 1. 何を解決するのか / 何を解決しないのか

### 解決すること

1. **「酸化剤」「還元剤」が物質の属性ではなく、相手との関係で決まる**ことを操作で体験させる。
   いまの14ステージは組み合わせが固定なので、この相対性が体験にならない
   （B3 が電池で「Cu は Zn と組めば正極、Ag と組めば負極」を見せるのと同じ狙いを、
   溶液中の酸化還元でやる）
2. **「反応しない」を、エラーではなく学習内容として返す**。なぜ起こらないかを、
   理由の種類ごとに言葉で説明する
3. 収録済み14ステージへの**入口を増やす**。自分で選んだ組み合わせがステージと一致したら、
   そのステージの解説へ橋を架ける

### 解決しないこと（非目標）

- **標準電極電位の数値を出すこと**。B3 §6 の非目標をそのまま継ぐ。
  数値を出すと「イオン化傾向の暗記」が「電位の暗記」にすり替わるだけで、高校範囲でも要求されない
- **反応の速さ・実際に観察できるかの判定**。梯子（§2）が語れるのは向きだけで、
  速いか遅いかは語れない。だから **「反応する」と言い切るのは教科書が扱う組み合わせに限る**（§2-5）
- **半反応式そのものをユーザーに組ませる練習**。`DESIGN_redox.md`「将来」に残る別テーマ
- **酸×塩基版**（Gemini 提案の3番）。§8-5 に採らなかった理由
- 電池・電気分解（B3 の担当）。自由組み合わせは**1つのビーカーの中**の話に限る

---

## 2. 組み合わせの判定モデル（本設計の核心）

### 2-1. 現物の棚卸し（憶測を混ぜないための出発点）

`ion-equation/model.js` を読んだ結果、いまあるもの／無いものは次のとおり。

| あるもの | 実体 |
|---|---|
| `HALF_REACTIONS` | **24本**。`{ disp, kind, left, right }`（`kind` は `"oxidation"` か `"reduction"`）。原子・電荷保存と e⁻ を含むことは既存テストが機械検証している |
| `kind` | **半反応式ごと**に酸化／還元が付いている。物質ごとではない（`Cu_ox` と `Cu_red`、`H2O2_red` が別々に立っている） |
| `electronsOf(hr)` | 授受する e⁻ の数 |
| `checkRedoxMultipliers(stage,a,b)` | e⁻ の数が合うか＋最簡整数比か |
| `combineHalves(stage,a,b)` | 倍率をかけて足し、両辺共通種を打ち消してイオン反応式にする |
| `OXIDATION` / `oxChangeOfHalf` | 原子ごとの酸化数と、その半反応式で変化した原子 |
| `REDOX_STAGES` | **14本**。`{ id, title, ox, red, answer:[a,b], mode?, intro, molecularEq?, cleavage? }` |

| 無いもの | 影響 |
|---|---|
| **酸化力・還元力の強さを表すデータが一切ない** | 「反応するか」を今のデータからは1ミリも導けない。ここが M6 の新規データの本体 |
| `IONIZATION_SERIES` | B3 で**設計はされたが未実装**（`model.js` に存在しない）。M6 はこれを金属限定ではなく一般化した形で先に入れる |
| 液性の情報 | ステージにも半反応式にも無い。ただし `left` に H⁺ があるか否かで**構造から導ける**（§2-4） |
| `I_ox`（2I⁻→I₂＋2e⁻） | `DESIGN_redox.md` の表には載っているが**未実装**。KI を還元剤に選ばせるなら要追加 |
| `H2O2_ox`（H₂O₂→O₂＋2H⁺＋2e⁻） | 同上・**未実装**。H₂O₂ の二役はこの設計の見どころなので要追加 |

酸化側の半反応式は14本、還元側は10本ある。ただし酸化側のうち
`iodoform_ox` / `acylRest_ox` / `formylRest_ox` は**切り離した断片**が出発点で、試薬として選べる物ではない。

### 2-2. 案A: 組み合わせを1つずつ手で書いた表

`MATRIX[oxId][redId] = { reacts, reason }` を人が全部埋める。

- **利点**: 1セルずつ人が化学の判断をするので、機械が勝手に結論を出さない。説明文も個別に書ける
- **欠点**:
  - **セル数が O(N²)**。いまの実装ぶんだけで 14×10＝**140セル**。試薬を1つ足すたびに列が丸ごと増える
  - **埋め忘れが「反応しない」と区別できない**。未記入セルを既定値で埋めた瞬間、
    書いていないことが断定になる（このリポジトリで最も避けたい事故の形）
  - 同じ原理（順位が逆）を140回書き写すので、**原理が表の中に散らばって消える**。
    「イオン化傾向」という1本の話が140個の独立した事実に化ける
  - 表の中で矛盾が起きても機械検査できない（A＞B・B＞C・C＞A を許してしまう）

### 2-3. 案B: 半反応式＋「強さの順位」から機械的に導く 【採用】

**採用する。** ただし「機械的に導く」の中身を、電位の数値ではなく**一次元の順位表（梯子）**にする。

```js
/* 酸化還元の梯子（酸性条件）。上にあるものほど「e⁻ を奪う力が強い＝酸化剤として強い」。
   値は**順位であって電位ではない**（10刻みなのは間に足せるようにするため）。
   同じ値＝この収録範囲では強弱を決めない（＝判定しない。§2-5 の3値目）。
   画面に数値そのものを出してはいけない（電位の暗記にすり替わる）。 */
const REDOX_LADDER_ACID = {
  "O3/O2":        200,   // O₃ ＋ 2H⁺ ＋ 2e⁻ → O₂ ＋ H₂O
  "H2O2/H2O":     190,   // H₂O₂ が酸化剤としてはたらくとき
  "MnO4-/Mn2+":   180,
  "Cr2O7^2-/Cr3+":170,
  "O2/H2O":       150,   // H2O_ox の対
  "NO3-/NOx":     130,   // 硝酸。希・濃で順位を分けない（§2-6・要判断）
  "Ag+/Ag":       120,
  "Fe3+/Fe2+":    115,
  "O2/H2O2":      110,   // H₂O₂ が還元剤としてはたらくとき。同じ物質が梯子に2回出る
  "I2/I-":        100,
  "Cu2+/Cu":       90,
  "H+/H2":         80,   // ここが「イオン化傾向で H より上か下か」の境目
  "Fe2+/Fe":       60,
  "CO2/C2O4^2-":   55,   // シュウ酸
  "Zn2+/Zn":       50,
  "Al3+/Al":       30,
  "Mg2+/Mg":       10,
};
```

- 梯子のキーは**半反応式の id ではなく「対（couple）」**。`Cu_ox` と `Cu_red` は
  向きが違うだけの同じ対なので、`HALF_REACTIONS` の各項に `couple: "Cu2+/Cu"` を足し、
  **梯子は couple → 順位** で引く。`Cu_ox` と `Cu_red` が同じ couple を指すことはテストで固定する
- 判定は1行:
  **`rank(酸化剤の対) > rank(還元剤の対)` なら反応が進む向き**。同値なら「判定しない」
- **イオン化傾向表は、この梯子から金属の対だけを抜き出したもの**になる。
  B3 が別に持とうとしていた `IONIZATION_SERIES` は、この梯子の部分集合として導出できる
  （`IONIZATION_SERIES = 梯子のうち "M^n+/M" 形の対を順位の低い順に並べたもの`）。
  B3 の実装が始まったらそちらから参照する ＝ **原理データを二重に持たない**

**利点**

- **データが O(N)**。試薬を1つ足すのは梯子に1行足すだけ。140セルが18行になる
- **矛盾が構造的に起こらない**。一次元の順位なので推移律が自動で成り立つ
- 「反応しない理由」が**1つの原理から生成される**ので、説明文がぶれない
- 既存14ステージが全部「反応する」と判定されることを**回帰テストで固定できる**
  （＝梯子が既存データと矛盾していないことの機械的な証明。案Aではこの検査が書けない）

**欠点と、その手当て**

| 欠点 | 手当て |
|---|---|
| 一次元の順位は、電位差がごく小さい対を無理に順序づけてしまう | **同じ値を許す**。同値＝「判定しない」。順位を付けられないものに順位を付けない |
| 有機の酸化（`EtOH_ox` 等）には高校で扱う順位が存在しない | **梯子に載せない**。有機の酸化剤は明示的な許可リスト（`pairsWith: ["Cr2O7_red","MnO4_red"]`）で相手を列挙する。ここだけは手書きの表が正しい（§2-7） |
| 順位が上でも実際には反応しないことがある（不動態など） | **例外表**（§2-7）。件数が少ないので O(数件) に収まる |
| 順位が上なら「必ず反応する」と機械が言い切ってしまう | 出力を3値にし、**「反応する」と言い切るのは収録した試薬の範囲内だけ**にする（§2-5） |

### 2-4. 液性の扱い — 「足りない」ではなく「別の式になる」

Gemini の例2「Cr₂O₇²⁻ は強酸性条件でのみ強い酸化力を発揮します。液性を『硫酸酸性』に
設定してください」は、**言い方として危うい**。MnO₄⁻ は中性・塩基性でも酸化剤として働き、
生成物が MnO₂ に変わるだけである（＝反応しないのではなく、**式が変わる**）。

そこで液性は次のように扱う。

1. **必要な液性は半反応式の形から導く**（機械的・追加データ不要）:
   - `left` に H⁺ を含む → 酸性が必要（`MnO4_red` `Cr2O7_red` `NO3_red` `O3_red` `H2O2_red` …）
   - `left` に OH⁻ を含む → 塩基性が必要
   - どちらも無い → 液性に依らない（`Zn_ox` `Cu_red` `Ag_red` `I2_red` …）
   - これは**必要条件であって十分条件ではない**。「H⁺ が無いからこの式では起こらない」までは
     言えるが、「だから何も起こらない」とは言えない
2. だから中性・塩基性を選んだときの返答は
   **「この式は酸性条件のもの。中性・塩基性では別の半反応式になる」**にする。
   別の式を収録していれば差し替えて動かし、収録していなければ「このアプリは未収録」と正直に言う
3. 収録するなら追加する半反応式（M6-D）:

   | id | 半反応式 | 備考 |
   |---|---|---|
   | `MnO4_red_neutral` | MnO₄⁻ ＋ 2H₂O ＋ 3e⁻ → MnO₂ ＋ 4OH⁻ | 中性・塩基性。MnO₂ は黒褐色の沈殿＝色の見どころ |

   `condition.html`（液性で書き換えるモード）は
   **「同じ酸化還元を書き換えただけ」**の場合しか扱わないと明記している（`toBasicHalf` の注記）。
   MnO₄⁻ の塩基性形は**別の半反応式**なので `CONDITION_STAGES` には入れず、M6 側で持つ。
   この線引きは既存コメントに書かれているとおりで、破らない

### 2-5. 出力は3値にする（Gemini 案からの最大の変更点）

Gemini のスケッチは「反応する／エラー」の2値だが、**2値にすると必ず嘘をつく**。
判断できないものを「反応しない」に寄せれば嘘、「反応する」に寄せれば嘘になる。

```js
/* matchRedox() の返り値 */
{
  verdict: "reacts" | "no-reaction" | "undecided",
  reasonCode: "...",        // no-reaction / undecided のときは必ず入る（enum）
  message: "...",           // 学習者に見せる説明文
  stage: { ox, red, answer, mode } | null,   // reacts のとき、既存エンジンに渡せる合成ステージ
}
```

- `reacts` … 反応する。段1以降（既存の倍率合わせ）へ進める
- `no-reaction` … **理由が言える**ときだけ。理由コードは下表の3つに限る
- `undecided` … 順位が同じ／梯子に無い／液性の式が未収録。**「判定しない」と正直に言う**。
  これを潰さないことが「正しさ最優先」の実装上の中身

### 2-6. 「反応しない」理由の種類（enum。この4つ＋1で全部）

| コード | いつ出るか | どう判定するか | 学習者に見せる文（案） |
|---|---|---|---|
| `same-role` | 両方とも酸化剤／両方とも還元剤 | `kind` が同じ | 「どちらも **e⁻ を受け取る側**の式です。e⁻ を出す相手（還元剤）を選ぼう」 |
| `ladder-reversed` | 順位が逆 | `rank(ox) < rank(red)` | 「Cu²⁺ は Ag より e⁻ を奪う力が弱いので、Ag から e⁻ を奪えません。順位は Ag⁺ ＞ Cu²⁺」 |
| `wrong-condition` | 選んだ液性でその式が成り立たない | §2-4 の構造導出 | 「この式は酸性条件のもの。中性・塩基性では **別の式**（MnO₄⁻→MnO₂）になります」 |
| `exception` | 例外表に載っている | 例外表を引く | 「Al は濃硝酸に入れると表面に緻密な酸化被膜（不動態）ができ、それ以上溶けません」 |
| （`undecided`） | 上のどれでもなく反応もしない | 梯子の同値・未収録 | 「この組み合わせは、このアプリでは強弱を決めていません」 |

**`same-role` の言い方に注意**。`H2O2` も `Fe^2+` も相手しだいで役が変わるので、
**「H₂O₂ は酸化剤です」と物質を主語にした断定を出してはいけない**。主語は必ず「いま選んだ式」にする。
むしろ H₂O₂ を選んだときに「この物質は梯子に2回出てくる」と見せられるのが、このモードの見どころ。

**Gemini の例3「イオン化傾向の差が小さいため、この条件では著しい反応が進行しません」は採らない。**
高校範囲では**差の大小ではなく順序**で決まる。差が小さくても順序が正しければ反応は起こる
（そこから先は平衡・速度の話で、高校では扱わない）。この文は生徒に誤った因果を教えるので、
`ladder-reversed` の説明では「順序が逆だから」とだけ言う。

### 2-7. 梯子で語れないものは、正直に列挙する

一次元の梯子で扱えないものが2種類ある。ここだけは**手で書いた表**が正しい。

```js
/* (a) 有機の酸化。高校で扱う順位が存在しないので、相手を明示列挙する */
const ORGANIC_OXIDANTS = {
  "EtOH_ox":   ["Cr2O7_red", "MnO4_red"],
  "MeCHO_ox":  ["Cr2O7_red", "MnO4_red"],
  "iPrOH_ox":  ["Cr2O7_red", "MnO4_red"],
};

/* (b) 例外表。順位では「反応する」になるが、実際には起こらない組み合わせ。
   件数が少ないので対で持つ。理由文は必須（理由が書けないものは載せない）。 */
const REDOX_EXCEPTIONS = [
  { ox: "NO3_red_conc", red: "Al_ox", code: "passivation",
    message: "Al は濃硝酸に入れると表面に緻密な酸化被膜ができ（不動態）、それ以上は溶けません。" },
  { ox: "NO3_red_conc", red: "Fe_ox", code: "passivation", message: "…" },
];
```

`qa/KNOWLEDGE_CAVEATS.md` の該当項目を必ず読んでから足す:

- **H-2 濃硫酸**: 酸化作用を示すのは**熱濃硫酸だけ**。冷濃硫酸は Al・Fe・Ni と不動態。
  → いまは H₂SO₄ の酸化剤としての半反応式を**持っていない**。M6 で足すなら
  「熱濃硫酸」と明記した試薬として足す（「濃硫酸」という札は使わない）。M6-C 以降の判断事項
- **H-3 過酸化水素の還元剤としての相手**: 実質 KMnO₄ と O₃ のみ。
  → 梯子で `O2/H2O2`（110）より上にある対は O₃・H₂O₂(酸化側)・MnO₄⁻・Cr₂O₇²⁻・O₂/H₂O。
  Cr₂O₇²⁻ は「一部条件」なので、**梯子の順位だけで H₂O₂×Cr₂O₇²⁻ を playable にしない**。
  §2-5 の「試薬の範囲内でだけ反応すると言い切る」で絞る
- **H-1 クロム酸⇄二クロム酸**: これは酸化還元ではない。CrO₄²⁻ を試薬に加えない
- **G-6 ボルタ電池の分極**: 過電圧が効く系は候補に出さない（B3 §6 と同じ）

### 2-8. どこまで言い切ってよいか（方針のまとめ）

1. **数値（標準電極電位）は持たない・出さない**。順位だけを持つ
2. **順位表を「表」として学習者に見せるかは要判断**（§9-2）。少なくとも
   「これは高校で扱う範囲での相対順位であって、暗唱表ではない」と添える
3. **「反応しない」と断定するのは、理由コードが付く4種類だけ**。それ以外は「判定しない」
4. **「反応する」と断定するのは、収録した試薬どうしの組み合わせだけ**。
   梯子は無限に外挿できてしまうので、選択肢の側で範囲を切る
5. 収録する試薬は**教科書の「主な酸化剤・還元剤」の表に載るもの**に限る。
   これが「教科書が扱う組み合わせ」という線引きの実体になる

---

## 3. 既存14ステージとの関係（データを二重に持たない）

### 3-1. 置き換えない。併存させる

`REDOX_STAGES` の14本は**解説つきの決まった道**（`intro`・`molecularEq`・`cleavage` を持つ）。
自由組み合わせは**道のない砂場**。役割が違うので併存させ、ステージ帯（1〜14）はそのまま残す。

### 3-2. 合成ステージ — 同じ形のオブジェクトを実行時に作る

既存エンジン（`redox.js`）はステージを `{ ox, red, answer, mode }` の形でしか見ていない。
だから自由組み合わせは**同じ形のオブジェクトをその場で組み立てて渡す**だけでよい。
`redox.js` 側の改修は「ステージの出どころ」を差し替える1点に閉じる。

```js
/* 半反応式2本から、REDOX_STAGES と同じ形のステージを合成する（純ロジック） */
function composeStage(oxId, redId) {
  const ox = HALF_REACTIONS[oxId], red = HALF_REACTIONS[redId];
  const eO = electronsOf(ox), eR = electronsOf(red);
  const l = eO * eR / gcd2(eO, eR);            // e⁻ の最小公倍数
  return {
    id: `free:${oxId}+${redId}`,
    title: `${dispOf(oxId)} × ${dispOf(redId)}`,
    ox: oxId, red: redId,
    answer: [l / eO, l / eR],                  // 持たずに導く
    mode: metalSourceOf(ox) ? undefined : "solution",   // 持たずに導く
    intro: "…",
  };
}
```

- **`answer` を導出にする**のが要点。既存14本は `answer` をデータで持っているが、
  それは最小公倍数から一意に決まる。
  **導出値が既存14本の `answer` と全部一致することをテストで固定する**（§6）。
  一致しなくなったら、どちらかのデータが壊れている
- **`mode`（板あり／溶液中）も導出する**。判定は「酸化側の出発種が
  電荷0・元素1種・気体でない＝金属板として置ける」かどうか（`redox.js` の `isDepositable`
  と同じ考え方）。これも14本の登録値と一致することをテストで固定する
- `molecularEq` / `cleavage` は合成しない。`redox.js` の `updateSheetTail` は
  `molecularEq` が無ければ段④⑤を出さない作りなので、**無改修で「段③まで」になる**
  （rs1〜rs3・ri1〜ri2 が既にその状態）

### 3-3. 収録ステージへの橋（対応表は書かない）※実装は §13

選んだ組み合わせが収録ステージと同じなら「解説つきのステージがあります」と案内する。
このとき **`(oxId, redId) → ステージ` の対応表を書かない**。`REDOX_STAGES` を実行時に走査して
`ox`・`red` が一致するものを拾う。理由はリポジトリの既定方針と同じで、
対応表を別に持つと収録を変えたときに黙って壊れるから
（`ion-equation` ↔ `ratio` の横断で「対応表は書かない・式の照合で引く」と決めた v114 の考え方）。

なお**同じ (ox, red) を持つステージが複数ある**（ri1 と ri2 は
どちらも `iodoform_ox`×`I2_red`）。だから引くのは**1件ではなく一覧**にする。
横断リンクで「対応表は代表1件しか持てないから式で照合する」と決めたのと同じ失敗が、
ここでも起きる。

---

## 4. 画面と操作

### 4-1. 新ページを作らない — redox.html の「段0」にする

B3（電池）は2槽＋導線で描画の前提が違うので新ページにしたが、M6 は
**ビーカーも筆算も既存とまったく同じ**で、増えるのは「相手を選ぶ」操作だけ。
だから `redox.html` に相乗りする。

- `#timeline` の先頭に **`<section class="step" id="stepPick">`** を足す。
  ヨードホルムの `#stepCleave`（段0「まずメチル基を切り離す」）と同じ、
  **条件つきで現れる段0**という既存の言い回しに乗る
- 入口は `redox.html?free=1`。`modeBar`（`.hscroll > .strip`）に
  `⚗ 自由に組み合わせる` を1つ足す。帯は折り返さず横スクロールするので、
  **リンクを1本足してもヘッダーは1段も太らない**
- **ヘッダーには何も足さない**。既存の回帰テスト
  「狭い縦持ち（320×568）でヘッダーが 120px を超えず、題名の行が折り返さない（全5ページ）」を
  守るため、新しい UI は必ず `<main>` の中に置く
- ステージ帯（1〜14）は自由モード中も残す。押せば通常のステージへ戻れる（＝行き止まりを作らない）

### 4-2. 段0の中身

```text
┌─ 0  反応させる相手を選ぶ ────────────────────────────┐
│  e⁻ を受け取る側（酸化剤）   [ 過マンガン酸カリウム KMnO₄  ▼] │
│  e⁻ を出す側（還元剤）       [ 硫酸鉄(Ⅱ) FeSO₄            ▼] │
│  液性                        (•) 硫酸酸性   ( ) 中性・塩基性  │
│                        [ ⚡ この組み合わせで試す ]            │
└──────────────────────────────────────────────────────┘
```

- **選ぶのは「物質」であって半反応式ではない**。学習者が手に持つのは試薬なので。
  物質＋液性（＋濃度）→ 半反応式の解決は `REAGENTS` 表が受け持つ（§5-2）
- ラベルは **「酸化剤」だけでなく「e⁻ を受け取る側」を併記**する。
  酸化剤／還元剤という語の取り違えは最頻出のつまずきで、
  既存の筆算も `【酸化剤】×2 還元される式` のように役割名を併記している。同じ流儀にそろえる
- 素の `<select>` を使う（自作ドロップダウンにしない）。モバイルで最も壊れにくく、
  既存の「押せるものが 32px 未満にならない」テストにもそのまま乗る
- 320px 幅では3つの区画を縦に積む。`<select>` は `width:100%`。
  **横に並べない**（Gemini のスケッチは3カラムだが、320px では成立しない）

### 4-3. 結果の見せ方

| verdict | 画面 |
|---|---|
| `reacts` | 段0が「選んだ組み合わせ」の要約に畳まれ、**段1（半反応式2本）以降が現れる**。以後は既存の体験とまったく同じ |
| `no-reaction` | 段0の下に**説明カード**（理由コードに対応した文＋「なぜそう言えるか」の1行）。段1は出さない。**押した操作が無駄にならないよう、直せる方向を必ず添える**（「e⁻ を出す側を選び直そう」） |
| `undecided` | 「このアプリでは強弱を決めていません」＋近い収録ステージへの誘導 |

`no-reaction` のカードは**赤いエラーにしない**。既存の `setStatusMsg` は
`ok` / `ng` / `info` の3系統を持つが、「反応しない」は**正解のひとつ**なので `info` で出す。
`ng`（過不足・進めない）は倍率合わせの失敗に取っておく。

---

## 5. モデル（model.js への追加。すべて純ロジック）

### 5-1. 追加するデータ

| 追加物 | 中身 |
|---|---|
| `HALF_REACTIONS[*].couple` | 各半反応式に対の名前（`"Cu2+/Cu"`）。既存24本すべてに付ける |
| `REDOX_LADDER_ACID` | couple → 順位（§2-3） |
| `ORGANIC_OXIDANTS` | 梯子に載せない有機酸化の相手一覧（§2-7a） |
| `REDOX_EXCEPTIONS` | 順位では反応するが実際は起こらない対（§2-7b） |
| `REAGENTS` | 選択肢に出す試薬（§5-2） |
| 半反応式の追加 | `I_ox`・`H2O2_ox`（どちらも `DESIGN_redox.md` に載っているのに未実装）。M6-D で `MnO4_red_neutral` |

### 5-2. `REAGENTS`（試薬 → 半反応式の索引）

```js
/* 選択肢に出す試薬。1つの物質が液性・濃度で違う半反応式になるので、ここで解決する。
   ここに無い物質は選べない ＝ §2-5「収録した範囲でだけ言い切る」の実体。 */
const REAGENTS = [
  { id: "KMnO4", sp: "KMnO4", side: "ox",
    half: { acid: "MnO4_red" },              // 中性・塩基性は M6-D で MnO4_red_neutral
    note: "赤紫色。酸性で最も強い酸化剤の代表" },
  { id: "K2Cr2O7", sp: "K2Cr2O7", side: "ox", half: { acid: "Cr2O7_red" } },
  { id: "HNO3_dil",  sp: "HNO3", side: "ox", half: { acid: "NO3_red" },      variant: "希" },
  { id: "HNO3_conc", sp: "HNO3", side: "ox", half: { acid: "NO3_red_conc" }, variant: "濃" },
  { id: "H2O2_ox",   sp: "H2O2", side: "ox", half: { acid: "H2O2_red" } },
  { id: "H2O2_red",  sp: "H2O2", side: "red", half: { acid: "H2O2_ox" } },   // 同じ物質が両側に出る
  { id: "HCl_dil",   sp: "HCl",  side: "ox", half: { acid: "H_red" }, note: "酸化剤としてはたらくのは H⁺" },
  ...
  { id: "Zn", sp: "Zn", side: "red", half: { any: "Zn_ox" } },   // 液性に依らない
];
```

- `side` は `HALF_REACTIONS[half].kind` から**導けるので持たない**、という選択もあるが、
  **持って、`kind` と一致することをテストで固定する**ほうがよい。
  データの書き間違い（酸化剤の欄に還元剤を並べる）が機械で止まる
- `H2O2` が **`side: "ox"` と `side: "red"` の両方に出る**のがこの設計の見どころ。
  UI では「過酸化水素（酸化剤として）」「過酸化水素（還元剤として）」と書き分ける
- **HCl / H₂SO₄ を「酸化剤」の欄に置くのは正しい**（酸化剤の本体は H⁺）。
  ただし札には「うすい塩酸（酸化剤としてはたらくのは H⁺）」と添える。
  ここを黙って `H⁺` と書くと、学習者が手に持つ物と対応しなくなる

### 5-3. 判定関数

```js
/* 純ロジック。DOM を触らない。返り値は §2-5 の形 */
function matchRedox(oxReagentId, redReagentId, condition /* "acid" | "basic" */) { … }
```

判定の順序（**先に出た理由で確定する**。順序自体が「どの理由を優先して見せるか」の設計判断）:

1. `side` が同じ → `same-role`
2. 液性でその試薬の半反応式が解決できない → `wrong-condition`
3. 例外表に載っている → `exception`
4. 有機の酸化（`ORGANIC_OXIDANTS` に載る）→ 相手が許可リストにあれば `reacts`、
   無ければ `undecided`（**`no-reaction` にしない**。順位を持っていないので「しない」と言えない）
5. 梯子に両方の対がある → 順位で `reacts` / `ladder-reversed` / 同値なら `undecided`
6. どちらかが梯子に無い → `undecided`

---

## 6. 回帰テストで担保すること（`ion-equation/test.html`）

### モデル群（`runModelTests`。ブラウザなしでも回る）

1. **梯子の健全性** — `REDOX_LADDER_ACID` の全キーが、`HALF_REACTIONS` のどれかの `couple` と一致する。
   値はすべて数値。孤児（誰も指していない順位）が無い
2. **対の一貫性** — `Cu_ox` と `Cu_red` のように向き違いの半反応式が**同じ couple を指す**。
   同じ couple を持つ半反応式は `kind` が異なる（同じ向きの重複が無い）
3. **既存14ステージが全部「反応する」になる** — `REDOX_STAGES` の全 `{ox, red}` を
   `matchRedox` に通して `verdict === "reacts"`。
   **これが梯子と既存データの整合の証明**であり、梯子を書き換えたときの最大の防波堤
4. **`answer` の導出一致** — `composeStage` が導く `[a,b]` が、14本の登録 `answer` と全部一致
5. **`mode` の導出一致** — 導出した板／溶液が、14本の登録 `mode` と全部一致
6. **全ペア総なめ** — 選べる試薬の全組み合わせ（`REAGENTS` の ox 側 × red 側）で
   `matchRedox` が例外を投げず、`verdict` が3値のいずれかで、
   **`reacts` 以外には必ず `reasonCode` と空でない `message` がある**
7. **反応すると判定したペアの式が壊れていない** — `reacts` の全ペアで
   `composeStage` → `combineHalves` → `compareSides` が原子・電荷ともつり合う。
   **機械が組み立てた式が化学として成立していることの担保**（案Aでは書けない検査）
8. **理由コードは enum の中** — `same-role` / `ladder-reversed` / `wrong-condition` / `exception` 以外が出ない
9. **液性の構造導出** — `left` に H⁺ を含む半反応式は `acid` 必須と導かれ、
   含まないものは液性に依らないと導かれる（全24本を総なめ）
10. **`REAGENTS` の健全性** — 全 `sp` が `SPECIES` にある。全 `half` が `HALF_REACTIONS` にある。
    `side` が `kind` と一致する
11. **例外表の健全性** — 全 `ox`/`red` が実在の半反応式で、**梯子では `reacts` になるペアだけが載っている**
    （順位でも反応しないペアを例外表に書くのは重複＝データの二重持ち）
12. **追加した半反応式** — `I_ox`・`H2O2_ox` が既存の「原子と電荷が保存され、e⁻ を含む」
    「Δ酸化数が e⁻ の数と一致する」テストに**自動で乗る**（データを足すだけで検査が増える）
13. **梯子の金属部分がイオン化傾向と一致する**（§9-2 の A案が要求する検査）—
    梯子から金属の対（`M^n+/M` の形）だけを順に抜き出すと、
    イオン化傾向の並び（Mg ＞ Al ＞ Zn ＞ Fe ＞ H ＞ Cu ＞ Ag）と**完全に一致する**。
    全体表を開いたときに「**下半分は覚えているイオン化傾向そのものです**」と言い切るので、
    **ここがずれるとアプリが嘘をつく**。順位を1つ動かしただけで落ちるように書く。
    B3 の `IONIZATION_SERIES` が先に実装されていれば、それと直接突き合わせる

### UI 群（`runRedoxUITests`。`redox.html` を iframe で駆動）

13. `redox.html?free=1` で段0が出て、ステージ帯（14個）は消えない
14. KMnO₄ × FeSO₄（硫酸酸性）を選ぶと段1に rs1 と同じ2本が出て、5:1 でクリアできる
15. 両方に酸化剤を選ぶと段1が出ず、`same-role` の説明が出る（`ng` ではなく `info` の見た目）
16. Cu × うすい塩酸で `ladder-reversed` の説明が出る（**r3 の Zn とちょうど対になる体験**）
17. 収録ステージと一致する組み合わせを選ぶと、そのステージへの誘導が出る。
    ri1/ri2 のように**複数一致する場合は複数出る**
18. 既存の横断テストに `redox.html?free=1` を足す — 「ヘッダーの帯が折り返さない」
    「320×568 でヘッダー 120px 以下」「押せるものが 32px 未満にならない」

### 監査（`audit.js`）

19. 全ペア × 倍率のファズ。`reacts` の全組み合わせで再生し、
    原子が保存され・アニメが止まらず・粒が枠外に出ないこと

---

## 7. 実装の刻み（1歩＝1コミット。それぞれ単体で価値がある）

- **M6-A｜モデルだけ（画面なし）** … **完了（v147・2026-08-06）**。実装記録は §10
  `couple` の付与・`REDOX_LADDER_ACID`・`composeStage`・`matchRedox` と、§6 の 1〜11。
  画面が1ピクセルも変わらないが、**既存14ステージの `answer` と `mode` が
  導出値と一致することの機械検査が増える**ので、これ単体で価値がある。
  未実装だった `I_ox`・`H2O2_ox` もここで足す（既存テストが自動で検査する）
- **M6-B｜段0のピッカーと「反応する」の道** … **完了（v148・2026-08-07）**。実装記録は §11
  `redox.html?free=1`・`#stepPick`・`modeBar` のリンク。液性は**酸性固定**。
  選ぶ → 反応する → 既存の段1以降がそのまま動く、まで。
  ここまでで「自由に組み合わせて遊べる」が成立する
- **M6-C｜「反応しない」の説明カード** … **完了（v149・2026-08-07）**。実装記録は §11
  理由コード4種＋`undecided` の文面。学習価値の本体はここ。
  §6 の 15〜16 のテストもここ
- **M6-D｜液性の選択** … **完了（v153・2026-08-07）**。実装記録は §12
  中性・塩基性の追加と `MnO4_red_neutral`。「足りない」ではなく「別の式になる」を実装で示す。
  `condition.html` への相互リンク
- **M6-E｜収録ステージへの橋** … **完了（v154・2026-08-07）**。実装記録は §13
  一致するステージの案内（§3-3）。
  ~~`portal.html` の単元表と `library.html` からの導線~~ ／ ~~`CURRICULUM` に単元を足すか~~ —
  **この2つは §9-0 の 9-7 と衝突していたので落とした**（本節が先に書かれ、9-7 の判断が
  あとから入ったときに直し忘れたもの。**9-7 が正**）。入口は `modeBar` のリンク1本だけで、
  portal / library への露出は入口の全体見直しに合流させる。`CURRICULUM` に足すことは
  `portal.html` への露出そのものなので、同じ理由で足さない
- **M6-F｜試薬の拡充**
  熱濃硫酸・SO₂・H₂S など。**1試薬＝1コミット**にする（梯子に1行・`REAGENTS` に1行・
  必要なら半反応式1本。全ペアのテストが自動で増える）

---

## 8. 採らなかった案

1. **組み合わせを1つずつ手で書いた表（Gemini 案そのまま）** — §2-2。
   140セルの人手・埋め忘れが断定に化ける・原理が表の中で消える・矛盾を機械検査できない
2. **標準電極電位の数値を持ち、差で判定する** — `DESIGN_battery_electrolysis.md` §6 の
   非目標と正面から衝突する。数値は高校範囲で要求されず、序列の暗記が電位の暗記に
   すり替わるだけ。加えて E° は熱力学しか語らないので、**数値を出すと
   「差があるのに反応しない」（不動態・過電圧）を説明できなくなる**
3. **「イオン化傾向の差が小さいので反応しない」という説明**（Gemini 例3） — §2-6。
   高校範囲では差の大小ではなく順序で決まる。誤った因果を教える
4. **出力を2値（反応する／エラー）にする** — §2-5。「判定しない」を潰すと必ず嘘になる
5. **酸×塩基版を同じ画面に同居させる**（Gemini 提案の3番） — 強弱の原理が違う
   （酸化還元は e⁻ の梯子、酸塩基は H⁺ の受け渡しやすさ）。同じ UI に混ぜると
   「強さの梯子が2種類ある」ことがかえって見えなくなる。**M7 として別に設計する**。
   なお酸塩基側は `index.html`（イオン反応モード）に土台があり、
   `WEAK_ELECTROLYTES` / `WATER_IONIZATION` という別のデータを既に持っている
6. **`redox.html` とは別のページを新設する** — §4-1。ビーカーも筆算も同じなので、
   ページを分けると `redox.js` を丸ごと写すことになる
7. **合成ステージを `REDOX_STAGES` に事前展開して持つ** — 組み合わせの数だけ
   ステージが増え、`answer` を人が書くことになる（§3-2 の導出と真逆）。
   ステージ帯が数百個になってヘッダーも壊れる
8. **濃硝酸・希硝酸に順位の差を付ける** — §9-1 のとおり教科書の言い方と E° の順序が逆で、
   どちらを採っても片方に嘘をつく。**順位は同じにし、「生成物が変わる」でだけ区別する**
9. **`reactions.json` に組み合わせを1件ずつ登録して判定に使う** — `reactions.json` は
   「収録した反応の索引」であって原理データではない。判定の根拠をここに置くと案A に戻る
   （索引としてのリンクは M6-E で使う）
10. **ヘッダーにドロップダウンを常設する**（Gemini フェーズ1のドロップダウン案） —
    ヘッダーは 320px で 120px 以下という既存の制約があり、増段させられない。
    フェーズ1の改善は本設計の範囲外（別レーンの担当）。
    **決着（v146・2026-08-06）**: 別レーンが「ヘッダーを増やさずに行き先を見せる」形で解いた。
    開く釦（`☰ 一覧`）を**既存のステージ帯と同じ行**に置き、一覧は `position:fixed` ＋ `[hidden]` の
    全画面シートにする ＝ **閉じているあいだ高さを持たない**ので段が増えない。
    実測でヘッダーは 320×568 / 568×320 / 375×812 のいずれも前後で不変（116 / 42 / 116px）。
    実装は `header-ui.js`、名前の供給は各モードの `buildStageNav` が付ける `data-label`。
    **本設計（M6）が §4-1 で「ヘッダーには何も足さない・新しい UI は `<main>` の中に置く」と
    決めた制約は、そのまま有効**。段0のピッカーはこの一覧とは無関係に `<main>` の中に作る

---

## 9. 判断を仰ぐ論点と、その結論

### 9-0. 決定（2026-08-06・ユーザー判断）

| # | 論点 | 決定 |
|---|---|---|
| 9-1 | 濃硝酸と希硝酸 | **順位を付けない**（生成物の違いだけで表す）。試験向けの「濃が強い」は `/qa/` 側で扱い、目録に1項目起こす |
| 9-2 | 順位表を見せるか | **既定は判定の2行。「梯子の全体を見る」を開けるようにし、開いたら「下半分はイオン化傾向そのもの＝新しく覚えることはない」と枠組みごと見せる**（A案。§9-2 の追記を参照） |
| 9-3 | 試薬の線引き | 熱濃硫酸は**入れる**（札は必ず「熱濃硫酸」）。SO₂・H₂S は後段だが、**出番が多く重要なので遠くへ送らない** |
| 9-4 | 中性・塩基性 | 最初の版ではやらない。**やらない間は液性の選択肢自体を出さない**。ただし出番が多く重要なので M6-D は必ず回収する |
| 9-5 | 不動態 | 「**表面に被膜ができて、そこで止まる**」で書く。あわせて**実際の入試での書かれ方を調査**し、目録に1行起こす（やや試験テクニック寄りの項目として） |
| 9-6 | 判定しないペア | **選ばせて `undecided` を返す**。選択肢から外すと、3値にした意味が画面上で消え、かつ「消えている＝反応しない」という強い誤読を生む |
| 9-7 | 入口 | `modeBar` にリンク1本だけ。portal / library への露出は入口の全体見直しに合流 |

**9-3・9-4 の「後回し」は優先度が低いという意味ではない**。SO₂ の二役も MnO₄⁻ の
液性による式の変化も入試での出番が多く、教材としての価値も高い。
1つずつ確実に入れるための順番であって、落とすものではない。

---


### 9-1. 濃硝酸と希硝酸、どちらを「酸化力が強い」とするか 【最重要】

教科書は「濃硝酸のほうが酸化力が強い」と教えるが、標準電極電位では
**希硝酸（NO₃⁻/NO ＝ +0.96 V）のほうが濃硝酸（NO₃⁻/NO₂ ＝ +0.80 V）より高い**。
教科書の言い方は濃度効果を「酸化力」と呼んでいるもので、一次元の梯子には乗らない。

- **本設計の暫定案**: 硝酸は**1つの対**として同じ順位に置き、希・濃の違いは
  「順位」ではなく「**生成物が変わる**（NO ⇄ NO₂）」でだけ表す（既存 rn1/rn2 の見せ方と同じ）
- 判断が要る: この扱いでよいか。「濃硝酸のほうが強い」と画面で言う場面を作るか、
  作らないか。作るなら `qa/KNOWLEDGE_CAVEATS.md` に1項目起こすべき内容

### 9-2. 順位表（梯子）を学習者に見せるか

見せると判定の根拠が分かるが、**新しい暗唱表を1つ増やす**ことになる。
見せないと「なぜ反応しないと言えるのか」がブラックボックスになる。

- 折衷案: **全体の表は出さず、判定に使った2つの対だけを「梯子の一部」として抜き出して見せる**
  （「Ag⁺ ＞ Cu²⁺」の2行だけ）。イオン化傾向表の暗記に置き換わらない
- 判断が要る: この折衷でよいか、全体表も見られるようにするか

#### 追記（2026-08-06・ユーザー指摘）— 隠すことの副作用

**「全体像を見せないと、どこまで覚えればよいのか分からず不安になる」**という指摘があった。
これは受験生にとって実在する不安で、2行だけ見せる案の弱点を正しく突いている。
範囲が見えないものは、学習者の頭の中で**際限なく大きく見積もられる**。
つまり「暗記表を増やさないために隠す」つもりが、**かえって暗記対象を無限に感じさせる**という
逆効果になりうる。

そこで次の形を推す（要合意）:

1. **既定は2行**（判定の導出だけを見せる。ここは変えない）
2. **「梯子の全体を見る」を開けるようにする**（既定は閉じている）
3. 全体を開いたときは、**必ず次の枠組みと一緒に**見せる:
   - 金属の部分は**イオン化傾向そのもの**（K Ca Na Mg Al Zn Fe Ni Sn Pb H Cu Hg Ag Pt Au）で、
     **新しく覚えることはない**
   - そこに足されるのは「MnO₄⁻・Cr₂O₇²⁻・O₃・H₂O₂ が金属より上にいる」という
     **ひとかたまりの事実だけ**
   - つまり**覚える量は増えていない**ことを、表そのもので示す

こうすると「どこまで？」に**上限を見せて答えられる**。隠して不安にさせるのでも、
新しい暗唱表を渡すのでもなく、**すでに覚えているものの地図の上に少し足すだけだと分からせる**のが狙い。

#### 決定（2026-08-06・ユーザー判断）— A案を採る

**既定は判定に使った2行。「梯子の全体を見る」を開けるようにする。**
（常時ぜんぶ出す B案は採らない ＝ 毎回18行が目に入ると、結局それが暗唱対象に見える）

**開いたときの見せ方が本体**なので、ここを実装で外さないこと。全体は**必ず上下に分けて**出す:

```
  O₃ ／ H₂O₂ ／ MnO₄⁻ ／ Cr₂O₇²⁻ ／ O₂ ／ 硝酸       ← 「強い酸化剤」として習うもの
  ──────────────────────────────────────────────
  Ag ＞ Cu ＞ (H) ＞ Fe ＞ Zn ＞ Al ＞ Mg           ← イオン化傾向、そのまま
```

添える文（案）: 「**下半分は、覚えているイオン化傾向そのものです。**
上半分は『強い酸化剤』として習うものが並んでいるだけで、
**上下の細かい順位まで覚える必要はありません**（このアプリが判定に使っているだけです）。」

- **順位の数値（180・170 …）は絶対に画面に出さない**（§2-3 の注記。電位の暗記にすり替わる）
- 金属の並びが `IONIZATION_SERIES` と一致することは**回帰テストで固定する**。
  ここがずれると「イオン化傾向そのもの」という説明が嘘になる（§6 に検査を足す）
- 開閉の状態は覚えない（毎回閉じた状態で始める）。既定を2行に保つことが A案の要

### 9-3. 収録する試薬の線引き

M6-A の提案は「教科書の『主な酸化剤・還元剤』の表に載るもの」。具体的には
酸化剤＝KMnO₄・K₂Cr₂O₇・希硝酸・濃硝酸・H₂O₂・I₂・O₃・希塩酸(H⁺)・CuSO₄・AgNO₃、
還元剤＝Mg・Zn・Fe・Cu・FeSO₄・シュウ酸・KI・H₂O₂。

- 判断が要る: **熱濃硫酸**を入れるか（入れるなら半反応式1本の追加と、
  `KNOWLEDGE_CAVEATS` H-2 のとおり「熱」の明記が必須）
- 判断が要る: **SO₂・H₂S** を入れるか（SO₂ は H₂O₂ と並ぶ「二役」の代表で教育価値が高いが、
  半反応式2本と種の追加が要る）

### 9-4. 中性・塩基性を選べるようにするか（M6-D をやるか）

`MnO4_red_neutral`（MnO₄⁻＋2H₂O＋3e⁻→MnO₂＋4OH⁻）は高校範囲の上限に近い。
入れると「液性で式が変わる」を実装で示せるが、収録範囲が広がる。

- 判断が要る: M6-D をやるか。やらない場合、液性の選択肢自体を出さない
  （出して「未収録」と返すより、**選べないほうが正直**という考え方もある）

### 9-5. 不動態を「反応しない」と言い切ってよいか

`KNOWLEDGE_CAVEATS.md` H-2 は不動態を事実として書いている（＝言い切ってよさそう）。
ただし「表面だけが反応して止まる」ので、厳密には「まったく反応しない」ではない。

- 判断が要る: `exception` の文面を「反応しません」にするか
  「**表面に被膜ができて、そこで止まります**」にするか。後者を推奨

### 9-6. 「判定しない」ペアを選択肢に出すか

`undecided` を返すくらいなら、**その組み合わせを最初から選べなくする**（片方を選んだ時点で
相手の `<select>` から外す）ほうが親切、という考え方がある。
一方で「選べないこと」自体が理由の説明にならないので、選ばせて説明する価値もある。

- 判断が要る: 選ばせて `undecided` を返すか、選択肢から外すか。
  B3 §0 の「判断できない反応は候補に出さない」に厳密に従うなら後者

### 9-7. 入口をどこに置くか

`modeBar` にリンクを1本足す案（§4-1）でよいか。
`memory` の「入口（導線）の見直し」（chem-entry-points-review）で
**新機能の入口は作り込まない・全体でまとめて見直す**という方針があるので、
M6-B ではリンク1本だけにとどめ、`portal.html` / `library.html` への露出は
入口の全体見直しに合流させる、という進め方を提案する。

---

## 10. M6-A の実装記録（v147・2026-08-06）— 原案から変えた点

**画面は1ピクセルも変わっていない**（実測: `redox.html` のヘッダー高さは
320×568 / 568×320 / 375×812 で 116 / 42 / 116px。ステージ帯14個・段の見出しも同じ。
既存14ステージは模範倍率で全部クリアまで完走・JS エラー0）。
回帰テストは 143 → 153 件（モデル群 34 → 44 件）で全合格。

### 10-1. 原案から変えた点と、その理由

| # | 原案（§2〜§5） | 実装 | 理由 |
|---|---|---|---|
| 1 | 対の名前は `"Cu2+/Cu"` | `"Cu^2+/Cu"`（**両側とも SPECIES の id**） | 対の両側が実在の化学種かを機械検査でき、金属の対の抜き出し（§6-13）も**構造から**導ける。表示は `coupleDisp()` が SPECIES の `disp` に直す |
| 2 | `"NO3-/NOx"` 1本で 130 | `"NO3-/NO"` と `"NO3-/NO2"` を**同じ 130** に | §9-1 の「順位を分けない」は保ったまま、§6-2「同じ couple の式は kind が異なる」を守るため。`NO3_red` と `NO3_red_conc` はどちらも還元の式なので、同一 couple にすると重複検査に引っかかる。**対は生成物で分かれるが、順位は分けない**という形にした |
| 3 | `ORGANIC_OXIDANTS` は3本（アルコール類） | `iodoform_ox` / `acylRest_ox` / `formylRest_ox` を追加 | §6-3（既存14ステージが全部 reacts）が **ri1 / ri2 で落ちた**。ヨードホルムの3本も梯子に順位を持たないので、同じく相手（`I2_red`）を列挙する必要がある。**テストが設計の穴を見つけた例** |
| 4 | `REDOX_EXCEPTIONS` は `{ ox, red }` | `{ oxidant, reductant }` | `REDOX_STAGES` の `ox` / `red` は**半反応式の向き**（`ox` = 酸化される式 = 還元剤）で、例外表の `ox`（= 酸化剤）と**意味が逆**。同じ名前で逆の意味は静かなバグの温床なので、例外表と `matchRedox` の引数は oxidant / reductant で書き分けた |
| 5 | 試薬 id は `H2O2_ox` / `H2O2_red` | `H2O2_asOxidant` / `H2O2_asReductant` | 半反応式 id の `H2O2_ox` と衝突する |
| 6 | `matchRedox(試薬, 試薬)` だけ | `matchHalves(半反応式, 半反応式)` を核にし、`matchRedox` がその上に乗る | §6-3 は収録14ステージを総なめするが、`iodoform_ox` のように**試薬として選べない式**があるので試薬経由では検査できない |
| 7 | `wrong-condition` は「反応しない」の理由 | **`undecided` の理由**にした | 「液性が足りないから反応しない」と言わないため（§2-4。MnO₄⁻ は中性・塩基性でも酸化剤としてはたらき、**式が変わる**だけ）。文面も「別の式になる」で固定し、テストで見張る |
| 8 | `undecided` の理由コードは無指定 | `no-rank` / `tie` / `not-listed` / `wrong-condition` の enum | §2-5 が「no-reaction / undecided のときは必ず入る（enum）」と書いているため。verdict ごとに使える値をテストで固定した |

### 10-2. 梯子は原案どおり（順序を標準電極電位と突き合わせて確認済み）

順位の**並び**は §2-3 の原案から1つも動かしていない。O₃ ＞ H₂O₂ ＞ MnO₄⁻ ＞ Cr₂O₇²⁻ ＞ O₂/H₂O
＞ 硝酸 ＞ Ag⁺ ＞ Fe³⁺ ＞ O₂/H₂O₂ ＞ I₂ ＞ Cu²⁺ ＞ H⁺ ＞ Fe²⁺ ＞ CO₂/シュウ酸 ＞ Zn²⁺ ＞ Al³⁺ ＞ Mg²⁺
は、標準電極電位の順序と全部一致する（**数値は持たない・出さない**）。
金属の対だけを抜くと **Mg ＞ Al ＞ Zn ＞ Fe ＞ H ＞ Cu ＞ Ag** になり、§9-2 A案が画面で言い切る
「下半分はイオン化傾向そのもの」が成り立つ（`IONIZATION_SERIES` として導出し、テストで固定）。

### 10-3. 梯子が広く取りすぎるところを、`pairsWith` で絞った

一次元の順位は**熱力学しか語れない**（§1 の非目標）ので、順位だけだと
「起こるはずだが実際には起こらない」組み合わせまで reacts にしてしまう。
順位を動かすのではなく、**言い切る範囲を絞る**（§2-3 の欠点の手当て・§2-8-4）形で処理した。

| 試薬 | 絞り方 | 理由 |
|---|---|---|
| 過酸化水素（還元剤として） | `pairsWith: ["MnO4_red", "O3_red"]` | `qa/KNOWLEDGE_CAVEATS.md` H-3。高校で出る相手は実質 KMnO₄ と O₃ だけ。§2-7 が「梯子の順位だけで H₂O₂×Cr₂O₇²⁻ を playable にしない」と指示していたぶん |
| シュウ酸 | `pairsWith: ["MnO4_red", "Cr2O7_red"]` | 順位（55）が低いので、絞らないと**ほぼ全部の酸化剤と反応する**ことになる。実際にはうすい塩酸から水素は出ないし、Ag⁺・Cu²⁺ が相手なら先にシュウ酸塩の沈殿ができる |
| 硝酸銀 × ヨウ化カリウム | 例外表に1件追加 | 順位では Ag⁺ ＞ I₂ だが、実際は先に AgI（黄色の沈殿）になってそこで止まる。**別の反応が先に起きる**場合は梯子では語れない |

**残した既知のずれ（M6-B 以降で要判断）**: Cu²⁺ × I⁻ は順位では `ladder-reversed`（反応しない）に
なる。実際は CuI が沈殿するぶん有利になって反応するが、高校の範囲外なので教科書どおり
「Cu²⁺ は I⁻ から e⁻ を奪えない」で通してある（`REAGENTS` の KI にコメントあり）。

### 10-4. 全ペア総なめの実測（`REAGENTS` の ox 10種 × red 12種 ＝ 120組）

| verdict | 件数 | 内訳 |
|---|---|---|
| reacts | 69 | — |
| no-reaction | 12 | `ladder-reversed` 9・`exception` 3 |
| undecided | 39 | `not-listed` 37・`tie` 2 |

`tie` の2件は Cu²⁺×Cu と I₂×KI（どちらも同じ対どうし）。
`same-role` と `wrong-condition` はこの総なめには出ない（別に個別のテストで見ている）。

### 10-5. やり残し（M6-A の範囲外）

- **熱濃硫酸**（§9-3 で「入れる」と決定）は半反応式と化学種の追加が要るので **M6-F** に置いた
  （§7 が「1試薬＝1コミット」と決めているため）
- §6 の 12（`I_ox`・`H2O2_ox` が既存テストに自動で乗る）は、既存の総なめテストが
  そのまま検査している。念のため「対の裏返しになっている」ことだけ別に1件足した
- §6 の 14 以降（UI 群）と 19（監査）は M6-B 以降

---

※ 用語は教科書表記に合わせる: 酸化剤／還元剤・酸化される／還元される・半反応式。
※ 画面では常に「e⁻ を受け取る側／出す側」を併記する（既存の筆算の役割ラベルと同じ流儀）。
※ 本設計は `DESIGN_battery_electrolysis.md`（B3）と**原理データを共有する**。
   B3 の `IONIZATION_SERIES` は本書の梯子から金属の対だけを抜き出したものとして導出し、
   二重に持たない。先に実装されたほうが元データを置き、あとから来たほうが参照する。

---

## 11. M6-B / M6-C の実装記録（v148・v149・2026-08-07）— 原案から変えた点

**ヘッダーは1pxも太っていない**（実測: `redox.html` と `redox.html?free=1` の両方で
320×568 / 568×320 / 375×812 とも **116 / 42 / 116px**。帯はどれも1段・横はみ出し0）。
既存14ステージは通常の入口で模範倍率どおり **14/14 完走**（JS エラー0）。
回帰テストは 153 → 156（M6-B）→ 159 件（M6-C）で全合格。
`node tools/check-mobile.mjs ion-equation`（100通り）も不合格0。

### 11-1. 原案から変えた点と、その理由

| # | 原案 | 実装 | 理由 |
|---|---|---|---|
| 1 | 酸化剤の欄には酸化剤だけ、還元剤の欄には還元剤だけを並べる（§4-2 の含み） | **どちらの欄にも全22試薬を並べる**（`<optgroup>` で役を示す） | 役で絞ると**両方に酸化剤を選べなくなり、`same-role` の説明が画面に出る道が消える**（§6 の検査15が原理的に不可能になる）。§4-2 自身が「酸化剤／還元剤の取り違えは最頻出のつまずき」と書いているのに、その取り違えを起こせない画面になってしまう。§9-6「判定しないペアも選択肢から外さない」と同じ考え方で、**選べないこと自体は理由の説明にならない**。なお欄を取り違えただけの選び方は `matchRedox` が `side` でそろえるので、正しい組み合わせならちゃんと反応する |
| 2 | `reacts` のとき段0は「選んだ組み合わせ」の要約に**畳む**（§4-3） | **畳まない**（ピッカーを出したまま結果を下に足す） | ここは「道のない砂場」なので、続けて別の組み合わせを試すのが主な動線になる。畳むと毎回開き直すことになる。段0は 320px で1画面に収まっており、畳まなくても段1が画面外へ押し出されない（実測） |
| 3 | 判定の根拠の2行は、判定のたびに出す（§9-2） | **その2つの順位で決着したときだけ**出す（`ladder-reversed` と、順位で決まった `reacts`） | 例外表（不動態・AgI）や許可リストで決まった結果に順位の2行を添えると、**「順位でそう決まった」という嘘**になる。順位で決まっていないときは、そう書いた見出しだけを出す |
| 4 | 梯子の全体は「上＝強い酸化剤／下＝イオン化傾向」の2段（§9-2 のスケッチ） | 2段は保ったまま、**下段に混ざる非金属の対（Fe³⁺/Fe²⁺・O₂/H₂O₂・I₂/I⁻・CO₂/C₂O₄²⁻）をうすい字で位置どおりに置く** | スケッチはこの4つを落としているが、落とすと**梯子の「全体」ではなくなる**（I₂ が絡む判定の根拠が見えない）。金属だけを太字にすることで「太字の並び＝イオン化傾向そのもの」は保たれ、うすい字は「あいだに入るだけで覚える対象ではない」と本文で言い切っている |
| 5 | 金属の対かどうかは画面側で判定 | **`IONIZATION_SERIES` に含まれるかで引く** | 条件を画面側に書き写すと、model 側の導出が変わったとき下半分が黙ってずれる。テストで「太字の並び＝`IONIZATION_SERIES` の逆順」を固定した |
| 6 | — | `header-ui.js` の「n/14」を、**どれも開いていないときは出さない**ように直した | 自由モード中はどのステージも `.active` でないので「0/14」と出ていた |

### 11-2. `wrong-condition` は画面にはまだ出ない（→ M6-D で出るようになった。§12）

液性は酸性固定（§9-4）なので、`matchRedox` に `"acid"` しか渡らず、
**`wrong-condition` が画面に出る道は現状ない**。文面と `FIX_HINT` の枠は用意してあり、
enum の検査もモデル側で生きているので、**M6-D で液性を選べるようにした瞬間に出る**。

### 11-3. やり残し（M6-B / M6-C の範囲外）

- **§6 の 17（収録ステージへの橋）は M6-E**。`stagesForHalves` はモデル側に既にあるが、
  画面からは呼んでいない
- **§6 の 19（監査）は未着手**。`audit.js` に全ペア × 倍率のファズを足すのは M6-E 以降
- `portal.html` / `library.html` への露出は作っていない（§9-7 のとおり入口の全体見直しに合流）

---

## 12. M6-D の実装記録（v153・2026-08-07）— 原案から変えた点

**ヘッダーは1pxも太っていない**（実測: `redox.html` / `redox.html?free=1` / `condition.html` の3ページとも
320×568 / 568×320 / 375×812 で **116 / 42 / 116px**。M6-C までと同じ値で、帯はどれも1段・横はみ出し0）。
既存14ステージは通常の入口で模範倍率どおり **14/14 完走**（JS エラー0）。
回帰テストは 159 → **166 件**（モデル群 +3・UI 群 +4）で全合格。
`node tools/check-mobile.mjs ion-equation`（100通り）も不合格0。
段0の高さは 320×568 で **415px**（画面からヘッダーを引いた 452px に収まる ＝ §11-1-2 で
「畳まない」と決めた前提を保っている）。

### 12-1. この段で足したもの

| 種類 | 中身 |
|---|---|
| 化学種 | `MnO2`（酸化マンガン(Ⅳ)・黒褐色）。`OXIDATION` に +4、`SPECIES_COLOR` と `RSTYLE` に暗い色 |
| 半反応式 | `MnO4_red_neutral`（MnO₄⁻ ＋ 2H₂O ＋ 3e⁻ → MnO₂ ＋ 4OH⁻）。対は `MnO4-/MnO2` |
| 試薬 | `KMnO4` の `half` に `basic:` を足した（**液性で式そのものが変わる唯一の試薬**） |
| 表 | `LISTED_OXIDANTS`（順位を持たない**酸化剤**の相手の明示列挙。`ORGANIC_OXIDANTS` の逆向き版） |
| 関数 | `writtenFor(hr)`（その式が酸性・塩基性どちらの**書き方**か） |
| 画面 | 段0の液性ラジオ2つ＋但し書き＋`condition.html` への橋。`condition.html` の帯に帰り道 |

### 12-2. 原案から変えた点と、その理由

| # | 原案 | 実装 | 理由 |
|---|---|---|---|
| 1 | `conditionOfHalf` は**左辺**だけを見る（§2-4） | **OH⁻ は左右どちらにあっても `basic`** に直した | `MnO4_red_neutral` は左辺に H⁺ も OH⁻ も無く、OH⁻ を**生む**形。左辺しか見ないと「液性に依らない式」に化けて、`REAGENTS.half` のキーと式の形が食い違う検査も素通りする。酸性の水溶液に OH⁻ は書けないので、右辺にあっても塩基性の書き方だと**構造から**言える（追加データは持たない）。**右辺の H⁺ は酸性必須にしない**（既存の有機の酸化・`H2O2_ox` などは「どちらにも書き直せる式」を酸性の書き方で登録しているので、acid にすると言い過ぎになる） |
| 2 | 液性の話は「その液性の式を持っているか」だけ（§2-4） | **`writtenFor` で「書き方がそろっているか」も見る**（2つめの `wrong-condition`） | 塩基性の MnO₄⁻ の式と、酸性の書き方の式（エタノール・H₂O₂）をそのまま足すと、**1本の式に H⁺ と OH⁻ が並ぶ**。実際には結びついて水になるので、そんな式は書かない。ここで返すのも「反応しない」ではなく「**この2本のままでは書けない**」まで |
| 3 | 中性・塩基性でも梯子で判定する（暗黙） | **中性・塩基性の MnO₄⁻ には順位を与えず、`LISTED_OXIDANTS` で相手を列挙**（`I_ox` 1本） | `REDOX_LADDER_ACID` は名前のとおり酸性の梯子。中性・塩基性の梯子を作れば**新しい暗唱表が1つ増える**し、高校ではそこまで扱わない。§2-7「梯子で語れないものは、正直に列挙する」に従った。列挙する相手は「H⁺ を含まない書き方＝そのまま足せる式」に限る（テストで固定） |
| 4 | 液性が合わない文面は固定 | **収録している例をデータから引く**（「中性・塩基性の式を持っているのは 過マンガン酸カリウム だけです」） | 名前を手で書くと、M6-F で試薬を増やしたとき黙って古くなる。**行き止まりに見せない**ための一行でもある（§9-4 の「未収録で返るだけを増やさない」への手当て） |
| 5 | 液性の但し書きに `condition.html` への橋を常設 | **中性・塩基性を選んだときだけ出す** | 常設すると段0が 320×568 の1画面（452px）から溢れ、段1が画面外へ押し出される。ページどうしの往復は**ヘッダーの帯**（`redox.html` の「液性で書き換える →」／`condition.html` の「⚗ 自由に組み合わせる」）が常に持っているので、ここは「いま話に出たから添える」ぶんに絞った |
| 6 | — | 見出しに液性を出し、**選び直したら見出しを素に戻す**ようにした | 同じ2つを選んでも液性で式が変わるので、どちらを見ているのか分からないと見比べられない。M6-C までは反応しなかったとき**前の見出しが残っていた**（液性まで出すようになって目立つ不整合になったので、ここで直した） |
| 7 | 判定の根拠の見出しは理由コードごと | `reacts` なのに順位で決まっていないときの文を足した | M6-C までは「この組み合わせは、強さの順位では決めていない。」が **reacts の下に出ていた**（有機の酸化がその状態）。中性・塩基性の MnO₄⁻ でも同じ形になるので、「順位ではなく、このアプリが収録している組み合わせとして扱っている。」に分けた |

### 12-3. 「別の式になる」が実物として動くこと（この段の要）

同じ **KMnO₄ × KI** が、液性を切り替えるだけで別の式になり、**どちらも最後まで解ける**。

| 液性 | 半反応式 | e⁻ | 倍率 | 組み上がる式 |
|---|---|---|---|---|
| 硫酸酸性 | MnO₄⁻ ＋ 8H⁺ ＋ 5e⁻ → Mn²⁺ ＋ 4H₂O | 5 | 5:2 | 10I⁻ ＋ 2MnO₄⁻ ＋ 16H⁺ → 5I₂ ＋ 2Mn²⁺ ＋ 8H₂O |
| 中性・塩基性 | MnO₄⁻ ＋ 2H₂O ＋ 3e⁻ → MnO₂ ＋ 4OH⁻ | 3 | 3:2 | 6I⁻ ＋ 2MnO₄⁻ ＋ 4H₂O → 3I₂ ＋ 2MnO₂ ＋ 8OH⁻ |

ビーカーには黒褐色の MnO₂ が2個と OH⁻ が8個できる（実測。`MnO2` は `SOLUTION_TINT` に入れていない
＝ 溶けるのではなく濁るので、液の色ではなく**粒**で見せる。紫が消えるのは MnO₄⁻ が減るぶんで出る）。

### 12-4. `wrong-condition` が実際に画面へ出た例（§11-2 の宿題を回収）

| 選んだもの | 液性 | 出る文 |
|---|---|---|
| 二クロム酸カリウム × 硫酸鉄(Ⅱ) | 中性・塩基性 | 「K₂Cr₂O₇ の式は酸性条件のものです。中性・塩基性では『反応しない』のではなく、別の式になります。その式はこのアプリにまだ収録していません（中性・塩基性の式を持っているのは 過マンガン酸カリウム だけです）。」 |
| 過マンガン酸カリウム × エタノール | 中性・塩基性 | 「KMnO₄ の式は中性・塩基性の書き方、CH₃CH₂OH の式は酸性の書き方です。書き方がそろっていないと1本にまとめられません。『反応しない』のではなく、どちらかを別の式に書き直す必要があります。」 |

どちらも `undecided`（`no-reaction` ではない）・**赤ではなく案内（info）**・「直せる方向」＋
`condition.html` への橋つき・順位の2行は出さない（順位で決まっていないので）。
テストで「『反応しません』と書いていない」ことまで見張っている。

### 12-5. 中性・塩基性を選んだときの全ペア（120組）の内訳

| verdict | 件数 | 内訳 |
|---|---|---|
| reacts | 16 | MnO₄⁻×KI 1件＋液性に依らない対（I₂・Cu²⁺・Ag⁺ × 金属など）15件 |
| no-reaction | 6 | `ladder-reversed` 5・`exception` 1 |
| undecided | 98 | `wrong-condition` 76・`not-listed` 20・`tie` 2 |

`wrong-condition` が多いのは、収録している式のほとんどが酸性の書き方だから。
**そこを黙って「反応しない」にしないことが M6-D の中身**なので、件数の多さは
そのまま「どの式が酸性条件のものなのか」を1つずつ言っていることになる。
なお I₂・CuSO₄・AgNO₃ が中性・塩基性でも酸性と同じ結論になるのは、その式に H⁺ も OH⁻ も
出てこない（＝液性で書き方が変わらない）ため。中性の CuSO₄ 水溶液に亜鉛板を入れる銅樹は
実際にこの液性の実験なので、ここを塞ぐと逆に嘘になる。

### 12-6. やり残し（M6-D の範囲外）

- **強い塩基性でだけ起こる副反応**（Ag⁺ や Cu²⁺ が水酸化物になる・Al や Zn が塩基に溶ける）は
  扱っていない。「中性・塩基性」をひとまとめにしている以上、厳密には中性側の話しか
  保証していない。高校の範囲では順位（イオン化傾向）を液性で分けないので現状のままにしたが、
  試薬を増やすとき（M6-F）に効いてくる可能性がある
- **中性・塩基性で選べる組み合わせは MnO₄⁻ × KI の1本だけ**。SO₂・H₂S（M6-F）や
  H₂O₂ の塩基性形を足せば増える。H₂O₂ は**酸性の書き方の式しか持っていない**ので、
  いまは `wrong-condition` で止まる（足すなら `H2O2 ＋ 2OH⁻ → O₂ ＋ 2H₂O ＋ 2e⁻` を1本）
- §6 の 17（収録ステージへの橋）・19（監査）は引き続き M6-E 以降

---

## 13. M6-E の実装記録（v154・2026-08-07）— 収録ステージへの橋

**ヘッダーは1pxも太っていない**（実測: `redox.html` / `redox.html?free=1` / `condition.html` /
`index.html` の4ページとも 320×568 / 568×320 / 375×812 で **116 / 42 / 116px**。
`library.html` 71/40/71・`portal.html` 69/40/69 も据え置き。横はみ出しはどのページも0）。
既存14ステージは通常の入口で模範倍率どおり **14/14 完走**（JS エラー0）。
回帰テストは 166 → **169 件**（UI 群 +3。モデル群は既存テストに検査を1つ足したので件数は同じ）で全合格。
`node tools/verify-release.js ion-equation` 合格・`node tools/check-mobile.mjs ion-equation`（100通り）不合格0。

### 13-1. §7 にあった矛盾を解消した

§7 の M6-E は「`portal.html` の単元表と `library.html` からの導線」「`CURRICULUM` に単元を足すか」と
書いていたが、これは §9-0 の **9-7（入口は `modeBar` にリンク1本だけ。portal / library への露出は
入口の全体見直しに合流）と衝突する**。§7 が先に書かれ、あとから入った 9-7 の判断を反映し忘れたもの。
**9-7 を正**として §7 から落とし、取り消し線つきで理由を残した。

- `portal.html` / `library.html` には**何も足していない**
- **`CURRICULUM` にも足していない**。`CURRICULUM` は `portal.html` が読む表なので、
  ここに足すことは portal への露出そのもの。「既存単元 `u-redox-half` に入れるのが素直」という
  §7 の但し書きも、露出をやる／やらないの判断が先に来る（memory の「新機能の入口は作り込まない・
  全体でまとめて見直す」）

### 13-2. 実装（原案から変えた点）

| # | 原案 | 実装 | 理由 |
|---|---|---|---|
| 1 | 橋は `redox.html?rxn=<id>` へのリンク（§9 の反応インデックスと同じ形） | **その場で切り替えるボタン**（ステージ帯を押したときとまったく同じ処理） | ページを開き直すと `?free=1` が外れ、**橋を渡った先から自由モードへ戻る道が消える**（帯の「⚗ 自由に組み合わせる」を押し直す＝もう一度ゼロから選び直しになる）。その場で切り替えれば段0が残るので、解説を見てすぐ次の組み合わせを試せる。`?rxn=` の入口は反応インデックス用にそのまま残っている |
| 2 | 引く材料は試薬 | **合成ステージの `ox` / `red`**（＝半反応式2本） | 収録ステージが持っているのも半反応式2本なので、同じ土俵で照合できる。試薬から引くと液性の解決をもう一度書くことになる |
| 3 | — | 橋を渡ったあとも**橋と判定は残す** | ri1/ri2 のように行き先が複数あるとき、1本目を見てから2本目へ行けなくなる。ステージ帯を押したときに判定が残るのと同じ振る舞いにそろえた |

### 13-3. 「1件ではなく一覧」をどう見張ったか

`stagesForHalves` は M6-A から一覧を返すが、**画面側で1件に間引いてしまう**のがこの段の
いちばんありそうな壊れ方（`ion` ↔ `ratio` の横断で実際に踏んだ形）。次の3段で見張っている。

1. モデル: **収録14ステージを総なめ**して、どのステージも `stagesForHalves(st.ox, st.red)` に
   自分自身が入る（収録を足したときに橋から漏れない）
2. 画面: **試薬から選べる収録ステージを総なめ**して、橋が出ること・その並びが
   `stagesForHalves` の結果と**文字列レベルで一致**すること（間引きがあれば落ちる）。
   届かないステージが `ri1` / `ri2` の2本だけであることも固定した（増えたら知らせる）
3. 画面: ri1 / ri2 は**試薬として選べない**（切り離した断片 `CH₃⁺` が出発点なので試薬に持てない）ため、
   ピッカー経由ではこの「複数一致」を一度も通れない。そこで描画そのものを半反応式2本で叩く口
   （`RedoxEq.free.bridgeIdsFor`）をテスト用に置き、`["ri1","ri2"]` が並ぶことを固定した

### 13-4. 橋が実際に架かった組み合わせ（硫酸酸性・実測）

試薬から選べる12本すべてで橋が出た（残る2本 ri1 / ri2 は上記のとおり試薬から選べない）。

| 選んだもの | 案内されたステージ |
|---|---|
| 硫酸銅(Ⅱ) × 亜鉛 | ステージ1：亜鉛 × 銅(Ⅱ)イオン |
| 硝酸銀 × 銅 | ステージ2：銅 × 銀イオン（銀樹） |
| うすい塩酸 × 亜鉛 | ステージ3：亜鉛 × 塩酸（水素発生） |
| 硫酸銅(Ⅱ) × アルミニウム | ステージ4：アルミニウム × 銅(Ⅱ)イオン（2:3） |
| 過マンガン酸カリウム × 硫酸鉄(Ⅱ) | ステージ5 |
| 二クロム酸カリウム × 硫酸鉄(Ⅱ) | ステージ6 |
| 過マンガン酸カリウム × シュウ酸 | ステージ7 |
| 二クロム酸カリウム × エタノール／アセトアルデヒド／2-プロパノール | ステージ8／9／10 |
| 希硝酸 × 銅／濃硝酸 × 銅 | ステージ13／14 |

橋が**出ない**ことも確かめた: KMnO₄ × KI（酸性・中性/塩基性のどちらも反応するが未収録）・
O₃ × H₂O₂・うすい塩酸 × 銅（`ladder-reversed`）・濃硝酸 × Al（`exception`）。

### 13-5. 段0の高さについて（M6-D の前提の但し書き）

段0の**初期高さは 415px のまま**（M6-D と同値。橋は選ぶまで出ないので、
「畳まなくても段1が画面外へ押し出されない」という §11-1-2 の前提は変わっていない）。

ただし**試したあとの段0**は M6-D の時点ですでに 1画面（452px）を超えている
（KMnO₄×FeSO₄ で 620px。判定文＋直せる方向＋判定の根拠＋「梯子の全体を見る」が下に伸びるため）。
橋はここにさらに 40〜76px 足す（同じ組で 696px）。**押したあとに下へ伸びるのは
「結果を読む」動線なのでそのままにした**が、行き先が3本4本と増えると効いてくる。
増えるときは見出し1行＋行き先ぶんに絞ってある今の形を見直すこと。

### 13-6. やり残し（M6-E の範囲外）

- §6 の 19（`audit.js` に全ペア × 倍率のファズ）は**引き続き未着手**
- `portal.html` / `library.html` への露出は 9-7 のとおり入口の全体見直し待ち
- ri1 / ri2 へ**ピッカーから**届く道は無い（アセトンやエタノールを「ヨードホルム反応の試薬」として
  持たせるには切り離しの段が要る。M6-F の範囲を超える）

---

## 14. M6-F の実装記録（試薬の拡充。**1試薬＝1コミット**）

§9-0 の 9-3 で「入れる」と決まった3つ（熱濃硫酸・SO₂・H₂S）を、1つずつ足していく。
どれも「梯子に1行・`REAGENTS` に1行・必要なら半反応式1本」で、全ペアのテストが自動で増える。

### 14-1. 熱濃硫酸（v155・2026-08-07）

**ヘッダーは1pxも太っていない**（`redox.html` / `redox.html?free=1` / `condition.html` / `index.html`
とも 320×568 / 568×320 / 375×812 で **116 / 42 / 116px**。横はみ出し0）。
回帰テストは 169 → **171 件**（モデル群 +2）で全合格。既存14ステージは通常の入口で 14/14 完走。

#### 足したもの

| 種類 | 中身 |
|---|---|
| 半反応式 | `H2SO4_hot_red`（H₂SO₄ ＋ 2H⁺ ＋ 2e⁻ → SO₂ ＋ 2H₂O）。対は `H2SO4/SO2` |
| 酸化数 | `H2SO4`（S＝+6）・`SO2`（S＝+4）。`SO2` は `redox.js` の `BUBBLE_SP` に既に入っていたので、泡になって出ていくところは無改修で動いた |
| 試薬 | `H2SO4_hot`（**札は「熱濃硫酸」**） |
| 列挙表 | `LISTED_OXIDANTS["H2SO4_hot_red"] = { partners: ["Cu_ox"], why: … }` |
| 粒の見た目 | `RSTYLE` に `H2SO4` と `SO2` |

#### 検算（自分で組み立てて確かめたもの）

- 原子: 左 H＝2＋2＝4・S＝1・O＝4 ／ 右 H＝4（2H₂O）・S＝1・O＝2＋2＝4 → **一致**
- 電荷: 左 0 ＋ (+2) ＋ (−2) ＝ 0 ／ 右 0 → **一致**
- 酸化数: S が **+6 → +4** で Δ＝−2 ＝ **e⁻ 2個**。O は左右とも 4個が −2 のまま、
  H は左右とも 4個が +1 のままで動かない（変化する元素が1つ ＝ 既存の総なめテストが要求する形）
- 足し合わせ（銅・1:1）: **Cu ＋ H₂SO₄ ＋ 2H⁺ → Cu²⁺ ＋ SO₂ ＋ 2H₂O**（実測でつり合い・SO₂ は泡で1個。
  教科書の分子式 Cu ＋ 2H₂SO₄ → CuSO₄ ＋ SO₂ ＋ 2H₂O と整合する。左辺の 2H⁺ がもう1分子の硫酸ぶん）

#### 判断したこと

1. **式は分子の H₂SO₄ で書く**（SO₄²⁻ ＋ 4H⁺ ＋ 2e⁻ → SO₂ ＋ 2H₂O にしない）。
   うすい硫酸の SO₄²⁻ は銅を溶かさないので、イオンの形で書くと**別の話に化ける**。
   教科書の「主な酸化剤」の表も、濃硫酸のときだけ分子の形で書いている
2. **梯子には載せない**（順位を与えない）。標準電極電位でいえば SO₄²⁻/SO₂ は
   Cu²⁺/Cu より**下**にあり、素直に梯子へ置くと「銅は溶けない」になってしまう。
   実際に溶かすのは「熱くて濃い」からで、これは §9-1 の濃硝酸とまったく同じ**濃度効果**＝
   一次元の梯子に乗らないもの。逆に無理やり上へ置けば、こんどは Zn・Fe・Mg・シュウ酸・KI…と
   下にあるもの全部に「反応する」と言い切ってしまう。§2-7「梯子で語れないものは、
   正直に列挙する」に従って `LISTED_OXIDANTS` に置いた
3. **相手は銅1本に絞った**。この試薬の見どころは
   「**銅は塩酸やうすい硫酸には溶けないのに、熱濃硫酸には溶ける**」という対比で、
   うすい塩酸 × 銅（`ladder-reversed`）とちょうど背中合わせになる。
   Zn・Fe・Mg も実際には溶けるが、生成物が SO₂・S・H₂S と条件で変わって一本に決まらない
   （しかも冷濃硫酸なら Fe は不動態）ので、言い切らずに `undecided`（`not-listed`）に落とす
4. **`LISTED_OXIDANTS` の値を配列からオブジェクト（`{ partners, why }`）に変えた**。
   M6-D の1件だけだったころは「梯子は酸性条件のものだけを持っています」という
   **液性の話**を理由文に固定していたが、熱濃硫酸で外れたときの理由は**濃度・温度の話**で別もの。
   同じ文を使い回すと画面が嘘をつくので、理由を鍵ごとに持たせた
   （使い回していないことをテストで見張っている）
5. **札は必ず「熱濃硫酸」**（`qa/KNOWLEDGE_CAVEATS.md` H-2）。
   「濃硫酸」「硫酸」という札を `REAGENTS` のどこにも作らないことをテストで固定した。
   但し書きに「冷たい濃硫酸は Al・Fe・Ni の表面に被膜をつくる（不動態）」を添えている

#### 実測（硫酸酸性・還元剤12種すべてに当てた）

| 相手 | 結果 |
|---|---|
| 銅 | **reacts**（1:1・SO₂ が泡で1個・クリアまで完走） |
| Mg・Al・Zn・Fe・FeSO₄・シュウ酸・KI・H₂O₂・エタノール・アセトアルデヒド・2-プロパノール | `undecided` / `not-listed`（「反応しない」とは言っていない） |
| 中性・塩基性（全12種） | `undecided` / `wrong-condition`（その液性の式を持っていないので、「反応しない」ではなく「別の式になる／未収録」） |

**残した気になる点**: 不動態は `REDOX_EXCEPTIONS` に足していない。例外表は
「**梯子では reacts になるペアだけ**を載せる」（二重持ちを防ぐため。テストで固定）ので、
そもそも順位を持たない熱濃硫酸は載せられない。冷濃硫酸の不動態は**試薬の但し書き**で伝えている。
不動態そのものを反応として扱うなら「冷濃硫酸」を別の試薬として立てることになるが、
それは 9-3 が決めた範囲の外（`qa/` 側の知識項目として扱うほうが素直）。

### 14-2. 硫化水素（v156・2026-08-07）— **SO₂ より先に入れた**

**順番を入れ替えた。** §9-3 と §7 の並びは「熱濃硫酸・SO₂・H₂S」だが、**SO₂ を先に入れると
「酸化剤の欄に置けるのに相手が一人もいない試薬」ができてしまう**。SO₂ が酸化剤としてはたらく
相手は、高校の範囲では**実質 H₂S だけ**（教科書も「SO₂ は還元剤としてはたらくことが多いが、
H₂S に対しては酸化剤としてはたらく」という言い方をする）。だから H₂S を先に入れ、
SO₂（14-3）で二役がその場で成立するようにした。並びは優先度ではなく、
**1つずつ確実に入れるための順番**（§9-0 の但し書き）なので、この入れ替えは方針に反しない。

**ヘッダーは1pxも太っていない**（4ページとも 116 / 42 / 116px・横はみ出し0）。
回帰テストは 171 → **172 件**（モデル群 +1）で全合格。既存14ステージは 14/14 完走。

#### 足したもの

| 種類 | 中身 |
|---|---|
| 化学種 | `H2S`（硫化水素・腐卵臭）・`S`（硫黄・淡黄色・水にとけない） |
| 酸化数 | `H2S`（S＝−2）・`S`（S＝0） |
| 半反応式 | `H2S_ox`（H₂S → S ＋ 2H⁺ ＋ 2e⁻）。対は `S/H2S` |
| 梯子 | `"S/H2S": 85`（H⁺/H₂ の 80 と Cu²⁺/Cu の 90 のあいだ） |
| 試薬 | `H2S`（**還元剤の欄にしか出ない**） |
| 例外表 | Cu²⁺ × H₂S → CuS（黒色沈殿）／Ag⁺ × H₂S → Ag₂S（黒色沈殿） |
| 粒の見た目 | `RSTYLE` に `H2S` と `S` |

#### 検算

- 原子: 左 H＝2・S＝1 ／ 右 S＝1・H＝2 → **一致**
- 電荷: 左 0 ／ 右 0 ＋ 2(+1) ＋ 2(−1) ＝ 0 → **一致**
- 酸化数: S が **−2 → 0** で Δ＝+2 ＝ **e⁻ 2個**（H は左右とも 2個が +1 のまま。変化する元素は1つ）
- 組み上がり（実測・どれもクリアまで完走）:
  - KMnO₄ と 5:2 → **5H₂S ＋ 2MnO₄⁻ ＋ 6H⁺ → 5S ＋ 2Mn²⁺ ＋ 8H₂O**（教科書どおり）
  - I₂ と 1:1 → **H₂S ＋ I₂ → S ＋ 2H⁺ ＋ 2I⁻**
  - O₃ と 1:1 → **H₂S ＋ O₃ → S ＋ O₂ ＋ H₂O**

#### 順位を 85 に置いた根拠

標準電極電位でいえば S/H₂S は **+0.14 V** で、H⁺/H₂（0.00 V ＝ 80）と Cu²⁺/Cu（+0.34 V ＝ 90）の
あいだに入る。ここに置くと、教科書が扱う結論が**全部そろって出る**:

- KMnO₄・K₂Cr₂O₇・O₃・H₂O₂・希硝酸・濃硝酸・I₂ に酸化される（＝ reacts）
- **うすい塩酸では酸化されない**（`ladder-reversed`）＝「H₂S は何にでも酸化される」わけではない

つまり `pairsWith` で絞る必要が無かった（熱濃硫酸やシュウ酸と違い、梯子がそのまま正しい）。
1つでも崩れたら順位を動かしたということなので、この7件と塩酸の1件をテストで固定した。

なお **S/H₂S（+0.14 V）と SO₄²⁻/SO₂（+0.17 V・14-3）は同じ 85 に置く**。差が
高校で扱える解像度より細かいのと、この2つが酸化剤×還元剤として突き合わされる場面が
そもそも無いため（どちらも酸化の側の対）。§2-3「順位を付けられないものに順位を付けない」。

#### Cu²⁺・Ag⁺ は例外表（AgI とまったく同じ形）

順位では Cu²⁺（90）・Ag⁺（120）＞ S/H₂S（85）なので「進む向き」になるが、実際には
**先に黒い硫化物の沈殿ができてそこで止まる**。しかも CuS も Ag₂S も**硫黄は −2 のまま・
金属も酸化数が動かない**ので、起きているのは酸化還元ですらない。説明文にもそう書いた。
例外表は「梯子では reacts になるペアだけを載せる」約束（二重持ちを防ぐ）を満たしている。

#### 実測（酸化剤11種すべてに当てた）

| 相手 | 硫酸酸性 | 中性・塩基性 |
|---|---|---|
| KMnO₄・K₂Cr₂O₇・希硝酸・濃硝酸・H₂O₂・O₃ | **reacts** | `wrong-condition` |
| I₂ | **reacts** | **reacts**（この式に H⁺ も OH⁻ も無いので液性で書き方が変わらない） |
| うすい塩酸 | `ladder-reversed` | `wrong-condition` |
| 熱濃硫酸 | `not-listed` | `wrong-condition` |
| 硫酸銅(Ⅱ)・硝酸銀 | `exception`（CuS / Ag₂S） | `exception` |

**気になる点として残したもの**: `H2S_ox` は右辺に H⁺ が出る「酸性の書き方」の式だが、
`writtenFor` の約束（**右辺の H⁺ は酸性必須にしない**。§12-2-1）に従って液性を絞っていない。
だから中性・塩基性でも I₂ とは反応すると言う。中性ならこれで正しい（ヨウ素滴定の環境）が、
強い塩基性では I₂ 自身が不均化するので厳密ではない。§12-6 の
「中性・塩基性をひとまとめにしている」という既知の限界と同じ場所に立っている。

### 14-3. 二酸化硫黄（v157・2026-08-07）— 二役の代表

**ヘッダーは1pxも太っていない**（4ページとも 116 / 42 / 116px・横はみ出し0）。
回帰テストは 172 → **173 件**（モデル群 +1）で全合格。既存14ステージは 14/14 完走。

#### 足したもの

| 種類 | 中身 |
|---|---|
| 酸化数 | `SO4^2-`（S＝+6） |
| 半反応式 | `SO2_ox`（SO₂ ＋ 2H₂O → SO₄²⁻ ＋ 4H⁺ ＋ 2e⁻・対 `SO4^2-/SO2`）／`SO2_red`（SO₂ ＋ 4H⁺ ＋ 4e⁻ → S ＋ 2H₂O・対 `SO2/S`） |
| 梯子 | `"SO2/S": 95`（I₂/I⁻ の 100 と Cu²⁺/Cu の 90 のあいだ）／`"SO4^2-/SO2": 85`（S/H₂S と同値） |
| 試薬 | `SO2_asOxidant`（酸化剤の欄）と `SO2_asReductant`（還元剤の欄）の**2枚** |
| 粒の見た目 | `RSTYLE` に `SO4^2-` |

#### 検算

| 式 | 原子 | 電荷 | 酸化数 |
|---|---|---|---|
| SO₂ ＋ 2H₂O → SO₄²⁻ ＋ 4H⁺ ＋ 2e⁻ | 左 S1/O4/H4 ＝ 右 S1/O4/H4 | 左 0 ／ 右 −2＋4−2＝0 | S **+4 → +6**・Δ＝+2 ＝ **e⁻ 2個** |
| SO₂ ＋ 4H⁺ ＋ 4e⁻ → S ＋ 2H₂O | 左 S1/O2/H4 ＝ 右 S1/O2/H4 | 左 0＋4−4＝0 ／ 右 0 | S **+4 → 0**・Δ＝−4 ＝ **e⁻ 4個** |

組み上がり（実測・どれもクリアまで完走）:

- **2H₂S ＋ SO₂ → 3S ＋ 2H₂O**（2:1。左右の H⁺ が打ち消えて、教科書の式そのものになる）
- **5SO₂ ＋ 2H₂O ＋ 2MnO₄⁻ → 5SO₄²⁻ ＋ 4H⁺ ＋ 2Mn²⁺**（5:2）
- **SO₂ ＋ 2H₂O ＋ I₂ → SO₄²⁻ ＋ 4H⁺ ＋ 2I⁻**（1:1・ヨウ素滴定）

#### 順位を置いた根拠と、絞った範囲

| 対 | 順位 | 根拠 |
|---|---|---|
| `SO2/S`（酸化剤の顔） | 95 | E° ＝ +0.45 V。I₂/I⁻（+0.54＝100）と Cu²⁺/Cu（+0.34＝90）のあいだ。ここに置くと **「SO₂ は I⁻ から e⁻ を奪えない（順序が逆）」**が梯子から出る ＝ SO₂ と I₂ が出会えば SO₂ のほうが還元剤にまわる、という向きを言える |
| `SO4^2-/SO2`（還元剤の顔） | 85 | E° ＝ +0.17 V。S/H₂S（+0.14）と差が高校の解像度より細かいので**同じ値にして強弱を決めない**（§2-3）。この2つはどちらも酸化の側の対なので、突き合わされる場面もない |

**絞ったところ**（順位を動かすのではなく、言い切る範囲を絞る。§2-8-4）:

- 酸化剤の顔 … `pairsWith: ["H2S_ox"]`。順位（95）だけで見ると Cu・Zn・Fe・Mg にも
  「反応する」と言うことになるが、それは教科書が扱う話ではない（硫化物ができる別の反応になる）。
  ここで `LISTED_OXIDANTS` ではなく `pairsWith` を使ったのは、**梯子の結論を1つ残したい**から
  ＝ KI 相手の `ladder-reversed` はこの試薬のいちばん大事な手ざわりで、
  列挙表に逃がすと（列挙表は梯子より先に効くので）その1行が出なくなる
- 還元剤の顔 … `pairsWith: ["MnO4_red","Cr2O7_red","I2_red","H2O2_red","O3_red"]`。
  順位（85）が低いので、絞らないとほぼ全部の酸化剤と反応することになる（シュウ酸と同じ事情）。
  硝酸・Ag⁺・Cu²⁺ は実際に起こることもあるが高校では扱わないので言い切らない
- **自分どうし（不均化）も言い切らない**。3SO₂ ＋ 2H₂O → S ＋ 2SO₄²⁻ ＋ 4H⁺ は
  水溶液では起こらないし高校でも扱わない。酸化剤側の `pairsWith` が
  そのまま塞いでいる（`undecided` / `not-listed`）

#### 実測

| 選んだもの（硫酸酸性） | 結果 |
|---|---|
| SO₂（酸化剤として）× 硫化水素 | **reacts**（2:1・S が3個できる・クリアまで完走） |
| SO₂（酸化剤として）× KI・硫酸鉄(Ⅱ)・H₂O₂ | `ladder-reversed`（順序が逆 ＝ 相手のほうが強い） |
| SO₂（酸化剤として）× Mg・Al・Zn・Fe・Cu・シュウ酸・有機3種・SO₂（還元剤として） | `undecided` / `not-listed` |
| KMnO₄・K₂Cr₂O₇・H₂O₂・O₃・I₂ × SO₂（還元剤として） | **reacts** |
| うすい塩酸 × SO₂（還元剤として） | `ladder-reversed` |
| 希硝酸・濃硝酸・硝酸銀・硫酸銅(Ⅱ)・熱濃硫酸 × SO₂（還元剤として） | `undecided` / `not-listed` |
| 中性・塩基性 | I₂ 以外はすべて `wrong-condition`（I₂ の式だけ H⁺ も OH⁻ も含まないため） |

#### 梯子の全体（320×568・実測）

順位の数値は出ないまま、下段に硫黄の対が3つ増える。金属の並びは変わらない。

```
  O₃／H₂O₂／MnO₄⁻／Cr₂O₇²⁻／O₂／NO₃⁻                       ← 「強い酸化剤」として習うもの
  ────────────────────────────────────────────────────
  Ag ＞ Fe³⁺/Fe²⁺ ＞ O₂/H₂O₂ ＞ I₂/I⁻ ＞ SO₂/S ＞ Cu
     ＞ S/H₂S ＞ SO₄²⁻/SO₂ ＞ (H) ＞ Fe ＞ CO₂/C₂O₄²⁻ ＞ Zn ＞ Al ＞ Mg
```

太字の金属だけを拾うと **Ag ＞ Cu ＞ (H) ＞ Fe ＞ Zn ＞ Al ＞ Mg**（＝イオン化傾向のまま。
テストで固定）。梯子の全体を開いた状態の高さは 361px で、320px でも横にはみ出さない。

**気になる点として残したもの**: **熱濃硫酸（14-1）は順位を持たないので、この表に出てこない**。
判定の見出しが「順位ではなく、このアプリが収録している組み合わせとして扱っている。」と
断るので嘘にはならないが、「強い酸化剤の代表なのに表にいない」ことの説明は画面に無い。
順位を持たないものを表に**うすい字で置く**（順位のあるものと見分けられる形で）のは
考えられるが、それは梯子の見せ方そのものの設計変更なので、ここでは触っていない。

### 14-4. M6-F を終えての残り

- **収録した試薬は 10 ox × 12 red → 13 ox × 14 red**（`REAGENTS` 22 → 25 枚）。
  全ペアの検査は `REAGENTS` を総なめするので**自動で 120 組 → 182 組**に増えている
- §6 の 19（`audit.js` に全ペア × 倍率のファズ）は**まだ未着手**
- §12-6 の「強い塩基性でだけ起こる副反応」は依然として扱っていない。
  H₂S・SO₂ を足したことで、中性・塩基性で I₂ とだけ反応すると言う組み合わせが増えた
  （中性なら正しいが、強い塩基性では I₂ 自身が不均化する）。ここは液性を
  「中性」と「塩基性」に割るかどうかの判断が要る ＝ 単独で1つの検討事項
