# 反応ライブラリとインデックス設計（2026-07-24 合意）

「反応式を網羅し、検索できるライブラリにする」ための設計書。
既存の [DESIGN_reaction_types.md](DESIGN_reaction_types.md)（反応3群とアニメ分類）と
[DESIGN_redox.md](DESIGN_redox.md)（酸化還元エンジン）の上位に立ち、データ構造・分類・検索・
アニメーションタイプの管理方針を定める。**進行状況は DEVELOPMENT.md が正**。

## 目的

1. 反応式を**網羅**する（中和・沈殿・気体・酸化還元・錯イオン…）
2. **分類から検索**（反応の型・塩の分類・酸化還元の種別・単元）
3. **登場する物質から検索**（「KMnO₄ を含む反応」「Fe²⁺ が出る反応」）
4. **分類に応じたアニメーションタイプ**を順次実装する（未実装が多い前提の設計）
5. 酸化還元を**溶液中の反応（KMnO₄ など）**に拡張する（金属樹だけでない）

## 設計の核：ライブラリ層とアニメ層を分離する

網羅（データ）とアニメ（実装）を切り離すのが要。

- **反応エントリ（データ）** — 反応式・イオン反応式・登場物質・分類・単元・`animationType`・解説。
  これは**先に全部そろえられる**。
- **アニメーションタイプ（実装）** — 分類に紐づく描画エンジン。`animationType` が実装済みなら
  「遊べるステージ」、未実装なら**インデックスに式・解説だけ載る参照エントリ**（または汎用フォールバック）。

これにより「まだ作っていないアニメが多い」問題が解ける：反応は先に網羅してインデックスに載り、
アニメは分類ごとに後から足す。反応が `animationType` を宣言し、レジストリが type→描画を対応づけ、
未実装なら「準備中」表示にフォールバックする。

```
反応エントリ ──(animationType)──▶ アニメレジストリ ──▶ 実装あり: 遊べる
                                                    └─▶ 実装なし: 参照のみ（式＋解説）
```

## データアーキテクチャ：reactions.json へ移行

現行の `model.js` 埋め込み配列（STAGES / REDOX_STAGES）から、**`reactions.json` に外出しして
fetch する**方式へ移行する（網羅・検索・自動検証・大量作成に向く。姉妹 Chem-Assembler と同じ流儀）。

- **重要な帰結**：`fetch` 導入で **file:// 直開きは不可**になり、**ローカルサーバー必須**になる。
  DEVELOPMENT.md の設計原則「v1 は JS 埋め込み」の記述をこの移行時に更新すること。
  公開は既にサーバー経由（GitHub Pages）なので本番影響はない。
- `model.js` は**純ロジック（判定・集計・電離・半反応式演算）に専念**し、データは JSON に移す。
  SPECIES / STRUCTURE / PARTS / DISSOCIATION も **JSON 化する**（例：`data.json`。種の網羅と検証がしやすい）。
- 派生インデックス（**物質→反応の逆引き**、分類→反応の一覧）は**ロード時にコードで構築**する
  （JSON には持たせない。`species` 配列から自動生成）。

### reactions.json スキーマ（案）

```jsonc
{
  "id": "neutral-h2so4-2naoh",              // 一意キー（kebab）
  "reactants": ["H2SO4", "NaOH"],
  "products":  ["Na2SO4", "H2O"],
  "coeffs":    [1, 2, 1, 2],                // reactants→products の順。最簡整数比
  "netIonic":  "H⁺ + OH⁻ → H₂O",
  "classes": {                              // 分類（複数軸。検索の主キー）
    "type": "中和",                          // 反応の型（enum、下記タキソノミー）
    "saltType": "正塩",                      // 塩の分類（該当時のみ）
    "redox": null                           // 酸化還元の種別（該当時のみ）
  },
  "units": ["中和"],                         // 単元タグ（教科対応。複数可）
  "species": ["H2SO4","NaOH","Na2SO4","H2O","H+","OH-","SO4^2-","Na+"], // 検索用（分子＋イオン）
  "animationType": "combine",               // アニメレジストリのキー
  "rules": [ { "find":["H+","OH-"], "make":"H2O", "kind":"combine" } ], // アニメ駆動データ（型依存）
  "note": "強酸×強塩基の完全中和。傍観イオン Na⁺・SO₄²⁻ が残る。",
  "difficulty": 1                           // 1〜5（表示・絞り込み用）
}
```

- **`species`** は検索の要。分子と、水中で生じる主なイオンの両方を入れる（逆引きの網羅性のため）。
  ロード時に `species → [reactionId...]` の逆引きマップを構築する。
- `rules` / 半反応式など**アニメ駆動データは型ごとに形が違う**。共通フィールドは上記、
  型固有フィールドは `animationType` ごとに定義（レジストリが解釈）。
- **`playable` フィールドは持たない**（v164 で削除。当初案にはあったが、手書きの真偽値は
  実装とすぐ食い違う）。遊べるかどうかは `resolvePlayback()` が**導出する**（次節）。
  遊べない反応もインデックスには出る（式・イオン反応式・解説・分類のみ表示）。

## 分類タキソノミー（enum）

検索フィルタの軸。**反応の型**を主軸に、補助軸で塩・酸化還元・単元を持つ。

### 反応の型 `classes.type`
`中和` / `沈殿` / `気体発生` / `弱酸弱塩基の遊離` / `酸化還元` / `錯イオン生成` /
`加水分解` / `分子反応`（化合・分解・燃焼） / `その他`

### 塩の分類 `classes.saltType`（中和・塩系のみ）
`正塩` / `酸性塩` / `塩基性塩`

### 酸化還元の種別 `classes.redox`（酸化還元のみ）
`金属の析出`（イオン化傾向・金属樹） / `金属と酸`（H₂ 発生） /
`溶液中の酸化剤還元剤`（KMnO₄・K₂Cr₂O₇ など） / `ハロゲンの酸化力` / `電池` / `電気分解`

### 単元タグ `units`（教科対応。ステージ横断）
`中和` / `中和滴定` / `塩の分類` / `沈殿反応` / `気体の製法` / `イオン化傾向` /
`酸化還元滴定` / `電池・電気分解` / `錯イオン` …（現行 STAGE_TAGS を発展）

## 検索・インデックス UI（案）

トップに**反応インデックス**画面を置く。

- **分類フィルタ**：型・塩の分類・酸化還元種別・単元・難易度をチップで AND/OR 絞り込み
- **物質検索**：入力（例「KMnO4」「Fe2+」）→ `species` 逆引きで該当反応を列挙
- 一覧の各行：反応式（係数つき）＋分類バッジ＋**導出で遊べるなら**「遊ぶ」ボタン、
  参照のみなら式・イオン反応式・解説を展開
- 反応詳細はディープリンク可能に（`?rxn=<id>`）— 授業・共有で個別反応を指せる（**採用**）
- 参照のみの反応も、式・イオン反応式・解説に加え**数合わせビューを出す**

## アニメーションタイプ・レジストリ（Phase 3・v163〜v164 で実装）

各反応が `animationType` を宣言し、レジストリ `ANIMATIONS[type]`（`library.js`）が
**どの画面のどのエンジンで再生するか**を対応づける。未登録・未実装は
**「準備中」フォールバック**（式・イオン反応式・数合わせビューだけは共通で出せる）。

| animationType | 画面 | エンジン | ステージの在処 | 状態 |
|---|---|---|---|---|
| `aqueous` | index.html | app.js（中和・沈殿・気体発生・目標の塩） | `STAGES[id]` | ✅ |
| `complex-ion` | index.html | app.js の `kind:"complex"` ルールで**代用中** | `STAGES[id]` | 🔶 専用アニメ未実装 |
| `redox-metal` | redox.html | redox.js 金属モード | `REDOX_STAGES[redoxStage]` | ✅ |
| `redox-solution` | redox.html | redox.js 溶液モード | `REDOX_STAGES[redoxStage]` | ✅ |
| `weak-partial` | index.html | app.js の `partialRule`（per 個に1個・矢印 ⇄） | `STAGES[id]` | ✅ v165〜v167 |
| `molecular` | index.html | app.js の気体の空間・原子化（`phase:"gas"`） | `STAGES[id]` | ✅ v40 |

> **【2026-08-09 修正・v168】この表は2行が嘘だった。** `molecular` は「未実装」と書いてあったが、
> C群エンジンは **v40 から動いていた**（燃焼4本がそれで遊べていた）。にもかかわらず
> その4本は `animationType` を `aqueous` と付け違えていたので、**宣言と実態が同じだけずれ**、
> どちらを見ても食い違いに気づけなかった。`weak-partial` も v165〜v167 で実装したあと
> 宣言だけが「未実装」のまま残っていた。
>
> 実害は2つ。**(1)** 次の人がすでにあるエンジンを作り直す。**(2)** 索引の「準備中」の理由が
> 嘘になる（本当はステージが無いだけなのに、エンジンのせいにする）。
> 実際 `synthesis-nh3` / `combustion-c-o2` / `synthesis-hcl` の3本は
> `engine-pending` と表示されていたが、正しくは `stage-missing` ＝ **式を書いてステージを
> 足すだけで遊べる**。エンジン待ちだと思って手つかずになっていた。
>
> 再発防止は宣言の言い直しではなく**導出**にした。`animationTypeOf(rx, STAGES, REDOX_STAGES)`
> がステージの中身（`phase:"gas"` / `partialRule` / `kind:"complex"` / redox の `mode`）から
> 種別を導き、手書きの宣言と突き合わせる。ステージがまだ無い反応だけは導きようがないので、
> そこだけ宣言を信じる。**わざと付け違えて落ちることを確認ずみ**。

初期案では `combine` / `precipitate` / `gas` / `acid-salt` を別の型に分けていたが、
実装は**同じ app.js のルール表**で全部を賄っている（違いは `rules[].kind` の値だけ）。
型を細かく切っても同じエンジンを指すだけなので、実体に合わせて `aqueous` 1本にまとめた。

### 遊べるかどうかは導出する（手で書かない）

`playable` の手書きをやめ、`resolvePlayback(rx, stageIndex(STAGES, REDOX_STAGES))` が決める。

```
animationType → レジストリ ─┬─ screen が無い        → 準備中（reason: "engine-pending"）
                            └─ screen あり ─┬─ rx[stageKey] が実在 → 遊べる（screen?rxn=<stageId>）
                                            └─ 実在しない          → 準備中（reason: "stage-missing"）
未登録の animationType                                             → 準備中（reason: "unknown-type"）
```

手書きだと次が黙って起きる。導出だと構造的に起きない（起きても回帰テストが落ちる）:

- ステージを足したのに真偽値を上げ忘れ、**索引から遊べないまま**になる
- ステージを消した／id を変えたのに真偽値が残り、**押すと何も無いページへ飛ぶ**
- `animationType` と実際の遊び先が食い違う

### なぜ `animationType` を残して鍵にしたか（2026-08-08 判断）

Phase 3 の入口で `animationType` は**どの .js からも読まれない死にデータ**だった。
選択肢は「鍵として使う」か「消す」の2つで、**使う**を採った。

- 行き先の分岐は長らく「`redoxStage` フィールドがあるか」という**構造の偶然**に乗っていた。
  「この反応をどう見せるか」は設計判断なので、フィールドの有無に暗黙で背負わせず1つの表に集める
- レジストリにすると、データ側の宣言もれ（未登録の型・redox を名乗るのに `redoxStage` が無い）が
  機械で捕まる。未実装の型は `screen` を持たないので、**必ず**「準備中」に落ちる
- `aqueous` と `complex-ion` はどちらも index.html へ行くので、**行き先の分岐には効いていない**。
  それでも別の型として残すのは、「錯イオン専用のアニメ（配位子付加・色変化・沈殿の再溶解）が
  まだ無く、水溶液エンジンで代用している」という事実そのものだから。1画面1タイプに丸めると、
  この宿題が表から消えてしまう。レジストリは代用を明示する場所でもある

- 下段の**数合わせビュー**は全型共通基盤（PARTS の差し替えで対応、DESIGN_reaction_types.md 参照）。
  参照のみの反応でも係数練習は成立させられる。

## 酸化還元の溶液中への拡張（redox-solution）

現行 redox は「金属板＋溶液イオン」前提。溶液中どうしの電子授受は**新アニメ**が要るが、
**モデルは既存の `HALF_REACTIONS` 拡張で足りる**（半反応式×倍率→e⁻ 打ち消し→イオン反応式、の枠に乗る）。

### 追加する半反応式（例）

酸化剤（e⁻ を受け取る・酸性条件で H⁺ と水が絡む）:
- `MnO₄⁻ + 8H⁺ + 5e⁻ → Mn²⁺ + 4H₂O`（**紫 → ほぼ無色/淡桃**）
- `Cr₂O₇²⁻ + 14H⁺ + 6e⁻ → 2Cr³⁺ + 7H₂O`（**橙 → 緑**）
- `H₂O₂ + 2H⁺ + 2e⁻ → 2H₂O`（酸化剤として働く場合）

還元剤（e⁻ を出す）:
- `Fe²⁺ → Fe³⁺ + e⁻`
- `(COO)₂²⁻（シュウ酸イオン） → 2CO₂ + 2e⁻`
- `H₂O₂ → O₂ + 2H⁺ + 2e⁻`（還元剤として働く場合＝両性）
- `2I⁻ → I₂ + 2e⁻`

代表反応（イオン反応式）:
- `MnO₄⁻ + 5Fe²⁺ + 8H⁺ → Mn²⁺ + 5Fe³⁺ + 4H₂O`
- `2MnO₄⁻ + 5(COO)₂²⁻ + 16H⁺ → 2Mn²⁺ + 10CO₂ + 8H₂O`

### 新アニメの見せどころ
- **色変化（溶液全体＋粒の両方）**：溶液全体の色と粒個々の色を反応進行で遷移（紫→無色）。
  滴定の終点＝「紫が消えなくなる」を溶液全体の色で、粒レベルの変化を粒色遷移で見せる →
  単元 `酸化還元滴定` に直結
- **H⁺ の消費・H₂O の生成**：酸性条件が反応に必須なことを可視化
- **板なしの電子授受**：溶液中の粒どうしで e⁻ が渡る（`redox-metal` の板を外した動き）
- モデル不変条件（既存テストと同型）：酸化数の合計＝電荷、Δ酸化数＝e⁻ 数、
  半反応式の原子・電荷保存、e⁻ 打ち消し後のイオン反応式がつり合う

### 酸化数の拡張が必要な種
`OXIDATION` 表に Mn（+7/+2）、Cr（+6/+3）、O（過酸化物 −1）、C（シュウ酸/CO₂）などを追加。
多原子イオン内の酸化数（MnO₄⁻ の Mn=+7 は O=−2 から逆算）を扱う規則を DESIGN_redox.md に追記する。

## 段階計画（進行は DEVELOPMENT.md が正）

- **Phase 0：設計（本書）** — スキーマ・分類・レジストリ・移行方針を確定 ← いまここ
- **Phase 1：データ化と検証** — reactions.json 定義、既存12＋主要反応を移行、
  機械検証（原子・電荷保存、`classes`↔`rules` 整合、`animationType` 実在、`species` 網羅）。
  ローダを model.js/app.js に追加（サーバー必須化を DEVELOPMENT.md に反映）
- **Phase 2：インデックス/検索 UI** — 分類フィルタ＋物質逆引き。参照エントリの表示形式
- **Phase 3：アニメレジストリ整理**（v163〜v164 完了） — `ANIMATIONS` レジストリ＋
  `resolvePlayback()` による導出。手書きの `playable` は削除。未実装は「準備中」フォールバック
- **Phase 4：redox-solution 実装** — KMnO₄ を先頭に、色変化・H⁺参加の新アニメ
- **Phase 5 以降：** 錯イオン生成 / 弱酸弱塩基（M4）/ 分子反応（C群）を順次

## 品質保証（reactions.json の機械検証）

コミット前テストに追加する検証（データ駆動なので反応を足すほど自動で網羅が広がる）:

1. 全反応で**原子と電荷が保存**（coeffs で左右一致、最簡整数比）
2. `netIonic` が原子・電荷保存（傍観イオン除去後）
3. `species` が反応の全登場種（分子＋主要イオン）を**漏れなく**含む（逆引きの正しさ）
4. `classes.type` / `saltType` / `redox` / `units` が**タキソノミーの enum 内**
5. `animationType` が**レジストリに存在**（未実装なら「準備中」扱いだが型名は既知）。
   実装ありの型は画面・引数・ステージ集合をそろって宣言し、未実装の型は理由（`pending`）を持つ
6. `rules` / 半反応式などアニメ駆動データが**原子・電荷保存**（型ごとの検証）
7. `id` の一意性。遊べる反応は既存の総なめ UI テスト対象に自動編入
8. **導出の固定**（Phase 3）: 遊べる44件／準備中5件の内訳と、準備中の理由コードまで固定。
   「▶遊ぶ」の行き先が実在すること、**逆に**ステージがあるのに索引から遊べない反応が無いこと。
   `playable` を JSON に書き戻したら落ちること（手書きの対応表を作らない、の見張り）

## 確定事項（2026-07-24・ユーザー回答）

- **難易度**：`difficulty` は **1〜5**（数値）
- **参照のみ反応の表示**：式＋イオン反応式＋解説に加え、**数合わせビューも出す**
  （PARTS があれば係数練習は成立するので、遊べなくても数合わせは提供する）
- **ディープリンク**：**入れる**（`?rxn=<id>` で個別反応を直接開ける。授業・共有用）
- **SPECIES / STRUCTURE / PARTS / DISSOCIATION も JSON 化する**（種の網羅・検証がしやすくなる。
  data.json などにまとめ、model.js は純ロジックに専念）
- **統合順序：両立期間を置く**。既存 STAGES / REDOX_STAGES を残したまま reactions.json を導入し、
  検証・UI を育ててから段階的に置き換える（一気に切り替えない）
- **redox-solution の色**：**両方**（溶液全体の色＋粒の色遷移）。滴定終点の「紫が消えなくなる」を
  溶液全体の色で、粒レベルの変化を粒色遷移で見せる

## 追記（2026-08-08・Phase 3）

- **`playable` は廃止し、導出に一本化**（上記「遊べるかどうかは導出する」）。
  導入時に49件すべてで手書き値と導出値を突き合わせ、**食い違い0件**を確認してから削除した
- **`animationType` は鍵として残す**（理由は上記の節）。死にデータのまま置かない、が方針
