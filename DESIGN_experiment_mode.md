# 実験モード 設計書（2026-09-01・ユーザー方針 A）

## ★ 進捗（この文書のどこが実装済みか）

| 段 | 中身 | 状態 |
|---|---|---|
| **第1段（器）** | 🧪 実験タブ・瓶23本を左パレットへ・空振りの返し | ★ **公開済み（v1494）**。テスト `EX2`〜`EX4` |
| ★★ **第2段（課題）** | 課題12問・出発物を置く・到達判定・やり直し・記録 | ★ **実装済み（このレーン。2026-09-02）**。テスト `EQ2`〜`EQ8`・データは `assembler/quests.json` |

⚠⚠ **この文書には D-E3 決定より前に書かれた節がある。** 読むときは
`DESIGN_review_pack1.md` §2 の一覧（ユーザー決定が未反映の13か所）に従うこと。
とくに **§3-4（条件の2択で行き先を伏せる）は節ごと廃止**（D-E3「伏せない」）。
★ **既存の `DESIGN_reagent_palette.md`（瓶そのものの設計）と `DESIGN_molecule_modal.md`
（いまの置き場所の設計）とは別の文書にしてある** —— ここで決めるのは *出題の向き* であって、
瓶の中身でもモーダルの並びでもないため。

---

## 0. きっかけ（ユーザー原文・2026-09-01）

> ★ **現状薬品は分子選択から表示されているが、可能な反応とかぶる部分があり、モーダルを巨大にしている**
> ★ **薬品はパレット化し、選択してキャンバス上の分子に適応する**
> **ボタンでパレットーをフロートさせて呼び出すか、現状のパレットを tab 化するなど（実験モード）**
> ★★ **可能な反応は必要な知識を調べる、実験モードは実際にやってみる・自分の知識を確認するのが目的**

追加（同日）:

> ★ **実験モードには課題を用意するとよさそう**
> ★★ **例：エタノールからエテンをつくれ**

★★ **芯は最後の2行。**

| 入口 | 向き | 何をする |
|---|---|---|
| **「⚗ この分子にできること」（既存）** | ★ **引く**（調べる） | **分子 → できる反応**。アプリが答えを並べる |
| ★★ **実験モード（新）** | ★ **思い出す**（試す） | **試薬 → 起こること**。ユーザーが答えを持ってくる |

⚠ **いまは *引く* 側しか無い。** 試薬の瓶は既にあるが、**「できる反応」の一覧と同じ画面に
背中合わせで置いてある**ので、瓶を押す前に答えが目に入る。
★ **＝ これは機能追加ではなく、既にある材料の *見せる順番* を変える話。**

⚠ **パズルモードの「お題」とは別物**（混同しないこと）:

| | 何をする | 道具 |
|---|---|---|
| **パズルモード（既存）** | ★ **目標の分子を *組み立てる*** | 原子・モジュールのパレット |
| ★★ **実験モード（新）** | ★ **すでにある分子を *変える*** | 試薬の瓶 |

---

## 1. ★ 測ったこと（⚠ 推測ではない。すべて v1488 の実機・Playwright）

**測定条件**: `exp-mode` worktree（`origin/main` ＝ `d54781b`・v1488）を `:9713` に配り、
Chromium で 375×812 と 1280×800。**ライブラリ全 1001 件を総当り**（エラー0件）。

### 1-1. 在庫（`window.REAGENTS` / `window.REACTION_RULES` を直接数えた）

| | 実数 |
|---|--:|
| **試薬の瓶** | ★ **23本** |
| ├ 変える（`kind: 'transform'`） | **18本** |
| └ 調べる（`kind: 'detect'`） | **5本** |
| **反応ルール** | **48本** |
| ├ 実行できる（`!info`） | **42本** |
| └ 解説だけ（`info`） | **6本** |
| ★ **実行できるのに瓶を持たないルール** | ⚠ **11本** |
| **`DETECTION_TESTS`（呈色・検出）** | **5件** |
| **化合物ライブラリ** | **1001件** |

★ **瓶を持たない実行ルール 11本**（`RG5` が固定している一覧そのもの）:

`acetalization_pva` / `addition_polymerization` / `alkyne_polymerization` / `amidation` /
`condensation_glycoside` / `condensation_polymerization` / `cyclize_glucose_alpha` /
`cyclize_glucose_beta` / `dehydration_anhydride` / `diene_polymerization` / `open_glucopyranose`

⚠ **どれも「割り当て漏れ」ではなく、意図して瓶を持たせていない**（`RG5` のコメントに理由が全部書いてある）。
★ **したがって実験モードでは、この11本は *瓶では起こせない* ＝ 4章の主題になる。**

**瓶を持つ実行ルールは 42 − 11 = 31本。** 瓶を持つ `info` ルールは 6 − 2 = 4本。

### 1-2. ★★ 「かぶっている」の実数

★ **ユーザーの言う「かぶる部分」は、実測すると *ほぼ全部* だった。**

**数え方**（`RG6` と同じ突き合わせ方）: 「⚗ この分子にできること」に出ている反応カードの見出しから
ルールを引き当て、そのルールが `reagentId` を持つか（＝瓶からも辿れるか）を見る。

| | 実数 |
|---|--:|
| **反応カードが1件以上出る分子** | **735 / 1001 件** |
| **反応カード 延べ** | **3,322 件** |
| ★★ **うち、瓶からも辿れるもの（＝かぶり）** | ★★ **2,791 件 = 84.0 %** |
| **瓶からは辿れないもの（重合・環化・アミド化など）** | **531 件 = 16.0 %** |

⚠ **つまり「かぶる部分がある」ではなく「84% がかぶっている」。**
★ **同じ画面の上下に、同じ反応が2通りの引き方で並んでいる。**

代表分子での内訳（375×812）:

| 分子 | 反応カード | うち瓶からも辿れる |
|---|--:|--:|
| セロトニン | 13 | **12** |
| カプサイシン | 13 | **12** |
| インジゴ | 12 | **11** |
| スチレン | 12 | **11** |
| アントラセン | 11 | **11** |
| ケイ皮酸 | 13 | **11** |

★ **かぶらない側（16%）の正体は §1-1 の11本**（重合4・糖の環化開環3・グリコシド縮合・
アミド化・酸無水物・ビニロン）＝ **「瓶が無い反応」がそのまま「かぶらない反応」になっている。**

### 1-3. ★ モーダルの実測（⚠ いちばん大きくなる分子を全1001件から探した）

**最大は セロトニン。**（`scrollHeight` ＝ 中身の全高。`modal-content` 自体は画面に収まる高さで止まり、中でスクロールする）

| | 375×812 | 1280×800 |
|---|--:|--:|
| **モーダルの枠** | **316 × 731** | **450 × 776** |
| ★ **中身の全高（最大・セロトニン）** | ★ **1,959 px** | ★ **1,714 px** |
| 中身の全高（中央値・1001件） | **1,459 px** | — |
| 中身の全高（最小・反応0件の分子） | **1,386 px** | **1,212 px**（メタン） |
| うち「⚗ 反応」カード（最大） | **1,460 px** | **1,191 px** |
| ├ 反応ボタン列（最大・13件） | **598 px** | **542 px** |
| └ ★ **試薬の瓶の一覧** | ★ **683 px** | ★ **488 px** |
| 「🔬 調べる」カード | **252 px** | **252 px** |

★★ **瓶の一覧が中身に占める割合:**

| 分子 | 375×812 | 1280×800 |
|---|--:|--:|
| セロトニン（最大の分子） | **34.9 %** | **28.5 %** |
| エタノール | **42.4 %** | **34.6 %** |
| ベンゼン | **46.5 %** | **37.6 %** |
| ★ **メタン（反応0件）** | ★ **48.1 %** | ★ **40.2 %** |

⚠ **瓶の一覧は分子によらず一定（683px / 488px）**。23本を必ず全部並べるので、
**反応が1つも無い分子でもモーダルの半分弱を占める。**
★ **375px では画面高 812px に対して 683px** ＝ **瓶を見るだけで1画面まるごと使う。**

⚠ **統合セッションの読みと違ったところ**: 「分子によって変わるはず」は**反応ボタン列だけ**で、
**瓶の一覧は 1001 件すべてで同じ高さ**だった（`DESIGN_reagent_palette.md` §1.2「瓶はグレーアウトしない」の帰結）。

### 1-4. ★ パレットとキャンバスの実寸

| | 375×812 | 1280×800 |
|---|---|---|
| **ヘッダー** | 375 × 44 | 1280 × 50 |
| **原子パレット（`.atom-palette`）** | **265 × 46**（6要素・y=751） | **238 × 166**（6要素・y=164） |
| **モジュールパレット（`.module-palette`）** | ⚠ **中身 1179 × 44**（11要素・y=751。**横スクロール**） | **238 × 548**（11要素・y=377） |
| **左パネル（`#left-panel`）** | **363 × 83**（y=723 ＝ **最下段の帯**） | **280 × 720**（x=15, y=65 ＝ **縦の柱**） |
| **作業帯（`#work-strip`）** | **361 × 128**（y=588） | **953 × 88**（y=696） |

★ **375 では、パレットは最下段の 83px の帯の中で横スクロールしている**
（モジュール 11個で 1179px ＝ **画面幅の3.1倍**）。
★ **1280 では、左に 280×720 の柱があり、原子 166px ＋ モジュール 548px = 714px。**

⚠⚠ **ここは初版で「柱に余白が無い」と書いたが、測り直したら間違いだった。**
★ **`#left-panel` の `scrollHeight` は 1,450px** ＝ **枠 720px に対して 2.01 画面ぶん、
いまでも縦にスクロールしている**（原子とモジュールの上に操作モード・結合タイプなどが載る）。
★ **「余白が無い」のではなく「もう溢れている」** —— **5章の見積りはこの数え直しの上に立つ。**

---

## 2. ★★ 課題（「出発物 → 目標物」）が何通り作れるか —— **実測した**

★ **測り方**（推測ではない）: ライブラリ 1001 件を1つずつキャンバスに呼び出し、
**実行できる 42 本のルールの `detect` を全部当てて、通ったものは `apply` まで実行**し、
できた分子の**正準コード**をライブラリのコード表と突き合わせて名前を引いた。
できた有向グラフを BFS して最短手数で数えた。

### 2-1. 数（★ 副生成物を目標から除いた本数が「課題として成立する組」）

| | 副生成物も目標に数える | ★ **副生成物を除く** |
|---|--:|--:|
| **1手で届く** | 800 | ★ **775 通り** |
| **最短2手** | 770 | ★ **703 通り** |
| **最短3手** | 454 | ★ **431 通り** |
| **合計（3手まで）** | 2,024 | ★★ **1,909 通り** |

- **出発物になれる分子**: **419件** ／ **目標物になれる分子**: **315件**
- ⚠ **1手も当たらない分子**: **266 / 1001 件**（アルカンなど）

★★ **2手以上のほうが数が出ないのではないかという心配は当たらなかった** ——
**2手 703・3手 431 で、1手 775 と同じ桁**。⚠ **したがって「1手から始めることになる」ではなく、
最初から2手の課題を出せる。**

### 2-2. ⚠ この数の但し書き（★ 実装前に読むこと）

1. ⚠ **測定は「キャンバスに分子1つ」しか置いていない。**
   ★ **したがって相手の分子が要る 10本のルールは1件も発火していない**:
   `esterification` / `dehydration_inter` / `amidation` / `condensation_glycoside` /
   `vulcanization` / `acetalization_pva` / 重合4本。
   ★ **＝ 1,909 通りは *下限* であって上限ではない。**
   （実際の画面には `findPartnerHints` の「＋ 酢酸 を呼び出す」という道が既にある）
2. ⚠ **箇所は先頭の1つしか当てていない。** 2箇所以上ある反応で別の生成物になるものは数えていない。
3. ⚠ **1手の適用 2,742 件のうち、生成物にライブラリの名前が付いたのは 742 件（27.1%）**。
   ★ **残り 73% は「名前を引けない分子ができる」** ——
   芳香族置換や Na との反応が、名前の無い誘導体を大量に作るため（下表）。
   ★ **課題の *目標* は名前で言わないと出題にならないので、この 27% が課題の母数になる。**
4. ★ **実行後にキャンバスの分子が2つ以上になる適用は 273 / 2,742 件（10.0%）**
   ＝ ⚠ **副生成物が残るのは全体の1割。** `D-E4`（到達判定）の重さはこの数で決まる。

### 2-3. ★ ルール別の寄与（★ 上位ほど「当たるが名前が付かない」）

| 当たった分子 | うち名前が付く | ルール |
|--:|--:|---|
| 287 | **13** | `react_sodium`（Na との反応） |
| 272 | **11** | `aromatic_halogenation` |
| 270 | **15** | `aromatic_nitration` |
| 270 | **9** | `aromatic_sulfonation` |
| 210 | **15** | `neutralize_naoh` |
| 191 | **16** | `acetylation_anhydride` |
| 113 | **84** | ★ `add_h2`（水素化） |
| 81 | **77** | ★ `hydrolysis_ester` |
| 81 | **76** | ★ `saponification` |
| 73 | **73** | ★ `iodoform` |
| 72 | **66** | ★ `oxidize_aldehyde` |
| 27 | **21** | `oxidize_primary_vigorous` |
| 21 | **17** | `dehydration_intra` |

★ **課題を作るのに向くのは下半分**（当たる数は少ないが、当たれば名前が付く）。
⚠ **上半分は「効くが目標に名前が無い」ので、課題の *目標* には使えないが、
*外した一手* としては普通に起きる** ＝ 4章の「外したときどうするか」がここに効く。

### 2-4. ★ ユーザーの例「エタノール → エテン」は **いまのアプリでそのまま通る**

★ **実測**（`dehydration_intra`・瓶は濃硫酸）:

```
エタノール --[分子内脱水（-H₂O） → アルケン]--> エチレン（エテン） ＋ 水
```

⚠ **足りないものは1つも無い。** 濃硫酸の瓶を押すと `condition` の2択
（130〜140℃ ＝ 分子間脱水／160〜170℃ ＝ 分子内脱水）が出て、後者で通る（§11 の仕組み）。
★ **ただし `水` が別の分子としてキャンバスに残る**（`nParts: 2`）＝ `D-E4` の判定の話になる。

★ **定番の出発物から1手で届く目標**（実測。課題の素材そのもの）:

| 出発物 | 1手で届く目標 |
|---|---|
| **エタノール** | アセトアルデヒド／酢酸／ヨードホルム／ギ酸ナトリウム／★ **エチレン**／ナトリウムエトキシド |
| **エチレン** | 1,2-ジブロモエタン／エタン／アセトアルデヒド／ブロモエタン／クロロエタン／ヨードエタン／エタノール |
| **アセチレン** | エチレン／塩化ビニル／アセトアルデヒド |
| **ベンゼン** | ニトロベンゼン／ベンゼンスルホン酸／クロロベンゼン |
| **トルエン** | 安息香酸／o-ニトロトルエン／o-トルエンスルホン酸／o-クロロトルエン |
| **フェノール** | 酢酸フェニル／2,4,6-トリブロモフェノール／o-ニトロフェノール／ナトリウムフェノキシド ほか |
| **アニリン** | アセトアニリド／2,4,6-トリブロモアニリン／o-ニトロアニリン ほか |
| **ニトロベンゼン** | ★ **アニリン**／o-ジニトロベンゼン ほか |
| **2-プロパノール** | アセトン／ヨードホルム／プロペン |
| **サリチル酸** | ★ **アセチルサリチル酸**／サリチル酸ナトリウム |

⚠ **「ベンゼン → アニリン」が2手**（ニトロ化 → 還元）＝ **教科書の定番がそのまま2手の課題になる。**

### 2-5. ★ 到達の判定は **正準コードでそのまま出せる**（確かめた）

`window.canonicalCode(part)` が **`splitMolecules()` で割った1分子にそのまま効く**ことを実測で確認した
（ライブラリ 1001 件が全部コード化でき、**別名を除くと 977 通り**＝ 24 件は同じ構造の別名）。

⚠ **したがって判定の仕組みは新規に要らない。** ★ **決めが要るのは「何と一致したら合格か」だけ**（`D-E4`）。

---

## 3. ★★ 「引く」と「試す」をどう分けるか

### 3-1. ⚠ まず、いまの設計が明文で禁じていることを覆す

`DESIGN_reagent_palette.md` §1.1 に、はっきりこう書いてある:

> **主は自動案内のまま。試薬パレットは従。** 切り替えではなく併存させるが、
> **モードにはしない**（モードにすると「いまどっちにいるか」を覚える負担が増える。
> `DESIGN_ui_modes.md` の3モードの上にさらに階層を作らない）。

⚠⚠ **今回の方針は、この決定を正面からひっくり返す。** ★ **覆す理由は2つあり、どちらも実測で立つ:**

1. ★ **併存の代償が測ってみると大きすぎた** —— §1-2 の **84%** と §1-3 の
   **「反応0件の分子でもモーダルの 48% が瓶」**。**「従」のはずのものが常に画面の半分を占めている。**
2. ★★ **「試す」は、併存では原理的に成立しない。** ⚠ **背中合わせに置いても、
   スクロールすれば答えが見える** —— **答えを隠さない限り「思い出す」にならない。**
   ★ §1.1 が守ろうとしていたのは *データを2つにしない* ことで、それは今回も守る（下）。

★ **したがって覆すのは §1.1 の *後半*（置き場所とモード）だけで、*前半*（同じ1つのルール表を
2つの向きから引く）は 1文字も変えない。**

| §1.1 の主張 | 今回 |
|---|---|
| 同じ `REACTION_RULES` の上に立つ／試薬側に判定を書き写さない | ★ **そのまま守る**（新しい化学は1本も足さない） |
| 瓶はグレーアウトしない（効かない組み合わせを選べることが価値） | ★ **そのまま守る。むしろこれが実験モードの中心** |
| **モードにはしない** | ⚠ **覆す**（§3-3） |
| 置き場所は分子モーダルの中 | ⚠ **覆す**（§5） |

### 3-2. ★★ 答えが漏れていないかの棚卸し（⚠ 実測。**7か所あり、2か所は致命的**）

★ **「可能な反応を隠せば済む」ではなかった。** 1280×800 の実機で、モーダルに出る文言を
全部読み出して数えた結果:

| # | 場所 | 実際に出ている文言（エタノール） | 漏れ | 実験モードでの扱い |
|---|---|---|---|---|
| **1** | `#reaction-actions` の反応カード | `酸化 [O] → アルデヒド` ほか **5枚** | ⚠⚠ **答えそのもの** | ★ **出さない** |
| **2** | 同・相手呼び出しの札 | `＋ 酢酸 を呼び出す → エステル化（…）` **2枚**（`相手の分子が要る反応（2件）`） | ⚠⚠ **答えそのもの**（しかも押すと実行まで進む） | ★ **出さない**（§4-3 で外したときだけ返す） |
| **3** | ★★ **条件の2択**（`renderConditionChoice`） | ⚠⚠ `約160〜170℃（高温） → 分子内脱水（-H₂O） → アルケン` ／ `約130〜140℃（低温） → 分子間脱水 → エーテル` | ⚠⚠ **最大の漏れ** | ★ **行き先を伏せる**（§3-4） |
| **4** | 空振りの `acts` | `臭素水（Br₂）が効くのは、C=C や C≡C の不飽和結合と…` | ⚠ **次に何を試すかの答え** | ★ **2段目に下げる**（§4-2） |
| **5** | 空振りの `miss` | `赤褐色が消えないこと自体が…` | ★ 漏れではない（**陰性そのものが教材**） | ★ **そのまま出す** |
| **6** | `#molecule-props` の分類 | `1級アルコール` ／ `ベンゼン環（芳香族）` | ⚠ **手がかり**だが答えではない | ★ **出す**（§3-5） |
| **7** | ★ **瓶の札** | `過マンガン酸カリウム KMnO₄` —— **名前と示性式だけ** | ★★ **漏れなし** | ★ **そのまま** |

★★ **#7 が今回いちばん大きな収穫。** **瓶は 23本すべてが常に同じ見た目で並び、
効くか効かないかの手がかりを1つも持たない**（`DESIGN_reagent_palette.md` §1.2 の
「瓶をグレーアウトしない」が、そのまま「答えを漏らさない」を満たしている）。
⚠ **1001件すべてで瓶の一覧の高さが同じだったこと（§1-3）は、その裏返しの実測**。

⚠⚠ **#3 は、ユーザーの例題そのものを壊す。** ★ **「エタノールからエテンをつくれ」を出しておいて、
濃硫酸の瓶を押した瞬間に `→ アルケン` と書いた選択肢が出る** ＝ **答えが1タップで見える。**
★ **したがって §3-4 の手当ては必須で、これが実験モードで唯一「新しい見せ方」が要るところ。**

### 3-3. ★ 3モードの上に階層は作らない —— **実験モードは 🧪 自由モードの中の切り替え**

⚠ `DESIGN_ui_modes.md` の 🧩パズル／📚学習／🧪自由 の隣に4つ目を置くと、
§1.1 が心配していた「いまどっちにいるか」がそのまま現実になる。

★ **決め: 実験モードは 🧪 自由モードの中で、*左のパレットが何を並べているか* の切り替えとして持つ。**

| 🧪 自由モードの中の状態 | 左パレット | 分子モーダル |
|---|---|---|
| **作図**（既定・いまと同じ） | 原子・モジュール | ★ **「⚗ この分子にできること」＝ 引く** |
| ★★ **実験**（新） | ★ **試薬の瓶 23本** | ⚠ **「できること」を出さない ＝ 試す** |

★ **これなら「モードが増えた」ではなく「パレットが持ち替わった」に見える** ——
⚠ **パズルモードで原子パレットとモジュールパレットを持ち替えるのと同じ操作。**

### 3-4. ⚠⚠ 条件の2択（#3）をどう伏せるか

いま（実測）:

```
約160〜170℃（高温） → 分子内脱水（-H₂O） → アルケン
約130〜140℃（低温） → 分子間脱水（アルコール2分子, -H₂O） → エーテル（いまの分子では条件が足りません）
```

★ **実験モードでは `condition.label` だけを出し、`rule.label`（行き先）を落とす:**

```
約160〜170℃（高温）
約130〜140℃（低温）
```

⚠ **「条件が足りません」の但し書きも落とす** —— **それ自体が「そちらは違う」という答えになる。**
★ **押して初めて「足りない」と返す**（§4）。

⚠⚠ **これは §11.3「通らない選択肢を『押せるが何も起きない』にしない」を破る。**
★ **破ってよい理由**: §11.3 は **調べに来た人**の詰まりを防ぐ規則で、
**思い出しに来た人には「押したら違った」こそが答え合わせ**だから。
⚠ **ただし「何も起きない」にはしない** ——「130〜140℃ では、いまの分子だけでは進みません」と
理由は返す（§4-1 の1段目）。**規則の *目的*（詰まりを作らない）は守り、*手段*（先に言う）だけ変える。**

★ **`condition.label` は既に行き先を含んでいない**ことを確認済み（`穏やかに酸化` / `激しく酸化` /
`約160〜170℃（高温）`）＝ **データはそのまま使える。落とすのは連結している `rule.label` のほうだけ。**

⚠ **1か所だけ例外がある**: `oxidize_primary_vigorous` の `condition` は
`（過マンガン酸カリウムではふつうこちら）` という **`usually` の印**を選択肢に付けている。
★ **これは「行き先」ではなく「ふつうどちらを使うか」なので落とさなくてよい** ——
むしろ試薬名を覚える助けになる（§12-1 の役割分担のとおり）。

### 3-5. ★ 分類（#6）は残す —— **これは「答え」ではなく「読み」**

`1級アルコール` は **構造を読んだ結果**であって、**反応の答えではない**。
★ **落とすと、実験モードが「当てずっぽうの23択」になる**（23本 × 箇所 ＝ 実質もっと多い）。
⚠ **高校化学の実験は「この官能基には何が効くか」を思い出す作業**なので、
**官能基まで伏せるのは出題として過剰。**

⚠ **ただし1つだけ実測で分かった穴**: `#molecule-props` は **キャンバス全体**を読んでいる可能性がある
（§13.3 が反応カードについて同じ穴を報告している）。★ **実験モードでは1分子しか置かないので
第1段では表面化しないが、`D-E7` に残す。**

---

## 4. ★★ 外したときにどうするか

⚠ **「思い出す」モードなので、外れることが前提。** ★ **課題が付くと外れる回数はさらに増える**
（§2-3 の上半分 ＝ 「効くが目標に名前が付かない」一手が 1,300件以上ある）。

### 4-1. ★ 前例（`explainPlacementMiss`）から借りる4つ

`game.js` の `explainPlacementMiss`（v1110）を読んだ。★ **借りるのは仕組みではなく、4つの決め:**

| # | `explainPlacementMiss` の決め | 実験モードでの当てはめ |
|---|---|---|
| **①** | ★ **言葉は「なぜ」・図は「どこ」** | **言葉＝なぜ効かないか／図＝どの原子を見に行ったか**（`detect` が返した箇所） |
| **②** | ⚠ **同じ文言は出し直さない**（表示時間だけ延ばす） | ★ **同じ瓶を続けて押しても画面を動かさない**（`MISS_HOLD_MS = 800` と同じ考え方） |
| **③** | ⚠ **数字を文言に入れない**（変わると嘘になる値を書かない） | ★ **「あと何手」「正解率」を書かない**（§7-4） |
| **④** | ★ **叱らない**（「間違いです」を出さない） | ★ `explainReagentMiss` が既に守っている（「効くのはこれ」を返す） |

### 4-2. ★★ 3段に割る（⚠ いまは1段目と2段目が同時に出ている）

★ **実測した現行の応答**（エタン × 臭素水）は、**1回目からこれが全部返る**:

> 臭素水（Br₂）が効くのは、C=C や C≡C の不飽和結合と、…です。いまの分子にはどれもありません。
> **赤褐色が消えないこと自体が**「不飽和結合が無い」ことの証拠で、…（以下 `miss` が全文）

⚠ **これは *調べに来た人* には完璧だが、*思い出しに来た人* には「答えを全部言ってしまっている」。**

★ **実験モードでは同じ材料を3段に割り、1段ずつ本人が開く:**

| 段 | 何を返すか | 材料 | いつ出るか |
|---|---|---|---|
| **1** | ★ **起きなかったという事実だけ** —— 「臭素水では、この分子は変わりませんでした」 | **新規に1行** | ★ **押した直後（自動）** |
| **2** | ⚠ **陰性そのものの意味** —— `miss`（「赤褐色が消えないこと自体が…」） | ★ **既存の `reagent.miss`** | ★ **「なぜ？」を押したとき** |
| **3** | ⚠⚠ **効く相手** —— `acts`（「効くのは C=C や C≡C…」）＋ 相手呼び出しの札 | ★ **既存の `reagent.acts` / `findPartnerHints`** | ⚠ **「答えを見る」を押したときだけ** |

★★ **順番を `miss` → `acts` にひっくり返しているのが要点。**
⚠ **いまは `acts` が先で `miss` が後だが、`acts` のほうが答えに近い**
（`DESIGN_reagent_palette.md` §9.1「空振りを教材にする」が言っているのは `miss` のほうで、
`acts` は「調べ物の答え」）。★ **＝ 2段目までは実験モードでも無料で出してよい。**

⚠ **「答えを見る」を押したかどうかは記録する**（§7-4）。★ **押しても減点しない**が、
**課題の終わりに「答えを見ずに通した課題／見た課題」で並べ替えられるようにする**（`D-E9`）。

### 4-3. ★ 「相手が要る」だけは別扱い

★ **実測**（酢酸 × 濃硫酸）:

> 濃硫酸（H₂SO₄）は、いまの分子だけでは効きません。相手を呼び出すとできます:
> **＋ エタノール を呼び出す → エステル化（カルボン酸＋アルコール, -H₂O）**

⚠ **これは「外した」のではなく「合っているが材料が足りない」。**
★ **1段目で返すのは「相手の分子が要ります」まで**（**誰を呼ぶかは言わない**）。
⚠ **札そのものは3段目**（押すと反応名まで書いてあるため）。

★ **なぜ1段目で「相手が要る」とだけ言うか**: ⚠ **黙って何も起きないと、
「試薬を間違えた」と読まれる** ＝ **合っている一手を捨てさせてしまう。**
★ **`explainPlacementMiss` の「どちらに外したのか（遠すぎ／近すぎ）が分からない」と同じ形の詰まり。**

### 4-4. ⚠ 図の側（`explainPlacementMiss` の ①）

★ **`detect` が0箇所を返すとき、図には何も描けない**（見に行った場所が無い）。
⚠ **したがって「図＝どこ」を借りられるのは、`condition` で落ちたときだけ**
（`detect` は通ったが条件が足りない ＝ **箇所は分かっている**）。

★ **決め: 条件で落ちたときは、その箇所を一瞬だけ光らせる。**
**`detect` が0箇所のときは言葉だけ**（⚠ **無理に図を出さない** —— 前例が
「原子に名前が無いので言葉で名指しできない ＝ 図で丸をつける」と言っているのは
**指す先がある場合の話**）。

---

## 5. ★ tab か フロートか（⚠ 両案の重さを実測から見積もった）

### 5-1. 実測（§1-4 の数え直し ＋ 追加測定）

⚠⚠ **統合セッションへ渡した §1-4 の読み「1280 の左柱には縦の余白が無い」は *外れ* だった。**
★ **`#left-panel` は 280×720 の枠に対して `scrollHeight` が 1,450px** ＝
**いまでも 2.01 画面ぶんスクロールしている。**「余白が無い」のではなく「もう溢れている」。

| | 320×568 | 375×812 | 768×1024 | 1280×800 |
|---|---|---|---|---|
| **キャンバス（`#chem-svg`）** | 306×311 | **361×555** | 754×818 | **953×659** |
| **パレットの器（`#left-panel`）** | 308×83 | **363×83** | 756×83 | **280×720**（⚠ 中身 **1,450**） |
| **瓶の格子の列数** | 2 | **2** | 7 | **4** |
| **瓶23本の格子の高さ** | 706px | **651px** | 273px | **456px** |
| セル | 94×55 | 119×55 | 80×55 | 84×55 |

★ **375 では、パレットは画面最下段の 83px の帯**（モジュール 11個が **1,179px 幅で横スクロール**
＝ 画面幅の 3.1倍）。★ **1280 では左の 280px の柱。**

### 5-2. 両案

#### 案A: tab化（★ 既存のパレットの器に「🧪 試薬」タブを足す）

- **375**: 帯の中で **横スクロール**。⚠ 瓶23本 × 約90px ＝ **2,070px** ＝ **画面幅の 5.5倍**。
  ★ **モジュール（1,179px ＝ 3.1倍）が既に成立しているので、形としては前例がある**が、**1.8倍長い**
- **1280**: 柱の中で **3列 × 8行 ≒ 440px**。★ **原子(166) ＋ モジュール(548) と入れ替わるので、
  中身は 1,450 → 約 1,340px に *減る***（⚠ **タブは既存を置き換える。足すのではない**）
- ★ **キャンバスを1pxも隠さない**

#### 案B: フロート（★ ボタンで呼び出してキャンバスの上に浮かせる）

- **375**: キャンバスは **361×555**。⚠ **2列の格子は 651px で、キャンバスに入らない**。
  ★ 4列に詰めれば セル約85px × 6行 ＋ 見出し ≒ **360px**（キャンバスの **65%** を覆う）
- **1280**: キャンバスは **953×659**。7列なら **273px ＝ 41%** を覆う
- ⚠ **320×568 では キャンバスが 306×311 しかなく、4列でも 360px は入らない ＝ 覆いきる**

### 5-3. ★★ 推奨: **案A（tab化）**。⚠ ただし **375 は横スクロールにしない**

★ **理由（数で）:**

1. ⚠⚠ **フロートは「分子を見ながら試薬を選ぶ」ができない** ——
   **375 で 65%、320 で 100% キャンバスを覆う。** ★ **実験モードの中心は
   「キャンバス上の分子に適用する」なので、分子が見えないのは目的そのものを壊す**
2. ★ **tab は 1280 で中身を *減らす***（1,450 → 約1,340px）。⚠ **フロートは減らさない**
   （モーダルから瓶が消えるぶんは同じだが、パレットは原子＋モジュールのまま残る）
3. ★ **前例がある** —— **パレットの持ち替えは、原子↔モジュールで既に日常の操作**

⚠ **ただし 375 の横スクロール 2,070px は採らない。** ★ **実験モードのときだけ帯を2段にする**
（83px → 約145px。**モジュールの 44px 行が消えて、瓶の 55px 行が2段**）。
⚠ **キャンバスは 555 → 約493px に縮む（11%）** —— ★ **フロートの 65% とは桁が違う。**

★ **決めきれないのはここではなく「呼び出し方」**（`D-E5`）。

---

## 6. ⚠ 瓶を持たない反応（加熱・光など）をどう扱うか

### 6-1. 実数（★ `RG5` が固定している 11本）

| ルール | なぜ瓶が無いか（`RG5` の注記） | ★ 課題への寄与（実測） |
|---|---|--:|
| `addition_polymerization` / `alkyne_polymerization` / `diene_polymerization` / `condensation_polymerization` | 「並べた単量体をまとめる」操作でしかない | **0**（1分子では発火しない） |
| `cyclize_glucose_alpha` / `cyclize_glucose_beta` | 分子内の平衡（試薬なしで起こる） | **2** |
| `open_glucopyranose` | 同上 | **2** |
| `condensation_glycoside` | ⚠ 「濃硫酸を加えるとこうつながる」が**正しくない** | **0** |
| `amidation` | ⚠ **高校教材が試薬を名指ししない** | **0** |
| `dehydration_anhydride` | ⚠ **教科書は「加熱すると」としか書かない** | **3** |
| `acetalization_pva` | ★ 橋の炭素は**ユーザーが呼び出したホルムアルデヒド** | **0** |

★★ **実測の結論: 1手で届く 800通りのうち、瓶で起こせるのは 790通り（98.8%）。**
⚠ **瓶を持たない11本が課題に効くのは 7件だけ**（糖の環化開環4・酸無水物3）。

### 6-2. ★ 決め: **第1段ではパレットに「操作」を並べない**

⚠ **並べない理由は「少ないから」ではない。** ★ **「加熱」を瓶と同じ棚に置くと、
`RG5` が11本ぶん積み上げてきた「資料に無い試薬を名乗らせない」が壊れる** ——
**「加熱」は試薬ではないので、瓶の格子（`変えるもの` / `調べるもの`）のどちらにも入らない。**

★ **代わりに、いまある道をそのまま使う:**

- **重合4本・ビニロン・グリコシド縮合・アミド化** … ★ **`findPartnerHints` の札が既に入口**
  （「＋ ホルムアルデヒド を呼び出す」＝ `PY7` が見張っている）。**実験モードでは §4-3 の3段目に置く**
- **糖の環化・開環** … ★ **`DESIGN_sugar.md` 側の入口がある**
- **`dehydration_anhydride`（加熱）** … ⚠ **ここだけ入口が無い** ＝ `D-E6`

### 6-3. ⚠ したがって「操作の棚」は、**加熱1つのために作るかどうか**の話に縮む

★ **作るなら区分は3つ目**（`変えるもの` / `調べるもの` / **`加える操作`**）で、
中身は **加熱** 1つ。⚠ **`RG1` の「死んだ瓶が無い」検査は `reagentId` の実在を見ているので、
瓶ではない棚を足すと検査の外に出る** ＝ **足すなら `RG5` 側に「操作は何本か」を固定する検査が要る**
（§9 の見積りに入れてある）。

---

## 7. ★★ 課題（「出発物 → 目標物」）

### 7-1. 形

```
課題: エタノール から エチレン（エテン） をつくれ
```

- ★ **出発物はアプリが置く**（`summonMolecule` で1分子。⚠ **ユーザーに作らせない** ——
  作図はパズルモードの仕事で、実験モードは「すでにある分子を変える」）
- ★ **目標物は名前で出す**（⚠ **図では出さない** —— **図を出すと「作れ」に読める** ＝
  パズルモードと混ざる。**§0 の表の右下と左下を混ぜないための、いちばん効く1本**）
- ★ **手数の目安は出す**（「1手」「2手」）。⚠ **どの試薬かは言わない**

### 7-2. ★ 判定（⚠ 正準コードでそのまま出せることを確認済み・§2-5）

```
window.canonicalCode(part) を splitMolecules() の各部分に当て、
目標物の正準コードと一致する部分が「ある」か
```

⚠⚠ **「ある」か「それだけ」かは決めが要る**（`D-E4`）。★ **実測が答えを持っている:**

- **実行後にキャンバスの分子が2つ以上になる適用は 273 / 2,742 件（10.0%）**
- ★ **ユーザーの例題そのものが該当する** —— **エタノール → エチレン ＋ 水**
- ⚠ **「それだけ」にすると、ユーザーが最初に挙げた例が通らない**

★ **したがって推奨は「ある」。** ⚠ **ただし素通しではなく、**
**「目標の分子が1つ以上ある」＋「余っているのは反応で出た副生成物だけ」**にする
（⚠ **呼び出した相手の分子を消し忘れて通るのを防ぐ**）。
★ **副生成物かどうかは `apply` の戻り値では分からない**ので、
**「反応の履歴に無い分子が残っていないか」で見る**（`D-E4` に選択肢として書いた）。

### 7-3. ★ 何回でも試せる。⚠ 手数は数えるが罰しない

- ★ **`↩ 反応前に戻す` は既にある**（`game.undo()`・画面下の帯。`RX_UNDO_POINTER` が
  「この画面を閉じても押せます」と案内している）＝ **新規に作らない**
- ★ **外した一手は、戻さずに続けてもよい** —— ⚠ **「外した先から目標へ戻る道」が
  そのまま次の課題になる**（§2-1 の 2手703通りがその在庫）
- ⚠ **手数は数えるが、`explainPlacementMiss` の決め③（変わると嘘になる数字を書かない）に従い、
  「最短◯手」を画面に断定して書かない** —— ★ **§2-2 の但し書きのとおり、
  いまの測定は箇所1つ・分子1つの下限**であって、**本当の最短ではない**

### 7-4. ★ 記録するもの（⚠ 出題の向きを増やしただけなので、成績は作らない）

| 記録 | 何に使うか |
|---|---|
| **通した課題** | ★ 一覧に印 |
| ★ **「答えを見る」（§4-2 の3段目）を押したか** | ★ **並べ替えだけ**（⚠ **減点しない**） |
| **手数** | ★ 見せるが比べない |

⚠ **`localStorage` に置くか、置かないか** ＝ `D-E9`。

### 7-5. ★ 課題の在庫（実測 §2-1 から選ぶ）

⚠ **1,909通りを全部出題にはしない。** ★ **§2-3 の下半分（当たれば名前が付くルール）と、
§2-4 の定番の出発物で絞る。** ★ **第1段は 20〜30 問**（⚠ **手で選ぶ。自動生成しない** ——
**「ベンゼン → クロロベンゼン」は出題だが「セロトニン → 名前の無い誘導体」は出題ではない**）。

★ **第1段の候補（実測で1手ないし2手が通ることを確かめたもの）:**

| 課題 | 手数 | 通る道 |
|---|--:|---|
| エタノール → エチレン | **1** | 濃硫酸（160〜170℃） |
| エタノール → 酢酸 | **1** | 酸化剤（激しく） |
| エタノール → アセトアルデヒド | **1** | 酸化剤（穏やかに） |
| **ベンゼン → アニリン** | **2** | 混酸 → 水素・Ni |
| ベンゼン → ベンゼンスルホン酸 | **1** | 濃硫酸 |
| トルエン → 安息香酸 | **1** | 酸化剤 |
| フェノール → 2,4,6-トリブロモフェノール | **1** | 臭素水 |
| サリチル酸 → アセチルサリチル酸 | **1** | 無水酢酸 |
| アセチレン → 塩化ビニル | **1** | 塩化水素 |
| エチレン → エタノール | **1** | 水・酸触媒 |
| ニトロベンゼン → アニリン | **1** | 水素・Ni |
| 2-プロパノール → アセトン | **1** | 酸化剤 |

---

## 8. ★ 判断待ち（`D-E1` 〜。⚠ **勝手に決めたもの**と**聞くべきもの**を分けてある）

### 8-1. ⚠⚠ 聞くべきもの（★ ユーザーの判断が要る）

| # | 何を決めるか | 選択肢 | ★ こちらの推し |
|---|---|---|---|
| **D-E1** | ⚠⚠ **`DESIGN_reagent_palette.md` §1.1 の「モードにはしない」を覆してよいか** | **(a)** 覆す（実験モードを作る） ／ **(b)** 覆さず、モーダルの中で瓶を畳むだけにする | ★ **(a)**。⚠ **ただし §1.1 は理由を書いて決めた文なので、上書きの承認が要る** |
| **D-E2** | ★ **実験モードで「できる反応」を *どこまで* 消すか** | **(a)** 反応カードも相手呼び出しも全部消す ／ **(b)** 反応カードだけ消し、相手呼び出しは残す ／ **(c)** 「答えを見る」で開ける折りたたみにする | ★ **(a)**。⚠ **(b) は「＋ 酢酸 を呼び出す → エステル化」が答えそのもの**（実測） |
| **D-E3** | ⚠⚠ **条件の2択（§3-4）で行き先を伏せてよいか** | **(a)** 伏せる（`約160〜170℃` だけ） ／ **(b)** 伏せない | ★ **(a)**。⚠ **伏せないとユーザーの例題「エタノール→エテン」が1タップで割れる**。★ ただし **§11.3 を破る**ので承認が要る |
| **D-E4** | ★★ **到達判定は「目標がある」か「目標だけ」か** | **(a)** 目標の分子がキャンバスにあれば合格 ／ **(b)** 目標だけが残っていること ／ **(c)** ★ **目標があり、かつ余っているのは反応で出た副生成物だけ** | ★ **(c)**。⚠ **(b) はユーザーの例（水が残る）が通らない**。★ **(a) は呼び出した相手を消し忘れても通る** |
| **D-E5** | ★ **実験モードへの入り方** | **(a)** パレットのタブ ／ **(b)** 課題を選ぶと自動で切り替わる ／ **(c)** 両方 | ★ **(c)**。★ **課題からの流入が主で、タブは戻り道** |
| **D-E6** | ⚠ **`dehydration_anhydride`（加熱）に入口を作るか**（§6-3） | **(a)** 第1段では作らない ／ **(b)** 「加える操作」の棚を作って加熱1つを置く | ★ **(a)**。⚠ **1本のために棚を作ると `RG1`/`RG5` の外に出る** |
| **D-E8** | ★ **課題を どこに置くか** | **(a)** `stages.json` の隣に新しい JSON ／ **(b)** `tutorials.json` に相乗り ／ **(c)** `compounds.json` に列を足す | ★ **(a)**。⚠ **(c) は `compounds.json` の1行1件の規約に触るので採らない** |
| **D-E9** | ★ **通した課題を `localStorage` に残すか** | **(a)** 残す ／ **(b)** 残さない（毎回まっさら） | ★ **(a)**。⚠ **ただし「成績」にはしない**（§7-4） |

### 8-2. ★ 勝手に決めたもの（⚠ **異論があれば止めてほしい、という形で並べる**）

| # | 決めたこと | 根拠 |
|---|---|---|
| **D-E10** | ★ **新しい化学は1本も足さない**（`REACTION_RULES` も `REAGENTS` も1行も変えない） | `DESIGN_reagent_palette.md` §1.1 の前半 |
| **D-E11** | ★ **`#molecule-props` の分類は残す** | §3-5。**伏せると23択の当てずっぽうになる** |
| **D-E12** | ★ **瓶の札は1文字も変えない** | §3-2 #7。**実測で漏れが無い** |
| **D-E13** | ★ **tab化を採り、フロートは採らない** | §5-3。**375 で 65%、320 で 100% キャンバスを覆う** |
| **D-E14** | ★ **`↩ 反応前に戻す` を新規に作らない** | 既存（`game.undo()`） |
| **D-E15** | ★ **目標物を図で出さない** | §7-1。**パズルモードと混ざる** |
| **D-E16** | ★ **課題は手で選ぶ**（自動生成しない） | §7-5。**1,909通りの大半は出題にならない** |

### 8-3. ⚠ 残っている穴（★ 測ったが決めていない）

| # | 何が分かっていないか |
|---|---|
| **D-E7** | ⚠ **`#molecule-props` がキャンバス全体を読んでいないか**（§13.3 が反応カードで報告した穴と同じ形）。★ **実験モードは1分子なので第1段では表面化しない** |
| **D-E17** | ⚠ **「箇所が2つ以上ある反応」で、どの箇所を選ぶかも出題になりうる**（測っていない）。★ 第1段は箇所1つの課題だけにする |

---

## 9. ★ 手数の見積り（⚠ `DEVELOPMENT.md`「見積りは『見せ方』を数に入れないと外れる」に従う）

⚠ **前回（`DESIGN_reaction_execution.md` §21-4）は `detect` と `apply` しか数えずに
「新規4・既存0・テスト0」と見積もり、実際は「新規5＋メソッド1・既存1・テスト2」になった。**
★ **今回は最初から4つの棚で数える。**

### 9-1. 第1段（★ 課題なし。**実験モードの器だけ**）

| 棚 | 数 | 中身 |
|---|--:|---|
| **判定・実行** | ★ **0** | ⚠ **新しい `detect`/`apply` は1本も要らない**（既存の瓶をそのまま呼ぶ） |
| ★ **見せ方** | **6** | ① パレットのタブ ② 375 の帯を2段にする ③ 反応カードの非表示 ④ 条件の2択から行き先を落とす ⑤ 空振りの3段（§4-2） ⑥ 「答えを見る」の札 |
| **触る既存関数** | **4** | `renderReagents` / `refresh` / `renderConditionChoice` / `explainReagentMiss` |
| ⚠ **触る既存の緑のテスト** | ⚠ **22〜29** | ★ **瓶に触れるテストが 22件・反応カードに触れるテストが 32件・両方が 7件**（実測）。**瓶が分子モーダルの外へ出るなら、`bottle(c, …)` を使う 22件が全部当たる** |

⚠⚠ **前回外した理由がそのまま当たっている棚は「既存の緑のテスト」。**
★ **今回は先に数えた: `#mm-reagents-grid` を引くヘルパー `bottle()` が 43回使われており、
瓶の置き場所を変えると 22件のテストが同時に赤くなる。**

★ **これを安くする道が1つある** —— ⚠ **瓶を「移す」のではなく「増やす」**。
**分子モーダルの中の瓶はそのまま残し（＝ 22件は緑のまま）、実験モードでは
パレット側の瓶を使う。** ⚠ **ただしユーザーの申し立て（モーダルが巨大）は解けない。**
★ **したがってこれは `D-E1` の (b) と同じ話** ＝ **判断待ちに含まれる。**

### 9-2. 第2段（★ 課題）

| 棚 | 数 | 中身 |
|---|--:|---|
| **判定・実行** | **2** | 到達判定（正準コード比較）／副生成物の除外（`D-E4`） |
| ★ **見せ方** | **5** | ① 課題の一覧 ② 課題の帯（出発物 → 目標物・手数） ③ 合格の見せ方 ④ ヒントの段（§4-2 の3段目と共通） ⑤ 通した印 |
| **触る既存関数** | **2** | `summonMolecule` の呼び出し口／`setMode` |
| **新規データ** | **1** | 課題の JSON（`D-E8`） |
| ⚠ **既存の緑のテスト** | **0〜2** | ★ **課題は新しい面なので既存を触らない**。⚠ **ただし `verify-release.js` に新 JSON の `?v=` が要る**（`qa/app.js` の前例） |

### 9-3. ⚠ 見積りに入れていないもの（★ 正直に書く）

- ⚠ **375 の帯を2段にすると、キャンバスの高さが 555 → 493px に変わる** ＝
  ★ **既存のモバイル検査（`check-mobile.mjs`・`MM9` の 320px）が当たる可能性がある**（測っていない）
- ⚠ **チュートリアル**（`tutorials.json`）が実験モードを教える必要があるか（測っていない）
- ⚠ **`audit.html` のランダム操作ファズが、新しいモードに入るか**（測っていない）

---

## 10. ★★ 第2段（課題）—— 実装の記録（2026-09-02）

### 10-1. ★ 決めたこと（未決だった D-E5・D-E8・D-E9 の決着）

| # | 決めた | 理由 |
|---|---|---|
| **D-E8** | ★ **(a) `stages.json` の隣に新しい JSON**（`assembler/quests.json`） | (c) `compounds.json` に列を足すのは「1行1件・末尾追記のみ」の規約に触る（過去3回の事故の元）。(b) `tutorials.json` への相乗りは、チュートリアル（手順を案内する）と課題（何も案内しない）で**出題の向きが逆**＝ 同じ器に入れると片方の規約がもう片方を縛る |
| ★ **形** | ★ **1行1問・行数 = 問数 + 2・CRLF・BOM なし**（`{"_readme":…,"quests":[` / 1問1行 / `]}`） | `compounds.json` の「膨らむ」事故（1件が92行 → 84,890行の差分）と `narrowing-problems.json` の「1行に潰れる」事故（何を直しても `1 1`）は、**どちらも差分から変更の大きさが読めない**という同じ害。両方を塞ぐ形を最初から採る。⚠ **`?v=` が付かない**（`{cache:'no-cache'}` で読む）ので `verify-release.js` の対象外 ＝ **`EQ2` が唯一の見張り** |
| **D-E5** | ★ **(c) 両方**（🎯 課題に挑む の札 ＋ `?open=experiment` / `?quest=<id>`） | `?quest=` は **`?open=` 無しでも効く**（`?summon=` と同じ扱い）＝ qa と参考書から短いリンクを張れる |
| **D-E9** | ★ **残す。⚠ ただし成績にはしない** —— `localStorage` の `chemAssembler.questCleared` に**通した id の集合だけ** | ① `explainPlacementMiss` の決め③「変わると嘘になる数字を書かない」（最短手数は §2-2 の下限であって本当の最短ではない） ② ⚠⚠ **あとで有料の問題集が採点・記録・解説を売る**（`DESIGN_paid_workbook.md` D-W13）。無料側に成績の器を先に作ると「後から取り上げる」か二重管理になる。**id の集合だけ**なら、有料側は自分の鍵で自分の記録を持てばよく、どちらも相手を知らないままでいられる |
| **D-E4** | ★ **(a) 素通しの「ある」** | `DESIGN_review_pack1.md` §2 #5 の読みどおり。⚠ **(c) の「履歴に無い分子の検出」は作らない**。★ 帰結を隠さず書く: **✏️作図 タブで目標を手描きしてもクリアになる**。門番を置くと、図がずれて原子を1つ動かした人まで黙って落とす ＝ 罰しない設計と噛み合わない |

### 10-2. ★★ 第1弾を 12 問にした根拠（⚠ 推測ではなく数で）

★ **母数を測り直した**（ライブラリ全 1085 件 × 実行できる全ルール・先頭の箇所のみ）:

| | 設計書 §2-1（v1488） | ★ 実測（2026-09-02・v1496） |
|---|--:|--:|
| **1手で届く（副生成物を除く）** | 775 通り | ★ **881 通り** |
| **出発物になれる分子** | 419 件 | ★ **483 件** |
| **目標物になれる分子** | 315 件 | ★ **332 件** |
| 1手の適用 | 2,742 件 | **2,875 件** |
| うち生成物に名前が付く | 742 件（27.1%） | **842 件（29.3%）** |

⚠ **§2-1 の数は v1488 のもので、C7 エステル 39件に名前が付いた v1495 以降は増えている。**
★ **どちらも「相手が要る10ルール」を1件も含まない下限**である点は変わらない。

★ **12問の選び方（3段のふるい）:**

1. ★ **目標に名前が付くこと**（1手の適用のうち 29.3% だけがここに残る）
2. ★ **教科書が名前で呼ぶ反応であること**（機械が作れても出題にならないものを落とす。
   例: `aromatic_nitration → aromatic_sulfonation` の *o-ニトロベンゼンスルホン酸* は実在するが定番ではない）
3. ★★ **入試頻度**（`qa/data/exam_usage.jsonl` ＝ 有機 283 大問の悉皆）。
   ⚠ **単純な上位取りはしない** —— **芳香族はコードが細かく割れていて件数が構造的に小さく出る**
   （`org.ali.ethanol-dehydration` 87 に対し `org.aro.nitration` 38）。
   これは頻度の差ではなく**数え方の副作用**なので、**教科書の柱6つ × 各2問**に割り付けたうえで
   柱の中で頻度順に採った。

| 柱 | 課題 | 手数 | 入試（/283） |
|---|---|--:|--:|
| アルケン・アルキン | エタノール → エチレン（エテン） | 1 | **87** |
| | アセチレン（エチン） → 塩化ビニル | 1 | 66 |
| アルコール | エタノール → アセトアルデヒド | 1 | 54 |
| | 2-プロパノール → アセトン | 1 | 43 |
| アルデヒド・ケトン | アセトアルデヒド → 酢酸 | 1 | 68 |
| | エタノール → ヨードホルム | 1 | 59 |
| カルボン酸・エステル | 酢酸エチル → 酢酸 | 1 | 57 |
| | サリチル酸 → アセチルサリチル酸 | 1 | 45 |
| 芳香族の置換 | トルエン → 安息香酸 | 1 | 58 |
| | ベンゼン → ニトロベンゼン | 1 | 38 |
| 芳香族の窒素化合物 | アニリン → アセトアニリド | 1 | 55 |
| | ★ **ベンゼン → アニリン** | **2** | 32 |

★ **12問すべて、実機で瓶からたどれることを確かめた**（濃硫酸@160〜170℃ ／ 混酸 → 水素・Ni ほか）。

⚠⚠ **設計書 §7-5 の「第1段は 20〜30 問」は採らなかった。** 理由は
`DEVELOPMENT.md`「数え上げの上限は *人が答えられるか* で決める」と同じ形 ——
**1問ずつ実機で経路を確かめ、否定対照まで置ける数**が上限で、それが 12 だった。

### 10-3. ⚠ 測ってみたら設計書と違ったこと

1. ⚠⚠ **「2手 703通り」は在庫としては大きいが、*教科書が名前で呼ぶ2手* に絞るとほとんど残らない。**
   ★ 実測で拾えたのは **ベンゼン → アニリン**（ニトロ化 → 還元）・**アセチレン → 酢酸**
   （水付加 → 酸化）・ニトロベンゼン → アセトアニリド の3本程度で、
   残りは `aromatic_nitration → aromatic_sulfonation` のような「実在するが定番ではない」組。
   ★ **第1弾に2手を1問しか入れなかったのはこのため**（数が無いのではなく、*名前で呼べる* 2手が無い）。
2. ⚠ **芳香族置換は先頭の箇所だけを当てると o- 体しか作らない**（§2-2 但し書き2 のとおり）。
   ★ ただし**活性化された環では複数箇所に当たる**（2,4,6-トリブロモフェノール・TNT が実測で出た）。
3. ⚠ **既存ステージとの「被り」は、目標名で見るとほぼ全部が被る**
   （エチレン・酢酸・アセトアルデヒド・アニリン・安息香酸 …… 12問中 10問の目標が既存の120ステージにある）。
   ★ **これは重複ではない** —— パズルは *原子から組み立てる*、課題は *試薬で変える* で、
   問われている知識が別（`§0` の表の左下と右下）。⚠ **むしろ被っているほうが自然**で、
   「組めるようになった分子を、今度は作り方から辿る」という順路になる。
   ⚠ 被りを避けるべきなのは**有料の練習問題**のほう（`DESIGN_review_pack1.md` §1-3）で、
   無料の課題には当てはまらない。

### 10-4. ★ 見積り / 実際（§9-2 の答え合わせ）

| 棚 | 見積り（§9-2） | ★ 実際 | 差の理由 |
|---|--:|--:|---|
| **判定・実行** | 2 | ★ **1** | 「副生成物の除外」は D-E4 が (a) に決まって**要らなくなった** |
| **見せ方** | 5 | ★ **5** | 一覧・帯・合格の見せ方・通した印・⚠ **「ヒントの段」の代わりに「名称から呼び出す段を畳む」**（帯の厚みの手当て。D-E3 でヒントの段が消えたぶんと入れ替わった） |
| **触る既存関数** | 2 | ★ **4** | `summonMolecule` / `setMode` に加え、⚠ **`setupBackdropClose`（モーダルを1枚足したので表に1行）** と **`obstructedInsets`（下の別件）** |
| **新規データ** | 1 | 1 | `quests.json` |
| ⚠ **既存の緑のテスト** | 0〜2 | ★ **0** | 課題は新しい面なので既存を1件も直していない。⚠ **`verify-release.js` の `?v=` も不要だった**（`{cache:'no-cache'}` で読むので対象外。`quiz-scope.json` と同じ） |

⚠ **見積りに入っていなかったもの（正直に）:**
- ⚠⚠ **`.modal-overlay` を1枚足すと機械の見張りが3つ増える**（BC1 の数え上げ・BC5 の閉じるボタン・MO の台帳）。
  **§9-2 は「新しい面なので既存を触らない」と書いていたが、*枠を1枚足すこと* の代償を数えていなかった。**
- ⚠⚠ **キャンバスを覆う量**。帯は 🧪自由 の `#ws-free` と**同時に出る**ので、素直に足すと 375px で **+79px**。
  ★ **課題の最中は「名称から呼び出す」の1段を畳む**（出発物はアプリが置くので使い道が無い）ことで **+34px** に収めた。

### 10-5. ⚠⚠ 別件で見つけた不具合（`obstructedInsets` の天井／床の取り違え）

★ **課題とは無関係に、いまの main で起きている。**
`obstructedInsets()` は天井か床かを「帯の上端がキャンバスの上半分にあるか」で決めていた ——
**帯が薄いことを前提にした式**で、キャンバスが薄くなると反転する。

★ **実測（320×568 ＋ 🧪 実験パレット。課題を1問も置いていない状態）**:
`#svg-wrapper` 234px ／ 下端の `#work-strip` 128px ／ 帯の上端 106px ≦ 117（= 234 の半分）
→ **天井と判定** → `top=140 / bottom=0` → 分子は「空いている下半分」＝ **帯の裏**へ寄せられ、
エタノールの **9原子すべてが見えない**。

★ **直しは「どちらの端に貼り付いているか」で決める**（端からの距離を比べる）。薄い帯では旧式と同じ答え。
**症状が表に出たのは v1494**（実験パレットが左パネルを 83 → 160px にした）。**テストは `EQ1`。**

### 10-6. ⚠ 残した宿題

1. ⚠⚠ **320×568 は、課題を置かなくても既に限界**。🧪 実験パレットでキャンバスが 234px しか無く、
   アニリン（12原子）・アセチルサリチル酸（16原子）で **2原子ぶんが帯にかかる**（実測）。
   ★ **375px 以上では 0**（結合 23.7〜27.3px で床 24px を保つ）。**第1段側の宿題**。
2. ⚠ **`reactor.selectReagent`（`?reagent=`）は今も `openMoleculeModal()` を呼ぶ**（第1段の申し送りどおり。
   今回も触っていない）。課題から `?reagent=` を使うなら分岐が要る。
3. ⚠ **`audit.html` のファズは課題の面に入らない**（`#reaction-actions` とキャンバスしか押さない）。
4. ⚠ **相手が要る反応（エステル化・重合ほか10ルール）の課題は1問も無い。**
   在庫を測り直してから第2弾で（`DESIGN_review_pack1.md` §8-4）。
5. ★ **D-E17（箇所が2つ以上ある反応の出題）は第1弾でも避けていない** ——
   混酸・臭素水は箇所が複数出るので、人は箇所を選ぶことになる。⚠ **どの箇所でも目標に届く課題だけを選んだ**ので
   第1弾では詰まらないが、**箇所によって別の生成物になる課題を作るなら測り直しが要る**。
