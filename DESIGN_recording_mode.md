# 録画モード（SNS素材制作基盤）設計書

SNS_PLAN.md（2026-07-27）で決めた「アプリ自体をコンテンツ生成機にする」ための仕組み。
URL パラメータでデモを自動再生し、外部ツール（Playwright＋ffmpeg）で収録して
ショート動画・講義動画の素材を半自動生成する。実装前の合意用ドキュメント。

---

## 1. 基本方針

- **新しい再生エンジンは作らない**。チュートリアル機構（tutorial.js の TutorialPlayer、
  DESIGN_tutorial.md）が既に持っている
  「ゴーストカーソル・実 PointerEvent 駆動・字幕・アクションDSL（click / drag / summon /
  reactionButton など14種）・tutorials.json」を土台に、**録画用のプレゼンテーション層**を被せる。
- **起動は URL パラメータのみ**。パラメータが無ければ一切動かず、通常利用・回帰テスト・
  夜間監査に影響ゼロ。既存コードへの変更は「オプション注入」（カーソル種別・速度・
  終了フック・復元スキップ）に留める。
- **レンズ共通規約として設計し、assembler で先行実装**。ion-equation / ratio（別ディレクトリ・
  別実装）も同じパラメータ名・完了シグナル・クリーン画面クラスに従えば、収録ツール側は
  1本で全アプリに使い回せる。
- 収録（動画ファイル化）はアプリの外の仕事。アプリは「決められた演技を、決められた
  見た目で、頭出しできる状態」までを責務とする。

## 2. 起動インターフェース（レンズ共通規約）

`/assembler/?rec=<demo-id>&format=short&speed=1.2&cursor=touch&caption=1&delay=1500`

| パラメータ | 値 | 既定 | 意味 |
|---|---|---|---|
| `rec` | デモID | — | これがあるとき録画モード。tutorials.json / demos.json の id |
| `format` | `wide` / `short` | `wide` | 画面比の想定。short=縦型(9:16)向けプレゼンテーション |
| `speed` | 数値 | `1.0` | 再生速度倍率（カーソル移動・wait に適用） |
| `cursor` | `mouse` / `touch` / `none` | `touch` | カーソル演出。SNS視聴者はスマホなので指先風が既定 |
| `caption` | `1` / `0` | `1` | 字幕表示。後編集でテロップを入れるときは 0 |
| `delay` | ms | `1000` | ロード完了から再生開始までの猶予（録画の頭合わせ用） |

- **開始条件**: アプリの初期化・データ fetch（stages.json 等）完了後に `delay` を置いて自動再生。
- **完了シグナル**（収録ツールが待ち受ける）:
  - `window.__recState` を `'loading' → 'playing' → 'done'` と遷移させる
  - あわせて `console.log('[rec] done <id>')`
  - Playwright 側は `page.waitForFunction(() => window.__recState === 'done')` で停止判定
- **クリーン画面**: 録画モード中は `<body class="recording">` を立て、CSS だけで
  ヘッダー・ヘルプ・デバッグ表示など「画面に映したくない UI」を隠す。盤面とパレットを主役に。

## 3. デモ台本

- **M1: tutorials.json をそのまま再生**。既存の操作チュートリアル（原子を置く・環の縮合・
  反応実行…）が最初の素材になる。追加コンテンツ制作ゼロで録画テストまで到達できる。
- **M2: demos.json（SNS 専用台本）を新設**。スキーマは tutorials.json と同一＋以下を拡張:
  - `state`: 開始時の作図状態（serializeState 形式）。空キャンバスからでなく
    「完成済みグルコースを立体ビューで回す」のような頭出しができる
  - `caption` の SNS 文言（「これ知ってる？」調。チュートリアルの説明調と分ける）
  - 必要なら演出用アクションを追加（例: `frame` = 指定範囲へパン・ズームして構図を作る）。
    ただし既存14種でかなり賄えるため、足すのは実台本で不足が出てから
- 台本の座標は SVG 論理座標・getScreenCTM 変換（開発方針どおり、tutorials.json と同じ）。

## 4. 見た目（録画プレゼンテーション）

- `format=wide`（M1）: クリーン画面のみ。レイアウトは既存のまま。講義動画・PC画面素材用
- `format=short`（M2）: 縦型 9:16 の構図。スマホ対応（フェーズ11）で作ったレスポンシブ
  CSS 資産を流用し、盤面を中央に大きく・操作パレットは下部最小限に
- 字幕: `format=short` かつ `caption=1` のとき、下 1/4 に縁取り太字の大きめテロップ調。
  wide のときは既存チュートリアル字幕のまま
- カーソル: 既存ゴーストカーソルの mouse / touch バリアントを流用。`none` は UGC 風
  （操作している手を見せない）素材用

## 5. 収録パイプライン（tools/record/ 新設・M3）

目標フロー（2026-07-27 ユーザー合意。できるだけ簡単に、編集ソフトを使わない）:

> **台本生成 → ユーザー確認 → 動画生成 → ユーザー調整（字幕など） → 音声吹込み → 完成**

1. **台本生成**: アシスタントが1本分の台本ファイルを生成する。台本＝単一の正
   （demos.json のステップ列＋各ステップの `narration`（読み上げ文）＋`caption`（字幕））
2. **ユーザー確認**: 台本（Markdownプレビュー付き）を読んで文言・構成を直す
3. **動画生成**: tools/record が自動収録。**各ステップの尺は narration の文字数から自動算出**
   （日本語の読み上げ目安 6〜7字/秒＋間 0.5s。アフレコが窮屈にならない余白を持たせる）。
   Playwright 収録 → ffmpeg で mp4 化・**字幕焼き込みまで自動**
   - 内部手順: ローカルサーバー → Chromium（short=1080×1920 / wide=1920×1080）→
     `?rec=...` へ遷移 → `__recState === 'done'` を待つ → 保存
4. **ユーザー調整**: 動画ファイルを直接編集しない。**台本を直して再生成**（数分で回る）。
   字幕の文言・タイミング・強調はすべて台本側のフィールドで制御する
5. **音声吹込み（アフレコ）**: 完成映像を再生しながら実声で読む → ffmpeg で音声トラック合成。
   録り方は「スマホ録音でも可」を前提に、合成コマンドを tools/record に含める
   - 補助: VOICEVOX ローカル API（:50021）で**仮ナレーション入りプレビュー**を自動生成できる
     ようにする（読む練習・尺確認用）。実声を録らずそのまま公開する選択肢にもなる
     （週次まとめなど量産系は VOICEVOX 完結が現実的）
6. アップロードは当面手動（三面投稿のメタ情報テンプレは SNS_VIDEO_PLAN.md §3）

映像が先・音が後（アフレコ方式）なので、同期の正体は「ステップ尺の自動算出」にある。
読み切れない場合は narration を短くする＝台本を直せば映像も直る、を守る。

## 6. 実装配置

| ファイル | 変更 |
|---|---|
| assembler/rec.js（新設） | URLパラメータ解釈・ready待ち・`__recState` 管理・TutorialPlayer 呼び出し・クリーン画面クラス付与 |
| assembler/tutorial.js | オプション注入のみ（速度倍率・カーソル種別強制・字幕スタイル切替・終了フック・復元スキップ） |
| assembler/index.html | script タグ1本追加（?v= 儀式対象） |
| assembler/style.css | `.recording` のクリーン画面／short 字幕スタイル |
| assembler/demos.json（M2 新設） | SNS 専用台本 |
| tools/record/（M3 新設） | Playwright 収録・ffmpeg 変換・VOICEVOX 結合 |

## 7. リスク・留意点

- **フレームレート**: Playwright 標準の動画記録は 25fps 前後でアニメがカクつく可能性。
  M3 で品質不足なら CDP screencast のフレーム列＋ffmpeg 合成へ切り替える（設計は同じ、
  収録ツール内部の差し替えで済む）
- **fetch タイミング**: 再生開始はアプリ ready（データロード完了）を待つフックが前提。
  ready の判定点を rec.js 実装時に明確化する
- **台本の壊れやすさ**: 座標ベース台本はレイアウト変更に弱い（tutorials.json と同質のリスク）。
  回帰テストに「登録済みデモ全件が完走する」ケースを追加して担保する。
  チュートリアル再生は既に回帰テストで駆動されているので同じ流儀で足せる
- **セッション競合**: 実装は assembler のワーキングツリーがクリーンになってから着手
  （本設計書のみ先行コミット）。DEVELOPMENT.md への登録も実装開始時に行う

## 8. マイルストーン（フェーズ13 提案・DEVELOPMENT.md 登録は実装開始時）

- **P13-1（M1）**: `?rec=` 自動再生＋クリーン画面＋完了シグナル＋速度/カーソル/字幕パラメータ。
  tutorials.json の再生まで。**この時点で手動画面録画の素材撮りに使える**
- **P13-2（M2）**: demos.json（SNS台本・開始状態指定）＋縦型 9:16 プレゼンテーション＋SNS字幕
- **P13-3（M3）**: tools/record 収録自動化（Playwright＋ffmpeg・字幕焼き込み・尺自動算出）
  ＋アフレコ合成コマンド＋VOICEVOX 仮ナレーションプレビュー
- **P13-4（M4）**: ion-equation / ratio へ共通規約で横展開
