# イオンの層と分液 —— 対イオンをどう持つか・分液をどこに置くか（設計のみ・2026-09-03）

⚠ **この設計書はコードを1行も変えていない。** 数はすべて **v1506（`main` e0a35c9）の
`assembler/chemistry.js` を Node の vm に読み込んで測った値**（`tools/verify-compounds.js` と同じ読み方）、
`compounds.json`・`stages.json`・`reactor.js`・`tests.js` を読んで数えた値、
または入試問題DB の一覧（`_解析/handoff/一覧-分離が出た大問.md`・64件）を機械で集計した値である。
推測で書いたところは「見積り」と明記する。測るために書いたスクリプトは scratchpad にあり repo には入れていない（§9）。

**きっかけ（ユーザー原文・2026-09-03）:**

> **「イオンを扱えるようにしたい」**
> **「複数の芳香族化合物の分離問題（入試の定番）をできるようにしたい」**

発注側の見立て: **この2つは同じ壁の裏表**（分液の本体は「塩になって水層へ移る」で、塩＝イオン）。
⚠ **本書はこの見立てを実測で否定する**（§0・§5）。**分液は電荷の壁の向こうに無い。**

---

## 0. ★★ 要旨（先に結論だけ）

| 問い | 測った答え |
|---|---|
| ★★ 「イオン」と「分液」は同じ壁か | ⚠⚠ **違う。** 入試64件のうち **58件は電荷を1つも持たずに表現できる**（§4-2）。足りないのは **「層」という入れ物**（UI）と **NaHCO₃・CO₂・塩酸の3つの反応**で、どれも既存の「線1本の塩」の流儀のまま書ける |
| いまの「線1本」の流儀はどこまで行けるか | ★ **O–金属の塩（-COONa／-ONa／-SO₃Na）までで、N の塩には届かない。** アニリン塩酸塩を線で書くと **N-クロロアニリン C₆H₆ClN の図**になる（別の分子）。ジアゾニウム N≡N⁺ は `isValencyValid` が弾く（**EL3 がわざと弾く検査を持っている**）。双性イオンは中性形と**正準コードが同一**（§3-1） |
| 対イオンを「結合を持たない粒」として持つと何が壊れるか | ⚠ **電荷を持たない粒は 4つ壊す**: 自動水素が生えて **NaH の図**になる／分子式に H が1つ余る／正準コードが登録16件と割れて名前が引けない／監査の「異性体列挙に自分が含まれる」が必ず落ちる（§3-2） |
| ★ ではどう持つか | ★★ **電荷を「原子の印」として `getFreeValency` 1か所に通す**（§3-3）。試作で **正準コード・同型判定・分子式・自動水素が全部いっぺんに割れた**（双性イオン ≠ 中性形／-NH₃⁺ に H が3つ／Cl⁻ に H が生えない）。**粒は「結合ゼロで電荷を持つ原子」＝ 電荷の一形態**で、別の概念にしない |
| 既存の塩16件はどうするか | ★ **変えない。** 線1本の流儀は O–金属で正しく動いており、**動画（V122）も回帰テストも登録データもこの形**。電荷は **線で書けないもの（N⁺・O⁻ の双性・N≡N⁺）にだけ**使う |
| 分液の置き場所 | ★ **実験パレット（🧪）の中の「混合物」の状態**として持つ（§4-4）。独立モードは作らない。**試薬の順を固定しない**（64件のうち教科書順「塩酸→NaHCO₃→NaOH」は **3件**しかない。§4-1） |
| 4成分は1画面に入るか | ★ **PC は入る**（中央値の4成分・上下2層で **49.9px**、最悪でも 28.0px）。⚠⚠ **スマホ縦は 2成分でも床を割る**（最良で 25.4px）＝ **分液は PC 前提**。⚠ **縦に積んでも救えない**（`fitCanvasToMolecule` が 4:3 に正規化するため。§4-3） |
| 「対イオンを1つ入れればまとめて開く」か | ⚠ **開かない。** 4件の壁は3種類: **分液＝層（電荷不要）／両性＝電荷（対イオン不要）／アニリン塩・ジアゾニウム＝電荷＋粒**（§5-1）。**電荷が共通の壁**で、対イオンではない |
| 第1段の最小単位 | ★★ **I-1「層」**（電荷なし・反応3本・瓶1本）。これだけで **入試64件中 51件**（CO₂ を足せば 58件）が画面で追える。**V122 が撮れなかった「水層に移る」がこの段で画になる**（§5-2） |

---

## 1. ⚠⚠ 発注の前提のうち、測ったら違っていたもの（★ 先に書く）

| # | 発注書・既存の記録の言い分 | 実測 |
|---|---|---|
| 1 | ★ 「方針は 2026-08-06 に決まっている: イオンの受け皿は assembler」 | ⚠⚠ **2026-09-01 にユーザー決定が上書きされている。** `qa/data/assembler_links.jsonl`（コミット `97ad298`）と `qa/tests.js` 997 行の棚卸し検査が、**「反応式を書く際には ion-equ というのが基本的な使い分け」（ユーザー・2026-09-01）**を引いて、`org.aroN.aniline-base`／`diazonium-decomp`／`bio.amino-acid-amphoteric`／`bio.amino-acid-polyprotic` の **4件の待つ相手を「ion-equation への収録」に付け替え済み**。`DEVELOPMENT.md` 3540 行は「2つは層が違うので両立しうる。ただし3項目はまさに重なるのでユーザーに確認」と書いたまま。★ **本書の (D) の最初の決めはこれ**（`D-I1`）。⚠ 本書は「assembler に置く」前提で設計したが、**この決めが逆なら §5 の I-3 以降は要らない**（I-1 の層は影響を受けない） |
| 2 | 「2つは同じ壁の裏表」 | ⚠⚠ **違う**（§0）。発信元自身が撤回気味なのは正しい |
| 3 | 「`liberate_weak_acid` が既にある」 | ★ **ある。ただし瓶は `h2so4_dil`（希硫酸）1本だけ。** 入試64件で遊離に使う試薬は **塩酸 45件・硫酸 14件・CO₂ 7件**（§4-1）。⚠ **いちばん多い塩酸で引けない**（`hcl` の瓶は「塩化水素」＝ C=C への付加専用）。★ `rule.reagentId` は**配列を受ける**（`reactor.js` 3049）ので、瓶を増やさず `['h2so4_dil','hcl']` にできる |
| 4 | 「入試の定番: 塩酸→NaHCO₃→NaOH」 | ⚠ **その順そのものは 64件中 3件。** 1手目は 塩酸 24／NaOH 22／NaHCO₃ 15／硫酸 3。**順序を固定した実装は実出題の 95% を表現できない** |
| 5 | 「名古屋工大2020 の『溶けるの一文だけ』型」 | ⚠ **入試DB側で 0件**（層に分ける操作が無いので64件から落ちている）。**分液の実装対象には数えない** |
| 6 | 「アンモニウム型 N(4) の足がかりがある」 | ★ ある。⚠ **ただし手で描く経路は無く、データにも 0件**（N(4) は登録データに **72原子**あり全部ニトロ基）。**明示水素も 0件**。＝ 足がかりは「検証が弾かない」の1点で、線でアニリン塩酸塩を書く道はそこから伸びない（§3-1） |
| 7 | `DESIGN_organic_tree.md` D-T10 「イオンの壁は木のために越えない」・`DESIGN_paid_workbook.md` §1-1 「分離は有機には無い・`/muki/` の系統分離が前例」 | ★ 前者は本書と矛盾しない（層は壁ではない）。⚠ 後者の「前例」は**無機の沈殿分離**で、**有機の分液（酸塩基で層を移す）とは操作の型が違う**。`/muki/separation-model.js` の表（イオン×札）の形は借りられるが、中身は借りない |
| 8 | 「反応の在庫」 | ★ `qa/tests.js` 997 行の棚卸し検査は **瓶23本・ルール49種** を固定している。**本書のどの段も瓶かルールを増やすので必ず鳴る**（設計された引き金。§7） |

---

## 2. ★ 実測 —— いまの在庫（v1506）

### 2-1. 「線1本の塩」の在庫

| 数えたもの | 値 |
|---|--:|
| `compounds.json` の件数 ／ 行数 | **1,030 ／ 1,032**（1行1件の規約どおり） |
| Na を含むエントリ | **16件**（酢酸Na・ギ酸Na・プロピオン酸Na・乳酸Na・シュウ酸Na・安息香酸Na・サリチル酸Na・ナトリウムフェノキシド・ナトリウムエトキシド・ベンゼンスルホン酸Na・ABS・セッケン4種）。Na 原子は **17個**（シュウ酸Na が2） |
| ★ Na の隣 | **17/17 が O（単結合1本）** ＝ 流儀は「-O-Na」だけ |
| K を含むエントリ | **2件**（酢酸カリウム・フタル酸水素カリウム） |
| 名前に「イオン」を含むエントリ | **0件** |
| N が価標4本の原子 | **72個・全部ニトロ基**（アンモニウム型 **0件**） |
| ★ 明示水素 `element: "H"` | **0件**（stages・compounds とも） |
| `reactions.json` の `charge` | **52か所**（機構データは電荷を持ち、`reaction.js renderCharge` が描く） |
| ★ `reactor.js` の写し `snapshotMolecule` / `snapshotFrame` | **既に `charge: a.charge \|\| 0` を運んでいる**（2393・8051行）＝ 機構へ渡す橋は掛かっている |
| `tools/verify-reactions.js valencyLimit(element, charge)` | **電荷つき価標の表が既にある**（C⁺ は base−1・他は base＋charge）。⚠ **Na⁺ を入れると 1+1＝2本になる**ので、アルカリ金属・ハロゲン化物イオンは別扱いが要る |

### 2-2. 分液の「結果」を使っている場所

| 場所 | 中身 |
|---|---|
| `narrowing.js` 182 | 札 `amine`「塩酸に溶けて塩をつくり、水層に移った」 ＝ **条件としては動く** |
| `narrowing-problems.json` 6 | 群馬大2020「エーテル層に出たアルコールC」 |
| `reactor.js` `neutralize_naoh` / `liberate_weak_acid` / `react_sodium` | 酸 ⇄ Na塩 の往復。**caption が「塩になると水に溶けやすくなります」と言葉で補っている** |
| `reactor.js` `nahco3`（3320行） | ★ **検出瓶**（CO₂ が出る／出ない）。**変換ではない** ＝ NaHCO₃ で塩にする反応は無い |
| `reactor.js` `reduce_nitro` caption | 「ここでは塩の段をとばして」＝ アニリン塩酸塩を **言葉で**補う |
| `video-scripts/meta/V122.json` | 「水層に移る」を**ナレーションで**補う（発注書のとおり） |
| `qa/data/codes_index.md` | `org.phenol.separation`・`org.aroN.separation-principle`・`org.aroN.separation-order`（Lv2 × 3） |
| `DESIGN_reaction_execution.md` 1339 | 入試台帳の `separation-funnel` は「反応ではない。reactor の守備範囲外」と除外 |

★ **＝ 「分ける操作」だけが無い。結果・条件・言葉は全部ある。**

---

## 3. ★★ (A) 対イオンの表し方 —— 候補と、それぞれが壊すもの（すべて実測）

### 3-1. (a) いまの「線1本」の流儀を広げる —— ⚠ **N には届かない**

`chemistry.js` を vm に読み、分子を組んで `isValencyValid`／`canonicalCode`／分子式（`computeMolecularFormula` の写し）を取った:

| 描きたいもの | 線1本で組むと | 結果 |
|---|---|---|
| アニリン塩酸塩 C₆H₅NH₃Cl | 環-N-Cl（-COONa と同じく金属の代わりに Cl を線で） | ⚠⚠ `isValencyValid` **true**・分子式 **C₆H₆ClN**・N の空き価標 1 ＝ **N-クロロアニリン（別の分子）の図**。「通ってしまう」のが危ない |
| 同上、N に明示水素 2 本 ＋ Cl（N が4本） | N(C,H,H,Cl) | `isValencyValid(N)` **false**（アンモニウム型②は相手が C か H だけ。Cl で落ちる） |
| 塩化ベンゼンジアゾニウム | 環-N≡N-Cl | **false**（N₁ 使用4・N₂ 使用4。どちらも特例に当たらない） |
| ジアゾニウム陽イオンだけ | 環-N≡N | **false**（EL3 が **「ジアゾニウム C-N≡N が通ってしまう」を赤にする否定対照**を持つ。7789行） |
| グリシンの双性イオン | 中性形と同じ原子・同じ結合 | 正準コード **同一** `C0[c0]();C2[c1](01);N2[c2](11);O0[c3](02);O1[c4](01)`・分子式 **同一** C₂H₅NO₂ |
| NH₄Cl | N-Cl | 分子式 **H₂ClN**（クロラミンの図） |

★ **なぜ O には効いて N には効かないか**: O は価標2で、H を1つ金属に置き換えても **残りの1本で骨格につながる**。N は価標3を **H 3つで使い切っている**ので、塩の4本目は「H の置き換え」では出せず、**必ず価標を超える**。線1本の流儀が O–金属で正しく動いているのは偶然ではなく、**この流儀の守備範囲そのもの**。

⚠ **DEVELOPMENT.md v554 の申し送り（1707行）「双性イオン・アニリン塩酸塩・NH₄⁺ はこの変更では扱えない」は正しかった**（本書はそれを数で追認した）。

### 3-2. (b) 結合を持たない粒（⚠ 電荷なし）—— **4つ壊す**

酢酸 ＋ 結合の無い Na 原子を同じキャンバスに置いて測った（`M3`）:

| 見るもの | 線1本の酢酸Na | 粒（電荷なし） | 何が起きるか |
|---|---|---|---|
| `canonicalCode` | `…;Na0[c2]();O0[c3](01.21);O0[c4](02)` | `…;O1[c3](01)` **`/Na1[c0]()`** | ⚠ **別のコード** ＝ 登録16件の名前が引けない。粒は `/` で区切られた第2成分になる |
| `verifyMolecule(線1本, 粒)` | — | **false** | クイズ「同じ？違う？」で別物扱い |
| 分子式 | C₂H₃NaO₂ | ⚠ **C₂H₅NaO₂** | H が2つ余る（-COOH の H ＋ **Na の自動水素**） |
| `calculateHydrogens` | Na に H なし | ⚠ **Na に H が生える**（価標1・結合0 → 空き1） | **NaH の図** |
| `findFunctionalGroups` | `carboxylate` | ⚠ `carboxyl` | 塩ではなく酸として分類される |
| 連結成分 | 1 | ⚠ **2** | `countMolecules`／`splitMolecules`／図の下の見出し①② が粒を「分子」として数える。**CV1 の物差し**（反応前1成分 → 後2成分 ＝ 分かれた）に当たる |
| 監査 ①ライブラリ検査「異性体列挙に自分自身が含まれる」（重原子5個まで） | 含まれる（H=3 で列挙 21種のうちに在る。実測） | ⚠⚠ **含まれない**（H=5 で 12種・列挙器は連結グラフしか作らない） | 粒を持つ登録エントリは**必ず赤** |

★ **＝ 電荷を持たない粒は、モデルの中で「価標1の孤立原子」でしかなく、アプリはそれを NaH と読む。粒だけ入れる案は成立しない。**

### 3-3. ★★ (b′) 電荷を「原子の印」として `getFreeValency` 1か所に通す —— **試作で全部いっぺんに割れた**

`M3b`: メモリ上で `Molecule.prototype.getFreeValency` だけを差し替え（**ファイルは触っていない**）、
`atom.charge` があれば上限を **N⁺→4・O⁻→1・C⁺→3・Na⁺/K⁺/Cl⁻→0** にした。

| 分子 | 正準コード | 分子式 | 自動水素 |
|---|---|---|---|
| グリシン中性 | `…;N2[c2](11);O0[c3](02);O1[c4](01)` | C₂H₅NO₂ | N に2 |
| ★ グリシン双性（N⁺・O⁻） | `…;N3[c2](11);O0[c3](01);O0[c4](02)` ＝ **別コード** | C₂H₅NO₂（同じ。正しい） | ★ **N に3・O⁻ に0** |
| `verifyMolecule(中性, 双性)` | **false** | | ★ 同型判定も自動で割れた |
| アニリン塩酸塩（N⁺ ＋ Cl⁻ の粒） | `…;N3[c4](01)/Cl0[c0]()` | ★ **C₆H₈ClN**（教科書の C₆H₅NH₃Cl と一致） | Cl⁻ に **0** |
| 塩化ベンゼンジアゾニウム（N⁺≡N ＋ Cl⁻） | — | C₆H₅ClN₂ | N⁺ 0・末端 N 0（正しい） |
| 酢酸イオン ＋ Na⁺ の粒 | `…;O0[c2](01);O0[c3](02)/Na0[c0]()` | C₂H₃NaO₂ | Na⁺ に **0**（NaH にならない） |

★★ **なぜ1か所で済むか**: `canonicalCode`（`buildHeavyGraph` のラベル `元素+空き価標`）・`verifyMolecule`（`getUserHCount`）・
`computeMolecularFormula`・`calculateHydrogens`・`rootedFragmentCode`（不斉判定）が **全部 `getFreeValency` を通る**。
DEVELOPMENT.md v554 が「N の4本目を図の上で明示できない」と書いた壁は、**電荷が空き価標を変えれば図に現れる**。

⚠ **試作で分かった「1か所では済まないもの」（★ 黙って落とさない）:**

| 別関数 | 何が要るか |
|---|---|
| `isValencyValid` | ジアゾニウム N⁺（三重結合を含む4本）は **`getFreeValency` が 0 を返しても `isValencyValid` は false のまま**（別関数）。電荷を見る分岐を足す。★ **電荷の無い C-N≡N は今後も弾く** ＝ EL3 の否定対照（7789行）は**そのまま緑** |
| `maxValencyOf` | `getFreeValency` と `isValencyValid` の両方がここを読むので、**電荷はここに入れるのが正しい**（試作は手抜きで `getFreeValency` に直書きした） |
| `buildHeavyGraph` のラベル | 空き価標だけだと **中性の N(4)（第四級アンモニウムを線で書いたもの）と N⁺(4) が同じ `N0`** になる。★ **ラベルに電荷を足す**（`N0+`）。既存データに電荷は 0件なので**公開済みのコードは1文字も変わらない** |
| 酢酸イオン ＋ Na⁺ の粒 ≠ 線1本の酢酸Na | コードが割れる（上表）。⚠ **これは直さない** ＝ **O–金属は線1本のまま、電荷は N にだけ使う**（§3-6）。両方の形を持つのは「同じ物質に2つのコード」で、名前引き・クイズの土俵を壊す |
| 列挙器 `enumerateConstitutionalIsomers` | 電荷を知らない（連結グラフのみ）。**電荷を持つエントリは監査のライブラリ検査で列挙の項を飛ばす**（1か所） |

### 3-4. (c) 「層」という入れ物を持ち、分子がどちらに居るかを持つ —— **化学モデルを1文字も触らない**

- 層は **原子の印 `phase`（`'ether'` 既定／`'aq'`）** として持つ。`fromReaction`・`benzeneAngle`・`isStereoMarked` と同じ「追加プロパティ」の作法で、
  `splitMolecules`（`Object.assign`）・`restoreState`（全プロパティ機械復元・開発方針 3.5章）を**そのまま通る**。
  ⚠ `summonMolecule`（7914行）と `createTargetFromData`（1441行）は `element,x,y` しか写さないが、**呼び出した分子は既定の層に置くので写す必要が無い**
- 描くのは **`uiGroup` に「水層」の帯を1枚**（作図の下。系統樹の設計 §5-3 と同じ置き場）と、成分を帯へ移す平行移動（座標は見た目専用）
- ★ **正準コード・同型判定・分子式・連結成分・列挙・監査のどれにも触れない**（印は `buildHeavyGraph` が読まない）
- ⚠ **壊すものは1つだけ**: 「どの層に居るか」を印で持つと、**分子を手でドラッグして帯の外へ出しても印は残る**。★ 帯の中に無い `aq` の成分は「層の外に出ています」と見出しに出す（`D-I8`）

### 3-5. ★★ 比較表 —— 候補 × 壊れるもの（すべて §3-1〜3-4 の実測）

| | (a) 線1本を広げる | (b) 粒・電荷なし | ★ (b′) 電荷の印（粒は結合ゼロの電荷つき原子） | (c) 層 |
|---|---|---|---|---|
| `canonicalCode` | 変わらない（が **N の塩は描けない**） | ⚠ 割れる（第2成分 `/Na1`） | ★ **電荷つきだけ新しいコード**。既存 1,150件は無差分（電荷 0件） | 変わらない |
| `verifyMolecule` | 同上 | ⚠ 線1本と不一致 | ★ 中性形と双性を**区別できる**（正しい） | 変わらない |
| `computeMolecularFormula` | 同上 | ⚠ H が余る（NaH） | ★ 正しい（C₆H₈ClN）。⚠ 電荷の表示（⁺/⁻）をどうするかは `D-I6` | 変わらない |
| 連結成分（CV1・CV4・見出し①②） | 変わらない | ⚠ 粒が1成分 | ⚠ **粒が1成分**（同左）。★ **塩をつくる反応は「1 → 2成分」＝ CV1 の物差しに乗るので、`changed` に粒の id を入れる**（脱離した水と同じ扱い）。CV4 の `changed` は結合次数の変化点＝ N と粒 | 変わらない |
| 監査（`audit.js`） | 変わらない | ⚠ 列挙の項が必ず赤 | ⚠ **列挙の項を電荷つきは飛ばす**（1か所）／変形（`transformCompoundDepiction`）が電荷を写す（1か所） | 帯は `uiGroup` なので当たり判定検査に影響なし（⚠ 未計測。§8） |
| 収録の台本（`video-scripts/`・`demos-*.json`） | 変わらない | — | 台本は DOM の `selector`（`#btn-molecule-modal` 等・653か所）と `summon` の名前で書かれている。**元素を足しても既存の台本は動く**（I の追加時と同じ） | 帯のボタンを足すだけ。既存の `selector` は無傷 |
| 登録データ（16件の塩） | そのまま | ⚠ 名前が引けなくなる | ★ **そのまま**（電荷は N にだけ使う） | そのまま |
| N の塩（アニリン塩酸塩・双性・ジアゾニウム） | ⚠⚠ **描けない**（§3-1） | ⚠ 描けない（N⁺ の4本目が無い） | ★★ **描ける**（3つとも実測） | 描けない（層の話ではない） |
| 分液（層を移る） | 描けない | 描けない | 描けない | ★★ **これだけが描ける** |

### 3-6. ★ 見積り（⚠ 「見せ方」を数に入れた版）

| 候補 | 新規関数 | 既存の共通関数の改修 | 「写し」の経路（`addAtom(a.element,…)`／`new Atom(a.id,…)` で分子を写す箇所・実測 **29か所**・テスト除く。うち `restoreState`・`splitMolecules` の 2か所は `Object.assign` で全プロパティを写すので無傷） | 在庫の数を固定した検査（§7） |
|---|---|---|---|---|
| (a) | 0 | 0 | — | — ⚠ **やっても N には届かないので見積りに意味が無い** |
| (b) | — | — | — | ⚠ **成立しないので見積もらない**（§3-2） |
| ★ (b′) 電荷 | **renderCharge を game.js へ 1**（`reaction.js` 560行のものを移す）／`findFunctionalGroups` に `ammonium`・`carboxylate_ion`・`diazonium` の型 **3分岐**／`iupacName` の門番 1（電荷つきは命名しない） | **`maxValencyOf`・`isValencyValid`・`getFreeValency`（Na⁺/Cl⁻ → 0）・`buildHeavyGraph` のラベル・`rootedFragmentCode` のラベル・`computeMolecularFormula` ＝ 6本** | **必ず通る 6か所**: `createTargetFromData`（game 1441）・`summonMolecule`（7914）・`moleculeWithCandidate`（310）・`separateComponent` の probe（reactor 1094・1719）・`verify-compounds.js buildMolecule`。残り 23か所は電荷つき分子が通らない経路（narrowing の加水分解プローブ 7・learn 3・quiz 1・reaction 3 ほか）だが、**「必ず通る」は実機で確かめること**（§8） | `EL3`（否定対照はそのまま緑・上表）／`RG5`（瓶を持たないルール 11本の名簿）／`REAGENTS.length === 23`（34287行）／`transform 18・detect 5`（34362行）／`qa/tests.js` 棚卸し（瓶23・ルール49）／`CV4_NO_CHANGE_RULES`（構造を変えないルールの名簿・46753行） |
| ★ (c) 層 | **帯の描画 1**／**成分を帯へ移す 1**／**混合物に瓶をかける（全箇所に順に apply）1**／ルール → 層の対応表 1／見出しの「（水層）」1 | `renderMoleculeLabels`（見出しに層を添える）1／`reactor.refresh`（混合物のときは箇所選択を出さない）1 | **0**（印は既定値で足りる） | `Q1`（`[data-modes]` は1つ）／帯の押しものの高さ 32px（`TAP1` 系）／`REAGENTS.length`（CO₂ の瓶で 23→24）／`qa` 棚卸し／`RG5` |
| ★ 新しい反応（層に要る3本） | `neutralize_nahco3`（カルボン酸・スルホン酸だけ）／`liberate_co2`（フェノキシドだけ）／`amine_hcl`（アミン → 水層。★ **I-1 では構造を変えない印だけ**） | `liberate_weak_acid.reagentId` を `['h2so4_dil','hcl']` に | — | `CV4_NO_CHANGE_RULES` に `amine_hcl` を足す（構造を変えないので）／`qa` 棚卸し |

### 3-7. ★★ 推奨

1. ★★ **(c) 層を、電荷なしで先に作る**（I-1・§5-2）。**分液は電荷の壁の向こうに無い**（§4-2 で 51〜58/64件）。
2. ★ **電荷は (b′) の形で、線で書けないもの（N⁺・双性の O⁻・N≡N⁺）にだけ入れる**（I-3）。粒は「結合ゼロで電荷を持つ原子」。
3. ⚠ **O–金属の塩16件は線1本のまま**。**同じ物質に2つの形を持たない。**
4. ⚠ **(b) 電荷なしの粒は採らない**（NaH になる。§3-2）。**(a) を N へ広げる案も採らない**（別の分子の図になる。§3-1）。

---

## 4. ★ (B) 分液を「操作」として持つなら、どこに置くか

### 4-1. ★★ 入試64件の実測（`一覧-分離が出た大問.md`・2019〜2022年入試の有機563大問のうち層に分ける操作があるもの）

⚠ **精度の申告（入試DB側の自己申告をそのまま引く）**: 試薬の順は 64/64 拾えている。**成分数は 27/64**（「4種類の芳香族化合物」の型を正規表現で拾っただけ）。水層へ移ったものも語で拾っており、段ごとの対応までは追えていない。

| 数えたもの | 値 |
|---|---|
| 試薬の出現（本文に現れる順・重複なし） | **水酸化ナトリウム 57／塩酸 45／炭酸水素ナトリウム 41／硫酸 14／二酸化炭素 7** |
| ★ 1手目の試薬 | **塩酸 24／水酸化ナトリウム 22／炭酸水素ナトリウム 15／硫酸 3** |
| 順そのもの（上位） | 塩酸→NaOH→NaHCO₃ **9**／NaOH だけ 7／NaOH→塩酸 6／NaHCO₃→塩酸→NaOH 5／NaHCO₃ だけ 4／… |
| ⚠ 教科書順「塩酸→NaHCO₃→NaOH」そのもの | **3件** |
| 水層へ移った種別 | 中性 59（残る側）／カルボン酸 56／フェノール 52／**アミン 46** |
| 成分数（拾えた27件） | 1: 1／2: 3／3: 6／**4: 7**／5: 4／6: 3／7: 2／12: 1 |
| 例外 | **CO₂ 吹き込み 7**／両性 4／イオン交換樹脂 2／「溶けるの一文だけ」**0** |

★★ **実装の形を決める2点:**
- ⚠⚠ **試薬の順を固定しない。** 「3段の決まった手順」にすると **61/64 を表現できない**。
  ★ **＝ 瓶を「混合物に」かける操作の繰り返し**として持つ（順は学習者が決める。§4-5）
- ⚠ **弱酸の遊離は「強酸」だけでなく「CO₂ 吹き込み」も要る**（7件・11%）。
  **`liberate_weak_acid` は希硫酸1本にしか付いていない**（§1 #3）。CO₂ は**炭酸より弱い酸（フェノール）だけ遊離させる**ので、**同じルールでは書けない**（酸の強さの分岐が要る。`acidKindOf` が既に3種を見分けている）

### 4-2. ★★ 対イオンを持たずに、64件のうち何件が表現できるか

「表現できる」＝ 問題文の各段（試薬を加える → どれが水層へ移る／戻る）を、キャンバスの状態変化として再現できること。

| 段階 | 部品 | 表現できる件数（/64） |
|---|---|--:|
| **A: きょうのアプリ**（酸 ⇄ Na塩 の往復のみ・層なし・アミンなし・NaHCO₃ は検出だけ） | — | **4**（NaOH／硫酸だけで、アミンの無い問題。⚠ 「層」が無いので *分液* とは言えず、V122 と同じ「往復」止まり） |
| ★★ **I-1: 層 ＋ アミンの札 ＋ NaHCO₃ の変換 ＋ 塩酸で遊離** | 電荷 **0** | ★ **51** |
| ★ **I-2: ＋ CO₂ の瓶（フェノキシドだけ遊離）** | 電荷 **0** | ★★ **58** |
| I-3: ＋ 電荷（両性・アニリン塩の構造） | 電荷 | **62**（両性 4 が加わる） |
| 対象外 | イオン交換樹脂 2 | — |

I-1／I-2 の 58件で要る部品の組み合わせ（例外6件を除く）: **アミン札＋NaHCO₃ 24／アミン札だけ 13／NaHCO₃ だけ 10／既存だけ 4／CO₂ を含む 7**。

★★ **＝ アニリン塩酸塩を *描けなくても*、「アニリンが塩酸で水層へ移った」は 46件で要る。**
I-1 の `amine_hcl` は **構造を変えず、層の印だけ変えて、見出しに「（塩酸塩として水層に）」と添える**。
⚠ 文言は **「塩の形はまだ描きません」**（「まだ」の取り決め・`chem-ion-in-assembler`）。**電荷が入ったら（I-3）同じルールが本物の塩を描く**。

### 4-3. ★★ 画面の広さ —— 4成分は1画面に入るか（★ 名前で選ばず、データから決めた）

物差し: **結合の下限 28px**（`DEVELOPMENT.md` 2026-09-02）。分子の「要る視野」は `tests.js requiredViewWidth`（座標だけで決まる・4:3 正規化・REF4 が実測で検証済み）を写して使い、
並べ方は **`summonMolecule` と同じ**（右へ2マス空けて並べ、2400 で折り返し）。画面は `DESIGN_organic_tree.md` §3-1 と同じキャンバス実寸
（**PC 953×659／スマホ縦 361×555／スマホ横 644×361**）。

**成分の選び方（★ 機械）**: 在庫 1,150件のうち **芳香環を持ち R・Na・K を含まないもの**を `findFunctionalGroups` で
**アミン 47／カルボン酸・スルホン酸 53／フェノール 38／中性 127** に分け、各クラスの **視野の最悪・中央・最小**を取った。

| 組 | 成分 | 並べ方 | PC 953×659 | スマホ縦 | スマホ横 |
|---|---|---|--:|--:|--:|
| ⚠ **最悪**（チロシン／アセチルサリチル酸／カプサイシン／無水安息香酸） | 4 | 1行 | 29.1 ○ | 12.0 ✗ | 15.9 ✗ |
| | 4 | **上2・下2（2層）** | **28.0 ○**（床ぎりぎり） | 11.5 ✗ | 15.3 ✗ |
| | 4 | 上3・下1（1段目の後） | 34.3 ○ | 14.1 ✗ | 18.8 ✗ |
| | 6 | 1行 | ⚠ **18.2 ✗** | 7.5 ✗ | 10.0 ✗ |
| | 6 | 上3・下3 | ⚠ **27.9**（床を 0.1 割る） | 11.5 ✗ | 15.3 ✗ |
| ★ **中央**（o-アミノベンゼンスルホン酸／m-ベンゼンジスルホン酸／p-クレゾール／ニトロベンゼン） | 4 | 1行 | 30.7 ○ | 12.6 ✗ | 16.8 ✗ |
| | 4 | **上2・下2** | ★ **49.9 ○** | 20.5 ✗ | 27.3 ✗ |
| | 6 | 1行 | 21.1 ✗ | 8.7 ✗ | 11.6 ✗ |
| | 6 | 上3・下3 | ★ **38.8 ○** | 16.0 ✗ | 21.3 ✗ |
| 最良（インドール／o-ブロモベンゼンスルホン酸／フェノール／ピリミジン） | 4 | 上2・下2 | 56.7 ○ | 23.3 ✗ | 31.1 ○ |
| | 6 | 上3・下3 | 44.8 ○ | 18.4 ✗ | 24.5 ✗ |
| スマホで「層を1つずつ見る」 | 2（中央: p-クレゾール＋ニトロベンゼン） | 1行 | 56.3 | ⚠ **23.1 ✗** | 30.8 ○ |
| | 2（最悪の水層: チロシン＋アセチルサリチル酸） | 1行 | 61.8 | 25.4 ✗ | 33.8 ○ |
| | 4（中央）を **縦1列** | 縦 | 28.5 | ⚠ **11.7 ✗** | — |

★★ **結論:**
1. ★ **PC は 4成分なら最悪でも入る（28.0px）。6成分は中央値なら 2層で入る（38.8px）が、最悪は割る（27.9px）** ＝ **「6まで見る」は PC で 2層に積めば足りる。**
2. ⚠⚠ **スマホ縦は 2成分でも床を割る**（最良で 25.4px）。**縦1列に積んでも 11.7px** —— ⚠ `DESIGN_organic_tree.md` §3-3 の「縦に並べればスマホでも 33px」（見積り）は **`fitCanvasToMolecule` の 4:3 正規化を入れると成り立たない**（縦長の視野も 4:3 に広げられ、幅で縮む）。
   ★ **＝ 分液は PC 前提。スマホは「1分子ずつ瓶をかける」（いまの実験モード）か、絞り込みの札で読む。**
3. ★ 1行に並べるより **上下2層に積むほうが常に大きい**（4:3 の視野に近づくため）＝ **層の帯は画面の広さの面でも正しい形**。

### 4-4. ★ 置き場所 —— 候補と推奨

| 候補 | 判断 | 理由 |
|---|---|---|
| ★★ **実験パレット（🧪）の中の「混合物」の状態** | ★ **推奨** | 瓶は既にそこに並ぶ（23本）。**分液の操作は「瓶を混合物にかける」そのもの**で、新しい語彙が要らない。`DESIGN_experiment_mode.md` §3-3「モードを増やさず、パレットを持ち替える」と同じ形。⚠ 実験モードは「1分子しか置かない」前提で `#molecule-props` を読んでいる可能性（D-E7）があり、**混合物では表面化する** |
| 独立したモード | ⚠ 採らない | `DESIGN_ui_modes.md` の3モードの上に4つ目を置かない（実験モードが同じ理由で退けた） |
| 絞り込みの札のまま | ★ **残す**（スマホと「読む」側の受け皿） | 札は「結果を条件として使う」道具で、操作とは役割が違う。両方あってよい |
| 実験モードの課題（`quests.json`）として出すだけ | ⚠ **形が合わない** | `quests.json` は `start`→`goal` の1分子。分液は「混合物 → 各成分が別の場所」で、**課題の形を1つ足す**ことになる（`D-I9`）。第1段では出さない |

### 4-5. ★ 実装の形（I-1 の中身）

1. ★ **状態**: 原子の印 `phase`（既定 `'ether'`）。帯は `uiGroup` に1枚（水層＝下）。**「分液を始める」で帯が出て、混合物モードになる**（🧪 パレットの中のトグル）。
2. ★ **操作**: 瓶を押すと **キャンバスの全成分に順に `detect` → `apply`**（新関数 `applyToMixture`）。
   箇所選択は出さない（混合物では「どれに効くか」が問いなので、**効いた成分だけが動く**）。
   ⚠ **`wholeCanvas`（重合）とは違う**: あれは detect が1回で全体を見る。こちらは**成分ごとに既存の detect を回す**だけ。
3. ★ **層の割り当て**: ルール → 層の対応表（`neutralize_naoh`／`neutralize_nahco3`／`amine_hcl` → `aq`、`liberate_weak_acid`／`liberate_co2`／`amine_liberate_naoh` → `ether`）。
   反応が効かなかった成分は動かない（＝ 中性が残る）。
4. ★ **見出し**: 図の下の①②に **「（水層）」** を添える。`amine_hcl` は **「（塩酸塩として水層に。塩の形はまだ描きません）」**。
5. ★ **試薬の順は自由**。⚠ **効かない瓶を押したときの答え方は実験モードの §4 の3段（理由 → 官能基 → 答えは言わない）をそのまま使う**。
6. ★ **層を分ける（分液漏斗）**: 「水層を取り出す」で `aq` の成分だけ残し、`ether` を退避（または逆）。⚠ 第1段では **表示の切り替えだけ**（成分は消さない）。

---

## 5. ★★ (C) 段階的に作れるか

### 5-1. ⚠ 「対イオンを1つ実装するとまとめて開く」か —— **開かない。壁は3種類**

| 開きたいもの | 要るもの | 対イオンの粒 | 電荷 | 層 | 実測の根拠 |
|---|---|:-:|:-:|:-:|---|
| ★ **分液（4〜6成分）** | 層・NaHCO₃・CO₂・塩酸の遊離・アミンの札 | 不要 | **不要** | **要** | §4-2（58/64） |
| **アニリンの塩基性**（アニリン塩酸塩を描く） | N⁺（電荷）＋ Cl⁻（粒） | 要 | 要 | 不要 | §3-3（C₆H₈ClN） |
| **アミノ酸の両性**（双性イオン・強酸／強塩基との塩） | N⁺・O⁻（電荷）。⚠ **対イオンは要らない**（双性は分子内で打ち消す）。強塩基との塩は **線1本の -COONa で既に描ける**、強酸との塩は N⁺＋Cl⁻ | 半分 | **要** | 不要 | §3-3（双性 ≠ 中性） |
| **ジアゾニウムの分解** | N⁺≡N（電荷 ＋ `isValencyValid` の分岐）＋ Cl⁻（粒）＋ ジアゾ化の瓶（NaNO₂＋HCl）＋ 加熱分解（瓶なし → RG5 の名簿） | 要 | 要 | 不要 | §3-3・EL3 |

★★ **共通の壁は「電荷」であって「対イオン」ではない。** 粒は電荷が入れば「結合ゼロの電荷つき原子」として**ついてくる**（§3-3 で Na⁺・Cl⁻ とも自動水素 0 を実測）。
⚠ **ジアゾニウムだけは電荷が入っても開かない**: 瓶（NaNO₂＋HCl）と分解の反応と `isValencyValid` の三重結合分岐が別に要る。
★ **前例**: `reactions.json` の `aniline_diazotization` は **Cl⁻ を省略している**（desc に明記）。**粒を描くか省くかは物質ごとに選べる**（`D-I5`）。

### 5-2. ★★ 第1段の最小単位 —— **I-1「層」（電荷なし）**

★ **「これだけ入れれば意味がある」の根拠**: **V122 が画にできなかった「水層に移る」が、この段で画になる**。入試64件の **51件**が追える。電荷モデルに 1文字も触らない。

| 段 | 中身 | 新規 | 触る既存 | 開くもの | ⚠ 固定検査（§7） |
|---|---|---|---|---|---|
| ★★ **I-1 層** | 印 `phase`・帯・`applyToMixture`・ルール→層の表・見出し／**`neutralize_nahco3`**（酸だけ。フェノールが残る）／**`amine_hcl`**（印だけ）／**`amine_liberate_naoh`**（印だけ）／`liberate_weak_acid.reagentId` に `hcl` を足す | 関数 5・ルール 3 | `renderMoleculeLabels`・`reactor.refresh`・`hcl` 瓶の `acts` 文（「アミンにも」） | 分液 51/64・V122 の続き | `REAGENTS` の本数は不変（瓶を足さない）／`qa` 棚卸し（ルール 49→52）／`CV4_NO_CHANGE_RULES`（`amine_hcl`・`amine_liberate_naoh`）／`RG5` は不変 |
| ★ **I-2 CO₂** | **瓶 `co2`**・**`liberate_co2`**（フェノキシドだけ）。`org.phenol.phenoxide-co2`（qa の★見直し候補）が同時に開く | 瓶 1・ルール 1 | `acidKindOf` の分岐を再利用 | 分液 58/64・コルベ・シュミット（系統樹 §2-3 (b) #11・32件）の**瓶が揃う** | `REAGENTS.length === 23`（34287）／`transform 18`（34362）／`qa` 棚卸し（瓶 23→24） |
| ★ **I-3 電荷** | `maxValencyOf`／`isValencyValid`／`getFreeValency`／ラベル×2／`computeMolecularFormula`／`renderCharge` 移設／`findFunctionalGroups` 3型／`iupacName` 門番／写し 6か所／監査の列挙を飛ばす／`verify-compounds.js buildMolecule`。**登録: アニリン塩酸塩・グリシン双性イオン**（`D-I7`） | 関数 2・改修 9・写し 6 | ⚠ **`chemistry.js` の土台**。★ **既存 1,150件のコード無差分を機械で確かめる**（v550 の手順: 全件の正準コード・分子式・空き価標を書き出して前後 diff） | アニリン塩基性・両性・`amine_hcl` が本物の塩を描く | `EL3`（緑のまま）／`verify-compounds.js` 検査1（`isValencyValid` 経由で自動追従） |
| I-4 ジアゾニウム | 瓶 `nano2_hcl`・`diazotization`・`diazonium_decompose`（加熱・瓶なし）・`diazo_coupling`（既存機構 `diazo_coupling` に `mechanismId` で接続）。登録: 塩化ベンゼンジアゾニウム | 瓶 1・ルール 3 | `isValencyValid` の三重結合分岐（I-3 に含めてよい） | `org.aroN.diazonium-decomp`・系統樹 §2-3 (a) の「塩 3件・8本」 | `RG5`（瓶を持たないルール名簿に `diazonium_decompose`）／`qa` 棚卸し |
| I-5 分液の課題 | `quests.json` に「混合物 → 各成分」の形を1つ足す | 課題の形 1 | `EQ1`（行数の検査） | 有料側の型（`DESIGN_paid_workbook.md` §2-3 #4「分離」） | `EQ` 系 |

⚠ **I-1 と I-2 は `D-I1`（§6）の答えが「ion-equation」でも意味を失わない**（層は反応式ではない）。**I-3 以降は `D-I1` 次第。**

★ **1修正＝1コミットで割ると**: I-1 は「印と帯」→「`applyToMixture`」→「`neutralize_nahco3`」→「`amine_hcl`／`amine_liberate_naoh`」→「`hcl` を遊離に足す」の **5コミット**が自然な粒（各段で否定対照を置ける）。

---

## 6. ★ (D) 決めが要る点（`D-I1` 〜）

### 6-1. ⚠⚠ ユーザーに聞くべきもの

| # | 問い | ★ 推奨 | 根拠 |
|---|---|---|---|
| **D-I1** | ⚠⚠ **N の塩（アニリン塩酸塩・双性イオン・ジアゾニウム）を assembler で描くか、2026-09-01 の「反応式は ion-equation」に従って ion-equation に収録するか** | ⚠ **聞く。** 本書の読み: 「反応式（係数・量）」は ion-equation、「**構造式の中の電荷**（-NH₃⁺ がどこに付くか・双性の形）」は assembler、と**層で分ければ両立する**（DEVELOPMENT.md 3546 行の統合セッションの読みと同じ）。ただし qa の4件は既に ion-equation 待ちに付け替え済みで、**両方に置くと「同じ知識に2つの行き先」になる** | §1 #1 |
| **D-I2** | ★ **分液を PC 前提にしてよいか**（スマホ縦は 2成分でも床を割る） | ★ **よい**。スマホは絞り込みの札と「1分子ずつ」の実験モードが受け皿 | §4-3 |
| **D-I3** | `amine_hcl` を **構造を変えない印だけ**で先に出してよいか（「塩の形はまだ描きません」と添える） | ★ **よい**（「まだ」の取り決めどおり）。⚠ **嘘を画面に出さない**ために見出しで断る | §4-2 |
| **D-I4** | 塩酸の瓶: 既存 `hcl`（塩化水素）に遊離とアミンの塩を**相乗り**させるか、**「塩酸」の瓶を1本足す**か | ★ **相乗り**（`DESIGN_reagent_palette.md` §10.5「瓶を足す前に既存の瓶に付けられないかを見る」）。名前を「塩化水素（塩酸）」にする | §1 #3 |
| **D-I5** | 対イオン Cl⁻ を **描く**か **省く**か（前例: 機構データは Cl⁻ を省略） | ★ **描く**（分子式が教科書の C₆H₅NH₃Cl と一致する。省くと C₆H₈N⁺ になり名前が付けにくい）。⚠ ただし **Na⁺ は描かない**（O–金属は線1本のまま） | §3-3 |
| **D-I6** | 電荷つきの分子式に ⁺/⁻ を出すか | ⚠ **聞く**。双性イオンは正味 0 なので出ない。アニリニウムだけを描く場面が無ければ出番は少ない | §3-3 |
| **D-I7** | 双性イオンを **別エントリとして登録**するか（グリシン中性形と別コードになる）、**表示の切り替え**（「双性イオン形で見る」）にするか | ★ **切り替え**（登録すると名前引きが「グリシン」と「グリシン（双性イオン）」に割れ、クイズの土俵に2つ出る） | §3-5 |

### 6-2. ★ こちらで決めたもの（⚠ 異論があれば止めてほしい、の形）

| # | 決め | 根拠 |
|---|---|---|
| **D-I8** | 層は **原子の印 `phase`** で持つ（座標ではない）。帯の外にドラッグされた `aq` 成分は見出しで「層の外」と言う | §3-4。座標は見た目専用（CLAUDE.md） |
| **D-I9** | 分液の課題は **第1段では出さない**（`quests.json` の形が1分子→1分子で合わない） | §4-4 |
| **D-I10** | 試薬の順は **固定しない**。瓶を押す順は学習者が決める | §4-1（教科書順は 3/64） |
| **D-I11** | O–金属の塩は **線1本のまま**。電荷は N⁺・双性の O⁻・N≡N⁺ にだけ使う。**同じ物質に2つの形を持たない** | §3-3・§3-5 |
| **D-I12** | 電荷は **`maxValencyOf` に入れる**（`getFreeValency` と `isValencyValid` の両方が読む1か所）。正準コードのラベルには **電荷を明示**する（既存コード無差分） | §3-3 |
| **D-I13** | 電荷を持つエントリは **監査の異性体列挙の項を飛ばす**（列挙器は連結グラフしか作らない） | §3-2 |
| **D-I14** | 電荷を **手で描く経路は作らない**（`-NO₂` と同じく反応とデータからだけ現れる）。手で置くならモジュールを1つ足す別レーン | DEVELOPMENT.md 1720 行の申し送りと同じ線 |
| **D-I15** | 「教科書の分離の順」を画面で名乗らない（順は入試側で 95% ばらける。§4-1）。**順序の正解は出さず、効いた／効かないだけ返す** | ユーザー明言「画面に出典を書かない」・`P12-1` |

---

## 7. ⚠ 在庫の数を固定した検査 —— 当たる箇所（grep で洗い出した・推測ではない）

| 検査 | 場所 | 固定しているもの | どの段で当たるか |
|---|---|---|---|
| `REAGENTS.length === 23` | `tests.js` 34287 | 瓶の本数 | I-2（CO₂）・I-4（NaNO₂＋HCl） |
| `transform 18 ／ detect 5` | `tests.js` 34362 | 瓶の種類別本数 | 同上 |
| `RG5` | `tests.js` 35007 | 瓶を持たない実行ルール **11本の名簿** | I-4（加熱分解） |
| `CV4_NO_CHANGE_RULES` | `tests.js` 46753 | 構造を変えないルール **6本の名簿** | I-1（`amine_hcl`・`amine_liberate_naoh`） |
| `CV_MUST_SPLIT` | `tests.js` 45556 | 分かれるルールの**下限**（増える分は自由） | 当たらない（ラチェット） |
| `EL3` | `tests.js` 7758〜 | 「ジアゾニウム C-N≡N は弾く」の否定対照 | ★ **当たらない**（電荷の無い形は弾いたまま） |
| `Q1` | `tests.js` 13022 | `[data-modes]` は1つ | I-1 で帯のトグルに `data-modes` を付けたら当たる（付けない） |
| `qa/tests.js` 棚卸し | 997 | 瓶 23・ルール 49 | ★ **全段で鳴る**（設計された引き金。鳴ったら qa の★見直し候補を拾い直す） |
| `EQ1` | `tests.js` | `quests.json` の行数＝問数＋2 | I-5 |
| `verify-compounds.js` 検査0・1・3 | `tools/` | 行の形・価標・コードの一意性 | I-3（電荷の欄を写す 1か所）／I-4（登録） |

⚠ **`?open=` の受け口（`OPEN_TARGETS`）は固定していない**（`DEVELOPMENT.md` 3579 行の実測どおり）。

---

## 8. ⚠ 測れなかったもの・黙って落とさないもの

- **画面の px は実機で `getScreenCTM` を当てていない**（`requiredViewWidth` の写しと `summonMolecule` の並べ方の写しで計算した。REF4 が同じ物差しを 0.1px まで検証しているので桁は信じてよいが、**帯（`obstructedInsets`）の縮みは入っていない**）
- **写しの経路 29か所のうち「必ず通る 6か所」は読んで決めた。** 実機で電荷つき分子を反応・undo・並べ直しに通して確かめること
- **実験モードの D-E7**（`#molecule-props` がキャンバス全体を読む）は混合物で表面化する可能性がある。未計測
- **CO₂ 吹き込みの7件の中身**（フェノキシドだけを戻す型か、炭酸水素塩まで戻す型か）は原典を開いていない。`liberate_co2` を「フェノキシドだけ」にしたのは酸の強さの序列から決めた
- **成分数は 27/64 しか拾えていない**（入試DB側の申告）。「6まで」の根拠は拾えた分の分布（4 が最頻・最大12）
- **`ion-equation` にアニリン・グリシン等が無いこと**は qa の記録（2026-09-01 実測）を引いた。本書では数え直していない
- 測定スクリプト `measure-ion.js`／`measure-ion2.js`／`measure-ion3.js` は scratchpad にあり repo に入れていない。I-1 着手時に `tools/` へ移すか、REF4 系の検査に「分液の N 成分」を足す形で吸収する

## 9. 関連

`DESIGN_reaction_execution.md` §9（複数分子）・§10.6（酸塩基の穴）／`DESIGN_experiment_mode.md` §3-3・§4／
`DESIGN_reagent_palette.md` §10.5（瓶を足す前に）／`DESIGN_compound_coverage.md` §6-2（線1本の流儀）／
`DESIGN_organic_tree.md` §2-3 (a)（塩 3件・8本）・§3（床）／`DESIGN_paid_workbook.md` §1-1・§2-3 #4（分離の枠）／
`qa/data/assembler_links.jsonl`（4件の待つ相手）／`_解析/handoff/一覧-分離が出た大問.md`（64件）

## 変更履歴

- 2026-09-03 初版（設計レーン `ion-design`。コード変更なし）

---

## 10. ★★ ユーザー決定（2026-09-03）—— `D-I1` と `D-I2` が決着した

### 10-1. `D-I1` ★★ **受け皿は assembler。構造式として扱う**

★ **ユーザー原文**:
> **assembler自身に置き、構造式として扱える必要があるかと考えています**

⚠ **決め手は ion-equation の実体を見たこと。** `ion-equation/reactions.json` の56反応は、
**物質を化学式の文字列で持っている**（実物: `"reactants":["HCl","NaOH"]` /
`"species":["HCl","NaOH","Na+","Cl-",…]`）。★ **構造は1件も持っていない。**
⚠ **＝ アニリン塩酸塩を出しても `C₆H₅NH₃Cl` という文字列で、
`-NH₃⁺` がどこに付いたかは画面に出ない。** ★ **「構造式として扱う」なら原理的に選べない。**

| | ★ **assembler に置く** | **ion-equation に置く** |
|---|---|---|
| ★★ **構造式で扱えるか** | ★★ **扱える** | ⚠⚠ **無理。文字列しか持たない** |
| **既存資産** | ★ **登録済みの構造 1,150件** | ⚠ **有機の分子を文字列で登録し直す** |
| **反応の実行・印・監査・書き出し練習** | ★ **そのまま乗る** | ⚠ **どれも無い** |
| **イオンの扱い** | ⚠ **これから作る** | ★ **既に一級市民**（傍観イオン・半反応式・酸化数） |
| **アプリ間の依存** | ★ **増えない** | ⚠ **増える**（`?v=` の相互拘束） |
| ⚠ **化学モデルへの影響** | ⚠ **§3-3 のとおり4つ同時に割れる** | **無い** |

### 10-2. ★★ 2026-08-06 と 2026-09-01 の決定は矛盾していない

| 決定 | 対象 |
|---|---|
| **2026-08-06「イオンの受け皿は assembler」** | ★ **構造式** |
| **2026-09-01「反応式を書く際には ion-equ」** | ★ **反応式（係数・量）** |

★ **＝ 別のものを指している。** ⚠ **qa の4件が ion-equation 待ちに付け替わったのは、
そこが反応式の項目だったからで、方針の上書きではない。**
★★ **層で分ければ両立する** ——「**構造式の中の電荷は assembler、反応式は ion-equation**」。
⚠ **同じ知識に2つの行き先を作らないこと**（`D-I1` の懸念はここだけ残る。
**qa の項目ごとに、どちらの面を問うているかで振り分ける**）。

### 10-3. `D-I2` ★★ **スマホは「札の一覧 ＋ タップで1件を拡大」**

★ **ユーザー原文**:
> **スマホではカードタップ時に拡大して構造式を示すのはどうです？**

★ **採用。** ⚠ **「PC 前提・スマホは捨てる」ではなくなった。**

| | 形 |
|---|---|
| ★ **PC** | **構造式を並べて見せる**（§4-3。4成分 49.9px・6成分でも最悪 27.9px） |
| ★★ **スマホ縦** | **名前の札の一覧 ＋ タップで1件を拡大** |
| ⚠ **共通** | **層（水層／有機層）の帯は、どちらでも文字で示す** |

★ **安く付く理由**: **拡大した1件は `fitCanvasToMolecule` がそのまま担当する**
＝ **新しい縮尺の理屈が要らない**（実測 320px・1分子: エタノール／ベンゼン／アニリン／デカン 24.0px、
サリチル酸 20.3、⚠ アセチルサリチル酸 15.5）。
⚠ **いちばん大きい分子は1件だけでも入らない** —— **これは分液の問題ではなく、
320px で帯が106pxしか残らない既存の課題**（別件）。

⚠ **教材としての費用を明記しておく**: **分液の芯は「どれが塩になって水層へ移ったか」を
見比べること**で、**1件ずつ拡大すると比較が「並べて見る」から「行き来する」に変わる。**
★ **札の一覧は常に見えているので、消えるのは「構造どうしを同時に見比べる」ことだけ。**

### 10-4. ★ 無機（`/muki/` 系統分離）との関係 —— 作法は借りるが、芯は落とさない

**`/muki/` の系統分離は候補を文字の札で並べている**（`separation.js` の `cand-list` が
`ionName(c)` の chip を作るだけ・`max-width: 760px` の1カラム）。
★ **絵を描いていないので、スマホの問題が最初から起きない。**

| | **無機（系統分離）** | **有機（分液）** |
|---|---|---|
| **候補の見せ方** | ★ **文字の札** | ⚠ **構造式の図** |
| **1件あたりの幅** | **数十 px** | ⚠ **1分子で最低 24px × 原子数** |
| **スマホ縦** | ★ **入る** | ⚠ **2成分でも床を割る** |

★★ **＝ 「札の一覧」という作法は借りる。⚠ ただし有機で文字だけにすると、
「どの構造が塩になったか」という芯が消える**ので、**そこに拡大を1段 足す**（10-3）。

### 10-5. ★ 着手の承認

**ユーザー承認（2026-09-03）: 第1段 `I-1`「層」から着手する**
（**電荷ゼロ・瓶0本・ルール3本で 64件中 51件**。§5-2）。

---

## 11. ★★ 実装の記録 —— `I-1`「層」（2026-09-03・レーン `sep1`・5コミット）

★ **`I-1` は実装済み**（電荷 0・瓶 0本追加・ルール 3本追加）。以下は**設計と実装が食い違った点だけ**を残す。
一致した部分（印 `phase`・帯・`applyToMixture`・ルール→層の表・見出しの「（水層）」）は設計書のとおりに入った。

### 11-1. ★ 数の実測（★ 設計書の見立てはそのまま再現できた）

| 数えたもの | 設計書 | 実装レーンの再実測 |
|---|--:|--:|
| 分離が出た大問 | 64 | **64** |
| ★ `I-1` で追える件数 | 51 | ★ **51**（例外＝ CO₂ 吹き込み 7／両性 4／イオン交換樹脂 2 の 13件を引いた残り） |
| 試薬の出現 | 塩酸45・NaOH57・NaHCO₃41・硫酸14・CO₂7 | **同じ** |
| 1手目 | 塩酸24・NaOH22・NaHCO₃15・硫酸3 | **同じ** |
| 教科書順そのもの | 3 | **3** |

★ **設計書より一歩進めた検算**: 51件の**試薬の語と「水層へ移ったもの」の語**を、
実装が実際に持っている瓶（塩酸・NaOH・NaHCO₃・希硫酸）とクラス（アミン・カルボン酸・フェノール・中性）に
突き合わせた ＝ **語彙の面で追えない行は 0件**。「例外が付いていない」だけでなく、
**中身も実装の守備範囲に収まっている**ことを確かめた。

### 11-2. ⚠⚠ 設計と違えた点 ①: **札にするかどうかの床は 28px ではなく 24px**

★ **`D-I2` の枝（PC は並べる／スマホは札）を生かすため**に、床を
**`SUMMON_MIN_BOND_PX`（24px。アプリが既に使っている「読める」下限）** にした。

⚠ **28px（§4-3 が使った `DEVELOPMENT.md` 2026-09-02 の床）だと、実測でどの枝も通らない**
（`SEP6` が毎回測り直す。実測値は下表）:

| 器（窓の幅 → キャンバス実寸） | 最悪4 | 中央4 |
|---|--:|--:|
| 320 → 306px | 4.8 | 7.3 |
| 375 → 361px | 5.7 | 8.7 |
| 768 → 754px | 14.9 | 22.8 |
| ⚠ 900 → **573px** | 12.0 | 18.4 |
| 1280 → **953px** | 18.4 | ★ **24.0** |

★ **原因は設計書 §8 が自ら断っていたところ** —— §4-3 の数は
**作業帯（`obstructedInsets`）の縮みを入れていない**見積りで、
**分液は自分の帯（`#ws-sep`）を出すぶんキャンバスがさらに狭くなる**。
⚠ **1280px でキャンバス実寸は 953px ＝ §4-3 が仮定した 953×659 と同じ**だが、
そこでも 4成分は 24.0px（§4-3 の見積り 49.9px の半分以下）。

⚠⚠ **もう1つ、幅で分岐してはいけないことの実証が出た**:
**768px でキャンバス 754px、900px で 573px** ＝ **窓を広げるとキャンバスが狭くなる幅がある**
（900px で左パレットが横に出る）。★ **だから物差しは幅ではなく「結合が何 px か」**。

★ **帰結**: **ライブラリの最悪ケース（チロシン／アセチルサリチル酸／カプサイシン／安息香酸ベンジル）は、
測ったどの幅でも札の一覧になる。** 中央値の4成分は **1280px から並べて表示**。
⚠ **§4-3 の「PC は 4成分なら最悪でも入る（28.0px）」は、実装では成り立たない。**

### 11-3. ⚠ 設計と違えた点 ②: **NaHCO₃ の瓶が「調べるもの」と「変えるもの」の両方になった**

⚠ **`tests.js` の `RG1 (4b)` が「変えるものと調べるものは排他」を不変条件にしていた**
（設計書 §7 の表には無い引き金）。★ **例外を `nahco3` の1本だけ認めた**:
**これは化学のほうが排他でない** —— NaHCO₃ にカルボン酸を入れると
**CO₂ が出る（＝ 調べる）と同時に塩ができる（＝ 変える）**。
★ **押したときに走るのは今までどおり検出のほう**（`onReagentClick` は `DETECTION_TESTS` を先に見る）で、
**反応への入口は「反応の一覧」と「分液の混合物」だけ** ＝ `RG7`・`RG8` の不変条件は1つも動かない。

### 11-4. ★ 在庫の数（★ `I-2` 以降の出発点）

| | 着手時（v1511） | `I-1` 完了 |
|---|--:|--:|
| 実行ルール | 56 | ★ **59** |
| ★ 瓶 | 24 | ★ **24（不変）** |
| `CV4_NO_CHANGE_RULES` | 6 | **8**（`amine_hcl`・`amine_liberate_naoh`） |
| `RG5`（瓶を持たないルール） | 12 | **12（不変）** |

⚠ **`qa/tests.js` の `KNOWN_RULES` は 56 → 59**（このレーンは `qa/` を触っていない）。

### 11-5. ★ この段でやらなかったこと（申し送り）

- ★ **層ごとの横詰め（上2・下2 の2列**に組み直す）は入れていない。移動は**縦だけ**。
  ⚠ §4-3 が「上下2層に積むほうが常に大きい」と言うのはこの横詰めを前提にした形で、
  **入れれば 11-2 の数は上がる**（＝ PC で並べて見せられる場面が増える）。⚠ ただし
  「**印を付けていない成分は動かない**」という `SEP1` の否定対照とぶつかるので、対照の作り直しが要る。
- **層を分ける（分液漏斗で片方を取り出す）**は入れていない（§4-5 #6。第1段は表示の切り替えだけ、の判断どおり）。
- ★ **反応カードから来たときの箇所選びは今までどおり**。混合物の一括適用は**瓶から押したときだけ**。
