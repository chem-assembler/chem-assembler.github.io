# DESIGN_reference_book.md —— 参考書化（読み物として体系化し、キャンバスで試せるようにする）

**状態: 設計のみ（コードは1行も書いていない）。着手前の判断待ちが §7 に並んでいる。**
発端は `DEVELOPMENT.md`「開発方針（2026-09-01・ユーザー原文）」の **B. 参考書化**。

このファイルは **`DESIGN_reference_book.md` 単独で完結する**。既存の設計書には混ぜていない
（`qa/DESIGN_assembler_bridge.md` とは強く関係するが、あちらは「一問一答から assembler へ飛ぶ」話で、
こちらは「assembler の中に読み物を置く」話。**§2 で、あちらの資産をそのまま使う設計にしている**）。

---

## 1. ★★ 何を載せてよくて、何を載せてはいけないか（最初に決める）

⚠⚠ **「教科書・参考書内容を収録する」は、字義どおりにやると著作権侵害になる。**
★ **このリポジトリは過去に2度、著作物が入って公開履歴に残っている**（`git-history-leak-decision`。
**履歴は書き換えないと決着済み** ＝ **3度目を作らないことだけが残っている手段**）。

### 1-1. 線引き（この節が、この設計書のいちばん上位の制約）

| | 載せてよい | ⚠ 載せてはいけない |
|---|---|---|
| **事実** | ★ **命名規則そのもの**（「C10 の直鎖アルカンはデカン」） | —— |
| | ★ **分子式・示性式・構造** | —— |
| | ★ **物性の数値**（沸点・融点・密度・溶解度） | —— |
| | ★ **反応の起こり方**（何と何が、どういう条件で、何になるか） | —— |
| **表現** | —— | ⚠ **本文の文章**（言い回し・比喩・語順） |
| | —— | ⚠ **図・表そのもの**（レイアウト・矢印の引き方・色分け） |
| | —— | ⚠ **例題の選び方**（どの化合物を代表に選んだか） |
| | —— | ⚠ **章立て・配列**（どの順に、どう束ねて並べたか） |

★★ **＝「読み物として体系化」は *写す* のではなく *書く* こと。**
**事実は誰のものでもないので自由に使えるが、「その事実をどう並べ、どう説明するか」は書き手の表現。**

### 1-2. ⚠ 見落としやすい境界 —— 「表を作る」は安全だが「表の *選び方*」は安全ではない

ユーザーの例「**表でメタンからデカンまで分子式と名称を載せる**」は、**中身は完全に事実の羅列で問題ない**
（C1〜C10 の名称と CₙH₂ₙ₊₂ は、どの本を見ても同じでなければ間違い ＝ 表現の余地が無い）。

⚠ **危ないのは中身ではなく、次の3つ:**

1. ⚠ **「なぜその表なのか」を教科書から持ってくること。**
   「教科書のこの節にこの表があったから同じ表を作る」は、**表の *選定* を写している。**
2. ⚠ **「なぜその順なのか」を教科書から持ってくること。**
   ★ **索引の並びは §2 で *既存の自社資産* から決める**（＝ 配列を自分で決めた根拠が repo の中にある）。
3. ⚠ **「どこまで載せるか」を教科書の節の切れ目で決めること。**
   ★ 範囲の判断材料に教科書を使うのは `DEVELOPMENT.md` §「教科書に載っているか」の**1番（開発の判断材料）で許されている**が、
   **その節の *境界そのもの* を索引の単元境界にすると、章立ての模倣になる。**

★ **運用ルール: 1つの資料ページを作るたびに、「この表を作った理由」を1行で書けること。**
**書けないなら、それはどこかから持ってきている。**

### 1-3. ⚠ 「教科書ではこうなっている」を画面に出さない

`DEVELOPMENT.md` §「『教科書に載っているか』の扱い方」（2026-08-31・ユーザー明言）の3番がそのまま効く。

- ★ **出典は「開発の内側」に置く**（この設計書・レビューメモ・`qa/` の `evidence` フィールド）。
- ⚠ **学習者に出すのは *中身* だけ。** 「教科書 p.XXX にこうある」を資料ページの本文に書かない。
- ⚠ **読み込んでいるのは数研1社だけ**（`C:/Users/maequ/マイドライブ/化学/教科書/R5化学Vol.2-*.pdf`）。
  ★ **「教科書に無いので載せない」を資料の取捨の *断定的な* 根拠にしない。**
  ★ **`DEVELOPMENT.md` の「2社目が来たら真っ先に突き合わせる箇所」の A/B/C 表に、資料ページも足りるようにする**
  （＝ 参考書化で書いた資料のうち、1社ぶんの読みに依存しているものは、そこへ登録してから公開する）。

### 1-4. ★ 一次資料の扱い

`C:/Users/maequ/マイドライブ/化学/` の PDF は **読んでよい。写してはいけない。**
★ **読む目的は「事実の裏取り」と「範囲の判断」の2つに限る。**
⚠ **本文をコピーして下書きにするのは、あとで書き直しても *翻案* にしかならない。**
★ **書き方: 資料を閉じてから、事実だけを手元のメモ（分子式・数値・条件）にして、ゼロから文を書く。**

---

## 2. ★ 索引の作り方 —— 単元の切り方は「既にあるもの」から取る

⚠ **教科書の章立てを写さない**（§1-2 の2番）。★ **写さずに済むのは、repo が既に4つの単元体系を持っているから。**

### 2-1. 実測：索引に使える既存の資産（2026-09-01・実測値）

| 資産 | 場所 | 数 | 何を切っているか |
|---|---|--:|---|
| ★★ **知識項目コード** | `qa/questions.json` の `patterns` | **316項目** | ★ **知識の1粒**（`org.ali.alkane-names` など。**一度振ったら変えない主キー**） |
| ★★ **小項目（`group`）** | 同上の `group` | **52** | ★ **知識の束**（「分類・一般式」「アルカン」「シクロアルカン」…） |
| ★ **単元（`units`）** | 同上の `units` | **11** | ★ **有機の大単元**（脂肪族／アルコール・エーテル／元素分析／カルボニル／芳香族／フェノール／芳香族窒素／油脂／生体／高分子／手がかり） |
| ★★ **assembler への飛び道具** | 同上の `link` | **175 / 316（55%）** | ★ **「この知識を assembler のどこで試せるか」が既に書いてある** |
| ★ **ステージの系列** | `assembler/stages.json` の `series` | **19系列 / 120ステージ** | ★ **組み立ての順序**（操作の練習 → 直鎖アルカン → 異性体 → …） |
| ★ **絞り込みの条件カード** | `assembler/narrowing.js` `NARROW_CARDS` | **52枚** | ★ **「見分ける手がかり」の語彙** |
| ★ **化合物ライブラリ** | `assembler/compounds.json` | **941件** | ★ **呼び出せる分子の在庫**（`?summon=` の引数になる） |
| ★ **反応ルール** | `assembler/reactor.js` | **約64種**（id の実測 70、瓶とルールで重複あり） | ★ **キャンバス上で実行できる操作の在庫** |

★★ **結論: 索引の骨格は `qa` の「11単元 → 52小項目 → 316知識項目」を使う。**

**理由（＝ 配列を自分で決めた根拠。§1-2 の2番に対する答え）:**

1. ★ **これは既に自分たちで作った体系**（`qa/TAXONOMY.md`）で、**教科書の章立てではない。**
   ⚠ 教科書を*材料*にはしているが、**粒度・束ね方・コードの振り方はこちらの設計。**
2. ★★ **`link` が 175件、既に「assembler のどこで試せるか」を持っている** ＝
   **参考書化の核心（読む → 試す）の配線が、索引側に半分以上すでに書いてある。**
   **ゼロから索引を作ると、この 175件を捨てて作り直すことになる。**
3. ★ **コードが安定な主キー**なので、資料ページを `code` で名指しできる ＝
   **資料と一問一答と入試DB（`qa/data/exam_usage.jsonl`）が同じ座標で繋がる。**
4. ★ **難易度（Lv 1〜4）と `evidence`（教科書／セミナー／入試の出現）が既に各項目に付いている** ＝
   **「初学者にはここまで」を索引側で切れる**（教科書の節の切れ目を借りずに済む）。

⚠ **ただし `qa` の11単元を *そのまま* 資料の目次にはしない。** ★ **理由は §2-2。**

### 2-2. ⚠ 実測して分かった、`qa` の体系をそのまま使えない点

★ **`qa` の単元は「測る」ための切り方で、「読む」ための切り方ではない。**

- ⚠ **`org.ali`（脂肪族）だけで 72項目** ＝ **1ページに 72項目は読み物にならない。**
  ★ **読み物の1ページは `group`（52ある小項目）の粒度が妥当**（脂肪族なら「分類・一般式」12項目・「アルカン」11項目・…）。
  ★ **＝ 索引は3層（単元11 → 小項目52 → 項目316）だが、*ページ*は真ん中の52に置く。**
- ⚠ **`anal`（元素分析）・`clue`（手がかり）は、assembler で試せる相手が少ない**
  （`link` が `kind:"none"` で「分子を1つ組んでも見えない」と明記されている項目が実在する）。
  ★ **「試せる」を売りにする以上、試せない単元は資料ページを作っても片肺になる。**
  ★ **対策: ページごとに「試せる／読むだけ」を索引に出す**（⚠ 隠すと「押しても何も起きない」になる）。
- ★ **`assembler/stages.json` の19系列は、`qa` の11単元と粒度が近いが一致しない**
  （例: `stages` は「異性体をつくる（枝分かれアルカン）」を独立した15ステージの系列にしているが、
  `qa` では `org.ali` の中の一部）。⚠ **どちらかに寄せるのではなく、資料ページから *両方* へリンクする。**

### 2-3. ★ 索引の形（ヘルプ的なインデックス）

★ **ユーザーの「ヘルプのようにインデックス化」に、既存の `#tutorial-list`（操作ガイド）が形として一番近い**
（`tutorials.json` は 13件、**`keywords` 配列を持ち `#tutorial-search` で絞り込める**）。

★ **同じ形（検索欄＋一覧）を資料側にも作る。** ⚠ **同じ *モーダル* に混ぜない**（操作の説明と化学の説明は別物）。

```
📖 資料（参考書）
 ├ [検索欄]  ← qa の code / knowledge 本文 / 別名 を横断
 ├ 脂肪族炭化水素（72）           [試せる:40]
 │   ├ 分類・一般式（12）
 │   ├ アルカン（12）             ← ★ 例のページ（試せる 4）
 │   ├ シクロアルカン（2）
 │   ├ アルケン（18）
 │   ├ アルキン（11）
 │   └ 異性体・命名（17）
 ├ アルコール・エーテル（19）      [試せる:16]
 └ …
```

⚠ **`[試せる:N]` の N は `link` を持つ項目数から機械で出す。手で書かない。**

★ **単元ごとの `link` 率（実測 2026-09-01。`link.kind !== "none"` を数えた）:**

| 単元 | 試せる / 全 | |
|---|--:|---|
| アルコール・エーテル | **16 / 19** | ★ **84%** |
| 手がかり（clue） | 10 / 13 | 77% |
| カルボニル | 32 / 42 | 76% |
| 芳香族炭化水素 | 17 / 24 | 71% |
| アルコール以外の芳香族（phenol / aroN） | 各 9 / 16 | 56% |
| 脂肪族炭化水素 | 40 / 72 | 56% |
| 高分子 | 19 / 35 | 54% |
| 油脂 | 6 / 15 | 40% |
| 生体（bio） | 16 / 46 | **35%** |
| ⚠ **元素分析（anal）** | ⚠⚠ **1 / 18** | ⚠⚠ **6%** |
| **合計** | **175 / 316** | **55%** |

⚠⚠ **`anal`（元素分析）は 1/18 ＝ 実質「読むだけ」の単元。**
★ **これは欠陥ではなく性質**（**燃焼の定量計算は、分子を1つ組んでも見えない**）。
★★ **＝ 索引で「試せる」を約束するなら、この単元だけは約束しないと最初に明示する。**
⚠ **`bio`（生体・35%）と `fat`（油脂・40%）も低い** —— ★ **どちらも分子が大きく、
§3-2 の実測でスクロース（424×238単位）が PC ですら 30.0px しか出ないことと符合する。**

---

## 3. ⚠ スマホ縦を「捨てる」前に測った結果

★ ユーザーは「**スマホ縦画面の対応はすててもよいかも**」と書いた。
⚠ **統合セッションの申し送りは「SNS は Instagram が効く＝入口は縦」だった。**
★ **測った結果、判断はもう少し細かくなった。**（実測は §3-2。）

### 3-1. ⚠ 申し送りの訂正：`.wide-figure` は「横向き前提の仕組み」ではない

★ **統合セッションの申し送り: 「`.wide-figure` という仕組みが立体視で使われています（既に横向き前提の機能があるか）」**

⚠ **読んだ結果、これは逆だった。**

`assembler/style.css` 2154-2167 / 2185-2222・`assembler/stereo.js` 1500 / 2542 の実体は:

- **`.wide-figure` は「図が広いあいだ」だけ `stereo.js` が付けるクラス**（鏡像を並べているとき）。
- ★ **効果は「2カラムのグリッドを1カラムに戻す」**（`display: grid` → `display: block`）。
- ★ **狭い画面用の枝（≤899px 等）では「高さの固定 168px を外して図を縦に伸ばす」。**

★★ **＝ これは *横向きを要求する* 仕組みではなく、*横に並べられないときに縦へ積む* 仕組み。**
★ **repo が既に持っている「2ペインが入らないときの答え」は「積む」であって「諦める」ではない。**
⚠ **参考書化の分割レイアウトも、同じ作法（広ければ横に並べ、狭ければ積む／切り替える）に寄せるのが自然。**

★ **横向き専用の枝は別に実在する**: `@media (max-width: 899px) and (orientation: landscape)`（`style.css` 2030-）。
⚠ **ただしこれは「モバイル横で高さが足りないときの詰め」であって、機能を横向きに限定してはいない。**
★★ **＝ 現時点で「横向きでないと使えない機能」は assembler に1つも無い。**
⚠ **参考書化が最初の「横向き限定機能」になるなら、それは前例を作る判断であり、設計書で明言すべき。**

### 3-2. ★★ 実測（2026-09-01・実アプリを `http://127.0.0.1:9847/assembler/` で駆動）

**測り方**: 端末サイズを与えてリロード → `game.summonMolecule()` で分子を出す →
`svg.getScreenCTM()` で**画面上の実寸**を取る（`CLAUDE.md` の規則どおり viewBox 比の手計算はしていない）。
★ **指標は「結合1本の長さ（px）」と「原子ラベルの高さ（px）」**。
⚠ **なぜこの2つか**: **repo が既に「読めない大きさ」の実測値を持っているから**（次項）。

#### ★★ 判定のものさしは repo の中にあった

`assembler/style.css` 2156-2160（2026-08-28 のコメント・実測の記録）:

> 立体ビューの鏡像が 0.368倍に縮み、**結合 78 → 28px・ラベル 12.5 → 4.6px**
> ＝ **読めない大きさに潰れていた（実測）**

★★ **＝ このアプリが自分で「読めない」と判定した実測値が 結合28px・ラベル4.6px。**
★ **参考書化のレイアウトも、この線を割ったら不合格とする**（⚠ **新しく基準を発明しない**）。

#### 実測① 375×812 縦・**資料ペインを足す前**（キャンバス 361×555px）

| 画面の内訳 | px |
|---|--:|
| ヘッダー | 44 |
| リボン（`.canvas-header`） | 110 |
| ★ **描画面（`#svg-wrapper`）** | ★ **361 × 555** |
| 下のパレット帯（`#left-panel`） | 83 |

★ **ユーザーの例そのもの（メタン→デカン）を、この面で1件ずつ出して測った:**

| 分子 | 結合 px | ラベル px | 判定（28 / 4.6 が床） |
|---|--:|--:|---|
| メタン | **32.4** | 6 | ○ |
| ブタン | **31.9** | 6 | ○ |
| ヘキサン | **25.9** | 5 | ⚠ **結合が床割れ** |
| オクタン | **21.8** | 4 | ⚠⚠ **両方割れ** |
| ★ **デカン** | ★ **18.9** | ★ **4** | ⚠⚠ **両方割れ** |

★★ **＝ 375px 縦では、資料を1pxも置かないうちに、ユーザーの例が C6 で床を割る。**
⚠⚠ **「スマホ縦を捨てるかどうか」以前に、この題材はスマホ縦で既に読めない。**

**理由は幅**: ★ **直鎖アルカンは 42px 刻みの水平1列で描かれる**（`GRID_SIZE = 42`）。
**デカンは 378 SVG単位の幅・高さ 0**。⚠ **375px 縦の描画面は 360 単位幅 × 554 単位高**
＝ ★★ **縦に 554 余っているのに、横が 360 しかなくて入らない。**
★ アプリは自動で縮めて収める（デカンで viewBox 803×602 まで引く）ので**切れはしないが、そのぶん潰れる**。

#### 実測② 812×375 横（＝「横向き前提にする」案）

| 分子 | 結合 px | ラベル px |
|---|--:|--:|
| ヘキサン | 34.0 | 6 |
| オクタン | 28.6 | 5 |
| デカン | **24.7** | 5 |

★ **横にすると デカン 18.9 → 24.7px**（**+5.8px**）。⚠ **それでも床（28）は割れたまま。**

#### 実測③ ★★ 横向きで「分割」したらどうなるか（812×375 横・資料ペインを左右に置く）

| 資料ペイン幅 | キャンバス幅 | デカン 結合px | ヘキサン 結合px |
|--:|--:|--:|--:|
| なし | 644 | 24.7 | 34.0 |
| **240** | 420 | ★ **18.4** | 25.3 |
| 280 | 380 | 16.7 | 22.9 |
| 320 | 340 | 14.9 | 20.5 |
| 360 | 300 | **10.9** | 15.0 |

⚠⚠⚠ **これが今回いちばん効いた1行:**

★★ **「スマホを横にして、資料とキャンバスを分割する」（デカン 18.4px）は、
「スマホ縦のまま、分割しない」（デカン 18.9px）より *悪い*。**

★ **240px は資料ペインとして最低限**（日本語なら 14px で1行 15字ほど ＝ すでに読み物として苦しい）。
**その最低限を払った時点で、横向きの取り分（+5.8px）を使い切って足が出る。**

⚠ **＝ 「縦を捨てて横向き前提で分割する」は、測ると *何も買えていない*。**
★ **横向きが有利なのは「分割しない」場合だけで、分割した瞬間に縦以下へ落ちる。**

#### 実測④ 1280×800（PC。キャンバス 953×659px）

| 資料ペイン幅 | キャンバス幅 | デカン 結合px | スクロース 結合px |
|--:|--:|--:|--:|
| なし | 953 | 51.7 | 48.2 |
| **320** | 649 | **38.2** | 33.1 |
| ★ **380** | 589 | ★ **34.7** | ★ **30.0** |
| 440 | 529 | 31.2 | **27.0** ⚠ |

★★ **PC では分割が成立する。380px の資料ペインを置いても、デカン 34.7px・スクロース 30.0px で床（28）を保つ。**
⚠ **440px まで広げるとスクロース（在庫で最大級・424×238単位）が 27.0px で床を割る。**
★★ **＝ 資料ペインの上限は 380px。** **これは「読める文字幅」としても十分**
（380 − 左右余白32 = 348px。14px の日本語で1行 約24字）。

### 3-3. ★★ 結論 —— 「捨てる」ではなく「分割はPCだけ・スマホはタブ」

| 端末 | 資料とキャンバスの関係 | 根拠（実測） |
|---|---|---|
| ★ **PC（≥1000px 程度）** | ★★ **左右に分割**（資料 ≤380px・キャンバスが残り） | 実測④。デカン 34.7px で床を保つ |
| ⚠ **スマホ横** | ★ **分割しない。タブで切り替える** | 実測③。分割すると縦以下（18.4 < 18.9） |
| ⚠ **スマホ縦** | ★ **分割しない。タブで切り替える** | 実測①③ |

★★ **＝ ユーザーの「スマホ縦は捨ててもよいかも」は、*捨てなくてよい*。**
⚠ **捨てるべきは「スマホで *分割* すること」のほうで、縦か横かは論点ではなかった。**
★ **タブ切り替えなら、キャンバスは今の 361×555px をまるごと保てる ＝ 何も失わない。**

⚠ **ただし正直に言うと、タブでも「デカンが読めない」は直らない**（実測①）。
★ **それは参考書化が持ち込んだ問題ではなく、既にある問題**（→ ★ `D-R5` で別に立てる）。

### 3-4. ★ タブは「逃げ道」ではなく、この題材では正攻法

⚠ 統合セッションの申し送りは「**タブは *捨てる* のではなく *別の形にする* です。逃げ道として成立するかを測ってください**」だった。
★ **測った答え: 逃げ道どころか、スマホでは唯一の選択肢で、しかも失うものが無い。**

**理由（実測から）:**

1. ★ **分割は必ずキャンバスから面積を取る。** ★★ **タブは1pxも取らない**（重ねるので）。
2. ⚠ **「資料を見ながら試す」の *同時性* は、スマホでは元から成立していない。**
   ★ **240px の資料ペインは1行15字 ＝ 表を出したら1列しか入らない。**
   ★★ **メタン〜デカンの表は「名称・分子式」の2列。2列を 240px に入れると、
   キャンバス側が 18.4px（＝縦より悪い）。「同時に出た」が「両方読めない」になる。**
3. ★ **repo の前例も「積む／切り替える」側**（§3-1 の `.wide-figure`）。

### 3-5. ★ 実装の下地はある（新しく作る量は少ない）

- ★ **タブの前例**: `.nw-mode-tabs`（絞り込みモード・`style.css` 2683・`index.html` 898）。**flex の素朴なタブ。**
- ★ **フロートの前例**: `.summon-ac`（名称候補リスト・`style.css` 2726-）。
  ★ **`document.body` 直下の `position: fixed`・z-index 1200 で、モーダル(1000)より上・収録(10000)より下。**
  ⚠ **この置き場所の理由（積み重ね文脈に閉じ込めない／モーダルの `overflow-y` に切られない）は、
  資料をフロートさせる場合もそのまま当てはまる ＝ 前例を踏襲すればよい。**
- ⚠ **`main` は既に flex で、狭い画面では `flex-direction: column` に倒れる**（`style.css` 1950-）。
  ★ **分割ペインを `main` の子として足せば、既存のメディアクエリの枠組みに乗る。**

---

## 4. ★ 資料とキャンバスの並べ方（3案の見積り）

⚠ **見積りは `DEVELOPMENT.md`「実装の見積りは『見せ方』を数に入れないと外れる」（2026-09-01・実発生）に従い、
判定・実行だけでなく「見せ方」と「既存の緑のテスト」を数に入れる。**

### 4-1. 3案

| | **案A 分割（split）** | **案B フロート** | **案C タブ** |
|---|---|---|---|
| **形** | `main` に資料ペインを足し、キャンバスと左右に並べる | `position: fixed` の窓を重ねる | キャンバスと資料を同じ枠で切り替える |
| ★ **同時に見える** | ○ | △（**重なるぶんキャンバスを隠す**） | ✕ |
| ★ **キャンバスの面積** | ⚠ **減る**（実測④で 953 → 589） | **減らないが隠れる** | ★ **1pxも減らない** |
| ★ **スマホで成立** | ⚠⚠ **しない**（実測③） | △（**隠す面積 = 資料の面積**なので分割と同じ痛み） | ★ **する** |
| ★ **前例** | `main` の flex（既存） | `.summon-ac`（既存） | `.nw-mode-tabs`（既存） |

### 4-2. 手数の見積り（⚠ 「見せ方」を数に入れた版）

★ **どの案でも共通で要るもの**（＝ 案の選択では減らない。**先に数える**）:

| 共通 | 数 | 中身 |
|---|--:|---|
| **資料データ** | **新規1ファイル** | `assembler/reference.json`（§5 の形） |
| **索引の描画** | **新規1本**（`reference.js`） | 単元11 → 小項目52 の2階層＋検索 |
| **本文の描画** | **同ファイル内** | 表・段落・「試す」ボタン |
| ★ **見せ方** | **≒ 本体と同量** | 開閉のアニメ／空のときの案内／読み終えた印／「試す」の押し味 |
| ⚠ **既存の緑のテスト** | ⚠ **要調査**（→ `D-R6`） | ★ **`tests.js` は 44,150行**。「在庫の数を固定した検査」があると必ず当たる |

★ **案ごとの差分だけ:**

| | 案A 分割 | 案B フロート | 案C タブ |
|---|--:|--:|--:|
| **新規 CSS ブロック** | 3（基底・≥1000px・狭い画面） | 2（fixed 窓・ドラッグ） | 2（タブ帯・切替） |
| ⚠ **触る既存 CSS** | ⚠ **`main` / `#canvas-container` の flex**（★ **全モード共通の骨格**） | 0 | 1（`#canvas-container`） |
| **新規 JS 関数** | 2（開閉・幅の記憶） | ★ **5**（開閉・ドラッグ・端の吸着・画面外復帰・z-index 調停） | 2（開閉・状態の記憶） |
| ⚠ **キャンバス実寸への影響** | ⚠⚠ **あり**（**`getScreenCTM` に依存する既存機能すべてが再測定の対象**） | なし | なし |
| **リスク** | ⚠⚠ **高**（骨格を触る） | ⚠ **中**（ドラッグの実装は必ず伸びる） | ★ **低** |

⚠⚠ **案A の「触る既存 CSS」が本当の値段。** ★ **`main` の flex はパズル・学習・自由・録画・絞り込みの
全部が乗っている骨格で、`.recording` のクリーン画面も同じ器を使う**（`style.css` 2226-）。
★ **ここを触ると、参考書と関係ない場所の実測がやり直しになる。**

### 4-3. ★★ 推奨: **案A（PC のみ）＋ 案C（スマホ）** —— 1つの実装で両方まかなう

★★ **同じ資料ペインの DOM を、メディアクエリで「横に並べる（PC）」と「重ねて切り替える（スマホ）」に振り分ける。**

```
[基底] 資料ペインは切り替え式（重ねる）  ← スマホ・タブレット縦がここに落ちる
@media (min-width: 1000px) { 資料ペインは横に並べる（幅 ≤380px） }
```

★ **`min-width: 1000px` を使うのは、`style.css` 2185 の立体ビュー2カラム化が既に同じ閾値を使っているから**
（⚠ **新しい閾値を発明しない**。**閾値が増えるほど、端末ごとの実測の組み合わせが増える**）。

★ **利点:**
- ★ **DOM は1つ。** **資料の中身も「試す」ボタンも1回書けば両方で効く。**
- ★ **JS の分岐が要らない**（**CSS だけで振り分く**）。⚠ **JS で端末を見て切り替えると、
  回転・ウィンドウ変形のたびに状態を持ち直す必要が出る**（★ **§3-2 の実測中に、
  `#svg-wrapper` を縮めても viewBox が再計算されないことを確認済み** ＝ **リサイズの追従は既に弱い**）。

⚠ **案B（フロート）は採らない。** ★ **理由は面積ではなく手数**:
**ドラッグ・端の吸着・画面外に出たときの復帰・他の層との z-index 調停で新規関数が 5本になり、
「見えている面積」は案A と変わらない**（★ **重ねて隠すか、押しのけて狭めるかの違いしかない**）。
⚠ **ユーザーは「分割するか、フロートさせる」と両方挙げているので、これは `D-R2` として判断を仰ぐ。**

### 4-4. ★ 実測: 「在庫の数を固定した検査」がどれだけあるか

⚠ **`DEVELOPMENT.md` の「見積りが外れた」実例は、まさにこれで赤くなった**
（★ **「瓶を持たない実行ルールは10件」が11件になった**）。★ **着手前に数を出しておく。**

| | 実測（`assembler/tests.js`・44,150行） |
|---|--:|
| **`test('…')` の総数** | **691** |
| **`assert(...)` の総数** | **7,194** |
| ⚠ **`.length === 定数` の assert** | **1,054** |
| ⚠⚠ **うち 定数が2桁以上（＝在庫を固定していそうなもの）** | ★ **63** |
| **「N件を期待」型のメッセージ** | 187 |

★ **実物を見た限り、当たりそうなのは次の系統:**

- ⚠ `RG5`（33243行）… **「瓶を持たない実行ルールは環化3件・重合4件・縮合3件・アセタール化1件」**
  ＝ ★ **これは反応ルールの話なので、資料を足すだけでは動かない。**
- ⚠ **見出しの数を固定した検査**（14508行「見出しが20個であるべき」・14545行「12個」）
  ＝ ★★ **UI の要素数を数えている ＝ 資料ペインを足すと当たりうる。**
- ★ **ライブラリ件数系**（エーテル10件・糖の環17件・異性体45通り…）
  ＝ ★ **資料は `compounds.json` を触らないので当たらない。**

★★ **＝ 危ないのは「化学のデータ」側ではなく「画面の要素を数えている」側。**
⚠ **§4-2 の「触る既存 CSS」（案A）と同じ場所を指している** ＝
★ **案A を採ると、この系統の検査を実際に走らせて確かめる手間が増える。**
★ **案C（タブ）は既存の要素を1つも動かさないので、ここが軽い。**

---

## 5. ★ 「読む → 試す」のつなぎ方（ユーザーの例で具体的に）

### 5-1. ★★ 配線は既にある（実測）

★ **`assembler/game.js` の `applyOpenParam()`（10860-11010）が受け口を持っている:**

| 受け口 | 効果 |
|---|---|
| `?summon=<名称 or id>` | ★ **分子を1つキャンバスに出す**（**モードは 🧪自由 に自動で切り替わる**） |
| `?open=<行き先>` | ★ **30個の行き先**（`OPEN_TARGETS`: quiz / naming / isomer / stereo / practice / mechanism / molecule …） |
| `?reagent=<試薬 or ルールid>` | ★ **試薬を選んだ状態にする** |
| `?series=<系列名>` | ★ **パズルの系列を選ぶ**（部分一致） |
| `?open=isomer&formula=<式>` | ★ **異性体の書き出しを分子式つきで開く** |
| `?from=<アプリ>&code=<コード>` | ★ **「来た道」の帯を出して戻れるようにする** |

★★ **＝ 「試す」ボタンは、この受け口を *内部で* 呼ぶだけでよい。**
⚠ **URL を組み立てて自分自身へ遷移させる必要はない**（**同一ページ内なので `window.game.summonMolecule()` を直接呼ぶ**）。
★ **ただし「資料ページへのリンク」を外から張れるように、`?open=reference&code=<知識コード>` は足す**（→ `D-R3`）。

### 5-2. ★★ ユーザーの例をそのまま書く: 「アルカンの命名」の資料ページ

**索引**: `脂肪族炭化水素` → `アルカン`（★ **`qa` の `group` = 実在する小項目**）

**ページの中身（★ 全部「事実」。§1-1 の左列しか使っていない）:**

```
■ 直鎖アルカンの名前は、炭素の数で決まる

（自分の言葉で 2〜3行。「炭素が1個ならメタ、2個ならエタ…という語幹に
  『飽和』を表す -ane を付ける」という規則そのものは事実）

┌────┬───────┬──────────┬──────────┐
│ C数 │ 名称   │ 分子式     │           │
├────┼───────┼──────────┼──────────┤
│  1 │ メタン  │ CH₄       │ [▶ 組んでみる] │
│  2 │ エタン  │ C₂H₆      │ [▶ 組んでみる] │
│  …                                        │
│ 10 │ デカン  │ C₁₀H₂₂    │ [▶ 組んでみる] │
└────┴───────┴──────────┴──────────┘

（表の下に、規則の言い換えを1〜2行）
```

★ **表の中身の出どころ**: ⚠ **教科書ではなく `assembler/stages.json` の系列「アルカン（直鎖）」。**
★★ **実測で確認: この系列は methane / ethane / propane / butane / pentane / hexane / heptane /
octane / nonane / decane の *ちょうど10件* で、`name` と `formula` を持っている。**
★★ **＝ ユーザーの例の表は、repo に既にあるデータから機械で出せる**（⚠ **手で打たない ＝ 写す余地がゼロ**）。

**★ 「▶ 組んでみる」を押したとき（3通りのうち、どれにするかは `D-R4`）:**

| | 動き | 良い点 | ⚠ 悪い点 |
|---|---|---|---|
| **(a) 出す** | `summonMolecule('デカン')` ＝ **完成品が出る** | 1手。名称表示がすぐ確認できる | ⚠ **「実際にキャンバスで C 原子をつなげ」ではない**（**組んでいない**） |
| ★ **(b) お題にする** | **パズルモードでその1件を出題**（`stages.json` に既にある） | ★★ **ユーザーの意図どおり「自分でつなげる」** | ★ **モードが変わる**（資料ペインは残す） |
| **(c) 空にする** | キャンバスを空にして、名称表示だけ見張る | 自由に試せる | ⚠ **正誤が出ない** |

★★ **推奨は (b)。** ⚠ **理由: `stages.json` の10ステージが、既に `target`（正解の原子と結合）を持っている**
＝ ★ **「つないだら合っているかを判定する」仕組みが、1行も書かずに使える。**
★ **(a) は「読むだけ」の項目（物性・用途など、組んでも見えないもの）用に残す**
（⚠ **`qa` の `link` が `kind:"none"` と書いている項目が実在するのと同じ切り分け**）。

### 5-3. ★ 戻り道（⚠ CLAUDE.md の「往復にする」）

⚠ **`CLAUDE.md`: 「アプリ横断のリンクは往復にする。片道だと辞書引きの流れがそこで途切れる」。**
★ **資料 → キャンバス は同一ページ内なので「横断」ではないが、流れが途切れる問題は同じ。**

★ **資料ペインを *開いたまま* にする**（**閉じない**）。
⚠ **モードが変わっても資料ペインは残す**（★ **5-2(b) でパズルへ移るので、ここが要**）。
★ **スマホ（タブ）では、キャンバスへ切り替えたときに「📖 アルカン に戻る」の小さな帯を出す**
（★ **`renderFromBand`（`game.js` 10905-）と同じ考え方。実装も流用できる**）。

---

## 6. ★ チュートリアル化するか（2-3 の実測をふまえて）

### 6-1. ⚠⚠ 実測: 既存の `tutorials.json` では **書けない**

★ **統合セッションの申し送り: 「チュートリアルの仕組みは既にあります。既存の仕組みで書けるかを測ってください」**
⚠ **測った答え: 書けない。しかも「足りない」のではなく「向きが逆」。**

**実測（`assembler/tutorials.json` 13件・`assembler/tutorial.js` 777行）:**

| | 数 |
|---|--:|
| 総エントリ | **13** |
| ★ **手順を持つもの** | **9**（**計 38ステップ**） |
| **FAQ（`answer` だけで手順なし）** | **4** |

★★ **38ステップの `actions` を型で数えた結果:**

```
button 19 / wait 19 / click 13 / hover 3 / clickBond 2 / undo 2 / summon 2 /
cutBond 1 / drag 1 / reactionButton 1 / wheel 1 / pan 1 / toggle 1
```

⚠⚠ **`click` は「ユーザーがここを押すのを待つ」ではなく「座標 (420, 294) をアプリが自分で押す」。**
★★ **＝ `tutorials.json` は *自動再生のデモ* であって、*ユーザーにやらせる演習* ではない。**

**裏付け:**
- ★ `tutorial.js` に **`stop` ボタンと `this.aborted`**（724行）＝ **再生を止める仕組みはあるが、待つ仕組みではない。**
- ★ `caption` が **`mouse` / `trackpad` / `touch` の3通り**を持つ ＝ **「こう操作します」という *説明* が本体。**
- ★ **13件は全部「操作の説明」**（原子を置く・結合を切る・環を置く・拡大する…）で、**化学の内容は1件も無い。**

★★ **＝ ユーザーの「実際にユーザーがメタンからデカンまでつくればクリア」は、
`tutorials.json` の延長線上には無い。** ⚠ **足りないのは「ユーザーの操作を待って、正誤を判定する」機構。**

### 6-2. ★★ ところが、その機構は **別の場所に既にある**

★ **`assembler/stages.json`: 120ステージ / 19系列。** ★★ **各ステージが `target`（正解の原子と結合）を持つ。**

★★ **そして系列「アルカン（直鎖）」は、ユーザーの例と *完全に一致する 10件* だった:**

```
methane メタン CH4 / ethane エタン C2H6 / propane プロパン C3H8 /
butane ブタン C4H10 / pentane ペンタン C5H12 / hexane ヘキサン C6H14 /
heptane ヘプタン C7H16 / octane オクタン C8H18 / nonane ノナン C9H20 /
decane デカン C10H22
```

⚠⚠⚠ **＝ ユーザーが「チュートリアル化するのも一案」と言った内容は、**
★★ **`stages.json` に *既に実装済み* で、しかも `?series=アルカン（直鎖）` で直接開ける。**

★ **足りないのは1つだけ: 「10件そろって初めてクリア」という *束* の概念。**
⚠ **いま `stages.json` は1ステージ = 1完了で、系列単位の達成を持っていない**（→ `D-R7`）。

### 6-3. ★ 結論: 新しくチュートリアルを作らない

| ユーザーの言葉 | ★ 実際にやること |
|---|---|
| 「チュートリアル化する」 | ⚠ **`tutorials.json` は使わない**（自動再生の器なので向きが逆） |
| 「メタンからデカンまでつくればクリア」 | ★ **`stages.json` の既存10ステージを、資料ページから `?series=` で開く** |
| **クリアの判定** | ★ **既にある**（各ステージの `target` との照合） |
| ⚠ **「そろってクリア」** | ⚠ **これだけが新規**（系列の達成率。→ `D-R7`） |

### 6-4. ⚠ 「可能な操作を制限する」は、既存の仕組みに **無い**

★ **統合セッションの申し送り: 「チュートリアルは手順を誘導しているはずなので、近いものがあるかもしれません」**
⚠ **測った答え: 誘導はしているが、それは *再生中にアプリが自分で押している* だけで、
「ユーザーが押せるものを減らす」仕組みではない。**

★ **近いものが1つだけある: モバイルでのボタン削減**（`style.css` 1930 付近）:

> `#btn-tool-select, #bond-type-group { display: none; }`
> ⚠ **DOM には残す**（★ **チュートリアル・テスト・PC表示のため**）

★★ **＝ 「見せない・DOM は残す」という前例が既にあり、しかも
「なぜ DOM を残すか」の理由（テストと再生が触るから）まで書いてある。**
★ **資料からの演習で操作を絞るなら、この作法（CSS で隠す・DOM は残す）を踏襲するのが安全。**

⚠ **ただし、絞るべきかどうかは別の問題** —— ★ **`D-R8` に立てる。**
⚠ **`DEVELOPMENT.md` の実験モードの記述に「完全に隠すと初学者が詰むので、
`explainPlacementMiss` と同じ *外したときにだけ効くヒント* に逃がせないか」とある。**
★ **同じ論点がここにも出る**（**操作を減らすか／減らさずに外したときだけ助けるか**）。

---

## 7. ★★ 無料と有料の線（2026-09-01・ユーザー追加方針）

> ★ **参考書は無料、例題つき** ／ ★★ **問題集は有料**

★ **＝ この設計書が扱う参考書は「無料・例題つき」で確定。** ⚠ **有料の問題集はこの設計書の担当外。**
★ **ここに書くのは「線をどこに引くかが、参考書の作り方にどう効くか」だけ。**

### 7-1. ★★ 「例題」と「問題」の線 —— *種類* で分ける

| | 形 | ⚠ 参考書への影響 |
|---|---|---|
| **数で分ける** | 無料は例題 N 問まで、続きは有料 | ⚠⚠ **参考書が「無料ぶんを解き切ったら有料へ」の入口になる** |
| ★★ **種類で分ける** | **例題＝読み物の一部（説明のための1問） ／ 問題集＝演習** | ★ **参考書は読み物として完結する** |

★★ **推奨は「種類で分ける」。** ⚠ **理由は方針の読みではなく、実測から出る:**

1. ★★ **ユーザーの例（アルカンの命名）は、数で分ける線を引きようがない。**
   ★ **表がメタン〜デカンの10行で、`stages.json` の10ステージと1対1**（§6-2 の実測）。
   ⚠⚠ **「7問目から有料」にすると、表の途中に壁ができる。**
   ★★ **＝ 表そのものが「C の数で名前が決まる」という規則の *説明* なので、
   途中で切ると説明が壊れる。** **例題は読み物の一部だという証拠がここにある。**
2. ⚠ **数で分けるには「何問目か」を数える必要があり、数えるには誰が解いたかを覚える必要がある。**
   ★ **実測: assembler に認証・ログインの実装は1行も無い**（`supabase` / `login` / `auth` の
   出現 0件）。★★ **＝ 数で分ける案は、参考書の着手前に認証の実装が要る。**
3. ★ **`stages.json` の120ステージは既に全部無料で公開中**（§7-3）。
   ⚠ **例題がそこから来る以上、例題を有料にするのは「取り上げる」形になる。**

★★ **＝ 参考書の中の例題は、全部無料で完結する。**
★ **ユーザーの例（表を見ながらキャンバスで C をつなげて名称を確認する）は、
既存の無料資産（`stages.json` 10件 ＋ `qa` の知識項目12件）だけで成立する。**
⚠⚠ **有料の要素を1つも足さずに書ける。**

### 7-2. ⚠⚠ 静的配信では、ブラウザに送ったものは全部読める（実測）

★ **統合セッションの指摘のとおりだった。実際に叩いて確かめた**（`http://127.0.0.1:9847/`）:

| ファイル | HTTP | 大きさ |
|---|--:|--:|
| `assembler/stages.json` | **200** | 190,037 B |
| `assembler/compounds.json` | **200** | 683,358 B |
| `qa/questions.json` | **200** | 677,195 B |
| `assembler/reactions.json` | **200** | 120,433 B |

⚠ **どれも URL を直接叩くだけで全文が返る**（★ **ビルドも難読化も無い ＝ `CLAUDE.md` の
「ビルドツールなし・静的配信」がそのまま意味している状態**）。

★ **3案のうち、参考書側に効く点だけ書く:**

| | 案 | ★ 参考書への影響 |
|---|---|---|
| **①** | **サーバー側で配る** | ⚠⚠ **配信の作りが変わる。** ★ **参考書は無料なので、参考書だけなら静的のままでよい** ＝ ★★ **参考書の着手を、この決定より先に進められる** |
| **②** | **建前で分ける**（読めるのは許容） | ★ **参考書に影響なし** |
| ★ **③** | **問題は見えてよい。「採点・記録・解説」を有料にする** | ⚠⚠ **境目が「コンテンツ」でなく「機能」になる** → ★ **7-2a が要る** |

#### ★ 7-2a. ⚠ 案③を採る場合 —— 参考書の例題に採点は付くのか

⚠⚠ **付く。付けないと参考書が成立しない。**

★ **理由（実測）: §5-2 で推奨した「▶ 組んでみる」→ (b) パズルのお題にする は、
`stages.json` の `target` との照合 ＝ *採点そのもの*。**
★★ **これは既に実装済み・既に無料で公開中**（120ステージ全部が今この瞬間に採点付きで動く）。

★★ **＝ 案③を採るなら、有料の線は「採点があるかどうか」ではなく、
もっと細かいところに引かないと、既存の無料機能を取り上げることになる。**
★ **引けそうな場所（★ どれも「これから作るもの」なので、取り上げにならない）:**

- ★ **記録**（**どこまで解いたか・間違えた項目の蓄積・習得マップの埋まり具合**）
  ⚠ **`qa` の「習得マップ」は既にある概念なので、どこまでが既存かを確かめてから線を引く。**
- ★ **解説の深さ**（**なぜ間違えたかの個別解説**。⚠ **正誤の表示そのものは無料側に置く**）
- ★ **束ねた演習**（**単元をまたいだ出題・弱点からの再出題**）

⚠ **＝ 参考書側の設計としては「例題は必ず採点まで無料」を前提に書いてよい。**
★ **`D-R10` で確認を取る。**

### 7-3. ★ 既存の資産は全部いま無料で公開中（実測）

| 資産 | 数 | 状態 |
|---|--:|---|
| **パズルのステージ**（`stages.json`） | **120** | ★ **公開・無料・採点つき** |
| **知識項目**（`qa/questions.json` の `patterns`） | **316** | ★ **公開・無料** |
| **設問**（`variants` の総数） | ★ **632** | ★ **公開・無料** |
| **化合物ライブラリ**（`compounds.json`） | **941** | ★ **公開・無料** |

⚠⚠ **＝ 有料の線は「これから作るもの」にしか引けない。**
★★ **参考書は「これから作るもの」だが、ユーザーは *無料* と決めた** ——
⚠ **つまり有料になるのは、参考書のさらに先（問題集）だけ。**
★ **この設計書は、その先を1つも前提にしないで書けている**（§7-1 の3点）。

### 7-4. ⚠ 無料で公開するぶん、§1 の線引きはいっそう厳しく

★★ **参考書は「無料で誰でも読める読み物」として公開される。**
⚠⚠ **＝ 著作権の線引き（§1）を割ったときに、それが公開物として残る。**
★ **§1-2 の運用ルール（「この表を作った理由を1行で書けること」）は、
資料ページを1枚作るごとに *必ず* 通す** —— ⚠ **有料なら人目が少ない、という逃げ道は無いし、
そもそも無料のほうが読まれる。**

---

## 8. ★ 判断待ち（ユーザーへ）

| # | 論点 | ★ こちらの推奨 | 効いてくる範囲 |
|---|---|---|---|
| **D-R1** | ⚠ **索引の骨格を `qa` の「11単元 → 52小項目 → 316項目」にしてよいか**（★ **教科書の章立てを写さないための、いちばん確実な手**。⚠ **代わりに、粒度は「測る」向きなので読み物としては粗いところがある**） | ★ **する** | 全体 |
| **D-R2** | ★ **フロート案を落としてよいか**（⚠ **ユーザーは「分割するか、フロートさせる」と両方挙げた**。★ **実測では、見える面積は分割と同じで、新規関数だけ 2 → 5 に増える**） | ★ **落とす**（分割＋タブ） | §4 |
| **D-R3** | ★ **`?open=reference&code=<知識コード>` を足してよいか**（★ **一問一答・ハブ・SNS から資料ページへ直接飛べるようになる**。⚠ **`OPEN_TARGETS` に1件足すだけ**） | ★ **足す** | §5-1 |
| **D-R4** | ★★ **「▶ 組んでみる」は (a) 完成品を出す / (b) パズルのお題にする / (c) 空にする のどれか** | ★ **(b)** ＋ **組めない項目だけ (a)** | §5-2 |
| **D-R5** | ⚠⚠ **「スマホ縦でデカンが読めない」（結合 18.9px・床は 28px）を、参考書化の中で直すか、別件にするか**（★ **これは参考書化が持ち込んだ問題ではなく、既にある問題**。⚠ **直すなら「直鎖を折り返して描く」等の作図の変更になり、範囲が跳ねる**） | ★ **別件にする**（`DEVELOPMENT.md` の待ち行列へ） | §3-2 |
| **D-R6** | ⚠ **`tests.js` の「在庫の数を固定した検査」63件のうち、資料を足すと当たるものを着手前に洗い出すか**（★ **実測は §4-4**） | ★ **洗い出す**（★ **63件は1レーンで見切れる量**） | §4-2 / §4-4 |
| **D-R7** | ★ **「系列をそろえてクリア」を作るか**（⚠ **`stages.json` は1ステージ=1完了で、系列単位の達成を持っていない**。★ **これが「チュートリアル化」で唯一の新規**） | ★ **作る**（**小さい**） | §6-2 |
| **D-R8** | ⚠ **資料からの演習で「可能な操作を制限する」か**（★ **前例は「CSS で隠す・DOM は残す」**。⚠ **実験モードの `explainPlacementMiss` 案と同じ論点なので、揃えたほうがよい**） | ⚠ **制限しない。外したときだけ助ける** | §6-4 |
| **D-R9** | ★ **最初に書く資料ページをどこにするか**（★ **推奨は `脂肪族炭化水素 / アルカン`** ＝ **ユーザーの例そのもの・`stages.json` の10件がそのまま使える・`qa` の知識項目が 12 ある**） | ★ **アルカン** | 着手順 |
| ★ **D-R10** | ★★ **「例題」と「問題」の線を *種類* で分けてよいか**（**例題＝読み物の一部で全部無料・問題集＝演習で有料**）。⚠ **数で分ける（無料は N 問まで）を採ると、①ユーザーの例の表がメタン〜デカンの10行で1対1なので途中に壁ができ、②誰が何問解いたかを数えるために認証が要る（★ **assembler に認証の実装は現在0行**）** | ★ **種類で分ける** | §7-1 |
| ★ **D-R11** | ⚠⚠ **有料の実体を「サーバー配信 ／ 建前 ／ 機能（採点・記録・解説）」のどれにするか**（★ **実測: `stages.json` も `questions.json` も URL 直打ちで全文が返る**）。★ **参考書は無料なので、参考書だけなら静的のままでよい ＝ この決定を待たずに着手できる** | ⚠ **参考書側からは推奨しない**（**問題集の設計で決める話**） | §7-2 |
| ★ **D-R12** | ★ **案③（機能で分ける）を採る場合、参考書の例題に採点は付くか**（★ **こちらの前提は「付く」** ＝ **`stages.json` の `target` 照合は既に無料で公開済みなので、外すと取り上げになる**） | ★ **付ける**（**例題は採点まで無料**） | §7-2a |

---

## 9. ⚠ 統合セッションの申し送りのうち、測ったら違っていたもの

| 申し送り | ★ 実測 |
|---|---|
| 「**`.wide-figure` という仕組みが立体視で使われています**（既に横向き前提の機能があるか）」 | ⚠ **逆だった。** ★ **`.wide-figure` は「図が広いときに2カラムを1カラムへ *戻す*」クラス**（`style.css` 2219）。**横向きを要求する仕組みではない。** ★★ **現時点で「横向きでないと使えない機能」は assembler に1つも無い。** |
| 「**スマホ縦を捨てるかどうか**（分割を横向き前提にするのは妥当だが…）」 | ⚠⚠ **論点が縦横ではなかった。** ★★ **横向き＋分割（デカン 18.4px）は 縦＋分割なし（18.9px）より悪い。** **捨てるべきは「スマホで *分割* すること」で、縦向きそのものではない。** |
| 「**タブは *逃げ道* として成立するか**」 | ★ **逃げ道ではなく、スマホでは唯一の選択肢。** ★ **しかもキャンバスを1pxも削らないので、分割より *強い*。** |
| 「**`tutorials.json` の既存の仕組みで書けるかを測ってください。書けないなら何が足りないかを数で**」 | ⚠ **「足りない」ではなく「向きが逆」だった。** ★ **38ステップの `actions` はすべてアプリが自分で押す型**（`click` 13・`button` 19…）＝ **自動再生のデモ。** ★★ **一方、ユーザーが求めた「作ればクリア」は `stages.json` に *既に10件そろって実装済み* だった**（メタン〜デカン）。 |
| 「**チュートリアルは手順を誘導しているはずなので、操作の制限に近いものがあるかもしれません**」 | ⚠ **誘導 = アプリの自動操作なので、制限の仕組みではない。** ★ **近い前例は別にあった**: **モバイルでのボタン削減（`style.css`「DOM には残す」）。** |

---

## 10. ★★ 第1ページ（アルカンの命名）の実装記録（2026-09-02・refbook-p1 レーン）

**状態: 実装済み。** ★ 3層のうち **説明（表）と 例題（代表1つ）の2層**。⚠ **練習問題（有料）は作っていない**
（配信の設計待ち）。★ **触ったのは `assembler/learn.js` / `index.html` / `style.css` / `tests.js` と
新規 `assembler/reference.json` だけ**（⚠ `game.js` / `chemistry.js` / `reactor.js` / `quiz.js` /
`compounds.json` / `stages.json` は1バイトも触っていない）。

### 10-1. ★★ 「この表を作った理由」（§1-2 の運用ルール・1行）

> ★ **C が1つ増えるたびに名前と分子式がどう動くかは、10行を縦に並べて初めて「規則」の形で見える**
> —— **1行では見えないから表にした。** ★ **行の中身は assembler が既に持っている出題順
> （`stages.json` の系列「アルカン（直鎖）」10件）から機械で組んでおり、どの本の表も見ていない。**

★ **この1行は `reference.json` の `why` に入っていて、空だと `REF5` が赤くなる**
（⚠ **人の記憶と自制に依存しない** ＝ `DESIGN_review_pack1.md` §5-1 の「機械の守り」2段目）。

### 10-2. ★ 決めたこと（判断待ちのうち、このレーンで決めたもの）

| # | 決め | ★ 理由 |
|---|---|---|
| **置き場所** | ★ **新規 `assembler/reference.json`**（**1行1ページ・末尾追記・行数 = 件数 + 2**） | ★ `CLAUDE.md` の作法にそろえる。⚠ **`REF5` が行数と形を見張る**ので、書き戻し事故がここで止まる |
| **スキーマ** | ★★ **行データを持たせない**（`name`・`formula`・`atoms`・`bonds`・`target`・`rows` の欄を**禁止**） | ★★ **手打ちの表が構造上存在できなければ、表の転写事故は起きない**（review §5-1 の「推奨をスキーマの制約に格上げ」をそのまま実装）。⚠ **`REF5` が禁止語を再帰的に探す** |
| **必須の欄** | ★ `why`（20字以上）・`source`（実在する系列）・`codes`（qa の知識コード）・`singleSource` | ★ §1-2 の運用ルールと §1-3 の2社目照合の下ごしらえを、**書けていないと赤くなる**形にした |
| **描画の置き場所** | ★ **`learn.js`**（⚠ `reference.js` を新設しない） | ★ 読む／練習する器（LearnView・書き出し練習3つ）が既にここに集まっている。⚠ ファイルを増やすと `?v=` の対象と `<script>` が1本増える |
| ★ **開く道（`D-R3`）** | ★ **`?open=reference[&code=<知識コード>]`** を足した。⚠ **`OPEN_TARGETS`（game.js）には足していない** | ⚠ **`game.js` は実験モードのレーンが同時に触っている。** ★ `applyOpenParam` は**知らない `open` を無視して普通に開く**（前方互換）ので、**`rec.js` と同じ作法**（自分のパラメータを自分で読み、`appReady` を待つ）で `learn.js` 側に置いた。★ **統合後に `OPEN_TARGETS` へ1件足せば、ハブ（EP6）からも指せるようになる** ＝ **そこだけが残り** |
| **入口** | ★ **📚 学習 → 📖 資料（参考書）**（`#learn-acc-reference`。⚠ **末尾に足す**） | ⚠ **リボンにタイルは足さない**（9枠でちょうど埋まっており、10枠目は 320px で溢れる）。★ 既定で閉じている ＝ **参照に一手かかるのは欠点ではない**（原則2） |
| ★ **「▶ 組んでみる」（`D-R4`）** | ★★ **(b) パズルのお題にする**（`setMode('puzzle')` → `loadStage(idx)` の2手だけ） | ★★ **新しい採点を1行も書いていない。** 正誤は既存ステージの `target` 照合がそのまま受け持つ（`REF6` が「実際に組み上げるとクリアが記録される」まで見る） |
| ⚠ **表に ▶ を並べない** | ⚠⚠ **§5-2 の図（10行それぞれに「▶ 組んでみる」）は採らなかった** | ★★ **表の10行にボタンを付けると、表は「説明」ではなく「10問の一覧」になり、説明と例題の層が混ざる。** ★ ユーザーの言葉は「**説明が表で提示され、例題はそのうちの1つを作ってみましょう**」＝ **例題は代表1つ**。⚠ 10件のステージは今までどおりパズルの系列から無料で解ける ＝ **何も取り上げていない** |
| **戻り道（狭い画面）** | ★ **例題を始めると資料は閉じる。戻るのは 📚 学習 → 📖 資料 の一手** | ★ §5-3 は「小さな帯を出す」と書いていたが、**帯を常設すると画面の要素が1つ増える**（`Q1` の `[data-modes]` の件と同じ轍）。⚠ **原則2 が「一手かかるのは欠点ではない」と言っているので、帯は要らない** |

### 10-3. ⚠⚠ 設計書の実測のうち、測り直したら違っていたもの

| 設計書 | ⚠ このレーンの実測（実機・`http://127.0.0.1:9412/assembler/`） |
|---|---|
| ★ §3-2 実測④「**380px の資料ペインでも デカン 34.7px**／キャンバス 589px」 | ⚠⚠ **`main` の `gap: 15px` が数に入っていない。** ★ **380px のペインを置くと キャンバスは 589 ではなく 558px**（953 − 380 − 15）で、**デカン 32.9px・スクロース 28.4px**。⚠ **床（28px）まで 0.4px しか無い。** ★ **天井を 340px に下げた** ＝ 1280px で キャンバス 618px・**デカン 35.2px・スクロース 30.5px** |
| ⚠⚠ ★ §4-3「**閾値は新設せず `min-width: 1000px` を流用**（立体ビューの2カラム化が既に使っている値）」 | ⚠⚠ **借りた値ではこの機能は床を割る。** ★ **閾値は「借りられる値」ではなく「測って決まる値」だった**（下の表）。★ **`min-width: 1200px` に上げた**（**1199px 以下は重ねて切り替える＝タブ。タブはキャンバスを 1px も削らないので、分割しないことで失うものは無い**） |
| ★ §3-2 実測④ が **デカンだけで幅を決めている** | ⚠ **デカンだけ見ていると 1150px でも通ってしまう**（**デカン 29.3 ○ / スクロース 25.4 ✗**）。★ **閾値を決めているのはスクロース**（在庫で最大級・424×238単位）<br>⚠⚠ **この行はあとで覆した → §12-2。** ★ **スクロースは在庫 1090件中26位で、最大級でも最悪ケースでもなかった**（1200px の実測も 28.4 ではなく **30.6px**）。★ 床を守る範囲そのものを §12-1 で決め直した |
| §3-2 実測①（375×812 縦・資料なし） | ○ **完全に再現**（描画面 361×555・メタン 32.4・ヘキサン 25.9・デカン 18.9） |
| §6-2「系列『アルカン（直鎖）』は10件で `name` と `formula` を持つ」 | ○ **正しい**（methane〜decane・`target` つき） |
| ★ §4-3「タブはキャンバスを 1px も減らさない」 | ○ **実測で確認**（375×812 で資料を出しても `#svg-wrapper` は 361×555 のまま・デカン 18.9px のまま）。⚠ **`REF4` がこれを見張る** |

**★ 実測（資料を開いて、前の分子を消してから1件ずつ呼び出したときの結合 px。床は 28px）:**

| 画面幅 | 資料ペイン | キャンバス | デカン | スクロース | |
|--:|--:|--:|--:|--:|---|
| **1000**（★ 設計書の閾値） | 270 | 388 | ⚠ **22.6** | —— | ⚠⚠ **床割れ** |
| **1100** | 297 | 461 | ⚠ **26.8** | —— | ⚠ 床割れ |
| **1150** | 311 | 498 | 29.3 ○ | ⚠ **25.4** | ⚠ **スクロースが割れる** |
| ★ **1200**（★ 採用した閾値） | 300 | 558 | **32.9** | **28.4** | ○ |
| **1280** | 320 | 618 | **35.2** | **30.5** | ○ |

⚠ **幅は `clamp(280px, 25vw, 340px)`。** ★ **27vw だと閾値ちょうど（1200px）でキャンバス 546px ＝ スクロース 27.9px で割る。**

### 10-4. ⚠ 発注書の指定のうち、現物と衝突したもの

- ⚠⚠ **テストの接頭辞 `RB` は使えない。** ★ **`RB` は既に「リボン統合」が RB1〜RB17 で使っている**
  （`RB14` は `index.html` のコメントからも名指しされている）。★ **`REF1`〜`REF7` にした。**

### 10-5. ★★ 「中身を切らない」の2回目の適用（2026-09-02・ユーザー決定の言い直し）

> ⚠ **画面に入らないことの解決策として「中身を切る」を選ばない。**
> ★ **切り替える・畳む・別ページにする・スクロールと拡大縮小で見せる、は可。**

★ **系統樹の議論（「全体が入った方がよい」）から出た言い直しだが、資料ページにもそのまま効く。**
⚠ **統合セッションはこの誤りを2回しかけている**（**参考書で「1行だけ帯で出す」／系統樹で「例題は部分木」**）
—— ★ **つまり「狭いから減らす」は繰り返し出てくる誘惑で、検査で止めるべき類。**

★ **第1ページはこの原則の側にある**（**切り替え＋スクロール**）が、⚠ **検査がそれを見張っていなかった** ——
`REF4` は当初「10行以上ある」しか見ておらず、**CSS で行を隠す形なら通ってしまった。**

★ **足したもの（`REF4` の 375×812 の枝）:**
- ★ **DOM の行数だけでなく *見えている* 行数**（`getClientRects()`）で数え、**どちらも系列の件数と一致する**こと
- ★ **入らないぶんはスクロールで見せる**（`#ref-body` の `overflow-y` が `auto`）
- ★ **否定対照**: `@media (max-width:899px){ .ref-table tbody tr:nth-child(n+4){display:none} }` を足すと
  **「375×812: 表が 3/10 行しか見えない」で赤くなる**（実際に赤くして確認・そのあと戻して差分ゼロ）

### 10-6. ⚠ 測るときの落とし穴（このレーンで1度踏んだ）

⚠⚠ **`screenPxPerGrid()` の値は *モードと localStorage* で変わる。**
★ **🧩パズルでは `fitCanvasToTarget()` が *お題* に合わせる**ので、
**同じ端末・同じ分子でも 🧪自由のときと違う値が出る**（実測: 375×812 のデカンで **18.9 と 20.6**）。

★ **正しい測り方**: **`localStorage` を消してから開く**（既定は 🧪自由）／
**分子を1件ずつ、前の分子を消してから呼ぶ**／**`getScreenCTM()` 由来の `screenPxPerGrid()` で読む**。
⚠ **一度これを外して「設計書の実測①は3px 悲観的だ」と誤った訂正をしかけた** ——
**測り直したら §3-2 実測① は1件も間違っていなかった**（メタン 32.4・ヘキサン 25.9・デカン 18.9 を完全に再現）。

### 10-7. ★ 残っていること（この先のレーンへ）

1. ★ **`OPEN_TARGETS` に `reference` を1件足す**（`game.js`）。★ **足すとハブから指せるようになり、`EP6` の突き合わせに乗る。**
   ⚠ **足さなくても `?open=reference` は今のまま動く**（`learn.js` が自分で読んでいる）。
2. ★ **索引（玄関）**。★ いまの索引は「📚 学習 → 📖 資料」の1階層で、ページが1枚しかないので単元・小項目の階層をまだ作っていない
   （⚠ **ページが増えてから §2-3 の形にする**。1枚のために3階層を作ると、空の階層だけが並ぶ）。
3. ★ **2枚目以降の資料ページ。** ★ **`reference.json` に1行足すだけで並ぶ**（索引は機械で組む）。
   ⚠ **1枚足すごとに `why` を1行書くこと** —— **書けないならどこかから持ってきている**（§1-2）。
4. ⚠ **`D-R7`（系列をそろえてクリア）は作っていない。** ★ 第1ページは例題1つなので、束の概念がまだ要らない。

---

## 11. ★★ 第2ページ・第3ページの実装記録（2026-09-02・refbook-p2 レーン）

**状態: 実装済み。** ★ 触ったのは `assembler/learn.js` / `game.js`（`OPEN_TARGETS` に1件）/
`style.css` / `tests.js` と `reference.json`（**2行追記**）だけ。
⚠ `stages.json` / `compounds.json` / `chemistry.js` / `reactor.js` は1バイトも触っていない。

### 11-1. ★ 何を選んだか、と選んだ理由

| | 2枚目 | 3枚目 |
|---|---|---|
| **id** | `alkane-isomers` | `functional-groups` |
| **題** | ★ **同じ分子式で、別の分子** | ★ **官能基のかたちと名前** |
| **表の元** | `stages:異性体をつくる（枝分かれアルカン）`（**15件**） | `stages:アルコールとエーテル`（8）＋ `stages:アルデヒドとケトン`（3）＝ **11件** |
| **見せ方** | ★ **分子式でまとめる**（`variant: "grouped"`。rowspan で左1列を共有・**行は1つも減らない**） | ★ **名称／分子式／官能基**（`variant: "functional"`） |
| **機械で作る列** | ★ 分子式（**図から計算**）・主鎖の C の数（`findLongestCarbonChain`） | ★ 分子式（**図から計算**）・官能基（`findFunctionalGroups` → `game.functionalGroupSummary`） |
| **例題** | 2-メチルブタン | 2-プロパノール |

★ **選定の物差し（入試DB `qa/data/exam_usage.jsonl`・有機283大問の悉皆）:**
**「その単元のコードが何回出たか」ではなく、`problems[].printed`（設問の印字番号）を集合にして
*大問の重複を除いた実数*と、*既に書いた分との差し引き*で見た。**

| 候補 | 母数（大問） | ⚠ **1枚目と2枚目を引いた新規** |
|---|--:|--:|
| 1枚目（既存・アルカンの命名） | 78 / 283 | —— |
| ★ **2枚目（異性体・枝分かれ）** | 34 | ★ **+17** |
| ⚠ **不飽和度・一般式**（`unsaturation` ほか・**コード単位では 84 で最多**） | 84 | ⚠⚠ **+4** |
| ★★ **3枚目（官能基）** | 131 | ★★ **+77** |
| 芳香族の o/m/p | 88 | +44 |

⚠⚠ **単純な上位取りをすると「不飽和度・一般式」を選ぶ**（`org.ali.unsaturation` 84・
`formula-alkene` 84・`formula-alkyne` 81 …が有機の最多層）。★ **ところがこの束は
`org.ali.formula-alkane`（＝1枚目）と *同じ大問に一緒に付く*** ——
**1枚目の 78 大問がまるごと入っており、新しく届くのは 4 大問だけだった。**
★★ **＝ 頻度は「重なりを引いてから」見ないと、同じ読者に同じ紙を2回渡すことになる。**

### 11-2. ★ 「この表を作った理由」（§1-2 の運用ルール。`reference.json` の `why` に入っている）

- **2枚目**: 分子式が同じ行が2つ以上並ぶことそのものが「構造異性体」の実物で、
  C4 から C7 までのあいだに枝分かれが 1→2→4→8 と増えていく増え方は、
  **15件を分子式ごとにまとめて縦に見て初めて規則の形になる**。1行だけ出すと
  「2-メチルブタンという分子がある」で終わって規則にならない。
- **3枚目**: 「名前の語尾」と「図の中のかたち」が1対1で対応していることは、
  **11件を1つの表に並べて初めて対応表の形になる**。さらにアルコールと、それを酸化した相手
  （メタノール↔ホルムアルデヒド・エタノール↔アセトアルデヒド・2-プロパノール↔アセトン）を
  同じ表に入れているので、**1級と2級で行き先が変わることが行どうしの対比として見える**。
  ⚠ **qa の単元をまたぐ（`alcohol` と `carbonyl`）のはそのためで、片方だけの表にすると対比が消える。**

### 11-3. ★★ 「行データを持たない」を一段先へ進めた

⚠ **`REF5` は `reference.json` に行データが無いことしか見ていない** ——
★ **`learn.js` に手で書いた表があれば、同じ事故が起きる。**

★ **足したもの（`REF9`）: 画面に出ている値が chemistry.js の計算と一致することを見る。**
**否定対照が2本、検査の中に埋まっている:**

1. ★ **分子式の列が `stages.json` の `formula` の写しでは *ない* こと** ——
   ⚠ **`formula` は示性式のことがある**（メタノールは `CH3OH`・グリセリンは `C3H5(OH)3`・
   1-ブテンは `CH2=CHCH2CH3`）。**図から数えた分子式と食い違う行が2件以上あること**を要求する
   ＝ **示性式をそのまま出しているだけの実装では通らない。**
2. ★ **主鎖の列が「C の数」の言い換えでは *ない* こと** ——
   **2,2-ジメチルプロパンは C5 だが主鎖3**。食い違う行が2件以上あることを要求する。

### 11-4. ⚠⚠ 測ったら違っていたこと —— **床を決めているのはスクロースではなかった**

> ⚠⚠ **この節はさらにもう一段ずれている → §12。** ★ 結論（スクロースではない）は正しいが、
> **ステアリン酸も最悪ケースではなく**（在庫 1090件中8位・最大はナイロン66）、
> **下表の2つの数字が実測と合わない**: スクロース 28.4 → **30.6**／1440 の ステアリン酸 28.3 ○ → **27.7 ✗**。
> ★ **この節は残す**（このレーンが何を見て「REF4 を強めない」と決めたかの記録）。

★ §10-3 は「**閾値を決めているのはスクロース**（在庫で最大級・424×238単位）」と書き、
`REF4` も **デカンとスクロースの2つ**で 1200px を見張っている。
⚠⚠ **在庫にはもっと横に長い分子があり、そちらは 1200px で床（結合 28px）を割る。**

**実測（`http://127.0.0.1:9463/assembler/` ・資料ペインを開いた状態・前の分子を消してから1件ずつ）:**

| 画面幅 | 資料ペイン | キャンバス | デカン | スクロース | ⚠ **マルトース** | ⚠ **パルミチン酸** | ⚠⚠ **ステアリン酸** |
|--:|--:|--:|--:|--:|--:|--:|--:|
| **1200**（★ 採用中の閾値） | 300 | 558 | 32.9 ○ | 28.4 ○ | ⚠ **26.1** | ⚠ **22.3** | ⚠⚠ **20.4** |
| **1440** | 340 | 758 | 45.5 ○ | 42.4 ○ | —— | —— | **28.3** ○ |
| **1200・資料を閉じた状態** | —— | 873 | 51.4 | 47.8 | —— | —— | 31.9 ○ |
| **375×812（資料はタブ）** | —— | 361×555 | 18.9 | 15.9 | —— | —— | **11.7** |

⚠⚠ **＝ ステアリン酸（C18 の直鎖・1列で横に伸びる）は、1200px では資料を開いた瞬間に床を割る。**
★ **床を保つには キャンバス 766px が要り、1200px でそれを作ると資料ペインは 92px** ＝ **成立しない。**
★★ **どのペイン幅を選んでも 1200px では無理で、実測では画面幅 1440px あたりが分かれ目。**

★ **ただし、これは参考書化が持ち込んだ問題ではない**（375px では資料が1pxも無くても 11.7px）——
**`D-R5`（直鎖が横に長い分子は画面に対して大きすぎる）そのもの。**
⚠ **今回 `REF4` を「ステアリン酸でも床を守れ」に強めることはしていない**
（**強めると 1200〜1439px で分割そのものが不成立になり、判断が参考書の範囲を超える**）。
★★ **残すのは事実のほう: 「スクロースが最悪ではない」を、次に閾値を触る人が知っていること。**

### 11-5. ★ 索引の階層化は「まだ」（何枚から要るか）

★ **3枚では作らない**（1枚のために3階層を作ると空の階層だけが並ぶ、の続き）。
★ **見立て: 1階層（単元の見出しでまとめる）が要るのは 8枚あたりから。**
根拠は実測 —— **索引のボタンは2行組みで約 56px**、375×812 の本文が 712px なので
**12枚で画面1つぶんを超える**が、**同じ単元が3枚以上並び始めるのが先に来る**
（いまは 脂肪族2・アルコール1）。⚠ **§2-3 の3階層（単元→小項目→ページ）と検索欄は、
1単元に5枚以上が2単元で起きてから** ＝ **ざっと20枚**。それより早く作ると、
**中身のない `group` の行を押させることになる。**

### 11-6. ★ `OPEN_TARGETS` に `reference` を足した（§10-7 の残り1件）

★ **`reference: { fn: () => window.referenceBook.openFromSearch(location.search) }`。**
⚠ **`mode` を持たせていない** —— 資料は「いまのモードのまま読む」もので、
`mode: 'learn'` にすると 📚 学習が開いて直後に資料側が閉じる ＝ ちらつく（`REF11` が見張る）。
⚠ **開く道が2本（`OPEN_TARGETS` と `learn.js` の `appReady` 待ち）になったので、
ページを決める場所は1本（`openFromSearch`）にまとめ、1回きりの札で二重描画を止めた**
（`REF11` が「表が1枚だけ」で見張る）。
★ **`EP6` は足す前も後も緑**（ハブはまだ `?open=reference` を張っていない ＝ 張れるようになっただけ）。

---

## 12. ★★ 床（結合28px）を守る範囲を決め直した（2026-09-03・refbook-floor レーン）

★ この節は **§11-4（`refbook-p2` レーンの実測表）を引き取って直したもの**。
⚠ **§11-4 の数字は2か所ずれている**（スクロース 28.4 → 実測 30.6／1440 の ステアリン酸 28.3 ○ → 実測 **27.7 ✗**）。
★ **§11-4 は残す**（そのレーンが何を見て判断したかの記録なので）。**新しいのは §12。**

### 12-1. ★★ ユーザー決定（2026-09-03）

| 問い | ★ 決めたこと |
|---|---|
| ★ **床（結合28px）を在庫のどこまでに適用するか** | ★★ **「資料が見せている分子」まで。** ＝ 資料ペインが、**読者がその資料のために触っている分子**を潰していないこと。⚠ **在庫ぜんぶではない** |
| ⚠ **`D-R5`（直鎖が横に長い分子は画面に対して大きすぎる）** | ★★ **折り返し作図は作らない。「折り返しよりもスクロール、拡大縮小でユーザーが対応」。** ＝ §10-5 の「切り替える・畳む・別ページにする・**スクロールと拡大縮小で見せる**、は可」のうち最後の1つを採る |
| ★ **分割の閾値（1200px）** | ★ **動かさない。** 範囲を絞った結果 1200 は余裕のある値になった（下げる余地はある）が、**下げるには 1000〜1199 を測り直すことになり、資料の話を超える**ので触らない |

### 12-2. ⚠⚠ 測ったら違っていたこと —— **最悪ケースは「名前で選ぶ」と必ず外す**

★ **`fitCanvasToMolecule` は viewBox を必ず 4:3 に正規化する**ので、画面上の縮尺は

> **結合の太さ px ＝（その画面で決まる定数）÷「その分子に要る視野の広さ」**

で**厳密に**効く。★ **要る視野は座標データだけで決まる**（余白 240×180・最小 360×270・4:3 へそろえる）
＝ **在庫 1090件を機械で並べられる。** ⚠ 実測と小数第1位まで一致した（下表）。

**★ 在庫（`stages.json` 120 + `compounds.json` 1030 → 名前で畳んで 1090件）の上位:**

| 順 | 分子 | 要る視野 | ⚠ 1200px・資料開 | 1200px・資料閉 |
|--:|---|--:|--:|--:|
| 1 | ⚠⚠ **ナイロン66** | 2046 | ⚠⚠ **9.9** | ⚠⚠ **15.5** |
| 2 | ⚠ ポリエチレンテレフタラート | 1404 | ⚠ **14.5** | ⚠ **22.6** |
| 3 | ⚠ ナイロン6 | 1164 | ⚠ **17.4** | ⚠ **27.3** |
| 4〜7 | ステアリン酸ナトリウム（セッケン）・油脂3種 | 1038 | ⚠ **19.6** | 30.6 |
| 8 | **ステアリン酸** | 996 | ⚠ **20.4** | 31.9 |
| 21 | マルトース | 724 | 28.0 | 43.9 |
| 26 | ⚠ **スクロース** | 664 | **30.6** | 47.8 |
| 39 | デカン | 618 | 32.9 | 51.4 |

- ⚠⚠ **§10-3 の「閾値を決めているのはスクロース（在庫で最大級）」は誤り。** ★ **26位**で、
  1200px でも **30.6px と床を割っていない**（§10-3・§11-4 が書いた 28.4 は測り損ね）。
- ⚠⚠ **§11-4 の「ステアリン酸も 1440px なら 28.3 ○」も届いていない。**
  ★ **実測 1440（ペイン340・キャンバス758）で 27.7 ✗**。★ **ステアリン酸の分かれ目は 1448px**（キャンバス766）。
- ⚠⚠ **そしてステアリン酸も最悪ではない**（8位）。**在庫最大のナイロン66 を床に載せるにはウィンドウ約 2255px**
  ＝ **どの分割の閾値を選んでも届かない。**

**★ 1200px で床を割るのは 20件（1.8%）。中身が2つにきれいに分かれる:**

| | 件数 | 中身 | ★ 何の問題か |
|---|--:|---|---|
| **資料を閉じても割る** | **3** | ナイロン66・PET・ナイロン6 | ★★ **`D-R5` そのもの**（資料は1pxも関係ない） |
| **資料を開いたときだけ割る** | **17** | ⚠ **油脂・セッケン・脂肪酸がまるごと**（ステアリン酸／パルミチン酸／オレイン酸とそのNa塩・メチルエステル・グリセリド）＋ カプサイシン | ★ 資料ペインがキャンバスの36%を取ることの代償 |

★ **この17件を床に載せるにはウィンドウ 1480px**（キャンバス798・実測でステアリン酸ナトリウムがちょうど 28.0）。
⚠ **1440 では足りない。** ★ **採らなかった** —— 閾値を 1480 にすると **1200〜1479px が全部タブ表示に落ちる**
（**1366×768 のノートで分割が消える**）。**1090件のうち17件を守るために、いちばん多い画面の分割を捨てる取引になる。**

### 12-3. ★★ `REF4` を「名前を焼き込まない」形に変えた

⚠ **前は `for (const nm of ['デカン', 'スクロース'])` と書いてあった。**
★★ **これが「スクロースが最大級」という間違いに気づけなかった理由そのもの** ——
**在庫に何が入っても検査は同じ2件しか見ない。**

★ **足した物差し（`tests.js` の REF 帯）:**

| | ★ 何をするか |
|---|---|
| `requiredViewWidth(target)` | ★ `fitCanvasToMolecule` と**同じ式**で「要る視野」を出す純関数。★ **この値の大きい順が「読みにくい順」そのもの** |
| `widestNarrowest(entries)` | ★ 一覧から**いちばん食う1件／食わない1件**を選ぶ |
| `referenceShownStages(W)` | ★ **`reference.json` の `source: "stages:<系列>"`** から系列を引き、その系列の stages を返す ＝ **床を守る範囲**。⚠ **ページを足せば物差しも自動で伸びる**（検査に系列名も件数も書かない） |

- **`REF4`**（1200×800・資料開）… ★ 資料が見せている分子のうち**機械で選んだ最大**が床を保つ。
  ★ さらに **物差しそのものの検算**を入れた —— **視野を食う側が実際に小さく出ていること**。
  ⚠ 逆なら「床を守れている」のではなく **`requiredViewWidth` が壊れている**。
- **`REF4b`**（新設）… ★ **在庫全体**から機械で選んだ最大（いまはナイロン66）で、**2つを固定する**:
  1. ⚠ **床を割るという事実**。⚠ ここが緑になったら**前提が変わっている** ＝ この §12 を測り直すこと。
  2. ★★ **逃げ道が穴を埋めていること** —— **Ctrl+ホイールで拡大すると床を越え、ホイールで横へパンできる**。
     ★ **`D-R5` を「折り返さずスクロールと拡大縮小で読む」と決めた以上、その道が生きていることは
     コメントではなく検査で持つ。** ①だけ認めて②が無いのは、読めない分子を放置しているのと同じ。

★ **`REF1`〜`REF7`＋`REF4b` の8件、ヘッドレスで全合格**（`node tools/run-tests.mjs --only=REF`）。
★ **`refbook-p2` の2枚（異性体・官能基）がマージされても `REF4` の最大はデカンのまま**（36件で確認済み・32.9px）。

### 12-4. ★ 触っていないもの

⚠ **`style.css` の閾値・ペイン幅（`min-width: 1200px` / `clamp(280px, 25vw, 340px)`）は1バイトも変えていない。**
★ 変えたのは **`tests.js`（物差し）と設計書だけ** ＝ **画面の挙動は今までどおり。**

### 12-5. ⚠⚠ **1200px は「いま測って決まっている値」ではない**（次に触る人へ）

★ **1200 は、床を在庫ぜんぶに適用していたころに スクロース で決めた値**（§10-3・§11-4）。
⚠⚠ **その根拠は §12-1 で消えた** —— **スクロースは範囲の外**になり、しかも実測 30.6px で
**そもそも床を割っていなかった**。★ **いま 1200 を支えている分子はデカン（32.9px）で、
床まで 4.9px も余っている** ＝ **測り直せば 1120px あたりまで下げられる見込み。**

★★ **それでも据え置いた**（統合セッションと合意・2026-09-03）:

- ⚠ **下げるには 1000〜1199 を測り直すことになり、範囲が広いわりに得るものが小さい**
  （下げて増えるのは「分割で読める画面」だが、**タブ表示はキャンバスを 1px も削らない**ので失っているものが無い）。
- ★ **余裕があること自体は悪くない。**

⚠⚠ **＝「1200 は測って決めた値だから動かせない」と読まないこと。**
**動かせるが、動かす価値が小さいので動かしていない。** ★ 値が黙って動かないよう、
`REF4` の否定対照（1199px は分割しない）だけを残してある。

---

## 13. ★★ 第4ページ ＝ 反応機構を参考書に内蔵した（2026-09-03・ref-mech レーン）

> ★ **ユーザー指示（原文）: 「反応機構モードは参考書の中に内蔵したほうがいいですね」**

**状態: 実装済み。** ★ 触ったのは `assembler/learn.js` / `style.css` / `tests.js` と
`reference.json`（**1行追記**）だけ。⚠ **`reaction.js` / `reactions.json` / `game.js` /
`index.html` は1バイトも触っていない。**

### 13-1. ★ 第1段の実測（実装の前に数えたもの）

**① いまの機構ビューアの入口（全部）**

| # | 入口 | 実測 |
|---|---|---|
| 1 | 📚 学習 → ⚗️ 反応機構ビューア（`#reaction-box` の中の一覧） | ★ 14件が8つの型に分かれて並ぶ（`reaction.js` `populateList`） |
| 2 | `?open=mechanism` / `?open=mechanism&id=<機構id>` | ★ `game.js` の `OPEN_TARGETS`（受け口④）→ `openById` |
| 3 | ルート `index.html`（化学レンズのハブ）の単元別索引「有機反応・反応機構」 | ★ `?open=mechanism` を1本 張っている |
| 4 | ⚗ 反応させる・調べる（`reactor.js`）の「機構を見る」 | ★ **`mechanismId` を持つ反応ルール 11件**から橋がかかっている |
| 5 | `qa`（一問一答）の飛び道具 | ★ **`qa/data/assembler_links.jsonl` の `kind:"mechanism"` が 10件** |
| 6 | ⚠⚠ **収録台本（`video-scripts/` と `assembler/demos-*.json`）** | ★★ **0本。1本も指していない**（`open=` は `video-scripts/` に1件も無く、`demos-*.json` にも `select-reaction` / `reaction-box` / `mechanism` の文字列が0件） |

⚠⚠ **＝ 発注書が心配していた「収録台本が受け口を使っている可能性が高い」は外れ。0本だった。**
★ **それでも `?open=mechanism&id=` は1バイトも触っていない**（入口2・3・4・5 が生きているので、
壊す理由がそもそも無い）。

**② 14件は既存3ページのどれに対応するか → ★★ 1件も対応しない**

★ **機械で突き合わせた**（`qa/data/assembler_links.jsonl` の `kind:"mechanism"` 10件の
知識コード × `reference.json` の3ページの `codes` 16件）:

| 機構 | qa の知識コード | 参考書の3ページ |
|---|---|---|
| `methane_chlorination` / `ethene_br2` / `ethene_h2o` | `org.ali.alkane-chlorination` / `alkene-br2` / `alkene-h2o` | ⚠ **無し** |
| `ethanol_oxidation` / `esterification` | `org.carbonyl.acetaldehyde-prep` / `ester-water-origin` | ⚠ **無し** |
| 芳香族3件・ジアゾ2件 | `org.aro.*` / `org.aroN.*` | ⚠ **無し** |
| `ethanol_e1` / `ethanol_ether` / `saponification` / `propanol2_oxidation` | ⚠ **qa からも指されていない**（4件） | ⚠ **無し** |

★ **交わりは 0件**（3ページの `codes` は命名・異性体・官能基で、反応の話が1つも無い）。
⚠ **近いのは第3ページだけ**（本文が「1級→アルデヒド・2級→ケトン」を説明している）**が、
それは「行き先」の話で「道すじ」ではない** —— 対応する `codes` も 0。
★★ **＝ 内蔵するには、新しいページが要る。**

### 13-2. ★★ 選んだ形と、落とした案

★ **選んだのは (c) を軸に (b) を足した形 ＝ 1枚のページに14件全部を機械で組んだ表を出し、
各行の ▶ で既存のビューアがその1件を再生する。**

| 案 | 採否 | ★ 理由 |
|---|---|---|
| **(a) 参考書のページの中に再生器を埋め込む** | ⚠ **落とした** | ★★ **`reaction.js` の再生器は「キャンバスを借りる」形でできている**（`borrowCanvas` が人の答案・履歴・視野を退避し、`goto()` が**本体 SVG の `#arrows-group`** に巻矢印を描く）。資料ペインの中で動かすには**2枚目の描画面**が要り、それは「入口を移す」ではなく**作り直し** ＝ 発注書の禁止そのもの |
| **(b) 一覧を出して押すと既存のビューアが開く** | ★ **採った（の一部）** | ★ 軽い。⚠ **ただし「一覧だけ」にすると、参考書に置く意味が薄い** —— 既にある一覧を場所を変えて2つ持つことになる |
| **(c) 静止画の段を並べ、動かしたい人だけビューアへ** | ★ **採った（の一部・ただし静止画ではなく表）** | ⚠ **段の静止画を並べるには `states` を資料側で描くことになり、(a) と同じ「2枚目の描画」に落ちる。** ★ **代わりに「型・名前・1行説明・段数」の表**にした ＝ **どれも `reactions.json` の1件から機械で取れる**（§1-2 の作法どおり、行を手で書く余地がゼロ） |
| ⚠ **反応の型ごとに8ページに割る** | ⚠ **落とした** | ⚠⚠ **1ページ1〜3行の表が8枚並ぶ ＝ 「型の地図」が消える。** ★ 原則1（参照は全体像で渡す）に真正面から反する |
| ⚠ **官能基のページ（3枚目）に混ぜる** | ⚠ **落とした** | ⚠ 14件のうち官能基のページに乗るのは酸化2件だけ。**残り12件の行き場が無い** |

### 13-3. ⚠⚠ 「表の行に ▶ を並べない」（§10-2）と逆にした —— その理由

⚠ **stages の表では「行に ▶ を置かない」と決めてある**（置くと表が「説明」ではなく
「N問の一覧」になり、説明と練習の層が混ざる）。★ **機構の表では逆に、14行 全部に ▶ を置いた。**

| | stages の表 | ★ 機構の表 |
|---|---|---|
| **1行が何か** | ⚠ **お題**（組んで採点される問題） | ★★ **読み物そのもの**（見るためのアニメ） |
| **押すと何が始まるか** | ⚠ **練習**（層が変わる） | ★ **読む続き**（層は変わらない） |
| **層が混ざるか** | ⚠ **混ざる** | ★ **混ざらない**（この資料に練習の層は無い。🎯 予測はビューアの中の別の道） |

★★ **＝ §10-2 の禁止は「行 ＝ 問題」のときの話で、機構には当たらない。**
⚠ **stages 側の禁止はそのまま機械で生きている** —— **表の class を分けた**ので
（`.ref-table` / `.ref-mech-table`）、`REF6`・`REF10` の
「`table.ref-table` の中にボタンが0個」は今までどおり赤くなる。

★ **▶ を14行に置いた実利**: 一覧が「全体像を見せる表」と同一物になる ＝
**表を読んでから、そのまま気になった行を押せる。** ⚠ 例題を代表1つにすると
**残り13件は今までどおり📚 学習の奥の一覧まで戻ることになり、「内蔵した」と言えない。**

### 13-4. ★ 内蔵の中身（何を作って、何を作っていないか）

| | ★ 作ったもの（`learn.js`・**新規3本**） |
|---|---|
| `refMechanisms()` | ★ 登録済みの機構を**全部**返す。⚠ **引数を1つも持たない** ＝ 「この型だけ」を渡す口が構造上ない（`renderStageTable` が行を選べないのと同じ守り）。実体は **`window.reactionPlayer.reactions`**（reaction.js が起動時に読んだ1本）で、**2つ目の fetch も2つ目の並び順も持たない** |
| `ReferenceBook.renderMechanismTable(block)` | ★ `block` から読むのは **`caption` だけ**。列は 反応の型（`series`）／反応の名前と1行説明（`name`・`desc`）／段数（`steps.length`）で、**どれも1件から機械で取る** |
| `ReferenceBook.playMechanism(id)` | ★ **`reactionPlayer.openById(id)` を呼ぶだけ。** ⚠ 巻矢印もステップ送りも予測も1行も書いていない |

⚠ **共通関数の改修は1本だけ**（`renderBlock` に `mechanismTable` の枝を1行）。

★ **入口の合流**: ▶ は `openById` → `#select-reaction` の値 → `change` → `enter()` と
**既存の1本に合流する**（`?open=mechanism&id=` と同じ道）＝ **「いま選ばれているのはどれか」を
持つ場所は1つのまま。** `REF12` が `#select-reaction` の追従を見張る。

### 13-4b. ⚠⚠ 実機で測って作り直した2点（★ どちらも「書いたときは気づけなかった」類）

★ **どちらも、緑のテストを見ているだけでは分からず、実機（1280×800・`http://127.0.0.1:9686/`）で
数を読んで初めて出た。**

| # | ⚠ 症状 | ★ 直し方 |
|---|---|---|
| ① | ⚠⚠ **▶ が画面の外にあった。** 「型 / 反応 / 段数 / ▶」の**4列**だと、実測で **表が 426px**・器（資料ペインの本文）が **271px** ＝ **右へ 155px はみ出す**。★ **▶ の右端は 1379px で器の右端は 1233px** ＝ **横スクロールしないと押せない**（＝ 内蔵した入口が見えない） | ★ **2列にした**（型 ／ 反応）。**段数と ▶ は反応の欄の中**へ入れ、型の列も折り返す（「芳香族置換反応」を nowrap にすると、それだけで器の3分の1を食う）。★ **実測 271px ちょうどに収まり、14個の ▶ が全部 器の中**（はみ出し 0） |
| ② | ⚠⚠ **付加反応が 1件ずつ2か所に割れていた。** ★ **`reactions.json` の並びは型の順ではない**（`ethene_br2` 付加 → `methane_chlorination` 置換 → … → `ethene_h2o` 付加）ので、**第2ページの `grouped` と同じ「続いているあいだ」でまとめると型が分裂する** ＝ **型の地図にならない** | ★ **`reaction.js` の `populateList` と同じまとめ方**（型は最初に出てきた順・その中は `reactions.json` の順）にした ＝ **ビューアの一覧と同じ並び**。★ **`REF12` は検査の中で並べ直さず、画面に出ている2つの一覧（資料の表とビューアの一覧）を突き合わせる** |

★ **実測（1280×800・ペイン 320px・器 271px）**: 表 271px（はみ出し 0）／▶ 14個とも器の中／
型の見出しは **8個**（付加2・置換1・縮合1・芳香族置換3・脱離2・加水分解1・酸化2・ジアゾ2 ＝ 14）。
★ **375×812 でも 14行が 14行とも見えている**（器 343px・表 343px）。

### 13-5. ★ 床（結合28px）—— 機構にも §12-1 の線を当てた（`REF13` 新設）

★ **§12-1 のユーザー決定「床を守る範囲 ＝ 資料が見せている分子まで」を、4枚目にも同じ形で当てた。**
⚠ **名前を焼き込まない**（§12-2 の教訓）—— **`fitToReaction` と同じ式**の
`requiredViewWidthForReaction()` で14件を並べ、**機械でいちばん食う1件を選ぶ**。

| 順 | 機構 | 要る視野 | ★ 1200px・資料ペインを開いたまま |
|--:|---|--:|--:|
| 1 | **ジアゾカップリング** | 650 | ★ **36.1px**（床 28px まで **8.1px の余裕**） |
| 2 | エタノールの分子間脱水／アニリンのジアゾ化 | 525 | —— |
| 14 | メタンの塩素化 | 406 | 57.7px |

★★ **＝ 14件とも、資料を開いたまま床を保つ。** ⚠ **§12-2 の在庫の話（油脂・セッケン17件が
1200px で割る）とは別** —— **機構の `states` は反応が見える大きさで作られていて、
在庫のいちばん横長の分子ほど広くない。**

⚠ **`requiredViewWidth`（分子・余白 240×180）を借りずに別に持った理由**: `fitToReaction` の
余白は **200×160** で式が違う。**借りると「借りた値」の二の舞**（§12-5）になる。

### 13-6. ★★ 否定対照（★ 全部、実際に赤くして確かめた）

| # | 何を壊したか | ★ 何が赤くなったか |
|---|---|---|
| **A** | 表を先頭1件だけにする（`slice(0,1)`） | `REF12`「機構の表が **1行**で、reactions.json の **14件**と合わない」 |
| ★ **A3** | ⚠ **件数は14のまま、並びだけ逆にする** | ★★ `REF12`「**1行目に「エテンへの臭素の付加」が出ていない**」＝ **名前で引いているので、数が合っていても赤くなる** |
| **B** | `block.only` で行を絞る口を生やす | `REF12`「引数（id / ids / series / only / row / limit）で 0 行に絞れてしまう」 |
| **C** | ▶ の `click` の配線を外す | `REF12`「▶「ジアゾカップリング…」を押しても機構ビューアが始まらない」 |
| ★ **D** | ⚠ **`openById(id)` をやめて `enter(0)` にする**（＝ v1466 の「人の代わりに先頭を選ぶ」症状） | ★★ `REF12`「▶「ジアゾカップリング…」を押したのに**再生されているのは「エテンへの臭素の付加」**」 |
| ★ **E2** | 資料ペインを広げる（`clamp(...,60vw,760px)` ＋ `max-width: 760px`） | `REF13`「…再生すると結合 **8.9px**（床は 28px）」＋ `REF4` も同時に赤 |
| ★ **F2** | ⚠ **`requiredViewWidthForReaction` の順位だけを逆にする**（値は正のまま） | ★★ `REF13`「視野を食う「メタンの塩素化」（25）が「ジアゾカップリング」（15）より小さく出ていない（57.7 / 36.1）＝ **選抜の物差しが効いていない**」 |
| ★ **H1** | ⚠ **型の中の並びだけを逆にする**（件数も型の数も変わらない） | ★★ `REF12`「**資料の表とビューアの一覧の並びが違う（型のまとめ方が2通りある）**」＋ 両方の id 列を並べて出す |
| **H2** | 型のまとめを「続いているあいだ」に戻す（＝ §13-4b ② の実発生） | `REF12`「機構の表が **21 行**で、reactions.json の 14 件と合わない」 |
| ★ **G** | 表のブロックを1つも持たないページを `reference.json` に足す | `REF5`「zzz-no-table: 表のブロック（stageTable / mechanismTable）が1つも無い」 |

★ **H1 は「数では気づけないもの」を名指しで拾う対照**（件数14・型8・名前も全部そろっているのに、
並びだけが違う）。⚠ **`REF12` は検査の中で正解の並びを作らない** ——
**画面に出ている2つの一覧を突き合わせる**ので、まとめ方を変えたら**両方**を変えるしかない。

⚠⚠ **E は1回 空振りした（＝ 対照そのものを検算して助かった例）。**
★ `flex: 0 0 clamp(...)` だけを 60vw に広げても **`max-width: 340px` が効いて幅が変わらず、
`REF13` は緑のまま**だった。**2行そろえて初めて赤くなった。**
★★ **＝「壊したつもり」を実測しないと、対照は静かに空振りする**（発注書の警告どおり）。

### 13-7. ⚠ 検査の範囲を1か所ずらした（すき間を作らないための手当て）

⚠ **`REF8` / `REF10` は「全ページ」を回っていて、どのページにも
「stages の系列10件以上の表」と「例題1つ」を要求していた** —— **機構のページはどちらも持たない。**
★ **`refStagePages()`（`stageTable` を持つページだけ）に絞った。**

⚠⚠ **絞ると「表を1枚も持たないページ」がすき間から逃げられる**ので、
★ **`REF5` に「どのページも表のブロック（`stageTable` / `mechanismTable`）を1つ以上持つ」を足した**
（対照 G）。⚠ **種類を増やすときはここに1行足すことになる ＝ 逃げ道は必ず差分に残る。**

### 13-8. ⚠ 触っていないもの / 残っていること

- ⚠ **`reaction.js` / `reactions.json` / `game.js` / `index.html` は1バイトも触っていない。**
  ★ 📚 学習 → ⚗️ の一覧も、`?open=mechanism&id=` も、ハブの1本も、reactor からの11本も、
  qa からの10本も**そのまま**。★ **「入口を移した」のではなく「入口を1本 増やした」** ——
  ⚠ **消すかどうかはユーザーの判断**（消すと `RX24`/`RX25`・`EP` 帯・qa の10本が影響を受ける）。
- ★ **`?open=reference&code=<qa の機構コード>` で4枚目が開く**（`codes` に qa の10コードを入れた）
  ＝ **qa の飛び道具から「機構の全体像」へ着地させる道が、いつでも張れる状態**になった。
  ⚠ **qa 側のデータは1バイトも触っていない**（張るのは qa レーンの仕事）。
- ⚠ **索引の階層化はまだ**（§11-5 の「8枚あたりから」は変えていない。いま4枚）。
- ⚠ **`ethanol_e1` / `ethanol_ether` / `saponification` / `propanol2_oxidation` の4件は
  qa から1本も指されていない**（この表には出ている）。★ qa 側の宿題として置いておく。


## 14. ★★ 学習メニューの ⚗️ の札を消した（入口の1本化・2026-09-03／04・ref-mech レーン）

> ★ **ユーザー決定（2026-09-03）: 「消す方がすっきりすると思います」**

★ §13 が残した「学習メニューの ⚗️ ビューアを残すか消すか」の答え。**消した。**
⚠ **消したのはメニューの札だけ**で、**機能は1つも失っていない**（14件の一覧は §13 の表に在る）。

### 14-1. ★ 消したもの／残したもの（実測で数えた）

| | 中身 | 行き先 |
|---|---|---|
| ⚠ **消した** | `<details id="reaction-box">` ごと（`summary`「⚗️ 反応機構ビューア」／案内 `#rx-viewer-note`／スイッチ `#check-reaction-mode`／一覧 `#reaction-list`）＋ 群の見出し「登録された反応を見る」 | ★ **一覧は 📖 資料の4枚目の表**（§13）／**案内の3語は資料の本文へ**（下） |
| ★ **残した（動かさない）** | **`?open=mechanism` / `&id=` の受け口** | ⚠ **qa の飛び道具10件・ハブの1本が使用中。URL は1文字も変えていない** |
| ★ **残した（触っていない）** | **`reactor.js` の「機構を見る」**（`mechanismId` を持つルール11本） | ⚠ `reactor.js` は1バイトも触っていない（別レーンが同じファイルを触っている） |
| ★ **残した（触っていない）** | **再生器そのもの**（巻矢印・ステップ送り・🎯 予測） | ★ `reaction.js` の変更は**ガード4か所と但し書き1つだけ** |
| ★ **移した** | **`#select-reaction`（選択の実体）** | ★ **帯 `#ws-reaction` の中へ。** ⚠ 消すと受け口と `RX24` が死ぬ |

### 14-2. ⚠⚠ 「メニューからしか行けなかったもの」は1つだけ在った

★ **発注どおり、消す前に測った。** ⚠ **機能としては1つも無かった**が、
**文としては1つ在った** —— `#rx-viewer-note`（行き先の案内）:

> 登録済みの反応を、**代表例の分子**で再生します。いま描いている分子を反応させるのは、
> キャンバスの「**⚗ 反応させる・調べる**」です。…**🎯 予測** もできます。

★ **これは `EP8` が3語（`⚗ 反応させる・調べる` / `代表例` / `🎯 予測`）で見張っていた資産**で、
**消すと「代表例と自分の分子の違い」を言う場所がアプリから消える。**
★ **受け皿を資料の4枚目の本文に作った**（`reference.json` の text ブロック2本）。
⚠ **`EP8` は消さずに、読む先を `#rx-viewer-note` から資料の本文へ付け替えた** ——
**守っているもの（2か所が同じ語で同じものを指す）は1文字も変えていない。**

### 14-3. ★ つなぎ替え（`?open=mechanism` の行き先）

⚠ **`OPEN_TARGETS.mechanism` は `{ mode:'learn', acc:'reaction-box' }` だった** ——
**箱が消えれば、`?open=mechanism` は「📚 が開くだけで何も起きない」着地になる。**
★ **行き先を資料の4枚目へ向け直した:**

| URL | 着地 |
|---|---|
| `?open=mechanism` | ★ **機構の表**（14件の全体像）。⚠ **何も再生しない**（`RX49` ③ が見張る） |
| `?open=mechanism&id=<機構id>` | ★ **表を開いたうえで、その1件を再生**。⚠ 狭い画面では資料が自分でどかる |

- ⚠ **`OPEN_TARGETS.mechanism` は空 `{}` にした。** `id` の有無で行き先が分かれるので、
  **受け口④の1か所でまとめて決める** —— `fn` を置くと**非同期の描画と再生が競って
  スマホで資料が開き直る**（設計中に踏んだ）。
- ⚠ **`reactionPlayer.openById` を直に呼ばず `referenceBook.playMechanism` を通す** ——
  **狭い画面では資料がキャンバスを覆う**ので、**再生を始めた側が自分でどかす**必要がある。
  ★ **資料が読めない場面では `openById` へ落ちる**（受け口を死なせない）。
- ★ **`playMechanism` は `openById` ではなく `pick()` を通すように変えた**（§13 からの変更）——
  `pick()` は「押しものから呼ばれる唯一の口」で `#select-reaction` の値を決めて `change` を撃つ。
  ⚠ **資料の ▶ は押しもの**なので、こちらが筋。**`RX24`（change を捨てない）が
  人の使う経路を見張り続けられる**のもこの形だから。

### 14-4. ★★ ハブ（ルート `index.html`）は **触らないと決めた**

⚠ 発注は「参考書の4枚目（`?open=reference&…`）に向け直すほうが素直かもしれない」だった。
★ **測ったうえで、向け直さない。理由は4つ:**

1. ★★ **URL を残すと決めた以上、ハブは直す必要がない** —— `?open=mechanism` は生きていて、
   着地が資料の表に変わっただけ（`EP5` / `ID5` / `REF14` が実測で確かめている）。
2. ⚠⚠ **`?open=reference&code=…` に向け直すと、ハブが assembler の内部
   （どのページ id か・どの知識コードか）を知ることになる。**
   ★ **`?open=mechanism` は「意図」を送る名前で、着地点を決めるのは assembler 側** ——
   これは `CLAUDE.md` の「**ratio が知ってよいのは URL の形だけ／どこへ着地させるかは ion が決める**」
   と同じ形。⚠ **複製すると、こちらが収録先を変えたとき黙って壊れる。**
3. ⚠ **ハブは自己完結・`?v=` の対象外**（`verify-release.js` のキャッシュ検査の外）。
   **直すと、検査の届かない1ファイルだけが単独でずれる。**
4. ★ **札「— 反応機構ビューア」は行き先の説明として今も正しい**
   （14件の一覧が出て、その場で再生できる面に着く）。

### 14-5. ⚠⚠ 波及先は発注書の3つより広かった（実測 19件）

★ **発注書が挙げたのは3つ**（`RX24`/`RX25`・`EP` 帯・qa の10本）。
⚠ **`grep` で数えたら、`#reaction-box` / `#reaction-list` / `#check-reaction-mode` /
`#rx-viewer-note` を触っている検査は 19件**あった:

| 帯 | 何をしていたか | ★ どう付け替えたか |
|---|---|---|
| `RX24` | スイッチの追従・チェックを外す出口 | ★ **`#select-reaction` の追従**・**帯の「やめる」** |
| `RX25` / `RX26` | 同上（出口がスイッチ） | ★ 帯の「やめる」。`RX26 (2)` は「状態が2つに割れない」→ **「`#select-reaction` が残っている」** |
| ★ `RX46` | 「一覧は押して確定する／14件に1手で届く」 | ★ **器が資料の表に変わっただけ**。3つの性質はそのまま、届かない件は**名前で**出す |
| ★ `RX47` | 「1件目を見たあとメニューへ戻れる」 | ★ **「PC は開いたまま／狭い画面は覆うので閉じる」**を `position` で判定（幅を2か所で数えない） |
| `RX48` | 一覧の押しもの・スイッチ・印 | ★ 資料の ▶・`#select-reaction`・**表の行の `aria-current`** |
| ★★ `RX49` | 「**スイッチ**は反応を選ばない」 | ★★ **守る対象を「入口を開いただけで先頭を選ばないこと」へ移した** —— 資料の表を開いただけ／`?open=mechanism`（id なし）の**両方**で見る |
| `LX6` (2) | Study の中のスイッチで止める | ★ Study の中に止める押しものが無いので `exit()` で止め、**画面を1回押して `setupLearnExit` を実際に走らせる** |
| `Q1` | 3つめのアコーディオン | ★ 📖 資料（`#reference-list`） |
| `EP5` / `ID5` / `ID6` | 「箱が開く」 | ★ **「機構の一覧に着く」**（行き先が生きている・勝手に始めない、の2つは不変） |
| ★ `EP7` | `<details>` 5つ・見出し3つ・子8つを**数で**書いていた | ★ **何を守る検査かを本文に書き、期待値は一覧から機械で出す形にした**（数字を2か所に書かない）。⚠ **「`#reaction-box` が復活していない」も1行足した** |
| ★ `EP8` | `#rx-viewer-note` の3語 | ★ **資料の本文の3語**（守るものは同じ） |
| `SF6`（`UX_SCREENS`） | 「学習: 反応機構ビューア」画面（上限115字） | ⚠ **画面ごと外した。★ 資料のページはここに足さない**（下） |
| `RB5` / `RB6` | 一覧の札・3つめの id | ★ 資料の ▶・`learn-acc-reference` |
| `REF12` | **ビューアの一覧と並びを突き合わせ**ていた | ★ 相手が消えたので、**`reactions.json` から同じ規則で組み直して照合**（実装の写しではなく**仕様**） |

⚠ **`SF6` に資料のページを足さないこと。** ★ この帯が数えているのは**操作の案内**で、
資料は**読み物そのもの**。⚠ **字数の上限をかけると「中身を切る」誘惑に直結し、
§10-5 のユーザー決定（画面に入らないことの解決策として *中身を切る* を選ばない）と正面から衝突する。**

### 14-6. ★★ 否定対照は「残った道が生きている」側に置いた（`REF14` 新設）

⚠ **「メニューが消えた」を数える検査は置いていない**（`EP7` の1行で足りる）。
★ **`REF14` が見るのは、残した2本が本当に使えること:**

| | 見るもの |
|---|---|
| ① **📖 資料の4枚目の ▶** | 押した1件が**名前どおり**再生される・帯と `#arrows-group` がそろう |
| ② **`?open=mechanism&id=`** | 同じ id で**同じ機構**・`#select-reaction` が追従・**全体像（14件の表）も一緒に着く**・いま見ている1件に印 |

⚠ **「何かが再生された」では、①と②が別々の機構を開いていても緑になる** ＝ **名前で引く。**
★ **見る相手はデータから機械で選ぶ**（先頭・最後・段数が最多）＝ 名前を検査に焼き込まない。

**★ 実際に赤くして確かめた否定対照（この節ぶん）:**

| # | 壊したもの | ★ 赤くなった内容 |
|---|---|---|
| **I** | `playMechanism` が `pick(0)` を呼ぶ（＝資料の ▶ が先頭を開く） | `REF14`「資料の ▶ で**ジアゾカップリング**を押したのに**エテンへの臭素の付加**が再生されている」 |
| **J** | 受け口④が `id` を落とす | `REF14`「`?open=mechanism&id=ethene_br2` で**エテンへの臭素の付加**が再生されない（qa の飛び道具10件とハブの1本がこの URL を指している）」＋ `ID5` も同時に赤 |

### 14-7. ★ qa の10本は壊れていない（実測）

★ **`qa/data/assembler_links.jsonl` の `kind:"mechanism"` 10件**を機械で突き合わせた ——
**10件とも `reactions.json` に在り、URL の形も変えていない。**
★ **`qa/test.html` 全走 94/94 合格**（⚠ `qa/` は1バイトも触っていない）。
★ **実機で `?open=mechanism&id=benzene_nitration`（qa の `org.aro.nitration` が指している形）を開き、
1280×800 で「左でニトロ化のアニメ・右で14件の型の地図」になることを確認した。**

★ **むしろ良くなっている** —— 以前の着地は「⚗️ の箱が開いてその1件が再生される」だったので、
**その1件しか見えなかった。** いまは**全体像が一緒に着く**（原則1「参照は全体像で渡す」）。

### 14-8. ⚠ 触ったファイルと、残した宿題

- ★ `assembler/index.html`（札の撤去・`#select-reaction` を帯へ）／`assembler/learn.js`
  （`openMechanisms` / `syncMechanismTable` 新設・`playMechanism` を `pick()` 経由へ）／
  `assembler/reference.json`（案内2本を本文へ・**1行の中だけ**）／`assembler/tests.js`。
- ⚠ **`assembler/game.js`（最小限）**: `OPEN_TARGETS.mechanism` を空にし、受け口④で資料を呼ぶ。
- ⚠ **`assembler/reaction.js`（最小限）**: `checkMode` を4か所ガード、`#study-body` の捕捉から
  「`#reaction-box` を除く」の但し書きを外した。**再生の中身は1行も触っていない。**
- ⚠⚠ **`reaction.js` の一覧まわり（`populateList` / `syncList` / `promptPick` / `clearPickPrompt` /
  スイッチの `change` 配線）は、いま `listEl` / `checkMode` が `null` なので不活性**（約100行）。
  ★ **消していない** —— どれも素通りするので害が無く、消すと v1439（押しもの化）・v1466（促し）の
  **記録まで消える。** ⚠ **掃除するなら別コミットで**（このコミットは「入口を消す」だけにした）。
- ⚠ **ハブ（ルート `index.html`）は不変**（§14-4）。**`reactor.js` / `qa/` も不変。**

---

## 15. ★★ 第5ページ ＝ アルコールと脱水生成物の対応表（2026-09-06・dehyd-map レーン）

> ★ **ユーザー原文: 「C5H12Oのアルコールの脱水生成物との対応表（反応系統樹）に
> 2,2-ジメチル-1-プロパノール は載せたいです（俯瞰できるようにしたい）」**

**状態: 実装済み。** ★ 触ったのは `assembler/learn.js` / `style.css` / `tests.js` と
`reference.json`（**1行追記**）だけ。⚠ **`chemistry.js` / `compounds.json` / `reactor.js` /
`game.js` / `index.html` / `stages.json` は1バイトも触っていない。版番号も触っていない。**

### 15-1. ★★ 指摘の中身 —— **「出題から外す」と「表から外す」は別**

⚠ `DESIGN_isomer_practice.md` §22-3 で、書き出し練習の在庫（`IP_COND_PRESETS` の型3）から
**メタノール**と **2,2-ジメチル-1-プロパノール**の2件を落とした。★ **理由は「答えが0個だと
器が組めない」**（`beginSession` が満点・「あと何種」・立体の段2の総数・クリア記録を
**答え1件以上**の前提で組む）。⚠⚠ **脱水できないことが教材として無価値だから落としたのではない。**

★★ **表は器の制約を受けない** ＝ **16件ぜんぶ載る**（脱水できる14件＋できない2件）。
⚠ **これは §22-3 の判断の訂正ではなく、適用範囲の切り分け**（出題の器と、俯瞰の表は別物）。

### 15-2. ★★ どこに載せたか、と落とした案

| 案 | 採否 | 理由 |
|---|---|---|
| ★★ **参考書の5枚目**（新しい資料ページ） | ★ **採用** | 「俯瞰できるようにしたい」＝ **読み物**の求め。参考書は既に「表が本体・行は機械で組む・1枚ごとに理由を1行」の作法と物差し（`REF3`/`REF5`/`REF9`）を持っており、**新しい器を作らずに済む** |
| ⚠ 書き出し練習の note に文章で足す | ✗ | **1回ぶんの断り文は俯瞰にならない**（16件を縦に見て初めて規則になる）。しかも「脱水できない回」自体が開かないので、2件は**どの回にも現れない** |
| ⚠ 既存の3枚目（官能基）に列を足す | ✗ | あちらの元は `stages.json` の2系列（11件）で、**C₅ のアルコール8件のうち stages に居るのは一部だけ** ＝ 行がそろわない。表の元が2つになる |
| ⚠ 出題の器を「答え0個」に対応させて練習で見せる | ✗ | §22-3 の見積りどおり**共通関数5本の改修**。しかも「俯瞰」の求めには答えていない（1回ぶんしか見えない） |
| ⚠ `reactions.json` に脱水を1件足して4枚目（機構）で見せる | ✗ | 機構の表は**1行＝1つの反応の道すじ**。⚠ アルコール16件の**対応**はそこには乗らない（型は1つ） |

### 15-3. ★★ 範囲は **C₁〜C₅**（⚠ 発注書の読みと違った点）

⚠ ユーザー原文は「**C5H12O のアルコールの**」なので、**C₅ だけ**にも読める。
★ **C₁〜C₅ にした理由は行数でも画面でもなく、中身:**

- ⚠⚠ **脱水できない2件は C₁ と C₅ に1つずつ居る**（メタノール／2,2-ジメチル-1-プロパノール）。
- ★ **C₅ だけにするとメタノールが消え、例外が「1件だけの変わり者」に見える。**
  2件そろって初めて **「β炭素が無い」「β炭素に水素が無い」の2通りの理由**として並ぶ
  ＝ ⚠ **「アルコールなら脱水できる」ではなく「隣の炭素に水素があるか」で決まる**、が言える。
- ★ **元の依頼（2026-09-03）も「C5まで、アルコールとアルケンの対応表」**で、C₅ 単独ではなかった。

★ **`REF15` が「理由が2種類ある」を見張る**ので、あとで C₅ だけに縮めると赤くなる。

### 15-4. ★ 行数と画面の実測（1200×800 と 375×812・実アプリ `http://127.0.0.1:8391/assembler/`）

| 画面 | 資料ペイン | 表の器 | **表の幅** | 列の内訳 | 横スクロール | **見えている行** |
|--:|--:|--:|--:|---|--:|--:|
| **1200×800**（分割） | 300 | 251 | **251** | 61 ＋ 91 ＋ 98 | **0px（要らない）** | **16 / 16** |
| **375×812**（タブ・`position: fixed`） | 375 | 343 | **343** | 61 ＋ 123 ＋ 159 | **0px（要らない）** | **16 / 16** |

- ★ **16行は既存の2枚目（15行）と同じ桁**で、床にも画面にも新しい問題を持ち込まない。
- ⚠ **3列で足りた**（4枚目が2列にせざるを得なかったのは ▶ を器の外へ出さないため。
  ここは**押しものが1つも無い**ので、名前が折り返せば入る）。
  ★ そのために `.ref-map-from` / `.ref-map-to` を **`white-space: normal`** にしてある
  （「2,2-ジメチル-1-プロパノール」を nowrap にすると、それだけで 14em を食う）。
- ★ 本文込みの縦は 1200×800 で **2432px / 器 663px** ＝ 縦スクロールで読む（`REF16` が
  `overflow-y: auto` と**行を1つも間引かないこと**を見張る）。

### 15-5. ★ 「この表を作った理由」（§1-2 の運用ルール。`reference.json` の `why` に入っている）

**16件を縦に並べて初めて、3つが同時に見える:**

1. ★ **行き先が重なる** —— 1-プロパノールと2-プロパノールはどちらもプロペン、
   2-メチル-2-ブタノールと3-メチル-2-ブタノールはどちらも 2-メチル-2-ブテン。
   ⚠ **アルケンからアルコールを言い当てることは、いつでもできるとは限らない**（＝ 逆向きの回の下地）。
2. ★ **2つできる行が4件**（ザイツェフ則が効く回。`REF15` が「4件以上」で見張る）。
3. ★★ **脱水できない行が2件** —— ⚠ **できる行だけを並べた表からは絶対に見えない。**

⚠ **どの本の表も見ていない**: 行は書き出し練習と**まったく同じ2本**
（`enumerateConstitutionalIsomers` の返り順そのまま ＋ `ipDehydrationEdges`）、
名前は `iupacName`。★ **並べ替えもしていない** ＝ **表を読んでから練習に行くと、答えの並びが同じ。**

### 15-6. ★ 実装（新規関数 **2本**・共通関数の改修 **1か所**）

| 足したもの | 場所 |
|---|---|
| `REF_DEHYD_MAX_CARBONS`（＝5）と `refIsMonoAlcohol` | learn.js |
| ★ `refDehydrationMap()`（**引数を1つも持たない**） | learn.js |
| ★ `ReferenceBook.renderDehydrationTable(block)`（`block` から読むのは `caption` だけ） | learn.js |
| `renderBlock` に `dehydrationTable` の1行 | learn.js（**唯一の改修**） |
| `.ref-map-table` ほか | style.css |
| `alcohol-dehydration` ページ（**末尾に1行追記**・`git diff --numstat` は `2 1`） | reference.json |

- ⚠ **炭素数を `reference.json` から渡させない**（`refMechanisms` が引数を持たないのと同じ守り）
  ＝ **「C₅ の行だけ」の表を作る口が構造上ない**（`REF16` の否定対照 F が実測で赤くなる）。
- ⚠ **class は `.ref-map-table`**（`.ref-table` にしない）——
  `REF8`/`REF9`/`REF10` は `table.ref-table` を「**`stages.json` の系列から作った表**」として
  見張っており（行数＝系列の件数・名称が `STAGES` に在る）、この表の元は列挙器なので
  混ぜると物差しが噛み合わない。★ 4枚目が `.ref-mech-table` を分けたのと同じ理由。
- ★ **`blocked`（脱水できない理由）は2つ目の脱水規則ではない** ——
  `ipDehydrationEdges` が**0本を返した行にだけ**、その関数が見ているのと同じ2つの材料
  （β炭素の数・その水素の数）を読み直して**なぜ0本か**を言う。
  ⚠ `REF15` が「`blocked` の印が付く行と、辺が0本の行が**1件もずれない**」を毎回 確かめる。

### 15-7. ⚠ 床（§12-1）は **`referenceShownStages` が届かない** ので、別に塞いだ

⚠⚠ **`REF4` の物差しは `reference.json` の `source: "stages:<系列>"` から系列を引く**が、
この表が見せているのは **stages でも compounds でもなく列挙器が組んだ分子**なので、
**ページを足しても物差しが1件も伸びない**（＝ すき間ができる）。

★ **`REF16` の②で塞いだ:**
- 表が見せる分子（C₁〜C₅ のアルコールとアルケン **45件**）を**その場で列挙**し、
  `requiredViewWidth` で**機械に最悪ケースを選ばせる**（⚠ 名前を検査に書かない）。
- ⚠ **`summonMolecule` は使えない**（列挙器が組んだ分子はライブラリに居ないものがある）。
  **分子そのものをキャンバスへ置いて実際の縮尺を測る。**
- ★ 物差しの向きの検算（視野を食う側が実際に小さく出ている）も `REF4` と同じ形で入れた。

★ **実測では 1200×800・資料を開いたままで全件が床（28px）を保つ**（C₅ までの小さい分子なので
デカン 32.9px より余裕がある）。⚠ **§12 の 1200px の判断は1文字も動いていない。**

### 15-8. ★★ 否定対照（★ 全部、実際に壊して赤くなるところまで確かめた）

| # | 壊したこと | ★ 赤くなった検査とメッセージ |
|---|---|---|
| **A** | ⚠⚠ **脱水できない2件を表から落とす**（出題から外したものを表からも外す） | `REF15`「表が **14 行**で、列挙器が返す C₁〜C5 のアルコール **16 件**と合わない」 |
| **B** | 「脱水できない」の文字を空欄にする | `REF15`「メタノール の行に「脱水できない」と書いていない」 |
| **C** | 2件の理由を同じ1文にする | `REF15`「脱水できない 2 件の理由が **1 種類**しかない」 |
| **D** | 「（主）」を1つしかできない行にも付ける | `REF15`「エタノール: できるアルケンが1つなのに「（主）」が付いている」 |
| **E** | ⚠ **範囲を C₅ だけにする**（メタノールが消える） | `REF15`「表が **8 行**で、…**16 件**と合わない」 |
| **F** | ⚠ **`block` から行を絞れる口を生やす**（原則1） | `REF16`「行を指す引数（carbons / only / row / limit / blocked）で表が **1 行**に絞れてしまう」 |
| **G** | 表の行に ▶ ボタンを置く | `REF16`「対応表の行にボタンが付いている（表は「説明」であって「N問の一覧」ではない）」 |
| **H** | ⚠ **主生成物の印を副生成物の側に付ける**（ザイツェフ則が逆になる） | `REF15`「2-ブタノール: 主生成物 2-ブテン に「（主）」が付いていない（画面: 1-ブテン（主）／2-ブテン）」 |
| **I** | `REF5` の `TABLE_KINDS` から `dehydrationTable` を外す | `REF5`「alcohol-dehydration: 表のブロックが1つも無い」＝ **すき間に逃げられないことの確認** |
| **J** | 表の横スクロールを止める（入らない列を切る） | `REF16`「1200×800: 対応表が横にスクロールできない」＋ `REF8` も同時に赤（既存の守りが生きている） |

### 15-9. ⚠ 触っていないもの / 残していること

- ⚠ **`chemistry.js` / `compounds.json` / `reactor.js` / `game.js` / `index.html` / `stages.json`**
  は1バイトも触っていない。★ **版番号も触っていない**（統合側で上げる）。
- ★ **この5枚目には例題（`ref-try`）を付けていない。** ⚠ 例題は `stages.json` の1件へ渡す仕組みで、
  この表が見せている16件のうち stages に居るのは一部だけ ＝ **代表を選ぶと「stages に在るから」で
  選ぶことになる**（§1-2 の「表の *選び方*」に近づく）。
  ★ **読む→試すのつなぎは、書き出し練習（形式2の14回）が既に持っている** ——
  ⚠ **表から練習へ飛ぶ導線は作っていない**（`OPEN_TARGETS` を増やすことになるため。次のレーンへ）。
- ★ **索引は5枚**（§11-5 の見立て「1階層が要るのは8枚あたりから」には、まだ届かない）。

---

## 16. ★★ 本文の正を Markdown に移した（2026-09-08・ref-md レーン）

**発端はユーザーの申し立て（原文）:**

> **json だとこちらで校正ができないので、校正手順を考えてください**

★ **実測**: 移行前の `assembler/reference.json` は **5ページ＝5行**で、**1行が 1,160〜2,048 文字**。
⚠ **「てにをは」を1つ直すと、その 2,048 字の行が丸ごと差分になる。**
★★ **ユーザーはこれから 52 グループぶんの本文を自分で書く**（`PAGES_52.md`）ので、
**この形のままでは着手できない。**

### 16-1. ★★ 決めた形 —— 正は Markdown・JSON は生成物

```
reference-src/<id>.md      ← ★★ 正（1ページ＝1ファイル）。人が読み書きするのはここだけ
reference-src/ORDER.txt    ← ★ 索引に出る順（id を1行ずつ）
tools/reference-md.js      ← 書式の parser / serializer（node とブラウザの両方から読む1本）
tools/gen-reference.mjs    ← 生成器（`--check` で「最新か」だけ見る）
assembler/reference.json   ← ⚠⚠ 生成物。手で編集しない（`REF17` が赤くする）
```

⚠ **`reference/` ではなく `reference-src/` にした**（★ 発注書との相違点その1）。
**理由は Jekyll**: `_config.yml` は `exclude: ["*.md"]` で**ルートの .md だけ**を外しており、
**下の階層の .md は配信される**。しかも **`---` の前書きを持つ .md は Jekyll が *ページ* とみなして
`<パス>.html` に変換して出す**。
★★ **`/reference/` は R-1 で「面A（公開ページ）の URL」に決まっている**ので、
そこに `alkane-naming.md` を置くと **Jekyll が `/reference/alkane-naming.html` を勝手に作り、
次のレーンが同じ道に置く面Aと衝突する**（⚠ どちらが勝つかはビルド任せ ＝ 黙って壊れる型）。
★ **`_config.yml` の `exclude:` に `reference-src/` を足した**（原稿は配信しない。面Aは `/reference/` に置ける）。

### 16-2. ★ 書式（決めごとは3つだけ）

#### (1) 前書き ＝ ファイル冒頭の `---` 囲み

```
---
id: alkane-naming
unit: aliphatic
unitLabel: 脂肪族炭化水素
group: アルカン
title: アルカンの命名
codes:
  - org.ali.alkane-names
  - org.ali.formula-alkane
source:
  - stages:アルカン（直鎖）
singleSource: false
why: C が1つ増えるたびに名前と分子式がどう動くかは、…
---
```

★ **文法は3行しかない**（⚠ YAML の一般形ではない。**この3つ以外は書けない**）:

| 書き方 | 意味 |
|---|---|
| `key: 値` | 文字列（前後の空白は落とす）。★ **最初の `:` だけで切る**ので、値の中に `:` があってよい（`stages:アルカン（直鎖）`） |
| `key: true` / `key: false` | 真偽値（★ **`singleSource` だけ**） |
| `key:` ＋ 次行から `  - 値` | 配列（★ **`codes` と `source` だけ**。1件でも `- ` で書く） |

⚠ **知らないキー・足りないキーはその場で赤**（生成器が止まる）。
★ **JSON のキーの並びは生成器が持っている**ので、前書きに書く順は自由（読みやすい順でよい）。
★ `why` は **開発の内側の欄**（§1-3。画面には出ない）だが、**空か 20 字未満だと `REF5` が赤**。

#### (2) 本文 ＝ ふつうの段落。★★ **1段落 = 1行**・空行で区切る

★ **`kind:"text"` の1つが1行に対応する。** ⚠ **段落の途中で改行しない**
（**改行の入れ方で本文が変わってしまう書式にすると、校正のたびに「意味の無い差分」が出る**）。
★ 1行は 100〜350 字ほど ＝ **エディタの折り返しで読め、差分は段落単位で出る。**

| 書く | 出る |
|---|---|
| `**…**` | `<b>…</b>` |
| `~…~` | `<sub>…</sub>`（⚠ **一般式の `C~n~H~2n+2~` のためだけ**） |
| `C₄H₁₀`・`H₂O` | ★ **そのまま**（下付きの Unicode は素で書く。移行前もそう書いていた） |

⚠⚠ **他の生 HTML は書けない。** ★ **移行前の全 147 文字列を数え上げた結果、
使われていたタグは `<b>` 58組 と `<sub>` 2組の *2種類だけ*** で、
**`&` も `"` も裸の `<` `>` も0件**だった（★ だから禁止しても何も失われない）。
★ 生成器は変換のあとに **`*` `~` `<` `>` `&` が1文字でも残っていたら赤**にする
＝ **`**` の閉じ忘れも、うっかりの生 HTML も、その場で止まる。**

⚠ **エスケープの用意はしていない。** 本文に `*` や `~` そのものを出したくなったら、
**このファイルに書き足してから**書式を足すこと（**波ダッシュ `〜`（U+301C）と全角 `＊` は ASCII ではないので今までどおり使える** ——
実際、既存の本文の `C1〜C10` は全部こちら）。

#### (3) 機械が作る表・例題 ＝ `:::` の囲み

```
:::stageTable
variant: functional
series:
  - アルコールとエーテル
  - アルデヒドとケトン
source: stages:アルコールとエーテル + stages:アルデヒドとケトン
caption: アルコール・エーテルとカルボニル化合物
:::
```

★★ **囲みの中は「前書きとまったく同じ `キー: 値`」**（⚠ **parser は1本**）。
★ 発注書は「囲みの中の本文が `caption` / `lead`、2つ目は `> ` の引用行」を提案していたが、**採らなかった**
（★ 発注書との相違点その2）:

- ⚠ **位置で意味が決まる書式は、`example` の `lead` と `note` のように *2つとも散文* のときに
  「どっちが上だっけ」を必ず起こす。** ★ `lead:` `note:` と名前が書いてあれば読んで分かる。
- ⚠ **`series` は文字列のことも配列のこともある**（3ページ目は2系列）。
  ★ 前書きと同じ文法なら、配列は `  - ` で書けて**新しい記法が1つも要らない。**
- ★ **覚えることが「`キー: 値`」1つで済む** ＝ 52 グループを書く人の負担がいちばん小さい。

★ **囲みの種類（`kind`）は、`learn.js` が実際に描ける4つだけ**:

| kind | 書けるキー（★ = 必須） | 何が出るか |
|---|---|---|
| `stageTable` | `variant` / ★`series` / ★`source` / ★`caption` | `stages.json` の系列から組む表 |
| `mechanismTable` | ★`source` / ★`caption` | `reactions.json` の全件から組む表 |
| `dehydrationTable` | ★`source` / ★`caption` | 列挙器から組むアルコール↔アルケンの対応表 |
| `example` | ★`stageId` / ★`lead` / ★`note` | 「▶ 組んでみる」（既存ステージの採点へ渡す） |

⚠⚠ **この4つは手で書いた一覧ではない。** ★ **`REF17` が `learn.js` の `renderBlock` の
実体（`Function.prototype.toString()`）から `b.kind === '…'` を拾って突き合わせる** ＝
**描けない種類を書いたらその場で赤**、**learn.js に5つ目を足したら一覧も自動で増える。**

### 16-3. ★★ 移行が無傷であることの示し方（この仕事のいちばん大事なところ）

⚠ **本文を書き換える仕事ではない。入れものを替えるだけ。**
★ **示し方は「深い比較で差分0」ではなく、その1段上の「1バイトも違わない」**にできた。

**手順（★ 2段に分けたのはこのため）:**

1. ★★ **生成器の書き出しを、移行前とまったく同じ整形**
   （`'[\r\n' + pages.map(p => JSON.stringify(p)).join(',\r\n') + '\r\n]\r\n'`）にして生成し、
   **`git diff -- assembler/reference.json` が空**であることを確かめた。
   ★★ **＝ 5ページ・147 文字列・30 コード・キーの並びまで含めて、1バイトも動いていない。**
   ⚠ **深い比較（`JSON.parse` して構造を突き合わせる）より強い**。深い比較は
   「キーの並びが変わった」「空白が入った」を*許して*しまうが、ここでは許す必要がなかった。
2. ★ **そのうえで、書き出しの整形だけを「1ブロック1行」へ変えた**（次項）。
   ⚠ **このときの差分は空白だけ**なので、**`JSON.parse` した結果の深い比較で差分0**を確かめた
   （★ 検算は「前の版の JSON を `git show` で取り出して `JSON.parse` し、キーの並びも含めて突き合わせる」）。

★★ **結果: 移行による中身の変化は 0 件。** ⚠ **「意図した整形」も 1 件しか無い**（下の 16-4 の空白）。

### 16-4. ★ 生成物 `reference.json` の整形（⚠ 発注書との相違点その3）

★ **移行前は「1ページ＝1行」**（`CLAUDE.md` の `compounds.json` の作法にそろえたもの・`REF5` が見張っていた）。
⚠ **これを「1ブロック＝1行」に変えた。**

```
[
{"id":"alkane-naming","unit":"aliphatic",…,"singleSource":false,
"why":"…",
"blocks":[
{"kind":"text","text":"…"},
{"kind":"stageTable","series":"アルカン（直鎖）","source":"…","caption":"…"},
…
]},
{…次のページ…}
]
```

| | 移行前 | ★ 移行後 |
|---|--:|--:|
| 行数 | 7 | **54** |
| ★ **いちばん長い行** | ⚠ **2,048 字** | ★ **475 字**（`why` の1本。本文の段落は 100〜350 字） |
| 1文字の直しで動く行 | 1（**2,048 字ぜんぶ**） | 1（**その段落だけ**） |

★ **狙いは1つだけ: `git diff` を人が読める大きさに保つこと。**
⚠ **`compounds.json` の「1行1件」規約からは外れる**が、**あちらは *正* で、こちらは *生成物***。
★★ **生成物の形を守る意味は「勝手に書き戻されないこと」だけ**で、それは
**`REF17`（生成し直したものと1バイト一致）が、行数の検査より強く見張る。**

⚠ **`REF5` の行数の検査は消していない。** ★ **新しい形（`2 + Σ(4 + ブロック数)` 行）を計算して見る**形に直した
＝ **`JSON.parse` → `JSON.stringify` で書き戻すと今までどおり赤**（★ 否定対照で確認済み）。
★ **`tools/verify-compounds.js` は `reference.json` を見ていない**ので影響なし（`compounds` / `stages` / `reactions` の3本だけ）。

### 16-5. ★★ 機械で守ること（新設 `REF17` ＋ `REF5` の直し）

| # | 何を守るか | どこで |
|---|---|---|
| ★★ 1 | **`reference.json` が `reference-src/*.md` から生成したものと *1バイト* 一致** | `REF17`（ブラウザ）＋ `gen-reference.mjs --check`（node） |
| ★ 2 | **`ORDER.txt` の並び ＝ `reference.json` の並び**（索引の順は原稿側が決める） | `REF17` |
| ★ 3 | **`codes` が qa の 316 項目に実在する** | `REF17`（⚠ **移行前は「配列で1件以上」しか見ていなかった** ＝ 新設） |
| ★ 4 | **`:::` の `kind` が `learn.js` の `renderBlock` が実際に描ける種類** | `REF17`（★ 一覧を手で書かず `toString()` から拾う） |
| ★ 5 | **`.md` が1枚も欠けていない／余っていない** | `gen-reference.mjs --check`（★ **ディレクトリを読めるのは node 側だけ**） |
| ★ 6 | 生成物が書き戻されていない（行数と形） | `REF5`（形を新しくして継続） |

⚠ **1つだけ役割を分けた**: **「`.md` が余っている」（＝ 原稿を足したのに生成し忘れ）は
ブラウザからは見えない**（HTTP でディレクトリ一覧が取れない）。
★ **`ORDER.txt` を置いたのは、この穴を半分埋めるため** —— **`ORDER.txt` に行を足して生成し忘れれば `REF17` が赤**。
★ **完全に塞ぐのは node 側**（`gen-reference.mjs --check` を **`verify-release.js` の規則10** から呼ぶ ＝ コミット前の儀式に乗る）。

★ **否定対照（★ 全部、実際に赤くして確かめた。§16-8）。**

### 16-6. ★★ 校正の手順（ユーザー向け・そのまま画面に貼れる形）

#### A. 本文を直す（誤字・言い回し・説明の足し引き）

1. **`reference-src/<ページの id>.md` を開く。**
   ★ **ふつうのテキストファイル**なので、**どのエディタでもよい**（メモ帳・VS Code・
   Google ドライブ経由でスマホのテキストアプリでも可）。
2. **直す。** ★ 覚えることは3つだけ:
   - **1段落 = 1行。** 段落のあいだは**空行**。⚠ **段落の途中で改行しない。**
   - 太字にしたいところは、**アスタリスク2つで挟む**（`アスタリスク2つ` ＋ ここ ＋ `アスタリスク2つ`）。
     下付きは `C₄H₁₀` のように**そのまま打つ**（⚠ 一般式の `C~n~H~2n+2~` だけ `~` で囲む）。
   - **`:::` で始まる囲みは機械が作る表**。⚠ **中の `caption:` や `lead:` の *文* は直してよいが、
     `series:` `source:` `stageId:` は直さない**（表の中身の出どころ）。
3. **「◯◯.md を直した」と伝える。**
4. こちらで **生成 → 全テスト → 公開**。
5. **公開ページで読み返す。**

#### B. 新しいページを1枚足す

1. **`reference-src/<新しい id>.md` を1枚置く**（★ 既存の1枚をコピーして書き換えるのが早い）。
2. **`reference-src/ORDER.txt` に、その id を1行足す**
   （★ **索引に出る順はこのファイルの順**。⚠ 足し忘れると生成器が「原稿が余っている」と言って止まる）。
3. **前書きの `why` を1行書く** —— ⚠ **「なぜこの表なのか」が書けないなら、それはどこかから持ってきている**（§1-2）。
4. **「◯◯ を足した」と伝える。** → **生成すると索引に並ぶ**（★ 索引は機械で組むので、他は何も触らない）。

⚠ **`reference.json` は直さない。** ★ **直しても生成のたびに上書きされ、
その前に `REF17` が「原稿と食い違っている」と赤で止める。**

#### C. こちら側の手順（生成 → 検算 → 公開）

```
node tools/gen-reference.mjs            … reference-src/ から assembler/reference.json を作り直す
node tools/gen-reference.mjs --check    … 作り直さずに「最新か」だけ見る（差分があれば終了コード1）
node tools/verify-release.js assembler  … 版・化け・BOM・死にリンク（★ 規則10 で上の --check も走る）
（ブラウザ）assembler/test.html         … 全テスト。★ 参考書の帯だけなら ?only=REF
```

### 16-7. ★ 面A（`/reference/` の公開ページ）を作るとき、この書式から何が取れるか

⚠ **面A の実装はこのレーンの仕事ではない**（発注書 §6）。★ **材料として何が揃ったかだけ書く。**

| 面A に要るもの | ★ この書式から取れるか |
|---|---|
| **本文の HTML** | ★★ **そのまま**（`kind:"text"` の `text` は既に `<b>` / `<sub>` 入りの HTML 断片） |
| **ページの題・単元・グループ** | ★ 前書きの `title` / `unitLabel` / `group`（★ **R-3 の2階層の索引は `unit` → `group` で組める**） |
| ★ **索引の並び** | ★★ **`ORDER.txt`**（⚠ **面A が別の順を発明しない**。索引が2つあると必ずずれる） |
| **`<title>` / OGP / meta description** | ★ `title` ＋ `unitLabel` は取れる。⚠ **要約文は無い** —— ★ **1段落目を使うか、前書きに `summary:` を足す**（**足すなら書式の変更なので、この設計書に書いてから**） |
| **表・例題** | ⚠⚠ **取れない。** ★ **`:::` は「どの表か」しか言っておらず、行は `learn.js` が
`stages.json` / `reactions.json` / 列挙器から**その場で**組む**（§10-2 の「行データを持たせない」）。<br>★★ **＝ 面A は「静的 HTML を書く」のではなく、`gen-isomer-pages.mjs` と同じく **ヘッドレスで本体を走らせて焼く** のが筋**（あちらも同じ理由でそうしている）。⚠ **面A 側に表を組む2本目の実装を書くと、`stages.json` を直したときに面Aだけ古くなる。** |
| **動画の埋め込み（R-5）** | ⚠ **無い。** ★ 足すなら **前書きに `video:` を1キー**（`video-scripts/` の id）。⚠ **本文の中に置かない**（本文は面A と面B で共通なので、面B にだけ無いものは前書き側に置く） |
| ★ **ハブからのリンク（R-6）** | ★ `ORDER.txt` ＋ 前書きだけで一覧が作れる（⚠ **`/isomers/` 13枚が孤立した事故の再発防止**） |

### 16-8. ★★ 否定対照（★ 全部、実際に赤くしてから戻した）

| | 壊した内容 | 何が赤くなったか |
|---|---|---|
| **A** | `assembler/reference.json` の本文を1文字だけ手で直す | ★★ `REF17`「生成し直したものと1バイト違う」＋ `gen-reference.mjs --check` が終了コード1 |
| **B** | `.md` の本文を1文字直して**生成し忘れる** | ★ 同上（**向きが逆でも同じ1本で捕まる**） |
| **C** | `reference.json` を `JSON.parse` → `JSON.stringify(a,null,2)` で書き戻す | ★ `REF5`「1ブロック1行でない（… 行 / 期待 … 行）」＋ `REF17` |
| **D** | `ORDER.txt` の並びを入れ替えて生成し忘れる | ★ `REF17`「索引の順が原稿（ORDER.txt）と違う」 |
| **E** | `codes` に実在しないコード（`org.ali.zzz`）を1件足す | ★ `REF17`「qa の 316 項目に無い」 |
| **F** | `:::` の `kind` を `learn.js` が描けない名前（`foobarTable`）にする | ★ `--check` **と** `REF17` の両方が赤「『:::foobarTable』は描けない種類です（書けるのは stageTable / mechanismTable / dehydrationTable / example）」 |
| **G** | 本文に生 HTML（`<i>強調</i>`）を書く | ★ 生成器が赤（変換のあとに `<` が残る） |
| **H** | `**` の閉じを1つ落とす | ★ 生成器が赤（変換のあとに `*` が残る） |
| **I** | 前書きから `why` を消す | ★ 生成器が赤「前書きに『why』がありません」 |
| **J** | `.md` を1枚足して `ORDER.txt` に書かない | ★ `gen-reference.mjs --check` が赤（⚠ **ブラウザからは見えない**。§16-5 の役割分担） |
| **K** | `ORDER.txt` に id を足して `.md` を置かない | ★ 同上（**逆向きも同じ1本で止まる**） |

### 16-9. ⚠ 発注書の指定のうち、現物と違っていたもの

1. ⚠ **「1行が 2,300〜4,600 文字」→ 実測 1,160〜2,048 文字。** ★ 症状の向きは同じ（読めない）。
2. ⚠ **置き場所 `reference/` → `reference-src/`。** ★ 理由は §16-1（Jekyll が `/reference/` に
   `.html` を勝手に作り、R-1 の面A と衝突する）。
3. ⚠ **`:::` の中身「囲みの本文が `caption`／`lead`、2つ目は `> ` の引用行」→ `キー: 値` に統一。** ★ 理由は §16-2(3)。
4. ★ **「差分0を深い比較で示す」→ *1バイトも違わない* で示せた**（§16-3）。**深い比較は2段目の整形変更のときだけ使った。**

### 16-10. ⚠ 触っていないもの / 残していること

- ⚠ **本文は1文字も書き換えていない**（§16-3）。★ **書式の決定と入れものの移設だけ。**
- ⚠ **`learn.js` / `style.css` / `index.html` の本体は1バイトも触っていない**（★ 版番号の `?v=` と表示だけ）。
  **`game.js` / `chemistry.js` / `stages.json` / `compounds.json` / `reactions.json` も不変。**
- ⚠ **面A（`/reference/` の公開ページ）は作っていない**（発注書 §6・次のレーン）。★ 材料は §16-7。
- ⚠ **`summary:` / `video:` の欄は足していない**（面A が要ると決めてから足す。★ **使う人が決まる前に欄を作らない**）。
- ⚠ **エスケープ記法は無い**（§16-2(2)）。★ **要るようになったら、まずここに書く。**

---

## 17. ★★ 面A ＝ `/reference/` という独立した読み物ページ（2026-09-08・ref-face レーン）

**発注書は [ORDER_reference_faceA_2026-09-08.md](ORDER_reference_faceA_2026-09-08.md)（R-1〜R-6 の原文は
[video-scripts/ORDER_reference_book_2026-09-08.md](video-scripts/ORDER_reference_book_2026-09-08.md) §5）。**
★ §16 が「本文の正を Markdown へ移す」までをやり、**このレーンは「その本文に URL を与える」**。

### 17-1. ★ 決まっていたこと（そのまま採った）

| # | 決定 | このレーンでの扱い |
|---|---|---|
| **R-1** | URL は `/reference/` | ★ そのまま。`/reference/index.html`（索引）＋ `/reference/<id>/index.html`（1ページ1枚） |
| **R-2** | HTML は原稿から**生成**する | ★ `tools/gen-reference-pages.mjs`。手書きしない |
| **R-3** | 索引は `unit` → `group` の2階層・並びは `ORDER.txt` | ★ そのまま（**面Aは順を発明しない**） |
| **R-5** | 面Aには埋め込む | ★ 一問一答・アプリの2つを埋め込み（動画は枠だけ） |
| **R-6** | ハブから1本リンクを張る | ★ ルート `index.html` に2か所（カードと単元表）。**機械で見張る**（17-7 の②） |
| ★ 要約文 | 前書きに `summary:` を足す | ★ **必須**にした（理由は 17-2） |
| ★ 一問一答 | **その場で解ける形**で埋める | ★ `/qa/?codes=…&mode=choice` を iframe。**qa 側に受け口を1つ足した**（17-4） |

### 17-2. ★ `summary:` は必須にした（発注書は「任意にして1段落目から作る」を推していた）

⚠ **採らなかった。** 理由は3つ:

1. ★★ **1段落目は要約ではない。** 実測で5枚の1段落目は **86〜148 字**あり、`<b>` を含む。
   `<title>` の下に出る文としては長すぎ、**途中で切られると意味が変わる**。
2. ★★ **自動で作った文は、ユーザーが校正できない。** §16 の目的そのものが
   「**本文を自分で直せるようにする**」ことで、**検索結果に出る文だけ機械が勝手に決める**のは逆向き。
3. ★ **書式に「任意のキー」を1つも持たずに済むわけではない**（`video:` が任意になる）が、
   **任意にしてよいのは「まだ実体が無いもの」だけ**にした ——
   `summary` は5枚とも**いま書ける**ので、書かせるほうが事故が少ない。

★ **`video:` は任意**（解説動画は 2026-09-08 時点で **0本**）。⚠ **無いページに枠を出さない**ことを
`REF18` が見張る（発注書の機械検査⑤）。

⚠ **書式が増えたのはこの2キーだけ。** `tools/reference-md.js` の `PAGE_KEYS` に `summary`、
新設の `PAGE_OPTIONAL_KEYS` に `video` を足した ＝ **parser も生成器も1本のまま**（§16-2 の約束）。

### 17-3. ★★ 表・例題は「焼く」—— 2か所で組まないことの示し方

⚠⚠ **§16-7 の申し送りのとおり、面Aは静的 HTML を書かない。**
★ `tools/gen-reference-pages.mjs` は **`gen-isomer-pages.mjs` とまったく同じ形**で、
ヘッドレスの Chromium で `/assembler/` を開き、**`learn.js` の `ReferenceBook.renderBlock` を呼んで
出てきた DOM をそのまま焼く**。

★★ **「2か所で組んでいない」ことの示し方は、コードの読み合わせではなく機械で:**

| ★ | どうやって示すか |
|---|---|
| ① | ⚠ **生成器は `stages.json` / `reactions.json` / 列挙器を1度も読まない**（`readFileSync` するのは `assembler/style.css` と `qa/index.html` だけ）。**表の行を作る式が生成器のどこにも無い** |
| ② | ★ `REF18` が、**焼いた HTML の中の表の行数**と、**その場でアプリに組ませた表の行数**を突き合わせる ＝ `stages.json` を直して焼き直し忘れると赤 |
| ③ | ★ **CSS も書き写さない** —— `assembler/style.css` から `.ref-*` の規則を**切り出して**埋める（`gen-isomer-pages.mjs` が計測スニペットを `qa/index.html` から切り出すのと同じ手）。⚠ 焼いた HTML に出てくる class に規則が1つも無ければ**生成器が赤**（learn.js が class を足したら気づく） |

★ **押しもの（`<button>`）はリンクへ置き換える。**
静的なページに押しても何も起きないボタンを残すのは**行き止まり**なので、生成器が変換する:

| 焼いたときの押しもの | 置き換え先 |
|---|---|
| `button.ref-mech-play`（▶ 見る） | `/assembler/?open=mechanism&id=<反応id>` |
| `button.ref-try`（▶ 組んでみる） | `/assembler/?open=reference&code=<そのページの先頭コード>` |

⚠ **変換したあとに `<button>` が1つでも残っていたら生成器が赤で止まる**（見落としを人の目に頼らない）。

### 17-4. ★★ 一問一答は「その場で解ける」＝ `/qa/` を iframe で埋め、qa に受け口を1つ足した

⚠ **`qa/` の実装は1行も書き写していない。** 埋めているのは **`/qa/` そのもの**（同一オリジンの iframe）。

⚠ **既存の受け口 `?code=` は1件のめくり（`mode:'flip'` 固定）**で、
**ページの持ちコード（2〜10件）をまとめて解く口が無かった**ので、**qa 側に足した**:

```
/qa/?codes=<コード1>,<コード2>,…&mode=choice&from=reference
```

| ★ | 決めたこと | なぜ |
|---|---|---|
| ★ | **モードは `choice`（測定）** | ユーザー決定「**読み物として答えを並べるのではなく、その場で解ける**こと」。めくり（flip）は自己申告で、**解く**にあたるのは複数選択のほう（`TAXONOMY` §4 の本則と同じ線） |
| ★ | **順は送られた順のまま**（他のセッションと違い混ぜない） | 参考書の本文が説明した順に並ぶ。⚠ ただし**何を出すかを決めるのは qa 側**（知らないコードは黙って落とす） |
| ⚠ | **どこへ着地させるかは qa が決める**（`scope:'codes'`・戻り先はホーム） | `ion-equation ⇄ ratio` と同じ約束。**面Aは「自分の持ちコード」しか送らない** |
| ⚠ | **1件も引けなければホームのまま**＋帯で理由を言う | 黙って白紙にしない（既存の `?code=` と同じ作り） |
| ★ | 帯の「来た道」に **`reference: '参考書'`** を足した | 相手を名指しできるのは**こちらが送った先だけ**、の既存の約束どおり |

★ **埋め込みは「押してから読み込む」**（`<iframe>` を最初から置かない）。理由は2つ:
- ⚠ **計測が二重になる** —— `/qa/` は自分で `page_view` を送るので、**開いてもいない一問一答が
  参考書を開いた回数だけ数えられる**（`ga4-bot-traffic` で1度やっている「数の出どころを取り違える」型）。
  ★ **押して初めて読み込む ＝ 数が「実際に解こうとした回数」と一致する。**
- ★ アプリの埋め込みも同じ（`/assembler/` は重い）。

### 17-5. ★ アプリの埋め込みは「そのページの面B」

★ 埋めるのは **`/assembler/?open=reference&code=<先頭コード>`** ＝ **同じページを資料ペインで開いたアプリ**。
⚠ **面Aがアプリのページ id を名指ししない**（`?open=reference&page=` という受け口は作らない）——
**どのページを開くかを決めるのは `ReferenceBook.pageByCode`** で、いまもそこ1か所（§14-4 の約束）。

⚠ **これが成り立つ条件は「1つのコードが2ページに出てこない」こと。** ★ 実測 30件すべて別 ＝
先頭コードは必ず自分のページへ戻る。**`REF18` が重複を機械で見る**（増えたら赤）。

### 17-6. ★ 動画は枠だけ（0本）

★ `video:` があるページにだけ `youtube-nocookie.com` の埋め込みを出す。
⚠ **いまは5枚とも `video:` を持たない ＝ どのページにも枠が出ない**（これが `REF18` ⑤の否定対照そのもの）。
⚠ 値は **YouTube の動画ID**（`video-scripts/` の台本 id ではない）。
**埋め込むには実際の配信先の id が要る**ので、台本 id を書くと「埋め込めない値が書式上は通る」ことになる。

### 17-7. ★★ 機械で守ること（発注書 §6 の5つ）

| # | 何を守るか | どこで |
|---|---|---|
| ★★ ① | **`/reference/` の全ページが原稿から生成したものと一致**（HTML を手で直したら赤） | `gen-reference-pages.mjs --check`（★ 本体を走らせ直して**1バイト一致**。`gen-isomer-pages.mjs --check` と同じ）＋ `REF18`（★ **ブラウザだけで届く範囲** —— 本文の段落・表の行数・見出し） |
| ⚠⚠ ② | **ハブから `/reference/` へのリンクが1本以上** | `REF18`（★ `/isomers/` 13枚が孤立してインデックスされなかった轍・`seo-orphan-pages`） |
| ★ ③ | **索引の並びが `ORDER.txt` と一致** | `REF18`（★ `REF17` が「`reference.json` の並び ＝ `ORDER.txt`」を見ているので、面A側は**焼いた索引の並び ＝ `reference.json` の並び**を見る ＝ 物差しは1本のまま） |
| ★ ④ | **`codes` の埋め込み先が qa に実在する** | `REF17` ④（既存）＋ `REF18`（★ **埋め込んだ URL の中のコード**が原稿の `codes` と一致することを見る ＝ URL を手で書き換えても赤） |
| ★ ⑤ | **`video:` が無いページに動画の枠が出ない** | `REF18` |

⚠ **`--check` は playwright が要る**（本体を走らせ直すので）。★ だから **`verify-release.js` には足していない** ——
`gen-isomer-pages.mjs --check` を足していないのと同じ理由で、**コミット前の儀式を重くしない**。
★ **軽いほうの検査（`REF18`）は `assembler/test.html` に乗るので、毎回の全走で必ず通る。**

### 17-8. ⚠ 発注書の指定のうち、現物と違っていたもの

1. ⚠ **「`qa/` を iframe で埋めるのが素直」は正しかったが、受け口が足りなかった** ——
   `?code=` は**1件・めくり固定**で、「解ける形」にならない。★ `?codes=` ＋ `mode=` を足した（17-4）。
2. ⚠ **「`summary:` は任意にして未記入なら1段落目から作るのが素直」→ 必須にした**（17-2）。
3. ⚠ **`video:` の値は「`video-scripts/` の id」（§16-7 の見立て）→ YouTube の動画ID**（17-6）。
4. ★ **`/reference/` と Jekyll の衝突は起きない**（`_config.yml` の `exclude:` は `reference-src/` だけ・
   `/reference/` は除外に1件も掛からない）。★ **実測で確かめた**（17-9）。

### 17-9. ★ 実測（ローカル :8486・リポジトリルート配信・印 `marker-rf` で配信元を確認）

**焼いた結果:**

| | |
|---|---:|
| ページ | **6**（索引＋5枚） |
| いちばん大きいページ | 反応機構 **22.3 KB**（14件の表） |
| `learn.js` から焼いたブロック | **32** |
| 切り出した `.ref-*` の CSS 規則 | **38**（＋ `:root` の変数 **6**。器の上書きは別に10） |
| ハブ → `/reference/` のリンク | **6 本**（カード1・単元表の3行に5本） |

⚠ **`--border-color` が未定義で表の罫線が1本も出ていなかった。**
★ 原因は「規則は切り出したが**変数は切り出していなかった**」こと ——
**予備の無い `var(--…)` は、定義が無いと宣言ごと無言で無効になる**（枠が消えても「それらしく」見える）。
★ 直し方は「色を書き写す」ではなく、**規則が使っている変数を `:root` から依存ごと持ってくる**にした
（`var(--x, 予備)` は予備が効くので要求しない ＝ `--color-bg` は :root に無いまま通る）。

**⭐ 否定対照（★ 7件すべて、実際に赤くしてから戻した）:**

| | 壊した内容 | `REF18` が言ったこと |
|---|---|---|
| **A** | 焼いた HTML の**段落を1文字**だけ手で直す | 「1 番目の段落が原稿と違う（生成物を手で直したか、直して焼き忘れたか）」 |
| **B** | ★★ **表の行を1つ手で消す** | 「3 番目（:::stageTable）が、**いまアプリが組む中身と違う**」＝ **焼き直し忘れが捕まる** |
| **C** | ⚠⚠ **ハブからのリンクを全部消す** | 「ハブから /reference/ へのリンクが1本も無い」 |
| **D** | 索引の2枚を入れ替える | 「索引の並びが原稿（ORDER.txt → reference.json）と違う」 |
| **E** | 一問一答へ送るコードを1つ書き換える | 「一問一答へ送るコードが原稿と違う」 |
| **F** | `mode=choice` を `mode=flip` にする | 「一問一答が測定モードで開かない（めくりは自己申告なので『解ける形』にならない）」 |
| **G** | `video:` の無いページに動画の枠を足す | 「video: が無いのに動画の枠が出ている」 |

**⭐ positive control（`video:` の出る側。⚠ 5枚とも持たないので毎回は走らない）:**
`reference-src/alkane-naming.md` に `video: dQw4w9WgXcQ` を1行入れて生成 → 焼き直し →
**そのページにだけ枠が出て**（他の4枚は 0 件）、`REF18` は緑。★ そのあと戻した。

**⭐ 一問一答の埋め込みを実際に押した:**
`iframe` が `/qa/?codes=org.ali.alkane-names,org.ali.formula-alkane&mode=choice&from=reference` を読み、
**1 / 2 問・選択肢6個・採点ボタン**が出た。帯は「**参考書から来ました（このページの 2 / 2 項目）**」＋
「**参考書へ戻る**」（`target="_top"`）。⚠ **「戻りました」を「来ました」に直した** ——
こちらが送り出していない相手に「戻りました」と言うと、押した覚えのない人に嘘をつくことになる。

⚠ **索引と `ORDER.txt` の順は、完全には一致しない。**
`ORDER.txt` は `aliphatic / aliphatic / alcohol / reaction / alcohol` の順で、
**索引は `unit` → `group` の2階層なので 5枚目（alcohol）が3枚目の隣へ寄る。**
★ **面Aが順を発明しているのではなく、まとめているだけ**（前後リンクは `ORDER.txt` のまま）。
⚠ **完全に一致させたければ `ORDER.txt` で alcohol の2枚を続けて置く**——
本文の読み順の判断なので、**このレーンでは触らずユーザー判断に残した。**

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-01 | 新規。§1 著作権の線引き／§2 索引（既存資産の実測）／§3 レイアウトの実測（375・812横・1280）／§4 3案の見積り／§5 読む→試す／§6 チュートリアル／§8 判断待ち D-R1〜D-R9 |
| 2026-09-01 | §7 追加（ユーザー追加方針「参考書は無料・例題つき／問題集は有料」）。例題と問題の線は *種類* で分ける推奨、静的配信の実測、既存の無料資産の量。`D-R10`〜`D-R12` を追加 |
| 2026-09-02 | ★ §10 追加（第1ページ「アルカンの命名」の実装記録）。実装済み ＝ 説明の表＋例題1つ・どちらも無料。⚠ §3-2 実測④ の 380px を **340px** へ訂正（`main` の gap 15px が数に入っていなかった）。⚠ §5-2 の「表の10行それぞれに ▶」は採らず、例題は代表1つに。テスト `REF1`〜`REF7`（`RB` は既に使われていた） |
| 2026-09-02 | ⚠⚠ **§4-3 の「閾値は新設せず 1000px を流用」を実測で覆した** —— 1000〜1150px では資料を開くと床（結合 28px）を割る（1000 でデカン 22.6・1150 でスクロース 25.4）。★ **分割は `min-width: 1200px` から**・幅は `clamp(280px, 25vw, 340px)`。`REF4` は 1199（分割しない）と 1200（閾値ちょうど・デカンとスクロースの両方）で見張る |
| 2026-09-02 | ★ §10-5 追加（ユーザー決定の言い直し「画面に入らないことの解決策として *中身を切る* を選ばない」を `REF4` に焼き込み・否定対照つき）／★ §10-6 追加（測るときの落とし穴 —— `screenPxPerGrid()` はモードと localStorage で変わる。⚠ これを外して §3-2 実測① を誤って訂正しかけたが、**測り直したら実測① は1件も間違っていなかった**） |
| 2026-09-04 | ★★ §14 追加（**学習メニューの ⚗️ 反応機構ビューアの札を消した**。ユーザー決定 2026-09-03「消す方がすっきりすると思います」）。⚠ **消したのはメニューの札だけ** —— `?open=mechanism[&id=]` の受け口（qa の飛び道具10件・ハブの1本）／`reactor.js` の11本／再生器は不変で、**`#select-reaction`（選択の実体）は帯 `#ws-reaction` へ移した**。★ `?open=mechanism` の行き先を**資料の4枚目**へ向け直し（`id` があればその1件を再生・無ければ表だけ）、⚠ **`OPEN_TARGETS` は空にして受け口④の1か所で決める**（`fn` を置くと非同期の描画と再生が競って**スマホで資料が開き直る**）。★ `playMechanism` を `openById` から **`pick()`**（押しものから呼ばれる唯一の口）経由へ。⚠⚠ **波及先は発注書の3つではなく実測19件**（`RX24`/`RX25`/`RX26`/`RX46`/`RX47`/`RX48`/`RX49`/`LX6`/`Q1`/`EP5`/`EP7`/`EP8`/`SF6`/`RB5`/`RB6`/`ID5`/`ID6`/`REF12` ほか）。★ **`RX49` は守る対象を「スイッチ」から「入口を開いただけで先頭を選ばないこと」へ移し**、`EP7` は**数字ではなく守っている3つ**（id と並び順・見出しが `<details>` の外・見出しと群の対応）を本文に書いて期待値を一覧から機械で出す形にした。★ **`EP8` の3語は資料の本文へ受け皿を作って残した**（`#rx-viewer-note` は消えたが、守るものは1文字も変えていない）。★ **否定対照は「残った道が生きている」側**（`REF14` 新設・I/J の2本を実際に赤くして確認）。⚠⚠ **ハブは触らないと決めた**（§14-4。URL を残す以上 直す必要が無く、`?open=reference&code=` にすると**ハブが assembler の内部を知る** ＝ ion→ratio で戒めた形になる。ハブは `?v=` の検査の外でもある） |
| 2026-09-03 | ★★ §13 追加（**反応機構を参考書に内蔵した ＝ 第4ページ**。ユーザー指示「反応機構モードは参考書の中に内蔵したほうがいいですね」）。⚠⚠ **発注書の「収録台本が `?open=mechanism` を使っている可能性が高い」は実測で 0本**（`video-scripts/` に `open=` が1件も無く、`demos-*.json` にも機構ビューアの選択子が0件）。★ 入口は実測で **6種**（学習メニュー／`?open=mechanism[&id=]`／ハブ1本／reactor の `mechanismId` 11本／qa の飛び道具10本／台本0本）で、**1つも壊していない**。⚠⚠ **14件は既存3ページのどれにも対応しない**（qa の知識コードで機械照合して交わり 0）＝ 新しいページが要ることを確認。★ 採ったのは **(c)+(b)**（14件を機械で組んだ表＋各行の ▶ で既存ビューアを再生）。**(a) 埋め込みは `borrowCanvas`／本体 SVG の `#arrows-group` に描く作りなので「2枚目の描画面」＝ 作り直しになるため却下**。⚠ **§10-2 の「表の行に ▶ を並べない」とは逆にした**（stages の行は *お題*・機構の行は *読み物* で、混ざる層が無い）——**表の class を分けたので stages 側の禁止は `REF6`/`REF10` で今までどおり生きる**。★ `REF12`（全件・名前で引く・絞れない・▶ が既存ビューアに合流・資料が再生器を持たない）と `REF13`（機構にも床28px。最大は **ジアゾカップリング 36.1px**・物差しの検算つき）を新設し、`REF5` に「どのページも表のブロックを持つ」を追加（`REF8`/`REF10` を stages の表を持つページに絞ったすき間を塞ぐ）。⚠⚠ **否定対照 E が1回 空振りした** —— ペイン幅を広げても `max-width: 340px` が効いて緑のまま。**2行そろえて初めて赤くなった**。⚠⚠ **実機で測って作り直した点が2つ**（§13-4b）: ① **4列だと ▶ が資料ペインの外**（表 426px / 器 271px・右へ 155px）＝ **押すものが見えなかった** → **2列**（段数と ▶ は反応の欄の中）／② **`reactions.json` の並びは型の順ではない**ので「続いているあいだ」でまとめると**付加反応が2か所に割れた** → `reaction.js` の `populateList` と同じまとめ方にし、`REF12` は**資料の表とビューアの一覧を突き合わせる**（検査の中で並べ直さない） |
| 2026-09-03 | ★★ §12 追加（床を守る範囲のユーザー決定 ＝「資料が見せている分子まで」／`D-R5` は「折り返しよりもスクロール・拡大縮小で対応」）。⚠⚠ **§10-3 の「閾値を決めているのはスクロース」を実測で覆した** —— 在庫 1090件を `requiredViewWidth` で機械的に並べると スクロースは26位で床を割らず（30.6px）、最大は **ナイロン66**（視野2046・1200px で 9.9px・床に載せるには約2255px ＝ どの閾値でも届かない）。⚠ §11-4 の「1440 なら ステアリン酸 28.3 ○」も実測 **27.7 ✗**（分かれ目は 1448px、油脂・セッケンの帯まで含めると 1480px）。★ `REF4` から分子の名前を外して**在庫から機械で選ぶ**形にし、`REF4b`（在庫最大は床を割る／拡大とパンの逃げ道が生きている）を新設。⚠ `style.css` は不変。★ §12-5 に「**1200 はいま測って決まっている値ではない**（据え置きの理由と、下げるなら何を測り直すか）」を残した ——「借りた値」の二の舞を避けるため。★ `DEVELOPMENT.md` の「閾値は借りられる値ではなく測って決まる値」の節も直した（教訓を「物差しに使う分子を1つにしない」から「**最悪ケースは選ぶのではなくデータから決まる形にする＋選抜そのものを検算する**」へ。2つに増やしても名前で選ぶ限り直らないため） |
| 2026-09-08 | ★★ §16 追加（**本文の正を Markdown に移した**。ユーザー原文「json だとこちらで校正ができないので、校正手順を考えてください」）。★★ `reference-src/<id>.md` が正・`assembler/reference.json` は生成物（`tools/gen-reference.mjs`）。⚠ 置き場所は発注書の `reference/` ではなく **`reference-src/`** —— **`---` の前書きを持つ .md を Jekyll が `.html` に変換して出す**ので、R-1 で面Aの URL に決まっている `/reference/` に置くと衝突する（`_config.yml` の `exclude:` に1行追加）。★ 書式は3つ（前書き `---` ／ **1段落=1行** の本文 ／ `:::kind` の囲み）で、囲みの中も**前書きと同じ `キー: 値`**（⚠ 発注書の「位置で決める・2つ目は `> `」は採らず。`series` が文字列と配列の両方を取ること・`lead`/`note` がどちらも散文であることが理由）。★★ **移行は「深い比較で差分0」ではなく *1バイトも違わない* で示した** —— 生成器の書き出しを移行前と同じ整形にして `git diff` を空にし、そのあと整形だけを「1ブロック1行」へ変えて `JSON.parse` の深い比較で差分0を確認（★ いちばん長い行 **2,048 → 475 字**）。★ `REF17` 新設（生成物と原稿の1バイト一致・`ORDER.txt` の並び・`codes` が qa の316項目に実在・`kind` を `learn.js` の `renderBlock` の `toString()` から拾って照合）／`REF5` の行数検査は新しい形に直して継続／`verify-release.js` に規則10（`gen-reference.mjs --check`）。⚠ **本文は1文字も書き換えていない**。⚠ 面Aは次のレーン（材料は §16-7） |
| 2026-09-06 | ★★ §15 追加（**第5ページ ＝ アルコールと脱水生成物の対応表**。ユーザー原文「C5H12Oのアルコールの脱水生成物との対応表（反応系統樹）に 2,2-ジメチル-1-プロパノール は載せたいです（俯瞰できるようにしたい）」）。⚠⚠ 指摘の中身は「**出題から外す**」と「**表から外す**」は別だということ —— `DESIGN_isomer_practice.md` §22-3 で書き出しの在庫から落とした2件は「答えが0個だと器が組めない」からで、教材として無価値だからではない。★ 表は器の制約を受けないので **16件ぜんぶ載る**。⚠ **範囲は C₅ 単独ではなく C₁〜C₅**（発注書は C5H12O とも読めたが、**脱水できない2件は C₁ と C₅ に1つずつ居る**ので、C₅ だけだとメタノールが消えて例外が「1件だけの変わり者」になる。2件そろって初めて「β炭素が無い／β炭素に水素が無い」の2通りの理由になる）。★ 行は**書き出し練習とまったく同じ2本**（`enumerateConstitutionalIsomers` の返り順そのまま ＋ `ipDehydrationEdges`）＝ 表を読んでから練習に行くと答えの並びが同じ。★ 新規関数 **2本**・共通関数の改修 **1か所**（`renderBlock` の1行）。⚠ **炭素数を `reference.json` から渡させない**ので「C₅ の行だけ」の表を作る口が構造上ない。⚠ class は `.ref-map-table`（`.ref-table` に混ぜると `REF8`/`REF9`/`REF10` の「stages の系列から作った表」の物差しと噛み合わない）。⚠⚠ **床（§12-1）は `referenceShownStages` が届かない**（この表が見せるのは stages でも compounds でもなく**列挙器が組んだ分子**）ので `REF16` の②で塞いだ —— 45件をその場で列挙して `requiredViewWidth` に機械で最悪ケースを選ばせ、⚠ `summonMolecule` が使えないので**分子を直接キャンバスへ置いて測る**。★ 実測 1200×800（ペイン300・表251px・列 61+91+98・横スクロール0・16/16行）／375×812（表343px・列 61+123+159・16/16行）。★ 否定対照 **10件すべて実際に赤くした**（§15-8。とくに **A: 脱水できない2件を落とすと `REF15` が 14行 vs 16件で赤**／**E: C₅ だけにすると 8行で赤**／**F: `block` から絞れる口を生やすと `REF16` が赤**）。⚠ **例題は付けていない**（`stages.json` に居る一部から代表を選ぶと「表の *選び方*」に近づく）。⚠ `chemistry.js`/`compounds.json`/`reactor.js`/`game.js`/`index.html`/`stages.json` と版番号は不変 |
| 2026-09-08 | ★★ §17 追加（**面A ＝ `/reference/` の公開ページ**。発注書 ORDER_reference_faceA_2026-09-08.md の R-1〜R-6）。★ 索引＋5枚を `tools/gen-reference-pages.mjs` で**焼く**（ヘッドレスで `/assembler/` を走らせ `ReferenceBook.renderBlock` の DOM をそのまま使う ＝ 表を組む式が生成器に1行も無い）。★ 書式に `summary`（**必須**）と `video`（任意）を追加 —— ⚠ 発注書の「summary は任意にして1段落目から作る」は**採らなかった**（1段落目は実測 86〜148字で `<b>` を含み、検索結果の文にならない／自動生成の文はユーザーが校正できず §16 の目的と逆向き）。★★ 一問一答は **`/qa/` を iframe で埋め、qa に `?codes=`＋`mode=` の受け口を1つ足した**（既存の `?code=` は1件・めくり固定で「解ける形」にならない）。★ 埋め込みは**押してから読み込む**（`/qa/` は自分で page_view を送るので、置いただけで「解こうとした回数」が水増しされる）。★ アプリは `?open=reference&code=<先頭コード>` ＝ 同じページの面B（面Aがアプリのページ id を名指ししない）。⚠ 動画は**0本**なので枠だけ（`video:` が無ければ何も出さない）。★ `REF18` 新設（焼いた本文と `reference.json` の一致／索引の並び／**ハブからのリンク**／埋め込み URL のコード／動画の枠）＋ `gen-reference-pages.mjs --check`（1バイト一致）。⚠ `--check` は playwright が要るので `verify-release.js` には足していない（`gen-isomer-pages.mjs --check` と同じ扱い） |
| 2026-09-09 | ★★ §19 追加（**参考書の器を6つ広げ、アルカンのページを1枚書いた**。発端は §18 と同じユーザーの申し立て「参考書の中身は全面的に修正したいです／現状では、化学学習者が参照する参考書になっていない」）。⚠ それまで `.md` に書けたのは**段落と、機械が組む4つの表／例題だけ**で、⛔ 見出し・箇条書き・図・化学反応式・手で書く表が1つも書けなかった。★ `:::` を **4 → 10 種**に（`section` / `list` / `figure` / `reaction` / `table` / `callout`）。★★ **ユーザー承認の構造は「1グループ = 1ページ。分割しない。ただし節へ直接着地できるようにする」**（面Aは2カラム・左に追従する目次／狭い面では折りたたみ／用語の索引 `/reference/terms/` を機械で組む）。⚠ **アンカーの綴りは1つだけ**（`#ref-sec-<anchor>`）—— 面Bはアプリの DOM の中なので素の id は `game.js` と衝突しうるし、面Aだけ素にすると同じ節に2つの id ができる。⚠⚠ **重要度の印は独立させず `:::reaction` の `level` にした**（§18-3 の実測どおり**印は必ず反応式に付く**）＋ **画面には記号だけを出さず言葉を添える**（★★★ 必ず覚える／★★☆ 余裕があれば／★☆☆ 参考）。⚠⚠ **化学反応式は「図＋下に文字」にせず文字だけで組んだ** —— 発注書の案は**式を2か所で持つ**ことになり、画像の中は機械で読めないので直し忘れが捕まらない。★ 図は **PDF から PyMuPDF で焼いて `reference-img/`**（`_config.yml` の exclude に掛からない ＝ 配信される。面A・面Bから同じ `/reference-img/<file>` で読める）。⚠ `.md` に書くのは**ファイル名だけ**でパスは書けない。★ 実測 **13〜73 KB / 平均 40 KB・アルカン1ページ5枚で 190 KB → 52ページで 8 MB 前後**。★ 目次は **`learn.js` の `renderToc` 1本**（面Aの生成器はそれを呼んで焼く ＝ 2本目を持たない）。⚠ **用語の索引は qa の316コードから引かなかった** —— 実物に**用語の欄が無い**（`knowledge` の1文と `variants[].q` の問いしかない）。⚠⚠ **`REF5` の「行データの欄を持たない」に穴を1つ開けた** —— あの守りの本体は「行を手で書くな」ではなく「**出どころの言えない行を書くな**」なので、**`:::table` の `rows` だけ通し、代わりに `source` を必須**にした（⚠ 他のブロックの `rows` は今までどおり赤）。★ 6ページ目 **アルカン**（qa グループ「アルカン」12件のうち `alkane-names` は命名ページ・`alkane-chlorination` は機構ページの持ちものなので **10件**を持つ／節6・40ブロック）。⚠ シクロアルカンの節は置かない（qa では別グループ）。⚠⚠ 練習問題は入れていない（無料／有料の線。判断は §19-11）。★ `REF19` 新設（節のアンカーの一意性・目次と節の1対1・用語索引の行き先・図が面A/面Bの同じ URL から画像として返る・**★★★ が基本の向きを言葉のほうから見る**・焼いた目次が `renderToc` から出ている・描く側と書式で決めごとが割れていない）。★ **否定対照12件すべて実際に赤くした**（§19-10b）。⚠ `REF1` からページの名前を外した（ページを足すたびに検査を直す形になっていた） |
| 2026-09-10 | ★★ §20 追加（**面Aを読める見た目にした**。発端はユーザーの申し立て「デザインを変更したい（黒背景は読みにくいのでは？）／わかりにくいというか、とっかかりにくい感じがしました」）。★★ **申し立ての2つを測って分けた** —— ⚠ **「黒背景」は主因ではなかった**（本文は 18.12:1 あった）が、⚠⚠ **添え物の4種が AA を割っていた**（最小 3.35:1）。★ 面Aだけ明るい地へ（`--bg:#f5f3ef` / `--fg:#20242b` ＝ 14.05:1・最小 4.56:1 ＝ **AA 割れ 0**）、⚠ 面Bは暗いまま（**触る画面と読む画面で要件が違う**）。⚠ 明るい地に `#00f2fe` は **1.25:1** なので `--accent:#0d6c78`（5.52:1）を別に置き、**ブランドの水色は帯の中だけに残した**。★★★ **「とっかかりにくい」の実体は配色ではなく見出し** —— **19/15.5 ＝ 1.23倍・章の切れ目なし**で 7画面ぶん続いていた → **23/17 ＝ 1.35倍＋節の上に線**（★ **面Bも 14/13 ＝ 1.08倍 → 17.5/13 ＝ 1.35倍**。決めごとは①上の線②約1.35倍③最初の節には引かない、の3つで両面共通）。⚠ **1行の字数はもう範囲の中だった**（36.9字）ので行長は主因ではない —— ⚠⚠ **ただし1カラム（<1000px）だけ器が 984px のままで 999px の窓で 56.0字**あり、`main.wrap{max-width:712px}` で塞いだ。★★ **「いま何章目か」は 7,000px のうち一瞬しか出ていなかった**（⚠ `IntersectionObserver` が「帯に *いま重なっている* 節」を見ていて、見出しが通り過ぎると全部消える。**印なし 33/55 地点**）→ **「どの節を通り過ぎたか」に変えて 0/62**。★ 狭い面は目次を貼り付け、札に「いま：<節名>」（88px 固定）。★ 3つの調子は**暗い地では地が全部同じ**だった → 地・線・札の字の3つを分けた。★★ **申し送り「アプリのペインの CSS が面Aの目次を上書きしている」は本当だった** —— 焼いた style は 面Aの器 → 切り出し → 読み替え の順なので、**同じ強さのセレクタは切り出しが勝つ**（⚠ `@media` は強さを足さない）。**広い画面でも資料ペインの小さなカードのまま出ていた** → 面A側を `.ref-layout` から始めて解決。⚠⚠ **`checkLightCoverage` に部分一致の穴**（`.ref-rx` が `.ref-rx-lv1` に釣られる）を否定対照で発見し、丸ごと一致に直した。★ 否定対照4件すべて実測 |

---

## §18 素材の在処と、★印の意味（2026-09-09・ユーザー確認済み）

⚠ **参考書の中身は「全面的に直す」ことになった。** ユーザー原文:

> **参考書の中身は全面的に修正したいです**
> **現状では、化学学習者が参照する参考書になっていない**

### 18-1. 一次資料の場所

| | 場所 | 数 |
|---|---|---|
| **講義スライド（本文＋読み上げ原稿）** | `マイドライブ/化学/動画スライド/*.pptx` | **71本**（有機25本・2,544枚・原稿 1,882枚） |
| ★★ **同じスライドの PDF** | `マイドライブ/化学/動画スライド/drive-download-20260908T201329Z-1-001/` | **46本**（有機19本） |
| 板書 | `マイドライブ/*.gdoc` | 193本。⚠ **中身はドライブ側にしか無く、ローカルからは読めない** |

### 18-2. ★★ 図は PDF から焼く。pptx からは取れない

- ⚠ **pptx の中に画像として入っているのは3枚だけ**（14枚の回で実測）。**構造式は PowerPoint の図形と文字**なので、ファイルとして取り出せない。
- ⚠ **この PC の PowerPoint は使えない**（課金が要る・ユーザー申告）。
- ★★ **PDF ＋ PyMuPDF（`fitz`）で焼ける。** 実測: 960×540pt のページを **dpi=200 → 2667×1500 の PNG**。**構造式・表・矢印・色つきの番号まで鮮明。**
- ★ **ユーザー方針**: **参考書の図はスライド由来のもの**（紙に書く構造式の形）を使う。
  **アプリの構造式は、アプリの操作を説明する場面だけ**に使う ——
  「アプリの構造式は、一般に紙に書く構造式とは異なるので、参考書のページとしては別なものを用いたほうがよい」。
  ⚠ ユーザーは「**参考書生成用にアプリを作るのも手**」とも言っている（＝ 紙の書き方で描くレンダラ。未着手）。

### 18-3. ★★ ★印の意味（⚠ 取り違えると52ページ全部に波及する）

**レジェンドは3段階で、`★☆☆` `★★☆` `★★★` と書く。**

| 印 | 意味 | 有機19本での数 |
|---|---|---:|
| **★★★** | ★★ **基本 —— 必ず書けるようにする** | **96** |
| **★★☆** | 一段上（工業的製法・全体式・発酵など） | **35** |
| **★☆☆** | 重要度は低い | **本文中は4**（他38は表紙・末尾の「難易度★☆☆」＝**回そのものの難易度**で別物） |

⚠⚠ **統合レーンは一度これを逆に取った。** 実物で確かめて直した ——
`★★★` が付いていたのは **アルコール＋Na／分子内脱水／1級・2級アルコールの酸化**（どれも基本）、
`★☆☆` が付いていたのは **メタノールの工業的製法（CO ＋ 2H₂）**。
★ **印は必ず化学反応式に付く**（定義・表・導出には付かない）＝ **反応式がこの教材の本体**。

⚠ **画面に出すときは記号だけにしない**（読者も同じところで迷う）。
「★★★ 必ず覚える／★★☆ 余裕があれば／★☆☆ 参考」のように**言葉を添える**。

### 18-4. ⚠ 素材が無いところ

**スライドがあるのは 52グループ中 22（42%）。** ⛔ **無いのは30** ——
フェノール・アニリン・分離・糖・アミノ酸・タンパク質・高分子・機器分析ほか。
⚠ **`4-1 芳香族炭化水素` は作りかけ**（26枚中5枚だけ生きていて、9枚目以降は `3-5 カルボン酸` の丸ごとコピー）。
⚠ **`3-8 エステルの構造決定` は骨組みだけ**（7枚・解答スライドが1枚も無い）。
⚠ **読み上げ原稿にもコピーの残骸がある**（`3-7 油脂` は21枚中17枚が別回のコピー。
`3-3 s12` は銀鏡反応の説明が丸ごと欠落）＝ **本文を正として読むこと。**

★ **ユーザー決定: 「とりあえず素材のある脂肪族について作成します」**（2026-09-09）。
---

## §19 ★★ 器を広げた（見出し・箇条書き・図・反応式・手書きの表・注意書き）と、アルカンのページ（2026-09-09・ref-wide レーン）

**発端は §18 と同じユーザーの申し立て:**

> **参考書の中身は全面的に修正したいです**
> **現状では、化学学習者が参照する参考書になっていない**

★ §16 が「本文の正を Markdown へ移し」、§17 が「その本文に URL を与えた」。
⚠ **どちらも器の中身の種類は増やしていない** —— 書けるのは**段落と、機械が組む4つの表／例題だけ**だった。
★★ **このレーンは「書ける形」を増やし、その証拠に1ページ書く。**

### 19-1. ★★ 決まった構造（ユーザー承認済み・2026-09-09）

> **検索して飛んでくるユーザーがページの該当箇所に飛べるのであれば、ページは分割しなくてもよいと考えます。**
> **検索から飛んできたけど自分が欲しい情報がどこにあるかわからない、という状況は避けたい**

| | 決定 | このレーンでの扱い |
|---|---|---|
| **ページの単位** | **1グループ = 1ページ**（52グループ → 52ページ） | ★ 分割しない |
| **節に アンカー** | 節へ直接着地できる | ★ `:::section` の `anchor:` |
| **面Aは2カラム** | 左に節の目次（追従・いまの節が光る）／右に本文 | ★ `.ref-toc` ＋ `.ref-layout` |
| **狭い面** | 目次を折りたたみに落とす | ★ **同じ markup**（`<details>`）を CSS で置き換える |
| **用語の索引** | ページを増やさずに節へ引く | ★ `/reference/terms/`（**機械で組む**） |
| **節の見出しの直下** | 「この節で分かること」1行 | ★ `:::section` の `lead:`（**必須**） |

### 19-2. ⚠ アンカーの綴りは1つにした（発注書との相違点その1）

★ 発注書の例は `/reference/alkene/#markovnikov`（素の語）。⚠ **採らなかった。**
**実際に出るのは `/reference/alkane/#ref-sec-substitution`**（`ref-sec-` を付ける）。

理由は2つ:

1. ⚠⚠ **面B（資料ペイン）はアプリの DOM の中**なので、素の `id="substitution"` は
   **`game.js` や `index.html` が持つ id と衝突しうる**。衝突しても画面は「それらしく」出て、
   `getElementById` が別のものを掴む形で**黙って壊れる**。
2. ⚠ **面Aだけ素の綴りにする**と、**同じ節に2つの id ができる**（生成器が焼くときに書き換える）。
   ★ §17-3 で「表を2か所で組まない」と決めたのと同じ理由で、**綴りも2つ持たない。**

★ 失うのは URL の見た目だけで、**節へ飛べること自体は1文字も変わらない**（ユーザーの要件はこちら）。

### 19-3. ★★ 広げた器 —— `:::` は 4 → 10 種

⚠ **`learn.js` の `renderBlock` に足すのが正**。★ `REF17` ③ が
`renderBlock.toString()` から `b.kind === '…'` を拾って書式（`tools/reference-md.js`）と突き合わせるので、
**片方だけ足すとその場で赤**になる（一覧を手で書かない仕掛けは §16-5 のまま）。

| kind | 書けるキー（★ = 必須） | 何が出るか |
|---|---|---|
| **`section`**★新 | ★`anchor` / ★`title` / ★`lead` | **節の見出し**。`id="ref-sec-<anchor>"` が付き、目次の行き先になる。`lead` は「この節で分かること」 |
| **`list`**★新 | `ordered` / ★`items` | 箇条書き。`ordered: true` で **番号つき**（手順 S1〜Sn 用） |
| **`figure`**★新 | ★`src` / ★`alt` / ★`caption` | 図。`src` は **`reference-img/` の中のファイル名だけ**（パスは書けない） |
| **`reaction`**★新 | ★`left` / ★`right` / `over` / `under` / ★`level` / `note` | **化学反応式**。左辺・矢印（上下に条件）・右辺・重要度の印 |
| **`table`**★新 | `caption` / ★`head` / ★`rows` | **手で書く表**（機械が行を作れないもの）。セルは ` \| ` で切る |
| **`callout`**★新 | ★`tone` / ★`text` | 注意の囲み。`tone` は `caution`（勘違いしやすい）／`memorize`（丸暗記でよい）／`skip`（覚えなくてよい） |
| `stageTable` | `variant` / ★`series` / ★`source` / ★`caption` | （既存）`stages.json` の系列から組む表 |
| `mechanismTable` | ★`source` / ★`caption` | （既存）`reactions.json` の全件から組む表 |
| `dehydrationTable` | ★`source` / ★`caption` | （既存）列挙器から組むアルコール↔アルケンの対応表 |
| `example` | ★`stageId` / ★`lead` / ★`note` | （既存）「▶ 組んでみる」 |

★ **書式の決めごとは §16-2 のまま1つも増えていない** —— 囲みの中は今までどおり `キー: 値`。
⚠ **足りなかったのは3つだけ**で、`tools/reference-md.js` に足した:

| ★ | 足したもの | なぜ |
|---|---|---|
| ① | **囲みの中の真偽値**（`bool:`） | `ordered: true` のため。前書きの `singleSource` と同じ扱い |
| ② | **配列の要素に本文の記法を通す**（`list` かつ `prose`） | `items:` と `rows:` の中で `**強調**` と `~下付き~` が使えないと、箇条書きも表も素の文しか書けない |
| ③ | **決まった語しか取らないキー**（`enum:`） | `level` と `tone`。⚠ **綴り違いを黙って通さない**（`★★`・`Caution` はその場で赤） |

#### ★★ 重要度の印は「反応式に付くもの」にした（発注書との相違点その2）

⚠ 発注書は「**重要度の印**」を独立した器として挙げていたが、**独立させなかった。**
★ 理由は §18-3 の実測そのもの —— **印は必ず化学反応式に付く**（定義・表・導出には1つも付いていない）。
⚠ **どこにでも付けられる印を作ると、素材に無い使い方が52ページぶん増える。**

★ **画面には記号だけを出さない**（ユーザー指示）。`learn.js` が言葉を添える:

| 書く | 出る |
|---|---|
| `level: ★★★` | **★★★ 必ず覚える** |
| `level: ★★☆` | **★★☆ 余裕があれば** |
| `level: ★☆☆` | **★☆☆ 参考** |

⚠⚠ **向きを取り違えないこと。`★★★` が基本。** ★ `REF19` が**言葉のほうから**見張る
（`★★★` の言葉に「必ず」が入っていること・3値がそろっていること）＝ **逆に入れたら赤**。

### 19-4. ★★ 図は PDF から焼いて `reference-img/` に置く

★ §18-2 のとおり **pptx からは取れない**（構造式は PowerPoint の図形）ので、**PDF を PyMuPDF で焼く**。

```
PDF: マイドライブ/化学/動画スライド/drive-download-20260908T201329Z-1-001/有機の基本2-2「アルカンの性質」.pdf
python: import fitz; d=fitz.open(p); d[6].get_pixmap(dpi=200, clip=fitz.Rect(x0,y0,x1,y1)).save(out)
```

⚠ **置き場所は `reference-img/`（リポジトリのルート直下）**。理由:

- ★ **`_config.yml` の `exclude:` に掛からない ＝ 配信される**（`reference-src/` は原稿なので除外されている）。
  ⚠ 図を `reference-src/` に置くと**面Aから読めない**（404）。
- ★ **面A（`/reference/<id>/`）と面B（`/assembler/`）の両方から同じ URL で読める**必要があるので、
  **ルート絶対の `/reference-img/<file>`** で出す。⚠ 相対にすると面Bで `/assembler/reference-img/` を探す。
- ⚠ **`.md` に書くのはファイル名だけ**（`src: alkane-substitution.png`）。
  **パスも `..` も書けない形**にしてあり、`/reference-img/` を付けるのは `learn.js` の1か所。

★ **容量**（★ このページの5枚で実測）: **必要な矩形を `clip` で切り、幅が 1200px になる dpi で焼く**と
**15 KB（式1本）〜 75 KB（導出の図・485px 高）／平均 40 KB**。**アルカン1ページぶん5枚で 200 KB。**
⚠ **全面を dpi=200 で焼くと 2667×1500 の1枚で 1 MB 近くなる** ＝ 切らずに置いてはいけない。
★ **52ページ × 4枚 ≒ 200 枚で 8 MB 前後**の見込み（リポジトリとしては許容範囲）。
⚠ **一度置いた図は差し替えても履歴に残る**ので、**切り出しの矩形は置く前に目で確かめること**
（★ このレーンでは5枚とも焼いてから見て、2枚は矩形を取り直した）。

⚠ **著作権**: スライドは**ユーザー自身の著作物**なので置いてよい（`git-history-leak-decision` の対象は他社の本）。

### 19-5. ⚠ 化学反応式 —— 画像に頼らず、文字で組んだ

★ 発注書の第1段の案は「**`:::figure`（式の画像）＋ 下に文字で式を書く**」。⚠ **採らなかった**（相違点その3）。

**理由**: ★ **式を2か所で持つことになる**（画像の中と、下の文字）。
⚠ 設計書がずっと戒めてきた「**同じものを2か所で組まない**」の、いちばん壊れやすい形
—— **画像を差し替えたのに文字を直し忘れる**は、機械で見張るのが難しい（画像の中は読めない）。

★ **採った形**: `:::reaction` は**文字だけ**で組む。

```
:::reaction
left: CH₄ ＋ Cl₂
over: 光
right: CH₃Cl ＋ HCl
level: ★★★
note: メタンの水素1個と、塩素分子の塩素1個が入れ替わっている。
:::
```

出るもの（`learn.js` が組む）:

```
┌─────────────────────────────────────────┐
│  ★★★ 必ず覚える                          │
│                    光                    │
│  CH₄ ＋ Cl₂  ──────────▶  CH₃Cl ＋ HCl   │
│  メタンの水素1個と、塩素分子の…            │
└─────────────────────────────────────────┘
```

| ★ | 何が良くなるか |
|---|---|
| ★★ | **検索にかかる**（画像だけだと本文0字）。スクリーンリーダーも読む |
| ★★ | **式が1か所にしかない** ＝ 直したら1回で両面に反映される |
| ★ | **面Aと面Bで同じ**（`renderBlock` 1本） |
| ★ | 図が要るとき（構造式で見せたい置換反応など）は **`:::figure` を隣に置く** ——
式そのものは重複せず、**図は「同じことを絵で見る」役**に徹する |

⚠ **やらなかったこと**: 分子を SVG で組んで矢印でつなぐ描画（`DESIGN_reaction_execution.md` の P12-5）。
★ **紙に書く構造式のレンダラが要る**（§18-2 のユーザー案「参考書生成用にアプリを作るのも手」）ので、
**このレーンの範囲を超える**。⚠ 作るなら **`:::reaction` の裏側だけを差し替えられる**形にしてある
（`.md` の書き方は変えずに済む）。

### 19-6. ★ 目次は `learn.js` が1本で組む（面Aが2本目を持たない）

⚠ **面Aの目次を生成器（`gen-reference-pages.mjs`）で組まない。**
★ `learn.js` に **`renderToc(page)`** を足し、**生成器はそれを呼んで焼く**（`renderBlock` と同じ扱い）。
⚠ **2本あると、`:::section` を足したときに面Aの目次だけ古くなる。**

| | 面A（`/reference/<id>/`） | 面B（資料ペイン 340px） |
|---|---|---|
| markup | ★ **同じ**（`<nav class="ref-toc"><details>…`） | ★ 同じ |
| 見え方 | 左に固定（`position: sticky`）・いまの節が光る | **上の折りたたみ**（既定で閉じる） |
| 切り替え | ★ **CSS の `@media (min-width: 1000px)` だけ** | 同じ |

⚠ `<details>` は閉じていると中身が描かれないので、**面Aでは幅が広いときに `open` を立てる3行**を
器のスクリプトに置いた（`REF19` が「目次の行 ＝ `:::section` の並び」を **HTML の側で**見るので、
開いているかに関係なく検査は通る）。

### 19-7. ★ 用語の索引 `/reference/terms/`（★ 機械で組む・手で並べない）

★ 見出し語の出どころは**2つだけ**:

1. **`:::section` の `title`**（全部・自動）
2. **`:::section` の `terms:`**（任意の並び。★ **その節に書く** ＝ 索引という別の台帳を作らない）

⚠ **`qa/questions.json` の 316 コードからは引かなかった**（発注書との相違点その4）。
★ 実物を見ると **qa の項目に「用語」の欄が無い** —— あるのは `knowledge`（1文の説明）と
`variants[].q`（問い）で、**どちらも見出し語にすると索引が文だらけになる**。
⚠ 機械で切り出せば作れるが、**切り出し方を決めるのは人**なので「機械で作った」ことにならない。
★ 代わりに **`codes` の重なりは `REF19` が見張る**（節の `terms` と qa の突き合わせは、
語彙が揃ってからでよい ＝ **使う人が決まる前に欄を作らない**・§16-10 と同じ判断）。

★ **並びは `localeCompare('ja')`。** ⚠ **読みを持っていないので、漢字はコードポイント順に落ちる**
（「置換反応」と「沸点」の前後は五十音ではない）。★ **52ページに広げるときに読みの欄が要るかを見直す**
——いま入れると**全ページで読みを書く義務**が生まれるので、**行数が増えてから決める**。

### 19-8. ★ 既存の `alkane-naming` との住み分け（発注書の確認事項）

★ **統合レーンの見立てどおり、別ページのままにした。**

| ページ | 何のページか | 持つコード |
|---|---|---|
| `alkane-naming`（既存） | **命名の規則**（型C）。C1〜C10 の名前と分子式の表 | `alkane-names` / `formula-alkane` |
| `alkane`（新） | **アルカンという物質のグループ**（型A）。構造・性質・反応・メタン | 残りの **10**（`alkane-shape` ほか） |

⚠⚠ **同じコードを2ページに書けない**（`gen-reference-pages.mjs` が止める・§17-5）。
★ **qa のグループ「アルカン」12件のうち `alkane-names` は命名ページの持ち物**なので、
新しいページは **11件から `alkane-names` を除いた 10件** を持つ。
★ **本文からは互いに1本ずつリンクする**（`:::section` の `lead` の中ではなく、段落で名指しする）。

### 19-9. ★ アルカンのページの節立て（型A・素材から起こした）

**素材: `有機の基本2-2「アルカンの性質」`（14枚）。** ★ 本文[スライドの文字]を正とし、
読み上げ原稿[N]は言い回しの材料にした（§18-4 の注意）。⚠ **この回の原稿にコピーの残骸は無かった**（実測）。

| 節 | anchor | 素材 | 型A のどれか |
|---|---|---|---|
| （前置き・節の外） | — | s3 | 要約先出し |
| どんな形をしているか | `structure` | s3 | 分類の定義 ＋ 図 ＋ ⚠ 注意の囲み |
| 一般式 CnH2n+2 はどこから出てくるか | `formula` | s5 | ★ 導出（メチレン基を足す2通り）＋ 図 |
| 主なアルカンと、常温での姿 | `list` | s4 | 数え上げ表（★ **手で書く表**）＋ 沸点・枝分かれ・溶解 |
| アルキル基 ―― アルカンから水素を1つ取ったもの | `alkyl` | s6 | 派生の導出 ＋ 箇条書き |
| アルカンの反応 ―― 置換反応 | `substitution` | s7 | ★ 反応式3本（★★★）＋ 図2枚 ＋ 番号つき箇条書き |
| メタン CH₄ | `methane` | s9 | 物質カード（★ 手で書く表）＋ 図 ＋ ★★☆ の製法 |

**40ブロック**（段落18・節6・図5・反応式4・注意3・表2・箇条書き2）。

⚠ **型Aの「判別まとめ表」「異性体の比較表」は置かなかった** —— **アルカンには判別の決め手が無い**
（反応しにくいことが性質なので、他と区別する呈色反応が無い）。★ **型は手引きであって、機械が強制するものではない**（発注書 §7）。

⚠⚠ **シクロアルカンの節も置かなかった**（発注書の見立てからの相違）。★ 素材（s8）にはあるが、
**qa では別グループ（`cyclo-props` / `cyclo-strain` の2件）**なので、「1グループ = 1ページ」の
決めに従えばそちらのページの持ちもの。**同じ内容を2ページに書くと、どちらが正か分からなくなる。**

⚠ **`org.ali.alkane-chlorination` は持てなかった。** ★ **既存の「反応の道すじ（反応機構）」ページが
先に持っている**（`gen-reference-pages.mjs` が「1つのコードが2ページに出てこない」で止めた）。
★ 本文からそちらを名指しして、**巻矢印のアニメへ流す**形にした ＝ **持ちコードは 11 → 10 件**。

⚠⚠ **練習問題は入れていない**（発注書の指示）。★ **入れるとどうなるか**は 19-11 に書いた。

### 19-9b. ⚠ 発注書の指定のうち、現物と違っていたもの

1. ⚠ **アンカーの綴り** —— 発注書の例は `/reference/alkene/#markovnikov`（素の語）。
   ★ **`#ref-sec-markovnikov` にした**（理由は 19-2）。**節へ飛べること自体は変わらない。**
2. ⚠⚠ **重要度の印を独立した器にしなかった** —— §18-3 の実測どおり**印は必ず反応式に付く**ので
   `:::reaction` の `level` にした（19-3）。**どこにでも付く印を作ると、素材に無い使い方が52ページぶん増える。**
3. ⚠⚠ **化学反応式を「図＋下に文字」にしなかった** —— **式を2か所で持つことになる**（19-5）。
   **文字だけで組み、図は「同じことを絵で見る」役に徹する。**
4. ⚠ **用語の索引を qa の316コードから引かなかった** —— **用語の欄が無い**（19-7）。
5. ⚠⚠ **発注書に無かった壁が1つあった: `REF5` の「行データの欄を持たない」。**
   ★ **手で書く表（発注書の器⑤）は、この検査に正面からぶつかる**（`rows` が禁止語）。
   ⚠ 名前を `body` などに変えれば通るが、**それは検査の字面を避けているだけ**なので、
   **検査のほうを開けて、代わりに `source`（この行がどこから来たか）を必須にした**（19-10 の②）。
6. ⚠ **`REF1` がページの名前を書き写していた**（「アルカンの命名」）。★ 6枚目を足したらそこだけ赤くなった
   ので、**`reference.json` の1番目の題を見る**形に直した ＝ **ページを足しても検査を直さなくてよい。**

### 19-10. ★★ 機械で守ること（新設 `REF19`）

| # | 何を守るか | どこで |
|---|---|---|
| ★★ ① | **新しい `:::` の種類が `learn.js` の `renderBlock` と一致** | `REF17` ③（既存の仕掛けにそのまま乗った ＝ **1行も足していない**） |
| ★★ ② | **節のアンカーが一意**（1ページの中で重複しない）・**目次の行き先が本文の節と1対1** | `REF19` |
| ★ ③ | **用語の索引の行き先が実在する節を指す** | `REF19`（面Aの `/reference/terms/` を読んで、`reference.json` の節と突き合わせる） |
| ★★ ④ | **図のファイルが実在し、面A・面Bの両方から同じ URL で読める** | `REF19`（`fetch` して 200 と `image/` を確かめる） |
| ★★ ⑤ | **重要度の印が3値のどれかで、`★★★` が基本の向き** | `REF19`（⚠ **言葉のほうから見る** ＝ 逆に入れたら赤） |
| ★ ⑥ | ハブ → 面A → 面B の鎖 | `REF18` ②（既存） |
| ★ ⑦ | **面Aの目次が `learn.js` の `renderToc` から焼いたもの** | `REF19`（焼いた目次と、その場でアプリに組ませた目次を突き合わせる ＝ `REF18` ① と同じ手） |
| ★★ ⑧ | **描く側（`learn.js`）と書式（`tools/reference-md.js`）の決めごとが割れていない** | `REF19`（⚠ **アプリ本体は `tools/` を読めない**ので、印の3値・図の置き場所・節の id の接頭辞は両側に在る。割れると黙って壊れる） |

#### ⚠⚠ `REF5` に穴を1つ開けた（手で書く表）

★ あの検査は **著作権の守り**（`DESIGN_review_pack1.md` §5-1「手打ちの表が構造上存在できなければ
転写事故は起きない」）で、`rows` は禁止語だった。
⚠ **素材がユーザー自身の講義スライドに変わり**（§18）、**機械が行を作れない表**が要るようになった
（常温での状態・構造異性体の数・気体の性質カード —— どれも `stages.json` にも `compounds.json` にも無い）。

| ★ | どう開けたか |
|---|---|
| ★ | **開けたのは `:::table` の `rows` だけ**。⚠ **他のブロックの `rows` は今までどおり赤**（否定対照 K） |
| ★★ | ⚠ **代わりに `source` を必須**にした ＝ **「その行がどこから来たか」を書かせる**（前書きの `why` と同じ考え・§1-2）。書けないなら、それはどこかから持ってきている |
| ★ | `TABLE_KINDS` に `table` を足した ＝ **検査のコメントに書いてある「種類が増えたらここに足す」の1行そのもの** |

★ **守りの本体は「行を手で書くな」ではなく「出どころの言えない行を書くな」**だと読み直した結果。

### 19-10b. ★★ 否定対照（★ 12件すべて、実際に赤くしてから戻した）

| | 壊した内容 | 何が赤くなったか |
|---|---|---|
| **A** | ⚠⚠ **`★★★` と `★☆☆` の言葉を入れ替える** | ★★ `REF19`「★★★ の言葉が『参考』になっている（★★★ は基本＝必ず覚える側）」＋ `REF18` も赤（焼いたものと食い違う） |
| **B** | 節のアンカーを2つ同じにする | `REF19`「節のアンカー『formula』が2回出てくる（# で飛ぶ先が定まらない）」 |
| **C** | 図のファイル名を実在しないものにする | `REF19`「図が読めない（HTTP 404）。`reference-img/` に在るか、`_config.yml` で除外していないか」 |
| **D** | 用語の索引の行き先を書き換える | `REF19`「#ref-sec-zzz が alkane に無い（節を消したか、アンカーを変えて焼き直し忘れたか）」 |
| **E** | ★ **焼いた目次から1行消す** | `REF19`「焼いた目次が、いまアプリが組む目次と違う」＝ **焼き直し忘れが捕まる** |
| **F** | 生成物の `:::table` から `source` を落とす | `REF5`「:::table が source（この行がどこから来たか）を持っていない」 |
| **G** | 原稿の `:::table` から `source` を消す | 生成器「:::table に『source』がありません」 |
| **H** | 図の `src` にパスを書く | 生成器「reference-img/ の中のファイル名だけを書きます（パスや .. は書けません）」 |
| **I** | `level` を `★★` にする | 生成器「`level` は ★★★ / ★★☆ / ★☆☆ のどれかです」 |
| **J** | 表のセルを1つ足す | 生成器「3 行目のセルが 6 個で、head の 5 列と違います」 |
| **K** | ⚠ **`:::callout` に `rows` を持たせる** | `REF5`「行データの欄 rows がある（手打ちの表は :::table だけ）」＝ **開けた穴が広がっていない** |
| **L** | `learn.js` から `:::callout` の描画を消す | `REF17`「書式が『callout』を許しているのに、renderBlock は描けない」＝ **既存の仕掛けが新しい器にも効く** |

### 19-11. ⚠ ユーザーに決めてもらう必要が残ったこと

1. ⚠⚠ **練習問題を入れるか**（§7-1 の「例題と問題の線」）。
   ★ **入れると起きること**: 素材のスライドは **14枚中5枚が練習**（s10〜s13 ＋ 表紙）なので、
   **入れれば1ページの3分の1が問題になる**。⚠ §7 の線引きは「**参考書は無料・例題つき／問題集は有料**」で、
   **例題（本編の一部として解いてみせる）と練習（自分で解く）は別枠**（`labels_A.md` の新設ラベル）。
   ★ **いまは「▶ 組んでみる」（`:::example`）と一問一答の埋め込みが例題の役**をしているので、
   **練習を足すと3つ目の層**になる。**足すなら `:::practice` を1つ新設して、有料の線をそこに引ける形にする。**
2. ⚠ **図の量**（19-4 の見込み 8 MB 前後）。★ 減らすなら **dpi を落とす**より **切り出す矩形を小さくする**ほうが効く。
3. ⚠ **索引の読み**（19-7）。**52ページに広げるときに決める**。
4. ⚠ **`ORDER.txt` の並び** —— §17-9 の申し送りのとおり、`alcohol` の2枚が離れている件は**まだ触っていない**。
5. ⚠ **ページどうしを結ぶリンクの器が無い。** ★ アルカンのページは
   「アルカンの命名」と「反応の道すじ」を**文の中で名指し**しているが、**押して飛べない**
   （本文に書けるのは強調と下付きだけで、生 HTML は禁止・§16-2）。
   ★ 足すなら **`:::link`（`to:` にページ id、`text:` に文）** を1つ。⚠ **行き先が実在するかを
   `REF19` で見られる**ので、素の `<a>` を本文に許すより安全。**52ページになると必ず要る。**
6. ⚠ **狭い資料ペイン（320px）では、横長の図が小さい**（3枚並びの構造式で幅 284px）。
   ⚠ **切れてはいない**（縮んでいるだけ）ので §12 の決定には反していないが、**読めるとは言いにくい**。
   ★ 手は2つ: **(a) 図に横スクロールを許す**（表と同じ扱い・`REF8` の物差しがそのまま使える）／
   **(b) 素材を1枚ずつに切り分ける**（3枚並びを3ファイルにする ＝ 容量は増えない）。
   ⚠ **(a) は「入らないものはスクロールで見せる」の既定路線**だが、**縦長の図まで横に伸びる**ので、
   **図ごとに決められる形（`:::figure` に `wide: true`）が要るかもしれない** —— **図が増えてから決める。**

---

## §20 ★★ ユーザーが自然に書いた形を受け取る（2026-09-09・ref-format レーン）

**発端は実測 —— ユーザーが `reference-src/alkane.md` を自分で校正したら、生成器が赤くなった。**

⚠⚠ **これは「ユーザーが書き方を間違えた」案件ではない。**
★★ **原稿の書式は「ユーザーが自分で校正できること」だけのために在る**（§16 の発端そのもの）ので、
**受け取れなかった生成器のほうを直す。** ⛔ **原稿を『正しい書式』に直して終わりにしない。**

★ **示し方: ユーザーの `reference-src/alkane.md` を 1文字も変えずに通す。**

### 20-1. ★ 何が受け取れなかったか（実測・7種）

| | ユーザーが書いた形 | 直す前 | ★ 直したあと |
|---|---|---|---|
| ① | 並びの `- ` が**列0**にある（`  - ` の2字下げでない） | ⛔ 赤 | ★ 通る（字下げは見ない） |
| ② | `terms:` と `- 用語` のあいだに**空行**が入る | ⛔ 赤 | ★ 通る（空行は読み飛ばす） |
| ③ | 囲みの見出しを**1行に詰める**（`:::section anchor: a title: 題 lead: …`） | ⛔ 赤 | ★ 通る（20-3） |
| ④ | 囲みを**同じ行の末尾の `:::`** で閉じる | ⛔ 赤 | ★ 通る |
| ⑤ | **型を書かない囲み**（`::: 囲みたい文 :::`） | ⛔ 赤 | ★ 通る（20-4） |
| ⑥ | `//` で始まる**自分あてのメモ** | ⛔ 赤（本文の段落として `/` が通り、そのまま画面に出る） | ★ 著者メモとして受ける（20-2） |
| ⑦ | **節の下の小見出し**を書きたい | ⛔ 器が無い | ★ `## タイトル`（20-5） |

⚠ **①②④は「書式が厳しかった」だけ**だが、**⑥は黙って画面に出る形**だった ＝ **赤より悪い。**

### 20-2. ★★ 著者メモ `//` —— 画面に出さないが、捨てもしない

ユーザーは原稿の中に **`//将来的にVsperの説明へのリンク`** のような**自分あての注文**を書く。

⚠⚠ **いちばん危ないのは「黙って捨てる」こと。**
★ メモは画面に出さないので、**出さないことと消えたことがユーザーから区別できない。**
★★ **だから、生成のたびに数と場所を言う**（`tools/gen-reference.mjs` の `reportMemos`）:

```
📝 まだ片付いていない著者メモ 15 件（画面には出しません）
   reference-src/alkane.md:41  将来的にVsperの説明へのリンク
   …
```

| ★ | 決めたこと |
|---|---|
| ★ | **`//` から行末まで**がメモ（行頭でも、行の途中でもよい） |
| ★ | `page.memos` に **行番号ごと**残す（⚠ `serialize` は `PAGE_KEYS` しか書かないので **`reference.json` には入らない**） |
| ★ | **生成は止めない** —— メモは「まだ片付いていない注文」であって、書式の誤りではない |
| ★★ | **`REF20` ② が「メモが本文に混ざっていないこと」を実データでも見る**（`reference.json` に `//` が1つも無い） |

### 20-3. ★ キーを1行に詰めて書ける（`:::section anchor: a title: 題 lead: …`）

⚠ **切れ目にしてよいのは「その囲みで書けるキー」＋「まだ出ていない」ものだけ**（`splitPacked`）。
★ 知らない語（`slides:` `s4:`）も、2回目の語も、**値の一部として素通しする** ——
そうしないと **`source: slides:有機の基本2-2…` が `slides` で切れる**（実データにある）。
⚠ キーと認めるのは **`キー:` の直後が空白か行末**のときだけ（`stages:アルカン（直鎖）` は切らない）。

### 20-4. ★ 型を書かない囲み `::: … :::` ＝ 補足

★ ユーザーは「**ちょっと囲みたい**」だけのことがある。⚠ **そのたびに `tone` を選ばせない。**
★ 行き先は **`callout` の `tone: note`**（`DEFAULT_FENCE`）。⚠ **既定を2か所に書かない** ——
書式が `{kind, tone}` を持ち、`learn.js` は **tone の語だけ**を持つ（`REF20` ③ が突き合わせる）。
★ 見せ方は**いちばん静か**にした（注意の黄・丸暗記の紫より目立たない）。
⚠ 中身は **1つの文だけ**（2行書いたら「種類を書くか、文を1つにするか」と断る）。

### 20-5. ★ 節の下の小見出し `## タイトル`（＝ `:::heading`）

★ それまで階層は `:::section` の1段だけで、**節の中を区切る手が無かった**。

| ⚠ | 決めたこと | なぜ |
|---|---|---|
| ⚠⚠ | **アンカーを持たない**（`id` を付けない） | ★ 綴りは **`#ref-sec-<anchor>` の1つだけ**、という §19-2 の決めを増やさない |
| ⚠⚠ | **目次（`renderToc`）に出さない** | ★ **節が「検索から着地する単位」**で、小見出しは「節の中の区切り」＝ 役が違う |
| ★ | 見え方は節より**一段小さく、地の色を持たない** | ⚠ 目次に出ないものが、節と同じ強さで並ばないように |
| ⚠ | **`# ` は書けない** | ★ ページの題は前書きの `title:` が持つ（2か所に持たない） |

### 20-6. ★★ 赤は「直し方」まで言う

⚠⚠ **受けないと決めた形が残るなら、その赤は *ユーザーが自分で直せる文言* でなければならない。**
★ **これは体裁ではなく、この仕事の目的そのもの** —— 読めない赤で止まったら、ユーザーは校正をやめる。

| 書いてしまった形 | 出る赤 |
|---|---|
| `zzz: よそもの` | 「知らないキー『zzz』があります（ここに書けるのは anchor / title / lead / terms）」 |
| 囲みの中の素の文 | 「『キー: 値』の形でない行があります … ★ 直し方: 行のあたまに『anchor: 』のようにキーを書きます。並びを書くなら次の行から『- 値』を1行ずつ」 |
| 同じキーが2回 | 「キー『title』が2回出てきます（同じ囲みの中で1回だけ書きます）」 |
| `:::` で閉じていない | 「★ 直し方: 囲みの終わりに『:::』だけの行を置くか、同じ行の末尾に『:::』を書きます」 |
| `terms:` の後に何も無い | 「★ 直し方: 1行の値なら『terms: ここに値』、並びなら次の行から『- 値』を1行ずつ」 |
| 本文の `# ` | 「ページの題は前書きの『title:』が持つので、本文に『# 』は書けません … ★ 直し方: 節にするなら :::section、節の下の小見出しなら『## 』」 |

★★ **`REF20` ⑤ が、これを機械で見張る** —— **赤のメッセージに「直し方」か「書ける語の一覧」が
必ず入っていること**を、6つの壊し方で確かめる。⚠ **文言が痩せたらテストが赤くなる。**

### 20-7. ★★ `:::link` —— **まだ書いていないページを指せること**が設計の急所

★ ユーザーが原稿に書いた注文のうち6件がリンクだった（`//一般式・同族体へのリンク` ほか）。

⚠⚠ **問題はリンクの作り方ではなく、行き先の半分がまだ存在しないこと。**
★ 52ページの計画のうち書けているのは **6枚**（`ORDER.txt`）。
⛔ **ふつうの `<a href>` にすると 404 が並ぶ。**
⛔ **`verify-release.js` の死にリンク検査に引っかからせて気づく形にもしない**
（★ **書式の側で「まだ無い」と言える**ようにするのがこのレーンの注文）。

#### (1) 書ける形は2つ。⚠ **どちらか片方だけ**

```
:::link
to: alkane-naming
text: ギリシャ語の接頭辞（ジ・トリ・テトラ…）を実際に使う場面は「アルカンの命名」で
:::

:::link
open: isomer
formula: C5H12
text: ペンタン C₅H₁₂ の異性体3つを自分で書き出してみる（沸点が違うのはこの3つ）
:::
```

| キー | 意味 |
|---|---|
| `to:` | ★ **参考書のページ id**。実在すればリンク、まだ無ければ**押せない「準備中」** |
| `open:` | ⚠⚠ **アプリの行き先。値は `game.js` の `OPEN_TARGETS` の名前そのもの** |
| `formula:` | ★ 受け口②（`?open=isomer&formula=`）の引数。⚠ **素の ASCII**（`C₅H₁₂` は受け口が読めない） |
| `text:` | 押す文。⚠ 「こちら」だけのリンクは赤（読み上げでも検索でも行き先が分からない） |

⚠⚠ **新しい URL の形を発明していない**（発注書の指示）。★ `REF21` ③ が
**`OPEN_TARGETS` と突き合わせて**、綴りだけの行き先が混ざらないようにしている。

#### (2) ★★ まだ無いページの名前をどう書くか ＝ `reference-src/PLANNED.txt`

★ **`.md` に「準備中」と書かせない。** 書くのは**行き先の id だけ**で、
**準備中かどうかは機械が決める** —— ★★ **ページを1枚書けば、そのページ宛のリンクは黙って生きる。**

⚠ ただしそれだけだと穴が1つ空く: **`to: alcohl`（綴り違い）が「まだ無いページ」と見分けが付かず、
永久に準備中で居座る**（★ この repo がいちばん嫌う「黙って壊れる」型）。

★★ **だから `reference-src/PLANNED.txt`**（`id␣␣表示名` を1行ずつ）:

| ★ | 効き方 |
|---|---|
| ★★ | **書いてよい行き先は「在るページ」＋「PLANNED.txt の id」だけ** ＝ 綴り違いはその場で赤 |
| ★★ | ⚠ **PLANNED.txt に、もう書けているページが残っていたら赤** ＝ 書いた日に消し忘れても止まる |
| ★ | **リンクが生きるかどうかは PLANNED.txt を見ていない**（見ているのは「ページが在るか」）＝ **表を消し忘れてもリンクは正しく生きる** |

⚠ **これは「52ページの計画表」ではない**（そちらは `PAGES_52.md`）。
★ ここに要るのは「**本文からもう名指ししてよい、まだ無いページ**」だけ。**いまは4件**
（`molecular-shape` / `general-formula` / `intermolecular-force` / `alcohol`）。

#### (3) ⚠⚠ 実測で1つ作り直した —— 「在るか」を描くときに調べてはいけない

★ 最初は `renderLink` が **`this.pages`**（＝ 読み込み済みのページ）を見て決めていた。

⚠⚠ **焼いたページだけ、在るページ宛まで「準備中」になった**（`REF18` が捕まえた）:

> 焼いたもの: …「アルカンの命名」で**準備中**
> アプリから: …「アルカンの命名」で

★ **原因**: 面Aの生成器（`gen-reference-pages.mjs`）は **`ReferenceBook` をページ読み込みなしで使う**
（`renderBlock` を呼ぶだけで `load()` を通らない）＝ **描く側は「そのページが在るか」を知らない。**

★★ **直し方: 判定を「読むとき」へ移した。**
`parsePage(text, where, {pages})` が **`soon: true` を生成物に焼き込み**、
**`renderBlock` はブロックだけを見て描く純粋な関数**に戻した。

| ⚠ | 波及 |
|---|---|
| ★ | `parsePage` の呼び手は **3か所**（`gen-reference.mjs` / `REF17` / `gen-reference-pages.mjs`）。**全部に ORDER.txt の id を渡す** |
| ⚠⚠ | **渡し忘れは黙って通さない** —— `to:` のリンクがあるのに一覧が無ければ**その場で赤**（否定対照 L6）。★ 黙って全部「準備中」にするより、止まるほうがよい |
| ⚠ | `tools/gen-reference-pages.mjs`（ref-read レーンの持ちもの）は**この呼び出し1行だけ**直した。**CSS と組版は1バイトも触っていない** |

★ **教訓**: ⚠⚠ **面Aは「アプリの一部を、状態を持たせずに借りて焼く」形**なので、
**`renderBlock` に「いまアプリが何を読み込んでいるか」を見せてはいけない。**
★ 見せると、**面Bでは正しく、面Aだけ間違っている**という一番見つけにくい形で壊れる。

#### (4) ⚠⚠ `REF5` に2つ目の穴を開けた（`:::link` の `formula`）

★ `REF5` の `BANNED` は**著作権の守り**で、`formula` は「分子式の表を写す」を構造上できなくするためのもの。
⚠ だが `:::link` の `formula` は**表の行ではなく、受け口の引数** ——
`:::example` の `stageId`（禁じていない）と同じ「どの練習を開くか」の指し示し。

⚠ **名前を変えて逃げない**（§19-9b の5番の教訓。URL の欄名が `formula` なので、
別名にすると「検査の字面を避けただけ」になり、しかも何の値か読めなくなる）。

★★ **代わりの縛りは `REF21`: その式で *実際に書き出し練習が始まる* ことを確かめる**
（`ip.startFromFormula` → `ip.active && problem.total >= 2`）。
★ **＝ 事実の転写ではなく「生きている行き先」であることを機械で示す** ——
`rows` に `source` を必須にしたのと同じ考え。
⚠ おまけに「**押しても何も起きないリンク**」（受け口が受けない式）も、ここで一緒に止まる。

#### (5) ★ 面Aと面Bで同じ綴り・違う動き

| | 面A（`/reference/<id>/`） | 面B（資料ペイン） |
|---|---|---|
| markup | ★ **同じ `<a href>`** | ★ 同じ |
| 行き先 | `/reference/<id>/`・`/assembler/?open=…`（**ルート絶対**） | 同じ |
| 押したとき | ふつうの遷移 | ★ **その場で開く**（`this.open(id)` / `applyOpenParam`）＝ アプリを出ない |
| まだ無いページ | ★ `<span>`（**`<a>` を作らない**） | 同じ |

⚠⚠ **`<button>` にしなかったのは面Aのため** —— 生成器は知らない押しものを見つけると
**止まる**（静的なページに死んだボタンを残さない約束）。★ `<a href>` なら**そのまま焼ける**し、
別タブで開くことも、右クリックでコピーすることもできる。
⚠ 面Bの横取りは **修飾キー付きのクリックを邪魔しない**（`ctrl`/`cmd`/`shift`/中クリック）。

⚠ **`utm_` は付けていない。** 生成器が焼く他の内部リンクは
`utm_source=reference&utm_medium=internal&utm_campaign=<page id>` を付けているが、
★ **`renderBlock` はページ id を受け取らない**（引数はブロック1つ・§10-2 の原則）ので、ここからは書けない。
⚠ **面A → アプリ の流入がこの1種類だけ計測から漏れる。** ★ 直すなら生成器側の変換に足すのが筋
（＝ ref-read レーンの持ちもの）。

### 20-8. ★ 小見出しを置いた —— と、⚠⚠ **素の1行を黙って段落にしない**

★ 器（`## タイトル` ＝ `:::heading`）は 20-5 で作ってある。**このレーンで実際に3つ置いた:**

| どこ | 見出し | 出どころ |
|---|---|---|
| 沸点の節 | **アルカンの常温での状態** | ⚠⚠ **ユーザーが素の1行で書いていたもの**（下） |
| 沸点の節 | 枝分かれすると沸点が下がる（発展） | メモ「ここは発展的・見出しつけたい」 |
| 置換の節 | 電子対はどう動いているのか（発展） | メモ「ここは発展」 |

★ **目次（`renderToc`）には出していない**（20-5 の決めのまま）。★ **面Aの目次は6節のまま**で、
`:::section` の並びと1対1（`REF19` ②）。⚠ **小見出しを足しても目次は増えない** ＝
「検索から着地する単位」は節だけ、という約束が保たれている。

#### ⚠⚠ 見つけた穴: 見出しのつもりの1行が、**赤にならずに段落として画面に出ていた**

★ ユーザーは `アルカンの常温での状態` と**1行だけ**書いていた。
⚠⚠ **これは書式としては通ってしまう**（1段落 = 1行なので、立派な段落に見える）＝
**赤も出ず、画面には本文として出る。** ★ **赤より悪い**（気づく手がかりが無い）。

★★ **線は実測で引いた:**

| | 実測（6ページ・`kind:"text"` 45段落） |
|---|--:|
| **句点（`。！？`）を1つも持たない段落** | ★★ **1**（＝ その行だけ） |
| **次に短い段落** | ★ **55 字** |
| いちばん短い段落（その行を除く） | 55 字 |

★ **＝ 「句点が無くて 30 字以下」で切っても、ふつうの段落には1つも当たらない。**

⚠⚠ **拾って勝手に見出しにはしない。** ★ **どちらのつもりだったかは書いた人しか知らない**
（`:::section` にしたいのかもしれない ＝ 目次に出るかどうかが変わる）。
★★ **読める言葉で断って、直し方を2通り見せる**（20-6 の約束どおり）:

```
❌ reference-src/alkane.md: 句点で終わらない短い1行があります（見出しのつもりですか？）
    → アルカンの常温での状態
    ★ 直し方は2通り: 見出しにするなら行のあたまに「## 」を付けます。
      本文の段落のつもりなら、文として書いて「。」で終えてください。
      （節の見出しにするなら :::section。そちらは目次に出ます）
```

⚠ **逃げ道は要らない** —— 段落のつもりなら句点を1つ足せば通るし、**句点があれば長さは見ない**
（`短い。` は通る）。★ `REF20` が**この赤と、ふつうの段落が当たらないこと**の両方を見る。

### 20-9. ★ `:::table` の列ごとの寄せ（`align:`）

★ ユーザーのメモ「**異性体の数は右揃え、その他は中央ぞろえ**」への答え。

```
head:
- n
- 名称
- 分子式
- 常温での状態
- 構造異性体
align: center | center | center | center | right
rows:
- 1 | メタン | CH₄ | 気体 | 1
```

| ★ | 決めたこと | なぜ |
|---|---|---|
| ★ | **セルと同じ ` \| ` で区切って1行**（列ごとに `- ` の並びにしない） | ★ **head と縦に見比べられる**／⚠ 5列の表で5行増えない |
| ⚠⚠ | **head と同じ数だけ書く**（足りない・多いは赤） | ⛔ **黙って詰めると「1列ずれた寄せ」が それらしく出る** —— 列の数が合わない `rows` を止めるのと同じ理由 |
| ⚠ | **書ける語は `left` / `center` / `right` の3つだけ** | ⚠ 日本語の「右」も受けると**同じことを2通りで書ける** ＝ どちらが正かが原稿ごとに割れる。★ 赤が必ず3語を並べる |
| ★★ | **書かなければ何も足さない** | ⚠ 既定の見え方は `style.css` の持ちもの。「既定はこれ」を `.md` にも `learn.js` にも**書き写さない** |
| ★ | 一覧を持つのは**書式（`tools/reference-md.js`）だけ** | ★ `learn.js` は値をそのまま `style.textAlign` に渡す ＝ **2か所に持たない**（`REF19` ⑧ の論点がここでは立たない） |

★ `REF22` が見るのは「寄っているか」ではなく **「1列ずれていないか」** ——
**原稿の `align` と、描いた `<th>`/`<td>` の `style.textAlign` を1列ずつ突き合わせる**
＝ ⚠ 書式だけ通って画面が変わらない、も捕まる。

**否定対照（すべて実際に赤くして戻した）:**

| | 壊した内容 | 何が赤くなったか |
|---|---|---|
| **T1** | `align` を1個少なく書く | 生成器「align が 1 個で、head の 2 列と違います（セルと同じ「 \| 」で区切って、列の数だけ書きます）」 |
| **T2** | `align` を多く書く | 同上（3 個 / 2 列） |
| **T3** | 日本語で `中央 \| 右` | 生成器「書けるのは left / center / right です（いまは「中央」）」 |
| **T4** | `centre`（英の綴り違い） | 同上（いまは「centre」） |

### 20-10. ★★ 図を1枚ずつに割った（＝ 著者メモ4件への答え）

★ ユーザーは原稿の中に **「ここにメタンの正四面体構造・電子式」「エタンの単結合が回転するず」**
のように、**図を置く場所と中身**をメモで指していた。⚠ **置き場所を決めたのはユーザー**で、
このレーンがしたのは **その場所に入る図を焼くこと**だけ。

#### ⚠⚠ 何が悪かったか —— 「スライドの横一列を、そのまま帯で切っていた」

★ §19-4 で図を焼いたときは、**スライドの1行＝1枚**で切っていた。
⚠ スライドは 960×540pt の**横長**なので、3つ並んだ図をまとめて切ると **5:1 や 14:1 の帯**になる。
⚠⚠ **本文は縦に細い**（面A で 572px・面B の資料ペインは 340px）ので、
`width: 100%` で入れると**帯は高さが数十 px まで潰れる**。

| 割る前 | 画素 | 比 | 572px 幅での高さ | ★ 割ったあと | 画素 | 比 | 高さ |
|---|--:|--:|--:|---|--:|--:|--:|
| `alkane-chlorination-chain.png` | 1207×86 | **14.0:1** | ⛔ **41px** | `…-chain-1.png` | 1152×170 | 6.8:1 | 84px |
| | | | | `…-chain-2.png` | 1150×149 | 7.7:1 | 74px |
| `alkane-structure.png` | 1223×216 | 5.7:1 | ⛔ 101px | `methane-tetrahedron.png` | 1149×732 | 1.6:1 | ★ 364px |
| | | | | `methane-electron-pair.png` | 1150×473 | 2.4:1 | ★ 235px |
| | | | | `ethane-rotation.png` | 1152×793 | 1.5:1 | ★ 394px |
| `alkane-formula-derivation.png` | 1204×471 | 2.6:1 | 224px | `alkane-methylene-chain.png` | 1152×837 | 1.4:1 | ★ 416px |
| （⚠ **参照ゼロだった**） | | | | `alkane-chain-branch.png` | 1150×833 | 1.4:1 | ★ 414px |

★★ **割ると縦に長くなるのではない。** ⚠ よくある誤解なので書いておく ——
**1枚あたりの幅が減るぶん、同じ字が大きく出る**。
連続置換の帯は `572 ÷ 1250 = 0.46 倍`で出ていたのが、割って `572 ÷ 684 = 0.84 倍`まで戻った
（＝ 橙の「クロロメタン」が 7px 相当から 15px 相当になった）。

★ **実測（アルカンのページの全部）: 5枚 192 KB → 9枚 306 KB**（+114 KB・1枚の平均は 38 KB → 35 KB）。
⚠⚠ **§19-4 の見込み「52ページ × 4枚 ≒ 200 枚で 8 MB」は外れる** —— **1ページ 9 枚**なら
**468 枚 ≒ 16 MB**。★ **1枚の平均は割っても変わらない**（幅を 1150px に揃えて焼くので、
割ったぶん高さが減って相殺する）＝ **効いているのは枚数だけ。**
⚠ **容量をどこまで許すかは、ユーザーに決めてもらう項目のまま**（§19-11 の2番）。

#### ★ 割らなかったもの / 落としたもの

| | どうしたか | なぜ |
|---|---|---|
| `alkane-substitution.png`（1196×246・4.9:1・118px） | ⚠ **割っていない** | ★ **1本の反応式**なので、左辺と右辺を別の図にすると式でなくなる。⚠ 床（10:1）は満たしている |
| `methane-electron-dot.png`（1199×560・2.1:1・267px） | ★ そのまま | もともと1つの図 |
| s5 の右にあった散文（「鎖を伸ばす場合も…ので一般式は C~n~H~2n+2~」） | ⚠ **焼くときに落とした** | ★ **本文の段落が同じことを書いている** ＝ §19-5 の「同じものを2か所で持たない」。落とし方は PyMuPDF の redaction（⚠ **PDF 原本は書き換えていない**。メモリ上の複製だけ） |

#### ★★ 機械で守ること（`gen-reference.mjs` に3つ）

⚠⚠ **ディレクトリを読めるのは node 側だけ**なので、`:::link` の行き先（§20-7）と同じ場所に置いた。
★ ブラウザ側の `REF19` ④ は「**本文が名指しした図が HTTP で取れるか**」＝ **向きが逆**で、すき間が2つ空いていた。

| # | 何を守るか | ★ なぜ要ったか |
|---|---|---|
| ① | 名指しした図が `reference-img/` に在る | ★ `REF19` ④ でも捕まるが、**生成のほうが先**なので赤が早く読める |
| ★★ ② | **どのページからも参照されていない図が残っていない** | ⚠⚠ **実際に1枚あった** —— `alkane-formula-derivation.png`（73 KB）は §19-4 で焼かれたのに、**本文から一度も名指しされないまま置かれていた**。⚠ **図は一度置くと履歴に残る**（§19-4）ので、黙って増えるのがいちばん困る |
| ★★ ③ | **平たすぎる図が無い**（幅 ÷ 高さ ≦ **10:1**） | ★ このレーンの症状そのもの。⚠ 割り忘れは**赤も出ず、ただ読めない画面**になる（§20-8 の「素の1行」と同じ、赤より悪い型） |

⚠⚠ **③ の 10:1 は「測って決まった値」ではない**（§12-5 の戒め）。★ **線の引き方はこう**:
**読めなかったのが 14:1（高さ 41px）／割ったあとに残ったいちばん平たい図が 7.7:1（74px）**
＝ **落としたものより上、残したものより下**に引いた1本。**図が増えたら引き直してよい。**

★ 大きさは **PNG の IHDR を16バイト読んで**取る（⚠ 画像ライブラリを足さない）。PNG 以外は測らない。

**否定対照（★ 3件とも実際に赤くしてから戻し、戻したあと `--check` が緑になることも確かめた）:**

| | 壊した内容 | 何が赤くなったか |
|---|---|---|
| **N1** | `ethane-rotation.png` を退避する | 生成器「`:::figure` の『ethane-rotation.png』が `reference-img/` にありません」＋ **いま在る図の一覧** |
| **N2** | 参照されない `zzz-unused.png` を置く | 生成器「参照されていない図が 1 枚あります」＋ **直し方2通り**（使うなら `src` に書く／使わないなら消す） |
| **N3** | `…-chain-1.png` を割る前の帯（1207×86）に差し替える | 生成器「平たすぎます（1207×86 ＝ 14.0:1）… 高さ 41px にしかならず」＋ **床の値** |

### 20-11. ★★ 赤は「どの行か」も言う

⚠⚠ **§20-6 の続き。** 「直し方まで言う」は入っていたが、**どこを直すかが入っていなかった。**

★ 実測でそうと分かった —— ユーザーの原稿を1文字も直さずに通そうとしたら、赤はこう出た:

```
❌ reference-src/alkane.md: 句点で終わらない短い1行があります（見出しのつもりですか？）
    → アルカンの常温での状態
```

⚠⚠ **`alkane.md` は 300 行ある。** ★ 引用してある文で検索すれば見つかるが、
**探すのはユーザーの仕事**になっており、**同じ文が2か所にある原稿では当てられない。**

★ **直した形**（`tools/reference-md.js` の `parseBody` に本文の先頭が何行目かを渡すだけ）:

```
❌ reference-src/alkane.md:118: 句点で終わらない短い1行があります（見出しのつもりですか？）
```

| ★ | 決めたこと | なぜ |
|---|---|---|
| ★ | **書き方は `ファイル:行:` にそろえた** | ⚠ 著者メモの報告（`reference-src/alkane.md:169`）と**同じ形** ＝ 覚えるものを増やさない |
| ★ | **囲みの中の赤は、囲みの1行目（`:::種類` の行）を指す** | ⚠ 中の何行目かまで言うと**行き先が2つ**になる（囲みは1つのかたまりとして直す） |
| ⚠ | **前書きの赤には行番号を付けていない** | ★ 前書きは**必ずファイルの先頭**（`---` から `---` まで）＝ 探す手間が無い |

⚠⚠ **見るのは「行番号が出るか」ではなく「その行番号が本当にその行か」。**
★ **ずれた行番号は、無いより悪い**（ユーザーを別の行へ直しに行かせる）。
★ `REF20` ⑥ が、**原稿の中から当該の行を探して番号を計算し、赤の中の番号と突き合わせる**
（⚠ テストの側に「18行目」と書き写さない ＝ 見本を1行足しただけでテストが嘘になる形にしない）。

#### ★ ついでに、赤を **51本ぜんぶ読み直した**

⚠ **直し方（または書ける語の一覧）が入っていなかったのは1本だけ**だった:

> `:::figure の alt（画像が出ないときに読まれる文）が短すぎます: 「図」`

★ **「短すぎます」と言われても、何を書けばよいかが書いていない。** 直して、
**何が描いてあるかを1文で書くこと**と、**`caption` と同じ文にしないこと**（読み上げが二重になる）を足した。
★ `REF20` ⑤ の壊し方に**この形を1つ足した**（＝ 文言が痩せたら赤くなる。否定対照 N-C で確認）。

### 20-12. ⚠ 発注書の見立てのうち、測ったら違っていたもの

1. ⚠ **著者メモは「4件」ではなく 5 件だった。** ★ 5件目は
   `//ここはアルカンの性質・配置を考える`（**図の注文ではない** ―― 性質の節の並べ方への注文）。
   ★★ **これは機械にも他人にも決められない**ので、**片付けずに残した**（`--check` が毎回言う）。
2. ⚠ **572px 幅での高さが、発注書の表と少し違った**（`alkane-structure.png` は 113px ではなく **101px**、
   `alkane-substitution.png` は 129px ではなく **118px**）。★ **症状の向きは同じ**（潰れている）。
3. ⚠ **`alkane-formula-derivation.png` は「割る対象」ではなく「参照ゼロの図」だった**（20-10 ②）。
   ★ 発注書の表に無かったのは、**本文から名指しされていないので目に入らなかった**ため。
4. ⚠ **原稿の改行コードが、ユーザーの手元と repo で違っていた** —— ユーザーの `alkane.md` は **CRLF**、
   前のレーンが直したあとは **LF**（他の `reference-src/*.md` は全部 CRLF）。
   ★ **`core.autocrlf = true` なので git は正規化して比べる ＝ 差分には出ない**し、
   parser も `\r\n` を落としている（`tools/reference-md.js:261`）ので **実害は無い**。
   ⚠ ただし「ユーザーが Windows のエディタで保存し直すと改行が変わる」ことは頭に置くこと。

---

## §21 ★★ 面Aを読める見た目にした（2026-09-09〜10・ref-read レーン）

**発端はユーザーの申し立て**（公開ページ `https://chem.schoollenz.com/reference/alkane/` を見て）:

> **デザインを変更したい（黒背景は読みにくいのでは？）**
> **漠然とした印象ですが、わかりにくいというか、とっかかりにくい感じがしました**

⚠ **申し立ては2つある。**「黒背景」は**言われたとおりの原因**で、
「とっかかりにくい」は**原因を言っていない症状**。★ **測って分けた。**

### 21-1. ★★ 実測 —— 「黒背景」は主因ではなかったが、無実でもなかった

★ **測り方**: 焼いた面Aを playwright で開き、`getComputedStyle` で文字色と
**祖先をさかのぼって重ねた実効の地色**を取り、WCAG のコントラスト比を全要素ぶん出した。

| | 直す前（暗い地） | 直したあと（明るい地） |
|---|--:|--:|
| **本文** | `#f5f6fa` on `#0a0c10` ＝ **18.12:1** | `#20242b` on `#f5f3ef` ＝ **14.05:1** |
| ⚠⚠ **いちばん低い文字** | ⚠⚠ **3.35:1**（`.ref-note` / 太字の注記） | ★ **4.56:1** |
| ⚠ **AA（4.5:1）に届かない種類** | ⚠⚠ **4種**（`.ref-note` `.ref-cap` `.ref-figure-cap` と注記の `<b>`） | ★★ **0種** |

★★ **＝ 暗い地そのものが「読めない」わけではなかった**（本文は 18:1 ある）。
⚠ **ただし添え物の4種が AA を割っていた**のは事実で、**ユーザーの体感はここを拾っている可能性が高い。**
⚠ **18:1 は高すぎもする** —— 面Aは1ページ **6,400px ＝ 7画面ぶん**を続けて読む場所で、
**暗い地に白文字を長く読むと文字がにじむ**（ハレーション）。**資料ペイン（面B）は数段落ずつ読む場所**なので、
★★ **同じ本文でも要件が違う ＝ 面Aだけ明るい地にして、面Bは暗いまま**にした。

⚠ **明るい地に移すと、もとのアクセント `#00f2fe` は 1.25:1 で読めない。**
★ 明るい地用の `--accent: #0d6c78`（**5.52:1**）を別に置き、
**ブランドの水色は `--brand` として帯（ヘッダー・フッター）の中だけに残した**
＝ **アプリやハブから来た人の「同じサイトだ」の手がかりを消していない。**

### 21-2. ★★★ 「とっかかりにくい」の実体は、配色ではなく **見出しが立っていないこと**

★ **測った3つ:**

| | 直す前 | 直したあと |
|---|--:|--:|
| ⚠⚠ **見出し ÷ 本文** | ⚠⚠ **19px / 15.5px ＝ 1.23倍** | ★ **23px / 17px ＝ 1.35倍** |
| ⚠⚠ **章の切れ目** | ⚠⚠ **無い**（`.ref-sec-h` の下線は白の 8% ＝ ほぼ見えない） | ★★ **節の上に 2px の線 ＋ 上下 74px の余白** |
| **見出しの色** | ⚠ **本文と同じ `#f5f6fa`** | 本文と同じ `#20242b`（★ **大きさと線で立てる**） |

★★ **＝ 7画面ぶんのあいだ、ほぼ同じ濃さ・同じ大きさの字が続いていた。**
⚠ **「どこから読めばいいか」の手がかりが、紙面に1つも無かった。**

★ **本文の組みは、測ったら *もう範囲の中* だった**（⚠ 発注の見立てと違った点）:

| | 直す前 | 直したあと |
|---|--:|--:|
| 本文の幅 | 572px | 680px |
| 文字の大きさ | 15.5px | 17px |
| ★ **1行の字数**（欄幅 ÷ 字の大きさ） | **36.9字** | **40.0字** |
| ★★ **1行の字数**（⚠ **数えた実測**・下の注） | **32.8字** | **33.6字** |
| 行送り（倍率） | **1.95**（★ **変えていない**） | **1.95** |
| 行送り（実寸） | 30.2px | 33.2px |

⚠⚠ **上の2行は別のものを測っている。** 「欄幅 ÷ 字の大きさ」は**全角だけが詰まった理想値**で、
★ **実際の本文には `CH₄`・`2n+2`・約物・強調の切れ目**が混ざるので、**行末が揃わず 3〜4字ぶん短くなる。**
★★ **数えたほう（段落12本ぶんの 文字数 ÷ 行数）を正とする** —— ⚠ **前のレーンの表は理想値だけを載せていて、
「35〜45 の範囲の中」という判定もその理想値に対するものだった**（★ **数えた 32.8字 は 35 をわずかに下回る**）。
⚠ **ただし結論は変わらない**（下の 999px の 46.9字 とは桁が違う）。

★★ **＝ 「読みにくい」の主因は行長ではない。** 大きさを上げ、**字数を保つために器のほうを広げた。**

⚠⚠ **ただし1か所だけ本当に外れていた** —— **1カラムのとき（<1000px）に器が 984px のままだった**
（★ **数えた実測**。括弧内は読み欄の実寸）:

| 窓の幅 | 直す前の1行 | 直したあと |
|--:|--:|--:|
| 768px | **42.3字**（736px） | 33.6字（680px） |
| 900px | ⚠ **46.9字**（828px） | 33.6字（680px） |
| 999px | ⚠⚠ **46.9字**（828px） | 33.6字（680px） |

⚠ **900px と 999px が同じ 46.9字なのは、`main.wrap` が 860px で頭打ちだったから**
（⚠ **前のレーンの表は 51.1 / 56.0字 と書いていて、幅が伸び続ける形になっていた ＝ 誤り**）。
★ **外れの大きさは変わらない**（1カラムの3幅とも **40字を超えていた**）。

★ **`@media (max-width:999px){main.wrap{max-width:712px}}` の1行で塞いだ**
（712 = 読み欄 680 + 左右の余白 16×2）。⚠ **1000px 以上は grid が 680px に決めているので触らない。**

### 21-3. ★★ 「いま何章目か」—— 印が **7,000px のうち一瞬しか出ていなかった**

⚠⚠ **これは配色とは無関係の、もとからあった不具合**（§17 の面Aと同時に入ったもの）。

★ **測り方**: アルカンのページを 100px 刻みでなめ、目次の `aria-current="true"` を数えた。

| | 印なしの地点 |
|---|--:|
| ⚠⚠ **直す前** | ⚠⚠ **33 / 55 地点（60%）** |
| ★ **直したあと** | ★★ **0 / 62 地点**（どの地点でも**ちょうど1件**） |

**原因**: `IntersectionObserver` に `rootMargin:'0px 0px -70% 0px'` を与え、
**「画面の上30%の帯に *いま重なっている* 節」**を現在地にしていた。
⚠ `.ref-sec` は**見出しだけの数十pxの箱**なので、帯を通り抜けた瞬間にどの節も重ならなくなる。

★★ **直し: 問いを「どの節が見えているか」から「どの節を通り過ぎたか」に変えた**
（帯の線より上に来た**最後の**節）。★ **節の高さに依らない** ＝ 見出しが1行でも図が10枚でも同じように効く。

⚠ **狭い面では目次が畳まれている** ＝ 印は目次を開かないと見えない ＝ **答えになっていない。**
★★ **そこで畳んだ目次の札を上に貼り付け（`position:sticky`）、札に「いま：<節名>」を出す。**
- ★ **札の高さは 88px で固定**（節名は1行に留めて `text-overflow:ellipsis`）。
  ⚠ **折り返させると、スクロールのたびに札の高さが変わって下の本文が飛ぶ**（実測して1行に決めた）。
- ⚠ **375px 幅の面Aは 9,477px ＝ 11.6画面**ある。**貼り付けないと、現在地の手がかりが
  ページの先頭に置いてきぼりになる ＝ 見に行けない ＝ 無いのと同じ。**
- ⚠ **目次の *行* は 1行も足していない**（`learn.js` の `renderToc` のまま。§19-6）。
  **足したのは札の「いま」の1つだけ ＝ 見え方の話。**

### 21-4. ★ 3つの調子（`caution` / `memorize` / `skip`）は、暗い地では **区別できていなかった**

⚠ **直す前は3つとも地が `rgba(255,255,255,.03)` で同じ**、違うのは**左の線の色だけ**だった。

| 調子 | 直したあと（地 / 左の線 / 札の字） |
|---|---|
| ⚠ `caution`（勘違いしやすい） | `#fdf4e0` / `#c07a00` / `#8a5600`（**5.63:1**） |
| ★ `memorize`（覚える） | `#f3ecfa` / `#7c4dab` / `#5f3585` |
| `skip`（覚えなくてよい） | `#eeece8` / `#9aa0a8` |

★ **地・線・札の字の3つを動かした** ＝ **色を1つしか見ない人にも、地の明暗で3つが分かれる。**

### 21-5. ★★ 明るい地への「読み替え漏れ」を機械で見る（既にあった仕掛けをそのまま使った）

⚠ **面Aの本文の色は `assembler/style.css` から切り出したもの**（§17-3 ③）＝ **暗い地の上に載る前提。**
★ `checkLightCoverage` が、切り出した規則の中の**暗い地を前提にした色のリテラルを数え上げ**、
`LIGHT_OVERRIDE`（読み替えた）か `LIGHT_KEEP`（明るい地でもよい理由つき）に無ければ**生成器を赤にする。**

⚠ **`LIGHT_OVERRIDE` は「載せただけ」では通らない** —— `LIGHT_CSS` がその class を実際に触っていること。
★ **＝ 名簿だけ増やして直し忘れる、を塞いでいる。**

⚠⚠ **この守りに穴が1つ空いていた**（2026-09-10・**否定対照を実際に走らせて見つけた**）。
★ 「触っているか」を **`light.indexOf('.' + c)` の部分一致**で見ていたので、
**`.ref-rx` の読み替えを丸ごと消しても、`.ref-rx-lv1` が居るせいで緑のまま**だった
（＝ **化学反応式の囲みが白地に白いカードで出ても、機械は何も言わない**）。
★★ **丸ごとの一致に直した**（`new RegExp('\\.' + c + '(?![A-Za-z0-9_-])')`）。
⚠ 同じ形の取りこぼしが `.ref-table`（→ `.ref-table-wrap`）・`.ref-callout`（→ `.ref-callout-caution`）にもあった
（★ **どちらも実際には読み替えてあったので事故にはなっていない**が、**見張りは効いていなかった**）。
★ **直したあとに同じ否定対照を走らせると赤になる**（20-8）。

### 21-6. ★★ 面B（アプリの資料ペイン）にも入れたもの・入れなかったもの

| | 面A | 面B |
|---|---|---|
| 地 | ★ **明るい** | ⚠ **暗いまま**（触って遊ぶ画面の一部。そこだけ白い板を貼るとアプリが壊れて見える） |
| ★★ **章の切れ目** | ★ 節の上に 2px の線 | ★ **節の上に 1px の線**（新設） |
| ★★ **見出し ÷ 本文** | 23 / 17 ＝ **1.35倍** | ⚠ **14 / 13 ＝ 1.08倍** → ★ **17.5 / 13 ＝ 1.35倍** |
| ⚠⚠ **最初の節** | ⚠ **実際には線が引かれる**（下） | ⚠ **同じく引かれる** |
| 本文の大きさ | 17px | ⚠ **13px のまま**（ペインは 340px 幅。上げると1行 20字を割る） |

⚠⚠ **「最初の節には線を引かない」は書いたが、実物では効いていない**（2026-09-10・実測で判明）。
★ 蓋は `.ref-sec:first-child{border-top:0}` だが、⚠ **本文は必ず導入の段落から始まる**ので
**最初の `.ref-sec` が `:first-child` になることが無く、蓋が1度も当たらない**
（★ 実測: 面A・面Bとも **6節すべてに線**。面A 2px / 面B 1px）。
★★ **見た目は妥当なので直していない** —— **引かれた線は「導入 ↔ 第1節」の切れ目**に落ちていて、
⚠ **題名のすぐ下に線が出るという、蓋が防ごうとしていた形にはなっていない。**
★ **蓋は残す**（導入を持たないページが将来できたときの備え）。⚠ **ただし「引かない」と読める書き方はやめた。**

★★ **決めごとは2つで、面Aと面Bで同じ**（①節の上に線 ②本文の約1.35倍）。
⚠ **px は違う**（読み欄 680px と資料ペイン 340px で同じ px を使うと、片方が必ず外れる）。
★ **両方のファイルに同じ注記を置いた**（`assembler/style.css` の `.ref-sec` と、
生成器の `SHELL_CSS` の `.ref-scope .ref-sec`）。

⚠ **面Bには現在地の印を入れていない** —— **ペインの目次はページの頭に置かれて一緒に流れる**ので、
貼り付ける仕組みが要る。★ **これは `learn.js` の描く処理に手を入れる話なので、別に立てる**（→ 20-9）。

### 21-7. ★★ 確認できた申し送り —— **アプリのペインの CSS が、面Aの目次を上書きしていた**

⚠ 前のレーンの申し送り「**アプリのペインの CSS が、面Aの目次の並びを上書きしている（もとからあった不具合）**」を、
★ **実測で確かめた: 本当だった。**

**仕組み**: 焼いたページの `<style>` は **`SHELL_CSS`（面Aの器）→ `assembler/style.css` の切り出し → `LIGHT_CSS`** の順。
⚠ **面A側が `.ref-toc details { … }` と書くと、切り出しの `.ref-toc details { … }` と *同じ強さ* なので、
あとに来る切り出しのほうが勝つ。**

★ **実測（直す前・1280px）:**

| | 面Aが書いていた値 | ⚠ 実際に効いていた値（＝ 資料ペインの値） |
|---|---|---|
| 目次の地 | `var(--panel)` → 広い画面では `none` | ⚠ **`rgba(255,255,255,.03)`** |
| 目次の枠 | 広い画面では `border:0; border-left:1px` | ⚠ **`1px solid rgba(255,255,255,.08)` の四角** |
| 角丸 | 広い画面では `0` | ⚠ **`8px`** |

★★ **＝ 広い画面でも「資料ペインの小さなカード」のまま出ていた。**
⚠ **`@media` は強さを足さない**ので、`@media (min-width:1000px)` の中に書いても勝てなかった。

★ **直し: 面A側のセレクタを全部 `.ref-layout` から始める**（`.ref-layout .ref-toc details` ＝ 強さが1つ上）。
⚠⚠ **これは目次に限った話ではない。** ★ **面Aが `assembler/style.css` と同じ名前の規則を書くときは、
必ず `.ref-scope` か `.ref-layout` から始めること**（本文側は最初からそうなっていた）。

### 21-8. ★ 否定対照（入れた守りを外すと赤くなること）

★ **4件とも 2026-09-10 にもう一度走らせ直した**（⚠ **前のレーンの数字と窓の大きさが違うので、下は撮り直した値**）。

| 外したもの | 何が起きたか（実測・1280×900） |
|---|---|
| ★★ **現在地の直しを戻す**（`_before` の版を同じ手でなめる） | ★ **印なしが 0 / 67 → 37 / 59 地点**（⚠ **狭い面 375×812 では 0 / 88 → 49 / 73**）。★ **どの地点でも印はちょうど1件**（2件以上は 0 地点） |
| ★ **`main.wrap` の 712px の蓋を外す** | ★ **999px の窓で読み欄が 680 → 952px・1行 33.6 → 46.9字** |
| ★ **`.ref-layout` の接頭辞を外す** | ★ 目次が**資料ペインのカード**に戻る:<br>地 `rgba(0,0,0,0)` → **`rgba(255,255,255,.03)`**／枠 なし → **1px**／角丸 `0` → **`8px`**（＝ 20-7 の実測値そのもの） |
| ★★ **`LIGHT_CSS` から `.ref-rx` の読み替えを1行抜く** | ⚠⚠ **直す前は緑のままだった**（部分一致の穴）→ ★ **丸ごと一致に直したあとは赤（終了コード 1）**:<br>「`.ref-rx` … `rgba(255, 255, 255, 0.03)`（LIGHT_OVERRIDE に載っているのに LIGHT_CSS が「.ref-rx」を触っていない）」 |

★ **4件とも、戻したあとに焼き直して `git status` が空**（＝ **積んである `reference/*/index.html` が、
いまの生成器がそのまま出すものと1バイト一致**）であることまで見た。

### 21-9. ⚠ ユーザーに決めてもらう必要が残ったこと

1. ⚠ **面Bの現在地の印**（20-6）。★ 資料ペインの目次はページの頭で一緒に流れるので、
   **貼り付ける仕組みを `learn.js` に入れるかどうか**。⚠ **ペインは 820px しか無いので、
   88px の札を貼ると本文が 1割減る。** ★ 面Aほど得が大きくない（面Bは 5,800px ＝ 6.8画面）。
2. ⚠⚠ **面Bの添え物が、いまも AA を割ったまま。** ★ いまは「**触る画面は暗い・読む画面は明るい**」で分けたので、
   ⚠ **20-1 で見つけた「AA を割る4種」は、面Aからは消えたが面Bにはそのまま残っている。**
   ★ **実測（2026-09-10・資料ペインを開いて数えた）: AA を割る種類は 5**

   | | コントラスト比 | 字の大きさ |
   |---|--:|--:|
   | ⚠⚠ `.ref-title`（ペインの見出し） | **3.14:1** | 12.5px |
   | ⚠ `.ref-note` と その `<b>` | **3.24:1** | 12px |
   | ⚠ `.ref-cap` / `.ref-figure-cap` | **3.43:1** | 11.5px |

   ⚠ **どれも 12px 前後の小さな字**なので、**大きい字の緩和（3:1）も使えない。**
   ★ **直すなら面Bの地を明るくしなくてもよい** —— **暗い地のまま字だけ明るくすれば届く。**
   ⚠ **ただしこの3つの色は `assembler/style.css` の共通の変数で、アプリ全体が使っている**
   ＝ **参考書だけの話にならない**ので、**このレーンでは触っていない。**
   ★★ **決めてほしいのは「アプリ全体の添え物の字を明るくしてよいか」**（⚠ 参考書だけ別の色にすると、
   同じ `.ref-note` が2つの色を持つ）。
3. ⚠ **面Bも明るい地にするか。** ⚠ **同じ本文が2つの見た目を持つ**ので、
   **アプリから面Aへ飛んだ人には色が変わって見える。**
   ★ **語もアンカーも同じ**なので迷子にはならないが、**気になるなら面Aを暗いままにする選択肢もある**
   （⚠ そのときは 20-1 の「AA を割る4種」を面A側でも別に直す必要がある ＝ 上の2と同じ話になる）。
4. ⚠ **スマホの本文が1行 18.7字**（375px・17px・★ **数えた実測**。⚠ 直す前は 21.2字だったので **2.5字ぶん減った**）。
   ★ **字を小さくすれば字数は増えるが、読みにくくなる。**
   ⚠ **どちらを取るかは好みの範囲**なので、実物を見て決めてほしい。
5. ⚠ **狭い面の貼り付いた札が、本文の 88px を常に隠す**（20-3）。★ **375px の窓の 10.8%。**
   ⚠ **現在地が見えることと引き換え**なので、**煩わしければ「札は畳んだときだけ」に落とせる。**

### 21-10. ★ 検収（2026-09-10・通信断で落ちた前レーンの続き）

⚠ **前のレーンは 20-1〜20-9 を書き上げたところで落ちた** ＝ **実物を見る手順が残っていた。**
★ **やり直したこと**（⚠ **数字はすべて撮り直し。前のレーンの表と食い違ったものは上で直してある**）:

| 見たもの | 結果 |
|---|---|
| ★★ **回帰テスト全走** | ★ **816 件すべて合格・1,238 秒**（`assembler/test.html`・chromium） |
| ⚠⚠ **走ったことの裏取り** | ★ 印（`assembler/__marker.txt`）を **走る前 200・走ったあと 200・消したあと 404** で確認（＝ **0バイトの空振りではない**） |
| ★ **版の揃い** | ★ `?v=1529` が **index 12・test 3・audit 2 か所**／`<div class="version">v1529`／`verify-release.js assembler` 緑 |
| ★ **原稿との一致** | ★ `gen-reference.mjs --check` 緑（6ページ / 98行）／**面Aを焼き直しても `git status` が空** |
| ★★ **前後の見比べ** | ★ 面A（1280×900 / 375×812）の**上と中**を、`_before/alkane/` と `reference/alkane/` で撮って並べた |
| ★★ **面Bの検分** | ★ **明るい地の漏れ 0 件**（ペインの実効の地は `rgb(13,16,23)` のまま。⚠ **子孫を全部なめて、相対輝度 0.5 超の地を数えた**）／見出し÷本文 **17.5 / 13 ＝ 1.346倍**／節の線 1px ／⚠ **添え物5種が AA 割れ**（→ 20-9 の2） |
| ★ **否定対照** | ★ **4件とも赤くなることを実測**（20-8） |

⚠⚠ **Browser ペインでは撮れなかった** —— **ペインの実寸が 280px しかなく、`resize_window` で
1280 を指定しても縮小されて写る**。★ **playwright（`tools/record/node_modules`）で幅を指定して撮った** ＝
**1280 も 375 も、実際にその幅で組まれた紙面**になっている。

---

## §21 ★★ 添え物の字を AA に載せた（2026-09-10・contrast レーン・v1530）

**§20-9 の 2「アプリ全体の添え物の字を明るくしてよいか」にユーザーが (a) で答えた**
（＝ 参考書だけ別色にすると、同じ `.ref-note` が2つの色を持つ）。その実装の記録。

### 21-1. ★ 変えたのは `:root` の**1変数だけ**

| | 前 | 後 |
|---|---|---|
| `--text-muted` | `#606880` | ★ **`#7a84a2`** |

★ **色相と彩度はそのまま、明度だけ 1.28 倍**（各チャネルを同じ倍率で持ち上げた）。
⚠ **明るくしすぎない**のが要点 —— 上げると本文（`--text-primary` 18.1:1）や
`--text-secondary`（8.3:1）と見分けが付かなくなり、**「添え物」に見えなくなる**。
★ **4.5:1 を満たす中でいちばん暗い側**を採った（いちばん明るい地で **4.69:1**）。
⚠ 数学的な下限は `#77819f`（**4.50:1**）だが、**境界に乗る**ので採らなかった
（パネルの敷きが 1段変わるだけで赤に落ちる）。

★ **三段の差は保たれている**（素の背景 `#0a0c10` の上）: 本文 **18.12** ／ 副 **8.25** ／ 添え物 **5.27**。

### 21-2. ⚠⚠ 発注は「5種」だったが、実測は **20種** だった

⚠ §20-9 は面Bで見つけた5種（`.ref-title` / `.ref-note` とその `<b>` / `.ref-cap` / `.ref-figure-cap`）
を挙げていたが、**同じ変数はアプリ全体で使われている**（`var(--text-muted)` が **57 か所**）。
★ **40通りの面をなめたら 20 種**が割れていた ＝ **面B以外に 13 種**（📚学習の説明文・
索引の副題・分子モーダルの補足・作業帯の「ステップ 1 / 2」・ヘッダーの版番号 …）。

| class | 実効の地 | 字 | 前 | 後 |
|---|---|--:|--:|--:|
| `span.ref-title` | `#171a20` | 12.5px | ⚠ **3.14** | ★ **4.69** |
| `p.ref-note` | `#1b1727` | 12px | ⚠ **3.15** | ★ **4.71** |
| `.ref-note > b` ／ `.ref-group > span` | `#14171e` | 12 / 11px | ⚠ **3.24** | ★ **4.82** |
| 📚学習の説明文4種 ／ `.card > p` | `#13181f` | 10.5〜12.5px | ⚠ **3.22** | ★ **4.80** |
| `span.ref-map-why` | `#12151c` | 11.5px | ⚠ **3.30** | ★ **4.91** |
| `.ref-index-btn > span` | `#0e1217` | 11.5px | ⚠ **3.39** | ★ **5.06** |
| `.ref-cap` ／ `.ref-figure-cap` ／ `td.ref-c` ／ 機構の2種 | `#0d1017` | 11.5〜13px | ⚠ **3.43** | ★ **5.12** |
| `div.version`（版番号） | `#0a0c10` | 12px | ⚠ **3.53** | ★ **5.27** |
| `.ws-row > div`（ステップ 1 / 2） | `#060a13` | 11px | ⚠ **3.57** | ★ **5.33** |

⚠⚠ **地は1つではない。** 同じ `#606880` でも **3.14〜3.57 の幅**がある
（パネル・モーダル・紫の薄敷きが重なるほど地が明るくなり、比は**下がる**）。
★ **`--bg-main` の上で測って済ませると、いちばん悪い場所を 0.4 ぶん見落とす。**

### 21-3. ★★ 機械で見張る —— `tools/check-contrast.mjs`

★ **手本は `checkLightCoverage`**（§20-5）と同じ考え方: **名簿に無い違反が出たら赤**。

- ★ **面を 40 通り**（🧪自由・🧩パズル・⚗反応機構・📚学習・🔍分子モーダル・✏命名クイズ・
  📇呼び出し・🧊立体 ＋ **資料ページ6枚 × 面B/面A**）× **1280px と 375px** でなめる。
- ★ 文字を持つ要素 **4,802 件**の「文字色 ／ **祖先をさかのぼって重ねた実効の地** ／ 実際の px と太さ」
  から比を出し、**種類ごとにいちばん悪かった地を正**とする。
- ★ **大きい字の緩和（3:1）は実測の px と太さで判定**（18.66px 以上の太字、または 24px 以上）。
- ⚠ **数えないものは「見かけ」ではなく WCAG の条文で決めた**:
  ① `:disabled` / `aria-disabled` の操作部品（1.4.3 の "inactive user interface components"）。
     ⚠ 実際 `.view-btn:disabled{opacity:.42}` の ⏮ が **2.64:1** で出るが、
     直そうとすると「押せない」の手がかりのほうを壊す。
  ② 画像（`url()`）の上の字は**計算できない**ので件数を別に出す（「見た」ふりをしない）。
- ★ **グラデーションは節を全部候補にして、いちばん悪い節を採る。**
  ⚠ ただし **`background-size` を見て「要素全体を覆っている層」だけ**を地に採る ——
  `#left-panel` の「まだ横に道具がある」ヒントは **15px の縁の光**なので、
  これを地に採ると **🧪自由 だけで偽の違反が 11 種**出た（実測）。
- ★ **名簿は床であって逃げ道ではない**: `min` を下回っても赤、**もう割れていないのに
  名簿に残っていても赤**（直したら降ろす）。

### 21-4. ⚠ 面Aの読み替えは**見直す必要が無かった**（実測）

★ `LIGHT_CSS` は `--text-muted:#636b78` を**絶対値で**置いているので、暗い地側の値が動いても
明るい地の見え方は 1px も変わらない。★ **実測でも面A 6ページの AA 割れは 0 種**
（`checkLightCoverage` も緑・焼き直して `git diff` は `:root` の1行だけ）。

### 21-5. ⚠ 名簿に載せた 2 種（＝ 直していない）

**どちらも `--color-br: #a85d00`（臭素の茶）で、「添え物の字」ではなく「元素の色」。**

| 種類 | 比 | 字 |
|---|--:|--:|
| `.atom-btn.atom-br > span`（パレットの札） | ⚠ **3.73:1** | 18px/700（★ **18.66px に 0.66 足りず**、緩和が使えない） |
| `svg text.svg-atom-text@var(--color-br)`（分子図の原子ラベル） | ⚠ **4.02:1** | 9px/700 |

★ **明るくして載せること自体はできる**（`#c06a00` で **4.65:1**・色相はそのまま）。
⚠ **ただし `tools/gen-isomer-pages.mjs` が同じ値を焼き込んでいる**（`/isomers/` の13枚）ので、
**そちらを焼き直さないと同じ Br が2つの色を持つ**。
★ **1つの決めとしてユーザーに出す**（→ 21-8）。

### 21-6. ★★ 門番は2つに分けた（道具は「広く」・回帰テストは「深く」）

⚠⚠ **道具（`tools/check-contrast.mjs`）は手で走らせるもの ＝ 門番ではない。**
★ `assembler/tests.js` に **`CT1`** を足した ＝ **コミット前に必ず走る場所**で退行が止まる。

| | 見る範囲 | 名簿 | いつ走る |
|---|---|---|---|
| `tools/check-contrast.mjs` | ★ **40面 × 全部の文字**（面Aも） | 持つ（いまは2種） | 手で |
| ★ `CT1`（tests.js） | ⚠ **`--text-muted` の字だけ**・3面 | ⚠ **持たない**（1変数の話なので要らない） | ★ **全走のたび** |

★ `CT1` も**祖先をさかのぼって実効の地を組み立てる**（`--bg-main` の上だけ見ない）。
★ **空振りで緑にしない** —— 見つけた字が 10 件未満、または3面そろっていなければ赤。
★ **`CT1` は自分の中に否定対照を持つ** —— **直す前の `#606880` を同じ地に載せると
10 件以上が AA を割る**ことを毎回確かめる（＝ 地の組み立てか比の式が壊れたら、
「全部合格」ではなくここが赤くなる）。

### 21-7. ★ 否定対照（入れた守りを外すと赤くなること・2026-09-10 実測）

| 外したもの | 何が起きたか |
|---|---|
| ★★ **`--text-muted` を `#606880` に戻す** | ★ **道具が赤（終了コード 1）・「名簿に無いもの 20 種」** |
| ★ **名簿から `svg text.svg-atom-text@var(--color-br)` を抜く** | ★ **赤**（「名簿に無いもの」に出る） |
| ★ **割れていない `div.version` を名簿に載せる** | ★ **赤**（「名簿にあるのに、もう割れていない … 5.27:1」） |
| ★ **`:disabled` の除外を外す** | ★ **赤** —— `button.view-btn`（**2.64:1**）と `.stereo-tab`（**3.13:1**）が出る ＝ 除外が本当に効いている |
| ★★ **グラデーションの扱いを直す前に戻す**（層に分けず・半透明の節を積み上げる） | ⚠⚠ **偽の違反 15 種**（パレットの札・原子ボタン・道具ボタンが**画面の端にしか無い水色の上**に載っていることになる）＋ ★ **名簿の床も鳴る**（`.atom-btn.atom-br > span` が 3.73 → **1.12**）＝ 「床であって逃げ道ではない」の枝も実測できた |
| ★ **`CT1` の物差し**（直す前の色を同じ地に載せる） | ★ **20 件超が AA 割れ** ＝ 測り方が空振りしていない（毎回の全走で確かめている） |

⚠⚠ **1つだけ「外しても赤くならなかった」ものがある** —— **`background-size` を見て
「要素全体を覆っている層」だけを地に採る蓋**（`covers()`）。
★ **偽の違反 15 種を消していたのは、その手前の「層ごとに分けて、いちばん上の層で決める」ほう**で、
`covers()` を外しても（＝ どの層も覆っているとみなしても）**いまの CSS では判定が1件も変わらない**
（`#left-panel` は**いちばん上の層の1つ目の節が不透明**なので、そこで決まって水色まで届かない）。
★ **蓋は残す** —— 外すと**測る地そのものが間違う**（30px の縁の飾りの色を、パネル全体の地として採る）。
⚠ **ただし「赤くなることを実測した守り」ではない**ので、そう書く。

### 21-8. ⚠ ユーザーに決めてもらう必要が残ったこと

1. ⚠ **臭素の茶 `#a85d00` を `#c06a00` に上げるか**（21-5）。★ 上げると AA に載るが、
   **`/isomers/` の13枚を同時に焼き直す**必要がある（別レーンの仕事になる）。
   ⚠ **元素の色は装飾ではない**（`:root` の注記のとおり I の `#7d5fff` と色相を分けてある）ので、
   ★ **上げるとしても明度だけ**にする。
2. ⚠ **`.view-btn:disabled` の 2.64:1** をこのままにするか（21-3 の除外①）。
   ★ WCAG の対象外なので**規格上は問題ない**が、⚠ **押せないことが「読めない」で伝わっている**
   状態ではある。★ 薄くするのをやめて「灰色にする」に変える手もある。

---

## §23 ★★ 発展の線引き・穴あきテンプレート・ぶら下げ（2026-09-11・ref-tidy レーン）

**発端はユーザーの注文3件（2026-09-11）:**

> **発展内容の線引き（枠で囲う、線を引く、）などを提案してください**
> **いくつか原稿を仕上げる中で標準フォーマットをつくりたい**
>
> **もつ　他に代替表現があれば採用を検討します**（`○○` の穴あきテンプレートについて）
>
> **意図して削除していません**
> **囲み、キャプション、見出し、ノートなどは適宜使ってください**
> **それらのMDでの表記をあまり把握せずに雑に作業しています**

⚠⚠ **3つ目が、前のレーンへの申し送りを否定している。**
前のレーンには「**②で消されたものは、消えたままにする**」と伝えてあったが、これは間違い。
★ **②稿から囲みが消えているのは判断ではなく、記法を知らないまま本文を書いた結果**
—— だから**中身が今も正しければ戻す**のが正しい扱い。⚠ ただし**言い回しが書き換わっている所は
ユーザーの新しい文を採る**（そこは意図した直し）。★ **見分けは「代わりが本文にあるか」**:

| ②稿で起きたこと | 判定 | 実際にどうしたか |
|---|---|---|
| 囲みがまるごと消え、**代わりの文が本文に無い** | ★ **記法を知らずに落ちた** | 戻す |
| 囲みの中身が**別の言い方で本文に書き直されている** | ⚠ **意図した直し** | 戻さない |

### 23-1. ★★ 発展の器 —— `:::section` / `:::heading` に `advanced: true`

⚠ **決める前に、いま何が在ったか（実測）:**

| 器 | 何を言っているか | 発展の器になるか |
|---|---|---|
| 小見出しの語尾「（発展）」 | `## 枝分かれすると沸点が下がる（発展）`（`alkane.md` に2つ） | ⚠ **素の文字**。機械は何も知らず、⛔ **目次に出る／出ないと連動しない** |
| `:::callout tone: skip` | 「覚えなくてよい」 | ⛔ **別のこと**（発展でも覚える所はある） |
| `:::reaction` の `level` | ★★★／★★☆／★☆☆ | ⛔ **重要度**。しかも §19-3 の決めで**印は必ず反応式に付く** |

★ **決めたこと**（詳しくは `REFBOOK_STYLE.md` §10）:

- ⚠ **新しい `:::` は作らない**（もう13種ある）。**既にある器に印を1つ足す。**
- ★★ **印が言うのは「高校の教科書の本文の外側にある」ことだけ。**
  ⚠⚠ **重要度も暗記の要否も言わない** —— 覚える範囲は今までどおり**本文の文**が言う（§8-5）。
  ★ だから `level`（重要度）とも `tone: skip`（覚えなくてよい）とも役が重ならない ＝ **同じことを2か所で決めない。**
- ★★ **付く先は節と小見出しだけ**（＝ **範囲の頭**）。発展は1文ではなく**ひとかたまり**。
- ★★ **節に付けると目次にも同じ印が出る** ＝ 語尾に書いていた頃のいちばんの穴が埋まった。
  ⚠ **印は `<a>` の外**（中に入れると `REF19` の「目次の文字 ＝ 節の題」が壊れる）。
- ★ 色は**緑**（注意＝橙・丸暗記＝紫・重要度＝★ と1色ずつ空ける）。**地を持たず縁だけの札**。
  ⚠ 面Aは明るい地なので `#7fd6a4` → `#2e6b45` に読み替える（白地で 5.7:1 ＝ AA）。
- ★ 札には**意味の1文**が `title` で付く（「印だけだと『覚えなくてよい』と読まれる」を塞ぐ）。

#### ★ 語尾の「（発展）」は **受け取って印に直す**（赤にしない・捨てない）

⚠⚠ ユーザーは②稿で実際にそう書いた。★ **生成器が語尾を外して `advanced: true` に寄せる**
（§20 の思想そのもの）＝ **原稿の見た目は変わらず、保存される形は1つだけ。**
⚠ 直したことは `--tidy` の一覧と③の報告に出る。

### 23-2. ★ 穴あきテンプレート `○○` —— **記法を増やさずに器にする**

★ ②稿に6回出た「主鎖の炭素数が4なら『**○○**ブタン』」。
⚠ **新しい印（`__` や `[]`）を作らなかった** —— ユーザーが自分から書いた形が既に `○○` なので、
**`○` が2つ以上並んだら空欄と見る**。原稿の見た目は1文字も変わらない。

★ **見せ方を4案、実物で並べて比べた**（`blank-4ways.png`）:

| 案 | 判定 |
|---|---|
| (1) 素の `○○` | ⚠ 空欄だと分からない（「○○ブタン」という名の化合物に見える） |
| (2) 下線の空欄 `＿＿ブタン` | ⛔ **不採用**（下の3つの実害） |
| (3) `◻︎◻︎` | ⚠ **環境依存の字**。実測でも異体字セレクタが落ちて `□ □` になった |
| ★★ (4) `○○` を残して**薄い地＋点線の下線** | ★ **採用** |

⚠⚠ **(2) を落とした理由は見た目ではない。3つとも実害:**

1. ★★ **声に出せなくなる。** 本人の書きぶりは「そのまま声に出せる短い文字列」が骨（§8-4）で、
   「まるまるブタン」は口に出せるが、**下線は読み上げで何も読まれない。**
2. ★★ **検索にかからない。** 面Aは公開ページなので「○○ブタン」で本文検索できることに意味がある。
   下線にすると**その場所の本文が0字**になる（§19-5 で図に式を持たせなかったのと同じ理由）。
3. ★ **字数を暗示してしまう。** 実際に入るのは②稿の6か所で **2〜8 字**（「メチル」「2,3-ジメチル」…）
   ＝ **固定長の空欄では表せない。**

### 23-3. ★ ぶら下げの補足 —— `:::list` の `items` で行頭を字下げする

★ ②稿では、`○○` の型は**必ず番号つき手順の1段下に、全角スペースで字下げして**置かれていた（§8-6）。
⚠ **その形をそのまま受ける** —— `items:` の中で**行頭が空白（全角でも半角でもよい）の行**は、
1つ前の項目の「ぶら下げの補足」になる。印は**行頭の全角スペース1つ**に正規化して持ち回る。

⚠⚠ **別の `<li>` にはしない。** `<ol>` の中では `list-style: none` にしても**勘定は進む**ので、
**次の段が 2 ではなく 3 になる**（＝ 手順の番号が黙ってずれる）。★ 1つ前の項目の**中**に入れる。

⚠ 受けるのは `hang` に挙げた並びだけ（`terms` や `rows` の字下げは今までどおり赤）。

### 23-4. ★★ 表記を機械で揃える `--tidy`（`REFBOOK_STYLE.md` §3 の実装）

```
node tools/gen-reference.mjs --tidy [ページのid …]
```

★ **黙って揃えない。** 直した所を**1件ずつ**（ファイル・行・規則・前・後）画面に出す（§1）。
⚠ **やってよいのは「元に戻せる表記の直し」だけ**（`tools/reference-tidy.js` の頭に線が書いてある）
—— ⛔ 素の1行を見出しに格上げする（§20-8）・段落を割る・言い回しを変える、は**書いた人にしか決められない**。

★ **迷ったら「同じ文を2人が別々に直したとき、同じ結果になるか」で判じる。**

実測（6枚・16か所）: `alkane-isomers` 5／`functional-groups` 5／`alkane` 3／`alkane-naming` 3／残り2枚は0。
★ 当てたあともう一度走らせて「そろえる所はありません」＝ **冪等**。

### 23-5. ★ ページの引っ越し2件（ユーザー決定）

| | 何を | なぜ |
|---|---|---|
| **(1)** | `functional-groups` を**②稿の「官能基とは何か」**にし、**11種の対応表は `alcohol-oxidation-map` へ**引っ越した | ⚠ ファイル名は `functional-groups` なのに中身は対応表だった（§4-1 の記録）。★ **悪いのは名前のほう。** ⛔ 表は消さない —— **1級と2級で酸化の行き先が変わることが行どうしの対比で見える**表で、単元をまたぐのはそのため（元の `why:` をそのまま引き継いだ） |
| **(2)** | **命名を `alkane-naming` に寄せ**、`alkane-isomers` を**異性体の数え方**に戻した | ★ 直鎖の名前・接頭辞・主鎖の選び方・番号の振り方は「1つの名前を作る」同じ作業の途中の段。別ページに割ると、名前を1つ作るあいだにページを2枚またぐ |

★ **2ページは `:::link` で往復させた**（`CLAUDE.md` の「アプリ間のリンクは往復にする」と同じ考え）。

⚠ `functional-groups` は `unit` を `alcohol` → `intro`（「有機化合物の基本」）に変え、
`ORDER.txt` で**索引の先頭**へ移した（有機の入口）。

### 23-6. ⚠ 発注書の指定のうち、実際には違っていたもの

1. ⚠ **`:::` は「11種」ではなく 13種**（`section` / `heading` / `list` / `figure` / `reaction` /
   `table` / `callout` / `link` / `stageTable` / `mechanismTable` / `dehydrationTable` /
   `example` / `exercise`）。★ 判断（増やしすぎない）は変わらないので、**足したのは印1つだけ**にした。
2. ⚠ **「語尾に（発展）と書く今のやり方は、目次に出る／出ないと連動していない」** —— 正しいが、
   **`## `（小見出し）はそもそも目次に出ない**（§20-5）ので、連動のしようが無かった。
   ★ 実際に穴だったのは「**節に発展を書いても目次に印が出ない**」ほうで、そこを埋めた。
3. ⚠ **メモ「3-エチル-2,3-ジメチルヘキサンの構造式」は焼けなかった。**
   ★ 対応するスライド（2-3 s6 例4）の分子は**主鎖5個のペンタン**（3-エチル-2,3-ジメチルペンタン）で、
   ②稿の本文の**ヘキサン**とは別の分子。⚠ **どちらも化学的には正しい**ので、
   こちらでは決められない（§5 の決裁事項に上げた）。**メモは残した。**
4. ⚠ **メモ「接頭辞の説明へのリンク」はリンクにしなかった。** ★ 接頭辞のページは 52 の計画に無く、
   しかも**命名を1ページに寄せた**のだから、接頭辞は**このページの持ちもの**。
   → `:::table`（数を表す接頭辞・10行）を本文に置いた。
5. ⚠ **`REF11` が「表が1枚だけ」を `table.ref-table` で数えていた。**
   ★ それは「このページが stages.json の表を1枚持っている」という**中身の事情**で、
   `functional-groups` が手で書く表になった瞬間に赤くなった。**症状は「題が2つ出る」**なので、
   `#ref-body h3` の数で見る形に直した。

---

## §24 ★★ 発展は**畳んで置く**（2026-09-11・adv-fold レーン・v1534）

> **初学者のニーズと発展レベル（難関大レベル、あるいは大学初級）のニーズを同時に満たしたいです。**
> **初学者がとっかかりにくくなってしまうのは避けたいですし、事典的に網羅し、**
> **レベルの高い学習者の要求にこたえられるようにしたい**
> **発展内容は折りたたむ、というのがよい案かもしれません**（ユーザー）

★★ **網羅する量は1文字も減らさない。減らすのは「最初に画面へ出る量」だけ。**
⚠ §23 で入れた印（緑の札＋線）は「ここは範囲の外」と**言う**ところまでで、
**寄り道の長さはそのまま画面を占めていた**（`functional-groups` の発展の節は 637px ＝ 本文の約1画面）。

### 24-1. ★ 器は `<details>` —— **JS が要らない**のがいちばんの理由

★ `:::exercise` の解答隠し（§22）と**同じ作り**にした（`learn.js` の `renderToc` 脇の先例）:
**markup は面Aと面Bで同じ。見え方の違いは CSS だけ。**

```
<details class="ref-adv ref-sec ref-sec-advanced" id="ref-sec-nitro-sulfo">
  <summary class="ref-adv-sum"><h4 class="ref-sec-h">題<b class="ref-adv-tag">発展</b></h4>
    <p class="ref-sec-lead">この節で分かること1行</p></summary>
  <div class="ref-adv-body">…畳んだ中身…</div>
</details>
```

- ⚠⚠ **id と `.ref-sec-advanced` は `<details>` が持つ** ＝ 目次・用語の索引の行き先
  （`#ref-sec-<anchor>`）は**1文字も変わらない**（§19-2 の「綴りは1つ」を増やさない）。
- ★ 畳んだままで見えるのは **発展の札・節の題・`lead` の1行**。
  ⚠⚠ **これが畳んでよい条件** —— 初学者が「読むかどうか」を1行で決められること。
  題だけにすると、事典として引きに来た人が「何の節か」を開かないと判断できない。

### 24-2. ★★ 並べ方は **`ReferenceBook.renderBlocks`** の1本（面Aの生成器も呼ぶ）

⚠ 前は生成器が `renderBlock` を1ブロックずつ呼んで並べていた。
**そのままだと、まとめ方を変えても面Aだけ古い並びで焼かれる**（表を2か所で組まないのと同じ話・§17-3）。
★ **「上に並べる要素の配列」を返す1本**に寄せて、面Aはそれを焼く。

**どこで畳み終わるか**（＝ かたまりの終わり）は2つだけ:

| 終わり | なぜ |
|---|---|
| 次の `:::section` | 節は「検索の着地点」なので、必ず折りたたみの外に出す |
| **発展でない `## 小見出し`** | ⚠ これが無いと `functional-groups.md` の `## 例題` が発展の中に吸い込まれる（例題はページ全体のもので、ニトロ基の話の続きではない） |

★ 逆に**発展の小見出しは吸い込む**（発展の節の中の区切り）。

#### ⚠⚠ **発展の「小見出し」単体は畳まない**（実データを見て決め直した）

★ はじめは小見出しも畳む実装にしたが、**焼いた実物を読んで取り消した。**
⚠⚠ 畳むには「**どこまでが発展か**」が器で言えることが要る。**節は次の節までと決まっている**が、
**小見出しにはそれが無い。**

⚠ 実測（`alkane.md`）:

| 小見出し | 発展なのは | そのあとに続くもの |
|---|---|---|
| `## 枝分かれすると沸点が下がる（発展）` | 1段落＋リンク2本 | ⚠ **「アルカンは水に溶けにくく、水より密度が小さい」**（教科書の範囲） |
| `## 電子対はどう動いているのか（発展）` | **1段落だけ** | ⚠⚠ 置換の連鎖の図2枚・置換体の名前・**★★★（必ず覚える）の反応式2本**（塩素化と完全燃焼） |

★★ **次の節まで畳むと、「必ず覚える」反応式が既定で隠れる** ＝ **網羅を減らしたのと同じこと**で、
この作業でいちばんやってはいけないことだった。
★ 小見出しの発展は今までどおり**印（緑の札＋左の緑の線）だけ**で示す。
⚠ 畳めるようにするなら、原稿側に「ここまで」を書く器が要る（＝ 記法が1つ増える）ので、いまは採らない。
★ `REF25` ⑦ が**否定対照**として「`alkane` に折りたたみが0個で、★★★ の式が外に出ている」ことを見張る。

### 24-3. ⚠⚠ 素の `<details>` では届かない3つと、その退避

★ 開け閉めは素でできる。⚠ **届かないのは「勝手に開いてほしい」3つ**だけで、
そこだけ `refAdvSetup`（`learn.js`）が面倒を見る。
⚠⚠ **面Aはこの関数を `gen-reference-pages.mjs` が丸ごと切り出して埋める**（書き写さない）＝
**両面で退避の仕方が割れない**（CSS を `style.css` から切り出しているのと同じ理由）。

| 要件 | 新しいブラウザ | ⚠ 退避（Chrome 131 / Firefox 139 / Safari 18.4 より前） |
|---|---|---|
| **ページ内検索で開く** | ★ 素で開く（閉じた `<details>` の中は `::details-content` が `content-visibility: hidden` ＝ `hidden="until-found"` と同じ仕掛け） | ★★ **開いたまま出す**（下） |
| **目次・索引から飛んだら開く** | ★ `:target` で素で開く | `hashchange` と目次の押しを見て `open` を立てる |
| **印刷は開いた状態** | ★ `@media print` の `::details-content{content-visibility:visible}` だけで開く（JS を通らない） | `beforeprint` で開き、`afterprint` で**元に戻す** |

⚠⚠ **面Aの `@media print` は `LIGHT_CSS`（いちばん後ろ）に置く。**
★ `@media` は**強さ（詳細度）を足さない**ので、`SHELL_CSS`（切り出しより前）に書くと、
**▶ の打ち消しが切り出した同じ強さの規則に負けて効かない** ——
実際、紙の上に「▶（まだ閉じている）」の印が残ったまま中身だけ出ていた（実測して直した）。
⚠ これは §19-6 で `.ref-toc details` が効かなかったのと**同じ踏み方**。

★★ **ページ内検索の退避は「見えるほうへ倒す」。**
⚠ 見分けは `CSS.supports('selector(::details-content)')` —— **自動で開く仕掛けと同じ版で入った疑似要素**
（`hidden="until-found"` の `onbeforematch` で見ると、Chrome 102〜130 が「持っているのに開かない」側に化ける）。
⚠⚠ **持たないブラウザでは畳まずに出す。**
理由は1つ —— **畳んだまま出すと本文がページ内検索から消える。**
参考書の目的は「事典的に網羅して、引きに来た人が見つけられること」なので、
**見つからなくなるほうが、初期状態が静かでないことよりも害が大きい。**
★ 開いたまま出しても**畳めることは変わらない**ので、失うのは初期状態の静けさだけ ＝ 退避として釣り合う。

### 24-4. ★★ 層の分け方は**面で逆向き**（実測して決めた）

| | 面A（明るい地・`/reference/`） | 面B（暗い地・資料ペイン） |
|---|---|---|
| 地 | ★ **緑寄りの紙を敷く** `#ebf2ec` | ★ **沈める** `rgba(0,0,0,.25)` |
| 左の帯 | `#2e6b45`（4px） | `#7fd6a4`（3px） |
| 印・▶ | `#2e6b45` | `#7fd6a4` |

⚠⚠ **暗い地に緑の薄敷きを置いたら実測で赤になった**（`node tools/check-contrast.mjs`）:
`rgba(127,214,164,.07)` で資料ペインの地が **#15181f → #1c2528** に明るくなり、
中に入る添え物の字（`--text-muted` `#7a84a2`）が **4.78:1 → 4.22:1** で AA を割った。
★ `--text-muted` は「4.5 を満たす中でいちばん暗い側」で選んである（§21・v1530）ので、
**この地の上に置ける敷きの明るさの上限は輝度で +40% ほどしかない。**
★ そこで**逆へ倒した** —— 沈めれば中の字は全部 4.78 → 5.04 に**上がる**。
緑の手がかりは**左の帯・▶ の印・「発展」の札**が持っているので、地まで緑にする必要はない。

**明るい地の実測**（`#ebf2ec` の上）: `lead` `#59616c` で **5.50:1**・発展の緑 `#2e6b45` で **5.58:1**（AA）。

### 24-5. ⚠ 見張り

- **`REF25`**（新設）… ① 既定は閉じ ② 畳んだ札に札・題・`lead` が出ている
  ③ ⚠⚠ **畳む前と畳んだあとでブロックの数が合う**（＝ 折りたたみで本文が落ちていない）
  ④ 発展でない小見出しで閉じる ⑤ アンカーは `<details>` が持つ ⑦ **発展の小見出しは畳まれていない**
  ⑥ 焼いた面Aの `refAdvSetup` が `learn.js` と**1バイト一致**（切り出しが外れたら赤）
- **`REF18`** … 焼いたページの突き合わせ相手を `renderBlock`（1対1）から **`renderBlocks`** に替えた。
  ⚠ 上端の要素は原稿のブロックと1対1ではなくなったが、**「焼いたもの ＝ いまアプリが並べたもの」**
  という物差しは変えていない。
- **`checkLightCoverage`** … 発展の緑 `#7fd6a4` と**色の付いた薄敷き**を見ていなかったので足した（下）。

### 24-6. ⚠ 明るい地の検査に空いていた穴（ついでに塞いだ）

`checkLightCoverage` が見る「暗い地を前提にした色」の名簿に、穴が2つあった:

1. ⚠ 薄敷きを `rgba(255,255,255,…)` と `rgba(0,0,0,…)` **でしか見ていなかった** ——
   `.ref-example` の紫 `rgba(155,89,182,.10)` と `a.ref-link:hover` の `rgba(224,176,255,.08)` が素通り。
   ★ **色付きの薄敷きは、明るい地で「消える」のではなく「ほとんど変わらない」** ＝ 画面を見ても気づけない。
   → **透け（小数の α）を持つ `rgba()` を全部**見るようにした。
2. ⚠ **`#7fd6a4`（発展の緑）を知らなかった** ＝ `.ref-adv-tag` / `.ref-sec-advanced` / `.ref-h5-advanced` の
   読み替えが**1つも検査されていなかった**（LIGHT_CSS には書いてあったので画面は正しく、
   黙って外しても誰も気づけない状態）。→ literal を足し、3つを `LIGHT_OVERRIDE` に載せた。

### 24-7. ⚠ 発注書の指定のうち、実際には違っていたもの

1. ⚠ **「明るい地の読み替えは `gen-reference-pages.mjs:667` あたり」は違う。**
   ★ 発展の緑の読み替え（`#7fd6a4` → `#2e6b45`）は **`LIGHT_CSS` の中・440行あたり**で、
   667行目は**用語の索引**（`collectTerms` の前書き）。
2. ★ **「`learn.js:6687` に『markup は面Aと面Bで同じ（`<details>`）』の先例がある」は正しい**
   （`renderToc` の前書き。⚠ ただし `:::exercise` の解答隠しではなく**節の目次**のほうの注記）。
3. ★ **「`gen-reference-pages.mjs:261` に『`.ref-toc details` がずっと効いていなかった』の記録がある」も正しい。**
   ⚠ 同じ踏み方を避けるため、面Aの寸法の上書きは**すべて `.ref-scope` から始めた**
   （素の `.ref-adv` では `.ref-scope .ref-sec` に負けて、上線と 52px の余白が残る）。
4. ⚠ **「`<details>` は素の HTML なので JS が要りません」は半分だけ正しい。**
   ★ **開け閉めには要らない**が、**ページ内検索・アンカー着地・印刷の3つは古いブラウザでは届かない**
   （§24-3）。⚠ 新しいブラウザなら本当に0行で済む ＝ 退避は「古い版のためだけ」に置いている。

5. ⚠⚠ **「発展があるのは `functional-groups.md`（`advanced: true` が1件）」は足りない。**
   ★ **`alkane.md` に語尾の「（発展）」で書かれた小見出しが2つある**（`:::heading` の `advanced`）。
   ⚠ 見落とすと「発展は節だけ」の前提で作ってしまうが、**実データでは小見出しのほうが多い**（2 対 1）。
   ★ そしてこの2つが、**小見出しを畳んではいけない**という §24-2 の決めの根拠そのものだった。
6. ⚠ **「⓵ 実例: 発展の緑 `#7fd6a4` は明るい地で 1.9:1 なので `#2e6b45` に読み替えてある」は正しいが、**
   **その読み替えは検査を1度も通っていなかった**（§24-6 の②）。
   ★ 「機械が見張っているから安心」と読めるが、実際には `DARK_ASSUMING` がこの色を知らず、
     **名簿（`LIGHT_OVERRIDE`）にも載っていなかった。**

---

## §25 ★★ 参考書のリンクから「分子式と分類で絞る回」へ飛べるようにした（2026-09-11・ref-land レーン・v1535）

★ 発端は `REF21` の赤: **ester のページの「C₄H₈O₂ の異性体を書き出す」リンクが、押しても何も起きなかった。**

### 25-1. ⚠⚠ 式を変えても直らない —— 実測

| 分子式 | 構造異性体の数（全体） | 書き出し練習（上限20種） |
|---|---:|---|
| C₃H₆O₂ | 34 | ⛔ 断られる |
| C₄H₈O₂ | 122 | ⛔ 断られる |

⚠ **カルボン酸とエステルの単元で扱う式は、全体で数えるとどれも上限を超える。**
★ ＝ `formula:` だけの受け口では、この単元から書き出し練習へ渡す道が1本も無い。

### 25-2. ★★ 行き先はもう在った（作ったのは配線だけ）

★ アプリの書き出し練習には **「分子式と分類で絞る回」が 15 件**ある（`learn.js` の `fgPresets`）。
⚠ 発注書は「実測16件」と書いていたが、**並べてある一覧も15行**で、数え違いだった（実測）。
その中に **C₄H₈O₂（エステル）＝ 4種**が居る。⚠ **新しい出題も新しい画面も作っていない。**

| 直したもの | 何をしたか |
|---|---|
| `tools/reference-md.js` | `:::link` に **`cls:`** を足した（`to` / `open` / `formula` / **`cls`** / `text`）。⚠ **`formula` と対**でしか書けない（分類だけでは式が決まらない） |
| `assembler/learn.js` | `startFromFgFormula(formula, cls)` を新設。★ 一覧のボタン（`startFromFgPreset`）は**これに委ねる** ＝ 道が2本にならない。⚠ **在庫の15件を参照しない**（一覧は押しやすい入口であって、開ける回の台帳ではない） |
| `assembler/game.js` | 受け口② を `?open=isomer&formula=<式>&cls=<分類>` に広げた。⚠ `cls` が無ければ今までどおり（前方互換） |
| `assembler/learn.js`（`renderLink`） | `&cls=` を URL に足した。★ 面Aの `<a href>` と面Bの click が同じ文字列を使う |

### 25-3. ★ 綴りの台帳は1つ（`IP_SCOPES`）

⚠ **書式の側（`tools/reference-md.js`）は分類の一覧を持たない** —— `open:` と同じ理由で、
このファイルは node とブラウザで共有していて `learn.js` を読めない。★ 見るのは形だけ（英小文字・数字）。

★★ **綴り違いを止めるのは `REF21` の⑥**:

1. 原稿の `cls` が **`IP_SCOPES` の `fg:` の鍵に実在する**（⚠ 検査に分類名を書き写さない）
2. **その式・その分類で書き出し練習が実際に始まる**（`ip.problem.fgClass` が頼んだ分類と同じ）
3. ⚠⚠ **否定対照 —— 分類を外すと同じ式では開けない。**
   ★ これが無いと「`cls` を足したから開いた」のか「もともと開いた」のかが分からず、
   **欄が何の役にも立っていないのに緑**になる。

### 25-4. ★ 使った所（4ページ）

| ページ | 書いたリンク | 種類数 |
|---|---|---:|
| `ester` | `formula: C4H8O2` ＋ `cls: ester` | 4 |
| `aldehyde` | `formula: C5H10O` ＋ `cls: aldehyde` | 4 |
| `ketone` | `formula: C5H10O` ＋ `cls: ketone` | 3 |
| `carboxylic-acid` | `formula: C5H10O2` ＋ `cls: acid` | 4 |

⚠ **`ketone` と `aldehyde` が同じ C₅H₁₀O を指しているのは偶然ではない** ——
この2つは互いに構造異性体で、**同じ式を分類で切り分けるのがこの欄の値打ちそのもの**。
★ クリアの記録の鍵も分かれている（`@fg-ketone` / `@fg-aldehyde`・§11-4）ので、片方を書いて両方に ✓ は付かない。

### 25-5. ★★ 床（§12-1）の物差しを2つに分けた —— 「表に出す分子」と「例題で組ませる分子」

★ 発端は `REF4` の赤: **1200×800 で、資料が見せている分子のうち いちばん横長のステアリン酸が 24.0px**（床は 28px）。

⚠⚠ **調べたら、物差しの側がずれていた。**

| | 前（v1534 まで） | ★ 直したあと |
|---|---|---|
| 何から系列を引くか | 前書きの `source: "stages:<系列>"` | **`:::stageTable` の `series`** |
| 何を測っていたか | 「このページが参照している repo データ」の系列ぜんぶ | **実際に表へ出している行** |
| 例題（`:::example`）の分子 | ⛔ **1件も測っていなかった** | ★ 別の線で測る（下） |

⚠ `source` は「参照している repo データ」の欄なので、**`:::example` の `stageId` を1件使うだけのページも
系列を名乗る**。★ 実際に `fat` がそうで、表は1行も出していないのに `stages:油脂と脂肪酸` を名乗り、
**読者が表で見もしないステアリン酸**が床の物差しに入っていた。

★★ `referenceShownStages` の前書きはもとから「**資料ページが実際に表へ出している分子**」と書いてあった
—— ⚠ **言っているとおりのものを返すようにしただけ**で、線を緩めたのではない。

★ **例題は別の線で測る**（`REF4` の②・新設）。⚠ `REF4b` が在庫の最大に当てているのと同じ読み:

- 床を割ってもよい。⚠ ステアリン酸は炭素18個（要る視野996）で、**資料を1pxも出さなくても床に載らない**
  ＝ どの分割の閾値でも救えない（§12-1「折り返しよりもスクロール、拡大縮小でユーザーが対応」の範囲）。
- ★ ただし **逃げ道が塞がっていないこと**（Ctrl+ホイールで床を越えられる）まで確かめる。
- ★ 否定対照つき: **床を割る例題が1件も無くなったら②は空回りしている**ので、そのとき赤くなる。

⚠ ＝ 分けた結果、**測る対象はむしろ増えている**（表を持たないページの例題も見るようになった）。
