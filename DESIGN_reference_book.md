# DESIGN_reference_book.md —— 参考書化（読み物として体系化し、キャンバスで試せるようにする）

**状態: 設計のみ（コードは1行も書いていない）。着手前の判断待ちが §7 に並んでいる。**
発端は `DEVELOPMENT.md`「開発方針（2026-09-01・ユーザー原文）」の **B. 参考書化**。

このファイルは **`DESIGN_reference_book.md` 単独で完結する**。既存の設計書には混ぜていない
（`qa/DESIGN_assembler_bridge.md` とは強く関係するが、あちらは「一問一答から assembler へ飛ぶ」話で、
こちらは「assembler の中に読み物を置く」話。**§2 で、あちらの資産をそのまま使う設計にしている**）。

---

## 1. ★★ 何を載せてよくて、何を載せてはいけないか（最初に決める）

⚠⚠ **「教科書・参考書内容を収録する」は、字義どおりにやると著作権侵害になる。**
★ **このリポジトリは過去に2度、著作物が入って公開履歴に残っている**（`git-history-leak-decision`。
**履歴は書き換えないと決着済み** ＝ **3度目を作らないことだけが残っている手段**）。

### 1-1. 線引き（この節が、この設計書のいちばん上位の制約）

| | 載せてよい | ⚠ 載せてはいけない |
|---|---|---|
| **事実** | ★ **命名規則そのもの**（「C10 の直鎖アルカンはデカン」） | —— |
| | ★ **分子式・示性式・構造** | —— |
| | ★ **物性の数値**（沸点・融点・密度・溶解度） | —— |
| | ★ **反応の起こり方**（何と何が、どういう条件で、何になるか） | —— |
| **表現** | —— | ⚠ **本文の文章**（言い回し・比喩・語順） |
| | —— | ⚠ **図・表そのもの**（レイアウト・矢印の引き方・色分け） |
| | —— | ⚠ **例題の選び方**（どの化合物を代表に選んだか） |
| | —— | ⚠ **章立て・配列**（どの順に、どう束ねて並べたか） |

★★ **＝「読み物として体系化」は *写す* のではなく *書く* こと。**
**事実は誰のものでもないので自由に使えるが、「その事実をどう並べ、どう説明するか」は書き手の表現。**

### 1-2. ⚠ 見落としやすい境界 —— 「表を作る」は安全だが「表の *選び方*」は安全ではない

ユーザーの例「**表でメタンからデカンまで分子式と名称を載せる**」は、**中身は完全に事実の羅列で問題ない**
（C1〜C10 の名称と CₙH₂ₙ₊₂ は、どの本を見ても同じでなければ間違い ＝ 表現の余地が無い）。

⚠ **危ないのは中身ではなく、次の3つ:**

1. ⚠ **「なぜその表なのか」を教科書から持ってくること。**
   「教科書のこの節にこの表があったから同じ表を作る」は、**表の *選定* を写している。**
2. ⚠ **「なぜその順なのか」を教科書から持ってくること。**
   ★ **索引の並びは §2 で *既存の自社資産* から決める**（＝ 配列を自分で決めた根拠が repo の中にある）。
3. ⚠ **「どこまで載せるか」を教科書の節の切れ目で決めること。**
   ★ 範囲の判断材料に教科書を使うのは `DEVELOPMENT.md` §「教科書に載っているか」の**1番（開発の判断材料）で許されている**が、
   **その節の *境界そのもの* を索引の単元境界にすると、章立ての模倣になる。**

★ **運用ルール: 1つの資料ページを作るたびに、「この表を作った理由」を1行で書けること。**
**書けないなら、それはどこかから持ってきている。**

### 1-3. ⚠ 「教科書ではこうなっている」を画面に出さない

`DEVELOPMENT.md` §「『教科書に載っているか』の扱い方」（2026-08-31・ユーザー明言）の3番がそのまま効く。

- ★ **出典は「開発の内側」に置く**（この設計書・レビューメモ・`qa/` の `evidence` フィールド）。
- ⚠ **学習者に出すのは *中身* だけ。** 「教科書 p.XXX にこうある」を資料ページの本文に書かない。
- ⚠ **読み込んでいるのは数研1社だけ**（`C:/Users/maequ/マイドライブ/化学/教科書/R5化学Vol.2-*.pdf`）。
  ★ **「教科書に無いので載せない」を資料の取捨の *断定的な* 根拠にしない。**
  ★ **`DEVELOPMENT.md` の「2社目が来たら真っ先に突き合わせる箇所」の A/B/C 表に、資料ページも足りるようにする**
  （＝ 参考書化で書いた資料のうち、1社ぶんの読みに依存しているものは、そこへ登録してから公開する）。

### 1-4. ★ 一次資料の扱い

`C:/Users/maequ/マイドライブ/化学/` の PDF は **読んでよい。写してはいけない。**
★ **読む目的は「事実の裏取り」と「範囲の判断」の2つに限る。**
⚠ **本文をコピーして下書きにするのは、あとで書き直しても *翻案* にしかならない。**
★ **書き方: 資料を閉じてから、事実だけを手元のメモ（分子式・数値・条件）にして、ゼロから文を書く。**

---

## 2. ★ 索引の作り方 —— 単元の切り方は「既にあるもの」から取る

⚠ **教科書の章立てを写さない**（§1-2 の2番）。★ **写さずに済むのは、repo が既に4つの単元体系を持っているから。**

### 2-1. 実測：索引に使える既存の資産（2026-09-01・実測値）

| 資産 | 場所 | 数 | 何を切っているか |
|---|---|--:|---|
| ★★ **知識項目コード** | `qa/questions.json` の `patterns` | **316項目** | ★ **知識の1粒**（`org.ali.alkane-names` など。**一度振ったら変えない主キー**） |
| ★★ **小項目（`group`）** | 同上の `group` | **52** | ★ **知識の束**（「分類・一般式」「アルカン」「シクロアルカン」…） |
| ★ **単元（`units`）** | 同上の `units` | **11** | ★ **有機の大単元**（脂肪族／アルコール・エーテル／元素分析／カルボニル／芳香族／フェノール／芳香族窒素／油脂／生体／高分子／手がかり） |
| ★★ **assembler への飛び道具** | 同上の `link` | **175 / 316（55%）** | ★ **「この知識を assembler のどこで試せるか」が既に書いてある** |
| ★ **ステージの系列** | `assembler/stages.json` の `series` | **19系列 / 120ステージ** | ★ **組み立ての順序**（操作の練習 → 直鎖アルカン → 異性体 → …） |
| ★ **絞り込みの条件カード** | `assembler/narrowing.js` `NARROW_CARDS` | **52枚** | ★ **「見分ける手がかり」の語彙** |
| ★ **化合物ライブラリ** | `assembler/compounds.json` | **941件** | ★ **呼び出せる分子の在庫**（`?summon=` の引数になる） |
| ★ **反応ルール** | `assembler/reactor.js` | **約64種**（id の実測 70、瓶とルールで重複あり） | ★ **キャンバス上で実行できる操作の在庫** |

★★ **結論: 索引の骨格は `qa` の「11単元 → 52小項目 → 316知識項目」を使う。**

**理由（＝ 配列を自分で決めた根拠。§1-2 の2番に対する答え）:**

1. ★ **これは既に自分たちで作った体系**（`qa/TAXONOMY.md`）で、**教科書の章立てではない。**
   ⚠ 教科書を*材料*にはしているが、**粒度・束ね方・コードの振り方はこちらの設計。**
2. ★★ **`link` が 175件、既に「assembler のどこで試せるか」を持っている** ＝
   **参考書化の核心（読む → 試す）の配線が、索引側に半分以上すでに書いてある。**
   **ゼロから索引を作ると、この 175件を捨てて作り直すことになる。**
3. ★ **コードが安定な主キー**なので、資料ページを `code` で名指しできる ＝
   **資料と一問一答と入試DB（`qa/data/exam_usage.jsonl`）が同じ座標で繋がる。**
4. ★ **難易度（Lv 1〜4）と `evidence`（教科書／セミナー／入試の出現）が既に各項目に付いている** ＝
   **「初学者にはここまで」を索引側で切れる**（教科書の節の切れ目を借りずに済む）。

⚠ **ただし `qa` の11単元を *そのまま* 資料の目次にはしない。** ★ **理由は §2-2。**

### 2-2. ⚠ 実測して分かった、`qa` の体系をそのまま使えない点

★ **`qa` の単元は「測る」ための切り方で、「読む」ための切り方ではない。**

- ⚠ **`org.ali`（脂肪族）だけで 72項目** ＝ **1ページに 72項目は読み物にならない。**
  ★ **読み物の1ページは `group`（52ある小項目）の粒度が妥当**（脂肪族なら「分類・一般式」12項目・「アルカン」11項目・…）。
  ★ **＝ 索引は3層（単元11 → 小項目52 → 項目316）だが、*ページ*は真ん中の52に置く。**
- ⚠ **`anal`（元素分析）・`clue`（手がかり）は、assembler で試せる相手が少ない**
  （`link` が `kind:"none"` で「分子を1つ組んでも見えない」と明記されている項目が実在する）。
  ★ **「試せる」を売りにする以上、試せない単元は資料ページを作っても片肺になる。**
  ★ **対策: ページごとに「試せる／読むだけ」を索引に出す**（⚠ 隠すと「押しても何も起きない」になる）。
- ★ **`assembler/stages.json` の19系列は、`qa` の11単元と粒度が近いが一致しない**
  （例: `stages` は「異性体をつくる（枝分かれアルカン）」を独立した15ステージの系列にしているが、
  `qa` では `org.ali` の中の一部）。⚠ **どちらかに寄せるのではなく、資料ページから *両方* へリンクする。**

### 2-3. ★ 索引の形（ヘルプ的なインデックス）

★ **ユーザーの「ヘルプのようにインデックス化」に、既存の `#tutorial-list`（操作ガイド）が形として一番近い**
（`tutorials.json` は 13件、**`keywords` 配列を持ち `#tutorial-search` で絞り込める**）。

★ **同じ形（検索欄＋一覧）を資料側にも作る。** ⚠ **同じ *モーダル* に混ぜない**（操作の説明と化学の説明は別物）。

```
📖 資料（参考書）
 ├ [検索欄]  ← qa の code / knowledge 本文 / 別名 を横断
 ├ 脂肪族炭化水素（72）
 │   ├ 分類・一般式（12）        [読む] [試せる:6]
 │   ├ アルカン（11）            [読む] [試せる:9]   ← 例のページ
 │   ├ シクロアルカン（…）        …
 │   └ …
 ├ アルコール・エーテル（…）
 └ …
```

⚠ **`[試せる:N]` の N は `link` を持つ項目数（実測 175/316）から機械で出す。手で書かない。**

---

## 3. ⚠ スマホ縦を「捨てる」前に測った結果

★ ユーザーは「**スマホ縦画面の対応はすててもよいかも**」と書いた。
⚠ **統合セッションの申し送りは「SNS は Instagram が効く＝入口は縦」だった。**
★ **測った結果、判断はもう少し細かくなった。**（実測は §3-2。）

*（この節は測定後に確定。以下 §3-1 は静的な読み取り。）*

### 3-1. ⚠ 申し送りの訂正：`.wide-figure` は「横向き前提の仕組み」ではない

★ **統合セッションの申し送り: 「`.wide-figure` という仕組みが立体視で使われています（既に横向き前提の機能があるか）」**

⚠ **読んだ結果、これは逆だった。**

`assembler/style.css` 2154-2167 / 2185-2222・`assembler/stereo.js` 1500 / 2542 の実体は:

- **`.wide-figure` は「図が広いあいだ」だけ `stereo.js` が付けるクラス**（鏡像を並べているとき）。
- ★ **効果は「2カラムのグリッドを1カラムに戻す」**（`display: grid` → `display: block`）。
- ★ **狭い画面用の枝（≤899px 等）では「高さの固定 168px を外して図を縦に伸ばす」。**

★★ **＝ これは *横向きを要求する* 仕組みではなく、*横に並べられないときに縦へ積む* 仕組み。**
★ **repo が既に持っている「2ペインが入らないときの答え」は「積む」であって「諦める」ではない。**
⚠ **参考書化の分割レイアウトも、同じ作法（広ければ横に並べ、狭ければ積む／切り替える）に寄せるのが自然。**

★ **横向き専用の枝は別に実在する**: `@media (max-width: 899px) and (orientation: landscape)`（`style.css` 2030-）。
⚠ **ただしこれは「モバイル横で高さが足りないときの詰め」であって、機能を横向きに限定してはいない。**
★★ **＝ 現時点で「横向きでないと使えない機能」は assembler に1つも無い。**
⚠ **参考書化が最初の「横向き限定機能」になるなら、それは前例を作る判断であり、設計書で明言すべき。**

---

*（§3-2 以降・§4〜§7 は執筆中）*
