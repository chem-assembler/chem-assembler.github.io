# 断片に割る（絞り込みモード M6）設計書

大きい分子を**断片の分子式に割って、小さくなったところで列挙に渡す**層。
`DESIGN_narrowing_mode.md` の M1〜M5 の続きではなく、**列挙（M1〜M4）と配分（M5）の間に挟まる**。

実装前の合意用ドキュメント。

## 0. なぜ要るか — 3問を数えて分かったこと

列挙が届かない3問（名古屋大 C12H14O2・岡山大 C13H16O4・神戸大 C14H20）の標準ルート29手を、
手筋の性質で3つに分けた（`_解析/tools/analyze-allot-boundary.js`）。

| | 手数 | 割合 | いまの状態 |
|---|---:|---:|---|
| ◆ **配分**（部品の割り振りだけで言える） | 4 | 14% | M5 で動いている |
| ◇ **断片**（分子を割って分子式を出す） | 12 | **41%** | **何も無い** |
| ○ **構造**（1つずつ見ないと言えない） | 13 | 45% | M1〜M4 で動いている |

**当初「大きい分子は配分が山」と見ていたが、外れだった。**
配分が担うのは各問の冒頭2手までで、4手のうち3手は「分子式から不飽和度を出す」。
本当に配分エンジンらしい仕事は岡山大の「O が4つ → 高々2価エステル」1手だけだった。

```
名古屋大  構造を見ずに進めるのは最初の 3 手（全 8 手）
岡山大    最初の 4 手（全 9 手）
神戸大    最初の 3 手（全 12 手）
```

### 決め手 —— 断片は列挙の射程に落ちる

| 問題 | 断片 | 重原子 | |
|---|---|---:|---|
| 名古屋大 | アルコール側 C4H8O | 5 | ✓ |
| 名古屋大 | 酸側 C8H8O2 | 10 | ✗ |
| 岡山大 | B 安息香酸 C7H6O2 | 9 | ✗ |
| 岡山大 | C 乳酸 C3H6O3 | 6 | ✓ |
| 岡山大 | D C3H8O | 4 | ✓ |
| 神戸大 | D〜F C9H10O | 10 | ✗ |
| 神戸大 | G・H C5H10O | 6 | ✓ |

**7つ中4つが射程（重原子8個）に入り、届かない3つはすべてベンゼン環を含む。**
そして**構造を見る13手は、すべて脂肪族の断片の上で起きている**。
ヨードホルム・不斉炭素・アルキル基の数え上げ・銀鏡は、どれも C3〜C5 の断片に対する判定。

芳香族側で構造を見るのは神戸大の「等価な水素の種類数」だけで、
これは列挙ではなく**置換位置の組み合わせ**の話（→ §6 やらないこと）。

**結論: 配分側にカードを作り直す必要は無い。要るのは橋。**

```
① 配分で枠を切る            M5（動いている）
② 断片の分子式を出す        ← ここが41%。M6 で作る
③ 脂肪族の断片を列挙へ渡す  M1〜M4（動いている・35枚のカードがそのまま効く）
```

## 1. 基本方針

- **断片も「分子式＋制約」で表す**。列挙側と同じ形にして、そのまま渡せるようにする
- **割り算は分子式の足し引きだけ**。構造は作らない（作れない大きさだから断片にしている）
- **割った結果が重原子8個以下なら、そのまま列挙モードを開く**。これが M6 の出口
- **新しい化学ロジックは書かない**の原則は維持する。足し引きの規則は既存の手筋の言い換え
- 既存の列挙・配分には**一切影響しない**。第3のパネルとして足す

## 2. 割り算は機械で出る（試作で確認済み）

`_解析/tools/split-fragments.js` で3問ぶんを試し、**5件すべて一致**した。

```
岡山大  2価エステル C13H16O4 ＋ H2O×2 − 安息香酸 − C3H8O = C3H6O3（乳酸）✓
岡山大  分子量90 − 45（−COOH）− 17（−OH） = 28 = C2H4 ✓
名古屋大 1価エステル C12H14O2 ＋ H2O − C4H8O = C8H8O2 ✓
神戸大  オゾン分解 C14H20 ＋ O×2 − C9H10O = C5H10O ✓
神戸大  分子量134 − 77（C6H5−） = 57 = C3H5O ✓
```

規則は4つだけ。

| 操作 | 足し引き | 元の手筋 |
|---|---|---|
| 加水分解 | ＋H2O ×（価数） | `ester-hydrolysis` |
| 式量の引き算 | − 官能基の式量 | `fragment-from-molecular-weight` |
| オゾン分解 | ＋O ×2 | `ozonolysis` |
| 側鎖酸化 | 側鎖を −COOH に置換 | `side-chain-oxidation` |

### ⚠ 置換基は分子ではない（試作で判明）

神戸大の「134 − 77 = 57 → C3H5O」で気づいた。
**一価の断片（置換基）は水素の数が奇数になり、不飽和度の式 (2C+2−H)/2 が半端な値になる**
（C3H5O なら 1.5）。付け根に H を1つ足して分子に閉じてから数える必要がある。

これは表示だけの問題ではない。
**「残り57」を C4H9（飽和）と決め打ちすると C3H5O（C=O ひとつぶん不飽和）を落とす**という、
`fragment-from-molecular-weight` の注意書きに書いてある取りこぼしそのもの。

### 式量から組成を出すときは、配分の残り不飽和度で絞る

式量だけで組成を探すと候補が複数出る。しかも**形式的に成り立つだけの組成が混ざる**
（式量57 なら C4H9・C3H5O のほかに C2HO2 が出る）。

そこで**配分エンジンが持っている「残りの不飽和度」を渡す**。これで1つに決まる。
試作で4件すべて一意になった。

```
神戸大 134−77 の残り     式量57  3候補 → 不飽和度1 で C3H5O のみ ✓
岡山大 90−45−17          式量28  1候補 → 不飽和度1 で C2H4 のみ  ✓
岡山大 C（分子量90）      式量90  5候補 → 不飽和度1 で C3H6O3 のみ ✓
神戸大 G・H（式量86）     式量86  5候補 → 不飽和度1 で C5H10O のみ ✓
```

**ここが M5（配分）と M6（断片）のつなぎ目**になる。
配分は「不飽和度6のうちベンゼン環に4、C=O に1、残り1」を出す層で、
その**残り1**が、断片の組成を1つに決める鍵として効く。

候補が2つ以上残るときは**全部出して選ばせる**。決め打ちしないことが
`fragment-from-molecular-weight` の取りこぼし対策そのものなので、
1つに絞れないことを隠さない。

⚠ 不飽和度の範囲は **0 以上・炭素数以下**で切る。これを入れないと
C3H9O2（閉じると不飽和度 −1）のような価標が成立しない組成が残る。

## 3. 画面

列挙・配分と並ぶ3つめのパネル。

```
┌─ 断片に割る ────────────────────────────────┐
│  もとの分子式  C13H16O4      不飽和度 6・O 4個   │
│                                                  │
│  ▸ 割り方                                        │
│     ◉ 加水分解（エステル）  価数 [2 ▾]           │
│     ○ オゾン分解（C=C を切る）                   │
│     ○ 式量を引く                                 │
│                                                  │
│  ▸ 分かっている断片                              │
│     [C7H6O2  安息香酸  ✕]                        │
│     [C3H8O            ✕]        [＋ 足す]        │
│                                                  │
│  ── 残り ────────────────────────────────       │
│     C3H6O3   不飽和度1・重原子6・式量90          │
│     → 重原子6なので **列挙で追えます**           │
│       [この断片を絞り込む]                       │
└──────────────────────────────────────────────────┘
```

- **分かっている断片を積んでいくと、残りが引き算で出る**（列挙側でカードを積むのと同じ操作感）
- 残りが重原子8個以下なら **[この断片を絞り込む] が出る**。押すと列挙パネルへ渡る
- 8個を超えていたら「まだ大きいので、もう1回割ってください」と出す
- 式量から引くときは、**組成の候補を全部出す**（C4H9 と C3H5O の両方）

## 4. データ

問題側は既存の `_解析/db/narrowing/*.json` を拡張する。列（target）に `split` を足す。

```json
{
  "label": "C を断片に割る",
  "split": {
    "whole": "C13H16O4",
    "op": "hydrolysis", "valence": 2,
    "known": ["C7H6O2", "C3H8O"],
    "expect": "C3H6O3"
  },
  "then": { "steps": [ … 列挙側のカード … ] }
}
```

`expect` を書くので、**割り算がずれたら生成器（`build-narrowing-data.js`）が落ちる**。
M4 で入れた「翻訳がずれていたら生徒に嘘の候補数を見せる」への歯止めと同じ考え方。

## 5. 段階案

### M6-1: 割り算パネル（最初に実装）

- 分子式を入れ、割り方（加水分解・オゾン分解・式量引き）を選ぶ
- 分かっている断片を積むと残りが出る
- 残りの重原子数を出し、8個以下なら列挙へ渡すボタン
- 式量引きは**候補の組成を全部出す**

### M6-2: 列挙への受け渡し

- `narrowing` の列挙側に**任意の分子式**を渡せるようにする
  （いまはプリセット9つだけ。断片は C3H6O3 のように未登録の分子式になる）
- 渡した先で既存の35枚のカードがそのまま使える
- 焼いていない分子式は列挙に時間がかかるので、**重原子7個以上は警告を出す**

### M6-3: 入試問題（3問）

- 名古屋大・岡山大・神戸大の仕様に `split` を書く
- **神戸大は出題ミスで絞り切れない**（`errata` に記録済み）。
  「ここから先は問題文が壊れている」と表示して止めるのが正しい見せ方

## 6. やらないこと

- **芳香環の置換位置の数え上げ**。o/m/p や三置換の 1,2,3 / 1,2,4 / 1,3,5 は
  列挙ではなく組み合わせの問題で、別の数え方が要る。神戸大がこれを使うが、
  そもそもあの問題は出題ミスで決まらない。**必要になってから**
- **芳香族の断片を構造まで追うこと**。C7H6O2 や C9H10O は重原子9〜10個で列挙が届かない。
  断片の分子式が出たところで止め、名前で答え合わせする
- **反応の妥当性の判定**。「この分子式ならこの加水分解が起きる」という判断はしない。
  どう割るかは生徒（または問題データ）が指定する

## 7. 検算

`test.html` に足す。

- 岡山大: C13H16O4 ＋ H2O×2 − C7H6O2 − C3H8O = **C3H6O3**
- 名古屋大: C12H14O2 ＋ H2O − C4H8O = **C8H8O2**
- 神戸大: C14H20 ＋ O×2 − C9H10O = **C5H10O**
- 分子量90 − 45 − 17 = **28**（CH2 で割り切れて C2）
- 分子量134 − 77 = **57**。候補は C4H9 / C3H5O / C2HO2 の3つで、
  **不飽和度1を渡すと C3H5O ひとつに決まる**
- 置換基の不飽和度は H を1つ足してから数える（C3H5O → 1。1.5 にならない）
- 不飽和度が 0 未満または炭素数超の組成は出ない（C3H9O2 が混ざらない）
- 割った結果が重原子8個以下なら列挙へ渡せ、9個以上なら渡せない
- 否定対照: **不飽和度を渡さなければ候補は複数のまま**（絞れたのが偶然でないこと）

## 8. 既存資産との関係

| 使うもの | 出どころ |
|---|---|
| 分子式の解析・足し引き | `_解析/tools/split-fragments.js`（試作済み・移植する） |
| 列挙・35枚のカード | `assembler/narrowing.js`（M1〜M4） |
| 配分エンジン | `assembler/narrowing.js`（M5） |
| 手筋の対応 | `_解析/db/moves.json` の `ester-hydrolysis` / `fragment-from-molecular-weight` / `ozonolysis` / `side-chain-oxidation` |

**新しい化学ロジックは書かない。** 足し引きの規則は、既存の手筋の注意書きをそのまま式にしただけ。
