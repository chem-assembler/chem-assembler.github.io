# DESIGN_paid_delivery.md —— 有料の問題集をどう配るか（配信・認証・決済）

**状態: 設計だけ。コードは1バイトも変えていない。外部サービスのアカウントも鍵も作っていない。**
基準は `origin/main` = `90ba424`（2026-09-02）。

★ **この文書の担当は「配る仕組み」だけ** —— **配信の単位／アプリ本体をどこまで静的に残すか／
認証／記録の持ち方／決済に要るもの／認証スパイクの発注書。**
⚠ **何を問題にするか（題材・型・玄関）は `DESIGN_paid_workbook.md`、線の引き方は
`DESIGN_review_pack1.md` §1、参考書は `DESIGN_reference_book.md` の担当で、ここでは繰り返さない。**

★ **前例ゼロの領域**（実測・再確認: `Authorization` / `credentials:` / `Bearer` / `supabase` の出現は
5アプリ全部で **0件**。`fetch` は全部相対 URL）。⚠ **ここで決めたことは無機の問題集にもそのまま効く**ので、
**assembler 専用に書かない**（§3-4）。

---

## 0. ★ 決まっていること（ユーザー決定。原文は `DEVELOPMENT.md`「開発方針（2026-09-01）」）

| # | 決定 | この文書でどう効くか |
|--:|---|---|
| 1 | **参考書は無料（例題つき）／問題集は有料** | 有料になるのは練習問題だけ。参考書・例題・既存資産は静的のまま1バイトも触らない |
| 2 | ★ **有料の実体は「サーバーで配る」** | §1・§2 |
| 3 | ★ **問題は「実際にアプリで解く」** | ⚠ **＝ 問題は必ずブラウザに届く**（§1 の出発点） |
| 4 | **説明＝表／例題＝代表1つ／練習問題＝同じテーマの別の1つ** | 配信の単位を「ページ（小項目）」に置く根拠（§2-1） |
| 5 | ★★ **有料の練習問題は新規題材のみ** | 既存 120 ステージを取り上げない ＝ 静的側の変更はゼロ |
| 6 | **値付けは後で・サブスクを検討** | 「いつまで」を守る仕組み（期限つきの資格）が最初から要る（§3-2） |
| 7 | ★ **玄関は案C（索引はハブ・中身はその分野のアプリ）／有機 → 無機** | 認証と配信は「アプリをまたぐ1つの層」にする（§3-4） |
| 8 | **アプリは5本キャップ。分野で割る** | 認証の層は6本目のアプリにしない（§3-4 で決めた） |

---

## 1. ★★ 前提 —— 「サーバーで配る」＝ 内容が隠れる、ではない

> ★★ **「アプリで解く」なら問題は結局ブラウザに届く。守れるのは「誰に・何を・いつ配るか」だけ。**

### 1-1. 何が届くか（現物で確かめた）

★ **練習問題は既存の枠で解く**（`DESIGN_paid_workbook.md` D-W13・ユーザー決定5と両立）。
**＝ 配るものは `stages.json` の1件と同じ形で、`target`（＝答えの構造）を含む。**
採点は `game.js` の正準コード照合、外したときの助けは `explainPlacementMiss`——**どちらも `target` を手元に要する。**
⚠ **＝ 答えごと届く。** 開発者ツールを開けば、届いた JSON はそのまま読める。

### 1-2. 誰から何を守るのか（守りの相手を4つに分ける）

| | 相手 | ★ 何で止まるか | ⚠ 配る単位は効くか |
|---|---|---|---|
| **(a)** | **買っていない人** | ★ **認証**（資格が無ければ 401） | **無関係**（一括でも1問ずつでも同じ） |
| **(b)** | **買った人が全部吸い出して配る** | ⚠⚠ **止まらない** | ⚠ **ほぼ無関係。** 52ページなら 52回、520問なら 520回叩けば終わる。**どちらもスクリプト1本・数秒〜数十秒**。手間の差は「1回」と「520回」だが、人が手で叩く量ではないので差にならない |
| **(c)** | **買った人の期限が切れたあと** | ★ **期限**（資格の `expires_at`）＋ ★ **問題を端末に永続保存しない**（§2-2） | ★ **効く**——ただし単位ではなく「その都度取りに行く」ことが効く |
| **(d)** | **アカウントの共有** | ⚠ 同時セッション数の制限などで *減らせる* が止まらない | 無関係 |

★★ **＝ 「1問ずつ配る」は、(b) に対して *ゼロ* ではないが *ほぼゼロ*。** (a)(c) は単位と無関係に守れる。
⚠ **`DEVELOPMENT.md` の「設計の芯は『1問ずつ配る』形にできるかどうか」は、芯ではない。**
**芯は「資格（誰に）」と「期限（いつ）」の2つで、配る単位はその外にある**（単位は §2-1 で *別の理由* から決める）。

### 1-3. ★★ 判断: 「守れないから意味がない」ではなく「これで十分」

⚠ **逃げずに言い切る: (b) は守らない。守ろうとしない。それで十分。** 理由は4つ:

1. ★★ **有料の価値は問題文ではなく「アプリで解く体験＋採点＋記録」にある**（`DESIGN_review_pack1.md` §1-2 の結論。
   ユーザー決定「学びは伏せることではなくやってみること」と同型）。**JSON を吸い出した人は、問題文は手に入れるが体験は手に入らない**
   （既存の無料の器で解くには、結局このサイトに戻ってくる）。
2. ★ **こちらの無料は既に厚い**（120ステージ・316項目・書き出し練習・入試問題25問がぜんぶ無料）。
   ⚠ **盗まれて損なわれる差分は「新規の練習問題 156〜520問」だけ**で、それを守るために全体の作りを重くする釣り合いが取れない。
3. ★ **先行例は紙のドリル**（`DEVELOPMENT.md`「化学のグルメ」の実測）。**紙はコピーし放題だが商売は成立している。**
   デジタルにしたから急に秘匿が要る、とはならない。
4. ⚠⚠ **秘匿しようとすると、採点をサーバー側へ移すことになる**（答えを届けない唯一の手）。
   これは **既存の無料の採点（正準コード照合・`explainPlacementMiss`）と二重の実装**になり、
   **1操作ごとにサーバー往復**が入り、**オフラインで解けなくなる**。しかも「ブタンをつくれ」は問題文が答えの名前そのもの。
   ★ **却下。採点はクライアントのまま。**

★ **代わりに守るもの（これは本気で守る）:**

- **(a) 資格の無い人には 401 を返す**——サーバーは資格を見ずに問題を返さない（§3）。
- **(c) 期限**——資格に `expires_at` を持ち、**アプリは問題を localStorage に置かない**（メモリだけ。§2-2）。
  ★ サブスクの「いつまで」はこの2つだけで成立する。

⚠ **1つだけ約束ごと**: ★ **「サーバーで配る」を、規約・SNS・内部文書のどこにも「コピーできない」「内容が守られる」と書かない。**
**書くなら「購入した人にだけ配信します」。** 守っていないことを守っていると言うのが、いちばん後を引く。

### 1-4. ★ 実測: 単位を変えても通信量は決め手にならない

⚠ `DESIGN_paid_workbook.md` §4-3 の「1ページ 3〜10 KB」を疑って測り直した（`stages.json` 120件・node の `zlib.gzipSync` level 6）:

| まとめ方 | gzip 後 | 1問あたり |
|---|--:|--:|
| **1問ずつ** | **391 B**（中央値・平均 396・最大 589） | 391 B |
| **3問** | **663 B** | 221 B |
| **5問** | 891 B | 178 B |
| **10問** | **1,419 B** | 142 B |
| **系列ごと**（5〜15問・実在の19系列） | **0.5〜2.1 KB** | 78〜301 B |
| 参考: 入試問題（絞り込み）1問 | 714 B | |

★★ **＝ 組む型の練習問題は、1ページ（3〜10問）で 0.7〜1.5 KB。**
⚠ **§4-3 の「3〜10 KB」は 3〜7倍大きい**（方向は変わらない。qa の設問を混ぜた見積りだったと思われる）。
★ **全 520問を1問ずつ取っても 200 KB、1ページずつなら 70 KB 前後。**
**通信量では単位が決まらない**——だから §2-1 は通信量を理由にしない。

---

## 2. ★ 配信の単位と、静的をやめる範囲

### 2-1. ★★ 単位: 「1ページ（小項目）」ずつ・1リクエスト

| 単位 | 回数（52小項目・156〜520問） | ⚠ 何が起きるか | 判定 |
|---|--:|---|---|
| **全部まとめて** | 1 | 一括で届く。(c) 期限は「次に開いたとき」まで効かない。★ 守りの差は小さいが、**開いてもいないページのぶんまで毎回運ぶ** | × |
| ⚠ **1問ずつ** | 156〜520 | ★★ **「次の問題」を押すたびに往復が入る**（別オリジン＋`Authorization` なので **CORS のプリフライトを含めて2往復**）。解いている最中に待ちが出る。得るものは §1-2 のとおりほぼ無い | × |
| ★★ **1ページずつ** | **52** | ★ **ページを開いた瞬間に1回。以後そのページの中では往復なし。**「読んだページのぶんだけ配る」＝ 玄関の単位（`DESIGN_paid_workbook.md` §4-3 の理由はこのまま正しい） | ★ **採用** |
| **1単元ずつ** | 11 | 回数は減るが、ページ単位の利点（開いたぶんだけ）を失う割に得るものが無い。⚠ 単元は 2〜72項目で大きさが揃わない | × |

★ **理由は「人が1回に読む量 ＝ 1回に配る量」の1つだけ。**守りでも通信量でもない（§1）。
★ **副次: ページを開くたびに資格の確認がサーバーで走る ＝ サブスクの「いつまで」がページ単位の粒で効く。**

**★ 応答の形（決めた）:**

```
GET https://<api>/workbook/<field>/<page>          field = organic | inorganic …（§3-4）
    Authorization: Bearer <アクセストークン>          page  = qa の小項目コード（例 org.aliphatic.alkane）

200  { "schema": 1,                                 ← §2-3 の握手
       "field": "organic", "page": "…",
       "expires": "2026-10-01T00:00:00Z",           ← 資格の期限（画面に出す用。守りは サーバー側）
       "problems": [ { …stages.json の1件と同じ形… }, … ] }
401  資格なし／期限切れ  → 「買うと解ける」の印（ページは無料部分だけで成立）
404  そのページに練習問題なし → 何も出さない（reaction.js と同じ「無ければ隠す」）
5xx / 通信不能         → 「いま届きません。無料部分はそのまま使えます」
```

⚠ **`problems` の1件は `stages.json` の1件と *同じ形* にする**（`id`・`name`・`target`・`series`…）。
**採点・ヒント・クリア判定の既存関数がそのまま動く ＝ 新しい採点は1本も書かない**（D-W13）。
★ **「反応をたどる」型は `DESIGN_review_pack1.md` §6-2 レーン2の課題 JSON の形をそのまま使う**（`id`/`start`/`goal`/`hands`）。
**型は `kind` の1フィールドで見分ける。**

### 2-2. ★ アプリ本体は静的のままでよい —— よい。前例と流儀が既にある

★ **前例**: `reaction.js` の `load()` は `reactions.json` が取れなければ `this.box.style.display='none'`（**ビューアごと隠す**）。
`narrowing.js` の `loadProblems()` は「読めなくてもモードは動く」と自分で書いている。
★ **有料の受け口は、この作法をそのまま別オリジンに向けるだけ。**

**⚠ 実測して分かった、設計書が見落としていた前提:**

| assembler が読む JSON | 読み方 | `?v=` |
|---|---|---|
| `stages.json` / `compounds.json` / `quiz-scope.json` | `fetch(url, { cache: 'no-cache' })` | ⚠ **無し**（コメント「?v=バスターが付かないため、更新が届かない事故を防ぐ」） |
| `reactions.json` / `narrowing-problems.json` / `tutorials.json` | 同上 | **無し** |
| `isomers-baked.json` | `?v=${APP_VERSION}` ＋ `no-cache` | 有り（★ 8か所中これ1つだけ） |
| 参考: `qa/app.js` の `questions.json` / `exam_usage.jsonl` | `?v=92` | 有り（qa の流儀） |

★★ **＝ assembler の JSON データは、もともと `?v=` ではなく `cache:'no-cache'`（毎回サーバーに再検証）で守られている。**
⚠ **「サーバー配信のデータに `?v=` が効かない」（`DESIGN_review_pack1.md` §3-2 #2）は、新種の問題ではなく assembler の既存の流儀そのもの。**
**API の応答に `?v=` を付けないのは例外ではない。** 効かせるべきは `?v=` ではなく §2-3 の握手。

**★ 静的側に足すもの（決めた）:**

| どこ | 何 | ⚠ 注記 |
|---|---|---|
| `account/client.js`（新規・共通層 §3-4） | API の URL・schema 定数／サインイン／`fetch` ラッパ | ★ **`Authorization` を書くのは repo 中ここ1か所だけ** |
| `assembler/workbook.js`（新規） | ページを開いたら `client.js` 経由で1回取る → `problems` を既存の枠に流す／401・404・5xx の見せ方 | 静的。`?v=` の対象 |
| `assembler/index.html` | 上の2本の `<script … ?v=NN>` | 規則4・規則6 が版ずれを検出（ion-equation → ratio の前例） |

⚠⚠ **問題を localStorage に置かない**（§1-3 (c)）。**メモリに持ち、ページを離れたら捨てる。**
★ **解いた記録（クリアの印）は今までどおり localStorage に置いてよい**——期限が切れても「解いた」事実は消えない（§6 で決めた）。
⚠ **Service Worker は5アプリとも使っていない**（実測 0件）ので、API 応答が勝手にキャッシュされる経路も無い。

### 2-3. ★★ `?v=` の規約と検査の死角をどう塞ぐか

**何がずれるか**: 静的な `app.js` は版で固定され、API の応答はサーバーで随時変わる。ずれは2方向:
**(i) 古い app.js × 新しい応答**（ブラウザのキャッシュ）／**(ii) 新しい app.js × 古い応答**（サーバーの更新もれ）。
`?v=` はどちらにも手が届かない。★ **手当ては3段:**

| 段 | 手当て | 前例 |
|--:|---|---|
| **1** | ★★ **応答に `schema` を持たせ、クライアントの定数 `WORKBOOK_SCHEMA` と不一致なら「無ければ隠す」＋「再読み込みしてください」の1行** | `reaction.js`（隠す）。**両方向のずれを同じ1つの分岐で受ける** |
| **2** | ★★ **API の URL と `schema` は `account/client.js` の先頭の定数にしか書けない**。`account/test.html` に検査を置く: (a) 定数が存在し `https://` で始まる (b) **定数ファイル以外の全 JS/HTML に API のホスト名（`supabase.co` / `functions/v1`）が出ない** (c) **サーバー側コードの `SCHEMA` 定数と一致する**（サーバー側コードも repo に置く・下記） | ★ **`qa/tests.js` の `runVersionTests`**（「verify-release の死角」を名指しで塞いだ検査）と同じ場所・同じ作法。⚠ 比べる相手が「index.html の版」ではなく「サーバー側の定数」になるのが違い |
| **3** | ★ **`verify-release.js` に規則10を足す（設計だけ・このレーンでは触らない）**: 「`https://` の URL のうち API のホストを含むものは、`account/client.js` 以外に出てはならない」 | ⚠ **`isExternal` の除外はそのまま残す**（外部 URL に `?v=` を求めるのは誤り）。**塞ぐのは「外部だから見ない」の穴のうち API の部分だけ** |

★ **サーバー側コードの置き場: repo の `server/`**（Edge Function の TypeScript 2本 ＋ テーブル定義の SQL）。
⚠⚠ **Jekyll がそのまま配信する**（`_config.yml` の実測: 追跡ファイルは既定で全部 chem.schoollenz.com から読める）。
★ **`_config.yml` の `exclude` に `server/` を足す**（`tools/` と同じ扱い）。鍵は置かない（Edge Function の secret はダッシュボード側）。

★ **版の運用（決めた）**: `schema` を上げるときは **サーバーを先に新旧両対応にしてから、アプリの版を上げる**（後方互換1版）。
逆順だと (ii) が全ユーザーに同時に出る。

---

## 3. ★★ 認証 —— 何を認証し、どこに置き、分野をどうまたぐか

### 3-1. 何を認証するか: **「購入者かどうか」だけ。ただし識別子はメールアドレス1つ持つ**

| 案 | 何を持つか | ★ 長所 | ⚠ 短所 | 判定 |
|---|---|---|---|---|
| **(a) ライセンスキー** | メールも持たない。決済→キー発行→入力 | 個人情報ゼロ。privacy.html を変えずに済む | ⚠⚠ **サブスクの更新と結べない**（キーに期限を焼くと更新のたび再発行）。失くしたら再発行の相手が分からない。端末をまたぐ手段がキーの手打ちだけ | × |
| ★★ **(b) メールアドレス＋6桁コード（OTP）** | **メール1つ**。氏名・学校・学年は持たない | ★ **決済（Stripe の顧客）とサーバー（資格）を同じメールで結べる**。端末をまたげる。パスワードを預からない | ⚠ **メールは個人情報**（APPI）→ privacy.html の改定が要る（§4）。メール送信の基盤が要る（§3-3） | ★ **採用** |
| **(c) Google サインイン** | Google の ID | 入力が楽 | ⚠ Google Cloud の OAuth 同意画面の作成（ユーザー作業）・Google 依存・学校端末で Google が塞がれている例 | 第2段で足す |

★ **(b) で「magic link」ではなく「6桁コード」にする理由**: リンクは iOS でメールアプリの内蔵ブラウザで開き、
**サインインした状態が Safari 側に残らない**事故が定番。コードなら開いているタブに打つだけ。

⚠⚠ **privacy.html はいま「氏名・メールアドレス…を一切取得しません」「ログイン機能はなく」と明記している**（実測・第1条）。
**(b) を採った時点で虚偽になる** ＝ **改定はユーザー作業（§4 の一覧）**。★ ハブの約束「無料・広告なし・ログイン不要」は
**無料部分については真のまま**なので、`schoollenz-strategy` の言い換え（「ログイン不要で使える／記録・問題集はログインすれば」）で残す（→ `D-D6`）。

### 3-2. ★ 記録の持ち方: 1ユーザー1行（D-W8 に同意）。ただし第1弾では *同期しない*

★ **サーバーに持つ表は2つだけ**（認証そのものの表は Supabase Auth 側が持つ）:

```
entitlements ( user_id, product, expires_at, source, updated_at )   ← 資格。product = 'organic-workbook' | 'inorganic-workbook' | 'all'
progress     ( user_id, app, data jsonb, updated_at )               ← 記録。app = 'assembler' | 'qa' | 'muki' …（アプリごと1行）
```

- ★ **`progress.data` の形は、各アプリがいま localStorage に置いている JSON そのまま**（`slz-qa-v2`・`chemAssembler.cleared` …）。
  `schoollenz-strategy` の「Lv1 ローカル → Lv2 同期」を、**形を変えずに持ち上げる**ため。
- ★ **1解答1行は採らない**（60 MB/1,000人 vs 4 MB/1,000人・`DESIGN_paid_workbook.md` §4-5）。
- ⚠⚠ **第1弾では `progress` を *作らない*。記録は localStorage のまま**（→ `D-D2`）。理由:
  ① 「誰に・いつ」を守るのに記録の同期は要らない ② 記録の同期は認証よりもう1段重い責任（他人の学習履歴を預かる＝APPI の保管・削除の経路）
  ③ 第1弾の新規は「認証つき fetch」だけに絞るほうが、前例ゼロの領域では安全（`DESIGN_review_pack1.md` §6-2 の「認証だけスパイク」と同じ向き）。
  ★ **表の形だけ先に決めておく**（上）ので、後で足しても移行は要らない。

### 3-3. ★★ Supabase に相乗りするか、別に立てるか —— 判断材料と推奨

⚠ **まず、設計書の前提を1つ直す**: `DEVELOPMENT.md` / `DESIGN_paid_workbook.md` §4-5 は「無料枠 1 GB が逼迫」を相乗りの是非の物差しにしているが、
★ **逼迫しているのは *File Storage*（録音・PDF・`gradelens-supabase-quota`）で、問題と記録が入るのは *Database*（Free で 500 MB・別の枠）**。
**問題 <1 MB・記録 4 MB/1,000人 は DB 枠の 1% 未満 ＝ 容量は相乗りの是非を決めない。** 効くのは次の2つ:

| ⚠ 本当に効くもの | 中身 | 出典 |
|---|---|---|
| ⚠⚠ **Free は「7日間 API 呼び出しが無いと停止」** | 別立て Free だと、**買う人がまだ少ない時期に *有料の中身が止まる***。停止中は 401 でも 404 でもなく接続不能 ＝ 買った人に「いま届きません」が出る | Supabase 料金ページ（2026-09-02 取得）「Free projects are paused after 1 week of inactivity」 |
| ⚠ **GradeLens の Fair Use** | 相乗りすると、GradeLens の Storage 超過で制限がかかったとき **化学側が道連れ**（同じプロジェクト＝同じ制限） | `gradelens-supabase-quota`（2026-08-29 から適用） |

**★ 料金と枠の実測（Supabase 料金ページ・2026-09-02）:**

| | Free | Pro |
|---|---|---|
| 月額 | $0 | **$25/月（組織単位）**＋ 2本目以降のプロジェクトの compute（最小 $10/月） |
| 有効プロジェクト | **2 本まで**（組織あたり） | 無制限 |
| Database | 500 MB | 8 GB |
| File Storage | 1 GB | 100 GB |
| MAU（認証） | 50,000 | 100,000 |
| Edge Function 呼び出し | 500,000/月 | 2,000,000/月 |
| ⚠ **停止** | **7日無活動で停止** | **停止しない** |
| メール送信 | ⚠ **内蔵 SMTP は 2通/時**（本番不可・自前 SMTP 必須） | 同左 |

**★ 4案:**

| | 案 | 月額 | 停止 | データの同居 | 判定 |
|---|---|--:|---|---|---|
| **A** | **GradeLens のプロジェクトに相乗り**（同じ DB に `chem` スキーマ） | $0 | GradeLens が動いている限り止まらない | ⚠⚠ **学校の成績（B2school・privacy 最重量）と消費者の記録が同じ器**。RLS の事故が片方に波及。Fair Use の道連れ。★ 後で分けるのは「移行地獄」（`schoollenz-strategy`） | △ |
| **B** | GradeLens の組織を Pro にし、その中に化学用の2本目を立てる | $35 | 止まらない | 分かれる。GradeLens の Storage 逼迫も同時に解ける | ○（GradeLens 側の判断が混ざる） |
| **C** | **別組織・Free**（化学専用） | $0 | ⚠⚠ **7日で止まる** | 分かれる | **有料商品には論外。★ スパイク専用** |
| ★★ **D** | **別組織・Pro**（化学専用） | **$25** | 止まらない | ★ **完全に分かれる**（B2C の器と B2school の器を分ける＝戦略どおり） | ★★ **推奨** |

★★ **推奨: スパイクは C（$0・捨てる前提）／製品は D（$25/月）。**⚠ **売上ゼロの間も固定費 $25（≒ ¥3,800 ≒ kimika.net の単巻1冊ぶん）がかかる**——ここはユーザーの判断（→ `D-D1`）。
★ A は「売上が立つまでの暫定」として魅力があるが、**暫定の同居が恒久になるのがこのリポジトリの歴史**（`compounds.json` の書き戻し3回と同じ形）なので推奨しない。

⚠ **Supabase 以外も見た**: Cloudflare Workers＋D1／Firebase は「止まらない無料枠」を持つが、**前者は認証を自作**（前例ゼロがさらに増える）、
**後者は2つめのプラットフォーム**（GradeLens で積んだ RLS・Edge Function の知見が使えない）。★ **先例の価値を優先して Supabase。**

⚠⚠ **メール送信は別に要る**: 内蔵 SMTP は 2通/時（スパイクの範囲内では足りる）。**本番は自前 SMTP（Resend / SendGrid / SES 等）が必須**で、
`info@schoollenz.com` の **SPF はまだ Squarespace 既定の `-all`・DKIM/DMARC 未設定**（`schoollenz-service` の残タスク）。★ **認証メールが届かない＝買った人が入れない**なので、§4 の一覧に入れた。

### 3-4. ★★ 分野をまたげる1つの層 —— `/account/`（アプリではなく「層」）

★ **有機の次は無機。認証・配信を assembler 専用に作ると無機で作り直しになる。**★ **決めた形:**

```
/account/                      ← ★ 新規。ルート直下。6本目の *アプリではない*（カード無し・単元行無し・索引に出ない）
   client.js                   ← 定数（API の URL・SCHEMA）／サインイン（OTP）／セッション保持／fetch ラッパ `slzFetch(path)`／「来た道」の帯
   index.html                  ← サインイン画面・購入状態（期限）・解約（Stripe Customer Portal へ）・規約/特商法/privacy へのリンク
   test.html + tests.js        ← §2-3 の検査（定数・ホスト名の漏れ・schema の一致）＋ 生きた API への往復（オフラインなら skip）
/server/                       ← Edge Function 2本（workbook / stripe-webhook）＋ SQL。⚠ `_config.yml` で配信から外す
/assembler/workbook.js         ← 有機の受け口（既存の枠へ流す）。無機は /muki/workbook.js を同じ形で
```

**★ なぜこの形か（根拠）:**

1. ★★ **localStorage は *オリジン* ごと。`/assembler/` も `/muki/` も `/qa/` も同じ `chem.schoollenz.com`** ＝ **1回サインインすれば全アプリで資格が見える**（トークンは `client.js` が1つのキーに置く）。アプリごとにサインインさせる必要が無い。
2. ★ **アプリが共通資産を読む前例が既にある**: `ion-equation/library.html` → `../ratio/model.js?v=41`。**`verify-release.js` の規則6が版ずれを検出する**——`account/index.html` に `?v=` を持たせれば `account` は検査上「アプリ」として数えられ、各アプリの `../account/client.js?v=NN` が規則6の対象になる。
3. ★ **分野は URL と資格の *データ* だけで区別する**（`/workbook/<field>/<page>`・`entitlements.product`）。**Edge Function は1本で field を引数に取る ＝ 無機はデータを足すだけ。**
4. ★ **「来た道」の帯**（`CROSS_APP_FROM`・`?from=`）の作法を `account/index.html` も踏む: **`../account/?from=assembler&back=<戻り先>` で来て、済んだら戻る。** 行き先はアプリが決め、`account` は URL の形しか知らない（`CLAUDE.md` の一方向依存の規約どおり）。

⚠ **「5本キャップ」との関係（決めた）**: キャップは「バージョン儀式10回・回帰テスト10セットになって腐る」ことを止めるための数（`chem-app-roadmap`）。
`/account/` は学習アプリではなく、**全アプリが共有する層**で、儀式は1回ぶん増える。★ **これは「アプリを足す」ではなく「`ratio/model.js` を共有した」のと同じ種類の出来事**と読んだ。⚠ 読みが違うなら `D-D5`。

**★ Supabase の SDK を読むか、REST を直接叩くか（決めた・スパイクで確かめる）:**
`CLAUDE.md`「ビルドツールなし」＋ 外部スクリプト依存を増やさない方針から、★ **まず REST 直叩き**
（OTP 送信 `POST /auth/v1/otp`・検証 `POST /auth/v1/verify`・更新 `POST /auth/v1/token?grant_type=refresh_token`・Function 呼び出しは素の `fetch`）。
**100行前後で済む見込み。**⚠ **スパイクで詰まったら UMD の `@supabase/supabase-js` を CDN から読む**（jsdelivr）。判断はスパイクの結果で。

---

## 4. ⚠ 決済 —— 要るものを並べるだけ（手順は書かない）

⚠⚠ **口座・カード・API 鍵・アカウントの作成は全部ユーザー本人の作業。ここでは「何が要るか」「誰の作業か」だけ。**

| # | 要るもの | 誰の作業 | ⚠ 注記（実測・出典つき） |
|--:|---|---|---|
| **1** | **決済事業者のアカウント**（推奨 Stripe） | ⚠ **ユーザー** | Stripe 日本: **国内カード 3.6%／海外カード +2%（両替時）／Stripe Billing（サブスク）+0.7%／Payment Links・Checkout は無料・月額なし**（Stripe 料金ページ・2026-09-02）。⚠ 無形商品は審査で確認が入ることがある。★ **Checkout（ホスト型）を使えばカード番号はこちらのサイトを通らない** |
| **2** | ★ **サブスクの器**: Stripe Billing ＋ **Customer Portal**（解約・カード変更を Stripe の画面に任せる） | ユーザー（設定）／技術（webhook） | ★ **解約画面をこちらで作らない**。`account/index.html` はポータルへのリンクだけ |
| **3** | ★ **Webhook → 資格の書き込み**（`server/stripe-webhook`：`checkout.session.completed`・`invoice.paid`・`customer.subscription.deleted` → `entitlements`） | **技術** | ★ **決済とサーバーを結ぶのはメールアドレス1本**（§3-1 (b) の根拠）。Webhook の署名鍵は Supabase 側の secret に置く |
| **4** | **特定商取引法に基づく表記**（新規ページ） | ⚠ **ユーザー** | ★ **個人でも「住所・電話番号」は「請求があれば遅滞なく開示する」旨を明記すれば省略できる**（消費者庁「通信販売広告Q&A」）。⚠ **氏名・価格・支払時期/方法・引渡し時期・返品/解約の条件は省略不可**。★ 問い合わせ窓口は `info@schoollenz.com` が既にある |
| **5** | **利用規約**（新規。有料部分の解約・返金・禁止事項・「配信は購入者にだけ。内容の秘匿は約束しない」§1-3） | ⚠ **ユーザー**（文面の確定） | 実測: `privacy.html` はあるが規約は無い |
| **6** | ⚠⚠ **privacy.html の改定**（メールアドレスを取得する・ログインがある・第三者＝Supabase/Stripe/メール送信業者に渡る・削除の求め方） | ⚠ **ユーザー**（文面）／技術（下書き） | 第1条「一切取得しません」「ログイン機能はなく」が虚偽になる（§3-1）。★ 第9条の作法どおり冒頭の改定日も直す |
| **7** | **消費税**（デジタル・国内向け） | ⚠ **ユーザー** | 課税売上 1,000万円以下なら免税事業者のまま。B2C なのでインボイス登録は要らない見込み。⚠ **MoR（Lemon Squeezy 5%＋$0.50・Paddle）は税の計算・納付を肩代わりする**が、免税の個人には過剰で、特商法表記は結局自分（→ `D-D3`） |
| **8** | ★ **メール送信の基盤**（OTP 用の SMTP・SPF/DKIM/DMARC） | ⚠ **ユーザー**（アカウント・DNS） | §3-3。**届かなければ買った人が入れない** |
| **9** | **Supabase の組織・プロジェクト**（製品用 Pro／スパイク用 Free） | ⚠ **ユーザー** | §3-3・`D-D1`。★ スパイクのレーンには「URL と anon key」だけ渡す |
| **10** | **返金・障害時の方針**（配信が止まったら有料部分だけ死ぬ） | ⚠ **ユーザー** | 「無料部分はそのまま使えます」の案内は技術側で最初から作る（§2-1 の 5xx） |
| **11** | **未成年の購入**（保護者の同意） | ⚠ **ユーザー**（規約の文面） | privacy.html 第6条が既に未成年に触れている。規約側にも1行 |
| **12** | ★ **ハブ・OGP の約束文の言い換え**（「ログイン不要」→「無料部分はログイン不要」） | ユーザー（文言）／技術 | → `D-D6` |

★★ **技術側で作るのは 3 と、§2〜§3 の client.js / workbook.js / Edge Function 2本 / テーブル2つ。**⚠ **それ以外は全部ユーザーの手続き。**
★ **順番**: 4・5・6 は **スパイクとは無関係に先に進められる**（文面だけ）。1・2・7 は値付け（後で）と不可分。

---

## 5. ★★ スパイクの発注書（レーン1本ぶん・製品に混ぜない）

**名前: `auth-spike`（実装レーン）。目的: 前例ゼロの3つ——認証つき `fetch`・CORS・Edge Function——を、製品と切り離した最小の形で *動かして測る*。**
⚠⚠ **出さない。学ぶだけ。** 終わったら `account/spike.html` と Supabase の Free プロジェクトは捨ててよい（`client.js` と `server/` は製品に引き継ぐ）。

### 5-1. 前提（ユーザーがレーンに渡すもの）

- ★ **Supabase の Free プロジェクト（別組織・スパイク専用）の URL と anon key**（⚠ **レーンはアカウントを作らない・鍵を発行しない**。渡された2つを `account/client.js` の定数に書く。anon key は公開前提の鍵だが、スパイク後はプロジェクトごと消す）
- OTP を受け取れるメールアドレス1つ（内蔵 SMTP・2通/時の範囲で試す）
- Edge Function のデプロイは Supabase CLI で行う。⚠ **CLI のログインはユーザー作業**（レーンは `supabase functions deploy` の実行までは頼まれた範囲で行うが、ログイン・トークンの発行はしない）

### 5-2. 作るもの（これだけ）

| # | ファイル | 中身 |
|--:|---|---|
| 1 | `account/client.js` | 定数 `API_BASE`・`SCHEMA`・`ANON_KEY`／`signIn(email)`→`verify(email, code)`／トークンを localStorage の1キーに保持・期限前に refresh／`slzFetch(path)`（`Authorization` を付け、`schema` を照合し、401/404/5xx を種類で返す）。**REST 直叩き**（§3-4）。⚠ SDK に逃げるのは詰まってから |
| 2 | `account/spike.html` | ボタン4つ: メール送信／コード検証／`workbook/organic/spike` を取る／サインアウト。結果を素の `<pre>` に出す。**見せ方は作らない** |
| 3 | `server/workbook/index.ts` | `GET /workbook/<field>/<page>`。JWT を検証 → `entitlements` を引く → 200 `{schema, field, page, expires, problems:[ダミー1問]}`／401／404。CORS ヘッダ（`Access-Control-Allow-Origin` は `https://chem.schoollenz.com` と `http://127.0.0.1:*`・`Allow-Headers: authorization, content-type`・`Max-Age`） |
| 4 | `server/schema.sql` | `entitlements` 表＋ RLS（本人の行だけ読める）。`progress` は **作らない**（`D-D2`） |
| 5 | `account/test.html` + `tests.js` | §2-3 の検査 (a)(b)(c)。＋ 生きた API への往復（未サインインで 401・サインイン後 200・`schema` をわざとずらして隠れる＝否定対照）。API に届かない環境では skip して黄色 |
| 6 | `_config.yml` | `exclude` に `server/` を足す（⚠ 設計上必要な唯一の既存ファイル変更。`tools/` の隣に1行） |

### 5-3. 作らないもの（⚠ 混ぜない）

決済（Stripe の何もかも）／`assembler/workbook.js`（製品の受け口）／本物の問題データ／記録の同期（`progress`）／ハブ・privacy・規約の文面／
Google サインイン／SDK の導入（詰まらない限り）／`verify-release.js` の規則10（設計は §2-3。実装は製品レーン）。

### 5-4. ★★ 何が分かれば成功か（全部「数」か「赤/緑」で答える）

| # | 問い | 合格の形 |
|--:|---|---|
| **1** | ★ **`127.0.0.1:96xx/account/spike.html` から Edge Function へ `Authorization` 付き `fetch` が通るか** | 200 が返り、DevTools の Network に **OPTIONS（プリフライト）＋ GET の2本**が見える。プリフライトが `Max-Age` でキャッシュされ2回目は GET だけになる |
| **2** | **未サインインで 401 → `slzFetch` が「資格なし」を返し、画面が黙って隠れるか** | 赤い例外が console に出ない |
| **3** | **`schema` をずらすと隠れるか（否定対照）** | client を `SCHEMA=2` にして 200 が来ても隠れる。戻すと出る |
| **4** | ★ **OTP が届き、6桁で入れるか。内蔵 SMTP の 2通/時に何回目で当たるか** | 届いた時刻・所要秒・3通目の応答（429 を確認） |
| **5** | **トークンの期限切れ → refresh → 失効（サインアウト）で 401 に戻るか** | Supabase 側の JWT 期限を短く（例 60秒）して実測 |
| **6** | ★ **1ページの往復の実測** | ms（プリフライト込み/なし）・bytes（ヘッダ込み）・リクエスト数 |
| **7** | ★★ **localStorage のトークンが `/assembler/` と `/qa/` から見えるか** | `spike.html` でサインイン → 別タブで `/assembler/` を開き `localStorage.getItem(<キー>)` が非 null |
| **8** | ⚠ **`node tools/verify-release.js`（★ worktree の *中* のもの）が `account/`・`server/` で何を鳴らすか** | 規則4（`?v=` 無し）・規則5・規則9 の出力を記録。**`account` がアプリとして数えられたか**（§3-4 根拠2の前提） |
| **9** | **REST 直叩きで足りたか** | 足りた／SDK に逃げた（理由） |
| **10** | ⚠ **Free の停止**: 7日放置して本当に止まるか（スパイクの最後に放置して確かめる。任意） | 停止後の `fetch` の失敗の種類（DNS か 5xx か） |

★ **報告は上の10行の表を埋めた形で。**⚠ **コミットは `account/`・`server/`・`_config.yml` だけ。版番号は `account` の `?v=1` だけ。他アプリの版は触らない。push しない。**

### 5-5. 見積り（4棚）

| 棚 | 数 | 中身 |
|---|--:|---|
| 判定・実行 | **3** | OTP の往復・JWT の検証（Function 側）・`schema` の照合 |
| 見せ方 | **1** | `spike.html`（`<pre>` だけ） |
| 触る既存関数 | ★ **0** | |
| 触る既存の緑のテスト | ★ **0**（⚠ 要確認: `verify-release.js` が `account/` を新アプリと数えて何か鳴らすか＝成功条件8で測る） | |

---

## 6. ★ 判断待ち（ユーザーへ）と、こちらで決めたもの

### 6-1. ★★ ユーザーに聞くもの

| # | 問い | ★ 推奨 | 根拠（1行） |
|---|---|---|---|
| ★★ **D-D1** | **Supabase をどこに置くか**: A 相乗り（$0）／B GradeLens 組織を Pro＋2本目（$35）／★ D 別組織 Pro（$25/月） | ★ **D**（スパイクだけ C の Free） | Free は7日で止まる＝有料商品には置けない。A は学校の成績と消費者の記録が同居し、後で分けるのが地獄（§3-3） |
| ★ **D-D2** | **第1弾に「記録のクラウド同期」を含めるか** | ★ **含めない**（localStorage のまま。表の形だけ決めておく） | 「誰に・いつ」に要らない。認証よりもう1段重い責任。前例ゼロの新規を1つに絞る（§3-2） |
| ★ **D-D3** | **決済は Stripe（税・特商法は自分）か、MoR（Lemon Squeezy / Paddle）か** | ★ **Stripe** | 国内・円建て・免税の個人には MoR の 5%＋$0.50 が過剰。Customer Portal で解約画面を作らずに済む（§4 #1・#7）。⚠ 課税事業者なら再考 |
| **D-D4** | **サインイン手段**: メール OTP だけで始めるか、Google も最初から | ★ **OTP だけ** | Google は OAuth 同意画面の作成（ユーザー作業）が増える。後から足せる（§3-1） |
| **D-D5** | **`/account/` をルート直下に置く**（6本目のアプリではなく共有の層）でよいか | ★ **よい** | localStorage がオリジン共通・`ratio/model.js` 共有の前例・規則6 が版を見張る（§3-4）。⚠ 5本キャップの読みが違うならここで止める |
| **D-D6** | **ハブ・OGP の「ログイン不要」をいつ・どう言い換えるか** | ★ **有料の公開と同時に「無料部分はログイン不要」** | 無料部分は真のまま。早く変えると「ログインが要るのか」と読まれる（§3-1） |

### 6-2. ★ こちらで決めたもの（理由つき。覆すなら早めに）

| 決めたこと | 理由 |
|---|---|
| **配信の単位は1ページ（小項目）・1リクエスト** | 守りは単位に無関係（§1-2）・通信量も決め手にならない（§1-4）・人が1回に読む量と一致（§2-1） |
| **採点はクライアントのまま（サーバー採点はしない）** | 答えを隠す唯一の手だが、既存の無料の採点と二重・1操作ごとに往復・問題文が答え（§1-3） |
| **「1問ずつ配る」はやらない** | 次の問題ごとにプリフライト込み2往復。守りの得は無い（§2-1） |
| **問題は localStorage に置かない。記録は置いてよい** | 「いつ」を守る唯一の手段が「その都度取りに行く」（§1-3・§2-2） |
| **API 応答に `?v=` を付けない。代わりに `schema` の握手** | assembler の JSON はもともと `no-cache` で `?v=` を使っていない（§2-2）。ずれは両方向とも `schema` で受ける（§2-3） |
| **API の URL と schema は `account/client.js` の定数1か所のみ。test.html で漏れを検査** | `qa/tests.js` の `runVersionTests` と同じ作法（§2-3） |
| **サーバー側コードは repo の `server/` に置き、Jekyll の exclude に入れる** | 追跡ファイルは既定で配信される（`_config.yml` 実測）。鍵は置かない |
| **認証はメール OTP・持つ個人情報はメールだけ** | 決済と資格を1本で結ぶ最小の識別子（§3-1） |
| **`entitlements`・`progress` の2表・記録は1ユーザー1行（アプリごと）** | §3-2 |
| **Edge Function は `workbook`（field 引数）と `stripe-webhook` の2本** | 無機はデータを足すだけ（§3-4） |
| **REST 直叩きを先に試し、詰まったら SDK** | 「ビルドツールなし」・外部依存を増やさない。判断はスパイクの成功条件9で |
| **`schema` を上げるときはサーバー先行（後方互換1版）** | 逆順だと全ユーザーに同時にずれが出る（§2-3） |
| **規約・SNS・内部文書に「内容が守られる」と書かない** | 守っていない（§1-3） |

---

## 7. ⚠ 既存の設計書・申し送りのうち、疑って確かめたもの

| 出どころ | 記述 | ★ 実測・判断 |
|---|---|---|
| `DEVELOPMENT.md`（ユーザー決定の注記） | 「設計の芯は『1問ずつ配る』形にできるかどうか」 | ⚠ **芯ではない。** 単位は (b) にほぼ効かず、(a)(c) は単位と無関係（§1-2）。芯は「資格」と「期限」 |
| `DESIGN_paid_workbook.md` §4-3 | 「1ページ（小項目）ずつ ＝ 3〜10 KB」 | ⚠ **組む型は 0.7〜1.5 KB**（3問 663 B・10問 1,419 B・gzip 実測）。**3〜7倍過大**。方向（1ページずつ）は変わらない |
| 同 §4-2 | `stages.json` gzip 13,310 B | 10,900 B（level 6）。圧縮レベルの差で、同じ程度 |
| 同 §4-4 ／ `DESIGN_review_pack1.md` §3-2 #2 | 「サーバー配信のデータに `?v=` が効かない ＝ 新種のずれ」 | ⚠ **新種ではない。** assembler の JSON 7/8 は `?v=` 無しの `cache:'no-cache'`。`?v=` を使うのは `isomers-baked.json` と qa だけ（§2-2）。塞ぐのは `?v=` ではなく `schema` の握手 |
| 同 §4-5 ／ `DEVELOPMENT.md` | 「無料枠 1 GB が逼迫」を相乗りの物差しに | ⚠⚠ **逼迫は File Storage。問題と記録は Database（別枠・500 MB）**。効くのは **「Free は7日で停止」** と **Fair Use の道連れ**（§3-3）。★ 設計書のどこにも「7日停止」が無かった |
| 同 §4-6 #6 | 「Edge Function 1本＋テーブル2つ」 | ★ **Function は2本**（配信＋Stripe の webhook）。テーブル2つは合っている（認証の表は Supabase 側） |
| 同 §4-6 #5 | 「ログイン（誰が買ったかを覚える手段）」 | ★ 合っている。⚠ 加えて **メール送信の基盤（SMTP・SPF/DKIM）** が要り、これは設計書に無かった（§3-3・§4 #8）。内蔵 SMTP は 2通/時 |
| `DESIGN_review_pack1.md` §3-2 #1 | 「qa の前例をそのまま踏む（定数1か所＋test.html）」 | ★ 一致。⚠ 比べる相手が「index.html の版」ではなく「サーバー側の `SCHEMA`」になる（§2-3 段2 (c)） |
| 同 §6-2 並行 | 「認証スパイク: サーバーに1小項目ぶんのダミー問題を置き…」 | ★ そのまま §5 に展開。⚠ 加えた成功条件: **localStorage のオリジン共有（7）・Free の停止（10）・内蔵 SMTP の上限（4）・verify-release の反応（8）** |
| `privacy.html` 第1条 | 「メールアドレス…を一切取得しません」「ログイン機能はなく」 | ⚠⚠ **認証を入れた時点で虚偽になる。改定はユーザー作業**（§3-1・§4 #6）。どの設計書も触れていなかった |
| ハブ `index.html` 244行 | 「無料・広告なし・ログイン不要」 | ★ 無料部分は真のまま。言い換えのタイミングは `D-D6` |
| `CLAUDE.md`「5本キャップ」 | `/account/` は6本目か | ★ **層であってアプリではない**と読んだ（§3-4）。`D-D5` で確認 |

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-02 | 新規。§1 前提（守るのは「誰に・いつ」だけ。1問ずつは守りにならず、それで十分）／§2 配信の単位（1ページ）・本体は静的・`?v=` の死角は `schema` の握手＋定数1か所＋test.html／§3 認証（メール OTP・1ユーザー1行・Supabase は別組織 Pro を推奨・`/account/` 共有層）／§4 決済に要るもの12件／§5 スパイクの発注書（成功条件10件）／§6 `D-D1`〜`D-D6` と自己決定13件／§7 疑って確かめたもの11件 |
