# 異性体の書き出し練習モード 設計書（P12-1）

分子式を提示し、ユーザーが構造異性体を描き出して、
「全部そろったか・重複していないか」をアプリが判定する練習機能。
2026-07-24 ユーザー要望（P12-1）に基づく。実装前の合意用ドキュメント。

---

## 0. 現在地（2026-08-13）— どこを読めばよいか

**§12 以降が現在の設計。§4〜§9 は経緯の記録として残してあるが、次の2点はすでに失効している。**

| 失効した記述 | いま | いつ変わったか |
|---|---|---|
| §5 の「登録済みと同型 → 重複通知でブロック」「重複時に主鎖ハイライト」 | 重複は弾かず全部残し、**答え合わせで**「①と②は同じ」と見せる | v147（P12-1-調整） |
| §5・§6 の「1分子ずつ登録」「登録トレイにサムネイル」 | **キャンバスそのものが答案用紙**。登録という操作を廃止（→ **§12**） | 本改訂 |
| §5.2 の主鎖ハイライトを `findLongestCarbonChain` で出す | **`findLongestCarbonChain` は IUPAC の主鎖ではない**（実測で食い違う）。主鎖と番号は `iupacNameDetail` が返したものだけを出す（→ **[DESIGN_iupac_check.md](DESIGN_iupac_check.md)**） | 本改訂 |

- **§12** キャンバスを答案用紙にする（複数分子対応・登録の廃止）
- **§13** 答え合わせの2パターンと段階ヒント
- **§14** アルキル基の書き出し（分子ではない構造）
- **§15** マイルストーン・テスト・否定対照・やらないこと・未決事項
- **§16** お題を総当たりで洗い出した（骨格の型で範囲を宣言する・v1433）
- **§17** ハースの糖を「立体の書き出し」のお題にできるか（**→ §18-6 で決着**・v1435）
- **§18** ★ **「立体が分かれる場所」を指して、立体込みの総数を答える**（v1435。段1・段2・満点の作り方・
  ハースの糖の α/β。起草と実測は [DESIGN_stereo_point.md](DESIGN_stereo_point.md)）
- **§19** 種類数を伏せ、ヒントを5段に組み直し、**分子式＋分類のお題**を足した（v1489）。
  ★ **§19-5 が最新** —— お題の上限を「人が答えられるか」で引き直し（**腕がペンチルに収まる**）、
  C₇ に名前を44件足して**お題は13件**になった。⚠ **§19-4 の宿題「何種類か答える出題」は
  §19-5c で「作らない」に決着**（母数が0になった）。★ **§19-6c-2（2026-09-02）: アルキル基の同点主鎖の規則を直した**
  （「置換基数が最多」を足した。C₆ 以上の基でしか効かず、登録図・ステージの名前は変わらない）
- **命名の確認（主鎖の明示・炭素番号）は [DESIGN_iupac_check.md](DESIGN_iupac_check.md) が持つ**（この文書からは §5 の API だけを参照する）

> **2026-08-15 追記 — 改訂の会議録が別にある。**
> ユーザーが実機を触って出した指摘（アルキル基の付け根の増やし方・芳香族のプリセット・
> 立体異性体の書き出し・学習メニューの言い直し・名称の説明）への決定は
> **[DESIGN_practice_revision.md](DESIGN_practice_revision.md)** にある。
> ★ **§14-1「最初から N 組並べてはいけない」は据え置きに決まった**（2026-08-15・ユーザー判断。
> 同書 §2 の U1）。⚠ **ただし §14-1 が書いている理由は、現行実装では成り立っていない** ——
> 実測で「**総数はお題ボタンが最初から `C₄H₉–（4種）` と出している**」と分かった。
> それでも据え置くのは**別の理由**による: **「（4種）」の文字は読み飛ばせるが、盤面の枠は目に入り続ける。**
> §14-1 本文を読んで「理由が成り立たないから変えてよい」と早合点しないこと。
> 決定が確定したら、同書の §3（付け根）・§4（芳香族）の本文は**この文書の §14・§4.1・§11-4 へ移す**。

---

## 1. 目的と学習価値

- 「C₄H₁₀O の異性体をすべて書け」は定期試験・入試の定番だが、紙では**自分の答えが
  重複していないか・網羅できたか**を自力で確かめられない。ここをアプリが即時判定する。
- 重複判定を通じて「**描き方が違っても同じ化合物**」（回転・裏返し・屈曲・ケクレ位相）を
  体感させる。フェーズ8「表記の同値性を学ぶ」の実践編にあたる。
- 既存の学習ビュー（📚異性体ギャラリー・書き出し方の4ステップ解説）を「答えを見る」側、
  本機能を「自分で書き出す」側として対にする。

## 2. 既存資産（すべて実装済み・流用）

| 資産 | 用途 |
|---|---|
| `enumerateConstitutionalIsomers(elements, hCount)`（chemistry.js） | 正解集合の生成（重原子6個まで） |
| `canonicalCode(mol)`（chemistry.js） | 登録済みとの重複判定・正解集合との照合 |
| `layoutMolecule` ＋ ギャラリー描画（learn.js） | 登録済み異性体のサムネイル表示 |
| `categorizeMolecule`（learn.js） | 官能基分類ヒント（アルコール◯種・エーテル◯種） |
| `computeMolecularFormula`・`splitMolecules`（game.js） | 分子式チェック・単一分子チェック |
| 学習モードの右パネル（P10） | 練習UIの置き場所 |

新規のアルゴリズムは**不要**。本機能はUIとゲームフローの追加のみ。

## 3. モード内の位置づけ

- **📚学習モードの一機能**とする（新トップレベルモードは作らない）。
  学習パネルに「✏️ 異性体の書き出し練習」カードを新設し、問題を選ぶと練習セッションが始まる。
- 反応機構ビューアと同様に「学習モードを離れたら練習セッションを終了」する。
- 作図は共通キャンバスをそのまま使う（描く道具・操作は通常と完全に同一。
  練習のために作図仕様を変えない）。

## 4. 出題

### 4.1 問題リスト（M1は固定リスト）

列挙エンジンの対応範囲（重原子≤6・列挙打ち切りなし）で、高校範囲の定番から段階的に:

| # | 分子式 | 異性体数 | ねらい |
|---|---|---|---|
| 1 | C₄H₁₀ | 2 | 入門（直鎖と枝分かれ） |
| 2 | C₅H₁₂ | 3 | 枝分かれの数え上げ |
| 3 | C₃H₈O | 3 | アルコールとエーテル |
| 4 | C₆H₁₄ | 5 | 骨格異性の網羅（主鎖の選び方） |
| 5 | C₄H₈ | 5 | 不飽和度1（アルケンの位置＋環） |
| 6 | C₄H₁₀O | 7 | 総合（アルコール4＋エーテル3） |

> ⚠ **この表は「最初の6問」の記録**。**v1433 でお題は 20（固定19＋芳香族プリセット1）になった** ——
> **正は [§16](#16-お題を総当たりで洗い出した骨格の型で範囲を宣言する-v1433)**。
> 芳香族プリセット（C₈H₁₀）は §11-4、鎖式・環式の回は §16 が持つ。

- 異性体数はデータに持たず**起動時に列挙エンジンから計算**する（数値の二重管理をしない。
  テストM1と同じ既知値で自己検証される）。
- 問題リストは learn.js 内の定数とする（数件なので新規JSONは作らない。増えたら再検討）。
- ⚠ **`problems` の添字は動かさない。** 回帰テストが `start(2)` のように**添字で開く**ので、
  並べ替えると「別の問題を開いたまま緑」になる。足すのは**末尾だけ**（`IP4` が添字ごとの
  期待値表で見張っている）。

### 4.2 幾何異性・立体異性の扱い

- 列挙・判定とも**構造異性（トポロジー）のみ**。2-ブテンのシス/トランスは1種と数える。
  問題カードに「シス・トランスや鏡像の区別は数えません」と明記する（現行の判定原則と一致）。

## 5. 練習のフロー

```
問題を選ぶ → 分子式と目標数を提示（例: C₄H₁₀O の異性体 全7種）
  ↓ キャンバスに1つ描く
「＋この分子を登録」ボタン
  ├─ 分子式が違う → 拒否トースト（いまの式を表示: C₄H₈O など）
  ├─ 分子が2個以上 → 拒否トースト（1分子ずつ登録）
  ├─ 登録済みと同型 → ★重複通知: 一致した登録サムネイルを点滅ハイライトし
  │   「登録済みの◯番『2-メチルプロパン』と同じ化合物です
  │   （描き方が違っても、つながり方が同じなら同一）」← 名称で同一性を示す
  └─ 新規 → 名称を判定してサムネイル＋名称ラベルをトレイに追加（n/全m 更新）、
      キャンバスを自動クリア
  ↓ 繰り返し
全種登録 → 完了演出（🎉）＋★答え合わせ一覧: 全異性体を系統順（7章の系列順）に
           名称付きで並べる ＋ 分類まとめ（アルコール4種・エーテル3種）＋ クリア記録
```

### 5.1 命名の役割（ユーザー要望 2026-07-24）

- **登録した構造には必ず名称を付けて見せる**。重複が起きたときに
  「別の絵だと思っていた2つが同じ名前」という形で同一性が腑に落ちる。
- 名称は既存の `lookupCompoundName`（compounds.json 照合）を使う。
  **前提整備**: 問題6問の全異性体（延べ25種）に名称が登録されていることを実装時に
  機械検証し、欠けは compounds.json に追補する（P12-3 と連動。テストにも組み込む）。

### 5.2 主鎖の図示 — 「適切な書き方」の教示（ユーザー要望 2026-07-24）

- 登録・重複通知の際、**ユーザーの作図が正準的な書き方でない場合**は、命名とあわせて
  **主鎖（最長の炭素鎖）を作図上にハイライト**して見せ、標準の書き方を理解させる。
- 「正準的でない」の検出: 作図上で**まっすぐ（ほぼ一直線・L字含む直交の連続）に見えている
  炭素列**と、実際の最長炭素鎖が**一致しない**とき（例: 4個を横一列に描いて枝に2個
  ぶら下げたが、実際の最長鎖は枝をまたぐ5個 — 命名の定番の落とし穴）。
- 表示: キャンバス上の該当原子・結合をオレンジでハイライト（不斉ハイライト P7-4 の流用）＋
  「この分子の主鎖はこの5個です（名称: 2-メチルブタン）。主鎖をまっすぐ描くと ↓」として
  標準レイアウトのサムネイル（layoutMolecule）を並置する。
- 実装: chemistry.js に `findLongestCarbonChain(mol)`（純粋関数・原子ID列を返す）を追加。
  環を含む分子は最長鎖の概念が別（環が主構造）なので、環検出時は環をハイライトして
  「環を基本骨格として描きます」と案内するにとどめる。

- **登録時のキャンバス自動クリアは saveState を積んでから**行う（↩で直前の作図に戻れる。
  登録トレイ自体は Undo の対象外＝練習の進行は巻き戻さない）。
- 途中でやめる: 「練習をやめる」ボタン＋学習モード離脱で終了。進行中セッションは破棄
  （クリア記録だけ localStorage に残す。ステージのクリア記録と同じ方式）。
- 正解集合との照合について: 分子式・価標・単一分子の検査を通った構造は原理的に必ず
  列挙集合に含まれるが、**防御的に照合し、万一不一致なら「判定できない構造」として
  console に記録**する（列挙エンジンの欠落検出を兼ねる。監査と同じ思想）。

## 6. UI（学習パネル内の練習カード）

- **問題選択**: カードに問題ボタンを列挙（`C₄H₁₀（2種）` … クリア済みには✓）。
- **練習中**: カードの表示を切り替え:
  - ヘッダ: `✏️ C₄H₁₀O の異性体 3/7`
  - 登録トレイ: サムネイル格子（ギャラリーと同じ描画。番号ラベル付き）
  - ボタン: `＋この分子を登録`（主ボタン）・`💡ヒント`・`練習をやめる`
- **ヒント（段階式・押すたびに1段階）**: ★未発見の構造から系統的に生成する
  （ユーザー要望 2026-07-24。「システマチックに全部書き出せる手順」を教えるのが目的）。
  1. **系列の内訳**: 未発見分を骨格系列ごとに数えて示す
     （例: 「主鎖5＋メチル基1つ の系列があと1つ / 主鎖4＋炭素2個分の側鎖 の系列があと2つ」）
  2. **系列内の書き出し手順**: 未発見構造の系列に対応する定型手順文を示す
     - 置換基・官能基の位置ずらし系: 「◯◯の付く位置を端から順に一通りずらして
       試しましたか（対称な位置は同じものになります）」
     - 炭素2個分の側鎖系: 「側鎖に炭素を2個使う置き方は3通り —
       **エチル基1つ / メチル基2つを同じ炭素に / メチル基2つを別の炭素に**」
     - 不飽和・環系: 「二重結合の位置ずらしと、環にする案の両方を数えましたか」
  3. **答えを見る**: 全異性体を系統順に並べた答え合わせ一覧（下記）を開く
- **答え合わせ一覧（完了時・ヒント3段階目で共通）**: 全異性体を**系統順に**並べて表示する。
  順序 = 主鎖の長い順 → 側鎖の付け方（エチル → メチル×2同一C → メチル×2別C の例示順）→
  官能基・多重結合の位置順。各項目は サムネイル＋名称＋発見済み✓/未発見（オレンジ枠）。
  「系列がすべて並んでいる」こと自体が書き出し方の教材になる。
  - 実装: 各異性体から系統キー `(最長炭素鎖長, 側鎖サイズ構成と同一炭素かの区別,
    官能基位置の正準番号)` を計算してソートする純粋関数を chemistry.js に追加
    （`isomerSeriesKey(mol)`）。ヒント1・2の系列分類もこのキーを共用する。
- **モバイル**: カードは右パネル（縦=下シート/横=ドロワー）内にそのまま載る。
  進行表示 `3/7` は化合物名チップ（P11-M3c）の隣に出すことも検討（M2）。

## 7. 実装方針

- **learn.js に IsomerPractice クラスを追加**（学習ビューの責務の範囲。
  ゲームフローの状態は practice インスタンスに閉じる。chemistry.js には手を入れない）。
- 状態: `{ problem, targetCodes: Map<code, iso>, found: Map<code, {mol, order}> }`。
  判定は `canonicalCode(userMolecule)` を targetCodes / found と突き合わせるだけ。
- index.html: 学習ブロックに練習カードの器（`data-modes="learn"`）とボタン類を追加。
- game.js への変更は最小限: 登録ボタンから `game.userMolecule` を読む・クリアする導線のみ
  （既存の verify/summon と同じ呼び方）。
- クリア記録: `localStorage: chemIsomerPractice.<分子式>` に完了フラグ。

## 8. マイルストーン

- **M1: コアループ** — 問題リスト・登録/重複/分子式チェック・**名称付き**トレイ表示・
  完了判定と系統順の答え合わせ一覧・クリア記録。compounds.json の25異性体名整備＋機械検証。
  ヒントは「残り数」のみ。テスト: 実イベントで C₄H₁₀ を2種登録して完了（名称表示込み）、
  重複登録の拒否と該当サムネイルのハイライト、分子式違いの拒否。
- **M2: 学習性の強化** — 系統的な段階ヒント（系列内訳→書き出し手順→答え。
  `isomerSeriesKey` による系列分類）、主鎖の図示（`findLongestCarbonChain`・
  非正準な書き方の検出とハイライト＋標準レイアウト並置）、完了演出と分類まとめ。
- **M3: 出題の拡張** — 任意の分子式入力での練習（列挙可能な範囲かを検査して受け付ける）、
  P12-2（列挙エンジンの範囲拡張）と連動して問題を追加。

## 9. リスク・未決事項

- **列挙の負荷**: 問題リストの6問はいずれも既知の高速ケース（最大 C₄H₁₀O=13ms）。
  M3の自由入力では overflow（打ち切り）検出時に「この分子式は練習に対応していません」と断る。
- **「登録＝クリア」の誤操作**: 登録でキャンバスが消えることに驚かないよう、
  初回登録時に一度だけトースト（↩で戻せる旨）を出す。
- **エノール等の不安定構造**: 列挙エンジンは価標上有効な構造をすべて数える。C₄H₈ 等の
  問題では発生しないが、M3 の自由入力で「教科書では数えない構造」（エノール等）が
  正解集合に入る分子式があり得る。M3 で分類フィルタ（enol 除外オプション等）を検討。
- **回帰テストのモード前提**: 練習はモード状態を持つため、テストは setMode('learn') →
  練習開始 → 終了までを1テスト内で完結させ、他テストへ状態を漏らさない（reset で練習を
  強制終了する処理を追加）。
- **命名の網羅**: 6問の全異性体に compounds.json の名称が必要（M1で機械検証）。
  IUPAC命名アルゴリズムは未実装のため、当面はライブラリ照合方式。M3 の任意分子式では
  名称のない異性体が出るので「（名称未登録）」表示を許容し、P12-3（系統名）で解消する。
- **主鎖検出と作図の照合**: 「まっすぐ描かれた炭素列」の判定は直交作図（水平・垂直の連続）
  を前提にした幾何判定になる。座標は本来トポロジー判定に使わない原則（開発方針4章）だが、
  これは正誤判定ではなく**表示上の教育支援**なので、シス/トランス命名（4章-4）と同じ
  例外扱いとする。

---

## 11. ベンゼン環を種にした異性体列挙（v910・2026-08-07）

§9 の「列挙の負荷」と DEVELOPMENT.md §7-1d が残した宿題に決着をつけた。
**C₈H₁₀ の芳香族異性体4種（o-/m-/p-キシレン＋エチルベンゼン）は傍用問題集の基本問題**
（qa の `org.aro.c8h10-isomers` は Lv2）なのに、総当たり列挙では **32.8秒かけて3523種**を
出したうえ打ち切られる ＝ 門番（§7-1e）が正しく断っていた。

### 11-1. approach: 探索を速くするのではなく、探索空間を変える

`chemistry.js` の `enumerateBenzeneRingIsomers(elements, hCount)`（純粋関数）。

1. ベンゼン環（C6・ケクレ交互）を**固定の種**として1つ置く
2. 残った重原子を1〜6個のブロック（＝置換基）に分ける
3. 各ブロックの骨格を `enumerateConstitutionalIsomers([...原子, 'R'], hs)` で作る
   （`'R'` は付け根を表す価標1の擬似元素。アルキル基の書き出し練習と同じ流儀）
4. 置換基を環の6箇所に配り、**重複は `canonicalCode` に畳ませる**

**対称性（6回回転＋鏡＝12通り）を自前で数え上げないのが肝。** 配り方を素直に全部作って
正準コードに任せる。自前で畳むと o- の裏返しのような場合を必ずどこかで取りこぼす。

**ケクレ位相も `canonicalCode` が吸収する。** 置換位置が二重結合側か単結合側かで
グラフとしては別物になるが、`canonicalCode` は芳香族結合を `'a'` に潰すので
o-キシレンが2種に割れない。**これは実装の偶然ではなく、依存している性質**なので
否定対照（BZ1）で固定した ——『畳み込みを外すと C₈H₁₀ が 4種 → 5種になる』。

**実測（`node tools/bench-isomers.js` で再現できる。同じ土俵で並べて出るようにした）**

| 分子式 | 総当たり | 種つき | 内訳 |
|---|--:|--:|---|
| **C₈H₁₀** | **32754ms / 3523種（打切）** | **3ms / 4種** | エチルベンゼン＋o/m/p-キシレン |
| C₇H₈ | 27195ms / 1031種（打切） | 0ms / 1種 | トルエン |
| C₈H₈ | 25649ms / 3955種（打切） | 0ms / 1種 | スチレン |
| C₆H₆ | 7207ms / 217種 | 0ms / 1種 | ベンゼン |
| C₉H₁₂ | —（重原子9個で即 overflow） | 25ms / 8種 | プロピル・クメン＋o/m/p-エチルトルエン＋トリメチル3種 |
| C₇H₈O | — | 5ms / 5種 | アニソール・ベンジルアルコール＋o/m/p-クレゾール |

名前は**アプリの化合物ライブラリ側でも独立に確認した**（`lookupCompoundName` が
C₈H₁₀ の4種すべて、C₇H₈O の5種すべてを正しく呼ぶ）。

### 11-2. どこまでを対象にするか ＝ 「ベンゼン環を含む式」一般。止めるのは**環の外の重原子4個**

C₈H₁₀ だけの特別扱いにはしない。炭素6個以上の式すべてに効かせ、
**環の外に置ける重原子を `BENZENE_REST_MAX = 4` 個で止める**。この線は実測で引いた
（全元素の組み合わせ 1,900 式あまりを総当たりした）:

| 環外の重原子 | 最悪ケース | 時間 |
|--:|---|--:|
| 1 | C₇H₈ | 5ms |
| 2 | C₈H₁₀ | 9ms |
| 3 | C₇H₈S₂ | 45ms |
| **4** | **C₇H₉ONS** | **426ms** |
| 5 | **C₆H₆S₅** | **13236ms** ← 崖 |

**5個にすると硫黄で13秒固まる。** 硫黄は価標6なので置換基の骨格が桁違いに増える ——
ユーザーが別途報告した 5.9秒／16.5秒のフリーズと同じ罠で、同じ轍は踏まない。
4個と5個のあいだに 30倍の崖があるので、線はここ以外に引きようがない。

もう1つの門番が **`BENZENE_SUB_DOU_MAX = 2`（置換基1つあたりの不飽和度）**。
**性能と教育の両方に効く**:
- 性能 … これが無いと C₁₂H₁₀ で **15.4秒**（環外6個の高度不飽和な骨格を数えるため）
- 教育 … 高校で出る置換基は -CH₃・-OH・-Cl（0）／-CH=CH₂・-CHO・-COOH（1）／
  -C≡CH・-CN（2）で尽きる。3以上は小員環・累積二重結合ばかり

どちらも**列挙に入る前の算術で判定できる**（不飽和度は分子式から出る）＝ §7-1e の
「数える前に断る」と同じ流儀を保っている。**待たせてから断るのではなく、待ちが無い。**

### 11-3. ナフタレンなど環が2つの式は**やらない**

理由は性能ではなく**設計が決まらない**こと。

- **種をどれにするか分子式から決まらない。** C₁₀H₈ を見てナフタレンを置くべきか、
  ベンゼン＋C₄鎖を置くべきか、ビフェニル（C₁₂H₁₀）はどうか —— 分子式は答えを持たない。
  種を増やすほど「どの種で数えた結果か」が曖昧になり、**数が信用できなくなる**
- **ナフタレンの対称性は環の12通りとは別物**（α位4つ・β位4つで、置換位置の同値類が違う）。
  同じ畳み込みが使い回せず、種ごとに対称性の議論をやり直すことになる
- **そもそも高校で問われる形が違う。** ナフタレンは「1-置換と2-置換の2通り」を答える
  問題であって、書き出して数え上げる練習にならない

いまの実装は**種を1個だけ置く**ので、ナフタレンは原理的に出てこない。
BZ4 が『生成物の環は必ず1個』を機械で見張っている（種が増えたら赤くなる）。

### 11-4. 「ベンゼン環を含む」は**判定しない。出題としてそう宣言する**

分子式からは決まらない —— C₈H₁₀ には非芳香族の異性体も理論上は大量にある（3523種のうち
芳香族はたった4種）。だから「この式は芳香族だ」と推論するのではなく、
**出題そのものを『C₈H₁₀ の芳香族異性体』に変える**。

これは教育的にも正しい向きだと判断した。高校の設問は
**「C₈H₁₀ の芳香族化合物の構造異性体をすべて書け」**であって、全異性体を問うものではない。
高校で扱わない骨格（小員環・アレン・累積二重結合）を混ぜるのは学習の邪魔にしかならない。

**宣言した以上、画面のどこでも隠さない**（`aromaticOnly` の旗が回るところ）:
- 見出し … 「✏️ C₈H₁₀ の**芳香族**異性体（全 4 種）」
- 注記 … 「※ ベンゼン環をもつ構造だけを数えます（環をもたない異性体は対象外）」
- 答え合わせの表題・作業帯の「お題（芳香族）」
- **登録の断り方** … 芳香族回では「分子式は合うが対象外」が**正常に起こる**。
  ここを既存の開発者向け文言（「開発ログに記録しました」）のままにすると、
  **正しく描けた生徒に不具合の顔を見せる**ので、範囲の説明に差し替えた（BZ5）
- クリア記録の鍵も分ける（`chemIsomerPractice.C₈H₁₀@ar`）＝ 同じ分子式でも別の出題

### 11-5. 門番（§7-1e）との関係 ＝ **緩めるのではなく、外に道を1本足す**

`learn.js` の `startFromFormula` で、**重原子の上限と不飽和度の門番より前**に分岐する。

```
不飽和度 < 4                       → 種つきを呼ばない（環が入る余地がない。C₆H₁₄ などが 0ms で抜ける）
種つきが 2〜20種を返した            → ★芳香族回として開く（門番を通さない）
種つきが 0/1種・打切・適用外        → そのまま従来の道へ落ちる（重原子上限 → 門番 → 生の列挙）
```

- **1種しか出ない式は開かない**（`IP_BENZENE_MIN_ISOMERS = 2`）。C₇H₈ はトルエンだけ・
  C₈H₈ はスチレンだけで、書き出して見比べる練習にならない。従来どおり断る
- **門番はそのまま**。C₆H₁₂（25種 > 20）や C₆H₆（217種）の扱いは1つも変わっていない。
  BZ3 が『種つきが効かない式の挙動が変わっていないこと』を機械で見張る

### 11-6. 否定対照（直しを外すと赤くなることを確かめた）

| 外したもの | 結果 |
|---|---|
| `canonicalCode` の芳香族畳み込み | C₈H₁₀ が **4種 → 5種**（o-キシレンがケクレ位相で割れる）／C₉H₁₂ が 8 → 11種 |
| 環の対称性の畳み込み | C₈H₁₀ が **4 → 36種**／C₉H₁₂ が 8 → 162種 |
| `BENZENE_SUB_DOU_MAX` | C₁₀H₈ が **3 → 28種**（高校で扱わない骨格） |
| `BENZENE_REST_MAX` を5に | 環外5個が断られなくなる（＝硫黄で13秒の罠が開く） |
| ケクレ交互（全部単結合に） | 全件が「芳香環なし」になる |
| `startFromFormula` の分岐 | **BZ3・BZ5 が失敗**（C₈H₁₀ が開かない） |
| 登録時の芳香族分岐 | **BZ5 が失敗**（対象外の構造に「開発ログに記録しました」が出る） |

### 11-7. 申し送り

- **置換基に名前が付かない異性体がある。** C₉H₁₂ の o/m/p-エチルトルエンは
  compounds.json に無く「（名称未登録）」になる。答え合わせの体験としては
  C₈H₁₀・C₇H₈O のほうが良い。名称の拡充は別レーンの仕事
- **環外4個の最悪（C₇H₉ONS の 426〜606ms）は、開かずに断る式でも払う。** 356種で
  「多すぎ」と断るまでに 0.6 秒待たせる。生徒が打つ式ではないので許容したが、
  気になるなら「種数が上限を超えた時点で打ち切る」枝刈りを足す余地がある

---

## 12. キャンバスを答案用紙にする（2026-08-13 ユーザー要望）

> 複数分子に対応したところで、異性体の書き出し問題は登録作業が要らなくなると思います。

そのとおりで、**「1つ描いて登録」は複数分子が扱えなかった時代の器**だった。

### 12-1. 決定: 登録トレイは**捨てる**（残さない）

「登録」ボタンも `entries[]` のスナップショット配列も廃止し、**キャンバス上の連結成分の集まりが答案**になる。

**理由**

1. **答案の在りかが2つあると必ずずれる。** トレイを残すと「キャンバスに描いてある図」と
   「トレイに入れた図」が別々に存在し、どちらが答案かを判定のたびに決め直すことになる。
   このリポジトリが繰り返し踏んでいる「同じ判定が3か所に分かれる」の型そのもの
2. **トレイは Undo の外にあった**（§5 の「登録トレイ自体は Undo の対象外」）。
   答案がキャンバスに乗れば **↩ がそのまま消しゴムの取り消しになる**。
   例外を1つ消せて、しかも操作は既存のものだけで足りる
3. **`snapshotTarget` のコピーが不要になる。** 図は1か所（`userMolecule`）にしかない
4. **「並べて比べる」という学習の芯に近づく。** v147 の調整（重複を弾かず全部残して比較させる）は
   すでにこの方向へ舵を切っていた。答案用紙化はその続きであって方針転換ではない

**代償を正直に書く**: 既存テスト IP1〜IP9 は全部 `register()` を叩いている。**捨てずに書き直す**
（分子式違いの断り・重複の扱い・クリア記録は実在した退行なので、判定の場所が変わっても検査は残す）。
新しい帯は **`IW`**（Isomer Worksheet）。

### 12-2. 「登録」が消えると、断りはどこへ行くか ＝ **門から採点表へ**

いまは登録の瞬間に弾いている（分子式違い・複数分子・対象外）。答案用紙では
**描きかけの分子が常にキャンバスに乗っている**（4つ目を描いている最中は必ず分子式が合わない）ので、
入口で叱ることができない。

→ **成分ごとの判定は「答え合わせ」を押したときにまとめて行い、図を指して言う。**

| 成分の状態 | 答え合わせでの言い方 |
|---|---|
| 正解集合にある・初出 | ✓（発見） |
| 正解集合にある・既出 | 「①と④は同じものです」（→ §13） |
| 分子式が違う | 「④は C₃H₈O です（お題は C₄H₁₀O）」＝ **描きかけもここに入る。責めない文言にする** |
| 分子式は合うが対象外（芳香族回） | §11-4 の文言をそのまま使う（不具合の顔を見せない） |
| 分子式・価標は合うのに正解集合に無い | 従来どおり `console.error`＋列挙エンジンの欠落として記録 |

書き出しの最中にキャンバスへ出すのは **個数だけ**（「いま 5個 描いてあります」）。判定は1つも出さない。

### 12-3. 使える材料と、そのままでは使えない材料（**実測**）

| 要るもの | 現状 | 手当て |
|---|---|---|
| 成分への分割 | `game.js` の `splitMolecules()` | そのまま |
| 成分ごとの番号 ①②③ | `markedMolecules()`（`MOLECULE_MARKS` は ㉟ まで＝35個。上限20の出題に足りる） | そのまま |
| 同一性の判定 | `canonicalCode(mol)` | そのまま |
| 正解集合 | `enumerateConstitutionalIsomers` / `enumerateBenzeneRingIsomers` | そのまま |
| 成分ごとの命名 | `renderMoleculeLabels()` が図の下に `🔍 ① 名前` を出す | **★ 伏せる必要がある（下記）** |

**★ 実測で見つけた事故（設計の前提が変わった）**

学習モードでキャンバスに2分子を置き、`updateDrawing()` 後の SVG を読んだ結果:

```
🔍 ① ブタン
🔍 ② 2-メチルプロパン
```

**答えが出る。** `isSoleLabeledPart()` は `currentMode === 'learn'` で名前を伏せているが、
それは**分子が1つのときの見出し**だけの門番で、`markedMolecules()` が番号を振る
**2分子以上の場合には門番が無い**（game.js 4696 行の `marks.has(p) ||` の側）。
いまは練習が「1分子ずつ」を強制しているので表に出ていないだけで、**答案用紙にした瞬間に開く穴**。

→ **決定**: 見出しの文字を作る場所を `captionForPart(part)` という**1つの入口**に集約し、
練習中はそれが**番号だけ**（`①`）を返すようにする。`renderMoleculeLabels` を別に書き写さない
（写した瞬間に「名前を伏せる規則」が2か所になる）。

→ **決定**: 練習中は**見出しをタップして分子モーダルを開けなくする**（`canvasEntryEnabled()` に
練習中を足す）。モーダルは名前・分子式・異性体・立体まで見せる総合窓口なので、
そこだけ部分的に伏せると穴を1つずつ塞ぐ作業が始まる。窓ごと閉める。

### 12-4. ★ ①②③ は識別子ではない（安定しない）

`splitMolecules()` は `userMolecule.atoms` の並び順で成分を見つけるので、
**成分の番号は「その成分のいちばん若い原子が配列の何番目にいるか」**で決まる。
先に描いた分子をあとから消して描き足すと、**番号が入れ替わりうる**。

→ **決定**: 番号は「いま画面に見えている並び」であって答案の identity ではない。
**採点の記録を番号で持たない**。判定は答え合わせを押した時点で `canonicalCode` から作り直し、
オーバーレイはそのとき計算した番号を表示する。
（memory の「原子IDに順序を頼らない」と同じ家族の罠。**順序が要るところは、その場で作り直す**）

### 12-5. 答案の置き場所（20個は 800×600 に入らない）

キャンバスの `viewBox` は 800×600、格子は 42px ＝ **19×14 マス**。C₄H₁₀O の異性体は1つ約 4×3 マス
（見出しの帯を含む）なので 7種で埋まり、C₈H₁₀ の芳香族4種（ベンゼン環）はもっと食う。上限20はまず入らない。

→ **決定**: 「**答案を並べ直す**」ボタンを置く。やることは **成分ごとの平行移動だけ**
（`GRID_SIZE` の整数倍で、格子のスロットへ配る）。

- **分子の内部座標には一切触れない。** CLAUDE.md の「整形で幾何が変わるなら座標を戻す」
  「シス/トランスが未確定の図は整形しない」に**触れずに済む**のは、剛体移動しかしないから
- 落下先の決め方は既存の分子ごとの移動（`ZD` の帯）と同じ規則を借りる ＝ **0.0px の完全重複を作らない**
- 拡大縮小・パンは既存のものがそのまま効く。並べ直しは「散らかったら押す」道具で、自動では走らせない
  （勝手に動くと、どれが自分の何番目の答えか分からなくなる）

#### 12-5a. 実装（**完了・v1383**）

実体は **`game.tidyAnswerSlots()`**（`game.js`）1つ。作業帯（`renderStrip`）に
**「🧹 並べ直す」**を足し、`learn.js` の `IsomerPractice.tidySheet()` が言葉と押しものだけを持つ
——**幾何の判断は learn.js に1行も置かない**。芳香族（§4-2）と立体（§5-6）が
同じ道具を使うので、実体は game 側に1つだけ置く。

**やること（この4つだけ）**

| | 中身 |
|---|---|
| 成分の並び | `componentIdSets()` ＝ `userMolecule.atoms` の並び。**図の下の ①②③ と同じ順**（§12-4）で左上から配る |
| スロットの大きさ | **全スロット同じ**（いちばん大きい答案 ＋ 隙間、格子の整数倍に切り上げ）。隙間は**横 `GAP = GRID_SIZE*2 = 84px` ／ 縦 `GAP_Y = GRID_SIZE*3 = 126px`** と**縦だけ1マス広い**（v1432・§12-5b）。大小を詰めると描き足すたびに ①②③ の場所が総入れ替えになる |
| 列数 | 全体が **4:3 に近くなる**列数（`round(√(n·cellH/cellW·4/3))`）。キャンバスも「全体表示」も 4:3 なので、細長い帯にすると視野の中で図が無駄に小さくなる |
| 移動量 | `dx, dy` とも **`GRID_SIZE` の整数倍**。格子に載っていた原子は載ったまま |

**やらないこと（機械で禁じたもの）**

- ★ **回転を混ぜない**（§5-3・§5-6）。「縦長の答案は 90° 回せば1列増える」は思いつくが、
  立体の読み（`isFischerOriented`・v446）は**縦置きの図だけをフィッシャー投影として読む**ので、
  並べ直しが図を回すと**描いた本人が触っていないのに立体の意味が変わる**。理由はコード側にも書いてある
- ★ **自動で走らせない**。呼ぶのは帯のボタンだけ（`drawn < 2` では押せない）
- ★ **置けないなら1つも動かさない**。上限（`CANVAS_LIMIT`）を越える／別成分の重原子が
  `MIN_COMPONENT_CLEARANCE`（＝ `GRID_SIZE*0.65` ＝ 27.3px）より近づく並べ方は、
  実測で確かめて**まるごと捨てる**。しきい値は新規配置・分子ごとの移動（`ZD`）と**同じ定数**
  （`game.js` の `MIN_COMPONENT_CLEARANCE` に1つだけ置き直した）
- 触るのは**視野だけ**（並べ直したあと `fitCanvasToMolecule` で全体に合わせる）。`saveState()` を
  通すので **↩ で1手で戻せる**

**実測（1280×800・v1383・`layoutMolecule` で作図した本物の C₄H₁₀O 7種）**

| 見たもの | 実測 |
|---|---|
| 散らかした状態（9px ずつずらして重ねる） | 別成分の重原子どうし **最短 9.0px**（27.3px 未満 ＝ 重なっている） |
| 並べ直したあと | 最短 **66.1px**／外接矩形 **453×480px** ＝ **800×600 に収まる** |
| **7種のうち画面に丸ごと入った数** | **7 個**（＝ 全部入る） |
| **20成分**（同じ7種を使い回して20枚） | **4列×5行**・外接矩形 952×735px・**内部の相対座標が変わった成分 0 / 20** |
| 4倍に拡大＋パンしてから押す | `moved=20`・内部座標の変化 0・押したあと **20/20 が画面に入る** |
| ↩ | 相対座標が違う成分 **0**（元に戻る） |
| 分子ごとのドラッグ（`ZD`） | 重なる移動を拒む=true／普通の移動は通る=true（**壊れていない**） |
| 帯の押しもの（320 / 375 / 768px） | 4つとも **68×34 / 82×34 / 112×34px** ＝ 1行に収まり **32px の床を割らない**（帯の高さは 72px のまま） |

**設計から動かしたところ**

- 作業帯の押しものが **3つ → 4つ**になった（`setPracticeStrip` の「よく押す3つだけ」に対する例外）。
  💡ヒントを帯から1手の面へ出したのと**同じ理由** ——「散らかっているのはキャンバス」なので、
  覆う面の中に隠すと押しどきが分からない。数そのものは `RB8` が数え続けるので、5つ目は必ずそこで止まる
  （アルキル基側の帯はもともと4つなので、床を割らないことは既に実測済み）
- **アルキル基の練習には足していない**。あちらは答案を作る時点で空きスロットを探して置く
  （§15-1 の W3）ので、散らかる経路が無い

#### 12-5b. ★ 番号①がまとめて下側に出るのを直した（**完了・v1432**）

ユーザー報告「書き出し中、化合物の番号①がまとめて下側に表示されることがある」
（発注書 `video-scripts/ORDER_isomer_2026-08-20.md` §A-3）。**原因は2つの数が別の単位で
書かれていたこと**で、直しも2か所ある。片方だけでは直らない。

| # | 直したもの | なぜ |
|---|---|---|
| **②** | `labelScale()` … **練習中は 1**（図と同じ縮尺） | 見出しチップの高さ 34px は**指で押す的だから**の下限。⚠ **練習中の見出しは押せない**（`canvasEntryEnabled()` が §12-3 の旗で false ＝ `pointer-events:none`・塗りも線も `none`）。押せないものに的の下限は要らない。いっぽう置き場所はモデル座標なので、**引くほどチップがモデル座標で太り**、隙間を必ず食い破っていた（実測 1280×800・20個で 101.8px／スマホ 375px で 184.7px ＞ 84px） |
| **①'** | `tidyAnswerSlots()` の**縦の隙間だけ 2マス → 3マス**（`GAP_Y`） | 84px を**3人で取り合っていた**: 帯 46.2 ＋ チップ 34 ＋ **下の行の自動水素 25**（重原子から 16px 伸ばして半径9）＝ **105.2px**。自動水素が必ずチップに食い込み、段送り（`labelPlacementCost` は距離を数えない）で逃げた見出しが**次の見出しの居場所に居座って数珠つなぎに下へ流れる** |

⚠ **①' の数を定数で書けるのは②のおかげ**。②より前はチップが画面px 固定だったので、
必要な隙間が「並べ直したあとの縮尺」で決まる鶏と卵になっていた（発注書が案①を退けた理由）。
**②の分岐を外すなら `GAP_Y` も一緒に見直すこと。**

**実測**（見出しが既定の置き場所＝分子の下端＋1.1マス から動いた数／最大のずれ）

| 場面 | v1431 | ②だけ | **②＋①'** |
|---|--:|--:|--:|
| PC 1280×800・20成分・並べ直しあり | 4/20・10マス | 4/20・11マス | **0/20** |
| スマホ 375×812・20成分・並べ直しあり | 16/20・**16マス（672px）** | 4/20・11マス | **0/20** |
| ⚠ スマホ 375×812・12成分・**並べ直しなし**（引いて全体表示） | 12/12・9マス | **0/12** | **0/12** |
| 実物の異性体20個・PC・並べ直しあり | 9/20・8マス | 7/20・8マス | **0/20** |

見張りは **`IW17`（本体）・`IW18`（漏れの見張り）**。⚠ 測るのは
**「見出しが既定の置き場所から動いたマス数」1本**にした ——「重なっていないか」で測ると
**遠くへ飛んだ見出しほど周りに何も無くて緑になる**（症状そのものが緑を出す形）。
⚠ **押せる的の下限（34px）は練習中以外では 1px も動かしていない**（`IW18`・`TAP1`・`ZM1`・`ZM2`）。

#### 12-5c. ★ v1432 が消しそこねた経路 —— 図が上下に隣接するとき（**完了・v1440**）

> **異性体の書き出し、図が上下に隣接すると丸数字がまとめてしたに行く**
> （ユーザー実機報告 2026-08-21。§12-5b の直しの**あと**に出た）

**v1432 の2つの直しは、どちらも「隙間を作る側」だった。**
②（`labelScale()` を練習中 1）はチップが太らないようにし、①'（`GAP_Y = 126px`）は
**`🧹 並べ直す` が作る配置**に必要な隙間を確保した。
⚠ **どちらも「人が自分で描いた配置」には1つも効かない。**

格子は 42px なので「**2マスあけて次の答案を描く**」＝ 縦の隙間 **84px** はごく自然に出るが、
見出しに要るのは 46.2（置かない帯）＋ 34（チップ）＋ 25（下の行の自動水素）＝ **105.2px**。
足りないので既定の置き場所が重なり、`placeMoleculeLabels` の段送りへ回る。

**実測（1280×800・v1439。4炭素の直鎖を縦に等間隔で6枚）**

| 縦の隙間 | 既定から動いた数 | 図との隙間の最遠 |
|---|--:|--:|
| **84px（2マス）** | **6 / 6** | **10マス（420px）** |
| 126px（＝ `🧹` が作る隙間） | 0 / 6 | 1.1マス（既定のまま） |

**なぜ IW17 が緑のままだったか**: あの検査が測ったのは
①`🧹 並べ直す` のあと と ②`ipTidySheet(…, **3**マス)` の2つだけ。
**2マスの配置は「v1431 までの並べ直しの再現」＝ ずれて当然の否定対照C** として置いてあり、
⚠ **それが人の手描きでいちばん自然な間隔だ**という見方が無かった。

**直したところ — 段送りの「値段」のほう**（発注書 §A-3 の案③。⚠ **練習中だけ**に閉じた）

| # | もの | 中身 |
|---|---|---|
| **上限** | `LABEL_DRIFT_MAX_ROWS = 2`（`labelDriftLimit()`） | 自分の図から **2マス**より遠い候補は**選ばない**。行き場が無ければ既定の位置に居座る |
| 値段 | `LABEL_DRIFT_PENALTY = 4000`（`labelDriftPenalty()`） | 上限の内側での好み ＝「1つの重なりを避けるためなら 2マスまで動く」（10000 ÷ 4000） |

★ **上限のほうが本体**。値段だけでは足りない —— `labelPlacementCost` の重なりは
**重なった図形の数だけ 10000 が積み上がる**（自動水素も1つずつ数える）ので、
実測では 1か所で 5〜6万になり、**どんな1マスあたりの値段でも遠くの空き行のほうが安くなる**
（4000/マスを入れただけの版で、飛距離は 10マス → 10マスのまま変わらなかった）。

★ **測るのは段の数ではなく「図との隙間」**（`labelDriftRows()`）。候補には
**分子の上へ回す**があり、これは既定から −7マス動くが**図には隣接している**。
段の数で足切りすると**いちばん近い候補を真っ先に捨てて**下へ押し戻す。

★ **練習の外へは 1px も広げない**（`worksheetActive()` で分岐。`labelScale()` と同じ流儀）。
練習中の見出しは**押せない番号**で仕事は「この図は何番か」だけ ＝ 遠くへ行った時点で仕事をしていない。
練習の外は**名前つきの押せる的**なので、読めない・押しにくいほうが害が大きい
（ML 帯・夜間監査ファズの実測値を1つも動かさない）。

**実測（v1440）**

| 場面 | 図との隙間の最遠 | 既定（1.1マス）から動いた数 |
|---|--:|--:|
| PC・縦 84px で6枚 | **3.09マス（130px）** | 1 / 6 |
| スマホ・縦 63px で6枚 | 3.1マス | 6 / 6（いずれも上限の内側） |
| 縦 126px で6枚 | 1.1マス | **0 / 6** |
| `🧹 並べ直す` のあと（20枚） | 1.1マス | **0 / 20**（v1432 のまま） |

見張りは **`IW30`**（物差しは「図との隙間」。否定対照は `labelDriftGuard = false` ＝ 上限を外すと飛ぶ）。
⚠ **IW17 の否定対照A・B・C も `labelDriftGuard = false` を通すようにした** ——
v1440 の上限は v1431 の症状にも**ついでに効く**ので、閉じないと
「②（`labelScale`）が何を直したか」を名指しできなくなる（実測 4件 → 1件に落ちた）。

### 12-6. 練習をやめたときに答案を消さない

いまの `stop()` はキャンバスに触らない。答案用紙では**キャンバスが成果物**なので、
この振る舞いをそのまま守る（消さないことを設計として明記する）。
学習モードを離れても図は残る ＝ 自由モードで続きを描ける。

### 12-7. ★ 答え合わせは**2列の対応表**（2026-08-15・v1370・C）

> 答え合わせは、正解と自分の図を縦に並べる方がよい／正解を左列、自分のこたえを右列で
> 対応関係をわかりやすくする（ユーザー・発注書 `video-scripts/ORDER_isomer_review_2026-08-15.md` の C）

**それまでの形**: 縦に2ブロック（`あなたの書き出し` ／ `標準の書き方と答え`）。
どちらも「全部を並べたギャラリー」で、**対応づけは読み手が目で突き合わせるしかなかった**。

→ **決定**: **行＝1つの異性体**の2列表にする。左が正解（標準の書き方・系統順）、右が自分の図。
**2列にするのは答え合わせだけ**（確認モードには並べる相手がそもそも無い。§13-1 の面の分担）。

| 2列に収まらないもの | どう出すか | なぜ |
|---|---|---|
| **未発見**（正解はあるが自分の図が無い） | 右セルを「未発見」の枠で空ける。**行は消さない** | 行が消えると「全部書けている」ように見える |
| **ダブり**（同じ正解に自分の図が2つ） | 右セルに **①④** と**並べる** ＝ 行の中で「同じものを2つ描いた」と言う | 表の外の「①と④は同じ」だけだと、どの正解の話か目で探すことになる |
| **お題に数えなかった図**（対応する正解が無い） | **表の下の別枠**。文言は従来どおり `・③ は C₂H₆O です（お題は C₃H₈O）`（**責めない文言**） | 左列が空の行を作ると「**正解が無い正解**」に見える |

**★ この表でいちばん危ない壊れ方は「行がずれる」**。自分の図の並び（①②③…）は
キャンバスの原子順で決まる（§12-4）ので、**未発見が1つ混じるだけで正解の並びとずれる**のに、
左右とも1行に1枚ずつ並ぶので**見た目は揃って見える**。

→ **決定**: 対応づけは `answerPairs()` が **`canonicalCode` の一致だけ**で行う（**並び順で突き合わせない**）。
描画から切り出してあるのは、この対応づけだけを検査できるようにするため。
検査は **`IW10`**（未発見・ダブり・お題外を同時に含む答案）と **`IW11`**（否定対照 ＝
添字で突き合わせる実装に差し替えると、左右が別の異性体を指す行が実際に見つかる）。
照合は**表の外**から引き直す（番号 → キャンバスの成分 → 正準コード）——
表が自分で書いた対応で照合すると、ずれたまま緑になる。

**縦の詰め方**（C₄H₁₀O は7行になる）: 行の高さは `IP_REVIEW_SCALES` に **`rowH` を足して
ギャラリー用の `h` と分けた**（小 56 / 中 104 / 大 168px）。図は `viewBox` が内容に合わせるので、
高さを下げても切れずに小さくなるだけ。左右のセルは行の flex（`align-items:stretch`）で
**必ず同じ高さ**になる ＝ 高さを2か所に書かない。

**実測（1280×800・v1370）**: 1行 **84px**（小）／132px（中）／196px（大）。
オーバーレイの見える高さ 659px のうち表より上が 265px（見出し・面の切替・集計・
未作成の内訳・同じものの箱）なので、**小で 7行中4行が画面に入り、残りはスクロール**。

**妥協点（正直に書く）**: 「同じもの（①と④は同じ ＝ 名前）」の箱と「未作成の異性体（官能基で分けた内訳）」の
箱は**表の上に残してある**。行の中でも同じことを言うので**重複**だが、箱の側は
「描き方が違ってもつながり方が同じなら同一」という**説明**と官能基ごとの**集計**で、
行の側は**場所**を指す、と役割が分かれている。消すなら 100px ぶん表が上がる ＝
画面に入る行が1つ増える。ここは見てから決める。
→ **§12-7a（v1372）で消した**。実測は 128px ぶんで、画面に入る行は 4 → 5 に増えた。

### 12-7a. ★ 3列にして、上の箱を表の見出しへ畳む（2026-08-15・v1372・D）

> 両方消したうえで、結果列を用意して3列構成にする／結果 / 〇 / 重複 / 未発見 で結果をわかりやすく／
> 名称（未発見）は名称のみに／名称のフォントが小さすぎるので少し大きくする／合計スコアもわかりやすくする
> （ユーザー・実機 v1370 を見ての続き要望）

**1. 表の上の2つの箱を消した**（`missingHintBox` は関数ごと削除・「同じもの」の箱は生成をやめた）。
どちらも行の中で同じことを言うので重複していた。実測でオーバーレイの上に積まれていた 265px のうち
**128px** がこの2つ。「まだ描けていないものを分類で束ねて見せる」役は**ヒントの段2**（骨格の系列ごとの
内訳）が持っているので、答え合わせの前に欲しい要約は失っていない。
ダブりの**色分け**（`dupColorOf`）は箱を消しても残す ＝ 表の中で「結果列の枠」と「自分の図の枠」を結ぶ。

**2. 結果列を足して3列にし、★ 左端に置いた**。

| 結果 | 正解（標準の書き方と答え・系統順） | あなたの答え |
|---|---|---|
| 〇 | 2-メチル-2-プロパノール | ② |
| 重複 | 1-ブタノール | ①④ |
| 未発見 | 2-ブタノール | —  |

> **なぜ左端か**: 読みの流れ（正解 → 自分 → 結果）を採るなら右端になる。だがこの画面で欲しいのは
> **走査** ——「一列を上から下へ舐めるだけで出来が分かる」こと。7行を縦に並べる器では、
> 結果が右端に散ると視線が毎行 往復する。反転は `resultCell` の挿入位置1行で戻せる。

**★ 「重複」は間違いではない（この節でいちばん壊されやすい決定）**。スコアは
「同じものを2回描いても減点はしません」と明記しており、**その異性体は正解として数えている**
（`scoreOf` の `raw = sheet.found.size`）。だから **✗ や赤にしない** —— 誤りに見せると、
**正しく見つけた人を叱る**ことになる。実装は 〇 と**同じシアン**（実測 `rgb(0,242,254)` で一致）で、
違うのは**枠の色だけ**（ダブりの組の色 ＝ 自分の図の枠と同じ）。
`IW12` が「〇 と同じ色」「✗・誤りの語を出さない」「重複した種も『見つけた』に数える」の3つを見張る。

**3. 名前は名前だけにした**。`✓` と `（未発見）` は結果列が言うので落とした。
自分の図の `（同じ）` も同じ理由で落とし、番号だけにした。

**4. 化合物名 10px/400 → 14px/600**。v1370 は**図の中の炭素番号（13px）より名前が小さい**という
逆転が起きていた。名前はこの表の主役なので上げる。
**★ 小/中/大では変えない** —— 変わるのは「図の大きさ」であって、**「小」でも名前は読めなければならない**
（`makeCell` の `labelSize`/`labelWeight` は呼び出し側が決め、`IP_REVIEW_SCALES` とは無関係）。
実測で 375px 幅でも最長名（`2-メチル-1-プロパノール（イソブタノール）`）が3行に折り返して収まり、
横スクロールは出ない。`IW12` が「14px 以上・太さ600以上・小/中/大で不変」を見張る。

**5. 集計とスコアを、表の**見出し行**に畳んだ**（別の箱にしない）。

```
全 7種中  〇 2種   重複 1種   未発見 4種                     スコア 3点 / 7点満点
描いた図 5個 → 見つけた 3種（〇 2 ＋ 重複 1）。正しく描けた 3種 − ヒント 0段 ＝ 3点（満点は … 7種。同じものを2回描いても減点はしません）
```

**なぜ別の箱にしないか**: 別の箱にすると**サマリーと行が食い違える**（片方だけ直して矛盾する事故が起きる）。
結果列を数えた値を、**その列の真上に**置く。

**★ 3つの数は `answerTally()` が `answerPairs()` の返した配列を数えて作る**。別計算にしない。
1行の結果は1つなので **`〇 + 重複 + 未発見 = 総数`** が必ず成り立つ。
⚠ `sheet` から作り直すと **`〇 = sheet.found.size`（重複した1種も 〇 に数える）**になり、
和が総数を超える —— しかも**表も見出しも崩れない**ので目では気づけない。
`IW13`（否定対照）がこの差し替えを実際に入れて赤くなることを確かめている。

**「見つけた 3種（〇 2 ＋ 重複 1）」を2行目に書いたのは**、`〇 2` と `正しく描けた 3種` が
食い違って見えるのを塞ぐため。**〇 は「単独で当てた行数」・見つけた種類は「〇 ＋ 重複」**で、
スコアは後者から出る。ここを書かないと「〇 が2つなのに3点」が謎になる。

**実測（1280×800・v1372・C₄H₁₀O・図5枚／3種・ダブり1・未発見4・お題外1）**:

| | v1370（2列） | v1372（3列） |
|---|--:|--:|
| 表より上に積まれた高さ | 265px | **86px** |
| 1行の高さ（小） | 84px | 89.2px（名前を 14px にしたぶん） |
| **画面に入る行数（小・全7行中）** | **4** | **5** |
| 化合物名 | 10px / 400 | **14px / 600**（小/中/大で不変） |
| 結果列の幅 | — | 62px 固定 |

**残した妥協点（正直に書く）**: 1行が 84 → 89.2px と**少し太った**。名前を大きくした代償で、
上の箱で稼いだ 179px に対しては小さい（差し引き 1行ぶん増えた）が、**次に何かを行へ足すときは
行の高さが先に効く**ことは覚えておく。`IW10` の「小で1行 ≤ 96px」がここの見張り。

### 12-8. 3つの書き出し練習の器と語をそろえた（経緯）

**立体異性体の書き出しだけ操作が違っていた件は解消した**（登録方式をやめ、異性体・アルキル基と
同じ「並べて描く」形に統一した。`DESIGN_practice_revision.md` §5-8a）。
以後 **3つとも「キャンバスが答案用紙」で、答案の在りかは連結成分の集まりだけ**。
あわせて**答案を指す語を「個」にそろえた**（アルキル基だけ「枠」と数えていた。発注書 §C）——
帯・パネル・答え合わせの見出しのどれでも `いま N個 描いてあります` ／ `描いた図 N個`。
⚠ **「枠」はコード側の内部の言葉としては残す**（付け根 C1–R の置き場所のこと）。
`＋ 答案` で明示的に増やす操作もそのまま（語だけそろえた）。

---

## 13. 答え合わせの2パターンと段階ヒント（2026-08-13 ユーザー要望）

> 完全な答え合わせ：すべて指摘・正解の明示 ／ ヒントだし：残りの異性体数の表示・重複の指摘・
> 未描画の異性体のヒント。ヒントは段階で小出しにするのもよい

### 13-1. 決定: **3つ目のモードを作らない。既にある2つの面に割り当てる**

いまオーバーレイには「表示: 確認／答え合わせ」のセグメント切替がある（v149・v150）。
ユーザーの2案は、この**2つの面にそのまま乗る**。

| 面 | いつ見えるか | 出るもの |
|---|---|---|
| **キャンバス（書き出し中）** | 常時 | **番号のみ・個数のみ。判定はゼロ** |
| **確認モード**（`openReview('progress')`） | 自分で開く | 自分の図だけを大きく並べる。名前なし・同一判定なし・残り数なし。**💡ヒントはここに置く** ＝「ヒントだし」 |
| **答え合わせモード**（`openReview('answer')`） | 自分で開く | 名前・「①と②は同じ」・正解一覧（系統順）・発見/未発見・集計 ＝「完全な答え合わせ」 |

**なぜ新しいモードを作らないか**: 「採点の厳しさ」を3つ目の軸として足すと、
確認/答え合わせ × 厳しさ の掛け算になり、**どの面で何が漏れるか**をもう一度全部決め直すことになる。
v402 で引いた線（未発見の官能基別要約は確認モードに出さない＝答えが割れる）は、
軸が1本増えるたびに引き直しが必要になる。**面は2つのまま、ヒントは面の中の段にする。**

**v402 の線はこの改訂でも守られる**: 未発見の要約は**確認モードに常時は出さない**。
出るのは**ユーザーが💡を押した段**だけ。押した人にだけ・段階で出るなら、線を越えない
（線の趣旨は「開いただけで答えが割れないこと」）。

### 13-1a. ヒントへの**到達手段**（2026-08-15・v1368。実装が §13-1 に追いついていなかった）

> **⚠ 実装は長いあいだ §13-1 とずれていた。** 💡は確認モードではなく **📚学習パネルのカード
> （`#isomer-practice-card`）にだけ**あり、練習を始めるとそのモーダルは閉じるので、
> **キャンバス（＝答案）を見ている画面からは押せなかった**。押すには 📚学習 を開き直すしかなく、
> そのあいだ答案はモーダルに隠れる ——「行き詰まったときに見るもの」なのに、
> **行き詰まっている画面からは押せない**。ユーザー報告「ヒントボタンが見当たりません」。
> 発注書は `video-scripts/ORDER_isomer_review_2026-08-15.md` の A。

**置き場所は §13-1 のまま（確認モード）。足りなかったのは「そこへの道」**なので、そこだけ決める。

| 決めたこと | 中身 | なぜ |
|---|---|---|
| **確認モードに 💡 を出す** | `renderReview()` の progress 面に「💡 次のヒント（あと N段・−1点）」＋段の中身 | §13-1 の表のとおり。**面は増やさない** |
| **作業帯から1手**（`🔎 確認・ヒント`） | 帯の押しものは**3つのまま**。ラベルにヒントを足して行き先を名乗る | 帯を厚くしない（`setPracticeStrip` の注意書き）。帯に4つ目を足すと 32px の床（§2-5）が割れる |
| **確認モードは0個でも開く** | `openReview('progress')` の「白紙なら開かない」を外す（**答え合わせ側は従来どおり**） | 行き詰まりは「まだ1つも描けない」から始まる。ここを塞ぐとヒントへの道が無い |
| **📚学習 のカードにも残す**（鏡写し） | 段も中身も `_hintLevel` 1つから作る（`buildHintBlock()` を共用） | お題選びの流れから押す道も残す。状態が1つなので**2面の表示が食い違わない** |
| 答え合わせの面には置かない | `carriesHintControl(face)` が `'answer'` と終了後を落とす | 答えが出ている面でヒントを売る意味が無い |

**教訓（帯の設計に効く）**: 「帯に無くても 📚 を開き直せば届く」は**いつでも成り立つ言い訳ではない**。
開き直すとモーダルが**手元（答案）を覆う**ものは、覆われたら用を成さない ＝ 届いていない。
**帯に足す代わりに、帯から1手の面に置く**のがこの器の解き方。

**検査**: `IW9`（帯のボタンを実際に押し → 確認モードの 💡 を押し → 段が1つだけ・減点も1点）。
IW5・IW6・IW8 は `nextHint()` を直接呼ぶので**到達手段を1つも見ていなかった**＝置いただけの緑だった。

### 13-2. ヒントは4段（押すたびに1段）

> **⚠ 更新（2026-08-13・§15-5）**: 「段は戻せる」は**失効**。
> **1 → 2 → 3 → 4 の一方向**で、段4 のあとは「答え合わせ」しか押せない。
> 表示中の段はキャンバスの変化に合わせて**自動で更新**し、押すのは次の段へ進むときだけ
> （読み返しで減点しないため。§15-5a）。

粒度は既存の `isomerSeriesKey`（`funcType` / `chainLen` / `sideSizes` / `gemPair` / `seriesLabel`）
がそのまま与える。**ヒント用に新しい分類を作らない。**

| 段 | 何を明かすか | 例 |
|--:|---|---|
| **1** | **あといくつ**（未発見の件数のみ）。＋ **ダブりが何組あるか**（数だけ） | 「あと 2種 あります。ダブりが 1組 あります」 |
| **2** | **どの系列が足りないか**（`seriesLabel` で数える） | 「主鎖4＋メチル1つ があと1つ／エーテル があと1つ」＝ ユーザーの言う「主鎖5の異性体が足りない」 |
| **3** | **書き出しの手順**（未発見の系列に対応する定型文だけ。既存の文言を流用） | 「炭素2個分の側鎖の置き方は3通り —— エチル基1つ／メチル×2を同じ炭素に／メチル×2を別の炭素に」 |
| **4** | **重複の組を名指し**（名前は出さない） | 「①と④は同じものです」 |

「答えを見る」は**ヒントの段ではない**（面の切替＝答え合わせモードへ行く）。
§6 の旧設計はヒント3段目を「答え」にしていたが、**段と面を混ぜない**ように外す。

### 13-3. 決定: 重複の指摘は「ヒントの最終段」。常時は出さない

迷いどころなので理由をはっきり書く。

- 重複の指摘は「①と④は同じ」＝ **同一性の判定を1件ぶんタダで渡す**行為。
  この練習の学習価値の半分は「**描き方が違っても同じ**」を自分で見抜くこと（§1）なので、
  常時出すと練習の芯が抜ける。「間違いではないから出してよい」は**情報量の話を見落としている**
- とはいえ「気づかないまま数だけ合わない」も学習にならない。だから**自分から押した最後の段**に置く
- **数と組を分ける**のが折り合い点。**「ダブりが1組ある」は段1で言う**（＝「探せ」の合図。
  どこを探すかは自分で決める）。**「①と④」は段4**（＝ 答え）
- **答え合わせモードでは従来どおり全部出す**（色枠＋「①と②は同じ ＝ 2-メチルプロパン」）。
  ここは答えを見に来た面なので伏せる理由がない

### 13-4. ミッシングの指摘

ユーザーの言う「ミッシングの指摘」は**段2（系列で）**と**答え合わせ（現物で）**に分けて割り当てる。
段2は「どの引き出しが空か」まで、現物（構造そのもの）は答え合わせでしか出さない。

---

## 14. アルキル基の書き出し（分子ではない構造）

> アルキル基の書き出し時のみ、分子ではない構造を描くので工夫が要る

**心配されているより事情は良い。**化学の側はすでに解けている:

- 付け根は **`R`（価標1の擬似元素・v162）としてグラフに入っている**ので、
  `canonicalCode` が根つきになり、**sec-ブチルとイソブチルは自動的に別物**として判別される
- 列挙は `enumerateConstitutionalIsomers([...C, 'R'])`、命名は `iupacAlkylNameFromR` で通る（v162〜v163）

**残る工夫は器の側だけ**なので、そこだけ決める。

### 14-1. 決定: 付け根は N 組。**自動で増やすが、一度に1組ずつ**

- 付け根（C1–R のロック済みペア）は**アプリが置く**。ユーザーには足させない。
  理由: 付け根は答案用紙の**罫線**であって答えではない。ユーザーが引くと、R を置き忘れた図が
  「ただの分子」として採点対象に混ざり、`AlkylPractice` が唯一きちんと弾いている条件
  （**R がちょうど1個**）が答案ごとに崩れる
- **最初は1組だけ置く。**「＋ 答案をもう1つ」で空きスロットに1組増やす。
  ★ **最初から N 組並べてはいけない** —— 盤面に8組の枠を置いた時点で「答えは8個」と教えてしまう。
  上限は設けない（20 まで許す）ので、押した回数からも答えは漏れない

> ★ **道しるべ（v1376 で足した・読む順の注意）**: この決定は**据え置き**（2026-08-15・ユーザー判断）だが、
> **増やし方は §14-5 で2つになった**（＋ 答案 のほかに「**孤立した炭素を置けば付け根が生える**」）。
> §14-5 が §14-1 を破らない理由も、上に書いた**理由が現行実装では成り立っていない**こと
> （お題ボタンが `C₄H₉–（4種）` と総数を先に出している）と、**それでも据え置く別の理由**も、
> **すべて §14-5 に書いてある。**ここだけ読んで「枠は＋ 答案でしか増えない」と思わないこと

### 14-2. 決定: 保護は「組ごと」ではなく従来どおり「原子ごと」で足りる

`isLocked` は原子の属性なので、N 組に増やしてもそのまま効く。直すのは**判定の粒度**だけ:

- `register()` の `rs.length !== 1`（R がちょうど1個）は**分子全体**を見ている。
  → **成分ごとに**「R がちょうど1個・余分な原子なし・炭素数が目標どおり」を見るよう作り替える
- R に接する結合の保護（削除・次数トグル不可）は結合ごとの判定なので、そのまま N 組に効く

### 14-3. 決定: 「これは分子ではない」は**問題カードに1回だけ**書く

- 成分の見出しは **`C₄H₉–`**（`R` を「–」と読ませる）にする。**「分子式」とは呼ばない**。
  見出しの語は「アルキル基 C₄H₉–」
- 「これは分子ではなく、分子の一部（基）です。R は他の原子とつながる手を表します」は
  **問題カードに常設**。トーストで毎回言わない（毎回言うと、正しく操作している人を毎回叱ることになる）

### 14-4. ★ ロック原子の案内（v1260 のあと）— **実測すると手当ては要らなかった**

`game.js` の pointerup を読むと、順序はこうなっている:

```
moved > 8px  → 黙って終わる（「動かせません」と毎回叱らないための決定）
       ↓
hit.isLocked → 「ここ（ロックした原子）は消したり別の元素に変えたりできません。」
```

つまり **タップ（8px 以内）なら案内は従来どおり出る。黙るのは引きずったときだけ。**
付け根が N 組に増えても、**触ったときの案内は生きている**。

→ **決定: トーストを増やす方向へ行かない。見た目を強くする。**
ロック原子はすでに破線の円（`stroke-dasharray 3,3`）で描かれているので、
**C1–R の組を薄い枠で囲って「答案の枠」に見せる**。触る前に分かるほうが、触ったあとに叱るより良い。

### 14-5. 付け根の増やし方（v1376・2026-08-15。実使用から出た指摘）

**正はここ**（会議録は [DESIGN_practice_revision.md](DESIGN_practice_revision.md) §3 → ここへ移した）。
ユーザーが実機を触って出した困りごとは3つに分かれ、**1つの直しでは足りず3つの決定**になった:

| # | 困りごと | 決定 |
|---|---|---|
| (a) | 2個目以降の増やし方が分からない | **A1**: 孤立した炭素を置いたら、その場に付け根が生える |
| (b) | アルキル基でない図を増やせてしまう | **A2**: 炭素以外はそのまま置かせ、**門を増やさず採点表で返す** |
| (c) | 増やした位置がやりづらい | **A3**: 「＋ 答案」は残し、**見えている範囲の空き**へ置く |

#### A1: 孤立した炭素を置いたら、その場に付け根が生える

アルキル基の練習中にかぎり、`placeAtomOrExplain` が**どこにも結合しない新しい炭素**を置いたとき
（＝ `getPlacementBondTargets` が空）、その炭素を **C1** とし、**隣の格子点に `R` を置いて単結合で結び、
両方を `isLocked`** にする（`AlkylPractice.sproutRootFor`）。

- **R の向きは 左 → 右 → 上 → 下**（既定の左隣は `addSlot` と同じ）。
  **42px 以内に重原子がある向きは飛ばす**
- ⚠ **4方向とも塞がっていたら炭素だけ置いて R を生やさない**（無理に置くと図が重なる）。
  その成分は `noroot` として答え合わせで指される ＝ **黙って変な図を作らない**
- **§14-1 を破らない。** 枠が増えるのは「押した回数ぶん」ではなく「**描いた回数ぶん**」なので、
  **盤面が答えの個数を先回りして名乗らない**。★ §14-1 の「最初から N 組並べてはいけない」は
  **据え置き**（2026-08-15・ユーザー判断）。⚠ §14-1 が書いている理由
  （「盤面に N 組置くと答えが漏れる」）は、**お題ボタンが `C₄H₉–（4種）` と総数を先に出している**ので
  現行実装では既に成り立っていない。それでも据え置くのは
  **「（4種）」の文字は読み飛ばせるが盤面の枠は目に入り続ける**という別の理由による
- **付け根の供給はアプリの仕事のまま**（§14-1 の本体）。ユーザーに `R` を足させないので、
  R を置き忘れた図が採点に混ざらない
- 異性体側の「**描けば答案**」（§12-1）と同じ規則になる ＝ 書き出し練習の中で覚えることが1つ減る

#### A2: 炭素以外を置いたときは、そのまま置かせる（R を生やさない）

- **R を生やしてはいけない理由**: `O–R` のような「アルキル基でないもの」に枠を与えると、
  **盤面が間違いを肯定する**。付け根は答案の**罫線**（§14-1）なので、罫線を引いた時点で
  「これは答案として成立する形だ」と言ってしまう
- 採点表の文言だけ直す。`noroot`（付け根 0）を
  **「アルキル基は炭素から始まります（この図には炭素の付け根がありません）」**に。
  ⚠ **`extra`（炭素と水素以外が混ざっている）と言い分ける** ——
  「C–O–R を描いた」（extra）と「O だけ置いた」（noroot）は**別の間違い**

#### A3: 「＋ 答案をもう1つ」は残すが、置き場所を変える

**残す理由**: ①炭素を置く操作に不慣れな人の保険 ②`AK3`（＋答案 の前は付け根が1組しか無い）が押している
③作業帯の `＋ 答案` は帯の3ボタンの1つで、消すと帯の構成が変わる。

**変えるところ**（実測 v1371: 固定格子は横 168〜672 ＝ **504px** で PC 幅の viewBox 408×306 に
最初から入らず、押すたびに viewBox.x が **72 → 324 → 66 → 72 → 324** と **±258px 振れて**いた）:

```
1. viewBox の中の格子点を走査し、既存の重原子から AK_SLOT_FREE(96px) 以上離れた点を探す
   → 見つかったらそこへ置く（★ viewBox を1px も動かさない）
2. 見えている範囲に空きが無いときだけ、いままでどおり固定格子の次へ送り scrollSlotIntoView で寄せる
```

- ⚠ **白紙のときは①を飛ばす**（`start()` / `restartProblem()`）。空きを探させると
  1枠目が視野の左上隅へ寄り、既定の視野の真ん中に付け根が出る形が崩れる
- **拡大率は触らない**（`scrollSlotIntoView` の注意書きのまま）

#### 回帰テスト（**AK5〜AK8**）と否定対照

| | 中身 | 外すと |
|---|---|---|
| **AK5** | 孤立した炭素を置くと `C`＋`R`・どちらも `isLocked`・単結合になる。4方向が塞がっていれば炭素だけ置く。**練習をやめた後は生えない** | 生やす仕掛けを外すと「成分の原子が 1個」で赤 |
| **AK6 ★** | **炭素以外**（O）では R が生えず、その成分だけが `noroot` で指され、他の成分の採点は通る。`extra` と文言が違う | 元素の判定を外す（どの元素でも生やす親切）と赤／文言を戻しても赤 |
| **AK7 ★** | 見えている範囲に空きがある状態で `＋ 答案` を押すと **viewBox が 1px も動かない** | 固定格子に差し戻すと 2回目で **x が 72 → 324（+252px）** 飛んで赤 |
| **AK8 ★** | 炭素を3か所に離して置くと枠が **1 → 4組**になり、**`＋ 答案` を押した回数は 0 のまま**（§14-1 と両立している証拠） | 生やす仕掛けを外すと「枠が 1組」で赤 |

---

## 15. マイルストーン・テスト・やらないこと

### 15-1. マイルストーン

命名の確認（**N1〜N3**）は [DESIGN_iupac_check.md](DESIGN_iupac_check.md) が持つ。
**W2 は N1 に依存**（ヒントの主鎖の言い方が主鎖の定義に依存するため）、**W1 は独立**。

- **W1: キャンバス答案用紙化** — 登録の廃止／成分ごとの採点（§12-2）／`captionForPart` で名前を伏せる／
  練習中は分子モーダルを開かない／個数だけのライブ表示／確認・答え合わせオーバーレイを
  スナップショットではなく**そのときのキャンバス**から作る。
  **完了条件**: C₄H₁₀O を7種すべてキャンバスに描いて答え合わせが「7/7・ダブり0」を出す。
  **IW1〜IW4 が緑**、旧 IP1〜IP9 を書き直したものが緑
- **W2: ヒント4段**（**完了・v1340**） — §13-2 の4段と、確認/答え合わせの面の分担。既存の `isomerSeriesKey` を流用。
  **完了条件**: 段1〜4 が押すたびに1段ずつ開き、**段3までは重複の組を明かさない**（**IW5・IW6・IW8**）。
  実機で C₄H₁₀O・ヒント2段・7種 → **スコア 5点 / 7点満点** を確認した
- **W3: アルキル基の答案用紙化**（**完了・v1367**） — 付け根 N 組（§14-1）・成分ごとの検査（§14-2）・
  見出しと枠（§14-3・§14-4）。**完了条件**: C₄ の4種を1枚に描いて答え合わせが 4/4 を出す（**AK2〜AK4**）。
  実機（C₄H₉–）で **4枠 → ちがう種類 4／全 4 種・ダブり0・未発見0** を確認した。
  実装で決めたこと（設計の追記）:
  - **`register()` は消した。** 異性体側（W1）と同じく、答案は `game.userMolecule` の連結成分だけ。
    旧 `AK1` は登録を叩くテストだったので、答案用紙の形に書き直した
  - **`worksheetActive()` にアルキル側を足した**（旗は1つのまま・§12-3）。これで名前を伏せる門番・
    分子モーダルの封鎖・成分ごとの番号付けが、アルキルの練習にもまとめて効く
    （**それまでアルキル練習中は分子モーダルが開き、名前が見えていた**）
  - **枠は「C1–R の2原子」ではなく成分ぜんぶを囲う**（`renderAnswerSlotFrames`）。
    炭素を伸ばしたら枠も育たないと、答案が枠の外へはみ出す絵になる
  - **空いているスロットを探して置く**。付け根の格子（3列・252×168）は答案が横に伸びると
    埋まるので、隣の空きへ送る。置いた枠が画面の外なら**平行移動だけ**で見える所まで送る
    （拡大率を触ると見出しの焼き直しになる）
  - **ヒント4段とスコアはアルキル側に入れていない**（W2 は異性体側の話・§15-1）。
    面の分担（確認 ⇄ 答え合わせ）だけ同じ形に揃えた
- **W4: 答案を並べ直す**（**完了・v1383**） — 剛体移動だけのスロット配置（§12-5・実装は §12-5a）。
  **完了条件**: 20成分を置いて並べ直しても、**どの成分の内部座標も1つも変わっていない**（**IW7**）。
  実機（1280×800）で **C₄H₁₀O の7種 → 3列に並べ直して外接矩形 453×480px ＝ 7種とも画面に入る**、
  **20成分 → 4列×5行・内部の相対座標が変わった成分 0/20**、4倍に拡大＋パンしてから押しても
  20/20 が画面に入ることを確認した。
  **否定対照を外して見た赤**（2通り）:
  ①「縦長の答案だけ 90° 回して詰める」最適化を混ぜる → `IW7: 成分 5 の内部座標が変わった`。
  ⚠ **1回目の実験は空振りした** —— 検査の答案が横長ばかりで、回転が**発火しなかった**。
  縦長の形（縦一直線の炭素鎖 ＝ フィッシャー投影が読む向きそのもの）を混ぜて初めて赤くなった。
  ② スロットの隙間を 0 にする → `IW7: 並べ直しが働いていない（moved=0 / reason=clearance）`
  ＝ 27.3px の関所が実際に効いている

### 15-2. 回帰テスト（接頭辞は `assembler/tests.js` 冒頭の台帳で未使用を確認済み）

**★ 台帳（`assembler/tests.js` 冒頭）は `| IW | 1〜13 | …並べ直し |` に更新済み**
（v1383 で「7 は W4 に予約」の但し書きを外し、連続した範囲に直した）
（`IN` は DESIGN_iupac_check.md 側で足す。`AK` は既存の帯を 2〜4 へ伸ばす）

| # | 何を見るか |
|---|---|
| **IW1** | C₄H₁₀ の2種を**1枚のキャンバスに**描き、答え合わせが 2/2 とクリア記録を出す（`register()` を一度も呼ばない） |
| **IW2** | 同じ構造を2つ描くと、答え合わせで「①と②は同じ ＝ 名称」が出て、**ちがう種類は1**と数える |
| **IW3** | 分子式の違う成分・描きかけの成分が混ざっていても**採点が成立し**、その成分だけ「C₃H₈O です」と指される（＝ 昔の「登録を断る」が採点表に移ったことの検査） |
| **IW4** | **★否定対照**: 練習中にキャンバスへ2成分を置いても、SVG の見出しに**化合物名が1つも現れない**（番号だけ）。分子モーダルも開かない ⇒ **`captionForPart` の門番を外すと `ブタン` `2-メチルプロパン` が現れて赤くなる**（この穴は実測で確認済み・§12-3） |
| **IW5** | ヒントを1段ずつ押すと、**段1=残り数とダブりの組数／段2=系列内訳／段3=手順／段4=重複の組**の順に**積み上がる**。段4 でボタンが「答え合わせ」に変わり、答え合わせを押すと問題が終了して**スコアが凍結**される（C₄H₁₀O・ヒント2段・7種 ＝ **5点**） |
| **IW6** | **★否定対照**: 段3までのヒントにも、確認モードを開いただけの画面にも、**重複の組も系列内訳も名前も1つも出ない**（§13-3・v402 の線）⇒ 門番（`revealsDupPairs` / `showsJudgments`）を true 固定に「親切にした」直しが赤くなる |
| **IW8** | **★否定対照**: **読み返しでは減点されない**（§15-5a）。ヒント欄の開閉は段を進めず、描き足すと**押し直さずに**中身が更新される（「あと4種」→「あと3種」）。スコアは到達した段の数だけで決まる ⇒ ヒントを押した瞬間の文面で固める（＝ 読み返しに押し直しが要る）直しが赤くなる |
| **IW7** | **★否定対照**（**v1383**）: 「並べ直す」の前後で、各成分の**原子の相対座標**（成分の重心からの差）が1つも変わらない ⇒ 内部の整形に手を出すと赤くなる。**「重ならなくなったこと」だけを見ない** —— 回転を混ぜた直しは重なりの検査を素通りするため。検査の答案には**縦長の形を必ず混ぜる**（横長ばかりだと「縦長だけ回す」最適化が発火せず空振りする。実験で実際にそうなった）。同じ検査の中で ②移動量が `GRID_SIZE` の整数倍、③別成分の重原子が 27.3px より近づかない、④比べ方が 90°回転を検出できること（空振り防止）、⑤帯のボタンが 2個未満で押せないこと、⑥↩ で戻せることも見る |
| **AK2** | C₄ のアルキル基4種を**1枚に**描いて 4/4（付け根4組） |
| **AK3** | 「＋ 答案をもう1つ」の**前は付け根が1組しか無い**（＝ 盤面が答えの個数を漏らさない） |
| **AK4** | **★否定対照**: 成分のうち1つだけ R を消す（消せないので、R を含まない成分を別に描く）と、**その成分だけ**が「付け根がありません」と指され、他の成分の採点は通る ⇒ 検査を分子全体のままにすると赤くなる |
| **AK5〜AK8** | 付け根の増やし方（**§14-5**・v1376）。6〜8 は否定対照 |

### 15-3. 否定対照をどこに置いたか（まとめ）

「直したつもりで何も検査していない」を避けるため、**4か所に「壊した実装なら必ず赤くなる検査」**を置いた。

| 検査 | 外すと壊れるもの |
|---|---|
| **IW4** | 名前の伏せ方。実測で**いま開いている穴**なので、これは「入れたら通る」ではなく「入れる前は赤い」検査になる |
| **IW6** | v402 の線（確認モードは答えを割らない）と §13-3 の線（組は段4 まで伏せる）。ヒントを常時表示に「親切にした」直しが赤くなる |
| **IW8** | 「押した回数 ＝ 到達した段」（§15-5a）。ヒントの中身を押した時点で固める直しが赤くなる ＝ **読み返しに課金する**作りを機械で禁じている |
| **IW7** | 剛体移動の約束。並べ直しに**回転や整形を混ぜた**直しが赤くなる（重なりだけを見る検査では回転が素通りする） |
| **AK4** | 成分ごとの検査。分子全体の検査に戻した直しが赤くなる |
| **AK6・AK7・AK8** | 付け根の増やし方（§14-5）。「どの元素でも R を生やす」親切／固定格子への差し戻し（**x が +252px 飛ぶ**）／「描いた回数ぶんしか増えない」の3つが、それぞれ赤くなる |
| **IN2**（別文書） | 主鎖と名前が同じ計算から出ること。`findLongestCarbonChain` に差し戻した直しが、**13件の化合物名を挙げて**赤くなる |

### 15-4. やらないこと（範囲外）

- **命名を入力させる採点**。今回作るのは「確認」＝ 主鎖と番号を**見せる**側だけ
- **立体異性の採点**（シス/トランス・鏡像）。構造異性のみ、という既存の線（§4.2）を動かさない
- **答案の保存・共有・提出**。クリア記録（localStorage）だけは従来どおり
- **並べ直しの自動実行**。押したときだけ動く
- **ナフタレン等の2環系**（§11-3 の判断をそのまま維持）
- **芳香族・カルボニル系の主鎖表示**（`iupacName` が未対応。P12-3 の仕事）

### 15-5. 決着（2026-08-13・ユーザー判断）

§15-5 の未決5件はすべて決まった。**進行は「ヒント4段 → 答え合わせ → 終了」の一方向**になる。

| | 決定 |
|---|---|
| ヒントの進み方 | **1 → 2 → 3 → 4 の一方向（ラチェット）。段は戻らない・ループしない** |
| 段4 のあと | **押せるのは「答え合わせ」だけ**（ヒントは打ち止め） |
| 答え合わせ | **押したら問題終了**。ダブりの明示・未描画の構造の提示をここで全部出す ＝ **1問1回** |
| スコア | 答え合わせ時に出す。**ヒントを押した回数だけ減点** |
| 重複の指摘 | **ヒントでのみ**（常時マーキングはしない）。数は段1・組は段4 のまま |
| 主鎖の帯 | **自由モードにも「命名確認」として常設**（分子モーダルの中だけにしない） |
| 名前 | **系統名を主**（慣用名は併記） |
| 採点 | **毎回ゼロから作り直す**（§12-4 の「番号に頼らない」と同じ結論） |
| クリア記録 | `chemIsomerPractice.<分子式>` を**引き継ぐ**（基準は変わらないため） |

**§13-2 の「段は戻せる」は失効**。ラチェットにしたので戻る道は無い。

#### 15-5a. ★ 「押した回数」と「到達した段」を同じものにする

ヒントの中身（残り数・未発見の系列）は**キャンバスが変わるたびに計算し直す**。
描き進めれば「あと2種」は「あと1種」に変わるので、**表示は常に現在のキャンバスに対する答え**でなければならない。

このとき**再表示のために押し直させてはいけない**。押し直しが要る作りにすると、
**読み返すだけで減点される**——これは「ヒントを使った量」ではなく「記憶力」を測ることになる。

→ **表示中の段は貼り付いたまま自動で更新し、押すのは「次の段へ進む」ときだけ。**
　こうすると **押した回数 ＝ 到達した段（最大4）** が一致するので、
　「ヒントを押した回数だけ減点」というユーザーの規則がそのまま素直な式になる。

#### 15-5a-2. ★ ヒントは**積み上がる**（段Nに到達したら 1〜N が全部見えている）

「いま到達した段だけを表示」にすると穴が開く。**段4（重複の名指し）まで進んだあとに
重複を直すと、段4 が言うことを失って画面が空になる**——払ったものが消える。

→ **段Nに到達したら 1〜N を並べて出す。** どれもキャンバスに合わせて自動更新するので、
　「あと N 種」は減っていき、重複が解消すれば段4 の行だけが消える（段1〜3 は残る）。

**段4 まで行ったあとに描き足した場合**: ヒントはもう押せない（打ち止め）が、
**表示は自動更新を続ける**。情報は生きたまま、追加はもらえない。

#### 15-5a-3. ★ ボタンの表示で費用を見せる

押すたびに1段進む以上、**「💡ヒント」とだけ書いてあると、いまの状態を見たいつもりで押して減点される**。

→ **「💡 次のヒント（あと3段・−1点）」**のように、**押す前に何が起きるかを見せる**。
　段4 まで来たら**ボタンを「答え合わせ」に置き換える**（押せないボタンを残さない）。

**表示のオンオフ（閉じて開き直す）は段を進めない。** 進むのは「次のヒント」を押したときだけ。

> この2つは「**無言で起きることが事故になる**」の一例。
> 同じ日に、無言の削除（v1260）・無言の結合失敗（v1221）・無言のくぐり（v1160/v1240）を
> それぞれ塞いでいる。**代償のある操作は、押す前に代償が見えていること。**

#### 15-5b. スコアの式（提案。実装レーンはここを起点にしてよい）

**単位を「異性体1つ」に揃える**のがいちばん説明しやすい。

```
満点   = その分子式の異性体の総数（C₄H₁₀O なら 7）
素点   = 正しく描けた種類数（重複は1種類として数える）
スコア = 素点 − ヒント到達段数（下限 0）
```

- **ヒント1段 ＝ 異性体1つぶん**というレートになるので、
  「ヒントを2段使って7種そろえた ＝ 5点」と口で説明できる
- **重複は減点しない。** 重複は「間違い」ではなく「同じものを2回描いた」だけで、
  素点が種類数なので**自然に得点にならない**（二重に罰する必要がない）
- 満点が問題ごとに違うのは意図的（**問題の重さがそのまま満点**）。
  百分率にすると「7種で1つ落とす」と「2種で1つ落とす」が同じ −50% 前後になって、
  難しい問題ほど1つの取りこぼしが軽く見える

**やらないこと**: 時間による加点・減点（書き出しは速さを競う練習ではない）。

---

## 16. お題を総当たりで洗い出した（骨格の型で範囲を宣言する・v1433）

**正はここ**（§4.1 の6問の表は「最初の6問」の記録として残す）。
2026-08-20 のユーザー判断と、発注書 [`video-scripts/ORDER_isomer_2026-08-20.md`](video-scripts/ORDER_isomer_2026-08-20.md) の A-5 の決着。

> **１・２ C6H12 は鎖式モードで妥当　環式ができても面白い**
> **一度に多すぎる書き出しを行うのが適当ではない、という意味で分けたほうがよいような印象**
> **C5H8　アレンを外すのは、ユーザーに指示がしにくい**
> **自由にテーマを選べるようになっているが、制約が大きいので、可能な問題はすべて用意したほうがよいと思います**

### 16-1. 決定: 「つまみ」ではなく **お題を分けて増やす**

発注書 A-5 の②（お題を増やす）を採った。**つまみ（☑ 鎖式だけ）にはしない。**

- **つまみは「いま何を数えているか」が画面から消えやすい**。§11-4 が
  「宣言した以上、画面のどこでも隠さない」と決めた所と真正面からぶつかる
- **正解の定義そのものが変わる**ので、母数を変えるだけのつまみ（クイズ側）と同じ形にすると、
  触った人が「答えの数が変わった」ことに気づけない
- **芳香族回（§11-4）とまったく同じ道をなぞれる**。`aromaticOnly` の旗が回っていた所を
  `IP_SCOPES` という**1つの表**にまとめ、鎖式・環式はそこへ行を足しただけ

### 16-2. ★ お題の名前で範囲を言い切る（但し書きに頼らない）

> **C5H8　アレンを外すのは、ユーザーに指示がしにくい**（ユーザー）

**範囲は「環をもつ / もたない / ベンゼン環をもつ」の3つだけ**にした。
**正解集合から特定の骨格（アレンなど）を抜く細工はしない。**

- 抜いた瞬間、お題の名前だけでは何が正解か言えなくなる ＝ **但し書きが要る**
- 「C₆H₁₂（鎖式・13種）」まで読めば、**開く前に何を書き出すのかが分かる**のが要件

### 16-3. 総当たりの結果 —— お題に採る線は4本

**分子式（C 1〜6・O 0〜2・不飽和度 0〜4）× 骨格の型（全 / 鎖式 / 環式）を総当たりした**
（既存の `enumerateConstitutionalIsomers` と `findAnyCycle` だけを使い、新しい数え方は書いていない）。

| # | 線 | 根拠 |
|---|---|---|
| ① | 種類数が **2〜20** | 上限 `IP_MAX_ISOMERS = 20` は据え置き（画面に入る行数の門番・§12-7）。下限 `IP_MIN_ISOMERS = 2` は芳香族回の `IP_BENZENE_MIN_ISOMERS` と同じ線 ＝「1種の式は書き出して見比べる練習にならない」。実測でここに当たるのは **C₃H₆ を型で絞ったとき（鎖式1・環式1）**。既存の C₄H₁₀ が2種で成立しているので 2 で足りる |
| ② | **既存の門番を破らない** | 重原子6個まで／重原子6個かつ不飽和度2以上は数える前に断る。⚠ **お題ボタンは開く前に種類数を出す**ので、**19件ぶんの列挙が起動時に1回走る**（★ v1489 で種類数を伏せたが、`problems` 側はボタンの**分子式の文字列**を列挙結果から作っているので、この1回はまだ残っている。**分類つきの回（§19-3）は押されるまで数えない**）（`renderList` は生成直後の `setTimeout(0)` で1度だけ。以後はキャッシュ）。実測 C₆H₁₀ 2.8秒・C₆H₈ 6.5秒・C₆H₆ 10.0秒 ＝ 門番はここでも要る |

**② の代償（実測・v1433）**

| 見たもの | 6問のとき | **19問のいま** |
|---|--:|--:|
| 初回 `renderList()`（PC 1280×800） | 約 133ms | **524ms** |
| 同（スマホ 375×812） | — | **757ms** |
| 2回目以降 | 0ms | **0ms**（`_cache`・`_rawCache`） |
| 起動直後の longtask 合計 / 最悪（PC） | — | **888ms / 406ms** |

⚠ **払うのは起動時に1回だけ**（学習パネルを開くたびではない）。いちばん重いのは
**C₆H₁₂ の 357ms** で、鎖式と環式の2つのお題が `_rawCache` で**この1回を分け合っている**
（分けなければ倍払う）。**これ以上お題を足すなら、ここが先に効く** ——
足す前に `renderList()` の初回時間を測ること。
| ③ | **正解の全部に名前が付く** | 答え合わせの左列が「（名称未登録）」にならないこと（§11-7 が残した宿題）。⚠ **ここが最も多くを落とした**（下表） |
| ④ | 型を分けるのは **環をもつ正解が1件以上ある式だけ** | C₄H₁₀ のように環が0件の式で「鎖式」の回を作ると、**全体の回と同じ集合が2つ並ぶ**だけ |

**③で落ちたもの（数で示す。⚠ 出し惜しみではない）**

| 落ちたお題 | 種類数 | 名前が付く数 |
|---|--:|--:|
| **C₅H₈（環式）** | 17 | **1** |
| C₄H₆（環式） | 5 | **0** |
| C₄H₈O（鎖式） | 15 | 7 |
| C₄H₆O（鎖式） | 20 | 6 |
| C₃H₄O₂（鎖式） | 19 | 3 |
| C₃H₆O₂（鎖式） | 19 | 4 |
| C₃H₆O₂（環式） | 15 | 0 |
| C₃H₈O₂ | 11 | 3 |
| C₃H₄O / C₃H₆O / C₂H₄O₂ ほか | 9〜13 | 2〜3 |

### 16-4. 収録したお題（**20**＝ 固定19 ＋ 芳香族プリセット1）

**⚠ `problems` の添字 0〜5 は動かさない**（回帰テストが `start(2)` のように添字で開く）。足すのは末尾だけ。

| # | お題 | 種類数 | 不飽和度 | 群 | 備考 |
|--:|---|--:|--:|---|---|
| 0 | C₄H₁₀ | 2 | 0 | 定番 | 既存 |
| 1 | C₅H₁₂ | 3 | 0 | 定番 | 既存 |
| 2 | C₃H₈O | 3 | 0 | 定番 | 既存 |
| 3 | C₆H₁₄ | 5 | 0 | 定番 | 既存 |
| 4 | C₄H₈ | 5 | 1 | 定番 | 既存 |
| 5 | C₄H₁₀O | 7 | 0 | 定番 | 既存 |
| 6 | C₂H₆O | 2 | 0 | 定番 | アルコールとエーテルの入口 |
| 7 | C₃H₆ | 2 | 1 | 定番 | プロペンとシクロプロパン |
| 8 | C₅H₁₀ | 10 | 1 | 定番 | 立体が分かれる場所2（鎖1・環1） |
| 9 | **C₅H₁₂O** | **14** | 0 | 定番 | ★ **不斉炭素をもつ種が4つある唯一のお題**（A-6 の段が成立する前提） |
| 10 | C₄H₈（鎖式） | 3 | 1 | 定番 | 教科書の「アルケン3種」 |
| 11 | C₄H₈（環式） | 2 | 1 | 定番 | |
| 12 | C₅H₁₀（鎖式） | 5 | 1 | 定番 | |
| 13 | **C₅H₁₀（環式）** | 5 | 1 | 定番 | ★ **メソ体（1,2-ジメチルシクロプロパン 2²=4 → 3）はここにしかない** |
| 14 | **C₆H₁₂（鎖式）** | **13** | 1 | 定番 | ★ 教科書のアルケン13種そのもの |
| 15 | C₆H₁₂（環式） | 12 | 1 | 定番 | メソ体（1,2-ジメチルシクロブタン 4 → 3） |
| 16 | C₃H₄（鎖式） | 2 | 2 | じっくり | プロピン・アレン |
| 17 | C₄H₆（鎖式） | 4 | 2 | じっくり | |
| 18 | **C₅H₈（鎖式）** | **9** | 2 | じっくり | ★ **アレン3件は外さない**（ユーザー判断） |
| — | C₈H₁₀（芳香族） | 4 | 4 | 芳香族 | 既存プリセット（§11-4） |

⚠ **19件すべてで、正解の名前が全部そろう**（「（名称未登録）」0件・実測）。

#### ★ 「鎖式に分ける」は環式を捨てることではない

**鎖式だけにすると、立体の見どころ（メソ体）が消える。** 実測:

| 式 | 構造異性体 | 立体が分かれる場所 | 2ⁿ が崩れる種 |
|---|--:|--:|---|
| C₅H₁₀（鎖式） | 5 | 1（2-ペンテン） | **0** |
| **C₅H₁₀（環式）** | 5 | 1 | **1,2-ジメチルシクロプロパン（4 → 3）** |
| C₆H₁₂（鎖式） | 13 | 5 | 0 |
| **C₆H₁₂（環式）** | 12 | 3 | **1,2-ジメチルシクロブタン（4 → 3）** |

ユーザーは別の往復で「メソ体は出題したい・C₅H₁₀ で絡みますね」と言っている。
**環式のお題を消すと、その題材が1つも残らない。** `IW18` の ⑥ が機械で見張っている。

### 16-5. ★ C₅H₈ を出せる根拠（立体の数え方の不具合とは別の話）

別レーンが「**アプリの立体の数え方は軸不斉を取り違える**」と報告している。実測で確認した:

| 分子 | `countStereoisomers` | 正しくは |
|---|--:|--:|
| 2,3-ペンタジエン | **3** | 2（軸不斉の R/S） |
| 1,2-ペンタジエン | **2** | 1（末端が H,H なので軸不斉にならない） |
| 3-メチル-1,2-ブタジエン | 1 | 1（正しい） |

⚠ **これは C₅H₈ のお題を止める理由にならない。** 書き出し練習の正解集合は
**`canonicalCode`（構造だけ）から作り、`countStereoisomers` を1度も呼ばない**（§4.2 の線）。実測:

- C₅H₈（鎖式）の **9件は正準コードが9通りとも相異なる**（同じ構造を2回数えていない）
- アレンは**ちょうど3件** ＝ 教科書の「鎖式9種 ＝ アルキン3 ＋ ジエン6」と一致
- **立体込みなら合計13**。お題の総数が **9 のまま**であることが、立体が混ざっていない証拠

⚠ **`IW18` の ⑤ がこの3つを機械で押さえている。**
「立体の数え方を直したから、お題の数も直そう」という**取り違えを止める**のがこの検査の役目
（軸不斉の直しは `chemistry.js` の立体判定の仕事で、お題のデータの話ではない）。

### 16-6. 不飽和度2以上は **群を分けて並べる**（出さない、ではない）

> **環や二重結合が複数ある化合物の書き出しは、入試問題に出される可能性は極めて低いが、
> トレーニングとしてはやる価値がありそう**（ユーザー・2026-08-20）

**出さないのではなく、入試で出るものと同じ顔で並べない。**

- 一覧を**見出しで区切る**。文言は
  **「じっくり練習する回（環や多重結合を合わせて2つもつ式。三重結合・ジエン・アレンまで数えます）」**
- ⚠ **難易度の段としては持たない。** これは難しさの話ではなく、正解に並ぶ骨格の話
- ⚠ **画面の文言で出題頻度を名乗らない。** repo の入試DB（`qa/data/exam_usage.jsonl`）は
  **知識項目 → 大問の逆引き**で、**分子式ごと・書き出し設問ごとの件数を持っていない**
  （近いのは `org.ali.isomer-classification` が 283大問中30件・`org.aro.c8h10-isomers` が7件だが、
  どちらも「不飽和度2以上の書き出し」の件数ではない）。**数えていないことを画面で断言しない**。
  `IW20` が見出しに「入試 / 頻度 / よく出る / まれ」が現れないことを見張っている

### 16-7. 実装（`IP_SCOPES` に名乗りを1つだけ置く）

```
IP_SCOPES = { aromatic: {tag,key,title,note,reject,tip}, chain: {…}, ring: {…} }
scopeInfo() … problem.aromaticOnly → aromatic ／ problem.skeleton → chain|ring ／ 無ければ null
```

**見出し・注記・作業帯・答え合わせの表題・クリア記録の鍵・採点表の断り文が、すべてここから出る。**
1つでも直書きすると「宣言したのに画面のどこかで隠れる」が生まれる（§11-4）。

- **クリア記録の鍵**は `chemIsomerPractice.<式>` ＋ `@ar` / `@chain` / `@ring`。
  同じ分子式でも**範囲が違えば別の出題**なので鍵を分ける（`IW19` が見張る）
- **生の列挙は分子式ごとに1回だけ**（`_rawCache`）。鎖式と環式は同じ列挙を分け合う
  ——C₆H₁₂ は1回で 357ms。2回数えると学習パネルを開くたびに倍払う
- **上限で断るときは行き先を出す**。`C6H12` と打つと
  「構造異性体が25種（20種まで）」で終わらず
  「C₆H₁₂（鎖式・13種）・C₆H₁₂（環式・12種）を用意しています」と続ける
  （`skeletonProblemsFor`）。⚠ 黙って断ると、13種の回があることに永久に気づけない

### 16-7a. ★ 正解の名前は**総称**で引く（環式のお題を足して初めて出た穴・v1433）

**お題が数えているのは構造異性体だけ**（§4.2「シス・トランスや鏡像の区別は数えません」）。
ところが `lookupCompoundName` は、**「立体を名前に反映する」トグルが ON のとき**、
ライブラリの**立体つき登録**と描かれた立体が一致しないと名前を返さない。
正解の分子は列挙が返したままで**座標を持たない**（＝ 立体が読めない）ので、
**環の不斉をもつ種がトグル ON のとき軒並み名無しになる。**

**実測（C₆H₁₂ 環式・12種）**

| トグル | 名前が出た行 |
|---|--:|
| OFF | **12 / 12** |
| **ON** | **9 / 12**（`1,1,2-トリメチルシクロプロパン`・`1,2-ジメチルシクロブタン`・`1-エチル-2-メチルシクロプロパン` が落ちる） |

⚠ **鎖式が無事なのは `iupacName` が拾うから**で、**環には系統名が無い ＝ 受け皿が無い**。
だから既存7問（環の不斉ゼロ）では**一度も表に出なかった**。環式のお題を足して初めて出た。

→ **決定: `IsomerPractice.constitutionalName(mol)` を1つ置き、答え合わせの正解名はそこから引く。**
中では `readStereo` を一時的に false にして総称で引き、`finally` で必ず戻す。

- ⚠ **トグルそのものは触らない**（自由モードの見え方は1つも変えない）
- ⚠ **正準コードにも列挙にも手を入れない**（別レーンの持ち場）
- **筋**: 数えていない軸（立体）のせいで、数えている軸（構造）の名前を落とさない

`IW21` が OFF/ON の両方で12行とも名前が出ることを見張り、**同じ検査の中で
「じかに引くと ON で3件落ちる」ことも測る**（空振り防止）。

### 16-8. 回帰テスト（`IW18`〜`IW21`）

| # | 何を見るか |
|---|---|
| **IW18** | お題の線 —— どのお題も 2〜20種／**鎖式＋環式＝その場で数え直した生の列挙**（自分の値どうしで辻褄を合わせない）／型を分けたお題があるのは環をもつ正解が1件以上ある式だけ／**★陰性対照＝既存7問の正解数が1つも変わらない**／**★C₅H₈ の9種が構造として正しい**（正準コード9通り・アレン3件・立体込み13とは別の数・§16-5）／**★C₅H₁₀（環式）にメソ体が1件残っている**（§16-4） |
| **IW19** | 宣言した出題として**最後まで名乗る** —— ボタン・見出し・注記・作業帯・答え合わせの表題・断り文・クリア記録の鍵 `@chain` の**6か所**。13種を1枚に描いて 13/13。シクロヘキサンを描くと**責めない文言**で範囲を説明する（開発者向けの文言を出さない） |
| **IW20** | **★否定対照** —— `ipMatchesSkeleton` を素通しに差し替えると C₆H₁₂（鎖式）が **13 → 25種**になり上限20を突き抜ける（＝ 13が「たまたま小さい」のではない証明）。＋不飽和度2以上が別の群に居ること・**群の見出しが出題頻度を名乗らないこと**・上限で断るときに行き先を出すこと |
| **IW21** | **★否定対照** —— **立体トグルが ON でも正解の名前が1つも落ちない**（§16-7a）。同じ検査の中で「じかに `lookupCompoundName` を引くと ON で3件落ちる」ことも測る（空振り防止）＋ `constitutionalName` がトグルを戻していること |

**実際に外して赤くなることを確かめた（5通り）**

| 外したもの | 赤くなったもの |
|---|---|
| `ipMatchesSkeleton` の `chain` を素通しに | IW18・IW19・IW20・IP4（C₆H₁₂鎖式 25種／C₄H₈鎖式 5種） |
| クリア記録の鍵から範囲を落とす | IW19・**BZ6**（芳香族側も同時に赤 ＝ 鍵の分け方が1本であることの証拠） |
| 群分けをやめて全部を定番の側へ | IW20（群が画面に無い） |
| 断り文から行き先を落とす | IW20（旧文言がそのまま出る） |
| 範囲外の判定を `aromaticOnly` に戻す | IW19（範囲外として指された図が0個） |
| 鎖式のふるいでアレンも落とす | IW18・IP4（C₃H₄（鎖式）が1種で下限割れ） |
| C₅H₁₀（環式）のお題を削る | IW18（メソ体の見どころが消える） |
| 正解名を `constitutionalName` から `lookupCompoundName` に戻す | IW21（トグル ON で名前の無い行が3件） |

### 16-9. やらなかったこと（数で理由を書く）

| やらなかったこと | 理由（数） |
|---|---|
| **上限20を上げる** | C₅H₈ 全26種・C₆H₁₂ 全25種は開けるようになるが、**答え合わせは20行スクロール**になる（§12-7 の実測: 画面に入るのは小で5行）。**型で分ければどちらも上限の内側**に入るので、上限を動かす理由が消えた |
| **C₅H₈（環式・17種）を出す** | 17種は上限の内側だが、**名前が付くのは1件だけ**（§16-3 の③）。答え合わせの左列が16行「（名称未登録）」になる |
| **アレンを正解から抜く** | 抜くと**お題の名前で範囲が言えなくなる**（§16-2・ユーザー判断） |
| **O を3個以上ふくむ式** | 高校で出る形から離れるうえ、③で全滅する（C₃H₈O₃ 28種／C₂H₆O₃ 10種＝名前1件 ほか） |
| **重原子6個かつ不飽和度2以上** | 既存の門番。列挙に 2.8〜10.0 秒かかり、**お題ボタンは開く前に数を出す**ので学習パネルが固まる |
| **不斉炭素モード**（A-6 の 13・14） | ⚠ **お題 C₅H₁₂O（14種・不斉4種）を足すところまで**がこの回の範囲。段そのものは別レーン |

---

## 17. ハースの糖を「立体の書き出し」のお題にできるか（判断25 の実測・v1433）

> **25「ろ」—— ハースの糖を `stereoPractice` のお題に足すか → はい**（ユーザー・2026-08-20）

⚠ **足していない。** 採点は通るが、**お題として成立しない**（数）。決めるのはユーザーなので止めた。

> ★ **決着（v1435）**: ユーザー判断「**とりあえず αβ だけでやってみては**」を受けて足した。
> §17-3 が求めた「動かしてよい立体単位を選ぶ軸」を `axis: 'anomeric'` として1本入れ、
> **2種**のお題にしてある。**→ §18-6**（この節の実測はそのまま残す ＝ 32 という数の出どころ）。

### 17-1. 通ったところ（**新しい読み取りは1行も要らなかった**）

`α-D-グルコース（α-D-グルコピラノース）` を `stereoPractice.problems` に仕込んで実測（1280×800・v1433）:

| 見たもの | 実測 |
|---|---|
| ハース図から読めた立体中心 | **5個**（`readRingParityFromHaworth`。`spDetectUnits` は5つとも `kind:'ring'` と判別） |
| `prepare()` | **通る**（704ms・`byCode.size === info.count === 32`） |
| 32枚をキャンバスに置いて `grade()` | **32/32 が `ok`・未発見0**（＝ **採点は届く**） |
| 原子数 | 384（`CANVAS_LIMIT` 5000 の内側） |

⚠ **フルクトフラノース（α/β）は `prepare()` が失敗して無効化される**
（`[StereoPractice] 題材を無効化`。環外の枝の形が `spFlipRingSub` の条件に合わない）。

### 17-2. 足せない理由 —— **数**

| | 実測 |
|---|--:|
| お題の総数 | **32種**（不斉炭素5個 ＝ 2⁵。⚠ `stereoPractice` に上限は無いので、そのまま出てしまう） |
| 32枚を `tidyAnswerSlots()` で並べ直した外接矩形 | **1998 × 1310px** |
| キャンバス | 800 × 600px |
| 参考: 構造異性体の上限20成分の実測（§12-5a） | 952 × 735px |

**上限20（構造異性体側）の 1.6倍の枚数で、外接矩形は 2.5倍 × 2.2倍。**
さらに答えの左列には **α/β-D-アロース**や**名称未登録**が並ぶ（グルコース・マンノース・アロースの6件は名前が出るが、残り26件は出ない）。

### 17-3. 何が足りないか（★ ユーザーが決めること）

**「α と β の2種」を問うには、お題の側で「動かしてよい立体単位」を選べる仕組みが要る。**

- いまの `prepare()` は `spDetectUnits` が返した単位を**全部** 2ⁿ 回す。
  **アノマー位（C1）だけに絞れば 2種**になり、ちょうど良いお題になる
- ⚠ これは**読み取りの追加でも正準コードの変更でもない**（どちらも既にある）。
  **お題の定義に「単位の部分集合」という軸を1本足す**話 ＝ 設計の決め
- ⚠ 決めるべきは「**α/β だけを問うのか（2種）／32種すべてを問うのか**」。
  ここを推測で決めると、あとから引き直すことになる

### 17-4. ★ 統合レーンの読みを1つ否定する —— 「黙ってスキップ」は**練習の中では起きていない**

> ⚠ 斜めに描くと黙ってスキップされ、理由が画面に出ないという報告もあります（発注書経由の申し送り）

**立体の書き出し練習の中では、理由は画面に出ている。** 実測（環外の -OH を1本だけ 45° に傾けた図を1枚置いて採点）:

- 読めた中心が **5個 → 4個**に減り、その成分は `unread`（第3の状態・§5-4）になる
- 採点表の文言 ——
  **「まだ立体が決まっていません（立体の読めない不斉炭素原子が1個あります（フィッシャー投影の十字＝縦横に、環の置換基は縦に描く））」**
  ＋ 答え合わせに **「整形して決めましょう →」** が出る
- 種類には数えず、**「まだ立体が決まっていない図: 1つ（種類には数えていません）」**と別枠で数える

⚠ **「黙る」のは自由モードの側**（分子モーダルの名前）。そちらでも完全に黙ってはおらず、
`lookupCompoundName` が **「アロース（アロピラノース）／ガラクトース（ガラクトピラノース）ほか3種 のどれか（立体で決まります）」**と返す
（＝ 発注書 B-2 の「い」は、**練習の中では既に片付いている**。残っているとすれば自由モードの見せ方だけ）。


## 18. 「立体が分かれる場所」を指して、立体込みの総数を答える（v1435）

**正はここ**（起草は `DESIGN_stereo_point.md`。あちらには**実測（§1）と決め方**が残してあり、
この節は**実装したもの**を書く）。発注書 `ORDER_isomer_2026-08-20` の **A-6**（不斉炭素まで答えさせる）と
**B-1**（立体異性体は何種類？）を1本にまとめ直したもの。

### 18-1. 何を足したか —— **面もモードも増やさない**

§13-1「3つ目のモードを作らない」を守り、**書き出し練習の中に段を2つ**足した。

```
お題を選ぶ  →  描く  →  ☆ 立体の場所（段1）  →  総数を書く（段2）  →  🔍 答え合わせ
                 ↑ ここまでは今までと同じ                                ↑ 1問1回のまま
```

- 作業帯に増えたボタンは **`☆ 立体の場所` の1つだけ**（異性体側の帯は 4 → 5）
- **段2 の入力欄は、段1 を一度でも開いたあとにだけ出る**（§18-4 の関門）
- お題は **`stereoAsked: true` の回だけ**（`problems[19]` = C₅H₁₀・`problems[20]` = C₅H₁₂O）。
  ⚠ **骨格の型は付けない** —— 鎖式に絞ると C₅H₁₀ のメソ体（1,2-ジメチルシクロプロパン）が消える
- ⚠ **列挙は1件も増えていない**（同じ分子式の既存お題と `_rawCache` を分け合う）

### 18-2. 段1 —— 原子と結合を**1つのモード**で指す

| タップ先 | 何が起きる |
|---|---|
| 炭素原子 | `atom.isAsymmetricMarked` をトグル（自由モードの不斉マークと**同じ1ビット・同じ見た目**） |
| 結合 | `bond.isStereoMarked` をトグル（新規の1ビット。中点にオレンジのリング） |
| 炭素以外の原子・何も無い所 | **黙って何もしない**（「そこは違う」と言うと、消去法で答えを教えることになる） |

**原子か結合かは「近いほう」で決める**（`game.stereoPointUnderPoint`）。
⚠ **半径では分けられない** —— 原子の当たり半径は 18px で結合1本は 42px しかなく、
両端が 18px ずつ取ると**結合に残るのは真ん中の 6px** になる。
中点までの距離と比べると「両端 10.5px が原子・真ん中 21px が結合」に落ちる。

⚠ **`tapHasOtherMeaning()` に必ず載せること**（`IW24` が見張る）。
判定線（`svg-bond-hitbox`）は `stroke-width:20` で冒頭に `stopPropagation()` があるので、
載せ忘れると**結合の中央十数 px がキャンバス側のモード分岐に一度も届かず**、
離したときの `click` が次数トグルへ落ちて **C=C が C≡C に化ける**
（`BUGNOTE_touch_ipad.md` S6 と同じ型。整形モードで実測された）。
⚠ **タッチだけの検査では捕まらない**（合成 click が無いため）。**マウスでも撃つこと**。

**印は締めない**（§18-4）: `☆` を OFF にしても印は残る。締めるのは `🔍 答え合わせ` だけで、それは1問1回。

### 18-3. ★ 部品の境界 —— **判定は1本**

```
chemistry.js  stereoUnitsOf(mol)      ★ 正解の唯一の出どころ（不斉炭素と C=C を別々に集めない）
              stereoFoldReason(mol)   'meso' | 'symmetry' | null（新設・純関数）
              stereoBondKey(a, b)     立体の単位を指すキー（⚠ 原子IDに `_` が入るので分解できない）
learn.js      stereoMarksOf(mol)      印の在りかは**図そのもの**（別の台帳を持たない）
              gradeStereoPoints(mol, marks) → { ok, missing…, extra… }
                                      ★ **これ1本だけ。**A-6（見せるだけ）も段1 もここを通る
game.js       印を集めて渡し、返ってきた結果を描くだけ
```

⚠ **「見せるだけだから `stereoUnitsOf` を直接呼べばいい」としないこと。**
そこが2つ目の判定になり、片方だけ直る事故の種になる（このリポジトリで繰り返している罠）。
`IW27` が「判定を1本すげ替えたら画面の段1 が全行そちらに従うか」で見張っている。

### 18-4. 満点と関門 —— ★ **満点は採点のときに示す**

> **満点は採点時に示せばよいのでは？**（ユーザー・2026-08-20）

この一言で「構造の数も隠す」が成立した（`DESIGN_practice_revision.md` §2 U1 案3 の却下理由②が解ける）。

```
満点   = N ＋ 1          （N = 構造異性体の数。＋1 は段2 の1問ぶん）
素点   = 正しく描けた種類数 ＋（総数が当たれば 1）
スコア = 素点 − ヒント到達段数（下限 0）
```

- **お題は分子式だけを名乗る**（`C₅H₁₀（立体まで）`）。⚠ **隠すのは4か所そろえる** ——
  お題ボタン・学習パネルの見出し・作業帯・確認モードの要約。**1か所でも漏れると隠したことにならない**（`IH1`）
- **満点は採点の瞬間に式ごと明かす**（`構造異性体 10種 ＋ 総数の1問 ＝ 11点`）
- ⚠⚠ **追記（v1489）: この「隠す」は全お題へ広がった。** 下の §19 が正。
  この節が書いた「既存モードの `（N種）` は1文字も変えていない」は**もう当たらない**
- **段1 は点にしない。**代わりに**段2 の関門**にする（`☆` を開かないと総数の欄が出ない）。
  ⚠ 採点表には ○△ を必ず返す（点にしないだけで、正誤は黙らない）

**なぜ段1 を点にしないか**（3通り試してどれも壊れた）:

| 案 | 満点 | 壊れ方 |
|---|---|---|
| 場所1つに1点 | 場所の総数（3） | ⚠ **満点が段1 の答えそのもの**（「3か所ある」と最初に教える） |
| 構造1つに1点 | N（10） | ⚠ **何も指さなければ 8/10 取れる**（場所を持つ構造は2件だけ） |
| 段1 全体で1点 | 1 | 1か所落として 0点。**部分点の無い段は「やらないほうが安い」と読まれる** |

### ★ 18-4a. U1 の却下理由 ①③ にどう答えたか

②は上のとおり解けた。⚠ **残る ① と ③ への答えを、押し切らずにここに書く。**

**① 選ぶ前に問題の重さが分からない**
→ **重さは隣のボタンが名乗っている。**「立体まで答える回」は選択画面の**別の枠**に置き、
その上の群に `C₅H₁₀（10種）` が並んでいる。この回は「**同じ書き出し ＋ 総数の1問**」であって、
未知の重さではない。枠の見出しにも「構造異性体を書き出し、…総数まで答えます」と書いてある。

⚠ **裏返すと、構造の数は隣から読める ＝ 隠しきってはいない。**
これは害ではなく**①への答えそのもの**（重さが読めることが要件だった）だが、
「10 を完全に伏せたい」なら**素の C₅H₁₀ の回を畳む**必要があり、それは別の決定になる。
⚠ **こちらでは決めない**（既存お題の表記を変えないという線に触れるため）。

**③ クリア済みの ✓ と噛み合わない**
→ **✓ の基準を段2 まで含めた**うえで、**満点は ✓ が付く瞬間に必ず画面に出る**ようにした。

- ✓ ＝「構造を全部描き、**かつ**総数にも当てた」（`grade()` の `cleared`）。鍵は `chemIsomerPractice.<式>@stereo`
- 答え合わせは**1問1回**で、そこに `スコア X点 / N＋1点満点` と式が出る
  ＝ **✓ の条件を知らないまま ✓ が付くことはない**（付いた瞬間に条件が読める）
- ✓ が付く前でも、押せば必ず満点が見える。**「何個そろえたら ✓ か」が永久に分からない状態は無い**

⚠ **残る差**: 1回目を解き終えるまでは「あと何個か」が分からない（素の回なら分かる）。
これは**この回の狙い**（総数を自分で数える）そのものなので、害として飲む。
⚠ 数がどうしても要る人には**ヒント段1（「あと N種 あります」）が −1点で買える**道が残っている。

### 18-5. 段2 —— 総数と、メソ体の文面

- 作業帯に `立体異性体も含めた総数: [ ] 種` を出す。**4択にしない**
  （`StereoCountQuiz` と同じになり、2ⁿ を選ぶだけの作業に戻る）
- 正解は `countStereoisomers` の合計（**座標を1つも見ずに数える**ので図の描き方に左右されない）。
  C₅H₁₀ = **13**（素朴な 2ⁿ の合計 14 との差 1 がメソ体）／C₅H₁₂O = **18**（畳み込み無し）
- 答え合わせでは**表より先**に総数の箱を出す（10行の表を下までスクロールさせない）
- ⚠ **「場所の指摘は合っていました」を先に言う**（段1 を正しくやった人を段2 の誤りで丸ごと否定しない）
- **畳み込みが起きた行だけ**解説を出す（多数派の「掛け算どおり」を並べると見どころが埋まる）。
  文面は `stereoFoldLines()` が組み立て、**`STEREO_FOLD_NOTES` は `stereoPractice` の `foldNote` と共有**
  （ORDER A-6「同じ数の説明が2か所に出るので文言をそろえる」への答え）

### 18-6. ハースの糖を α/β の2種にした（§17-3 への答え）

> **とりあえず αβ だけでやってみては**（ユーザー・2026-08-20）

§17-3 が「お題の側で**動かしてよい立体単位**を選べる仕組みが要る」と書いた、その1本を足した。

- `stereoPractice.problems` に **`axis: 'anomeric'`** を持つ回を1つ（`α/β-D-グルコピラノース`）
- `spAxisFilter()` が「環の酸素と環外の -OH の**両方**が付いた環炭素」だけを残す。
  ⚠ **読み取りでも正準コードでもない**（`readRingParityFromHaworth` も `countStereoisomers` も**無改造**）
- 実測: `countStereoisomers` は **32 のまま**・用意する変種は **2**・採点 2/2・
  名前は `α-D-グルコース` / `β-D-グルコース`
- **数の保証**が軸ありと軸なしで変わる（`prepare()`）:
  軸なしは従来どおり `byCode.size === countStereoisomers().count`。
  軸ありは **`spApplyFlip` の不変条件**が持つ ——`spFlipRingSub` / `spFlipGeoEnd` は
  「選んだ単位だけが反転し、ほかの中心・C=C は1つも動いていない」ことを毎回確かめてから図を返す
- **軸の外の立体は `'axis'`**（責めない文言で断る）。⚠ `'unknown'`（開発ログ行き）に戻すと、
  正しく読める図を描いた人に不具合の顔を見せることになる（§11-4・BZ5 と同型）

⚠ **フルクトフラノースは入れていない**（§17-1 のとおり `prepare()` が通らない）。

### ★ 18-8. 鎖式のシス・トランスは**別の答案**として書き出す（v1440）

> **C5H10 立体の書き出し／鎖式は シス・トランスを別に書き出すのが自然／
> 不斉炭素のマークと、立体異性体の総数を答えさせる**（ユーザー実機報告 2026-08-21）

**直す前の実測（v1439）**: C₅H₁₀（立体まで）でシス-2-ペンテンとトランス-2-ペンテンを
両方描くと、採点表は2枚目を **「同じものをもう一度」**（`dup`）と言い、種類は **1** としか数えなかった。

⚠ **見分けられないのではない。** 同じ図を `readStereoOf` は

```
シス   … C1[c0]();…|g0-1c
トランス … C1[c0]();…|g0-1t
```

と**読み分けている**。**採点だけが `canonicalCode`（構造だけ）で突き合わせていた**ので、
読めている違いをその場で捨てていた。⚠ §4.2 の「シス・トランスは1種と数える」は
**素の回の規則**で、v1435 で足した「立体まで答える回」には持ち込むべきではなかった。

#### どこまで分けるか —— ★ **鎖式の C=C だけ**

| 立体の単位 | 分ける？ | なぜ |
|---|---|---|
| **鎖式の C=C**（`stereoUnitsOf().bonds`） | **分ける** | **いまのキャンバスで描き分けられる**。二重結合を横にして両側の基を軸の上下に置けば、`readBondGeoFromCoords` が座標から向きを読む |
| 不斉炭素（`…centers`） | 分けない | **平面の直交作図では描けない**。くさび／フィッシャー投影が要る ＝ `stereoPractice` の担当 |
| 環の置換基（1,2-ジメチルシクロプロパン） | 分けない | 同上（ハース図が要る）。⚠ ユーザーも「**鎖式は**」と言っている |

⚠ **線は化学ではなくキャンバスが引いている。**そろえようとすると
「描けないものを描け」という出題になる。**不斉炭素・環のぶんは今までどおり
段1（☆ 場所を指す）と段2（総数）で答える** ＝ ここが「不斉炭素のマークと、
立体異性体の総数を答えさせる」への答えでもある（新しく足すものは無く、**残す**）。

**結果（C₅H₁₀）**: **書き出すのは 11種**（構造 10 ＋ 2-ペンテンのシス／トランス）・
**段2 の総数は 13 のまま**。⚠ **差の 2 は 1,2-ジメチルシクロプロパンの立体**で、
これは図では描き分けず段1・段2 で答える。⚠ **この差は隠さない** ——
満点の説明を `満点は 書き出す 11種（構造異性体 10種のうち 1種はシス・トランスを別に数えます）＋ 総数の1問 ＝ 12点`
に変えた（`answerCountLabel()`）。⚠ 旧文言の `構造異性体 11種` は**嘘**になる。

#### ★★ 分けてよいのは「正解図を描き分けられて、読み返せた」ものだけ

`ipGeoVariants(mol)` は次を**全部**満たしたときだけ2つに割る。1つでも欠けたら**据え置き**（1件）:

1. 鎖式 C=C が**ちょうど1本**（`IP_GEO_SPLIT_BONDS`。2本以上の置き方は決めていない）
2. その C=C が**主鎖に乗っている**（`ipLongestHeavyPath`）
3. 枝が空いている格子点に**置ける**
4. ★ **置いた図を読み返して**（`readBondGeoFromCoords`）向きが決まり、**2つのコードに割れた**

⚠ **4 が肝。**ここを省くと「答えは2つあるのに、正解の列に出せる図は1つ（＝向きの読めない直線）」
という表ができる。実際、`ipNumberedLayout` は 2-ペンテンを**主鎖一直線**に描くので、
`renderStandardFigure` は**座標を焼き付けた答案（`_ipFixedLayout`）を最優先で描く**ようにした
（この順を入れ替えるとシスもトランスも同じ絵になる）。

#### 第3の状態 —— **未確定は間違いにしない**

主鎖を一直線に描いた 2-ペンテンは向きが読めない。これを不正解にすると
「正しく描いたのに ✗」になるので、`stereoPractice` の `unread`（§5-4）と**同じ扱い**にした:

```
まだシス・トランスが決まっていません（C=C の向きが読めません: 1本。
二重結合を横にして、両側の基を軸の上下に描いてください）
```

**正解でも不正解でもない**（種類に数えない）。⚠ CLAUDE.md の
「シス/トランスが未確定の図は整形しない」と同じ考え方で、**未確定を確定させるのは描いた人の仕事**。

**見張りは `IW32`**（否定対照 ＝ `ipGeoVariants` を「分けない」に差し替えると `dup` が戻る／
陰性対照 ＝ 素の C₅H₁₀ の回は 10種のまま・C₅H₁₂O は 14種のまま）。
⚠ `IW23`・`IW25`・`IW27` の期待値も 10 → 11 へ直した（答案が1枚増えたぶん）。

### 18-7. やらなかったこと（次の人へ）

- **段2 のヒント（掛け算の表）**。`DESIGN_stereo_point.md` §5-2 にある「①〜⑩ を並べて
  『合計 14 ？』と出し、どの行かは言わない」段。⚠ ヒントを 4 → 5 段にする話なので、
  スコアの式（§15-5b）と `IP_HINT_MAX` に触れる。**段1・段2 とは独立に足せる**
- **A-6 案①（答え合わせの正解図に `☆` で印を重ねて見せる）**。
  ⚠ 足すときも **`gradeStereoPoints` を通すこと**（§18-3）
- **アレンの数え方**（`DESIGN_stereo_point.md` §1-6）。既存の穴なので別途。
  ⚠ **C₅H₈ を `stereoAsked` のお題にしない**ことで避けている（`IW22` が構造側の数を見張っている）
- **お題を増やす**。§1-2a の実測では C₆H₁₂ も成立するが **25種 > 上限20** で開かない

## 19. 種類数を伏せ、ヒントを組み直し、分類つきのお題を足した（v1489・2026-08-31）

**ユーザーの言葉（同じ回のメッセージ3本）:**

> ★ **全部で何種類か、はオープンにしない出題の方がよいのでは？**
> ★ **現状ヒントがあまりヒントになっていないので、中身を見直したいです**
>   （・全部で何通りあるか ・重複の有無，残り何種類か ・抜けている構造の名称）
> ★ **重元素7以上でも分類ごとの書き出しは対応を増やしたい／例えば C₆H₁₂O のケトン、
>   C₇H₁₄O₂ のエステル**／**C₅H₁₀O₂ で不斉炭素原子をもつエステル、など
>   書き出し練習方向からの絞り込みが合ってもよいかもしれません**

### 19-1. 種類数を伏せる（`IH1`）

**理由は「難しくするため」ではない** —— **「もう出し切ったか」を自分で決めるのが、
そもそも入試で問われる力**だから。答えの個数を先に配ると、その判断が丸ごと消える。

⚠ **隠す場所は4か所そろえる**（1か所でも漏れたら隠したことにならない・§18-4a の線を全件へ）:

| 場所 | 前 | 後 |
|---|---|---|
| お題ボタン | `C₆H₁₄（5種）` / `C₆H₁₂（鎖式・13種）` / `C₈H₁₀（芳香族・4種）` | `C₆H₁₄` / `C₆H₁₂（鎖式）` / `C₈H₁₀（芳香族）` |
| 学習パネルの見出し | `✏️ C₅H₁₀ の異性体（全 10 種）` | `✏️ C₅H₁₀ の異性体` |
| 作業帯 | `お題 C₇H₁₆ の異性体 全 9 種 ／ いま 0個 描いてあります` | `お題 C₇H₁₆ の異性体 ／ いま 0個 描いてあります` |
| 確認モードの要約 | `あなたが描いた図 3個（全 10 種）。` | `あなたが描いた図 3個。` |

★ **数を出す場所は3つに絞った**: **答え合わせ**（満点の内訳）・**ヒントの段1**・
**上限20種で断るトースト**（⚠ 断った式そのものの `25種` は残す —— それが断る理由で、
伏せると理由が消える。⚠ 一方、行き先として案内する**お題の名前**はボタンと同じ表記にそろえた）。

⚠⚠ **検査は「数が消えた」だけを見てはいけない。** それだけなら**数を全部消した実装**が通る。
`IH1` は消えたこと（4か所）と、**移った先で確かに出ること**（ヒント段1 の `全部で 10 種`・
答え合わせの `全 10種中`）を両方見る。

⚠ **触っていないもの**: アルキル基の書き出し（`✏️ アルキル基 C₄H₉–（全 4 種）`）と
立体異性体の書き出し（`🪞 2-ブテン の立体異性体（全 2 種）`）。同じ理屈は当たるが、
今回のユーザー要望は異性体の書き出しの話だったので広げていない（**次に触るならここ**）。

### 19-2. ヒントを 4段 → 5段に組み直した（`IW5`）

| 段 | 旧 | **新** |
|---|---|---|
| 1 | 残り数 ＋ ダブりの組数 | ★ **全部で何種類か**（伏せた数の初出） |
| 2 | 系列の内訳 | ★ **ダブりが何組 ＋ 残り何種類** |
| 3 | 書き出しの手順 | 系列の内訳 ＋ 書き出しの手順（旧2と旧3は同じ問いへの答え） |
| 4 | 重複の組の名指し | 重複の組の名指し（`IP_HINT_DUP`） |
| 5 | — | ★★ **抜けている構造の名称**（`IP_HINT_NAMES`・いちばん強い） |

- 段1 が「残り」ではなく「全部」なのは、**残り数は総数を知って初めて意味を持つ**から
  （引き算の答えを先に配ると段2 が空になる）
- `IP_HINT_DUP` を `IP_HINT_MAX` と別の定数にしたのは、**ダブりを知りたいだけの人に
  答え（名称）まで配らない**ため。束ねると段4 が最終段になって必ず名前が付いてくる
- ⚠ 段5 で**名前が付かない構造**が出たら、**行を落とさない**。落とすと「あと3種」と
  言ったのに2つしか並ばず、**数え間違いに見える**。名前の無いぶんは
  「名前の付かない構造が N つ残っています」と**数で**言う
  （お題22件は `IP4`・分類つき12件は `IH2` が全件命名を保証しているので、
  実際に出るのは任意の分子式・芳香族の回だけ）

### 19-3. 分子式 ＋ 分類で絞るお題12件（`IH2`・案A ＝ お題ボタンを増やす）

⚠⚠ **設計の分かれ目は「全部列挙してから分類で捨てる」が破綻すること**（`IH3` が実測で見張る）。
C₇H₁₄O₂ は重原子9個で、総当たり `enumerateConstitutionalIsomers` の上限8個を超えて
**1件も出せない**。だから **`enumerateBenzeneRingIsomers` とまったく同じ形**にした ——
ベンゼン環の代わりに **-CO- / -CHO / -COO- / -COOH の種**を置き、残りの炭素を
**腕（付け根 `R` つきの骨格）**として既存の列挙器に数えさせて貼る。
★ **新しい列挙アルゴリズムは1つも書いていない**（`enumerateFunctionalGroupIsomers`）。

| 分類 | 種 | 腕 | 腕が H でよいか |
|---|---|---|---|
| ケトン | R–**CO**–R′ | 2本 | 不可（H ＝ アルデヒドになる） |
| アルデヒド | R–**CHO** | 1本 | 不可（H ＝ ホルムアルデヒド） |
| エステル | R–**COO**–R′ | 2本 | ★ アシル側だけ可（**ギ酸エステル**。C₃H₆O₂ の2種の片方がこれ） |
| カルボン酸 | R–**COOH** | 1本 | 不可（H ＝ ギ酸） |

**性能の要は「腕の不飽和度」の枝刈り**（`FG_ARM_DOU_MAX`）。腕の水素を多い順に全部試すと
水素0本の腕（＝C₅ を不飽和度5で組む）まで数える。**必要な不飽和度は分子式から先に分かる**
（分子全体 − 種の C=O ぶん1。CₙH₂ₙO・CₙH₂ₙO₂ なら **0** ＝ 腕は飽和アルキル基だけ）。

| 実測（PC） | 枝刈り前 | **枝刈り後** |
|---|--:|--:|
| ケトン C₆H₁₂O（6種） | 67ms | **7ms** |
| アルデヒド C₆H₁₂O（8種） | 1615ms | **22ms** |
| エステル C₆H₁₂O₂（20種） | 1343ms | **20ms** |
| エステル C₇H₁₄O₂（45種） | **1件も出せない**（4,000,000ノード超） | **294ms** |
| エステル C₈H₁₆O₂（105種） | — | 5248ms ＝ **`FG_MAX_CARBON = 7` で断る** |

⚠ 断り方は**すべて `overflow`**（＝数え切れなかった）。**黙って数え落とさない**
（静かに落とすと「20種のはずが18種のお題」が生まれる）。

**並んだ12件**（条件は §16 と同じ ①2〜20種 ②正解の全部に名前が付く）:

| 分類 | 成立した式（種類数） |
|---|---|
| ケトン | C₅H₁₀O(3)・C₆H₁₂O(6) |
| アルデヒド | C₄H₈O(2)・C₅H₁₀O(4)・C₆H₁₂O(8) |
| エステル | C₃H₆O₂(2)・C₄H₈O₂(4)・C₅H₁₀O₂(9)・C₆H₁₂O₂(20) |
| カルボン酸 | C₄H₈O₂(2)・C₅H₁₀O₂(4)・C₆H₁₂O₂(8) |

⚠ **C₇ の4件が落ちたのは条件①ではなく②**（数ではなく名前）:
ケトン 15種（名無し12）／アルデヒド 17種（名無し16）／カルボン酸 17種（名無し16）／
エステル 45種（名無し39）。★ **15種・17種は上限20の内側**なので、`compounds.json` に
名前を足せばそのまま開く。

★ **お題ボタンは押されるまで数えない**（`IH2` の①が `enumerateFunctionalGroupIsomers` の
呼び出し回数0を見張る）。⚠ これは §16 の申し送り「これ以上お題を足すなら初回
`renderList()` が先に効く」への答えで、**種類数を伏せた（§19-1）ので、一覧を出すのに
数える理由がもう無くなった**のが効いている。12件ぶん（合計 ~60ms）は起動から消えた。

### 19-4. やらなかったこと（次の人へ）

- ★★ **21種以上を「何種類か答える」出題にする**（ユーザー要望
  「書き出し練習として45種類は現実的ではない／構造異性体が何種類か答える問題が適当」）。
  ⚠ **発注書が受け皿と見込んでいた `quiz.js` の `StereoCountQuiz` は、実測では使えない**:
  あれは**1つの分子の図を描いて**その立体異性体を数えさせるもので、
  `countStereoisomers` と図（`renderMoleculeIntoSvg`）に張り付いている。
  「分子式＋分類の構造異性体は何種類？」には**描く図が無い**ので、問い・選択肢・解説の
  どれも作り直しになる。★ **むしろ近いのは書き出し練習の中の「立体込みの総数」欄**
  （§18-5・作業帯の `fields`）で、そこに `countOnly` のお題を1種類足す形なら
  既存の採点・記録・帯にそのまま乗る。
  ★ **出せる母数**（`FG_MAX_CARBON = 7` の内側）: **候補は4件** ——
  ケトン C₇H₁₄O(15)・アルデヒド C₇H₁₄O(17)・エステル C₇H₁₄O₂(45)・カルボン酸 C₇H₁₄O₂(17)。
  ⚠ ヒントは**丸ごと外す**（段1 が答えそのものになる）。

  ⚠⚠ **追記（再開レーンの実測 2026-09-01）—— 母数は「どこに線を引くか」で 4件にも 1件にもなる。
  ここを決めずに作ると、作ったあとで置き場所ごと無駄になる。**

  | 線の引き方 | 母数 | 中身 |
  |---|--:|---|
  | **ユーザーの言葉どおり**（「45種類は現実的ではない」＝ 上限20種を超える回） | **1件** | エステル C₇H₁₄O₂(45) だけ |
  | 上の「名前が足りない回」 | 4件 | ＋ ケトン15・アルデヒド17・カルボン酸17 |
  | `compounds.json` に名前を足したら | **1件** | 15/17/17 は**ふつうの書き出しの回になる**（②を満たす） |

  ⚠ **「15種・17種は名前が足りないので書き出しの回にはできない」は言い過ぎだった**（上の行を訂正）。
  実測: `beginSession` に C₇H₁₄O のケトン15種を直接渡すと**そのまま開く**。
  ヒント段5 も壊れず「2-ヘプタノン・3-ヘプタノン・4-ヘプタノン ＋ **名前の付かない構造が 12つ**」と
  §19-2 の作りで degrade する（`C₄H₆` の回＝9種中5つ名無し が既にその形で動いている）。
  ★ つまり②は**ハードな制約ではなく品質の線**で、15種中3種しか名前が出ない画面を
  出したくない、という判断。**外して12件を16件にすることもできる**（決めるのはユーザー）。

  ★★ **だから次の人がまず訊くべきは「④を作るか」ではなく「C₇ に名前を足すか」**。
  足すなら ④ の母数は 1件（エステル45）に縮み、`countOnly` という新しい出題の型を
  1問のために作ることになる ＝ **割に合うかが変わる**。

  ⚠ **ほかに未決のまま残っている設計** —— どれもユーザーに訊かないと決まらない:
  1. **置き場所**（この節は書き出し練習の `fields`、発注書は `quiz.js` を見込んでいた）
  2. **答えさせ方**（`StereoCountQuiz` は4択・書き出し練習の総数欄は自由入力）
  3. ★ **何を数えさせるか** —— ユーザーの原文は
     「**構造異性体（立体異性体）が何種類か**」で、**構造だけなのか立体込みなのかが読み取れない**
     （エステル C₇H₁₄O₂ は構造45種。立体込みはさらに増える）
  ⚠⚠ **この節は §19-5 で決着した。上の3つの表は経緯として残す**（結論は §19-5）。

- ★ **不斉炭素をもつエステル、のような「分類 ＋ 条件」の絞り込み**（ユーザーの3本目）。
  ★ 材料はそろっている —— `narrowing.js` の条件カードは述語が `(mol) => boolean` なので、
  上の列挙結果に `filter` を1回かけるだけ。⚠ 決まっていないのは**入口の作り**で、
  ユーザー自身が「インターフェースが複雑になりそう」と難点に挙げている。
- **アルキル基・立体の書き出しにも §19-1 を広げる**（いまは `（N種）` のまま）。

### 19-5. ★★ お題の上限を「人が答えられるか」で引き直した（腕がペンチルに収まる・2026-09-01）

**ユーザーの言葉（2026-09-01）:**

> ★ **C7 足す**
> ★ **C7H14O2のエステルは人間には現実的ではなさそうです。**
> ★★ **ペンチル基に収まるところまででよいと思います**

⚠ **上限は「アプリが計算できるか」ではなく「人が紙で数え切れるか」で決まる**
（DEVELOPMENT.md「数え上げの上限は『人が答えられるか』で決める」）。

### 19-5a. C₇ に名前を44件足した（`IC2`）

**実測（足す前）**: ケトン C₇H₁₄O 15種＝名前あり3／アルデヒド C₇H₁₄O 17種＝名前あり1／
カルボン酸 C₇H₁₄O₂ 17種＝名前あり1。**足りないのは 12 + 16 + 16 = 44 件**。

⚠⚠ **申し送りの「`iupacName` と突き合わせる」は、そのままではできなかった。**
`iupacNameDetail` は**酸素に単結合以外があれば `null` を返す**（chemistry.js の
「酸素の分類: -OH かエーテル。それ以外（C=O・ペルオキシド等）は未対応」）ので、
**C₇ のケトン・アルデヒド・カルボン酸に1件も名前を出せない**
（C₇H₁₆ の9種で使えたのは、あれがアルカンだから）。

→ ★ **代わりに2つの独立な道で作って突き合わせた**（**49件すべて一致・食い違い0・同点0**）:

| | 道 |
|---|---|
| **(A)** | **C=O を C-OH に読み替えたアルコール**を `iupacName` に名付けさせ、語尾だけ移す（`ノール`→`ノン`／`ナール`／`ン酸`）。**主鎖の選び方（主特性基を含む最長鎖）と番号（主特性基が最小）は -OH と C=O で同じ規則**なので、位置番号と置換基はそのまま持ち越せる |
| **(B)** | 主鎖候補・番号・枝の名前を**自前で**組み立てる |

図は `layoutMolecule` に置かせたが、⚠ **原子の並び順で図が変わる**ので
`verify-compounds.js` と同じ3つの物差しで採点して並びを選んだ
（素の並びでは 2,3,3-トリメチルブタン酸の自動水素が**結合線の真上 0.0px**に来て不合格）。

### 19-5b. ★★「ペンチル基に収まる」の読みは2つある（実測で分かれる）

| 読み | 中身 | 落ちるもの | お題の数 |
|---|---|---|--:|
| **A** | **名前に置換基として現れる**アルキル基 ≤ 5 | **エステル C₇ だけ**（ギ酸ヘキシルの「ヘキシル」） | **15件** |
| ★ **B** | **種に付く腕 R・R′** ≤ 5 | アルデヒド C₇・カルボン酸 C₇・エステル C₇（腕6＝ヘキシル） | **13件** |

**腕の最大炭素数（`functionalArmCarbons` の実測）**

| 分類 | C₃ | C₄ | C₅ | C₆ | C₇ |
|---|--:|--:|--:|--:|--:|
| ケトン | — | — | 3 | 4 | **5** |
| アルデヒド | — | 3 | 4 | **5** | 6 |
| エステル | 2 | 3 | 4 | **5** | 6 |
| カルボン酸 | — | 3 | 4 | **5** | 6 |

★ **採ったのは B**。理由は **4分類とも同じ1本の線になる**こと ——
どの分類も「腕がヘキシルになる式」の**1つ手前**で止まり、
**既存の12件はちょうど全部この線の内側**、新たに通るのは **C₇H₁₄O のケトン15種だけ**。
読み A は最大値が 1〜2 のところと 6 のところしか無く、**エステルにしか当たらない**
＝「ペンチル基に収まる」という言葉が線として機能しない。

⚠ **ただしこれは編集判断**。読み A を採るなら `fgPresets` に
`{ formula:'C7H14O', cls:'aldehyde' }` と `{ formula:'C7H14O2', cls:'acid' }` を足すだけで開く
（**名前は 19-5a で `compounds.json` に入れてある**）。**ヘプタナールは C₆H₁₃–CHO ＝ 腕がヘキシル**
なので B では落ちる、という1点だけが分かれ目。

#### ★★ 19-5b-2. その2件を足したので、**ペンチルの線は門番から外した**（同日・v1493）

**ユーザー判断（DEVELOPMENT.md「判断（2026-09-01）」D-C1）:**

> ★ **アルデヒド C₇（17種）と カルボン酸 C₇（17種）をお題に足す。13 → 15件。**

⚠⚠ **＝ 上の「読み B で落ちる3件」のうち2件を、ユーザー自身が採ることにした。**
★ **残る1件（エステル C₇）は 45種で、上限20 だけで既に落ちる。**
→ **ペンチルの線は、1件も *余分に* 落とさなくなった。**

**★ 実測（`IC1` が毎回やり直す。⚠ 統合セッションの指摘は正しく、しかも指摘より強かった）:**

`FG_MAX_CARBON = 7` の内側の**全域**（4分類 × C₂〜C₇ × 不飽和度1〜4・2種以上・overflow を除く）で
**「①上限20 を通るのに 腕>5」を数えると 2件**:

| 式 | 種類数 | 腕 | |
|---|--:|--:|---|
| **アルデヒド C₇H₁₄O** | 17 | 6 | ★ **今回お題に足した** |
| **カルボン酸 C₇H₁₄O₂** | 17 | 6 | ★ **今回お題に足した** |

⚠ **飽和の式に限らず、不飽和まで広げても他に無い**（指摘は「飽和の範囲で」だったが、全域で成り立つ）。
★ **＝ 腕の線を門番として置くと、いま並んでいるお題を2件減らすだけになる。**

**★ したがって:**
- ⚠ **お題を選ぶ門番からは外した。** 効いていない門番を残すと、次に読む人が
  「この線で何かが落ちている」と誤解する。**落としているのは「上限20」1本だけ**（エステル C₇）。
- ★ **`functionalArmCarbons` は残す**（**測る道具**）。`IC1` の否定対照が
  「門番に戻したらお題が2件減る」ことと、線がどこを通るか（ケトン C₇ は 5・エステル C₇ は 6）を
  毎回測り直す ＝ **線が動いたら知らせる**。
- ⚠⚠ **`FG_ARM_MAX` に下ろしてはいけない**のは、門番から外した今も同じ
  （5 に下げるとエステル C₇ が黙って 45→44種になる）。

**★ お題の選び方は3本の線に戻った**（`IC1` が**集合ごと**突き合わせる）:
① 種類数 2〜20 ／ ② 正解の全部に名前が付く ／ ③ 不飽和度1（飽和の式・§19-5c）。
**並んでいる15件は、この3つを通るものの *全部* である。**

**★ どの線が何を落としているか**（同じ全域を数えた実測）:

| 線 | 落とす数 | 中身 |
|---|--:|---|
| ① 下限2種 | **23** | C₃H₆O のケトン・C₂H₄O のアルデヒド など |
| ① 上限20種 | **14** | ⚠ うち**飽和はエステル C₇H₁₄O₂（45種）だけ**。残り13件は不飽和 |
| ② 名前が出そろう | **11** | どれも不飽和の式（飽和の式は名前が出そろっている） |
| ③ 不飽和度1 | **0** | ⚠ ②が先に全部落とすので**いまは重なっている** |

⚠⚠ **③ が0件でも外さない。** ②は「名前を足せば通る」線なので、
**不飽和の式に名前を足した瞬間に、数え落としたお題が通ってしまう**（§19-5c・`IC3`）。
★ **「効いていないから外す」を適用しないのはここだけ** —— 腕の線は*編集の好み*だったが、
③は**数の正しさ**の線で、重なりが外れるのは「名前が増えたとき」＝ こちらから見えない出来事。

⚠⚠ **この線を列挙の門番（`FG_ARM_MAX`）に下ろしてはいけない。**
実測: 7 → 5 に下げると、腕が2本のエステル C₇H₁₄O₂ は入口検査
（`m > FG_ARM_MAX * 2`）を素通りし、`chooseCarbons` の `Math.min(restC, FG_ARM_MAX)` だけが効いて
**ギ酸ヘキシルが1件だけ消えた 44種**が返る ＝ §19-3 が禁じた「20種のはずが18種のお題」そのもの。
**断るなら `overflow`、選ぶなら外側で。** 物差しは純粋関数 `functionalArmCarbons(mol, className)`
として chemistry.js に置き、**お題を選ぶ側（`fgPresets`）と検査だけが使う**。

### 19-5c. ★★「何種類か答える」出題（§19-4 の宿題）は **作らない**

**理由は「難しいから」でも「作れないから」でもなく、★ 母数が 0 になったから。**

| 分類 | 式 | 構造異性体 | ★ 立体まで含めた総数 | 腕 |
|---|---|--:|--:|--:|
| ケトン | C₅H₁₀O / C₆H₁₂O / **C₇H₁₄O** | 3 / 6 / **15** | 3 / 7 / **19** | 3 / 4 / **5** |
| アルデヒド | C₄H₈O / C₅H₁₀O / C₆H₁₂O | 2 / 4 / 8 | 2 / 5 / 11 | 3 / 4 / 5 |
| エステル | C₃H₆O₂ / C₄H₈O₂ / C₅H₁₀O₂ / C₆H₁₂O₂ | 2 / 4 / 9 / 20 | 2 / 4 / 10 / **25** | 2 / 3 / 4 / 5 |
| カルボン酸 | C₄H₈O₂ / C₅H₁₀O₂ / C₆H₁₂O₂ | 2 / 4 / 8 | 2 / 5 / 11 | 3 / 4 / 5 |
| ⚠ 線の外 | アルデヒド C₇H₁₄O / カルボン酸 C₇H₁₄O₂ | 17 / 17 | 28 / 28 | 6 / 6 |
| ⚠ 線の外 | **エステル C₇H₁₄O₂** | **45** | **64** | 6 |

★ **ペンチルの線を引いたあと、残った13件は全部「書き出せる」**（構造異性体が 2〜20種）。
⚠ ＝ **飽和の式（CₙH₂ₙO・CₙH₂ₙO₂）の中に「書き出すには多いが、数えるなら答えられる」式が
1つも残らない**（⚠ 不飽和の式には残る。**それでも数えない理由は下の節**）。
④ を作る理由だったエステル C₇H₁₄O₂（構造45種・立体込み64種）は、**ユーザー自身が出題から外した**。

→ **決定: `countOnly` の出題は作らない。**（§19-4 が挙げていた未決 —— 置き場所・4択か自由入力か・
構造だけか立体込みか —— は**問いごと消える**。1問のために新しい出題の型を作ることになっていた）

⚠ **作る理由が戻ってくる条件を書いておく**: **腕がペンチル以内のまま 21種以上になる分子式**が現れたとき
（＝ `FG_MAX_CARBON` を上げる／新しい分類の種を足す／不飽和の式を扱う、のどれか）。
**そのときは `IC1` が赤くなる**（お題に並べられない式が生まれたことを、件数ではなく線で知らせる）。

#### ⚠⚠ 不飽和の式（CₙH₂ₙ₋₂O など）を数えると、**腕がペンチル以内で21種以上が13件出る**。それでも作らない

**実測（腕≤5 かつ 構造 21種以上）**: ケトン C₆H₈O(32)・C₇H₁₂O(59)・C₇H₁₀O(142)／
アルデヒド C₅H₆O(21)・C₆H₁₀O(35)・C₆H₈O(85)／エステル C₅H₈O₂(22)・C₅H₆O₂(34)・
C₆H₁₀O₂(71)・C₆H₈O₂(149)／カルボン酸 C₅H₆O₂(21)・C₆H₁₀O₂(35)・C₆H₈O₂(85) の **13件**。

★ **それでも母数に数えない。理由は「多いから」ではなく `enumerateFunctionalGroupIsomers`
が不飽和の式では *数を間違える* から。**

⚠⚠ **種と腕の作りでは、カルボニル炭素が環に載った分子を1つも作れない。**
腕は**それぞれ独立した断片**を1本ずつ貼るので、**2本の腕をつないで環にする道が無い**。
**実測: C₆H₁₀O のケトン18種に シクロヘキサノン が入っていない**
（環をもつ生成物は5件あるが、**カルボニル炭素が環に載っているものは 0件**）。
シクロヘキサノン・2-メチルシクロペンタノン・ラクトン（環状エステル）は**高校で出る形**なので、
これは許容できる取りこぼしではない ＝ **§19-3 の「黙って数え落とさない」に真っ向から反する数**。

★ **飽和の式（CₙH₂ₙO・CₙH₂ₙO₂）では起きない** —— C=O が不飽和度を使い切るので
**環は原理的に作れず、列挙は完全**。⚠ **いまのお題13件が全部飽和なのは偶然ではなく、
この道が数を保証できる範囲がそこまでだから。**（不飽和へ広げるなら、
まず「環に載った種」を置く道を足すことになる ＝ §11-3 でナフタレンを断ったのと同じ議論が要る）

★ **参考（出題には使わない）**: 立体まで含めた総数がいちばん大きいのは
**エステル C₇H₁₄O₂ の 64種**。⚠ 元の申し送り「エステル45種は*構造*の数で、立体込みはさらに増える」は
そのとおりだった（45 → 64）。

### 19-5d. 検査

| # | 見るもの |
|---|---|
| **`IC1`** | ★ **2026-09-01 に架け替えた**（§19-5b-2）。いま見るのは **お題 = 「2〜20種 ＋ 名前が出そろう ＋ 不飽和度1」を通るものの全部**。①`FG_MAX_CARBON` の内側の全域を数え直して**集合ごと**突き合わせる（黙って1件外す／余計に足すと赤）／②★否定対照 = **落ちたのはエステル C₇ だけで、理由は数（45 > 20）**／③★★否定対照 = **腕の線を門番に戻すとお題が2件減る**（アルデヒド C₇・カルボン酸 C₇）＝「もう何も落とさない」の裏取り。線がどこを通るかも7式で測る／④★否定対照 = ギ酸エステルの腕は `0+2`・酢酸メチルは `1+1`（2本を1つに混ぜていない）、エステルを `ketone` として数えると `null`（分類の取り違えが黙って通らない） |
| **`IC2`** | ★ **足した名前を1件ずつ名指し**（49件を全部そろえて突き合わせる）。⚠ 件数で見ると**位置番号を1つ間違えた名前**が緑になる |
| **`IC3`** | ★★ **数え落としの門番** —— お題は全部が不飽和度1（飽和形）であること ＋ ★否定対照 = C₆H₁₀O のケトン18種に**シクロヘキサノンが入らない**（かつ腕の中の環は作れている ＝ 対照が空振りでない）。**シクロヘキサノンが入るようになったら赤** ＝ 環に載った種を置く道ができたので §19-5c を見直す合図 |
| **`IH2`** | 分類つきのお題が **15件**（顔ぶれと総数と「全部に名前が付く」＋ ★**足した2件を名指し**） |
| **`IH4`** | ★ 引き金の架け替え先。C₇ の4件の①種類数 ②名前が**4分類とも**出そろっている（**逆向きの見張り**）③**お題に入るか＝上限20 の内か**が一致する（数以外の線で選んでいたら赤）④**エステルが上限20 の外にいる**（＝ 出題から外している唯一の理由。内側に入ったら赤） |

## 19-6. ★★ エステル C₇H₁₄O₂ の名無し39件に名前を足した（v1494・`ID10`・`ID11`）

**ユーザー判断（DEVELOPMENT.md「判断（2026-09-01）」D-C3）:**

> ★ **エステル C₇H₁₄O₂ の名無し39件にも名前を足す**（出題には使わない。自由モードで名前が出る）。

⚠ **出題には入らない。** 理由は**数ひとつ**（45種 > 上限20）で、**名前を足しても変わらない**。
★ これで C₇ の4分類は**全部「名前が出そろった」状態**になった。

### 19-6a. `IH4` の引き金が引かれた（★ 設計どおり）——「外したままでよいか」を測り直した

★ 前レーンは「エステルに名前が出そろったら赤くなる」見張りを残していた
（**出題から外したままでよいかを再検討させる引き金**）。**足した瞬間に赤くなった。**

**★ 測り直した答え: 外したままでよい。**

| 外していた理由の候補 | 名前を足す前 | 足した後 |
|---|---|---|
| ⚠ **数（45種 > 上限20）** | ★ 効いている | ★ **そのまま効いている** |
| 名前が足りない | 効いていた | **消えた** |
| 腕がヘキシル | ⚠ **もともと効いていない**（§19-5b-2） | 効いていない |

→ ⚠ **数を消して通したのではない。** 見張りを**逆向きに架け替えた**（`IH4` ②④）:
**①名前が欠けたら赤**（自由モードが「（名称未登録）」に戻る）／
**②45種が上限20 の内側に入ったら赤**（＝ 外している唯一の理由が消えたら知らせる）。

### 19-6b. 名前の作り方 —— 前レーンの2つの道は **そのまま使えた**（形だけ違う）

⚠ `iupacNameDetail` は**酸素に単結合以外があると `null`**なので、エステルにも直接は使えない。
★ **前レーンの道（§19-5a）を、エステルの「アルカン酸アルキル」という形に合わせて2つに割っただけ**:

| | アシル側（〜酸） | アルコール側（〜アルキル） |
|---|---|---|
| **(A)** | R-COOH を **R-CH₂-OH に読み替えて** `iupacName` に名付けさせ、`1-〜ノール` → `〜ン酸` へ語尾を移す | 断片を切り出して**付け根 R を付け**、アプリの `iupacAlkylNameFromR` に名付けさせる |
| **(B)** | 主鎖候補（付け根＝C1）・番号・枝の名前を**自前で**組み立てる | 同左（慣用名 イソプロピル・sec-ブチル… の置き換えも自前） |

★ **アシル側で (A) が成り立つ根拠**（前レーンと同じ）: **主鎖の選び方（主特性基を含む最長鎖）と
番号（主特性基が C1）は -COOH と -CH₂OH で同じ**なので、位置番号と置換基がそのまま持ち越せる。

**★ 実測: 45件中44件で (A) と (B) が一致。**
★ さらに**もともと名前があった6件**（酢酸イソペンチル・酢酸ペンチル・プロピオン酸ブチル・
ブタン酸プロピル・ペンタン酸エチル・ヘキサン酸メチル）を**2つの道が作り直せた** ＝ 道の空振り防止。

**表記はライブラリの既存の流儀に合わせた**（C₃〜C₆ のエステルから読んだ）:
- アシルは慣用名が正（**ギ酸・酢酸・プロピオン酸**）。慣用名と体系名が両方使われるものは
  **`酪酸X（ブタン酸X）`・`イソ酪酸X（2-メチルプロパン酸X）`・`吉草酸X（ペンタン酸X）`** の併記形
- 慣用名を持たない酸（**2-メチルブタン酸・3-メチルブタン酸・2,2-ジメチルプロパン酸・
  ヘキサン酸とその枝つき**）は体系名だけ（既存の `3-メチルブタン酸メチル` と同じ）
- アルキル側は**位置番号を含むときだけ丸括弧**（既存の `ギ酸(2-メチルブチル)` と同じ）

### 19-6c. ⚠⚠ 1件だけ食い違った ——「アプリの中で規則の順番が2か所ちがう」（★ **2026-09-02 に直した**）

> ★★ **決着（2026-09-02・Fable レーン `alkyl-name`）: `iupacAlkylName` の同点処理に「置換基数が最多」を足した。**
> 下の経緯はそのまま残す。**直した後の実測と根拠は末尾の「19-6c-2」**。

**分子** CH₃CH₂–CH(–)–CH(CH₃)₂（炭素6個のアルキル基）の名前が2つに割れた:

| | 名前 | 採った鎖 |
|---|---|---|
| **(A) 直す前のアプリの `iupacAlkylNameFromR`** | 1-イソプロピルプロピル | 枝1本（位置 `[1]`） |
| **(B) 自前・古典 IUPAC ＝ ★ 直した後のアプリ** | **1-エチル-2-メチルプロピル** | 枝2本（位置 `[1,2]`） |

⚠⚠ **原因はアプリの中の規則の順番**（バグというより、2か所で並びが違った）:

| | 同点の主鎖の決め方（直す前） |
|---|---|
| `iupacName`（アルカン・アルコール側） | OH位置 → 多重結合位置 → ★ **置換基数が最多** → 置換基位置最小 → 辞書順 |
| `iupacAlkylName`（置換基・アルキル基側） | ⚠ **置換基位置最小 → 辞書順だけ**（「置換基数が最多」が無かった） |

★ **古典 IUPAC（1979 規則 A-2.6(a)「側鎖が最も多い鎖」。⚠ 前レーンは「A-2.3(a)」と書いたが番号違い ＝ A-2.3 は辞書順の規則）は (B)。**
`compounds.json` には **(B) を入れた**。

**⚠⚠ これは「この39件だけの話」ではない。どこまで広がるかを測った（2026-09-01）:**

| 測ったもの | 割れる数 | 中身 |
|---|--:|---|
| 単体のアルキル基 C₁〜C₅ | **0** | ★ アルキル基の書き出し練習（`carbonCounts=[3,4,5]`）は**無傷** |
| 単体のアルキル基 **C₆**（17種） | **1** | `1-イソプロピルプロピル` → `1-エチル-2-メチルプロピル` |
| 単体のアルキル基 **C₇**（39種） | **3** | `1-tert-ブチルプロピル` → `1-エチル-2,2-ジメチルプロピル`／`1-イソプロピル-1-メチルプロピル` → `1-エチル-1,2-ジメチルプロピル`／`2-イソプロピルブチル` → `2-エチル-3-メチルブチル` |
| 単体のアルキル基 C₈ | 測れず | 列挙が `overflow`（この道の外）→ ★ **19-6c-2 で根付き木として測った（9件）** |
| ★ **`compounds.json` の登録図ぜんぶ**（1,025件） | **0** | 主鎖から出る枝で「同点の最長鎖の枝の本数が違う」ものが**1件も無い** |

★★ **＝ 穴は実在したが、画面に出ていた名前は1つも間違っていなかった。**
⚠ **表に出るのは「炭素6個以上の枝／アルキル基を名付けたとき」だけ**で、
今回のエステル C₇（アルコール側が C₆）が**初めてそこへ届いた**。

#### 19-6c-2. ★★ 直した（2026-09-02）—— 何を・どこに・なぜ

**変更は `iupacAlkylName` の候補比較の条件1つ**（`assembler/chemistry.js`）:
同点（同じ長さ）の鎖を **置換基数が最多 → 置換基位置最小 → 辞書順** で選ぶ（`iupacName` の `named.sort` と同じ並び）。

**★ 挿す位置を規則から決めた**（申し送りの問い1）。主鎖側の「OH位置 → 多重結合位置」に当たるものが基の側にあるか:

| 規則 | 順番 | 基の側でどうなるか |
|---|---|---|
| **1979 規則** A-2.25 | 基の鎖 = **自由原子価の炭素から始まる最長鎖**（同点の決め方は書いていない）→ 同点は A-2.6 に従う: **(a) 側鎖が最も多い鎖 → (b) 側鎖の位置番号が最小 → (c) 小さい側鎖の炭素が多い → (d) 枝分かれの少ない側鎖** | (a) を足せばよい。(c)(d) は (a)(b) で決まらなかったときの話で、今回の4件はすべて (a) で決まる |
| **2013 勧告** P-46.1「置換基の主鎖」 | (a) ヘテロ原子数 → **(b) 最長** → (c) ヘテロ原子の種類 → **(d) 多重結合が最多** → (e) 非標準結合数 → (f)(g) ヘテロ原子の位置 → **(h) 自由原子価の位置が最小** → **(i) 多重結合の位置が最小** → (j) → **(k) 置換基が最多** → **(l) 置換基の位置が最小** → **(m) 辞書順先の置換基の位置が最小** | 方法(1)「yl 型・付け根が必ず 1」にも適用される（P-46.1.11 に明記）。**付け根が必ず C1 なので (h) は常に同点、枝は飽和（`iupacNameDetail` は多重結合を主鎖に全部収める）なので (d)(i) も常に同点** ＝ 基の側の同点処理は **(k) から始めてよい**。★ 主鎖側の「OH位置・多重結合位置」に当たる段は**存在するが常に同点** |

★ **2013 P-46.1.11 の例がそのまま今回の形**: `2-hydroxy-1-methylethyl` **[not `1-(hydroxymethyl)ethyl`]**
（付け根 CH に「メチル＋ヒドロキシメチル」か「メチル＋ヒドロキシ」か ＝ 枝2本の鎖を採る）。
**古典と現行で扱いは違わない**（違うのは「好ましい接頭辞」が 2013 では方法(2)の `pentan-2-yl` 型になったことだけで、
方法(1)の `1-methylbutyl` 型も一般命名法として残り、鎖の選び方は同じ P-46.1）。
出典: 1979 規則 A-2（acdlabs の写し r79_36.htm）／ 2013 勧告 P-29.2・P-46.1（iupac.qmul.ac.uk/BlueBook/P2.html・P4.html）。
⚠ **手元の参考書（ハート有機化学・化学新研究・駿台有機化学）は画像 PDF で文字が取れず、確かめていない。**
教科書（数研）も確かめていない ＝ 「教科書にある／無い」はどちらも言わない（DEVELOPMENT.md「教科書の出典は慎重に扱う」）。

**★ 直した後の実測（HEAD と直した版を同じ入力で突き合わせ。`AK10` が固定）:**

| 対象 | 変化 | 中身 |
|---|--:|---|
| アルキル基 C₁〜C₅（列挙器） | **0** | ★ 書き出し練習は無傷。理由 = 同点候補の枝の本数が**全部等しい**ので規則が何も選べない |
| C₆（17種）／C₇（39種） | **1／3** | 上の表のとおり（申し送りと一致） |
| ★ C₈／C₉／C₁₀（**根付き木で自前列挙**。木の数 89／211／507 = 既知のアルキル基の数と一致） | **9／20／44** | 例: `1-イソブチルブチル` → `3-メチル-1-プロピルブチル`、`1-tert-ブチル-2-メチルプロピル` → `1-イソプロピル-2,2-ジメチルプロピル`。**null になる数（入れ子で名付けない）は変わらない**（C₉ 3・C₁₀ 21） |
| `compounds.json` 1,025件の `iupacName` | **0** | ⚠ ただし **`iupacName` が付くのは 99件だけ**（環・カルボニルは null）。R 付きの7件（高分子）も null のまま |
| `stages.json` 114件 | **0** | |
| 登録名との食い違い | 変わらず | 登録名と一致も包含もしない図は直す前も後も **10件**（別件） |

**★ 置換基の *接頭辞* への波及**（申し送りの問い2）: `iupacName` は枝の名前を `_iupacCollectSubs` → `iupacAlkylName` で作るので届く。
**ただし枝が丸ごと残るには主鎖が枝より長い必要がある**。この C₆ の基（付け根から3炭素）を枝として残すには
**主鎖の両側に4炭素以上 ＝ 総炭素 15 以上**（`5-(1-イソプロピルプロピル)ノナン` → `5-(1-エチル-2-メチルプロピル)ノナン`）。
幹の表は C₁₀ までなので、**総炭素 15〜16 の主鎖 9〜10 にしか出ない** ＝ 登録図・ステージでは 0 件。
アルコールも同じ（主鎖は OH を含む最長 → `5-(…)-1-ノナノール`）。**主鎖が短ければ枝は主鎖に取り込まれる**ので無関係
（`3-エチル-2,4-ジメチル-1-ペンタノール` は前後で同じ）。**エーテルだけは付け根が O なので短くても出る**
（`1-イソプロピルプロピルメチルエーテル` → `1-エチル-2-メチルプロピルメチルエーテル`・C₇）。
`AK11` が名指しで固定。

★ **画面**: `game.iupacNumberingDetail` は R 付きの図で `iupacAlkylDetailFromR` の鎖を帯にするので、
**C₆ 以上の基を描いて 🔢 を押した人には、直した名前と、その名前を作った鎖（イソプロピル側を通る3本）が出る**
（名前と鎖は同じ1回の計算 ＝ N-6 は保たれる。`ID11` が鎖も見る）。

⚠ **`compounds.json` は触っていない**（登録名がもとから (B) 側。`node tools/verify-compounds.js` 不合格 0）。

### 19-6d. 図の並び

★ 前レーンと同じく、`layoutMolecule` に置かせたうえで **`verify-compounds.js` と同じ3つの物差し**
（重原子どうし ≥24px・自動水素込み ≥12px・自動水素が結合線の下 ≥12px）で
**原子の並びを総当たりして採点し、いちばん良い並びを選んだ**。
実測の最小は 重原子42px・自動水素11.5px（2件・**既存データにも同じ 11.5px がある警告どまり**）・
結合線との隙間42px。`node tools/verify-compounds.js` は**不合格0**。

### 19-6e. 検査

| # | 見るもの |
|---|---|
| **`ID10`** | ★ **45件を1つずつ名指し**（件数では見ない）。⚠ さらに **名前の集合だけでは「どの構造にどの名前が付いたか」を見ていない**ので、**図を手で組んだ4件**（ギ酸ヘキシル・酢酸ネオペンチル・2-エチルブタン酸メチル・ギ酸(1-エチル-2-メチルプロピル)）を構造から名前で引き当てる。★ 名前が付いても**出題には入らない**ことも見る |
| **`ID11`** | ★★ **2026-09-02 に架け替えた**（§19-6c-2）。①直した名前 `1-エチル-2-メチルプロピル` ＋ **返す鎖がイソプロピル側（枝2本）を通る**（名前と帯が同じ計算）／②★否定対照 = **C₃〜C₅ では起こりえない**（同点の最長鎖で枝の本数が違うものが 0件。C₆ ではちょうど1件 ＝ 空振りでない） |
| **`AK10`** | ★★ **影響範囲の名指し**。①効く相手の数 = C₁〜C₅ で 0・C₆ で 1・C₇ で 3（列挙は C₇+R = 8原子が上限）／②その4件の名前を旧→新で名指し ＋ 木から直に引き直す／③★否定対照 = **全域で「採った鎖の枝 = 同点候補の最大」**（規則 (k) が成り立つ）・C₁〜C₅ は同点候補の枝が全部等しい ＝ 書き出し練習の名前は1つも変わらない／④慣用名（sec-ブチル・tert-ブチル・イソペンチル・ネオペンチル）は生きている |
| **`AK11`** | ★ **接頭辞・エーテルへの波及**。①`5-(1-エチル-2-メチルプロピル)ノナン`（総炭素15）／②アルコールの枝も同じ／③エーテルの基名／④★否定対照 = 主鎖が短いと枝は残らない（前後で同じ名前）／⑤登録図 1,025件で慣用名つきの複合枝が残っていない |

⚠ **番号について**: 発注は接頭辞 `ID` を指定したが、**`ID1`〜`ID9` は「化合物 id と URL の受け口」で
使用済み**だった（`ID1` と `ID2` が実在する）。★ 続き番号 **`ID10`・`ID11`** にし、
`tests.js` 冒頭の台帳にも「10〜11 は別の話」と書いた。

---

## 20. ★★ 書き出しを2種類に分ける ——「一通りすべて」と「条件にある構造」（v1510・2026-09-03）

### 20-0. ユーザー原文（これが仕様）

> **書き出しを充実させ／一通りすべて書き出させる／条件にある構造を書き出す／に分けるイメージです**
>
> **問題としては、脱水して2-ブテンになるアルコールを書き出す、立体異性体の有無、あるいは
> 水をふかして2-ブタノールになるアルケンの書き出し、立体異性体の有無 が実践的です**
>
> **これは書き出しというよりも構造推定です**
>
> **現在の異性体の書き出しに、分子式以外の条件を追加したものになります**
>
> **答えが多くない書き出し問題は練習問題として有用と考えます**

| 種類 | 中身 | 現物 |
|---|---|---|
| ① **一通りすべて書き出す** | 分子式（＋分類）を与えて、その異性体を全部 | §1〜§19 の書き出し練習が **これ** |
| ② **条件にある構造を書き出す** | 「脱水して2-ブテンになるアルコール」など。答えは 1〜数個 | **§20 で足した** |

⚠ **2つは並ぶ選択肢で、上下関係ではない。** ②を①の絞り込みオプションとして中へ埋めると、
この分けが画面から読み取れなくなる（`CS1` が「②が①の枠の中に入っていないこと」を見張る）。

⚠ **画面の見出しはユーザーの言い方をそのまま使う。**「構造推定」は
**ユーザーが説明のために言った言葉**で、見出しに使えとは言われていない
（`CS1` が「画面に『構造推定』が出ていないこと」まで見る）。

### 20-1. ⚠⚠ 「答えが少ない」は落とす理由にならない（★ 覆された判断の記録）

**別レーンが実測で示した**: 1つのアルケンを作るアルコールは化学の構造上 **最大2種**
（C₃〜C₇ を全列挙して、組の大きさは 1 か 2 だけ）。
**それを受けて発注側は「数える中身が無いので書き出し練習には回さない」と判断した。**

★★ **ユーザーはこの判断を覆した** ——「**答えが多くない書き出し問題は練習問題として有用と考えます**」。

⚠ **＝ 答えの少なさは門番にしない。むしろ狙いどころ。**
実装では `IP_MIN_ISOMERS`（2種未満は出さない・§16-3 の④）を**この型には掛けない**。
掛けると、ユーザーが名指しした「脱水して2-ブテンになるアルコール」（答えは 2-ブタノールの1種）が消える。

★ **`CS3` が門番になっている**。⚠ ただし**在庫の表を突き合わせるだけでは守れなかった**
（2026-09-03 に実測）——「実行時に `IP_MIN_ISOMERS` を掛け直す」破壊を入れたところ、
**在庫の表は1文字も変わらないので `CS3` は緑のまま**で、赤くなったのは別の3件だった。
だから `CS3` は「並んでいるか」ではなく **答えが1種の6件を実際に開いてみる**。

### 20-2. 関係は1本 ——「脱水」と「水の付加」は同じ辺の逆向き

```
アルコール A（-OH が Cα）  ──[Cβ の H と、Cα の OH が取れる]──▶  アルケン K（Cα=Cβ）
                           ◀──[C=C のどちらかの炭素に -OH が付く]──
```

⚠ **この辺を数える関数を2つ書かない。** `ipDehydrationEdges`（learn.js）が唯一の出どころで、
`ipHydrationEdges` は同じ辺をアルケンの側から組み立てるだけ。
★ §21 の「アルコールとアルケンの対応表（反応系統樹）」も、この辺をそのまま並べる。

⚠⚠ **`reactor.js` の `dehydration_intra` は借りられなかった**（借りようとして測った）。
あちらは `game.userMolecule` を書き換える**実行**の道で、
**ザイツェフ則の主生成物1つだけ**を作り、返すのは画面用の caption つきの結果オブジェクト。
「その構造ができるアルコールをすべて」を組むのに要る**辺の一覧**が取れない。
★ ここは列挙側の道具なので、判定を借りるのではなく**同じ規則を列挙用に書いた**。

### 20-3. ★★ 「立体異性体の有無」は 13件すべてに付ける（段の有無が答えを漏らす）

ユーザーは**両方の型に**「立体異性体の有無」を添えた。
★ **2-ブテンにはシス/トランスがあり、2-ブタノールには不斉炭素がある** ——
この2つが両方 出てくる題材を選んだのは、おそらく偶然ではない。

実装は **`stereoAsked`（§18 の「立体まで答える回」）をそのまま使う**。新しい段も新しい面も作らない:
構造を全部描く → 立体が分かれる場所を指す → 立体込みの総数を答える。

⚠⚠ **段を「立体異性体がある回」にだけ出してはいけない。**
**段の有無そのものが答えになる**（＝ 段が出た時点で「ある」と分かる）。
だから **13件すべてが `stereoAsked`**。実測の内訳は **あり6件・なし7件**（`CS4` が名指しで固定）。

★ §18-4a が「つまみにすると 7問中5問が空振りする」と書いて**お題側に置いた**経緯があるが、
ここではその結論を**採らない**。理由が違うから ——
あちらは「立体の段は付録」だったが、こちらは**立体の有無が設問そのもの**。

### 20-4. 出荷する13件と、落とした11件（★ 落としているのは化学の読みが割れるものだけ）

★ 並べたのは「**ザイツェフ／マルコフニコフの主生成物だけを数えても、副生成物まで数えても、
答えの集合が変わらない**」もの **の全部**（`CS3` が C₂〜C₅ を総当たりで作り直して集合ごと突き合わせる）。

| 型 | 出題 | 答え | 立体 |
|---|---|---|---|
| 脱水 | エテン | エタノール | — |
| 脱水 | プロペン | 1-/2-プロパノール | — |
| 脱水 | 2-メチルプロペン | 2-メチル-1-/2-プロパノール | — |
| 脱水 | **2-ブテン** ★ユーザーの例 | **2-ブタノール**（1種） | **あり**（不斉炭素） |
| 脱水 | 2-メチル-2-ブテン | 2-メチル-2-/3-メチル-2-ブタノール | あり |
| 脱水 | 2-ペンテン | 2-/3-ペンタノール | あり |
| 付加 | エタノール | エテン | — |
| 付加 | 2-プロパノール | プロペン | — |
| 付加 | 2-メチル-2-プロパノール | 2-メチルプロペン | — |
| 付加 | **2-ブタノール** ★ユーザーの例 | **1-ブテン・2-ブテン** | **あり**（シス/トランス） |
| 付加 | 2-メチル-2-ブタノール | 2-メチル-1-/2-ブテン | — |
| 付加 | 2-ペンタノール | 1-/2-ペンテン | あり |
| 付加 | 3-ペンタノール | 2-ペンテン（1種） | あり |

⚠ **落とした11件は「答えが少ないから」ではない。** 実例:

- 「脱水して**1-ブテン**になるアルコール」…… 副生成物まで数えると {1-ブタノール, **2-ブタノール**}、
  ザイツェフの主生成物だけなら {1-ブタノール}。★ **どちらの答えも教室で通る**ので出さない
- 「水を付加して**1-ブタノール**になるアルケン」…… マルコフニコフ則では**1つも無い**
  （1-ブテンの主生成物は 2-ブタノール）。ぜんぶ数えれば {1-ブテン}。同じ理由で出さない

★ **答えが1種の回は6件、そのまま出荷している**（§20-1）。

### 20-5. 実装 —— 新しい枠組みを作らない

★ 条件の回も **`IP_SCOPES` の同じ箱**に乗る（§16-7 の流儀）。
見出し・注記・お題外の断り文・記録の鍵が、その1つの箱から全部出る。
足したのは **`headline`（条件そのものを見出しにする欄）** 1つだけ ——
「C₄H₁₀O の異性体（立体まで）」では**何を書き出すのか言えていない**。

| 足したもの | 場所 |
|---|---|
| `ipDehydrationEdges` / `ipHydrationEdges` / `ipCloneWithMap` / `ipImplicitH` / `ipHydroxylOxygens` | learn.js（DOM 非依存の純関数） |
| `ipSolveCondition` / `ipCondEnumerate` | learn.js（出題を解く側） |
| `IP_COND_KINDS`（2つの型の文言）／`IP_COND_PRESETS`（13件） | learn.js |
| `startFromCondPreset` / `prepareCond` / `condPresets` | `IsomerPractice` |
| `scopeKeyOf` に1行（`condId` → `cond:<id>`） | 既存の共通関数**1本**の改修 |
| 見出し・答え合わせの表題・作業帯で `headline` を先に見る | 3か所（どれも1つの `if` を足しただけ） |

⚠ **`chemistry.js` には手を入れていない**（5レーン同時作業のため。本来は化学モデル側の住人なので、
統合後に移すなら移せる）。⚠ **`compounds.json` / `stages.json` などのデータは1行も触っていない。**

★ **押されるまで数えない**（`fgPresets` と同じ約束）＝ お題一覧を出す費用は 0ms（`CS1` が見張る）。
押したときの実測は**最も重い C₅ の初回で 106ms**、13件ぜんぶ押しても合計 154ms
（同じ分子式の列挙を `IP_COND_ENUM_CACHE` で分け合う。素だと 574ms）。

### 20-6. 検査（`CS1`〜`CS5`）と、実際に赤くしてみた破壊

| # | 見るもの |
|---|---|
| `CS1` | 見出し2本が順に並ぶ／②が①の枠に埋まっていない／「構造推定」が画面に出ない／一覧で1件も数えない／**13件を開いて答えを名前で**突き合わせ／見出し・作業帯が条件を名乗る／ボタンの表記 |
| `CS2` | ★否定対照。同じ分子式でも条件に合わない構造は `scope`（断り文が条件を名乗る）／**合う構造は `ok`**（＝「何でも scope」の空振りでないこと）／逆向きの回でも同じ |
| `CS3` | 在庫の門番。C₂〜C₅ を総当たりで作り直し集合ごと一致／落選11件を名指し／**答えが1種の6件が実際に開く** |
| `CS4` | 13件すべて `stereoAsked`／立体あり6件・なし7件を名指し／ユーザーの2例を数で固定（2-ブタノール 1→2種・1-/2-ブテン 2→3種） |
| `CS5` | ★陰性対照。②を開く前後で①の答えの数が1つも動かない（`IP4` の既知値の表とは**別の壊れ方**＝ キャッシュ汚染を見る）／記録の鍵が①と衝突しない |

★ **対照が効くことを、壊して実測した**（このリポジトリが繰り返している「効かない対照」を避けるため）:

| 壊したもの | 赤くなったもの |
|---|---|
| ②の枠を①（分類で絞る回）の中へ入れた | `CS1` |
| `scopeKeyOf` が `condId` を返さない | `CS1`・`CS2`（`scope` → `unknown`）・`CS5`（鍵が `@stereo` だけになる） |
| `startFromCondPreset` に `IP_MIN_ISOMERS` を掛け直した | `CS1`・`CS2`・`CS3`・`CS4` |
| `stereoAsked` を「立体異性体がある回」だけにした | `CS1`・`CS4` |
| 条件の回が①の列挙キャッシュを踏むようにした | `CS5` |
| `ipDehydrationEdges` を主生成物だけに絞った | `CS3`（出荷13件 → 全部通ってしまう） |

⚠ **1回目の `CS3` は空振りだった**（§20-1 の後半）。**在庫の表と実行時の門番は別のもの**で、
表を突き合わせるだけの検査は「実行時に足した門番」を1つも見ない。

### 20-7. 次の人へ（やらなかったこと）

- **C₆ 以上は入れていない。** C₆ の在庫は作れる（実測: 型1 が13件・型2 が17件の候補）が、
  ⚠ **答えの分子式の異性体が 32種**になり、答え合わせの左列が長くなる。
  ★ ユーザーの原文は「**C5まで**」なので、そこで止めた
- **環状アルコール（シクロペンタノール → シクロペンテン）は入っていない。**
  ⚠ 分子式の族が違う（CₙH₂ₙO → CₙH₂ₙ₋₂）ので、いまの13件と同じ枠にそのまま入る。
  入れるなら `IP_COND_KINDS` に3つ目の型ではなく、**`carbons` の代わりに分子式を持たせる**のが筋
- **「条件」の語彙は2つだけ**（脱水・水の付加）。§21 の対応表と同じ辺を使うので、
  ★ **足すなら辺のほうを増やす**（酸化 → アルデヒド/ケトン、など）

---

## 21. ★ アルコールとアルケンの対応表（反応系統樹）—— **見積りだけ**（v1510・2026-09-03）

> ★★ **続き（2026-09-05・v1516）: 形式2 は §22 で実装した。** 実測は「新規関数0本・共通関数0本・
> ⚠ **在庫の固定検査は3件**」で、下の見積り（形式2 ＝ 新規1本）より軽かった一方、
> **固定検査の数は数えられていなかった**。★ **形式1（矢印を引かせる）の見積りは据え置き** ——
> ただし「在庫の数を固定した検査」は **2件ではなく5件**（`EP7`・`BC1` ＋ `CS1`・`CS3`・`CS4`）。詳しくは §22-1。

### 21-0. ユーザー原文

> **C5まで、アルコールとアルケンの対応表（反応系統樹）／左列にアルコール右列にアルケンをならべ、→で対応**
> **も問題として有用です。**
>
> **構造式をすべて並べさせた状態で矢印を引かせる**
> **左列の構造式と→がある状態で右列の構造式を書かせる**

### 21-1. ★★ これは①でも②でもない「3つ目の形」…… だが**片方だけは②に入る**

| 出題形式 | どこに置けるか |
|---|---|
| **形式1**（構造式を全部並べて **矢印を引かせる**） | ⚠ **3つ目の面が要る**。答えが「描いた構造」ではなく **対応（辺）** なので、`grade()` の物差し（`canonicalCode` で照合）がそのままでは効かない |
| **形式2**（左列と → がある状態で **右列を書かせる**） | ★ **②「条件にある構造を書き出す」の3つ目の型として、そのまま入る**（「A を脱水すると何ができるか、すべて」＝ 答えは構造） |

★ **＝ 形式2 のほうが圧倒的に軽い。片方だけ先に作るなら形式2。**

### 21-2. 見積り（★ 数はすべて実測）

#### (a) C₅ までの表の大きさ —— ⚠ **1画面には入らない**

| | 行数 |
|---|---|
| 左列（鎖式飽和一価アルコール C₁〜C₅） | **16行**（C₁:1 / C₂:1 / C₃:2 / C₄:4 / C₅:8）。C₂〜C₅ なら **15行** |
| 右列（鎖式アルケン C₂〜C₅） | **10行**（C₂:1 / C₃:1 / C₄:3 / C₅:5） |
| 矢印 | **18本** |

⚠ **左列には「脱水できない」行が2つある**（メタノール・2,2-ジメチル-1-プロパノール ＝ β水素が無い）。
★ 教材としては良い落とし穴だが、**いまの器は「答え0個」を出せない**（`beginSession` は答えが1件以上を前提）。

⚠ **スマホ縦（375×812）で1画面に入らない。** 構造式1つが読める最小は
このアプリの標準の結合長 46px で 2〜3段 ＝ **1行あたり 90〜110px**。
16行 × 100px = **1,600px** で、モーダルの中身に使える高さ（実測の目安 500px 前後）の **3倍以上**。
★ **C₄ までに畳めば 8行 ＋ 5行**で、縦スクロール1回ぶんに収まる。**C₅ まで一度に見せる形にしない**のが要点。

#### (b) 新規の関数（形式1）

| # | 関数 | 中身 |
|---|---|---|
| 1 | `ttBuildTable(maxC)` | 左列・右列・正しい辺の集合。★ **化学は新規ゼロ**（§20-2 の `ipDehydrationEdges` をそのまま使う） |
| 2 | `ttRender()` | 2列の図を並べる（`layoutMolecule` ＋ サムネイル描画） |
| 3 | `ttHitTest(x,y)` | ポインタの下の行 |
| 4〜6 | `ttPointerDown/Move/Up` | **矢印を引く操作**（下の (d)） |
| 7 | `ttRenderArrows()` | 引いた矢印の描き直し |
| 8 | `ttGrade()` | 引いた辺 ↔ 正しい辺の突き合わせ（**新しい物差し**。`canonicalCode` の照合では足りない） |
| 9 | `ttStart()` / `ttStop()` | 面の開始・終了（練習の登録） |

★ **9本**（形式2 なら **1本**＝ 生成物に座標を付ける小関数だけ。あとは `IP_COND_KINDS` に3つ目の型を足すだけ）。

#### (c) 共通関数の改修

| # | 相手 | なぜ |
|---|---|---|
| 1 | `game.js` `checkPractices()` | 4つ目の練習を登録する（いまは書き出し3種を名指し。`LX1`〜`LX3` が読む） |
| 2 | `game.js` の離脱ガード（`blocksEditing` / 帯の付け外し） | 新しい面も「書きかけを黙って捨てない」の輪に入れる |
| 3 | `index.html` `#study-body` に6枚目の `<details>` | 入口 |
| 4 | `setupBackdropClose` の表 | 新しいモーダルを立てるなら |

★ **2本以上**（1・2 が確実。3・4 は面の置き方しだい）。
⚠ **＝ 発注の線（新規5本以上／共通2本以上）を両方 超える。** だから **§21 は見積りだけで止めた。**

#### (d) 矢印を引く操作の当たり判定 —— ⚠ **前例がゼロ**

⚠ **このアプリに「A から B へ線でつなぐ」操作は1つも無い**（実測: `learn.js` にドラッグの実装は 0件、
キャンバスのドラッグは「原子を置く／結合を伸ばす」で、**別の要素どうしを結ぶ**ものではない）。

| 案 | スマホ縦で成立するか |
|---|---|
| **ドラッグ**（左を押して右まで引く） | ⚠ **モーダルの中は縦スクロール領域**。指を縦に動かすとスクロールと取り合う（`touch-action` を切るとスクロールできなくなる）。★ 左右の列が横に並ぶので**ドラッグは主に横方向**だが、行が違えば斜め ＝ 縦成分が入る |
| **タップ2回**（左を選ぶ → 右を選ぶ） | ★ **こちらが成立する**。当たり判定は行そのもの（高さ 90〜110px ＝ §hit_areas の 44px を大きく超える）。選択中の行を光らせれば取り消しも自然 |

★ **結論: タップ2回。** ドラッグは「机の上で線を引く」感覚に近いが、
**縦スクロールと同居できない**ので、この面では採らない。

#### (e) 「在庫の数を固定した検査」に当たる箇所 —— ★ **実測 2件**

⚠ **推測せず、当て木（6枚目の `<details>` ＋ 新しいモーダル1枚）を入れて全 752 件を流した**（753秒）:

| 落ちた検査 | 何を言っているか |
|---|---|
| `EP7` | `#study-body` 直下の `<details>` が5つでない（6つになった）。★ **id と並び順は不変条件**なので、足すのは末尾に限る |
| `BC1` | 新しいモーダルが背景クリックで閉じない（`setupBackdropClose` の表に無い） |

★ **それ以外は 750/752 が緑**。`MO1`〜`MO4`（モーダルの縦オーバー）と `UX1`（操作の案内の字数）は
**悉皆で回る形**なので、面を足しても検査の数字を書き換える必要は無い ——
ただし **`UX1` の総量の余裕は 172字**（528 / 700）しかないので、新しい面の説明はそこに収まること。

### 21-3. 形式2 の在庫（★ こちらは今すぐ作れる）

「**A を脱水すると何ができるか（すべて書き出す）**」を1件ずつの出題にすると、C₁〜C₅ で:

- **答えが1個 …… 10問**
- **答えが2個 …… 4問**（2-ブタノール・2-ペンタノール・2-メチル-2-ブタノール・3-メチル-2-ブタノール ＝ **ザイツェフ則が効く回**）
- ⚠ **脱水できない …… 2件**（メタノール・2,2-ジメチル-1-プロパノール）。
  ★ **いまの器では出せない**（答え0個）。出すなら「0個」を答えとして受ける道が要る ＝ ★ **設計判断**

★ **合計 14問**。⚠ **答えが2個の4問は、§20-4 で「読みが割れる」として落としたものとは別物** ——
あちらは「その構造を作るアルコールはどれか」（逆向き）で読みが割れたが、
**こちらは「このアルコールから何ができるか」なので、主生成物も副生成物も並べて書けばよい**（割れない）。

---

## 22. ★★ アルコールを脱水するとできるアルケンを書き出す（**形式2 の実装**・v1516・2026-09-05）

### 22-0. 何を作ったか

§21 が「見積りだけ」で止めた2つの出題形式のうち、**形式2（左列と → がある状態で右列を書かせる）**
を実装した。⚠ **形式1（矢印を引かせる）は作っていない**（§21-2 の線を超えるという判断は据え置き。
更新点は §22-1）。

★ 中身は **§20 の器（`IP_COND_KINDS`）に3つ目の型 `products` を足しただけ**。
新しい面・新しいモーダル・新しい `<details>` は1つも増えていない。

| | 逆方向（§20・2つの型） | ★ 順方向（§22・新しい型） |
|---|---|---|
| 相手（`target`）は | **生成物**（アルケン／アルコール） | **出発物**（アルコール） |
| 設問 | 「脱水すると 2-ブテン になるアルコール」 | 「**2-ブタノール を脱水するとできるアルケン**」 |
| 辺の渡り方（`from`） | `'answer'` … 候補ごとに辺を数える | `'target'` … 相手の辺を1回だけ数える |
| 在庫 | 13件 | **14件** |

⚠ `ipSolveCondition` にあった `pre.kind === 'dehydration' ? ipDehydrationEdges : ipHydrationEdges`
の三項は**外した**。型が3つになった時点で、新しい型が黙って片方へ落ちる書き方だったため
—— **向きも辺の関数も、型そのものが名乗る**（`from` / `edges`）。

### 22-1. ★ §21 の見積りを実測で更新した

| §21 が言っていたこと | 実測（v1516） |
|---|---|
| 形式2 の新規関数は **1本**（生成物に座標を付ける小関数） | ★ **0本**。答えは列挙器が返した分子そのもの（座標も名前も付いている）で、逆方向とまったく同じ道を通る |
| 共通関数の改修 | ★ **0本**。`scopeKeyOf` は §20 で `condId` を見るようになっているので、そのまま効いた |
| 形式2 は「②の3つ目の型として**そのまま入る**」 | ⚠ **在庫の数を固定した検査に3件 当たった**（`CS1`・`CS3`・`CS4`）。どれも「②のお題は13件」を数で持っていたので、**逆方向13件だけを見る**ように直した。★ §21-2 (e) は形式1 について「固定検査は2件」と測っていたが、**形式2 にも 3件ある**。数えていなかった |
| 形式1（矢印を引かせる）の見積り | ★ **そのままでよい**。新規9本・共通2本以上・当たり判定の前例ゼロ・スマホ縦に入らない、はどれも動いていない。⚠ 1つだけ足すなら「**在庫の固定検査は 2件ではなく 5件**」（`EP7`・`BC1` に加えて `CS1`・`CS3`・`CS4` が「②のお題の数」を持っているため） |

### 22-2. ⚠⚠ 「主生成物だけを数える読み」と割れる4件を、**ここでは落とさない**

⚠ §20-4 は「主生成物だけを数えても副生成物まで数えても答えが変わらないもの**だけ**を出荷する」
という線を引いた。**順方向にそのまま当てると、ザイツェフ則が効く4件が全部 消える**
（2-ブタノール・2-ペンタノール・2-メチル-2-ブタノール・3-メチル-2-ブタノール）
—— ユーザーが「**アルコールの分子内脱水は重要です**」と名指しした、いちばん中身のある回である。

★ **落とさない。割れ方の性質が違うから。**

| | 逆方向（§20-4 で落とした） | ★ 順方向（§22 で出荷する） |
|---|---|---|
| 設問 | 「脱水して **1-ブテン** になるアルコールを**すべて**」 | 「**2-ブタノール** を脱水するとできるアルケンを**すべて**」 |
| 何が割れているか | 「2-ブタノールは *1-ブテンになるアルコール* か」＝ **言葉の意味**（集合に入るかどうかの判定） | 「主生成物だけ挙げるか、副生成物まで挙げるか」＝ **設問が数える範囲そのもの** |
| 「すべて」で決まるか | ⚠ **決まらない**。「すべて」は集合の**外側**に掛かるので、中の判定はそのまま揺れている | ★ **決まる**。揺れているのが数える範囲そのものなので、範囲を言えば終わる |
| 画面で決められるか | ⚠ 決めるには「副生成物として生じるものも含める」と書くことになり、**その回の答えが増えることを告げてしまう** | ★ `note` に **「主生成物・副生成物の区別は問いません」** と書く。**どの回でも同じ断り文**なので、答えの数は1文字も漏れない |

★ 実装は `IP_COND_KINDS.products.note` の1文。`DH1` が **14件すべての画面にこの1文が出ている**ことを見張る
（外すと赤 —— 実測済み）。

⚠⚠ **`note` に「2種類できる回では」と数を書いてはいけない。** 一度そう書いて `DH1` が止めた
（`IH1`＝「種類数を先に出さない」の約束・v1489 に当たる）。**「複数できる場合」と書く。**

### 22-3. ⚠ 脱水できない2件は**出さない**（判断 (a)）。(b) の見積り

C₁〜C₅ の鎖式飽和一価アルコールは **16件**。そのうち **2件は脱水できない**:

- **メタノール** …… β炭素そのものが無い
- **2,2-ジメチル-1-プロパノール** …… β炭素に水素が無い

★ 教材としては良い落とし穴（「β位に水素が無いから脱水できない」は入試で問われる）だが、
**いまの器は「答え0個」を出せない**ので出題から外した。

⚠ **数の訂正**: 発注書は「(a) を採ると 14問が12問になる」と書いていたが、
**14 はすでにこの2件を除いた数**（16 − 2）。★ **(a) を採っても 14問のまま。**

#### (b)「できない」を答えにする —— **共通関数5本**（発注の線「2本以上」を超える）

⚠ 推測ではなく、器がどこで「答えは1件以上」を前提にしているかを数えた:

| # | 相手 | 何が前提になっているか |
|---|---|---|
| 1 | `startFromCondPreset` | `computeMolecularFormula(solved.answers[0])` ＝ 答えの1件目から分子式を作っている |
| 2 | `beginSession` | 満点（`targets.size`）・構造の数・**立体の段2の総数**（`stereoTotal` を答えから足し上げる）が全部 0 になる |
| 3 | `renderSession` の案内 | 「キャンバスが答案用紙です。思いつく構造を並べて描き…」が**やることの無い画面**の説明になる |
| 4 | `grade()` / `answerPairs` / `buildAnswerSummary` | 「あと何種」「全部見つけた」が 0/0 で成立しない。**白紙で答え合わせを押すと満点**という採点になる |
| 5 | クリア記録 | 「白紙のまま押せば ✓」＝ 記録の意味が他のお題と変わる |

★ **＝ 器の改修ではなく「答えが構造ではない出題」を作る話**（§21-1 の言う3つ目の形と同じ壁）。
だから (a) を採り、**`ipSolveCondition` が `null` を返して開かない**ことで担保した
—— 在庫に足しても黙って通らない（`DH3` が2件について `null` を実測する）。

⚠ **(c)「脱水できるのはどれか」を選ばせる** も採らなかった。答えが**選択**なので採点の物差しが
また別（＝ (b) より重い）。★ **教材としての価値は残っているので、次に作るならこちら**（§22-8）。

### 22-4. ⚠ 逆方向と**答えの集合が完全に同じ**になる回が7件ある（意図した重なり）

★ §20-2 のとおり関係は**1本の辺**なので、同じアルコール A について
「A を脱水すると何ができるか」と「水を付加すると A になるアルケンはどれか」の
**答えの顔ぶれは必ず一致する**。実測でも 7件が一致した:

`エタノール` / `2-プロパノール` / `2-メチル-2-プロパノール` / `2-ブタノール` /
`2-メチル-2-ブタノール` / `2-ペンタノール` / `3-ペンタノール`

⚠ **＝ 順方向14件が「14件ぶんの新しい中身」ではない。** 新しい答えの集合は **7件**で、
残り7件は**同じ答えを別の反応から問い直す**回である。

★ **それでも両方 残した**理由:
- ユーザーの原文は**対応表を左から右へ**問う形（＝ 左列の16行から2件を引いた14行）で、
  半分を抜くと「表を1行ずつ埋める」形が崩れる
- 問うている反応が違う（脱水 ↔ 水の付加）。⚠ ただし**答えを書くだけの練習としては同じ**なので、
  ここは**判断の余地がある**（削るならユーザーに聞くこと）
- `DH5` が7件を**名指しで固定**しているので、あとで削る判断をしたときに必ず赤くなる

### 22-5. 立体異性体の有無 —— §20-3 と同じく **14件すべてに付ける**

★ `stereoAsked` をそのまま使う（新しい段も新しい面も作らない）。
⚠ 「立体異性体がある回」にだけ段を出すと**段の有無そのものが答えを漏らす**ので、全件に付ける。

実測の内訳は **あり3件・なし11件**（`DH4` が名指しで固定）:

| 回 | 構造 | 立体込み | 何が分かれるか |
|---|---|---|---|
| 2-ブタノール を脱水 | 2 | **3** | 2-ブテンのシス・トランス |
| 2-ペンタノール を脱水 | 2 | **3** | 2-ペンテンのシス・トランス |
| 3-ペンタノール を脱水 | **1** | **2** | ★ 構造は1つでも立体は2つ（「1種だから立体は無い」の思い込みを外す回） |

### 22-6. 実装（触ったファイルは2つだけ）

| 足したもの | 場所 |
|---|---|
| `IP_COND_KINDS.products`（3つ目の型・`from`/`edges`/`label` を持つ） | learn.js |
| 既存2型に `from` / `edges` / `label` | learn.js（`ipSolveCondition` の三項と、ボタンの `→` の組み立てを型へ移した） |
| `IP_COND_PRESETS` に14件（末尾に追記） | learn.js |
| `ipSolveCondition` の向き分け（`kind.from`） | learn.js（**唯一の改修関数**） |
| `DH1`〜`DH5` ／ `CS1`・`CS3`・`CS4` を「逆方向13件だけ」に | tests.js |

⚠ **`chemistry.js`・`reactor.js`・`compounds.json`・`index.html`・`style.css` は1バイトも触っていない**
（`compounds.json` は電荷レーンが作業中）。★ 新しい関数は**0本**（§22-1）。

★ 費用: 使う分子式は逆方向と同じ8つで、`IP_COND_ENUM_CACHE` を共有するので**列挙は1回も増えない**。
14件を全部 開いても、逆方向13件のあとなら追加の列挙は0回。

### 22-7. 検査（`DH1`〜`DH5`）と、**実際に赤くしてみた破壊**

| # | 見るもの |
|---|---|
| `DH1` | 14件を開いて答えを**名前で**／見出し・作業帯・ボタンが**順方向**を名乗る（矢印の向き）／★ `note` が「主生成物・副生成物の区別は問いません」と言い切る／種類数を出さない |
| `DH2` | ★否定対照。同じ C₄H₈ で、辺の先でない 2-メチルプロペンは `scope`・**副生成物の 1-ブテンは `ok`**／⚠ **1-ブタノールの回では同じ 2-ブテンが `scope`**（出発物ごとに辺を数えている証拠） |
| `DH3` | 在庫の門番。C₁〜C₅ のアルコール**16件を総当たり**し、脱水できる14件が全部 出荷され**実際に開く**／ザイツェフ則が効く4件が**構造2つで開く**／脱水できない2件は出荷に無く `ipSolveCondition` が `null` |
| `DH4` | 14件すべて `stereoAsked`／あり3件・なし11件を名指し／3-ペンタノールの 1→2 を数で固定 |
| `DH5` | ★陰性対照。①の答えの数が動かない／記録の鍵が27件すべて相異なる／★ 逆方向と一致する7件を名指し（空振り検出つき） |

★ **対照が効くことを、壊して実測した**（このリポジトリが繰り返している「効かない対照」を避けるため）:

| 壊したもの | 赤くなったもの |
|---|---|
| 順方向を**ザイツェフ則の主生成物だけ**に絞った | `DH1`・`DH2`・`DH3`・`DH4`・`DH5`（5件） |
| **脱水できないメタノールを在庫に足した** | `DH1`・`DH3`・`DH4` |
| `ipSolveCondition` の「答えが0個なら開かない」を外した | ★ **`DH3` だけ**（この線を持っているのが `DH3` 1件であることまで測れた） |
| `stereoAsked` を「立体異性体がある回」だけにした | `CS1`・`CS4`・`DH1`・`DH4` |
| `note` から「主生成物・副生成物の区別は問いません」を外した | ★ **`DH1` だけ** |
| ボタンの表記を逆方向と同じ `脱水 → …` に戻した（矢印の向きが逆） | ★ **`DH1` だけ** |
| `from` を `'target'` → `'answer'`（辺を逆向きに渡る） | `DH1`〜`DH5`（5件。14件とも開かなくなる） |
| `scopeKeyOf` が `condId` を返さない | `CS1`・`CS2`・`CS5`・`DH1`・`DH2`・`DH5` |
| ザイツェフ則が効く4件を在庫から抜いた | `DH1`〜`DH5`（5件） |

⚠ **`note` の1文と、ボタンの矢印の向きは、それぞれ検査1件しか持っていない。**
どちらも「画面の言葉」なので、実装を直しても検査が黙っている型 ＝ 触るときは `DH1` を必ず読むこと。

### 22-8. 次の人へ（やらなかったこと）

- ⚠ **「脱水できるのはどれか」を選ばせる出題（(c)）は作っていない。** β水素の有無という
  入試で問われる中身が、いまの3つの型のどれにも入っていない。★ **答えが構造ではないので、
  形式1（矢印）と同じ「3つ目の形」の側**。作るならそちらとまとめて設計すること
- **主生成物がどれかは、答え合わせでも示していない。** `ipDehydrationEdges` は `major` を返して
  いるので材料はあるが、答え合わせの左列の名前は `answerPairs`（①と共有）が組んでおり、
  ★ そこに印を足すと**書き出し練習ぜんぶ**に触ることになる（`IW10`・`IW11` が読む欄）。
  ⚠ **`note` で規則を言うところで止めた**（§22-2）
- **C₆ 以上・環状アルコールは入れていない**（§20-7 と同じ理由）
- ⚠ **順方向と逆方向で答えが同じ7件**（§22-4）は、削るならユーザー判断が要る
