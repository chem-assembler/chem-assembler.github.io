# 設計書: 長い鎖を畳んで見せる（Gemini レビュー項目25）

ステアリン酸のような長い鎖（炭素18本）をそのまま全部描くと、線が密集して読めない。
**モノステアリン酸グリセリドは 798×168px に25原子**あり、クイズの図枠（200×133px）に
入れると潰れて構造が判別できない。

## 決めたこと（2026-08-02 ユーザー判断）

| 場所 | ふるまい |
|---|---|
| **クイズの図** | **常に畳んだ表示**にする。見やすさが目的で、編集はしないので固定でよい |
| **キャンバスの図** | **縮小⇆展開を切り替えられる**のが理想。畳んだ状態から開いて中身を確かめられる |

**ラベルの整形（置換基名）はすでに済んでいる。** `substituentLabel` は
モノステアリン酸グリセリドの置換基を `C₁₉H₃₇O₂` と返す（v405 時点で実測）。
レビューが言う「立体クイズでの置換基カード化」はここでは達成済みで、
**残っているのは図そのものの描き方**。

## 何を畳むか

**直線に並んだメチレン鎖**（`-CH₂-` が連続する部分）だけを畳む。条件:

- 重原子の隣が**ちょうど2つ**で、どちらも炭素（＝鎖の途中）
- **置換基を持たない**（自動水素のみ）
- **環に入っていない**・**不斉炭素でない**（畳むと立体が読めなくなる）
- **3個以上**続いている（2個までは畳んでも短くならない）
- **一直線に並んでいる**（曲がった鎖を畳むと、どこへ折り返すか決められない）

畳んだ部分は `-(CH₂)₁₆-` のように**個数つきのラベル1つ**に置き換える。

## 畳むだけでは幅は縮まない

**ここが実装の要点。** ラベルに置き換えても、鎖の両端の原子は元の座標のままなので
**図の広がりは 798px から変わらない**。見やすくするには、
**畳んだぶんだけ向こう側を手前に寄せる**必要がある。

一直線の鎖に限れば単純で、**鎖の方向へ `(本数-1) × 刻み` だけ、
向こう側の原子をまとめて平行移動する**だけで済む。曲がった鎖を対象外にしているのは、
この移動が一意に決まらなくなるため。

## 置き場所

**表示専用の座標変換**として作る。`userMolecule` や `target` のデータそのものは変えない
（変えると正準コード・立体の読み・保存が全部ずれる）。

- **第1段（クイズ側・先に実装）**: `quiz.js` の `renderMoleculeIntoSvg` の中で、
  描く直前に畳んだ座標を作る。**quiz.js は統合セッションの持ちファイル**なので今すぐ着手できる
- **第2段（キャンバス側）**: 畳む・展開するのトグルが要る。`game.js` の描画と当たり判定、
  および「畳んだラベルをタップしたらどうなるか」の扱いが要る。
  **`game.js` は現在レーンE が持っている**ので、空いてから
- 両方できた時点で、共通部分を `chemistry.js` の純関数（`condenseChainForDisplay(target)`）へ
  引き上げる。**いまは `chemistry.js` をレーンF（R/S）が持っている**ので置かない

## どの図で畳むかは**呼び出しごとに選ぶ**（実装中に見つかった落とし穴）

**すべてのクイズ図で一律に畳んではいけない。**
「🎓 同じ化合物？」は**主鎖をわざと曲げて出題する**（同じ分子を違う形で描いて見比べさせるのが
問題そのもの）。畳む条件は「一直線に並んでいる」なので、**曲げた側だけ畳まれず、
同じ分子の2枚がまったく違う絵になる**。これは出題を壊す。

そこで `renderMoleculeIntoSvg(game, svgId, target, showWedge, condense)` の
**最後の引数で呼び出しごとに決める**（既定は畳まない）。

- **畳む**: 立体のクイズ（🎯 同じ立体はどれ？ ／ 同じ？違う？）。
  出題の作り方が「中心での巡回（＝枝の剛体回転）」なので、**一直線の鎖は一直線のまま**。
  4枚の選択肢で畳み方が食い違うことがない
- **畳まない**: 🎓 同じ化合物？（上の理由）／くさび図モード（立体を見せる図なので中身を隠さない）

## 気をつけること

- **立体を壊さない。** 畳む対象から不斉炭素と環を外しているのはこのため。
  畳んだ図から `fischerSlots` を読み直すことは無い（表示専用なので）が、
  同じ SVG を使う立体クイズで見た目が変わるので、**畳む前後で判定が動かないこと**を検査する
- **小さい分子は今までどおり。** 3個以上続くメチレン鎖を持たない分子（大半）は
  一切変わらない。既存のクイズ図が動かないことをテストで固定する
- キャンバス側では、**畳んだ状態で編集させない**のが安全（開いてから編集させる）。
  畳んだラベルを消しゴムで消したらどうなるか、といった問いを増やさないため
