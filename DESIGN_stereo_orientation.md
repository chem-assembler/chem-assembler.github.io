# 設計整理: 作図の向きと「立体を読む」の関係

**Gemini レビュー項目21 の2点目**（ユーザー指示「２については、全体の設計をあらためて整理して確認」）。
1点目（乳酸の -OH を斜めから軸上へ戻す）は **v433 で完了**。この文書はその副作用を出発点に、
「図の見た目」と「立体の宣言」がひとつの座標に同居している問題を整理する。

## 1. 何が起きているか

`readAtomParityFromFischer()` は、**sp3 の不斉炭素の枝4本がたまたま上下左右の軸に載っていれば
無条件に発動する**。「フィッシャー投影として描いた図」と「たまたま十字になった構造式」を
区別する仕組みが無い。

そのため作図者は、立体を宣言したくないとき **わざと十字を崩す**（置換基を斜め45°に置く）しかない。
これが項目21 の見た目の悪さの正体で、`stages.json` のアラニン（NH₂ を 328,270 の斜めに置く）や
改修前の乳酸がそうなっていた。**斜め配置はバグではなく、「立体は未指定」を表す唯一の手段だった。**

v433 で乳酸を軸上に戻した結果、見た目は直ったが、**トグル ON では「D-乳酸」と名乗る**ようになった。
既定 OFF（v376）なので通常は見えないが、意図した表示ではない。

## 2. 実測（v433・ライブラリ207件を全走査）

十字として読める化合物は **24件**。これを「主鎖が縦か横か」で割ると、きれいに二分される。

| 主鎖の向き | 件数 | 中身 |
|---|---|---|
| **縦**（＝本物のフィッシャー投影） | **13** | D-グルコース（鎖状）・D/L-グリセルアルデヒド・D-ガラクトース・D-マンノース・D/L-アラニン・**D/L-乳酸**・D-フルクトース・グルコン酸・ジステアリン酸グリセリド |
| **横**（＝ふつうの構造式） | **11** | 3-メチルヘキサン・2,3-ジメチルペンタン・**乳酸**・セリン・システイン・メチオニン・バリン・ロイシン・リシン・2-ブタノール・酒石酸 |

**縦の13件はすべて立体を意図した登録（名前に D-/L- が付くか糖）**、
**横の11件はすべて立体を意図していない登録**。例外は1件も無い。

トグル ON にしたときに名前が変わるのは **乳酸だけ**（→ D-乳酸）。
他の10件が無事なのは、対になる D-/L- のエントリが登録されていないため
基底名フォールバックに落ちるからで、**設計で守られているわけではない**。
D-セリンを1件登録した瞬間に、ふつうのセリンが「D-セリン」と名乗り始める。

さらに ON のとき、**セリンとその鏡像が別分子として扱われる**（`canonicalStereoCode` が違う）。
図は「セリン」と書いてあるだけなのに、パズルの正誤やクイズの同一判定が
描いた向きで変わってしまう。

## 3. 直し方の候補

### 案A: データに宣言を持たせる（`stereo: "fischer" | "none"`）

エントリごとに「この図は立体を表している」と明示し、宣言のあるものだけ読む。

- ○ 意図がデータに残る。図の見た目を自由にできる
- ✕ **ユーザーが自分で描いた分子には宣言が無い**。キャンバス上の判定には使えないので、
  結局もう一本、幾何だけで決める規則が要る
- ✕ 207件に手を入れる（実際に必要なのは24件だが、今後の登録すべてに規約が増える）

### 案B: いまの慣習（斜め＝未指定）を明文化するだけ

- ○ コード変更ゼロ
- ✕ **項目21 の指摘そのものが直らない**（見た目が悪いまま）
- ✕ 暗黙の規約なので、次に図を足す人が必ず踏む

### 案C: 向きで決める ―― **縦置きの主鎖だけをフィッシャー投影と読む**（推奨）

**不斉炭素の主鎖（長いほうの枝2本）が縦の軸に載っているときだけ**立体を読む。
主鎖が横なら、置換基が軸上にあっても「ふつうの構造式」として読まない。

- ○ **教科書の約束そのもの**。フィッシャー投影は縦に描くと定義されている。
  「縦は奥・横は手前」という規則も、縦置きでなければ意味を持たない
- ○ **上の実測どおり、データを1件も直さずに 24件が正しく二分される**。
  乳酸・セリン・バリン等の11件は読まれなくなり、糖とD/L化合物の13件はそのまま読まれる
- ○ **`assignDLDescriptor` の既存のふるまいと一致する**。この関数はすでに
  「主鎖が横に走っていたら断定しない」と決めてある。いまは
  *名前は D と言うのに D/L 判定は黙る* という食い違いがあるが、案C でそれが消える
- ○ **キャンバスの自作分子にも同じ規則が効く**（幾何だけで決まるため）
- ○ **斜め45°の逃げ道が要らなくなる**。項目21 の「アミノ基が斜めに置かれる」が根から消え、
  `stages.json` のアラニンも軸上に戻せる（C-4b の成果が実際に画面へ出る。C-4c 参照）
- ✕ 横置きで立体を描きたい人は描けない。ただし**そもそも横置きのフィッシャー投影は無い**ので
  実害は無く、立体ビューの整形（`commitProvisional`）は縦横どちらの軸にも枝を置くので操作は変わらない
- ✕ 横置きの図で立体ビューを開くと「読めない図」になり、v370 で入れた
  **「仮の立体」から始める経路**に入る。これは想定どおりのふるまい（不利益ではない）

### 判定の細部（案Cを採る場合）

「主鎖が縦」を **重原子が上下に2つ** で近似すると、乳酸（左右が鎖・上が OH・下が H）は
正しく「横」と出る。ただし**4方向すべて重原子**の中心では上下左右いずれも2つになり決まらない。
正確には **枝の長いほう2本が上下に載っているか**で判定する
（`rootedFragmentCode` の長さで比べられる）。実測した24件はすべて近似で足りるが、
規則としては後者を書く。

## 4. 推奨

**案C を主規則にする。案A は将来の逃げ道として置くだけで、いまは実装しない。**

案C は「データを直さずに、意図どおりの二分が得られる」ことが実測で確かめられており、
既にある `assignDLDescriptor` の判断とも揃う。案A を先に入れると、
自作分子用に結局案C 相当の幾何規則を書くことになり、規則が二重になる。

## 4b. ⚠ 実装して分かった訂正: 門番は「名前」に掛ける（2026-08-03・v445）

**案C を書いたときの手順1「`readAtomParityFromFischer()` に縦置き判定を足す」は誤り。
そのまま入れると立体パズルが壊れる。**

立体パズルは**3巡回で枝を動かす**ので、解いている途中で主鎖が横に来る図がふつうに現れる。
実測すると、**タイムアタックの出題15問のうち12問が横置きの分子**
（アラニン・システイン・リシン・メチオニン等）から作られていた。
読み取り側に門番を掛けると、この12問が「立体が読めない図」になって出題が成立しない。
**「回しても同じ分子」を教えるのがパズルの芯**なので、向きで読めなくなっては本末転倒。

**正しい置き場所は名前を付けるところ**（`lookupCompoundName`）。

| 層 | 門番 | 理由 |
|---|---|---|
| `readAtomParityFromFischer`（立体の読み取り） | **掛けない** | パズル・クイズ・同型判定の土台。向きに依存してはいけない |
| `lookupCompoundName`（名前） | **掛ける** | 立体の**呼び名**は投影の約束に依存する。ここだけが向きを知るべき |
| `assignDLDescriptor` / `assignRSDescriptor` | 掛ける（既にそうなっている） | 同上 |

これはユーザーの求め（「フィッシャーとして内部的に読んで、**ユーザーからはわからないように**
すればよい」）とちょうど一致する。内部では読み続け、外に出す名前だけを抑える。

実装は `chemistry.js` の `isFischerOriented(mol)`（読める中心すべてで主鎖が縦か）と、
`lookupCompoundName` の `useStereo = this.readStereo && isFischerOriented(mol)`。
横置きの図はトグル ON でも OFF と同じ経路（総称名）に落ちる。

## 5. 実装するときの手順（1 は 4b のとおり訂正ずみ）

1. ~~`readAtomParityFromFischer()` に縦置き判定を足す~~
   → **`lookupCompoundName` に掛ける**（v445 で実装ずみ）
2. 回帰テストに「横置きの十字は読まない・縦置きは読む」を固定する。
   実測した24件の二分（縦13／横11）をそのまま検査にする
3. **`stages.json` のアラニンの NH₂ を斜め45°から軸上へ戻す**（項目21 の残り）。
   C-4b で `compounds.json` 側は整えてあるので、`stages.json` を揃えれば
   呼び出しの図も直る（C-4c）
4. `assignDLDescriptor` の「横置きなら断定しない」は**そのまま**。案C 適用後は
   名前側と判断が一致するので、特例ではなく同じ規則の一部になる

## 6. 影響を受ける動画台本（着手前に確認すること）

- **V50 / V51 は D-乳酸・L-乳酸（縦置き）を使う**。案C でも縦置きは読まれ続けるので影響なし。
  v433 で座標を変えたのは**立体名の付かない「乳酸」のエントリだけ**で、D-/L- は触っていない
- **V31 はアミノ酸3種を `state` で置いている**（`summon` の向きが揃わないため。C-4c）。
  手順3で `stages.json` を揃えると `summon` でも揃うようになるが、
  **V31 の座標は `state` 指定なので台本は変わらない**
