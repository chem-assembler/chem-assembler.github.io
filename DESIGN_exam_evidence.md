# 入試の出題実績を知識項目の根拠にする（設計・骨子）

- 状態: **骨子 → Fable 検収済み（2026-09-25）**。台帳 **I-0146**。検収で固めた箇所には「★ Fable 検収（2026-09-25）」の印。
  変えた点・残した点・ユーザーに決めてもらう点は末尾の「検収の記録」
- 決めが要る点は **案 → 推奨 → 理由 → 未決** の順に書く。推奨はこのレーンの見立てで、確定ではない
- 各節で、既存の土台を **そのまま使う／足す／作り直す** に分けて書く
- 数字はすべて 2026-09-25 にこのレーンが読み取り専用で数えた値（`_解析/db/` と `qa/data/`）

## 0. この文書の位置

**ユーザーの方針（2026-09-25）**
> 最終的にはDBに基づいて、出題率をもとにどのラインまで暗記すべきかを根拠とともに示す
> この調査は単発で済ませるべきものでなく、データの保持形式や活用の仕方までしっかり設計すべき

→ **知識項目（qa の `code`）ごとに入試の出題実績を持ち**、参考書の「覚える範囲」の線と qa の画面に**根拠つきで**出す。
その**データの形・母集団と率の定義・生成と更新・見せ方**を決める。

**持ち主の区分**（ここを混ぜると事故になる。記憶 exam-import-owned-by-integration）

| もの | 持ち主 | この文書が決めること |
|---|---|---|
| `_解析/db/`（事実）・`_解析/tools/`（生成の道具） | **入試側セッション** | 何を持ってほしいか（発注の中身の案）まで。**道具の直しは入試側がする** |
| `qa/data/exam_usage.jsonl` ほか公開の集計 | 生成は入試側、**形の契約と検査は chem 側** | 公開の形（スキーマ）と検査 |
| qa の画面・参考書（`reference-src/`・`tools/gen-reference.mjs`） | chem 側 | 見せ方 |

**踏まえる文書**（同じものを二重に作らないため）

- `qa/DESIGN_difficulty_frequency.md` §7（**2軸**: `difficulty` と `priority`。入試は Lv に混ぜない）・§9（Lv の根拠の表と上書き）
- `REFBOOK_STYLE.md` §2-7（網羅は頻度で切らない・覚える範囲は頻度で引く）・§8-5（どこまで覚えるかを言い切る）・§8-7（試験での重み・**統計の形にしない**）・§10-3（器が言うこと／本文が言うこと）・§13-5
- `_解析/SCHEMA_問題DB.md` §2b（出題された／解答が要求された／解く途中で起きた、の3層）・§2c（設問は解答欄の数）
- `_解析/README.md` の「母数を2つに分ける」「共通テストは原典から」・`_解析/PLAN_単元展開.md`
- 記憶: exam-db-frequency・exam-db-expansion-plan・exam-explained-vs-memorized・exam-db-purposes・qa-difficulty-frequency・exam-data-use-loaded-assets・kyotsu-test-originals

**節の番号の読み方**: 本文の §7-x・§9-x は `qa/DESIGN_difficulty_frequency.md`、§2-7・§8-x・§10-x は `REFBOOK_STYLE.md` を指す。
それ以外（§1〜§6 の小節）はこの文書の節。

---

## 1. 目的と、いま在るもの

### 1-1. 土台の実測（2026-09-25）

| もの | 中身 | 数 |
|---|---|---|
| `_解析/db/problems.jsonl` | 1行1レコード（大問を分けたものを含む） | **1,110 レコード ＝ 911 大問**（`daimon_key` で畳む）・8巻（2020〜2027年版 ＝ **2019〜2026年入試**）・**有機のみ**（熱化学の小問2件だけ先取り）・75大学。★ Fable 検収（2026-09-25）: **911 は `daimon_key ?? id` で数えた値**。`daimon_key` を持つのは **348 レコードだけ**で、残り 762 は「1レコード＝1大問」と**仮定**している。その 762 の中に、同じ巻・同じ大学・同じ大問番号で枝番だけ違う組が **15組・31レコード**ある（例: `2022-東京理科大学-薬4(1),(2)`／`薬4(3)`／`薬4(4)`、`2022-大阪大学-4【Ⅰ】`／`【Ⅱ】`）＝ **911 はやや多い**（§1-4 の 7） |
| 巻ごとのレコード | 2020:150／2021:143／2022:141／2023:130／2024:130／2025:130／2026:161／2027:125 | |
| `routes`（解法の手筋の列） | **2024〜2026年版は 0**。2020:150・2021:142・2022:131・2023:130・2027:121 | 5巻ぶんだけ |
| `db/moves.json` | 手筋 271件 | ルートで使われたのは 265件 |
| `db/moves_qa_map.json`（手筋 → qa） | 163手筋（2026-08-15 生成）・qa が付くのは109 | **ルートで使われた265手筋のうち102が対応表に無い** |
| `db/problem_qa_map.json`（大問 → qa） | 1,108 レコード・**すべて org**。本文の物質名・実験名を**キーワードの正規表現**で引いた機械のリンク | |
| `explained_in_text` / `knowledge_required` | 「問題文が型を説明したか／説明なしで要ったか」。**値は手筋の id**（qa のコードではない） | **25 レコードだけ** |
| `answer_required`（誰が答えるか） | 全員10・方式で異なる30・選択6・一部6 | 52（残り1,058は未確認） |
| `question_kinds` | 設問の種類×解答欄の数 | 549 |
| `qa/data/exam_usage.jsonl` | qa コード → 出題実績。`build-exam-usage.js` が生成 | **276コード（全部 org）**・母集団は 2021・2022年版（2020・2021年入試）・1.39MB |
| `qa/data/exam_answer_type.jsonl` | コード×大問の「解答そのものになったか」（true／false／null） | 1,579行・**50コード × 2021年入試だけ**（true 38／false 1,407／null 134） |
| `qa/data/level_matrix.jsonl` | Lv の根拠の表。`exam: {asAnswer, asTool}`・`asToolNow`・`lvBy.exam` は **null**（入試は Lv の規則に入れない＝§7-2） | 892行 |
| `qa/questions.json` | 892項目（org 354／inorg 160／theo 378）。`evidence.exam` は古い値のまま。**`priority` 欄はまだ無い** | |
| `reference-src/*.md` | 143ページ中140が前書きに `codes:` を持つ → **892コードすべてがどこかのページに属する** | 本文の「覚える」系の文 約690・「覚えなくてよい」系 約200（正規表現での粗い数え） |

### 1-2. 生成の流れ（いま）

```
problems.jsonl ─┬─ routes → moves_qa_map.json ──→ 「手筋」経由のコード
                └─ problem_qa_map.json ─────────→ 「題材」経由のコード
exam_answer_type.jsonl（Gemini の判定・上位50コード） → asAnswer
          ↓ build-exam-usage.js（入試側・--write で chem の qa/data へ書く）
qa/data/exam_usage.jsonl → qa/app.js（?v= で読む）・qa/tests.js・level_matrix の asToolNow
```

### 1-3. どこで使われているか

- `qa/app.js` `usageHtml`・`usageDetailHtml` … こたえの下の帯（「◯年入試の有機◯問中 N問」・★以上・解くのに要った・直接問われた）と内訳。**`_label` を画面の文言に使い、直書きしない**（既存の良い決め）
- `qa/tests.js` `runUsageTests` … 出どころ行・コードの実在・漏洩の検査／`asToolNow` の検査
- `tools/verify-release.js` … `data/exam_usage.jsonl?v=NN` の版の同期
- `DESIGN_reference_book.md` の「選定の物差し」（大問の重なりを引いてから見る）・`assembler/learn.js`・`narrowing.js` のコメント

### 1-4. ⚠ 土台の欠陥（この検収で見つけたもの。直すのは入試側・報告だけ）

1. **判定の年の取り違え**。`build-exam-usage.js` は判定を `code|printed` で引く。ところが **printed は巻をまたいで同じ文字列が 213 ある**（「東北大・前3」は 2020〜2022・2024年版に出る）。
   結果、**2021年入試ぶんの判定が 2020年入試の同名の大問に 75組（コード×大問）付いている**（例: `org.anal.molecular-formula` の東北大・前3）。→ 鍵は**レコードの id** にする（§4-3）。
   ★ Fable 検収（2026-09-25）: 検算した。判定 1,579 行のうち **221 行は、同じ printed が CENSUS 内の別の入試年にも在る**（`exam_answer_type.jsonl` は `year` を持っているのに、鍵に入れていない）。
   いまの画面の「直接問われた」の数はこの取り違えを含む ＝ **段0 の最初に直す**。応急には鍵を `code|printed|year` にすれば止まるが、正しくは id
2. **出どころ行が古い**。`_readme` は「2022年版（2021年入試）の有機…140/140」のまま、`_label` は「2020・2021年入試の有機283問中」。文を手で書いている箇所が残っている
3. **レコードを大問として数えている**。`daimon_key` は「統計はこれで重ねずに数える」と定義済み（`fields.json`）なのに、分母も分子もレコード数（全体で 1,110 対 911）
4. **CENSUS が `[2021, 2022]` のまま**。8巻が悉皆になった
5. **「手筋」経由の数が巻によって欠ける**。ルートの無い3巻と、対応表に無い102手筋。8巻へ広げると、手筋経由の数は**巻の被覆のむら**をそのまま拾う
6. `exam_answer_type.jsonl` に出どころ行（何を・誰が・いつ判定したか）が無い
7. ★ Fable 検収（2026-09-25）: **`daimon_key` が全レコードに付いていない**（348/1,110）。§3-1 の「`daimon_key` で畳む」は、**無いレコードは1大問**という仮定の上に立つが、枝番だけ違う組が 15組・31レコード見つかった（§1-1）。
   → 入試側に **「分けたレコードには必ず `daimon_key`」を `check-db.js` の検査にし、既存の 762 を一度通す**ことを頼む（`check-splits.js` が近い）。これが済むまで分母は「約911」
8. ★ Fable 検収（2026-09-25）: **事実の表が chem 側に置かれている。** `qa/data/exam_answer_type.jsonl`（Gemini の判定＝事実）を `build-exam-usage.js` が読みに来ている。
   §0 の持ち主の区分（事実は入試側）と逆で、chem 側に出どころ行の無い事実が1つ残る形。→ §2-3 の `qa_roles.jsonl` へ**移して chem 側から消す**（§4-6）

---

## 2. 知識項目ごとに持つ欄

### 2-1. 事実の単位は「大問 × コード × 役割」の1件

**集計（出題数・率）は結果で、正は1件ずつの事実。** 集計だけ持つと、判定を足すたびに数え直しの根拠が消える
（`evidence.exam.asTool` が 206件古くなったのはこの型。`qa/DESIGN_difficulty_frequency.md` §9-5）。

**役割（role）の段 —— 既存の3つの欄を1本の段に並べる**

| 役割 | 意味 | いまの出どころ | 暗記への効き |
|---|---|---|---|
| `asked` | 答案に書かせた（名称・反応式・構造を答えさせた） | `exam_answer_type` の `asAnswer: true` | **覚える**の強い証拠 |
| `needed` | 解くのに要り、**問題文に説明が無い** | `knowledge_required`（手筋 id 単位） | **覚える**の証拠 |
| `given` | 解くのに使うが、**問題文が型を説明・与えた** | `explained_in_text`（手筋 id 単位） | **覚えなくてよい**の陽性の証拠 |
| `used` | 解くのに使った（与えたか未判定） | `via: "手筋"` | 道具としての重み。暗記の要否はまだ言えない |
| `appeared` | 題材として紙面に出ただけ | `via: "題材"`（機械のキーワード照合） | 弱い。**暗記の根拠にしない** |
| （判定なし） | 上のどれとも判定していない | — | **0 と書かない**（既存の決めを維持） |

★ 記憶 exam-explained-vs-memorized の実例がこの段の必要を示している: **過マンガン酸の C=C 開裂は6大問中5で型が明示（暗記率17%）、オゾン分解は16大問中7が説明なし（44%）**。
同じ「出た」でも、`given` と `needed` で言えることが逆になる。

★ Fable 検収（2026-09-25）—— **段の切り方は妥当。`asked` と `needed` を分けるのは要る**（「名称を書かせた」は暗記の直接の証拠、「説明なしで使った」は間接の証拠で、§5-3 の区分が別々に引く）。決めを3つ足す:

- **段は「階段」ではなく「重なってよい札」**。1つの大問で同じコードが `asked` と `given` の両方になることがある（反応式は問題文が与え、生成物の名称を答えさせた）。事実の表は**1組1行で役割ごとに行を分ける**（§2-3）。
  集計で1大問を1回だけ数えるときは **強い順 `asked` > `needed` > `given` > `used` > `appeared`** で代表させる。この順は道具と画面が共有する1か所に置く
- **`used` は「手筋経由の全部」で、`needed`／`given` はその中の判定済み**。だから `n.used ≥ n.needed + n.given`。`used − (needed + given)` が「解くのに使ったが、与えられたかは未判定」の数。画面はこの差を**未判定**として出す（0 と書かない）
- **`appeared` は数に入れるが、線の根拠には使わない**（機械のキーワード照合。§2-4 の `conf` で出どころを分ける）

### 2-2. 判定を手筋の単位で持つか、コードの単位で持つか

- **案A**: いまのまま手筋 id で持ち（`explained_in_text`／`knowledge_required`）、`moves_qa_map` でコードへ写す
- **案B**: **コード単位の判定**を正として持つ。案A の写しは機械の下書き（候補）にする
- **推奨: B**
- **理由**: 1つの手筋が複数のコードにぶら下がるので、写すと兄弟のコードが同じ判定になる。`build-exam-usage.js` 自身が「判定のある50コードのうち22コードが7つの束」「フェーリングだけ名前を問われた問題は原理的に表現できない」と書いている。
  「試薬が違えば手筋を分ける」（過マンガン酸を分けた判断）と同じ理由で、**暗記の線はコードの単位でしか引けない**
- ★ Fable 検収（2026-09-25）—— **B で確定。ただし「判定の手間」は、下書きの出どころを役割ごとに変えると1桁減る**（実測: 手筋経由の 大問×コード は 5巻で **13,092組**、大問×手筋 なら **5,364組**。題材は 15,804組）:

  | 役割 | 機械の下書き（`src: 機械`）の出どころ | 読み・人の判定が要る組 |
  |---|---|---|
  | `asked` | **`answers[].what`（設問が何を答えさせたか・1,048/1,110 に埋まっている）と `asks`** をコードの語彙で照合 | 照合が当たった組の確認だけ。当たらなかった大問は「判定なし」のまま |
  | `given`／`needed` | **手筋の単位**で持つ `explained_in_text`／`knowledge_required`（いまの欄のまま）を `moves_qa_map` でコードへ写す | **「問題文が与えうる手筋」に限る**。`moves.json` に **`givable`（問題文が型を与えることがあるか）** を1回付ける（271手筋・1便）。元素分析・不飽和度のような「与えようがない手筋」は判定の対象から外れ、`needed` の既定にもしない（＝判定なし） |
  | `used`／`appeared` | いまの `via` | 要らない |

  - **コード単位の行（`qa_roles.jsonl`）が正**で、手筋から写した行は `src: 機械`。兄弟コードの片方だけ違う（フェーリングだけ名前を問われた）ときは、**読み・人がコード単位の行で上書き**する（`src` が強いほうが勝つ: 人 > 読み > 機械）。これで案A の欠点（兄弟が同じになる）は「上書きできる」形に変わる
  - 判定の順は **出題数の上位コードから**（ユーザーの決め §6-1 の 5）。全部に付けなくても、`conf` と「判定なし」で**付いていないことが読める**のが要件

### 2-3. 事実の表（非公開・入試側の持ちもの）—— 足す

`_解析/db/qa_roles.jsonl`（名前は仮）。1行 = 1組。

| 欄 | 中身 |
|---|---|
| `id` | 大問レコードの id（**printed ではない**。§1-4 の1） |
| `daimon_key` | 数えるときの大問の鍵 |
| `code` | qa のコード |
| `role` | §2-1 の段 |
| `q` | 設問の葉の id（`answers[].q` と同じ形・任意）。どの設問でそうなったか |
| `src` | `機械`（キーワード・手筋の写し）／`読み`（Gemini・Claude が本文で判定）／`人` |
| `date` | 判定した日 |
| `move` | ★ Fable 検収（2026-09-25）: 任意。`src: 機械` の行が**どの手筋から写されたか**（`moves.json` の id）。上書きのときに「何を上書きしたか」が引ける |

★ Fable 検収（2026-09-25）: **いまの `qa/data/exam_answer_type.jsonl`（1,579行・50コード・2021年入試）はこの表へ移す**（`asAnswer: true` → `role: asked`・`src: 読み`・`date: 2026-08-13`。`false` は「asked でない」という判定なので **`role: not-asked`** として残す ＝ 「判定した結果 asked でなかった」と「判定していない」を分けるため。`null` は落とす）。
鍵は `year` が付いているので `printed + year → id` で一意に引ける。移したら chem 側のファイルは消す（§4-6）

- **案**: `problems.jsonl` に欄を足す／別ファイル → **推奨: 別ファイル**。
  理由: `problems.jsonl` は 20MB・並列レーンで壊れやすい（PLAN の地雷4）・判定は巻の取り込みと別の時期に積み上がる。欄の定義は `fields.json` と同じ手続きで書く
- **本文を入れない**（`check-no-prose-leak.js` の対象に足す）

### 2-4. 公開の集計（qa/data 側）—— 欄の一覧

| 欄 | 中身 | 土台 |
|---|---|---|
| `code` | qa のコード | そのまま |
| `pop` | 母集団の id（§3-2。二次と共通テストを分ける） | **足す** |
| `n` | 大問の数（`daimon_key` で畳む）を役割の段ごとに: `asked`・`needed`・`given`・`used`・`appeared`・`judged` | **作り直す**（いまの `count`・`asToolCount`・`asAnswerCount`・`judgedCount` を置き換える） |
| `years` | 入試年ごとの大問の数を**役割ごとに**（`{asked: {2019: 3, …}, used: {…}}`）→ 「8年中 k年」が引ける。★ Fable 検収（2026-09-25）: **年ごとの分母は宣言（§3-2 の `daimonByYear`）が持つ**ので、ここは分子だけ。分母を持たないと §3-7 の「窓は表示で切る」ができない | **足す** |
| `univ` | 大学の数。★ Fable 検収（2026-09-25）: **「大学×学部×日程」は当面出さない** —— レコードに学部の欄が無く、`printed`（「慶應大・薬5Ⅱ」）から切り出すのは壊れやすい。要るなら入試側に欄（`faculty`）を頼んでから | **足す** |
| `covered` | ★ Fable 検収（2026-09-25）: **役割ごとに「判定が及んでいる入試年」**（`{used: [2019,2020,2021,2022,2026], asked: [2021]}`）。ルートの無い3巻（2023〜2025年入試）は `used` の被覆から外れる。これが無いと「8年中 k年」の k が被覆のむらを拾う（§1-4 の 5） | **足す** |
| `difficulty` | 本の印（○・無印・★・★★）の分布 | そのまま（数え方だけ大問単位へ） |
| `basis` | 根拠の大問 id と役割の列 | **作り直す**（いまの `problems[]` は大学名・印字を毎回くり返して 1.39MB） |
| `conf` | 判定の出どころの内訳（機械／読み／人）と、巻の被覆（ルートの無い巻がどれか） | **足す** |
| `sharedWith` | 判定が兄弟コードと同じ（束）のとき、その兄弟 | そのまま（`answerJudgmentSharedWith`） |
| 出どころ行 | 母集団の宣言（§3-2）・生成日・`_label`・**`_schema: 2`** | **作り直す**（文を宣言から生成し、手書きをなくす） |

**確からしさ（`conf`）を必ず持つ理由**: `appeared` は機械のキーワード照合で、表記ゆれにも一律の付け足しにも弱い
（PLAN「`build-qa-map.js` の MAP に一律でコードを足さない」＝ 足すと頻度が水増しされる）。
画面が「◯問」と言うとき、その数がどの出どころの足し算かを読めるようにしておく。

---

## 3. 母集団と率の定義

### 3-1. 分母は大問（`daimon_key`）

- レコードは取り込みの都合（1大問を「3問1」「3問2」に分けた）で、出題の単位ではない
- 分子も分母も **`daimon_key` で畳んだ大問**で数える（いま 911）
- `solvable: false`・`out_of_scope` の扱いは宣言に書く（出題されたことは事実なので、**出題の分母には残す**のを推奨。解けたかどうかの分母から外す）
- ★ Fable 検収（2026-09-25）: 上の2つは**残すで確定**（実数は `solvable: false` 4・`out_of_scope` 1。分母の 0.5%）。
  **前提として `daimon_key` が全レコードに付いていること**（§1-4 の 7）。付くまでは「約911」と言い、率は出さない

### 3-2. 母集団は「宣言」として持つ —— 足す

率を出してよいのは、**悉皆だと宣言した範囲の中だけ**（記憶 exam-data-use-loaded-assets:「統計は範囲を確定してから」）。
宣言を1つのデータにし、帯の文言も分母もここから作る。

| 欄 | 例 |
|---|---|
| `pop` | `niji-org`（二次・有機） |
| `books` / `years` | 2020〜2027年版 ／ 2019〜2026年入試 |
| `field` | 有機（のちに無機・理論） |
| `daimon` | 911 |
| `source` | 『全国大学入試問題正解 化学』の収録分に対して悉皆 |
| `frozenAt` | 範囲を確定した日 |
| `daimonByYear` | ★ Fable 検収（2026-09-25）: **入試年ごとの大問の数**（収録校は年で変わる: レコードで 125〜161）。§2-4 の `years` の分母・「直近5年」の窓の分母はここから |
| `covered` | ★ Fable 検収（2026-09-25）: 役割ごとに判定が及んでいる年（母集団の側にも書く。コードの側の `covered` と同じ値の写しでなく、**こちらが正**でコード側は導出） |

★ 有機はいま **2026年入試まで入った**ので、記憶の条件（2026 入試まで取り込んでから）は満たしている。
**確定の宣言を出すかどうかはユーザーの判断**（§6-1）。

### 3-3. 何を数えるか —— 3つの物差し

| 物差し | 形 | 強み | 弱み |
|---|---|---|---|
| 大問の率 | 911大問中 N | 分かりやすい | 学部の多い私大（慶應・早稲田・東京理科）に重みが寄る |
| **年の出現** | **8年中 k年** | 「毎年出る」と「その年に出た」を分けられる（記憶: 年を増やした目的） | 大学の入れ替わりに鈍い |
| 大学の数 | N大学 | 一部の大学の癖を薄める | 分母（年ごとの収録校）がぶれる |

- **推奨**: 画面の主は **件数＋年数**（「8年のうち7年・のべ42大問」）。率は添える程度
- **理由**: 収録校は年で入れ替わり（75大学）、学部の数で大問が増える。率だけ出すと、本の収録方針が数に化ける

### 3-4. 偏りとして書いておくこと

- **本に対して悉皆で、入試全体に対して悉皆ではない**（収録校の選び方は本の側）
- 「出題された」と「受験者が解いた」は別（SCHEMA §2b）。`answer_required` は 52/1,110 しか埋まっていないので、**当面は「出題された」だけを分母にする**
- 大問の粒度は大学でちがう（小問集合の大問は1つで多くのコードに触れる）
- 大学別の分析は年数そのものが精度（記憶 exam-db-expansion-plan）。**8年では大学別の率は出さない**のを推奨

### 3-5. 共通テスト —— 別の母集団

- 原典: R6〜R8 は `問題集/共通テスト/`（センター公式）・R3〜R5 は本から（決定済み）。**取り込みはまだ無い**
- **別の宣言**（`pop: kyotsu-kagaku`・`kyotsu-kiso`）。**合算しない**（README の決め）。「共通テストでは出ないが二次では頻出」の差そのものが生徒に有用
- 座標は「年・本試/追再・科目・第N問」。大学名が無いので `printed` を前提にした道具（`census.js`）は別に組む
- 化学基礎の知識（I-0138 の同素体・I-0048 の半反応式）は**ここが主な母集団**になる

### 3-6. 有機以外への広げ方（全問題化と噛み合わせる）

- 方針: 最終的に理論・無機も含めた全問題（2026-09-23 ユーザー判断）
- **母集団は分野ごとに別の宣言**。コードの分母は**そのコードの分野の母集団**
- 他の分野の大問に出たもの（有機の大問の中の熱化学など）は、**別欄の件数**として持ち、率に混ぜない
- 順: **有機8年（いま）→ 共通テスト（小さい・1便未満）→ 無機（qa が効く）→ 理論（手筋が効く）**（PLAN_単元展開 の第0段・第1段・第2段に合わせる）
- ⚠ 無機は「覚えているかで決まる」（PLAN）ので、**暗記の線の根拠として最も効くのは無機**。有機で形を固めてから広げる
- ⚠ 記憶 exam-db-expansion-plan は全問題化を「台帳 I-0108」と書くが、**いまの台帳の I-0108 は作図器の別件**（印の文字の重なり・done）。
  ★ Fable 検収（2026-09-25）: 全問題化の id は **I-0147**（`issues.js find 全問題` で1件・open・担当 入試問題取り込み作業）。記憶のほうを直す

### 3-7. 窓（直近5年か、全部か）

- README が決めずに残している論点（「移動窓にすると 2019・2020年入試が分母から外れる」）
- **推奨**: 集計は**年ごとの列**（§2-4 の `years`）で持つ。窓は表示の側で切る ＝ データの形はどちらでも変わらない
- 既定の表示は「集まったぶん全部（8年）」。直近5年は必要になったら表示で足す

### 3-8. 小さい数と0の扱い

- 1〜2件は「出た例がある」まで（例の大問を示す）
- 0件は「取り込み済みの範囲では見つからない」まで。**「出ない」とは言わない**（§7-3「出ていない＝優先度が低い、とは言わない」）
- ★ **「覚えなくてよい」の根拠は0件ではなく、陽性の証拠を要件にする**:「出た N大問のすべてで問題文が型を与えた（`given`）」。
  例: ¹⁴C の半減期は求める問題でなければ与えられる（千葉工大2020・中央大2022・I-0146 の note）
- ★ Fable 検収（2026-09-25）—— 統計としての筋は通っている。言い方の要件を2つ足す:
  - 「覚えなくてよい」は **`given` が複数年**にあり、かつ **`asked` が 0 かつ `needed` が 0**（判定済みの範囲で）のときだけ。`given` だけ見て `asked` を見ないと、「反応式は与えられるが名称は書かせる」型を取りこぼす。閾値（何大問・何年）は §5-3 のとおり分布を見てから
  - 率を添えるときは**同じ役割の被覆（`covered`）の年だけで割る**。`used` は5年ぶん、`appeared` は8年ぶんなので、同じ「8年中」に並べると被覆のむらが率に化ける

---

## 4. 生成と更新

### 4-1. 持ち主（再掲）

**事実の表と生成の道具は入試側。** この節の 4-3 は**入試側への発注の中身の案**で、このレーンは道具に手を出さない。
chem 側が持つのは**公開の形の契約**（この文書の §2-4）と、**qa・参考書の検査**。

### 4-2. 公開のファイルの分け方 —— 作り直す

いま 1.39MB（2年・276コード）。8年・892コードに広げると**1桁増える見込み**で、qa の画面が起動時に丸ごと読む形は持たない。

| ファイル（名前は仮） | 中身 | 読むとき |
|---|---|---|
| `qa/data/exam_usage.jsonl` | 出どころ行（宣言）＋ コードごとの集計（§2-4 から `basis` を除いたもの） | 起動時（いまと同じ・`?v=` もいまと同じ） |
| `qa/data/exam_problems.jsonl` | 大問の辞書: id → 大学・入試年・印字・日程・本の印 | 内訳を開いたとき |
| `qa/data/exam_basis/<分野>.jsonl` | コードごとの根拠の列（大問 id と役割だけ） | 内訳を開いたとき |

- **推奨**: ファイル名 `exam_usage.jsonl` は残す（`app.js`・`tests.js`・`verify-release.js` がすでに配線済み）。中身の形は `_schema: 2` で区別
- ★ Fable 検収（2026-09-25）: **分野で分ける（単元では分けない）で確定**。見積り: いまの 1.39MB は `problems[]` が大学名・印字・手筋名を組ごとにくり返しているのが原因。
  v2 で集計だけにすると `exam_usage.jsonl` は 892行 × 約300B ≈ **0.3MB**、大問の辞書は 911 × 約150B ≈ **0.15MB**、根拠の列は（手筋経由 13,092組 → 8巻で約2万組 ＋ 題材 15,804組）× 約40B ≈ **1.5MB**。
  分野で分けても有機が1枚で 1.5MB、無機・理論が入っても各1枚 ＝ 開いたときだけ読むなら単元まで割る理由が無い。⚠ 根拠の列は **id と役割の記号だけ**（大学名を持たない）にするのが大きさの前提
- ★ Fable 検収（2026-09-25）: `app.js` が読む新しいファイルの `?v=` は、qa/tests.js の「app.js に出てくる `?v=` を全部見る」検査がそのまま拾う（名指しでない）＝ 新しい検査は要らない

### 4-3. `build-exam-usage.js` の広げ方（入試側への発注の案）

1. CENSUS の配列を捨て、**§3-2 の宣言から母集団を作る**。`_readme`・`_label` も宣言から生成する（手書きの文を残さない）
2. **`daimon_key` で数える**
3. 判定は **レコードの id で引く**（`code|printed` をやめる。§1-4 の1）
4. 役割の段（§2-1）を出す。`qa_roles.jsonl` が無い組は `used`／`appeared` まで
5. **巻の被覆を出す**: ルートの無い巻では「手筋」経由を**判定なし**として扱い、0 と数えない
6. 検査: 否定対照を1つ（判定を printed で引く旧い形に戻すと赤くなる、など）

### 4-4. 再生成の手順

1. 入試側が下見（`--write` なし）で要約の差分を出す
2. 入試側が `CHEM_REPO=<chem の作業ツリー>` で `--write`（chem の本流へ直に書かない）
3. chem 側: qa の版上げ（`qa/app.js` の `exam_usage.jsonl?v=`・`questions.json?v=`・`index.html`・`test.html`）→ `node tools/run-tests.mjs` で qa/test.html → `node tools/verify-release.js`
4. ~~参考書に根拠を出す段になったら: `node tools/gen-reference.mjs` → assembler の版上げ。参考書の根拠を `gen-reference.mjs` が `qa/data` から作る形にすれば `REF17` が同期もれを止める~~
   ★ Fable 検収（2026-09-25）—— **この案は `REF17` の作りと合わないので採らない。** `REF17` はブラウザで `reference-src/*.md` だけを `tools/reference-md.js` で組み直して `reference.json` と1バイト比較する（qa/data は読まない）。
   数字を `reference.json` に焼くと、`REF17` にも同じ焼き込みを入れて qa/data を読ませることになり、**「物差しを2本にしない」の決め（REF17 の注記）に触れる**うえ、入試データが更新されるたびに reference.json の再生成と assembler の版上げが要る（二重持ち＋版ずれの芽）。
   → **数字は `reference.json` に入れない。参考書の画面（`learn.js`）が `../qa/data/exam_usage.jsonl` を開いたときに読んで描く**（§5-1 の案 a′）。`reference.json` と同じく `cache: 'no-cache'` で読み、`?v=` の規則の外に置く（verify-release の `isServedPath` の注記どおり）。
   入試データの更新で動くのは qa の版だけ。参考書の本文も生成物も1バイトも変わらない

### 4-5. 検査（足すもの）

| どこ | 何を |
|---|---|
| `qa/tests.js` | 出どころ行に宣言（巻・入試年・大問数・日付）がある／`_label` が宣言から導ける／集計の数が根拠の列から数え直した値と一致／**同じ大問 id が1コードに2度出ない**／判定の無い組に数を出していない（既存）／漏洩（既存の LEAK を維持） |
| `_解析/tools/check-db.js`（入試側） | `qa_roles.jsonl` の id・コードの実在・役割の値 |
| `verify-release.js` | いまの `?v=` の規則のまま（新しいファイルを `app.js` が読むなら、その `?v=` も対象に入るか確かめる） |

### 4-6. `questions.json` の `evidence.exam` と `level_matrix` の `asToolNow`

- 同じ数を2か所に持っていて、**すでに206件が古い**（§9-5）
- **案A**: `evidence.exam` を `exam_usage` から生成し直す便を決まった手順にする／**案B**: `evidence.exam` をやめ、`exam_usage` を正にして `level_matrix` はそこから読む
- **推奨: B**（二重の持ち方をやめる。入試は Lv の規則に入っていないので、Lv の表が持つ理由が薄い）
- ★ Fable 検収（2026-09-25）—— **B で確定。波及は次の6か所**（読んで確かめた）:

  | どこ | いま | 直し |
  |---|---|---|
  | `qa/questions.json` | 781項目に `evidence.exam: {asAnswer, asTool}` | `exam` を消す（`textbook`・`seminar` は Lv の根拠なので残す）。整形済み JSON なので差分は約3,000行だが構造どおり |
  | `qa/tools/build_evidence.js` | `exam_answer_type.jsonl`・`exam_usage.jsonl` を読んで `evidence.exam` を書く | 入試の部分を外す（教科書・セミナーだけ） |
  | `qa/tools/gen_level_matrix.js`・`level_rules.js` | `asToolNow`（古い値との差）・`countAsTool` | `asToolNow` と `lvBy.exam` を落とす。入試は表に持たない（§7-2「規則なし」のまま） |
  | `qa/tests.js` 「入試: 手筋の数が古い行には…」 | 二重持ちの同期を見る検査 | 消して、代わりに **「集計 = 根拠の列から数え直した値」**（§4-5）を置く |
  | `qa/tools/verify_new_evidence.js` の B | `exam_answer_type.jsonl` を `code+printed` で突き合わせ | B を退役（判定は入試側の `qa_roles.jsonl` に移る・§2-3） |
  | `qa/data/exam_answer_type.jsonl` | chem 側に事実が置かれている（§1-4 の 8） | 入試側へ移して消す |

- ★ **`priority`（§7-3）も `questions.json` に持たない。** 表示側が `exam_usage` v2 から出す（§7-3「当面は回数をそのまま持ち、表示側で判断する」と同じ）。
  閾値を決めた日に `qa/tools/priority_rules.js`（`level_rules.js` と同じく道具・画面・テストが共有する1か所）へ書く。`questions.json` に写すと `evidence.exam` と同じ古び方をする

---

## 5. 見せ方

### 5-1. 参考書 —— 線は本文、根拠は器

**衝突する2つの決め**
- ユーザー（2026-09-25）: 出題率をもとに、どこまで暗記すべきかを**根拠とともに示す**
- `REFBOOK_STYLE.md` §8-7: 「入試で頻繁に出る」とは書かない。**頻度は主観表現で書き、統計・出典の形にしない**

**解き方の案**（§10-3 の「器が言うこと／本文が言うこと」の分担をそのまま使う）

| 案 | 形 | 本文の書きぶり（§8-5・§8-7） | 手間 |
|---|---|---|---|
| a | 節の末に**畳んだ「入試での出方」**を **`reference.json` に焼く**（生成物）。コードごとに「8年中 k年・のべ N大問・うち答えさせた a／説明なしで要った b／問題文が与えた c」 | 変えない | `gen-reference.mjs` に囲みを1つ。⚠ `REF17` にも同じ焼き込みが要る（§4-4 の 4） |
| **a′** | ★ Fable 検収（2026-09-25）: 同じ見た目を、**`learn.js` が開いたときに `qa/data/exam_usage.jsonl` から描く**。原稿にも `reference.json` にも数字を入れない | 変えない | `learn.js` に描画を1つ（qa の `usageHtml` と同じ材料・同じ `_label`）。`REF17`・生成器は触らない |
| b | 線の文の後ろに小さな札（「根拠」） | 文に札が付く | 原稿の書式が増える（書式は3つだけ、の決めに触れる） |
| c | 参考書には出さず、qa の該当項目へリンク | 変えない | 小さいが、根拠が参考書から見えない |

- ~~**推奨: a**~~ → ★ Fable 検収（2026-09-25）: **推奨: a′**（a の見た目・a の分担のまま、焼かない）
- **理由**: 本文は作者の声のまま（言い切り・主観表現）で、数字は**器が機械で出す**ので「統計の形で書く」ことにならない。§10-6 の「畳んで置く」とも合う。原稿の書式を増やさない。
  a′ にする理由は §4-4 の 4（`REF17` と二重持ち）。数字の正は `qa/data/exam_usage.jsonl` の1か所で、qa の画面と参考書が同じ行を読む
- ★ Fable 検収（2026-09-25）—— REFBOOK_STYLE との両立は次のとおり:
  - **§10-3 の表に1行足す**と読む: 「何回・どう出たか」→ **器**（生成の囲み）。§8-7「統計の形にしない」は**本文の文**への決めで、器はもともと「どこから来た話か」を言う場所。ユーザーの了承は要る（§6-1 の 2）
  - **§10-6 の畳む条件をそのまま当てる**: 畳んでも「札・題・1行の要約」が見える／ページ内検索で開く／印刷は開く。要約の1行は `_label` から機械で作る（「2019〜2026年入試の有機 約911大問での出方」）
  - **置き場所は「節の末」**: `:::section` は `codes` を持てる（I-0143・`reference-md.js` の section の `codes`）ので、節に `codes` があれば節の末（「この節の一問一答」の口の隣）、無ければページの末に `codes:` ぶんをまとめて。**原稿の書式は増えない**
  - §8-5「線は本文の主役の位置」は動かない。囲みは線の**あと**に置く根拠で、線そのものを囲みに退避させない
  - 数字を「よく出る」「頻出」などの主観語に**言い換えない**（それは本文の仕事。§13-2 の「数で束ねた決まり文句」にも当たらない —— 数そのものが中身）

### 5-2. 線と根拠をどう結ぶか

- **案1**: 原稿の線の文ごとに印（新しい書式）
- **案2**: ページの前書き `codes:` を節の単位に割る
- **案3**: **線の台帳**（`qa/data/memorize_line.jsonl`・名前は仮）。1行1コード: 機械の案・人の決め・理由・日付・根拠の要約の写し
- **推奨: 3**（＋ 節とコードの対応は既存の `codes:` から引く）
- **理由**: `level_matrix.jsonl` の `override` と同じ形（**機械の案＋人の決め＋理由**。道具を回しても人の決めが戻らない関所）が既にあり、形を流用できる。
  ユーザーは原稿を自分で校正するので、原稿の文を印で縛ると校正のたびに印が壊れる
- ~~**未決**~~ ★ Fable 検収（2026-09-25）: **候補を出して人が読む、で確定**（機械で赤にしない）。台帳の形も決める:
  - 置き場所: **`qa/data/memorize_line.jsonl`**（chem 側。`level_matrix.jsonl` の `override` と同じく**人の決めだけを手で書く**表）。1行1コード: `code`・`machine`（§5-3 の規則が出した区分・生成日）・`decision`（人の区分）・`reason`・`date`・`page`（原稿の id）
  - 検査（qa/tests.js）: `machine` は `exam_usage` v2 から計算し直した値と一致（表が古くない）／`decision` が `machine` と違う行には `reason` がある（`level_matrix` の「食い違いには上書きの記録」と同じ約束）
  - **区分の規則は `priority_rules.js` に置く**（§4-6）。線の区分と qa の `priority` は同じ材料から出るので、規則を2か所に持たない

### 5-3. 線の区分（案）

| 区分 | 根拠の形（片側の論法・§7-2 と同じく強い信号だけを採る） |
|---|---|
| 覚える（書かせる） | `asked` が複数年 |
| 覚える（説明なしで使う） | `needed` が複数年 |
| 使えればよい（問題文が与える） | `used` の大半が `given` |
| 覚えなくてよい | **出た大問のすべてで `given`**（陽性の証拠・複数年）**かつ `asked`・`needed` が 0**（★ Fable 検収 2026-09-25・§3-8）。「出題が極めて少ない」だけでは言わない（0件で「出ない」と言わないのと同じ線） |
| 言えない | 判定が無い・件数が小さい・**被覆（`covered`）が2年に満たない** |

- **閾値は先に決めない**（難易度の設計書 §7-3・§3-5 の轍）。有機8年で分布を見てから決める
- ★ **網羅（載せるか）には使わない**（§2-7。統合側が一度取り違えた）

### 5-4. qa の画面

- 帯の文言は宣言から生成した `_label`（いまの仕組みをそのまま使う）
- **Lv（`difficulty`）には入れない**（§7-2 を維持）。入試は `priority` の軸にだけ効かせる
- 「直接問われた」の表示を役割の段（答えさせた／説明なしで要った／与えられた／題材）に置き換える
- 判定の無い組を 0 と書かない（既存の決めを維持）

### 5-5. 著作権

- 公開物に出すのは**大学名・入試年・印字・本の印・役割・手筋の名前**まで（いまの `LEAK` 検査を維持）
- 大問 id（「2022-東京大学-1I」）は事実の組み合わせなので公開してよいと見るが、**Fable の確認を求める**
  → ★ Fable 検収（2026-09-25）: **出してよい**（`printed` と同じ性質の索引情報で、本文も解答も含まない）。ただし2点: ① id の先頭の年は**版の年**（2022 ＝ 2021年入試）なので、**画面には id を見せず**、大問の辞書（§4-2）の `year`（入試年）・大学・印字で表示する。id は鍵としてだけ使う。② いまの `LEAK` 検査は `printed` を外して見ているので、根拠の列（id だけ）と辞書にも同じ検査を当てる（辞書の `printed` は同じ理由で対象外）
- 問題文・本の解答解説は出さない（規則9 のまま）。演習として問題を出す話は別（`clearance`・`POLICY_past_exams.md`）

---

## 6. 未決の点と検収の点

### 6-1. ユーザーに決めてもらう点

★ Fable 検収（2026-09-25）で並べ直した。**1〜3 が決めの要る点、4〜5 は検収の推奨どおりで異論が無ければ進める。**

1. **有機8年（2019〜2026年入試・約911大問）を「範囲確定」と宣言して率を出してよいか**（記憶の条件は満たした。⚠ `daimon_key` が全部に付いてから ＝ 段0）
   - (a) 確定して率を出す ／ (b) 確定は共通テストが入ってから ／ (c) 率は出さず件数と年数だけ（§3-3 の推奨はこれに近い）
2. 参考書の根拠の出し方: **a′（画面が qa のデータから描く・原稿も生成物も触らない）**でよいか。あわせて §10-3 の表に「何回・どう出たか → 器」を1行足す（§8-7 は本文の文への決めのまま）と読んでよいか
   - (a) a′ で進める ／ (b) 参考書には出さず qa へのリンクだけ（案 c） ／ (c) 出し方を実物で見てから決める（段2 で1ページ試作）
3. 「覚えなくてよい」の要件: **陽性の証拠（出たときは与えられている・複数年）かつ答えさせた・説明なしで要った が 0** でよいか（§3-8・§5-3）
   - (a) よい ／ (b) 「与えられている」だけで足りる（`asked` は別に見る）
4. 画面の主を「件数＋年数」にし、率は添える程度にする（§3-3）—— 推奨どおりで進める
5. 判定（役割の段）を出題数の上位コードから順に入れる（§2-2。下書きは機械・手筋に `givable` を1回付ける）—— 推奨どおりで進める

### 6-2. Fable に検収してほしい点

★ Fable 検収（2026-09-25）: **7点すべて答えた**（各節の印の箇所）。

1. 役割の段（§2-1）の切り方 —— 妥当。`asked`／`needed` は分ける。段は重なってよい札・代表は強い順・`used` は判定済みの上位集合、を足した
2. コード単位を正にする判断（§2-2）—— B で確定。下書きの出どころを役割で変える（`asked` は `answers[].what`、`given`/`needed` は手筋の欄＋`givable`）
3. 分母（§3-1）—— 確定。前提に `daimon_key` の完備（§1-4 の 7）
4. 公開ファイル（§4-2）—— 分野で分ける。見積りを書いた
5. `evidence.exam` をやめる（§4-6）—— B で確定。波及6か所を表にした。`priority` も持たない
6. 大問 id（§5-5）—— 出してよい。画面には入試年で見せる
7. 入試側に頼む順 —— §7 の段0 に書いた（年の取り違え → `daimon_key` の完備 → 判定の移設 → `givable`）

### 6-3. 束ねる注文（いまの土台で答えられるか）

| 注文 | 母集団 | 役割 | いまの土台で |
|---|---|---|---|
| I-0138 同素体の「見分けの一言」 | 化学基礎（共通テスト・化学基礎）＋ 二次の理論・無機 | `asked` | **答えられない**（有機しか取り込んでいない）。共通テストの取り込みが先 |
| I-0048 文系に半反応式の暗記が要るか | 共通テスト 化学基礎・化学 | `given` と `needed` | **答えられない**（共通テストが未取り込み）。台帳の note のとおり「率でなく件数」 |
| 元素の検出（`element-detection`）の Cl・S の扱い | 二次・有機 | `asked` と `given` | **件数なら今でも引ける**（`problem_qa_map` のキーワード＋本文の検索）。役割の判定は入っていない |

---

## 7. 進める順（案）

1. **段0（入試側・発注1本）**: §1-4 の欠陥を直す。★ Fable 検収（2026-09-25）の順:
   ① 判定の鍵を id に（いまの画面の数に効いている・応急は `code|printed|year`）→ ② **`daimon_key` の完備**（分けたレコードに必ず付ける検査＋既存 762 の一巡）→ ③ `exam_answer_type.jsonl` を `qa_roles.jsonl` へ移す（§2-3）→ ④ `moves.json` に `givable` → ⑤ `daimon_key` で数える・宣言から文言を生成・`covered` を出す。
   ⚠ ルートの無い3巻（2023〜2025年入試）のルート付けは**この段に入れない**（I-0147 の側の計画）。v2 は `covered` で被覆の穴を明示して出す
2. **段1（chem 側）**: 有機8年で公開の形を v2 にする（役割は機械の下書き＋上位コードの判定）。qa の画面を役割の段へ。`evidence.exam` をやめる（§4-6 の6か所）
3. **段2（chem 側）**: 線の台帳（§5-2 案3）を有機の数ページで試し、参考書の畳んだ囲み（§5-1 案 a′）を1ページで実物にする。ユーザーの決め（§6-1 の 2）はこの実物を見てからでもよい
4. **段3（入試側）**: 共通テストの取り込みと別の宣言（I-0048・I-0138 がここで答えられる）
5. **段4（入試側）**: 無機 → 理論（PLAN_単元展開 の順・I-0147）

---

## 8. 検収の記録（Fable・2026-09-25）

**結論: この骨子の筋で進めてよい。** 事実を1件ずつ持って集計は生成する・分母は大問・母集団は宣言・線は本文で根拠は器・入試は Lv に入れない、の骨はどれも正しい。直したのは「決めを固める」「土台の実測の誤り」「REF17 との噛み合わせ」の3種で、筋は変えていない。

**変えた点**
- §4-4 の 4・§5-1: 参考書の根拠を `reference.json` に焼く案（a）を**やめ、画面が qa のデータから描く a′ に**。`REF17` は原稿だけから組み直す作りで、焼くと物差しが2本になり、入試の更新のたびに参考書の再生成と版上げが要る
- §1-1・§1-4 の 7・§3-1: 「911大問」は `daimon_key` が 348 レコードにしか無い上の仮定と分かった（枝番違いが 15組・31レコード）。完備を段0 に入れ、それまで率は出さない
- §1-4 の 8・§2-3・§4-6: `qa/data/exam_answer_type.jsonl`（事実）が chem 側に在るのを、入試側の `qa_roles.jsonl` へ移して消す。持ち主の境界がこれで一本になる（事実と道具は入試側、公開の形と検査と画面は chem 側）
- §2-2: コード単位が正（B）は維持しつつ、下書きの出どころを役割で変えて判定の手間を1桁減らした（`asked` は `answers[].what` から、`given`/`needed` は手筋の欄＋ `givable` で対象を絞る）
- §2-1: 段は重なってよい・代表は強い順・`used ⊇ needed + given`
- §2-4・§3-2: 年ごとの分母（`daimonByYear`）と役割ごとの被覆（`covered`）を宣言に。これが無いと「窓は表示で切る」「8年中 k年」が成り立たない。`univ` は大学の数だけ（学部の欄が無い）
- §3-8・§5-3: 「覚えなくてよい」は `given` 複数年 **かつ** `asked`・`needed` が 0
- §4-6: `priority` も `questions.json` に持たない（規則は `priority_rules.js` の1か所）
- §5-2: 線の台帳の置き場所・欄・検査を決めた
- §3-6: 全問題化の台帳 id は I-0147

**残した点（そのまま）**: 事実の表を `problems.jsonl` と別ファイルに／共通テストは別母集団で合算しない／画面の主は件数＋年数／0件で「出ない」と言わない／網羅には使わない／`_label` を画面で直書きしない／閾値は分布を見てから

**ユーザーに決めてもらう点**: §6-1 の 1〜3（範囲確定と率／参考書の出し方 a′ と §10-3 の読み／「覚えなくてよい」の要件）。4〜5 は推奨どおりで進める

**検算に使った実測（2026-09-25・読み取り専用）**: レコード 1,110・`daimon_key` 348・`daimon_key ?? id` の異なり 911・printed の重複 213・ルートのある巻 5（2024〜2026年版は 0）・`answers` 1,048・`explained_in_text`/`knowledge_required` 25・`answer_required` 52・判定 1,579行（50コード・2021年入試のみ・printed が別年にも在る行 221）・手筋経由の 大問×コード 13,092／大問×手筋 5,364・題材 15,804・`solvable:false` 4・`out_of_scope` 1

---

## §6-2 ★★ ユーザー決定（2026-09-25・Fable 検収のあと）

### (1) 母集団 —— 見る年数は「何の率か」で変える
> 2次試験全体は出題範囲は新課程入試になってからがよさそう、大学個別の出題率や、共通テストについてはもっと長く見る

- **2次試験全体の率**: 母集団は**新課程入試になってから**（2025年入試〜）だけ。旧課程の年は混ぜない（範囲が違う年を分母に入れない）
  ⚠ いまは2年ぶんしか無い ＝ 当面は件数＋年数を主に出し、率は年が積み上がってから
- **大学個別の率**・**共通テスト**: **もっと長く見る**（旧課程の年も含める。範囲の差は、項目ごとに新課程の範囲かどうかで見る）
- ⇒ §3 の「宣言」（CENSUS）は**1本ではなく、率の種類ごとに持つ**（2次全体・大学別・共通テスト）

### (2) 見せ方 —— 節ごとの「覚える段階のマトリックス」
> 節ごとに覚えるべき内容の列記と、出題率データ、どこまで覚えればよいかをわかりやすくマトリックスに
> 化学基礎なら共通テストでは、覚えてなくても多分大丈夫なんだけど覚えていると有利になる（選択肢が1つ知識で削れるとか）項目もたくさんある
> 現在qaでは4段階になっているが、その意味付けも分かりやすくユーザーにつたえたい、つまりユーザーが自分はどの段階まで覚えればいいのかをわかりやすく

- 参考書の各節に、**その節で覚える内容（知識項目）× 出題データ × 覚える段階**の表を置く
  - 行 ＝ 節の知識項目（節の `codes:` ＝ v1658 で割り振り済み）
  - 列 ＝ 出題データ（共通テスト／2次全体／大学別 …）と、**覚える段階**（qa の Lv: 1 生存・2 標準・3 受験標準・4 難関）
- **段階の意味づけを読者に伝える**のが主目的: 「自分はどの段階まで覚えればよいか」が表から分かること
  - 例: 化学基礎・共通テストでは「覚えていなくても多分大丈夫だが、覚えていると有利（選択肢を1つ知識で削れる）」項目が多い ＝ **段階の説明に「有利」の層を持つ**
  - ⇒ qa の4段階の定義（qa/DESIGN_difficulty_frequency.md・LEVEL_MATRIX.md）を、読者向けの言葉（どの試験を目指す人がどこまで）に言い直した凡例が要る
- Fable 検収の案 a′（数字は画面が qa のデータから描く・原稿に焼かない）はそのまま。**表の形がマトリックスになる**
- ⚠ 決め (3)「覚えなくてよい」の要件は、このマトリックスの**いちばん下の段に入れる条件**として、マトリックスの設計と一緒に決める（ユーザーに案 A／B を説明済み・未決）

### 次の段
1. マトリックスの試作を1節（案: 化学基礎の1ページ）で作り、見てもらう
2. 段階の凡例（読者向けの言い直し）を下書き
3. 率の種類ごとの宣言（CENSUS）の形を §3 に反映 → 入試側に build-exam-usage.js の発注
