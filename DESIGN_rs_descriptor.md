# 設計書: R/S 判定（CIP）を足す（発注書 第4段 4b）

`video-scripts/ORDER_stereo_puzzle.md` 第4段 4b。**4a（D/L）は v370 で完了**しているので、
残るのはこれだけ。**CLAUDE.md の「化学モデルの正しさが問われる変更」に当たるので Fable 5 の担当**。
この設計書は Opus 側で下ごしらえとして書いた（実装前の判断材料をそろえるのが目的）。

---

## 1. これは方針変更である

R/S は現在、**アプリの「やらないこと」として4か所に明文化されている**（2026-08-02 時点の実在行）:

```
chemistry.js:1288  **CIP の順位付けはしない**（原子番号による優先順位や重複原子の扱いには踏み込まない）
chemistry.js:1370  記述子は CIP を使わず、置換基の rootedFragmentCode の辞書順（＝構成的で…）
quiz.js:640        **CIP（R/S）は使わない**。P12-7 で作った立体コードの比較だけで判定できる
stereo.js:183      R・S の判定（CIP）は実装しない方針なので、ここでするのは姿勢を作ることだけ
stereo.js:453      **CIP の順位付けはしない**（優先順位ではなく、辿って食い違う場所だけを示す）
```

**着手するなら、この4か所のコメントを書き換えるところまでが仕事。**
「やらないと書いてあるのに実装されている」状態を残さない。

## 2. 設計の要点（発注書の指定）

> **正準コード・同型判定に手を入れずに、判定だけを別関数として足せるか**が設計の要点。

これは守れる見込みが高い。理由:

- `canonicalStereoCode` が使う記述子は **`rootedFragmentCode` の辞書順**で決まっており、
  CIP とは独立した「構成的な」基準。**同値関係（同じ分子か）はこれで閉じている**
- R/S は**呼び名**であって同値判定には要らない。4a の `assignDLDescriptor(mol)` が
  すでに「判定だけを別関数として足す」形の前例になっている（chemistry.js:1710 付近）
- したがって **`assignRSDescriptor(mol)` を新設し、正準コード側には一切触らない**のが筋

**逆に、触ってはいけないもの**（触ると同型判定・異性体列挙・メソ体の畳み込みが全部揺れる）:
`canonicalCode` / `canonicalStereoCode` / `computeAtomParity` / `rootedFragmentCode` /
`stereoIsomorphismCompare` / `countStereoisomers`。

## 3. 実装に要るもの（発注書の4点）

1. **原子番号による優先順位**
2. **重複原子の扱い**（二重結合・三重結合を複製原子として展開する）
3. **同順位の掘り下げ**（第1原子が同じなら次の球へ、を再帰的に）
4. **最下位を奥に置いたときの残り3つの回る向き**の判定

### 4 について: パリティはもう読めている

**ここが一番の省力ポイント。** アプリは `readAtomParityFromFischer(mol)` で
**フィッシャーの十字から ±1 のパリティ**を読めている（`fischerSlots` が
上・右・下・左の ref を返す）。`assignDLDescriptor` はこれを使って D/L を出している。

R/S も同じ材料で出せる: **スロットの並び（上・右・下・左）に CIP 順位を割り当て、
最下位を奥（＝フィッシャーでは縦のどちらか）に持っていく偶置換を数えるだけ**。
「最下位が横（手前）にあるときは読みが裏返る」という、D/L の `flipped` と同じ扱いになる。

つまり **1〜3（CIP の順位づけ）が本体で、4 は既存の道具でほぼ片付く**。

## 4. 断定しない場合を決めておく（4a の教訓）

4a では**嘘をつかないことを最優先**にして、次を `null` にした。R/S でも同じ姿勢を取る:

- **順位が決まらない**（掘り下げても同順位のまま＝そもそも不斉でない）
- **フィッシャー投影として読めない図**（主鎖が横向き・軸から外れている）
- **環の中の中心**（ハースは別の規約。相互排他）
- **同位体や擬似不斉が要る場合**（高校範囲外。踏み込まない）

4a で実際に踏んだ落とし穴も引き継ぐ:

- **エステルをカルボン酸と間違えた**（単結合の O の先が炭素ならエステル）。
  CIP でも「その原子の先に何があるか」を見るので、同種の取り違えが起きやすい
- **α炭素で読む規則にしたらグルコン酸が L と出た**（正しくは最下位で読む）。
  規則の当てはめ先を間違えると、答えは出るが**静かに間違う**

## 5. 検証のしかた

**名前で答え合わせができる。** `compounds.json` には D-/L- の付いた化合物が11件あり、
4a はその全件で名前と一致することを ST33 で固定した。R/S も同様に:

- **教科書で R/S が確定している化合物**を数件選び、期待値をテストに直書きする
  （L-アラニンは (S)、D-グリセルアルデヒドは (R)、D-乳酸は (R) など）
- **鏡像にすると必ず反対の記号になる**（`fischerOpSwap` で中心を反転）。
  これは化合物ごとの正解を知らなくても検査できる**強い不変量**
- **既存の同型判定が1つも変わっていない**ことを確認する（ST27・ST28・ST29・ST33 が全部通る）

## 6. 進め方の提案

1. `assignRSDescriptor(mol)` を chemistry.js に新設（4a のすぐ下が置き場所として自然）
2. CIP の順位づけを別の純関数に切り出す（`cipRank(mol, centerId)` のような形）。
   **ここだけを単体で試せる形**にしておくと、重複原子の展開が正しいか検算しやすい
3. テスト ST36 を足す（上の「検証のしかた」の3本立て）
4. **4か所のコメントを書き換える**（1章）
5. UI へ出すのは最後。**入口は増やさない**（アプリ全体の入口見直しが控えているため、
   既存の「🎯 同じ立体はどれ？」の出題か、立体ビューの読み物に足す）

## 7. 引き継ぎメモ

- 版帯は **v440〜v459** を予約する（DEVELOPMENT.md の割り当て表を参照）
- 触るファイルは `chemistry.js` と `tests.js`。**他レーンが style.css・stereo.js・
  reactor.js・game.js・quiz.js を持っている**ので、そこには手を入れない
- 4a の実装（`assignDLDescriptor`）が最も近い前例。**基準炭素の選び方で2回間違えた**
  経緯がコミット `5e66cbb` のメッセージに残っている
