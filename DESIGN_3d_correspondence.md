# 立体対照ビュー 設計書（P7-5 / コンセプト1.1-3）

開発コンセプト「③立体模型との対照：作図はあくまで平面の構造式としつつ、『作図上ではこう書くが、実際の分子はこうなっている』という対照提示ができるとなおよい」を実現するための設計。実装前の合意用ドキュメント。

## 1. 基本方針

- **平面作図の正しさと立体的な実像を混同させない**ことが最重要。パズルの作図・判定には一切影響を与えず、**別枠の「対照表示」**として提供する。
- 外部ライブラリ（three.js等）は導入しない（開発方針2章）。化学の教科書で使われる**くさび形表記（wedge-dash）**を第1段階とし、必要ならSVG/CSSだけで実現できる疑似3Dへ段階的に進む。
- 表示は分子データから**自動生成**し、追加のコンテンツ作成を不要にする（プリセット立体配座のみ例外）。

## 2. 段階案

### M1（推奨・最初に実装）: sp3炭素の局所くさび図

- **何を見せるか**: 選択したsp3炭素1個を中心に、4つの置換基を教科書の慣習で描いた図：
  - 紙面内の結合2本（実線・上下方向）
  - 手前に出る結合1本（黒塗りくさび ▶）
  - 奥に向かう結合1本（破線くさび）
- **置換基の表現**: 単原子ならその元素記号、枝なら簡易式（-CH₃、-C₂H₅、-OH…）を `describeStructure`/断片コードから生成。
- **UI**: 右パネルに「🧊 立体で見る」ボタン → モーダル。左に現在の2D作図（対象炭素をハイライト）、右にくさび図。対象炭素はキャンバスでクリックして選ぶ（sp3炭素のみ選択可）。
- **教育文言**: 「作図では90°に描いていますが、実際のsp3炭素の結合角は約109.5°で、4つの置換基は正四面体の頂点方向に伸びています」。
- **不斉学習との連携**: 不斉炭素を選んだ場合、「4つの置換基がすべて異なる＝鏡像と重ならない」ことを明記。将来的には鏡像体のくさび図を並べて表示（R/S表記の導入口）。

### M2: 分子全体の標準くさび描画

- 主鎖をジグザグ（120°）で描き、各sp3炭素の残り2結合を上下のくさび/破線で自動配置した「教科書式の全体図」を対照表示。
- 正準コード（P8-2）から決定的にレイアウトすれば、ユーザーの描き方に依存しない標準図になる。
- 環はM2では六員環（いす形は描かず平面多角形＋くさび）まで。

### M3（将来）: 疑似3D回転表示

- CSS 3D transform または SVG投影の自作で、棒球モデルを回転させて見せる（依存なしで実現可能）。
- シクロヘキサンの**いす形配座**など、平面作図と実像の差が大きい代表例のプリセット（conformers.json）を用意し、パズル側の該当ステージから参照できるようにする。

## 3. 実装配置

- `stereo.js`（新設）: StereoView クラス。モーダル・くさび図SVG生成。chemistry.js の断片コード/describeStructure を流用。
- chemistry.js への追加は「sp3判定（既存の不斉判定の前提部分の切り出し）」程度に留め、純粋ロジックを維持。
- 検証・スナップ・座標系には一切手を入れない。

## 4. リスク・留意点

- くさび図の**見やすさ**（置換基ラベルの重なり）: M1は局所表示（中心1炭素）に限定して回避。
- **環内炭素**: M1では対象外（選択不可）とし、M2以降で扱う。
- R/S命名は本設計のスコープ外（将来、鏡像図と合わせて検討）。

## 5. マイルストーン（ロードマップ登録用）

- **P7-5-M1** sp3炭素の局所くさび図モーダル（選択UI・自動生成・教育文言・不斉連携）
- **P7-5-M2** 分子全体の標準くさび描画（正準レイアウト）
- **P7-5-M3** 疑似3D回転とプリセット配座（いす形など）

---

## 6. M4: 分子全体の立体ビュー（回せる3D） — 2026-07-27 追加

ユーザー要望「中心炭素を1つ選ばず、分子まるごとを立体で回したい」。
M3（P12-7-M3 で実装済み）は**1つの sp3炭素まわり**の疑似3Dなので、別物として設計する。

### 6.1 中心にある判断: 3D座標をどこから作るか

**キャンバスの作図をそのまま z 方向に持ち上げる案は採らない。**
このアプリの作図は直交格子（結合角90°）が仕様であり、それを立体として回すと
**結合角90°の分子模型**を見せることになる。手書き感覚の平面図としては意図された割り切りだが、
立体として提示した瞬間に「化学的に誤った図」になる。教材としてこれは通らない。

したがって **トポロジー＋既存の立体記述子から、正しい角度で3D配置を組み直す**。
座標は作らず、次の情報だけを使う（すべて既存）:

| 使うもの | 由来 |
|---|---|
| 結合の接続と次数 | `Molecule`（トポロジー） |
| 各 sp3炭素の手性 | `readAtomParityFromFischer` / `readRingParityFromHaworth`（描いた図から読む） |
| 4方向の生成（手性つき） | `tetrahedralDirs(mol, id, parity)`（M3 で実装済み・テスト済み） |
| 手性の検算 | `parityFromDirs`（同上） |

### 6.2 組み立てアルゴリズム

BFS で骨格をたどりながら、原子ごとに「局所の方向セット」を作って親に接ぐ。

1. 根を選ぶ（最長炭素鎖の端。無ければ最初の重原子）。原点に置く
2. 原子ごとに立体構造を決める。σ結合の相手数ではなく**電子対の数**で決める
   - 三重結合あり → 直線（2方向・180°）
   - 二重結合あり → 平面三角（3方向・120°）
   - それ以外 → 正四面体（4方向・109.5°）。O（2結合）・N（3結合）も
     非共有電子対を数に入れて正四面体扱いにする（実測 104.5°/107° への近似）
3. sp3炭素は `tetrahedralDirs(mol, id, parity)` をそのまま使う。
   **これで手性は既に正しい**
4. 親へ向かう方向が「親からこちらへ来た向きの逆」に重なるよう、方向セット全体を回す。
   **回転は真の回転（行列式+1）なので手性は保たれる**。ここが設計の要点で、
   個別に方向を選び直すと手性が壊れる
5. 親軸まわりの自由度（ねじれ角）は**アンチ（180°ずらし＝ジグザグ）**を既定にする

### 6.3 明示すべき近似（図に注記を必ず添える）

- **配座は代表例**: ねじれ角はアンチ固定。実際の分子は回転しており、この図は
  「教科書のジグザグ」に対応する1つの形（環ビューの平面近似の注記と同じ扱い）
- **結合長は一律**: C-C と C-O の差は付けない（模式図）
- **表示専用**: 判定・正準コード・命名には一切影響しない（既存方針の踏襲）

### 6.4 段階

- **M4a（最初）: 非環分子** — アルカン・アルコール・カルボン酸・アミノ酸まで。
  高校範囲の大半がここに入る。sp3 は `tetrahedralDirs` の再利用で済む
- **M4b: 二重結合の面** — C=C まわりのシス/トランスは「三角形の面をどう向けるか」で決まる。
  `bondGeo`（既存）から面の法線を決めて伝播させる
- **M4c: 環** — 閉環は「順にたどって最後が閉じる」保証がないため別扱い。
  当面は環ビューと同じ**平面多角形**として置き、環外の置換基だけ正しい角度で生やす。
  いす形は P7-5-M3 のプリセット（conformers.json）で別途
- **M4d: 操作** — 描画は環ビューのパイプライン（模型→回転→弱透視→画家のアルゴリズム）を
  そのまま流用できる。回転操作・H の表示切替・注記も同じ形に揃える

### 6.5 入り口

立体モーダルの4つ目のタブ「🧊 分子全体」。中心炭素の選択は不要（P12-8 で
入り口を「選ばず開く」に変えたので一貫する）。原子数が多い分子では
ラベルが重なるため、環ビューの `staggerRingLabels` と同じ回避を入れる。

---

## 7. 環ビュー「⬡ 環を横から」の正（2026-08-22・v1442 で二糖まで到達）

**この章がこのビューの正**。実装は `stereo.js` の `buildRingModel` / `renderRing` /
`updateRingNote`。単糖までの経緯は P12-8、二糖は `DESIGN_sugar.md` 段2〜段3 から移した。

### 7.1 模型の組み方（変わっていない芯）

| 何を | どこに置くか |
|---|---|
| 環原子 | 描かれた 2D 座標のまま **z = 0** の平面 |
| 環外の置換基 | 描かれた 2D 座標のまま、面(±1) に応じて **z = ±depth** |
| 暗黙の H | 環外の重原子がちょうど1本の環sp3炭素だけ、置換基の反対側・反対の面 |

面の読み方は `chemistry.js` の `readRingParityFromHaworth` と**同一**
（`haworthFace` が最優先、無ければ縦位置 ±25°）。⚠ **ここを読み替えないことが
「命名で使う立体＝画面に見える立体」の一致を保証している。**

倒し角は **0〜180°**（0=描いたハース図そのもの／90=真横／180=裏返したハース図）。
180° は `rotateZX` の angleX=π ＝ (x,y,z)→(x,−y,−z) の**回転**なので分子は変わらない。

### 7.2 受け入れる形／断る形（v1442 で3つに割った）

| 形 | 扱い | 断り文の定数 |
|---|---|---|
| 環1つ・環内すべて単結合 | ○ | —— |
| **原子を共有しない環2つ＋原子1つの橋**（二糖） | **○**（v1442 で開けた） | —— |
| 環2つが**原子を共有**（縮合環・スピロ） | × | `RING_FUSED_REASON` |
| 環3つ以上 | × | `RING_MANY_REASON` |
| 環2つだが橋が1原子でない（直結・2原子以上） | × | `RING_LINK_REASON` |
| 橋の面がどちらの環からも読めない | × | `RING_BRIDGE_FACE_REASON` |
| 環内に多重結合 | × | `RING_UNSATURATED_REASON` |
| 環が無い | × | `RING_NO_RING_REASON` |

⚠ **v1440 までは「連結成分の閉路数 ≠ 1」で一括して断っており、二糖まで
「縮合環では上下の意味が決まりません」と言っていた**（事実と違う。二糖は縮合環ではない）。

### 7.3 二糖 —— ★「2つのハース環が同一平面」

2つの環を**同じ z=0 平面**に置く。⚠ このとき**橋の酸素は両方の環の置換基を兼ねる**ので
z をただ1つに決めなければならないが、面は「その環の炭素から見た縦位置」で決まるため
**両側から別々に読める**。

#### ⚠ 7.3a 訂正（2026-08-22・v1446。**そろえる仕事は登録の図の側へ移った**）

ユーザーの発注「**2糖は必ず真横に環を並べる**」に合わせて `compounds.json` の二糖4件を
**2つの環の中心の y をそろえた図**に描き直した（`DESIGN_sugar.md` §11・テストは `DS1`）。
このとき分かったのは、**真横に並べるには片方の環を裏返した向きで描くしかない**ということ:

- ハース投影では環外の置換基を**縦（±25°以内）**に描かないと面が読めない。
  2つの環の中心を同じ高さに置くと、橋の -O- は**両方の host の上か下**にしか置けない
  （1つの原子だから）。α-1,4 は「両方とも下」で成り立つが、β-1,4 と α1↔β2 は矛盾する。
- ＝ **矛盾する3件（セロビオース・ラクトース・スクロース）は、片方の環を裏返して描いてある。**
  教科書のセルロースの図で1つおきに環がひっくり返っているのと**同じ理由**。

**したがって下の表は「v1445 までの登録の図」の記録**で、いまの登録では **4件とも一致**する:

| 二糖 | 環A側 | 環B側 | 模型の裏返し | 登録の図の番号のたどる向き |
|---|---|---|---|---|
| マルトース（α-1,4） | −1 | −1 | 不要 | 時計回り・時計回り（**そろっている**） |
| セロビオース（β-1,4） | +1 | +1 | 不要 | 反時計回り・時計回り |
| ラクトース（β-1,4） | +1 | +1 | 不要 | 反時計回り・時計回り |
| スクロース（α1↔β2） | −1 | −1 | 不要 | 時計回り（六員）・**反時計回り（五員）** |

⚠ **スクロースの「30°問題」は消えた**（真横に置き直すと橋は縦から 18.7°）。
＝ `bridge.derivedB` の保険に頼らず**両側から直接読める**。§6-3 の保険そのものは
`ST41` が図をその場で作って見張る（登録がたまたま保険を通っているかどうかに預けない）。
⚠ 「⇅」は**登録（＝教科書の向き）と「2つの環を同じ向きに描いた図」を行き来する**ボタンになった。

#### 7.3b v1445 までの実測（記録）

| 二糖 | 環A側 | 環B側 | 判定 |
|---|---|---|---|
| マルトース（α-1,4） | −1 | −1 | 一致 ＝ 裏返し不要 |
| セロビオース（β-1,4） | −1 | +1 | 食い違い ＝ 裏返し1回 |
| ラクトース（β-1,4） | −1 | +1 | 食い違い ＝ 裏返し1回 |
| スクロース（α1↔β2） | −1 | +1（※） | 食い違い ＝ 裏返し1回 |

※ **スクロースだけ、橋への結合が縦から 30°** で ±25° を外れて直接は読めなかった。
⚠ **黙って推測しない。** `readRingParityFromHaworth` が使うのと**同じ規約**
（`DESIGN_compound_coverage.md` §6-3。ハース投影では環外に出る2本は必ず反対の面）で
同じ炭素の -CH₂OH の反対と決め、**そう決めたことを画面に書く**（`bridge.derivedB`）。
⚠ **その規約でも読めなければ、組まずに断る**（`RING_BRIDGE_FACE_REASON`）。
この仕掛けは**残してある**（ユーザーが図を手で動かせば同じ状況が起きる）。

**環A（動かさない）／環B（裏返すかもしれない）の決め方**（`orderDisaccharideRings`。
⚠ **選んだ炭素に依らない** ＝ 同じ分子はいつも同じ絵になる）:

1. 橋を**アノマー炭素**で持っている側（グリコシドを供与した側）を B にする
2. 両方／どちらもアノマーなら（スクロース・トレハロース型）**大きい環を A**
   → スクロースはグルコース（六員）が A・フルクトース（五員）が B
   ＝ **教科書どおり「フルクトース側だけが裏返る」**

**裏返しの当て方は2つの層に分かれている（ここが要）**:

| 層 | 何を | 折り返しの軸 |
|---|---|---|
| **絵**（模型のノード） | y を折り返し、face と z を反転 | **その環自身の重心** ＝ その場で裏返る（相手の環に重ならない） |
| **分子**（`flipHaworth`） | 同上＋面マークも反転 | ⚠ **橋の原子の y** でなければ立体が壊れる |

両者は**縦の平行移動ぶんしか違わない**ので、面（z）と立体はまったく同じ。
⚠ 軸の話は `chemistry.js` の `flipHaworth` のコメントと SG9 が持っている。

### 7.4 画面に書くこと（⚠ 断定しない）

- **「2つの環が同じ平面にある」は仮定**（`RING_COPLANAR_CAVEAT`）。実際はグリコシド結合
  -O- のまわりが回るので相対的な向きは1つに決まっていない。ただし **上下（α/β）は
  どの向きでも変わらない**ので、この図で読める α/β は正しいまま —— ここまで書く
- **裏返した側の環**と、その理由（橋の酸素の上下がそろわないから）
- **番号をたどる向き**（時計回り／反時計回り）を環ごとに数で言う
- **面を規約で導出したとき**は、どの置換基から導いたかまで書く（スクロースの30°）
- 環の大きさが同じ二糖では「左の六員環／右の六員環」と呼び分ける

### 7.5 ⚠ 「⇅ ○員環を裏返す」ボタンは **v1447 で外した。作り直さないこと**

v1442 で足し、v1447 で外した。⚠ **便利そうに見えるので、放っておくと必ずもう一度作られる。**
外した理由は2つで、**どちらも単独で十分**:

1. ★ **二糖の「片方の環だけ」を裏返すことは、グリコシド結合を切らないとできない。
   そして切る操作こそが加水分解である。** ＝ つながったままの二糖に対するこのボタンは、
   **起きえないことを押させていた**（ユーザー 2026-08-22
   「分子全体をフリップするならともかく、2糖のうち一部を反転させる意味はありません」）。
2. ⚠ **文言が画面に内部の言葉を出していた** ——「`⇅ 五員環を登録の向きへ`」の「登録の向き」は
   `compounds.json` に保存されている図の向きという**アプリの都合**で、利用者には無関係
   （同「まず言葉の意味がユーザーに対して伝わりません」）。

**残したもの**（⚠ 消しすぎ注意）:

- ★ **2つの環を同一平面に置くための内部の反転そのものは残す。**
  これが無いと橋の面がそろわず、**二糖4件のうち3件が環ビューに入らない**（§7.2・v1442 の芯）。
  外したのは**人が押して切り替える口**だけで、**環ビューは「橋の面がそろう向き」に固定**になった
  ＝ **見た目は既定のまま・切り替えが無くなっただけ**。
- ★ **分子まるごとの操作は残す** —— `⟳ 裏返す`（倒し角180°）と独楽回転。
  ⚠ **こちらには意味がある**:「同じ分子を違う向きで描いた図」を見抜く力そのもので、
  **入試の応用問題の型**（マルトースを上下反転した図を並べて、どれが正しい図かを選ばせる）。
  ⚠ **「フリップは加水分解のときだけ」と読まないこと。** 止めたいのは**片方の環だけ**の反転である。

門番は `SG8` の `need === flipped`（内部の反転を誰かが外すと3件で赤くなる）と、
`SG10` / `SG15` の「ボタンが存在しないこと」「画面に『登録』の語が出ないこと」。

### 7.6 検収（`tests.js`）

| 帯 | 主張 |
|---|---|
| ST21 | 対応範囲（飽和環：単環＋橋でつないだ2環）と断り文 |
| ST20 | 独楽回転の軸は環の面に垂直 |
| SG7 | 倒し角 0〜180°・180° で裏返って見える |
| **SG8** | 二糖4件が入る／橋の面が両側でそろう／描かれた上下と face が一致する |
| **SG9** | 部分反転で立体コードが変わらない 8/8（軸・面マークの否定対照つき） |
| **SG10** | スクロースが教科書の向きで見える／⚠ **切り替えのボタンが無いこと・画面に「登録」の語が無いこと** |
| **SG11** | 「同一平面」は仮定だと画面に書いてある |

---

## 8. キャンバスで環を裏返す（2026-08-22・v1445 → **v1447 で入口を作り替えた**）

§7 の反転は**模型の中だけ**なのでキャンバスの図は1ピクセルも動かない。
こちらは**キャンバスの図そのもの**を置き直す側。

### 8.0 ★ 何のためにあるか（v1447 で確定。**ここを読み違えると作りが丸ごとずれる**）

> **ユーザー（2026-08-22）: 「フリップするのは加水分解前後の分子の形に対応するためです」**

二糖の中では、片方の環は**相手とつながる都合で**単独のときと逆向きに描かれている
（教科書のスクロースのフルクトース環がその代表）。切って1分子になった瞬間その理由は消えるので、
**切り離された単糖を「その分子を単独で描くときの形」へ移す** ＝
**切る前の図と切ったあとの図が、同じものを指していると読める**。これが目的である。

⚠ **「登録の向きにそろえる」は目的ではない。** 登録の図は「単独で描くときの形」を
**取ってくる手段**として引いているだけ（`game.haworthRegisteredRingSense`）。
ここを取り違えると、加水分解と切り離して呼ばれる「図の整形機能」に化ける。

### 8.1 入口 ——⚠ **押すボタンは無い**（v1445〜v1446 の `#mm-haworth` は v1447 で廃止）

**入口は加水分解1つだけ**。`reactor.js` の `hydrolysis_glycoside` が切ったあとに
`game.restoreHaworthOrientation()` を呼ぶ。⚠ **作図のたびには呼ばない**
（前後の対応とは無関係に図を描き直すことになる）。

廃止したボタンの理由は `DESIGN_molecule_modal.md` §6-2a が正。要点だけ:

| | 扱い |
|---|---|
| **分子まるごとのフリップ** | **意味がある**（入試の「どれが正しい図か」の型）。⚠ **土台は残してある** —— `canFlipHaworth` / `flipHaworth` / `haworthCanvasFlip` は環1つの分子には**分子まるごと**の反転として当たる |
| **二糖の片方の環だけの反転** | ⚠ **グリコシド結合を切らないと起こせない ＝ 起きえない。** 人に押させない。切る操作＝加水分解のときだけ内部で起こす |

**どの分子を相手にするかは `haworthFlipPlan` が決める**（門番は据え置き）:

| 分子 | 通るか |
|---|---|
| ハース図として読む糖の環（ピラノース・フラノース）を持つ | **○**（登録 939件のうち **16件** ＝ §1-5 の16件と一致） |
| 環が無い／ベンゼン・シクロヘキサン／**ラクトン・酸無水物** | **×** |
| 糖の環はあるが裏返せない（環3つ以上・橋1原子でない・フィッシャーの中心が混ざる） | **× 理由つき**（`many` / `link` / `gate`） |

⚠ **`restoreHaworthOrientation` が実際に触るのは、このうち環が1つの分子だけ。**
環が2つある分子（二糖）には当てない —— どちらの環を動かすかは相手しだいで、
「単独で見たときの形」という物差しがそもそも当たらない。

⚠ **ラクトン・酸無水物を外すのに1手要る。** 環の O の隣の炭素が -O- を持つので
「アノマーらしく」見えてしまう（そのままだと 22件になる）。**アノマー炭素は sp3** なので、
二重結合を持つ炭素を弾いて 16件にそろえた（`_haworthSugarCycle`）。

### 8.2 置き直しの手順（3手）——★ 3手目を省かない

1. `flipHaworth(mol, ids, 軸)`。⚠ **軸は環が2つなら橋の原子の y・1つなら重心**
2. **重なりを解く**（縦横にずらす）。裏返しただけだとスクロースは相手の環と**最短 5px**
3. ★ `canonicalCode` ＋ `canonicalStereoCode` が変わっていないことを確かめ、**変わる置き方は1つも採らない**

⚠⚠ **§7.3 の「絵の軸と分子の軸は縦の平行移動ぶんしか違わないので面と立体は同じ」は、
キャンバスには当てはまらない。** 環ビューは**面を先に読んでから絵を折る**ので平行移動が効かないが、
キャンバスは**折った座標から面を読み直す**ので、橋の結合の向きが変われば読みが変わる
（実測: 軸＝橋の y は 8/8 立体保存・軸＝環の重心は 1/8・軸＝集合の重心は 0/8）。

**ずらし方**（`haworthCanvasFlip`）:

- **ずらさずに済むならずらさない**（図が飛ばないのがいちばん読みやすい。橋の結合の長さも元のまま）
- 駄目なら (dx, dy) を升目（環結合の半分）で総当たりし、**橋の結合がいちばん短くなる**置き方を採る。
  ⚠ **縦だけに逃がすと橋が 335px（平常の4倍）まで伸びる**ので、横も候補に入れる（実測: 152px に収まる）
- 実測（二糖4件）: マルトース／セロビオース／ラクトースは**ずらさずに済む**（最短 115/38/38px・橋 121/38/38px）。
  スクロースだけ `dx=-37, dy=+223`（最短 42px・橋 152px）

### 8.3 `haworthCanvasFlip(mol, {undo:{dx,dy}})` —— **厳密な逆操作**

覚えておいたずらし量 (dx, dy) を打ち消してから同じ軸で反射する。
⚠ **探索でやり直しても元には戻らない** —— 探索は「相手から離れる側」しか見ないので、
2回目に打ち消す置き方を選べない（実測: 二糖8件中6件で図がずれたまま残る）。

⚠ **v1447 現在、アプリのコードからは呼ばれていない**（呼んでいたのは廃止した ⇅ ボタン）。
`SG14` の否定対照④が動かし続けているので**腐らない**。
**残してあるのは、§8.0 の「前後の対応」をアニメーションで見せるときに要るから**
（`restoreHaworthOrientation` の戻り値が `dx` / `dy` / `axisY` を持っているのと同じ理由）。

### 8.4 ★ 環ビューとキャンバスは**同じ環**を裏返す

環の並べ方（`orderHaworthRings`。§7.3 の A/B の決め方）を **`chemistry.js` の1本に統一**し、
`StereoView.orderDisaccharideRings` はそこへ委譲するだけにした。
番号の起点と向き（`haworthNumberingStart`）も同じく1本。
⚠ 別々に持つと「横から見たときとキャンバスの図とで裏返る環が違う」が起こりうる。

### 8.5 ★ 加水分解での自動フリップ（v1447。**ここが §8 の本体**）

⚠ **v1445 の §8.5 は「自動反転は入れない」と書いていた。これは誤りだった。**
根拠にしていたのは「裏返してから切っても**生成物の名前が変わらない**」という実測だが、
⚠ **名前は裏返しても絶対に変わらない**（§1-2 の② が 16/16 ＝ 同じ分子）。
**名前で見るかぎり、対応がずれていることには永久に気づけない。見るべきは向きだった。**

向きで測り直した実測（`readStereo = true`・実画面・生成物の番号のたどり方）:

| 出発 | そのまま切る | **片方の環が逆向きの図**（教科書のスクロースがこれ）から切る |
|---|---|---|
| マルトース | α-Glc(時計) ＋ α-Glc(時計)／戻し **0件** | 同じ名前・**両方 時計**／戻し 1件 |
| セロビオース | β-Glc(時計) ＋ β-Glc(時計)／戻し **0件** | 同じ名前・**両方 時計**／戻し 1件 |
| ラクトース | β-Gal(時計) ＋ β-Glc(時計)／戻し **0件** | 同じ名前・**両方 時計**／戻し 1件 |
| スクロース | α-Glc(時計) ＋ β-Fruf(時計)／戻し **0件** | 同じ名前・**両方 時計**／戻し 1件 |

⚠ **戻さないと、同じ1回の加水分解でできた2つが、キャンバスに並んで逆向きの約束で描かれる**
（実測: 片方 反時計・もう片方 時計）。**これが直したかった食い違い。**
`canonicalCode` / `canonicalStereoCode` は4件とも両通りで完全一致 ＝ 戻しても分子は同じ。

**約束**:

- **判定は向きだけ**（`いまの向き ≠ 単独で描くときの向き`）。⚠ **分子の名前で分岐しない**
- **環が1つの分子にだけ当てる** ＝ 動くのはいつも**分子まるごとの裏返し**
- ⚠ **立体が変わる置き直しは1つも採らない**（`haworthCanvasFlip` の③＋呼び出し側でも指紋を突き合わせ）
- **戻り値に材料を残す**: `[{ids, name, senseBefore, senseAfter, axisY, dx, dy, before[], after[]}]`。
  ⚠ 目的が「前後の対応を見せる」ことなので、黙って書き換えて終わりにすると補間する材料が残らない
- **戻したときだけ** caption に断りを1文足す（何も戻していないときは出ない）

### 8.6 検収（`tests.js`）

| 帯 | 主張 |
|---|---|
| **SG14** | 登録16件で「裏返しても同じ分子・名前も同じ・向きは反転・逆操作で厳密に戻る」／重なりと橋の長さ／門番／否定対照4本 |
| **SG15** | ⚠ **押して裏返す口がどこにも無いこと**（`#mm-haworth` / `btn-haworth-flip` / `btn-stereo-ring-flipring` が存在しない・画面に「登録の向き」の語が無い）／★ **分子まるごとの裏返しの土台は残っていること**／環ビューが橋の面をそろえたままであること |
| **SG18** | ★ 加水分解の自動フリップ（4件×2通り。素のままは0件・逆向きの図からは1件・名前と立体コードは両通りで一致・記録に before/after が入る・別の反応では動かない） |
