# DESIGN_review_pack3.md —— v1496 → v1502（2026-09-02 の1日・6版）の定期レビュー

**状態: レビューのみ。コード・既存設計書・データ・版番号は1バイトも変えていない。**（2026-09-03・review3 レーン・ブランチ `review3`）

**対象**: `git log 0356302~2..075d889`（v1496 `f0de0e3` → v1502）。`assembler/` の差分は 21 ファイル・+4,406 / −83 行
（`video-scripts/`・`demos-*.json` は対象外）。中身: 参考書の第1ページ（v1497）／付加重合の生成物5件（v1498）／
実験モードの課題12問＋`obstructedInsets` の直し（v1499）／印の悉皆 `CV4` と印の直し4件（v1500）／
系統樹の反応4本（v1501・qa v93）／監査ファズを反応の面へ（v1502）。

**凡例**（すべての指摘に出典を付ける）:
- 【現物】= リポジトリの現物（コード・データ）を読んで確かめたこと（`ファイル:行`）
- 【実測】= 今日、この worktree を `python -m http.server 9713` で配信し、`tools/record/` の Playwright（Chromium・headless）で
  実アプリを動かして測ったこと。スクリプトは scratchpad（`probe.mjs`・`probe2.mjs`・`audit-run.mjs`）に置き、リポジトリには入れていない。
  配信の前後で印ファイルを curl し、自分の worktree を配っていること（前）と削除後に 404（後）を確かめた
- 【記述】= 設計書・DEVELOPMENT.md に書いてあること
- 【意見】= このレビューの判断

---

## 0. 要旨（先に結論だけ）

1. ★★ **(A) この6版で足した面のうち、監査のファズが実際に触るのは 8 面中 2 面**（§1）。
   **ユーザーに見える新しい面4つ（参考書ページ・課題12問・`?open=reference`・`?quest=`）は 0 面**。
   300 反復 × 80 操作【実測】で `referenceBook.open/render` 0 回・`startQuest` 0 回・`setPalette('exp')` 0 回・瓶のクリック 0 回。
   届いたのは **新反応4本のうち3本**（無水酢酸 7 回・環の水素化 2 回・酢酸ビニル 1 回。⚠ **クメンは 0 回** —— 組は9回並んだが札が押されなかった）と
   **登録した5化合物**（①ライブラリ検査で 原形＋変形 の 2 件ずつ・0 件失敗）。
   `audit.html` の自己申告（「実験モード 外」「learn.js は一度も呼ばれない」）は**正しい**。ただし
   **「v1497 で②に組の枝を足した」は誤り（v1502）**・「分子を変えられる 43 本」は v1501 で **47 本**になっている（reach の行は動的なので画面の数は合っている）。
2. ★ **(B) `CV4` の穴（消えた原子・H の id が `changed` に混ざる）は、突いた結果 0 件**（§2）。
   全 53 本 × 最大 6 題材 × 最大 4 箇所 ＝ **276 回の apply で absent 0・H 0・重複 0**【実測】。
   原子を消す反応 4 本（側鎖酸化・酸化開裂・ニトロ還元・弱酸の遊離）も、消した原子の id を印に入れていない。
   ⚠ さらに「H の id」の側は**構造的に起きない** —— このモデルに H の原子は存在せず（題材 53 本すべてで明示 H が 0 個）、H を置く道具も無い。穴は空いているが、いま通るものが無い。
3. ★★ **(C) 化学: 「起こってはいけない相手で起きる」例が 2 本の反応で見つかった**（§3）。
   - ⚠⚠ **`dehydration_anhydride_inter`（酢酸2分子 → 無水酢酸）が、アミノ酸・ヒドロキシ酸でも札を出す**:
     **グリシン＋グリシン**（教科書はペプチド結合＝アミド化）・**酢酸＋グリシン**・**酢酸＋アラニン**・**酢酸＋乳酸**・
     **酢酸＋サリチル酸**（★ 課題 `eq-salicylic-aspirin` の分子モーダルに「＋ 酢酸 を呼び出す → 酸無水物」の札が出る）・
     酢酸＋ピルビン酸・ギ酸＋ギ酸（無水ギ酸は単離できない）。門番が「-COOH が1つだけ」（`reactor.js:5216-5219`）しか見ておらず、
     `TR2` の否定対照（`tests.js:45848-45860`）にアミノ酸・ヒドロキシ酸が1つも無い。
   - ⚠ **`add_carboxylic_acid_alkyne`（アセチレン＋酢酸 → 酢酸ビニル）は酸の側に門番が無い**: 安息香酸・ギ酸・グリシン・乳酸・
     アクリル酸・アジピン酸・サリチル酸・テレフタル酸・ステアリン酸 —— 試した 14 組のうち **13 組で「（未登録）」の生成物**ができる。
     アルキン側は「アセチレンだけ（教科書 p.31）」と絞ったのに、酸の側は教科書の「酢酸」に絞っていない（`reactor.js:4799`）。
   - ⚠ **`hydrogenate_benzene_ring` の門番が「分子」ではなく「キャンバス全体」を見る**（`reactor.js:4951, 4953`）:
     **ベンゼン＋エタノール／ベンゼン＋水（前の脱水の残り）／ベンゼン＋シクロヘキセン**で札が消え、瓶（`h2_ni`）は
     「いまの分子にはどれもありません。…ふつうの条件では進みません」と**ベンゼン単独なら進む反応を「進まない」と説明する**【実測・DOM】。
   - ○ `alkylate_arene_propene` は設計どおり（トルエン3・ナフタレン2 は申告済み）。⚠ 申告に無いもの: **スチレン（3箇所）・ビフェニル（3）・エチルベンゼン（3）・クメン自身（3）**でも進む。
     環の水素化が「側鎖に C=C があれば先にそちら」と門番を立てたのに、こちらは側鎖の C=C を見ていない。
   - ○ 生成物の名前（シクロヘキサン・クメン・無水酢酸・酢酸ビニル）は登録名と一致・無水酢酸の往復は正準コードが一致【実測】。
4. ★ **(D) 参考書: 「1行だけ出す」抜け道は表については無い**（描画器は `series` しか受けない・`REF3` ③で確認）。
   ⚠ ただし **`text` / `lead` / `note` の各ブロックは素の HTML で、行の中身を散文で書くことは何も止めていない**（`learn.js:5596, 5653`）。
   いまの第1ページの例題の `lead`「炭素を6個つないでヘキサンを組みます」がその形（表の C6 行そのもの）だが、
   お題のパズルが名前と分子式を出すので**実害は無い**。
   **課題12問は全問「1手で届く道がちょうど1本／2手の1問は1手で届かない」**【実測・全ルール × 全箇所】。
   **目標を名前で呼び出す道**は、課題中は呼び出し欄が `display:none`、分子モーダルの相手札は 12 問すべてで目標と一致しない【実測】。
   ⚠ **`g.summonMolecule(目標)` を呼べば合格になる**（API 経路。決定事項なので指摘ではなく記録）。
5. ★ **(E) 数**: 緑の注記（53/53・47 本・16 本ぶん・16 本）はすべて**動的**で実態と合っている【実測・全走 743 件】。
   ⚠ **「実行ルールを1本足すと必ず当たる4か所」は v1502 で 2 か所増えた** —— 相手が要る反応なら
   `audit.js` の `PAIR_SAMPLES`（`FZ1` が `CV_PAIR_SAMPLES` と突き合わせる）と `FZ2` の「題材を作れなかった反応が 0 本」。
   参考書1枚／課題1問を足しても**数を固定した検査には当たらない**（当たるのは形の門番: `REF5`・`EQ2`）。
   **題名・注記が古い検査が 4 件**（`RG5` 題名「12件」→15・`GC5` 題名「2組」→4・`RG1`(5) 注記「32件」→36・`FZ` 注記「17本」→16）。
6. ○ **確かめて問題なかった範囲**は §8。回帰 743 件全合格（620 秒）・qa 94 件全合格・`verify-release` / `verify-compounds` 合格（worktree の中で実行）・
   監査 ①ライブラリ 2,497 件 0 問題・②ファズ 300 反復の失敗率 16.3%（CI 12.6〜20.9・pack2 の 17.1% と区間が重なる）。

---

## 1. (A) この6版で足した面のうち、監査が実際に触るのは何面か

**方法【実測】**: `audit.html?seed=1000` を headless で開き、①ライブラリ ON・②ファズ 300 反復 × 80 操作・③④ OFF。
開始前に iframe の `game.startQuest` / `setPalette('exp')` / `setQuestOpen` / `referenceBook.open` / `setOpen` / `render` /
`fitCanvasToMolecule` / `obstructedInsets` を包んで呼び出し回数を数え、`.rg-bottle` のクリックを capture で数えた。
所要 437 秒。`comparableKey = ops=80/thr=24,12,16,12/mix=3/grp=0.45/cov=0.5`。

| # | 6版で足した面 | 版 | 監査 | 実測 |
|--:|---|---|---|---|
| 1 | 参考書ページ（`learn.js` `ReferenceBook`） | v1497 | **外** | `open` 0・`setOpen` 0・`render` 0 回 |
| 2 | `?open=reference[&code=]` | v1497 | **外（構造的）** | iframe の src は `index.html?v=1502` 固定（`audit.html:127`）。起動時にしか読まれない受け口には**永遠に届かない** |
| 3 | 付加重合の生成物 5 件（`compounds.json`） | v1498 | ★ **内（①）** | ポリエチレン／ポリプロピレン／ポリ塩化ビニル／ポリアクリロニトリル／ポリ酢酸ビニル 各 2 件（原形・変形s2）・**0 問題** |
| 4 | 実験モードの課題 12 問（`quests.json`・課題の帯・一覧モーダル） | v1499 | **外** | `startQuest` 0・`setQuestOpen` 0・`setPalette('exp')` 0・瓶クリック 0 回 |
| 5 | `?quest=` | v1499 | **外（構造的）** | #2 と同じ |
| 6 | `obstructedInsets()` の天井／床の直し | v1499 | △ **関数は通るが症状の条件は作れない** | `summon` 経由で `fitCanvasToMolecule` 2,812 回 ＝ `obstructedInsets` 2,812 回。ただし iframe は PC 幅で、課題の帯も実験パレットも出ないので v1499 の症状（320px・帯の裏）は再現できない |
| 7 | 印の直し 4 件（ジエン・加硫・ヨードホルム・ビニロンの `changed`） | v1500 | **外** | ジエン重合 4 回・ヨードホルム 15 回は押されたが、`inspectMolecule` は印を見ない。加硫・ビニロンは **0 回** |
| 8 | 新反応 4 本 | v1501 | ★ **内（②）** —— **3/4 本** | 無水酢酸 7 回（押した 9・組 14）／環の水素化 2（2・11）／酢酸ビニル 1（1・14）／⚠ **クメン 0（押した 0・組 9）** |

★ **答え: 8 面中 2 面（#3・#8）。ユーザーに見える新しい面（#1・#2・#4・#5）は 0 面。**

**ファズの到達（同じ実行）**: 分子を変えられる **47 本のうち 38 本**に到達・**0 回 9 本**
（`condensation_glycoside`・`vulcanization`・`acetalization_pva`・`bromination_activated_ring`・**`alkylate_arene_propene`**・
`hydrolysis_glycoside`・`cyclize_glucose_α/β`・`open_glucopyranose`）。react の空振り **43.2%**（1,119/2,589）・
押した 1,470 回の内訳: 分子を変える札 46.4%・前後を見る 31.3%・解説カード 275。
⚠ 300 反復は夜間監査の 1/17 なので「届かない」の断定はしない（レーンの 5,037 反復では 0 回 2 本）。
ただし **クメンは「組を並べても押されない」形**で 0 回になっている（相手のプロペンが C=C を持つので、
並べた瞬間に付加の札が 6 枚並び、誘導が効かない残り半分の乱数では 1/12 以下）—— 加硫（`@二本の鎖`・0 回）と同じく、**組を並べることと札が押されることは別**。

**自己申告（`audit.html` の `<details>`）との突き合わせ【現物】**:
- ○ 「実験モード 外（タブも瓶も押さない）」「learn.js は一度も呼ばれない」「絞り込み・クイズ・立体視 外」 …… 実測と一致。
- ⚠ `audit.html:82` 「**v1497** で②に『相手を並べる組』の枝と…足し」・`:102` 「**v1497** から内」 …… **v1502 の誤り**（`git log -- assembler/audit.html`: 組の枝は `d0ca66b` v1502）。
  v1497 は参考書の版なので、次に読む人が「参考書の版で監査が変わった」と誤読する。
- ⚠ `:85, :88` 「分子を変えられる **43** 本」 …… 実測当時（v1500）の数。v1501 で 47 本。実測の記録なので直す必要は無いが、「いまは 47」の一言が無い。
- ⚠ 「実験モードの課題」「参考書」「`?open=reference`」「`?quest=`」は表に**行が無い**（「実験モード（🧪 タブ・試薬の瓶）」の行に含めて読むしかない）。

**入口の側【現物】**: `?open=reference` と `?quest=` を指すリンクは**リポジトリのどこにも無い**
（`grep -rn "open=reference\|quest=eq-"` → `learn.js:5719` の注記のみ。`qa/data/assembler_links.jsonl` 316 行の `open=` は `stereo` 2 件だけ）。
★ しかも **qa の `org.ali.formula-alkane` は `kind:"none"`・理由「1分子を組んでも n への依存は見えない」のまま** ——
参考書の第1ページ（`codes` にこのコードを持つ）がまさにその穴を埋めたのに、qa 側は知らない。
＝ **2つの受け口は「作ったが、まだ誰も指していない」**。監査の外である以上、回帰テスト（`REF7`・`EQ3`）が唯一の門番。

---

## 2. (B) `CV4` の穴を突いた —— 0 件

**穴の所在【現物】**: `tests.js:46598` `marks = changed.filter(id => afterSig.has(id) && !water.has(id))` ——
`afterSig` は反応後の**重原子**の署名なので、**消えた原子の id・H の id・存在しない id は黙って落ちる**（missing にも extra にも数えない）。
画面側も同じ形（`reactor.js:7700-7702` `.map(find).filter(Boolean)`）なので、混ざっていても例外は出ず、印が1つ少なく出るだけ。

**方法【実測】**: `cvPickSamples` と同じ題材（`CV_PAIR_SAMPLES` 16 本＋ライブラリ走査）に加え、
ルールごとにライブラリから**最大 6 題材**・相手つきの反応には別の組（安息香酸×2・トルエン＋プロペン・ナフタレン＋プロペン・
アセチレン＋安息香酸・グリセリン＋酢酸・グリシン×2 など 11 組）を足し、**箇所は最大 4 つ**まで 1 つずつ apply して、
`changed` の各 id を「重原子として在る／H として在る／無い（前は在った・前も無い）／重複」に分類した。

| | 値 |
|---|--:|
| 見た反応 | **53/53 本** |
| apply した回数 | **276 回** |
| `changed` の id のべ数（CV4 題材・先頭箇所のみ） | 181 |
| 無い id（消えた原子） | **0** |
| 無い id（元から無い） | **0** |
| H の id | **0** |
| 重複 | **0** |
| 原子を消す反応（消した id が印に無いことを確認） | `oxidize_side_chain` 3 回・`oxidative_cleavage` 3・`reduce_nitro` 11・`liberate_weak_acid` 6 |

★ **「H の id」は構造的に起きない**: この 276 回のどの分子にも `element === 'H'` の原子は 0 個（自動水素は `calculateHydrogens()` の計算値で原子ではない）。
H を置く道具もパレットに無い（`index.html` に `atom-h` 無し）。＝ 穴のうち「H」の側は、H の原子を持つ分子が生まれない限り通るものが無い。

【意見】**穴は実在するが、いま通るものは 0 件。** 塞ぐなら `changed` の id が `mol.atoms` に無いとき **extra として赤**にするのが 1 行
（`tests.js:46598` の filter を外し、`afterSig.has(id)` でないものを `ghost` に分けて `problems` へ）。
否定対照は `liberate_weak_acid` の Na の id を混ぜれば作れる。急がない。

---

## 3. (C) 新反応 4 本の化学

**方法【実測】**: 各ルールの `detect` を名指しの組で回し、`apply` して生成物を `lookupCompoundName` で読んだ。
瓶の経路は 🧪 実験パレットの札を実クリックして `#exp-reagent-note` を読んだ。

### 3-1. ⚠⚠ `dehydration_anhydride_inter`（酢酸2分子 → 無水酢酸）—— 起こってはいけない相手で起きる

門番【現物】`reactor.js:5213-5232`: 「その分子の -COOH が **1 つだけ**」＋「別分子どうし」。**それ以外の官能基を見ない。**

| 組 | 箇所 | 生成物 | 同時に出る札 | 判定 |
|---|--:|---|---|---|
| 酢酸＋酢酸 | 1 | **無水酢酸**＋水 | — | ○ |
| 安息香酸＋安息香酸 | 1 | **無水安息香酸**＋水 | — | ○（登録あり） |
| 酢酸＋安息香酸 | 1 | （未登録 C₉H₈O₃） | — | △ 申告済み（混合酸無水物） |
| ⚠⚠ **グリシン＋グリシン** | 1 | （未登録 C₄H₈N₂O₃） | `amidation`（2箇所） | ★ **教科書はペプチド結合**。アミノ酸2分子で「酸無水物」の札が並ぶ |
| ⚠⚠ **酢酸＋グリシン**／**酢酸＋アラニン** | 1 | （未登録） | `amidation` | ★ アミンが居る相手で酸無水物（N-アセチル化と並ぶ） |
| ⚠ **酢酸＋乳酸** | 1 | （未登録 C₅H₈O₄） | `esterification` | ヒドロキシ酸。教科書はエステル |
| ⚠ **酢酸＋サリチル酸** | 1 | （未登録 C₉H₈O₄） | `esterification_phenol_info` | ★ **課題 `eq-salicylic-aspirin` の分子モーダルに「＋ 酢酸 を呼び出す → 分子間脱水 → 酸無水物」の相手札が出る**（正解は無水酢酸の瓶） |
| 酢酸＋p-ヒドロキシ安息香酸／サリチル酸×2 | 1 | （未登録） | — | 同上 |
| ⚠ **ギ酸＋ギ酸** | 1 | （未登録 C₂H₂O₃） | — | 無水ギ酸は単離できない（化学として存在しない生成物） |
| 酢酸＋ピルビン酸／アクリル酸／プロピオン酸／ステアリン酸／オレイン酸 | 1 | （未登録） | — | △ 教科書外だが化学としては可 |
| 酢酸＋シュウ酸／アジピン酸／フェノール／アニリン／エタノール／酢酸ナトリウム | **0** | — | — | ○ 門番どおり |

★ `TR2` の否定対照（`tests.js:45848-45860`・11 組）には **アミノ酸・ヒドロキシ酸・ギ酸が 1 つも無い**ので緑。
【意見】**直す。** 門番を「-COOH のほかに -OH／-NH₂ を持たない分子どうし」に絞るか、少なくとも `amidation`／`esterification` が同じ組で出るときは譲る
（`dehydration_inter` が「アルコール限定」で守っているのと同じ線）。1 行では直らないので発注書（§9-A）。
⚠ グリシン×2 は **qa の `bio.` 系（ペプチド）と正面から食い違う**画面なので、次の動画・課題より前。

### 3-2. ⚠ `add_carboxylic_acid_alkyne`（アセチレン＋酢酸 → 酢酸ビニル）—— 酸の側に門番が無い

門番【現物】`reactor.js:4797-4809`: アルキン側は `acetyleneUnits`（アセチレンだけ）。**酸の側は `carboxyl` なら何でも**（`4799`）。

| 組 | 箇所 | 生成物 |
|---|--:|---|
| アセチレン＋酢酸 | 1 | **酢酸ビニル** ○ |
| アセチレン＋安息香酸／ギ酸／乳酸／アクリル酸／サリチル酸／ステアリン酸 | 1 | （未登録） |
| アセチレン＋グリシン | 1 | （未登録 C₄H₇NO₂）⚠ アミンが居る |
| アセチレン＋アジピン酸／テレフタル酸 | **2** | （未登録）—— 二酸でも両方の -COOH に出る |
| アセチレン＋酢酸ナトリウム／フェノール／プロピン＋酢酸 | 0 | ○ |

試した 14 組のうち **13 組が「（未登録）」**。`TR8` の否定対照（`tests.js:46125-46136`）は**アルキン側と「-COOH でないもの」**だけで、
「-COOH だが酢酸でないもの」が無い。
【意見】教科書 p.31 は「酢酸 CH₃COOH を付加すると」と**酢酸を名指し**している。ワッカー法を「エチレンだけ」に絞った §4-1 の線と同じなら、
**酢酸だけ**（または「-COOH 以外の官能基を持たない一価カルボン酸」）に絞るのが筋。ビニルエステル一般は化学として実在する（安息香酸ビニル・ステアリン酸ビニル）ので
「間違い」ではなく「教科書の線を越えている」の分類。急がない。

### 3-3. ⚠ `hydrogenate_benzene_ring`（ベンゼン → シクロヘキサン）—— 門番がキャンバス全体を見る

門番【現物】`reactor.js:4949-4955`: ① `mol.atoms.some(ヘテロ)` ② `multipleBondSites(mol).length` —— **どちらも `mol`（＝キャンバス全体）**。
他の反応（`alkylate` の `componentOf`）が成分ごとに見ているのと違う。

| キャンバス | 箇所 | 瓶 `h2_ni` の返事【DOM】 |
|---|--:|---|
| ベンゼン | 1 | 実行 → シクロヘキサン |
| トルエン／エチルベンゼン／o-キシレン／ビフェニル（2） | 1〜2 | メチルシクロヘキサン／エチルシクロヘキサン／（未登録 C₈H₁₆）／（未登録 C₁₂H₁₆） |
| スチレン → `add_h2` → エチルベンゼン → 環 | 0 → 1 | ○ 教科書の順序が操作の順序（`TR3` ④のとおり） |
| ナフタレン／アントラセン／フェノール／ニトロベンゼン／アニリン／安息香酸／クロロベンゼン | 0 | ○ 設計どおり |
| ⚠ **ベンゼン＋エタノール** | **0** | 「いまの分子にはどれもありません。ベンゼン環も高温・高圧なら付加しますが、**ふつうの条件では進みません**」 |
| ⚠ **ベンゼン＋水**（エタノールを脱水した残り） | **0** | 同上 |
| ⚠ **ベンゼン＋シクロヘキセン**／ベンゼン＋エチレン | **0** | 同上（別分子の C=C が環を止める） |
| ベンゼン＋ベンゼン | 2 | ○ |

★ **同じ画面で、ベンゼン1個なら進み、隣に水が1個あると「進みません」と説明される。** 説明文（`reactor.js:3202` の `miss`）は
v1501 で「高温・高圧なら付加します」と足されているので、**進まない理由が「条件」だと読める**が、本当の理由は「隣の分子」。
`TR4` の否定対照（`tests.js:45946-45959`）は**単独の分子 12 件**だけで、「ベンゼン＋別分子」が無い。
【意見】**直す（各 1 行）**: ①を `componentOf(mol, site[0])` の中で、②を同じ成分の中で数える。設計の意図（「同じ分子の側鎖が先」「同じ分子にヘテロ原子があれば出さない」）はそのまま保てる。
⚠ 系統樹の「全体を1つのキャンバスに置く」（D-T1）の面では、**キャンバスに他の分子が置いてあるのが常態**なので、いまの門番だと環の水素化は木の上でほぼ出ない。

### 3-4. ○ `alkylate_arene_propene`（ベンゼン → クメン）—— 設計どおり。申告に無い相手が 4 つ

| 組 | 箇所 | 生成物 |
|---|--:|---|
| ベンゼン＋プロペン | 1 | **クメン（イソプロピルベンゼン）** ○（マルコフニコフの向き・`TR5`） |
| トルエン（3）／ナフタレン（2） | | （未登録）—— **申告済み**（§10.13 E） |
| ⚠ **スチレン**（3）／**ビフェニル**（3）／**エチルベンゼン**（3）／**クメン自身**（3 → ジイソプロピルベンゼン） | | （未登録）—— 申告に無い |
| フェノール／ニトロベンゼン／アニリン／安息香酸／クロロベンゼン／1-ブテン／エチレン／プロピン／手描きのシクロプロペン | 0 | ○ |

⚠ **スチレン**: 環の水素化は「側鎖に C=C があればそちらが先」と門番を立てた（`4953`）のに、アルキル化は側鎖の C=C を見ない。
クメン自身に進む（多重アルキル化）のは化学として正しい（クメン法の副反応）が、caption は「クメンができました」と言い切る。
【意見】申告と同じ「既存の芳香族置換4本に揃えた」線の内側。**ユーザー判断（絞るか）に、スチレン・多重アルキル化の 2 件を足して戻す。**

### 3-5. ○ 正しかったこと

- 生成物の名前: シクロヘキサン・クメン（イソプロピルベンゼン）・無水酢酸・酢酸ビニル ＝ 登録名と一致【実測】
- 無水酢酸の往復（加水分解 → 酢酸×2 → 分子間脱水）で正準コードが一致【実測】
- クメンの caption が②③（ヒドロペルオキシド・フェノール＋アセトン）を言葉で断っている（`TR5` ④）
- 酢酸ビニルは水が出ない（付加）・酸素 2 個のまま（`TR7`）
- `PARTNER_CANDIDATES` にプロペンが無い ＝ ベンゼンのモーダルにクメンの札は出ない（申告どおり）【実測: 課題 `eq-benzene-*` の相手札 0 件】

---

## 4. (D) 参考書と課題

### 4-1. 参考書 —— 「別の道から1行だけ出る」抜け道

| 道 | 現物 | 判定 |
|---|---|---|
| `renderStageTable(block)` | `learn.js:5606-5636`。`block.series` 以外を読まない。`REF3` ③が `stageId/row/only` を渡して 10 行のままを確認 | ○ 構造で閉じている |
| `render(page)` | `5576-5590`。ページ1枚だけ | ○ |
| `?open=reference&code=` | `5733-5735`。`code` はページを選ぶだけで、行を指せない | ○ |
| 例題 `startExample` | `5677-5692`。既存ステージを開くだけ。⚠ PC（分割）では**資料を開いたまま**組む ＝ 表を見ながら解く。ユーザー原則（参照は全体像）の内側 | ○ |
| ⚠ `text` / `lead` / `note` ブロック | `5596` `p.innerHTML = b.text`・`5653`・`5668`。**素の HTML／文字列で、内容の制約が無い**。`REF5` の禁止語は**キー名**だけ（`name`・`formula`…）で、値の中身は見ない | ⚠ **散文で1行ぶんを書く道は開いている**。第1ページの `lead`「炭素を6個つないでヘキサンを組みます」がその形。ただしお題のパズルが「ヘキサン C₆H₁₄」を出すので、いまは答えの提示になっていない |
| `caption` | `5616` 「（10行）」と行数を出す | ○（行数を出すのはむしろ「全部ある」の証拠） |

【意見】表については抜け道なし。散文の穴は「ページを書く人の自制」に依存しているので、**2枚目以降を書くレーンへの注意書き**として §9 に入れる（機械で守るのは難しい —— 表の名称・分子式の文字列が `text` に出たら赤、程度）。

### 4-2. 課題 —— 成立しない問題は無いか

**方法【実測】**: 12 問それぞれの出発物に、**全実行ルール × 全箇所**（最大）を 1 手だけ当て、目標の正準コードに届く道を数えた。

| 課題 | 手 | 1手で届く道 | detect が通るルール数 |
|---|--:|---|--:|
| エタノール → エチレン | 1 | `dehydration_intra` のみ | 5 |
| アセチレン → 塩化ビニル | 1 | `add_hcl` のみ | 6 |
| エタノール → アセトアルデヒド | 1 | `oxidize_primary` のみ | 5 |
| 2-プロパノール → アセトン | 1 | `oxidize_secondary` のみ | 4 |
| アセトアルデヒド → 酢酸 | 1 | `oxidize_aldehyde` のみ | 2 |
| エタノール → ヨードホルム | 1 | `iodoform` のみ | 5 |
| 酢酸エチル → 酢酸 | 1 | `hydrolysis_ester` のみ | 2 |
| サリチル酸 → アセチルサリチル酸 | 1 | `acetylation_anhydride` のみ | 17 |
| トルエン → 安息香酸 | 1 | `oxidize_side_chain` のみ | 11 |
| ベンゼン → ニトロベンゼン | 1 | `aromatic_nitration` のみ | 4 |
| アニリン → アセトアニリド | 1 | `acetylation_anhydride` のみ | 11 |
| ベンゼン → アニリン | 2 | **0 本**（1手では届かない） | 4 |

○ **12 問すべて成立**（出発物＝目標が無い・1手の問題は道がちょうど 1 本・2手の問題は 1 手で届かない）。`EQ2` の「出発物≠目標」も現物で確認。

**目標を「作らず出す」道【実測】**:
- 課題中の「名称から呼び出す」欄: `display:none`・高さ 0（`EQ3` のとおり）
- 分子モーダルの相手札: 12 問すべてで**目標と一致する札は無い**（`goalIsPartner` 12/12 false）。
  ⚠ ただし §3-1 のとおり `eq-salicylic-aspirin` に **v1501 の新しい札「＋ 酢酸 を呼び出す → 酸無水物」が並ぶ**（誤答への誘い）
- `?summon=<目標>&quest=<id>`: `game.js:11358-11367` の注記どおり `?quest=` が後で、`placeQuestStart` がキャンバスを空にするので漏れない【現物・未実測】
- `g.summonMolecule(目標)`（API）: **合格になる**（`_questDone` true・記録に残る）。✏️ 手描きと同じ「素通しの『ある』」（D-E4 (a)）の帰結で、決定事項

---

## 5. (E) 数と門番

### 5-1. 緑の注記【実測・全走 743 件・620 秒】

| 注記 | 出どころ | 動的か |
|---|---|---|
| 「見張った **53/53** 本（分かれた反応 15 本・分かれない反応 38 本）／…水 12 個」 | `CV1`（`tests.js:45681`） | ○ `rules.length` |
| 「印を突き合わせた **53/53** 本（うち結合が変わった 47 本）／許した 2 個／水 12 個」 | `CV4`（`46672`） | ○ |
| 「題材 **16** 本ぶん・化合物名 36 件」 | `FZ1`（`46980`） | ○ `Object.keys(PAIR_SAMPLES).length` |
| 「届いていなかった **16** 本すべてでボタンを確認（否定対照 7 件）」 | `FZ2`（`47049`） | ○ `FZ_ZERO_REACH_20260902.length`（名指し 16 本のラチェット） |
| 監査「分子を変えられる **47** 本のうち 38 本」 | `audit.js:854` | ○ `rulesExecutable` |

すべて実態と一致。⚠ **書いた数が古いもの**（緑のまま黙って合わなくなっている）:

| 場所 | 書いてある | 実態 |
|---|---|---|
| `RG5` の題名（`tests.js:34974`）「環化3件・重合5件・縮合3件・アセタール化1件だけ」＝ 12 | 本文の `expected` は 15 件 | 15 |
| `GC5` の題名（`44489`）「宣言した**2組**だけ」 | `REVERSIBLE_REACTION_PAIRS` は **4 組**（v1501 で酸無水物の分子間が増えた。本文は名指しなので緑） | 4 |
| `RG1` (5) の注記（`34282`）「第2段で紐づくのは **32** 件ちょうど」 | assert は 36 | 36 |
| `FZ` 帯の注記（`46905`）「届いていなかった **17** 本」 | 一覧は 16 本（題名も 16） | 16 |
| `audit.html:82, 102` 「**v1497** で②に組の枝」 | v1502 | v1502 |
| `DESIGN_reaction_execution.md` §10.13 D「回帰テスト 739 件」 | v1502 で FZ1〜4 が足されて 743 | 743（当時の数として正しい） |

### 5-2. 在庫の数を固定した検査 —— 一覧の検算

**実行ルールを1本足すと「必ず」当たる**（§10.13 A の表）: `KNOWN_RULES`（`qa/tests.js:1047`）／`CV_NO_SAMPLE`（`tests.js:45503`。**宣言は1つで `CV1`・`CV4` の2本が読む**）／
`RG1`(5)（`34315`）か `RG5`（`35016-35029`） …… **正しい**。ただし:
- ⚠ **v1502 で 2 か所増えた（相手が要る反応のとき）**: `audit.js:94` `PAIR_SAMPLES`（`FZ1` が `CV_PAIR_SAMPLES` と1文字単位で突き合わせる・`46958`）と
  `FZ2` の「題材を作れなかった反応が 0 本」（`46996-46999`）。§10.13 A の表は v1501 に書かれたので載っていない。
  ★ v1501 と v1502 が出会ったとき、実際にこの2つが赤くなって3行を足した記録が `audit.js:109-113` にある ＝ 動作は確認済み。
- 条件つき: `CV4_NO_CHANGE_RULES`（`46561`。`_info` を足すと）／`RG5` の `infoUnlinked`（`35032`。瓶を持たない info）／`RG1`(6) `condition` 4 件（`34321`）／`GC5`（対を足しても本文は名指しなので緑・題名だけ古くなる）。

**瓶を1本足すと 5 か所**: `REAGENTS.length === 23`（`34254`）／札 23（`34328`）／transform 18・detect 5（`34329`）／320px で 23 本（`35274`）／qa `KNOWN_BOTTLES` …… **正しい**（5 か所）。

**参考書を1枚足すと**: 数を固定した検査は **0 か所**。形の門番は `REF5`（行数＝件数＋2・`why` 20 字以上・`source` の系列が実在・`codes`・`singleSource`・禁止キー 6 語）。
`REF1` はボタン数＝ページ数（動的）。`REF2/3/4/6/7` は `alkane-naming`／`hexane` を名指しした標本検査で、2枚目には効かない
（⚠ **2枚目の「表は全行」「例題は代表1つ」は誰も見張らない** —— `REF3`・`REF6` はページ id を書き写している）。
`EP7` は `<details>` の id 5 件を固定しているが、ページを足しても details は増えない。

**課題を1問足すと**: 数を固定した検査は **0 か所**。形の門番は `EQ2`（行数＝問数＋2・CRLF・BOM なし・`hands ∈ {1,2}`・`why` 10 字超・start≠goal・名前が引ける・一覧に台帳が漏れない）。
`EQ3` は札の数＝`QUESTS.length`（動的）。⚠ **「1手で届く道が本当にある」「2手の問題が1手で届かない」は誰も見張らない**（`EQ4` は 2 問を名指しで通すだけ）。§4-2 の掃引はそのまま `EQ9` にできる。

---

## 6. 見つけた問題（重い順）と「直すべきか・急ぐか」

| # | 問題 | 出典 | 直すべきか | 急ぐか |
|--:|---|---|---|---|
| **1** | ★★ 分子間の酸無水物が**アミノ酸・ヒドロキシ酸・ギ酸**でも札を出す（グリシン×2 で「酸無水物」／課題のサリチル酸に「＋酢酸 → 酸無水物」） | §3-1・`reactor.js:5213-5232`・`TR2` | 直す（門番に -OH／-NH₂ の除外・`TR2` にグリシン×2・乳酸・サリチル酸・ギ酸を足す） | ★ 課題・動画がこの札を踏む前 |
| **2** | ★ 環の水素化の門番が**キャンバス全体**を見る（隣に水1個で「進みません」） | §3-3・`reactor.js:4951, 4953`・`TR4` | 直す（成分の中で数える・各1行）＋ `TR4` に「ベンゼン＋エタノール」「ベンゼン＋水」 | ★ 系統樹（D-T1: 全体を1キャンバス）の前 |
| **3** | ★ 監査は新しい面 8 つのうち 2 つにしか届かない。**参考書・課題・2つの受け口は 0**。クメンは 300 反復で 0 回 | §1 | 監査に「実験パレットの瓶を押す」「課題を1問始める」の枝を足すか、**外だと表に明記**（いまの表に行が無い） | 次に課題・参考書を増やす前に決める |
| **4** | ★ 酢酸ビニルの酸の側に門番が無い（14 組中 13 組が未登録の生成物） | §3-2・`reactor.js:4799`・`TR8` | 直す（酢酸に絞る。§4-1 の線）＋ `TR8` に安息香酸・グリシン | 急がない |
| **5** | ⚠ `?open=reference`・`?quest=` を指すリンクが 0 本。qa の `org.ali.formula-alkane` は `kind:none`（理由は参考書が埋めた穴そのもの） | §1 末尾・`qa/data/assembler_links.jsonl` | qa レーン（配線 2 行）＋ `OPEN_TARGETS` に `reference`（§10-7 の残り） | 参考書を増やす前 |
| **6** | ⚠ `audit.html` の自己申告が「v1497」（正: v1502）・表に参考書／課題／受け口の行が無い | §1 | audit.html 2 行＋表 3 行 | 次の監査レーンが読む前 |
| **7** | ⚠ 題名・注記が古い検査 4 件（`RG5` 12→15・`GC5` 2組→4・`RG1`(5) 32→36・`FZ` 17→16） | §5-1 | 各1行 | 急がない |
| **8** | ⚠ アルキル化がスチレン・ビフェニル・エチルベンゼン・クメン自身でも進む（申告はトルエン・ナフタレンのみ） | §3-4 | ユーザー判断へ（絞るなら側鎖 C=C と多重アルキル化） | 急がない |
| **9** | ⚠ `CV4` の穴（消えた原子の id）は 0 件だが空いたまま | §2・`tests.js:46598` | 直す（`afterSig` に無い id を `ghost` として赤・1〜3 行。否定対照は Na の id） | 急がない |
| **10** | ⚠ 参考書 2 枚目以降の「表は全行」「例題は代表1つ」・課題 13 問目以降の「1手で届く道がある」を見張る検査が無い（`REF3/REF6/EQ4` は id を書き写している） | §5-2 | `REF3`・`REF6` を全ページで回す／§4-2 の掃引を `EQ9` に | 2枚目・13問目の前 |
| **11** | ○ 参考書の `text/lead/note` は素の HTML で、散文で1行ぶんを書く道が開いている | §4-1 | 機械では守りにくい。2枚目を書くレーンへの注意書き | —— |

---

## 7. 疑わしいが確証が無いもの（分けて書く）

- ⚠ **クメンが夜間監査で届くか**: 300 反復では 0 回（組 9 回）。レーンの 5,037 反復では 0 回の一覧に無いので届いている。
  「組を並べても押されない」構造（プロペンの付加札 6 枚と競合）は現物から読めるが、**率は測っていない**。
- ⚠ **`obstructedInsets` の直し（v1499）が監査で守られているか**: 関数は 2,812 回通るが、症状の条件（320px・厚い帯）は監査の iframe では作れない。
  「守られていない」と言い切るには③画面サイズ検査（320×568 は入っている・`audit.js:392`）に**実験パレット ON** の枝が要るかを見る必要があり、今日は見ていない。
- ⚠ **アルキル化のスチレン**は「教科書外」であって「誤り」とまでは言えない（フリーデル・クラフツ条件でスチレンが自己重合するのは事実だが、高校の線では扱わない）。
- ⚠ **`GC5` の題名「2組」**: v1472 で 3 組になった時点から古い可能性がある（pack2 は見ていない）。いつからかは追っていない。

---

## 8. 確かめて問題なかったもの（見ていない所と区別がつくように）

**回したもの【実測】**
- `assembler/test.html` 全走（headless・本体の `tools/run-tests.mjs` を借りて worktree の URL を渡した）: **✅ 743 件全合格・620 秒**（隣のレーンの走行と同時）
- `qa/test.html`: **✅ 94 件全合格**（`KNOWN_RULES = 53` が緑 ＝ v1501 の申し送りは統合済み）
- `node tools/verify-release.js`（**worktree の中のを呼んだ**）: ✅ 5 アプリ（assembler v1502・ion v198・muki v30・qa v93・ratio v41）・556 テキストに化け／BOM なし
- `node tools/verify-compounds.js`（同上）: ✅ 合格（警告 7 件は「自動水素がやや近い 11.5px」。pack2 の 5 件＋2）。`compounds.json` は 1,032 行 ＝ 1,030 件＋2
- 監査 ①ライブラリ: **2,497 件・0 問題**（v1498 の 5 件を含む）／②ファズ 300 反復: 失敗 49（**16.3%**・CI 12.6〜20.9）。中身は 結合線の下の自動水素 42・結合線の下の原子 26・原子の重なり 5・自動水素 2・見出し 6 ＝ pack2 と同じ分布。**退行の証拠なし**
- `CV4` の穴: 276 回の apply で 0 件（§2）
- 新反応の生成物名 4/4・無水酢酸の往復・クメンの向き（§3-5）
- 課題 12 問の成立（§4-2）・目標を名前で呼ぶ道の閉じ方（§4-2）
- 緑の注記 5 件が動的であること（§5-1）

**読んで一致していたもの【現物】**
- `DESIGN_reaction_execution.md` §10.13 D の数（53 本・瓶 23・瓶なし 15・瓶あり 36・対 4 組・CV1 15 本）と現物（`RG1`/`RG5`/`CV_MUST_SPLIT`/`REVERSIBLE_REACTION_PAIRS`）
- §24 の `CV4` の記述（物差し・許容 2 件・変化なし 6 本）と `tests.js:46553-46564`
- `DESIGN_experiment_mode.md` §10-1〜10-4（D-E4 (a)・D-E5 (c)・D-E8 (a)・D-E9・見積り／実際の表）と `game.js:8688-8816`・`quests.json`
- `DESIGN_reference_book.md` §10-2（表は series 単位・例題は代表1つ・`?open=reference` は learn.js 側）と `learn.js:5485-5738`
- `audit.html` の「外」の一覧（実験モード・learn.js・絞り込み・クイズ・立体視）と実測の 0 回

**見ていないもの（正直に）**
- iPad 実機・WebKit（`--engine=webkit` は回していない）
- 動画レーンの完成品での目視
- 監査の③画面サイズ・④当たり判定（今日は OFF）。pack2 で 0 件だったものを再測していない
- 夜間監査規模（5,000 反復）の再走。クメンの到達率はそこで見るべき
- `muki`／`ion-equation`／`ratio` の回帰テスト（版整合は `verify-release` で見た）

---

## 9. 次に出すべき発注書の形（★ どのレーンに何を渡せば効くか）

### 発注書 A: 反応の門番 3 本（`reactor.js`・★ 急ぐ・1 レーン）

1. `dehydration_anhydride_inter.detect`（`5213`）: 「-COOH が1つ」に加えて **その分子が -OH／-NH₂（フェノール性・アミノ）を持たない**ことを門番に足す。
   ギ酸は「-COOH の炭素に H が付いている」で落とす。★ **先に `TR2` の否定対照にグリシン×2・酢酸＋グリシン・酢酸＋乳酸・酢酸＋サリチル酸・ギ酸×2 を足して赤を見てから**直す。
   `RG5`・`KNOWN_RULES` は動かない（本数は変わらない）。相手札（課題 `eq-salicylic-aspirin`）から酸無水物が消えることを `EQ` 側で1行確かめる。
2. `hydrogenate_benzene_ring.detect`（`4949-4955`）: ①②を `componentOf(mol, ring[0])` の中で数える（`isolatedBenzeneRings` の各環ごとに判定）。
   `TR4` に「ベンゼン＋エタノール → 1 件」「ベンゼン＋水 → 1 件」「ベンゼン＋シクロヘキセン → 1 件（環）＋ add_h2 はシクロヘキセン側」を足す。
   ⚠ `miss` の文（`3202`）は据え置きでよい（門番を直せば嘘にならない）。
3. `add_carboxylic_acid_alkyne.detect`（`4799`）: 酸の側を酢酸（または「-COOH 以外の官能基を持たない一価酸」）に絞る。`TR8` に安息香酸・グリシンを足す。
   ⚠ どちらの線にするかはユーザー判断（§4-1 は「教科書が名指しした相手だけ」＝ 酢酸だけ）。
4. 見積り: `detect` 3 本（各 3〜6 行）・新規テスト 0・触る緑のテスト 3（`TR2`/`TR4`/`TR8` に負例を足すだけ）。版バンプ・全走・qa 全走。

### 発注書 B: 監査の「外」を減らすか、外だと書くか（`audit.js`/`audit.html`・急がない・1 レーン）

1. `audit.html` の `<details>`: 「v1497」→「v1502」（2 か所）。表に **参考書ページ／`?open=reference`／課題12問／`?quest=`** の 4 行を「外」で足し、代わりの門番（`REF1-7`・`EQ2-8`）を書く。
2. 決めること: ②ファズに「🧪 実験パレットの瓶を押す」（`setPalette('exp')` → `.rg-bottle` を1つクリック → 条件があれば1つ選ぶ）の枝を **5%** で足すか。
   足せば課題の帯を除く実験モードの面が内側に入る。⚠ `OP_MIX_ID` を 4 に上げる（`comparableKey` が変わる）。
3. クメンの到達: `pickReactionButton` の誘導は「届いた回数 → 押した回数」なので、プロペンの付加札 6 枚と同点のまま乱数で負け続ける。
   同点のときは **`groupSummons` で並べた題材の rule id を優先**する 1 行を足すと、組を並べた直後の 1 回で押される。★ 5,000 反復で率を出してから。
4. `tests.js:46598`（`CV4`）: `afterSig` に無い id を `ghost` として赤に（否定対照は `liberate_weak_acid` の Na の id）。

### 発注書 C: 参考書 2 枚目・課題 13 問目の前に置く門番（`tests.js`・急がないが「前に」・1 レーン）

1. `REF3`・`REF6` をページの `for` にする（`reference.json` 全件で「表は series の全行」「例題は代表1つ」）。いまは `alkane-naming` を書き写している。
2. `EQ9`: §4-2 の掃引（全ルール × 全箇所を 1 手当てて、`hands=1` は届く道が ≥1・`hands=2` は 0 本）を全問で回す。§4-2 の表が否定対照の答え。
3. qa レーン: `assembler_links.jsonl` の `org.ali.formula-alkane` を `kind:none` から `?open=reference&code=` へ（2 行）。`game.js` の `OPEN_TARGETS` に `reference`（`DESIGN_reference_book.md` §10-7 の 1 番）。
4. 題名・注記の古い数 4 件（`RG5`・`GC5`・`RG1`(5)・`FZ`）を各 1 行。

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-03 | 新規。5 軸（(A) 監査 300 反復＋呼び出し回数の計測／(B) `CV4` の穴 276 回／(C) 新反応 4 本 × 名指しの組 60 件・瓶の DOM 経路／(D) 参考書の抜け道・課題 12 問の全ルール掃引／(E) 注記と在庫の検査の検算）＋問題 11 件＋発注書 3 本。v1502（`075d889`）時点。ブランチ `review3`・触ったのはこのファイルだけ |
