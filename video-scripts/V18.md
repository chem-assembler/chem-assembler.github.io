# V18 完全台本「酢酸＋エタノール＝いいにおいの正体」（反応シリーズ③）

**⚠ 収録し直しが必要（2026-08-01）**。台本・ナレーション・デモ・投稿文はすべて完成している。
公開を止めていたアプリ側の作図バグは **`b86b9c1`（v341）で修正済み**なので、**収録と mux をやり直せば公開できる**。経緯は §3。

V11 の次回予告（「次回: 酢酸 ＋ エタノール ＝ いいにおいの正体」）を回収する回。
録画モード demo id: `reaction-esterification`。**収録は等速**。ナレーション7行・31.8秒（通し 35.4秒）。
テロップはナレーションの 0.28〜0.38秒後に出るところまで追い込み済み（`--lead=0.3`）。

## 1. 画面の流れ（demos.json の7ステップと一致）

| # | 画面（操作） | テロップ |
|---|---|---|
| 1 | 自由モードへ → 酢酸・エタノールを続けて呼び出し、引きで並べる | 酢酸 ＋ エタノール、温めると？ |
| 2 | 静止 | ① -COOH ／ ② -OH。ここが反応する |
| 3 | 「エステル化（カルボン酸＋アルコール, -H₂O）」を押す | エステル化 → 酢酸エチル ＋ 水 |
| 4 | 静止（変化箇所のオレンジ点線が残る） | -OH と -H がとれて H₂O になった |
| 5 | 寄る | つながった -COO- がエステル結合 |
| 6 | 引く | 酢酸エチル ＝ 果物のようないいにおい |
| 7 | 静止 | 次回: 石けんはどうやってできる？ |

**見せ場は「とれた水が②として画面に残る」こと**。生成物の名称チップが
「① 酢酸エチル ＋ ② 水」になるので、脱水縮合の「水が1つとれる」が字面でも図でも見える。

## 2. ナレーション全文（ずんだもん版・VOICEVOX）

正は [narration/V18.json](narration/V18.json)。読み仮名は7行すべて確認済み（誤読なし。
`カルボキシき`・`ヒドロキシき` は「もと」誤読を避けて仮名、`シーオーオー` は C-O-O の読み上げ）。

> 酢酸とエタノール。混ぜて温めると、何ができる？
> ①はカルボキシき、②はヒドロキシき。ここが反応するのだ。
> エステル化。できたのは、酢酸エチルなのだ。
> カルボン酸のヒドロキシきと、アルコールの水素がとれて、水になったのだ。
> つながった、シーオーオーが、エステル結合なのだ。
> 酢酸エチルは、果物みたいな、いいにおいがするのだ。
> 次は、石けんのつくり方なのだ。

## 3. 公開を止めていた作図バグ（2026-08-01・修正済み）

**反応が生成した原子を 42px 刻みで置くのに対し、母体の分子は 80px 刻みで描かれているため、
新しい原子が既存の結合線の上に乗る。** 結果、結合線が非結合の原子を貫通し、
**構造式が別の物質に見える**。

### 酢酸エチル（この回の主役）

実機の実測値:

```
原子: C(360,300) C(440,300) O(440,220) C(322,300) C(402,300) O(482,300)   ＋ 水 O(546,210)
結合: C(360)-C(440) / C(440)=O(440,220) / C(322)-C(402) / C(402)-O(482) / O(482)-C(440)
```

**結合は正しい**（CH₃-C(=O)-O-CH₂-CH₃ ＝ 酢酸エチル。名称チップも「酢酸エチル」）。
**当時**、
壊れているのは**配置**で、y=300 上に x=322, 360, 402, 440, 482 の5原子が並ぶのに、
結合は隣どうしではなく飛び越してつながっている:

- `C(360)-C(440)` が **C(402) を貫通**
- `C(322)-C(402)` が **C(360) を貫通**
- `O(482)-C(402)` が **C(440) を貫通**

**画面には「C-C-C-C(=O)-O」＝酪酸（ブタン酸）に見える図が出る**。
完成 mp4 のフレームを拡大して確認済み。自動水素の数は正しい側に付いているが、
骨格が一直線に見えるので、読む側は確実に誤読する。**この図は出せない。**

### 酢酸（V6 にも影響している）

同じ原因で、**V6 の最後の答えの画（酢酸）も壊れている**:

```
原子: C(360,300) C(440,300) O(520,300) O(482,300)
結合: C(440)-C(360) / C(440)=O(520) / C(440)-O(482)
```

2つの酸素が両方とも C(440) の右の同一直線上にあり（+42 と +80）、
**C=O の二重線が -OH の酸素を貫通する**。画面では「C-O-O」に二重線がかかった図に見える。
**V6-final.mp4 は投稿前に取り直しが必要**（台本・ナレーション・投稿文はそのまま使える。収録し直すだけ）。

### 影響しないもの

- **呼び出しただけの分子は正常**（compounds.json の配置をそのまま使うため）。
  → **V11 は無傷**。マレイン酸・フマル酸・無水マレイン酸はすべて呼び出しのみで、
  実測しても貫通ゼロ。フレームの座標も当時と同じ（42px 刻みのまま）
- **エタノール → アセトアルデヒド も正常**（貫通ゼロ）。V6 の途中の画は問題ない

### 再開条件

**反応の生成物配置を、母体の分子と同じ刻みに合わせる**（アプリ側の修正）。
直ったら以下を実行するだけで V18 は完成する:

```
node tools/record/record.mjs --demo=reaction-esterification --format=short --base=http://localhost:8132/assembler/
node tools/record/mux.mjs --video=video-scripts/out/reaction-esterification-short.webm \
  --audio=video-scripts/audio/V18/full.wav \
  --bgm=video-scripts/audio/bgm/のんきな日常.mp3 --se=video-scripts/audio/se/tap.wav \
  --events=video-scripts/out/reaction-esterification-short.events.json \
  --meta=video-scripts/meta/V18.json --trim=auto --lead=0.3 --size=1080x1920 \
  --out=video-scripts/out/V18-final.mp4
```

**取り直したら必ず `--events` の時刻とナレーションの累積秒を突き合わせる**
（配置が変わると `frame` の寄り引きも見直しが要る。現在の値は 520 → 420 → 320 → 420）。
そして**生成物のフレームを拡大して、結合線が原子を貫通していないことを目視で確かめる**。

### 修正済み（`b86b9c1` v341「反応の生成物を母体と同じ刻みで置く」）

並行のアプリレーンが直した。**同じ手順を実機で再検算し、解消を確認した**（2026-08-01・v342 時点）:

```
結合: C(360,300)-C(440,300) / O(440,220)=C(440,300) / C(440,300)-O(440,380)
      / O(440,380)-C(360,380) / C(360,380)-C(280,380)
貫通: 0件   重原子の重なり: 0件
```

生成物が母体と同じ 80px 刻みで、**2段（y=300 と y=380）に折り返して**置かれるようになった。
V6 の鎖（エタノール→アセトアルデヒド→酢酸）も貫通0・重なり0 で再検算済み。

**教訓は残る**: 名称チップが正しくても図が正しいとは限らない。
公開前に LANES.md §4 の貫通チェックを流すこと。

## 4. 検算のしかた（今後の反応回で必ずやる）

生成物の図が信用できるかは、**結合線と非結合原子の距離**を測れば機械的に分かる。
実機のコンソールで次を流す（`< 16` は原子の半径 17 に合わせた閾値）:

```js
const m = window.game.userMolecule, byId = Object.fromEntries(m.atoms.map(a=>[a.id,a]));
const heavy = m.atoms.filter(a=>a.element!=='H'), bad=[];
for (const b of m.bonds) { const p=byId[b.atomId1], q=byId[b.atomId2];
  for (const a of heavy) { if(a.id===p.id||a.id===q.id) continue;
    const L2=(q.x-p.x)**2+(q.y-p.y)**2; if(!L2) continue;
    const t=((a.x-p.x)*(q.x-p.x)+(a.y-p.y)*(q.y-p.y))/L2; if(t<=0.02||t>=0.98) continue;
    if (Math.hypot(a.x-(p.x+t*(q.x-p.x)), a.y-(p.y+t*(q.y-p.y))) < 16)
      bad.push(`${p.element}-${q.element} が ${a.element}(${a.x},${a.y}) を貫通`); } }
bad
```

**名称チップが正しくても図が正しいとは限らない**。名称は結合から計算されるので、
配置が壊れていても正しい名前が出る。**この回はそれで危うく公開しかけた。**

## 5. 投稿メタ

正は [meta/V18.json](meta/V18.json)。**反応シリーズ③**。V6 → V11 → **V18** の順。
カバーは「エステル化 → 酢酸エチル ＋ 水」の瞬間。
固定コメントの問いは「水の酸素はどっちから来ると思いますか？」（答えはカルボン酸側。
アプリのトーストも同位体の話に触れている）。
