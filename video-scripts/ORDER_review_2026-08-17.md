# 発注書: 実機レビュー 2026-08-17

ユーザーが実機で一通り触って出した指摘の台帳。**この文書が正**。着手したら状態欄を更新する。

状態: `未着手` / `着手中（ブランチ名）` / `✅着地（版）` / `⏳判断待ち` / `やらない（理由）`

---

## A. 自由モード・反応まわり（レーン `feat/rx-deadend`）

**共通の症状: 反応に入ると出られない／描けなくなる。**「行き止まり」が4つ集まっている。

| # | 指摘 | 状態 |
|---|---|---|
| A-1 | **生成物予測モードで原子が置けない。自由モードに戻っても作図できない** ← 最優先。アプリが実質壊れる | ✅着地（**v1409**①） |
| A-2 | 反応させたあと、**もとの分子に戻る道がない** → 「↩ 反応前に戻す」ボタン | ✅着地（v1409②） |
| A-3 | **何に対する予測なのか（反応物は何か）が不明瞭**。予測モードはキャンバスを空にするので画面から消える | ✅着地（v1409③） |
| A-4 | 「反応させる分子を選ぶ」が行き止まり —— 作成済みの分子しか選べない／作図できなくなる／もとに戻れない／**1分子しか作っていないときどうする** | ✅着地（v1409①④）＋⏳**残り2件は判断待ち**（下記） |

### A の実態（v1409・4件まとめて着地）

⚠ **犯人は予測モードではなく「🎯 反応させる分子を選ぶ」の居座りだった**
（統合レーンの事前の見立て「予測モードの退避・復帰の漏れ」は**実測で否定された**）。

ON にするとキャンバスのタップが丸ごと選択に振り替わるのに、**下ろす手段が分子モーダルの中のボタン1つだけ**。
モーダルを閉じた瞬間、ON だと分かる手がかりが画面から消え、**どこへ行っても1原子も置けない**。
不斉マーク・整形・ハース面はもともと `setTool()` が一括で下ろしていたのに、
**このモードだけ列から漏れていた**（ON にするときは他を下ろすので**片道だけ**実装されていた）。

⚠ A-3 の落とし穴: `reactions.json` の反応名は **14件中6件が「（エテン生成）」のように答えそのもの**を
抱えている。末尾の「（…生成）」だけ落とす（「（酸触媒）」「（ラジカル置換）」は残す）。

⚠ A-4 で「**分子が1つなら押せなくする**」は却下した。1つでも
「先に1つ選んでから相手を呼ぶ」のが式の左右を決める正しい順番なので、案内を1文足すだけにした。

### A-4 の残り（2026-08-18 に設計を決定・着手中）

**① 選ぶモードの常設バッジ**（`feat/select-badge`・v1412）

このモードは「モーダルを閉じてキャンバスをタップする」のが使い方なので、
**ON の印（閉じたモーダルの中のボタンの `.active`）が使用中は画面に無い**。
v1409① で出口は付いたが、**「いま入っている」ことが見えない**点は手つかずだった。

- **決定: キャンバス上の常設バッジ。**帯（`#ws-free`）に1行足す案は却下
  （320px で作業帯が既に 128px。段を増やす代償が高い）
- ⚠ **文言は「編集できません」と書かない。**止まるのは**タップの意味だけ**で、
  **↩ 戻す・分子ごとのドラッグ・全消去は生きている**。
  2分子を並べて見るときに動かせるのは欲しい操作なので、**このままでよい**（決定）
- ⚠ 編集を止めること自体は「よい／悪い」ではなく**そうするしかない**。
  同じタップに「分子を選ぶ」と「原子を置く」の2つの意味は持たせられない

**② 相手を呼び出す導線の作り直し**（`feat/partner-flow`・v1410）

`if (executable === 0)` のときしか案内が出ないため、**エタノール（単独で4件できる）では
エステル化への道が一覧に生えない**。申し立ての「作成済みの分子しか選べない」はこれ。
さらに押しても `summonMolecule` を呼ぶだけで、**モーダルも閉じず・選択もせず・実行もしない**
＝ 実際にエステル化するには**そこから7手**かかる。ボタンには行き先が書いてあるのに連れて行かない。

- **決定: 常に出す。ただし畳んだ見出しの下に。**上位N件で切るのは禁止（切った分が黙って消える）
- **決定: 押したら約束を果たす。ただし箇所数で分岐する**:

| `sites.length` | 動き |
|---|---|
| **1つ** | 呼ぶ → 両方を選ぶ → **実行** → モーダルを閉じる |
| **2つ以上** | 呼ぶ → 両方を選ぶ → **モーダルを閉じて箇所選びに入る**（`narrow()` のハイライト状態で止まる） |

  ⚠ **どちらでもモーダルは閉じる。**箇所選びはキャンバスをクリックする操作で、
  実行後の `↩ 反応前に戻す` は帯（`#ws-free`）にあるため、開いたままだと裏に隠れる
- **決定: 札に箇所数を書く**（`＋ 無水酢酸 を呼び出す → アセチル化（3箇所から選ぶ）`）。
  書かずに途中で止まると**約束を破ったように見える**
- **決定: 式の左右は気にしない**（ユーザー確認済み）
- ⚠ **セルロース＋無水酢酸のような複数生成物の型**がこの分岐の理由。
  いまの仕組みは**1回1箇所**なのでモノ・ジ・トリを作り分けられる ＝
  アセテート繊維・トリアセチルセルロースの扱いにむしろ都合がよい。
  **ただしセルロースは compounds.json に無い**（無水酢酸はある）＝ D-2/D-3 側の話

**③ うまくいかなかったことを報告できるようにする**（`feat/partner-flow`・v1411）

- **決定: 受け皿はクリップボードにコピーのみ。**貼り先（フォーム・SNS・メール）は**未定**。
  **1行で差し替えられるよう定数1つに閉じ込める**
- **決定: 仕組みは汎用に作り、最初の設置場所を②の失敗にする。**
  「行き止まりで黙る」は他でも起きる（今日だけで呼び出し・全体表示・モーダル・選ぶモードの4件）
- **決定: GA4 の自動計測も入れる。**報告ボタンは押されないのが普通なので、
  これが無いと**起きていること自体に気づけない**。
  ⚠ **送るのは「止まった段階」と「反応ルールの id」だけ。**
  正準コードのような種類が無数にあるものを送ると集計でバラけて何も読めなくなる

**②で失敗し得る場所（統合レーンが調査済み）**

| | 内容 |
|---|---|
| 選択が伴わない | 試作品は「この分子＋相手」で判定するが、呼んだだけでは何も選ばれていない ＝ **往復が発生していた理由**。②が塞ぐ |
| 呼び出し自体の失敗 | `summonMolecule()` は `false` を返しうる（名前が無い／**キャンバスの端まで分子が並んだ**・作図の上限 \|x\| > 5000・10分子ほどで到達）。**いまは戻り値を見ていない** |
| 置く場所の差 | 試作品は相手を **400 離して**置くが、実際は「いまの行の右／折り返し」。位置に依存する判定があるかは**未確認**（レーンが全ルールを当たる） |
| 大物では出ない | 重原子 **30個超**で総当たりを打ち切る。押せないのではなく出ない |

⚠ **`findPartnerHints()` は当て推量ではない** —— 候補を足した試作品を作って
`rule.detect()` を**実際に走らせている**（`reactor.js:3322`）ので、約束の根拠は堅い。

## B. モーダルが縦にはみ出す（レーン `fix/modal-overflow`）

**2つの別々の画面で報告された ＝ 共通の仕組みで直さないと3箇所目が出る。**

| # | 指摘 | 状態 |
|---|---|---|
| B-1 | 自由モードでエテン →「反応させる」「調べる」のモーダルが**縦オーバー**。他の分子でも | ✅着地（**v1407**・`a7661a8`） |
| B-2 | **クイズのモーダルも縦オーバー** | ✅着地（同上） |

### B の実態（v1407）

**原因は1つ。**`.modal-content` の高さ上限が **20枚中5枚にしか入っておらず、残り15枚が素通し**だった。
`.modal-overlay` は `align-items: center` なので、あふれると**上下が均等に切れて、どちらへもスクロールできない**
＝ 下の決定ボタンに永久に届かない。

⚠ **狭い画面向けのメディアクエリが全モーダルを救っていたため、デスクトップ幅だけが穴だった。**
⚠ **同じ型は 2026-08-10 に絞り込みモーダルで一度直されている**（そのとき他の15枚に及ばなかった）。

| モーダル | 1280×800 | 1280×600 |
|---|---|---|
| エテンの「反応させる・調べる」 | 上下316px・**画面外のボタン6個** | 上下397px・**9個** |
| 乳酸の分子モーダル | 上下249px・4個 | 上下335px・8個 |
| タイムアタック | 上下154px・4個 | 上下255px・4個 |
| 命名クイズ | なし | 上下107px・2個 |

直し方は**共通の仕組み1つ**（個別に `max-height` を足して回っていない）。
`.modal-content` に高さの上限とスクロール、**見出しと操作ボタンの帯を貼り付けて中身だけが送られる**形。
余白は `--mp-x` / `--mp-y` / `--mp-gap` に一本化（直書きすると貼り付きの覆いだけ取り残される）。
**修正後は両サイズ・全22通りで はみ出し0px・画面外ボタン0個。**

`MO1` が**20枚を悉皆で開いて測る**ので、次に増えたモーダルも捕まる。

## C. 主鎖と番号の表示（`game.js` 描画・呼び出しレーン着地後）

| # | 指摘 | 状態 |
|---|---|---|
| C-1 | **エタンの切り方**。「エタ\|ン」ではなく「**エ\|タン**」の方が自然 | ✅着地（**v1413**・幹の中を2色に。下記「C-1 の着地」） |
| C-2 | **名称のマーカーが C=C に重なると、どこが二重結合か分からなくなる** | ✅着地（**v1405**・`470319a`） |

### C-2 の実態（v1405）

**線は1本も隠れていなかった。**潰れていたのは**二重結合の2本線の「あいだ」**。
帯は太さ 11px で 2本線（間隔 10px）の**すきまをまるごと覆う**ため、
すきまが地の色からオレンジに変わり「太い1本の帯」に見えていた。

直し方は**幾何を1pxも動かさず、番号を出しているあいだだけ結合線を濃くする**
（`0.4` → `0.92`）。物差しは「**番号を出していないときのコントラストを下回らない**」。

| 状態（2-ブテン） | 直す前 | v1405 |
|---|--:|--:|
| 番号を出していない（＝基準） | 3.78 | 3.78 |
| 帯＋光（「ン」を押した状態） | **2.08** | **4.44** |

⚠ **サムネイル（答え合わせの表）の方が激しかった**（2本線の間隔 5px・すきま 2.8px）。
呼び出し口は3つとも塞いだ（キャンバス／サムネイル／アルキル基の図）。

### C-1 の判断材料（`feat/name-mark` が実測）

**⚠ 統合レーンの事前の見立て（「プロパン・ヘキサンはそう切れない」）は外れ。**
`プロ｜パン・ヘキ｜サン・ヘプ｜タン` と切れる。数詞はアルキル名（メ・エ・プロ・ブ・ペン・ヘキ）と一致する。

**⚠ さらに重要: `エタン / エテン / エチン` の対比は、いまの切り方で*すでに画面から消えている*。**
3つとも語尾のボタンは「**ン**」で字面が同じ。単／二重／三重を分けているのは
**幹の最後の1字（タ / テ / チ）**で、幹の中に埋まっている。
しかも説明文は「『エン』＝二重結合」と言うのに、**「エン」という連続した並びは「エテン」の中に無い**（エ-テ-ン）。

**ただし「エ｜タン」は一般化しない。**壊れるのは3つ（すべて実測で確認）:

1. **アルコール** —— いまは `エタ｜ノール`。新規則では `エ｜タ｜ノール` の3片が要り、
   真ん中が**かな1字**で幹ごとに姿が変わる（タ/パ/サ/ナ/カ）
2. **ジエン** —— `1,3-ブタ｜ジエン` の「タ」は buta-diene の**つなぎの母音**で**飽和の印ではない**。
   `ブ｜タジエン` は無意味で、「ア段＝飽和」という読みがここで破れる
3. **語尾が一定でない** —— タン/パン/サン/ナン/カン。「タン＝アルカン」を固定の札にできない。
   生徒が知っている数詞（ペンタゴン・ヘキサゴン）を途中で割ることにもなる

**根っこ**: 日本語では形態素の境目が**モーラの内側**に落ちる（ethane → エ+タ+ン）。
`IN10`（かけらの連結＝名前）はカタカナの厳密な分割を要求するので、**どう切っても妥協**になる。

### C-1 の着地（v1413・`feat/stem-color`）

**下の「代案」を採った。**切り方（`nameParts`）は1バイトも変えず、幹のボタンの中だけを塗り分けた。
正は [DESIGN_iupac_check.md §3-0-1](../DESIGN_iupac_check.md)。要点だけ:

- **境目は炭素数 `size` から引く**（`IUPAC_NUMERAL`。幹の表と同じ場所）。文字数で割ると
  `1,2-エタンジオール` の幹「エタン」が `エタ｜ン` になって破綻する（否定対照で実測）
- **ジエンの「タ」は `link`（つなぎの母音）**として灰＋点線で描き、「単結合」とは言わない
  —— ブタジエンに単結合の印は無いので、そう説明したら**化学的に誤り**
- **説明文を画面の字面にそろえた**。旧文「『エン』＝二重結合」は `エテン`（エ-テ-ン）に
  「エン」が無い。同じ食い違いが `ノール`（「オール」は名前に無い）にもあった。
  `SC3` (c) が「引用した語は名前の中にある」を機械で見張る（旧文に戻すと 55件で赤）
- 見張り: **`SC1`**（数詞＝3つの幹の共通の頭）・**`SC2`**（割れ方の台帳 34件＋ライブラリ全掃き）・
  **`SC3`（★否定対照）**・**`SC4`**（ボタンは1つのまま・床 32px・色以外の目印）

**代案（レーンの提案・採用済み）**: 切り方は変えず、**幹のボタンの中を2色に塗り分ける**。
「エタ」を `エ`（数詞の色）＋`タ`（段の色）で描き、
「**エ**＝炭素2個／**タ**＝すべて単結合（エタン・エテン・エチンで **タ→テ→チ**）」と説明する。
`nameParts` を1バイトも変えないので `IN10` は無傷、ジエンもノールも壊れない。
**いま消えている単／二重／三重の対比が、初めて画面に出る。**

## D. パズルモード

| # | 指摘 | 状態 |
|---|---|---|
| D-1 | **シリーズに難易度を設定**（現状は難易度が低いコンテンツがある） | ⏳**保留**（2026-08-17・ユーザー判断。決め方が定まるまで動かさない） |
| D-2 | **枝分かれの環式炭化水素**の系列を追加 | 未着手 |
| D-3 | **2,4,6-トリブロモフェノール** など教科書に載っている化合物を追加 | 未着手 |
| D-4 | **ランダム出題**の実装 | 未着手（**母集団はシリーズ内**・既出は一巡するまで出さない ← 2026-08-17 決定） |

## E. クイズ

| # | 指摘 | 状態 |
|---|---|---|
| E-1 | **タイムアタックモード・スコア表示** | 未着手（設計が要る） |
| E-2 | **結果を表示できるように** | 未着手 |
| E-3 | モーダルの縦オーバー → B-2 と同じ | 着手中（`fix/modal-overflow`） |

## F. L・D 判定

| # | 指摘 | 状態 |
|---|---|---|
| F-1 | **どの C 原子が基準なのか**が分からない | 未着手 |
| F-2 | **L/D の説明を別モーダルで**出せるようにする | 未着手 |

---

## 判断の記録（2026-08-17・ユーザー）

- **次に着手するのは C（主鎖と番号の表示）**。呼び出しレーンが着地してから
- **D-1 難易度の設定は保留。** 決め方（実測の正答率／原子数などの機械的な指標／入試の頻度）が
  定まっていない。**保留のまま D-4 を「シリーズ内」で作れば、D-1 に依存せず動ける**
- **D-4 ランダム出題の母集団はシリーズ内。** 既出は一巡するまで出さない。
  シリーズを選ぶという既存の導線をそのまま使えるので、難易度が未定でも成立する

## まだ判断が要るもの

- **E-1 タイムアタック** —— 既存の立体タイムアタックと同じ枠組みにするのか、別物か
