# V61 完全台本「立体異性体は何種類？」（クイズシリーズ②）

**完成尺 30.6秒**・縦型 9:16（1080×1920）・三面投稿＋ X。
素材アプリ: /assembler/ の **🔢 立体異性体は何種類？**（`StereoCountQuiz`）。
録画モード demo id: `quiz-count`。**収録は `--speed=2.0`**。

- ナレーション 5行・合計 27.2秒（行間 0.6秒 ×4 ＝ 通し 29.6秒）
- 完成品: `video-scripts/out/V61 立体異性体は何種類？（クイズシリーズ②）.mp4` ＋ 投稿文 `.txt`
- `meta/V61.json` は `series: クイズシリーズ` / `campaign: v61` / `prev: V60`

## 1. この回の芯

**立体異性体の数は「立体を決めるところ」がいくつあるかで決まる。** n個なら **2ⁿ が上限**で、
重なるもの（メソ体など）があればそのぶん減る。

## 2. ⚠ 「不斉炭素の個数」と言ってはいけない（2026-08-09 に踏んだ）

**最初のナレーションは「数えるのは不斉炭素の個数」と書いていた。撮ったら破綻した。**

出題は乱数で、**マレイン酸（不斉炭素 0個・シス/トランスだけ）**が出た。
アプリの解説文はこう出る:

> ⭕ 正解！ 立体を決めるところは**シス/トランスのある C=C 1本**。それぞれ2通りなので、
> 単純に数えると 2¹=2 通りです。この分子では重なるものが無いので、そのまま2種類になります。

**アプリのほうが正しく一般化していた。** ナレーションとテロップを
**「立体を決めるところ」**に直し、内訳（不斉炭素 or シス/トランスの C=C）を1行で添えた。

**教訓**: 出題が乱数の回では、**アプリが解説文で使っている語をそのまま採る**。
自分の言葉で言い換えると、出題の幅から外れる。

## 3. 画面の流れ（demos-quiz.json の6ステップと一致）

| # | 画面（操作） | テロップ |
|---|---|---|
| 0 | 🔢 立体異性体は何種類？ を開く | **（無し。頭は `--trim` で落とす。§4）** |
| 1 | 静止 | この分子、立体異性体は何種類？ |
| 2 | 静止 | 数えるのは「立体を決めるところ」の数 |
| 3 | 静止 | 不斉炭素か、シス/トランスの C=C |
| 4 | 静止 | n個なら 2ⁿ 種類が上限 |
| 5 | **正解を押す**（`quizAnswer`） | 答え合わせ |

## 4. ⚠ 1ステップ目はテロップを持たせない（収録の都合）

**このクイズは `build()` でライブラリ全件の立体異性体を数えるので、開くのに待たされる。**

| どこで | 時間 |
|---|---|
| 実機（ブラウザ） | **1.6秒** |
| 収録中（ヘッドレス＋録画） | **2.2〜23秒**（回によってばらつく。録画の負荷で最大14倍） |

その間は**空のキャンバスが映るだけ**で、テロップも出ない。だから

- **ステップ0はモーダルを開くだけ**にして `caption` を空にする
- **`--trim=<秒>` で頭を落とす**。**`--trim=auto` は使えない**——
  auto は「最初の操作の `--lead` 秒前」までしか切らないが、
  **遅延は操作の後ろにある**ので空白が残る
- 落とす秒数は毎回変わる。**収録のたびに `--events` を見て、
  ステップ1の wait が始まる時刻から 0.3秒引いた値**を渡す

実測（この収録）: `button 3.52` → `wait 5.67`（ステップ0）→ `wait 6.88`（ステップ1）。
**`--trim=6.6`** でテロップは声の 0.3秒後にそろった。

## 5. ナレーション全文（ずんだもん版・VOICEVOX）

正は [narration/V61.json](narration/V61.json)。

> この分子、立体異性体は何種類あると思う？
> 数えるのは、立体を決めるところの数なのだ。
> 4つとも枝がちがう炭素か、シス・トランスのある二重結合。このどちらかなのだ。
> 見つかった数だけ、2をかけるのだ。1個なら2、2個なら4。これが上限なのだ。
> こたえあわせ。重なるものがあれば、そのぶん減るのだ。

**「不斉炭素」は `ふせい炭素` とひらがなで書く**——漢字だと `フヒトシタンソ` と誤読する
（2026-08-09 に実際に出た。V31・V50・V52 も同じ書き方）。
いまの本文では「4つとも枝がちがう炭素」と言い換えているので、この語自体を使っていない。

## 6. 収録メモ

```
node tools/record/record.mjs --demo=quiz-count --format=short --speed=2.0 \
  --base=http://localhost:8123/assembler/
node tools/record/mux.mjs --video=video-scripts/out/quiz-count-short.webm \
  --audio=video-scripts/audio/V61/full.wav \
  --bgm=video-scripts/audio/bgm/のんきな日常.mp3 \
  --se=video-scripts/audio/se/tap.wav \
  --events=video-scripts/out/quiz-count-short.events.json \
  --meta=video-scripts/meta/V61.json --trim=6.6 --lead=0.3 --size=1080x1920 \
  --out=video-scripts/out/V61-final.mp4
```

**`--trim` の値は収録ごとに確認する**（§4）。ここが唯一の手作業。
