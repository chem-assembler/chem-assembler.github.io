# 発注書: 作図の当たり判定（2026-08-12・「化合物作ってみた」レーンからの申し送り）

**ユーザー方針の直接の続き**——

> 操作が可能なことと、やりやすいことは別なので、判定がシビアなものがあれば調整する必要があると
> 考えています。それぞれの操作に対応するエリアの面積が狭すぎたり、直感に反する位置にある場合は
> 調整が必要と思います。（2026-08-11）

大物（ニコチン・カプサイシン・インジゴ・コレステロール・モルヒネ）が描けるかを実測した結果、
**「描けない」ものはほとんど無く、詰まるのは全部 当たり判定だった**。以下は机上の推測ではなく
`tools/record/probe.mjs` と実測スクリプトで数字を取ったもの。

## 0. 先に結論

| 何を作りたいか | 成否 | 詰まるところ |
|---|---|---|
| 長い脂肪鎖（カプサイシン） | **✅ 描ける** | 14本連続で伸ばせた（C₂₀H₃₄）。ただし横一直線だと **x=1029 まで伸びて構図から外れる** |
| 環と環を単結合でつなぐ（ニコチン） | **✅ 描ける** | ただし**2つめの環を置ける窓が 70〜100px しかない**（下記2） |
| 4環の縮合（ステロイド核＝コレステロール） | **✅ 描ける** | 六員環×3＋五員環を順に縮合して 17原子。**素直に通った** |
| 環をまたぐ橋（モルヒネ・テトロドトキシン） | **✅ 作れる** | 結合ツールのドラッグで環の対角どうしを結べた |
| 環にぶら下がった鎖の先に枝（リモネン） | **⚠ 実質むり** | **有効な的が 10px 幅 × 5方位中2方位**（下記3） |

**→ アプリの表現力は足りている。足りていないのは「狙ったところに置ける」ことだけ。**

## 1. 無言で失敗するのが最大の問題

`game.js` の配置ロジックは、近傍原子が見つからないと `isValid: false` を返して
**クリックを黙って捨てる**。エラーもトーストも出ない。

- 描いている本人には「押したのに何も起きない」としか見えない
- **どちらに外したのか（近すぎ／遠すぎ／別の原子に取られた）が分からない**ので、直しようがない

**要望A（最優先）**: 置けなかったクリックに**理由を出す**。
「近すぎます」「遠すぎます」「◯◯に取られました」の3つが言い分けられれば足りる。
トーストでも、押した点にゴーストを一瞬出すだけでもよい。

## 2. 数字（実測値。すべて `--speed` 無関係の素の判定）

`game.js` の定数:

```
GRID_SIZE     = 42     ← 標準の結合長
SNAP_RADIUS   = 45     ← 既存原子への吸着半径
MIN_CLEARANCE = 27.3   ← BOND_LENGTH * 0.65。近すぎる判定
ベンゼン環炭素の置換基 = 頂点から 27.97px（BOND_LENGTH * 0.666）
```

### 2a. 結合長 42 に対して吸着半径 45 ＝ 的が「置きたい場所」の外周にしかない

**次の原子を置きたい場所（42px 先）を押すと、吸着半径 45px のふちを押していることになる。**
3px 外せば無反応。実測でも 46px で `×` に変わった。

**要望B**: `SNAP_RADIUS` を結合長より**明確に大きく**する（60〜70px 程度）。
遠すぎる側は「近傍原子なし」で弾けばよいので、広げても誤爆は増えにくい。

### 2b. ベンゼンの置換基だけ 28px、それ以外は 42px

鎖を伸ばすと **1本目だけ 28px、2本目以降は 42px** になる。

```
1本目 → (483,294) 前から 28px      ← ベンゼン環炭素の置換基は 0.666 倍
2本目 → (525,294) 前から 42px
3本目 → (567,294) 前から 42px      （以下 14本目まで 42px）
```

台本を書く側は**この1本目だけを覚えていないと必ず外す**（実際に外した）。
見た目の理由（環に近づけたい）は分かるが、**同じ操作の距離が場所で変わるのは直感に反する**。

**要望C**: 置換基の距離を 42px にそろえるか、そろえないなら
**押した点から出る位置をゴーストで見せる**（外す前に分かる）。

### 2c. 別分子として環を置ける窓が 70〜100px しかない

ベンゼンの右頂点(455,294)から N px 離して五員環モジュールを置いた結果:

```
 42px: 置けなかった   ← MIN_CLEARANCE で弾かれる
 60px: 置けなかった
 70px: 置けた（別分子として）
 80px: 置けた
 90px: 置けた
100px: 置けた
120px: 置けなかった   ← SNAP_RADIUS を超えて「近傍原子なし」
160px: 置けなかった
200px: 置けなかった
```

**幅 30px の帯を狙わないと2つめの環が置けない。** ニコチンやインジゴのように
「環と環を単結合でつなぐ」分子は、**まずこの帯を当てるところから始まる**。

**要望D**: **空いているところにはどこでも置けてよい**はずだ。
既存分子から遠い＝別分子を作る意図が明らかなので、`isValid: false` にする理由がない。
（キャンバス上限の内側であること、他の原子と重ならないことだけ見ればよい）

### 2d. 「環→鎖→枝」は有効な的が 10px × 2方位しかない（リモネンが組めない原因）

シクロヘキサンの頂点(456,273)から鎖を1本出し、その**先端(493,252)に枝を足す**ときの実測:

| 先端からの距離 | → | ↓ | ↑ | ↘ | ↗ |
|---|---|---|---|---|---|
| 20px | **削** | 削 | 削 | 削 | 削 |
| 26px | **削** | 削 | 削 | 削 | 削 |
| 32px | **○** | △環 | △環 | △環 | **○** |
| 38px | **○** | △環 | △環 | △環 | **○** |
| 42px | **○** | △環 | △環 | △環 | **○** |
| 46px | × | △環 | △環 | △環 | × |
| 50px | × | △環 | △環 | △環 | × |

○＝先端に枝が生えた／△環＝**環の炭素(456,273)に取られた**／×＝無反応／**削＝先端の原子が消えた**

**3つ問題がある**:

1. **26px 以内を押すと、足すつもりが先端の原子が消える**。
   足す操作と消す操作が**同じ場所で切り替わる**のは事故のもと（実際に4回踏んだ）
2. **5方位のうち3方位が環の炭素に横取りされる。** 先端のほうがカーソルに近いのに、
   `secondBranchPoints` の候補点が環側にあるぶん環が勝つ
3. **成功する帯は 32〜42px の 10px 幅だけ**

**要望E**: **いちばん近い原子が勝つ**ようにする（現状は「候補点まで含めた最短距離」で
遠い原子が勝つことがある）。せめて**同点に近いときは、指の直下にある原子を優先する**。

**要望F**: 「原子を消す」を**同じタップから切り離す**（消去ツールに寄せる／長押しにする）。
少なくとも **足す操作の成功帯（32〜42px）と消える帯（〜26px）のあいだに、
何も起きない緩衝帯を置く**。


## 2e. 【追加・2026-08-12】同じ台本が、収録レイアウトだと 42px ずれる

**いちばん厄介な1件。** まったく同じ手順が、

| 開き方 | 五員環モジュールの落ちる先 |
|---|---|
| `?mode=free`（ふつうに開く） | 頂点が **(546,258)** |
| `?rec=…`（収録レイアウト） | 頂点が **(546,216)** ＝ **グリッド1つぶん(42px)上** |

**再現条件**: `frame` で構図を寄せてから、**近くに原子が無い場所**へモジュールを置く。
`frame` を入れない場合は両者とも同じ位置に落ちた ＝ **構図の viewBox が効いている**。
原子を既存原子のそばに置く操作は影響を受けない（吸着先が決まるため）。

**何が起きたか**: V85（ニコチン）の台本が、下見では ニコチン C₁₀H₁₄N₂、
収録では **C₉H₁₄N₂（環が閉じない4原子）** になった。
下見が通ったのに収録が違う、という**いちばん気づきにくい形の失敗**。

**要望G**: **モジュールの落ちる位置を viewBox に依存させない。**
配置は SVG 論理座標だけで決まるべきで、画面の見え方（ズーム・要素の寸法）で変わってはいけない。

**→ 決着（2026-08-12・v1150）。** 原因は `getScreenCTM()` の**逆行列**が返す 5.7e-14 の丸め誤差で、
**誤差の向きが画面レイアウトで変わる**こと。`y=273` は 42 の倍数のちょうど半分なので、
その 1ulp が `Math.round` の分かれ目に乗って 42px 反転していた（`?rec=` はヘッダーを隠すぶん
SVG の上端が 110px → 66px へ動く）。丸める前に 1e-6 論理px より下の桁を落とす `snapToGrid()` に
寄せて解決。二分の記録と前後表は `DESIGN_hit_areas.md` §9。

**台本の作法**（残る注意）: **半格子（42 の倍数 ±21。273・21・63…）を狙わない。**
向きは固定したので下見と収録は必ず一致するが、分かれ目の上にいると
「1つ隣の格子を狙ったつもり」が読み取りにくいのは変わらない。
（`build-nicotine` は `(546,273)` → `(546,252)` に寄せた。他の台本に半格子の自由配置は無い）


## 2f. 【追加・2026-08-12】残る2本（インジゴ・コレステロール）は、どちらも既出の要望で止まっている

**新しい制限は出なかった。** 上の 2c・2d がそのまま効いて、2本とも組み上がらない。

### コレステロール ← **要望E・F**（2d と同じ）

**ステロイド核（4環17原子）も -OH も2本のメチル基も C5=C6 も通った。**
止まるのは**側鎖のたった1本のメチル基**——C17 から出した C20 に枝を足すところ。

```
atom c U from (605,365) 40px → (618,325)          ← C20 は置ける
✗ atom c ["L","R","LU","LD","RD"] from (618,325)  ← C21（メチル）はどの向きでも通らない
```

C20 は**環にぶら下がった鎖の先**なので、2d で測った「10px 幅 × 5方位中2方位」の的になる。
しかも環D の原子が近いぶん、**その2方位も取られて 0 になる**。
`C₂₆H₄₄O` まで組めて、**あと炭素1個**で `C₂₇H₄₆O` に届かない。

**→ リモネンとまったく同じ理由で止まっている。** この1点を直せば**2本まとめて通る**。

### インジゴ ← **要望D**（2c と同じ）

左右対称の2つの単位を **C2=C2' でつなぐ**分子。片方の単位は組める
（ベンゼンの右辺に五員環を縮合でき、C2 にあたる頂点が (493,336) に出る）。

止まるのは**2つめの単位を置く位置**。左右対称に並べると
2つめのベンゼンの中心は**1つめから 119〜122px** になるが、
**モジュールを置ける窓は 70〜100px** なので**置けない**。
窓に収まる 95px あたりに寄せると、こんどは2つの C2 が 18px まで近づいて重なる。

**→ 要望D（空いているところにはどこでも置ける）が入れば、そのまま組める。**


## 2g. 【決着・2026-08-12・v1200】3本とも組み上がった。残る壁は当たり判定ではない

要望 B・D・E・F・G が入った版（v1181 時点のコード）で**3本とも最後まで組める**ことを、
`?mode=free` と `?rec=` の両方で確かめた。**当たり判定を理由に止まっている回は無くなった。**

| 回 | 結果 | 名称チップ |
|---|---|---|
| **リモネン** | ✅ `C₁₀H₁₆`・原子10/結合10 がライブラリ登録の図と同一 | **リモネン** |
| **コレステロール** | ✅ `C₂₇H₄₆O`・原子28（ステロイド核17＋メチル2＋OH＋側鎖8） | **コレステロール**（v1220 で登録） |
| **インジゴ** | ✅ `C₁₆H₁₀N₂O₂`・原子20（ベンゾ2＋五員環2＋N2＋O2） | **インジゴ**（v1220 で登録） |

**→ リモネンを台本にし（V75・`build-limonene`）、残る2本は v1220 で
`compounds.json` に登録した（937→939件）。3本とも名前が出るので、
台本を書けばそのまま撮れる。** 下の手順はどちらも実測ずみ（1手も外していない）。

> **⚠ コレステロールの登録図だけ、下の手順から側鎖を階段に折ってある**（v1220）。
> 横一直線だと C17→C20 が縦から **17°** しか開かず、`_haworthFaceOf`（許容 ±25°）が
> 「ハース環の面」と読んでしまう。すると `stereo` を1文字も書いていないのに
> `getCompoundLibrary()` がエントリに `stereoCode` を付け、**立体トグル ON のとき
> この1件だけ名無しになる**（実測）。45° 開けば面が読めなくなって素のエントリに戻る。
> トポロジーは下の手順どおりで、手で組んだ図との同型（正準コード一致）は保たれている。
> 同じ穴を機械が数えるように **XL2**（立体 ON/OFF の両方で 939件すべてが名乗る）を置いた。

### コレステロール（`C₂₇H₄₆O`・28原子）

**要望E の効き方**: C17 から C20 を出す的は **(405,285)〜(437,281) の広い帯**で、
そこに置けば C20 は (408,283) に落ちて **C17 につながる**（環D側に取られない）。
C20 からの2本目の枝も**上 (408,241)・右 (450,283) の両方**が通る。
⚠ **(366,283) を押すと C18 のメチルが消える**（要望F の消去帯。左方向は使わない）。
⚠ **モジュールの自由配置は中心が 42 の倍数に丸められる**ので、環の中心は格子点で指定する。

```
六員環 (210,420) → 六員環 (282,420) → 六員環 (319,357) → 五員環 (392,357)   ← 角型のステロイド核17原子
C (355,294) C18 / C (246,357) C19 / -OH は原子 (174,441) を直接クリック
clickBond (264,451)                                                        ← C5=C6
C (415,290)→(408,283) C20 / (408,245)→(408,241) C21 / (450,290)→(450,283) C22
C (492,283) (534,283) (576,283)  C23・C24・C25 / (576,241) C26 / (618,283) C27
```

⚠ **側鎖は横一直線に置いてよい**（x は 618 まで＝ viewBox 800 に収まる）。
核を左下（環A の中心 (210,420)）に寄せるのがコツで、原点まわりに置くと側鎖が画面から出る。
⚠ **官能基モジュール（-OH など）は「接続先の原子そのもの」をクリックしないと置けない**
（`placeModule` が `clickedAtom` 必須。近くの空きを押しても無反応。ここで1回外した）。

### インジゴ（`C₁₆H₁₀N₂O₂`・20原子）

**要望D の効き方**: 左の単位を組み切ってから2つめの六員環を **(462,294)**（＝ C2 から 118px）に
置ける。**(504,294) でも置けた**ので、窓はもう帯ではない。

```
六員環 (210,294) → 五員環 (283,294) → N (286,260) → O (310,362)            ← 左の単位
clickBond (228,262) (228,325) (174,294)   ← ベンゾ環のケクレ / (292,348) ← C3=O
六員環 (462,294) → 五員環 (389,294) → N (386,328) → O (362,226)            ← 右の単位
clickBond (444,262) (444,325) (498,294) (379,240)
結合ツールで (311,294)→(361,294) をドラッグ → clickBond (336,294)          ← 中央の C2=C2'
```

⚠ **ベンゼンモジュールは六員環モジュールと形が違う**（尖りが左右・半径35）。
インジゴのように「環の右の辺に五員環を縮合して横へ伸ばす」構図では、
**六員環モジュール＋ケクレの二重結合3本**のほうが素直に並ぶ（ピリジンを作る V85 と同じ手）。

## まとめ: 優先順位

| 要望 | 効く相手 |
|---|---|
| **A** 置けなかった理由を出す | 全部（**2026-08-12 に実装ずみ**） |
| **E・F** いちばん近い原子を勝たせる／消す操作を切り離す | **リモネン・コレステロール**（**2026-08-12・§2g で3本とも通ることを確認**） |
| **D** 離れた場所にもモジュールを置ける | **インジゴ**・環と環をつなぐ分子ぜんぶ（**同上・解決ずみ**） |
| **G** 収録レイアウトで 42px ずれない | 台本の作りやすさ（**2026-08-12・v1150 で解決ずみ**） |
| **B・C** 吸着半径と置換基の距離 | 作図全般の当たりやすさ（**B は snapRadius 42→63 で入った**） |

**この発注書は役目を終えた**（2026-08-12・v1200）。最後まで残っていた
「`compounds.json` にコレステロールとインジゴが無い」も **v1220 で解消**——§2g を参照。
**残るのは台本づくりだけ**（`build-cholesterol` / `build-indigo`）で、
発注書の側の宿題は無い。

## 3. 構図の問題（作図とは別だが、動画で毎回出る）

- **長い鎖を横に引くと画面から出る**。カプサイシンは脂肪鎖が8炭素あるので
  横一直線だと確実にはみ出す。**折れ線で引くか、縦に引く**必要がある
- **名称チップが分子の上端に重なる**（V80 で撮り直した）。
  自動 fit は `obstructedInsets` で避けるようになったが、**台本の `frame` は手で避けている**。
  台本側からも「隠れない構図」を頼めるとよい

### 【追加・2026-08-13】要望H: 自由モードで「全体表示」が効かない（**アプリの不具合**）

`#btn-reset-view` の中身は `fitCanvasToTarget()` ＝ **ステージのお題に合わせる**関数なので、
**お題の無い自由モードでは押しても何も起きない**。呼び出した長い分子や反応の生成物が
画面から出たままになる（2026-08-12 検品: V91 のジエチルエーテル・V94 のヨードホルムが
どちらも画面の外へ出ていた。生成物は反応のたびに広がるので、押した人ほど困る）。

**直し方**: お題が無いとき（自由モード・名称呼び出し）は `fitCanvasToMolecule(this.userMolecule)`
を呼ぶ。関数は既にあり、名称呼び出しの経路で使われている。**動画側は `frame` で回避ずみ**なので
急がないが、**これは利用者にも起きる**（動画だけの問題ではない）。

### 【追加・2026-08-13】要望I: 同じ分子を2つ呼び出すと、向きがそろって背中合わせになる

`summon` を2回すると **C-C-O が2つ横に並ぶ** ＝ -OH が背中合わせ。
分子間脱水（2分子の -OH から水がとれる）を見せるとき、**どこから水が出るのかが読めない**
（2026-08-13 ユーザー検品）。**向かい合わせ（C-C-O ／ O-C-C）に置ければ、
反応の絵がそのまま説明になる**。

**案**: ① 2つめ以降を呼び出すとき、**先に置いた分子の官能基に近い側へ鏡像で置く**／
② 反応を実行するとき、反応する原子どうしが向き合うように**反応前に並べ直す**。
②のほうが効きは広い（呼び出しに限らず、手描きでも効く）が、
**「利用者が描いた図を勝手に動かす」ことになる**ので、
やるなら「並べ直しました」と分かる動き（アニメーション）が要る。

**動画側は手描きで回避した**（V91 は前半だけ自分で2分子描いている）。

## 4. 監査への申し送り

`audit.js` の ④（タップ目標の面積と直感）は**この形を見ていない**。
対象分子に「**環 → 鎖 → その先端に枝**」を1件足せば、上の 2d が機械で出る。
判定は「**足せる方位が5方位中いくつあるか**」と「**成功する距離の帯の幅**」でよい。

## 2g. 【追加・2026-08-12】伸長した結合線が、既存の原子の下をくぐる（ユーザー報告・再現ずみ）

> **決着（2026-08-12・v1160）**: 塞いだ。判定は `atomUnderBondLine`（しきい値 16px）に1本化し、
> 手描きの配置・整形モード・夜間監査が同じ物差しで見る。§2g の手順で **5.6px → 18.2px**。
> 登録図 923件は1件も弾かれない（最小 25.99px）。回帰テストは `BX1`〜`BX4`（`BX3` は否定対照）。
> 経緯・しきい値の根拠・前後表は `DESIGN_hit_areas.md` §10。
>
> **続き（2026-08-13・v1240）**: 残っていた**自動水素**のぶんも塞いだ
> （`hydrogenUnderBondLine`・しきい値 **12px**。重原子の 16px とは別に持つ）。
> 登録図まわりの 116,872 通りの置き方のうち **719 通り**でくぐっていたのが **0 通り**に。
> 登録図 939件は1件も弾かれない（最小 14.32px）。回帰テストは `HX1`〜`HX4`。
> 詳細と代償（置けなくなった 387通り）は `DESIGN_hit_areas.md` §10-7。

**要望A〜G とは別の穴。** ユーザーの手順そのままで再現した:

1. 六員環の1つの炭素にメチル基を付ける
2. そのメチル基から**下へ**伸ばしてエチル基にする
3. **同じ環炭素**（枝の付け根）に、もう1本メチル基を付ける（右向き）

```
新しい結合: (456,273)-(505,273)  長さ 48px  ← 42px から**伸長されている**
その線から 5.6px の位置に C(477,279) が居る   ← 原子の丸は半径10px ＝ 線の上に乗る
```

**原因**: `MIN_CLEARANCE`（27.3px）は**原子どうしの距離しか見ていない**。
置き場所が詰まると結合を最大 84px まで延ばして逃がすが、
**延ばした線が既存の原子の下を通るかは誰も見ていない**。

**この検査は他の経路には既にある**:
- `reactor.js` … 生成物の配置で「結合線が無関係な原子を貫通しない」ことまで見る（strict）
- `game.js:3542` … 整形モードで「結合が別の重原子を貫通した」ら座標を戻す
- **手描きの配置だけ無い**

自動水素も同じ穴にある（**モデルに存在せず描かれているだけ**なので、
`MIN_CLEARANCE` の対象外。登録図は `verify-compounds.js` が 11.5px の近接を報告しているが、
手描きには相当する見張りが無い）。

**測り方**: 重原子と、その原子を端点に持たない結合線との距離（点と線分の距離）。
素の格子では最小 21px（= 42·sin30°）なので、**21px を明確に下回ったら異常**。

⚠ 総当たり（側鎖3通り × 全周24方向 × 距離3通り）では**出ない**。
**伸長が起きる配置**でだけ出るので、再現には「詰まった場所へ足す」手順が要る。

## 2h. 【決定・2026-08-12】個々の原子のドラッグを廃止し、ベンゼンの特別扱いをやめる（案B-2）

**ユーザー判断。** きっかけは「**ベンゼン環の原子だけドラッグできて変な感じがする**」。

### 調べて分かったこと

- 個々の原子のドラッグは**設計された機能ではなく、ジェスチャの割り当ての余り**だった。
  普通の原子はタップが「消す／置き換える」で埋まっているので、
  **タップが空いている原子（ロック原子・ベンゼン環の原子）にだけドラッグが残っていた**
- 動かすと**六角形が1頂点だけ歪む**（環ごとではない）。
  間隔を空けるなら**結合線のドラッグ（伸縮）**、どかすなら **Shift＋ドラッグ（分子ごと）**が
  本来の道具で、どちらも別に用意されている
- **ベンゼンの炭素を消しても問題は起きない**（実測）:

  ```
  消す前  C₆H₆  「ベンゼン」        官能基: ベンゼン環（芳香族）
  消した後 C₅H₈ 「1,3-ペンタジエン」 官能基: C=C二重結合 ×2 ／ 価標エラーなし
  ```

  ケクレ構造で持っているので素直にペンタジエンになる。**守っていた理由はモデルの安全ではなく
  「うっかり環を壊さない」というUXの都合**で、しかも**消しゴムツールでは既に消せる**（守り切れていない）
- 消したあと、**残った原子に「ベンゼン印」（`benzeneCenter` / `benzeneAngle`）が付いたまま**になる。
  この印が付いた原子は置換基を **42px ではなく 27.97px・環の外向き**に置く特別扱いを受けるので、
  **もうペンタジエンなのに置換基だけベンゼンの作法で付く**

### やること（順序つき）

1. **個々の原子のドラッグを廃止**（ベンゼン・ロック原子とも）
2. **ベンゼンの特別扱いをやめる** — 同元素タップで他の原子と同じく消える
3. **消したあとに残る「ベンゼン印」を落とす**（環でなくなったら印も外す）
4. **`ZD1`・`ZD2` を Shift＋ドラッグ側へ移し替える**。
   ⚠ **0.0px の完全重複を作らない罠は必ず残すこと**（v736 の実事故の再発防止で置かれたもの）

### 決定の理由

例外を1つ減らすのではなく**ゼロにする**。残る規則は2行:

> **原子は同元素タップで消える。個々の原子は動かせない**（分子ごとに動かすか、結合を伸ばす）

事故の心配は v1131 で下がっている ＝ **タップで消えるのは原子の絵から 18px 以内だけ**（旧 28px）。

**着手は `?rec=` の 42px ずれ（要望G）が片づいてから**（同じ `game.js` を触るため）。

### 決着（v1180・2026-08-12）

4項目とも入った。**例外はゼロ**になり、規則は宣言どおりの2行だけになった。

| 項目 | 入った形 |
|---|---|
| 1 個々の原子のドラッグを廃止 | `handleMouseDown` の select 分岐から掴みの枝を削除。`handleMouseUp` に残るのは Shift＋ドラッグだけ |
| 2 ベンゼンの特別扱いをやめる | 同元素タップで削除（`C₆H₆` →`C₅H₈`）。異元素タップの置換は従来どおり |
| 3 ベンゼン印を落とす | `dropStaleBenzeneMarks()` を `updateDrawing()` の先頭で1回 |
| 4 `ZD1`・`ZD2` の移し替え | 判定を `canMoveComponentBy()` として切り出し、Shift＋ドラッグ側で同じ罠を張り直した |

- **印を落とす条件は「環結合を1本も持たなくなったこと」**（既存の `_ringAtomIdSet()` を再利用）。
  見かけ（六角形か・炭素6個か）では決めない。印が指しているのは
  「置換基は**環の外向き**へ 27.97px」という作法なので、**環でなくなった時点で意味を失う**から。
  縮合環で片方だけ壊れた場合、残る環に乗っている原子は環結合を持つので印が残る。
  経路（削除・消しゴム・右クリック・結合の削除）ごとに書かず、
  **描き直しのたびに前提を見直す**（印を持つ原子が無ければ即 return なので費用はゼロ）
- **ロック原子**は消せないまま。無反応にせず
  「ここ（ロックした原子）は消したり別の元素に変えたりできません。」を出す（GB4）
- 副産物: `DESIGN_hit_areas.md` §8-1 の「ベンゼン環の原子から 19〜28px はタップしても何も起きない」が
  **掴みの半径を下げずに**消えた（その帯も配置経路へ落ちるようになったため）。§8-2 に追記
- テストは 355 → 359 件（`GB1`〜`GB4` を追加）。全合格

### 置き土産の後始末（決定 2026-08-13・v1260）——「指が動いたタップはタップではない」

**B-2 の決定は覆っていない。**「**個々の原子は動かせない**」はそのまま。
塞いだのは、ドラッグの枝を消したことで生まれた**副作用**だけ:

> 原子の中心を押して指を引きずると、**動かないのではなく「消える」**（統合レーンが実機で確認）

```
③-a 素の炭素を中心から引きずる: [[420,294],[462,294]] → [[462,294]]   ＝ 消えた
③-b ベンゼン環炭素を引きずる  : 6個 → 5個                            ＝ 消えた
```

**閾値を足すだけでは直らなかった。** 削除・置換は `handleMouseDown` の中で**押した瞬間に**
起きていたので、「動いたら取り消す」を書く場所が無い（動いたかは離すまで分からない）。
**破壊操作の実行を `handleMouseUp` へ遅らせた**:

| 時点 | やること |
|---|---|
| `handleMouseDown` | 破壊操作は実行せず、対象を `this.pendingTap` に**控えるだけ** |
| `handleMouseUp` | `resolvePendingTap()` —— 押した点から **8 client px** を超えて動いていたら**何もせず終わる** |

- **配置（`placeAtomOrExplain`）は押した瞬間のまま**。非破壊で Ctrl+Z 1回で戻せるうえ、
  ホバーのプレビューが「ここに出る」と見せている ＝ 指を離す前に出るほうが手応えが良い。
  遅らせると「少しぶれたら置けない」を新しく作ることになる
- 取り消したときは**黙って終わる**（「動かせません」と言うと、動かないこと自体は決定どおりなのに毎回叱られる）
- 8px の根拠と前後の実測表は `DESIGN_hit_areas.md` §8-3。要点は
  ①**同ファイルの結合線の伸縮がすでに 8px** を使っている ②端末の tap slop が 8dp
  ③縮尺の実測（1 client px = 0.957 論理px @ iPhone 12）から **8px = 最悪 7.7 論理px < `ATOM_TAP_RADIUS` 18**
- **消しゴムツールは範囲外**（引きずっても押した点の1個が消える。実測でも変わっていない）。
  同じ危うさはあるが、消しゴムは「消す」と宣言して選んだ道具で、
  「なぞって連続で消す」が期待かもしれない ＝ **変えるかどうかはユーザー判断**
- テストは 374 件（`TC1`〜`TC6` を追加。マウス・タッチの両方で回す）。
  **`TC5` が否定対照**（`HIT_AREAS.legacyTapNoCancel` で本番の経路を旧挙動で走らせる）。全合格
