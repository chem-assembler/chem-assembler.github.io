# V32 完全台本「-OH があるのにアルコールじゃない」（官能基シリーズ④）

V31 の次回予告を回収する回（2026-08-01）。縦型 9:16（1080×1920）・三面投稿＋ X。
素材アプリ: /assembler/（パズルでみる有機化学）。録画モード demo id: `fg-phenol-vs-alcohol`。
**収録は等速（`--speed` を付けない）**。

- ナレーション 8行・合計 38.7秒（行間 0.6秒 ×7 ＝ 通し **42.9秒**）。LANES.md §3-7 の「30〜45秒」に収まる
- **V13 → V30 → V31 に続く官能基シリーズ④**。次は「官能基はいくつある？」（数え上げクイズ）を予告している
- 作図も反応実行もせず、**`summon` と `frame` だけ**で組んである（V31 と同じ型）

## 0. ⚠ 収録前に座標と `frame` を実測し直すこと

**この台本の座標はすべて 2026-08-01 時点の `compounds.json` の実測値**である。
アプリ改修セッションが **C-4b（主鎖を一直線に）／C-6（呼び出しの初期配置）で compounds.json の
座標を書き換えている最中**なので、改修が終わったら**必ず次の順で測り直す**:

1. 自由モードで `シクロヘキサノール` → `ヒドロキノン（p-ジヒドロキシベンゼン）` → `2-ナフトール` を
   この順に呼び出す
2. 実機コンソールで分子ごとの座標範囲とキャンバス見出し（`text` 要素）の `getBBox()` を読む
3. §4 の表を上書きし、`assembler/demos-fg.json` の `frame` を入れ替える
4. **LANES.md §4 の貫通チェックを流し直す**（空配列であること）

`wait`（§5）はナレーション音声だけで決まるので、**座標が変わっても計算し直す必要はない**。

## 1. この回の芯: 「-OH がある＝アルコール」を1本で壊す

高校化学で最も踏みやすい取りちがえが「**-OH があればアルコール**」。
実際は **-OH がついている炭素が芳香環の一員かどうか**で名前も性質も変わる。

アプリはこの区別を**自前で判定して持っている**。`chemistry.js` の `findFunctionalGroups()` は
`-OH` の隣の炭素が芳香環に属していれば `phenol`＝**「フェノール性ヒドロキシ基」**、
そうでなければ `alcohol1/2/3`＝**「1級／2級／3級アルコール」**に振り分ける（`chemistry.js:756-780`）。
つまり**この回の主張はアプリの実装そのもの**で、台本が勝手に言っているのではない。

さらに都合がよいことに、**芳香環はキャンバス上で交互の二重結合として描かれる**（実測: 3分子で
二重結合が8本＝ヒドロキノン3本＋2-ナフトール5本）。
**「環の中に二重結合が並んでいるかどうか」が、そのまま画で見える**ので、
テロップで主張しなくても視聴者の目で判定できる。ここがこの回を成立させている一点。

### 素材の選び方（なぜこの3つか）

- **① シクロヘキサノール**（アルコール側）… 六角形の環に -OH がついているのに**アルコール**。
  「環＋OH＝フェノール」と早合点しがちな形をわざと最初に置く。環は単結合だけ
- **② ヒドロキノン**（フェノール側）… ①と**同じ六員環**。ちがいは環の中の二重結合だけ。
  この2つが並ぶことで「何が効いているか」が1つに絞られる
- **③ 2-ナフトール**（フェノール側）… 芳香環が2つに増えても同じ結論、という一般化の駄目押し

## 2. 画面の流れ（demos-fg.json の8ステップと一致）

| # | 画面（操作） | テロップ | ナレーション |
|---|---|---|---|
| 1 | 自由モードへ → 3つを続けて呼び出し、引きで並べる | 3つとも -OH。アルコールはどれ？ | 01-hook |
| 2 | ① に寄る（width 230） | ① 六角形は単結合だけ → アルコール | 02-alcohol |
| 3 | ② に寄る（cy・width 同じ＝真横に振るだけ） | ② ベンゼン環に -OH → アルコールではない | 03-phenol |
| 4 | ③ に寄る（同上） | ③ こちらもベンゼン環に -OH | 04-naphthol |
| 5 | 引く（width 660） | -OH がベンゼン環に直結 ＝ フェノール類 | 05-answer |
| 6 | 静止 | フェノール類は弱い酸性 → NaOH 水溶液に溶ける | 06-acid |
| 7 | 静止 | アルコールは中性。ここが分かれ目 | 07-contrast |
| 8 | 静止 | 次回: 官能基はいくつある？ | 08-next |

- **キャンバス上に「① シクロヘキサノール」「② ヒドロキノン（p-ジヒドロキシベンゼン）」
  「③ 2-ナフトール」の見出しが出る**ので、テロップ側で名前を書き直していない（V11・V31 と同じ）
- **分子式はどのテロップにも書いていない**。名称チップは3分子ぶんの合計 **C₂₂H₂₆O₄** を出すので、
  個別の分子式を書くと画面と食い違う（V11・V31 で踏んだ落とし穴）

## 3. ナレーション全文（ずんだもん版・VOICEVOX）

正は [narration/V32.json](narration/V32.json)。

> 3つとも、オーエイチをもっているのだ。アルコールはどれだと思う？
> シクロヘキサノール。ろっかくけいは単結合だけ。アルコールなのだ。
> ヒドロキノン。こちらはなかに二重結合。ベンゼンかんなのだ。
> ナフトール。こちらもベンゼンかんに、直接オーエイチなのだ。
> オーエイチでも、ベンゼンかんに直結したら、フェノール類なのだ。
> フェノール類は弱い酸性。水酸化ナトリウムの水溶液にとけるのだ。
> アルコールのほうは中性。ここが分かれ目なのだ。
> 次は、かんのうきの数え上げなのだ。

**読み仮名は8行すべて確認済み**（ミッツトモ／オオエイチ／ロッカクケイ／タンケツゴオ／ヒドロキノン／
ニジュウケツゴオ／ベンゼンカン／チョクセツ／チョッケツ／フェノオルルイ／ヨワイサンセエ／
スイサンカナトリウム／スイヨオエキ／チュウセエ／ワカレメ／カンノオキ／カゾエアゲ）。

### この回で見つかった誤読（LANES.md §4 に足すこと）

- **「ベンゼン環」は漢字だと ベンゼン**タマキ**と読まれる**（1回目の合成で実際に踏んだ）。
  既知の「環→たまき」がそのまま出る。**`ベンゼンかん` と仮名で書く**
- **「六角形」は ロッカッケー と縮めて読まれる**。`ろっかくけい` と仮名で書くと正しく ロッカクケイ になる
- **「官能基」は「基→もと」の系統で崩れうる**ので `かんのうき` と仮名で書いた（→ カンノオキ）

### 「2-ナフトール」の「2」は読ませていない

検品 B-3（「2-メチル…」の「に」が短くて埋もれる）を踏まえ、**ナレーションでは単に「ナフトール」**と読む。
1位か2位かはこの回の主題ではなく、**テロップとキャンバス見出しに「③ 2-ナフトール」と出る**ので
情報は落ちない。位置番号を読ませる必要がある回では「にばんの」「にの位置に」と伸ばすこと。

## 4. 座標と構図の根拠（2026-08-01 実測・**改修後に要再測**）

呼び出しは 210px きざみで右へ置かれる。実機で `game.userMolecule.atoms` と
キャンバス見出しの `getBBox()` を読んだ値:

| 対象 | 分子の範囲 | キャンバス見出しの範囲 | frame |
|---|---|---|---|
| ① シクロヘキサノール | x 363.6–436.4 / y 216–342 | x 323.4–476.6 / y 373.3–394.3 | `cx 400, cy 285, width 230` |
| ② ヒドロキノン | x 528–692 / y 265.4–334.6 | x 458.7–761.3 / y 365.9–386.9 | `cx 610, cy 285, width 230` |
| ③ 2-ナフトール | x 763–920.5 / y 263.7–354.6 | x 787.8–895.7 / y 385.9–406.9 | `cx 842, cy 285, width 230` |
| 3分子＋見出しまとめて | x 363.6–920.5 / y 216–354.6 | 合わせて x 323.4–920.5 / y 216–406.9 | `cx 622, cy 311, width 660` |

- **寄りの3枚は `cy 285`・`width 230` で固定**。`cx` だけ 400→610→842 と動くので、
  カメラは純粋な横パンになる（V31 と同じ作り）
- **`width 230` は「分子だけを入れて、見出しを画面外に落とす」値**。
  高さは 230×3/4 ＝ 172.5 なので y は 198.75〜371.25。いちばん上にある ① の見出し（y 373.3〜）が
  ぎりぎり外に出る。**250 に広げると隣の分子（② は x 528 から）が 14px 覗く**ので広げない
- 引きの `cx 622, cy 311, width 660` は**見出しまで入れる値**（union は x 323.4–920.5 / y 216–406.9）。
  名前を読ませるのは引きの絵だけなので、ここだけ見出しを入れる

### ⚠ 見出しどうしが 18px 重なる（引きの絵で要確認）

**「① シクロヘキサノール」の右端 476.6 と「② ヒドロキノン（p-ジヒドロキシベンゼン）」の左端 458.7 が
横に 17.9px 重なり、y も 366〜394 で重なっている。** ② の名前が長いのが原因（正式名の
かっこ書きまで見出しに出る）。引きの絵で読みづらいなら、収録時に次のどちらかを採る:

- **呼び出す順を ① シクロヘキサノール → ② 2-ナフトール → ③ ヒドロキノン に入れ替える**。
  実測するとこの順では重なりが消える（② の分子の右端 668.5 ／ ③ の見出しの左端 668.7）。
  ただし「①と③が同じ六員環」という対比が最後に来るので、ナレーションの 03/04 も入れ替えが要る
- 引きの `width` を 700 前後まで広げて、見出しを小さく見せて逃げる

**どちらを採るかは改修後の座標を見てから決める**。改修で間隔が変われば重なり自体が消える可能性もある。

### 名称チップが長い（要目視）

3分子を並べたときのチップは
**「① シクロヘキサノール ＋ ② ヒドロキノン（p-ジヒドロキシベンゼン） ＋ ③ 2-ナフトール　C₂₂H₂₆O₄」**
で約58文字。`rec-short` のチップは 25px・`max-width: 86%`・`top: 23%` なので、
**4行に折り返して画面の 33% あたりまで伸びる見込み**（1行 35px 換算）。
分子はキャンバス中央（およそ 50%）なので被らないはずだが、**完成 mp4 のフレームで必ず目視すること**。
被るようなら、上の「呼び出す順の入れ替え」ではなく **`#btn-clear-all` を挟んで1分子ずつ見せる構成**に
落とす（チップが1分子ぶんになって短くなる。ただし横並びの対比は失われる）。

## 5. `wait` の計算（音声の実測値から逆算・**収録前に実測はできないので計算値**）

`narrate.py` が出した wav の長さを直接測った値と、LANES.md §4-2 の等速コスト表から逆算した。
`wait =（その行の秒数 ＋ 0.6）−（アクションの実測コスト合計 ＋ 1.13）`

| step | 行 | 秒 | アクションのコスト（等速） | **`wait`(ms)** |
|---|---|---|---|---|
| 1 | 01-hook | 5.291 | button 1.34 ＋ summon 1.04×3 ＋ frame 0.66 ＝ **5.12** | **50** ← 下記 |
| 2 | 02-alcohol | 5.344 | frame 0.66 | **4150** |
| 3 | 03-phenol | 5.163 | frame 0.66 | **3970** |
| 4 | 04-naphthol | 4.981 | frame 0.66 | **3790** |
| 5 | 05-answer | 5.141 | frame 0.66 | **3950** |
| 6 | 06-acid | 5.739 | なし | **5210** |
| 7 | 07-contrast | 3.904 | なし | **3370** |
| 8 | 08-next | 3.147 | なし | **2620** |

**step1 だけは計算どおりに置けない。** `wait` を 50ms（実質ゼロ）まで削っても
`5.12 ＋ 0.05 ＋ ステップ間 1.13 ＝ 6.30秒`かかり、声（5.291 ＋ 間 0.6 ＝ 5.891秒）より **0.41秒長い**。
これは V31 の step1 とまったく同じ状況で（V31.md §5）、**この 0.41秒がそのまま後続へ持ち越されて
テロップが声の +0.4秒あとに出る**。狙いの「+0.1〜0.4秒あと」の上端なので許容できる。

> **呼び出しを4分子に増やすと破綻する**（V31.md §5 と同じ）。この構成は3分子が上限。

## 6. 収録メモ（**アプリ改修の完了後**）

```
node tools/record/record.mjs --demo=fg-phenol-vs-alcohol --format=short --base=http://localhost:8152/assembler/
node tools/record/mux.mjs --video=video-scripts/out/fg-phenol-vs-alcohol-short.webm \
  --audio=video-scripts/audio/V32/full.wav \
  --bgm=video-scripts/audio/bgm/のんきな日常.mp3 \
  --se=video-scripts/audio/se/tap.wav \
  --events=video-scripts/out/fg-phenol-vs-alcohol-short.events.json \
  --meta=video-scripts/meta/V32.json --trim=auto --lead=0.3 --size=1080x1920 \
  --out=video-scripts/out/V32-final.mp4
```

- **冒頭 3.5秒ほどはシートが開いている**（`summon` が右パネルを開くため。V11・V31 と同じ・仕様）。
  カバー画像には使わず、**3分子が並んだ 6〜7秒あたり**を選ぶ
- 音声は作成済み（`video-scripts/audio/V32/`。`audio/` は .gitignore 対象なのでリポジトリには入らない）

## 7. 実機で確かめたこと（2026-08-01・呼び出しのみ。収録はしていない）

### フェノール単体は収録されていない

`compounds.json` の**全143件を走査して確認**。「フェノール」単体・サリチル酸・クレゾール・
カテコール・レゾルシノール・ピクリン酸は**いずれも無い**。
フェノール性ヒドロキシ基をもつのは次の5件だけ:

**ヒドロキノン（p-ジヒドロキシベンゼン）／1-ナフトール／2-ナフトール／チロシン／
p-ヒドロキシアゾベンゼン（p-フェニルアゾフェノール）**

### 分子式と、アプリ自身の官能基分類（「⚗ この分子の反応」カード）

キャンバスに1つだけ置いて `#molecule-props` を読んだ実測値:

| 名称 | 分子式 | アプリの分類 |
|---|---|---|
| シクロヘキサノール | **C₆H₁₂O** | **2級アルコール** |
| ヒドロキノン（p-ジヒドロキシベンゼン） | **C₆H₆O₂** | **フェノール性ヒドロキシ基×2、ベンゼン環（芳香族）** |
| 1-ナフトール | C₁₀H₈O | フェノール性ヒドロキシ基、ベンゼン環（芳香族）×2 |
| 2-ナフトール | **C₁₀H₈O** | **フェノール性ヒドロキシ基、ベンゼン環（芳香族）×2** |
| チロシン | C₉H₁₁NO₃ | カルボキシ基（カルボン酸）、アミノ基、フェノール性ヒドロキシ基、ベンゼン環（芳香族） |
| ベンゼン | C₆H₆ | ベンゼン環（芳香族） |

3つ並べたときは **「【3分子】 2級アルコール、フェノール性ヒドロキシ基×3、ベンゼン環（芳香族）×3」**。
**テロップの主張はアプリの判定と完全に一致している。**

> **この分類テキストは画面に写らない。** `#molecule-props` は右パネル（モバイルではシート）の中にあり、
> `frame` はシートを閉じるので、`format=short` の収録では見えない。
> **画で見せられるのは「環の中の二重結合の有無」だけ**なので、証拠の役はテロップが負う。
> 「アプリもそう判定している」は投稿文（YouTube の説明欄）に書いてある。

### 貫通チェック（LANES.md §4 の必須項目）

3分子を呼び出した状態で実機コンソールに流し、**空配列**を確認。
結合の内訳は**単結合19本・二重結合8本**で、芳香環が交互二重結合として描かれていることも確かめた。

## 8. 投稿メタ

正は [meta/V32.json](meta/V32.json)。`V32-final.txt` の「ここから貼る」をコピーするだけ。

- **官能基シリーズ④**。V13 → V30 → V31 → **V32** の順で再生リストに入れ、X は V31 のツイートにぶら下げる
  （`meta/V32.json` の `"prev": "V31"`）
- X の本文は**重み 206**（上限280・目安260。`node tools/videos.js` で検算済み）
- カバー画像は**3分子が並んだ冒頭**（6〜7秒付近。「3つとも -OH。アルコールはどれ？」が出ている画）。
  V31 と同じクイズ型なので、選択肢が見えている画がいちばん止まる
- 次の予告は**「官能基はいくつある？」**（LANES.md §5 fg の数え上げクイズ枠 `#btn-count-quiz`）。
  この回で「-OH は2種類に分かれる」を教えたので、**1つの分子の中の官能基を数える**回に自然につながる
  （チロシンならカルボキシ基・アミノ基・フェノール性ヒドロキシ基・ベンゼン環の4つ）。
  **`quiz.js` を使う回なので、収録はやはりアプリ改修の完了後**（PLAN_during_refactor.md「やらないこと」）
