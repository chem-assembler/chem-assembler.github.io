# V105 台本「C₄H₉– は4種類」（異性体シリーズ・アルキル基）

**縦 9:16・目標尺 35〜40秒・ずんだもん**。題材は **✏️ アルキル基の書き出し練習**。
まだ1本もショートにしていない機能。**「分子ではなく、分子の一部」を数える**のがこの回の芯。

## 0. 実測（**v1389**・2026-08-16 に実機で確認しなおした）

**📚学習 → `✏️ 構造の書き出し練習` → `✏️ アルキル基の書き出し練習`**（⚠ アコーディオンは既定で閉じている）

**お題は3つ**: `C₃H₇–（2種）` / **`C₄H₉–（4種）`** / `C₅H₁₁–（8種）`

`C₄H₉–` を始めたときのパネルの文（そのまま使える）:

> ✏️ アルキル基 C₄H₉–（全 4 種）
> **これは分子ではなく、分子の一部（基）です。R は他の原子とつながる手を表します**（見出しの「–」がその手）。
> キャンバスが答案用紙です。**枠の C1 から炭素を伸ばして基を1つ描き**、
> 別の基を描くときは「＋ 答案をもう1つ」で枠を増やします。

- **始めた時点でキャンバスに付け根が置かれている**（実測: `H H H C R ①`。`R` が結合手、`①` が枠の番号）
- 画面に残る帯（**2026-08-16 に再実測。v1389 で変わった**）:
  **`お題 C₄H₉–（分子ではなく基）全 4 種 ／ いま 0個 描いてあります`**
  ＋ `＋ 答案` / `🔍 答え合わせ` / `🔎 確認` / **`🧹 並べ直す`** / `やめる`
- ✅ **単位は「個」にそろった**（発注書 §C の決着。以前は「枠」だった）。
  3つの練習すべてが「いま N個 描いてあります」になっている
- ✅ **2つ目からは `＋ 答案` を押さなくてもよい**——パネルにこう書いてある:
  「**2つ目からは、空いている所を炭素でタップすれば付け根がその場に出ます**」
- ✅ **`🧹 並べ直す` が増えた**（W4）。散らかった答案を剛体移動だけで整列できる。
  **4枠が横に広がる問題の受け皿になる**（下の章4の注意）

## 1. なぜアルキル基か

**高校で「ブチル・イソブチル・sec-ブチル・tert-ブチル」を名前だけ暗記させられる所**だから。
4つあることは教わるが、**なぜ4つで打ち止めなのか**は手を動かさないと分からない。
`C₄H₉–` は炭素4個ぶん——数えるのに向いた大きさで、**30秒で全部描ける**。

**分子の異性体（V4・V9・V90…）とは別物**として立つ。分子は両端が閉じているが、
基は**片方が開いている（R）**。だから「どこに R を付けるか」で数が変わる——ここが芯。

## 2. ナレーション（ずんだもん）

> **フック（0:00–0:08）**
> ブチル基。C₄H₉。**4種類ある**と習うけど、なぜ4つで止まるのだ？

> **描く（0:08–0:28）**
> 付け根は最初から置いてあるのだ。R が、ほかとつながる手。
> まっすぐ4つ——**ブチル**。
> 枝を1つ横に——**イソブチル**。
> R の隣を分ける——**sec-ブチル**。
> 真ん中に3つ集める——**tert-ブチル**。

> **落ち（0:28–0:38）**
> 骨組みは2つしかないのに、**R をどこに付けるかで4つになる**のだ。
> これは分子ではなく、分子の**一部**。だから片方が開いているのだ。

## 3. テロップ

| 秒 | 文字 |
|---|---|
| 0:00 | **C₄H₉– は4種類** |
| 0:10 | R ＝ ほかとつながる手 |
| 0:14 | ブチル／イソブチル |
| 0:22 | sec-ブチル／tert-ブチル |
| 0:30 | **骨組みは2つ。R の位置で4つ** |

## 4. 収録（手動キャプチャ）

| # | 押すもの |
|---|---|
| 0 | 上の **📚学習** → **`✏️ 構造の書き出し練習`** を開く |
| 1 | **`✏️ アルキル基の書き出し練習`** の **`C₄H₉–（4種）`** |
| 2 | パネルの文（**分子ではなく分子の一部**）で**2秒静止** |
| 3 | 枠①に**まっすぐ4つ**（ブチル） |
| 4 | **空いている所を炭素でタップ** → 付け根がその場に出る → **枝を1つ**（イソブチル） |
| 5 | 同じく空いた所をタップ →（sec-ブチル） |
| 6 | 同じく空いた所をタップ →（tert-ブチル） |
| 7 | ⭐ 帯の **`🧹 並べ直す`**（4つが整列する。**ここが絵として効く**） |
| 8 | 帯の **`🔍 答え合わせ`** → 全部そろった画で**4秒静止** |

⚠ **答え合わせは最後の1回だけ**（異性体の練習と同じで、押すと終わる型のはず。
**1テイク目で確かめる**——違っていたら台本のここを直す）。

⚠ **切り出しはキャンバス側**（L1 リールの `crop=960:720:540:90 → 1080:810` が使える）。
**4つ並ぶので横に広がる**——1テイク目で**縦枠に収まるか**を確認する。
✅ **`🧹 並べ直す` が入った（v1389）ので、以前より収めやすい**。
それでも収まらなければ**2つずつ2画面**に割る（ナレーションはそのまま使える）。

## 5. 投稿メタ（meta/V105.json に転記）

- **タイトル案**: 【高校化学】C₄H₉– はなぜ4種類？ #Shorts
- **series**: `異性体シリーズ`
- **ハッシュタグ案**: #異性体シリーズ #アルキル基 #化学 #有機化学 #Shorts #高校化学
- **固定コメント案**: 「骨組み（炭素のつながり方）は2つしかありません。
  R をどこに付けるかで4つに分かれます。C₅H₁₁– なら8種類。アプリで数えられます。」
