# V50 完全台本「乳酸の D体と L体」（立体シリーズ④）

LANES.md §5 stereo のネタ「乳酸の D体と L体」を実制作したもの（2026-08-01）。
**縦型 9:16（1080×1920）・三面投稿＋ X**。素材アプリ: /assembler/（パズルでみる有機化学・v345 で確認）。
録画モード demo id: `stereo-lactic-dl`。**収録は等速（`--speed` を付けない）**。

- ナレーション 8行・合計 38.4秒（行間 0.6秒 ×7 ＝ **通し 42.6秒**）
- **V12 の次回予告「乳酸の D体と L体」を回収する回**。次は「フィッシャー投影を90°回すと？」を予告している
- **V10（鏡像異性体をもつ最小のアルコール）と同じ道具立て**（立体対照ビューの 🪞鏡像と並べる → 並びを揃える）。
  V10 が「不斉炭素とは何か」を教える回なのに対し、**この回は「その2つには別々の名前と別々の役目がある」まで進む**身近枠

## ⚠ 収録前に必ずやること（2026-08-01 時点で未収録）

**この台本は「収録以外を先に済ませる」方針（PLAN_during_refactor.md）で作った。収録はしていない。**
アプリ改修セッションが **compounds.json の初期座標**を触っている最中なので、
**下に書いた座標・`frame`・`state` はすべて暫定**。収録時に次の3つを必ず実測し直すこと。

1. **`demos-stereo.json` の `state`（D-乳酸の6原子）を compounds.json の最新の D-乳酸に合わせ直す。**
   ステップ1の分子だけ `state` で固定・ステップ6の L-乳酸は `summon` なので、
   **片方だけ古い座標のままだと ①と② の骨格の向きが揃わない**
2. **`frame` の cx / cy / width を実測し直す**（§4 の手順。V18.md §3・V12.md §4 と同じ）
3. **§4 の「D/L の左右」が生きているか確かめる。**
   テロップ・ナレーション・投稿文はいずれも
   **「COOH を上・CH₃ を下に置いたフィッシャー投影で、OH が右なら D」**という**縦置きの絵**を前提にしている。
   改修（REVIEW C-4b「主鎖を一直線に伸ばす」）で**乳酸が横置きになったら、この回は台本から作り直し**になる
   （横置きのフィッシャー投影は D/L を左右で語れない）

## 1. 画面の流れ（demos-stereo.json の8ステップと一致）

| # | 画面（操作） | テロップ | アプリの表示 |
|---|---|---|---|
| 1 | D-乳酸が置かれた状態から開始（`state`）。寄りの構図 | ヨーグルトの乳酸と、筋肉の乳酸。同じもの？ | 名称チップ「D-乳酸　C₃H₆O₃」 |
| 2 | 自由モード → 🧊 立体で見る | ① まん中の炭素は4つとも違う ＝ 不斉炭素 | **中心の炭素: 不斉炭素（他に sp3炭素が 1 個）**／4スロットが **COOH / CH₃ / OH / H** |
| 3 | 🪞 鏡像と並べる | ② 鏡像と並べてみる | 右に「🪞 鏡像」が出る（COOH / CH₃ / **H / OH** ＝左右が逆） |
| 4 | 並びを揃える | ⚠ 2か所が残る ＝ 重ね合わせられない | **⚠ 枠の2か所だけが違います＝重ね合わせられません**（オレンジの破線枠） |
| 5 | 閉じる → キャンバスへ戻る | ③ 鏡像のほうにも名前がある | 名称チップは「D-乳酸　C₃H₆O₃」のまま |
| 6 | L-乳酸を呼び出す → 引きで2分子を並べる | 答え: ① D-乳酸 と ② L-乳酸（どちらも C₃H₆O₃） | 名称チップ「**① D-乳酸 ＋ ② L-乳酸　C₆H₁₂O₆**」／図の下に ① ② の見出し |
| 7 | 静止 | 体の中でできるのは L体だけ／ヨーグルトは菌しだい | 〃 |
| 8 | 静止 | 次回: フィッシャー投影を90°回すと？ | 〃 |

- **ステップ2で `#btn-stereo` は自動的に不斉炭素を選ぶ**（分子が1つなので迷いがない）。
  炭素を選び直す操作は要らない。**軸ボタンが COOH / CH₃ / H / OH に化けることで置換基が読める**
- **`#btn-stereo-wedge-layout-align`（並びを揃える）は 🪞鏡像と並べる を押すまで表示されない**ので、
  ステップ3とステップ4の順を入れ替えてはいけない
- **鏡像モードでの「重ならない」の見せ場は1回だけ**にした。V10 は ✔ と ⚠ の A/B で見せたが、
  この回は**②の登場（名前が付く）**が山なので、⚠ 側だけに絞って尺を空けている

### ⚠ 名称チップの C₆H₁₂O₆ に注意（ステップ6〜8）

**2分子を並べると、名称チップの分子式は①②の合計になる**（V10・V11 と同じ挙動）。
乳酸は C₃H₆O₃ なので、**合計はちょうど C₆H₁₂O₆ ＝ グルコースの分子式**になる。
**前作 V12 がグルコースの回**なので、続けて見た人が「またグルコース？」と誤読しかねない。

- テロップに **「（どちらも C₃H₆O₃）」を明記**して打ち消してある（ステップ6）
- ナレーションでも**分子式の数字は読み上げない**（「分子式は同じでも、別の物質なのだ」）。
  読み上げると、耳では合計値と区別が付かない

## 2. ナレーション全文（ずんだもん版・VOICEVOX）

正は [narration/V50.json](narration/V50.json)。音声は `video-scripts/audio/V50/`（行ごと ＋ `full.wav`）。

> ヨーグルトの乳酸と、筋肉の乳酸。同じもの？
> 乳酸のまん中の炭素は、4つとも違う。ふせい炭素なのだ。
> だから、きょうぞうと並べてみるのだ。
> どう並べかえても、2か所が残る。重ならないのだ。
> じつは、鏡のむこう側にも名前があるのだ。
> ディーたいとエルたい。分子式は同じでも、別の物質なのだ。
> 体の中でできるのはエルたいだけ。ヨーグルトは、にゅうさんきんの種類しだい。
> 次は、フィッシャー投影。九十度回すと別の分子になるのだ。

読み仮名は8行すべて `narrate.py` の出力で確認済み（2026-08-01）。**仮名で書いて回避したもの**:

- **「鏡像」→ きょうぞう**（漢字のままだと **カガミゾオ**。LANES.md §4 の既知の誤読）。
  出力は キョオゾオト' で正しい。**5行目の「鏡」は単独なので漢字のままでよい**（カガミ' と読む）
- **「不斉炭素」→ ふせい炭素**（_フセイタ'ンソ）。V10 と同じ手当て
- **「D体・L体」→ ディーたい・エルたい**（ディイタ'イ・エ'ルタイ）。
  漢字の「体」は **エルカ'ラダ** と読まれる（V12 で踏んだ）
- **「90度」→ 九十度**（キュウジュウ'ド）。算用数字は読みが揺れる（V15 と同じ）
- 確認して問題なかったもの: 乳酸→ニュウサン／筋肉→キ'ンニク／2か所→ニカ'ショ／
  分子式→ブンシ'_シキ／物質→ブッ_シツ／乳酸菌→ニュウサンキン／投影→トオエエ'
- **「OH」「COOH」は一度も読み上げない**（オオエ'ッチ と間延びする。V12 の判断を踏襲）。
  置換基の名前は**アプリの軸ボタンとテロップに任せる**

## 3. 化学の裏取り

### 3-1. 実機で確認したこと（v345・2026-08-01）

- **D-乳酸・L-乳酸はどちらも compounds.json に収録済み**（`乳酸`（立体なしの総称）を含め3件）。
  呼び出すと名称チップが **「D-乳酸　C₃H₆O₃」「L-乳酸　C₃H₆O₃」**と**出し分かれる**。
  これは `check-read-stereo`（**立体（D/L・α/β）を名前に反映する**）が **既定 ON** だから
  （`game.js` の `this.readStereo = true` ＋ index.html の `checked`）。
  **OFF にすると両方とも「乳酸」になり、この回は成立しない**。収録はまっさらなブラウザなので既定 ON でよいが、
  手元で確認するときは localStorage（`chemAssembler.readStereo`）に前回の値が残る点に注意
- **不斉炭素はまん中の炭素ただ1つ**。実機で `mol.isAsymmetricCarbon()` を全炭素に流した結果:

  | 炭素 | 位置（暫定） | sp3 | 不斉 |
  |---|---|---|---|
  | COOH の C | (358, 258) | ✗（C=O があるため） | ✗ |
  | **まん中の C** | (358, 300) | ✓ | **✓** |
  | CH₃ の C | (358, 342) | ✓ | ✗ |

  アプリのモーダルも「中心の炭素: **不斉炭素**（他に sp3炭素が 1 個）」と書く。
  **`#btn-asym-mark`（✳不斉炭素マーク）は使わない**——押した炭素が不斉かを検査しないので、
  当て損なうと嘘の絵になる（LANES.md §5 の落とし穴）。**判定はモーダルの表示に任せる**
- **立体対照ビューの4スロットは COOH / CH₃ / OH / H**（軸ボタンのラベルで確認）。
  4つとも違うので、モーダルは「この炭素は不斉炭素です。4つの置換基（COOH、CH₃、OH、H）が
  すべて異なるため、鏡に映した分子とは重ね合わせられません」と書く
- **🪞鏡像と並べる → 並びを揃える で「⚠ 枠の2か所だけが違います＝重ね合わせられません」が出る**（実機で実行して確認）。
  「並びを揃える」は**偶置換12通りを総当たりして最も近い並びを出す**モードなので、
  **それでも残る食い違い＝回転では重ね合わせられない**ことの証明になる（V10 §3 と同じ根拠）
- **`readStereoOf()` が読むパリティは D-乳酸が +1・L-乳酸が −1**で、compounds.json の
  `stereo.atomParity` と一致する。**名称チップが正しいことは座標から独立に裏が取れている**

### 3-2. D と L の決め方（テロップとナレーションの根拠）

**フィッシャー投影で COOH を上・CH₃ を下に置いたとき、OH が右にあるものが D-乳酸、左が L-乳酸。**
アプリの収録データもこの規約どおりで、実機の座標（暫定）は次のとおり:

| | まん中の C | その OH | 見え方 |
|---|---|---|---|
| D-乳酸 | (358, 300) | **(400, 300) ＝ 右** | ① |
| L-乳酸（②として呼び出したとき） | (526, 300) | **(484, 300) ＝ 左** | ② |

**①と② が画面上でそのまま左右反転の絵になる**ので、ステップ6は説明なしで「鏡像だ」と分かる。
**この見え方は縦置き（フィッシャー投影）でしか成り立たない。**改修で横置きになったら台本ごと作り直すこと（冒頭の警告 3）。

### 3-3. 乳酸と体・ヨーグルト（言ってよいこと／言わないこと）

**❌ 「乳酸は疲労物質」とは言わない。**現在は否定的に見られている。
運動でできた乳酸は**エネルギー源として使い直される**（乳酸シャトル）ことが分かっており、
筋疲労の主因は乳酸そのものではなく、グリコーゲンの枯渇や水素イオンによる pH の低下などとされる。

- **動画では「筋肉の乳酸」としか言っていない**（冒頭のフック）。**たまる／疲れる とは一言も言わない**
- 投稿文（YouTube 説明欄）にだけ、**「長く疲労物質と呼ばれてきたが、現在はそう考えられていない」**という
  補足を1行入れてある。**動画本編に入れると尺を食ううえ、この回の主題（D/L）から外れる**
- **コメントで「乳酸＝疲労物質」と書かれたら、否定せず補足する**方針を meta のチェックリストに書いた

**❌ 「ヨーグルトの乳酸菌が作るのは主に L体」とは言わない。裏取りの結果、これは誤り。**

- **ヒト（筋肉）が作るのは L体だけ** — ほ乳類の細胞は D型の乳酸脱水素酵素（D-LDH）を持たず、
  乳酸脱水素酵素は L体に対してだけはたらく。**ここは言い切ってよい**
- **ヨーグルトは菌の種類で変わる** — サーモフィルス菌（*Streptococcus thermophilus*）は**主に L体**、
  ブルガリア菌（*Lactobacillus delbrueckii* subsp. *bulgaricus*）は**主に D体**を作る。
  この2種は必ず組で使われるので、**市販のヨーグルトには D体も L体も入っている**
- したがってナレーションは **「ヨーグルトは、にゅうさんきんの種類しだい」**という言い方に留めた。
  **どちらが多いかは言わない**（銘柄・菌株で変わるため）

裏取りに使った出典:

- [D-Lactic Acid as a Metabolite: Toxicology, Diagnosis, and Detection（PMC）](https://www.ncbi.nlm.nih.gov/pmc/articles/PMC7320276/) — ほ乳類の細胞は D-LDH を欠き、ほぼ L体だけを作る
- [Influence of Different Ratios of *L. bulgaricus* and *S. thermophilus* on Fermentation Characteristics of Yogurt（PMC）](https://www.ncbi.nlm.nih.gov/pmc/articles/PMC10004190/) — S. thermophilus は主に L体、L. bulgaricus は主に D体
- [ラクトバチルス デルブリッキィ 亜種 ブルガリクス（ヤクルト中央研究所 菌の図鑑）](https://institute.yakult.co.jp/bacteria/4246/) — 同上（日本語の確認先）
- [老廃物や疲労の素ではない（日本スポーツ栄養研究誌 6巻 3–9, 2013）](https://www.jsna.org/cms/wp-content/uploads/2022/12/6-p3-9.pdf) — 乳酸は疲労物質ではなくエネルギー源
- [乳酸とは（健康長寿ネット）](https://www.tyojyu.or.jp/net/kenkou-tyoju/undou-kiso/nyusan.html) — 筋疲労の原因はグリコーゲンの枯渇や pH 低下とする現在の見方

## 4. 座標と構図の根拠（**すべて暫定。収録時に実測し直すこと**）

v345 の compounds.json で実測した値。**改修で座標が変わったら全部やり直し。**

| 対象 | 描画の外接（H・見出しこみ・実測） | frame（暫定） |
|---|---|---|
| ① D-乳酸 単体 | x 336–422 / y 206–364 | `cx 379, cy 285, width 280` |
| ①② 2分子まとめて | x 336–590 / y 206–394.3 | `cx 463, cy 295, width 330` |

実測のやり方（実機のコンソールで流す。`atoms-group` と `bonds-group` だけを見るのは、
`ui-group` が空のとき原点を含んでしまい外接がゼロ側へ伸びるため）:

```js
const bb = () => { const gs = ['atoms-group','bonds-group'].map(id=>document.getElementById(id).getBBox());
  return [Math.min(...gs.map(g=>g.x)), Math.max(...gs.map(g=>g.x+g.width)),
          Math.min(...gs.map(g=>g.y)), Math.max(...gs.map(g=>g.y+g.height))].map(v=>+v.toFixed(1)); };
game.summonMolecule('D-乳酸'); bb();          // ① 単体
game.summonMolecule('L-乳酸'); bb();          // ①② まとめて
```

- 収録は 810×1440 ビューポート。SVG は 796×1224 なので、**見える論理高さ ＝ `width` × 1.538**
  （V12.md §4 と同じ。`preserveAspectRatio` の meet で幅が先に合う）
- **上 27% はテロップ（top 9%）と名称チップ（top 23%）で死んでいる／下 25% は SNS の UI 帯**。
  したがって使える範囲は **`cy − 0.23H` 〜 `cy + 0.25H`**。上の暫定値はこれで上下 20〜30 の余白を取ってある
- **① ② の見出しは分子の下（y 390.3）に描かれる**ので、2分子の `cy` は単体より 10 下げてある
  （V10 §4 と同じ手当て。分子の中心に合わせると見出しが切れる）

## 5. `wait` の逆算（LANES.md §4 の式）

`wait =（その行の秒数＋0.6）−（アクションの実測コスト合計＋1.13）`。
**ステップ1は `state` 始まりなので固定オーバーヘッドを 0.77 で計算**（V10 §5 の実測）。

| # | 行 | 秒 | アクションのコスト | wait |
|---|---|---|---|---|
| 1 | 01-hook | 4.9 | frame 0.66（＋0.77） | **4070** |
| 2 | 02-asym | 5.4 | button 1.34 ＋ button 1.34 | **2190** |
| 3 | 03-mirror | 2.8 | モーダル内ボタン 1.24（1回目） | **1030** |
| 4 | 04-align | 4.4 | モーダル内ボタン 0.89 | **2980** |
| 5 | 05-name | 3.8 | 閉じる 0.89 ＋ frame 0.66 | **1720** |
| 6 | 06-dl | 5.4 | summon 1.46（直前が frame）＋ frame 0.66 | **2750** |
| 7 | 07-body | 6.2 | なし | **5670** |
| 8 | 08-next | 5.5 | なし | **4970** |

**コストは他の回の実測値からの見積もり**なので、収録後に `--events` の時刻とナレーションの
累積秒を突き合わせ、**ずれが 0.1〜0.4秒の窓（テロップが声より少しあと）**に入るまで詰めること。

## 6. 収録メモ（未実行）

```
node tools/record/record.mjs --demo=stereo-lactic-dl --format=short --base=http://localhost:8154/assembler/
node tools/record/mux.mjs --video=video-scripts/out/stereo-lactic-dl-short.webm \
  --audio=video-scripts/audio/V50/full.wav \
  --bgm=video-scripts/audio/bgm/のんきな日常.mp3 \
  --se=video-scripts/audio/se/tap.wav \
  --events=video-scripts/out/stereo-lactic-dl-short.events.json \
  --meta=video-scripts/meta/V50.json --trim=auto --lead=0.3 --size=1080x1920 \
  --out=video-scripts/out/V50-final.mp4
```

- **反応実行をしないので貫通チェック（LANES.md §4）は不要**だが、
  **改修で座標が変わっていたら念のため流す**（呼び出した2分子が重なっていないかの確認になる）
- **収録前に、まず単体で `--demo=stereo-lactic-dl` を1回撮ってフレームを目視する**
  （`node tools/frames.js video-scripts/out/V50-final.mp4 --steps`）。
  見るのは **①の名称チップが「D-乳酸」になっているか**と、**⑥の①②の見出しが切れていないか**の2点
- `video-scripts/out/.tmp` は収録の共有作業場所。別レーンと同時だと `EBUSY` で落ちるので再実行する（V12 §5）

## 7. 投稿メタ

正は [meta/V50.json](meta/V50.json)。`series: 立体シリーズ` / `campaign: v50` / `prev: V12`。

- **立体シリーズ④**。V15 → V10 → V12 → **V50** の順で再生リストに入れ、X は V12 のツイートにぶら下げる
- カバー画像は**ステップ4の「⚠ 2か所が残る ＝ 重ね合わせられない」**（オレンジの枠が2つ光っている絵）。
  V10 と同じ理由で、✔ より ⚠ のほうが止まる。
  **ステップ6の①②並びも候補**だが、名称チップの C₆H₁₂O₆ が静止画で誤読されるので採らない
- 次の予告は**「フィッシャー投影を90°回すと？」**（LANES.md §5 stereo のネタ表 ◎。`#btn-fp-rot90cw`）。
  この回で「入れ替えると鏡像」を見せているので、**「回すだけでも鏡像になってしまう」がそのまま次の問い**になる
