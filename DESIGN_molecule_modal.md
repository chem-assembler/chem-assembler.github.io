# 分子モーダル 設計書（分子を選んで「その分子について」をまとめて開く）

**この文書は設計だけで、コードは1行も変えていない。** 版番号も上げていない。実装はここで合意してから別レーンで行う。

基準の版は **v524**。実測はすべて v524 のコードをローカルサーバー（`python -m http.server`）で開き、
Playwright（`tools/record` の依存を借用）で 320×568 / 375×812 / 1280×800 を測った値。
`DESIGN_entry_points.md` の実測は v446 時点のものなので、**この文書で取り直している**（差分は §2-1）。

## 0. なぜこの文書か

ユーザーの提案（原文）:

> 試薬については、**分子を選択するとモーダルを表示**、がよいかもしれません。
> **この物質の反応を見る、立体視もまとめてモーダル化**するといいかも、と思いましたが、項目が多すぎるでしょうか？
> スマホ対応なども考えると、**右ペインはあまり使わない方がよさそう**です

この方向で設計する。答えるのは6つ:

| # | 問い | 答える節 |
|---|---|---|
| 1 | 何をモーダルへ入れるか（入口の全数の振り分け） | §3 |
| 2 | 何が失われるか（作図しながら見られなくなるもの） | §4 |
| 3 | 開き方と閉じ方（複数選択・作図への復帰） | §5 |
| 4 | 試薬パレットの置き場所 | §6 |
| 5 | 段階に割る | §7 |
| 6 | スマホ（320px）で成立するか | §8 |

関連する既存の設計書との関係:

- **`DESIGN_entry_points.md`** … 入口の全数と不変条件の出どころ。案A の8手のうち7手が v447〜v454 で入り済み。
  この文書はその**続き**で、A-8 で「🔍 いま描いている分子」カードに集めた道具の**次の置き場所**を決める
- **`DESIGN_reagent_palette.md`** … §1.3 が「置き場所はここでは決めない・入口再編の結論に従う」としている。**この文書の §6 がその結論**
- **`DESIGN_ui_modes.md` / `DESIGN_mobile.md`** … 3モード・シート化の骨格。**どちらも作り替えない**
- **`DESIGN_reaction_execution.md`** … 反応の実行そのもの（M1〜M5 クローズ済み）。実行経路には触れない

---

## 1. 結論の先出し

1. **入れるのは3節だけ。5節ではない。**「名前・分子式のライブ表示」と「名称から分子を呼び出す」は
   **モーダルに入れてはいけない**（§4・§3 の 41・46）。残る3節（⚗ 反応＋試薬／🔬 調べる／見出しの分子切替）で
   **320px の実測 779px ＝ 1.5画面**。**項目は多すぎない**
2. **立体は中に畳めない。** `#stereo-modal` の中身だけで **320px 幅で 1305px**（可視 507px ＝ 2.6画面）ある。
   1つの節にすると分子モーダルが 2000px を超える。**ボタンを分子モーダルへ集約し、実体は既存モーダルのまま**にする
   ＝ 入口としては「まとめてモーダル化」できているが、**画面としては1枚にしない**
3. **「分子そのものをタップして開く」は採れない。** select ツールの素のタップは**原子の削除／元素の置換**に
   割り当て済みで（`game.js:1481`）、分子の見出しは「そこに原子を置けなくなる」という理由で
   **明示的に `pointer-events:none`** にしてある（`game.js:3929`）。開くのは**ボタン**にする
4. **モーダルは重ねない。** 分子モーダルから 🧊 立体・📚 異性体を開くときは**自分を閉じる**。
   14枚のモーダルはすべて `z-index:1000` で、同時に開くと **DOM の後ろにある方が上に来る**（実測）
5. **反応の「実行」はモーダルの中では完結しない。** 適用箇所の選択（`reactor.js:2204` の `narrow`）・
   モーフィング・前後比較（`#rx-compare-overlay`）が**すべてキャンバスの上**で起きる。
   **押したらモーダルを閉じてキャンバスへ返す**のが唯一の筋
6. **右ペインは消さない。縮める。** 320px 実測でパズル 1251px・自由 630px。
   反応と調べるをモーダルへ出すと自由は **300px 台**まで下がり、モバイルで「シートを開かなくても遊べる」に近づく

---

## 2. 先に確かめた事実（v524）

レビューや前の設計書の記述をそのまま前提にせず、コードと実機で取り直した。

### 2-1. 入口の数え直し（v446 → v524 の差分）

`DESIGN_entry_points.md` §2 の「47個」は **1〜47 の通し番号**で、内訳は
**ヘッダー・キャンバス上ツールバー 9個（#1〜9）＋ 右パネル 38個（#10〜47）**。
左パレットの25ボタンはこの47には含まれない（§2-2 で別に数えられている）。

v446 から v524 で右パネルは次のように変わっている（すべて入口見直しの実装ぶん）:

| 変化 | 内容 |
|---|---|
| 追加 | **← 自由に戻る**（`#btn-back-to-free`・v448） |
| 追加 | クイズの沈んでいた出題3つ（`#btn-choice-quiz-dl` / `#btn-choice-quiz-pair` / `#btn-time-attack-advanced`・v453） |
| 移動 | **📚 異性体・🧊 立体・名称呼び出し** が `#reaction-card` → `#compound-info` へ（v450・v452） |
| 変化 | 遊び方が `<details id="hint-details">` に（v451） |

**現物の数（375px・シートを開いた状態で計測）**: `#right-panel` の中に
`button` 43個（うち動的 15個 ＝ 書き出し練習 ip7/ak3/sp4、反応ボタン1）・`input` 5個・`select` 3個・`summary` 4個。

**右パネルの総スクロール高（実測・v524）**

| 画面 | 🧪 自由 | 🧩 パズル | 📚 学習 | 可視高 |
|---|---|---|---|---|
| 320×568 | **630px** | **1251px** | 408px | 464px |
| 375×812 | 631px | 1166px | 408px | 664px |
| 1280×800 | 718px | 1276px | 718px | 718px |

> v451（遊び方を畳む）が効いていて、**学習と自由はすでに1画面前後**。
> 残っている厚みは**パズル**（判定オプションの長文）と、自由の「⚗ この分子の反応」。

### 2-2. 「どの分子の話か」の仕組みは**もうある**

これが今回いちばん重要な事実。ゼロから作る話ではない。

| 仕組み | 実体 | 何を指すか | 変える手段 |
|---|---|---|---|
| `focusedMolecule` | `game.js:71` | 「⚗ この分子の反応」が**分類を出している**分子 | ① `#molecule-props` の先頭に出る**①②③のチップ**（`game.js:4083`）／② 🎯 選択モードでのタップ |
| `selectedMolecules` | `game.js:67` | **反応を絞り込む**分子（最大4・選んだ順が式の並び） | 🎯 反応させる分子を選ぶ（`#btn-reaction-select`）＋キャンバスのタップ |
| `markedMolecules` | `game.js:3891` | 図と右パネルで**同じ①②③**を振る（2分子以上のときだけ） | — |

- 図では **琥珀の実線枠＋「⚗ 分析中」**（分析対象）と **青い破線枠＋番号**（反応の選択）を**わざと描き分けている**（`game.js:4608`）
- つまり **「分子を選ぶ」という語には既に2つの意味がある**。設計はどちらを指すかを毎回明示すること

### 2-3. いまの「調べる道具」は分子を選べない（＝直すべき欠陥）

| 道具 | 実装 | 複数分子のときの挙動 |
|---|---|---|
| 📚 異性体を調べる | `learn.js:73` | **拒否する**。「分子が複数あります。1つだけにしてから調べてください」 |
| 🧊 立体で見る | `stereo.js:363` の `autoCenter` | `game.userMolecule`（＝**キャンバス全部**）から sp3炭素を探す。**どの分子の炭素かは選べない**。中心ラベルに `componentSuffix` で「どの分子か」を後付けで書いているだけ（`stereo.js:455`） |
| ⚗ 反応 | `reactor.js:2020` | **選択で絞れる**（`selectedMolecules`）。3つ以上選んでも2分子反応が消えない条件になっている |

**つまり「分子を選択すると…」の提案は、UI の趣味ではなく機能の穴を塞ぐ。**
反応だけが分子を選べて、調べる系は選べない。

### 2-4. 「分子そのものをタップ」は空いていない

| 操作 | いま割り当たっているもの | 出典 |
|---|---|---|
| 原子を素でタップ（select） | 同じ元素なら**削除**、違う元素なら**置換** | `game.js:1481-1495` |
| 原子を Shift+ドラッグ | 分子を丸ごと移動 | `game.js:1466` |
| 結合線をタップ | 次数トグル | ヘルプ |
| 結合線を長押し／2回タップ | 削除 | ヘルプ |
| 何もない所をタップ | **原子を置く** | `game.js:1520` |
| 分子の見出し（① 乳酸）をタップ | **何も起きない**。`pointer-events:none` を**意図して**付けてある | `game.js:3929`「見出し自体を当たり判定にすると、見出しの位置に原子を置けなくなる」 |

タップが空いているのは **`reactionSelectMode` が ON のときだけ**（`game.js:1421` で作図を完全にブロックしている）。
**開く手段はボタンにする**（§5）。

### 2-5. 反応の実行はキャンバスに結びついている

| 段 | 何が起きるか | どこに出るか |
|---|---|---|
| 箇所の絞り込み | `narrow()` が候補原子を `highlightAtoms` して**「クリックしてください」** | **キャンバス** |
| 実行 | `execute()` → 座標を計算して分子を書き換え | キャンバス |
| 失敗 | 「生成物を配置する空間がありません。**分子を離してから**実行してください」（`reactor.js:1208` ほか4箇所） | トースト＋キャンバス |
| 演出 | 2段モーフィング（`_morphing`） | キャンバス |
| 前後比較 | `#rx-compare-overlay`（`class="canvas-overlay"`・**モーダルではない**） | **キャンバスの上** |

**全画面のモーダルが乗っていると、この5つが全部見えない。**
→ 反応ボタンを押したら**モーダルを閉じる**（§5-3）。

### 2-6. モーダルの寸法（実測）

| 画面 | `.modal-content` の枠 | 可視高 | 参考: 右パネル（シート）の可視高 |
|---|---|---|---|
| 320×568 | 288×511 | **507px** | 464px |
| 375×812 | 338×731 | **727px** | 664px |
| 1280×800 | 450×807（`max-width:450px`） | 803px | 718px |

> **モーダルはシートより縦に広い。** 規約は `max-height:90dvh`（`style.css:1066`）で、
> シートは `max-height:82dvh`（`style.css:1151`）。320px で **43px 得**する。
> 「右ペインをあまり使わない方がよさそう」は寸法の上でも正しい。

**中身の高さ**

| 中身 | 320px | 375px | PC |
|---|---|---|---|
| `#compound-info`（🔍 いま描いている分子・乳酸） | 247px | 247px | 247px |
| `#reaction-card`（⚗ この分子の反応・反応1件） | 155px | 155px | 155px |
| **`#stereo-modal` の中身** | **1305px** | 1206px | 799px |
| 試作: 見出し＋上の2カード＋**試薬20本**（4×5・44px札） | **779px** | 761px | 803px |
| 　うち試薬グリッドだけ | 244px | 244px | 244px |

- 試作モーダルは 320px で**横あふれなし・32px 未満のタップ標的なし**（TAP1 相当の走査で0件）
- **立体を節にすると 779＋1305＝2000px を超える。** これが §1-2 の根拠

### 2-7. 台本・テストの当たり（v524 で数え直し）

`DESIGN_entry_points.md` §7 の「31本」は v524 では **36本**（`demos-longform.json` の5本が増えた。**全部 `sns: true`**）。
別に `tutorials.json` が13本ある。

| 実測 | 数 |
|---|---|
| demos*.json の本数 | **36本**（全部 sns） |
| `selector` の使用 | **186箇所・36種類** |
| `summon` アクション（`#summon-input` を直に引く） | **21箇所**（v446 時点は18） |
| `reactionButton`（`#reaction-actions button` をテキストで探す） | **13箇所 / 7本** |
| `#btn-stereo` を押す | **5箇所 / 4本** |
| `#btn-isomers` を押す | **0箇所**（`tutorials.json` に1箇所） |
| `#btn-reaction-select` を押す | **0箇所** |
| `.mode-tab[data-mode="free"]` | 12箇所 / 12本 |

**動きの契約（`tutorial.js` を読んで確認）**

| アクション | 隠れた要素（rect=0）のときの挙動 |
|---|---|
| `button`（181箇所） | **カーソル演出を省いてクリックだけ実行**（`tutorial.js:340`）。さらに v502 以降、**右パネルの中で見えないボタンのためにシートを開かない**（`syncSheetForButton`） |
| `summon`（21箇所） | ⚠ **ガードが無い**。`getBoundingClientRect()` の結果をそのまま使う（`tutorial.js:435`）ので、隠れた入力欄だと**カーソルが画面左上(0,0)へ飛ぶ** |
| `reactionButton`（13箇所） | ⚠ **同じくガードが無い**（`tutorial.js:448`） |

**この2つのガード欠落が、振り分けを決めている**（§3 の 43・46）。

---

## 3. 入口の全数の振り分け

`DESIGN_entry_points.md` §2 の通し番号 1〜47 に、v524 で増えた分（48〜52）を足して**1つずつ**振り分けた。
「どちらでもない」＝ この見直しの対象外（触らない）。

### 3-1. ヘッダー・キャンバス上ツールバー（1〜9）

| # | 入口 | 振り分け | 理由 |
|---|---|---|---|
| 1 | ❓ ヘルプ | **右パネル外に残す** | アプリ全体のガイド。分子の話ではない |
| 2 | X: 0, Y: 0 | どちらでもない | 表示。入口ではない |
| 3 | ↩ 戻す | 残す | 作図の道具。モーダル中も効く（§5-4 の注意） |
| 4 | ↪ やり直し | 残す | 同上 |
| 5 | ⤓ JSON | 残す | 制作用。モバイルでは非表示のまま |
| 6 | 🔤 官能基をまとめる | 残す | **表示の切替**であって「この分子を調べる」ではない。`DESIGN_ui_modes.md` §3.3 との食い違い（分析ツールに集約すると書いてあった）は**実装が正**として据え置く |
| 7 | 🔍 全体表示 | 残す | 作図の道具 |
| 8 | 全消去 | 残す | 作図の道具 |
| 9 | ☰ お題・設定 | 残す | シートの開閉。**モーダル化で押す回数が減るのが狙い** |

### 3-2. 右パネル（10〜47）

| # | 入口 | いまの場所 | 振り分け | 理由 |
|---|---|---|---|---|
| 10–12 | 🧪 自由 / 🧩 パズル / 📚 学習 タブ | `#mode-tabs` | **残す** | `setMode` の3値は不変条件（`DESIGN_entry_points.md` §7） |
| 13 | シリーズを選択 | パズル | 残す | お題の選択。分子の話ではない |
| 14 | 問題を選択 | パズル | 残す | 同上 |
| 15 | 構造判定 (Submit) | パズル | 残す | **判定には一切触らない** |
| 16 | お手本を見る | パズル | 残す | お題の話 |
| 17 | ☑ 不斉炭素原子の判定 | パズル | 残す | 判定オプション |
| 18 | ☑ 立体を名前に反映する | パズル | 残す | ⚠ **モーダルの見出しの名前にも効く**（`lookupCompoundName`）。文言はそのまま |
| 19 | 🎓 クイズに挑戦（acc） | 学習 | 残す | 学習タブの練習は「分子を調べる」ではない |
| 20–27 | クイズ8ボタン | 学習 | 残す | 同上（v453 で3群に整理済み） |
| 28 | ✏️ 書き出し練習（acc） | 学習 | 残す | 同上 |
| 29–31 | 異性体／アルキル基／立体異性体の書き出し練習 | 学習 | 残す | 同上。⚠ カード内15個（実測14個）に id が無いのは**この見直しでも直らない** |
| 32 | ⚗️ 反応機構ビューア（acc） | 学習 | 残す | **代表例の分子**で再生する面。ユーザーの分子の話ではない（`reactor.js:2188` が明示している） |
| 33 | ☑ 反応機構モード | 学習 | 残す | 同上 |
| 34 | 反応の選択（select） | 学習 | 残す | 同上 |
| 35–38 | 🔄 ⏮ ▶ ⏭ | 学習 | 残す | 同上 |
| 39 | 🎯 生成物を予測する | 学習 | 残す | 同上 |
| 40 | 予測を判定 / やめる | 学習 | 残す | 同上 |
| 41 | 🔍 いま描いている分子（名前・分子式） | `#compound-info` | **残す（常時表示）＋モーダルの見出しにも出す** | **描きながら変わるのを見るもの**。§4 の核心 |
| 42 | 🎯 反応させる分子を選ぶ | `#reaction-card` | **残す（役割を1つ足す）** | 選択モードは残し、**選択した状態から開ける導線**を足す（§5-1） |
| 43 | 反応ボタン列 `#reaction-actions` | `#reaction-card` | **モーダルへ（第2段）** | ⚠ `reactionButton` 13箇所にガードが無い（§2-7）ので、**7本の台本に「開く」1手を足す**。⚠ 32px の床（`style.css:520` の `#reaction-card .view-btn`）が**外れる**ので敷き直す（v523 と同じ轍） |
| 44 | 📚 同じ分子式の異性体を調べる | `#compound-info` | **モーダルへ（第1段）** | 分子を選べるようになる（§2-3）。台本 0箇所・`tutorials.json` 1箇所 |
| 45 | 🧊 立体で見る | `#compound-info` | **モーダルへ（第1段）。ただしボタンだけ** | 中身 1305px は畳めない（§2-6）。4本の台本の**画作りだけ**変わる |
| 46 | 名称から分子を呼び出す（`#summon-input`） | `#compound-info` | **残す（モーダルに入れない）** | ① **作図の道具**であって「この分子を調べる」ではない（A-4 でわざわざ反応カードから救い出した）。② `summon` **21箇所にガードが無く**、隠すとカーソルが (0,0) へ飛ぶ |
| 47 | 📝 遊び方と操作方法 | `#hint-details` | 残す | v451 で畳み済み（44px） |

### 3-3. v524 で増えた分（48〜52）

| # | 入口 | 振り分け | 理由 |
|---|---|---|---|
| 48 | ← 自由に戻る（`#btn-back-to-free`） | 残す | v448 の行き来の導線 |
| 49 | ✕ 閉じる（`#sheet-close`・モバイル） | 残す | シートの開閉 |
| 50 | 🅳 D体・L体はどれ？（4択） | 残す | 学習タブの練習 |
| 51 | ⚖ 同じ？違う？（連続） | 残す | 同上 |
| 52 | ⏱ タイムアタック 上級 | 残す | 同上 |
| （動的） | `#molecule-props` の ①②③ チップ | **モーダルへ移す（見出しの分子切替として）** | いまは「分類の対象を切り替える」だけ。モーダルの中では**見出しのタブ**になる |

### 3-4. 新しく入るもの

| 入口 | 場所 | 出典 |
|---|---|---|
| 🔬 この分子を調べる（モーダルを開く） | `#compound-info` に1つ | §5-1 |
| 試薬の瓶（第1段3本 → 第2段以降 20本） | モーダルの「⚗ 反応」節 | §6 |
| R/S の受け皿（1行） | モーダルの「🔬 調べる」節 | §6-3・別レーンの結論待ち |

**振り分けの要約: モーダルへ入るのは 43・44・45・チップ の4つ＋新規2つ。残り48個は動かさない。**

---

## 4. 何が失われるか

モーダルは画面を覆う。**作図しながら見られなくなるもの**を先に確定させる。

### 4-1. 「常に見えているべきもの」と「呼び出して見るもの」

| 常に見えている | 呼び出して見る |
|---|---|
| 名前・分子式（描くたびに変わる ＝ **変わる過程が学習内容**） | 反応の一覧と実行 |
| ①②③ の番号（図と右パネルの対応） | 試薬の瓶 |
| いま選んでいる分子の枠（青の破線／琥珀の実線） | 異性体の一覧 |
| **できる反応が何件あるか**（数だけ） | 立体（くさび図・3D・環・分子全体） |
| キャンバスそのもの | R/S・D/L の説明 |

**分ける基準**: 作図の手を止めずに知りたいものは常時、手を止めて読むものは呼び出し。

### 4-2. 実際に失われるもの（3つ）

| 失うもの | いまの姿 | 手当て |
|---|---|---|
| **「-OH を付けた瞬間に『酸化』ボタンが生える」気づき** | `updateDrawing` → `updateReactionCard` → `reactor.refresh()` で**描くたびに**反応ボタン列が組み直る（`game.js:3879`） | カードに残す1ボタンのラベルを **「🔬 この分子を調べる（反応 2件）」**にして、**件数だけライブ更新**する。0件のときは「反応 —」。**数が増えたことは見えるが、中身は開かないと分からない**——ここは正直に失う |
| **反応の実行を見ながらパネルを見続けられる**（PC） | PC は3カラムなので、右パネルの反応ボタンとキャンバスが同時に見える | PC では失う。ただし**押した直後にモーダルを閉じる**ので、実行中の絵は今までどおり（§5-3） |
| **異性体・立体を開く手数** | カードのボタンを1回 | **2回になる**（モーダルを開く→押す）。⚠ ただし `?open=isomer` `?open=stereo` の深いリンク（v454）は**受け口を変えずに1手のまま**にする（§7 の各段で `OPEN_TARGETS` を更新） |

### 4-3. `#mobile-name-chip` は答えの一部（ただし全部ではない）

- 実体は `index.html:170` ＋ `style.css:985`。**キャンバスの左下に浮く**（`z-index:250`・`pointer-events:none`）
- 中身は `syncMobileNameChip()`（`game.js:2210`）で `#compound-name` と `#compound-formula` を連結。
  名前が引けなければ**分子式だけ**。**学習モードと空のキャンバスでは消す**
- **モバイルでは、右パネルを開かなくても名前が見える状態がすでに作ってある。**
  分子モーダルは「名前を見せる装置」ではないので、**41 を常時表示に残す判断はこのチップで裏打ちされている**

⚠ ただし **チップは1行にすべての分子を並べる**（`①乳酸 ＋ ②エタノール`）ので、
分子が3つ以上あると `max-width:68%` で切れる。**モーダル側の見出しでは分子ごとに1つだけ出す**（§5-2）ので、
ここは重ならない。

---

## 5. 開き方と閉じ方

### 5-1. 開く（3経路。すべてボタン）

**分子そのもののタップは使わない**（§2-4）。

| 経路 | 何を押すか | 対象の分子 | 備考 |
|---|---|---|---|
| **主** | `#compound-info` の **「🔬 この分子を調べる」** 1ボタン | `focusedMolecule`（未指定なら①） | `data-modes="puzzle free"` のカードの中なので**パズルでも開く**（A-8 と同じ理屈） |
| **副** | 🎯 反応させる分子を選ぶ でタップして選んだあと、同じボタン | **選択があればそれ**（複数なら §5-2） | タップの意味は**選択のまま変えない**。「選んだら勝手に開く」にはしない（4つまで選ぶ流れが毎回モーダルで途切れる） |
| **外** | `?open=molecule`（＋`summon=<化合物名>`） | 呼び出した分子 | `OPEN_TARGETS` に1行足すだけ（v454 の仕組み。⚠ `?rec=` のときは踏まない） |

> **なぜ「選んだら開く」にしないか。** `MAX_REACTION_SELECTION` は4で、油脂のように
> 同じ反応を3回繰り返す使い方が想定されている（`reactor.js:2019`）。タップのたびにモーダルが出ると、
> **2つ目を選ぶためにいちいち閉じる**ことになる。選ぶ＝キャンバスの仕事、開く＝ボタンの仕事、と分ける。

### 5-2. 複数の分子を選んでいるとき

| 状態 | モーダルの見出し | ⚗ 反応の節 |
|---|---|---|
| 分子1つ | `乳酸 C₃H₆O₃` | その分子の反応 |
| 分子が複数・選択なし | **①②③のタブ**（`markedMolecules` の番号をそのまま使う）。既定は `focusedMolecule` | いまと同じ（絞り込みなし） |
| 選択1つ | 見出しはその分子。タブは出す（切り替えられる） | その分子が関わる反応（`siteAllowed` の1個ぶん） |
| **選択2つ以上** | 見出しに **`① 酢酸 ＋ ② エタノール`** と**選んだ順**で並べ、その下に**タブ** | **2分子以上に跨る反応**（`reactor.js:2027-2029` のまま） |

- **番号と順序の意味は変えない**。「先に選んだ方が式の左」（`game.js:4531`）はモーダルの見出しでもそのまま
- タブの切替は **`setFocusedMolecule()` を呼ぶだけ**（`game.js:4601`。既存関数）。
  `selectedMolecules` には触らない ＝ **反応の絞り込みは切り替わらない**。
  この2つを混ぜないことが `game.js:4610` の描き分け（琥珀 ⇄ 青）と揃う
- ⚠ **📚 異性体は「1分子だけ」でしか動かない**（`learn.js:73`）。モーダルの中では
  **見出しで選んでいる1分子だけを渡せる**ようになるので、この拒否は**第1段で解消できる**
  （`showIsomers()` に対象分子の引数を足す。**列挙のロジックは触らない**）

### 5-3. 閉じる／作図に戻る

| きっかけ | 挙動 |
|---|---|
| ✕ 閉じる | モーダルを閉じるだけ。**選択・分析対象・キャンバスは何も変えない** |
| 反応ボタンを押した | **必ず閉じてからキャンバスへ返す**（§2-5）。箇所の選択（`narrow`）・モーフィング・前後比較が全部キャンバスの上だから |
| 試薬の瓶を押した | 反応が起きるときは上と同じ。**「効きません」の説明だけのとき（`info` 経路）は開いたまま**にする（`DESIGN_reagent_palette.md` §4。分子は1原子も変わらないので閉じる理由がない） |
| 🧊 立体・📚 異性体を押した | **分子モーダルを閉じてから**相手を開く（重ねない。§5-5） |
| 背景のタップ・Escape | **今は付けない**。既存14枚のモーダルはすべて**明示的な閉じるボタンだけ**で、背景タップも Escape も実装していない（実測）。ここだけ違う作法にしない。→ §9 の未決 |

### 5-4. モーダルの中で作図が変わることはあるか

**ある。だからその場合は必ず閉じる**（§5-3）。あわせて次の3つを知っておく:

| 事実 | 影響 |
|---|---|
| `Ctrl+Z` / `Ctrl+Y` / `Delete` は `window` の keydown（`game.js:700`）で、**モーダルが開いていても効く** | 既存14枚と同じ状態。**新しい問題ではない**が、分子モーダルは「分子を触る面」なので誤爆が目立ちやすい。§9 の未決 |
| 反応の実行は `saveState()` を積む | **↩ 戻す で元に戻せる**。モーダルを開き直せば前の状態が見える |
| 検出試薬（銀鏡など）は `saveState()` を積まない（`DESIGN_reagent_palette.md` §2.5） | **Undo 履歴を汚さない**。閉じる必要もない |

### 5-5. モーダルを重ねない（実測に基づく制約）

- 14枚すべて `z-index:1000`。**同時に開ける**（`#btn-stereo` → `#btn-isomers` の順で押すと両方 `hidden` が外れる。実測）
- **同じ z なら DOM の後ろにある方が上**。`#stereo-modal` は `index.html:856`、`#learn-modal` は `index.html:1024`
- ⚠ 分子モーダルを `index.html` の末尾に足すと、**そこから開いた立体モーダルが分子モーダルの裏に回る**
- **対策は「重ねない」。** 子を開くときに自分を閉じる。z-index の綱引きも、✕ が2つ並ぶ絵も生まない
- 立体・異性体を閉じたあとは**キャンバスへ戻る**（分子モーダルには自動で戻らない）。
  「◀ 分子にもどる」を付けるかは §9 の未決

---

## 6. 試薬パレットの置き場所（`DESIGN_reagent_palette.md` §1.3 の宿題）

### 6-1. 結論: **分子モーダルの「⚗ 反応」節の中に置く。別の面は作らない**

理由:

1. **試薬は「この分子に何が効くか」の話**で、対象の分子が決まっていないと意味がない。
   分子モーダルは**対象が決まっている唯一の面**（見出しに①②が出ている）
2. `DESIGN_reagent_palette.md` §1.1 は「**モードにしない**（いまどっちにいるかを覚える負担を増やさない）」と決めている。
   独立したパレット面を作るとモードに近づく。**自動案内のボタン列のすぐ下に瓶を並べれば、
   「分子 → 反応」と「試薬 → 反応」が同じ画面で背中合わせになる**（同書 §1.1 の表そのまま）
3. **寸法が足りる**。試薬20本（4×5・44px札）で **244px**。
   見出し＋分類＋反応ボタン＋瓶20本の試作が **320px で 779px＝1.5画面**（§2-6）。
   スクロールで足り、タブに割る必要はない
4. **左パレットには置かない。** `DESIGN_entry_points.md` §8 の未決5は「A-1 で左パレットが
   『原子 → 環・官能基 → 道具 → 印』に並ぶなら試薬は第5グループが自然」と書いていたが、
   **左パレットは 320px で 1936px（v447 実測）ある帯**で、そこへ20本足すと
   **A-1 で直したばかりの「炭素が画面外」が再発する**。第5グループ案は**採らない**

### 6-2. 節の中の並び（上から）

```
⚗ 反応
├ 分類（#molecule-props の文言。いまのまま）
├ [🎯 反応させる分子を選ぶ]      ← 42。押すと閉じてキャンバスへ（選ぶのはキャンバスの仕事）
├ 選択の説明（#reaction-selection）
├ できる反応のボタン列（#reaction-actions）  ← 43。自動案内＝「分子 → 反応」
└ 試薬をかける
  ├ 変えるもの（15本）
  └ 調べるもの（5本）                        ← 試薬＝「試薬 → 反応」。区分は同書 §3.2
```

- **瓶はグレーアウトしない**（同書 §1.2）。効かない組み合わせを選べることが価値
- ⚠ **32px の床**（`style.css:508` の論点C）は瓶にも要る。`#reaction-card .view-btn` の指定は
  モーダルへ移した時点で外れるので、**節の中の押せるものすべてに敷き直す**。TAP1 が門番になる

### 6-3. R/S・D/L は独立の節にしない

- `DESIGN_rs_descriptor.md` §6 は「**UI へ出すのは最後。入口は増やさない**（アプリ全体の入口見直しが控えているため）」と決めている
- D/L は**すでに名前に出る**（「立体を名前に反映する」が ON のとき。#18）ので、
  **モーダルの見出しの名前がそのまま答えになっている**
- R/S は **`#stereo-modal` の読み物「📖 R・S ってなに？」**が既にあり、別レーンが `stereo.js` に入口を作っている
- **決定**: 分子モーダルには **「🔬 調べる」節に1行の受け皿**（例: `R/S を見る` ボタン）だけ空けておき、
  中身は立体モーダル側に置く。**節にはしない**（統合時に別レーンの結論を1行で受け止められる形にする）

---

## 7. 段階（各段は独立して入れられる）

**不変条件は `DESIGN_entry_points.md` §7 のまま**: **`id` を変えない・消さない／`setMode` の3値を変えない**。
版番号は実装レーンが自分の帯で振る。

### 第1段 — 器を作り、「調べる道具」だけ移す（台本は無傷）

| 動かすもの | 中身 |
|---|---|
| 新設 | `#molecule-modal`（`class="modal-overlay hidden"`）。**`#stereo-modal`（`index.html:856`）より前**に置く（§5-5） |
| 移設 | `#btn-isomers`・`#btn-stereo` を `#compound-info` → モーダルの「🔬 調べる」節へ。**id 据え置き** |
| 移設 | `#molecule-props` の ①②③ チップ → モーダルの見出しタブへ（`setFocusedMolecule` を呼ぶだけ） |
| 新設 | `#compound-info` に **「🔬 この分子を調べる」** 1ボタン（件数バッジ付き） |
| 直す | `learn.js:73` の「分子が複数あります」を、**見出しで選んでいる分子だけを渡す**形に（列挙ロジックは不変） |
| 直す | `stereo.js` の `autoCenter` に**対象分子の原子集合**を渡せるようにする（既定は今までどおり全体） |
| 直す | `OPEN_TARGETS` の `isomer` / `stereo` を、モーダルを開いてから押す形に（**1手のまま**にする） |
| 直す | `style.css` の 32px の床を **id 指定のまま**維持（`#btn-isomers` / `#btn-stereo` は既に id 指定＝v523 のとおりで外れない） |

- **テストで固定すること**
  - 新規 **MM1**: 「`#btn-isomers`・`#btn-stereo` が `#molecule-modal` の子孫にあり、パズル・自由の**両方**で開ける」
  - 新規 **MM2**: 「**分子が2つあるとき、②を選んで 📚 異性体を開くと②の分子式で列挙される**」
    （いまは拒否される。**この段でいちばん価値のある1件**）
  - 新規 **MM3**: 「分子モーダルから 🧊 を開くと**分子モーダルが閉じている**」（重ねない・§5-5）
  - 新規 **MM4**: 「`#molecule-modal` が `#stereo-modal` より DOM で前にある」（z の後勝ちを構造で防ぐ）
  - 既存で守られる: `EP3`（🧊・📚 がパズルでも使える）は**行き先が変わるので書き換えが要る**。
    ⚠ **EP3 は「`#compound-info` の中にあること」を見ているので、そのまま通らない。** 意図どおりに直す
  - 既存: `TAP1`（32px の床）が新しいボタンも走査する
- **台本の影響**
  - `#btn-isomers`: demos **0箇所**。`tutorials.json` 1箇所 ＝ **`button` アクションなので rect=0 でクリックだけ実行**。壊れない
  - `#btn-stereo`: **4本・5箇所**。壊れないが**画作りが変わる**——
    いまは右パネルの中なのでシートが開いてカーソルが動く。移設後は
    `syncSheetForButton` が「右パネルの外」と判定して**シートを閉じ**、rect=0 なのでカーソル演出を省く。
    **キャンバスが映ったまま立体モーダルが開く絵**になる。⚠ 収録は 810×1440 ＝ モバイル縦（`record.mjs:53`）
  - **撮り直しはしない**（`DESIGN_entry_points.md` §7-3 と同じ判断。学習内容は変わらない）

### 第2段 — 反応をモーダルへ（台本7本に1手足す）

| 動かすもの | 中身 |
|---|---|
| 移設 | `#molecule-props`・`#reaction-selection`・`#reaction-actions` を `#reaction-card` → モーダルの「⚗ 反応」節へ |
| 移設 | `#btn-reaction-select`（42）も同じ節へ。押したら**閉じてキャンバスへ返す** |
| 新設 | 反応ボタンを押したら**モーダルを閉じてから** `onRuleClick` に入る（§2-5） |
| 残す | `#compound-info` の「🔬 この分子を調べる」に**反応の件数**を出す（§4-2） |
| 直す | `style.css:520` の `#reaction-card .view-btn` を **`#reaction-actions .view-btn` などの場所に依らない指定**へ。⚠ **v523 とまったく同じ退行の作り方**なので、ここは必ず TAP1 で確認する |

- **テストで固定すること**
  - 新規 **MM5**: 「反応ボタンを押すと `#molecule-modal` が閉じ、`reactor.picking` の箇所選択がキャンバスで始まる」
  - 新規 **MM6**: 「モーダル経由で実行した反応の結果が、カード経由のときと**同じ正準コード**になる」
    （`DESIGN_reagent_palette.md` RG4 と同じ考え方＝入口が増えても中身は1つ）
  - 既存: `RX` 系（反応の実行）が**そのまま通る**こと。`execute` から先は1行も変えない
  - 新規 **MM7**: 「`#reaction-actions` のボタンが 32px を下回らない」（TAP1 の走査に入るが、明示でも押さえる）
- **台本の影響**
  - ⚠ **`reactionButton` 13箇所（7本）**。`tutorial.js:448` に rect=0 のガードが無いので、
    モーダルを閉じたまま押すと**カーソルが (0,0) へ飛ぶ**
  - **対策は「台本に1手足す」を採る**（`tutorial.js` にガードを足す案は採らない）。
    ガードを足すと**カーソルもボタンも映らないまま反応が進む**絵になり、
    「エステル化のボタンを押した」という**動画の要点が消える**。
    足すのは `{"type":"button","selector":"#btn-molecule-modal"}` の1行 ×（7本のうち反応を実行する箇所の直前）
  - 対象: `reaction-oxidation` / `reaction-esterification` / `reaction-saponification` /
    `reaction-aromatic-substitution` / `reaction-bromine-water` / `reaction-markovnikov` /
    `stereo-mutarotation` / `l1-digest`（longform）
  - ⚠ **`video-scripts/V*.md` の対応する台本の記述も同時に直す**（開発方針 5章「ヘルプと実装は常に一致させる」）

### 第3段 — 試薬パレット 第1段（瓶3本）

`DESIGN_reagent_palette.md` §5 の第1段を、**この節の中に**実装する。

- 瓶3本（臭素水 Br₂ / 酸化剤 [O] / 濃硫酸）＋ 該当ルール7件に `reagentId`＋条件2つ
- **テストで固定すること**: 同書の **RG1〜RG4** をそのまま。追加で
  - 新規 **MM8**: 「効かない瓶を押しても**分子が1原子も変わらず、モーダルも閉じない**」（§5-3）
- **台本の影響**: **なし**（新しいボタン。既存のセレクタに触れない）

### 第4段 — 試薬 20本 ＋ R/S の受け皿

- `DESIGN_reagent_palette.md` の第2段（12本すべて）・第3段（検出5本）をこの節に載せる
- 「🔬 調べる」節に R/S の1行を足す（中身は別レーンの結論に従う。§6-3）
- **テストで固定すること**: 同書 RG5〜RG8。追加で
  - 新規 **MM9**: 「320px でモーダルが**横にあふれない**・32px 未満の標的が0件」（`tools/check-mobile.mjs` と同じ観点を回帰テストにも）

### 全段共通の手順（開発方針 6章・7章）

1. `assembler/index.html` / `test.html` / `audit.html` の `?v=` とヘッダー表示を同時更新
2. `test.html` を開いて**全件合格**（RX13 は約10%の頻度で落ちるので1回だけ再実行して切り分ける）
3. `node tools/verify-release.js assembler`
4. `node tools/check-mobile.mjs assembler`（**320px を含む20端末**。この見直しは 320px が主戦場）
5. 320px 縦・375px 縦・375px 横・1280×800 の**4つで実機確認**し、コミットメッセージに書く
6. **UTF-8（BOMなし）**・化けパターンの確認

---

## 8. スマホでの見え方（320px で成立するか）

### 8-1. 実測（v524 の現物と試作）

| 見るもの | 320×568 | 判定 |
|---|---|---|
| `.modal-content` の枠 | 288×511（可視 507px） | 右パネルのシート（464px）より **43px 広い** |
| 試作モーダル（見出し＋2カード＋試薬20本） | 中身 **779px** | **1.5画面**。スクロールで成立 |
| 横あふれ | **なし**（`c.scrollWidth === c.clientWidth`・body も横スクロールなし） | 合格 |
| 32px 未満のタップ標的 | **0件** | 合格（ただし §6-2 の床を敷いた前提） |
| `#stereo-modal`（現物） | 中身 **1305px**・可視 507px | **2.6画面**。いまでも縦に長い ＝ **中に畳めない根拠** |

### 8-2. 守る規約

- **`max-height:90dvh; overflow-y:auto; max-width:94vw`**（`style.css:1066`）。`.modal-content` に自動で効くので、
  分子モーダルは `.modal-overlay > .modal-content` の形さえ守れば**新しい CSS を書かなくてよい**
- **PC の既定は `max-width:450px`**。試作は PC で 450×807・中身 803px ＝ ちょうど1画面。
  `#stereo-modal` のように広げる（980px）必要は**ない**
- 図を入れるときは `.modal-content svg { max-width:100% }`（`style.css:730`）に乗せる。
  ⚠ 高さが属性固定の SVG は幅だけ詰まって空白が残る前例がある（`style.css:734` のコメント）。
  **分子モーダルには図を持たせない**（図はキャンバスと立体モーダルにある）ので、この落とし穴は踏まない

### 8-3. 右ペインはどれだけ軽くなるか（見込み）

| モード | v524 実測（320px） | 第2段のあと（見込み） |
|---|---|---|
| 🧪 自由 | 630px | **約 320px**（`#reaction-card` 155px と `#compound-info` の2ボタン分が出る） |
| 🧩 パズル | 1251px | 約 1100px（判定オプションが主。ここは別の話） |
| 📚 学習 | 408px | 変わらない |

**自由モードは可視 464px を下回る ＝ シートを開いても1画面で収まる。**
「右ペインをあまり使わない方がよさそう」という見立ては、この見積もりの上でも合っている。

---

## 9. 未決事項（実装前にユーザーに確認したいこと）

1. **背景タップ・Escape で閉じられるようにするか。** いまは14枚とも**閉じるボタンだけ**。
   分子モーダルだけ違う作法にはしたくないので、**やるなら14枚まとめて**別項目にするのを推す
2. **立体・異性体を閉じたあと、分子モーダルへ戻すか。**
   戻すなら「どこから開いたか」の状態を1つ増やす（`?open=` からの着地でも矛盾しない形が要る）。
   **まず戻さない形で入れて、実物を触って判断する**のを推す
3. **モーダルが開いている間、`Ctrl+Z` / `Delete` を止めるか。** いまは全モーダルで効く（既存の挙動）。
   分子モーダルは分子を触る面なので誤爆が目立つ可能性がある。**この見直しの範囲外として保留**を推す
4. **第2段で台本7本に1手足すことの是非**（§7 第2段）。
   足さない案（`tutorial.js` にガードを足す）は**動画から「反応ボタンを押す絵」が消える**ので推さないが、
   台本を触るのを避けたい場合はこちらもありうる
5. **「🔬 この分子を調べる」の文言。** ボタン1つに反応・試薬・異性体・立体が全部ぶら下がるので、
   語が広すぎないか。候補: 「🔬 この分子を調べる」／「⚗ 反応と立体を見る」／「🔍 くわしく見る」。
   ⚠ **`.hint-box` と FAQ の該当箇所も同時に直す**（開発方針 5章）
6. **`#compound-info` に残す件数バッジの粒度。**「反応 2件」まで出すか、「反応あり」だけにするか。
   件数はライブ更新（`reactor.refresh()` のたび）なので、**数字が出たり消えたりする**点をどう見るか

---

## 10. ユーザーの決定（2026-08-04）

§1 の結論のうち、判断が要る3点に回答を得た。**設計はこの節が正**。

| 論点 | 設計書の見立て | **決定** |
|---|---|---|
| 立体を中に畳めない | ボタンだけ集約し実体は既存モーダル | **そのまま採用**（別モーダルの呼び出し） |
| 分子そのものをタップは採れない | 開くのはボタン | **キャンバス上の分子の見出し（`①乳酸` の文字）をタップして開く** |
| 反応の実行 | 押したらモーダルを閉じてキャンバスへ返す | **そのまま採用**（実行はキャンバス内） |
| 収録済み台本7本 | 1手足す or ガード | **台本に1手足す**（動画は撮り直さない） |

### 10-1. 開く入口は「キャンバスの見出しをタップ」

**`#compound-info`（右パネルの「🔍 いま描いている分子」）は入口にしない。**
ユーザーの言葉は「**右のパネルの今書いている分子は使いづらい**」。
右パネルはモバイルでシートを開くまで存在ごと見えないので、**そこを入口にすると
「右ペインをあまり使わない」方針と噛み合わない**。

見出しは `game.js:3973` でいま `pointer-events: none` にしてある。理由もコメントに残っている:

> 見出し自体を当たり判定にすると、**見出しの位置（分子の1マス強下）に原子を置けなくなる**

**この制約は実在する。** 当たり判定は**文字の矩形だけ**に絞り、実装時に次を実測して決める:

- 見出しをもう半マス下げて、原子を置ける帯を空けるか
- マーク系ツール（✳・⇄・⬍）が ON のあいだは `pointer-events` を戻すか
- 見出しの左右に余白を作り、**文字の幅ぶんだけ**を的にするか

**押せることが見た目で分かる必要がある**（いまはただの文字）。下線・ホバー・`cursor:pointer` の
どれを使うかは実装時に決める。**タッチには hover が無い**ので、hover だけに頼らないこと。

### 10-2. 名前の置き場所は、この決定で見直しの余地が出た

**キャンバスの見出しが入口になるなら、右パネルの名前表示は役割が重なる。**
いま名前は3か所に出ている:

| 場所 | 条件 |
|---|---|
| キャンバスの見出し（`①乳酸`） | **分子が2つ以上のときだけ**（`renderMoleculeLabels`） |
| `#compound-info` の `#compound-name` | パズル・自由（右パネル） |
| `#mobile-name-chip` | モバイルで右パネルが閉じているとき |

⚠ **見出しは「分子が2つ以上のとき」しか出ない。** 1分子のときに入口が消えるので、
**1分子でも見出しを出す**か、**1分子のときは別の入口を用意する**かを決める必要がある。
これは第1段の実装前に確定させること（§7 第1段の受け入れ条件に足す）。

### 10-3. 台本は「1手足す」（④A）

**動画の撮り直しはしない。台本ファイルに1行足すだけ。** 対象は反応ボタンを押す7本。

放置すると **N2（SNS デモの再生）が1本ごとに失敗を検出する**ようになっている（v437）ので、
**そのままでは回帰テストが赤くなり「コミット前に全テスト合格」が塞がる**。
1手足すのが最も安く、壊れを見えなくもしない。

**足す位置は「反応ボタンを押す手順の直前」**。`quizForce`（v441）と同じく、
画面に何も起こさないアクションではなく **実際にモーダルを開くタップ**にすること
（台本は「本物の操作でアプリを駆動する」のが原則）。

