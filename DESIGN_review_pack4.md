# DESIGN_review_pack4.md —— v1516 → v1519（2026-09-04〜06 の4版）＋ ion-equation v200 / qa v98 の定期レビュー

**状態: レビューのみ。コード・既存設計書・データ・版番号は1バイトも変えていない。**（2026-09-06・review4 レーン・ブランチ `review4`）

**対象**: `git log b5c23c3..f9b7841`（v1515 `b5c23c3` → v1519）。中身:
脱水の書き出し14問（v1516）／★★ 電荷を化学モデルに入れた（v1517・`chemistry.js` の土台）／
参考書の5枚目「アルコールの脱水の対応表」（v1518）／★ アミンの塩を本物に描く（v1519）／
ion-equation の有機の酸・塩基10反応（v200）／qa の `kind:"ion"`（v98）。
`video-scripts/`・`demos-*.json` は対象外。

**凡例**（すべての指摘に出典を付ける）:
- 【現物】= リポジトリの現物（コード・データ）を読んで確かめたこと（`ファイル:行`）
- 【実測】= この worktree（`C:/Antigravity/worktrees/review4`）を配信し、node（vm に `chemistry.js` を載せる）か
  `tools/record/` の Playwright（Chromium・headless）で実アプリを動かして測ったこと。
  スクリプトは scratchpad に置き、リポジトリには入れていない
- 【記述】= 設計書・DEVELOPMENT.md に書いてあること
- 【意見】= このレビューの判断

⚠ **この報告書は「見つけたことを1件ずつ書き足してコミット」の形で育てる**（スリープ復帰でアプリが落ちる環境のため）。
節の順は最後に整えるが、**途中で落ちても書いた分は残る**ことを優先する。

---

## 0. 要旨（先に結論だけ）

（作業の最後に書く。ここまでに書けた節が下にある）

---
