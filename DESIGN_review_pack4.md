# DESIGN_review_pack4.md —— v1516 → v1519（2026-09-04〜06 の4版）＋ ion-equation v200 / qa v98 の定期レビュー

**状態: レビューのみ。コード・既存設計書・データ・版番号は1バイトも変えていない。**（2026-09-06〜07・review4 レーン・ブランチ `review4`）

**対象**: `git log b5c23c3..f9b7841`（v1515 `b5c23c3` → v1519）。中身:
脱水の書き出し14問（v1516）／★★ 電荷を化学モデルに入れた（v1517・`chemistry.js` の土台）／
参考書の5枚目「アルコールの脱水の対応表」（v1518）／★ アミンの塩を本物に描く（v1519）／
ion-equation の有機の酸・塩基10反応（v200）／qa の `kind:"ion"`（v98）。
`video-scripts/`・`demos-*.json` は対象外。

**凡例**（すべての指摘に出典を付ける）:
- 【現物】= リポジトリの現物（コード・データ）を読んで確かめたこと（`ファイル:行`）
- 【実測】= この worktree（`C:/Antigravity/worktrees/review4`）を `python -m http.server 9714` で配信し、
  node（vm に `chemistry.js` を載せる）か `tools/record/` の Playwright（Chromium・headless）で実アプリを動かして測ったこと。
  スクリプトは scratchpad（`wide-dump.js`・`charge-probe.js`・`probe-amine.mjs`・`probe2.mjs`・`probe-audit.mjs`）に置き、リポジトリには入れていない。
  配信の前後で印ファイル `review4-marker.txt` を curl し、自分の worktree を配っていること（前）と削除後に 404（後）を確かめた
- 【記述】= 設計書・DEVELOPMENT.md に書いてあること
- 【意見】= このレビューの判断

⚠ **この報告書は「見つけたことを1件ずつ書き足してコミット」の形で育てた**（スリープ復帰でアプリが落ちる環境のため）。

---

## 0. 要旨（先に結論だけ）

1. ★★ **(A) 電荷: 道具が見ていない面を突いた結果は 0 件**（§1）。
   `dump-canonical.js` の 7 列を **20 列**（`verifyMolecule` の相方になる `rootedFragmentCode`・`findOutOfScopeMotifs`・`stereoUnitsOf`・`countStereoisomers`・
   `describeStructure`・`isomerSeriesKey`…）に広げて v1515 と v1519 の `chemistry.js` で 1,151 件を書き出し、**既存 1,150 件で diff 0 行**【実測】。
   **`verifyMolecule` は同じ分子式の 6,245 組すべてで `canonicalCode` と一致し、旧→新で答えが変わった組 0**【実測】。
   電荷つき 15 種を原子順 30 通りで回して正準コードは各 1 つ。価標を原子単位で読むのは `maxValencyOf` 1 か所【現物・grep】。
   ⚠ 見つけたのは土台ではなく**周辺**: **`buildExportJson`（作図エクスポート）が `charge` を落とす**（§1-5・次に塩を登録するレーンが踏む）／
   相方の居ない Cl⁻ が見出し「Cl」で出る（§1-6）／`chargedValency` が符号と元素の組を検査しない（§1-4）。
2. ★★ **(B) アミンの塩: 162 件すべてで塩→名前→NaOH で元に戻る**【実測】。**起こってはいけない相手で起きたのは
   ピロール・インドール・インジゴ**（芳香族の N・ビニロガスアミドは塩基でないのに「ピロール塩酸塩」）（§2-1）。
   ⚠⚠ **いちばん重いのは逆向き**: **⇄ で双性イオン形にしたグリシンに塩酸／NaOH の瓶を押すと「いまの分子にはどれもありません」「塩もアミンもありません」**と
   画面が言う（§2-3）。設計書 §14-7 は「両性は入れていない」と正直だが、画面は「起こらない」と言い、**同じ日に ion-equation v200 が `glycine-hcl`/`glycine-naoh` を教えている**。
   ピリジン・キノリンにも「アミンがありません」（§2-2）。名前の折り返しは 161 件で効くが、**ジアミン 15 件の 2 回目**は「🔍 C₆H₁₈Cl₂N₂」に落ちる（§2-4）。
3. ★ **(C) ion-equation の 10 反応: 化学として直すべき式は 0 本**（§3）。原子・電荷の収支は全式・イオン式とも合う【実測】。
   グリシン二段（COOH → NH₃⁺）・グルタミン酸三段（α-COOH → 側鎖 COOH → NH₃⁺）の順は正しい。ジアゾニウムの「5℃ を超えると」が式の隣にある。
   qa からの着地 4 コード（2・1・2・5 件）は往復の帯つきで開く【実測・DOM】。
   ⚠ `library.html` の「反応の型」の絞り込みに `その他`（電離平衡 5 本）・`弱酸弱塩基の遊離` が並ばない（v200 以前からの作り）。
4. ★★ **(D) 監査は 8 面中 4 面に届く**（§4・300 反復 × 80 操作・287 秒・seed 1000）。
   届く: 電荷つき登録（① 0 問題・② 呼び出し 12 回）／参考書 5 枚目（39 回）／`amine_hcl` 42 回・`amine_liberate_naoh` 23 回／名前の折り返し 283 回。
   **届かない: ⇄ 双性イオン形 0 回・脱水の書き出し 14 問 0 回（構造的）・caption 4 か所（字幕を読まない）・ion/qa（別アプリ）**。
   ＝ **§2-3 の嘘は監査では一生見つからない面**にある。
5. ★ **(E) 数**: 緑の注記はすべて実態と一致（61/61・55 本・16 本・電荷つき 1 件）【実測・全走 802 件・1,006 秒】。
   **`?only=` で落ちる帯は 4 → 1（`LB1` だけ）**。`EQ`・`EX3/EX4`・`FZ2` は単独でも緑。`CV4_NO_CHANGE_RULES` 6 本 ＝ `_info` 6 本ちょうど。
6. ○ **確かめて問題なかった範囲**は §8。回帰 802 件・ion・qa 96 件・`verify-release`／`verify-compounds`（worktree の中）すべて合格。

---

## 1. ★★ (A) 電荷の検収 —— 「道具が見ていない面」を突いた結果

### 1-1. ★ 道具の外まで広げた無差分 —— **既存 1,150 件 × 20 列で 0 行**【実測】

`tools/dump-canonical.js` は 7 列（正準コード・IUPAC 名・立体コード・分子式・空き価標・価標検査・官能基の型）。
★ **`verifyMolecule` は列に無い**し、`findOutOfScopeMotifs`・`findCondensableGroups`・`stereoUnitsOf`・`countStereoisomers`・
`describeStructure`・`isomerSeriesKey`・`findLongestCarbonChain`・`getDoubleBondGeometry`・`rootedFragmentCode`（不斉判定の断片コード）・
`findAromaticBondKeys` も無い。

**方法**: v1515（`b5c23c3`）の `chemistry.js` と v1519 の `chemistry.js` を別々の vm に載せ、v1519 の `stages.json`＋`compounds.json`
1,151 件を両方で走らせて **20 列**（上の 13 関数＋道具の 7 列。官能基は `atomIds` まで）を書き出し `diff`（`wide-dump.js`）。

| | 値 |
|---|--:|
| エントリ | 1,151（stages 121・compounds 1,030） |
| 差分が出た行 | **1 行 ＝ `aniline-hydrochloride` だけ**（旧は電荷を読まないので `N2/Cl1`・`amine1`、新は `N3+/Cl0-`・`ammonium`。期待どおり） |
| 既存 1,150 件の差分 | **0 行**（20 列すべて） |
| `ERR:` の列 | 0 |

★ ＝ **実装レーンの主張「1,151件の 7 列は1バイトも変わっていない」は、列を 20 に広げても成り立つ。**

### 1-2. ★★ `verifyMolecule`（同型判定）を全件×同じ分子式で測った —— **0 件**【実測】

「前は同じと判定していた組が違うと判定される」を、**同じ分子式の組だけ**に絞って全部見た（`charge-probe.js` §3）。

| | 値 |
|---|--:|
| 分子式の種類 | 399 |
| 同じ分子式の組 | **6,245 組** |
| 新: `verifyMolecule` ≠ (`canonicalCode` 一致) の組 | **0** |
| 旧 → 新で `verifyMolecule` の答えが変わった組 | **0** |
| 旧 → 新で `canonicalCode` 一致/不一致が変わった組 | **0** |

★ **同値関係は `canonicalCode` と `verifyMolecule` で一致している**（設計書 §13-3 の「同一でなければならない」を全件で確認）。

### 1-3. ★ 電荷を持つ分子の正準コードが原子順に依存しないか —— 15 種 × 30 通りで各 1 コード【実測】

アニリン塩酸塩／アニリニウム単独／グリシン双性イオン／グリシン塩酸塩／N(4) 中性・N⁺(4)・N⁺(4)＋Cl⁻／ジアゾニウム塩／
ニトロ（中性・電荷分離）／メトキシド／カルボカチオン など 15 種を、原子順を 30 通りに並べ替えて `canonicalCode` → **すべて 1 コード**（`charge-probe.js` §1）。

**割れる／割れない（新）**（`charge-probe.js` §2）:

| 組 | 旧 | 新 | 判定 |
|---|---|---|---|
| N(4) 中性 vs N⁺(4)（第四級） | 同じ | **割れる** | ★ 狙いどおり |
| グリシン vs 双性イオン形 | 同じ | 割れる | 狙いどおり（分子式は同じ C₂H₅NO₂） |
| アニリン vs アニリニウム | 同じ | 割れる | ○ |
| メタノール vs メトキシド | 同じ | 割れる | ○（`iupacName` はメトキシドで null） |
| アニリン塩酸塩 vs N-クロロアニリン | 割れる | 割れる | ○ |
| ⚠ **ニトロ N(=O)(-O) 中性 vs N⁺(=O)(-O⁻) 電荷分離** | 同じ | **割れる** | ⚠ 同じ物質の2つの書き方が別コードになる。**ただし電荷を手で描く経路は無い（D-I14）ので、いまは通るものが無い**。ニトロを電荷分離で登録する日が来たら当たる |

### 1-4. ★ 価標を読む経路を機械で洗った —— 原子単位で読むのは `maxValencyOf` 1 か所【現物】

`VALENCIES[` を直接読む所は 12 か所（`grep`）。**原子（`atom`）を引数に価標を決めるのは `chemistry.js:489` の `maxValencyOf` だけ**で、
残りは元素名だけで読む（列挙器の足切り `chemistry.js:554,594,637,655,917`、パレットの `game.js:2270,2759`、`getMaxBondType` `game.js:11758`、
`tests.js:18223`）＝ **まだ置いていない原子・元素の組み合わせの上限**なので電荷の出番が無い。`audit.js:380` は `maxValencyOf` を読む。
★ **1 か所で足りている。** ⚠ 例外は既知のとおり `tools/verify-reactions.js valencyLimit`（機構データ用に別表。§13-6 申し送り済み）。

`chargedValency` の表【実測・`charge-probe.js` §4】: N⁺4／N⁻2／O⁻1／O⁺3／C⁺3／C⁻3／S⁺3／Na⁺K⁺Cl⁻Br⁻I⁻ 0。
⚠ **符号と元素の組を検査しない**ので、Cl⁺→0・Na⁻→0・H⁺→2・N²⁺→5 も黙って通る。データからしか入らないので実害は無いが、
`ION1` の「名簿に無いものが電荷を持てば赤」は**件数**を見張っていて**符号**は見ていない（アニリン塩酸塩の Cl を +1 にしても赤にならない —— 未実測・現物の読み）。

### 1-5. ⚠ 写しの経路 —— `copyAtomMarks` を通らない `addAtom` の写しが **4 か所**【現物】＋ 書き出し JSON が電荷を落とす【実測】

`.addAtom(a.element, a.x, a.y)` の形の写し 27 か所のうち、`copyAtomMarks` を通らないもの:

| 場所 | 何の写しか | 電荷を落とすと |
|---|---|---|
| ⚠ **`game.js:4211-4214` `buildExportJson`** | **作図エクスポート（P7-3・コンテンツ制作用 JSON）**。`haworthFace` は写すが `charge` は写さない | ★ **アニリン塩酸塩を呼び出して書き出すと `charge` が消える**【実測: `buildExportJson()` の文字列に `charge` 無し】。**次に「◯◯塩酸塩」を登録するレーンが、`amine_hcl` で作って書き出すとアニリン＋Cl（HCl）の図が JSON になる** |
| `learn.js:5251` `addCopy`（書き出し練習の「もう1つ置く」） | `haworthFace` を手で写している（`copyAtomMarks` の注記「印の一覧はここ1か所」に反する形） | 練習の題材に電荷は無い。実害なし |
| `learn.js:5315-5318` `figureOf` | 答案の図データ | 同上 |
| `quiz.js:1023` | 芳香族結合の検出用の一時分子 | 出力の `atoms` は `{...a}` で写すので電荷は残る。実害なし |
| `reaction.js:837, 1098` | 予測モードの主生成物 | 生成物は中性。実害なし |
| `narrowing.js` 8 か所 | 手描きの分子の絞り込み | 手描きに電荷は無い |

★ ＝ 実害があるのは **`buildExportJson` の 1 か所**。1 行（`charge: a.charge` を足す）で直るが、
「コンテンツ制作の道具」なので**次に電荷つきの登録を作るレーンへの申し送り**として書く（§9）。

### 1-6. ★ `attachCounterIons`（粒を相方に付ける）—— 3 成分・同点・相方なし【実測・`probe2.mjs`】

| 場面 | 結果 |
|---|---|
| アニリン＋フェノールに塩酸 | Cl⁻ は **アニリン**に付く（フェノールは正味 0 で候補に入らない）。見出し「アニリン塩酸塩」「フェノール」 ○ |
| アニリン塩酸塩 × 2 を並べて片方に NaOH | 戻ったのは 1 つだけ。「アニリン」＋「アニリン塩酸塩」 ○（`nearestCounterIon` は自分の Cl⁻ を取った） |
| ⚠ **相方が居ない Cl⁻**（ベンゼン＋粒だけ） | 粒は独立した成分。**見出しは「Cl」・名前なし・分子式「Cl」** ＝ 塩化物イオンが塩素原子の式で出る（`D-I6` と同じ穴。到達するのは陽イオン側を消しゴムで消したとき） |
| 粒に官能基モジュールを置く／結合を足す | `getFunctionalGroupPlan` が `valency` で拒む（空き 0）○ |

⚠ 3 成分以上・同点の実測は「2 つの塩」までで、**同じ距離に 2 つの相方が並ぶ図は作っていない**（座標が同じ距離になるのは手で動かしたときだけ）。

### 1-7. ★ その他、確かめて問題なかったこと【実測】

- `iupacName`（系統名）は道具の列 5 にあり、既存 1,150 件で不変（§1-1）。`iupacNameDetail` の門番で名前が消えたのはメトキシドのような**電荷を持つ図だけ**（ライブラリでは 0 件）
- 立体コード（列 6）・不斉判定の断片コード（`rootedFragmentCode`）は既存で不変。アニリン塩酸塩の断片コードは `N3+` を含む
- 反応機構（`reactions.json` の `charge` 52 か所）は `renderState` が**明示 H で静的に描く**（`reaction.js:540-560`）ので、価標の変更が絵に効かない ○
- 第四級 N(4) を**電荷なし**で描いた図: 官能基が立たず `amine_hcl` も効かない（`quatNeutral`: groups [] ・detect 0）○ ＝ 「アンモニウムは塩でありアミンではない」の線どおり

---

## 2. ★★ (B) アミンの塩の化学（v1519）—— 実機で 1 件ずつ当てた

**方法【実測・`probe-amine.mjs`】**: ライブラリ全件（`getCompoundLibrary()` 1,151 件）に `amine_hcl.detect` を当て、
当たった **162 件**を 1 件ずつ `summonMolecule` → `apply` → `splitMolecules` → `lookupCompoundName` → `amine_liberate_naoh` で戻す。
⚠ 実装記録（§14-5）は「塩基性アミン 177 件」と書いているが、`getCompoundLibrary()` で数えると 162 件（差 15 は数え方の違いと思われる。未追跡）。

| | 値 |
|---|--:|
| `amine_hcl` が当たった | **162 件**（2 か所あるもの 15 件） |
| `apply` が例外 | 0 |
| `changed` の id が全部 `mol.atoms` に在る・重複なし | **162/162** |
| 1 回目の塩酸で名前が付いた | **162/162**（アニリンは登録・161 は折り返し） |
| NaOH で元の正準コードに戻った | **162/162**（2 か所のものも 2 回で戻る） |
| 否定対照（detect 0）: ニトロ・ニトリル・アミド・アニリド・尿素・ラクタム・ナイロン・アニリン塩酸塩 | **179 件すべて 0** |
| 双性イオン形のグリシン／ジアゾニウム塩（合成） | `amine_hcl` 0・`amine_liberate_naoh` 0 ○ |

### 2-1. ⚠⚠ 「起こってはいけない相手」で起きる —— **ピロール・インドール・インジゴ**（名前で）

| 相手 | 何が出るか | なぜ間違いか |
|---|---|---|
| ★ **ピロール** | 瓶「塩をつくる（アミン + 塩酸）→ 水層へ」→ **「ピロール塩酸塩」** | ピロールの N の非共有電子対は芳香族六電子の一部で、**塩基性を示さない**（共役酸の pKa ≈ −3.8）。塩酸では塩にならず重合する |
| ★ **インドール** | 同上 → **「インドール塩酸塩」** | 同じ理由（インドールの N も芳香族） |
| ⚠ **インジゴ** | 2 か所 → 「インジゴ塩酸塩」 | N はカルボニルと共役したビニロガスアミド。塩酸で塩にならない |
| △ イミダゾール | 「イミダゾール塩酸塩」 | 名前は実在するが、**H⁺ が付く位置が逆**（塩基性なのは =N– の側。アプリは N–H 側を N⁺H₂ にする）。分子式は同じ |
| △ カフェイン／テオブロミン／キサンチン／プリン | 「◯◯塩酸塩」 | ごく弱い塩基（カフェインの pKa ≈ 0.6）。位置もイミダゾールと同じく N-CH₃ 側 |

原因【現物】: `findFunctionalGroups` の N の枝（`chemistry.js:1592-1611`）は「単結合だけ・相手が C だけ」で `amine1/2/3` を立て、
**環の芳香族性を見ない**。`basicAmineNitrogens`（`reactor.js:2196`）はアミドだけを除く。
⚠ ついでに: ピロール・インドール・イミダゾールには **「付加: HCl（マルコフニコフ則）」の札も並ぶ**（芳香族の C=C に HCl 付加）。
これは v1519 より前からの穴（5 員環の芳香族を `findAromaticBondKeys` が拾わない）で、今回の窓の外だが同じ画面で見える。

【意見】ピロール・インドールは高校の範囲外だが、登録されている以上「ピロール塩酸塩」は化学として誤り。
直し方は `basicAmineNitrogens` に「N が 5 員環の中に居て、環内に C=C が 2 本ある（芳香族 5 員環）なら除く」の 1 条件。
`TR`/`SEP` の否定対照にピロールを足してから。

### 2-2. ⚠ 「起こるべきなのに起こらない」—— ピリジン・キノリン（画面が「アミンが無い」と断言する）

ピリジン（登録あり）・キノリン・ニコチン酸に塩酸の瓶【実測・DOM】:
> 「…アミン（塩になって水層へ移ります）です。**いまの分子にはどれもありません**。…いまの分子には**塩もアミンもありません**」

ピリジンは塩基（pKa 5.2・塩酸で塩化ピリジニウム）。高校範囲外だが、**「アミンが無い」は嘘**（前回の「ベンゼン＋水で進みません」と同じ型の、画面の断りが化学を間違える件）。
原因: 二重結合を持つ N は `findFunctionalGroups` で何の型も立たない（`chemistry.js:1613` `!nb.every(n => n.type === 1)` で return）。
ニコチンは**ピロリジンの N** で塩になる（正しい位置）。

### 2-3. ⚠⚠ 双性イオン形に塩酸／NaOH をかけると「いまの分子にはどれもありません」【実測・DOM】

グリシンを ⇄ で双性イオン形にして瓶を押す:

| 瓶 | 返事（抜粋） | 化学 |
|---|---|---|
| 塩酸 | 「…アミン（塩になって水層へ移ります）です。**いまの分子にはどれもありません**。…**いまの分子には塩もアミンもありません**」 | ⚠ **嘘**。-COO⁻ が H⁺ を受け取って陽イオンになる（**同じ日に ion-equation v200 に入った `glycine-hcl` がまさにこの式**） |
| NaOH | 「…酸性の -OH をもつもの…です。**いまの分子にはどれもありません**」 | ⚠ **嘘**。-NH₃⁺ が H⁺ を渡して陰イオンになる（ion の `glycine-naoh`） |
| NaHCO₃ | 「CO₂ は発生しません。フェノールも酸性を示しますが…」 | △ 双性イオンにフェノールの話をする（文は汎用） |

設計書 §14-7 は「両性は入れていない」と正直に書いているが、**画面は「起こらない」と言う**。⇄ のボタンが双性イオン形へ誘っておいて、
そこに瓶を押すと否定される。★ **同じリリースで ion-equation がこの2式を教えている**ので、2 アプリの間で食い違う。
【意見】両性のルール 2 本（§14-7 の見積り）を入れるまでは、`miss` の文に「双性イオン形にはまだ対応していません」の 1 文を足すのが最小。

### 2-4. ⚠ 名前の折り返し「◯◯塩酸塩」—— 変な名前の組

| 組 | 出る名前 | 判定 |
|---|---|---|
| 別名つきの 15 件（例: **グリシルグリシン（ジペプチド）塩酸塩**・トレオニン（スレオニン）塩酸塩・1,4-ブタンジアミン（プトレシン）塩酸塩・アドレナリン（エピネフリン）塩酸塩） | 「名前（別名）塩酸塩」 | ⚠ 「（ジペプチド）塩酸塩」は別名の側に塩酸塩が付いて読める。登録名の流儀の帰結なので誤りではないが、**「◯◯塩酸塩（別名塩酸塩）」か別名を落とす**のが読みやすい |
| ジアミン 15 件の **2 回目**（ヘキサメチレンジアミン・エチレンジアミン・o/m/p-フェニレンジアミン・リシン・ピペラジン・ヒスタミン…） | 1 回目「ヘキサメチレンジアミン塩酸塩」→ 2 回目 **「🔍 C₆H₁₈Cl₂N₂」（名前なし）** | ⚠ 二塩酸塩は名乗れない（電荷 4 個で門番落ち）。「（未登録）を出さない」の狙いが、ジアミンの 2 回目では届いていない。★ **1 回目の「◯◯塩酸塩」も一塩酸塩と読める**が、化学としては可 |
| **D-アラニン**に塩酸 | 立体トグル OFF「アラニン塩酸塩」／ON「**D-アラニン塩酸塩**」 | ○ 見出しの立体トグルと同じ挙動（OFF なら塩酸をかける前も「アラニン」）。指摘ではない |
| スルファニル酸／o,m-アミノベンゼンスルホン酸 | 「スルファニル酸塩酸塩」 | ⚠ 実物は双性イオン（-SO₃⁻/-NH₃⁺）で、塩酸に溶けない。-SO₃H のまま N⁺Cl⁻ の図は存在しない種 |
| 2,4,6-トリブロモアニリン／o,m,p-ニトロアニリン | 「◯◯塩酸塩」 | △ ごく弱い塩基（pKa ≈ −0.3〜1.0）。希塩酸には溶けない（§7 の「疑わしい」） |

### 2-5. ○ `changed`（印）と `CV4`

- 162 件すべてで `changed` の id が在る・重複なし（§2 の表）。`amine_hcl` は `[N, Cl⁻]`・`amine_liberate_naoh` は `[N]` ○
- `CV4` の物差し `cvBondSig`（`tests.js:47138`）が「隣の id ＋ 結合次数**＋電荷**」になった【現物】。電荷の変化は結合を変えないので、
  これが無いと `amine_hcl` の N が `moved` に入らず「印を付けたほうが extra」で赤になる、という注記は筋が通る。
  ★ **反応後に新しく現れた Cl⁻ の粒**は `cvMarkMismatch`（`tests.js:47169`）の `!beforeSig.has(id)` で `moved` に入る ＝ 印に入っていなければ `missing` で赤。
  `amine_hcl` の `changed: [nId, cl.id]` はこれを満たしている（現物・緑）。物差しの直しは正しい
- `CV4_NO_CHANGE_RULES` 6 本 ＝ `reactor.js` の `_info` ルール **6 本ちょうど**（`grep id: '..._info'`）。`_info` は解説だけの札なので「変わらないのが正しい」 ○

### 2-6. ⚠ 到達経路は無いが、門番が緩い —— 第四級アンモニウム塩に NaOH【実測・合成した図】

N⁺(CH₃)₄ Cl⁻ を `createTargetFromData` で置いて `amine_liberate_naoh.detect` → **1 か所**。apply すると
caption「**-NH₃⁺ → -NH₂** …もとのアミンが遊離」で、残るのは **線 4 本の N（電荷なし）C₄H₁₂N** ＝ 存在しない中性種。
`ammoniumSaltNitrogens`（`reactor.js:2218`）は「型 `ammonium`・成分の正味 +1」だけで **N⁺ に H が残っているか**を見ない。
⚠ **到達経路は無い**（電荷を手で描けない・第四級を作るルールも無い）ので、いまは指摘ではなく**次に第四級を登録する前の 1 行**（`getFreeValency(N) >= 1`）。

---

## 4. ★★ (D) 監査は v1516〜v1519 の面に届くか

**方法【実測・`probe-audit.mjs`】**: `audit.html?seed=1000`・①ライブラリ ON・②ファズ **300 反復 × 80 操作**・③④ OFF。所要 **287 秒**。
`comparableKey = ops=80/thr=24,12,16,12/mix=5/grp=0.45/cov=0.5/fac=0.5`（⚠ pack3 は mix=3 なので失敗率は並べない）。
開始直後に iframe の `game.toggleZwitterion`／`summonMolecule`／`ammoniumSaltName`／`zwitterionName`／`refDehydrationMap`／`referenceBook.open` を包んで数えた。

| # | v1516〜v1519 で足した面 | 版 | 監査 | 実測（300 反復） |
|--:|---|---|---|---|
| 1 | 脱水の書き出し 14 問（`learn.js` 書き出し練習） | v1516 | **外（構造的）** | ファズに書き出し練習の枝が無い。0 回（pack3 の「learn.js の練習は外」のまま） |
| 2 | 電荷つきの登録（アニリン塩酸塩） | v1517 | ★ **内（①②）** | ①ライブラリ 2,499 件・0 問題（電荷つきは列挙を飛ばす `audit.js:515`）／② `summon('アニリン塩酸塩')` **12 回**・キャンバスに電荷が在った標本 7 回 |
| 3 | ⇄ 双性イオン形の切り替え | v1517 | **外** | `toggleZwitterion` **0 回**・`zwitterionName` 当たり **0 / 2,650 回** |
| 4 | 参考書の 5 枚目（脱水の対応表） | v1518 | ★ **内（②）** | `alcohol-dehydration` を **39 回**開き、表は毎回行あり（📖 5/5 ページ・空の表 0） |
| 5 | `amine_hcl` が本物の塩を描く | v1519 | ★ **内（②）** | 押した **45**・分子が変わった **42**／`amine_liberate_naoh` 押した **23**・変わった **23** |
| 6 | 名前の折り返し `ammoniumSaltName` | v1519 | ★ **内（②）** | 呼ばれた 2,933 回・**名乗った 283 回** |
| 7 | caption の断り 4 か所（`ION8`） | v1519 | **外** | 監査は字幕の文面を読まない（回帰テストだけが門番） |
| 8 | ion-equation v200 の 10 反応／qa v98 の `kind:"ion"` | — | **外（別アプリ）** | assembler の監査の外。`ion-equation/audit.html` は今回回していない |

★ **答え: 8 面中 4 面（#2・#4・#5・#6）に届く。ユーザーに見える新しい操作のうち「⇄ 双性イオン形」と「脱水の書き出し」は 0 回。**
⚠ #3 が 0 なのは、モーダルの `⇄` ボタンを押す枝がファズに無いから（`OP_MIX` の 13 種に無い）。§2-3 の「双性イオン形に瓶を押すと嘘を言う」は
**監査では一生見つからない面**なので、回帰テストの否定対照に入れるしかない。

**同じ実行の他の数**: 反応 55 本中 42 本に到達・0 回 13 本（`alkylate_arene_propene`・`dehydration_anhydride`・`hydrolysis_anhydride`・`alkali_fusion`・`liberate_co2`・
`condensation_polymerization`・糖の 4 本・加硫・アセタール化・グリコシド）／瓶 25/25／課題 9/12（0 回: `eq-salicylic-aspirin`・`eq-benzene-nitrobenzene`・`eq-aniline-acetanilide`）／
失敗率 11.0%（CI 7.9〜15.0）・内訳 結合線の下の自動水素 26・原子 8・重なり 4・見出し 2・**瓶「br2_water」無反応 1**（`audit.js:1198` の既知の取りこぼし）。

---

## 5. ★ (E) 数と門番

### 5-1. 緑の注記【実測・全走 802 件・1,006 秒】

| 注記 | 実態 | 動的か |
|---|---|---|
| 「見張った 61/61 本（分かれた 15・分かれない 46）」（`CV1`） | 実行ルール 61 | ○ |
| 「印を突き合わせた 61/61 本（うち結合が変わった 55 本）」（`CV4`） | 61 − `CV4_NO_CHANGE_RULES` 6 = 55 | ○ |
| 「題材 16 本ぶん・化合物名 36 件」（`FZ1`）／「届いていなかった 16 本」（`FZ2`） | pack3 と同じ | ○ |
| 「アニリン→アニリン塩酸塩（登録と同じコード・C₆H₈ClN）→アニリン／否定対照5つ」（`SEP4`） | 実測と一致 | 名指し |
| 「電荷つき登録 1 件（名簿どおり）」（`ION1`） | `compounds.json` の `charge` 1 件 | ○ |
| 「アニリンは登録で・他3種は折り返しで」（`ION7`） | §2 で 161 件が折り返し | 名指し 3 |
| 監査「分子を変えられる 55 本」 | `rulesExecutable` 55 | ○ |

⚠ 全走の所要 **1,006 秒**（単独）。上限 2,400 秒の **42%**。pack3 の 620 秒 → 802 件で 1,006 秒。「上限は所要の 2 倍以上」はまだ保つが、同時走行で 1.4 倍なら 1,400 秒。

### 5-2. `?only=` で落ちる帯 —— **4 → 1 に減った**【実測】

| 帯／1 件 | 結果 |
|---|---|
| `EQ`（帯）・`EQ4`（1 件） | ✅ 10/10・✅ 1/1（v1504 の直しが効いている） |
| `EX`（帯）・`EX3`・`EX4`（1 件ずつ） | ✅ 4/4・✅ 1/1・✅ 1/1 |
| `FZ`（帯）・`FZ2`（1 件） | ✅ 8/8・✅ 1/1 |
| ⚠ **`LB`（帯）・`LB1`（1 件）** | ❌ **1 件失敗（LB1: オレイン酸はシスでだけ名乗る…トランスに描いた図がオレイン酸を名乗る）** —— 全走では緑 ＝ 前のテストが残す状態（立体の読み）に依存 |
| 今回足した帯 `DH`(5)・`ION`(8)・`REF`(17)・`SEP`(8)・`CV`(4) と 1 件ずつ（`ION7`・`ION8`・`DH3`・`REF15`・`REF16`・`SEP4`・`CV4`） | ✅ すべて合格 |

★ ＝ 発注書の「3 つ（`EQ`・`EX3/EX4`・`FZ2`・`LB1`）」は **`LB1` の 1 つだけ**になっている。増えていない。

### 5-3. 回した門番

- `assembler/test.html` 全走: **✅ 802 件全合格・1,006 秒**
- `ion-equation/test.html`: ✅ ALL PASS（17 秒）／`qa/test.html`: ✅ 96 件全合格
- `node tools/verify-release.js`（**worktree の中**）: ✅ assembler v1519・ion v200・muki v30・qa v98・ratio v41・563 テキストに化け／BOM なし
- `node tools/verify-compounds.js`（同）: ✅（警告 4 件「自動水素がやや近い 11.5px」）。`compounds.json` 1,033 行 ＝ 1,031 件＋2
- 監査 ①ライブラリ 2,499 件 0 問題／②ファズ 300 反復（§4）

---

## 3. ★ (C) ion-equation v200 の 10 反応 —— 化学として 1 本ずつ

**方法**: `reactions.json` の 10 件と `model.js` の種（16 件）・電離表（4 件）を読み、node で **原子数と電荷の収支**を全式・イオン式とも検算した【実測】。
`library.html?from=qa&code=` の 4 コードを実際に開き、着地（2 件・1 件・2 件・5 件）を確かめた【実測・DOM】。

| # | id | 式 | 収支 | 化学の判定 |
|--:|---|---|---|---|
| 1 | `aniline-hcl` | C₆H₅NH₂ ＋ HCl → C₆H₅NH₃Cl（正味 C₆H₅NH₂ ＋ H⁺ → C₆H₅NH₃⁺・Cl⁻ 傍観） | ○ | ○ 弱塩基が H⁺ を受け取る。note の「アンモニアより弱い」も正しい |
| 2 | `aniline-free-naoh` | C₆H₅NH₃Cl ＋ NaOH → C₆H₅NH₂ ＋ NaCl ＋ H₂O（イオン式 C₆H₅NH₃⁺ ＋ OH⁻ → C₆H₅NH₂ ＋ H₂O） | ○ | ○ 弱塩基の遊離。Na⁺・Cl⁻ 傍観 |
| 3 | `diazonium-decomp` | C₆H₅N₂Cl ＋ H₂O → C₆H₅OH ＋ N₂ ＋ HCl（イオン式 C₆H₅N₂⁺ ＋ H₂O → C₆H₅OH ＋ N₂ ＋ H⁺） | ○ | ○ 「5℃ を超えると」「だから氷冷」が式の隣にある ＝ 発注の観点どおり。Cl⁻ が消えることも書いてある |
| 4 | `glycine-hcl` | ⁻OOCCH₂NH₃⁺ ＋ HCl → HOOCCH₂NH₃⁺ Cl⁻ | ○ | ○ **H⁺ を受け取るのは -COO⁻** と明記（-NH₃⁺ ではない）。正しい |
| 5 | `glycine-naoh` | ⁻OOCCH₂NH₃⁺ ＋ NaOH → ⁻OOCCH₂NH₂ Na⁺ ＋ H₂O | ○ | ○ **H⁺ を渡すのは -NH₃⁺**。正しい |
| 6 | `glycine-ionization1` | HOOCCH₂NH₃⁺ ⇄ ⁻OOCCH₂NH₃⁺ ＋ H⁺（K₁） | ○ | ○ 第1段は -COOH（pK₁ ≈ 2.3） |
| 7 | `glycine-ionization2` | ⁻OOCCH₂NH₃⁺ ⇄ ⁻OOCCH₂NH₂ ＋ H⁺（K₂） | ○ | ○ 第2段は -NH₃⁺（pK₂ ≈ 9.6）。等電点を K₁K₂ から、も正しい |
| 8 | `glutamate-ionization1` | α-COOH から | ○ | ○ pK₁ ≈ 2.2（α位が側鎖より先） |
| 9 | `glutamate-ionization2` | 側鎖 -COOH から | ○ | ○ pK_R ≈ 4.3 |
| 10 | `glutamate-ionization3` | -NH₃⁺ から | ○ | ○ pK₂ ≈ 9.7。「双性イオンは第1段と第2段のあいだ・等電点はそちらに寄る」も正しい（pI ≈ 3.2） |

★ **化学として直すべき式は 0 本。** グリシンの二段・グルタミン酸の三段の**順も正しい**（COOH → 側鎖 COOH → NH₃⁺）。

細かい点（直さなくてよい）:
- #10 の note「リシンも三段だが、増えるのが側鎖のアミノ基なので順が入れ替わる」は言い足りない（リシンは COOH → α-NH₃⁺ → ε-NH₃⁺。「入れ替わる」のは 2 段目が -COOH でなく -NH₃⁺ になること）。誤りではない
- ⚠ `library.html` の **「反応の型」の絞り込みに `その他`（6・電離平衡 5 本を含む）・`加水分解`（2）・`弱酸弱塩基の遊離`（2・#2 を含む）が並ばない**【実測・DOM: 型は 中和15・沈殿7・気体発生4・酸化還元15・錯イオン生成8・分子反応7 ＝ 56/66】。
  新しい 10 本のうち 6 本は「型」からは辿れず、「単元」（電離平衡 9・アミノ酸 7・両性 7・弱塩基 5）から辿る。v200 より前からの作り（`その他` は以前からある）
- `diazonium-decomp` の `species` に `N`（窒素原子）が入っている。原子の内訳ビュー用と思われ、表示は確認していない
- qa → ion の着地【実測】: `org.aroN.aniline-base` 2 件・`org.aroN.diazonium-decomp` 1 件・`org.bio.amino-acid-amphoteric` 2 件・`org.bio.amino-acid-polyprotic` 5 件。
  帯「一問一答の◯◯から来ました。この項目に当たる反応式を N 件だけ出しています／← 一問一答へ戻る」 ＝ 往復の規約どおり

---

## 6. 見つけた問題（重い順）と「直すべきか・急ぐか」

| # | 問題 | 出典 | 直すべきか | 急ぐか |
|--:|---|---|---|---|
| **1** | ⚠⚠ **双性イオン形に塩酸／NaOH の瓶を押すと「いまの分子にはどれもありません」「塩もアミンもありません」** ＝ 化学の嘘。⇄ ボタンが双性形へ誘い、同じリリースの ion-equation が `glycine-hcl`/`glycine-naoh` を教えている | §2-3・`reactor.js` の `miss` 文・`basicAmineNitrogens` が N⁺ を拾わない（設計どおり）・`neutralizableAcidSites` が -COO⁻ を拾わない | 両性のルール 2 本（§14-7 の見積り）を入れるか、**それまでは `miss` に「双性イオン形にはまだ対応していません」の 1 文** | ★ 次の動画・課題がアミノ酸を踏む前 |
| **2** | ⚠⚠ **ピロール・インドール・インジゴに塩酸で「◯◯塩酸塩」**（芳香族の N・ビニロガスアミドは塩基でない）。同じ札に「付加: HCl」も並ぶ | §2-1・`chemistry.js:1592-1611`・`reactor.js:2196` | `basicAmineNitrogens` に「芳香族 5 員環の N を除く」1 条件＋否定対照にピロール | 急がない（高校範囲外の登録分子） |
| **3** | ⚠ **ピリジン・キノリンに「アミンがありません」**（塩基だが型が立たない） | §2-2・`chemistry.js:1613` | 「起こらない」でなく「対応していない」と言うか、`pyridine` 型を立てる | 急がない |
| **4** | ⚠ **`buildExportJson` が `charge` を落とす**（作図エクスポート）。次に「◯◯塩酸塩」を登録するレーンが踏む | §1-5・`game.js:4211-4214` | 1 行（`charge: a.charge`） | ★ 次に電荷つきを登録する前 |
| **5** | ⚠ ジアミン 15 件の **2 回目の塩酸で名前が消える**（「🔍 C₆H₁₈Cl₂N₂」）。「（未登録）を出さない」が二塩酸塩に届いていない | §2-4・`game.js:8991-8993`（電荷 2 個の門番） | 電荷 4 個（N⁺×2・Cl⁻×2）で「◯◯二塩酸塩」を組む | 急がない |
| **6** | ⚠ 監査が **⇄ 双性イオン形を 0 回**・**脱水の書き出しを 0 回**（8 面中 4 面に届く） | §4 | `OP_MIX` に「モーダルの ⇄ を押す」の枝（#1 の面がここにある） | #1 を直すレーンと同時 |
| **7** | ⚠ 「名前（別名）塩酸塩」の 15 件（例: グリシルグリシン（ジペプチド）塩酸塩） | §2-4 | 「◯◯塩酸塩」を別名の前に入れるか別名を落とす | 急がない |
| **8** | ⚠ `?only=LB1` だけが単独で落ちる（4 → 1 に減った） | §5-2 | LB1 の立体の読みを自前で立てる | 急がない |
| **9** | ○ 相方の居ない Cl⁻ の見出し「Cl」・分子式「Cl」（D-I6 の穴の 3 つ目の出番） | §1-6 | D-I6 の決め（正味の電荷が 0 でないとき右肩に符号） | D-I6 と一緒 |
| **10** | ○ `chargedValency` が符号と元素の組を検査しない（Cl⁺→0・N²⁺→5）／`ION1` は件数だけで符号を見ない | §1-4 | 登録データにしか入らないので急がない。`ION1` に「符号が MONATOMIC の元素と合う」を 1 行 | 急がない |
| **11** | ○ 第四級アンモニウム塩に NaOH が効く門番（到達経路なし） | §2-6 | `ammoniumSaltNitrogens` に `getFreeValency(N) >= 1` | 第四級を登録する前 |

---

## 7. 疑わしいが確証が無いもの（分けて書く）

- ⚠ **2,4,6-トリブロモアニリン・o/m/p-ニトロアニリンの「◯◯塩酸塩」**: 共役酸の pKa が −0.3〜1.0 で、希塩酸には溶けない（濃塩酸なら塩になる）。
  「起こってはいけない」とまでは言えないので §6 に入れていない。教科書の線は「アニリンは弱塩基」までで、置換基による強弱は扱わない
- ⚠ **スルファニル酸塩酸塩**: 実物は双性イオン（-SO₃⁻/-NH₃⁺）で塩酸に溶けない。アプリは -SO₃H を中性で持っているので「-SO₃H のまま N⁺Cl⁻」の図になる。
  ⚠ 双性イオン形の ⇄ はアミノ酸（-COOH）だけに出るので、スルファニル酸は双性形にできない（`zwitterionPlan` は `carboxyl` を要求）
- ⚠ **実装記録の「塩基性アミン 177 件」と実測の 162 件**の差 15。`getCompoundLibrary()` は別名の重複を畳む可能性があり、数え方の違いと思われる。追っていない
- ⚠ **失敗率 11.0%（mix=5）**は pack3 の 16.3%（mix=3）と `comparableKey` が違うので並べられない。v1515 の「課題と資料の割合を 1/3 に下げた」実行と同じ mix=5 での過去値は `audit.html` に無く、退行の有無は言えない
- ⚠ **ニトロの電荷分離形 vs 中性形が別コード**になる（§1-3）。電荷を手で描く経路が無いので今は通るものが無いが、機構データ（`reactions.json` の `charge` 52 か所）を
  いつか「主生成物の判定」に電荷つきで通す日が来れば当たる（いまは `buildMainProductTarget` が電荷を落とすので当たらない）
- ⚠ **`attachCounterIons` の同点**（2 つの相方が同じ距離）は図を作れず未実測。`sort` の安定性（Chromium は安定）で最初に見つけた成分に付く、が現物の読み

---

## 8. 確かめて問題なかったもの（見ていない所と区別がつくように）

**回したもの【実測】**
- `assembler/test.html` 全走: ✅ 802 件全合格・1,006 秒（単独）／`ion-equation/test.html`: ✅／`qa/test.html`: ✅ 96 件
- `verify-release.js`・`verify-compounds.js`（worktree の中）: ✅
- 広い写し 20 列 × 1,151 件の旧新 diff: 既存 1,150 件で 0 行（§1-1）／同じ分子式 6,245 組で `verifyMolecule` ≡ `canonicalCode`・旧新差 0（§1-2）
- 電荷つき 15 種 × 30 通りの原子順で正準コードが 1 つ（§1-3）
- `amine_hcl` 162 件: 例外 0・印の id 162/162・名前 162/162・NaOH で全件が元のコードへ／否定対照 179 件で detect 0（§2）
- 双性イオン形・ジアゾニウム塩に `amine_hcl`/`amine_liberate_naoh` が効かない／粒に結合・モジュールを置けない／混合物で粒が正しい相方に付く（§1-6・§2）
- クイズの土俵にアニリン塩酸塩が入り、変形した図が電荷を保つ（`transformCompoundDepiction`）【実測】
- ion-equation 10 本の原子・電荷の収支（全式・イオン式）と qa からの着地 4 コード（§3）
- 監査 ①ライブラリ 2,499 件 0 問題（電荷つき 1 件を含む）／②300 反復で瓶 25/25・資料 5/5（§4）
- `?only=` 21 通り（帯 9・1 件 12）: 落ちるのは `LB1` だけ（§5-2）
- `CV4_NO_CHANGE_RULES` 6 本 ＝ `_info` 6 本ちょうど（§2-5）

**読んで一致していたもの【現物】**
- `DESIGN_ion_layer.md` §13-2〜13-4・§14-1〜14-4 と `chemistry.js`（`chargedValency`・`heavyAtomLabel`・`verifyMolecule` の 1 行）・`game.js`（`attachCounterIons`）・`reactor.js`（`placeCounterIon`・`ammoniumSaltNitrogens`）
- §13-7・§14-8 の在庫（ルール 61・瓶 25・登録 1,151・電荷つき 1・`CV4_NO_CHANGE_RULES` 6）と現物

**見ていないもの（正直に）**
- iPad 実機・WebKit（`--engine=webkit`）
- 監査の③画面サイズ・④当たり判定（OFF）・夜間規模（5,000 反復）
- `ion-equation/audit.html`・`muki`・`ratio` の回帰
- 脱水の書き出し 14 問の**中身**（`DH1〜5` が緑であること以上は見ていない。§22 の設計と `learn.js:799-812` の 14 問の id は読んだ）
- 参考書 5 枚目の**文章**の化学（表の 16 行と「脱水できない 2 件」は `REF15/16` と監査の 39 回で見た）

---

## 9. 次に出すべき発注書の形（★ どのレーンに何を渡せば効くか）

### 発注書 A: 双性イオン形の両性（`reactor.js`・★ 急ぐ・1 レーン・qa の棚卸しを拾えるレーン）

1. §14-7 の見積りどおり **ルール 2 本**（双性イオン ＋ NaOH → -COO⁻Na⁺ を線で／双性イオン ＋ 塩酸 → -COOH ＋ N⁺Cl⁻）。
   ⚠ **先に `miss` の文を直す**（「双性イオン形にはまだ対応していません」）＝ ルールが入るまでの間も嘘を言わない。
2. `KNOWN_RULES = 61` → 63（`qa/tests.js`）・`CV_PAIR_SAMPLES`／`PAIR_SAMPLES`（相手は要らないので不要）・`RG5`（瓶なし）・`ION` 帯に否定対照（双性形に塩酸で -COO⁻ が -COOH に戻り N⁺ は変わらない）。
3. ★ 同時に `audit.js` の `OP_MIX` に **「分子モーダルの ⇄ を押す」の枝**（§4 #3）。`OP_MIX_ID` 5 → 6。
4. 見積り: ルール 2・関数 1（双性イオンの箇所）・名前の折り返し 1（-COONa 側。「◯◯ナトリウム塩」）・テスト 3〜4・監査の枝 1。

### 発注書 B: 塩基でない N を外す＋名前の 2 件（`chemistry.js`／`reactor.js`／`game.js`・急がない・1 レーン）

1. `basicAmineNitrogens`: 芳香族 5 員環の N（ピロール・インドール・インジゴ）を除く。否定対照にピロール・インドール。
   ⚠ ついでに「付加: HCl」がピロールに並ぶ件は**別件**（`findAromaticBondKeys` の 5 員環）。触らない。
2. ピリジン型 N の `miss` の文（「アミンがありません」→「環の中の N にはまだ対応していません」）。
3. `ammoniumSaltName`: 電荷 4 個（N⁺×2・Cl⁻×2）で「◯◯二塩酸塩」／「名前（別名）塩酸塩」の並びを「名前塩酸塩（別名塩酸塩）」か別名なしに。
4. `buildExportJson` に `charge: a.charge`（1 行）。⚠ **次に電荷つきを登録するレーンより前**。

### 発注書 C: 門番の 1 行ずつ（`tests.js`・急がない・どのレーンのついででも）

1. `ION1`: 名簿の件数に加えて**符号と元素**（`MONATOMIC_ION_ELEMENTS` の Na/K は +・Cl/Br/I は −）。
2. `ammoniumSaltNitrogens` に `getFreeValency(N) >= 1`（第四級を通さない）＋否定対照。
3. `LB1` の状態依存を断つ（`?only=LB1` で緑に）。
4. D-I6（正味の電荷が 0 でないとき分子式に符号）の決めが下りたら、`computeMolecularFormula` と `dump-canonical.js` の写しの 2 か所。

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-06〜07 | 新規。5 軸（(A) 広い写し 20 列・同型判定 6,245 組・電荷 15 種の原子順・価標の経路・写しの経路・粒の付け方／(B) `amine_hcl` 162 件＋否定対照 179 件・瓶の DOM／(C) ion-equation 10 本の収支と着地／(D) 監査 300 反復＋面ごとの呼び出し回数／(E) 注記・`?only=` 21 通り・`CV4_NO_CHANGE_RULES`）＋問題 11 件＋発注書 3 本。v1519（`f9b7841`）時点。ブランチ `review4`・触ったのはこのファイルだけ |
