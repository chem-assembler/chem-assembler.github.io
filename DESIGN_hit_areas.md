# 作図の当たり判定（設計判断・2026-08-12・Fable 5）

**状態: 決定。実装はこれから**（Opus レーンに出せる粒度で書いてある）。
元資料: `video-scripts/ORDER_drawing_hit_areas.md`（実測）／ `HANDOFF_hit_areas.md`（引き継ぎ）。
要望 B〜F を **1つの数直線と1つの選択則** に還元して決めた。要望G（収録レイアウトの42pxずれ）は
入力座標系のバグでこの設計の外（§7）。

---

## 0. 決定の要旨

### 決定1: タップの意味は「最寄り原子までの距離 d」の3帯で決める

```
d:  0 ──────── 18 ─────────────────── 63 ──────────────→
     その原子への操作      隣に足す（吸着）        自由配置
     （削除・元素置換）                        （新しい分子を始める）
```

- `ATOM_TAP_RADIUS = 18`（新設）… **破壊的な操作（削除・置換）は原子の上（グリフ半径10px＋余白8px）だけ**
- `SNAP_RADIUS = GRID_SIZE * 1.5 = 63`（45 から変更）… 置きたい点（42px 先）が帯の**中**に入る（±21px の余裕）
- **63px を超えたら isValid:false('far') にせず、グリッドに丸めて置く**（自由配置）

### 決定2: 吸着先は「**新しい原子が出る位置**が指に最も近い原子」が勝つ

現状の「候補点込みの最短距離で持ち主を選ぶ」をやめ、
**各原子について実際の配置結果（候補角→占有除外→クリアランス延長まで通した最終位置）を計算し、
その最終位置がタップ点に最も近い原子を勝たせる**。差が 8px 以内の近差なら、指の真下
（原子への素の距離が近い方）を優先する。

これは既存原則「**プレビュー＝実結果**」（getPlacementBondTargets・getRingPlacementPlan のコメント）を
スナップの勝敗にまで通すこと。指のそばに現れる結果が勝つので、要望E（横取り）と要望C（28px の的）が
同時に解ける。

### 要望との対応

| 要望 | 決定 |
|---|---|
| B 吸着半径 | `SNAP_RADIUS = 63`（= GRID_SIZE×1.5。定数として意味が読める値） |
| C ベンゼン置換基 27.97px | **揃えない（維持）**。compounds.json は触らない。狙いのずれは決定1・2が吸収する |
| D 自由配置 | **許す**。単原子は 'far' を廃止しグリッド配置。環モジュールは孤立禁止則（75px 以内強制）を削除 |
| E 横取り | 決定2（成果の近さで選ぶ＋近差は指の真下優先） |
| F 緩衝帯 | 削除・置換を d≤18 に限定。**中立帯（何も起きない帯）は作らない**（§1-F に理由） |

---

## 1. 各決定の理由

### B: SNAP_RADIUS = 63

置きたい点は結合長 42px 先にある。45 では的の**外周 3px** を狙う操作だった（実測: 46px で×）。
63 なら 42px の点が帯の中央付近に来て、両側に約 21px（指の半径相当）の余裕ができる。
遠すぎる側の誤爆は、決定2 が「指の近くに結果が出る原子」しか選ばないので増えない。
60〜70 のどれでも動くが、`GRID_SIZE * 1.5` は定数の由来がコードから読める。

### C: 27.97px は維持（揃えない）

- **登録図との同一性が優先**。`compounds.json` の 921 件はベンゼン置換基 27.97px で描かれており、
  手描きだけ 42px にすると「名前で呼び出した図」と「手で描いた図」の形が分かれる。
  フラノースのとき「1px も違わない」ことをわざわざ確かめた前例（DEVELOPMENT.md ハース環の項）に反する。
  揃える側に倒すと登録図の描き直し（大量差分・SNS動画の既収録分との食い違い）まで背負う。割に合わない。
- **「1本目だけ 28px を覚えていないと外す」問題は、揃えなくても消える**。
  発注書 2b の失敗は「置きたい点を正確に押す」前提の失敗。決定1・2 の下では、
  頂点から 42px 外を押しても（置換基点まで 14px）ベンゼン頂点が勝って 27.97px の定位置に置かれる。
  **押す場所の精度が要らなくなるので、距離を覚える必要もなくなる。**
- ゴースト: デスクトップは既存のホバープレビュー（drawAtomPreview）がそのままゴースト。
  ただし現状は findAtomAt(28) がプレビューを消す帯（18〜28px）があるので、
  プレビューの分岐半径も `ATOM_TAP_RADIUS` に揃える（§3-5）。タッチにホバーは無いが、
  帯が広がる＋v1111 の理由表示があるので追加の仕掛けは作らない。

### D: 自由配置を許す

- 単原子: getSnappedCoords の手順3（近傍原子なし→isValid:false 'far'）を、
  **グリッドに丸めて isValid:true（snapAtom:null）** に変える。条件は
  (a) キャンバス上限内、(b) 丸めた点が全重原子から `MIN_CLEARANCE`(27.3px) 以上、だけ。
  (b) を割ったら v1111 の語彙で「近すぎます」を出す（'far' の字幕はほぼ出番がなくなる）。
  結合は従来どおり getPlacementBondTargets が見る（44px 以内に直交整列した原子があれば自動接続
  ＝格子の合流の既存挙動は変えない）。
- 環モジュール: getRingPlacementPlan の**孤立禁止則（`isNearAnyExistingAtom` ≤75px 強制）を削除**。
  残すのは頂点マージ（12px）・MIN_CLEARANCE 衝突・価標検査。これで「70〜100px の帯」が
  「クリアランスさえ満たせばどこでも」になる。ハース環（placeHaworthRing）に同種の孤立則が
  あれば同じく削除（実装時に確認）。
- **設計思想**: 発注書 §1 の裏返しで「**タップは黙って捨てない。何かが見えて起きるか、理由が出るか**」。
  63px 超の狙い外しは今後エラーでなく孤立原子になるが、それは**見えて**、タップ1回
  （同元素タップ＝削除）か Ctrl+Z で戻せる。無言で捨てるより一貫している。

### E: 「成果の近さ」で選ぶ — 既知の2場面の両立を確認済み

単純な「素の距離で最寄り原子が勝つ」は **P12-8 の退行を起こす**ので採らない。数字で確認:

- **P12-8（メチルシクロヘキサンの2本目）**: 2本目の出現位置は既存メチル炭素から
  2·42·sin15° ≈ **21.7px** しか離れていない。素の距離ならメチルが勝って退行する。
  成果の近さなら、環炭素の成果（±30° 振り分け点）はタップ点のほぼ真下（〜0px）、
  メチルの成果は 25px 以上 → **環炭素が勝つ**。正しい。
  さらに現状はこの点のタップ自体が findAtomAt(28) に食われて**メチルの削除**になるが、
  21.7px > ATOM_TAP_RADIUS(18) なので決定1 がこれも直す。
- **発注書 2d（環→鎖→先端に枝）**: 先端 T の真下 38px のタップは、T の候補点（真下42px）まで 4px、
  環炭素の振り分け点まで約 18px → **T が勝つ**。↑・→・↗ も同様に T。
  ↘ だけは環側の成果が指に近く環が勝つが、その場合も**新しい原子は指のほぼ真下に現れる**ので
  「押した所と違う所に出た」体験にはならない。**5方位中 4方位が先端に付く**（現状 2方位）。
- **近差の tie-break（8px 以内は指の真下優先）が要る理由**: T から 20px 真下のような近距離では、
  環側の成果（クリアランス延長後）と T の成果が 1〜2px 差まで接近する。ここで環に取られると
  発注書 2d の再来なので、近差は素の距離が近い方（＝指の真下の原子）に倒す。ε=8px は実測で調整可。
- **スコアは必ず「クリアランス延長後の最終位置」で取る**こと。延長前の候補点（42px）で取ると
  上の近距離の場面で環側が 5px まで寄ってしまい、tie-break に入らず誤る。

### F: 中立帯は作らず、破壊操作を「原子の上」に限定する

発注書は「足す帯と消える帯のあいだに何も起きない緩衝帯」を選択肢に挙げたが、**採らない**。
何も起きない帯は要望A で潰した「無言のタップ」の再導入だから。代わりに:

- 削除（同元素タップ）と元素置換は **d ≤ 18px（グリフ10px＋8px）に限定**。
  18〜28px の旧削除帯は「足す」に変わる。事故だった「20〜26px を押して先端が消えた」（4回実発生）は
  グリフを直接押さない限り起きなくなる。
- 逆向きの事故（消すつもりで 18px を外して原子が増える）は、増えた原子がその場に見えて、
  もう一度タップ（同元素＝削除）で消せる。非対称だが、**見えて戻せる方の誤りに倒す**。
- **非破壊の操作は 28px のまま**: ドラッグの掴み（ロック原子・ベンゼン・Shift+分子移動）、
  結合ツールの始点、立体/反応のピック、消しゴムツール（ツール選択自体が意図表明）。
  ZD1〜2 が見ているドラッグ挙動には触れない。
- 破壊操作の判定は「配列順で最初に見つかった原子」でなく**半径内の最寄り原子**にする
  （findAtomAt は `.find` で配列順依存。原子IDに順序を頼らない原則と同根の直し。
  差し当たり破壊経路だけで良い）。

---

## 2. 変更しないもの（明示）

- `assembler/compounds.json`（1バイトも触らない）
- ベンゼン置換基の 0.666 倍距離（game.js 手順4 と、それを写した reactor 側 v510 の式）
- v1111 の「理由を伝える」層の構造（トースト・`#miss-group`・throttle）。
  文言の出し所が変わるだけ（'far' がほぼ退場、「近すぎ」に自由配置のクリアランス違反が加わる）
- 消しゴムツール・ドラッグ系の判定半径（28px）
- MIN_CLEARANCE(27.3)・MAX_EXTEND(84)・延長ステップ・候補角の決め方（手順6）そのもの

---

## 3. 実装指針（game.js）

1. **定数**: モジュール先頭に `ATOM_TAP_RADIUS = 18` を新設。`SNAP_RADIUS` は
   getSnappedCoords ローカルのまま `GRID_SIZE * 1.5` に。
2. **成果計算の関数化**: getSnappedCoords の手順4〜9（ベンゼン特例・候補角・占有除外・
   クリアランス延長）を「原子1つ → 最終配置 {x,y,adjust,reason…}」の純関数に括り出す。
   勝敗のスコアも最終配置もこの1関数から取る（= secondBranchPoints と手順6の
   「条件をそろえてある」二重管理を解消できる。secondBranchPoints は tests から参照が
   あるため互換で残してよい）。
3. **手順2の置き換え**: 空き価標のある重原子のうち direct ≤ SNAP_RADIUS + MAX_EXTEND の
   ものだけ成果を計算し、min(direct, 成果までの距離) ≤ SNAP_RADIUS のものを競わせる。
   勝者 = 成果がタップ点に最も近い原子。8px 以内の近差は direct の近い方。
   touchedAtom / stolen の帳簿（v1111）はそのまま生かす。
4. **手順3の置き換え**: 競合ゼロ → グリッド丸め自由配置（§1-D の条件）。
5. **handleMouseDown**: `clickedAtom`(28px) は残しつつ、**削除・置換の分岐だけ**
   「素の距離 ≤ ATOM_TAP_RADIUS」を追加条件にする。外れたら配置経路へ落とす。
   mousemove のプレビュー分岐（`!clickedAtom && coords.isValid`）も同じ条件に揃える
   （プレビューが出る場所 = タップで置ける場所、を崩さない）。
6. **getRingPlacementPlan**: 孤立禁止則（reason:'isolated' の節）を削除。

---

## 4. 既存テストへの影響（先に分かっている赤）

- **PW1(a)・PW3**: 200px 先のクリックで 'far' を誘発している。要望D で
  「原子が増えない」の assert が落ちる。**PW1(a) は『自由配置で原子が増え、字幕は出ない』の
  検査に書き換え**、4通りの言い分けは (far の代わりに) 自由配置クリアランス違反の「近すぎ」で構成し直す。
  PW3 の連打誘発は 'overlap'（近すぎ）の場面に差し替える。
- **PW2**: 「取られた」場面の作り方が幾何依存。決定2 の下でも stolen は起きる（↘ 方向など）が、
  findMissSpot の探索が現行の場面で見つかるかは実機で確認・必要なら場面を作り直す。
- **P12-8 系**（メチルシクロヘキサン2本目の吸着テストがあれば）: 通ることを§1-E の数字どおり確認。
- ZD1〜2・FR1・MM9 は設計上ノータッチ（落ちたら実装ミス）。

---

## 5. 受け入れ基準（発注書 §2 と同じ測り方で前後表を出す）

| 実測 | 現状 | 合格ライン |
|---|---|---|
| 2a 鎖の延長が成功する距離帯 | 32〜42px（実質10px） | **18〜63px（45px 幅）** |
| 2d 先端に枝が付く方位 | 2/5 方位 | **≥ 4/5 方位** |
| 2d 「足すつもりで削除」帯 | 20〜26px で発生 | **0（18px 超で削除は起きない）** |
| 2c 別分子の環を置ける帯 | 70〜100px | **クリアランス以遠ぜんぶ（70〜200px 全部○）** |
| 2b ベンゼン1本目 | 28px を正確に狙う必要 | 頂点から 20〜63px のどこを押しても定位置に付く |

**否定対照**（直しを外すと赤くなるテストを最低4本）:
SNAP_RADIUS を 45 に戻す → 2a テスト赤／勝敗則を旧式（候補点込み最短）に戻す → 2d テスト赤
（かつ P12-8 テストは緑のまま＝両立の証明）／ATOM_TAP_RADIUS を 28 に戻す → 削除帯テスト赤／
孤立禁止則を復活 → 自由配置テスト赤。

**夜間監査**: audit.js ④ に「環 → 鎖 → その先端に枝」を1件追加。
判定は「足せる方位が5方位中いくつか（≥4）」「成功する距離帯の幅（≥40px）」。

---

## 6. 作法（HANDOFF §7 の再掲・レーン向け）

worktree で作業／1修正=1コミット・パス指定コミット／版番号は index.html・test.html・audit.html の
全 `?v=` と version 表示を同時更新／回帰 343件＋check-mobile 25端末＋verify-release で締める。

---

## 7. 場外（この設計では扱わない）

- **要望G（`?rec=` で自由配置が 42px ずれる）**: 本設計の自由配置は SVG 論理座標の
  絶対グリッドに丸めるだけで viewBox に依存しない。ずれの原因は入力側
  （client座標→論理座標の生成経路）にあるはずで、別件として調査する。
  当面はレーン回避策（`?rec=` 経路でも下見）を継続。
- 自由配置の初回に案内トーストを出すか（「新しい分子として置きました」）は実装レーンの裁量。
  出すなら v1111 の throttle に乗せること。
