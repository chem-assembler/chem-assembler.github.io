# 作図の当たり判定（設計判断・2026-08-12・Fable 5）

**状態: 決定。実装はこれから**（Opus レーンに出せる粒度で書いてある）。
元資料: `video-scripts/ORDER_drawing_hit_areas.md`（実測）／ `HANDOFF_hit_areas.md`（引き継ぎ）。
要望 B〜F を **1つの数直線と1つの選択則** に還元して決めた。要望G（収録レイアウトの42pxずれ）は
入力座標系のバグでこの設計の外（§7）。

---

## 0. 決定の要旨

### 決定1: タップの意味は「最寄り原子までの距離 d」の3帯で決める

```
d:  0 ──────── 18 ─────────────────── 63 ──────────────→
     その原子への操作      隣に足す（吸着）        自由配置
     （削除・元素置換）                        （新しい分子を始める）
```

- `ATOM_TAP_RADIUS = 18`（新設）… **破壊的な操作（削除・置換）は原子の上（グリフ半径10px＋余白8px）だけ**
- `SNAP_RADIUS = GRID_SIZE * 1.5 = 63`（45 から変更）… 置きたい点（42px 先）が帯の**中**に入る（±21px の余裕）
- **63px を超えたら isValid:false('far') にせず、グリッドに丸めて置く**（自由配置）

### 決定2: 吸着先は「**新しい原子が出る位置**が指に最も近い原子」が勝つ

現状の「候補点込みの最短距離で持ち主を選ぶ」をやめ、
**各原子について実際の配置結果（候補角→占有除外→クリアランス延長まで通した最終位置）を計算し、
その最終位置がタップ点に最も近い原子を勝たせる**。差が 8px 以内の近差なら、指の真下
（原子への素の距離が近い方）を優先する。

これは既存原則「**プレビュー＝実結果**」（getPlacementBondTargets・getRingPlacementPlan のコメント）を
スナップの勝敗にまで通すこと。指のそばに現れる結果が勝つので、要望E（横取り）と要望C（28px の的）が
同時に解ける。

### 要望との対応

| 要望 | 決定 |
|---|---|
| B 吸着半径 | `SNAP_RADIUS = 63`（= GRID_SIZE×1.5。定数として意味が読める値） |
| C ベンゼン置換基 27.97px | **揃えない（維持）**。compounds.json は触らない。狙いのずれは決定1・2が吸収する |
| D 自由配置 | **許す**。単原子は 'far' を廃止しグリッド配置。環モジュールは孤立禁止則（75px 以内強制）を削除 |
| E 横取り | 決定2（成果の近さで選ぶ＋近差は指の真下優先） |
| F 緩衝帯 | 削除・置換を d≤18 に限定。**中立帯（何も起きない帯）は作らない**（§1-F に理由） |

---

## 1. 各決定の理由

### B: SNAP_RADIUS = 63

置きたい点は結合長 42px 先にある。45 では的の**外周 3px** を狙う操作だった（実測: 46px で×）。
63 なら 42px の点が帯の中央付近に来て、両側に約 21px（指の半径相当）の余裕ができる。
遠すぎる側の誤爆は、決定2 が「指の近くに結果が出る原子」しか選ばないので増えない。
60〜70 のどれでも動くが、`GRID_SIZE * 1.5` は定数の由来がコードから読める。

### C: 27.97px は維持（揃えない）

- **登録図との同一性が優先**。`compounds.json` の 921 件はベンゼン置換基 27.97px で描かれており、
  手描きだけ 42px にすると「名前で呼び出した図」と「手で描いた図」の形が分かれる。
  フラノースのとき「1px も違わない」ことをわざわざ確かめた前例（DEVELOPMENT.md ハース環の項）に反する。
  揃える側に倒すと登録図の描き直し（大量差分・SNS動画の既収録分との食い違い）まで背負う。割に合わない。
- **「1本目だけ 28px を覚えていないと外す」問題は、揃えなくても消える**。
  発注書 2b の失敗は「置きたい点を正確に押す」前提の失敗。決定1・2 の下では、
  頂点から 42px 外を押しても（置換基点まで 14px）ベンゼン頂点が勝って 27.97px の定位置に置かれる。
  **押す場所の精度が要らなくなるので、距離を覚える必要もなくなる。**
- ゴースト: デスクトップは既存のホバープレビュー（drawAtomPreview）がそのままゴースト。
  ただし現状は findAtomAt(28) がプレビューを消す帯（18〜28px）があるので、
  プレビューの分岐半径も `ATOM_TAP_RADIUS` に揃える（§3-5）。タッチにホバーは無いが、
  帯が広がる＋v1111 の理由表示があるので追加の仕掛けは作らない。

### D: 自由配置を許す

- 単原子: getSnappedCoords の手順3（近傍原子なし→isValid:false 'far'）を、
  **グリッドに丸めて isValid:true（snapAtom:null）** に変える。条件は
  (a) キャンバス上限内、(b) 丸めた点が全重原子から `MIN_CLEARANCE`(27.3px) 以上、だけ。
  (b) を割ったら v1111 の語彙で「近すぎます」を出す（'far' の字幕はほぼ出番がなくなる）。
  結合は従来どおり getPlacementBondTargets が見る（44px 以内に直交整列した原子があれば自動接続
  ＝格子の合流の既存挙動は変えない）。
- 環モジュール: getRingPlacementPlan の**孤立禁止則（`isNearAnyExistingAtom` ≤75px 強制）を削除**。
  残すのは頂点マージ（12px）・MIN_CLEARANCE 衝突・価標検査。これで「70〜100px の帯」が
  「クリアランスさえ満たせばどこでも」になる。ハース環（placeHaworthRing）に同種の孤立則が
  あれば同じく削除（実装時に確認）。
- **設計思想**: 発注書 §1 の裏返しで「**タップは黙って捨てない。何かが見えて起きるか、理由が出るか**」。
  63px 超の狙い外しは今後エラーでなく孤立原子になるが、それは**見えて**、タップ1回
  （同元素タップ＝削除）か Ctrl+Z で戻せる。無言で捨てるより一貫している。

### E: 「成果の近さ」で選ぶ — 既知の2場面の両立を確認済み

単純な「素の距離で最寄り原子が勝つ」は **P12-8 の退行を起こす**ので採らない。数字で確認:

- **P12-8（メチルシクロヘキサンの2本目）**: 2本目の出現位置は既存メチル炭素から
  2·42·sin15° ≈ **21.7px** しか離れていない。素の距離ならメチルが勝って退行する。
  成果の近さなら、環炭素の成果（±30° 振り分け点）はタップ点のほぼ真下（〜0px）、
  メチルの成果は 25px 以上 → **環炭素が勝つ**。正しい。
  さらに現状はこの点のタップ自体が findAtomAt(28) に食われて**メチルの削除**になるが、
  21.7px > ATOM_TAP_RADIUS(18) なので決定1 がこれも直す。
- **発注書 2d（環→鎖→先端に枝）**: 先端 T の真下 38px のタップは、T の候補点（真下42px）まで 4px、
  環炭素の振り分け点まで約 18px → **T が勝つ**。↑・→・↗ も同様に T。
  ↘ だけは環側の成果が指に近く環が勝つが、その場合も**新しい原子は指のほぼ真下に現れる**ので
  「押した所と違う所に出た」体験にはならない。**5方位中 4方位が先端に付く**（現状 2方位）。
- **近差の tie-break（8px 以内は指の真下優先）が要る理由**: T から 20px 真下のような近距離では、
  環側の成果（クリアランス延長後）と T の成果が 1〜2px 差まで接近する。ここで環に取られると
  発注書 2d の再来なので、近差は素の距離が近い方（＝指の真下の原子）に倒す。ε=8px は実測で調整可。
- **スコアは必ず「クリアランス延長後の最終位置」で取る**こと。延長前の候補点（42px）で取ると
  上の近距離の場面で環側が 5px まで寄ってしまい、tie-break に入らず誤る。

### F: 中立帯は作らず、破壊操作を「原子の上」に限定する

発注書は「足す帯と消える帯のあいだに何も起きない緩衝帯」を選択肢に挙げたが、**採らない**。
何も起きない帯は要望A で潰した「無言のタップ」の再導入だから。代わりに:

- 削除（同元素タップ）と元素置換は **d ≤ 18px（グリフ10px＋8px）に限定**。
  18〜28px の旧削除帯は「足す」に変わる。事故だった「20〜26px を押して先端が消えた」（4回実発生）は
  グリフを直接押さない限り起きなくなる。
- 逆向きの事故（消すつもりで 18px を外して原子が増える）は、増えた原子がその場に見えて、
  もう一度タップ（同元素＝削除）で消せる。非対称だが、**見えて戻せる方の誤りに倒す**。
- **非破壊の操作は 28px のまま**: ドラッグの掴み（Shift＋分子移動）、
  結合ツールの始点、立体/反応のピック、消しゴムツール（ツール選択自体が意図表明）。
  （**v1180 で更新**: 「ロック原子・ベンゼン」の 1原子ドラッグは廃止した。§8-2）
- 破壊操作の判定は「配列順で最初に見つかった原子」でなく**半径内の最寄り原子**にする
  （findAtomAt は `.find` で配列順依存。原子IDに順序を頼らない原則と同根の直し。
  差し当たり破壊経路だけで良い）。

---

## 2. 変更しないもの（明示）

- `assembler/compounds.json`（1バイトも触らない）
- ベンゼン置換基の 0.666 倍距離（game.js 手順4 と、それを写した reactor 側 v510 の式）
- v1111 の「理由を伝える」層の構造（トースト・`#miss-group`・throttle）。
  文言の出し所が変わるだけ（'far' がほぼ退場、「近すぎ」に自由配置のクリアランス違反が加わる）
- 消しゴムツール・ドラッグ系の判定半径（28px）
- MIN_CLEARANCE(27.3)・MAX_EXTEND(84)・延長ステップ・候補角の決め方（手順6）そのもの

---

## 3. 実装指針（game.js）

1. **定数**: モジュール先頭に `ATOM_TAP_RADIUS = 18` を新設。`SNAP_RADIUS` は
   getSnappedCoords ローカルのまま `GRID_SIZE * 1.5` に。
2. **成果計算の関数化**: getSnappedCoords の手順4〜9（ベンゼン特例・候補角・占有除外・
   クリアランス延長）を「原子1つ → 最終配置 {x,y,adjust,reason…}」の純関数に括り出す。
   勝敗のスコアも最終配置もこの1関数から取る（= secondBranchPoints と手順6の
   「条件をそろえてある」二重管理を解消できる。secondBranchPoints は tests から参照が
   あるため互換で残してよい）。
3. **手順2の置き換え**: 空き価標のある重原子のうち direct ≤ SNAP_RADIUS + MAX_EXTEND の
   ものだけ成果を計算し、min(direct, 成果までの距離) ≤ SNAP_RADIUS のものを競わせる。
   勝者 = 成果がタップ点に最も近い原子。8px 以内の近差は direct の近い方。
   touchedAtom / stolen の帳簿（v1111）はそのまま生かす。
4. **手順3の置き換え**: 競合ゼロ → グリッド丸め自由配置（§1-D の条件）。
5. **handleMouseDown**: `clickedAtom`(28px) は残しつつ、**削除・置換の分岐だけ**
   「素の距離 ≤ ATOM_TAP_RADIUS」を追加条件にする。外れたら配置経路へ落とす。
   mousemove のプレビュー分岐（`!clickedAtom && coords.isValid`）も同じ条件に揃える
   （プレビューが出る場所 = タップで置ける場所、を崩さない）。
6. **getRingPlacementPlan**: 孤立禁止則（reason:'isolated' の節）を削除。

---

## 4. 既存テストへの影響（先に分かっている赤）

- **PW1(a)・PW3**: 200px 先のクリックで 'far' を誘発している。要望D で
  「原子が増えない」の assert が落ちる。**PW1(a) は『自由配置で原子が増え、字幕は出ない』の
  検査に書き換え**、4通りの言い分けは (far の代わりに) 自由配置クリアランス違反の「近すぎ」で構成し直す。
  PW3 の連打誘発は 'overlap'（近すぎ）の場面に差し替える。
- **PW2**: 「取られた」場面の作り方が幾何依存。決定2 の下でも stolen は起きる（↘ 方向など）が、
  findMissSpot の探索が現行の場面で見つかるかは実機で確認・必要なら場面を作り直す。
- **P12-8 系**（メチルシクロヘキサン2本目の吸着テストがあれば）: 通ることを§1-E の数字どおり確認。
- ZD1〜2・FR1・MM9 は設計上ノータッチ（落ちたら実装ミス）。

---

## 5. 受け入れ基準（発注書 §2 と同じ測り方で前後表を出す）

| 実測 | 現状 | 合格ライン |
|---|---|---|
| 2a 鎖の延長が成功する距離帯 | 32〜42px（実質10px） | **18〜63px（45px 幅）** |
| 2d 先端に枝が付く方位 | 2/5 方位 | **≥ 4/5 方位** |
| 2d 「足すつもりで削除」帯 | 20〜26px で発生 | **0（18px 超で削除は起きない）** |
| 2c 別分子の環を置ける帯 | 70〜100px | **クリアランス以遠ぜんぶ（70〜200px 全部○）** |
| 2b ベンゼン1本目 | 28px を正確に狙う必要 | 頂点から 20〜63px のどこを押しても定位置に付く |

**否定対照**（直しを外すと赤くなるテストを最低4本）:
SNAP_RADIUS を 45 に戻す → 2a テスト赤／勝敗則を旧式（候補点込み最短）に戻す → 2d テスト赤
（かつ P12-8 テストは緑のまま＝両立の証明）／ATOM_TAP_RADIUS を 28 に戻す → 削除帯テスト赤／
孤立禁止則を復活 → 自由配置テスト赤。

**夜間監査**: audit.js ④ に「環 → 鎖 → その先端に枝」を1件追加。
判定は「足せる方位が5方位中いくつか（≥4）」「成功する距離帯の幅（≥40px）」。

---

## 6. 作法（HANDOFF §7 の再掲・レーン向け）

worktree で作業／1修正=1コミット・パス指定コミット／版番号は index.html・test.html・audit.html の
全 `?v=` と version 表示を同時更新／回帰 343件＋check-mobile 25端末＋verify-release で締める。

---

## 7. 場外（この設計では扱わない）

- ~~**要望G（`?rec=` で自由配置が 42px ずれる）**~~ → **§9 で決着（v1150）**。
  当時の見立て「絶対グリッドに丸めるだけだから viewBox に依存しない」は**半分だけ当たっていた**。
  入力側が原因なのはそのとおりだが、**丸めるほうも無実ではなかった**（下記）。
- 自由配置の初回に案内トーストを出すか（「新しい分子として置きました」）は実装レーンの裁量。
  出すなら v1111 の throttle に乗せること。

---

## 8. 検収（2026-08-12・v1131・ユーザーが実機で操作）

実装は `feat/hit-areas` → main（v1131）。**受け入れ基準（§5）は全項目で合格ラインを超えた**。

| 実測 | 前(v1121) | 合格ライン | 後(v1131) |
|---|---|---|---|
| 2a 鎖の延長が成功する距離帯 | 30〜44px | 18〜63px | **20〜104px** |
| 2d 先端に枝が付く方位 | 2/5 | ≥4/5 | **5/5** |
| 2d「足すつもりで削除」帯 | 4〜28px | 0 | **4〜18px（原子の上だけ）** |
| 2c 別分子の環を置ける帯 | 72〜112px | クリアランス以遠ぜんぶ | **72〜212px 走査すべて○** |
| 2b ベンゼン1本目 | 30〜44px | 20〜63px | **30〜90px**（19〜28px は §8-1） |

**ユーザーによる実機確認**（数字ではなく体験の側）:

- **②（環 → 鎖 → その先端に枝）… 問題なし。リモネンも作れた**
  発注書 §0 で唯一 **「⚠ 実質むり」** と判定されていた分子。**当たり判定を直す動機そのものが解けた**
- **③（足すつもりで消える）… 問題なし** — 4回実発生していた事故が再現しない
- **④（離れたところに別の分子）… 問題なし** — 孤立原子が増える体験は**煩わしくなかった**。
  §1-D の「無言で捨てるより、見えて戻せるほうがよい」という判断が実地で支持された

**P12-8 の非退行**も実測で確認（成果の距離: 環炭素 0.0px / メチル 21.7px。設計 §1-E の
`2·42·sin15° ≈ 21.7px` と一致）。否定対照 HA7 が「旧式に戻すと 2d が 2/5 に落ち、
**かつ P12-8 は緑のまま**」を示していて、両立が証明されている。

### 8-1. 残った1点 → **現状維持で決着**（2026-08-12・ユーザー判断）

**ベンゼン環の原子から 19〜28px の帯だけ、タップしても何も起きない。**

§1-F（非破壊の掴みは 28px 据え置き）と §5（2b は 20〜63px）が両立しないため、
実装レーンは §1-F を優先した。ベンゼン環内の原子は `findAtomAt(28)` でドラッグの掴みになり、
**動かさず離すと何も起きない** ＝ 要望A で潰したはずの「無言のタップ」がこの帯だけ残る。

選択肢は3つ。**距離を覚える必要は消えたので実害は小さく、放置も選択肢**:

1. そのまま
2. 掴みの半径を下げる（ドラッグの掴みやすさが落ちる）
3. **動かさずに離したら配置に落とす**（掴みやすさは保つが「掴んでやめた」が原子追加になる）

**決定: 1（そのまま）**。ユーザーが実機で①〜⑤の全項目を操作し、**5項目すべて「問題なし」**
（②はリモネンまで到達）。⑤（ベンゼンに置換基）を含めて気にならなかったため、
掴みの半径にも「動かさず離したら配置」にも手を入れない。

この帯が残ってよい理由は「小さいから」ではなく、**押す場所の精度が要らなくなったから**。
決定1・2 の下では頂点から 30〜90px のどこを押しても定位置に付くので、
19〜28px を狙う動機がそもそも無い（§1-C の「距離を覚える必要が消える」の帰結）。
**将来ここを触るなら、先に §1-F の「非破壊の 28px 据え置き」を読むこと** —
掴みの半径を下げるとドラッグ（ZD1〜2 が見ている）に響く。

### 8-2. その 19〜28px の帯は、v1180 で**別の理由から**消えた（発注書 §2h・案B-2）

8-1 は「掴みの半径を下げるか」で悩んでいたが、**下げずに解決した** ——
1原子のドラッグそのものを廃止したから。

きっかけはユーザーの「ベンゼン環の原子だけドラッグできて変な感じがする」。調べると、
1原子ドラッグは設計された機能ではなく**ジェスチャの割り当ての余り**だった。
普通の原子はタップが削除・置換で埋まっているので、**タップが空いている原子
（ロック原子・ベンゼン環の原子）にだけ**ドラッグが残っていた。しかも
ベンゼンの炭素は**消しゴムでは既に消せて**おり（守り切れていない例外）、
消しても `C₆H₆ → C₅H₈`（1,3-ペンタジエン）になるだけで価標も壊れない。

**残る規則は2行**:

> 原子は同元素タップで消える。個々の原子は動かせない
> （どかすなら Shift＋ドラッグで分子ごと、間隔を空けるなら結合線のドラッグ）

副産物として **8-1 の「何も起きない帯」は消えた**。ベンゼン環の原子から 19〜28px は
他の原子と同じく配置経路へ落ちるので、§5 の「2b は 20〜63px」と §1-F が両立する。

残る唯一の「タップが空いている原子」は**ロック原子**（アルキル基練習の付け根 C1 と R マーカー）で、
これは無反応にせず**理由を出す**（「ここ（ロックした原子）は消したり別の元素に変えたりできません。」＝ GB4）。

⚠ **ベンゼン印の後始末が要る**。`benzeneCenter` / `benzeneAngle` は
「置換基は環の外向きへ 27.97px」という配置の作法を指しているので、
環でなくなった原子に残っていると**もうペンタジエンなのに置換基だけベンゼンの作法で付く**。
`dropStaleBenzeneMarks()` が描き直しのたびに「環結合を1本も持たない原子」から印を落とす（GB2）。

⚠ **0.0px の完全重複の罠（ZD1・ZD2）は Shift＋ドラッグ側へ移した**。
判定の実体は `canMoveComponentBy`。1原子ドラッグを消したついでに罠まで消さないための移設で、
否定対照 ZD2 がここを差し替えて「外すと 0.0px が必ず戻る」ことを見張る。

---

## 9. 要望G の決着 ——「半格子を狙うと、丸めの向きが画面レイアウトで決まっていた」（2026-08-12・v1150）

### 9-1. 見立ての答え合わせ

§7 の見立ては **場所は当たり・機構は外れ**だった。

- ✅ **当たり**: 値が分かれるのは入力側（client 座標 → 論理座標）。
- ❌ **外れ**: 「`getScreenCTM()` を使わず viewBox 比を手計算している所がある」——**無い**。
  `game.js` の変換は `clientToSvg()` 1本で、`getScreenCTM().inverse()` を正しく使っている
  （手計算は `viewBox.width /…` を含めて1箇所も残っていない）。
- ❌ **外れ**: 「絶対グリッドに丸めるだけなので viewBox に依存しない」——**依存していた**。
  丸めの**入力**が viewBox に依存した誤差を持っていたので、丸めの**出力**も依存した。

### 9-2. 二分の記録（実測値。810×1440・台本 V85 の点 `(546,273)`）

| 段 | `?mode=free` | `?rec=`（frame あり） |
|---|---|---|
| SVG の矩形 | top **110** px | top **66** px（ヘッダーを隠すぶん） |
| CTM の平行移動 f | 95.52424242424246 | 73.52424242424244 |
| ① 論理 → client（`svgPoint`） | (559.3757575757576, **754.0333333333333**) | (559.3757575757576, **732.0333333333333**) |
| ② イベントに載った client | 同上（欠落なし） | 同上（欠落なし） |
| ③ client → 論理（`clientToSvg`） | **273** | **272.99999999999994** |
| ④ `y / 42` | **6.5** | **6.499999999999998** |
| ⑤ `Math.round` | **7** → 中心 294 | **6** → 中心 252 |
| ⑥ 五員環の頂点 | (546, **258**) | (546, **216**) |

**分かれ目は③**。①②は 1ulp まで一致していて、client 座標の生成にも受け渡しにも欠落は無い。
`getScreenCTM().inverse()` を通した戻り値に **5.7e-14 の丸め誤差**が乗り、
**誤差の向きが行列の中身 ＝ 画面レイアウトで変わる**。ふだんは無害だが、
`y=273` は **42 の倍数のちょうど半分**なので、この 5.7e-14 が `Math.round` の分かれ目に乗る。

つまり **42px のずれの正体は「1ulp の誤差で丸めが反転したこと」**。
`frame` は必須条件ではなく（`frame` 無しでも `clear-all` 後の viewBox 次第で反転する）、
**半格子を狙ったこと**が必須条件だった。

### 9-3. 直したこと

`snapToGrid(v)`（game.js）を新設し、絶対グリッドへ丸める箇所をすべてここへ寄せた
（単原子の空キャンバス・自由配置・環モジュール・ハース環・分子の丸ごと平行移動）。

```js
// 丸める前に、意味のある桁より下を落とす（1e-6 論理px ＝ 図の 1nm 相当）
Math.round(Math.round(v * 1e6) / 1e6 / GRID_SIZE) * GRID_SIZE
```

`clientToSvg` 側で丸めないのは、**拡大率のアンカーやパンの差分など連続量として使う経路**が
あるため ＝ 離散化する所でだけ離散化する。丸めの慣習（半格子は上側）は変えていない。

### 9-4. 前後の実測

タップは 3経路とも論理 `(546,273)`。表の値は落ちた最初の重原子。

| 経路 | | free / frameなし | free / frameあり | rec / frameなし | rec / frameあり |
|---|---|---|---|---|---|
| 単原子 | 前 | C(546,294) | C(546,294) | **C(546,252)** | **C(546,252)** |
| | 後 | C(546,294) | C(546,294) | C(546,294) | C(546,294) |
| 環モジュール（五員環） | 前 | C(546,258) | C(546,258) | C(546,258) | **C(546,216)** |
| | 後 | C(546,258) | C(546,258) | C(546,258) | C(546,258) |
| ハース環（ピラノース） | 前 | C(491,246) | C(491,246) | C(491,246) | **C(491,204)** |
| | 後 | C(491,246) | C(491,246) | C(491,246) | C(491,246) |

### 9-5. 台本側の後始末

**`build-nicotine`（V85）は `?rec=` 側の丸め（中心 252）に合わせて座標が採られていた。**
直したあと `?mode=free` の丸め（中心 294）にそろうため、そのままだと**両方そろって外れる**
（実測: 直す前は 下見 C₁₁H₁₈N₂ ／ 収録 C₁₀H₁₄N₂、直した直後は 両方 C₁₁H₁₈N₂）。
五員環のタップを `(546,273)` → `(546,252)` に寄せて、**下見・収録とも ニコチン C₁₀H₁₄N₂** にした。

**台本を書くときは半格子（42 の倍数 ±21）を狙わない。** 向きは固定したが、
分かれ目の上にいると「1つ隣の格子を狙ったつもり」が読み取りにくいのは変わらない。
`demos-build.json` の全21本を 通常レイアウト × 収録レイアウト で流して、
**全本で同じ分子になること**を確認ずみ（直す前は `build-nicotine` の1本だけが不一致）。

回帰テストは `GR1`〜`GR3`（`GR3` は否定対照 ＝ `HIT_AREAS.legacyGridRound` で
素の `Math.round` に戻すと 3経路とも 42px ずれることを示す）。

## 10. §2g の決着 ——「伸ばした結合線が、既存の原子の下をくぐっていた」（2026-08-12・v1160）

### 10-1. 何が起きていたか

`MIN_CLEARANCE`（27.3px）は**原子どうしの距離しか見ていない**。置き場が詰まると
結合を 42→84px まで延ばして逃がすが、**延ばした線が既存の原子の下を通るか**は
どの門も見ていなかった。ユーザー報告の手順（発注書 §2g）で再現:

1. シクロヘキサンを置く（頂点の1つが `(456,273)`）
2. その環炭素にメチル基 → `(493,252)`
3. そのメチルから**下へ**伸ばしてエチル基 → `(493,294)`
4. **同じ環炭素**に右向きでもう1本

4 で「側鎖2本目」の振り分け（P6-3）が働き、既存の枝が二等分線の反対側 ―― つまり
**新しい結合の通り道**へ ―― 平行移動する。原子どうしは 27.3px 空くので門は開き、
結合は 48.3px まで延びる。その線から **5.6px** の位置にエチルの炭素が居た（実測）。
原子の絵は半径10px なので、**線が丸の上に乗る ＝ 別の構造式に見える**。

### 10-2. 同じ判定を1本にした（3か所 → 1か所）

同じ意味の検査は前から2か所にあり、**手描きの配置だけ無かった**:

| 場所 | 何を見ていたか |
|---|---|
| `reactor.js` の `search(_, strict)` | 生成物の置き場所で結合線が無関係な原子を貫通しないか |
| `game.js` の `_hasBondThroughAtom` | 整形（`reshapeVinylAngles`）で結合が別の重原子を貫通したら座標を戻す |
| **手描きの配置（`placementFor` の 9.）** | **無し** ← ここを塞いだ |

`atomUnderBondLine(a, p, q, clearance)` を1つ作り、**整形モードと手描きの配置と
夜間監査がこれを呼ぶ**形にした（`reactor.js` の strict は後述の理由で残置）。

**ついでに1件みつかった退行**: `pointSegmentDistance` は game.js と reactor.js に
**同名で引数の形が違う実装が2つ**あった。どちらも classic script のトップレベル宣言なので
`window` の同じ名前を取り合い、**あとから読まれる reactor.js が勝つ**。その結果
`segmentHitsRect` の「矩形の4隅が帯の中」の枝は `(number, number, {x1..})` を
`(p, a, b)` に渡していて **常に NaN → 常に false**（＝黙って効いていなかった）。
実装を game.js の1本に寄せ、reactor.js 側は削除した。

**`reactor.js` の strict はそのまま残した**。あちらのしきい値 `SHOVE_LINE_CLEARANCE`（14px）は
「**分子ごと逃がす**ときにどれだけ空けるか」で、単位も母数も違う（母体の刻み `bondStep` は
80px 刻みの登録図に合わせて動く）。数値を統一すると片方の意味が壊れるので、
**距離の計算（`pointSegmentDistance`）だけを共有**し、判定の使い分けは残す。

### 10-3. しきい値を 16px にした理由

**上から**: 素の格子で起こりうる最小は **21px（= 42·sin30°）**
＝ 環炭素に側鎖が2本（二等分線±30°）付いたときの「原子 ↔ 隣の結合線」。
つまり 21px は**正常な作図の下限**なので、判定はそれを明確に下回るところに置く。

**下から**: 原子の絵は半径10px。16px なら白場が 6px 残り、「線の下に丸が潜っている」
ようには見えない。

**実測の裏づけ**: 名称ライブラリ **923件**の登録図を全部測ったところ、
「原子 ↔ その原子を端点に持たない結合線」の最小は **25.99px**（糖のハース環）。
16px はもちろん、24px でも1件も弾かれない ＝ **登録図の再現性を壊さない**。
16 は整形モードが前から使っていた値でもあるので、**新しい数字を増やしていない**。

### 10-4. 直し方（順序）

`placementFor` の 9. で長さを探すとき、原子どうしの距離に加えて
**新しい結合線 ↔ 既存の重原子** と **新しい原子 ↔ 既存の結合線** の両方向を見る。

**避け方の順序は「配置を変える」→「延ばす」を守る**:

1. **振り分け（`adjust`）を続けたまま** 42px から段階的に延ばして探す
2. どの長さでもくぐるなら、**振り分けをやめて**もう一度探す
   （やめれば既存の枝が二等分線上に残り、新しい結合は素の 21px を確保できる）
3. それでも無理なら置かない ―― ただし**黙って捨てず**、専用の理由 `'crossing'` を返す

3 の文言は `'overlap'` と**言い分ける**。逃げ道が違うからで、
`'overlap'` は「結合線をドラッグして伸ばすと空間ができます」が効くが、
**くぐりは伸ばすほど深くくぐる**（垂線の長さは L に依らない）。

### 10-5. 前後の実測

**§2g の手順**（原子 ↔ その原子を端点に持たない結合線の最短距離）:

| | 4 の配置 | 新しい結合 | 最短距離 |
|---|---|---|---|
| 前 | `(504.7,273)`・振り分けあり | `(456.4,273)-(504.7,273)` 48.3px | **5.6px**（C(477.4,278.6) が線の下） |
| 後 | `(511.0,273)`・振り分けなし | `(456.4,273)-(511.0,273)` 54.6px | **18.2px** |

**全体**（登録図 923件のすべての重原子のまわり 8方位 × 30/45px を叩き、
置けた場合に実際の座標で測る。11万通り）:

| | 置けた | うち線が原子の下をくぐる | 最悪 |
|---|---|---|---|
| 前 | 113,805 | **1,975** | **0.0px**（トリステアリン。線が原子の中心を通る） |
| 後 | 112,074 | **0** | ― |

置けなくなった 1,731 通りはすべて**本当にくぐる置き方**で、
`'crossing'` の理由が字幕に出る（例: β-D-グルコースのアノマー -OH から左へ出すと、
73.5px の結合線が環内の O の **10.0px** 横を通る）。逆に **3通りは新しく置けるようになった**
（振り分けをやめる 2 のおかげ）。

### 10-6. 夜間監査に足した

`audit.js` の `inspectMolecule` に「**結合線の下の原子**」を追加した
（`①ライブラリ検査`・`②ファズ` の両方で毎回出る）。しきい値は持たず
**アプリ本体の `BOND_ATOM_CLEARANCE` を起動時に写す** ＝ 監査だけ別の物差しにならない。
`comparableKey` に `bondLinePx` を足したので、検査が増える前の回とは並べない。

**初回（150反復 × 80操作）の結果**: ライブラリ検査は **0件**、
ファズで **6件**（0.0px・2.9px・11.4px・12.0px・13.0px）。
ファズの6件は**手描きの配置以外の経路**（結合の伸縮ドラッグ・モジュール配置・反応の実行）で
出ているので、**この穴はまだ全部は塞がっていない**。毎晩機械で出るようになったので、
数と出どころを見てから塞ぐ順を決める。

### 10-7. 自動水素の穴も塞いだ（2026-08-13・v1240・しきい値 12px）

**自動水素はモデルに存在せず、描かれているだけ**（`userMolecule.atoms` に H は入っていない）。
そのため 10-2 で足した判定も、既存の `MIN_CLEARANCE` も、**H を1つも見ていなかった**。
伸ばした結合線が、別の原子から生えた H のグリフ（半径6px）の上を通っても誰も止めない。

**⚠ v1160 のこの節には物差しの取り違えがあった**（ここで訂正する）。
「`verify-compounds.js` が 11.5px を『やや近い』として通しているので 16px を当てると
登録図が弾かれる」と書いていたが、あの **11.5px は原子と H の距離**であって、
いま塞ぐ「**H が結合線の下に来る**」とは**別の量**。混ぜて読むと、実際には
2.3px の余裕がある 12px を「危ない」と誤って評価する。

#### しきい値 12px の根拠

**下から**: H のグリフは半径6px。12px なら白場が 6px 残る ―― **重原子の 16px（絵は半径10px）と
残す白場をそろえた**結果が 12。重原子より緩いのは、**H の絵が小さいぶん実害が出る距離も近い**から。

**実測**: 登録図 **939件**（比較できる結合を持つ **926件**）で
「H の中心 ↔ その H の親原子を端点に持たない結合線」を全部測ると

| | |
|---|---|
| 最小 | **14.32px**（スクロース・β-D-フルクトフラノース の2件） |
| 14px 未満 | **0件** |
| 典型 | 25〜26px |

12px なら**登録図は1件も弾かれない**（余裕 2.3px）。

**数字は 16 と別に持つ**（`HYDROGEN_BOND_CLEARANCE`）。理由が違うものを同じ定数にまとめると、
片方の理由で動かしたときにもう片方が黙って壊れる。
`reactor.js` の `SHOVE_LINE_CLEARANCE`（14px）にも手を触れていない（10-2 と同じ理由）。

#### 直し方

測り方は 10-3 と同じ（点と線分の距離・端点そばの t は見ない）で、幾何は
`underBondLine` に1本化し、`atomUnderBondLine`（16px）と
`hydrogenUnderBondLine`（12px）が**別々のつまみ**でそれを呼ぶ
（`legacyBondCross` / `legacyHydrogenCross`。片方を外したときに何が守っているのか読めるように）。

**点の集合を広げるだけでは足りない。** H の向きは分子ぜんたいの形で決まる ――
`calculateHydrogens` は結合方向に加えて **75px 以内の非結合重原子の向きを ±60° 避ける**ので、
新しい原子が生えるだけで**近くの H が別の向きへ動く／消える**。しかも新しい原子自身にも
H が生えて、それが既存の結合線をくぐりうる。**置く前の H を平行移動しても当たらない**ので、
候補ごとに「置いたあとの分子」を使い捨てで組んで（`moleculeWithCandidate`）数え直す。

判定は「くぐりが**増えるか**」―― 置く前から抱えているくぐり（伸縮ドラッグや反応で
できた図など）を理由に以後いっさい置けなくなるのを避ける。

**吸着と自由配置は別の道を通る。** `placementFor`（吸着）だけ塞いだ時点で残ったくぐりは
**全部が自由配置**だった（下の表の 110通り）。どの原子とも競わない遠さに置かれた原子は
結合を持たないので、`calculateHydrogens` の「近隣原子がない」枝で **H が上下左右に4本**出る。
そこには「延ばす」逃げ道が無い（結合そのものが無い）ので、理由も
`'crossing'` と**言い分けて** `'hcrossing'` を立てた
（文言は「結合線が…通る」ではなく「その原子から出る水素が別の結合線に重なる」）。

#### 前後の実測

登録図 939件のすべての重原子のまわり **8方位 × 30/45px**（125,856通り）を叩き、
置けた場合に実際の座標で「H ↔ その H の親を端点に持たない結合線」を測る。
「前」は `legacyHydrogenCross` を立てて**本番の経路をそのまま旧挙動で**走らせたもの。

| | 置けた | うち H が結合線の下（<12px） | 最悪 |
|---|---|---|---|
| 前 | 116,872 | **719** | **0.0px**（β-D-グルコース。H の中心が線の上） |
| 後 | 116,493 | **0** | ― |

**代償**（門番は必ず何かを禁じる）:

| | |
|---|---|
| 置けなくなった置き方 | **387通り**（50個の図。多い順にクメン38・コレステロール36・ヒスタミン34） |
| 新しく置けるようになった置き方 | **8通り**（振り分けをやめる 10-4 の 2 のおかげ） |
| 差し引き | 116,872 → 116,493（**−379**） |
| 断った理由の増減 | `'crossing'` 1,778→2,047（+269）／`'hcrossing'` 0→110／`'overlap'` +9・`'crowded'` −9 |

最後の ±9 は、置けなくなった原子の**スコア**（＝ 成果の位置）が変わったぶん。
吸着の候補になれるかは「成果が吸着半径の中か」で決まるので、門番が1つ増えると
**同じタップが吸着の道から自由配置の道へ移る**ことがある（数は動くが、どちらの道にも門番がある）。

**費用**（`calculateHydrogens` は分子ぜんたいを見るので大物では効く。pointermove 1回あたり実測）:

| 図 | 原子数 | 前 | 後 |
|---|---|---|---|
| エタノール | 3 | 0.02ms | 0.03ms |
| スクロース | 23 | 0.08ms | 0.41ms |
| コレステロール | 28 | 0.24ms | 1.19ms |
| トリステアリン | 63 | 0.19ms | 4.02ms |

基準値は `getSnappedCoords` 1回につき1度だけ数えるよう持ち回っている（`memo`。これで 7.5→4.0ms）。
**手で描く大きさ（〜30原子）では 1.2ms 以下**で、4ms が出るのは
「油脂の登録図を呼び出したうえで、その上に描き足す」ときだけ。

#### 見張り

| どこ | 何を | しきい値 |
|---|---|---|
| `game.js`（本番） | 吸着・自由配置の門番 | `HYDROGEN_BOND_CLEARANCE` = 12 |
| `audit.js`（夜間監査） | 「**結合線の下の自動水素**」を毎回出す | アプリ本体から**写す**（`hydrogenLinePx`） |
| `verify-compounds.js` | 登録図に1件も無いこと | 12（写し。ここは chemistry.js しか読めない） |
| `tests.js` HX1〜HX4 | HX3・HX4後半が否定対照 | ― |

`audit.js` の `comparableKey` に `hydrogenLinePx` を足した ＝ **検査が増える前の回とは並べない**。
**初回（ライブラリ検査 939件 ＋ ファズ 150反復 × 80操作）**: ライブラリは **0件**、
ファズで **21件**（0.0px・4.0px・5.0px・9.5px …）。10-6 と同じ読み方で、
ファズの 21件は**手描きの配置以外の経路**（結合の伸縮ドラッグ・モジュール配置・反応の実行）から出ている
＝ **この穴もまだ全部は塞がっていない**（同じ回で「結合線の下の原子」も 9件出る）。
`verify-compounds.js` では「自動水素がやや近い」（原子と H の距離）と**別項目**にしてある。
混ぜないのが、この節の最初の取り違えを繰り返さないための形。

#### やっていないこと（申し送り）

- **重原子のほうも自由配置の経路は素通り**（10-2 で塞いだのは `placementFor` だけ）。
  今回の掃き出しでは重原子のくぐりは自由配置から1件も出なかったので手を付けていない
- **`getPlacementBondTargets` の自動接続は仮の分子に入れていない**。直交整列した原子へ
  勝手に増える結合は、置いたあとで初めて決まる。掃き出しの後 0件なので実害は出ていないが、
  ここは近似のまま（ずれれば夜間監査の「結合線の下の自動水素」に出る）
- **結合の伸縮ドラッグ・モジュール配置・反応の実行**は 10-6 と同じく未着手。
  夜間監査に H のぶんも出るようになったので、数と出どころを見てから塞ぐ順を決める
