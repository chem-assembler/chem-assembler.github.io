# 紙の書き方で構造式を描く（表示の切り替え）設計書

参考書（`reference-src/*.md` → `reference.json`）や説明画像に載せる構造式を、
**アプリの作図ではなく「紙に書くときの書き方」**で描いて、SVG と PNG に書き出すための設計。

⚠ **この設計書は第1段の設計まで。実装は含まない。** 第2段以降（§9）は範囲だけを置く。

---

## 1. なぜ要るか

### 1.1 ユーザーの言葉（この設計の出どころ）

参考書のページに構造式をどう載せるかを相談したときの発言。

> **アプリの構造式は、一般に紙に書く構造式とは異なるので、参考書のページとしては別なものを
> 用いたほうがよい（アプリの操作の説明場面であれば用いる）と思います**
> **参考書生成用にアプリを作るのも手だと思います**

いま図をどう作っているか:

> **構造式はいつも手作業で作っています**
> **ppt 内で作る場合と、canva で png を作る場合があります**
> **（反応機構の図も）手書きで作業しています**

★ 省略の段について（**この設計の中心**）:

> **通常は CH 結合 と OH 結合 のみ省略する表記が多いですが、**
> **反応機構の説明画像としては、すべての結合を省略しない方がよいときがあります。**
> **また、必要に応じて矢印などを書き加えます**

置き場所（ユーザーの提案。統合レーンも同意）:

> **あるいは現在の assembler を改修して、見た目を切り替え変更できるようにする手もありますね**

### 1.2 いまの状態

- **参考書の本文には構造式の図が1枚も無い。** `reference-src/` の5ページが持つ囲みは
  `:::stageTable` `:::example` `:::mechanismTable` `:::dehydrationTable` の4種類だけで、
  **図を差し込む書式が存在しない**（2026-09-09 に全文を確認）
- アプリの図（`assembler/compounds.json` の 921 件と、キャンバスの作図）は
  **暗い地に色つきの円と色つきの元素記号**で描かれている。`renderTargetAtom` は
  1原子ごとに `fill:#0f141c` の円を敷き、`stroke` と文字色を `var(--color-c)` などで塗る
  （`assembler/game.js:5855`）。紙に置くと別物になる
- 書き出しの口が無い。SVG も PNG も出せない（`XMLSerializer` を使っているのは
  `assembler/tests.js:7477` の検査だけ。`a.download` は `assembler/audit.js:1612` の
  監査 JSON だけ）

### 1.3 発注の芯と一致している

`DEVELOPMENT.md`「発注の芯」（2026-08-22・ユーザーが言語化）:

> **発注の意図は一貫して、紙面での構造式を理解できるようにすることです**

★ この設計は、その芯を**逆向きに一度だけ通す**もの ——
これまでは「紙の図を読めるようにアプリを直す」だった。ここでは
**「アプリの分子から紙の図を出す」**。同じ規則を使うので、片方を直すともう片方も正しくなる。

---

## 2. 紙の書き方の規則（実物から取り出したもの）

⚠ **想像で決めていない。** 根拠はすべてユーザー自身の授業スライド
`マイドライブ/化学/動画スライド/drive-download-20260908T201329Z-1-001/` の有機19本
（960×540pt・PowerPoint 由来）。**どのファイルの何枚目か**を必ず添える。

### 2-A. 主鎖は横一直線（★ 明文の規則がスライドにある）

| 根拠 | 中身 |
|---|---|
| `有機の基本2-3「アルカンの命名法」.pdf` **p8** | 黄色の枠で **「主鎖は横一直線に書く」** と書いてある。同じ枚に、主鎖が折れた図（2-エチル-3-メチルブタン）に**赤い×**が重ねてある |
| `有機の基本2-4「アルカンの書き出し」.pdf` **p3** | 箇条書きに **「構造式を書くときは必ず主鎖を横一直線に書く」** |
| `有機の基本2-5「アルキル基の書き出し」.pdf` **p3** | 同じ文が再掲されている |

⚠⚠ **これは発注書の前提（「120° のジグザグ」）とは違う。**
ユーザーの紙の書き方は**ジグザグではなく、主鎖を水平の1直線に置く直交作図**である。
120°（斜め）が出るのは C=C・C≡C・環の周りだけ（2-C・2-D）。
**§4 の並べ直しはこの実測に合わせて設計する**（ジグザグは採らない。§4.6 の「採らなかった案」）。

### 2-B. 枝は上下に出し、縦棒1本でつなぐ

`有機の基本2-3 p8` / `有機の基本2-3「アルカンの命名法」.pdf p12` / `有機の基本3-1「アルコールの性質」.pdf p19`。

```
        CH3   CH2 － CH3
         |     |
 CH3 －  CH －  CH  － CH3
```

- 枝の札は主鎖の**真上または真下**。結合は**縦棒1本**（`|`）だけで、札と主鎖の間に置く
- **上下の両方に出せる**（`2-3 p12` の (1) は上に `CH3`・`CH3`、下に `CH3—CH2`）
- ★ **枝の札の中身は付け根から外へ読む。** 上に出す枝は `CH2－CH3`（左から右）、
  下に出す枝は `CH3－CH2`（右から左）と**並びが逆**になる（`2-3 p12` の (1) で実測）
  ＝ 付け根に近い原子が主鎖のいちばん近くに来るように書く
- `-OH` も同じ扱いで**下に出す**（`3-1 p19` の 2-ブタノール・2-メチル-2-プロパノール）

### 2-C. C=C・C≡C のまわりだけ斜めに開く

| 根拠 | 中身 |
|---|---|
| `有機の基本2-7「アルケンの性質」.pdf` **p4**（主なアルケンの表） | `H＼ C ＝ C ／CH3` の形。**C=C は水平**、4つの置換基は**斜め**（左上・左下・右上・右下） |
| `有機の基本3-5「カルボン酸の性質」.pdf` **p6** | マレイン酸・フマル酸も同じ形。シス/トランスの差が図で読める |
| `有機の基本2-9「アルキンの性質」.pdf` **p2** | C≡C は**一直線**（180°）。「C≡Cに隣接する原子までが一直線上に固定されている」と明記 |

★ **これはアプリが既に持っている規則と同じ**（CLAUDE.md「名称から呼び出した分子の、
環に含まれない C=C まわりは ±120° に開いて置く」・v352）。§3 で使い回す。

### 2-D. 環は正多角形。ベンゼンは「上下が水平な六角形」

| 根拠 | 中身 |
|---|---|
| `有機の基本2-2「アルカンの性質」.pdf` **p8** | シクロプロパン＝**頂点が上の正三角形**、シクロブタン＝**軸に沿った正方形**。環の H は外向きに放射 |
| `有機の基本2-9「アルキンの性質」.pdf` **p18**（練習1(7)） | ベンゼン＝**上下の辺が水平な正六角形**（左右が頂点）。上下の C の H は斜め上／斜め下、左右の C の H は**水平** |

⚠ アプリのライブラリのベンゼンは `y` が `265.36 / 300 / 334.64` の3段
（`compounds.json` で実測）＝ **同じ向き**（上下の辺が水平）。**この点は既に一致している。**

### 2-E. 省略の段は3つではなく4つある（★ 発注書との差）

`有機の基本1-2「有機化合物の表し方」.pdf` **p2** が、酢酸とギ酸メチルで3段を並べて見せている:

| スライドの呼び名 | 中身（酢酸） |
|---|---|
| **構造式** | `H－C－C－O－H` に H を縦棒で足し、C=O を縦の二重線で下に置く。**全部の結合を書く** |
| **一部を省略した構造式** | `CH3－C－OH`（下に `‖ O`）。CH3 を1つの札にまとめ、OH も1つの札 |
| **示性式** | `CH3COOH`（1行の文字列。線が1本も無い） |

そして `有機の基本2-4 p3` に、**もう1段**が明記されている:

> **この動画ではH原子を省略し，結合の手（価標）のみ書く方法を採用する**

`有機の基本2-4 p4` / `p10` がその実例で、**C の四方に短い棒（価標）を4本出したまま H を書かない**。
使わない手も棒として残す ＝ **原子価が4本そろっているかを目で数えるための書き方**。

★ したがって紙の段は **L0 全部書く／L0.5 価標のみ（H を書かない）／L1 一部省略／L2 示性式** の4つ。
⚠ **発注書の「L2 骨格構造式」（線だけの zig-zag）はスライドに1枚も出てこない。**
高校化学では使わない書き方なので、**L2 は骨格構造式ではなく示性式にする**ことを提案する（§5・要決定）。

### 2-F. 番号・色・注記

`有機の基本2-3 p8` / `2-4 p4,p10` / `3-1 p19` で共通:

- **主鎖の番号**は**橙色**の小さい数字。原子の札の**左上**に置く
- **化合物名**も橙色で、図の**下**に置く（`2-4 p4` の「ブタン」「2-メチルプロパン」）
- **番号を振る向き**は**青い矢印**で示す（`2-3 p8`）
- **不斉炭素**は橙色の `*` を C の**上**に（`3-1 p19`）
- **囲み**は緑の楕円（`3-1 p19` のヨードホルム反応する CH3）、**枠つき札**は橙の角丸（「直鎖」「枝分かれ」）
- 本文の黒は純黒、地は白

### 2-G. 反応式（矢印と条件）

| 根拠 | 中身 |
|---|---|
| `有機の基本2-9 p18` | `CaC2 ＋ 2H2O ⟶ H－C≡C－H ＋ Ca(OH)2`。**係数は構造の左に置く**。`+` で並べ、**長い直線の矢印**で右へ |
| `有機の基本2-2 p8` | `＋ Br2 → Br－CH2－CH2－CH2－Br`。短い矢印もある（幅は絵の都合で決めている） |
| `有機の基本2-7「アルケンの性質」.pdf` **p22** | ★ **これが「手書きしている機構の図」の実物。** 反応物を L0 で描き、**橙の曲がった矢印**を結合から結合へ引き、**切れる結合に橙の ✕**を重ね、生成物名を橙で下に置く |

⚠ **矢印の上に条件（濃硫酸 160〜170℃ など）を書いた図は、有機19本の中には見つからなかった。**
条件は**問題文の日本語**（「エタノールに濃硫酸を加えて160～170℃に加熱した」`2-7 p23`）に書かれている。
**未調査ではなく「無い」**（19本 321 枚の本文を機械で抜いて確認）。§9 で扱う。

### 2-H. 特殊な表記

- **配位結合は矢印**。硫酸の S=O が `O` の上に `↑`（`2-7 p22`）
- **R 基**は `R` の1文字、フェニル基は六角形の絵（`有機の基本1-1「有機化合物の特徴」.pdf p4`）
- **くさび**は「▲＝手前・破線＝奥」（`2-2 p8`。**アプリの `stereo.js` と同じ規約**）

---

## 3. 既存の部品のどれが使えるか

⚠ **すべて実際に読んだ結果。読んでいないものは「未調査」と書く。**

| 部品 | 場所 | 判定 |
|---|---|---|
| `renderMoleculeIntoSvg` | `assembler/quiz.js:1458` | **骨組みは借りる。描画そのものは使えない**（下 3.1） |
| `renderTargetAtom` / `renderTargetBond` | `assembler/game.js:5855` / `:5882` | ⚠ **使えない**。暗い地の色が**べた書き**（下 3.1） |
| `condensedMode`（🔤 官能基をまとめる） | `game.js:509,835,6009` ／ `findCondensableGroups`（`chemistry.js:1353`） | **L1 の芯として使える**（下 3.2） |
| `condenseChainForDisplay`（`(CH₂)ₙ` に畳む） | `quiz.js:1257` 付近 | ⚠ **L1 とは別物**。紙には出てこない書き方なので使わない |
| 「🧹 分子を並べ直す」 | `index.html:483` | ⚠ **使えない**。釦の説明そのものに **「図の形は変えません」**とある ＝ 分子を格子のスロットへ配り直すだけ |
| ★★ **`renderStandardFigure`（「標準の書き方」の図）** | `learn.js:3854` | **★★ これが第1段の並べ直しの本体。ほぼそのまま使える**（下 3.5） |
| ★ `ipNumberedLayout` / `ipStraightLayout` / `ipLayoutFromChain` | `learn.js:1036 / 1065 / 1106` | **★★ 主鎖を横一直線に置き、枝を上下に伸ばす計算がもうある**（下 3.5） |
| ★ `ipGeoChainPos` | `learn.js:1209` | **★ C=C を必ず水平に置いてシス/トランスを描き分ける**（下 3.5） |
| ★ `ipCloneWithPos` / `copyMoleculeForThumbnail` | `learn.js:1165 / 69` | **★ 複製を作る道具がもうある**（§8.3 の担保に直接使える） |
| ★ `drawIupacNumberingIntoSvg` / `drawAlkylNumberingIntoSvg` | `game.js:11203` 付近 / `:11160` | **★ 主鎖の番号を図に重ねる道具がもうある。色は `--neon-orange #ffa502` ＝ スライドの橙とほぼ同じ** |
| `layoutMolecule`（環のテンプレート） | `chemistry.js:1264` | **半分だけ使える**。⚠ **環のテンプレートが長方形・家型で、正多角形ではない**（下 3.5） |
| ±120° に開く規則 | `game.js:5092 reshapeDoubleBond` | **★ 使える。ただし §3.5 のとおり第1段では出番が小さい**（下 3.3） |
| `_subtreeIds` / `_moveSubtree` / `_rotateSubtree` | `game.js:5306 / 5323 / 5336` | **★ 使える。並べ直しの基本操作**（ただし `Game` のメソッドなので切り出しが要る） |
| `_hasBondThroughAtom` | `game.js:5053` | **★ 使える。**「結合が重原子を貫通していないか」の門番 |
| ⇄ シス/トランス整形 | `index.html:161` ／ `flipCisTrans`（`game.js:5272`） | **触らない**。第1段は表示だけで、シス/トランスの確定はしない（§8） |
| `chemistry.js` の主鎖・命名・環の判定 | `chemistry.js` | **★ 使える**。DOM 非依存。主鎖の取り出しと番号付けをそのまま借りる |
| 反応機構の状態データ | `assembler/reactions.json`（14反応・47状態・35ステップ） | **★ 使える**（§11） |

### 3.1 `renderMoleculeIntoSvg` は「紙の書き方」に使えるか → **半分だけ**

実際に読んだ（`quiz.js:1458-1520`）。やっていることは:

1. `game.createTargetFromData({target})` で `Molecule` を組む
2. `mol.calculateHydrogens()` で H の座標を出す（`chemistry.js:194`）
3. 全体の bbox を取って `viewBox` を合わせる
4. `game.renderTargetBond` で結合を、`game.renderTargetAtom` で原子を描く

★ **1〜3 はそのまま使える。**とくに 2 は重要で、`calculateHydrogens` は
**二重結合の端では 120°、三重結合の端では 180°、それ以外は空いている方向**へ H を置く
（`chemistry.js:243-252`。コメントに「手書きの H–C≡C–H と同じ描き方」と書いてある）
＝ **紙の H の置き方の規則が、もう入っている。**

⚠ **4 は使えない。** 理由は3つあり、どれも直すのではなく**別の描画関数を作る**方が安い:

- `renderTargetAtom` は原子ごとに **`fill="#0f141c"` の円**を敷く（`game.js:5863`）。
  紙の図に丸は無い。文字色も `var(--color-c)` などの**色つき**
- `renderTargetBond` の線は `rgba(255,255,255,0.4)`（`game.js:5899`）＝ **白半透明のべた書き**。
  白地に置くと消える
- 結合の端の空け方が **前後 10px の固定**（`game.js:5891`）。
  紙は `CH3` のような**幅のある札**に合わせて空ける必要があり、固定値では札に線が刺さる

★ **結論**: 新しい `renderPaperSvg(mol, opts)` を1本作る。中身は
「`renderMoleculeIntoSvg` の 1〜3 ＋ 紙用の描画」。**既存の描画は1行も変えない。**

### 3.2 `condensedMode` は L1 に使えるか → **芯としては使える。足りないものが2つある**

`findCondensableGroups`（`chemistry.js:1353-`）を読んだ。返すのは
`{label, anchorIds, memberIds}` の配列で、いま拾えるのは
**`NO₂` / `SO₃H` / `COOH`（末端カルボキシ）/ エステルなど、C=O を含む官能基**。
`stillConnected` で「環の一辺になっている原子団」を外す作りまで入っている。

⚠ **足りないもの**:

1. **`CH₃` `CH₂` `CH` をまとめる機能が無い。**
   紙の L1 でいちばん多いのは `CH3－CH2－CH2－OH` の形で、これは
   「炭素1個と、それにぶら下がる H を1つの札にする」だけ。**官能基検出とは別の、
   もっと単純な規則**（下 §5.2）。既存の関数に足すのではなく、新しい関数にする
2. **`OH` を1つの札にする扱いが無い。**
   ユーザーの言う L1 は **「CH 結合 と OH 結合 のみ省略する」** なので、
   O—H も畳む必要がある。`findCondensableGroups` は末端 -OH を単独では拾わない

### 3.3 ±120° の整形は使えるか → **使える。ただし2つ直す**

`reshapeDoubleBond`（`game.js:5092-5177`）を読んだ。C=C 軸を座標で正規化してから
（原子IDが乱数なので `cB.x < cA.x` で向きを決める。v377 の実測メモつき）、
両端の置換基を ±120° に置く。環を含む枝は**回し**、含まない枝は**平行移動**する。
`_orientCarbonylsOutward` で C=O を外向きに直すところまで入っている。

⚠ **直すところ**:

1. `this.userMolecule` に直接触る ＝ **キャンバスの分子に結びついている**。
   書き出しは「ライブラリの図（`compounds.json`）を、キャンバスを汚さずに紙用に組み直す」ので、
   **`Molecule` を引数で受け取る純粋関数**に切り出す必要がある
   （`_subtreeIds` / `_moveSubtree` / `_rotateSubtree` / `_hasBondThroughAtom` も同じ）
2. **シス/トランスが未確定の図（2-ブテン・ブテン二酸）は整形してはいけない**（CLAUDE.md）。
   ⚠ ただし**書き出しは表示だけ**なので、この禁止は「元の分子を書き換えるな」の意味になる。
   **複製に対して整形する**なら未確定は未確定のまま残る（§8 でこれを検査にする）

### 3.5 ★★ いちばん大事な発見 —— 「紙の並べ直し」はもう作られている

⚠⚠ **設計の途中で見つけた。最初に書いた §4 は作り直しの設計になっていたので、書き換えた。**

`learn.js:3854 renderStandardFigure` は、コメントにこう書いてある:

> **標準の書き方の図: 主鎖を横一直線にし、主鎖の炭素へ位置番号を振る（環は layoutMolecule）**

★ これは §2-A・2-B の紙の規則そのもの。中身は3段の落ちで、**すでに動いている**:

| 段 | 関数 | 何をするか |
|---|---|---|
| 1 | `ipNumberedLayout`（`learn.js:1036`） | **IUPAC の主鎖**（`iupacNameDetail(mol).mainChain`）を横一直線に置き、**番号も返す**。★ ⚠ 番号は `findLongestCarbonChain` ではなく `iupacNameDetail` からしか取らない（v1340 の事故メモ: 最長鎖で番号を振ると図と名前が黙って食い違い、標準6問の25異性体で**2件**ずれていた） |
| 2 | `ipStraightLayout`（`:1065`） | 番号は振らないが鎖は横一直線（エーテル用）。★ v1440 に **ユーザー自身が「書き出しの正解／エーテルのアルキル基の主鎖を横一直線に」と言って入った道**。実測で「10件のうち2件が途中で直角に曲がって縦へ伸びていた」のを直したもの |
| 3 | `layoutMolecule`（`chemistry.js:1264`） | 1・2 が null（環・芳香族）のときの受け皿 |

そして共通の並べ方 `ipLayoutFromChain`（`:1106`）が §4 の②と④をそのままやっている:

```js
order.forEach((id, i) => pos.set(id, { x: i * IP_HSTEP, y: 0 }));   // ② 主鎖を水平に
...
const sign = (ri % 2 === 0) ? -1 : 1;   // ④ 枝は 最初は上・次は下（gem-ジメチル対応）
```

★ `IP_HSTEP = 46`（`learn.js:921`）。⚠ 環の `layoutMolecule` は 42px なので、
`renderStandardFigure` は環の座標を `k = IP_HSTEP / 42` 倍して**縮尺を1つに揃えている**
（「同じ表に2つの縮尺を並べない」）。**この揃え方は紙の書き出しでもそのまま要る。**

さらに `ipGeoChainPos`（`:1209`）は **C=C を必ず水平に置いて**（`pos.set(chain[p], {x:0,y:0})`・
`pos.set(chain[p+1], {x:H,y:0})`）前後の鎖を上下へ折り、シス/トランスを描き分ける。
★ **§4.3 で「C=C を水平に置くのが正しい」と結論したのと同じ置き方が、もう入っている。**

#### ★ したがって第1段で**足りない**のは4つだけ

| # | 足りないもの | 根拠 |
|---|---|---|
| **A** | ⚠⚠ **環が正多角形でない。** `RING_TEMPLATES`（`chemistry.js:1276`）は 6員環が `[[0,0],[G,0],[2G,0],[2G,G],[G,G],[0,G]]` ＝ **2×3 の長方形**。5員環は「家型」 | 紙は**正三角形・正方形・正六角形**（`2-2 p8`・`2-9 p18`）。★ ただし `compounds.json` の環は**もう正多角形**（ベンゼンの `y = 265.36/300/334.64` を実測）なので、**ライブラリの分子を出すときは環のテンプレートを通らない**（座標をそのまま使う） |
| **B** | **C=C まわりを斜めに開いていない。** `ipLayoutFromChain` は全部を直交格子に置く | 紙は `2-7 p4`・`3-5 p6` のとおり斜め。`reshapeDoubleBond` の計算（`game.js:5092`）を複製に当てれば足りる |
| **C** | **紙の見た目が無い**（丸・暗い地・色つき文字・固定 10px の端空け） | §3.1 |
| **D** | **省略の段と書き出しが無い** | §5・§6 |

⚠ **A〜D はどれも `renderStandardFigure` の外側の話**で、
**この関数を1行も変えずに足せる**。★ そこが第1段をいちばん安くする。

⚠⚠ **`learn.js` はいま別のレーンが触っている**（2026-09-09 時点）。
**新しい呼び出し口を `learn.js` に足すのは、そのレーンが着地してから。**
先に着手できるのは §11（機構の書き出し）と §6（描画と書き出し）で、どちらも `learn.js` を触らない。

### 3.4 ライブラリの図はいまどのくらい「紙」に近いか（★ 実測した）

`compounds.json` の 921 件・**8,158 本の結合**を全部数えた（2026-09-09）:

| 結合の向き（180°で畳んだ角度） | 本数 | 割合 |
|---|---|---|
| 0°（水平）・90°（垂直） | 5,943 | **72.9%** |
| 30° / 60° / 120° / 150°（環と C=C の斜め） | 1,716 | 21.0% |
| その他 | 499 | 6.1% |

| 結合の長さ | 本数 |
|---|---|
| 42px（`GRID_SIZE`） | 6,111 |
| 40px（環の一辺） | 1,506 |
| 上記以外 | 541 |

★ **もう93%が「42px か 40px の、直交か60°刻み」**。紙の書き方と**同じ骨格**でできている。

⚠ **足りないのは「主鎖が横一直線か」だけ**。最長炭素鎖が C3 以上の 943 件で測ると:

| 種類 | 主鎖が横一直線 | 例外の例 |
|---|---|---|
| 鎖・単結合のみ | **75 / 106（71%）** | イソプロピルメチルエーテル、ジイソプロピルアミン |
| 鎖・多重結合あり | **280 / 425（66%）** | シス-2-ブテン、マレイン酸、アラニン、酢酸プロピル |
| 環・単結合のみ | 1 / 59（2%） | シクロヘキサン（環に主鎖の概念を当てるのが誤り） |
| 環・多重結合あり | 0 / 353（0%） | ベンゼン、スチレン（同上） |

★ **鎖の分子は約 1/3 が階段になっている。ここを直すのが第1段の山**（§4）。

---

## 4. 並べ直しの計算

⚠⚠ **この節は §3.5 の発見のあとに書き直した。**
当初は「並べ直しを作る」設計だったが、**主鎖を横一直線にして枝を上下へ出す計算は
`learn.js` にもう入っている**。ここでは**足りない4つ（§3.5 の A〜D）だけ**を設計する。

### 4.1 何を作るか

```
paperLayout(mol) → 新しい Molecule（座標だけが違う複製）
```

⚠ **入力の分子は1原子も動かさない。** 複製に対して座標を書き、複製を描く。
これが §8 の「パズルに触らない」の実体でもある。
★ **複製を作る道具はもうある** —— `ipCloneWithPos(mol, pos)`（`learn.js:1165`）は
「元の分子と、新しい座標表」から新しい `Molecule` を組む。**そのまま使う。**

### 4.2 手順

**① 既存の3段の落ちをそのまま呼ぶ**（`renderStandardFigure` と同じ順・§3.5）

```
ipNumberedLayout(mol)          … IUPAC 主鎖を横一直線・番号つき
  || ipStraightLayout(mol)     … 横一直線・番号なし（エーテル）
  || （環）layoutMolecule(mol) … 環のテンプレート。座標を IP_HSTEP/42 倍して縮尺を揃える
```

⚠ **①を書き直さない。**書き直すと「図に出る番号」と「名前」が別々の計算になる
（`learn.js:1023-1028` が実測つきで釘を刺している事故の型）。

**② ライブラリの分子は、①を通さず元の座標を使う**（★ 判断）

`compounds.json` の 921 件は**人が置いた図**で、環はもう正多角形、
主鎖も 68%（鎖の分子）が横一直線（§3.4）。
★ **①を通すと、残り 32% は良くなるが、いま良い 68% も別の図に置き換わる。**
参考書に載せる図は**アプリのライブラリと同じ形**であってほしい（同じ分子が2つの顔を持たない）。

⚠ **したがって既定は「元の座標のまま」**、
`?relayout=1` のときだけ①を通す。★ **どちらを既定にするかはユーザーに聞く**（§13）。

**③ 環を正多角形にする**（§3.5 の A。**①を通したときだけ要る**）

`layoutMolecule` の `RING_TEMPLATES` は長方形・家型なので、紙には合わない。
⚠ **`RING_TEMPLATES` を書き換えてはいけない** ——
異性体練習のギャラリー（`learn.js:271,381`）・絞り込み（`narrowing.js:2221`）・
テスト（`tests.js` 7か所）が同じテンプレートの上に乗っている。
★ **紙用の別のテンプレート**を持つ:

| 環 | 紙の置き方（根拠） | 頂点 |
|---|---|---|
| 3員 | 頂点が上の正三角形（`2-2 p8`） | 半径 R、角度 −90°, 30°, 150° |
| 4員 | 軸に沿った正方形（`2-2 p8`） | 角度 −45°, 45°, 135°, 225° |
| 5員 | 正五角形・頂点が上 | 角度 −90° から 72° 刻み |
| 6員 | **上下の辺が水平・左右が頂点**（`2-9 p18`） | 角度 0° から 60° 刻み |

★ R は **40px**（`compounds.json` のベンゼンで実測: `y` の幅 69.28 = 2·40·sin60°）。
⚠ 環と鎖で結合長が違う（40 と 46）のは紙でも同じ（`2-9 p18` の六角形は辺が短い）ので、揃えない。

**④ C=C まわりを斜めに開く**（§3.5 の B）

`reshapeDoubleBond`（`game.js:5092`）の計算を**複製に**当てる。
- **環の中の C=C には当てない**（ベンゼンは③の正六角形のまま）
- C≡C は 180°（`calculateHydrogens` が既にそう置く。`chemistry.js:239-242`）
- ⚠ **元の図が持っているシス/トランスの側（外積の符号）を必ず保つ** ——
  `reshapeDoubleBond` の `rawSide` がそれをやっている

### 4.3 二重結合をどう置くか（★ ①と④がぶつかる）

⚠ **主鎖に C=C があると、①「主鎖を水平に」と④「±120° に開く」が同じ原子を取り合う。**
スライドの実物（`2-7 p4`・`3-5 p6`）を見ると答えははっきりしている:

> **C=C そのものを水平に置き、その両端の置換基だけを斜めに開く。**

つまり **② が勝ち、⑤ は「C=C の隣の原子だけ」に効く**。
主鎖が `C1-C2=C3-C4` なら、C1〜C4 の**すべて**を水平の1直線に置くのではなく、
- C2=C3 を水平に
- C1 は C2 から **-120°**（左上または左下）、C4 は C3 から **+120°**（右上または右下）
- ⚠ **シス/トランスはこの上下の選び方そのもの** ＝ 元の図の側（外積の符号）を必ず保つ
  （`reshapeDoubleBond` の `rawSide` がやっていること）

★ したがって **「主鎖が横一直線」はアルカン系の規則で、C=C を含む鎖では
「C=C を水平に」に置き換わる**。これは実測（`2-7 p4` の 1-ブテンは `CH2－CH3` が右上に斜めに出る）
と一致する。**この読み替えは設計の判断であり、スライドにその文字で書かれてはいない**（§13）。

★★ **そして、この読み替えはアプリの中でも既に済んでいる。**
`ipGeoChainPos`（`learn.js:1209`）のコメントの図が、まさにこの置き方:

```
  C1        C4-C5
   |         |
  C2 ===== C3          ← C=C は必ず水平（座標から向きを読む条件）
```

⚠ ただし前後の鎖の折れ方は **±46px の垂直（＝軸から45°の位置に札が来る）**で、
`reshapeDoubleBond` の **±120°（＝軸から60°）**とは違う。
スライド（`2-7 p4`）の斜め線は目測で 40〜45° に見えるが、**角度を測っていない**（§13 の未調査）。
★ **第1段は `reshapeDoubleBond` の ±120° に揃える**（アプリの中で既に使っている角度と同じにして、
図が2種類の顔を持たないようにする）。

### 4.4 通らなかったときどうするか

⚠ **並べ直しは失敗しうる**（環がいくつもある・橋かけ・大きな糖）。
失敗の形は2つ:

1. **結合が重原子を貫通する** → `_hasBondThroughAtom` で検出できる
2. **重原子どうしが近すぎる**（20px 未満） → 距離で検出できる

★ **どちらかが起きたら、並べ直しを丸ごと捨てて元の座標で描く。**
「半分だけ紙になった図」がいちばん読めない。
⚠ **捨てたことを画面と書き出しの両方に出す**（「元の配置で描いています」）。
黙って落ちると、図が正しいと思って参考書に貼ってしまう。

### 4.5 どのくらい効くかの見込み

§3.4 の実測から:

- ★ **既定（4.2 の②「元の座標のまま」）なら、並べ直しは1件も走らない。**
  ライブラリの図は結合の 93.4% が 42px か 40px の直交・60°刻みで、環は既に正多角形。
  **変えるのは見た目（§3.5 の C）と省略の段（D）だけ**
- `?relayout=1` を通したときに変わるのは、鎖の分子 531 件のうち
  **約 176 件（33%）**（＝主鎖が階段になっている件数）
- 環の分子 412 件は ③ が効くが、①が環を `layoutMolecule` へ落とすので
  **長方形テンプレート → 正多角形テンプレート**の差が丸ごと出る
- ★ **監査ページ（`audit.html`）に「並べ直しの前後」を全件流す検査を足す**（§8）。
  921 件は目で見て確かめられる規模ではないので、**機械が見る条件（§4.4 の2つ）で落とし、
  落ちたものだけを見る**

### 4.6 採らなかった案

| 案 | なぜ採らないか |
|---|---|
| **120° のジグザグに組み直す**（発注書の前提） | ⚠ **実測と食い違う。** ユーザーのスライドは19本すべて「主鎖は横一直線」で、ジグザグの図は1枚も無い。ジグザグは大学以降の骨格構造式の書き方 |
| **既存の `reshapeDoubleBond` をそのままキャンバスに当てて、その結果を書き出す** | キャンバスの分子を書き換える ＝ **パズルに触る**。§8 の担保が立たない。整形はシス/トランスを確定させる操作でもあり、書き出しの副作用でそれが起きてはいけない |
| **`compounds.json` の座標を紙の形に直してしまう** | ⚠ **921 件の全行が差分になる**（CLAUDE.md の重要ルール）。かつアプリ側の図の意味（直交作図＝手書き感覚のコンセプト）が変わる。**紙は表示だけの話に閉じる** |
| **外部のライブラリ（RDKit.js など）に描かせる** | ビルドツール無しの静的配信という前提を壊す。かつ**紙の書き方はユーザー固有**で、汎用ライブラリの出す図（骨格構造式）とは別物 |
| ⚠⚠ **「主鎖を横一直線に置く計算」を新しく書く** | **`ipLayoutFromChain`（`learn.js:1106`）が既にやっている**（§3.5）。⚠ **この設計書は最初それを見落として、作り直す設計を書いていた。** 2本目を作ると、番号の計算が3本目に増える —— `learn.js:1023-1028` が「v147〜v1340 はここが3つ目の番号づけ経路になっていた」と実測つきで記録している、このリポジトリで実際に起きた事故と同じ形 |
| **`RING_TEMPLATES`（`chemistry.js:1276`）を正多角形に直す** | 異性体練習・絞り込み・テスト7か所が同じテンプレートの上に乗っている。**紙のために全部の図を差し替えることになる**。紙用の別のテンプレートを持つ（§4.2 ③） |

---

## 5. 省略の段

### 5.1 段の定義（★ §2-E の実測に合わせた）

| 段 | 呼び名 | 何を書くか | 使う場面 |
|---|---|---|---|
| **L0** | 構造式（すべて書く） | C・H をすべて書き、結合を全部線で引く | **機構の説明画像**（ユーザーの言葉）。`2-7 p22` がこれ |
| **L0.5** | 価標のみ | H を書かず、**結合の手（棒）は4本すべて残す** | 異性体の書き出しの練習（`2-4 p4,p10`） |
| **L1** | 一部を省略した構造式（**既定**） | **C—H と O—H だけ**畳んで `CH3` `CH2` `OH` の札にする。C—C・C=O・C—O は線のまま | 参考書の本文。ユーザーの「通常は」がこれ |
| **L2** | 示性式 | 1行の文字列（`CH3COOH`）。線を引かない | 表の中・本文中 |

⚠⚠ **発注書の「L2 骨格構造式」は採らない。** §2-E のとおり、
スライド19本に骨格構造式は1枚も無く、高校化学では使わない。
**L2 は示性式**にする。★ **ここはユーザーの確認が要る**（§13）。

### 5.2 L1 の作り方（`condensedMode` との関係）

L1 は**2つの部品の足し算**:

```
L1 = 既存の findCondensableGroups（官能基）
   + 新しい foldHydrogens（C—H と O—H を札にする）
```

**`foldHydrogens` の規則**（新規。単純）:

1. 重原子 `A` について、`calculateHydrogens` が出した H を数える → `n`
2. `n === 0` なら札は元素記号のまま（`C`）
3. `n >= 1` なら札を `元素記号 + H + 下付き n`（`CH3` `CH2` `CH` `OH` `NH2`）にする
4. **その H への線は引かない**
5. 札の幅が変わるので、**結合の端の空け方を札の幅から計算する**（§3.1 の3つめ）

⚠ **L1 で `COOH` を1つの札にするか**は**しない**。
`3-5 p6` のカルボン酸は `C` に `O`（二重線）と `OH` を**線でつないで**描いてある。
`COOH` の1札は L2（示性式）の書き方。
★ 一方 `NO₂` `SO₃H` は `1-1 p5` で**1つの札**になっている
（「(8)(9)は官能基内の価標を省略してよい」と `1-1 p6` に明記）。
**`findCondensableGroups` のうち `NO₂` と `SO₃H` だけを L1 で使う。**

### 5.3 L2（示性式）はどこから来るか

★ **作らない。`compounds.json` の `formula` フィールドに既にある**
（`{"id":"cis-2-butene","formula":"CH3CH=CHCH3", ...}`）。
下付きの整形（`CH3` → `CH₃`）だけを掛ける。
⚠ キャンバスの作図（ライブラリに無い分子）から示性式を組み立てる機能は**無い**。
**L2 はライブラリの分子だけに出す**（第1段の割り切り）。

---

## 6. SVG / PNG の書き出し

### 6.1 要件（ユーザーの使い方から）

> **SVG が加筆用・PNG がそのまま貼る用**

★ したがって **SVG は「線と文字が別々の部品として開けること」**が要件。
Illustrator / Inkscape / PowerPoint で開いたときに、**矢印を1本足す・札を1つ動かす**ができること。

### 6.2 SVG の作り

```xml
<svg viewBox="..." width="..." height="...">
  <g id="bonds">   <line .../> <line .../> ... </g>
  <g id="labels">  <text>CH</text><text>3</text> ... </g>
  <g id="marks">   （第2段の矢印・番号・囲みはここへ）</g>
</svg>
```

- ⚠ **`<use>` も `<symbol>` も `<defs>` も使わない。**参照が入ると編集ソフトで「シンボルの実体」を
  探しに行くことになり、1本だけ動かせなくなる。**線は `<line>`、文字は `<text>` の実体で書く**
- ⚠ **フォントは埋め込まず、`font-family` を明示する**（`serif` を先頭に。紙の図は明朝・Times 系）。
  ★ **下付きは `<tspan baseline-shift>` ではなく、`<text>` を2つ置いて `font-size` を落とす** ——
  `baseline-shift` は PowerPoint の SVG 取り込みで落ちることがある（★ **未検証**。§13）
- **色はすべて属性でべた書き**（`stroke="#000"`）。CSS 変数を使わない ＝ 外に出したら CSS は付いてこない
- **`viewBox` は図の bbox ＋ 余白 12px**。`width` / `height` は px で入れて、
  貼ったときの初期サイズが決まるようにする

### 6.3 PNG の作り

- ブラウザの中で: SVG を `XMLSerializer` で文字列にし、`Image` に読ませて `<canvas>` に描き、
  `canvas.toBlob('image/png')` → `a.download`。
  ★ **`audit.js:1612` が同じ形（`a.download`）で監査 JSON を落としている**ので、経路は前例がある
- **倍率は 3x を既定**（貼って縮小する使い方なので、粗いより重い方が良い）。
  1x / 2x / 3x / 4x を選べるようにする
- **地は白で塗る**（透過にしない）。⚠ 透過 PNG を PowerPoint に貼ると、
  暗いテーマのスライドで黒い線が消える
- ⚠ **`foreignObject` を使わない**（`canvas` に描けない）。文字は `<text>` のまま

### 6.4 まとめて出す口（★ 参考書のために要る）

参考書は**ページごとに何枚も**要る。1枚ずつ手で落とすのは現実的でない。

```
node tools/gen-structures.mjs --ids=ethanol,acetic-acid --level=L1 --out=docs/fig/
```

★ **`tools/still.js` が既に Playwright を持っている**（`tools/record/node_modules/playwright`）。
同じ探し方でヘッドレスに開き、`page.evaluate` で SVG 文字列を取り出して書けば
**SVG と PNG をまとめて出せる**。⚠ **Playwright が入っていない環境では動かない**
（`still.js` は「`cd tools/record && npm install` を実行してください」と言って落ちる）。
**画面からの1枚ずつの書き出しは Playwright 無しで動く**ので、そちらを先に作る。

---

## 7. 画面のどこに置くか

### 7.1 置き方

★ **既存の「📖 お手本」モーダル（`renderMoleculeIntoSvg` を使っている分子モーダル）に、
表示の切り替えを1列足す。** 新しいモードも新しい画面も作らない。

```
[ アプリ | 紙 ]        ← 見た目
[ L0 | 価標 | L1 | 示性式 ]  ← 省略の段（「紙」のときだけ出る）
[ ⤓ SVG ] [ ⤓ PNG ]     ← 書き出し（「紙」のときだけ出る）
```

- **既定は「アプリ」**。何も押さなければ今までと1pxも変わらない
- 「紙」を押したら**その場で白地に切り替わる**（モーダルの中だけ。キャンバスは変わらない）
- 選んだ段は `localStorage` に覚える（毎回選び直させない）

### 7.2 パズルの邪魔をしないこと

⚠ **キャンバス（作図の画面）には釦を1つも足さない。**
理由: キャンバスの上の釦はパズルの操作と混ざる。`DESIGN_ribbon_consolidation.md` が
実測しているとおり、モバイル横（568×320）ではリボンに**余りが 23px しかない**。
**紙の書き出しは「見る」機能なので、見る場所（モーダル）に置く。**

### 7.3 `/tools/structure/` の薄い1枚

★ **URL を持つため**だけの1枚。

```
/tools/structure/?id=ethanol&level=L1
   → assembler を「紙・L1」で開いて、その分子だけを出す
```

- ⚠ **6本目のアプリにはしない**（`chem-app-roadmap`「アプリは5本キャップ」）。
  ハブ（ルート `index.html`）の**アプリ一覧には載せない**。
  ★ **これは「アプリ」ではなく「assembler の1つの URL」**という位置づけ
- 中身は `assembler/index.html` へ**転送するだけ**の HTML 1枚にする
  （`/assembler/?paper=1&id=…&level=L1`）。⚠ 実体を2つ持たない
- 参考書のページから図を作るときの手順が「URL を叩く → ⤓ を押す」になる

★ **代案（採らなかった）**: `/assembler/paper.html` を別ページにする。
→ **キャッシュバスターの管理が1つ増える**（`verify-release.js` 規則3〜6 の対象が増える）。
転送1枚なら `?v=` を持たないので増えない。

---

## 8. パズルに一切触らないことの担保

### 8.1 何を守るか

| 守るもの | 具体 |
|---|---|
| **価標の検査** | 作図中の原子価チェックは1行も変えない。紙の描画は `getFreeValency` を**読むだけ** |
| **`userMolecule`** | 書き出しは複製（`createTargetFromData` が作る新しい `Molecule`）にしか触らない |
| **シス/トランスの確定** | 未確定の図（2-ブテン・ブテン二酸）を確定させない。⚠ CLAUDE.md「未確定を確定させるのは整形モードのタップだけの仕事」 |
| **`compounds.json`** | 1バイトも変えない（座標を直すのは表示のときだけ） |
| **既存の描画** | `renderTargetAtom` / `renderTargetBond` / `renderMoleculeIntoSvg` を変えない |

### 8.2 どの検査が見張るか

★ **人の記憶に頼らない。機械が見る。**

| 検査 | 何を見るか | どこに置くか |
|---|---|---|
| **SR1** | 「紙」に切り替えた前後で `game.userMolecule` の**正準コードと全原子の座標**が1つも変わらない | `assembler/tests.js`（新規） |
| **SR2** | 「紙」に切り替えた前後で**立体コード**（`canonicalStereoCode`）が変わらない ＝ シス/トランスを確定させていない。⚠ **2-ブテン・マレイン酸／フマル酸を名指しで通す** | `tests.js`（新規） |
| **SR3** | `paperLayout` は**入力の `Molecule` を書き換えない**（呼ぶ前後で入力の座標が同じ） | `tests.js`（新規・純粋関数として単体で） |
| **SR4** | 並べ直したあと、**結合が重原子を貫通していない・重原子どうしが 20px 以上離れている**。⚠ 通らなかったものは §4.4 のとおり元の座標に戻り、**戻ったことが返り値に出る** | `tests.js`（新規） |
| **SR5** | ★ **否定対照** —— 「紙」の描画関数が `renderTargetAtom` / `renderTargetBond` を**呼んでいない**（暗い地の描画が混ざり込んでいない） | `tests.js`（新規） |
| **SR6** | 出した SVG に **`<use>` / `<symbol>` / CSS 変数（`var(--`）が無い**（§6.2 の「別々の部品」の担保） | `tests.js`（新規） |
| **既存の全 505+ 件** | パズル・クイズ・反応・立体の全部。**1件も落とさない**のがコミットの条件 | `assembler/test.html` |
| **監査ページ** | ライブラリ 921 件を「紙・L1」で並べ直して、SR4 の2条件を全件で見る。**落ちた分子の id を出す** | `assembler/audit.html`（既存のファズに1項目足す） |
| **`verify-release.js`** | 版番号・BOM・化け・死にリンク。⚠ **`/tools/structure/` を足すなら規則3〜6 の対象になる**ので、転送1枚に留める（§7.3） | 既存 |

### 8.3 いちばん効く担保は「複製にしか触らない」の1行

⚠ SR1〜SR6 は全部後追いの検査であって、**設計そのもので防ぐ方が強い**。

★ **`paperLayout(mol)` は `Molecule` を返す純粋関数にし、`Game` のメソッドにしない。**
`Game` のメソッドにすると `this.userMolecule` に手が届いてしまう。
届かない場所に置けば、**触りようがない**。

★ **`learn.js` 側の道具はもう純粋関数になっている** ——
`ipNumberedLayout` / `ipStraightLayout` / `ipLayoutFromChain` / `ipCloneWithPos` は
どれもモジュール直下の関数で `Game` を持たない（`learn.js:1036 / 1065 / 1106 / 1165`）。
`layoutMolecule`（`chemistry.js:1264`）も同じ。⚠ ただし `layoutMolecule` は
**引数の分子の座標を書き換える**ので、必ず複製に当てる
（`learn.js:66` の「原本をそのまま渡してはいけない」がその実測メモ）。

⚠ **`Game` のメソッドとして残っているのは `reshapeDoubleBond` 系だけ**
（`game.js:5092 / 5306 / 5323 / 5336 / 5053`）。ここだけ切り出しが要る。
**切り出しが第1段の作業のうちでいちばん壊しやすいところ**なので、
**切り出しだけを1コミットにして、既存の全テストが通ることを確かめてから次へ進む**。

---

## 9. 第2段以降（★ 範囲だけ。作らない）

### 9.1 加筆（矢印・ラベル・×印・囲み）

`2-7 p22` と `3-1 p19` が実物。必要なもの:

| 部品 | 紙での形（根拠） |
|---|---|
| 巻矢印 | 橙の2次ベジェ。羽は2本（電子対）／1本（単電子）。★ `reaction.js:606 drawCurvedArrow` がもう描いている |
| 反応矢印 | 黒の直線＋三角の矢じり。長さは絵に合わせて可変（`2-9 p18` は長い・`2-2 p8` は短い） |
| ✕ 印 | 橙の小さな ✕ を**結合の中点**に重ねる（`2-7 p22`） |
| 囲み | 緑の楕円（`3-1 p19`）・橙の角丸の枠（`2-4 p4` の「主鎖を超える」） |
| 番号 | 橙の小さな数字。札の左上（`2-3 p8`）。★ **もうある** —— `drawIupacNumberingIntoSvg`（`game.js:11203` 付近）が `renderMoleculeIntoSvg` で描いた図に主鎖の帯と `C₁` を重ねる。色は `var(--neon-orange, #ffa502)` ＝ **スライドの橙とほぼ同じ**。⚠ 置き方だけ違う（アプリは炭素の丸の中の添え字・紙は札の左上の独立した数字） |
| 名前の札 | 橙の文字。図の下（`2-4 p4`） |
| 不斉炭素の `*` | 橙。C の上（`3-1 p19`） |

⚠ **条件（濃硫酸 160〜170℃）を矢印の上に書いた図は、スライド19本に無かった**（§2-G）。
**第2段でそれを作るかはユーザーに聞く**（§13）。

### 9.2 R 基・δ+/δ−・電子式

- **R 基**: 元素記号の代わりに `R` を置く（`1-1 p4`）。⚠ **原子価の検査と衝突する**
  （`R` は元素ではない）ので、**表示だけの札**として持つ
- **δ+/δ−**: `2-2 p9` / `2-7 p12` に実物がある。原子の近くに小さく置く
- **電子式**: `2-2 p9`（メタン）・`2-7 p12`（エテン）に実物。`H : C : H` の点。
  ⚠ **これは構造式とは別の図**で、共有電子対の数を出す新しい計算が要る

### 9.3 自由作図（検査を切る）

⚠⚠ **ここだけがパズルの根っこに触る。**
「原子価が合っていなくても置ける」モードは、パズルの正しさの土台を外す。
★ **第1段では絶対にやらない。** やるとしても
**別の `Molecule` の入れ物**（`FreeSheet` のような）にして、
`userMolecule` とは型から分ける。

### 9.4 反応式の組み立て（2分子＋矢印＋条件）

`2-9 p18` の形（`3 H－C≡C－H ⟶ [ベンゼン]`）。
分子を横に並べて `+` と `⟶` と係数を置くだけなので、
**第1段の書き出しが1分子ぶん動けば、並べるのは足し算**。

---

## 10. `/tools/structure/` の薄い1枚

§7.3 に書いた。要点だけ再掲:

- **assembler をその見た目で開くだけの転送1枚**。URL を持つのが目的
- ⚠ **6本目のアプリにしない**（`chem-app-roadmap` の5本キャップ）。ハブに載せない
- `?v=` を持たない ＝ `verify-release.js` の対象を増やさない

---

## 11. 反応機構ビューアの1コマを SVG で書き出せるか（★ 調べた）

### 11.1 結論: **できる。しかも第1段より簡単**

`reaction.js` を読んだ結果:

| 分かったこと | 根拠 |
|---|---|
| 機構のデータは**座標つきで自己完結**している。`states[i].atoms` に `{element, x, y, charge?, radical?}`、`states[i].bonds` に `{atom1Index, atom2Index, type}` | `reactions.json` を読んだ（`ethene_br2` の state 0 は原子8個・結合7本） |
| ★ **H が最初から全部書いてある** ＝ **もう L0**。並べ直しも H の補完も要らない | `reaction.js` の冒頭「反応モードでは自動水素補完を使わず、states に明示された原子のみを描画します」 |
| 巻矢印は `steps[i].arrows` に `{source:{type:'bond'|'atom'|'mid', ...}, target:{...}, style:'pair'|'single', curvature}` として入っている | `reactions.json` の `ethene_br2` step0 を実測 |
| 矢印を座標にする計算は `resolvePoint`（`reaction.js:594`）、描くのは `drawCurvedArrow`（`:606`）。**どちらも `state` と2点しか見ていない** | 読んだ |
| 在庫は **14反応・47状態・35ステップ** | `reactions.json` を数えた |

★ したがって書き出しに必要なのは
**`(state, arrows) → SVG 文字列`** の純粋関数1本だけ。
`renderState`（`reaction.js:541`）は `this.game.renderBond` / `renderAtom` を呼んで
**キャンバスを借りて**描いているが、**借りる必要は無い** ——
データはキャンバスと無関係に完結している。

### 11.2 出せるコマの数

- **状態だけ**（矢印なし）: 47 枚
- **状態＋その場の巻矢印**: 35 枚
- 合わせて **82 枚**が、手を動かさずに出せる

### 11.3 手書きが要らなくなるのはどこまでか

★ **正直に**:

| 要らなくなる | まだ要る |
|---|---|
| 反応物・中間体・生成物の**構造式**（L0） | **条件の文字**（`2-7 p22` の「濃硫酸」等。データに無い） |
| **巻矢印**（曲がり・羽の種類・向き） | **✕ 印**（切れる結合の印。データに無い） |
| 電荷（`charge`）・ラジカルの点（`radical`） | **生成物名の橙の札**（`name` はあるが図の中の位置は無い） |
| 原子の配置（座標がデータにある） | **14反応に入っていない反応**（スライドの練習問題はもっと多い） |

⚠ **✕ 印は足せる。** `steps[i].arrows` の `source` が `{type:'bond'}` のとき、
その結合が**次の状態で消えているか**を照合すれば「切れる結合」が機械で分かる。
**第2段に置く**（§9.1）。

### 11.4 色をどうするか

⚠ 巻矢印は `#ff2a85`（マゼンタ）でべた書き（`reaction.js:625`）。
紙は**橙**（`2-7 p22`）。**書き出しでだけ色を差し替える**
＝ `drawCurvedArrow` に色を引数で渡せるようにする（既定は今の色のまま）。

---

## 12. 見積り（★ 正直に）

⚠ 「日」はこのプロジェクトの1レーンが1日に進む量（＝コミット4〜8本ぶん）で数える。

⚠⚠ **この見積りは §3.5 の発見のあとに下方修正した。**
最初は「並べ直しを作る」前提で **8〜11日**と見ていたが、
**主鎖を横一直線に置く計算も、番号を重ねる道具も、複製を作る道具も既にある**ので、
第1段の実体は「見た目」「省略の段」「書き出し」の3つに縮む。

| 段 | 中身 | 見積り | `learn.js` に触るか |
|---|---|---|---|
| **0** | ★ **機構の1コマ書き出し**（§11）。純粋関数1本＋釦1つ＋SVG/PNG | **0.5日** | 触らない |
| 1a | 紙の描画（`renderPaperSvg`）。札の幅に合わせた線の空け方・下付き・白地・黒線（§3.1・§6.2） | **1日** | 触らない |
| 1b | 省略の段 L0 / L0.5 / L1 / L2（§5）。`foldHydrogens` は新規だが単純 | **1日** | 触らない |
| 1c | SVG / PNG の書き出しと画面（§6.1〜6.3・§7.1） | **0.5日** | 触らない |
| 1d | 環の正多角形テンプレート（§4.2 ③）と C=C の ±120°（④）。`reshapeDoubleBond` の切り出し | **1日** | 触らない（`game.js` 側） |
| 1e | ⚠ `renderStandardFigure` の3段の落ちを**書き出しからも呼べるようにする**（§4.2 ①）。`?relayout=1` の道 | **0.5日** | ★ **触る**（別レーンの着地待ち） |
| 1f | 検査 SR1〜SR6 ＋ 監査ページの全件流し（§8.2）と、落ちたものの手直し | **1日** | 触らない |
| 1g | `/tools/structure/` の転送1枚（§10）と `tools/gen-structures.mjs`（§6.4） | **0.5日** | 触らない |
| | **第1段の合計** | **★ 6日** | |

⚠ **読み切れないのは1a と1f。**
1a の「札の幅に合わせて線を空ける」は、**文字幅をブラウザに測らせる**（`getBBox`）か
**表を持つ**かの判断が要り、前者は SVG を DOM に入れないと測れない
（＝ ヘッドレスの書き出し §6.4 と噛み合うか未確認）。
1f は 921 件を機械で流すので、落ちる件数次第。
★ **上振れを見て 6〜8日**。

★ **段0（機構の書き出し）だけは独立していて 0.5 日で終わる。**
ユーザーがいま手で描いているものの一部が、その日のうちに消える。
⚠ **`learn.js` を触らないので、別レーンと衝突しない。**
**ここから先に着手することを勧める。**

---

## 13. 決めていないこと（★ ユーザーに聞く）

| # | 何を決めるか | こちらの案 |
|---|---|---|
| **1** | ⚠⚠ **L2 は「骨格構造式」か「示性式」か。** 発注書は骨格構造式だが、**スライド19本に骨格構造式は1枚も無い**（§2-E） | **示性式**を推す。高校化学で使う書き方はこちら |
| **2** | ★ **L0.5（価標のみ・H を書かない）を第1段に入れるか。** スライドの書き出し回（`2-4` `2-5`）が全面的にこの書き方（§2-E） | **入れる**。実装は L0 から H の線と札を落とすだけで安い |
| **3** | ⚠ **主鎖に C=C があるとき、「主鎖横一直線」と「±120°」のどちらを優先するか**（§4.3）。実物からは「C=C を水平に」と読めるが、そう書かれた文は無い | **C=C を水平に**（`2-7 p4` の見た目に合わせる） |
| **4** | **書き出しの既定の倍率**（§6.3）。3x で良いか | **3x** |
| **5** | **矢印の上に条件（濃硫酸 160〜170℃）を書くか**（§2-G・§9.1）。スライドには無く、問題文の日本語で言っている | 第2段で**書けるようにする**（使うかは書く人が決める） |
| **6** | **参考書に図を差し込む書式**（`:::figure` のような囲み）を `reference-src` に足すか。★ **足すなら `DESIGN_reference_book.md` §16 と `gen-reference.mjs` の仕事**で、この設計書の外 | 別レーンに分ける |
| **7** | **番号（1,2,3…）を第1段に入れるか。** `2-3 p8` `2-4 p10` では番号が図の一部になっている。★ **道具はもうある**（`drawIupacNumberingIntoSvg`・§9.1） | ★ **入れてよい**（安い）。ただし置き方が紙と違う（丸の中の添え字 vs 札の左上）ので、**どちらに寄せるか**を聞く |
| **8** | ⚠⚠ **ライブラリの図を並べ直すか、置いてある座標のまま出すか**（§4.2 ②）。並べ直すと 32% が良くなるが、いま良い 68% も別の図になる ＝ **同じ分子がアプリと参考書で違う顔になる** | **並べ直さない**を既定に推す。`?relayout=1` で選べるようにする |

### 未調査（★ 推測を断定にしないための記録）

- **`<text>` を2つ置く下付きが PowerPoint の SVG 取り込みで生き残るか**（§6.2）。
  ⚠ **試していない。** 実装のいちばん最初に確かめる
- **Canva が SVG の `<line>` と `<text>` を別々の部品として開くか**。
  ⚠ **試していない。** ユーザーは Canva も使っている（§1.1）ので、
  **Illustrator ではなく Canva で開けるか**が本当の要件かもしれない
- **有機19本以外のスライド**（無機・電池・電気分解・速度平衡など27本）は読んでいない。
  有機の構造式の書き方を決めるのに要らないと判断したが、**読んでいないのは事実**
- ⚠ **C=C まわりの斜めの角度を測っていない**（§4.3）。スライドの見た目は 40〜45° に見えるが、
  アプリは ±120°（軸から60°）。**PDF の座標から測れば確定する**が、していない
- ⚠ **`getBBox` で札の幅を測る道が、ヘッドレスの一括書き出し（§6.4）で通るか未確認**（§12 の1a）
- ⚠ **`reference-src` に図を差し込む書式（§13 の6）と `gen-reference.mjs` の中身は読んでいない。**
  「囲みが4種類しか無い」ことだけを `reference-src/*.md` の全文から確かめた
